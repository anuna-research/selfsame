# Wallet manual API handoff

Implementation complete on `wallet-manual` in `/Volumes/anuna-03/codex-wallet-manual-closure/selfsame`, base `d84c984efc318b966840a449ec21e030a1895ae0`. Root owns independent review, exact native/served integration and serial merge.

The new Tauri command is `cbcl_v2_begin_manual` with `{request:{bootstrap:string,words:string}}`. It uses a closed camelCase/deny_unknown_fields request and returns the existing `{attemptTag,phase:"reserved",applicationId,relayOrigin}`. Both raw secret inputs are zeroized in native scope; the accepted shared `CredentialV2ManualBootstrap::recognise_pair` checks the complete pair before `RecognisedCredentialV2Entry::from_parts` and `reserve_entry`. All manual grammar/checksum/version failures map to redacted `RecognitionFailed`. No input selects another mode.

Every subsequent command is the existing tagged SingleLink command: `contact`, `unlock_preview`, `preview_rendered`, `link`, `continue_link`, `finish_link`, `cancel_link` with existing `cbcl_v2_` prefixes and nested request grammar. No extra approval, passcode, policy state or manual flag enters these commands. The native test JSONL host adds `begin-manual` with `args:{bootstrap,words}`, then uses the existing operations unchanged.

The phone selects manual entry through **Enter three words**. Its manual invitation scan/paste and separate phrase field remain local until explicit Start completes shared recognition. Manual scan captures bootstrap only; it never contacts while words are absent. Complete QR scan remains the default zero-code route and never falls back. The UI clears secret inputs on successful reservation and disposal; a local recognition refusal retains editable fields with a single generic error. Cancel/navigation fence pending scans and returned reservations by epoch/tag.

Trace: [[SPEC-078-selfsame-manual-pairing#CON-001]], [[SPEC-078-selfsame-manual-pairing#CON-002]], [[SPEC-078-selfsame-manual-pairing#CON-005]], [[SPEC-079-selfsame-single-link-consent#CON-001]], [[SCREEN-003-wallet-pairing#REQ-954]]. Validation: 84 native unit tests, 10 isolated native regressions, 30 phone UI tests, production native library Clippy, formatting/syntax checks, and eight restored behavioral guard mutations passed. Exact commands, source hashes, logs, and the contract-to-evidence limitations are committed under `evidence/spec078-wallet-manual/`. Full/manual protocol fixtures consume the actual recognized entry but inject an authenticated profile/local transport and stop before issuer construction; separate shared transaction/recovery fixtures do not establish an end-to-end Manual installation. Existing binding-substitution, every-executor-boundary, legacy pending-fixture, and platform limits remain. SPEC078 TEST008 / SPEC079 TEST011 and production approval remain root-owned.

The phone-facing labels are **Manual invitation** and **Scan the manual QR code**. API names retain `bootstrap`. The separate native-host-jobs helper is not included in this component; root will merge its `start-continue-link` / `poll-continue-link` and recovery operations after independent review. This component owns only `begin-manual` and its actual command/contact-failure tests in that host.
