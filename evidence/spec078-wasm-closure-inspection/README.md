# SPEC-078 WASM closure inspection implementation evidence

This adapter authenticates an expired allocator checkpoint solely to check exact hub closure. It returns a separate opaque inspection type and leaves ordinary live restore and offer expiry enforcement intact. Root owns independent review, the generated package, real Chrome execution, and browser ownership integration.

Trace: [[SPEC-078-selfsame-manual-pairing#CON-004]], [[SPEC-078-selfsame-manual-pairing#CON-005]], [[SPEC-078-selfsame-manual-pairing#TEST-004]], [[SPEC-078-selfsame-manual-pairing#TEST-005]], [[SPEC-078-selfsame-manual-pairing#TEST-006]]. The governing specification is the accepted bus companion; these are partial adapter receipts for its integration gates.

`verification.json` records the base, actual clean dependency heads, final source hashes, exact commands, exit codes, and parsed test summaries. The public API and disposable browser fixture schema are in `api-handoff.md`. No dependency pin, manifest, generated bundle, or checkpoint schema changed.

The inspector uses the accepted shared checkpoint decoder, the existing zeroizing installation-seed/HKDF wrapping path, and the actual Selfsame body verifier. It stores the exact decoded carrier. Signed offer context must match profile, carrier, allocator, request/intent, transcript, deadline, authority signature, and every retained body binding. Bootstrap and Begin cannot gain finality from public metadata. Established mode remains unauthenticated metadata and is not exposed as a restored mode.

PayloadSent final status uses the actual retained peer Payload and restored body facts. Terminal Receipt recovery reconstructs the exact canonical candidate receipt and checks its sealed intent/content hashes, then runs the existing closed-core and actual Ed25519 final-status recognizer. The public helper independently recognizes the signed offer and returns no protocol capability. A matching sealed Receipt containing a bad signature is refused. A matching sealed Receipt with a correctly signed extra/noncanonical core is also refused.

`mutations.json` and its named logs record deliberate behavioral failures for closure expiry, exact whole-carrier binding, retained-offer binding, terminal receipt hashes, terminal signature, and ordinary live expiry. The runner requires the exact test to panic and report FAILED after compilation; a build failure is not evidence. All source bytes are restored in `finally`, with matching hashes. Mutation capture precedes the additive canonical-core test, while every mutated production hash matches the final source. Fixture setup corrections made before green are not claimed as behavioral red evidence.

The fixture exporter is an optional environment hook in the real signed test builder. `/tmp/spec078-closure-wasm-fixtures.json` contains PayloadSent, terminal Receipt, and exact sealed invalid-signature Receipt cases with disposable public keys and 2025 deadlines. All three cases are checked by the actual native inspector before export. Its hash and regeneration command are recorded. The Payload grant is only syntactically valid test content; application credential acceptance is outside this adapter evidence.

Strict production Clippy passes. Strict all-tests Clippy reports the unchanged base `manual_range_contains` assertion in `src/lib.rs` and `useless_vec` in `credential_v2_manual_tests.rs`. The exact unrestricted failure is retained; the same test check with only those two categories allowed passes. The final web-device lib and both allocator integration suites pass, as do the relevant shared pairing suites and compile-fail capability restrictions.

This evidence does not prove browser compare-and-delete, relay delivery, server closure, live session resurrection, or production readiness. Local elapsed expiry remains insufficient to delete recovery. Root must verify exact authenticated hub closure and the ownership CAS through the actual generated WASM/browser integration before accepting the containing task.
