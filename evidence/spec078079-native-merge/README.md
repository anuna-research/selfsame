The accepted wallet Manual adapter and test-only continuation/recovery host are combined here. Root supplied one integration arm for closed BeginManual arguments in the occupied-job recognizer, so Manual cannot bypass its mutation guard. The existing Full/Manual host fixture checks valid Manual refusal while occupied and malformed Manual refusal before dispatch.

The test-only `metrics {}` operation reads three existing cumulative counters: entry to issuer-artifact construction, memory-custody writes, and legacy policy operations. It introduces no instrumentation or authority and returns no input, secret, key, checkpoint or transfer. Each field is a separate counter read; this is not an atomic all-effects snapshot, and identityEffects does not count every cryptographic primitive. Both modes verify the closed read-only projection while a job is retained. Root's final integration can compare these counters at controlled phase boundaries alongside signed authority/installed evidence.

Before this final metrics projection, the combined native library passed 89 tests with 11 intentional ignores. After the projection, six focused host tests passed with four intentional ignores, and isolated actual Full/Manual host cases passed once each. Scoped rustfmt and diff-check passed; production code outside the accepted parents did not change. The logs are committed with trailing whitespace normalized and both original/stored hashes recorded. The ordinary library suite was not redundantly repeated after the narrow host-only projection.

Independent review checks the root-owned adapter delta and preserved parent bytes. Full local native/served-WASM ceremonies, live pending-comparison cancellation and actual-expiry recovery remain separate successor-integration gates. This component evidence does not claim those depth tests or production execution.
