{
  "name": "selfsame",
  "private": true,
  "description": "Screen-render checks for the Selfsame frontend",
  "scripts": {
    "screens": "node tests/screens.mjs screens-out",
    "e2e:wasm": "wasm-pack build crates/selfsame-web-device --target web --out-dir ../../tests/e2e/device/pkg --dev -- --locked",
    "e2e:preflight": "node tests/e2e/preflight.mjs",
    "e2e": "node tests/e2e/driver.mjs"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0"
  }
}
