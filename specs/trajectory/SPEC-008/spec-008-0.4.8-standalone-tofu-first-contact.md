---
id: SPEC-008-amendment-0.4.8
title: SPEC-008 0.4.8-draft — Reachable confirmed migration and dormant hub authority
status: draft
version: 0.4.8-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-804]]; [[SPEC-007-cbcl-pairing-cutover#REQ-805]]; [[SPEC-007-cbcl-pairing-cutover#REQ-807]]; [[SPEC-007-cbcl-pairing-cutover#REQ-810]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-007-cbcl-pairing-cutover#REQ-813]]; [[SPEC-007-cbcl-pairing-cutover#NFR-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; [[SPEC-007-cbcl-pairing-cutover#CON-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-806]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-204]]; [[SPEC-004-application-scoped-identity#CON-206]]; [[SPEC-004-application-scoped-identity#CON-209]]; [[SPEC-004-application-scoped-identity#CON-211]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-221]]; [[SPEC-004-application-scoped-identity#CON-226]]; did-crdt HTTP and sync service contracts; cbcl-pairing SPEC-001 through coordinated 0.3.0, 0.4.0, 0.4.1, 0.4.2, and 0.4.3 drafts; cbcl-bus SPEC-053 0.16.1-draft"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007; cbcl-bus SPEC-053 REQ-033, GATE-00, GATE-01, GATE-03, and GATE-04"
supersedes: "[[spec-008-0.4.7-standalone-tofu-first-contact]]"
adopts: "All 0.4.4, 0.4.5, 0.4.6, and 0.4.7 clauses except the exact replacements and corrections named by this amendment"
review-inputs: "[[spec-008-0.4.7-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.3.0 through 0.4.3; cbcl-bus SPEC-053 0.16.1-draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
owner-direction: "2026-08-23 — standalone first-contact option 1 is the target architecture; implementation remains subordinate to fresh review PASS"
last-updated: 2026-08-23
---

# SPEC-008 0.4.8-draft — Reachable confirmed migration and dormant hub authority

> **Design target, not implementation authority.** The fresh Claude Opus/max
> Circus review rejected 0.4.7 with zero critical, three high, two medium, and
> six low findings. It confirmed every 0.4.6 finding closed and passed the
> cbcl-pairing 0.4.2 grammar correction. This amendment closes the new findings
> through coordinated Selfsame, cbcl-pairing, and cbcl-bus authority. No source,
> schema, fixture, vector, allocation, release, or deployment change is
> authorized until another fresh-context Claude-driver Circus review returns
> PASS over the complete effective set.

## Effective candidate

The candidate is 0.4.4 through 0.4.7 except where this document replaces text,
plus cbcl-pairing SPEC-001 0.3.0 through 0.4.3 and cbcl-bus SPEC-053
0.16.1-draft. Standalone first contact,
live application authentication, exact `(applicationId, relayOrigin)` TOFU,
two-stage consent, post-consent wallet effects, and the distinct credential/v2
wire remain unchanged.

## Disposition of the 0.4.7 review

| Finding | 0.4.8 disposition |
|---|---|
| **H-1 — no reachable claimant** | [[spec-008-0.4.8-standalone-tofu-first-contact#REQ-970]] makes the deployed Path-A member the normal positive branch, allocates only pending account/scope state, and completes the existing confirmed migration after exact final consent. It does not assert an empty “eligible no-legacy-member” class. |
| **H-2 — console migration is destructive and circular** | REQ-970 deletes the console alternative. The hub mints the scope first; preliminary consent derives the real DID; the final wallet surface names DID/fingerprint and every room; signed reverse evidence authorizes one atomic migration. |
| **H-3 — hub surface breaches GATE-00** | [[spec-008-0.4.8-standalone-tofu-first-contact#REQ-971]] and cbcl-bus GATE-04 require a constant-in-source closed gate, physical production-route/dialect absence, opaque prohibited-action tests, and a non-production-only compiled-open test build. The runtime allocator flag is only an additional post-gate throttle. |
| **M-1 — legacy/room digests undefined** | [[spec-008-0.4.8-standalone-tofu-first-contact#REQ-972]] adopts cbcl-bus CON-012's exact domain strings, deterministic-CBOR field tuples, sort order, null/tag rules, confirmation digest, and independent vectors. |
| **M-2 — durability list omits fail-open ledgers** | [[spec-008-0.4.8-standalone-tofu-first-contact#NFR-910]] adds `cbcl-successor-pointer`, records its un-spent-key failure, records `cbcl-pairaccount-grant`'s unratified-device failure, and explicitly scopes the gate rather than claiming complete hub disaster recovery. |
| **L-1 — `Bound(accountDid)` store ambiguous** | REQ-970 separates membership classification from reciprocal alias authority and names `cbcl-webfinger-account`, keyed by stable application account, as the sole source of the latter. |
| **L-2 — root correction not ledgered** | [[spec-008-0.4.8-standalone-tofu-first-contact#CON-961]] explicitly replaces 0.4.6 CON-952's later-operation sentence. |
| **L-3 — cbcl-pairing TEST-050 still names 4,096** | cbcl-pairing 0.4.3 CON-020 replaces that positive witness through the owning amendment channel. |
| **L-4 — inert rendezvous recognition narrows REQ-807 unnamed** | REQ-807 is now in `amends:` and CON-961 records the narrow pure-recognition/no-selection reading and its implementation cost. |
| **L-5 — browser cannot enforce wallet-side ordering** | REQ-970 says the allocator sends no `offer` until the hub authority result is non-terminal; terminal results close without a relay offer. |
| **L-6 — membership primary keys misstated** | [[spec-008-0.4.8-standalone-tofu-first-contact#CON-959]] pins both raw length-prefixed binary keys and lists `bind-account/2` as non-sanctioned for credential/v2. |

## Requirements

### REQ-970 — Existing Path-A membership is the standalone first-link branch

This requirement replaces 0.4.7 REQ-967, the legacy portions of 0.4.6 REQ-965,
and the legacy migration/eligibility paragraphs of 0.4.4 REQ-947 and CON-941.
There is no reachable authenticated “eligible no-legacy-member account” at the
reviewed cbcl-bus baseline: every ordinary authenticated handle has a
`cbcl-member` row, and an account-bound handle cannot authenticate while the
Path-B gate is closed. The protocol treats that fact as an explicit dependency,
not a fixture problem.

Credential/v2 uses two independent authority axes:

```text
SessionMembership = Unknown
                  | PathA(MigrationSnapshot)
                  | PathB(accountDid, applicationAccountId)

ReciprocalAlias = Unknown
                | NoReciprocalAlias
                | ReciprocalAlias(homeDid)
```

`SessionMembership` comes only from the live signed session plus
`cbcl-member`/`cbcl-account-binding` and membership rows. `ReciprocalAlias`
comes only from the account-keyed `cbcl-webfinger-account` authority store using
the stable application account ID; it is not the DID value in the
handle-keyed `cbcl-account-binding` row. A caller, peer, claimant, handle string,
or prepared DID cannot supply either axis.

For `PathA`, before displaying a public carrier the gated hub allocation command
locks and snapshots the enrolled key plus every legacy room row, allocates one
pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId`, and stores
one bounded pending ceremony. Those values become durable account identity only
at the final authority transaction. Decline, expiry, or pre-commit failure
deletes them; a retry may derive a different preview DID and must display it
again. Concurrent first-link allocation for the same `(applicationId, handle,
enrolledKey)` refuses or returns the byte-identical active ceremony; it never
creates two candidate accounts.

After both Finished values, the hub offer contains cbcl-bus CON-012's complete
authenticated `PathAToB` transition. The browser completes the correlated
`selfsamepairauthority` read before it releases that offer. `Unknown`, a
different reciprocal DID, changed pending state, or a gate refusal closes the
ceremony with no `offer` relay object. The allocator SHALL NOT send `offer`
until the result is non-terminal. For a first link, `NoReciprocalAlias` is held
through preview and maps to CON-221 `NoBinding`; a byte-identical reciprocal DID
maps to `BoundSameDid`; any other DID maps to `BoundDifferentDid` and refuses.

The claimant authenticates the offer and displays the transition before the
preliminary decision. Preliminary approval authorizes only passcode/presence,
one zeroizing derivation, and disclosure of the preview DID/fingerprint. The
final surface then names that locally derived DID, displays its fingerprint,
names the legacy handle/key, enumerates every authenticated affected room, and
states that approval will move those memberships to the account and switch the
browser to its distinct granted device key. Final approval authorizes the
existing wallet issuance effects and the exact migration; the signed reverse
evidence carries CON-012's `migrationConfirmationDigest`.

Only after resolver verification, grant verification, and browser local strict-
durability installation may the hub perform CON-012's one transaction. That
transaction verifies the unchanged snapshot, writes the account/device/
pairaccount/alias authority, migrates every room, and commits the binding and
immutable status together. The browser receives or later recovers the exact
acknowledgement under the new device key, closes the old-key socket, reconnects
through Path B, and only then can the wallet accept a receipt and report linked.

`PathB` relink is permitted only in a build where GATE-01 is actually closed and
the session already passed Path-B standing. A session with neither valid Path-A
nor valid Path-B authority is `Unknown`; no fresh unauthenticated-handle branch
is invented. `confirmed-migrate-path-a-handle/3` and remote-console invocation
are not ceremony paths and SHALL NOT be used while Path B is closed.

### REQ-971 — Credential/v2 hub authority is physically dormant until all gates close

The hub half conforms to cbcl-bus `SPEC-053-key-root-identity#GATE-00` and
`spec-053-0.16.1-credential-v2-confirmed-migration#GATE-04`. At the reviewed
baseline `cbcl-chat-credential-v2-gate:profile/0` is the sole enablement point,
is constant in source, returns `#(error tier-1-unapproved)` unconditionally,
and reads no environment, database, request, header, build flag, or deployment
value. Every allocation, prepare, finalize, authority, accept, status, and
migration handler consults it first, before parsing application bodies beyond
route recognition, allocating nonce/scope/relay state, writing, resolving, or
TOFU-enrolling.

Production `cbcl-chat-app:dispatch/0` has no credential/v2 authority route and
the production CBCL grammar/WS dispatcher recognizes no credential/v2 authority
performative. The production browser neither advertises nor instantiates the v2
allocator without a compiled-open capability. A gate that merely defaults off
in configuration is non-conforming; the allocation throttle in 0.4.5 CON-947 is
an additional control only after GATE-00/GATE-04 close.

Test source may build an explicitly labelled non-production route/profile and
use the real local browser, relay, and wallet. That build is never a release
candidate: relx manifests, release build scripts, binary scans, and the closed-
gate suite prove that the fixture, open constant, authority routes, selectors,
and conformance-allocation capability are absent from the production artefact.

Closing GATE-04 requires GATE-01 and GATE-03 closed, the coordinated protocol
review PASS, migration mutation/crash/race evidence, human review of the
compromised-Path-A-key takeover trace and consent copy, both durability gates,
and separate owner deployment confirmation. This amendment closes none of
those boxes.

### REQ-972 — Migration authentication has one canonical digest graph

The Selfsame consumer adopts cbcl-bus CON-012 byte-for-byte:

- `legacyKeyDigest` hashes its exact domain plus the raw 32-octet enrolled key;
- `roomSetDigest` hashes its exact domain plus deterministic CBOR of the complete
  unique raw room-byte array sorted by unsigned-octet order;
- `migrationSnapshotDigest` hashes its exact domain plus deterministic CBOR of
  the exact `cbcl-member` and complete sorted `cbcl-roommember` field vectors,
  including primary key, `granted`, `via`, and nullable `since`;
- `migrationConfirmationDigest` hashes its exact domain plus raw
  `offerCoreDigest`, UTF-8 account DID, canonical fingerprint bytes, the three
  snapshot/key/set digests, snapshot nonce, and device-key digest.

All JSON values use base64url-no-pad; concatenations use raw decoded octets.
Both browser and wallet recompute offer/descriptor/intent inputs independently.
The wallet recomputes the confirmation from its local preview and authenticated
offer; the hub recomputes it from held state and its own fingerprint renderer.
Copying a peer/caller digest or accepting a browser boolean is non-conforming.

The offer-core cap is 60,000 RFC-8785 octets, `migrationRooms` is complete at no
more than 256 entries and 48,000 encoded UTF-8 octets, and the enclosing
credential/v2 large object remains at `logicalLength <= 62,000`. Oversize is
`MigrationPlanTooLarge` before carrier allocation; there is no truncation,
paging, hidden remainder, or partial migration in this version.

The CON-226 `credential_v2` corpus contains every digest domain/preimage,
canonical bytes, positive output, null/atom mapping, unsigned sort edge, empty/
one/maximum room sets, every field mutation, and independently reproduced
vectors. The migration confirmation is a signed consumer claim inside the
reverse bundle and does not add a shared cbcl-pairing performative or field.

### NFR-910 — Hub off-machine readiness has an explicit identity-authority scope

This requirement replaces 0.4.7 NFR-909's table and loss-consequence lists. It
does not claim complete cbcl-bus disaster recovery. The
`hub-identity-authority-off-machine-ready` gate covers exactly all current
identity/standing state used by credential/v2 and per-frame identity:

```text
cbcl-webfinger-alias
cbcl-webfinger-account
cbcl-webfinger-withdrawn
cbcl-selfsame-* (every stable-account, scope, ceremony, device, pending,
                 finalization, status, index, and compensation table)
cbcl-account-binding
cbcl-device-grant
cbcl-member
cbcl-roommember
cbcl-account-roommember
cbcl-pairaccount-grant
cbcl-successor-pointer
```

The authenticated, encrypted, versioned off-machine snapshot and restore drill
must reproduce exact row bytes, indexes, schema version, tombstones, pending and
finalized ceremony/status records, and authority queries. Adding a table read by
credential/v2 or per-frame identity without adding it to this manifest makes the
gate false.

Loss consequences are explicit:

- withdrawal-tombstone loss is fail-open for withdrawn alias generations;
- successor-pointer loss is fail-open because an empty table makes every
  departed key appear unspent;
- pairaccount-grant loss is fail-open because an empty table makes every
  paired-but-unratified device appear eligible for enrolment;
- pending-compensation/status loss makes accepted outcomes unrecoverable; and
- binding/device/membership/alias loss is otherwise fail-closed for existing
  accounts and grants.

The gate does not promise recovery of chat content, MLS state, invitations, or
unrelated Mnesia tables; their owners require separate continuity manifests.
Production allocation requires this gate and did-crdt `durable-ready`
continuously true. Local/test builds remain explicitly non-production.

## Contracts

### CON-959 — Snapshot and finalization use actual stores, keys, and one writer

The Selfsame hub adapter implements cbcl-bus CON-012 rather than reconstructing
membership semantics. Actual relevant legacy keys are:

```text
cbcl-roommember:
  u32be(len(room)) || room || handle

cbcl-account-roommember:
  u32be(len(room)) || room || u32be(len(accountDid)) || accountDid || handle
```

They are raw binaries, not tuples or delimiter-joined strings. Snapshot reads
take binding/member exact-key write locks plus whole-table read locks over both
membership tables; final migration takes the stable-account/pending lock,
binding/member locks, then whole-table write locks, then the device/pairaccount
count/index and exact-key locks, alias/account/tombstone keys, and finalization/
compensation locks in the one documented order. Mnesia abort/deadlock is a
closed refusal or idempotent retry before commit, never permission to skip a
check.

`finalize-credential-v2-migration/1` is the sole credential/v2 membership/
binding writer. `bind-account/2`, `bind-and-store-verified-device-grant/8`, and
`confirmed-migrate-path-a-handle/3` are explicitly non-sanctioned and have no
credential/v2 production call edge. The new writer stages all target room rows
preserving `granted`/`via`/`since`, grant/pairaccount/alias/account authority,
binding, stable indexes, immutable acknowledgement/status, and finalization;
then deletes the exact legacy room rows; then commits all or none. It retains
`cbcl-member` only as non-authoritative history.

Before that commit, compensation removes only ceremony-owned pending rows or
restores exact captured rows under revision/exact-byte conflict checks. The
commit is irreversible protocol acceptance: after it, timeout or lost
acknowledgement cannot recreate legacy rows, delete the binding, or revoke the
accepted grant. Browser and wallet retain `outcome-unknown`; the new device key
recovers the immutable status and exact acknowledgement after reconnect. A
later unlink is a new confirmed protocol.

### CON-960 — Authenticated transition display and effect order are closed

`OfferCoreV2` gains one required `accountTransition` object:

```text
NoTransition
PathAToB {
  legacyHandle,
  legacyKeyDigest,
  migrationRooms,
  roomSetDigest,
  migrationSnapshotDigest,
  snapshotNonce
}
```

`NoTransition` is valid only for a session already authenticated through open
Path-B standing. `PathAToB` is valid only for the exact signed legacy session
and snapshot. Every member is part of RFC-8785 `offerCoreDigest`, cbcl-pairing's
domain-separated `intentDigest`, CON-214 evidence, provider hint, display
object, final confirmation, reverse payload, hub acceptance, acknowledgement,
and receipt correlation as applicable.

The credential/v2 `IntentVerifier` returns a closed typed `AccountTransition`
only after the live profile, carrier, descriptor, transcript, offer, hub
evidence, session account, and every transition field agree. Generic
`DisplayField`, peer-claimed values, caller booleans, and post-verification
mutation cannot represent it. cbcl-pairing 0.4.3 CON-020 is the only shared
display-layer amendment.

The exact order is:

```text
closed source gate -> live signed session -> stable snapshot/pending scope
-> public carrier -> CPace + both Finished -> signed offer finalized
-> reciprocal authority result -> allocator releases offer
-> claimant authenticates offer/transition -> preliminary display/decision
-> zeroizing DID preview -> browser comparison/binding result
-> final DID/fingerprint/complete-room display and decision
-> re-derive/sign/publish/grant -> browser verifies and installs locally
-> one hub migration/finalization transaction -> new-key status/ack recovery
-> receipt -> wallet linked record
```

No wallet custody access, derivation, signing, publication, policy write, grant
write, or durable identity state precedes its existing consent edge. No hub
binding, grant, alias, membership, or successor effect precedes final signed
evidence and local installation. The request tombstone plus separately accepted
TOFU/policy/rate state remain the only permitted pre-final wallet mutations.

### CON-961 — Effective-text and authority correction ledger

The effective set applies these replacements explicitly:

1. 0.4.7 REQ-967/ADR-939's terminal legacy refusal and console alternative are
   replaced by REQ-970 and cbcl-bus REQ-040/ADR-017/DEC-015.
2. 0.4.4 REQ-947's “existing accounts are migrated before either v2 command”
   is replaced by pending allocation plus post-final CON-012 migration.
3. 0.4.4 CON-941's overloaded `Unknown/NoBinding/Bound(did)` is replaced by
   REQ-970's independent session-membership and reciprocal-alias axes; the
   reciprocal DID comes from `cbcl-webfinger-account`, not account binding.
4. 0.4.4/0.4.6 post-final automatic rollback is replaced for the migration
   branch by CON-959's irreversible commit plus durable status recovery.
5. 0.4.6 CON-952's “new root only in a later operation” sentence is replaced by
   0.4.7 CON-958's same-invocation non-crash completion rule.
6. cbcl-pairing 0.4.3 CON-020 replaces TEST-050's 4,096 positive control
   witness; Selfsame does not claim authority to replace it.
7. The pure profile recognizer may completely recognize rendezvous-v1 solely
   for frozen profile compatibility, but SPEC-007 REQ-807 rejects the resulting
   legacy descriptor before network, derivation, selection, advertisement, or
   identity action. Implementing that inert branch still requires the complete
   PROTO-002 CON-301 and PROTO-003 CON-401 grammar and corpus; partial parsing is
   not permitted.
8. The allocator withholds `offer` until reciprocal authority is non-terminal;
   a terminal result closes or expires the mailbox without an offer object.
9. CON-959's actual membership keys replace 0.4.7 CON-956's tuple/unnamed-key
   cells, and its writer list includes the otherwise-exported `bind-account/2`.
10. NFR-910 replaces NFR-909's scope and consequences; 0.4.7 REQ-968/REQ-969,
    CON-957's deterministic v1 dispatch, and CON-958's published-profile record
    remain unchanged.

## Decisions

### ADR-941 — Exact final consent joins pairing and migration without enrolment

The account scope must exist before a home DID can be derived, and the hard stop
forbids that derivation before preliminary consent. Therefore a separate
pre-pairing migration cannot name the DID REQ-033 demands. The credential/v2
ceremony already provides the authenticated application, signed scope/room
snapshot, preview-only decision, locally derived DID/fingerprint, exact final
decision, and signed reverse evidence. Using its final screen as the migration
consent is the smallest sound standalone protocol and keeps enrolment optional.

### ADR-942 — Complete dormant code is preferable to a configurable live gate

GATE-00 permits reviewed implementation to land while authority stays physically
unreachable. A source constant, absent production routes/dialect selectors, and
release-isolation tests make that property reviewable. A runtime flag that
defaults off is operationally convenient but is not a Tier-1 gate and cannot
substitute for the compiled boundary.

## Test specification

All effective TEST-1011 through TEST-1082 remain release-blocking except where
the rows below replace an expectation. Companion TEST-030 through TEST-052 and
cbcl-bus TEST-043 through TEST-047 are mandatory.

### TEST-1083 — A real legacy member reaches final linked state in the gated-open fixture

Use the non-production compiled-open cbcl-bus profile, a real signed Path-A chat
session, the real local blind relay, the browser allocator, and the Selfsame
wallet claimant. Require the complete CON-960 order, one migrated account, a
new-key Path-B reconnect, exact acknowledgement/receipt, and one linked wallet
record. No enrolment ceremony or held pairing-trust record participates.

### TEST-1084 — Decline and every pre-final failure leave legacy authority exact

Snapshot all wallet/resolver/browser/hub rows. Exercise origin rejection,
wrong PAIR1, Finished mismatch, offer/authority mismatch, preliminary decline,
final decline, timeout, resolver failure, grant refusal, and browser-install
failure. Aside from request tombstone and separately accepted TOFU/policy/rate
state, wallet identity stays unchanged; every legacy member/room row remains
byte-identical and no binding/device/pairaccount/alias target exists.

### TEST-1085 — Migration digests are independently reproducible

Two independent stacks reproduce empty, one-row, maximum-bound, `since = null`,
and both `via` vectors for all four REQ-972 digests. Exercise unsigned-octet
ordering and exact primary keys. Mutate each field/domain/encoding/sort/null tag,
copy a hub value without local recomputation, or change the rendered fingerprint
and require refusal before the corresponding display or final transaction.

### TEST-1086 — Snapshot races and transaction crashes never widen consent

Add, delete, re-key, and metadata-mutate legacy rooms after display. Require
`MigrationSnapshotChanged`, exact legacy preservation, and zero target writes.
Inject Mnesia abort/crash at every CON-959 step; recovered state is wholly legacy
or wholly account-bound, never mixed. Race eviction, room join, withdrawal,
device-cap changes, and a second ceremony under the declared lock order.

### TEST-1087 — Lost acknowledgement converges under the new key

Commit the migration, drop acknowledgement, kill browser/hub, and restart. The
old key cannot send an ordinary frame. The new key retrieves the immutable
status, obtains the byte-identical acknowledgement, produces one receipt, and
does not issue a second grant or migrate twice. No timeout or compensation
restores legacy authority after commit.

### TEST-1088 — GATE-00 and GATE-04 remain physical in release artefacts

Set hostile environment/configuration values and require the source gate remain
closed. Production route table, parser/dialect, WS dispatch, static UI, relx
manifest, and binary scan contain no credential/v2 authority selector or open
test fixture; every request is opaque and produces zero writes/allocation. The
separate non-production build is visibly labelled and is the only positive E2E
control.

### TEST-1089 — Authority and reciprocal alias axes cannot be confused

Cross-product Path-A/Path-B/unknown membership with absent/same/different/
corrupt reciprocal alias state. Require the exact REQ-970 outcome. In
particular, a `cbcl-account-binding.account` value cannot satisfy reciprocal
alias authority, and a caller/prepared DID cannot turn `Unknown` into absence.

### TEST-1090 — Off-machine readiness fails for every scoped authority class

Extend TEST-1080 with `cbcl-successor-pointer` and
`cbcl-pairaccount-grant`. Delete each from a restored snapshot and require the
gate false. With an empty successor table demonstrate a departed key would look
unspent; with empty pairaccount demonstrate an unratified paired key would look
eligible. Verify the gate makes no claim about unrelated chat/MLS continuity.

### TEST-1091 — Effective corrections have one owner and no stale contradiction

Static/spec checks require the CON-961 replacements, fully qualified REQ-807/
GATE-00/GATE-01/GATE-03/GATE-04 links, actual membership keys, explicit
`bind-account/2` disposition, allocator no-offer terminal rule, same-invocation
root rule, and companion TEST-050 replacement. Reject any effective text that
restores the console workaround, empty eligible-account premise, configurable
gate, automatic post-migration rollback, or 4,096 positive control witness.

## Implementation and gate sequencing

The coordinated repository slices remain design-only until PASS. A PASS
authorizes an Elephant SPL/test-first plan, not production enablement.

1. Run a fresh-context Claude Opus/max Circus review over this complete set,
   the cbcl-pairing 0.4.3 companion, cbcl-bus 0.16.1 candidate, the 0.4.7 review,
   every carried finding, and exact four code baselines.
2. Only after PASS, encode TEST-1011 through TEST-1091, companion TEST-030
   through TEST-052, and cbcl-bus TEST-043 through TEST-047 as executable
   Elephant gates.
3. Commit failing schemas/vectors/corpus/pure reducers first; then wallet/hub/
   resolver effect and crash tests; then dormant routing/UI wiring.
4. Validate the real browser/relay/wallet path only in the visibly
   non-production compiled-open build. Validate the production build's physical
   absence independently.
5. Keep production credential/v2 allocation disabled. GATE-01, GATE-03,
   GATE-04, human cryptography/migration review, independent CPace evidence,
   hub off-machine authority, did-crdt durability, privacy, rollback review,
   owner approval, and separate deploy confirmation all remain required.

## Changelog

- **0.4.8-draft — 2026-08-23 — review candidate, not approved.** Makes the
  existing Path-A member the reachable standalone first-link branch; binds
  exact room migration to the wallet's final signed consent after DID preview;
  adopts the owning cbcl-bus increment and physical GATE-00 landing shape;
  defines migration digests, irreversible status recovery, actual membership
  keys, complete identity-authority durability scope, and every 0.4.7 low
  correction. No implementation, production allocation, or deployment
  authority is claimed.
