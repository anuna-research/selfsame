---
id: SPEC-008-amendment-0.4.11
title: SPEC-008 0.4.11-draft — Distinct v2 trust anchor and complete authority ordering
status: draft
version: 0.4.11-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-807]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-226]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.4-draft; cbcl-bus [[SPEC-076-pairing-account-grant#REQ-002]]; cbcl-bus [[SPEC-076-pairing-account-grant#CON-001]]; cbcl-bus [[SPEC-076-pairing-account-grant#CON-002]]; cbcl-bus [[SPEC-076-pairing-account-grant#ADR-001]]"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007; cbcl-bus SPEC-053 GATE-00, GATE-01, GATE-03, and corrected GATE-04"
supersedes: "[[spec-008-0.4.10-standalone-tofu-first-contact]]"
adopts: "All 0.4.4 through 0.4.10 clauses except the exact replacements and corrections named by this amendment; cbcl-pairing SPEC-001 0.4.4 remains independently unchanged"
review-inputs: "[[spec-008-0.4.10-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.4-draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
owner-direction: "2026-08-23 — standalone first-contact option 1 remains the target; implementation awaits fresh cross-model PASS"
last-updated: 2026-08-23
---

# SPEC-008 0.4.11-draft — Distinct v2 trust anchor and complete authority ordering

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Design correction, not implementation authority.** The 0.4.10 review found
> zero critical, two high, four medium, and three low defects. It found the
> standalone architecture sound. This amendment closes every finding against
> the cited code. No product code, fixture, release, or deployment is authorized
> until the coordinated set receives a fresh-context cross-model PASS.

## Review disposition

| 0.4.10 finding | 0.4.11 disposition |
|---|---|
| **H-1 — v2 has two incompatible trust anchors** | [[spec-008-0.4.11-standalone-tofu-first-contact#REQ-981]] retains the distinct v2 ApplicationId and profile. Only the statement discriminator becomes `evidenceVersion = 2`. |
| **H-2 — cbcl-bus 0.16.3 deleted adopted authority clauses** | [[spec-008-0.4.11-standalone-tofu-first-contact#CON-970]] adopts cbcl-bus 0.16.4's explicit survivor and replacement ledger. |
| **M-1 — size arithmetic has no fixed point** | [[spec-008-0.4.11-standalone-tofu-first-contact#REQ-982]] declares every v2 variable-field maximum and derives the compact JWS bound. |
| **M-2 — member collision rule breaks handle changes** | [[spec-008-0.4.11-standalone-tofu-first-contact#REQ-983]] preserves multi-handle use of one plain enrolled key. |
| **M-3 — grant deleters escape the guard discipline** | REQ-983 covers both revocation writers and states the exact post-revocation result. |
| **M-4 — the lock order omits Mnesia table locks** | [[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] names every `?ALL` table lock and accepts measured contention as an open gate. |
| **L-1 — local possession error has no wire carrier** | [[spec-008-0.4.11-standalone-tofu-first-contact#CON-969]] makes it browser-local and requires a control-frame handler. |
| **L-2 — browser differential lane reuses Rust WASM** | REQ-982 names one source-disjoint JavaScript canonical implementation. |
| **L-3 — coordination metadata is stale** | This document, cbcl-bus 0.16.4, and cbcl-pairing 0.4.4 name this review unit. |

The original F-A through F-E disposition remains unchanged. Relay policy is
keyed by the exact application and relay pair. Authenticated application data
is checked before display. Custody remains untouched before preliminary
consent. The hub supplies the pending fresh scope. Relay operatorship remains
an accepted residual, not an inferred fact.

## Requirements

### REQ-981 — Credential/v2 has a distinct profile and a distinct evidence version

This requirement replaces 0.4.10
[[spec-008-0.4.10-standalone-tofu-first-contact#REQ-977]] in full.

The live profile at `https://chat.anuna.io/selfsame/application` remains
byte-for-byte unchanged. It remains `profileVersion = 1`. Existing CON-219
evidence remains `evidenceVersion = 1` and `profileVersion = 1`.

Credential/v2 uses the distinct canonical ApplicationId
`https://chat.anuna.io/selfsame/application/v2`. Its document uses the exact
nine-member v2 grammar in
[[spec-008-0.4.4-standalone-tofu-first-contact#CON-933]]. Its document carries
`profileVersion = 2`. Its signed statement carries `evidenceVersion = 2`,
`profileVersion = 2`, and that v2 document's exact digest.

Only statement-level version selection uses `evidenceVersion`. Profile
recognition continues to use `profileVersion`. App identity performs total
closed dispatch before member recognition. The version-2 branch does not
compare against the crate-wide version-1 constant.

The v2 profile has exactly these four allowed permissions:

```text
https://chat.anuna.io/selfsame/application/v2#channel-join
https://chat.anuna.io/selfsame/application/v2#chat-read
https://chat.anuna.io/selfsame/application/v2#chat-send
https://chat.anuna.io/selfsame/application/v2#mls-commit
```

Requested permissions are a non-empty sorted subset of that exact set. The v1
permission set and its bytes do not change.

The person-owned policy key is the exact pair:

```text
(https://chat.anuna.io/selfsame/application/v2,
 https://chat.anuna.io:9443)
```

A v1 policy entry cannot suppress this first v2 prompt. Later use of the exact
v2 pair can use its accepted entry. Both explicit migration consent screens
remain mandatory on every migration.

The generator owns two immutable deployment outputs. The v1 output and digest
remain frozen. The new v2 route, browser bytes, wallet vector, offer, and policy
pair all use one generated v2 byte string and digest. Serving the v2 profile
does not open allocation.

### REQ-982 — Credential/v2 size bounds have declared finite inputs

The following constants apply only to credential/v2. They do not narrow or
widen any v1 grammar:

```text
V2_MAX_APPLICATION_ID_OCTETS                 = 96
V2_MAX_RELAY_ORIGIN_OCTETS                  = 272
V2_MAX_REQUESTED_PERMISSIONS                = 4
V2_MAX_PERMISSION_OCTETS                    = 128
V2_MAX_RETURN_URI_OCTETS                    = 160
V2_MAX_PROVIDER_ID_OCTETS                   = 63
V2_MAX_PLATFORM_BINDING_ID_OCTETS           = 160
V2_MAX_TIMESTAMP_OCTETS                     = 32
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_OCTETS   = 256
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_JCS      = 256
V2_MAX_CREDENTIAL_KID_OCTETS                = 128
V2_MAX_DID_OCTETS                           = 512
V2_MAX_LEGACY_HANDLE_OCTETS                 = 256
V2_MAX_MIGRATION_ROOMS                      = 256
V2_MAX_MIGRATION_ROOMS_JCS_OCTETS           = 48,000
V2_MAX_OFFER_CORE_NON_ROOM_OCTETS           = 4,067
V2_MAX_OFFER_CORE_OCTETS                    = 56,000
V2_MAX_PAYLOAD_OCTETS                       = 56,000
V2_MAX_CREDENTIAL_MEMBER_OCTETS             = 62,000
```

Every octet count is over UTF-8 before cloning. URI values must also pass their
existing canonical URI grammar. Application display text is NFC, has 1 through
128 Unicode scalars, and excludes C0 and C1 controls. Both its UTF-8 bytes and
its RFC-8785 string-content bytes fit the stated caps.

The complete v2 statement has a maximum RFC-8785 payload of 2,304 octets. Its
protected header has maximum 187 octets. The fixed type is
`selfsame-credential-v2+jws`. The protected `kid` satisfies its 128-octet cap
and the v2 HTTPS-fragment grammar.

The complete compact-JWS maximum is derived exactly:

```text
base64url-no-pad(187)  = 250
base64url-no-pad(2304) = 3,072
base64url-no-pad(64)   = 86
two separators         = 2
--------------------------------
maximum compact JWS     = 3,410
policy cap              = 4,000
residual                 = 590
```

The 2,304-octet witness includes all 21 statement members. It uses four
128-octet permissions and every other declared maximum. It includes the five
credential/v2 additions from
[[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]].

The provider hint has maximum 512 canonical octets. The four-member device JWK
has maximum 96 canonical octets. The external device proof is exactly 86
base64url octets. All remaining closed keys, controls, heads, names,
delimiters, and enclosing bytes have maximum 512 octets.

```text
512  provider hint
 96  canonical device JWK
 86  external device proof
512  every remaining closed/enclosing contribution
---
1,206 actual declared auxiliary maximum
  794 residual inside the auxiliary budget
---
2,000 auxiliary budget
```

The maximum `OfferCoreV2` has the 22 members from 0.4.4 CON-939. It adds
`accountDid`, `legacyHandle`, `migrationRooms`, three migration digests, and
`snapshotNonce`. Those 29 members are the complete closed core.

The maximum non-room contribution is 4,067 canonical octets. The maximum room
array is 48,000 canonical octets. The generated core is therefore 52,067
octets, leaving 3,933 under both 56,000-octet constants.

The generator constructs that maximum legal `OfferCoreV2`. It refuses above
`V2_MAX_PAYLOAD_OCTETS`. It then constructs the complete credential member and
refuses above 62,000 octets.

The browser implementation is
`apps/cbcl_chat/priv/web/credential-v2-canonical.mjs`. It implements only the
closed RFC-8785 and deterministic-CBOR subsets used here. It SHALL NOT import,
call, copy expected bytes from, or derive through `selfsame_web_device*`.
Static import checks and differential vectors enforce source disjointness.

Rust is the hub authority through the NIF. The JavaScript module is the
browser-side independent implementation. BEAM-through-NIF is a marshalling and
integration lane, not a third canonical algorithm.

### REQ-983 — Authority mutation preserves handle changes and covers revocation

This requirement corrects 0.4.10
[[spec-008-0.4.10-standalone-tofu-first-contact#REQ-978]]. Verified authority
dominates pairaccount only while a `cbcl-device-grant` or `cbcl-member` row
exists.

`cbcl-chat-members:enroll-or-check/2` becomes one transaction. Its transaction
result reaches the caller. It takes the exact device guard before an absent
handle write.

An existing exact handle and key remains idempotent. An existing handle with a
different key refuses. For an absent handle, any device-grant row for the key
refuses. Another member row for that key does not refuse. The deployed behavior
where one browser key enrolls under successive handles remains supported.

The member public-key index remains mandatory. Pairaccount claim and
`unratified-device?` use it for authority dominance. New member enrollment does
not reinterpret it as a global member-key uniqueness rule.

If member enrollment wins the device guard, a later pairaccount claim sees the
member row and refuses. If a valid pairaccount claim wins first, later member
enrollment can create the stronger member authority. That member row makes the
claim inert. Neither ordering locks out the enrolled identity.

Both grant deleters join the guard discipline. `revoke-device-grant/2` takes
the account guard, device guard, and exact grant write lock. It then deletes.
`revoke-observed/2` takes the account guard and reads the account index. It
sorts selected raw device keys and takes their guards in raw-byte order. It
rechecks each exact row, then deletes matching rows.

Authorized revocation intentionally removes the higher authority row. An old
pairaccount row can then become visible again. Ordinary Path-B resolution still
refuses because the grant is absent or revoked. A surviving member row still
dominates. No test claims the deleted grant remains authoritative forever.

Every creation and deletion updates Mnesia indexes through ordinary Mnesia
write/delete semantics. Index-parity, restart, restore, and race tests cover
both deletion functions.

## Non-functional requirement

### NFR-913 — Lock evidence names Mnesia's real table locks and contention

`mnesia:index_read/3` inside a transaction takes a read lock on `{Table,
?ALL}`. `mnesia:foldl/3` also takes a table read lock. The implementation and
evidence SHALL name these locks. A secondary-index lookup is not described as
a per-record lock.

The operation matrix is normative:

| Operation | Acquisition sequence |
|---|---|
| grant creation | account guard; device guard; `cbcl-device-grant ?ALL` index reads; exact grant write |
| exact grant revocation | account guard; device guard; exact grant write |
| observed revocation | account guard; `cbcl-device-grant ?ALL` account index; sorted device guards; exact grant writes |
| new member enrollment | device guard; `cbcl-device-grant ?ALL` pubkey index; exact member write |
| pairaccount claim | handle guard; device guard; grant `?ALL`; member `?ALL`; pairaccount `?ALL`; exact claim write |
| unratified read | grant `?ALL`; member `?ALL`; pairaccount `?ALL` in that table order |

An operation never reverses the listed order on retry. Multi-device acquisition
uses raw-byte order. Reads spanning authority tables always use grant, member,
then pairaccount table order.

Different-account grant writers can both hold table read locks and then seek
record writes. Mnesia can abort one transaction as cyclic. This is expected.
Transactions use bounded retries. Retry exhaustion returns a closed
`authority-busy` result and performs no partial effect.

The per-frame authority read takes table locks on hot tables. This is an
accepted operational residual for this candidate. GATE-04 remains open until a
representative load test records latency, throughput, cyclic retries, writer
progress, and zero incorrect decisions. This amendment claims no unmeasured
performance property.

## Contracts

### CON-969 — Pairaccount possession failure is browser-local

`pairaccount-possession-required` is a browser-local terminal state. It has no
hub wire carrier. The hub cannot infer that a client lacks a private key.

The four-message wire remains:

```text
pairaccountgrant
pairaccountchallenge
pairaccountgrantproof
pairaccountgranted
```

The browser adds an explicit `pairaccountchallenge` handler. If the exact
claimed seed is unavailable, it emits the local terminal state and sends no
proof frame. It never renders the control frame as room content. A
credential/v2 browser with the retained seed signs the exact challenge
preimage and continues.

The public-key-only fixture remains a negative fixture. It stops locally and
cannot manufacture, embed, or recover a missing private seed.

### CON-970 — Effective-text survivor and replacement ledger

The effective set applies these exact corrections:

1. REQ-981 replaces 0.4.10 REQ-977 in full.
2. 0.4.10 CON-968 clause 1 is replaced. Only evidence statements use
   `evidenceVersion = 2`; the distinct v2 profile keeps `profileVersion = 2`.
3. 0.4.5 REQ-957 and CON-947 survive for the distinct v2 ApplicationId and
   generator output.
4. 0.4.6 REQ-962 and CON-951 survive. Credential/v1 cannot select v2.
5. 0.4.7 CON-957 survives. The v2 and cutover-v1 descriptor grammar remains
   shared, while profile documents remain distinct.
6. REQ-982 replaces 0.4.10 REQ-979's undeclared maxima and NFR-912's bound
   arithmetic.
7. REQ-983 replaces the unconditional “for all time” sentence in REQ-978. It
   also replaces the ambiguous member-collision sentence.
8. NFR-913 replaces NFR-912's global lock-order paragraph.
9. CON-969 replaces “receives `pairaccount-possession-required`” with a local
   browser terminal.
10. The source-disjoint module in REQ-982 replaces every unqualified claim of
    independent browser JavaScript.

The coordinated hub effective set is cbcl-bus 0.16.1, 0.16.2, 0.16.3, and
0.16.4 under 0.16.4's survivor ledger. In particular, cbcl-bus 0.16.2 REQ-041,
REQ-042, REQ-044, CON-013, CON-014, CON-015, and TEST-048 through TEST-056
survive unchanged.

All prior corrections not named here remain effective. They include exact-pair
TOFU, pre-display application checks, pre-consent no-custody ordering, and
pending fresh scope. They also include the dispatch-first gate, two fixture
gates, immutable status, zero terminal relay offers, and no console migration.

## Decisions

### ADR-946 — A distinct v2 ApplicationId preserves protocol identity

Evidence version describes the signed statement grammar. Profile version
describes the authenticated application document grammar. Collapsing them
merges trust anchors, permissions, and TOFU prompt history.

A distinct v2 ApplicationId makes first v2 contact visible by construction. It
also leaves deployed v1 bytes untouched. The cost is one additive profile route
and a per-branch profile recognizer. That cost is accepted.

### ADR-947 — Mnesia table-lock contention is explicit gate evidence

The current Mnesia index API trades simple consistent reads for whole-table
read locks. Hiding that lock makes the concurrency proof false. This
increment records the lock, bounded retry behavior, and production-load gate.

A cache or dirty read is rejected for this increment. Either requires a new
staleness and invalidation proof across revocation, restart, and multiple nodes.
A later reviewed optimization can replace the residual.

## Test specification

All effective TEST-1011 through TEST-1107 remain mandatory except where this
amendment replaces an expectation. Cbcl-pairing TEST-030 through TEST-055 remain
mandatory. Cbcl-bus TEST-043 through TEST-047, TEST-048 through TEST-056,
TEST-057 through TEST-064, and TEST-065 through TEST-071 are mandatory under
the 0.16.4 ledger.

### TEST-1108 — The v2 trust anchor is distinct and prompts once

Keep the live v1 bytes and digest exact. Generate the distinct v2 document and
digest. Require v1 evidence/profile 1/1 only and v2 evidence/profile 2/2 only.
Mutate ApplicationId, digest, branch, member set, or permission namespace and
require refusal.

Preload only the v1 policy pair. Require a v2 first-contact prompt. Accept it,
repeat the exact v2 pair, and require no second TOFU prompt. Both migration
decisions still appear. A relay-only or v1 policy entry cannot authorize v2.

### TEST-1109 — Every declared maximum reproduces the complete bound

Construct all 21 statement members at their maxima. Require payload 2,304,
header 187, compact JWS 3,410, and policy residual 590. Mutate each cap by one
and require the appropriate recognizer or generator failure.

Generate the maximum provider hint, JWK, proof, fixed/enclosing contribution,
OfferCoreV2, and complete credential member. Record exact actual totals and
residuals. Require core at most 56,000, auxiliary at most 2,000, and complete
member at most 62,000. A 62,001-octet member refuses before send.

### TEST-1110 — Enrollment, claims, grants, and revocation have monotonic outcomes

Enroll two different handles with one plain member key and require both to
work. Reuse one handle with another key and require refusal. Attempt enrollment
with a device-grant key and require refusal.

Race member enrollment, pairaccount proof, both grant creators, and both grant
deleters for one key. Require one documented ordering with no partial state.
While a grant or member row exists, pairaccount is inert. After authorized grant
deletion, require Path-B refusal and the documented visibility of any old claim.

Restart and restore. Compare every authority index with a reference scan after
each creation and deletion.

### TEST-1111 — Real Mnesia locks and cyclic retries are observable

Instrument every NFR-913 operation. Require the exact guard, `?ALL`, and record
lock sequence. Race distinct accounts until a cyclic abort occurs. Require a
bounded automatic retry or closed `authority-busy`, never a skipped check.

Run representative established-frame traffic while creating, enrolling, and
revoking authority. Record latency, throughput, retry counts, writer progress,
and decisions. GATE-04 stays open until a human accepts that report.

### TEST-1112 — Browser control handling and independent vectors are real

Deliver `pairaccountchallenge` to the public-key-only fixture. Require the local
terminal state, zero proof frames, and no room rendering. Deliver it to a
credential/v2 browser holding the exact seed and require a valid proof.

Run the structured corpus through Rust and
`credential-v2-canonical.mjs`. The JavaScript lane receives no Rust-produced
expected bytes. Static checks reject imports or calls to `selfsame_web_device*`.
BEAM-through-NIF must match Rust and verify marshalling.

Static effective-text checks require every CON-970 survivor and replacement.
They reject open-ended test ranges, a live-v1 v2 trust anchor, unconditional
post-revocation authority, or a per-record description of `index_read`.

## Gate and changelog

The gate remains closed. A fresh Circus review using Claude Max subscription
authentication must return PASS. The Anthropic API key SHALL be absent from that
review process. Owner approval remains necessary and does not replace review.

- **0.4.11-draft — 2026-08-23 — review candidate, not approved.** This draft
  restores the distinct v2 trust anchor and declares finite size inputs. It
  preserves handle changes, covers grant deletion, and records real Mnesia
  table locks. It defines the local possession terminal and names a
  source-disjoint browser implementation. No implementation or deployment
  authority is claimed.
