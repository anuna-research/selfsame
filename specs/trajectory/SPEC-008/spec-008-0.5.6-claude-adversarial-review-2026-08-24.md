---
id: spec-008-0.5.6-claude-adversarial-review
title: SPEC-008 0.5.6-draft — Tier-1 adversarial review of the corrected credential/v2 reissue
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 7
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `258a367fda192b31c672fac3233dc1ae94f3483b` SPEC-008 0.5.6-draft and SPEC-007 0.3.4-draft; cbcl-bus `8d3776555aafc8e21d5eb8b43de688d6e5dfc682` SPEC-053 0.17.6-draft; cbcl-pairing `4133eddb7170cd091dd7107f1164d9370213b821` SPEC-001 0.5.5-draft; did-crdt `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 7. It imported no conclusion from an earlier Claude conversation. It read the 0.5.5 report only after an independent first pass over the four coordinated parents, the deprecated did-crdt disposition, and the five pinned code baselines."
date: 2026-08-24
---

# SPEC-008 0.5.6-draft — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the coordinated 0.5.6 reissue is singular, safe, bounded,
and implementable without invention.

Metaphor: a bridge inspection before the first vehicle. The plaque now matches
most of the ground beneath it. One bolt in the new span carries no specified
thread.

Structure:

```text
independent pass over four parents + five code baselines
        -> two new findings + six observations
        -> attempt-6 N-1 through N-7 re-test
        -> earlier N, C, H, M, L, F-A through F-E, and REQ-812 re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; one HIGH; one MEDIUM; six LOW
Load-bearing: one Ed25519 proof in the increment carries no defined preimage
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         two blocking corrections and six recorded observations
Detail:       the finding sections and the assessment sections below

This report uses descriptive controlled language. Its wikilink anchors are fully
qualified. Cross-vault identifiers stay unlinked, matching the parents.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-opus-5`. The generating author is
OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model requirement
holds.

The launcher removed `ANTHROPIC_API_KEY` before this session started. The reviewer
tested that variable in the process environment. The variable is unset, and a
counting scan of the environment matched zero entries. No value reached this
report.

`claude auth status --json` ran with that variable removed. It reported
`authMethod = claude.ai`, `apiProvider = firstParty`, and
`subscriptionType = max`. This report records no email, organization identifier,
access token, API key, or account identifier.

The driver SHA-256 is
`4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

The session is fresh. The reviewer completed the independent pass before it opened
the 0.5.5 report. Every prior finding below carries an independently re-derived
disposition.

## Exact review set, baselines, and pre-implementation status

Each candidate object exists and carries the declared version.

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `258a367fda192b31c672fac3233dc1ae94f3483b` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.6-draft | yes |
| selfsame | `258a367fda192b31c672fac3233dc1ae94f3483b` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.4-draft | yes |
| cbcl-bus | `8d3776555aafc8e21d5eb8b43de688d6e5dfc682` | `specs/SPEC-053-key-root-identity.md` | 0.17.6-draft | yes |
| cbcl-pairing | `4133eddb7170cd091dd7107f1164d9370213b821` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.5-draft | yes |
| did-crdt | `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

The SPEC-008 blob is `b35873808d67cdd71f10723612d01f594c429536`. The SPEC-007 blob
is `00dfeafc4509f9ab41237f0c7bf75e17e8644839`. The SPEC-053 blob is
`42809d32146ccb2c1724c26ce6453e09d87e6e5f`. The SPEC-001 blob is
`186da4ccdbefc11fb5982144699da9e699032a05`. The SPEC-037 blob is
`5ff881b24a37e2ed3cd142f963769fa9617f379e`.

Every implementation baseline resolves as a commit. They are selfsame `0220cec2`,
cbcl-bus `ce894a7f`, cbcl-pairing `c22f2d05`, did-crdt `1f409a42`, and the
anuna-ssi namespace pin `c7d46202`.

The reviewer read every candidate through `git show COMMIT:path`. No working-tree
substitute entered this review.

The candidate is intentionally unimplemented. The reviewer treated absent candidate
code as expected. It judged the prose against the pinned baselines.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 1 | yes |
| MEDIUM | 1 | yes |
| LOW | 6 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Two blocking findings
survive, so the verdict is REJECT.

## Findings

### N-1 The credential/v2 device-possession proof carries no defined signing input

Severity: HIGH. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 declares the command
`selfsamepairofferfinalize(carrierCeremonyId, offerCoreDigest,
devicePossessionProof)`. It states that Ed25519 proofs decode to 64 octets.

cbcl-bus REQ-082 states the obligation in one sentence. The browser signs the
domain-separated device-possession input. The hub verifies byte identity and the
Ed25519 proof under the held device JWK.

No clause in any coordinated parent defines that input. The reviewer scanned all
four parents for `devicePossession`, `device-possession`, and `domain-separat`.
cbcl-bus lines 6696, 6833, and 7143 are the only device-proof matches. None carries
a preimage, a domain-separation label, a member list, or an encoding.

Every other Ed25519 and JWS construction in this increment states its exact
preimage. The offer signature covers one domain label and the offer-core digest.
`authorityStatusSignatureInput` and `browserStagingSignatureInput` each name one
32-octet SHA-256 result over an enumerated deterministic-CBOR value.
`deviceRecoveryProof` covers a four-member deterministic-CBOR array.
`finalStatusJws` and `recoveryStatusJws` name header, payload, and signing input.
cbcl-pairing CON-031 states the complete channel schedule.

cbcl-bus TEST-117 requires the exact 32-octet authority signature input. It requires
the exact 32-octet staging signature input. It requires the standard EdDSA signing
input for final status. It names the device proof only inside a mutation list and
requires no reproduction.

The gap has three consequences.

**Two implementations cannot agree.** cbcl-pairing TEST-066 and
[[SPEC-008-production-pairing-claimant#TEST-1160]] require independent
implementations to reproduce every cryptographic byte. An LFE hub and a browser
signer share no definition to reproduce for this proof.

**Cross-context reuse stays unexcluded.** cbcl-bus REQ-082 states that the browser
persists one distinct installation device seed. That single seed signs the
possession proof, the recovery proof, and the staging receipt. The increment names
no domain for the possession proof, so it excludes no input that collides with the
recovery array or the staging preimage.

**Cross-ceremony replay stays unexcluded.** The seed survives every ceremony. An
input that omits `carrierCeremonyId` or `offerCoreDigest` accepts a proof captured
during an earlier ceremony. That proof gates the step where the hub registers
ceremony-pending device state and signs the complete application offer.

The hub code baseline supplies no inherited construction. The reviewer scanned
`ce894a7f` under `apps/` for a device-proof preimage and found none. GATE-04 states
that credential/v2 carries no pairaccount writer or call edge, so the pairaccount
challenge supplies none. The existing Selfsame `CON-207` possession input at
`apps/cbcl_chat/priv/web/selfsame/selfsame_web_device.js:790` belongs to
credential/v1, which [[SPEC-008-production-pairing-claimant#REQ-1005]] keeps frozen.

Correction: state the exact `devicePossessionProof` preimage in cbcl-bus CON-012.
Give it one domain-separation label and one enumerated deterministic-CBOR value.
Bind at least the carrier ceremony, the offer-core digest, and the socket
generation. Add its reproduction to cbcl-bus TEST-117.

### N-2 One named failure mode still misdescribes the pinned baseline

Severity: MEDIUM. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant]] states four named failure modes. Its
`Named failure modes` section declares that the current baseline repairs each one.
The declared baseline is `0220cec2dec44cd95d4f411ea4814d790b6716d2`.

Three of FM-3's four clauses now resolve exactly. The reviewer re-derived each one.

`src-tauri/src/cbcl_context.rs:342` constructs `SelfsameVerificationContext` in
production. That construction closes `assemble_claimant`, declared at `:244`. The
file's first `#[cfg(test)]` attribute sits at `:359`.
`crates/selfsame-web-device/src/lib.rs:205` is the second production site.

`src-tauri/src/cbcl_pairing.rs:171` calls `assemble_claimant` inside the
`#[cfg(not(feature = "local-pairing-demo"))]` branch declared at `:140`. The
enclosing command is `cbcl_pairing_start`. That function calls
`selfsame_app_identity_net::profile::fetch_or_cached` at `:265`,
`cbcl_relay::verify_invitation_origin` at `:275`,
`selfsame_app_identity_net::webfinger::fetch_and_verify` at `:306`, and
`selfsame_app_identity_net::state::resolve_closure` at `:317`.

**The fourth clause is false.** FM-3 states that the registered `app_grant.rs`
commands remain absent from the UI.

`src-tauri/src/lib.rs:96` through `:101` register six commands from that module.
`src/index.html:951` loads `app.js` as a module script, so `src/app.js` is the
shipped wallet surface. `src/app.js:580` invokes `cbcl_enrol_prepare`.
`src/app.js:615` and `:621` invoke `cbcl_enrol_confirm`.
`src-tauri/src/app_grant.rs:752` and `:812` define those two commands.
`src-tauri/src/lib.rs:100` and `:101` register them.

Two of the six registered `app_grant.rs` commands therefore reach the person's own
screen. Only `app_grant_review`, `app_grant_prepare`, `app_grant_confirm`, and
`cbcl_enrol_start` stay unreachable.

The 0.5.5 report reached the opposite conclusion from a search over `src/index.html`
and `src/pairing.js`. That search omitted `src/app.js`, the module `index.html`
actually loads. The 0.5.6 revision carried the incomplete conclusion into the
parent.

The consequence is a planning defect rather than a mechanism defect. Every
requirement, contract, bound, and test stays internally consistent. The reachable
pair is the SPEC-004 enrolment wire that
[[SPEC-008-production-pairing-claimant#REQ-1005]] requires credential/v2 to leave
frozen and untouched. A plan built from this description understates that frozen
surface and its live entry point.

[[SPEC-008-production-pairing-claimant#TEST-1157]] requires every current witness to
resolve. This clause does not.

FM-1, FM-2, and FM-4 all resolve. `src-tauri/src/cbcl_pairing.rs:177` calls
`connect_wss`. `src-tauri/Cargo.toml:46` enables `rustls-tls-webpki-roots`.
`crates/selfsame-pairing/src/lib.rs:16` gates `local_demo` behind
`local-pairing-demo`, and `local_demo.rs:15` holds the demo digest.
`src-tauri/src/cbcl_registry.rs:17` declares `APPROVED_CONFORMANCE` with the
`anuna-1` entry beginning `0x70, 0x29, 0xf2, 0x22` at `:25`, and
`production_relay_policy()` sets `allow_loopback: false` at `:38`.

Correction: restate FM-3's fourth clause against `0220cec2`. Name
`cbcl_enrol_prepare` and `cbcl_enrol_confirm` as reachable from the UI, or remove
the clause.

## Low observations

### L-1 One refusal clause names a boundary the design crosses by intent

[[SPEC-008-production-pairing-claimant#REQ-906]] states that TLS failure,
candidate-profile failure, malformed origin, and consent-store unavailability end
the attempt before network I/O.

A TLS failure follows network I/O by construction.
[[SPEC-008-production-pairing-claimant#CON-988]] requires a live HTTPS profile fetch
before the exact-pair prompt, so that request precedes consent by design.

The intended boundary is the relay socket. The `Controls` list, CON-988,
[[SPEC-008-production-pairing-claimant#CON-902]], and
[[SPEC-008-production-pairing-claimant#CON-986]] each state that boundary exactly,
so no implementable ambiguity survives.

### L-2 The shared receipt body mixes two member-naming conventions

cbcl-pairing CON-028 declares `credential-v2-receipt-body` with four members. Three
use kebab case: `carrier-ceremony-id`, `final-status-jws`, and
`final-status-digest`. One uses camel case: `predecessorDigest`.

The nine bodies in [[SPEC-008-production-pairing-claimant#CON-987]] spell the same
ceremony value `carrierCeremonyId`. One protocol therefore carries two spellings of
one identifier across its eleven bodies.

Both grammars are closed and exact, so recognition stays deterministic.
cbcl-pairing TEST-064 requires the exact receipt map.

### L-3 One declared frame sum is a valid bound rather than the exact maximum

cbcl-bus CON-012 states that the final command's other encoded values contain at
most 7,042 ASCII octets.

The reviewer recomputed those seven values. Five 32-octet digests encode to 43
unpadded base64url octets each. `issuerDid` contains at most 512 ASCII octets. A
4,608-octet `stagingReceipt` encodes to 6,144 octets. The sum is 6,871.

The declared value therefore over-states the maximum by 171 octets. The claim stays
true, the 1,470,680 total stays a valid bound, and the 29,320-octet headroom stays
available. cbcl-bus TEST-117 requires at least that headroom.

### L-4 A carrier can pass allocation and still lose its whole preparation window

cbcl-bus REQ-082 refuses a carrier with 300 or fewer remaining seconds. It admits
301. `pendingExpiresAt` then equals that carrier expiry.

Offer preparation requires a remaining interval from 300 through 600 seconds. A
carrier admitted at 301 remaining seconds therefore leaves a two-second preparation
window. A carrier admitted at 400 remaining seconds leaves 101 seconds.

cbcl-pairing NFR-023 permits a v2 mailbox lifetime from 60 through 600 seconds. No
coordinated clause requires the browser to request a lifetime that supports a whole
offer window.

The outcome is deterministic and fail-closed. The ceremony expires atomically and
writes no identity effect. cbcl-bus TEST-117 exercises 299, 300, and 301.

### L-5 One consolidated contract carries no inbound requirement edge

[[SPEC-008-production-pairing-claimant#CON-986]] states that it implements REQ-902,
REQ-906, and REQ-1006. None of those three requirements names CON-986 in its own
trace line.

[[SPEC-008-production-pairing-claimant#CON-987]],
[[SPEC-008-production-pairing-claimant#CON-988]], and
[[SPEC-008-production-pairing-claimant#CON-989]] each carry the matching inbound
edge from [[SPEC-008-production-pairing-claimant#REQ-1006]].

CON-986 stays reachable through the `Controls` list in the `Orientation` section.

### L-6 Local contract references appear in two citation forms

[[SPEC-008-production-pairing-claimant]] cites
[[SPEC-004-application-scoped-identity#CON-204]],
[[SPEC-004-application-scoped-identity#CON-225]], and
[[SPEC-004-application-scoped-identity#REQ-231]] as fully qualified wikilinks.

It cites `CON-220`, `CON-201`, `CON-219`, and `CON-409` as bare identifiers. All
four resolve locally. `CON-220`, `CON-201`, and `CON-219` head sections in
`specs/SPEC-004-application-scoped-identity.md`. `CON-409` heads a section in
`specs/PROTO-003-selfsame-pairing-v1.md`.

[[SPEC-008-production-pairing-claimant#CON-985]] justifies unlinked cbcl-pairing,
cbcl-bus, and anuna-ssi identifiers as cross-vault pointers. That justification does
not reach these four local identifiers.

## Disposition of the 0.5.5 findings

Each row is independently re-derived at the current candidates and the pinned code.

| 0.5.5 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 two named failure modes contradict the pinned baseline | **PARTLY CLOSED; one clause REOPENED** | FM-3's construction, caller, and live-fetch clauses now resolve exactly at `0220cec2`, and FM-4 resolves in full. FM-3's fourth clause does not. See [[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-2 One named failure mode still misdescribes the pinned baseline]]. |
| N-2 the generation provenance names the wrong correction input | **CLOSED** | The `Tier-1 generation provenance` section now names [[spec-008-0.5.5-claude-adversarial-review-2026-08-24#Collected corrections and owners]]. That heading exists in the named report, and the listed corrections match its seven rows. cbcl-bus names the same report and its three hub rows. |
| N-3 the signed negative depends on an optional expiring tombstone | **CLOSED** | cbcl-bus CON-036 states that a lapsed tombstone makes a terminal ceremony and an unheld ceremony indistinguishable. Both return `unknown`, and the hub signs no terminal state without retained evidence. [[SPEC-008-production-pairing-claimant#CON-989]] preserves pending, and [[SPEC-008-production-pairing-claimant#TEST-1161]] requires that outcome. |
| N-4 one account-scope disposition is narrower than the value's travel | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] now names the protected channel, signed final status, and private installed record. [[SPEC-004-application-scoped-identity#REQ-217]] prohibits the scope in a DID, DID Document, `acct:` URI, VC, JWS header, WebFinger response, provider hint, status entry, log, analytics event, or other public artefact. The scope enters a JWS payload rather than a header, and no credential/v2 carrier is public. |
| N-5 one capacity counter mixes transient and permanent records | **CLOSED** | cbcl-bus REQ-082 states that active pending records and immutable accepted statuses both consume the 65,536 capacity. CON-036 states that each accepted status consumes one record until explicit service erasure. cbcl-bus TEST-117 fills the capacity with a mixture and requires exactly one returned slot after erasure. |
| N-6 one narrow relay lifetime admits an allocation that cannot prepare | **CLOSED as bounded** | cbcl-bus REQ-082 now refuses 300 remaining seconds as well as 299. cbcl-bus TEST-117 requires refusal for 299 and 300 and success for 301. The preparation interval stays separate at 300 through 600 seconds. See [[spec-008-0.5.6-claude-adversarial-review-2026-08-24#L-4 A carrier can pass allocation and still lose its whole preparation window]]. |
| N-7 two consolidated contracts carry no trace lines | **CLOSED** | CON-988 names REQ-906 and REQ-1006, and it names TEST-1160 as its verifier. CON-989 names REQ-1006 and TEST-1161. [[SPEC-008-production-pairing-claimant#REQ-906]] carries the inbound edge to CON-988, and REQ-1006 carries both. TEST-1161 names CON-989 in its validates line. |

## Disposition of the 0.5.4 findings

| 0.5.4 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 two hub authority-command arguments carry no size bound | **CLOSED** | cbcl-bus CON-012 bounds `rawGrant` at 1 through 49,152 octets and `resolverClosure` at 1 through 1,048,576 octets. `crates/selfsame-app-identity-net/src/state.rs:342` declares `MAX_CLOSURE_OCTETS` as `1_048_576` and gates `bounded_body` at `:559`. `crates/selfsame-app-identity/src/grant.rs:55` and `crates/selfsame-app-identity/src/ceremony.rs:70` both read `65_536`. |
| N-2 the logical-body contract has no traceability edge | **CLOSED** | CON-987 names REQ-1006 and TEST-1160. REQ-1006 carries the inbound edge. |
| N-3 one signed staging-receipt member has no stated meaning | **CLOSED** | `browser-staging-receipt-v2` declares thirteen members, and the signature input enumerates the preceding twelve. The reviewer recomputed the grammar maximum as 3,015 octets, matching the stated value. |
| N-4 one prohibition list depends on prepositional attachment | **CLOSED** | CON-986 opens the sentence with the credential/v2 scope. REQ-1006 keeps the standing registry compiled. |
| N-5 two signature clauses name their input less exactly than the third | **CLOSED for the two named clauses** | cbcl-bus CON-012 states both 32-octet inputs exactly. A third Ed25519 proof in the same contract carries no input. See [[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-1 The credential/v2 device-possession proof carries no defined signing input]]. |
| N-6 one current hub clause fails strict controlled-language lint | **CLOSED** | The reviewer extracted all fourteen SPEC-053 coordinated sections by their exact heading boundaries. Every extraction returns zero errors and zero warnings. |
| N-7 the bound authority outcome is unreachable in this increment | **CLOSED** | cbcl-bus CON-012 records the `bound` arm as a fail-safe for a colliding, restored, or corrupt reciprocal-alias row. A normal Path-A first link reaches only `no-binding`. |
| N-8 a backwards hub clock step yields a non-conforming offer core | **CLOSED** | cbcl-bus REQ-082 returns closed `unavailable` above 600 seconds. It preserves the pending record, emits no offer core, and permits an authenticated resample. |
| N-9 the post-payload checkpoint expiry rule names only one role | **CLOSED** | cbcl-pairing CON-030 permits a null expiry only for a claimant checkpoint after durable payload send. An allocator checkpoint always carries its numeric expiry. TEST-065 requires refusal for a substituted allocator null. |

## Disposition of the 0.5.3 findings

| 0.5.3 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 hub authority response has no grammar | **CLOSED** | `authority-status-response-v2` declares six members. The reviewer recomputed the maximum bound-DID encoding as 1 + 45 + 34 + 34 + 6 + 515 + 66, which equals 701 octets against a 768-octet bound. |
| N-2 ordinary-build registry prohibition contradicts two current tests | **CLOSED** | REQ-1006 scopes the prohibition to credential/v2. `src-tauri/src/cbcl_registry.rs:17` survives unchanged. |
| N-3 offer window has no stated terminal effect | **CLOSED** | cbcl-bus REQ-082 samples `issuedAt` under the pending lock, expires below 300 remaining seconds, and refuses above 600. CON-012 makes the interval exclusive with no skew allowance. |
| N-4 post-payload refusal strands claimant recovery | **CLOSED** | cbcl-pairing CON-028 projects `payload -> receipt` with no refusal arm. CON-030 makes a post-payload refusal invalid input, and CON-032 keeps the checkpoint pending. |
| N-5 eleven object kinds carry no integer assignment | **CLOSED** | cbcl-pairing CON-028 assigns `0 offer` through `10 receipt`. The control arm fixes `1: 1..8` and the large arm fixes `1: 0 / 9 / 10`. |
| N-6 claimant checkpoint wrapping key has no info encoding | **CLOSED** | CON-986 states `checkpointInfo` with explicit big-endian length prefixes, the raw 32-octet salt, and the extract order. |
| N-7 one checkpoint member carries no name | **CLOSED** | cbcl-pairing CON-030 names the fifth outer member the raw carrier ceremony identifier, and TEST-065 requires that equality. |
| N-8 declared Selfsame test set omits the other Selfsame parent | **CLOSED** | CON-985 names TEST-901 through TEST-915 and TEST-1156 through TEST-1161 here, and TEST-801 through TEST-821 in the other parent. The reviewer counted fifteen, six, and twenty-one headings. |
| N-9 one construction count cannot be resolved | **CLOSED** | CON-988 numbers seven ordered steps, and the reviewer counted seven. |
| N-10 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | REQ-1006 requires the comment to cite [[SPEC-007-cbcl-pairing-cutover#CON-806]]. `src-tauri/src/cbcl_registry.rs:1` still reads the old citation at `0220cec2`, which is declared implementation work. |
| N-11 one code citation names the wrong line range | **CLOSED** | The parent cites `src/pairing.js:159`, and that line opens the scan function at `0220cec2`. |

## Disposition of the 0.5.2 findings

| 0.5.2 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 credential/v2 key schedule exists in no current parent | **CLOSED** | cbcl-pairing CON-031 states a thirteen-member public context, one transcript hash, one extract under an empty salt, seven expands, both Finished values, the counter rule, the nonce rule, and the AAD map. The reviewer counted thirteen members. |
| C-2 nine of eleven logical body grammars exist in no current parent | **CLOSED** | CON-987 declares nine bodies, and the reviewer counted nine grammar blocks. The offer resolves to cbcl-bus `signed-offer-v2` and the receipt to cbcl-pairing `credential-v2-receipt-body`. |
| H-1 pre-socket CON-220 gate loses its binding step | **CLOSED** | [[SPEC-004-application-scoped-identity#CON-220]] numbers six obligations. Its steps 1 through 5 match CON-988's restatement exactly, including the 65,536-octet body cap. Step 6 needs a `CON-409` record that credential/v2 never creates. |
| H-2 signed offer-core member has no defined input | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-806]] fixes six descriptor members and unique origins. cbcl-bus CON-035 selects the unique descriptor by exact relay-origin equality and refuses caller digests. |
| M-1 safety parent contradicts the offer-core source rule | **CLOSED** | CON-806 states that credential/v2 takes the application identifier and origin only from the CON-988-bound live profile. |
| M-2 two parents disagree about the relay-origin bound | **CLOSED** | cbcl-pairing CON-027 declares a 272-octet version-2 origin. `schemas/pairing-v1.cddl:13` still reads `relay-origin = tstr .size (1..255)` at `c22f2d05`. |
| M-3 post-final wallet recovery has no path after the window | **CLOSED** | CON-989, cbcl-bus CON-036, and cbcl-pairing CON-032 state the token, commitment, route, closed request, four-arm response, and reducer. The reviewer recomputed the request maximum as 2,187 octets against 2,304, and the largest response arm as 8,294 octets against 9,216. |
| L-1 two parents decompose one claimant differently | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-807]] and CON-986 name the same five functions and the same recovery adapter. |
| L-2 profile grammar attributed to an absent SPEC-004 amendment | **CLOSED** | CON-806 states that it defines `cbclPairingRelays` itself. |
| L-3 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | See the 0.5.3 N-10 row. |
| L-4 one version constant declared twice under one name | **CLOSED** | REQ-1005 records one duplication. `crates/selfsame-app-identity/src/lib.rs:109` and `crates/selfsame-app-identity/src/profile.rs:60` each declare `PROFILE_VERSION` at `0220cec2`. |

## Disposition of the 0.5.1 findings

| 0.5.1 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 declared hub test set exists only in trajectory | **CLOSED** | cbcl-bus declares TEST-115 through TEST-119, and the reviewer counted five headings. |
| H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | cbcl-pairing REQ-031 names the carrier ceremony identifier the sole credential/v2 identifier. The reviewer traced it through carrier, CPace context, every body, every command, every response, both checkpoints, recovery, receipt, and installed state. |
| H-2 reconnect recovery has no command | **CLOSED** | cbcl-bus CON-012 carries `selfsamepairstatus`, the socket-generation digest, and the device recovery proof. `apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:62` and `:63` define `gen-conn-nonce` as sixteen strong random octets. |
| H-3 four offer-core members carry no grammar | **CLOSED** | The reviewer counted exactly twenty `OfferCoreV2` members. `deviceDid` carries the 56-octet `did:key` grammar with a multicodec check. |
| H-4 rendezvous dependency has no consumer | **CLOSED** | CON-986 forbids every Selfsame and did-crdt rendezvous edge. did-crdt SPEC-037 0.1.2 keeps the deprecation and burns its four former identifiers. |
| H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The candidate diff from `1f409a42` to `6d1cdbeb` adds one specification file and changes no code. `src/service/rendezvous.rs` stays byte-identical. |
| H-6 REQ-812 amendment uses an unavailable channel | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#REQ-812]] states the boundary directly, and all four parents record the same coordinated set and session. |
| H-7 hub names a cbcl-pairing test that does not exist | **CLOSED** | cbcl-pairing TEST-063 exists, and the reviewer counted eight credential/v2 test headings from TEST-060 through TEST-067. |
| M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | CON-986 scopes the prohibition to credential/v2. The standing caller is `src-tauri/src/app_grant.rs:539`. |
| M-2 exact-pair consent boundary has no owning function | **CLOSED** | CON-986 gives `authorise_claimant_relay` the prompt and gives `prepare_claimant` the policy write after CPace binding. |
| M-3 `WIRE_VERSION` names half of its namespace | **CLOSED** | REQ-1005 states that the constant governs both link-offer and link-grant records under the pinned anuna-ssi commit. |
| M-4 did-crdt worst-case store rises seventeen-fold | **CLOSED** | The rendezvous service stays byte-identical at the candidate commit. |
| L-1 executor order stated in three places with two shapes | **CLOSED** | CON-902, CON-986, and TEST-1158 state one order. |
| L-2 schema-migration enumeration misses a third site | **CLOSED** | NFR-928 names `cbcl-chat-roomcfg:transform-room/1` as the third schema migration outside the transaction inventory. |
| L-3 two failure modes misdescribe the pinned code | **PARTLY CLOSED** | FM-1 and FM-2 hold, and FM-4 now holds. FM-3 retains one false clause. See [[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-2 One named failure mode still misdescribes the pinned baseline]]. |
| L-4 stale `cbcl_registry.rs` comment | **CLOSED as a normative instruction** | See the 0.5.3 N-10 row. |

## Hard-stop disposition, F-A through F-E

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-903]] keys the sealed set on the exact pair. It excludes a relay-only key, a global allowlist, a conformance-digest registry, and an application wildcard. REQ-1006 requires a fresh prompt for a new application on an accepted relay. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input and requires byte equality on every overlapping value. REQ-1006 adds peer labels, caller booleans, and DOM room values to the prohibition. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | At `0220cec2` the scope read, derivation, issuer creation, WebFinger call, and closure resolution all precede `connect_wss` at `src-tauri/src/cbcl_pairing.rs:177`. CON-902, CON-986, and REQ-812 move every one after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end | **CLOSED** | cbcl-bus REQ-082 allocates the account identifier, scope, and snapshot nonce inside one level-1 transaction. The stable principal commits the raw identifier through a domain-separated SHA-256 preimage, and `OfferCoreV2` carries no raw account identifier. |
| F-E what does and does not prove relay operatorship | **CLOSED** | [[SPEC-008-production-pairing-claimant#ADR-902]] states that the application profile does not prove relay operatorship. CON-806 keeps operator and privacy policy display-only, and TEST-915 keeps the two-operator depth test open. |

## Inherited SPEC-007 REQ-812 boundary

[[SPEC-007-cbcl-pairing-cutover#REQ-812]] remains a hard stop and states the
credential/v2 boundary directly.

| REQ-812 clause | Coordinated agreement |
|---|---|
| no application identity effect before preliminary approval | REQ-902, CON-902, CON-986, and TEST-1158 agree. |
| preliminary approval authorizes one zeroizing derivation and a pure preview | CON-986 authorizes one custody call and forbids signature, publication, grant, alias, bundle, and durable write. |
| no signing, publication, resolution, issuance, persistence, staging, or payload before final approval | CON-986 and cbcl-bus REQ-082 agree. The hub performs no account, alias, grant, membership, room, or successor write at preliminary approval. |
| final approval authorizes only the declared causal construction | CON-986 lists one order, and TEST-1158 requires it. |
| one encrypted non-authorizing completion checkpoint before the first causal effect | CON-986 requires the sealed pending completion first and states its exact wrapping-key derivation. |
| browser staging is crash-safe and non-authorizing until immutable acceptance | cbcl-bus CON-012 defines a thirteen-member staging receipt with twelve signed members and a 3,015-octet grammar maximum. |
| compensation touches only ceremony-owned effects | CON-986 routes compensation through [[SPEC-004-application-scoped-identity#CON-204]] and forbids a change to operator withdrawal state. |

The boundary holds. Neither new finding breaches it. Both concern a missing
construction and a descriptive clause, and neither creates an early identity effect.

## Attempt-6 correction re-test

| Required correction | Result |
|---|---|
| 1 FM-3 and FM-4 describe `0220cec2` exactly | **NO.** FM-4 and three FM-3 clauses resolve. FM-3's fourth clause does not. See N-2. |
| 2 provenance and coordinated-parent pointers name the immediate input and exact set | **YES.** All four parents name the 0.5.5 report, the same generation session, and the same four-document review set. |
| 3 a lapsed tombstone makes terminal and unheld ceremonies indistinguishable | **YES.** cbcl-bus CON-036 returns `unknown` for both. CON-989 and TEST-1161 preserve pending until explicit unlink or root purge. |
| 4 account scope travel agrees with SPEC-004 confidentiality and grants no public authority | **YES.** CON-986 names all three carriers. [[SPEC-004-application-scoped-identity#REQ-217]] prohibits the scope in a JWS header and in public artefacts, and no credential/v2 carrier is either. |
| 5 the 65,536 capacity counts pending and accepted records with exact erasure effects | **YES.** cbcl-bus REQ-082 and CON-036 state both, and TEST-117 requires exactly one returned slot per erasure. |
| 6 allocation refuses 299 and 300, accepts 301, and keeps the 300-through-600 interval | **YES.** cbcl-bus REQ-082 and TEST-117 state each outcome. See L-4 for the residual window. |
| 7 CON-988 and CON-989 carry correct outbound and inbound trace edges | **YES.** Both name their requirements and verifying tests. REQ-906 and REQ-1006 carry the inbound edges. |

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact application-relay TOFU resists a new application; operatorship residual honest | **YES.** CON-903 keys on the exact pair, REQ-1006 requires a fresh prompt for a new application on an accepted relay, and ADR-902 states the residual plainly. |
| 2 CPace binds the origin-authenticated profile before durable policy, display, custody, or effect | **YES.** CON-988 returns an origin-recognised candidate and denies it verifier, display, policy, and key authority. The profile digest enters both endpoint contexts and the public context, and both Finished values precede the row. |
| 3 no peer, carrier, DOM, route, verifier, or caller value reaches either consent screen early | **YES.** cbcl-pairing CON-029 builds the display only from the authority input after byte equality. The verifier returns a closed verdict, and the final screen adds only the local DID and fingerprint. |
| 4 every effect follows its consent gate | **YES.** REQ-902, CON-902, CON-986, REQ-812, CON-807, cbcl-bus REQ-082, and TEST-1158 state one ordering with no gap. |
| 5 fresh scope bootstrap removes the enrolment cycle without leaking the account identifier | **YES.** The hub allocates fresh CSPRNG values inside its authenticated transaction. The principal commits the raw identifier through a domain-separated preimage, and no offer member carries it. |
| 6 one carrier ceremony identifier throughout | **YES.** The reviewer traced the value through carrier, CPace context, all eleven bodies, all seven commands, all eight responses, both checkpoints, recovery, receipt, and installed state. |
| 7 two implementations reproduce the schedule, kinds, bodies, predecessors, and checkpoint KDF | **YES for the named items.** CON-031 states the thirteen-member context and the complete schedule. CON-028 assigns all eleven kinds. CON-987 states nine bodies, and CON-986 states the checkpoint KDF with explicit length prefixes. |
| 8 every declared maximum recomputes | **YES.** The reviewer recomputed 50,221, 1,583, 1,570, 701, 3,015, 1,470,680, and 29,320 by hand and matched each exactly. The signed offer recomputes as 56,625 against a declared 56,640. The 49,152, 1,048,576, and 1,500,000 values match the code and the transport. See L-3. |
| 9 Cowboy refuses complete and fragmented oversize before the parser | **YES.** Cowboy 2.12.0 `cowboy_websocket.erl:450` refuses a declared frame length above `max_frame_size` at header parse. Line `:494` refuses the cumulative fragment buffer inside `dispatch_frame`, before `websocket_handle`. Line `:666` sends close code 1009. Decoded argument bounds refuse before lookup, mutation, resolver recognition, or NIF entry. |
| 10 the closure limit matches the resolver client; the grant limit matches payload and recognizer | **YES.** `MAX_CLOSURE_OCTETS` reads `1_048_576` at `crates/selfsame-app-identity-net/src/state.rs:342`. The 49,152 grant bound equals CON-987's payload member and sits below the `65_536` recognizer. |
| 11 authority and staging Ed25519 inputs are exact 32-octet results; every signer fixed | **PARTLY.** Both named inputs are exact 32-octet SHA-256 results over enumerated deterministic-CBOR values, and every named signer is fixed. The third Ed25519 proof this increment introduces has a fixed signer and no fixed input. See N-1. |
| 12 removing the staging generation leaves stale substitution prevented | **YES.** The receipt binds application, ceremony, account, scope, device, offer, payload, grant, issuer, profile, and recovery commitment under a deterministic signature. |
| 13 signed `bound` status is a safe fail-safe and cannot authorize a mismatched preview | **YES.** cbcl-bus CON-012 records the fail-safe scope. CON-987 requires a bound DID byte-identical to `previewIssuerDid` and permits only refusal otherwise. |
| 14 one exclusive deadline with exact outcomes at every boundary | **YES.** `expiresAt` equals `pendingExpiresAt` with no skew allowance. Below 300 seconds expires, above 600 returns closed `unavailable`, equality refuses, and post-payload expiry preserves the slot for recovery only. |
| 15 null checkpoint expiry cannot be misused; allocator expiry composes with recovery | **YES.** Only a claimant checkpoint takes a null expiry, and only after durable payload send. After allocator expiry the browser's hub-pending and staging state drive signed status recovery. |
| 16 no retry, rebind, crash, replay, rotation, erasure, or expiry duplicates an effect | **YES for every stated path.** Exact retransmission is the only idempotence. Every hub retry returns byte-identical responses, the wallet slot is a tagged union, and recovery cannot install without the retained pending slot. N-1 leaves one proof replay unexcluded. |
| 17 hub locks, capacity, migration, terminals, grammar, limiter, retention, and staging total | **YES.** All three lock orders, both capacity bounds, six terminal classes, eight closed responses, the anonymous limiter, and the retention rule are stated. |
| 18 credential/v1 bytes and valid production behavior stay frozen under the new ceiling | **YES.** The ceiling binds only the authenticated chat WebSocket. Its largest declared bounded field decodes to 262,144 octets at `apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:1727`. The cbcl-pairing relay keeps its 70,000-octet ceiling. |
| 19 did-crdt absent from every credential/v2 edge; the sole HTTPS exception is status recovery | **YES.** CON-986 forbids every rendezvous edge, TEST-1159 permits only the CON-989 route, and the did-crdt candidate diff adds one specification file and no code. |
| 20 every current clause traceable and strict-lint clean without trajectory authority | **YES with two observations.** All sixteen extracted SPEC-008 sections, all fourteen extracted SPEC-053 sections, and the complete cbcl-pairing and SPEC-007 parents return zero errors and zero warnings. Every self-anchor resolves. See L-5 and L-6. |

## Repository and code-claim assessment

Every testable code claim except FM-3's fourth clause resolves exactly at its stated
baseline.

**Selfsame version sites.** All eighteen witness rows in
[[SPEC-008-production-pairing-claimant#REQ-1005]] resolve at `0220cec2` with the
stated class. The additional comparison row at
`crates/selfsame-app-identity/src/enrollment.rs:592` resolves. So do the three
ingest substitutions at `crates/selfsame-web-device/src/lib.rs:831`, `:847`, and
`:861`. So do the three untyped ingress rows. The recorded duplication is real.

**cbcl-bus consumer sites.** Every line
[[SPEC-008-production-pairing-claimant#NFR-928]] names resolves with the stated
consumer at `ce894a7f`. The reviewer checked `cbcl-chat-session-ws.lfe:347`, `:385`,
`:512`, `:869`, `:965`, `:1050`, `:1076`, `:1683`, `:1788`, and `:1898`. It checked
`cbcl-chat-room.lfe:201`, `:221`, `:263` through `:270`, `:550`, and `:677`. It
checked `cbcl-chat-rooms.lfe:49`, `:54`, `:60`, `:61`, and `:99` through `:104`. It
checked `cbcl-chat-path-b-poller.lfe:17` and `:32`, `cbcl-chat-pairgrant.lfe:78`,
`cbcl-chat-successor.lfe:109` and `:134`, and
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76`.

**The named repairs match real defects.** `cbcl-chat-successor.lfe:109` wraps
`cbcl-chat-members:pubkey-tx/1` in a catch. Line `:134` both catches and calls the
transaction-opening `cbcl-chat-account-grants:device-grant`. NFR-928 forbids exactly
those edges.

**The Cowboy pin and bound hold.** `rebar.config:107` pins `cowboy` at `2.12.0`. No
handler sets `max_frame_size` today, so the chat socket inherits an unbounded
default. `cowboy_websocket.erl:450` compares the declared frame length before
payload buffering. Line `:494` compares the fragment buffer sum inside
`dispatch_frame`, and that clause matches both continuation and final fragments.

**The upstream bounds exist.** cbcl-pairing `src/mailbox.rs:15` sets
`MAX_FRAME_BODY_BYTES` to `69_632`. `src/channel.rs:21` sets `MAX_SEALED_PLAINTEXT`
to `69_556`. `schemas/pairing-v1.cddl:13` keeps the version-1 relay origin at 1
through 255 octets. `Cargo.toml` pins every cryptographic dependency exactly and
carries `bip39` for the presence-code exclusion.

**The CON-220 restatement is exact.**
[[SPEC-004-application-scoped-identity#CON-220]] numbers six obligations. Steps 1
through 5 require HTTPS certificate validation, redirect rejection, media-type and
encoding rejection, a 65,536-octet body cap, complete CON-201 recognition, and exact
identifier equality. CON-988 restates each one and substitutes CPace binding for
step 6.

**The device proof has no baseline referent.** A scan of `ce894a7f` under `apps/`
found no device-possession preimage. See
[[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-1 The credential/v2 device-possession proof carries no defined signing input]].

**FM-3's fourth clause does not resolve.** See
[[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-2 One named failure mode still misdescribes the pinned baseline]].

## Consent assessment

Two decisions gate every identity effect. The exact-pair decision precedes the
socket, and the two-stage ceremony decision precedes derivation and delivery.

Rejection at the pair prompt creates no policy entry, socket, wallet scope, key,
alias, DID, grant, or publication. The durable row waits for the CPace-bound profile
digest.

Preliminary approval authorizes one zeroizing derivation, a pure DID and
fingerprint, and one disclosure the screen states. Final approval authorizes the
declared construction and nothing earlier.

Every displayed value comes from the authenticated authority input. No peer string,
DOM value, caller boolean, or verifier return reaches either screen.

## Wire and bounds assessment

Eleven object kinds map to integers 0 through 10. Only the offer, payload, and
receipt use the large arm.

Every declared maximum recomputes as a correct bound. Control bodies stay within
2,048 octets and large bodies within 62,000. The reviewer recomputed the comparison
body at 1,583 octets and the binding body at 1,570. It recomputed the payload body
at 50,221, the authority response at 701, and the staging receipt at 3,015.

The final hub command sums to at most 1,470,680 encoded argument octets. It leaves
at least 29,320 octets for syntax under a 1,500,000-octet frame. Cowboy refuses a
larger complete or fragmented message with close code 1009.

## Cryptography assessment

The public context contains thirteen enumerated members. The transcript, extract,
seven expands, both Finished values, the counter rule, the nonce rule, and the AAD
map are exact.

Every concatenated preimage places its variable-length value last or gives it an
explicit length prefix. The recovery commitment, the checkpoint info, and the
migration confirmation digest each satisfy that rule.

Every label is disjoint from version 1. The checkpoint wrapping key states an
explicit salt, an explicit info encoding, and an explicit output length.

One Ed25519 proof carries no input. See
[[spec-008-0.5.6-claude-adversarial-review-2026-08-24#N-1 The credential/v2 device-possession proof carries no defined signing input]].

## Recovery and atomicity assessment

The claimant checkpoint precedes the first identity effect. The browser writes one
inactive staging record before hub finalization.

The hub commits one final transaction or none. The browser activates only after it
verifies the immutable status, and the wallet installs only after it verifies the
same bytes.

Signed status recovery reproduces the ordinary receipt transition. It rebuilds no
grant, repeats no signature, republishes no DID, and resends no payload.

## Persistence assessment

The wallet slot is one tagged pending-or-installed union. The hub holds one pending
record per ceremony and one immutable status per finalized account.

Every crash boundary recovers exactly one of absent, the complete pending record, or
the complete finalized state.

Capacity is bounded at 69,632 octets per pending record and 65,536 records in total.
Accepted statuses consume that budget until explicit service erasure.

## Rendezvous assessment

No credential/v2 object reaches a Selfsame or did-crdt rendezvous slot. The only
non-relay route is the signed post-window status recovery to the authenticated
application origin.

did-crdt SPEC-037 0.1.2 records the deprecation and burns its former identifiers.
The candidate diff adds one specification file and changes no code.

## Compatibility assessment

Credential/v1 keeps its carrier, schedule, display, wire bytes, and decisions. The
version-1 recogniser cannot read a version-2 carrier.

The new WebSocket ceiling binds only the authenticated chat socket. Its largest
declared bounded field decodes to 262,144 octets, well below the ceiling.

The standing registry and selection control stay compiled and unchanged, with one
corrected implementation comment.

## Authority assessment

Each parent states its own current law and names the same coordinated set. All five
documents record the same generation family, version, and session.

Every version pointer between parents matches the reviewed object. No duplicate
artefact identifier exists in any parent, and no burned identifier returns to use.

The reviewer counted 42 GATE-04 boxes, 20 `OfferCoreV2` members, and 15
`FinalStatusCoreV2` members. It counted nine Selfsame successor body grammars. It
counted 21 SPEC-008 tests, 21 SPEC-007 tests, five hub tests, and eight
cbcl-pairing credential/v2 tests.

## Stopping-rule assessment

The reviewer stopped after the finding rate reached two blocking defects and six
observations across a complete pass.

The pass covered four parents, one deprecated disposition, five pinned baselines,
one Cowboy release, and the shipped wallet surface. It recomputed every declared
maximum by hand rather than by trusting the text.

Further search does not change the verdict, because one HIGH finding already blocks
PASS.

## Surfaces found sound

- The two-stage consent boundary and its instrumented prohibition list.
- The seven-step CON-220 substitute and its explicit CPace digest binding.
- The complete credential/v2 channel schedule and its version-1 disjointness.
- All eleven logical bodies, their predecessors, and their recomputed maxima.
- The Cowboy frame ceiling, including its cumulative fragment guard.
- The Mnesia lane model, its level-1 requirement, and its static nesting proof.
- The signed final status, its recovery token, and its commitment binding.
- The indistinguishable `unknown` result and its pending-slot retention rule.
- The shared capacity counter and its exact erasure effect.
- The exact-pair policy contract and its honest operatorship residual.
- Strict controlled-language lint across every extracted section and two parents.

## Residual assumptions

- The application profile does not prove relay operatorship, and exact-pair consent
  contains that residual without solving it.
- The wallet fetches the candidate profile over HTTPS before the exact-pair prompt,
  so a scanned carrier reaches its named origin before consent.
- Explicit service data erasure of an accepted status leaves the wallet fail-closed
  with no capability and the hub with an orphaned account.
- The browser's comparison assertion stays a browser-side claim. The wallet's own
  final screen and its local DID remain the wallet's protection.
- The raw recovery token stays outside page script only while the browser allocator
  endpoint runs outside page script.
- The relay supplies the carrier expiry on its own clock, and the hub compares that
  value against its own whole UTC seconds.
- The chat WebSocket carries no frame ceiling today, so the new bound tightens an
  unbounded surface rather than relaxing a stated one.
- Two depth tests stay open with named owners, covering a real relay ceremony and
  two independently operated relays.

## What this verdict authorizes and does not authorize

This verdict authorizes nothing. It records two blocking corrections and six
non-blocking observations for the next coordinated reissue.

It does not authorize the Elephant plan, a test-first plan, implementation,
production allocation, release, or deployment.

It closes no GATE-04 box and changes no production gate. It changes no requirement,
contract, test, or bound in any parent.

## Collected corrections and owners

| Finding | Severity | Owner | Correction |
|---|---|---|---|
| N-1 | HIGH | cbcl-bus | State the exact `devicePossessionProof` preimage with one domain label and one enumerated deterministic-CBOR value. Bind the carrier ceremony, offer-core digest, and socket generation. Require its reproduction in TEST-117. |
| N-2 | MEDIUM | selfsame | Restate FM-3's fourth clause against `0220cec2`. Name `cbcl_enrol_prepare` and `cbcl_enrol_confirm` as reachable from the UI, or remove the clause. |
| L-1 | LOW | selfsame | Replace REQ-906's network-I/O boundary with the relay-socket boundary the other clauses state. |
| L-2 | LOW | cbcl-pairing | Align `credential-v2-receipt-body` member naming with the nine consumer bodies, or record the split convention. |
| L-3 | LOW | cbcl-bus | Restate the 7,042-octet figure as the exact 6,871-octet maximum, or mark it a declared upper bound. |
| L-4 | LOW | cbcl-bus | Require a v2 mailbox lifetime that supports a whole 300-second preparation window. |
| L-5 | LOW | selfsame | Give CON-986 the inbound trace edge its sibling contracts carry. |
| L-6 | LOW | selfsame | Cite `CON-220`, `CON-201`, `CON-219`, and `CON-409` as fully qualified wikilinks. |
