---
id: spec-008-0.5.4-claude-adversarial-review
title: SPEC-008 0.5.4-draft — Tier-1 adversarial review of the coordinated response, expiry, and recovery closure
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 5
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `67f61f4218583fb1240a10515d027dd6bae45c91` SPEC-008 0.5.4-draft and SPEC-007 0.3.2-draft; cbcl-bus `c0676fdbe0a18465168c4e4e73fd0a6c09fd263c` SPEC-053 0.17.4-draft; cbcl-pairing `506eab9f2e201c986b9b6e0b85b9f7cdab36b488` SPEC-001 0.5.3-draft; did-crdt `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 5. It imported no conclusion from any earlier Claude conversation. It read the 0.5.3 report only after an independent first pass over the four coordinated parents, the deprecated did-crdt disposition, and the five pinned code baselines."
date: 2026-08-24
---

# SPEC-008 0.5.4-draft — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the coordinated 0.5.4 reissue is singular, safe,
bounded, and implementable without invention.

Metaphor: a bridge inspection before the first vehicle. Every span from the
previous inspection now carries its stated load. One newly examined deck
fitting still has no specified capacity.

Structure:

```text
independent pass over four parents + five code baselines
        -> nine new findings
        -> prior N/C/H/M/L re-test
        -> F-A through F-E re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; zero HIGH; one MEDIUM; eight LOW
Load-bearing: two hub authority-command arguments carry no size bound
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         one blocking correction and eight recorded observations
Detail:       the finding sections and the assessment sections below

This report uses descriptive controlled language. Its wikilink anchors are
fully qualified. Cross-vault identifiers stay unlinked, matching the parents.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-opus-5`. The generating author
is OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model
requirement holds.

The launcher removed `ANTHROPIC_API_KEY` before starting this session. The
reviewer tested that variable's presence in the process environment. The
variable is unset, and a second scan of the environment matched zero entries.
No value reached this report.

`claude auth status --json` ran with that variable removed. It reported
`authMethod = claude.ai`, `apiProvider = firstParty`, and
`subscriptionType = max`. This report records no email, organization
identifier, access token, API key, or account identifier.

The driver SHA-256 is
`4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

The session is fresh. The reviewer completed the independent pass before
opening the 0.5.3 report. Every prior finding below carries an independently
re-derived disposition.

## Exact review set, baselines, and pre-implementation status

Each candidate object exists and carries the declared version.

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `67f61f4218583fb1240a10515d027dd6bae45c91` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.4-draft | yes |
| selfsame | `67f61f4218583fb1240a10515d027dd6bae45c91` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.2-draft | yes |
| cbcl-bus | `c0676fdbe0a18465168c4e4e73fd0a6c09fd263c` | `specs/SPEC-053-key-root-identity.md` | 0.17.4-draft | yes |
| cbcl-pairing | `506eab9f2e201c986b9b6e0b85b9f7cdab36b488` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.3-draft | yes |
| did-crdt | `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

The SPEC-008 blob is `2104b274c312583f523fd06583bca8822d0e409f`. The SPEC-007
blob is `d4419d6b4df8c42f9e6887754cc1df75e4ca162c`. The SPEC-053 blob is
`4ecd761e1b95885ecdfeaf887a5b9654c7e3a0fd`. The SPEC-001 blob is
`e5c9ae878c6cdf46f10efa7576b7cad682fa6a5b`. The SPEC-037 blob is
`5ff881b24a37e2ed3cd142f963769fa9617f379e`.

Every implementation baseline resolves as a commit. They are selfsame
`0220cec2`, cbcl-bus `ce894a7f`, cbcl-pairing `c22f2d05`, did-crdt `1f409a42`,
and the anuna-ssi namespace pin `c7d46202`.

The reviewer read every candidate through `git show COMMIT:path`. No
working-tree substitute entered this review.

The candidate is intentionally unimplemented. The reviewer treated absent
candidate code as expected. It judged the prose against the pinned baselines.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 | yes |
| LOW | 8 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. One blocking finding
survives, so the verdict is REJECT.

## Findings

### N-1 Two hub authority-command arguments carry no size bound

Severity: MEDIUM. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 declares seven commands at SPEC-053 lines 6823 to 6835. The
final authority command is
`selfsamepairaccept(carrierCeremonyId, rawGrant, resolverClosure,
payloadDigest, migrationConfirmationDigest, issuerDid, grantId, stagingReceipt,
receiptRecoveryCommitment)` at lines 6829 to 6832.

The same contract then bounds its arguments at lines 6838 to 6842. It states
that request, ceremony, nonce, commitment, and SHA-256 digest arguments decode
to 32 octets. It states 64 octets for `transcriptHash`, 16 for `recoveryNonce`,
64 for Ed25519 proofs, and at most 4,608 for `stagingReceipt`.

That list omits `rawGrant` and `resolverClosure`. Lines 6843 and 6844 then
state that unknown arguments, duplicate arguments, wrong lengths, or trailing
terms refuse before lookup or mutation. A wrong length has no meaning for an
argument whose length rule is absent.

The reviewer searched all four coordinated parents for a bound on either value.
SPEC-053 mentions `resolverClosure` only at line 6829 and line 7249. Line 7249
requires the hub NIF to recognise the raw grant and resolver closure before the
final transaction. SPEC-008 mentions the resolver closure at lines 1218 and
1398 and states no octet bound. SPEC-007 and cbcl-pairing SPEC-001 mention
neither value.

No inherited bound exists either. The hub's current closure path takes its
closures from a configured local resolver command rather than from a caller.
`apps/cbcl_chat/src/shell/cbcl-chat-path-b-resolver.lfe:19` accepts
`resolver_closures` from `cbcl-chat-path-b-poller.lfe:27`, which invokes the
configured resolver command. Credential/v2 newly accepts a caller-supplied
closure on an authority command.

The transport applies no ceiling. `cbcl-chat-session-ws.lfe:97` to `:103`
return `cowboy_websocket` with only `idle_timeout` set, so the frame size stays
at cowboy's unbounded default. `websocket_handle` at `:154` to `:156` passes
every binary and text frame straight to `handle-frame`.

The failure scenario is concrete. An enrolled Path-A member holds a valid
pending ceremony and sends `selfsamepairaccept` with a 200-megabyte
`resolverClosure`. The socket buffers it, the CBCL parser builds the term, and
the NIF recognises it, all before any stated bound applies. A few connections
exhaust the node and stop the chat service.

The omission also contradicts the coordinated posture. cbcl-bus CON-036 bounds
an anonymous HTTP request at 2,304 octets and its response at 9,216. SPEC-001
NFR-023 bounds every relay value. The deployed repository bounds every other
caller-supplied blob. `cbcl-chat-session-ws.lfe:1723` to `:1728` caps an opaque
GroupInfo at 262,144 octets. Its comment states that bounds keep a relay from
storing arbitrary blobs.

`rawGrant` is the weaker instance. SPEC-008 CON-987 bounds the payload body's
`grant` member to 49,152 octets, so an honest browser cannot exceed that value.
A hostile browser can, and the hub carries no stated refusal. `issuerDid` is
different again: cbcl-bus CON-012 line 7280 caps the `FinalStatusCoreV2` member
at 512 UTF-8 octets, so its bound follows from the core grammar.

An implementer must therefore invent one capacity value on an authority path.
Too small a value refuses legitimate closures for accounts with long delta
history. Too large a value keeps the exhaustion scenario. That choice is a
security decision, and no clause in the coordinated set makes it.

### N-2 The logical-body contract has no traceability edge

Severity: LOW. Owner: selfsame repository owner.

SPEC-008 CON-987 declares the nine Selfsame logical bodies at lines 1272 to
1405. It is the most load-bearing wire contract in the parent.

The identifier `CON-987` appears exactly once in SPEC-008, at its own heading.
No requirement traces to it, and no test names it. The contract carries no
`Implements:` line and no `Verified by:` line.

Its neighbours all carry both. CON-901 states them at lines 277 and 278.
CON-902 states them at lines 368 and 369. CON-903 states them at lines 394 and
395. CON-986 states them at lines 1269 and 1270.

The traceability table at lines 546 to 559 covers REQ-901 through REQ-908 and
both NFR rows. It covers no consolidated identifier, so CON-987 appears nowhere
in the graph.

One inbound pointer exists outside this vault. cbcl-pairing CON-029 states that
the consumer's nine successor body grammars are Selfsame SPEC-008 0.5.4-draft
CON-987.

The content is present and exact, so nothing is undecidable. A plan generated
from the parent's own trace graph can still omit the contract.

### N-3 One signed staging-receipt member has no stated meaning

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 declares `browser-staging-receipt-v2` at SPEC-053 lines 7223
to 7238. Its twelfth member is `bstr .size 32` with the comment
`inactiveStagingGeneration`.

That identifier appears exactly once in the whole coordinated set, at line
7235. No clause states its preimage, its generator, its equality obligation, or
its consumer.

The member enters the signature. Lines 7241 to 7245 require the signature to
cover the preceding thirteen members. They require the hub to verify it before
its acceptance witness. The hub holds an expected value for every other signed
member and none for this one.

Nothing depends on it. `FinalStatusCoreV2` at lines 7258 to 7274 omits it, so
the immutable status never echoes it. Activation matching already keys on the
carrier ceremony identifier, which appears in both objects.

The neighbouring standard is explicit. cbcl-pairing CON-031 states that every
comment in the public context names the semantic value in that exact position.
The 0.5.3 report raised the same class of omission as its N-7, and cbcl-pairing
0.5.3-draft closed it by naming the fifth checkpoint member.

An implementer must invent the value. A fresh 32-octet random value satisfies
every stated obligation, so the invention stays inconsequential.

### N-4 One prohibition list depends on prepositional attachment

Severity: LOW. Owner: selfsame repository owner.

SPEC-008 CON-986 states a build prohibition at lines 1258 to 1261. It states
that the wallet ordinary build contains no compiled conformance registry, relay
allowlist, held-enrolment precondition, or fallback to `assemble_claimant` on
the credential/v2 path.

One reading attaches the closing phrase to the whole list. A second reading
attaches it only to the last item, which makes the first three prohibitions
build-scoped.

The second reading contradicts the same parent. SPEC-008 REQ-1006 lines 741 to
743 state that the standing credential/v1 registry and selection control remain
compiled and unchanged.

The first reading is the correct one. The verb governs the list, the next
sentence repeats the same scope for `record_pairing_trust`, and REQ-1006
settles the outcome in both directions.

The pinned code shows why the scope matters.
`crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93` define
eligibility, and line 90 requires `approved_conformance.contains(...)`.
Deleting `src-tauri/src/cbcl_registry.rs:17` therefore refuses every
non-loopback origin on the deployed credential/v1 path.

No implementer choice survives, because REQ-1006 states the credential/v1
disposition directly. The residue is one ambiguous sentence.

### N-5 Two signature clauses name their input less exactly than the third

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 states the offer signature input at SPEC-053 lines 7124 to
7126. It states that the Ed25519 signature input is a null-terminated offer
domain string followed by `offerCoreDigest`.

Two other objects use a different phrase. Line 6911 states that the authority
response signature covers SHA-256 over its domain followed by the exact
deterministic-CBOR encoding of the preceding five members. Lines 7240 to 7242
state the same shape for the staging receipt's thirteen members.

The two shapes differ in the document. The offer signs a concatenation
directly. Under the plain reading the other two sign a 32-octet digest of a
concatenation.

Only that reading stays technically coherent. Ed25519 hashes with SHA-512, so a
named SHA-256 can only be the signed message. A reader who signed the
concatenation leaves the phrase SHA-256 over with no role.

The reproducibility requirement makes exactness load-bearing. The hub signs the
authority response and the Selfsame wallet verifies it, so two codebases in two
repositories must agree byte for byte.

The coordinated tests catch a divergence before deployment. cbcl-bus TEST-117
requires construction and verification of both outcomes. SPEC-008 TEST-1160
requires independent generation and byte-identical vectors.

### N-6 One current hub clause fails strict controlled-language lint

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus generation provenance requires the reviewer to execute strict
controlled-language lint before deciding. The reviewer extracted each
consolidated current-law section of SPEC-053 by its exact heading boundaries.

All sixteen consolidated SPEC-008 sections return zero errors and zero
warnings. The complete SPEC-008, SPEC-007, SPEC-001, and SPEC-037 parents also
return zero errors and zero warnings.

Thirteen of fourteen extracted SPEC-053 sections return zero errors. CON-012
returns one `sentence_over_limit` error, with a longest sentence of 27 words
against the 25-word descriptive limit.

The complete SPEC-053 parent returns 1,341 errors. That count belongs to the
document's large historical body, which no current clause in this increment
governs.

SPEC-008 TEST-1157 names cbcl-pairing and SPEC-007 for the whole-parent lint
and does not name SPEC-053. The single CON-012 error therefore sits outside the
declared Selfsame lint scope.

### N-7 The bound authority outcome is unreachable in this increment

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 declares the authority response at SPEC-053 lines 6899 to
6906. Lines 6909 and 6910 give status `no-binding` a null DID and give status
`bound` one canonical reciprocal DID read under lock.

REQ-082 line 6704 states that the command reads reciprocal alias authority by
the pending stable application account. Lines 6615 and 6616 state that the hub
allocates fresh independent 32-octet CSPRNG values for `applicationAccountId`,
`accountScopeId`, and `snapshotNonce`.

A fresh account holds no alias. The reciprocal alias for that account is
written only by this ceremony's own final transaction, at lines 6726 to 6728.
No concurrent ceremony can reach it, because every ceremony allocates its own
account.

The outcome is therefore structurally unreachable for a Path-A first link.
REQ-082 line 6676 states that a caller cannot select `NoTransition`, so every
ceremony in this increment is a first link.

The arm remains a required fail-safe rather than dead weight. A restored or
corrupt alias row under a colliding account must still refuse, and
`binding-confirmed` supplies the refusal.

Both endpoints still carry implementation and test cost. cbcl-bus TEST-117 and
SPEC-008 TEST-1160 both require the outcome and its vectors.

### N-8 A backwards hub clock step yields a non-conforming offer core

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 bounds the signed window at SPEC-053 lines 7080 to 7083. It
states that `expiresAt` equals the held `pendingExpiresAt` and follows
`issuedAt` by 300 through 600 seconds.

REQ-082 lines 6633 and 6634 set `pendingExpiresAt` to the earlier carrier relay
expiry or allocation time plus 600 seconds. Line 6669 samples `issuedAt` under
the pending lock at offer preparation.

The upper bound holds only for a monotone hub clock. A backwards step between
allocation and preparation makes `pendingExpiresAt` minus `issuedAt` exceed 600
seconds.

The preparation guard does not catch it. Lines 6670 and 6671 expire the
ceremony when fewer than 300 seconds remain, and a backwards step leaves more.

The hub then emits a core outside its own grammar. A conforming wallet or
browser recogniser refuses it, so the ceremony fails closed with no identity
effect.

No parent names a terminal for that case. cbcl-pairing NFR-011 states the
equivalent relay rule, so the neighbouring style covers backwards steps
explicitly.

### N-9 The post-payload checkpoint expiry rule names only one role

Severity: LOW. Owner: cbcl-pairing repository owner.

cbcl-pairing CON-030 declares `credential-v2-checkpoint` at SPEC-001 lines 1891
to 1901. Its seventh member is `null / uint`, at line 1898.

Lines 1906 and 1907 state that `null` applies only after a claimant durably
sends its payload and before receipt. That sentence names a window and not a
role.

Both roles checkpoint inside that window. Line 1927 requires the allocator to
checkpoint from carrier allocation until terminal receipt, and the allocator
still owes the receipt after the payload arrives.

The erasure rule protects only one role. Lines 1943 and 1944 state that relay or
offer expiry after durable payload send does not erase the claimant checkpoint.

The recognizer accepts either value. Line 1923 requires the state to be
unexpired or to use the permitted post-payload `null` value.

An allocator checkpoint sealed with `null` therefore never expires and never
gains the claimant's retention guarantee. The record stays browser-local and
non-authorizing, so no protocol effect follows.

## Disposition of the 0.5.3 findings

Each row is re-derived from the current candidates and the pinned code.

| 0.5.3 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 hub authority response has no grammar | **CLOSED** | cbcl-bus CON-012 adds `selfsamepairauthorityresult` to the closed response set at SPEC-053 lines 6851 to 6853. Lines 6900 to 6919 declare `authority-status-response-v2` as a six-member deterministic-CBOR array, state the domain-separated signature, require the offer `kid` and key, and define `authorityStatusDigest` over the complete response. The reviewer recomputed the maximum bound-DID encoding as 1 + 45 + 34 + 34 + 6 + 515 + 66 = 701 octets against the stated 768. SPEC-008 CON-987 carries `authorityStatusResponse` in both confirmation bodies and states the wallet's digest, signature, ceremony, offer-digest, status, DID, and preview checks. |
| N-2 ordinary-build registry prohibition contradicts two current tests | **CLOSED** | SPEC-008 REQ-1006 lines 736 to 743 now scope the prohibition to the credential/v2 path and state that the standing credential/v1 registry and selection control remain compiled and unchanged. SPEC-007 TEST-817 lines 1100 and 1101 keep the empty or hostile registry for credential/v2 and require its inertness. SPEC-008 TEST-1159 lines 1848 to 1851 fail only a match on the credential/v2 path. The destructive reading disappears, and `src-tauri/src/cbcl_registry.rs:17` survives. A residual ambiguity remains as [[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-4 One prohibition list depends on prepositional attachment]]. |
| N-3 offer window has no stated terminal effect | **CLOSED** | cbcl-bus REQ-082 lines 6669 to 6674 remove the 120-second window, sample `issuedAt` under the pending lock, expire below 300 remaining seconds, and set `OfferCoreV2.expiresAt` equal to `pendingExpiresAt`. CON-012 lines 7080 to 7083 make the interval exclusive and remove every offer clock-skew allowance. REQ-082 lines 6721 to 6724 resample time after locks at each final-transaction attempt. SPEC-008 CON-986 lines 1164 to 1172 state the wallet's refresh points and both dispositions. TEST-1158 and cbcl-bus TEST-117 exercise `expiresAt - 1`, `expiresAt`, and `expiresAt + 1`. A narrow clock-anomaly residual survives as [[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-8 A backwards hub clock step yields a non-conforming offer core]]. |
| N-4 post-payload refusal strands claimant recovery | **CLOSED** | cbcl-pairing CON-028 line 1789 now projects `payload -> receipt` with no refusal arm. Lines 1794 to 1799 state that no endpoint emits or accepts a post-payload refusal. CON-030 lines 1947 and 1948 make an attempted post-payload refusal invalid input that neither advances nor erases the retained checkpoint. CON-032 lines 2044 and 2045 state that no received refusal moves a post-payload claimant out of that state. SPEC-008 CON-986 lines 1248 and 1249 repeat the retention. cbcl-pairing TEST-067 and SPEC-008 TEST-1161 both drive the case. |
| N-5 eleven object kinds carry no integer assignment | **CLOSED** | cbcl-pairing CON-028 lines 1723 to 1738 assign `0 offer` through `10 receipt` and state that no list order, enum order, caller value, or version-1 assignment replaces the table. The assignment agrees with both envelope arms, because the control arm fixes `1: 1..8` and the large arm fixes `1: 0 / 9 / 10`. TEST-063 and TEST-066 now require the exact mapping and refuse a permuted assignment. |
| N-6 claimant checkpoint wrapping key has no info encoding | **CLOSED** | SPEC-008 CON-986 lines 1183 to 1196 state `checkpointInfo` as a length-prefixed pair over the label and the canonical application identifier. They state `checkpointPrk` as an HKDF-SHA512 extract, and they name the ceremony identifier the raw 32-octet salt. The 64-octet hierarchy root matches `crates/selfsame-app-identity/src/hierarchy.rs:101`, which declares `HierarchyRoot(Zeroizing<[u8; 64]>)`. |
| N-7 one checkpoint member carries no name | **CLOSED** | cbcl-pairing CON-030 line 1893 now comments the fifth member `carrierCeremonyId`, and line 1901 states it in prose. TEST-065 requires the fifth outer member to equal the raw carrier ceremony identifier. |
| N-8 declared Selfsame test set omits the other Selfsame parent | **CLOSED** | SPEC-008 CON-985 lines 1088 to 1092 rename the range the SPEC-008 test set and state that SPEC-007 TEST-801 through TEST-821 remain the other current Selfsame test set. All twenty-one SPEC-007 headings exist, from `#### TEST-801` at line 941 to `#### TEST-821` at line 1133. |
| N-9 one construction count cannot be resolved | **CLOSED** | SPEC-008 CON-988 lines 1438 to 1447 now number the seven ordered steps explicitly. The reviewer counted seven numbered items. |
| N-10 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | SPEC-008 REQ-1006 lines 742 and 743 require the implementation comment to cite SPEC-007 CON-806 rather than CON-903. `src-tauri/src/cbcl_registry.rs:1` still reads `SPEC-008 CON-903` at `0220cec2`, which is now declared implementation work rather than a specification conflict. |
| N-11 one code citation names the wrong line range | **CLOSED** | SPEC-008 line 100 now cites `src/pairing.js:159`. That line at `0220cec2` reads `async function scan() {`. `src-tauri/capabilities/mobile-scanner.json` grants exactly the check-permissions, request-permissions, and scan barcode-scanner permissions. |

## Disposition of the 0.5.2 findings

Each row is independently re-derived at the current candidates.

| 0.5.2 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 credential/v2 key schedule exists in no current parent | **CLOSED** | cbcl-pairing CON-031 states `credential-v2-public-context` at SPEC-001 lines 1957 to 1971. The reviewer counted thirteen members and confirmed the per-position comments. The contract states `TH`, an HKDF-SHA512 extract under an empty salt, seven expands at 32, 32, 32, 32, 12, 12, and 32 octets, both 64-octet Finished values, the counter rule, the nonce rule, and `credential-v2-aad`. The labels stay disjoint from version 1, whose `src/channel.rs:118` to `:146` use `pairing-v1` labels at `c22f2d05`. |
| C-2 nine of eleven logical body grammars exist in no current parent | **CLOSED** | SPEC-008 CON-987 declares nine CDDL bodies at lines 1276 to 1364. The offer resolves to cbcl-bus CON-012 `signed-offer-v2` and the receipt to cbcl-pairing CON-028. Every `predecessorDigest` binds CON-031's raw `objectContentHash`. The former residual `authorityStatusDigest` now carries its own response grammar. |
| H-1 pre-socket CON-220 gate loses its binding step | **CLOSED** | SPEC-008 CON-988 states the seven-step substitute and forbids a CON-409 record, rendezvous slot, caller digest, TLS-only profile, or durable TOFU row. SPEC-004 CON-220 numbers six obligations, and steps 1 through 5 match CON-988's restatement exactly. `crates/selfsame-app-identity-net/src/profile.rs:64` to `:98` implement steps 1 through 5 and state in comment that step 6 belongs to the caller. `crates/selfsame-app-identity/src/discovery.rs:48` sets `MAX_BODY_OCTETS` to 65,536. |
| H-2 signed offer-core member has no defined input | **CLOSED** | SPEC-007 CON-806 fixes six descriptor members, requires unique operator identifiers and relay origins, and states the descriptor digest over the canonical descriptor. cbcl-bus CON-035 selects the unique descriptor by exact relay-origin equality and refuses duplicates, origin-only projections, other descriptors, and caller digests. |
| M-1 safety parent contradicts the offer-core source rule | **CLOSED** | SPEC-007 CON-806 lines 780 to 785 state that for credential/v2 the application identifier and HTTPS origin come only from the CON-988-bound live profile. The preceding paragraph carries the credential/v1-only qualifier. |
| M-2 two parents disagree about the relay-origin bound | **CLOSED** | cbcl-pairing CON-027 declares a separate 272-octet version-2 origin rule and states the unchanged version-1 rule. `schemas/pairing-v1.cddl:13` at `c22f2d05` still reads `relay-origin = tstr .size (1..255)`. SPEC-007 CON-806 and cbcl-bus CON-012 both state 272. |
| M-3 post-final wallet recovery has no path after the window | **CLOSED** | SPEC-008 CON-989 states the exporter-derived token and commitment. cbcl-bus CON-036 states the route, media type, closed request, four-arm response, and locked terminal-absence predicate. cbcl-pairing CON-032 states `recover_receipt` and the private authority. The reviewer recomputed the request maximum as 2,187 octets against 2,304, and the largest response as 8,294 octets against 9,216. |
| L-1 two parents decompose one claimant differently | **CLOSED** | SPEC-007 CON-807 and SPEC-008 CON-986 name the same five functions and the same terminal recovery adapter. |
| L-2 profile grammar attributed to an absent SPEC-004 amendment | **CLOSED** | SPEC-007 CON-806 states that this contract defines `cbclPairingRelays` and that no SPEC-004 amendment supplies the member. |
| L-3 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | See the N-10 row above. |
| L-4 one version constant declared twice under one name | **CLOSED** | SPEC-008 REQ-1005 records one duplication and keeps `profile::PROFILE_VERSION` the sole definition. Both `crates/selfsame-app-identity/src/lib.rs:109` and `crates/selfsame-app-identity/src/profile.rs:60` still declare the constant at `0220cec2`. |

## Disposition of the 0.5.1 findings

Each row is independently re-derived at the current candidates.

| 0.5.1 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 declared hub test set exists only in trajectory | **CLOSED** | cbcl-bus REQ-081 declares TEST-115 through TEST-119, and all five headings exist at SPEC-053 lines 7781, 7907, 7933, 8049, and 8069. SPEC-008 CON-985 names the same range. |
| H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | cbcl-pairing REQ-031 names the carrier ceremony identifier the sole credential/v2 ceremony identifier. The reviewer traced it through the carrier, the CPace context, the public context, all eleven bodies, all seven hub commands, the response set, the checkpoint salt and fifth member, the receipt, the recovery request, the final status, and the installed record. |
| H-2 reconnect recovery has no command | **CLOSED** | cbcl-bus CON-012 carries `selfsamepairstatus`, the 16-octet recovery nonce, the socket-generation digest, and the 64-octet device recovery proof. `cbcl-chat-session-ws.lfe:62` and `:63` define `gen-conn-nonce` as sixteen strong random octets, matching the stated width. Lines 110 to 113 and 143 to 149 emit the bootstrap frame carrying that nonce and the hub identifier. |
| H-3 four offer-core members carry no grammar | **CLOSED** | The reviewer counted exactly twenty `OfferCoreV2` members. `deviceDid` carries the 56-octet `did:key` grammar with a multicodec check. `applicationAccountDisplay`, `platformBindingId`, and `returnUri` appear only in TEST-117's negative row at SPEC-053 line 7983. |
| H-4 rendezvous dependency has no consumer | **CLOSED** | SPEC-008 CON-986 forbids every Selfsame and did-crdt rendezvous edge on the credential/v2 path. GATE-04 carries the matching box. did-crdt SPEC-037 0.1.2 keeps the deprecation and burns its former identifiers, and commit `829855c22f254192fa56fd2a69ecc85d11711683` exists. |
| H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The whole candidate diff from `1f409a42` to `6d1cdbeb` adds only `specs/SPEC-037-pairing-rendezvous-body-bound.md`. `src/service/rendezvous.rs` stays untouched, and line 65 still declares its 4,096-octet slot bound. |
| H-6 REQ-812 amendment uses an unavailable channel | **CLOSED** | SPEC-007 0.3.2-draft states the boundary directly in REQ-812 and CON-807. All four parents name the same coordinated set and the same generation session. |
| H-7 hub names a cbcl-pairing test that does not exist | **CLOSED** | cbcl-pairing TEST-063 exists at SPEC-001 line 2130. cbcl-bus TEST-115 and SPEC-008 TEST-1156 name the same accepted and refused control-body rows. |
| M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | SPEC-008 CON-986 scopes the prohibition to the credential/v2 path and preserves the standing SPEC-004 caller. That caller is `src-tauri/src/app_grant.rs:539` inside `commit_pairing_trust`, which `:512` and `:852` invoke. |
| M-2 exact-pair consent boundary has no owning function | **CLOSED** | SPEC-008 CON-986 gives `authorise_claimant_relay` the prompt and provisional capability, and gives `prepare_claimant` the policy write after CPace binding. CON-902 names `cbcl_pairing_relay_approve` and `cbcl_pairing_relay_decline` the only page entry points. |
| M-3 `WIRE_VERSION` names half of its namespace | **CLOSED** | SPEC-008 REQ-1005 states that the constant governs both link-offer and link-grant records. `crates/selfsame-core/src/record.rs:329`, `:396`, `:462`, and `:490` carry the offer and grant sites at `0220cec2`. |
| M-4 did-crdt worst-case store rises seventeen-fold | **CLOSED** | The rendezvous service stays byte-identical at the candidate commit, and its slot bound stays 4,096 octets. |
| L-1 executor order stated in three places with two shapes | **CLOSED** | SPEC-008 CON-902, CON-986, and TEST-1158 state one order: re-derivation, preview equality, issuer creation, signing, publication, closure verification, WebFinger verification, grant construction, and payload construction. |
| L-2 schema-migration enumeration misses a third site | **CLOSED** | SPEC-008 NFR-928 names `cbcl-chat-roomcfg:transform-room/1` the third schema migration. `apps/cbcl_chat/src/shell/cbcl-chat-roomcfg.lfe:81` to `:84` define it over `mnesia:transform_table/4`. |
| L-3 two failure modes misdescribe the pinned code | **CLOSED** | SPEC-008 labels FM-1 and FM-2 historical and repaired. `src-tauri/src/cbcl_pairing.rs:20` imports `MaybeTlsStream`, and `:177` reaches `cbcl_transport::connect_wss`. |
| L-4 stale `cbcl_registry.rs` comment | **CLOSED as a normative instruction** | See the N-10 row above. |

## Hard-stop disposition, F-A through F-E

Each row is re-derived from the candidate text and the pinned code.

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | SPEC-008 CON-903 keys the sealed set on the exact pair and excludes a relay-only key, global allowlist, conformance-digest registry, and application wildcard. REQ-906 states that relay-only trust never authorizes a new application. REQ-1006 requires a fresh prompt when a profile names an accepted relay under another application. TEST-909, TEST-910, and TEST-1159 drive it. The replaced control is `crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93`. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input and none from the peer input, requires byte equality on every overlapping value, and forbids generic `DisplayField` values and the wire `authority_summary`. SPEC-008 REQ-1006 adds peer labels, caller booleans, and DOM room values to the prohibition. The pinned surface still carries both prohibited shapes: `src-tauri/src/cbcl_pairing.rs:57` declares `authority_summary`, and `:61` to `:67` declare the generic label and value fields. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | At `0220cec2`, `src-tauri/src/cbcl_context.rs:279` reads the scope, `:285` derives the identity, `:288` creates the issuer, `:306` verifies WebFinger, and `:317` resolves the closure. All precede `connect_wss` at `src-tauri/src/cbcl_pairing.rs:177`. SPEC-008 CON-902 and CON-986 and SPEC-007 REQ-812 move every one after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end | **CLOSED** | cbcl-bus REQ-082 allocates the account identifier, scope, and snapshot nonce inside one authenticated level-1 transaction. The stable principal commits the raw identifier through a SHA-256 preimage, and `OfferCoreV2` carries no raw account identifier. `crates/selfsame-app-identity/src/scope.rs:20` states that the scope appears in no DID, DID Document, or account URI. The derivation stays namespaced under the application node. |
| F-E what does and does not prove relay operatorship | **CLOSED** | SPEC-008 ADR-902 states that the application profile does not prove relay operatorship and that exact-pair consent contains the residual. SPEC-007 CON-806 keeps the operator and privacy policy display-only. TEST-915 keeps the two-operator depth test open with a named owner. |

## Inherited SPEC-007 REQ-812 boundary

REQ-812 remains a hard stop and states the credential/v2 boundary directly. The
reviewer re-tested every clause against SPEC-008 and cbcl-bus.

| REQ-812 clause | Coordinated agreement |
|---|---|
| no application identity effect before preliminary approval | SPEC-008 REQ-902, CON-902, CON-986, and TEST-1158 agree. |
| preliminary approval authorizes one zeroizing derivation and a pure preview | SPEC-008 CON-986 authorizes one custody call and forbids signature, publication, grant, alias, bundle, and durable write. `src-tauri/src/custody.rs:312` already exposes `use_hierarchy_root`. |
| no signing, publication, resolution, issuance, persistence, staging, or payload before final approval | SPEC-008 CON-986 and cbcl-bus REQ-082 agree. The hub performs no account, alias, grant, membership, room, or successor write at preliminary approval. |
| final approval authorizes only the declared causal construction | SPEC-008 CON-986 lists the nine steps in one order, and TEST-1158 requires that order. |
| one encrypted non-authorizing completion checkpoint before the first causal effect | SPEC-008 CON-986 requires the sealed pending completion first and states its exact wrapping-key derivation. |
| browser staging is crash-safe and non-authorizing until immutable acceptance | cbcl-bus CON-012 defines `browser-staging-receipt-v2` with fourteen members and thirteen signed. The reviewer computed its maximum as 3,049 octets against the stated 4,608. |
| compensation touches only ceremony-owned effects | SPEC-008 CON-986 routes compensation through SPEC-004 CON-204 and forbids a change to operator withdrawal state. |

The boundary holds.
[[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-1 Two hub authority-command arguments carry no size bound]]
does not breach it. That finding concerns capacity on the final authority
command and creates no early identity effect.

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact application-relay TOFU resists a new application; operatorship residual honest | **YES.** CON-903 keys on the exact pair, REQ-1006 requires a fresh prompt for a new application on an accepted relay, and ADR-902 states the residual plainly. |
| 2 CON-220 steps 1 to 5 authenticate only the origin candidate; CPace binds the same digest first | **YES.** CON-988 returns an origin-recognised candidate, denies it verifier, display, policy, and key authority, and requires both Finished values before the durable row, the display, or profile-key use. |
| 3 no peer, carrier, DOM, verifier, route, or caller value reaches a consent screen early | **YES.** cbcl-pairing CON-029 builds the display only from the authority input after byte equality, and the verifier returns a verdict only. The former unverifiable member now carries its own signed grammar and wallet checks. |
| 4 every effect follows its exact consent boundary | **YES.** REQ-902, CON-902, CON-986, SPEC-007 REQ-812 and CON-807, cbcl-bus REQ-082, and TEST-1158 state one ordering with no gap. |
| 5 hub fresh scope breaks the enrolment cycle without leaking the account identifier | **YES.** REQ-082 allocates inside the authenticated transaction, the principal commits a 32-octet CSPRNG preimage, and `OfferCoreV2` carries no raw account identifier. |
| 6 one carrier ceremony identifier throughout | **YES.** The value reaches the carrier, the CPace context, the public context, every body, every command, every response, the checkpoint salt and fifth member, the receipt, the recovery request, the final status, and the installed record. |
| 7 two implementations reproduce context, transcript, schedule, Finished, nonces, AAD, hashes, predecessors, kinds, checkpoint member, and wrapping key | **YES.** CON-031 states the thirteen-member context and the complete schedule. CON-028 assigns all eleven kind integers. CON-030 names the fifth checkpoint member. SPEC-008 CON-986 states the claimant wrapping key with explicit length prefixes and an explicit salt. |
| 8 eleven body grammars exist; 50,221 recomputes; padding and frames agree | **YES.** The reviewer recomputed the payload body maximum as 1 + 201 + 50,019 = 50,221 octets. Comparison-confirmed recomputes to 1,583 and binding-confirmed to 1,570, both under the 2,048-octet control limit. |
| 9 the descriptor digest binds one uniquely selected six-member descriptor; v1 and v2 stay disjoint | **YES.** CON-806 fixes six members and unique origins, CON-035 selects by exact origin equality, and REQ-1006 states the disjoint registry, contract, test, and comment dispositions. |
| 10 hub authority response self-contained, offer-key signed, ceremony bound, byte-preserved, wallet verified | **YES.** cbcl-bus CON-012 states the six-member array, the offer `kid` and key, and the browser's byte-preservation duty. SPEC-008 CON-987 states the wallet's digest, signature, ceremony, offer-digest, status, DID, and preview checks before either confirmation. |
| 11 one pending deadline; boundaries, sampling, retries, and recovery total | **YES.** REQ-082 removes the second clock, CON-012 makes the interval exclusive with no skew allowance, and CON-986 states every wallet refresh point. One clock-anomaly residual survives as [[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-8 A backwards hub clock step yields a non-conforming offer core]]. |
| 12 refusal after durable payload; transient busy distinguished from durable terminal | **YES.** cbcl-pairing CON-028, CON-030, and CON-032 make a post-payload refusal invalid input. cbcl-bus CON-012 separates the two transient classes from the four durable terminal classes and forbids a second identity effect. |
| 13 migration digests and staging receipt exact, bounded, recomputable, free of caller booleans | **PARTLY.** Every migration preimage carries a domain string, a stated order, and a length prefix where needed. The staging receipt computes to 3,049 octets against 4,608. One signed member has no stated meaning; see [[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-3 One signed staging-receipt member has no stated meaning]]. |
| 14 retries, rebinding, checkpoints, status, and crash boundaries free of duplicate effects | **YES.** REQ-082 rebinds only the socket-generation digest, advances no phase, and permits a fresh allocation only after authenticated absent status. REQ-1006 forbids the local re-grant shortcut. |
| 15 recovery cannot cause false installation or unsafe pending deletion | **YES for false installation.** Only verified accepted status installs. A signed negative needs the hub's locked terminal-absence predicate, and rotation needs explicit consent. Deliberate service data erasure of an accepted status permits one signed negative for a ceremony that did finalize; the wallet then fails closed with no capability. |
| 16 the nine declared bounds and the CBCL, WebSocket, and HTTP parsers agree | **NO.** The nine declared bounds agree arithmetically. Large object 64,560 plaintext plus a 16-octet tag fits 69,632 and 70,000. Offer 56,625, payload 50,221, and receipt 8,373 fit 62,000. Two authority-command arguments carry no bound at all; see [[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-1 Two hub authority-command arguments carry no size bound]]. |
| 17 activation cannot precede immutable signed acceptance | **YES.** CON-986, SPEC-007 CON-807, and cbcl-bus REQ-082 order inactive staging, one final transaction, JWS verification under the offer `kid`, activation, receipt, and installation. |
| 18 hub authentication, locks, capacity, expiry, migration, tombstone, response grammars, limiter, retention, staging total | **PARTLY.** All seven commands now carry a success response and one closed refusal. Both lock orders, the phase projection, the terminal classes, the limiter, and the retention rule are stated. Two argument bounds are absent; see N-1. |
| 19 did-crdt rendezvous absent; the sole HTTPS exception is signed status recovery | **YES.** CON-986, GATE-04, cbcl-bus TEST-117, SPEC-008 TEST-1159, and did-crdt SPEC-037 0.1.2 agree. The candidate diff touches no rendezvous code. |
| 20 credential/v1 bytes and production behavior frozen | **YES.** REQ-031, TEST-063, and REQ-1005 require byte-identical version-1 output and no version-2 reach into a classified version-1 site. REQ-1006 keeps the standing registry and selection control compiled and unchanged. |

## Repository and code-claim assessment

Every testable code claim resolved exactly at its stated baseline. This remains
the strongest part of the reissue.

**Selfsame version sites.** All eighteen witness rows in SPEC-008 REQ-1005
resolve at `0220cec2` with the stated class.
`crates/selfsame-web-device/src/lib.rs:600` reads
`selfsame_app_identity::PROFILE_VERSION`. Lines `:1346` and `:1360` read
`profile_version: 1`, and `:1369` reads `profile_version: core.profile_version`.
`crates/selfsame-app-identity/src/ceremony.rs:185` and `:190` emit members,
`:308` and `:510` compare, and `:460` emits.
`crates/selfsame-app-identity/src/enrollment.rs:373`, `:394`, and `:552`
compare, `:452` assigns, and `:662` and `:666` emit.
`crates/selfsame-app-identity/src/profile.rs:385` compares, and
`crates/selfsame-app-identity/src/provider_hint.rs:67` emits while `:115`
compares.

The additional evidence rows also resolve.
`crates/selfsame-app-identity/src/enrollment.rs:592` compares.
`crates/selfsame-web-device/src/lib.rs:831`, `:847`, and `:861` substitute on
ingest. `crates/selfsame-app-identity/src/ceremony.rs:413`,
`crates/selfsame-app-identity/src/provider_hint.rs:96`, and
`crates/selfsame-app-identity/src/profile.rs:384` convert untyped ingress.

**The recorded duplication is real.**
`crates/selfsame-app-identity/src/lib.rs:109` and
`crates/selfsame-app-identity/src/profile.rs:60` each declare the same public
constant. A whole-crate scan finds exactly two definitions and six ordinary
uses.

**cbcl-bus consumer sites.** Every line SPEC-008 NFR-928 names resolves with
the stated consumer. `cbcl-chat-session-ws.lfe:512` consumes `device-grants`,
`:869` consumes `rooms-for-account`, `:965` consumes `list-public`, and `:1683`
consumes `grants`. `cbcl-chat-room.lfe:677` consumes `peer-grants`, and `:221`
and `:550` consume `get-dialects`. `cbcl-chat-path-b-poller.lfe:17` consumes
`accounts` and `:32` consumes `revoke-observed`.

The boolean and webhook rows resolve too. `cbcl-chat-room.lfe:263` to `:270`
carry the boolean contexts, `cbcl-chat-pairgrant.lfe:78` negates
`member-grant?`, and `cbcl-chat-session-ws.lfe:1050` tests it.
`cbcl-chat-rooms.lfe:54`, `:60`, and `:61` carry the room-configuration calls,
`:49` carries the ingest edge, and `:99` to `:104` carry `start-room`.
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76` carries the HTTP edge.
`cbcl-chat-session-ws.lfe:1076` and `:1788` carry both `get-or-start`
consumers, and `:1898` discards the `claim/5` result.

**The named repairs match real defects.** `cbcl-chat-successor.lfe:109` and
`:134` each wrap a call in `catch`, and `:134` opens a second transaction
through `device-grant/2`. Both sit inside `accept/3`'s transaction at `:49`, so
the body both nests and catches. NFR-928 forbids exactly those two edges.

`cbcl-chat-rooms.lfe:57` matches the unknown-room error before claiming the
public default, and `:66` catches every other error in one wildcard. NFR-928
splits that wildcard into the two named unavailability classes.

**The WebFinger owners match.** `cbcl-chat-webfinger-store.lfe` exports
`provision/3` at `:124`, `withdraw/2` at `:199`, `reinstate/2` at `:295`, and
`lookup/1` at `:314`. The current `tombstone/2` reads the account row first at
`:207` and the alias row second at `:233`. NFR-928 requires an arity-3
`tombstone/3` with the reverse lock order, which is a stated change rather than
a misdescription. `lookup/1` already returns `unavailable`, matching the stated
exemption.

**The CON-220 substrate exists.**
`crates/selfsame-app-identity-net/src/profile.rs:64` to `:98` implement steps 1
through 5 and state in comment that step 6 belongs to the caller.
`crates/selfsame-app-identity/src/discovery.rs:48` sets `MAX_BODY_OCTETS` to
65,536, matching CON-988's restatement.

**The relay control exists.**
`crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93` define descriptor
eligibility, and `:90` requires the approved-conformance membership that
CON-903 replaces for credential/v2. `src-tauri/src/cbcl_registry.rs:17` to
`:30` declare one ratified entry, and `:35` to `:41` return the ordinary-build
policy.

**The upstream bounds exist.** cbcl-pairing `src/mailbox.rs:15` sets the frame
body cap to 69,632 octets, and `:13` sets the per-membership frame cap to 16.
`src/bin/cbcl-pairing-relay-ws.rs:32` and `src/bin/cbcl-pairing-relay.rs:31`
both set the wire message cap to 70,000. `src/wire.rs:14` declares the suite
identifier that both version-2 contexts name. `Cargo.toml` pins every
cryptographic dependency to an exact version.

**Version isolation holds in the pinned assets.** `schemas/pairing-v1.cddl:12`
and `:13` keep the version-1 identifier and origin at 96 and 255 octets.
`src/context.rs:7` to `:9` keep the three version-1 domains, and every
version-2 domain differs.

**did-crdt stays untouched.** The complete diff from `1f409a42` to `6d1cdbeb`
adds only `specs/SPEC-037-pairing-rendezvous-body-bound.md`. The deprecation's
claim about `src/service/rendezvous.rs` is exact, and commit `829855c2` exists.
The vault numbers per document, so SPEC-036's own requirement at line 368 does
not conflict with SPEC-037's burn statement.

## Consent assessment

Both consent screens draw only from authenticated sources. cbcl-pairing CON-029
copies every displayed value from the authority input and none from the peer
input, and the injected verifier returns a closed verdict.

Every overlapping peer value requires byte equality before display allocation.
The peer intent digest must bind the authority's signed offer-core digest.

The preliminary screen shows the authenticated application identifier, HTTPS
origin, relay origin, account provenance, permissions, installation binding,
exact-pair state, and the complete transition. The final screen adds only the
locally derived DID, the fingerprint, and the effect statement.

The exact-pair prompt precedes the socket and names only the origin-recognised
candidate. The durable policy row follows both Finished values and the explicit
digest equality check.

Preliminary approval authorizes one zeroizing custody call, one pure DID and
fingerprint computation, and one disclosure to the bound application. Every
fresh ceremony draws fresh account and scope randomness, so a repeated declined
ceremony discloses no linkable value.

Final approval authorizes the nine declared causal steps in one order. Every
parent states the same order.

## Wire and bounds assessment

The reviewer recomputed every stated maximum from the grammars alone.

| Object | Recomputed maximum | Declared bound | Agrees |
|---|---:|---:|---|
| payload body | 50,221 | 62,000 | yes |
| comparison-confirmed body | 1,583 | 2,048 | yes |
| binding-confirmed body | 1,570 | 2,048 | yes |
| receipt body | 8,373 | 62,000 | yes |
| signed offer | 56,625 | 56,640 | yes |
| authority response | 701 | 768 | yes |
| staging receipt | 3,049 | 4,608 | yes |
| recovery request | 2,187 | 2,304 | yes |
| largest recovery response | 8,294 | 9,216 | yes |
| large object plus tag | 64,576 | 69,632 and 70,000 | yes |
| control object plus tag | 4,160 | 69,632 and 70,000 | yes |

The eleven kind integers partition correctly. The control arm carries 1 through
8 and the large arm carries 0, 9, and 10, matching the three large kinds.

The thirteen-member public context and the six-member descriptor count exactly
as declared. The twenty offer-core members, the fifteen final-status members,
and the nine Selfsame successor grammars also count exactly. The room-array cap
recomputes as 256 times 131, plus 255 commas and two brackets, giving 33,793.

The 272-octet version-2 relay origin agrees across cbcl-pairing CON-027,
SPEC-007 CON-806, and cbcl-bus CON-012. The unchanged 255-octet version-1 rule
survives in the pinned schema.

The transport parsers agree with the object bounds. The hub session socket sets
only an idle timeout, so a 75,520-character base64url signed offer transits
without a frame conflict.

Two hub command arguments break the otherwise total bound coverage; see
[[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-1 Two hub authority-command arguments carry no size bound]].

## Cryptography assessment

The credential/v2 schedule is complete and version-isolated. The transcript
hash is SHA-512 over deterministic CBOR of the public context and both CPace
frames. The extract uses an empty salt, and the seven expands carry distinct
labels and stated output lengths.

Both Finished values are 64-octet HMAC-SHA-512 outputs verified in constant
time before application traffic. The nonce is the direction initialization
vector combined with a 96-bit big-endian counter, and the AAD is one closed
four-member map.

Every version-2 label differs from its version-1 counterpart at
`src/channel.rs:118` to `:146`, so no version-1 value authenticates a version-2
frame.

The object content hash covers fields 0 through 4 with the selected padding,
and the kind integers now enter that preimage deterministically.

The recovery construction is injective. The token is an HMAC-SHA-256 over a
domain string and the fixed-length transcript hash. The commitment places the
variable-length application identifier last, after two fixed 32-octet values.

The claimant checkpoint wrapping key states an explicit salt, an explicit
keying input, and length-prefixed info. The 64-octet hierarchy root matches
`hierarchy.rs:101`, and the HKDF-SHA512 primitive matches `hierarchy.rs:332`.

Key reuse across the profile signing key is domain-separated three ways. The
offer signs a null-terminated domain prefix, the two JWS objects carry distinct
`typ` values, and every payload carries a distinct `role` literal.

One phrasing residual survives; see
[[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-5 Two signature clauses name their input less exactly than the third]].

## Recovery and atomicity assessment

The wallet writes one sealed pending value before its first identity effect.
The slot is a tagged pending-or-installed union rather than two records.

Only a verified ordinary receipt or a verified recovered receipt replaces that
value. Only a verified signed negative, an explicit unlink, or a root purge
clears it after a durable payload send.

The hub commits all identity effects in one level-1 transaction or none. A
crash resolves to absent, the exact pending record, or the complete immutable
state.

The browser stages one inactive record before requesting finalization and
activates it only after authenticating the immutable status. The staged record
grants no capability.

Transient and durable hub terminals stay separate. The two transient classes
preserve a valid pending record, while the four durable classes identify
terminal state.

The signed negative requires the hub's locked terminal-absence predicate, so
the negative outcome is monotone and a replay stays harmless.

## Persistence assessment

The relay retains at most one 32-octet commitment per waiting version-2 mailbox
and never retains the claim token. The store record carries a closed admission
union, and a missing version-2 admission field never becomes version 1.

The hub pending record encodes to at most 69,632 octets and holds only bounded
values. The account record stays absent until final acceptance.

Checkpoints stay sealed under consumer-supplied wrapping keys, carry no
consumer key or derived key, and erase both presence secrets on schedule.

The wallet's installed record names every field it must reverify on reload.
Unavailability stays distinct from revocation and absence and never clears the
record.

One checkpoint expiry residual survives; see
[[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-9 The post-payload checkpoint expiry rule names only one role]].

## Rendezvous assessment

No credential/v2 edge reads or writes a rendezvous slot. SPEC-008 CON-986
forbids every Selfsame and did-crdt rendezvous read, write, route, and client
edge on that path.

The sole direct HTTPS exception is CON-989's signed post-window status
recovery, addressed to the application origin retained from CON-988. SPEC-008
TEST-1159 and cbcl-bus TEST-117 both permit that one route and fail every
other.

GATE-04 carries the matching box, and did-crdt SPEC-037 0.1.2 states the
disposition. The candidate diff touches no rendezvous source.

## Compatibility assessment

Credential/v1 bytes stay frozen. The version-1 carrier, schedule, display,
decisions, and wire rules remain unchanged, and cbcl-pairing REQ-031 forbids
either version from relabelling the other.

The typed invitation split prevents version selection by generic bytes,
application text, or a caller version. The version-1 grammar cannot recognise a
version-2 carrier.

The standing registry and selection control remain compiled for credential/v1,
so the deployed enrolment path keeps its origin gate. Only the comment changes.

The production scan closes over five identifiers and seven classes, and every
named witness resolves. The scan, not the witness list, remains the
completeness authority.

## Authority assessment

Each parent states its own current law directly, and all four name the same
coordinated review set and the same generation session.

Every consolidated identifier is defined exactly once outside the trajectory
directory. The trajectory copies carry no normative precedence.

The burned identifiers REQ-909, TEST-916, and TEST-917 appear as no heading in
any current Selfsame parent.

Every wikilink anchor in SPEC-008 and SPEC-007 is fully qualified, and no
unqualified fragment link exists. Every internal anchor resolves to a heading
in the same parent.

The cross-vault identifiers stay unlinked by declared intent, and the anuna-ssi
pin resolves namespaces only.

One traceability edge is missing; see
[[spec-008-0.5.4-claude-adversarial-review-2026-08-24#N-2 The logical-body contract has no traceability edge]].

## Stopping-rule assessment

The parent states one stopping rule and honors it. No implementation,
allocation, release, or deployment follows before one coordinated fresh Tier-1
PASS.

GATE-04 carries exactly 42 boxes, and the reviewer counted them. Every box is
open, and the reissue closes none.

SPEC-007 REQ-809 stays untouched, and TEST-911 keeps production allocation
disabled and fail-closed.

The gate evidence record names this attempt's review as unverified and assigns
it to the Selfsame security owner.

## Surfaces found sound

- The exact-pair TOFU rule, its prompt owner, and its post-Finished write
  ordering.
- The pre-socket origin recognition and its explicit non-authority.
- The CPace substitute for CON-220 step 6, including its seven ordered steps.
- The complete credential/v2 key schedule, transcript, AAD, and content hash.
- The eleven kind integers and their arm partition.
- The nine Selfsame logical-body grammars and every recomputed maximum.
- The signed transferable authority response and its wallet verification.
- The single exclusive pending deadline and its sampling points.
- The two-stage consent boundary and the nine-step executor order.
- The post-payload refusal prohibition and the retained receipt state.
- The signed post-window status recovery and its monotone negative.
- The hub lock orders, phase projection, and closed response set.
- The busy and unavailable contracts across every named consumer.
- Every code claim in all four parents, at every stated line.

## Residual assumptions

- Explicit service data erasure of an accepted status permits one signed
  negative for a ceremony that did finalize. The wallet then fails closed with
  no capability, and the hub retains an orphaned account.
- Immutable accepted statuses count inside the same 65,536-record cap that
  gates allocation, so lifetime successful pairings share that budget with
  concurrent pending records.
- The relay mailbox expiry and the offer deadline coincide in the common case.
  A ceremony completing near that deadline therefore resolves through CON-989
  rather than through the ordinary relay receipt.
- The application profile does not prove relay operatorship, and exact-pair
  consent contains that residual without solving it.
- The browser's comparison assertion stays a browser-side claim. The wallet's
  own final screen and its local DID remain the wallet's protection.
- Two depth tests stay open with named owners, covering a real relay ceremony
  and two independently operated relays.

## What this verdict authorizes and does not authorize

This verdict authorizes nothing. It records one blocking correction and eight
non-blocking observations for the next coordinated reissue.

It does not authorize the Elephant plan, a test-first plan, implementation,
production allocation, release, or deployment.

It closes no GATE-04 box and changes no production gate.

## Collected corrections and owners

| Finding | Severity | Owner | Correction |
|---|---|---|---|
| N-1 | MEDIUM | cbcl-bus | State decode bounds for `rawGrant` and `resolverClosure` in CON-012, with the matching refusal before lookup or mutation. |
| N-2 | LOW | selfsame | Give CON-987 an `Implements:` line, a `Verified by:` line, and one inbound trace edge. |
| N-3 | LOW | cbcl-bus | State the staging generation's preimage, generator, and consumer, or remove the member. |
| N-4 | LOW | selfsame | Rewrite CON-986's prohibition sentence so the credential/v2 scope needs no attachment judgement. |
| N-5 | LOW | cbcl-bus | Restate both signature inputs in the explicit form CON-012 already uses for the offer. |
| N-6 | LOW | cbcl-bus | Split the one 27-word CON-012 sentence. |
| N-7 | LOW | cbcl-bus | Record that the bound outcome stays a fail-safe for a colliding or restored alias row in this increment. |
| N-8 | LOW | cbcl-bus | Name the terminal for a backwards hub clock step between allocation and offer preparation. |
| N-9 | LOW | cbcl-pairing | State whether the allocator checkpoint may use the post-payload null expiry, and which retention rule then applies. |
