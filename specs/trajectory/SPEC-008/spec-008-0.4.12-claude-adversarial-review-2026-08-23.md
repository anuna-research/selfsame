---
id: SPEC-008-review-0.4.12-claude-2026-08-23
title: Tier-1 adversarial review — SPEC-008 0.4.12 coordinated standalone pairing
status: complete
tier: 1
kind: adversarial-review
verdict: REJECT
reviewed-candidates: "selfsame e2d5eff3d4a9c7256a97e28b807c22e3024634e2; cbcl-pairing 4babf001ac0bad19b25e83f4a64189720d86da28; cbcl-bus bb807d7183bb717966e4564b2a354059246460cb; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
code-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
severity-counts: "0 critical, 1 high, 2 medium, 5 low"
last-updated: 2026-08-23
---

# Tier-1 adversarial review — SPEC-008 0.4.12 coordinated standalone pairing

## Model, driver and authentication context

| Field | Value |
|---|---|
| Model | Claude Opus 5 (`claude-opus-5`) |
| Driver | Claude Code TUI, Circus worktree `circus/spec008-v0412-review/1` |
| Authentication | Claude Max subscription (OAuth). `ANTHROPIC_API_KEY` absent from the process environment |
| Paid/API calls | None. No external paid API, no account-billed Anthropic API call, no web access |
| Context | Fresh. No prior turn of this conversation preceded the review prompt |
| Working directory | `/Users/anuna-01/Code/selfsame-spec008-v0412-review-1` (isolated worktree) |
| Sibling repositories read | `/Users/anuna-01/Code/cbcl-pairing`, `/Users/anuna-01/Code/cbcl-bus`, `/Users/anuna-01/Code/did-crdt` |
| OTP source read | `/opt/homebrew/Cellar/erlang/28.5/lib/erlang/lib/mnesia-4.25.3/src/{mnesia.erl,mnesia_locker.erl}` (OTP 28.5, Mnesia 4.25.3) |

The reviewer's mandate was defect-finding, not drafting. No implementation code,
candidate spec, test, manifest, configuration, or sibling repository was edited.
This review file is the only change.

### Reviewed commits, verified

```text
selfsame       HEAD = e2d5eff3d4a9c7256a97e28b807c22e3024634e2   (matches candidate)
cbcl-pairing   HEAD = 4babf001ac0bad19b25e83f4a64189720d86da28   (matches candidate)
cbcl-bus       HEAD = bb807d7183bb717966e4564b2a354059246460cb   (matches candidate)
did-crdt       HEAD = 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1   (matches candidate = baseline)
```

Every declared implementation baseline was checked for drift rather than
assumed:

```text
git diff --name-only c6af6df..e2d5eff   ->  23 files, ALL under specs/trajectory/SPEC-008/
git diff --name-only 8e55160..4babf00   ->   6 files, ALL under specs/trajectory/SPEC-001/
git diff --name-only fdbe414..bb807d7   ->   6 files, ALL under specs/ (SPEC-053 parent + trajectory)
did-crdt candidate == did-crdt baseline
```

No product code changed in any repository between the declared baseline and the
candidate. Every code claim below is therefore a claim about the baselines.
did-crdt's two dirty files (`specs/did-crdt-spec.md`, an untracked ADR-007) are
unrelated to this review unit and were ignored per the mandate.

### Documents read to determine effective text

Selfsame SPEC-008 0.4.0 through 0.4.12 and every adversarial review 0.4.1
through 0.4.11; `SPEC-008-production-pairing-claimant`; `SPEC-007-cbcl-pairing-cutover`;
`SPEC-004-application-scoped-identity`; `PROTO-003`/`PROTO-004`;
`pairing-completion-runbook`; `req-909-adversarial-review-2026-08-22`.
cbcl-pairing SPEC-001 0.3.0 through 0.4.4. cbcl-bus SPEC-053 0.16.1 through
0.16.5, `SPEC-053-key-root-identity`, `SPEC-076-pairing-account-grant`.
Code as cited per finding.

---

# Verdict: REJECT

**0 critical · 1 high · 2 medium · 5 low.**

The standalone architecture remains sound and the amendment is a real
improvement: it closes 0.4.11's `H-1` (the tagged nested transition is now the
single digest preimage, and `accountDid` — which the hub could not have known at
offer-construction time — is correctly gone from the core), `H-2` (the
claim-first outcome now matches `cbcl-core-enrol` exactly), `M-2` (cbcl-bus
0.16.5 restates all thirty-nine mandatory GATE-04 boxes), `L-1`, `L-3` and `L-4`
completely, and most of `M-1` and `M-3`.

It fails on one thing, and it is the thing `REQ-985` is named after.

`REQ-985` — "Credential/v2 bounds have one reproducible canonical fixed point" —
publishes eight exact figures and four exact residuals, makes them the
generator's acceptance criterion, and mirrors them into cbcl-bus `NFR-012`,
`TEST-1113`, `TEST-072` and `GATE-04`'s "every NFR-012 maximum and residual
reproduces in all three lanes" box. In the same paragraph it says "Existing URI,
DID, identifier, timestamp, and key grammars also apply." Two of those grammars
are fixed-length in the code and are **shorter than the caps the arithmetic
uses**: `deviceDid` is a `did:key` Ed25519 identifier of exactly **56** octets
against a declared `V2_MAX_DID_OCTETS = 512`, and the one accepted
`dateTimeStamp` spelling is exactly **20** octets against a declared
`V2_MAX_TIMESTAMP_OCTETS = 32`. Nine of the twelve published figures are
therefore unreachable by any grammar-legal value, and the mandatory equality
assertions cannot pass. That is `H-1`, and on its own it blocks.

I reproduced every figure independently before finding this — the arithmetic
*from the declared caps* is correct to the octet, which is why the defect is in
the inputs rather than the sums. The correction is bounded and clerical: declare
the two caps at their grammar-forced values (or restate the figures as
recognizer ceilings and relax the test's equalities to "at most, actual
recorded"), then re-derive. No architecture moves.

Nothing in this review reopens `F-A` through `F-E`, the consent order, the
custody boundary, or `SPEC-007 REQ-812`. All five original findings hold, and
0.4.11's one open caveat on `F-B` is now cleared.

---

# Findings

## H-1 (HIGH, release-blocking) — `REQ-985`'s "one reproducible canonical fixed point" is not reachable: `V2_MAX_DID_OCTETS = 512` and `V2_MAX_TIMESTAMP_OCTETS = 32` exceed the code-fixed lengths of `deviceDid` (exactly 56) and of the sole accepted `dateTimeStamp` spelling (exactly 20), so nine of the twelve published figures cannot be constructed from any legal value and `TEST-1113`/`TEST-072`'s equality assertions and `GATE-04`'s reproduction box cannot be satisfied

**Owner: Selfsame `REQ-985`/`CON-971`/`TEST-1113`**, coordinated into **cbcl-bus
`NFR-012`/`CON-023`/`TEST-072`/`GATE-04`**.

### What the candidate says

`REQ-985`:

> Every textual cap below limits both UTF-8 content and RFC-8785 escaped string
> content. A value SHALL satisfy both limits before cloning. **Existing URI, DID,
> identifier, timestamp, and key grammars also apply.**
>
> ```text
> V2_MAX_TIMESTAMP_OCTETS   = 32
> V2_MAX_DID_OCTETS         = 512
> ```
>
> The complete 21-member statement has maximum 2,560 RFC-8785 octets. […]
> The 23 top-level core member contributions total 3,814 non-room octets. […]
> **The generator constructs the maximum statement, header, hint, transition,
> core, offer, and CBCL member. It records every actual result and residual.**

`TEST-1113` makes it an equality, not a bound:

> Construct every maximum. **Require statement 2,560, header 187, compact JWS
> 3,752, JWK 93, hint 507, non-room core 3,838, core 51,838, full offer 56,250,
> and every stated residual.**

cbcl-bus `NFR-012` repeats the whole table verbatim and `TEST-072` repeats the
requirement ("Require every exact result and residual"). `GATE-04` box 15 is
"every NFR-012 maximum and residual reproduces in all three lanes".

### What reproduces exactly (recorded so the repair does not disturb it)

I rebuilt every level from the declared constants, RFC-8785 (no whitespace,
minimal escaping, member names sorted — sorting does not change octet counts),
before checking reachability. **Every sum in `REQ-985` is arithmetically
correct.** Per-member `"name":value` octets for the 23-member core:

```text
payloadVersion            18   accountPrincipalDigest    70   platformBindingId 182
role                      14   applicationAccountDisplay 542  returnUri         174
ceremonyId                58   accountScopeId            62   issuedAt           45
requestId                 57   deviceDid                526   expiresAt          46
applicationId            114   deviceKeyJwk             108   accountTransition 594
profileVersion            18   requestedPermissions     548        (non-room part)
profileDigest             61   intentNonce               59
descriptorDigest          64   relayOrigin              288
carrierDigest             61   transcriptHash           105
                                              members  3,814   <- REQ-985 exact
                       + 2 braces + 22 commas             24
                       ---------------------------------------
                       non-room core                  3,838   <- REQ-985 exact
                       + complete migrationRooms array 48,000
                       ---------------------------------------
                       complete core                 51,838   <- REQ-985 exact
```

The nested `accountTransition` non-room part, independently:

```text
"kind":"path-a-to-b"          20   "migrationSnapshotDigest":".."  71
"legacyHandle":"<256>"       273   "roomSetDigest":".."            61
"legacyKeyDigest":"<43>"      63   "snapshotNonce":"<43>"          61
"migrationRooms":             17   + 2 braces + 6 commas            8
                                                     ----------------
                                   transition object              574
                                   + "accountTransition":          20
                                                                  594
```

The statement, header and JWS chain:

```text
21 statement members (0.4.11's exact 2,282 with display at 286, plus 256
for the restored 512-octet display cap)              = 2,538
+ 2 braces + 20 commas                               =    22
                                                       -------
statement payload                                       2,560   <- exact
protected header {"alg":"EdDSA","kid":"<128>","typ":"selfsame-credential-v2+jws"}
  1 + 13 + 1 + 136 + 1 + 34 + 1                      =   187    <- exact
b64url-no-pad(187)=250 · b64url-no-pad(2560)=3,414 · b64url-no-pad(64)=86 · 2 seps
                                                       = 3,752  <- exact
policy 4,000 - 3,752                                   =   248  <- exact
```

The seven-member `CON-945` hint at `applicationId` 96 and `providerId` 63 is
`114+18+78+64+59+61+105 = 499`, `+2 braces +6 commas = 507` — exact. The
four-member `DEVICE_JWK_MEMBERS` object (`ceremony.rs:278`, OKP/Ed25519/EdDSA) is
`{"alg":"EdDSA","crv":"Ed25519","kty":"OKP","x":"<43>"}` = `1+13+1+15+1+11+1+49+1`
= **93** — exact, and the 0.4.11 `M-1(a)` 93-versus-96 split is genuinely gone.
The complete offer:

```text
"devicePossessionProof":"<86>"   112   "providerHint":{<507>}    522
"enrollmentEvidence":"<3752>"  3,775   + 3 separating commas       3
                                                        ----------
                                 complete addition           4,412  <- exact
                                 + complete core            51,838
                                                        ----------
                                 complete offer             56,250  <- exact
```

Residuals `52,000-51,838 = 162`, `57,000-56,250 = 750`, `62,000-56,250 = 5,750` —
all exact. **The auxiliary budget is gone and `deviceKeyJwk` is charged once**
(0.4.11 `M-1(b)` closed). `CON-945` is cited as the hint's member source
(`M-1(c)` closed). Both units are declared for every textual cap (`M-1(d)`
closed). The `migrationRooms` unit is now one thing — "the complete RFC-8785
array, including brackets, commas, quotes, and escaping" (`M-1(e)` closed for the
unit).

I also verified the 512/512 display conjunction is *exactly* tight, which is
what makes `L-4` genuinely closed rather than differently broken. With
1..128 NFC scalars and C0/C1 excluded, RFC-8785 escapes only `"` and `\`
(one octet each, expanding to two). For `k` escapable one-octet scalars and
`128-k` four-octet scalars: UTF-8 `= 512-3k ≤ 512` and JCS `= 512-2k ≤ 512`.
Both caps hold for every legal string, with equality at `k = 0`. No legal
128-scalar display is excluded, in either unit.

### What does not reproduce

**(a) `deviceDid` is exactly 56 octets and can never be 512.** The offer core's
`deviceDid` is not a free string. `recognise_offer` requires it to encode the
same key as `deviceKeyJwk`:

```rust
// crates/selfsame-app-identity/src/ceremony.rs:330,373-376
let device_did = text("deviceDid")?;
…
crate::didkey::matches_jwk(&device_did, &device_public_key)
    .map_err(|_| bad("deviceDid", "does not encode deviceKeyJwk"))?;
```

and `didkey` admits exactly one shape (`didkey.rs:16-21,76-98`):

```text
did:key:z<base58btc(0xED 0x01 || public_key_32)>
```

`decode` refuses unless the multibase prefix is `z`, the multicodec prefix is
`0xED 0x01`, and `octets.len() == 34` (`didkey.rs:93-98`). base58btc of 34 octets
whose leading byte is `0xED` is always 47 characters; I encoded 200 random
Ed25519 keys through the module's own algorithm and every result was **56**
octets total. So:

```text
declared   "deviceDid":"<512>"  = 526 octets
reachable  "deviceDid":"<56>"   =  70 octets      difference: 456
```

**(b) A `dateTimeStamp` is exactly 20 octets and can never be 32.**

```rust
// crates/selfsame-app-identity/src/time.rs:53,56-59
pub const STAMP_CHARS: usize = 20;
pub fn parse_date_time_stamp(text: &str) -> Result<UnixSeconds, TimeError> {
    let b = text.as_bytes();
    if b.len() != STAMP_CHARS { return Err(TimeError::Malformed); }
```

Not a maximum — an equality, with the punctuation positions fixed at indices
4, 7, 10, 13, 16, 19 and every other position an ASCII digit. `format_date_time_stamp`
(`time.rs:98-108`) emits the same twenty. `recognise_unsigned` additionally
requires `canonicalise(build(&statement)) == statement_octets`
(`enrollment.rs:317`), so a statement whose timestamps did not round-trip through
that formatter is refused before a private key is constructed. So:

```text
declared   "issuedAt":"<32>"  = 45      reachable "issuedAt":"<20>"  = 33
declared   "expiresAt":"<32>" = 46      reachable "expiresAt":"<20>" = 34
                                        difference: 12 + 12 = 24
```

**(c) `legacyHandle` has a declared 256-octet cap and no declared grammar,** so
its 273-octet contribution cannot be checked in either direction. The deployed
handle grammar the browser enforces is `/^@[a-z0-9_-]{1,32}$/`
(`cbcl-bus apps/cbcl_chat/priv/web/app.js:1074`), i.e. at most 33 octets; the
authoritative grammar is in `cbcl_erl:parse_message`, which is not a declared
baseline. `REQ-985`'s "Existing URI, DID, identifier, timestamp, and key
grammars also apply" names no handle grammar at all. (The `migrationRooms`
element is the same class and is filed separately as `M-2`, because its
correction differs.)

### The corrected figures

Applying only (a) and (b) — the two that are fixed by code the review can read,
and that hold for **any** v2 application, not merely `chat.anuna.io`:

```text
                              REQ-985 / NFR-012      grammar-reachable
statement payload                        2,560                  2,536
protected header                           187                    187   (unchanged)
compact JWS                              3,752                  3,720
compact-JWS policy residual                248                    280
seven-member provider hint                 507                    507   (unchanged)
four-member device JWK                      93                     93   (unchanged)
external device proof                       86                     86   (unchanged)
23-member non-room core                  3,838                  3,358
complete core                           51,838                 51,358
52,000-octet core residual                 162                    642
"enrollmentEvidence" member              3,775                  3,743
complete offer payload                  56,250                 55,738
57,000-octet payload residual              750                  1,262
62,000-octet CBCL member residual        5,750                  6,262
```

(`b64url-no-pad(2536) = ceil(20288/6) = 3,382`; `250 + 3,382 + 86 + 2 = 3,720`.)

### Why this is substantive, not editorial

1. **The requirement contradicts itself in one paragraph.** It declares the caps
   and then declares that the DID and timestamp grammars also apply. Both
   sentences are normative; they cannot both be satisfied by a value at the cap.

2. **The mandatory tests are equality assertions on unconstructible values.**
   `TEST-1113` says "Require statement 2,560 … non-room core 3,838 … full offer
   56,250, and every stated residual", and `TEST-072` says "Require every exact
   result and residual". A generator that obeys "Existing … DID … timestamp …
   grammars also apply" produces 2,536 / 3,358 / 55,738 and fails both.
   `GATE-04` box 15 — "every NFR-012 maximum and residual reproduces in all
   three lanes" — is therefore a box that cannot be closed, which is exactly the
   condition 0.4.11 `M-1` and `M-2` were filed under.

3. **It is not the safe direction of an over-declaration.** A conservative
   ceiling would be harmless if the text called it a ceiling. It calls it a
   constructed maximum and an "actual result", and cbcl-bus `NFR-012` calls the
   same table "The exact maximum results are". The generator gate — "It fails
   when any legal maximum exceeds its declared cap" (`CON-023`) — is a *ceiling*
   test and passes; the *equality* test is the one that breaks, and the two are
   in the same requirement.

4. **`REQ-985`'s own title and the 0.4.12 disposition table claim this is
   closed.** The disposition row reads "M-1 — bounds do not reproduce | REQ-985
   derives every level from one declared grammar." Two of the declared grammars
   are not the code's grammars, so the level does not derive.

5. **The residual arithmetic is what a reviewer of the next round would rely
   on.** 162 octets of core headroom is a number an implementer will treat as
   near-exhausted; the real figure is 642. A future member added on the strength
   of "162 left" would be sized against a fiction.

### Minimum sound correction

**Selfsame `REQ-985`** (with cbcl-bus `NFR-012` moving in lockstep). Either:

- **Declare the caps at their grammar-forced values** — replace
  `V2_MAX_DID_OCTETS = 512` with `V2_MAX_DEVICE_DID_OCTETS = 56` (citing
  `didkey.rs`'s single admitted `did:key:z…` shape and the `octets.len() == 34`
  check), replace `V2_MAX_TIMESTAMP_OCTETS = 32` with `= 20` (citing
  `time.rs:53` `STAMP_CHARS`), declare the `legacyHandle` grammar and cap it at
  its real bound, and re-derive all twelve figures and four residuals; **or**
- **Say the figures are recognizer ceilings, not constructed maxima** — state
  that each published number is the cap-derived ceiling, that the generator
  constructs the largest *grammar-legal* witness and records its actual value
  and the gap to the ceiling, and change `TEST-1113`/`TEST-072` from "Require
  statement 2,560 …" to "Require every constructed witness at most its published
  ceiling, and require the recorded actual and residual to match the published
  derivation."

`CON-971` must ledger whichever is chosen. If a separate `V2_MAX_DID_OCTETS`
remains for the wallet's `accountDid` in `REQ-972`'s confirmation preimage, name
it separately from the offer core's `deviceDid`, because they have different
grammars.

---

## M-1 (MEDIUM) — the v2 version-dispatch rule is scoped to *comparisons* against two constant spellings inside "app identity", which leaves `selfsame-web-device`'s hard-coded `selfsame_app_identity::PROFILE_VERSION` **substitution** — on the very browser path `CON-939` step 2 requires to check the hub's v2 core — outside both the rule and the surface enumeration

**Owner: Selfsame `REQ-984`/`TEST-1115`**, mirrored in **cbcl-bus `REQ-056`/`TEST-074`**.

### What the candidate says

`REQ-984`:

> **App identity** performs closed version dispatch before member recognition.
> The v2 call graph has these distinct surfaces:
>
> ```text
> profile-v2 recognizer
> provider-hint-v2 recognizer
> offer-v2 builder and recognizer
> credential-v2 statement recognizer, builder, and signer
> ```
>
> […] No v2-reachable **comparison** uses `crate::PROFILE_VERSION`,
> `profile::PROFILE_VERSION`, or a version-1 literal.

This is a real advance on 0.4.11: it names both constants (they are genuinely
two — `lib.rs:109` and `profile.rs:60`), it adds the profile, hint and offer
surfaces 0.4.11 `M-3` said were missing, and it covers the `payloadVersion ==
Some(1)` literal at `ceremony.rs:307`. I re-verified the four in-crate
comparison sites and they are exactly where `M-3` said:

```text
profile.rs:385         if version != PROFILE_VERSION  ("must be exactly 1 …")
enrollment.rs:394      != Some(crate::PROFILE_VERSION)
enrollment.rs:552      statement.profile_version != crate::PROFILE_VERSION
provider_hint.rs:115   self.profile_version != crate::PROFILE_VERSION
```

and confirmed `HINT_MEMBERS` is closed at five (`provider_hint.rs:54-59`) against
`CON-945`'s seven, so the v2 hint genuinely needs its own branch — which
`REQ-984` now enumerates.

### What the rule does not reach

There is a fifth site, in a second crate, and it is not a comparison:

```rust
// crates/selfsame-web-device/src/lib.rs:596-600
Ok(format!(
    r#"{{"applicationId":{},"accountAuthority":{},"profileVersion":{},"profileDigest":{},…"#,
    json_string(profile.application_id.as_str()),
    json_string(profile.account_authority.as_str()),
    selfsame_app_identity::PROFILE_VERSION,      // <- hard-coded 1, not profile's
    json_string(&selfsame_app_identity::codec::b64url(profile.digest())),
```

`profile_facts` **substitutes** the crate constant for the recognised document's
actual version. Three separate scoping choices in `REQ-984` each fail to reach
it:

- it is a **substitution**, not a "comparison";
- the spelling is `selfsame_app_identity::PROFILE_VERSION`, which is neither of
  the two named strings (from inside `selfsame-web-device`, `crate::` names a
  different crate);
- it is in `selfsame-web-device`, not "app identity", and it is not one of the
  four enumerated surfaces.

The site's own docstring names it as load-bearing for exactly the credential/v2
question: "The facts a joiner must compute BEFORE it can build an offer … `build_offer`
insists that every check compares against **the JOINER'S OWN profile**, never a
value the hint supplied." `CON-939` step 2 requires the browser to "check every
carrier, transcript, profile, descriptor, permission, platform, device-key,
ceremony, and request value it knows" against the hub-constructed `OfferCoreV2`,
and `profile_facts` is the only export in the tree that hands a browser its own
recognised `applicationId`/`profileVersion`/`profileDigest`/`descriptorDigest`/
`relayOrigin`. `apps/cbcl_chat/priv/web/selfsame/` is the vendored build of this
crate, and `selfsame-application-profile.mjs` states "The Selfsame WASM
recogniser remains the authority for CON-201 recognition" — so the browser's
CON-201 half is this crate, and `credential-v2-canonical.mjs` (`REQ-985`) is a
canonicaliser, not a profile recogniser.

**Honest scoping of the hazard.** The site is *not* v2-reachable today:
`ApplicationProfile::recognise` refuses `profileVersion != 1` at `profile.rs:385`,
so `profile_facts` can never see a v2 document at the baseline, and its only
current caller is the PROTO-003 mailbox harness `pairPathBOverMailbox`
(`app.js:1912`). It becomes reachable the moment `CON-951`'s per-branch
recogniser lands, which is the same edit `REQ-984` mandates. The failure it
would then produce is fail-closed — the browser would compare `profileVersion` 1
against the core's 2 and refuse — not an escalation. That is why this is MEDIUM
rather than HIGH.

`TEST-1115`'s wording is broader than the requirement's — "Reject any
v2-reachable global v1 constant or version-1 literal" — and would catch it *if*
the trace crosses the crate boundary. A static check written against `REQ-984`'s
literal rule would not. Requirement and test disagreeing about scope is the
defect.

### Minimum sound correction

**Selfsame `REQ-984`**: change "No v2-reachable comparison uses" to "No
v2-reachable comparison **or substitution** uses", add the cross-crate spelling
`selfsame_app_identity::PROFILE_VERSION`, and either add
`selfsame-web-device`'s profile-facts export to the enumerated v2 surfaces or
state that credential/v2's browser derives its own profile facts through a named
v2 branch and never through the v1 facts export. **`TEST-1115`** should
enumerate the sites by file and line rather than by prose, and its trace must be
declared to cross `selfsame-app-identity` → `selfsame-web-device`. cbcl-bus
`REQ-056`/`TEST-074` move with it.

---

## M-2 (MEDIUM) — a `migrationRooms` element has no declared grammar or cap in any effective clause, and the grammar that would bound it lives in `cbcl_erl:parse_message` (repository `cbcl-rs`), which is not a declared baseline; so the 48,000-octet array witness cannot be shown constructible and the mandatory exact-room-list consent display has no per-element bound

**Owner: Selfsame `REQ-984`/`REQ-985`**, mirrored in **cbcl-bus `REQ-056`/`NFR-012`**,
coordinated into **cbcl-pairing `NFR-018`**.

### What the candidate says

`REQ-984`:

> Each `migrationRooms` value is **exact canonical room text**. Its UTF-8 bytes
> are the `roomBytes` used by the room-set digest. Values are unique and sorted
> by unsigned UTF-8 octets. The 48,000-octet limit covers the complete RFC-8785
> array, including brackets, commas, quotes, and escaping.

`REQ-985` declares `V2_MAX_MIGRATION_ROOMS = 256` and
`V2_MAX_MIGRATION_ROOMS_JCS_OCTETS = 48,000`, and its cap rule names the
grammars that "also apply": "Existing URI, DID, identifier, timestamp, and key
grammars". No room grammar.

This is a genuine improvement — 0.4.11 `M-1(e)`'s two-units problem is gone, the
JSON array order and the CBOR array order are now the same relation (both
unsigned-octet ascending, matching adopted `REQ-972`'s "complete unique raw
room-byte array sorted by unsigned-octet order"), and RFC-8785's
array-order-preserving rule is respected. What is missing is the element bound.

### Why it matters

**(a) The 48,000 witness may not be constructible.** Every other variable member
in `REQ-985` has an element cap; this one has only a container cap. The 48,000
array is reachable only if 256 elements can average ~187 octets. The room-name
grammar is not in any of the four declared baselines: `cbcl-parser`
(`cbcl-bus apps/cbcl_bus/src/cbcl-parser.lfe`) is a crash-trapping wrapper over
`cbcl_erl:parse_message` — "It never performs I/O, touches Mnesia, or holds
process state" — and the grammar it wraps lives in `cbcl-rs`. If room text is
short-bounded (the browser's own handle grammar next door is 33 octets), a
256-element array is a few thousand octets and `TEST-1113`'s "Require one
48,000-octet RFC-8785 array" joins `H-1`'s unreachable set. If it is unbounded,
a single element may be ~47,990 octets. The effective text does not say which,
and neither does any repository a reviewer of this unit can read.

**(b) The consent display has no per-element bound.** `CON-960` makes the
complete room list part of the final consent screen; `GATE-04` box 27 makes
"exact room-list consent" a human-review condition; cbcl-pairing `NFR-018`
requires the typed transition to be "bounded by the same effective offer limits
and SHALL not clone unbounded input during display". A container-only cap
satisfies "not unbounded" but leaves `PathAToBDisplay.migrationRooms` able to
carry one 47,990-octet string into the screen a person is asked to read and
approve. That is a legibility and consent-integrity property, and it is the one
`F-E`'s human-review box is supposed to accept knowingly.

**(c) It defeats the reproduction claim symmetrically with `H-1`.** `REQ-985`'s
whole method is "declare every input, derive every total". One input is not
declared, and its owner is outside the review unit.

### Minimum sound correction

**Selfsame `REQ-984`/`REQ-985`**: declare `V2_MAX_MIGRATION_ROOM_OCTETS`
(both UTF-8 and RFC-8785-escaped, as every other textual cap now is), state the
canonical room-text grammar or cite the clause that owns it, and derive
`V2_MAX_MIGRATION_ROOMS_JCS_OCTETS` from `V2_MAX_MIGRATION_ROOMS ×
(V2_MAX_MIGRATION_ROOM_OCTETS + 3) - 1 + 2` rather than declaring it
independently — so the two caps cannot disagree and the array witness is
constructible by construction. If the grammar's authority really is `cbcl-rs`,
add that repository and commit to `implementation-baselines:` or restate the
bound locally as a v2-only recogniser rule. cbcl-bus `REQ-056`/`NFR-012` and
cbcl-pairing `NFR-018` move with it.

---

## L-1 (LOW) — `NFR-914`'s five named cyclic sources omit the pair that will dominate under load — the per-frame authority read against every grant and member writer — and cbcl-bus `NFR-012` makes its list open-ended ("Expected cyclic sources **include** …"), which hollows out `TEST-074`'s "Trigger every named cyclic source"

**Owner: Selfsame `NFR-914`/`TEST-1116`**, mirrored in **cbcl-bus `NFR-012`/`TEST-074`**.

The two load-bearing claims are **correct**, and I verified both in OTP 28.5 /
Mnesia 4.25.3 source rather than accepting them. Wait-die:

```erlang
%% mnesia_locker.erl:341-343 — the module's own statement of the rule
%% 1) Impose an ordering on all transactions favour old (low tid) transactions
%%    newer (higher tid) transactions may never wait on older ones,
%% mnesia_locker.erl:410-411,430-436
can_queue(Tid, Tid, _Oid, Res) -> Res;                 % own locks never conflict
allowed_to_be_queued(WaitForTid, Tid) ->
    case get(pid_sort_order) of
        undefined -> WaitForTid > Tid;                  % wait only on the younger
```

`can_queue/4` and `allowed_to_be_queued/2` exist at exactly those arities.
`index_read` and `foldl` locks:

```erlang
%% mnesia_locker.erl:1086-1101   ixrlock/5 -> need_lock(Store, Tab, ?ALL, read)
%% mnesia_locker.erl:326-333     grant_lock(_, {ix_read,…}, Lock, {Tab,_}) -> {granted, Res, [?ALL]}
%% mnesia_locker.erl:1083-1084   rlock_table(Tid, Store, Tab) -> rlock(Tid, Store, {Tab, ?ALL})
%% mnesia.erl:1953-1957          init_iteration/4 -> lock(…, {table, Tab}, LockKind)
```

`NFR-914`'s "It is not a deadlock proof" is the right characterisation, and
0.4.11 `L-1` is closed.

What is still missing is the *dominant* source. `can_lock` for a record write
selects held `{Tab, ?ALL}` locks **of any mode**, not only write:

```erlang
%% mnesia_locker.erl:357-360
can_lock(Tid, write, Oid = {Tab, Key}, AlreadyQ) when Key /= ?ALL ->
    ObjLocks = ets:select(mnesia_held_locks, [{ {{Oid,  '_', '$1'}}, [], ['$1'] }]),
    TabLocks = ets:select(mnesia_held_locks, [{ {{{Tab, ?ALL}, '_', '$1'}}, [], ['$1'] }]),
```

so a held `{cbcl-device-grant, ?ALL}` **read** blocks or kills any concurrent
`{cbcl-device-grant, Key}` write. `NFR-913`'s matrix — retained normative —
puts `grant ?ALL; member ?ALL; pairaccount ?ALL` on the **per-frame** "unratified
read". Every frame on every connection therefore takes two table read locks that
today's code does not take (the deployed per-frame path reads
`cbcl-pairaccount-grant` by index and `cbcl-device-grant` by exact key only —
`cbcl-chat-session-ws.lfe:347,385,392-396`), and every one of them can force a
grant creator, member enroller, revoker or finaliser to die cyclically whenever
the reader is the older transaction. That pair — not "different-account grant
creators" — is what a representative load test will actually observe.

Two further unnamed pairs from the same matrix: grant creation (`…; grant ?ALL;
member ?ALL; exact grant write`) against member enrollment (`device guard; grant
?ALL; exact member write`) is a classic shared-read-then-upgrade cycle on two
tables; and pairaccount claim (`handle guard; device guard; grant ?ALL; member
?ALL; pairaccount ?ALL; exact claim write`) against both grant writers.

`NFR-913`'s residual paragraph does record per-frame contention as an accepted
operational residual and `GATE-04` boxes 31/33/34 do gate it, so the *substance*
is covered — hence LOW. The defect is the checklist: `TEST-1116` says "Trigger
each cyclic source in `NFR-914`" and `TEST-074` says "Trigger every named cyclic
source", and cbcl-bus's list is prefixed "include", which makes "every named"
unenumerable.

**Minimum sound correction:** add "the per-frame authority read against every
grant, member and pairaccount writer" and "grant creation against member
enrollment" to `NFR-914`'s numbered list; change cbcl-bus `NFR-012`'s "Expected
cyclic sources include" to a closed enumeration matching it. Owner: Selfsame
`NFR-914`, cbcl-bus `NFR-012`.

---

## L-2 (LOW) — "fixed bounded `mnesia:transaction/2`" is only bounded in one of that function's two overloads; `transaction(Fun, Args)` is `infinity`, so an implementation can satisfy the clause as written and still be non-conforming

**Owner: Selfsame `NFR-914`**, mirrored in **cbcl-bus `NFR-012`**.

`NFR-914`: "Every affected operation uses fixed bounded `mnesia:transaction/2`
or `mnesia:transaction/3` restarts. The default unbounded restart form is
non-conforming."

The premise is right — `transaction/1` is unbounded:

```erlang
%% mnesia.erl:720-721
transaction(Fun) ->
    transaction(get(mnesia_activity_state), Fun, [], infinity, ?DEFAULT_ACCESS, async).
```

But `transaction/2` has two clauses and only one of them bounds anything:

```erlang
%% mnesia.erl:729-734
transaction(Fun, Retries) when is_integer(Retries), Retries >= 0 -> … Retries …;
transaction(Fun, Retries) when Retries == infinity                -> … infinity …;
transaction(Fun, Args)                                            -> … infinity …;
```

An LFE call site written as `(mnesia:transaction (lambda (h k) …) (list handle pk))`
is `mnesia:transaction/2`, satisfies the clause verbatim, and retries forever.
The same trap exists in `transaction/3` only in the sense that
`transaction(Fun, Args, Retries)` (`mnesia.erl:806-807`) *is* always bounded —
so `/3` is safe and `/2` is not.

**Minimum sound correction:** state the arity *and the argument*: "through
`mnesia:transaction/2` with an explicit non-negative integer `Retries`, or
`mnesia:transaction/3`", and have `TEST-1116`/`TEST-074`'s static check reject a
`transaction/2` call whose second argument is a list. Owner: Selfsame `NFR-914`,
cbcl-bus `NFR-012`.

---

## L-3 (LOW) — `REQ-986`'s account-bound browser terminal has no declared state source; the only state that could drive it is in-memory (`pathBBinding`, `heldGrant`), so `TEST-1114`'s "zero enrollment frames" fails after a reload, and the obvious durable substitute becomes a stale over-refusal after authorized grant revocation

**Owner: Selfsame `REQ-986`/`TEST-1114`**, mirrored in **cbcl-bus `REQ-057`/`CON-024`/`TEST-073`**.

`REQ-986`: "The browser intercepts handle changes while account-bound. It
displays the local terminal `account-device-handle-change-refused` and sends no
enrollment frame."

The authority half is **correct and verified**, which is why this is LOW. After
migration the handle is account-bound; a change to a new handle resolves
`unbound` at `dispatch/3` (`cbcl-chat-session-ws.lfe:212-233`), reaches `tofu`,
and `REQ-983` rule 3 refuses because the key carries a `cbcl-device-grant` row.
Changing to an existing key-bound handle refuses on `handle-key-mismatch`;
changing to another account's handle refuses on `not-issued`
(`cbcl-core-enrol.lfe:241`). The hub is closed on every branch, and the
interception point is real — the deployed `change` listener is one function
(`app.js:1071-1125`) and it does not itself send a frame; the enrolment happens
on the next signed frame.

What is undeclared is how the browser knows. Both candidate sources are in
memory only:

```js
// apps/cbcl_chat/priv/web/app.js:1671
let pathBBinding = null;
// apps/cbcl_chat/priv/web/app.js:6279
let heldGrant = null;
```

Neither is persisted; `presentPathBGrant` (`:1861-1871`) sets `pathBBinding` and
nothing writes it to `localStorage` or IndexedDB. So after a page reload an
account-bound browser does not know it is account-bound: it will accept the
handle change, emit a frame, and take the hub refusal — `TEST-1114`'s "Require
the local `account-device-handle-change-refused` terminal and zero enrollment
frames" fails, and the person sees a hub error slug instead of the truthful
local terminal that `REQ-908` requires.

The obvious fix — a durable "account-bound" flag — introduces the stale-state
case `REQ-983` and `REQ-057` explicitly contemplate elsewhere: after authorized
grant revocation the hub *would* permit enrolment (no grant row), but the stale
flag keeps refusing locally, so the person is locked out of a control the hub no
longer objects to. `CON-969`'s precedent is the right shape and does not have
this problem, because the possession terminal is decided by whether a seed is
present, which is itself the durable fact.

**Minimum sound correction:** name the state source in `REQ-986`/`CON-024` — the
retained credential/v2 device seed plus the installed grant record, both of
which `CON-962` already requires the browser to persist — and state the
revocation transition: when the browser's next frame is refused `not-issued`,
the local account-bound state clears and the handle control is restored. Add the
reload arm and the post-revocation arm to `TEST-1114`/`TEST-073`. Owner:
Selfsame `REQ-986`, cbcl-bus `REQ-057`/`CON-024`.

---

## L-4 (LOW) — adopted `CON-960`'s "Every member is part of … CON-214 evidence, provider hint" is reconciled with the 21-member statement and the exact seven-member hint only by its own "as applicable" hedge, and `CON-971` does not ledger it; a literal reading makes `REQ-985`'s release-blocking statement bound impossible

**Owner: Selfsame `CON-971`.**

`CON-960` (0.4.8, adopted, and the clause `REQ-984` says it "clarifies" rather
than replaces):

> Every member is part of RFC-8785 `offerCoreDigest`, cbcl-pairing's
> domain-separated `intentDigest`, **CON-214 evidence, provider hint**, display
> object, final confirmation, reverse payload, hub acceptance, acknowledgement,
> and receipt correlation **as applicable**.

`REQ-985` fixes the CON-214 statement at **21** members and 2,560 octets, and the
hint at **exactly** `CON-945`'s **seven** members and 507 octets. Neither carries
a transition member; both carry `offerDigest`, through which the transition is
bound transitively. That is almost certainly the intent, and "as applicable"
carries it — but it carries it in a clause whose companion is a
release-blocking arithmetic gate. Read literally, a `PathAToB` transition inside
the CON-214 statement adds 594 non-room octets plus a 48,000-octet array to a
2,560-octet payload, i.e. it fails the 4,000-octet JWS policy by an order of
magnitude. `CON-971`'s eight-item ledger names `REQ-982`, `CON-960`'s *JSON
encoding*, `CON-939`'s display grammar, `REQ-983`'s claim-first sentence,
`NFR-913`, `ADR-947` and four tests. It does not name this sentence.

**Minimum sound correction:** one `CON-971` item — "`CON-960`'s membership
sentence is clarified: transition members bind into the CON-214 statement, the
provider hint, and the final confirmation **through `offerCoreDigest`** (and, for
the three digests and the nonce, through `REQ-972`'s
`migrationConfirmationDigest` preimage), and are members of the offer core
alone." Owner: Selfsame `CON-971`.

---

## L-5 (LOW) — coordination metadata is partly stale again: cbcl-bus's parent `SPEC-053-key-root-identity` still points DEC-012's release condition at 0.16.2's GATE-04, and Selfsame's test spec reverts to an open cbcl-bus test range that includes the four tests 0.16.5 replaces

**Owner: cbcl-bus `SPEC-053-key-root-identity` (DEC-012 paragraph)** and
**Selfsame 0.4.12 test-specification preamble**.

(a) The parent's frontmatter is fresh —
`candidate-amendment: "[[spec-053-0.16.5-tagged-transition-closed-authority]]"` —
but the body paragraph added under DEC-012 is not:

> [[spec-053-0.16.2-credential-v2-authority-corrections]] supplies the corrected
> authority, transaction and
> [[spec-053-0.16.2-credential-v2-authority-corrections#GATE-04]] that this
> decision required. DEC-012's production deferral remains in force until **that
> amendment** passes its coordinated Tier-1 review and every GATE-04 box … is
> closed.

`REQ-055` item 8 makes 0.16.5's GATE-04 the only effective one, and 0.16.2's
eleven-box list has been replaced twice over. An operator following the parent
reaches a superseded gate. This is 0.4.10 `L-3`'s class, partially reopened.

(b) 0.4.12's test preamble says "Cbcl-bus TEST-043 through TEST-075 is mandatory
under its exact survivor ledger." 0.16.5 `REQ-055` replaces `TEST-066`,
`TEST-067`, `TEST-069` and `TEST-071`, and its own preamble is the enumerated
"TEST-043 through TEST-065, TEST-068, and TEST-070 … plus TEST-072 through
TEST-075". The Selfsame range names four superseded tests as mandatory and
relies on the pointer to disclaim them — the open-ended shape 0.4.11 replaced
with a block enumeration and `TEST-1112` was written to reject ("They reject
open-ended test ranges").

**Minimum sound correction:** update the DEC-012 paragraph to name 0.16.5
(cbcl-bus owner), and restore the block enumeration in Selfsame's test preamble
(Selfsame owner).

---

# Disposition of the original `F-A` – `F-E`

| Finding | Disposition | Evidence |
|---|---|---|
| **F-A** — trust is application+relay scoped; a new application prompts | **HOLDS** | Adopted `REQ-981` is unreplaced by `CON-971` and retains the distinct canonical ApplicationId `https://chat.anuna.io/selfsame/application/v2` with the person-owned policy key `(.../application/v2, https://chat.anuna.io:9443)` and the explicit sentence "A v1 policy entry cannot suppress this first v2 prompt." `REQ-984` restates the distinct id in its first line. The v2 permission namespace is legal at the baseline: `profile.rs:530-556`'s `permissions()` requires each entry to be a canonical HTTPS URI with a fragment **on the applicationId origin** (`parts.origin != application_id.origin()`), and both ids share origin `https://chat.anuna.io`. The live document is untouched — `selfsame-application-profile.mjs`'s ratified base64url decodes to `"profileVersion":1` with four `.../application#…` permissions and `"relayOrigin":"https://chat.anuna.io:9443"`, so `relayOrigin` provenance stays authenticated rather than wire-claimed. `TEST-1108` (retained) requires the preload-v1-then-v2 prompt case; cbcl-bus `GATE-04` box 14 mirrors it. |
| **F-B** — approval displays the CON-220-authenticated application/transition; cross-check precedes display | **HOLDS, and 0.4.11's caveat is cleared** | Adopted `CON-967` and cbcl-pairing `CON-021` still agree word for word that the `IntentVerifier` "can return only `Ok(())` or a closed profile error" and that only the shared profile may then construct `CredentialV2Display`. cbcl-pairing 0.4.4 differs from the 0.4.11-reviewed commit by exactly two frontmatter lines (`git diff f4dbb0c 4babf00` = `consumer-design` and `review-input`), so its verified constructibility at `8e55160` stands. **The 0.4.11 caveat — that what is displayed was well-owned but not well-defined — is resolved:** `REQ-984`'s tagged JSON is a one-for-one image of `CON-021`'s `AccountTransition = NoTransition \| PathAToB(PathAToBDisplay)`, member for member (`legacyHandle`, `legacyKeyDigest`, `migrationRooms`, `roomSetDigest`, `migrationSnapshotDigest`, `snapshotNonce`), with `kind` as the wire tag the Rust sum carries structurally. |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias provisioning or grant precedes the appropriate explicit consent | **HOLDS** | `CON-971`'s ledger touches `REQ-982`, `CON-960`'s JSON encoding, `CON-939`'s display grammar, `REQ-983`'s claim-first sentence, `NFR-913`, `ADR-947` and four tests — **nothing in `CON-962`**, whose sixteen-step order therefore remains intact. Checked each new clause for a pre-consent effect: `REQ-984` and `REQ-985` are grammar and bounds only; `REQ-986`'s browser terminal is a local refusal that sends no frame and writes nothing; `NFR-914` is lock and retry evidence. `REQ-981`'s "Both explicit migration consent screens remain mandatory on every migration" is preserved by the 0.4.12 preamble. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS, and is slightly improved** | Adopted `REQ-970`/`CON-939` still mint one pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId` before the public carrier, durable only at the final transaction, inside the distinct v2 namespace. `REQ-984`'s removal of `accountDid` from the offer core is a *correctness gain* here, not a loss: the hub constructs `OfferCoreV2` at `CON-939` step 2, before the wallet has derived its issuer DID, so a mandatory `accountDid` core member was a value the hub could not have known. It now enters only `REQ-972`'s `migrationConfirmationDigest`, whose declared preimage already contains "UTF-8 account DID". |
| **F-E** — per-app consent is a mitigation, not a relay-operatorship binding claim | **HOLDS, and 0.4.11's gate weakening is repaired** | 0.4.12's disposition section restates it and claims nothing further. The relay-visible ciphertext-length residual survives (`MAX_ADJACENT_BODY_OCTETS = 65_536`, `cbcl-pairing/src/cbcl_protocol.rs:34`), and — unlike 0.4.11, where `M-2` had removed it — the human privacy-acceptance box is back in the effective cbcl-bus gate as `GATE-04` box 28: "that review accepts relay-visible size correlation and terminal oversize exclusion". |

---

# Disposition of every 0.4.8 finding (1C / 4H / 3M / 7L)

Re-tested against the 0.4.12 effective set and code.

| 0.4.8 finding | Re-test verdict | Evidence |
|---|---|---|
| **C-1** (CRITICAL) — the transaction writes a `cbcl-pairaccount-grant` for the key the person must reconnect with; both keys inert | **CLOSED** | Both mechanisms survive and are now *more* explicit. `REQ-986` and cbcl-bus `CON-024` keep credential/v2 free of any pairaccount write ("It performs no pairaccount write"), and make `unratified-device?` return literal `false` on any grant or member row, so the granted key passes the unchanged `cbcl-core-enrol.lfe:125-131` clause. `GATE-04` box 8 restates "credential/v2 has no pairaccount writer or call edge". The possession-proof half (`REQ-053`) removes the third-party writer; the gap it closes is real at the baseline — `do-pairaccountgrant` (`cbcl-chat-session-ws.lfe:1667-1678`) takes `devkey` verbatim from the frame with no possession check. |
| **H-1** (HIGH) — WS gate runs after TOFU and default fan-out | **CLOSED** | Re-verified structurally: `handle-frame` (`:204-211`) performs decode and parse only before `dispatch/3`, and `dispatch/3` (`:212-233`) puts `grant`/`grantproof` first, ahead of the `account-binding` read, `resolve-key`, `verify-and-route` and TOFU. The fall-through hazard is still real (`route-member`'s default is `do-publish`, `:960`). cbcl-bus `REQ-042` survives per `REQ-051`, restated by `REQ-055`, and `GATE-04` box 10 keeps the dispatch-before-authorization evidence mandatory. |
| **H-2** (HIGH) — `fingerprintCanonicalBytes` undefined; hub has no BLAKE3 | **CLOSED** | `enrollment::device_key_digest` is `SHA-256(canonicalise(deviceKeyJwk))` over the closed four-member `DEVICE_JWK_MEMBERS` (`ceremony.rs:278`), which is what makes the 93-octet figure in `REQ-985` computable and exact. |
| **H-3** (HIGH) — the positive fixture opens an unnamed second Tier-1 gate | **CLOSED, and the gate box is restored** | Both gates verified closed at the baseline: `cbcl-chat-path-b-gate:profile/0` returns `#(error tier-1-unapproved)` as a source constant (`cbcl-chat-path-b-gate.lfe:16-18`), and `apps/cbcl_chat/src/shell/` holds only `cbcl-chat-path-b-gate.lfe`, `cbcl-chat-selfsame-application-gate.lfe` and `cbcl-chat-webfinger-gate.lfe`, so `cbcl-chat-credential-v2-gate` is genuinely absent. 0.4.11 `M-2` had dropped the CI-separation release box; 0.16.5 restores it twice — `GATE-04` boxes 24 and 38. |
| **H-4** (HIGH) — the live `/selfsame/enrolment/sign` is an unauthenticated widening oracle | **CLOSED** | The hazard is as filed: `sign_enrollment_profile_bound` (`selfsame-beam/src/enrollment.rs:129-152`) takes caller-supplied `statement` octets. The repair is verified free: `build()` hard-codes `("evidenceVersion", Json::int(1))` (`enrollment.rs:661`), `EnrollmentStatement` (`:161-191`) carries no evidence-version field at all, and `recognise_payload` compares `profileVersion` to `crate::PROFILE_VERSION`. Today's builder **is** a v1-only builder. `REQ-984` keeps the route v1-only; cbcl-bus `REQ-056` restates "The live v1 profile, evidence, route, bytes, and digest stay unchanged." |
| **M-1** (MEDIUM) — wallet cannot recompute two of three digests | **CLOSED** | Adopted `REQ-975`/`CON-013` state the per-party split honestly and survive `REQ-051`/`REQ-055`. |
| **M-2** (MEDIUM) — no credential/v2 reply performative is in `hub-originated?` | **CLOSED** | `hub-originated?/1` (`cbcl-core-dialect.lfe:79-85`) is a closed `lists:member` literal already containing `account-linked` and `pairaccountgranted`. `REQ-050` survives; `GATE-04` box 11 keeps shared `account-linked` protected by `SPEC-053 REQ-033`. |
| **M-3** (MEDIUM) — the typed `AccountTransition` has no carrier | **FULLY CLOSED** (0.4.11 had it "closed in the carrier, reopened in the content") | cbcl-pairing 0.4.4 `REQ-026`/`CON-021` supply the carrier and are unchanged; `REQ-984`'s tagged nested object is now the same value, so carrier and content agree. `ADR-948` and cbcl-bus `ADR-022` both record the reason a flat shape was rejected — it cannot represent `NoTransition`. |
| **L-1** (LOW) — the lock order names a nonexistent "device count index" | **CLOSED** | Re-verified: `ensure-device-table` (`cbcl-chat-account-grants.lfe:53-62`) declares no index and `account-device-count/1` (`:204-207`) is a whole-table `mnesia:foldl`; `cbcl-chat-members.lfe:15-24` likewise declares no `pubkey` index. `NFR-913`/`NFR-012` name both as genuine new migrations, and `GATE-04` box 22 gates the guard/index migration. |
| **L-2** (LOW) — `CON-941`'s `cbcl-webfinger-account` primary key | **CLOSED** | 0.4.9 `CON-965` item 5 remains effective; not reopened. |
| **L-3** (LOW) — a nonexistent gate stated as a present-tense fact | **CLOSED** | Effective text says "On landing"; the module is absent at the baseline (verified above). |
| **L-4** (LOW) — mixed NUL notation / field-vector overclaim | **CLOSED** | One rule, one exact member vector. `cbcl-member` is `(handle pubkey enrolled)` (`cbcl-chat-members.lfe:13`), so excluding the wall-clock `enrolled` from the snapshot vector remains correct. |
| **L-5** (LOW) — the offer bound lacks arithmetic | **NOT CLOSED — becomes `H-1` (this review)** | The derivation is now complete and internally correct at every level, which is real progress over both 0.4.10 and 0.4.11. It still does not close, for a new reason: two of the declared inputs exceed what the code's own DID and timestamp grammars can produce, and the amendment says those grammars apply. |
| **L-6** (LOW) — oversize silently excludes the person | **CLOSED** | Adopted `NFR-911`'s terminal-exclusion statement retained; cbcl-bus `GATE-04` box 28 keeps "terminal oversize exclusion" in the human-review box. |
| **L-7** (LOW) — no allocator-observable no-offer test | **CLOSED** | cbcl-pairing's relay still separates control frames from adjacent bodies (`src/cbcl_protocol.rs:32-34`), so "zero `offer` objects" remains observable; 0.4.9 `CON-965` item 9 and `TEST-1101` survive. |

---

# Disposition of every 0.4.9 finding (1C / 1H / 3M / 4L)

| 0.4.9 finding | Re-test verdict | Evidence |
|---|---|---|
| **C-1** (CRITICAL) — an arbitrary pairaccount claim can brick a granted key after commit | **CLOSED, including 0.4.11's residual self-lockout** | The third-party writer is removed by `REQ-053`'s exact-key possession proof. The self-inflicted lockout 0.4.11 filed as `H-2` is no longer claimed away: `REQ-986` now states it truthfully ("Later member enrollment for that key refuses with `pairing-unratified`. It reaches no member write. This version provides no pairaccount ratification or deletion surface."), which is exactly what `cbcl-core-enrol.lfe:125-131` does. See the 0.4.11 `H-2` row below. |
| **H-1** (HIGH) — `profileVersion = 2` cannot verify against the live version-1 profile | **CLOSED** | Adopted `REQ-981` plus `REQ-984`'s "Its profile and statement use versions 2/2. The live v1 profile and its statement stay versions 1/1." Per-branch recognition is implementable: `profile.rs:385`'s single-version refusal is precisely the site `CON-951`'s dispatcher replaces, and `lib.rs:109`/`profile.rs:60` are two genuinely distinct constants that `REQ-984` now names separately. Residual: `M-1` (this review) — a fifth, cross-crate site the rule's wording does not reach. |
| **M-1** (MEDIUM) — 8,192-byte v1 evidence cannot fit a 6,000-byte non-core budget | **CLOSED for the v1 collision; the fixed point becomes `H-1`** | `ENROLLMENT_JWS.max_octets = 8_192` (`enrollment.rs:58`) stays outside the v2 arithmetic and the v2-specific 4,000-octet policy is correctly separate. The `MAX_ENROLLMENT_KID_OCTETS = jws::HEADER_LIMITS.max_bytes` = 4,096 v1 kid cap (`enrollment.rs:75`) is likewise correctly superseded for v2 by `V2_MAX_CREDENTIAL_KID_OCTETS = 128`, which is what makes the 187-octet header exact — and `REQ-985`'s both-units rule covers the escaping hazard the code's own comment names at `enrollment.rs:295-298` ("C0 bytes can expand sixfold when the protected header is written"). |
| **M-2** (MEDIUM) — adopted `CON-960` contradicts the verdict-only display API | **CLOSED** | 0.4.10 `CON-968` clause 3 named the exact sentence and replacement; `CON-971` leaves it effective, and cbcl-pairing `CON-021`/`ADR-019` are unchanged. |
| **M-3** (MEDIUM) — the lock and cap rule does not cover both real grant writers | **CLOSED** | Re-verified at the baseline: `store-device-tx/7` (`cbcl-chat-account-grants.lfe:189-202`) is absent from the `defmodule` export list and already enforces the cap; `store-verified-device-grant/6` (`:216-237`) is exported, uncapped and has no production caller, so `REQ-057`'s "No uncapped exported creator remains" is a safe removal. The account guard key is exact and real — `cbcl-device-grant.account` holds the same account DID binary that `cbcl-account-binding.account` holds. |
| **L-1** (LOW) — `account-linked` is not a credential/v2 kind | **CLOSED** | In `custom-kind` and in `hub-originated?/1` (`cbcl-core-dialect.lfe:82-84`); `REQ-050` survives; `GATE-04` box 11 explicit. |
| **L-2** (LOW) — ciphertext length leaks coarse migration size | **CLOSED, and the gate box is restored** | The residual is correctly stated and bounded, and 0.16.5 `GATE-04` box 28 returns the human privacy-acceptance box that 0.4.11 `M-2` had removed. |
| **L-3** (LOW) — the hub lacks CBOR/JCS primitives | **CLOSED** | Adopted `REQ-980`'s closed typed NIF boundary survives. `REQ-985` keeps "Rust remains the hub authority through the NIF. BEAM-through-NIF remains a marshalling lane", `CON-023` restates "It accepts typed values, not caller-selected preimages", and `TEST-1113` requires the static check. `rustler::init!("cbcl_selfsame_erl", …)` (`selfsame-beam/src/lib.rs:21`) is the single boundary. |
| **L-4** (LOW) — the shared `build()` can silently widen v1 | **CLOSED** | `REQ-977`'s four boundaries survive and `REQ-984` adds three more surfaces; verified free at the baseline (see 0.4.8 `H-4`). |

---

# Disposition of every 0.4.10 finding (0C / 2H / 4M / 3L)

| 0.4.10 finding | Re-test verdict against 0.4.12 and code | Evidence |
|---|---|---|
| **H-1** (HIGH) — v2 has two incompatible trust anchors | **CLOSED** | `REQ-984` restates the distinct `.../application/v2` ApplicationId, `profileVersion = 2` on the document, `evidenceVersion = 2` / `profileVersion = 2` on the statement, and frozen 1/1 for the live pair. The exact v2 permission namespace is verified legal (`profile.rs:530-556`, origin-scoped). Per-branch profile recognition is implementable (`profile.rs:385` is the one site). The first v2 pair prompts by construction because the policy key is the exact pair. The live v1 bytes and their digest are untouched — nothing in `CON-971` touches the ratified document. Residual: `M-1` (this review). |
| **H-2** (HIGH) — cbcl-bus 0.16.3 deleted adopted authority clauses | **CLOSED** | `REQ-051` remains effective by name (`REQ-055`'s survivor list begins with `REQ-051`), and `REQ-055` adds its own twelve-item replacement ledger with an explicit survivor list. The clause numbers map onto real clauses with no orphan and no double-replacement. See the cbcl-bus verification section for the box-by-box GATE-04 comparison. |
| **M-1** (MEDIUM) — size arithmetic has no fixed point | **NOT CLOSED — becomes `H-1` (this review)** | Every 0.4.11 sub-defect is genuinely repaired: one JWK value used in both places (93, and the auxiliary budget is gone entirely), `deviceKeyJwk` charged once, `CON-945` cited as the hint's member source with 507 stated and verified, both units declared for every textual cap, one unit for `migrationRooms`. What replaces it is narrower and equally blocking: two declared caps exceed the fixed grammars the same requirement invokes. |
| **M-2** (MEDIUM) — member collision rule breaks handle changes | **CLOSED for multi-handle; see 0.4.11 `H-2`/`L-2` for the rest** | `REQ-986` keeps "Another handle MAY use the same plain member key" and "Plain member keys retain the deployed multi-handle behavior", which preserves the deployed `app.js` flow (one wire seed per browser, handle editable). Making `enroll-or-check/2` one transaction whose verdict reaches the caller is necessary and implementable: today `pubkey/1` runs one transaction, the write runs another, and `'ok` is returned unconditionally (`cbcl-chat-members.lfe:41-53`); `enrol-commit` already maps any non-`ok` to `#(error rejected)` (`cbcl-chat-session-ws.lfe:454-462`), which also gives `authority-busy` a closed consumer. |
| **M-3** (MEDIUM) — grant deleters escape the guard discipline | **CLOSED** | Both deleters are named with implementable guard orders. Verified against the code they replace: `revoke-observed-tx` (`:297-305`) currently folds the whole table then deletes, and `revoke-device-grant/2` (`:309-313`) deletes with no guard at all. Candidate discovery through the account index, raw-byte-sorted device guards and an exact recheck is a faithful replacement, and Mnesia maintains indexes on delete. **I additionally verified the post-revocation claim against code rather than accepting it:** `account-bound` requires `issued` to be literal `'true` (`cbcl-core-enrol.lfe:241`) and `path-b-issued?` is a *fresh durable read* of `cbcl-device-grant` on every frame (`cbcl-chat-session-ws.lfe:392-396`), which sits **before** the cached `standing` clause — so an exact deletion takes effect on the very next frame even though `revoke-device-grant/2` performs no live eviction. `REQ-057`'s "After authorized deletion, Path-B refuses the removed grant" is therefore true as written. (`cbcl-chat-path-b-live:evict/2` exists and is called only from the resolver poller's `apply-verified-union`, `cbcl-chat-path-b-poller.lfe:30-33`; a session receiving `path-b-revoked` stops, `cbcl-chat-session-ws.lfe:167`.) |
| **M-4** (MEDIUM) — the lock order omits Mnesia table locks | **CLOSED** | Both lock claims re-verified in OTP 28.5 source (see `L-1`). The shared grant core does read both authority tables after its account and device guards (`NFR-913` row 1, `NFR-012` row 1: `account guard; device guard; grant ?ALL; member ?ALL; exact grant write`). The credential/v2 finalizer does take both outer guards before the inherited 0.16.2 migration sequence and does read grant, member and pairaccount before its exact grant write. **I analysed the complete finalizer sequence for inversion rather than trusting the matrix** — see the dedicated section below. Per-frame contention is recorded as an accepted operational residual with `GATE-04` open (boxes 33/34) and no unmeasured performance claim; `ADR-947`'s rejection of a cache or dirty read for this increment is well argued and unchanged. Residual: `L-1` (this review). |
| **L-1** (LOW) — local possession error has no wire carrier | **CLOSED** | `CON-969` and cbcl-bus `REQ-053`/`CON-022` agree: four wire messages, no fifth slug, hub never sends it. The browser deliverable is real and needed — `onFrame` (`app.js:2264-2282`) consumes `identityhold`/`identityclear`, `grantchallenge` and `pairaccountgranted` and then falls through to `renderIncoming(text)`, so an unhandled `pairaccountchallenge` would render as room content. `handleGrantChallenge` (`:1762-1774`) is the exact pattern to copy. `GATE-04` box 35 keeps it: "pairaccount possession failure stays local and challenges never render as chat." |
| **L-2** (LOW) — browser differential lane reuses Rust WASM | **CLOSED** | The premise is confirmed: `selfsame-application-profile.mjs` states "The Selfsame WASM recogniser remains the authority for CON-201 recognition", and `apps/cbcl_chat/priv/web/selfsame/` is the vendored `selfsame_web_device` build (with `selfsame_web_device_bg.wasm`, a `.provenance.json` and `selfsame-vendor.test.mjs`). `credential-v2-canonical.mjs` does not exist at the baseline; the in-repo precedent for an honest hand-written JS canonicaliser plus a static vendor check does (`mls-ds-canonical.mjs`, `mls_ds_canonical_vectors.txt`, `selfsame/selfsame-vendor.test.mjs`). `REQ-985` keeps the module source-disjoint and demotes BEAM-through-NIF to a marshalling lane; `GATE-04` box 37 keeps the static import check. |
| **L-3** (LOW) — coordination metadata is stale | **PARTLY CLOSED — becomes `L-5` (this review)** | The three amendment frontmatters name this review unit correctly and cbcl-pairing's whole delta is the two coordination lines. The parent `SPEC-053-key-root-identity`'s DEC-012 paragraph and Selfsame's cbcl-bus test range did not move with them. |

---

# Disposition of every 0.4.11 finding (0C / 2H / 3M / 4L)

| 0.4.11 finding | 0.4.12 claim | Re-test verdict | Evidence |
|---|---|---|---|
| **H-1** — flat 29-member `OfferCoreV2` contradicts `CON-960`, makes `NoTransition` unrepresentable, and adds an unsourced `accountDid` | `REQ-984` retains one tagged, nested `accountTransition` | **CLOSED** | One top-level core of exactly 23 members (`CON-939`'s 22 plus `accountTransition`) with two exact closed RFC-8785 shapes. `{"kind":"no-transition"}` gives `NoTransition` an encoding in a core where "No listed member is optional", so `CON-939` and `CON-960` are consistent again. The tag makes the preimage decidable, so hub, browser and wallet — which `CON-013` requires to recompute `offerCoreDigest` independently — cannot disagree about nesting. `accountDid` is gone from the core and explicitly relocated to `REQ-972`'s `migrationConfirmationDigest`, whose declared preimage already lists "UTF-8 account DID"; that is the right home, because the hub constructs the core at `CON-939` step 2 before the wallet has derived its DID. `CON-971` items 1 and 2 ledger it, `ADR-948` records the decision, cbcl-bus `REQ-056`/`ADR-022` and cbcl-pairing `CON-021` all carry the same sum. I reconstructed the shape independently and reproduced `REQ-985`'s 3,814/3,838 exactly. |
| **H-2** — "neither ordering locks out the enrolled identity" is false at the baseline | `REQ-986` retains the intentional SPEC-076 refusal | **CLOSED** | `REQ-986` now says what the code does: "A valid pairaccount claim that wins first writes one unratified row. Later member enrollment for that key refuses with `pairing-unratified`. It reaches no member write. This version provides no pairaccount ratification or deletion surface." Verified: `cbcl-core-enrol:resolve` refuses `pairing-unratified` **before** the `binding` case and therefore before `tofu` (`cbcl-core-enrol.lfe:125-131`), `enroll-or-check/2` is reached only from `enrol-commit` (`cbcl-chat-session-ws.lfe:454-462`) which is reached only after `resolve` accepts (`:282-296`), and `cbcl-chat-pairaccount` exports exactly `(ensure-table 0) (claim 2) (grants 1) (unratified-device? 1)` with `state` stamped `'unratified` "until a ratification surface exists" — no `revoke`, no `ratify`. The refusal is a conforming, stronger implementation of SPEC-076 `REQ-004` ("a paired-but-unratified device SHALL NOT authorise channel membership, message send, or further pairing"), whose parent already records "Ratification UX" as an **Open** item owned by the repository owner. `ADR-949`/`ADR-023` reject expanding the authority surface and say so. `TEST-1114`/`TEST-073` require the closed refusal on the claim-first arm. The account-device / plain-member role split is safe and implementable: grant keys are account authority and refuse legacy enrolment (`REQ-983` rule 3), plain member keys keep multi-handle use, and the two predicates are on different indexes. |
| **M-1** — bounds do not reproduce | `REQ-985` derives every level from one declared grammar | **NOT CLOSED — becomes `H-1` (this review)** | Every arithmetic sub-defect is repaired and every figure reproduces from the declared caps (I recomputed all twelve independently). Two of the caps are not reachable under the grammars `REQ-985` itself invokes. |
| **M-2** — the hub gate loses eight mandatory boxes | `CON-971` adopts the complete cbcl-bus 0.16.5 gate | **CLOSED** | 0.16.5's `GATE-04` restates thirty-nine boxes explicitly and its preamble says "Every prior GATE-04 box is restated below. No open-ended gate reference supplies release authority." I compared it box by box against 0.16.1, 0.16.2, 0.16.3 and 0.16.4 — table below. All eight boxes 0.4.11 `M-2` listed as dropped are back. `TEST-075` requires "every GATE-04 box in this amendment" and "Removing any box fails the check"; Selfsame `TEST-1116` requires "the cbcl-bus 0.16.5 GATE-04 text byte-for-byte in gate evidence". |
| **M-3** — v2 dispatch misses real sites | `REQ-984` enumerates profile, hint, offer, statement, and signer boundaries | **SUBSTANTIALLY CLOSED — residual becomes `M-1` (this review)** | The three in-crate sites 0.4.11 named are now covered: `profile.rs:385` by the `profile-v2 recognizer` surface, `provider_hint.rs:115` by the `provider-hint-v2 recognizer` surface (and `HINT_MEMBERS`' five-versus-seven mismatch makes that branch unavoidable), and `ceremony.rs:307`'s `payloadVersion == Some(1)` by "or a version-1 literal". Both constant spellings are named. One cross-crate substitution site is not reached by the rule's wording. |
| **L-1** — lock inversions are unnamed; wait-die unrecorded | `NFR-914` names wait-die and every known inversion | **CLOSED for what it names; residual becomes `L-1` (this review)** | Wait-die is recorded with the correct functions and arities and is verified in OTP source; "It is not a deadlock proof" is the right framing. Both cross-operation inversions 0.4.11 named appear as sources 2+3 and 4+5. The dominant load source is not in the list. |
| **L-2** — account devices lose handle change | `REQ-986` states the accepted role boundary and local browser terminal | **CLOSED in authority; the state source becomes `L-3` (this review)** | The consequence is now stated instead of silent, the hub is closed on every branch (verified), and `TEST-1114`/`TEST-073` cover it. |
| **L-3** — one payload constant has two meanings | `REQ-985` gives the core and complete offer different constants | **CLOSED** | `V2_MAX_OFFER_CORE_OCTETS = 52,000` and `V2_MAX_OFFER_PAYLOAD_OCTETS = 57,000` are separate names for separate levels, and cbcl-bus `CON-023` routes them to separate recognisers: "The complete offer recognizer has its own 57,000-octet limit. The core recognizer has its own 52,000-octet limit." Neither can be confused with the deployed `MAX_PAYLOAD_OCTETS = 69_607` (`ceremony.rs:64`), which is v1-only and bounds the whole payload. The collision-prone `V2_MAX_PAYLOAD_OCTETS` name is gone. The effective-text chain is clean: 0.4.8 `REQ-972`'s 60,000 core cap was replaced by 0.4.9 `CON-965` item 8, that by 0.4.10 `CON-968` item 4, that by 0.4.11 `CON-970` item 6, and that by `CON-971` item 3 — and `TEST-1101` (still effective) statically rejects a resurrected 60,000. |
| **L-4** — display bounds narrow international text | `REQ-985` restores the complete 128-scalar grammar with 512-octet caps | **CLOSED** | `CON-971` item 4 ledgers the restoration explicitly, and the 512/512 conjunction is provably exact: for 1..128 NFC scalars excluding C0/C1, UTF-8 `= 512-3k` and JCS `= 512-2k` where `k` is the number of `"`/`\` characters, so both caps hold for every legal string with equality at `k = 0`. No legal 128-scalar display — CJK, emoji or Indic — is excluded. The expanded display is carried into every new bound: the statement's 2,560 and the core's 3,838 both include the 542-octet member. |

---

# Verification of the coordinated amendments

## cbcl-pairing SPEC-001 0.4.4 — passes on its own terms, unchanged

The candidate differs from the 0.4.11-reviewed commit by exactly two frontmatter
lines (`git diff f4dbb0c 4babf00`: `consumer-design: selfsame SPEC-008
0.4.12-draft` and a refreshed `review-input`). Re-verified against `8e55160`:

- `DisplayIntent`/`DisplayField` become opaque with private fields and crate-private
  construction; every existing construction is in-crate, so no external producer
  breaks.
- `ApplicationProfile::recognise_intent(&mut self, …)` type-checks at both call
  sites, preserves `dyn` object safety, and needs no interior mutability.
- `CON-021`'s verdict-only verifier still agrees word for word with Selfsame
  `CON-967`.
- `TEST-055`'s control witnesses are grounded in the real constant
  `MAX_CONTROL_OCTETS = 2_048` (`src/cbcl_protocol.rs:32`), and the 62,000-octet
  credential ceiling is real and enforced in both directions (`src/profile.rs:334`
  on encode, `:920` on decode), comfortably inside
  `MAX_ADJACENT_BODY_OCTETS = 65_536`.
- `CON-021`'s `AccountTransition = NoTransition | PathAToB(PathAToBDisplay)` now
  matches Selfsame `REQ-984` member for member, which resolves 0.4.11 `H-1`'s
  coordination consequence in cbcl-pairing's favour with no change to this
  document.

**No finding in this review is owned by cbcl-pairing**, with one coordination
consequence: `M-2`'s per-element room bound has to reach `NFR-018`'s
"bounded by the same effective offer limits" so the typed display carries a real
per-element limit rather than only a container one.

## cbcl-bus SPEC-053 0.16.5 — the gate passes; the arithmetic and dispatch inherit

Verified sound and implementable against `fdbe414`:

- **`REQ-055`.** A real, checkable ledger. It keeps `REQ-051` itself alive by
  name (so 0.16.1's and 0.16.2's survivor lists remain effective), enumerates
  nine surviving 0.16.4 clauses, and maps twelve replacements onto real clause
  numbers with no orphan and no double-replacement. Its closing sentence — "Every
  prior GATE-04 box is restated below. No open-ended gate reference supplies
  release authority" — is the exact repair 0.4.11 `M-2` asked for, and unlike
  0.16.4's "All prior surviving GATE-04 boxes remain open" it does not quantify
  over an empty set.
- **`REQ-056`.** Correct and matched to Selfsame `REQ-984`, including the tagged
  transition, the 23-member core, `accountDid`'s exclusion, and the room-array
  unit. Inherits `M-1` (the "No v2-reachable path compares with" wording has the
  same comparison-only scope) and `M-2`.
- **`REQ-057`.** Correct on every arm I could test against code: multi-handle
  preservation, the device-grant collision predicate, `enroll-or-check/2`
  returning its verdict, both creators through one private capped core, both
  deleters with guards and exact rechecks, the claim-first `pairing-unratified`
  outcome, and the post-deletion result (verified true via `path-b-issued?`'s
  per-frame durable read).
- **`NFR-012`.** Lock claims verified correct against OTP source; wait-die named
  with the right functions; contention correctly an open gate. Arithmetic
  inherits `H-1`; retry wording inherits `L-2`; the cyclic-source list inherits
  `L-1`.
- **`CON-023`/`CON-024`.** Implementable. `CON-023` finally separates the two
  payload levels by recogniser. `CON-024`'s `unratified-device?` ordering ("Any
  grant or member returns `false`. The exact pairaccount result applies only when
  both stronger indexes are empty. Any error returns `unknown`") is exactly the
  fail-closed shape the existing consumer requires — `cbcl-core-enrol` proceeds
  only on literal `'false`. One under-specification worth naming in the SPL, not
  as a finding: `CON-024` says the finalizer "reads grant, member, and pairaccount
  tables" without saying what verdict each read produces; `NFR-913` supplies the
  purpose of two of the three ("global key compatibility", "the earlier
  unratified-key collision") and none for the grant read.
- **`ADR-022`/`ADR-023`.** Sound. `ADR-023` states the authority boundary
  honestly rather than promising a recovery the code cannot perform.
- **`GATE-04`.** **Passes.** Box-by-box restoration of everything 0.4.11 `M-2`
  found missing:

| Box 0.4.11 `M-2` listed as dropped | Restored in 0.16.5 as |
|---|---|
| GATE-01 and GATE-03 close without a credential/v2 bypass | box 25 (both parent gates exist: `SPEC-053-key-root-identity` §GATE-01, §GATE-03) |
| human security/privacy review — compromised-Path-A takeover, irreversible migration, exact room-list consent, relay-visible length correlation, terminal oversize | boxes 26, 27, 28 |
| hub identity-authority off-machine + did-crdt durable readiness | boxes 29, 30 |
| production closure artefact and two-gate-open fixture on distinct CI controls | boxes 24 and 38 |
| dispatch-before-authorisation zero-write/fan-out evidence | box 10 (+ `REQ-055`'s "six kind sets remain effective") |
| frozen v1 and disjoint evidence-v2 call graphs | box 12 |
| guard/index migration, mutation/crash/race suite, immutable status recovery, authority-continuity restore | boxes 22, 23 |
| credential/v2 has no pairaccount writer/call edge; migration leaves the new key live with recoverable status | boxes 8, 9 |

  Every 0.16.4 box is likewise present (boxes 1, 12–14, 15, 17–21, 31–35, 37, 39).
  Three boxes are *compressed* rather than dropped — box 10 says "all client and
  hub v2 kinds" instead of "the six exact client and six exact hub v2 kinds", box
  25 drops "including every inherited upstream and human-security box they
  retain", and box 37 drops the Rust/BEAM role half that box 15's "all three
  lanes" now carries. In each case the underlying requirement survives by name in
  `REQ-055`, so these are compressions, not deletions. There is no circular gate
  authority: box 1 requires the cross-model PASS, `TEST-075` requires the box
  list, and neither closes the other.

The parent `SPEC-053-key-root-identity` change is a candidate-amendment pointer
bump plus a DEC-012 paragraph that still names 0.16.2 — see `L-5`.

## did-crdt

Candidate equals baseline (`1f409a4`); no clause in this review unit amends it.
Its role is unchanged: `GATE-04` box 30 requires "did-crdt durable readiness
remains continuously true", and `R-4` below records what depends on it.

---

# The complete finalizer lock sequence, analysed rather than assumed

`NFR-913`'s matrix is retained normative and `NFR-914` supplements it. I built
the resource-order graph from the matrix and checked every pair, rather than
trusting the new list.

```text
A  = account guard (exact key, absent-row write lock)   G? = {cbcl-device-grant, ?ALL} read
D  = device guard (exact key)                           M? = {cbcl-member, ?ALL} read
H  = handle guard (exact key)                           P? = {cbcl-pairaccount-grant, ?ALL} read
Gx = exact grant write     Mx = exact member lock       Px = exact claim write
SA = stable account   PC = pending ceremony   HB = handle binding
RM = whole cbcl-roommember   ARM = whole cbcl-account-roommember

1 grant create      A  D  G? M? Gx
2 finalizer         A  D  SA PC HB Mx RM ARM G? M? P? Gx [alias, account, tombstone,
                                                          finalization, status, compensation]
3 exact revoke      A  D  Gx
4 observed revoke   A  G? D(sorted) Gx
5 member enroll     D  G? Mx
6 pairaccount claim H  D  G? M? P? Px
7 authority read    G? M? P?
```

Pairwise inversions present in the matrix:

- **(1,5,6) `D` before `G?`  vs  (4) `G?` before `D`.** Named — `NFR-914` sources
  2 and 3.
- **(2) `Mx` before `G?`  vs  (5) `G?` before `Mx`.** Named — sources 4 and 5.
- No other pair reverses. The three authority tables are always taken in the
  order grant → member → pairaccount (rows 1, 2, 6, 7). Guards are always taken
  before tables. `H` appears only in row 6 and no row takes `D` before `H`, so
  the handle guard introduces none. `RM`/`ARM` appear only in row 2.

Self-conflict inside row 2 is not a hazard: the finalizer takes `{cbcl-member,
Kx}` at step 6 and `{cbcl-member, ?ALL}` at step 10, and `can_queue(Tid, Tid, _,
Res) -> Res` (`mnesia_locker.erl:410`) makes a transaction's own locks
non-conflicting in both directions.

The finalizer does take both outer guards **before** the inherited 0.16.2
migration locks (`SA`, `PC`, `HB`, `Mx`, `RM`, `ARM`), and does read grant,
member and pairaccount **before** its exact grant write, as `NFR-913` and
`CON-024` require. Its outer position also means it holds `A` and `D` for the
whole migration, which serialises it against every other row — correct for a
once-per-account irreversible operation.

What the matrix does contain, and `NFR-914` does not name, is the read-then-write
conflict class rather than an order reversal: rows 1, 2, 4, 5, 6 and **7** all
hold `{Tab, ?ALL}` read locks that `can_lock(Tid, write, {Tab,Key})` treats as
conflicting (`mnesia_locker.erl:357-360` selects `{Tab, ?ALL}` locks with mode
`'_'`). Row 7 is the per-frame path. That is `L-1`.

Bounded retries and the absence of partial effects follow from Mnesia's
transaction semantics; `authority-busy` has closed consumers at every site I
could reach — `cbcl-core-enrol` proceeds only on literal `'false` for both
`spent` and `pairing-unratified`, `unratified-device?` returns `'unknown` on any
non-`atomic` result, `enrol-commit` maps any non-`ok` to `#(error rejected)`, and
`do-pairaccountgrant`'s catch-all replies `pairaccount-store-failed`.

---

# Independent attack pass — what I looked for and did not find

Recorded so a later reviewer knows which stones were turned.

| Attack | Result |
|---|---|
| **profile-version downgrade** | No. The v1 and v2 profiles are distinct documents with distinct digests, and the statement carries `profileDigest`; `REQ-984` forbids version-1 constants on v2 paths. The one live risk is the substitution site in `M-1`, which fails closed. |
| **v1/v2 trust-anchor confusion via shared descriptors** | No. Both profiles may declare byte-identical relay descriptors with equal `descriptorDigest`, but `ProviderHint::verify` compares `application_id` against the *profile's* id first (`provider_hint.rs:110-113`), so a v1 hint cannot authenticate against a v2 profile. |
| **permission crossing** | No, but the separation is document-level, not grammatical. `permissions()` (`profile.rs:530-556`) checks only that each entry shares the applicationId **origin**, so `.../application#chat-send` and `.../application/v2#chat-send` are both legal in either document. What keeps them apart is that each profile's allowed set is fixed by its frozen generated bytes and the wallet checks the requested set against the authenticated document. Recorded as `R-11`. |
| **TOFU suppression** | No. The policy key is the exact `(applicationId, relayOrigin)` pair and the v2 id is new, so first v2 contact prompts by construction. Recorded as `R-12`: the two ids differ only by a `/v2` path segment, so the prompt's discriminating power for a person is low. |
| **size undercount** | No. Every published figure is a strict over-declaration relative to what the grammars admit — the error in `H-1` is in the safe direction for admission and the unsafe direction for the reproduction gate. No member can exceed its cap, and no total can exceed its ceiling. |
| **JSON escaping expansion** | No. `REQ-985`'s "Every textual cap below limits both UTF-8 content and RFC-8785 escaped string content" closes 0.4.11 `M-1(d)` for every member. The display conjunction is provably exact (see `L-4` disposition). The code's own comment at `enrollment.rs:295-298` confirms the sixfold C0 expansion the rule guards. |
| **offer member omission** | No. `REQ-985` states three excluded members (`devicePossessionProof`, `enrollmentEvidence`, `providerHint`) against the deployed `OFFER_EXCLUDED_MEMBERS`' two (`ceremony.rs:96`), so a v2 recogniser reusing the v1 constant would put the proof inside the core and change the digest — and the amendment says three explicitly. `CON-939` independently states the proof is not a core member. |
| **JWS `kid` overflow** | No. `V2_MAX_CREDENTIAL_KID_OCTETS = 128` is a distinct v2 cap against the deployed `MAX_ENROLLMENT_KID_OCTETS = jws::HEADER_LIMITS.max_bytes` = 4,096 (`enrollment.rs:75`), and both units are capped. The 187-octet header depends on it and `TEST-1113` mutates every cap by one. |
| **auxiliary double-counting** | No. The 2,000-octet auxiliary budget is gone; `REQ-985` states "No auxiliary budget charges a core member twice" and accounts the three excluded members individually. |
| **cap mismatch** | Yes, and it is `H-1`. Also checked and clean: `V2_MAX_OFFER_CORE_OCTETS` vs `V2_MAX_OFFER_PAYLOAD_OCTETS` vs `V2_MAX_CREDENTIAL_MEMBER_OCTETS`, the 62,000 figure against cbcl-pairing's real enforcement, and the 4,000 policy cap against the 3,752 derived maximum (a legal 4,000-octet JWS gives a 56,498-octet offer, still inside 57,000). |
| **pairaccount substitution** | No new hole. `do-pairaccountgrant` binds the claim to the **verified sender's** handle, never a named target (`cbcl-chat-session-ws.lfe:1661-1671`), and `REQ-053`'s inner proof removes the third-party `devkey` writer. Recorded as `R-13`: the per-handle cap in `unratified-count-tx` is still evaluated on the sender's own handle, so the possession proof is what prevents an attacker consuming another account's slots. |
| **nonce exhaustion** | Unchanged residual `R-3`. |
| **handle/member authority confusion** | No. Grant rows and member rows are on different tables with different indexes; `REQ-983` rule 3 and `REQ-986`'s claim-first rule together make every (grant, member, pairaccount) combination decided. |
| **revocation resurrection** | No. Verified in code: `account-bound` requires a fresh durable `issued` read that precedes the cached `standing` clause, so an exact deletion bites on the next frame. `REQ-983`'s "An old pairaccount row can then become visible again" is correct and is stated. |
| **missing writer/deleter** | No. Both creators and both deleters are named; `cbcl-chat-members` and `cbcl-chat-pairaccount` export no deleter and the amendment says so rather than implying one. Guard tables hold absent-row locks and need no reaper. |
| **Mnesia lock inversion** | Two, both named. See the finalizer section. |
| **unbounded transaction restart** | The rule is right; its arity wording is not — `L-2`. |
| **load-gate evasion** | No. `TEST-074` explicitly "provides gate evidence; it sets no acceptance threshold", and box 34 puts the acceptance on a human. Not circular. |
| **stale cache assumptions** | No. `ADR-947` rejects a cache or dirty read for the authority tables. The pre-existing per-connection Path-B **standing** cache (`cbcl-chat-session-ws.lfe:400-411, 603-615`) is bounded by an absolute expiry derived from the profile's revocation policy and is gated behind the fresh `issued` read. Recorded as `R-14`. |
| **browser seed absence** | No. `CON-969`'s local terminal is correct and the public-key-only fixture holds no seed. The *account-bound* terminal's state source is `L-3`. |
| **control fallthrough** | Live hazard, correctly owned. `onFrame` falls through to `renderIncoming` for any unrecognised hub frame; `CON-969` and `CON-024` both require the new handler to consume `pairaccountchallenge` before generic rendering, and `REQ-986`'s local terminal has "no hub-originated performative". |
| **NIF caller-chosen bytes** | No. `REQ-980` survives; `CON-023` restates "It accepts typed values, not caller-selected preimages"; `TEST-1113` requires the static check. `sign_enrollment_profile_bound` does take caller octets, but it recognises the profile before any `kid` lookup and delegates to the round-tripping `sign_enrollment_statement`, and today's `build()` is v1-only. |
| **consent confusion** | No. `CON-962` is untouched, both migration screens remain mandatory, and the tagged transition makes what is displayed decidable. |
| **partial migration** | No. `REQ-972`'s "no truncation, paging, hidden remainder, or partial migration" survives; oversize is terminal. |
| **acknowledgement loss** | Covered. 0.16.1 `TEST-046` and 0.16.2's immutable status/acknowledgement recovery survive `REQ-051`/`REQ-055`; `GATE-04` boxes 9 and 23 gate them. |
| **recovery divergence** | Covered. `NFR-012`'s recovery paragraph enumerates guard tables, authority tables, indexes, both creators, both deleters, enrollment, claim dominance, pending/final status and continuity restore. |
| **production gate leakage** | No. Boxes 24 and 38 keep the closure artefact and the two-gate-open fixture on physically separate CI controls, and both Tier-1 source constants are closed at the baseline. |
| **relay withholding** | No. 0.4.9 `CON-965` item 9 and `TEST-1101` survive; the relay still distinguishes control frames from adjacent bodies. |
| **claimed test seam absent from code** | Recorded, not a defect: `credential-v2-canonical.mjs`, the v2 profile route, the v2 hint recogniser, the `cbcl-member` `pubkey` index, both guard tables and the browser `pairaccountchallenge` handler are all absent at the baselines. That is correct for a design-only increment. `REQ-985`'s "The browser source-disjoint implementation **remains** `…/credential-v2-canonical.mjs`" is present tense about a file that does not exist; harmless, but the SPL should not read it as a description of the tree. |

## Inherited SPEC-007 `REQ-812` audit — unwaivable hard stop, not breached

`REQ-812` as amended by 0.4.4 ("Mismatch, protocol violation, cancellation, relay
closure, and expiry SHALL produce no accepted credential or identity side effect
before approved payload delivery. After approved delivery, only the standing
CON-204 provisioning and compensation effects are permitted before CON-206
returns acceptance") is inherited unwaivable and is **not breached by 0.4.12**.

The three effects the coordinated set adds before final approval are unchanged
by this amendment: the pending scope/ceremony pair (deleted on decline, expiry or
pre-commit failure — adopted `REQ-970`), the RAM-only pairaccount challenge
(`CON-016`, no durable authority), and a browser-local device seed (`CON-966`/
`CON-969`, neither a hub nor a wallet mutation). `CON-962`'s order — untouched by
`CON-971` — places every binding, grant, alias, membership and room deletion
after signed final approval, verified publication and local installation.

Checked each new clause individually: `REQ-984` and `REQ-985` are grammar and
bounds and create no effect; `NFR-914` is lock and retry evidence; `REQ-986`'s
browser terminal refuses locally and sends no frame, so it is not an effect
either. `REQ-986`'s enrolment lockout arises from the adjacent SPEC-076 claim
path, not from a credential/v2 pre-approval effect, so it is not a `REQ-812`
breach.

## CPace and blind-relay bounds — re-checked, unchanged

Audited even though the amendment claims no contact:

- `cbcl-pairing/src/cpace.rs` is pinned to `draft-irtf-cfrg-cpace-21` with
  `CPACE_DSI = "CPace255"` and `CPACE_ISK_DSI = "CPace255_ISK"` (`:23-30`).
- `x25519_scalar_mult_vfy` rejects the neutral element in constant time via
  `ct_eq` (`:144-147`), and `calculate_generator` builds the exact generator
  string from PRS, CI and session id (`:95-143`).
- `MAX_CONTROL_OCTETS = 2_048` and `MAX_ADJACENT_BODY_OCTETS = 65_536`
  (`src/cbcl_protocol.rs:32-34`) are unchanged. The 62,000-octet credential member
  plus the four ≤255-octet text fields and their deterministic-CBOR heads is
  ≈63,081 octets, comfortably inside 65,536.
- 0.4.12 introduces no new contact with the CPace transcript, the PROTO-004
  envelope, or SPEC-075 relay opacity. Checked for *absence of new contact*, not
  re-audited from first principles.

---

# Residual assumptions and open gates — *not* design blockers

Recorded so they are not mistaken for findings and so `GATE-04`'s reviewers have
them in one place. `R-1` through `R-10` are carried forward from 0.4.11 and
re-verified at these baselines; `R-11` through `R-14` are new observations from
this round.

- **R-1 — `maxDevicesPerAccount` still does not exist.**
  `ratified-max-devices-per-account/0` returns the atom `'unratified`
  (`cbcl-chat-session-ws.lfe:898`), so `path-b-max-devices/0` returns
  `#(error unratified-device-limit)` (`:889-893`) and the capped binder is
  unreachable in production. `NFR-013`-class cap language is inert until
  GATE-01's CON-002 box closes — which 0.16.5 box 25 correctly re-links.
- **R-2 — the compiled conformance allowlist is still in the code.**
  `RelayPolicy.approved_conformance: &'a [[u8; 32]]` and `cbcl_relay::eligible`
  require descriptor membership in a build-time digest list. Adopted `CON-933`
  forbids exactly this, so its removal is implementation work the coordinated
  text requires and nowhere schedules.
- **R-3 — challenge-table aggregate RAM is bounded per connection only.**
  `REQ-045` caps two live challenges per (connection, verified handle) with
  120 s expiry, but TOFU makes connections free and no clause states a global
  ceiling or a restart reaper. `cbcl-chat-grant-nonce` plus
  `cbcl-chat-grant-nonce-reaper` is the pattern to copy. Unchanged by 0.4.12.
- **R-4 — post-commit authority depends on continuous resolver availability.**
  After the irreversible commit the legacy key is dead and standing runs through
  `cbcl-chat-path-b-resolver`, which fails closed. A did-crdt outage is a total
  lockout with no legacy fallback, by design. `GATE-04` box 30 gates it.
- **R-5 — `authority-busy` still has no clause naming its per-frame surface.**
  `NFR-914` makes it "a closed refusal at every consumer" and the code is already
  fail-closed at every consumer I traced, but no clause states that the per-frame
  authority read's `authority-busy` refuses the *frame*. It should be stated as
  the intended behaviour rather than inferred.
- **R-6 — `mnesia:transaction/1` defaults to unbounded restarts.** Confirmed at
  `mnesia.erl:720-721`. Bounded retries are a real code change at every listed
  operation, not a description of current behaviour. See `L-2` for the arity trap.
- **R-7 — CPace and the blind-relay bounds were re-checked and are unchanged.**
  See the audit above.
- **R-8 — the compromised-Path-A-key takeover remains an accepted, gated risk**,
  and 0.16.5 box 26 restores the human-review box that gates it.
- **R-9 — removing `store-verified-device-grant/6` breaks roughly ten existing
  test call sites** in `cbcl-chat-account-grants-tests.lfe` and
  `cbcl-chat-account-roommember-tests.lfe`. Bounded work, named so it is not a
  surprise.
- **R-10 — every new artefact is absent at the baselines.**
  `credential-v2-canonical.mjs`, the v2 profile route, the v2 provider-hint
  recogniser, the `cbcl-member` `pubkey` index, the `cbcl-device-grant`
  `account`/`pubkey` indexes, both guard tables, and the browser
  `pairaccountchallenge` and account-bound handle handlers. Correct for a
  design-only increment; recorded so no reader mistakes the specification's
  present tense for deployed behaviour.
- **R-11 — v1/v2 permission separation is a document property, not a grammar
  property.** `profile.rs:530-556` enforces only the origin, so nothing in the
  recogniser would refuse a `.../application/v2#…` permission inside the v1
  document or vice versa. The separation rests on the two frozen generated byte
  strings and on the wallet comparing the requested set against the authenticated
  document. Sound, but it means a generator bug — not a recogniser bug — is what
  would cross the namespaces, so the generator's two immutable outputs are the
  control.
- **R-12 — the v2 TOFU prompt is structurally distinct but visually close.**
  `https://chat.anuna.io/selfsame/application` and
  `.../application/v2` differ by one path segment. `F-A` holds by construction,
  and a person asked to accept the second after having accepted the first has
  little to distinguish them by. Worth a sentence in the consent copy that the
  human review accepts.
- **R-13 — the pairaccount per-handle cap is evaluated on the sender's own
  handle.** `unratified-count-tx` counts by `handle` index
  (`cbcl-chat-pairaccount.lfe:70-75`) and `do-pairaccountgrant` binds the claim to
  the verified sender, so slot exhaustion is self-inflicted. Under `REQ-053` the
  attacker must also possess the claimed key, so both halves are closed — but the
  cap's protection comes from the possession proof, not from the handle binding
  alone.
- **R-14 — the per-connection Path-B standing cache is a real, bounded
  staleness.** `path-b-cache-store` admits an absolute expiry computed by
  `cbcl_selfsame_erl:path_b_standing_cache_expiry` from the profile and the
  grant's `valid_until`; the live ratified document declares
  `maxClosureAgeSeconds: 900` and `propagationSlaSeconds: 60`. It caches
  *standing and permissions*, never issuance, and `path-b-issued?` re-reads the
  durable grant row every frame — so revocation is not delayed by it. Recorded
  because `ADR-947`'s "a cache or dirty read is rejected for this increment"
  should not be read as saying no cache exists.

---

# What would make this pass

1. **Selfsame `REQ-985` plus cbcl-bus `NFR-012`/`TEST-072`** — close `H-1`.
   Either declare `deviceDid` and the timestamps at their grammar-forced 56 and
   20 octets (plus a declared `legacyHandle` grammar) and re-derive all twelve
   figures and four residuals, or restate the figures as recognizer ceilings and
   change `TEST-1113`/`TEST-072` from equalities to "at most, with the actual
   recorded". `CON-971` ledgers the choice.
2. **Selfsame `REQ-984`/`TEST-1115` plus cbcl-bus `REQ-056`/`TEST-074`** — close
   `M-1`. "Comparison **or substitution**", the cross-crate spelling, and a
   site-enumerated static check whose trace crosses into `selfsame-web-device`.
3. **Selfsame `REQ-984`/`REQ-985` plus cbcl-bus `REQ-056`/`NFR-012` and
   cbcl-pairing `NFR-018`** — close `M-2`. Declare
   `V2_MAX_MIGRATION_ROOM_OCTETS` in both units, cite or state the canonical
   room-text grammar, and derive the 48,000 array cap from it.
4. **The five lows** — `L-1` (add the per-frame reader and the create-vs-enroll
   pair; close cbcl-bus's "include"), `L-2` (name the integer `Retries`
   argument), `L-3` (name the account-bound state source and its revocation
   transition), `L-4` (one `CON-971` item for `CON-960`'s membership sentence),
   `L-5` (repoint the parent's DEC-012 paragraph; restore the block enumeration
   of cbcl-bus tests).

None of these is architectural. `H-1` blocks on its own because the amendments
make their own generator gate, their own mandatory tests, and one release-gate
box normative over figures that cannot be produced. `M-1` and `M-2` do not block
independently but both leave a mandatory test unable to distinguish a conforming
implementation from a non-conforming one.

---

# Authority statement

**This review confers no implementation, allocation, enablement, release or
deployment authority whatsoever, and a PASS from a future review would not
either.** Specifically:

- The verdict is **REJECT**; the coordinated set — Selfsame 0.4.12, cbcl-pairing
  0.4.4, cbcl-bus 0.16.5, did-crdt at `1f409a4` — is not approved.
- Even on a later PASS, `CON-971`'s own sentence governs: "A PASS authorizes only
  the Elephant SPL and test-first implementation plan. It authorizes no
  deployment." The adopted 0.4.10 sequencing says the same.
- `GATE-04` remains no-go with all thirty-nine boxes open, and this amendment
  correctly says it closes none of them. `GATE-01` and `GATE-03` remain required
  and are not addressed here; note `R-1`, which makes GATE-01's CON-002 box a
  precondition of the device-grant path working at all.
- Production credential/v2 allocation stays closed. Both Tier-1 source constants
  stay closed at the baselines (`cbcl-chat-path-b-gate:profile/0` returns
  `#(error tier-1-unapproved)`; no `cbcl-chat-credential-v2-gate` module exists).
- Human cryptography, migration and privacy review, independent CPace and
  endpoint vectors, both durability gates, the representative load report and its
  human operational acceptance, owner approval, and separate deployment
  confirmation all remain outstanding and are not substituted for by this review.
- SPEC-007 `REQ-812`, `REQ-809`, `REQ-814`/`CON-801` and the cbcl-pairing
  production gates are inherited hard stops that this review neither waives nor
  weakens.
