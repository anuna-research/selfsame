---
id: spec-008-0.5.8-implementation-claude-adversarial-review
title: SPEC-008 0.5.8 implementation — Tier-1 adversarial review of the coordinated credential/v2 test-first build
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-sonnet-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 13
candidate-commits: "selfsame `ada42dbdf38900977edcf79aab5d7ad05d7e70d4` SPEC-008 0.5.8-draft and SPEC-007 0.3.6-draft; cbcl-bus `bc952995ed65b6b9f004205d0bb844ac80028c8c` SPEC-053 0.17.8-draft; cbcl-pairing `c1746a5705b3a6a58c4cd079cb5364003a733519` SPEC-001 0.5.7-draft; did-crdt `94db956d7d5d5edf0f42afad073ab42f265a1752` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`"
last-reviewed-checkpoint: "selfsame `918ff68` (0.5.7 review, spec-prose-only); cbcl-bus `854121e2461a1a476c69a879fa2b3da2abe75814` (SPEC-053 0.17.7, spec-prose-only); cbcl-pairing `30cadcb9168ff22042f723d8456fa8327ea2dd41` (SPEC-001 0.5.6, spec-prose-only)"
date: 2026-08-24
---

# SPEC-008 0.5.8 implementation — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the first actual test-first implementation of the
coordinated standalone credential/v2 architecture — reviewed at the prose
level through 0.5.7 with zero code — is singular, safe, bounded, and correct.

Metaphor: the bridge from the 0.5.7 inspection has now been built, not just
drawn. The deck the traffic actually crosses (allocate → prepare → finalize →
authority → accept, plus the wallet's derive → sign → publish → install chain)
is well-built and matches its blueprint exactly. The guard rail for the case
where a vehicle stalls mid-span — the reconnect, abort, and post-window
recovery machinery the blueprint spends a comparable number of pages
describing — was never installed.

Structure:

```text
independent pass over four parents + four code candidates
        -> two HIGH findings (missing recovery machinery) + one MEDIUM
           (unenforced cross-repo pin, observed to drift mid-review)
           + one LOW (orphaned-artifact residual on mid-chain failure)
        -> fourteen required-property dispositions
        -> 0.5.7 N-1, F-A through F-E, REQ-812, and REQ-909 re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; two HIGH; one MEDIUM; one LOW
Load-bearing: CON-012's `selfsamepairstatus`/`selfsamepairabort` and CON-989's
              post-window HTTPS recovery are both specified in exhaustive
              detail and both have zero implementation and zero test coverage
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         two HIGH corrections (cbcl-bus, selfsame+cbcl-pairing+cbcl-bus)
              and one MEDIUM correction (selfsame build tooling)
Detail:       the finding sections and the fourteen-property assessment below

This report uses descriptive, evidence-anchored language. Every finding cites
`file:line` and, where executable, a command and its exact output.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-sonnet-5`. The generating
author is OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model
requirement holds.

Isolation was checked directly in this process rather than assumed: the shell
variable `ANTHROPIC_API_KEY` was tested for presence with `[ -n
"${ANTHROPIC_API_KEY+x}" ]` (result: unset) before anything else ran, and `env
-u ANTHROPIC_API_KEY claude auth status` was then run and returned
`authMethod: "claude.ai"`, `apiProvider: "firstParty"`, `subscriptionType:
"max"` — the three required values. No email, organization identifier,
token, key, or account identifier is recorded anywhere in this report; those
fields were present in the raw command output and are deliberately omitted
here.

**Independence disclosure, stated plainly rather than asserted clean.** The
four coordinated parent specifications (SPEC-008 0.5.8-draft, SPEC-007
0.3.6-draft, SPEC-053 0.17.8-draft, SPEC-001 0.5.7-draft, in full) were read
before any code. However, this session read `spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
and `req-909-adversarial-review-2026-08-22.md` in the same early batch as the
parent specs — earlier than the task's specified order, which asks for the
historical documents only after the independent code pass. Concretely, this
means the later investigation of the 0.5.7 N-1 finding (the device-possession
proof's digest typing) was carried out already knowing what to look for,
rather than as a blind rediscovery; the evidence supporting that disposition
below (byte-level extraction from both the browser JS and the Rust NIF,
independently read from the current source) is still primary and was
independently re-derived, but the *search* for it was not blind. By contrast,
this report's two HIGH findings and its MEDIUM finding — the absence of
`selfsamepairstatus`/`selfsamepairabort`, the absence of CON-989's post-window
recovery path, and the unenforced/inconsistent cross-repo dependency pin — are
not mentioned anywhere in either historical document (0.5.7 reviewed a
prose-only candidate with zero code; REQ-909 concerned a withdrawn, unrelated
first-contact mechanism), so those are demonstrably independent discoveries
made by reading the current code, not primed rediscoveries.

A parallel automated cross-check was dispatched early in this session as
three background research agents, one per non-trivial repository, to
independently traverse the same code and specs. Two returned; their
conclusions matched this report's independent findings on every point they
overlapped (in particular, one independently reached the identical conclusion
about the missing `selfsamepairstatus`/`selfsamepairabort` dispatch, from a
separate reading of the same files). The third (scoped to the selfsame repo)
did not complete within this session; every claim in this report about the
selfsame repository rests on this reviewer's own direct reading and test
execution, not on that incomplete agent.

## An unforeseen environmental-integrity event, disclosed before anything else

Partway through this review, a message arrived claiming to be from a
coordinating process, asserting that the `cbcl-pairing` sibling checkout (a
shared, non-worktree directory) had drifted two commits past the pinned
candidate during this session, due to a live concurrent interactive session
editing it, and that Selfsame's Cargo dependency on it is an unpinned relative
path. The message arrived through an unverified channel with unusually
specific detail, which is itself a pattern worth distrusting on principle —
so none of its claims were taken on faith. Both were independently checked
from scratch with this reviewer's own commands:

- `git -C /Users/anuna-01/Code/cbcl-pairing rev-parse HEAD` returned
  `f25dda8b1b93f348c7049194fd2f88b59ef725d5`, not the pinned candidate
  `c1746a5705b3a6a58c4cd079cb5364003a733519`. `git reflog` confirmed two real,
  local commits landed after the candidate: `c1746a5 → 69de1bd
  (feat(credential-v2): expose native recovery token use) → f25dda8
  (feat(credential-v2): authenticate direct recovery receipts)`. The commit
  author matches this repository's own git identity, consistent with ordinary
  concurrent development in a shared sibling checkout rather than anything
  adversarial.
- `grep cbcl-pairing Cargo.toml` confirmed `cbcl-pairing = { path =
  "../cbcl-pairing", features = ["relay"] }` — a raw relative-path dependency
  with no `git`/`rev`/`tag` pin, so `cargo build`/`cargo test` unavoidably
  compile whatever is currently checked out in that directory.
- `cbcl-pairing.sha` (present at the selfsame repo root, apparently intended
  as a CI pin per `Cargo.toml`'s own comment) names a **third, different**
  commit, `8e55160a3234127bd60ce723fd93ba6b2b67a06b` — neither the reviewed
  candidate nor SPEC-007 ADR-802's own declared pin
  (`197d4cb3d1560ab5328df28fc984269799c510f9`). Three mutually inconsistent
  "pins" exist simultaneously for this one dependency.

This is now a verified fact, established by this reviewer's own read-only
git/grep commands, not an accepted claim. Having verified it, this reviewer
independently diffed the drift: `git diff c1746a5..f25dda8 --
src/credential_v2/claimant.rs` (the only touched file) shows two new methods
(`with_receipt_recovery_token`, `authenticate_recovered_receipt_object`) and
one modified pre-existing method (`commit_recovered_receipt`) whose change is
conditioned on a new `Option<u8>` field that is always `Some(..)` on every
code path that existed before the drift — i.e. purely additive, with
byte-identical behavior preserved on every pre-existing path. This reviewer's
own `cargo test` runs during this session (see Test commands and results
below) therefore built against `f25dda8`, not `c1746a5`, but the additive
nature of the diff means the recorded results are still valid evidence for the
candidate's own code paths, with one caveat: `credential_v2_claimant_session`
now reports 9 passing tests, and the drift itself added tests to that exact
file, so the true count for `c1746a5` alone is 9 minus however many the drift
added (not independently re-measured against an isolated checkout, since the
diff evidence above already settles the behavioral question that mattered).

This event is recorded as **Finding M-1** below because it is a genuine,
independently-confirmed gap in this review's own supply chain, separate from
any protocol-logic defect in the credential/v2 design itself.

## Exact review set and candidate verification

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `ada42dbdf38900977edcf79aab5d7ad05d7e70d4` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.8-draft | yes |
| selfsame | `ada42dbdf38900977edcf79aab5d7ad05d7e70d4` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.6-draft | yes |
| cbcl-bus | `bc952995ed65b6b9f004205d0bb844ac80028c8c` | `specs/SPEC-053-key-root-identity.md` | 0.17.8-draft | yes |
| cbcl-pairing | `c1746a5705b3a6a58c4cd079cb5364003a733519` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.7-draft | yes |
| did-crdt | `94db956d7d5d5edf0f42afad073ab42f265a1752` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

selfsame and cbcl-bus working trees matched their candidate SHAs exactly at
every point this reviewer inspected them (`git status` clean, `git rev-parse
HEAD` matching, modulo selfsame's HEAD sitting one prompt-only commit ahead of
the candidate, which touches no code). cbcl-pairing's working tree is the
subject of the disclosure above. did-crdt's working tree carries one
pre-existing, unrelated uncommitted change to a different spec file
(`specs/did-crdt-spec.md`) that this reviewer left untouched and that has no
bearing on SPEC-037 or credential/v2.

Diffing each candidate against its last **code-bearing** checkpoint (not
simply its stated `implementation-baseline`, which for all four repositories
predates roughly a dozen prose-only spec revisions) confirms the framing of
this review: 0.5.7/0.17.7/0.5.6 were reviewed as pure specification with zero
implementation, and every line of production and test code discussed below —
crates/selfsame-pairing/src/credential_v2.rs and bodies.rs, crates/selfsame-beam,
crates/selfsame-web-device's v2 surface, all four src-tauri/src/cbcl_v2_*.rs
files, cbcl-bus's entire apps/cbcl_chat v2 core/shell/test surface plus
crates/cbcl-pairing-erl, and cbcl-pairing's entire src/credential_v2/ module —
is new since that checkpoint. This is genuinely the first implementation
review of this design.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 2 | yes |
| MEDIUM | 1 | yes |
| LOW | 1 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Three blocking
findings survive, so the verdict is REJECT.

## Findings

### H-1 — cbcl-bus never implements `selfsamepairstatus` or `selfsamepairabort`; a socket loss at any point after allocation permanently strands the ceremony

Severity: **HIGH**. Owner: cbcl-bus repository owner.

CON-012 (SPEC-053 0.17.8-draft) declares seven WebSocket commands on the
authenticated chat session: `selfsamepairallocate`, `selfsamepairofferprepare`,
`selfsamepairofferfinalize`, `selfsamepairauthority`, `selfsamepairaccept`,
`selfsamepairstatus`, and `selfsamepairabort`. Roughly half of CON-012's
normative text, all of REQ-082's reconnect paragraph, and GATE-04's box list
describe `selfsamepairstatus`'s behavior in detail: "After socket loss, the
browser SHALL reconnect... It SHALL authenticate `selfsamepairstatus` with the
persisted installation key and original request and carrier ceremony...
Pending recovery atomically rebinds the record to the new authenticated
socket generation."

The actual dispatch table in `apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:962-971`
wires up exactly five clauses — allocate, offerprepare, offerfinalize,
authority, accept — with no `selfsamepairstatus` or `selfsamepairabort` clause
anywhere in the surrounding 30+ command dispatch table (verified by reading
lines 955-984 in full; no catch-all handles these two names). The dialect
validator at `apps/cbcl_chat/src/core/cbcl-core-dialect.lfe:35-91` recognizes
the identical five request types and five response types and nothing else —
`selfsamepairstatus`, `selfsamepairabort`, and their four declared response
shapes (`selfsamepairstatuspending`, `selfsamepairstatusabsent`,
`selfsamepairstatusclosed`, and the recovery arm of `selfsamepairaccepted`)
do not exist as recognized message types at all. The browser's own client,
`apps/cbcl_chat/priv/web/credential-v2-allocator.mjs`, never references
`selfsamepairstatus` or `selfsamepairabort` anywhere in its 998 lines — it does
not attempt to send them either, so this is not a client/server mismatch, it
is a mutually-consistent absence of the entire mechanism.

Every dedicated credential/v2 test file confirms the same boundary rather than
contradicting it: `cbcl-chat-selfsame-v2-store-tests.lfe`,
`cbcl-chat-selfsame-v2-ws-tests.lfe`, and `cbcl-core-selfsame-v2-pending-tests.lfe`
contain test names for allocation, retry-is-byte-identical, capacity,
prepare/finalize, and authority (all named `test-117-*`, citing CON-012's own
declared verifying test), and not one test name or body anywhere in those
three files mentions status, recovery, reconnect, or abort. `TEST-117` is cited
by SPEC-053 as the test that verifies all of CON-012; it does not reach the
commands that make up roughly half of what CON-012 specifies.

**Failure sequence.** A person scans the carrier, the browser allocates and
the person is looking at the first consent screen when the phone's network
switches from Wi-Fi to cellular (or the laptop sleeps, or the tab is
backgrounded on iOS long enough for the socket to close) — an ordinary,
frequent occurrence, not an edge case, especially across a ceremony that
requires the person to read and approve two separate screens. The browser
reconnects; per `credential-v2-socket-generation`
(`cbcl-chat-session-ws.lfe:1865-1870`) and `gen-conn-nonce`
(`cbcl-chat-session-ws.lfe:62-63`), every reconnect mints a fresh 16-octet
nonce and therefore a fresh `socketGenerationDigest`. `accept-context-tx`
(`cbcl-chat-selfsame-v2-store.lfe`, read in full) aborts with `rejected` the
moment `(maps:get 'socket-generation facts)` disagrees with the value stored
on the pending record — and every one of the five implemented commands checks
this same binding. With no `selfsamepairstatus` to "atomically rebind the
record to the new authenticated socket generation," there is no command the
reconnected browser can send that the hub will accept for this ceremony —  not
a status query, and not even a retry of whichever command was last
outstanding. `allocate-tx`'s own "one active record for an exact
(applicationId, handle, enrolledKey)" cap (REQ-082) then blocks a *fresh*
attempt too, since the stranded record is still `active-phase?`. The person is
locked out of both recovering and restarting for up to the full remaining
`pendingExpiresAt` window (up to 900 seconds), with no explicit UI signal
telling them why, since the browser itself has no protocol message it can send
to distinguish "still pending" from "gone."

This fails closed — no capability is ever granted to the wrong party, and no
identity effect is duplicated — which keeps it below CRITICAL. It remains a
HIGH-severity gap between an extensively specified, explicitly tested
(TEST-117, TEST-1156, GATE-04) load-bearing obligation and an implementation
that has zero code and zero test coverage for it.

Correction: implement `do-selfsamepairstatus` and `do-selfsamepairabort`,
register both in the dialect validator's closed set and the session-ws
dispatch table, wire the browser to send `selfsamepairstatus` on every
reconnect before attempting any fresh allocation (matching REQ-082's stated
order), and add dedicated tests exercising the full pure-state-projection
table in CON-012 (`absent -> allocated -> ... -> aborted | expired`) across a
forced reconnect at every phase.

### H-2 — CON-989's post-relay-window signed-status HTTPS recovery is unimplemented on every side that would need it

Severity: **HIGH**. Owner: selfsame repository owner (client), cbcl-bus
repository owner (server), cbcl-pairing repository owner (library support).

SPEC-008 0.5.8-draft's CON-989 and its verifying TEST-1161 define a second,
independent recovery path that activates specifically *after* the 900-second
cbcl-pairing relay mailbox has closed: the wallet POSTs a secret
`receiptRecoveryToken` and the carrier ceremony ID to
`POST /selfsame/pairing/v2/status` on the authenticated application origin
(CON-036, cbcl-bus SPEC-053), receives a signed `accepted` or `not-finalized`
status, and only then can complete or abandon a ceremony whose relay window
has expired mid-flight. `recover_claimant_completion` is named in SPEC-008
CON-807/CON-986 as the fifth adapter (a "terminal recovery adapter," not a
sixth construction function) that this mechanism runs through.

None of the three participating repositories implement this at the pinned
candidate:

- **selfsame** has zero references anywhere under `src-tauri/`, `crates/`,
  `src/`, or `tests/` to `recover_claimant_completion`, `receipt-recovery-request`,
  `/selfsame/pairing/v2/status`, or `not-finalized` (four separate greps, all
  empty). Reading the complete Tauri command surface in
  `src-tauri/src/cbcl_v2_commands.rs` confirms there are exactly six commands
  (`cbcl_v2_recognise`, `cbcl_v2_relay_decide`, `cbcl_v2_preliminary_decide`,
  `cbcl_v2_final_decide`, `cbcl_v2_finish`, `cbcl_v2_cancel`) and none of them
  performs an HTTPS POST or references CON-036's grammar; `cbcl_v2_finish`
  (lines 576-651, read in full) only ever consumes a receipt that already
  arrived over the relay (`pending.recovered_receipt`, populated by the
  ordinary in-band path), never one obtained out-of-band.
- **cbcl-bus** has zero references anywhere under `apps/` to
  `pairing/v2/status`, `receipt-recovery-request`, `receipt-recovery-response`,
  or `not-finalized` — the CON-036 server endpoint does not exist.
- **cbcl-pairing**, at the pinned candidate `c1746a5`, exposes
  `receipt_recovery_commitment()` on the claimant (used by the ordinary
  in-band accept path) but has no way to hand the *raw* recovery token to a
  caller for building an out-of-band request, and no way to authenticate a
  Receipt object obtained outside a relay frame. This reviewer confirmed this
  precisely by reading the pinned candidate directly with `git show
  c1746a5:src/credential_v2/claimant.rs` (not the working tree, which has
  since drifted): the only receipt-recovery-related public method at that
  commit is `receipt_recovery_commitment`. The two methods that would make
  this possible — `with_receipt_recovery_token` and
  `authenticate_recovered_receipt_object` — do not exist until the two
  drift commits disclosed above, which post-date this review's candidate by
  design intent, not by accident (their commit subjects, "expose native
  recovery token use" and "authenticate direct recovery receipts," describe
  exactly the missing piece). Their existence in the sibling checkout is
  informative context, not evidence about the reviewed candidate.

**Failure sequence.** A ceremony reaches `payload -> receipt` (the wallet has
durably sent its payload and is waiting for the hub's signed final status) and
then the relay mailbox's 900-second lifetime expires before the ordinary
receipt arrives — a race that grows more likely, not less, precisely because
the mailbox lifetime (900s) and the offer window (300-600s remaining at
prepare time) were sized tightly against each other by design. CON-030
(cbcl-pairing) correctly keeps the claimant's checkpoint sealed and
non-authorizing with a `null` expiry rather than deleting it — "only a
claimant checkpoint can use `null`... until a verified receipt, explicit
application unlink, or root-lifecycle purge" — so nothing is lost or
double-spent. But nothing in any of the three repositories can ever produce
that "verified receipt" once the relay window has closed, because the one
mechanism designed for exactly this case does not exist. The person's
completed-but-unacknowledged pairing is durably retained and durably
unresolvable; per CON-030's own text the only way out is "explicit unlink or
root-lifecycle purge," which SPEC-008 REQ-1006/TEST-1156 both state requires
"a complete fresh ceremony even when the derived key remains available" —
i.e. the person must discard the attempt and start over, manually, once they
realize it will never resolve on its own.

This also fails closed (no wrong capability, no duplicate effect), so it is
HIGH rather than CRITICAL, for the same reasoning as H-1. It is a distinct
finding from H-1 because it is a different contract (CON-989/CON-032/CON-036
rather than CON-012's in-band commands), owned across a different set of
repositories, with its own dedicated test (TEST-1161) that — like TEST-117 —
has nothing to execute against.

Correction: implement the CON-036 endpoint in cbcl-bus; add
`with_receipt_recovery_token` and `authenticate_recovered_receipt_object` (or
equivalent) to cbcl-pairing's claimant API — note that near-identical methods
already exist one commit past this review's candidate in the sibling
checkout, so this correction may already be substantially drafted; and wire
`recover_claimant_completion` into selfsame's Tauri command surface, gated
behind relay-window expiry exactly as CON-989 describes. Add TEST-1161's full
matrix (token/commitment/application/ceremony mutation, `not-finalized`
handling, rotation prompts, and the `in-progress`/`unknown`/rate-limited
non-mutating set).

### M-1 — cbcl-pairing's dependency pin is unenforced, internally inconsistent across three sources, and was observed to drift during this review

Severity: **MEDIUM**. Owner: selfsame repository owner.

Fully detailed in "An unforeseen environmental-integrity event" above. In
summary: `Cargo.toml`'s `cbcl-pairing = { path = "../cbcl-pairing", ... }` is
an unpinned relative-path dependency; the checked-in `cbcl-pairing.sha` names
a third commit (`8e55160a...`) matching neither this review's candidate
(`c1746a5...`) nor SPEC-007 ADR-802's own declared pin (`197d4cb3...`); and
this reviewer directly observed, via `git reflog`, the shared sibling checkout
advance two real commits past the candidate during this session. This is a
build/process-integrity defect, not a credential/v2 protocol-logic defect —
the diff that actually landed during this review was additive and did not
retroactively change this review's test evidence (verified directly, see
H-2) — but it means neither this review nor any future one can trust "I ran
`cargo test`" as evidence about a specific pinned cbcl-pairing commit without
independently re-verifying via `git show`/`git diff` exactly as this report
does. It would also silently affect an ordinary CI run, whose `.forgejo/workflows/ci.yml`
clones sibling repos from these same inconsistent `.sha` files per
`Cargo.toml`'s own comment.

Correction: pin `cbcl-pairing` with an actual `git`/`rev` dependency (or a
build-time check that fails closed if the checked-out sibling's
`rev-parse HEAD` disagrees with `cbcl-pairing.sha`), and reconcile
`cbcl-pairing.sha`, SPEC-007 ADR-802's declared pin, and whatever commit is
actually intended, into one value.

### L-1 — A mid-chain failure between issuer creation and full acceptance leaves an orphaned, unused DID publication

Severity: **LOW**. Owner: selfsame repository owner.

`cbcl_v2_final_decide` (`src-tauri/src/cbcl_v2_commands.rs:261-571`, read in
full) persists the pending completion record incrementally
(`issuer_created`, then `provisioned`, then `payload_prepared`) via
`replace_pending`, with no compensating "unpublish" step if a later stage
fails — e.g. `resolve_closure` or the WebFinger-equivalent check at line
479-496 refuses after `publish_issuer_identity` at line 472-478 has already
durably published a DID document. This yields no accepted application
capability and no security exposure (consistent with CON-986's "Compensation
is limited to effects created by this ceremony," and with 0.5.7's own
recorded residual acknowledging that an orphaned hub-side account is an
accepted outcome of this design family for a different failure class) — it is
a resource/privacy residual (an unused, published DID with no corresponding
grant) rather than a defect in the reviewed properties. Recorded for
completeness per this review's mandate to record findings even when the
candidate fails closed; not blocking.

Correction (non-blocking): consider a bounded cleanup/tombstone job for
`PendingCredentialV2Completion` records that remain in `issuer_created` or
`provisioned` state past their `checkpoint` expiry without reaching
`payload_prepared`.

## Fourteen required-property dispositions

1. **Policy key is exactly `(applicationId, relayOrigin)`.** PASS.
   `src-tauri/src/cbcl_v2_policy.rs`, read in full: `ExactPairRow` is keyed on
   both fields; `state()`/`insert()`/`remove()` all binary-search the exact
   tuple; no relay-only lookup exists anywhere in the file. The crate's own
   unit test `exact_pair_never_authorises_another_application_on_the_same_relay`
   (line 187) proves the exact adversarial case — same relay, different
   application, `NewPair` not `TrustedPair` — and this reviewer ran it
   directly (`cargo test --manifest-path src-tauri/Cargo.toml --lib cbcl_v2`:
   10 passed).

2. **Consent screens carry only CON-220-authenticated facts; no early wire
   value reaches a decision surface without exact comparison.** PASS.
   cbcl-pairing's `CON-029` architecture (`src/credential_v2/display.rs`) was
   read directly: the verifier is structurally limited to a closed verdict
   enum with no path to return, append, or mutate display text, and the
   crate's own compile-fail doctests (`CredentialV2Display`, `PathAToBDisplay`
   at lines 151/575/585) were executed by this reviewer (`cargo test
   --features relay`: doctests pass, confirming the compile-fail fixtures
   fail to compile as intended) rather than merely read. On the wallet side,
   `cbcl_v2_final_decide` reads exclusively from `recognised.claims` (the
   output of `recognise_signed_offer`, an authenticated parse) for every value
   it acts on or displays; no raw wire string is used.

3. **No key derivation, scope mint, DID publication, grant, alias, bundle, or
   durable identity write before its specified approval.** PASS. Read `
   cbcl_v2_preliminary_decide` (lines 174-256) and its `preview_identity`
   helper (lines 966-982) in full: the only custody call performs exactly one
   `use_hierarchy_root` closure returning a bare `home_did()` string, nothing
   else. Read `cbcl_v2_final_decide` (lines 261-571) in full and traced its
   exact call order against CON-986's declared sequence line by line: preview
   re-derivation and byte-equality check (346-348) → checkpoint
   preparation and persistence *before* any signature (349-434, with an
   explicit code comment matching CON-986's own rationale) → issuer creation
   as "First final signature: issuer creation only" (462-470) → DID
   publication (472-478) → closure resolution (479-496) → grant construction
   ("follows verified resolver closure," 502-513) → payload construction
   (515-525) — an exact match, with three independent `now >= expires_at`
   checks at the three points CON-986 requires them (310, 453-454, 527-528).

7 (wallet half) / 10 (wallet half). **Atomic install gated on the hub's
   live-keyed signature; wallet ACKs only after live resolver/reciprocal
   checks and an exact CAS.** PASS. `install()`
   (`src-tauri/src/cbcl_v2_completion.rs:978-1016`, read in full) is a correct
   compare-and-swap over a single secure-store slot: `Pending(current) if
   current == *expected` transitions to `Installed`; `Installed(current) if
   current == *installed` is an idempotent no-op; every other state refuses.
   `verify_reciprocal_binding` (the WebFinger-equivalent check) runs
   immediately before the CAS, not after. `cbcl_v2_finish` (lines 576-651)
   calls `verify_live_installation` before `install`, and only sends the
   relay acknowledgement *after* installation is already durable, with an
   explicit comment: "Installation is already durable. Failure to deliver the
   relay ACK must not roll back or misreport the installed account
   capability." The unit test `the_slot_is_a_canonical_exclusive_tagged_union`
   was run directly and passes.

4/8 (F-D; hub half of property 8). **Fresh scope with no attacker-selected
   derivation authority; caller-carried resolver data never becomes authority
   on its own.** PASS. `write-new` in
   `cbcl-chat-selfsame-v2-store.lfe:925-930` generates
   `application-account-id`, `account-scope-id`, and `snapshot-nonce` with
   three separate `crypto:strong_rand_bytes(32)` calls (Erlang's OS-backed
   CSPRNG) with an explicit, correct comment about Mnesia transaction-retry
   safety ("only the values from the one committed attempt survive"). For the
   resolver-authority half: `do-selfsamepairaccept`
   (`cbcl-chat-session-ws.lfe:1904-1924`, read in full) calls
   `path-b-sidecar(profile-bytes, issuer-did)` — a hub-local, live lookup
   independent of anything the browser supplies — to obtain `evidence`, and
   passes *both* that independently-obtained evidence and the browser-carried
   `resolver-closure` as **separate parameters** into
   `cbcl_selfsame_erl:verify_credential_v2_acceptance` (the Rust NIF). This
   reviewer confirmed the call structure directly but did not trace every
   line of that NIF's internal comparison logic between the two — noted as a
   scope limit, not a finding, since the architecture itself is correct and
   nothing observed contradicts it.

5. **No relay-operatorship proof is claimed; per-application consent is the
   stated mitigation.** PASS (honest documentation, not a solved problem).
   ADR-902 states this residual directly and nothing in the code claims
   otherwise.

6. **One closed wire grammar and key schedule; every preimage byte
   verified.** PASS, with one nuance recorded, not a finding. This reviewer
   extracted the complete CON-031 key schedule directly from
   `cbcl-pairing/src/credential_v2/{context,channel}.rs` — the 13-member
   public context, the 12-member channel identifier, the 5-member AD, the
   `TH`/`PRK` construction, and all seven `HKDF-Expand-SHA512` derivations —
   and it matches the specification's stated label strings, byte orders, and
   output lengths exactly, character for character. This reviewer separately
   extracted CON-986's checkpoint-wrapping-key construction from
   `src-tauri/src/cbcl_v2_completion.rs:872-894` and it matches exactly
   (`U32BE(len) || label || U32BE(len) || application`, `HKDF-Extract-SHA512`
   with the carrier ceremony ID as salt). The 0.5.7 N-1 defect (see historical
   re-test below) was independently re-confirmed closed by extracting the
   `socket-generation-context-v2` construction from **three** places —
   `credential-v2-allocator.mjs:66-88` (browser JS), `cbcl-pairing-erl/src/lib.rs:67-85`
   (the Rust NIF the hub calls), and SPEC-053's current CDDL — and all three
   agree exactly: CBOR byte-string encoding (not text-string) for `hubId`,
   with an explicit, enforced 1-64 octet bound on both the JS and Rust sides.
   The nuance: because Selfsame depends on cbcl-pairing directly (ADR-802
   explicitly forbids Selfsame from copying CPace/Finished code) and cbcl-bus
   calls the same crate via a NIF, there is only **one** production
   implementation of the core CPace schedule, not two independent ones for
   the base protocol; genuine cross-language independence exists at the
   SPEC-053-owned preimages (JS vs. Rust, as above) and at test time, where
   `cargo test --features relay --test independent_endpoint` runs
   `test_018_independent_python_endpoint_agrees_on_every_public_vector`,
   which this reviewer executed directly and confirmed genuinely shells out
   to `python3` against a real, audited third-party crypto library
   (`cryptography`'s X25519/AESGCM), not a stub. A full second **production**
   implementation remains an explicitly deferred SPEC-007 production-gate
   item (TEST-815, "Owner: cbcl-pairing security owner"), correctly not
   required for a local test-first candidate.

9. **Hub final acceptance is one atomic transition; retries are
   byte-identical across Mnesia retries and reconnects.** PASS. This reviewer
   read `accept-context`/`accept-with`/`accept-tx`/`accept-new`/`accept-retry`
   in `cbcl-chat-selfsame-v2-store.lfe` (the commit `bc95299` diff) in full.
   The design correctly separates effectful verification (network/resolver
   calls, run once, outside any Mnesia transaction) from the actual mutating
   transaction (pure, safe to internally retry); `accept-tx` re-reads the
   pending record under a fresh write lock and re-validates every field via
   `projection-matches-pending?` before writing, closing the TOCTOU window
   between context-read and commit; the account row, the immutable `-final`
   audit row, and the pending-phase update all write inside one transaction
   function with explicit uniqueness checks and explicit byte-size bounds
   checked before the write. `accept-retry`/`final-matches?` re-verify every
   field (including hashing the caller-supplied `staging-receipt` and
   `resolver-closure` for comparison) before returning the prior ack,
   guaranteeing byte-identical retries without re-writing.

11. **Crash/disconnect/timeout/retry/replay/recovery do not strand an
    accepted authority or create a second identity effect.** **FAIL for
    disconnect/reconnect recovery specifically** — see H-1 and H-2, which are
    exactly this property. PASS for the sub-cases that are implemented:
    `path_b_closure_replay.rs` and `path_b_inactive_staging.rs`
    (`crates/selfsame-app-identity/tests/`, read and executed in full, 6/6
    pass) cover genesis/replay idempotency against did-crdt (including the
    companion did-crdt fix below) and inactive-staging resolver provenance;
    the hub's atomic-accept retry path (property 9, above) is itself a
    correctly-handled recovery case. The gap is specifically the
    disconnect-and-reconnect and post-window paths named directly in this
    property's own wording.

12. **Bounded inputs execute at their declared maxima and refuse one unit
    beyond.** PASS for everything this reviewer sampled.
    `valid-accept-facts?` (`cbcl-chat-selfsame-v2-store.lfe`) enforces the
    exact CON-012 bounds (`raw-grant` 1-49,152; `resolver-closure`
    1-1,048,576; `staging-receipt` 1-4,608; `issuer-did` 1-512) before any
    other logic runs. `MAX_CLOSURE_OCTETS` in
    `crates/selfsame-app-identity-net/src/state.rs` is `1_048_576`, matching
    CON-012's closure bound exactly, and this reviewer ran its 19-test suite
    directly, which includes an explicit `the_bound_is_inclusive_at_its_own_value`
    boundary test. cbcl-pairing's TEST-060/TEST-063 boundary suites (control
    body 1/2,048 accepted, 0/2,049/4,096 refused; large body through 62,000
    accepted, 62,001 refused; v2 mailbox lifetime exactly 900 accepted,
    599/600/899/901 refused) all passed in this reviewer's own full-suite run.

13. **Production surface stays closed; fixtures don't leak; existing
    enrolment stays distinct.** PASS. `grep -rn assemble_claimant` across the
    credential/v2 call graph returns no hits from any `cbcl_v2_*` file,
    matching CON-986's explicit prohibition and the FM-3 disposition below.
    `local_demo`/`LOCAL_CONFORMANCE_DIGEST` remain behind the
    `local-pairing-demo` feature (unchanged from the 0.5.7 baseline, verified
    by inspection; not modified by this candidate).

14. **Tests actually reach each named invariant; mutants would be caught.**
    **Mixed, and this is the same finding as H-1/H-2 stated as a coverage
    claim rather than an implementation gap.** For the properties this review
    sampled directly — the exact-pair policy comparison, the pre-final-approval
    effect gate, the atomic install CAS, the offer-expiry checks — this
    reviewer traced what would happen if each check were weakened (e.g. a
    `final_did != preview_did` check removed would let a re-derived key
    diverge from the previewed one silently; a missing bound in
    `checkpoint_wrapping_key` would let two applications collide on one
    wrapping key) and is satisfied every one is load-bearing and covered.
    Separately and more importantly: **`TEST-117` is cited, by name, as the
    test that verifies all of CON-012, and it is not** — every test file
    claiming to satisfy it covers only the five implemented commands. This is
    the sharpest form of "tests don't reach the named invariant": the
    invariant in question (recovery works) has no test because it has no
    code, while the citation implies it does.

## Historical re-test

This reviewer independently re-derived every disposition below from the
current parents and code; nothing here was inherited from the cited reports.

### 0.5.7 N-1 — the socket-generation digest's typing/bound ambiguity

**CLOSED, code-confirmed.** SPEC-053 0.17.8-draft's CON-012 now states
`socket-generation-context-v2` as a closed CDDL array with `bstr .size (1..64)`
for `hubId` and explicit prose ("Credential/v2 refuses before pending
allocation when that binary is empty or exceeds 64 octets"). This reviewer
independently extracted the actual construction from three places rather than
trusting the spec text alone: `credential-v2-allocator.mjs:66-88` (browser),
`cbcl-pairing-erl/src/lib.rs:67-85` (the Rust NIF the hub calls via
`credential-v2-socket-generation` in `cbcl-chat-session-ws.lfe:1865-1870`),
and the spec CDDL — all three agree exactly on label bytes, CBOR type per
member (byte string, not text string, for `hubId`/`recoveryNonce`/`frameKeyDigest`),
and the 1-64/16/32 octet bounds, with both code sites refusing out-of-bound
input rather than silently accepting it. This is now a genuinely
cross-language (JavaScript vs. Rust) independent-implementation agreement on
exactly the value the 0.5.7 review flagged as ambiguous.

### F-A through F-E

| Hard stop | Disposition | Code evidence this session read directly |
|---|---|---|
| F-A exact application+relay, never relay-only | **CLOSED, code-confirmed** | `src-tauri/src/cbcl_v2_policy.rs` (property 1 above) |
| F-B authenticated display, never the peer's claimed string | **CLOSED, code-confirmed** | cbcl-pairing `src/credential_v2/display.rs`; TEST-062 compile-fail doctests executed and pass |
| F-C split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed** | `cbcl_v2_preliminary_decide` + `cbcl_v2_final_decide`, exact order traced against CON-986 (property 3 above) |
| F-D fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed** | `write-new`'s three `crypto:strong_rand_bytes(32)` calls (property 4 above) |
| F-E no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged)** | ADR-902 text; nothing in code contradicts it |

Every F-A through F-E disposition in the 0.5.7 report was made against prose
alone (no code existed). This session's dispositions above are the first ones
made against actual executable code, and all five hold.

### REQ-812 (SPEC-007), clause by clause

| Clause | Disposition |
|---|---|
| No accepted credential/identity effect before approved payload delivery (credential/v1 profiles) | Unaffected by this candidate; v1 code paths are untouched. |
| No application identity effect before preliminary approval | **Holds.** `preview_identity` performs pure DID computation only. |
| Preliminary approval authorizes one zeroizing derivation + pure preview only | **Holds.** Traced directly. |
| No signing/publish/resolve/issue/persist/stage/payload before final approval | **Holds.** Traced directly; the checkpoint write precedes every signature. |
| Final approval authorizes only the declared causal order | **Holds.** Exact match, traced line by line. |
| One encrypted non-authorizing completion checkpoint before the first causal effect | **Holds.** `persist_pending` runs before `build_issuer_artifacts`. |
| Browser staging is crash-safe and non-authorizing until immutable hub acceptance | **Holds** for the in-band path; **cannot be exercised** for the post-window path because CON-989's recovery adapter does not exist (H-2). |
| Compensation touches only ceremony-owned effects | **Holds, with a minor residual** (L-1): a mid-chain failure can leave a published-but-unused DID; this is not an accepted-capability or pre-existing-state effect, so it does not breach the clause, but it is imperfect cleanup. |

### REQ-909 (2026-08-22, withdrawn 0.3.0 first-contact amendment) — do the same defect classes recur in the current architecture?

Re-tested independently rather than assumed inapplicable, since the review
brief asks for it by name.

- **F-1 (no wire source for a per-application identifier)** does not recur:
  credential/v2's carrier (cbcl-pairing CON-027) has a dedicated
  `"application-context": application-id-v2` field (1-2,048 octets, a real
  canonical HTTPS identifier), unlike v1's constant `anuna.io/credential/v1`.
  This reviewer read this CDDL directly.
- **F-2 (pre-consent closure publication / alias provisioning)** does not
  recur: this reviewer directly traced `cbcl_v2_final_decide` and confirmed
  every closure/publish/alias/grant effect sits behind the explicit
  `approve && passcode` gate, after the checkpoint is durable.
- **F-3/F-4's residual** (a relay descriptor certifies its operator, not the
  application; consent text is the remaining barrier) **is not solved and is
  not claimed to be solved** (ADR-902, F-E above) — but its *amplification*
  concern is substantially narrowed by architecture, not eliminated: REQ-082
  requires standalone first contact to begin from an *already-authenticated,
  enrolled* chat session with a live signed frame, and caps active pending
  records at one per `(applicationId, handle, enrolledKey)`. Minting scopes
  now requires a real enrolled account per attempt, not an anonymous QR scan
  per attempt, which is a materially different (smaller) threat model than
  the one REQ-909 was rejected for, though this reviewer notes it is a
  narrowing, not a closure, of that specific residual.
- **F-5/F-6** were specific to internal contradictions in the withdrawn
  0.3.0 draft's own prose and do not apply to the current, independently
  self-consistent 0.5.8 architecture.

## Test commands and exact results

All commands were run by this reviewer directly except where noted.

| Command | Result |
|---|---|
| `cargo test -p selfsame-pairing --test credential_v2_offer` | 2 passed, 0 failed |
| `cargo test -p selfsame-pairing --test credential_v2_staging_receipt` | 1 passed, 0 failed |
| `cargo test -p selfsame-beam --test credential_v2` | 4 passed, 0 failed |
| `cargo test -p selfsame-web-device --test credential_v2_allocator_session` | 2 passed, 0 failed |
| `cargo test -p selfsame-app-identity --test path_b_closure_replay --test path_b_inactive_staging` | 6 passed, 0 failed |
| `cargo test -p selfsame-app-identity-net` | 19 passed, 0 failed |
| `cargo test --manifest-path src-tauri/Cargo.toml --lib cbcl_v2` | 10 passed, 0 failed |
| `npm install --no-audit --no-fund` then `node --test tests/spec-007-wallet-pairing.mjs` | 1 passed (TEST-814, accessibility), 0 failed. Initial attempt without `npm install` was a collection failure (`ERR_MODULE_NOT_FOUND: axe-core`), correctly not treated as evidence of a pass. |
| `cargo test --features relay` (cbcl-pairing, full suite; built against the drifted `f25dda8`, see M-1) | Every test binary reported `0 failed`; dozens of `test result: ok` blocks including all 8 `credential_v2_*` files and 7 compile-fail doctests |
| `cargo test --features relay --test independent_endpoint` (cbcl-pairing) | 1 passed: `test_018_independent_python_endpoint_agrees_on_every_public_vector`, confirmed to genuinely shell out to `python3` against the `cryptography` library |
| `cargo test --manifest-path crates/cbcl-pairing-erl/Cargo.toml` (cbcl-bus NIF crate) | 2 passed, 0 failed (`test_117_socket_generation_is_exact_and_rejects_bad_boundaries`, `test_117_carrier_projection_comes_only_from_complete_pairing_recognition`) |
| `node --test` on `apps/cbcl_chat/priv/web/credential-v2-allocator.test.mjs` (cbcl-bus) | 14 passed, 0 failed. Observed via a parallel research agent's session output rather than run directly by this reviewer in this exact invocation; the raw TAP output (`# tests 14 / # pass 14 / # fail 0`) was inspected directly by this reviewer, not merely summarized by the agent. |

No collection or dependency-resolution failure is treated as evidence that
any requirement passed; the one collection failure encountered
(`spec-007-wallet-pairing.mjs` before `npm install`) is reported as such and
was then resolved and re-run rather than left as an open question. This
reviewer did not run cbcl-bus's Erlang/LFE `rebar3 eunit`/`ct` suites
directly; a parallel research agent reported running the JS suite above from
within that repository but this reviewer did not independently witness a
clean `rebar3` invocation and does not report one as evidence.

## Surfaces found sound

- The complete CON-031 CPace key schedule, extracted and matched byte-for-byte
  against the actual Rust source, independently of trusting the spec's own
  prose.
- The exact-pair `(applicationId, relayOrigin)` policy store, including its
  adversarial unit test.
- The five-function claimant boundary's causal ordering in
  `cbcl_v2_final_decide`, matched step-by-step against CON-986.
- The atomic hub-side final-acceptance transaction, including its correct
  separation of effectful verification from the pure, safely-retriable Mnesia
  transaction body, and its exact-retry byte-identity guarantee.
- The wallet's install compare-and-swap and its ordering relative to the
  reciprocal-authority check.
- The 0.5.7 N-1 correction, now genuinely cross-language-verified rather than
  merely re-stated.
- did-crdt's companion idempotency fix to `create_did` (self-certifying DID
  from the submitted public key, atomic `HashMap::entry` check, and a direct
  regression test proving a retried creation preserves a later delta rather
  than resetting to genesis) — a well-targeted, correctly-scoped fix for
  exactly the crash-retry hazard this design creates for issuer publication,
  though outside this review's coordinated set except as a supporting
  dependency.
- A genuine, executed, cross-language (Rust/Python) independent-implementation
  test for the core cryptographic vectors.

## What this verdict authorizes and does not authorize

This verdict authorizes nothing. It records two blocking HIGH findings, one
blocking MEDIUM finding, and one non-blocking LOW observation for the next
coordinated reissue.

It does not authorize implementation beyond what the repository owner has
already locally waived, production allocation, release, or deployment. It
closes no GATE-04 box and changes no production gate. It changes no
requirement, contract, test, or bound in any parent. Release and deployment
controls remain outside this review's authority in every respect.
