---
id: spec-008-0.5.11-implementation-reviewer-prompt-2026-08-24
title: SPEC-008 0.5.11 implementation — Tier-1 adversarial review prompt
status: active
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-24
---

# SPEC-008 0.5.11 implementation — Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
approve work because it is extensive or because its author reports green test
suites. Review the complete coordinated standalone credential/v2
implementation, including the 0.5.11 remediation. Do not repair the candidate.
Write only the review report, commit it in the Circus worktree, and complete
through the appended Circus instruction.

## Authentication and isolation

This review runs through Circus with the interactive Claude CLI so it uses the
person's Claude Max subscription. The API-billed `circus-driver-claude` SHALL
NOT be invoked. The parent and child environments remove `ANTHROPIC_API_KEY`
and `ANTHROPIC_AUTH_TOKEN`. Check that both variables are absent without
printing values. Run `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude
auth status` and record only:

- `authMethod`
- `apiProvider`
- `subscriptionType`

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier. A different result
is a review-process blocker. The generating family is OpenAI GPT-5; the
reviewing family is Anthropic Claude. This session receives no generating-
session reasoning or transcript.

## Exact candidate set

Use `git show COMMIT:path` and `git diff BASELINE..COMMIT`. Do not substitute
uncommitted files. The commits below are the candidate even though the Circus
integration ref also contains this prompt.

- Selfsame candidate `0c5801259802b767bd8f9230ff6cd32dff6fffa5`
  - pre-0.5.11 implementation candidate
    `f054bad1840ffb5ce3840bba87c79a8f21bd3740`
  - original implementation baseline
    `0220cec2dec44cd95d4f411ea4814d790b6716d2`
  - governing parents:
    `specs/SPEC-008-production-pairing-claimant.md` and
    `specs/SPEC-007-cbcl-pairing-cutover.md`
- cbcl-bus candidate `965d6cc44cfdf0f8d00a562bb587b7785396ee97`
  - pre-0.5.11 implementation candidate
    `bc1cf5d23029e36543df37d32f3234a2f6bb20c5`
  - original implementation baseline
    `ce894a7f2debc111b610296b1a3780de6084e37e`
  - governing parent: `specs/SPEC-053-key-root-identity.md`
- cbcl-pairing candidate
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
  - pre-0.5.11 implementation candidate
    `3d013306cab68c9e258e149387e420058b57e80f`
  - original implementation baseline
    `c22f2d0526432c01c56d64be35a8d4f87516ec92`
  - governing parent: `specs/SPEC-001-reusable-blind-pairing.md`
- did-crdt service repository HEAD
  `94db956d7d5d5edf0f42afad073ab42f265a1752`
  - dependency revision pinned into the coordinated runtime build:
    `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
  - determine from code whether service-only changes after the pin participate
    in any production credential/v2 runtime; do not assume they do
- cbcl-rs dependency revision
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
  - inspect only the consumed core/parser surfaces and pin closure; unrelated
    later or uncommitted work is outside the candidate

The owner explicitly authorized test-first implementation before review PASS.
Production release, public allocation, pushes, and deployment remain
prohibited. A PASS does not relax those controls.

## Required independent pass

Read the four coordinated parent specifications, the complete candidate diffs,
and the executable browser, hub, pairing-library, and wallet paths before
opening an earlier review. Treat prose, test names, and this prompt's claims as
untrusted until code or a runnable test supports them.

Check at least these properties:

1. Relay consent is keyed exactly by `(applicationId, relayOrigin)`, and all
   displayed application facts come from the live CON-220-authenticated profile
   rather than peer-controlled strings.
2. No application derivation, scope mint, publication, grant, alias, bundle, or
   durable identity effect occurs before its specified person approval. Fresh
   first-contact scope has authenticated, non-attacker-selected authority.
3. The design claims no relay-operator proof. Exact per-application consent is
   the explicit mitigation when a profile names another operator's relay.
4. CPace credential/v2 has one closed wire grammar and key schedule. Verify
   cryptographic preimages, Finished order, and checkpoint-key separation.
5. Browser installation-device possession is proved before offer signing, with
   exact socket-generation typing, bounds, and replay resistance.
6. Resolver-closure replay cannot become resolver authority. Hub and wallet
   independently obtain and compare live authority.
7. Hub final acceptance is atomic and retries remain byte-identical. Browser
   activation follows signed final acceptance; wallet installation follows the
   authenticated receipt, live reciprocal binding, and durable compare-and-swap.
8. Browser restart works at every pre-terminal checkpoint. A restored allocator
   authenticates the replacement chat socket, queries hub status, reopens the
   exact blind-relay membership, and resends only an outstanding exact cached
   frame. Drive all four bootstrap checkpoints, including acknowledged
   `FinishedSent`, which must reopen with `Open` alone.
9. A wallet failure after durable final approval but before durable
   `PayloadPrepared` cannot strand an application slot. Cleanup is armed with no
   post-persist gap, preserves the original error, handles committed-but-
   reported-failed replacement writes, and never deletes a real
   `PayloadPrepared` recovery slot. A fresh same-application ceremony can start.
10. Every pending phase is visible as non-secret local state and has a confirmed,
    current-root-presence-gated local abandonment path. That path removes only
    the exact pending slot, exact `(applicationId, relayOrigin)` policy, and
    slot-owned cache; it preserves the hierarchy root, sibling applications,
    and remote records and claims no remote revocation.
11. `selfsamepairstatus` and `selfsamepairabort` have closed grammars, current-
    generation authentication, exact binding, bounded state, and no unauthorized
    advance. Explicit cancel and transport failure are not confused.
12. Post-relay-window HTTPS recovery authenticates recovery token, application,
    ceremony, offer, receipt commitment, live profile, and authority rotation.
    It does not derive, publish, issue, resend payload, or install twice. HTTP
    remains POST-only, CBOR-only, bounded, non-redirecting, uncacheable, and
    rate-limited before parsing or lookup; limiter failure fails closed.
13. Recovery commitment binding is server-owned: first correct use is bound
    atomically under the pending-row write lock; wrong or absent commitments are
    indistinguishable from unknown; signed `not-finalized` never echoes an
    unauthenticated caller value. Check pending and finalized retries.
14. Expiry is sampled and enforced at the mutating accept transaction's
    linearization point after the row lock. A verifier delay crossing expiry
    cannot project a witness or create account, migration, WebFinger, final, or
    other durable acceptance effects. Accepted retries retain their specified
    idempotent behavior.
15. Every restored display and decision authority survives checkpointing
    without trusting duplicated JavaScript state or admitting oversized state.
16. Installed-link reload verifies the stored grant plus current profile,
    resolver authority, issuer, reciprocal WebFinger binding, and hub status
    before exposing capability. Profile-only refresh is atomic; authority or
    issuer rotation prompts; handle change refuses; unavailable and revoked are
    distinct and do not silently erase the record. An oversized bounded
    WebFinger response is unavailable, not revoked.
17. Confirmed unlink applies to pending and installed slots and removes only the
    selected application slot, its exact `(applicationId, relayOrigin)` policy,
    and application-owned cached material. It neither purges the hierarchy root
    nor claims remote revocation. Re-grant after hub deletion requires a complete
    fresh ceremony.
18. Dependency pins name one exact, buildable closure across Selfsame, cbcl-bus,
    [[SPEC-007-cbcl-pairing-cutover#ADR-802]], CI, Android, tests, and WASM
    provenance. The explicit witness checks HEAD and tracked status for all
    three siblings (`cbcl-pairing`, `cbcl-rs`, and `did-crdt`) even when the
    labelled development override is set. The vendored WASM rebuilds
    byte-identically from clean exact pins and exposes the restored allocator ABI.
19. Crash, disconnect, timeout, replay, wrong order, cancel, expiry, and recovery
    cannot create a second identity effect or accepted authority. Evaluate the
    known orphan-DID residual separately and decide whether it is blocking.
20. Bounds and one-over-limit refusals are executable. The ordinary production
    entry remains closed; loopback staging and existing enrolment remain
    distinct.
21. Tests reach the named invariants. Mutate load-bearing predicates mentally or
    experimentally and identify surviving mutants, especially acknowledged-
    Finished reopen, every post-persist/pre-payload failure edge, pending
    abandonment, installed-link verification/unlink, recovery binding, expiry
    linearization, browser durable-state recognition, phase reconciliation,
    rate limiting, oversized reload classification, and all sibling pin guards.

Run focused suites and narrower adversarial tests as needed. Record exact
commands and outcomes. A collection failure or dependency-resolution failure
is not evidence that a requirement passed. Use clean detached clones or
worktrees for strict source-closure builds. Do not modify a candidate repository
or read its uncommitted files as candidate evidence.

The generating agent reports these results; independently confirm enough of
them to establish whether they exercise the claims:

- cbcl-pairing `cargo test --workspace --locked`: PASS
- exact clean cbcl-bus `rebar3 eunit`: 975 PASS
- cbcl-bus complete browser test set: PASS
- Selfsame focused post-persist cleanup and fresh-same-application regression:
  PASS
- Selfsame wallet browser pending-visibility/abandonment regression: PASS
- clean exact-pin `selfsame-pairing` dependency test: PASS without override
- dependency witness deliberately fails against a tracked-dirty sibling even
  with the labelled override
- exact-pin WASM rebuild: byte-identical, SHA-256
  `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`
- toolchain: rustc/cargo 1.96.0, wasm-bindgen 0.2.126,
  aarch64-apple-darwin

## Prior-review re-test

Only after the independent pass, read:

- `specs/trajectory/SPEC-008/spec-008-0.5.10-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.9-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`

Independently re-test every 0.5.10 finding: H-1 acknowledged-Finished reopen,
H-2 pending-slot cleanup and abandonment, L-1 orphan DID publication, L-2
reverse-proxy limiter scope, L-3 all-sibling pin witness, and L-4 oversized
WebFinger classification. Also re-test every named 0.5.9, 0.5.8, and 0.5.7
finding, F-A through F-E, and [[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Do not
inherit any disposition.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.11-implementation-claude-adversarial-review-2026-08-24.md`.
The report SHALL contain:

- reviewer identity, subscription auth path, Circus attempt, exact commits, and
  independence statement;
- verdict `PASS` or `REJECT` and severity counts;
- every finding with severity, code evidence, exploit or failure sequence,
  affected fully-qualified requirement, and measurable correction;
- explicit dispositions for all twenty-one required checks;
- explicit dispositions for every named 0.5.10, 0.5.9, 0.5.8, 0.5.7, F-A
  through F-E, and [[SPEC-007-cbcl-pairing-cutover#REQ-812]] item;
- exact test commands and results;
- a statement that PASS does not authorize release, push, or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. A load-bearing missing
obligation is a finding even if the candidate fails closed. LOW residuals may
remain only when their consequence and operational treatment are explicit.
Release and deployment controls are outside this review's authority.
