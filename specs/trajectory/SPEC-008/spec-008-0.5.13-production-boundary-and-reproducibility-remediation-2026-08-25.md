---
id: spec-008-0.5.13-production-boundary-and-reproducibility-remediation-2026-08-25
title: SPEC-008 0.5.13 — production-boundary and reproducibility remediation
status: owner-authorized-test-first-candidate
tier: 1
date: 2026-08-25
parent: "[[SPEC-008-production-pairing-claimant]]"
review-of: "[[spec-008-0.5.12-implementation-claude-adversarial-review-2026-08-24#Findings]]"
---

# SPEC-008 0.5.13 — production-boundary and reproducibility remediation

## Decision and authority

The repository owner authorized test-first implementation before review PASS
and authorized the consolidated reissue. The selected architecture remains
standalone first-contact credential/v2 pairing through cbcl-pairing, live
application authentication, and person-owned exact-pair TOFU. This correction
does not reintroduce enrolment or a compiled relay allowlist.

The 0.5.12 fresh-context review found zero CRITICAL and zero HIGH defects. It
found no exploit in the shipped consent, binding, effect-order, or installation
predicates. It rejected on four MEDIUM verification and lifecycle findings.
This reissue closes those four findings without widening the protocol.

No part of this artefact authorizes push, production allocation, release, or
deployment. Those remain prohibited by
[[SPEC-008-production-pairing-claimant#CON-985 — Consolidated current authority and gate]]
until a fresh cross-model Tier-1 PASS and separate owner approval.

## Exact candidate closure

- Selfsame implementation:
  `ff2f1394813a38ce9d0409726892b6e0c63d00ea`
- cbcl-bus coordinated candidate:
  `22fcc012ff4d4746f251b497c11495b1b26c0f29`
- cbcl-pairing:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `23e14bf658321b49f759e43d002797e2f3cb3184fbe460c99b0b8f580ab5e781`
- reproducible host and tools: `x86_64-unknown-linux-gnu`, rustc/cargo
  1.96.0, wasm-bindgen 0.2.126

## M-1 — production-boundary transaction

`cbcl_v2_final_decide` now delegates to an injectable inner function. Its
fifteen fallible post-approval operations execute through the exact
`PrePayloadBoundary` hooks in
[[SPEC-008-production-pairing-claimant#TEST-1162 — Pre-payload failure and confirmed abandonment release one exact slot]].
The test checks that each hook occurs exactly once and in causal source order.

`PrePayloadPendingTransaction` is the sole command-visible authority for the
pending slot. `begin` arms cleanup before initial persistence, so a backend
that commits and then reports failure is compensated. `replace_at` owns each
durable phase replacement and its production injection point.
`commit_payload` owns the final durable `PayloadPrepared` replacement and the
private cleanup disarm. The command cannot move disarm before the store write.

The isolated process-global keyring test injects both pre-commit and
post-commit storage failures. Moving arming after initial persistence fails the
test. Moving disarm before the `PayloadPrepared` replacement also fails the
test. The restored candidate passes. This is executable closure of
[[SPEC-008-production-pairing-claimant#CON-990 — Pre-payload failure cannot strand an application slot]],
not a table of synthetic labels.

## M-2 — retry-safe confirmed unlink

`load_local_link` retains the exact slot and the exact-pair policy state
observed for the person's selection. `unlink_local` refuses if either state
changes before confirmed execution. If the selected policy was already absent,
unlink deletes the exact slot without attempting policy removal. If a prior
unlink removed policy but failed to delete the slot and failed best-effort
restoration, a fresh selection therefore remains deletable.

The isolated TEST-1162 case requires both changed-between-selection refusal and
successful absent-at-selection retry. This closes the permanent stranding
sequence while preserving exact compare-and-delete under
[[SPEC-008-production-pairing-claimant#REQ-1006 — V2 presence, authenticated display, and installed state are direct]].

## M-3 — CI-compatible browser WASM

The vendored Selfsame browser WASM was generated from clean detached checkouts
inside a real `linux/amd64` container. The container reported rustc 1.96.0,
host `x86_64-unknown-linux-gnu`, and wasm-bindgen 0.2.126. A second clean rebuild
byte-compared all four generated files and passed. The resulting WASM digest is
the value in the candidate closure above.

cbcl-bus now pins Selfsame at the implementation candidate in `selfsame.sha`,
the Dockerfile, and provenance. Vendor mode updates every recorded dependency
SHA from the already verified pin files as well as the digest and host. The hub
CI host can therefore execute
[[SPEC-008-production-pairing-claimant#TEST-1165 — Hub CI reproduces the exact browser WASM]]
rather than failing before its Erlang and browser gates.

## M-4 — one profile-version definition

`selfsame-app-identity::profile::PROFILE_VERSION` is the only workspace
definition. The crate root publicly re-exports it. The executable recursive
source test requires one definition, its exact owning source, and equality
through the public export. This implements
[[SPEC-008-production-pairing-claimant#TEST-1166 — One profile-version definition governs the workspace]].

## LOW corrections and retained residuals

The isolated CI command now requires the exact `1 passed; 0 failed; 0 ignored`
summary, so selector drift cannot silently run zero tests. The credential-v1
compiled registry module now cites
[[SPEC-007-cbcl-pairing-cutover#CON-806 — Approved relay conformance registry]]
rather than SPEC-008 CON-903.

The two accepted LOW residuals remain unchanged. A mid-chain post-approval
failure can leave a non-authoritative orphan issuer publication. The anonymous
recovery limiter remains keyed on the TCP peer behind the Fly proxy. Neither
residual grants capability or changes the standalone protocol.

## Executed evidence and review gate

The generating run passed the isolated TEST-1162 process and profile-version
source test. It passed the wallet library and vendored browser ABI suites. Hub
pin checks and the exact Linux WASM rebuild also passed. The two required
arm/disarm placement mutants failed TEST-1162. The restored candidate passed.

The broader local workspace run reached the strict dependency witness and
refused the person's known tracked-dirty cbcl-rs and ahead-of-pin did-crdt
siblings. That refusal is expected and is not release evidence. The fresh
review SHALL run the full locked suites and pin witness from clean detached
checkouts at the exact closure above.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. The reviewer SHALL use
Circus and the interactive Claude subscription driver with Anthropic API
environment variables absent. PASS still authorizes no push, production
allocation, release, or deployment.
