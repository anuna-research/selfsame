---
id: SPEC-008-amendment-0.4.6
title: SPEC-008 0.4.6-draft — Closed compatibility grammar and serialized durable authority
status: draft
version: 0.4.6-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-804]]; [[SPEC-007-cbcl-pairing-cutover#REQ-805]]; [[SPEC-007-cbcl-pairing-cutover#REQ-810]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-007-cbcl-pairing-cutover#REQ-813]]; [[SPEC-007-cbcl-pairing-cutover#NFR-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; [[SPEC-007-cbcl-pairing-cutover#CON-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-806]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-204]]; [[SPEC-004-application-scoped-identity#CON-206]]; [[SPEC-004-application-scoped-identity#CON-209]]; [[SPEC-004-application-scoped-identity#CON-211]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-221]]; [[SPEC-004-application-scoped-identity#CON-226]]; did-crdt HTTP and sync service contracts; cbcl-pairing SPEC-001 through coordinated 0.3.0, 0.4.0, and 0.4.1 drafts"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007 unchanged"
supersedes: "[[spec-008-0.4.5-standalone-tofu-first-contact]]"
adopts: "All 0.4.4 and 0.4.5 clauses except the exact replacements and corrections named by this amendment"
review-inputs: "[[spec-008-0.4.5-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.3.0 at 7ddea48; cbcl-pairing SPEC-001 0.4.0 at 47424b7; cbcl-pairing SPEC-001 0.4.1 draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
last-updated: 2026-08-23
---

# SPEC-008 0.4.6-draft — Closed compatibility grammar and serialized durable authority

> **Design target, not implementation authority.** The owner selected standalone
> first-contact pairing with person-owned exact-pair relay TOFU. The fresh
> Claude/Opus Circus review rejected 0.4.5 with zero critical, two high, four
> medium, and eight low findings. This amendment closes those findings and the
> review's five recorded residual wording gaps. No source, schema, fixture,
> vector, release, allocation, or deployment change is authorized until another
> fresh-context Claude-driver Circus review returns PASS over the complete
> coordinated set.

## Effective candidate and unchanged outcome

The effective candidate is:

1. [[spec-008-0.4.4-standalone-tofu-first-contact]], except where 0.4.5 or this
   document replaces a clause;
2. [[spec-008-0.4.5-standalone-tofu-first-contact]], except where this document
   replaces or corrects a clause;
3. cbcl-pairing SPEC-001 0.3.0, 0.4.0, and 0.4.1 coordinated amendments.

The security outcome remains standalone pairing with no enrolment prerequisite
and no compiled relay allowlist. The wallet authenticates the application live,
consents to the exact `(applicationId, relayOrigin)` pair, uses co-present CPace,
shows only the verified authenticated intent, derives and publishes identity
effects only after their respective explicit decisions, and releases a grant
only after durable resolver and hub acknowledgement.

## Disposition of the 0.4.5 review

| Finding | 0.4.6 disposition |
|---|---|
| **H-1 — frozen live v1 profile rejected by the new dispatcher** | [[spec-008-0.4.6-standalone-tofu-first-contact#REQ-962]] and [[spec-008-0.4.6-standalone-tofu-first-contact#CON-951]] explicitly amend [[SPEC-004-application-scoped-identity#CON-201]] version 1 with two disjoint closed member-set branches. The exact live cutover bytes pass one branch; the original rendezvous shape passes the other. Credential/v1 no longer points at the credential/v2 profile. |
| **H-2 — credential/v2 reverses cbcl-pairing CON-007 without authority** | [[spec-008-0.4.6-standalone-tofu-first-contact#REQ-963]] adopts cbcl-pairing SPEC-001 0.4.1 REQ-023 and its complete v2 profile, assets, and tests. |
| **M-1 — policy reset crash test assumes a valid invalid slot** | [[spec-008-0.4.6-standalone-tofu-first-contact#CON-952]] requires ordinary read refusal while the remaining slot is invalid and reset retry to completion. |
| **M-2 — revisionless rollback can restore an operator-lifted tombstone** | [[spec-008-0.4.6-standalone-tofu-first-contact#REQ-964]] and [[spec-008-0.4.6-standalone-tofu-first-contact#CON-953]] make withdrawal rows read-only in both forward and compensation directions and use exact locked-byte comparison for revisionless legacy rows. |
| **M-3 — distinct ApplicationId makes v1/v2 account bindings mutually exclusive** | [[spec-008-0.4.6-standalone-tofu-first-contact#REQ-965]] states the derived DID/scope/alias change, one-binding limit, preflight, refusal, and operator transition procedure. |
| **M-4 — did-crdt gossip bypasses the proposed lock** | [[spec-008-0.4.6-standalone-tofu-first-contact#REQ-966]] and [[spec-008-0.4.6-standalone-tofu-first-contact#CON-954]] serialize every HTTP, gossip, sync, and bootstrap writer through the same durable mutation primitive. |
| **L-1/L-2 — incorrect object-head and control-size equalities** | [[spec-008-0.4.6-standalone-tofu-first-contact#NFR-908]] gives the six exact size cases and adopts cbcl-pairing SPEC-001 0.4.1 CON-018. |
| **L-3/L-4/L-6 — carrier expiry and envelope grammar ambiguities** | [[spec-008-0.4.6-standalone-tofu-first-contact#CON-955]] makes companion CON-013 the sole carrier grammar; companion CON-017 assigns observable expiry duties; companion CON-018 makes length classes disjoint. |
| **L-5/L-7 — purge recovery and stale adopted sentences** | [[spec-008-0.4.6-standalone-tofu-first-contact#CON-952]] defines one recovery result; [[spec-008-0.4.6-standalone-tofu-first-contact#CON-955]] records every inline replacement. |
| **L-8 — SPEC-075 named but not amended** | SPEC-075 REQ-007 is removed from `amends:` and listed under `conforms-to`; its required live cutover shape is admitted by CON-951 without changing its bytes or contract. |

## Requirements

### REQ-962 — Version 1 has two explicit closed profile branches

This requirement replaces 0.4.4 CON-933's version-1 dispatcher paragraphs and
0.4.5 REQ-957's statement that the live compatibility profile does not satisfy
the dispatcher. [[SPEC-004-application-scoped-identity#CON-201]] version 1 is
explicitly amended by [[spec-008-0.4.6-standalone-tofu-first-contact#CON-951]].

The exact RFC-8785 live bytes served at
`https://chat.anuna.io/selfsame/application` SHALL remain byte-identical and
SHALL be recognized by the cutover-v1 branch. The original CON-201 rendezvous-v1
branch remains recognized. A profile matching neither complete member set, or
mixing members from both, refuses. `profileVersion = 1` alone never selects a
partial grammar.

[[SPEC-007-cbcl-pairing-cutover#CON-806]] step 1 is replaced for credential/v1:
it recognizes the legacy ApplicationId under CON-951 and selects
`cbclPairingRelays` only from the cutover-v1 branch. Credential/v1 SHALL NOT fetch
or use the distinct credential/v2 ApplicationId. The v2 branch remains exclusive
to credential/v2. This removes the adopted 0.4.4 instruction that credential/v1
also accept a version-2 profile.

Client rollback is explicit. Disabling or rolling back credential/v2 does not
remove the cutover-v1 recognizer. The current pre-dispatcher wallet baseline
already recognizes the frozen live bytes; a release rollback to that baseline
therefore retains chat.anuna.io credential/v1 behavior, while an original
rendezvous-v1 profile requires the new dual-branch recognizer.

### REQ-963 — The complete cbcl-pairing profile amendment is inseparable

This requirement supplements 0.4.5 REQ-954. Credential/v2 SHALL conform to the
complete cbcl-pairing 0.3.0, 0.4.0, and 0.4.1 coordinated set. In particular,
cbcl-pairing SPEC-001 0.4.1 REQ-023 explicitly amends parent CON-007, retains
the agent profile and abuse
controls, defines the complete credential/v2 profile, places allocation-expiry
construction on the allocator, labels public context, and supplies disjoint
envelope CDDL.

The wallet and browser SHALL use the separate typed machine-carrier and PAIR1
presence-field inputs. The pairing library, not application glue, enforces their
non-substitutability, version isolation, CPace source, sender/state rules, and
fixed envelope. All companion TEST-030 through TEST-050 are release-blocking.

### REQ-964 — Ceremony rollback never writes withdrawal authority

This requirement strengthens 0.4.5 REQ-958 and replaces the adopted 0.4.4
CON-941 rollback write set. Every `cbcl-webfinger-withdrawn` row is read-only to
every credential/v2 command in both the forward and compensation directions.
The ceremony never captures a withdrawal row for restoration and never creates,
deletes, lifts, replaces, or rewrites one.

Before pending state, the hub obtains the exact computed alias-key write lock and
checks the applicable withdrawal row. The existing operator `withdraw/2` and
`reinstate/2` paths serialize on that same alias key when v2 tables exist. Their
completed result wins: a concurrent withdrawal blocks finalization; a concurrent
reinstatement remains lifted after any later compensation.

For revisionless legacy rows that the ceremony is authorized to write, conflict
detection is an exact byte-for-byte comparison between captured bytes and a
write-locked re-read in the compensation transaction. Any difference leaves
compensation pending. Revision comparison applies only to the additive side
tables that actually carry a revision.

### REQ-965 — Distinct application identity makes v1/v2 binding exclusive

The credential/v2 ApplicationId changes the hierarchy derivation input and
therefore produces a distinct home DID, CON-211 account scope, reciprocal alias,
and exact-pair TOFU key from credential/v1. The reviewed
`cbcl-webfinger-account` table is a set keyed by the opaque provider account and
holds one alias/home DID. A chat account can therefore hold at most one v1-or-v2
binding; coexistence for one account is not claimed.

At the reviewed code baseline, `provision/3` has no production caller. Before
enabling v2 allocation, the operator SHALL nevertheless audit actual Mnesia
state rather than infer emptiness from code. Any account with a v1 binding is
v2-ineligible and a v2 attempt returns `BoundDifferentDid` without mutation.

A future v1-to-v2 transition is an explicit operator workflow: identify the
exact existing account/home DID, run `withdraw/2`, run `reinstate/2` for that
same DID to remove the withdrawal block without restoring the binding, verify
the account is unbound, and only then let the person start a fresh v2 ceremony.
No ceremony performs or collapses those operator actions.

### REQ-966 — Resolver durability covers every in-process writer

This requirement replaces 0.4.5 REQ-961's handler-only wording. Every path that
can mutate the production did-crdt document store—including create, HTTP delta,
gossip receive/merge, sync bootstrap/admission, and any future local writer—SHALL
use [[spec-008-0.4.6-standalone-tofu-first-contact#CON-954]]'s one per-DID durable
mutation primitive. No path may persist or swap a snapshot built outside that
serialization boundary.

The lock is sufficient only under the deployed single-process premise: one Fly
machine owns one mounted volume. Scaling to multiple writers is prohibited until
a reviewed cross-process CAS or lock replaces that premise. `durable-ready`
must remain true continuously, not only at profile generation. Loss of the
storage adapter or read-back integrity makes health fail, blocks new v2
allocation/offer preparation, and returns a closed service failure for writes.

### NFR-908 — Fixed-class arithmetic has six exact application-object cases

This requirement replaces 0.4.5 NFR-907 and the adopted TEST-1030 sentence that
every control object encodes to 4,144. The field-4 byte-string head is always
three octets for both fixed application classes. Exact deterministic-CBOR object
lengths are:

| Class | `logicalLength` | Encoded length |
|---|---:|---:|
| control | 1..23 | 4,142 |
| control | 24..255 | 4,143 |
| control | 256..4,096 | 4,144 |
| large | 1..23 | 64,558 |
| large | 24..255 | 64,559 |
| large | 256..62,000 | 64,560 |

The five-octet byte-string head occurs only on the outer 66,644-octet ciphertext.
All previously reviewed maximum plaintext, ciphertext, relay-body, queue, and
frame-count bounds remain unchanged.

## Contracts

### CON-951 — CON-201 v1 closed-branch dispatcher and immutable v2 identity

The five common CON-201 recognition steps remain: UTF-8/size, closed JSON parse,
complete member grammar, exact RFC-8785 reproduction, then semantic action. The
dispatcher first reads the integer `profileVersion` and then requires one whole
closed grammar:

| Branch | Exact top-level member set |
|---|---|
| rendezvous-v1 | `profileVersion`, `applicationId`, `accountAuthority`, `verifierAudience`, `allowedPermissions`, `enrollment`, `rendezvous`, `stateResolvers`, `revocation`, and optional `pairingRecordRelays` |
| cutover-v1 | `profileVersion`, `applicationId`, `accountAuthority`, `verifierAudience`, `allowedPermissions`, `enrollment`, `cbclPairingRelays`, `stateResolvers`, `revocation`; no optional members |
| credential-v2 | the exact nine-member v2 grammar in 0.4.4 CON-933 at the distinct ApplicationId `https://chat.anuna.io/selfsame/application/v2` |

Both v1 branches require `profileVersion = 1`. The rendezvous-v1 nested grammar
is the ratified parent CON-201 grammar. The cutover-v1 `cbclPairingRelays` nested
grammar is exactly [[SPEC-007-cbcl-pairing-cutover#CON-806]] and cbcl-bus
SPEC-075 REQ-007: one to sixteen closed descriptors, with the existing
operator/origin/digest/priority/weight checks. Every other common nested member
retains CON-201's grammar. The v2 branch requires `profileVersion = 2` and all
0.4.4 CON-933 nested rules.

The two v1 member sets are disjoint by `rendezvous` versus
`cbclPairingRelays`. Both, neither, `pairingRecordRelays` on cutover-v1, an
unknown member at any depth, or any nested mismatch refuses. This is closed
member-set dispatch, not wire negotiation or permissive extension.

The generator and corpus SHALL pin three positive fixtures: the parent
rendezvous-v1 fixture, the exact live cutover-v1 octets and digest, and the
distinct v2 fixture/digest. The live cutover fixture must pass the cutover-v1
branch byte-for-byte. Old current-wallet fixtures must continue to accept it.

### CON-952 — Policy reset and identity purge have reachable crash states

This contract replaces 0.4.5 CON-944's reset and purge-recovery paragraphs. If
at least one policy slot is present and neither slot is valid, gated reset
deletes and verifies slot A then slot B under the installation lock. A crash
after the first deletion leaves the other slot present and invalid. Ordinary
policy read still refuses; the gated reset path remains available and retry
finishes deletion. No valid policy is asserted or applied in that state.

Once the `purging` marker is durably read back, recovery has exactly one result:
no root and no safety state. The running operation and startup recovery delete
and verify, in order, both policy slots, the origin-ledger record and key, and the
old `SealedRoot`; then delete and verify the marker. Startup never writes a new
or restored root. The person must explicitly retry restore or new-root creation
after recovery. A crash before durable marker commit leaves the pre-operation
identity untouched; any crash after commit converges to no root. A new root is
written only in a later operation after the marker and all old state are absent.

The equal-valid-slot rule from 0.4.5 remains: identical equal revisions select A
and ordinary update overwrites B. Different plaintext at equal revision refuses.

### CON-953 — Stable-account transaction and exact compensation conflicts

This contract replaces 0.4.5 CON-946's table and rollback paragraphs. No legacy
record arity or primary key changes. The complete legacy authority write set and
its additive stable-account index are:

| Legacy table | Existing key retained | Additive index / v2 treatment |
|---|---|---|
| `cbcl-webfinger-alias` | normalized alias URI | stable account ID to alias key + revision |
| `cbcl-webfinger-account` | opaque provider account | stable account ID to account key + revision |
| `cbcl-pairaccount-grant` | `(Handle, DeviceKey)` | stable account ID to legacy key + ceremony + revision |
| `cbcl-account-binding` | handle | stable account ID to binding key + revision |
| `cbcl-device-grant` | `(Account, PubKey)` | stable account ID to device-grant key + revision |
| `cbcl-member` | handle | stable account ID to member key + revision |
| `cbcl-webfinger-withdrawn` | alias URI | **read-only; never captured or restored by v2** |

The additive index tables are respectively `cbcl-selfsame-alias-index`,
`cbcl-selfsame-account-index`, `cbcl-selfsame-pairaccount-index`,
`cbcl-selfsame-account-binding-index`, `cbcl-selfsame-device-grant-index`, and
`cbcl-selfsame-member-index`. The existing 0.4.5 stable account/scope/ceremony/
device/compensation tables remain. Schema creation is idempotent and precedes
the feature gate; per-account population is a separate all-or-none data
transaction.

Every authority/finalization transaction acquires the stable-account row and
the exact legacy keys with write locks before re-reading authority and the
computed withdrawal key. It records only rows it may write. Compensation locks
the same keys; it compares revisioned side rows by revision and every
revisionless legacy row by exact captured bytes. Any mismatch preserves the
intervening writer and leaves compensation pending. A withdrawal row is outside
both comparisons and both write sets.

Operator `withdraw/2` and `reinstate/2` acquire the same exact alias-key lock and,
when a stable account mapping exists, the same stable-account lock. They remain
non-HTTP operator actions. Ceremony failure after a completed reinstatement
cannot recreate the tombstone; ceremony finalization racing a withdrawal cannot
commit past the withdrawal lock/check.

### CON-954 — One durable did-crdt mutation primitive owns all writers

Production `AppState` exposes one keyed asynchronous lock per DID and one
fallible durable store. HTTP create, HTTP delta, gossip merge, sync bootstrap,
and every future writer call the same primitive. Under the per-DID lock it:

1. clones the current canonical document;
2. validates and applies exactly one create, delta, bootstrap, or merge to the
   clone;
3. persists the complete candidate snapshot to the configured blob store;
4. reads it back through the committed DID tag and verifies exact canonical
   document equality;
5. swaps the in-memory document; and only then
6. emits HTTP success or marks the inbound sync item accepted.

No writer may hold a stale clone across the lock, call `save` independently, or
swap memory without the verified save. A failed validation/save/read-back leaves
memory and the durable tag at the prior accepted state. A crash before tag commit
recovers the prior state; a crash after commit recovers the candidate. A merged
gossip delta accepted before an HTTP acknowledgement, or vice versa, is present
in the acknowledged snapshot and after restart; neither writer can erase it.

Production strict mode fails startup on configured-store open failure,
incomplete enumeration, `load_all` failure, corrupt snapshot, or tag/read-back
mismatch. The reusable library may retain an explicitly selected permissive
inspection mode, but the Fly service and v2 health endpoint may not. The health
endpoint continuously reports durable adapter, volume, load, and read-back
readiness. The hub checks every declared resolver before allocation/offer and
fails closed if readiness is absent. Announcement and DHT publication remain
post-commit best-effort discovery effects and cannot reverse acknowledgement.

### CON-955 — Effective-text correction ledger

For avoidance of contradictory adopted prose, the following exact corrections
are normative:

1. 0.4.4 CON-934's carrier field list is illustrative and replaced in full by
   cbcl-pairing SPEC-001 0.4.0 CON-013, including mandatory
   `relay-expires-at`; cbcl-pairing SPEC-001 0.4.1 CON-017 owns
   allocator-versus-claimant expiry duties.
2. 0.4.4's sentence making the effective deadline the earlier of a state idle
   deadline and absolute deadline is replaced by 0.4.5 CON-948's cumulative
   checkpoints plus relay expiry.
3. 0.4.4's sentence attributing migration to CON-939 is replaced by
   [[spec-008-0.4.6-standalone-tofu-first-contact#CON-953]].
4. 0.4.4's legacy-key table is replaced by CON-953; no
   `cbcl-chat-pairaccount` table or account-as-handle claim remains.
5. 0.4.4 TEST-1030's control-object equality is replaced by
   [[spec-008-0.4.6-standalone-tofu-first-contact#NFR-908]].
6. 0.4.5 CON-947's “no profile reversal” sentence is replaced: server bytes never
   move, while client rollback is the explicit REQ-962 compatibility rule.
7. Credential/v2 application objects use the companion CON-018 disjoint CDDL;
   no prose-only `logicalLength` constraint remains.
8. Credential/v2 offer/application objects do not use PROTO-004 sealed records;
   [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]] and
   [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]] are conformed to, not amended.
9. [[SPEC-004-application-scoped-identity#CON-206]] step 9 is amended for v2:
   the hub-signed pending binding is a fresh authenticated cache of the exact
   authority; public alias resolution occurs only during finalization and gates
   the receipt. A pending record is never displayed as a finalized public alias.
10. 0.4.4 CON-938 `commit_issuance` performs derivation and all local signatures
    inside the final custody closure, then zeroizes the root. Resolver publication
    occurs afterward from signed bytes and cannot retain or re-open the root.

The reverse device-possession check remains a self-challenge by the browser: it
binds the different device key named by the grant but is not third-party proof.
That limitation, pending-binding ordering, and the 180-second first human budget
are explicit residuals, not upgraded guarantees.

## Architecture decisions

### ADR-935 — CON-201 v1 gains a closed cutover branch

The production profile was deliberately minted as version 1 with
`cbclPairingRelays` and no `rendezvous`. Freezing its authenticated bytes while
installing a dispatcher requires recognizing that exact closed language. A
second disjoint v1 member set resolves the existing SPEC-004/SPEC-007/SPEC-075
authority contradiction without content negotiation or permissive unknown-field
handling.

### ADR-936 — Credential/v2 is a distinct derived application identity

A distinct immutable ApplicationId makes rollback and caching tractable, but it
also changes every hierarchy-derived identity. The one-binding hub store means
v1 and v2 are alternatives for one account. That cost is accepted and exposed
as an operator-mediated transition rather than hidden as apparent coexistence.

### ADR-937 — Withdrawal state is never compensation state

Withdrawal is an operator authority boundary. Exact rollback is safe only for
rows the ceremony was authorized to write. Excluding tombstones from capture and
restore preserves concurrent operator decisions and makes “ceremony cannot
withdraw or reinstate” true in both directions.

### ADR-938 — did-crdt acknowledgement is serialized across all writers

Durable-before-acknowledge is meaningless if gossip can later commit a stale
snapshot. One per-DID mutation primitive provides a causal durable order for all
in-process writers. The deployed single-machine/one-volume premise bounds that
lock; multi-machine operation requires a later cross-process design.

## Test specification

All effective TEST-1011 through TEST-1064 remain release-blocking except where a
replacement below changes the expected outcome. Companion TEST-030 through
TEST-050 are mandatory.

### TEST-1065 — Exact live profile passes the closed cutover-v1 branch

Feed the exact deployed live octets and digest to the new dispatcher and require
cutover-v1 success. Feed the parent rendezvous-v1 fixture and require that branch.
Feed the distinct v2 fixture and require only v2. Add both relay members, remove
both, add `pairingRecordRelays` to cutover-v1, change version, or mutate each
nested field and require refusal. The current pre-dispatcher wallet fixture still
accepts the frozen live bytes.

### TEST-1066 — Client and server profile rollback are independent and safe

Serve frozen v1 and distinct v2 bytes concurrently. Disable allocation, drain
and compensate, expire carriers, withdraw v2 route, and roll the wallet fixture
back to the current pre-dispatcher baseline. The live v1 profile remains
recognized and byte-identical. An original rendezvous-v1 profile is documented
as requiring the dual-branch release, not silently accepted by the old fixture.

### TEST-1067 — Invalid policy reset and purge crash recovery are reachable

Start with at least one present slot and no valid slot. Crash after either first
deletion; ordinary read refuses, remaining slot is present and invalid, and
gated retry reaches verified both-absent. For lifecycle purge, crash before
marker commit and require the old identity untouched; crash after every later
boundary and require startup convergence to no root, no policy, no ledger/key,
and no marker. A new/restored root appears only after a separate explicit retry.

### TEST-1068 — Compensation preserves concurrent operator reinstatement

Run a ceremony that will fail after finalization. Concurrently complete
operator `reinstate/2` for an existing tombstone. Compensation restores only its
authorized exact rows and the tombstone remains absent. Mutate each revisionless
legacy row and each revisioned side row between capture and rollback; exact-byte
or revision conflict leaves compensation pending and preserves the intervening
writer. Race `withdraw/2` with finalization and require withdrawal to win.

### TEST-1069 — V1-bound accounts refuse v2 until operator transition

Create a v1-bound account and attempt v2; require `BoundDifferentDid`, no pending
state, and no mutation. Verify v1 and v2 derive different home DIDs, scopes,
aliases, and TOFU pairs. Rehearse exact operator withdraw plus same-DID reinstate,
verify unbound state, then complete a fresh v2 ceremony. Audit non-empty and empty
production-preflight fixtures; allocation cannot enable with unclassified rows.

### TEST-1070 — Every hub authority table is transactionally covered

Populate every CON-953 legacy table and side index. Fail schema creation and
per-account data migration at every boundary; legacy tuples remain byte-identical
and new rows commit all-or-none. Fail finalization and compensation after each
write; exact authorized rows restore or remain pending on conflict. Prove
`cbcl-webfinger-withdrawn` is never in a v2 write or rollback trace.

### TEST-1071 — Concurrent HTTP and gossip writes survive restart

At every CON-954 boundary, race HTTP create/delta with a same-DID gossip merge
and sync bootstrap. Require one serialized order, no stale-clone save, no lost
accepted delta, and complete acknowledged state after process restart. Inject
save, tag, read-back, swap, and response failures. Run strict production startup
against corrupt/incomplete storage and require failure; permissive library mode
cannot satisfy production health.

### TEST-1072 — Runtime resolver readiness gates new ceremonies

Start durable-ready, then remove or fault the configured adapter/volume and
read-back. Health becomes non-ready, writes fail closed, and hub v2 allocation
and offer preparation stop. Restore and verify before resuming. Attempt a second
Fly writer or volume owner and require the release gate to fail until a reviewed
cross-process mechanism exists.

### TEST-1073 — Every fixed object size boundary is exact

Run companion TEST-050 and assert all six NFR-908 totals. Include control lengths
1, 23, 24, 255, 256, and 4,096 and large lengths 1, 23, 24, 255, 256, 61,999,
and 62,000. Prove the three-octet inner and five-octet outer byte-string heads
occur only where specified; re-run all unchanged outer bounds.

### TEST-1074 — Effective-text replacements have no executable stale branch

Static conformance checks map every CON-955 replacement to one implementation
entry point and reject use of the stale carrier list, idle timer, migration
attribution, table name, control-size equality, rollback claim, loose CDDL, or
PROTO-004 sealing. Trace custody to local signing only and resolver publication
to post-zeroization bytes. Verify pending binding never appears as finalized
public alias and the reverse device check is labeled self-challenge.

## Cross-repository implementation boundary

| Repository | Effective added/replaced slice |
|---|---|
| `cbcl-pairing` | Complete 0.3.0 + 0.4.0 + 0.4.1: protected admission, v2 crypto/channel/reducer, explicit credential profile amendment, allocator/claimant expiry duties, disjoint envelope, manifests, and independent vectors. |
| `selfsame` pure core | Three-way closed profile dispatcher; exact live v1 fixture; v2-only ApplicationId; authenticated display/profile adapter; CON-226 corpus; fixed object bounds. |
| `selfsame` Tauri wallet | Exact-pair policy and origin ledger; corrected invalid-slot reset; purge-to-no-root recovery; bounded custody operations; post-zeroization publication. |
| `cbcl-bus` / `cbcl_chat` | Frozen v1 and distinct v2 profiles; preflight; full additive stable-account indexes; exact locked conflict checks; tombstone-read-only transaction; explicit v1-to-v2 operator workflow; feature gates and rollback. |
| `did-crdt` | One per-DID durable mutation primitive for HTTP, gossip, sync, and bootstrap; strict production load/read-back; continuous health; single-writer release guard. |

## Gate and sequencing

1. Run a fresh-context Claude-driver Circus Tier-1 adversarial review of the
   exact effective Selfsame 0.4.4/0.4.5/0.4.6 set, cbcl-pairing
   0.3.0/0.4.0/0.4.1 set, and all four code baselines. It must re-audit every
   0.4.5 finding, inherited hard stop, authority change, persistence boundary,
   rollback direction, bound, and recorded residual. Owner direction cannot
   substitute for PASS.
2. Only after coordinated PASS, encode the effective design as an executable SPL
   Elephant theory. TEST-1011 through TEST-1074 and companion TEST-030 through
   TEST-050 become named gates.
3. Commit failing schemas, vectors, corpus, and pure-core tests before production
   code. Implement cbcl-pairing and pure Selfsame contracts before effectful
   shells.
4. Implement and validate wallet lifecycle, resolver serialization, hub additive
   schema/transactions, restart, compensation, and rollback before browser/wallet
   release wiring.
5. Run unit, mutation, prohibited-action, work-bound, corpus, reproducible-WASM,
   Android, browser, restart, rollback, and local real-relay E2E gates.
6. Keep production allocation disabled. Obtain separate owner confirmation
   before any cbcl-bus or did-crdt deploy. Record the immediately prior Fly
   releases first; there is no canary.

## Explicit open production gates and residuals

- Human cryptography review and independent CPace group math remain open.
- The amended SPEC-004 CON-201/204/206/209/211/214/219/220/221/226,
  SPEC-007 clauses, all three cbcl-pairing companions, and did-crdt HTTP/sync
  contracts require their named owners' approval and recorded evidence.
- Relay operatorship, same-origin compromise, deliberate forwarding of both
  inputs, relay observation of `T`, lost-claim mailbox stranding, device-local
  rollback counters, operator availability, privacy/security review, and the
  tight 180-second first-human checkpoint remain explicitly accepted residuals.
- The reverse device check is a browser self-challenge, not third-party proof.
  CON-206 v2 step 9 accepts a signed pending authority cache before public alias
  finalization; receipt still waits for finalization.
- Production allocation remains closed after design PASS until every separate
  human and release gate passes. This amendment never authorizes a deploy.

## Changelog

- **0.4.6-draft — 2026-08-23 — review candidate, not approved.** Adds a closed
  CON-201 cutover-v1 branch for the frozen live profile; adopts the explicit
  cbcl-pairing credential/v2 profile amendment; corrects invalid-slot and purge
  recovery; removes tombstones from compensation; states v1/v2 account-binding
  exclusivity and transition; serializes every did-crdt writer; fixes envelope
  CDDL and object-size cases; and records all stale-text and residual
  dispositions. No implementation or deployment authority is claimed.
