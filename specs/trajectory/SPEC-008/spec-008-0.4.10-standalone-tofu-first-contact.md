---
id: SPEC-008-amendment-0.4.10
title: SPEC-008 0.4.10-draft — Monotonic device authority and version-separated evidence
status: draft
version: 0.4.10-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-807]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-226]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.3-draft; cbcl-bus [[SPEC-076-pairing-account-grant#REQ-002]]; cbcl-bus [[SPEC-076-pairing-account-grant#CON-001]]; cbcl-bus [[SPEC-076-pairing-account-grant#CON-002]]; cbcl-bus [[SPEC-076-pairing-account-grant#ADR-001]]"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007; cbcl-bus SPEC-053 GATE-00, GATE-01, GATE-03, and corrected GATE-04"
supersedes: "[[spec-008-0.4.9-standalone-tofu-first-contact]]"
adopts: "All 0.4.4 through 0.4.9 clauses except the exact replacements and corrections named by this amendment; cbcl-pairing SPEC-001 0.4.4 remains independently unchanged"
review-inputs: "[[spec-008-0.4.9-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.3-draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
owner-direction: "2026-08-23 — standalone first-contact option 1 is the target architecture; implementation remains subordinate to fresh review PASS"
last-updated: 2026-08-23
---

# SPEC-008 0.4.10-draft — Monotonic device authority and version-separated evidence

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14 (RFC 2119, RFC 8174). This
interpretation applies only when they appear in all capitals.

> **Design target, not implementation authority.** The fresh Claude Opus/max
> Circus review rejected 0.4.9 with one critical, one high, three medium, and
> four low findings. It again found the standalone first-contact architecture
> sound. This amendment closes the bounded defects against the running code.
> No code, schema, fixture, vector, allocation, release, or deployment change
> is authorized until a new fresh-context cross-model review returns PASS over
> the complete coordinated set.

## Review disposition

| 0.4.9 finding | 0.4.10 disposition |
|---|---|
| **C-1 — an arbitrary pairaccount claim can brick a granted key after commit** | [[spec-008-0.4.10-standalone-tofu-first-contact#REQ-978]] adopts possession proof and grant-dominant monotonic authority. A verified device grant can never be made inert by a later pairaccount claim. |
| **H-1 — `profileVersion = 2` cannot verify against the live version-1 profile** | [[spec-008-0.4.10-standalone-tofu-first-contact#REQ-977]] uses statement-local `evidenceVersion = 2`; the profile and its pinned digest stay version 1. |
| **M-1 — 8,192-byte v1 evidence cannot fit a 6,000-byte non-core budget** | [[spec-008-0.4.10-standalone-tofu-first-contact#NFR-912]] defines a distinct 4,000-byte credential/v2 evidence policy and a generated `56,000 + 4,000 + 2,000 = 62,000` proof. |
| **M-2 — adopted CON-960 contradicts the verdict-only display API** | [[spec-008-0.4.10-standalone-tofu-first-contact#CON-968]] explicitly replaces that sentence: the verifier returns only a verdict and the shared profile constructs display. |
| **M-3 — the lock and cap rule does not cover both real grant writers** | REQ-978 and cbcl-bus 0.16.3 name absent-row guard locks, indexes, one private write core, and removal of the uncapped exported writer. |
| **L-1 — `account-linked` is not a credential/v2 kind** | CON-968 separates the six new result kinds from the existing shared SPEC-053 REQ-033 notice. |
| **L-2 — ciphertext length leaks coarse migration size** | NFR-912 records the relay-visible length correlation as an accepted residual and makes privacy acceptance a gate. |
| **L-3 — the hub lacks CBOR/JCS primitives** | [[spec-008-0.4.10-standalone-tofu-first-contact#REQ-980]] routes closed typed digest construction through the existing Rust NIF boundary and retains browser differential vectors. |
| **L-4 — the shared `build()` can silently widen v1** | REQ-977 requires disjoint v1/v2 recognisers, builders, signers, and call graphs. |

The original 0.4.0 F-A through F-E corrections remain unchanged. Relay policy
is exact `(applicationId, relayOrigin)`. The CON-220-authenticated application
and transition are checked before display. Wallet custody is untouched before
preliminary consent. The hub allocates the pending fresh account scope.
Per-application consent remains a mitigation, never a relay-operatorship claim.

## Requirements

### REQ-977 — Credential/v2 is evidence version 2 over the unchanged version-1 profile

The live CON-220 application profile, its `profileVersion = 1`, its canonical
octets, and its pinned digest remain byte-for-byte unchanged. Credential/v2 is
identified by the statement-local `evidenceVersion = 2`; its statement carries
`profileVersion = 1` and the exact live profile digest. The current CON-219
grammar remains `evidenceVersion = 1` and `profileVersion = 1`.

App identity exposes disjoint closed entries:

```text
recognise_enrollment_v1 / build_enrollment_v1 / sign_enrollment_v1
recognise_credential_v2 / build_credential_v2 / sign_credential_v2
```

These names describe architectural roles. Repository style MAY use different
names. The implementation SHALL preserve four boundaries. Evidence-version
dispatch is total before member recognition. Each version has a closed member
set. Each recogniser round-trips only through its version-specific builder.
Each signer calls only its matching recogniser and builder. A version-general
or credential/v2 builder is unreachable from the HTTP enrolment-v1 signer.

`CREDENTIAL_V2_EVIDENCE_JWS` is a new v2-only policy. It uses the existing
enrollment evidence's JOSE algorithm discipline and caps compact JWS length at
4,000 octets. Existing `ENROLLMENT_JWS.max_octets = 8,192` is
unchanged and is never used as a credential/v2 body bound.

### REQ-978 — Verified device authority is monotonic across pairaccount

The wallet adopts cbcl-bus 0.16.3's correction to
[[SPEC-076-pairing-account-grant#REQ-002]]. A `pairaccountgrant` request no
longer writes a claim. It starts a short-lived challenge. Successful proof
requires an Ed25519 signature by the exact 32-octet claimed device key. The
ordinary outer signed frame still authenticates the verified sender handle and
live connection. A client that does not possess the claimed private
key receives `pairaccount-possession-required` and cannot create a claim.

The credential/v2 transaction itself still writes no pairaccount row. Device
authority obeys this invariant for all time, not merely at two pre-commit
instants:

> Once a raw device key has verified grant or enrolled-member authority, no new
> pairaccount claim can target it. An old claim can never make that key fail
> ordinary per-frame resolution.

The hub establishes the invariant twice under one device-authority guard. A new
pairaccount claim refuses any key in the device-grant or enrolled-member
public-key index. `unratified-device?` returns literal `false` whenever either
index contains authoritative standing. Read/index failure remains `unknown`, so
the existing consumer stays fail-closed. Existing exact pairaccount rows remain
idempotently readable; they do not outrank later verified authority.

All device-grant creation paths use the same account and device guard locks,
the same indexed cap, and one private write core. New `cbcl-member` enrollment
writes also take the device guard and check both authority indexes. The account guard key is the
exact raw canonical bytes stored in `cbcl-device-grant.account`, whether or not
a guard row exists. The device guard key is the exact raw 32-octet public key,
whether or not a guard row exists. The existing uncapped exported
`store-verified-device-grant/6` is removed rather than grandfathered.

The final migration still checks pairaccount collision before carrier and in
the final transaction. Those checks are concurrency and eligibility evidence,
not a claim that absence is permanently stable. After commit, arbitrary
handles, reconnects, restarts, proof replays, and claim/grant races cannot make
the verified device fail the unchanged reducer.

### REQ-979 — The offer bound and relay-visible length are explicit

The complete credential member accepted by cbcl-pairing remains at most 62,000
octets. Its generated maximum is partitioned without reusing the wider v1
policy:

```text
56,000  canonical OfferCoreV2
 4,000  complete compact JWS under CREDENTIAL_V2_EVIDENCE_JWS
 2,000  aggregate of every other encoded contribution
-------
62,000  maximum credential member
```

The 2,000-octet aggregate includes the provider hint, device-possession proof,
key/control members, member names, delimiters, and enclosing representation.
The generator encodes every legal field at its declared maximum and records the
exact subtotal and residual. If any legal maximum makes either subtotal or the
62,000-octet total fail, the manifest fails; prose estimates cannot waive it.
The separate cbcl-pairing adjacent-body ceiling is not misreported as the
credential member ceiling.

The blind relay observes the adjacent AEAD ciphertext/body length. For this
increment that length can correlate with encoded migration size and coarse room
count, up to 256 rooms and 48,000 encoded room octets. Exact application,
account, handle, and room identifiers remain encrypted. This correlation is an
explicit accepted privacy residual, subject to human acceptance before
enablement. Padding is deferred to a separately reviewed increment.

### REQ-980 — Canonical digest authority crosses the existing Rust NIF as closed typed data

Selfsame app identity owns the credential/v2 restricted canonical/digest
module. RFC-8785 processing reuses its one JSON recogniser and canonicaliser.
The deterministic-CBOR grammar is limited to the typed arrays, text strings,
byte strings, non-negative integers, and null used by the room and membership
snapshot definitions. No generic CBOR or JSON document accepted from LFE is an
authority input.

The existing `cbcl_selfsame_erl`/`selfsame-beam` boundary gains v2-only closed
functions. They receive typed atom-keyed values and raw binaries. They
reconstruct closed structures, including the four-member device JWK. They
return only closed digest/signature results or a closed rejection. LFE neither parses nor
reimplements deterministic CBOR or RFC-8785, and cannot supply caller-made
canonical bytes as the authoritative preimage.

Rust and independent browser JavaScript reproduce the corpus; BEAM invokes the
same Rust authority through the NIF and verifies its result at the integration
boundary. The three-stack corpus remains mandatory: it is differential
evidence for browser interoperability and NIF marshalling, not a claim that LFE
contains an independent canonicaliser.

## Non-functional requirement

### NFR-912 — Authority serialization, bounds, privacy, and frozen code paths are gate evidence

The hub creates two durable Mnesia `set` guard tables. The account guard uses
the exact raw canonical bytes stored in `cbcl-device-grant.account`. The
authority guard uses the exact raw 32-octet device public key. Every writer obtains write locks by reading the
exact key even when no row exists. Guard rows need not be persisted, so an
attacker cannot grow these tables merely by naming new keys.

The hub idempotently adds and verifies `account` and `pubkey` indexes on
`cbcl-device-grant` plus the `pubkey` index on `cbcl-member`. Startup and the
credential/v2 gate fail closed if any guard table or index is unavailable. Every grant-creation path reaches one
private transaction core. It takes the account guard, the device guard, then
the member/device-grant indexes and exact row. It performs the recognized
`maxDevicesPerAccount` indexed count, collision checks, and write. No exported
uncapped writer remains.

Pairaccount claims additionally use a handle guard keyed by the exact verified
sender handle, preventing distinct device keys from racing the per-handle cap.
The global lock order is account guard (grant mutations), handle guard
(pairaccount claims), device guard, device-grant indexes/exact row, then
pairaccount exact/devkey/handle indexes. No operation acquires a device guard
before an account or handle guard it also needs.

Release evidence includes the generated 62,000-octet bound and accepted
relay-length residual. It also includes the frozen v1 call graph, NIF canonical
vectors, and index/table restore. The evidence proves device authority remains
monotonic through every pairaccount/grant race and restart.

## Contracts

### CON-966 — Pairaccount proof is an adjacent security correction, not credential/v2 authority

The wallet consumes the exact cbcl-bus 0.16.3 challenge protocol and does not
reinterpret it as ceremony evidence. The challenge is RAM-only, single-use,
socket-bound, sequence-bound, handle-bound, claimed-key-bound, hub-bound, and
expires within 120 seconds. Restart loss and every store/read error fail closed.
Mismatch or failed proof burns the nonce before returning one opaque refusal.

The existing browser demo post-hook currently holds only a fixture public key.
It SHALL NOT fake possession, embed a server seed, or reuse the live wire key as
the claimed pairing key. Without the claimed private key it stops at
`pairaccount-possession-required`. A credential/v2 browser locally creates and
durably retains its distinct device seed before allocation. The credential/v2
migration never needs pairaccount to make that key authoritative.

### CON-967 — Display verification returns a verdict; display construction stays shared

The consumer implements cbcl-pairing SPEC-001 0.4.4 CON-021 unchanged. The
credential/v2 `IntentVerifier` receives immutable parsed authenticated input
and returns only `Ok(())` or a closed profile error. It cannot return, replace,
append, or mutate transition/display data. Only after success does the shared
cbcl-pairing profile construct `CredentialV2Display` from the exact retained
parsed bytes. Selfsame receives read-only typed fields and cannot substitute a
wire-claimed application identifier or room list.

### CON-968 — Effective-text correction ledger

The effective set applies these replacements in addition to the 0.4.9 ledger:

1. Every `profileVersion = 2` credential/v2 discriminator in adopted text is
   replaced by `evidenceVersion = 2`; `profileVersion` and the live profile stay
   1.
2. The frozen v1 boundary names `recognise_enrollment_v1`,
   `build_enrollment_v1`, and `sign_enrollment_v1`; v1 reaches no shared,
   version-general, or v2 builder.
3. Adopted 0.4.8 [[spec-008-0.4.8-standalone-tofu-first-contact#CON-960]]'s
   sentence that the verifier returns `AccountTransition` is replaced by
   [[spec-008-0.4.10-standalone-tofu-first-contact#CON-967]]: verdict only;
   shared profile construction only.
4. The 56,000 + 6,000 budget is replaced by REQ-979's
   56,000 + 4,000 + 2,000 generated credential-member bound.
5. Pairaccount absence is no longer described as a stable post-commit negative
   fact. REQ-978's grant-dominant invariant is the authority claim.
6. Device cap serialization uses the exact absent-row guard tables, migrated
   `account`/`pubkey` indexes, one private capped write core, and no uncapped
   export.
7. The six new credential/v2 hub result kinds remain the exact v2 set.
   `account-linked` is an existing shared notice owned by cbcl-bus
   [[SPEC-053-key-root-identity#REQ-033]] and reused after commit.
8. Relay-visible ciphertext length correlation is the accepted residual in
   REQ-979; no padding property is implied.
9. Hub deterministic CBOR and RFC-8785 work is performed through the closed
   Rust NIF functions in REQ-980, never an LFE parser.
10. [[spec-008-0.4.9-standalone-tofu-first-contact#REQ-973]]'s statement that
    SPEC-076 is unchanged is replaced. Credential/v2 still does not write its
    rows. Cbcl-bus 0.16.3 hardens the adjacent writer because it controls
    credential/v2 liveness.

All prior corrections not named here remain effective. They include exact
application-and-relay TOFU and pre-display authenticated transition checks.
They retain pre-consent no-custody ordering and pending fresh account scope.
They also retain dispatch-before-authorization and the two named fixture gates.
Reciprocal authority, immutable status, zero terminal relay offers, and no
console migration remain effective.

## Decision

### ADR-945 — Pairaccount must prove the claimed key, and a verified grant wins forever

The outer signed CBCL frame proves which enrolled handle sent a claim. It does
not prove possession of an arbitrary public key named inside that claim.
Challenge-response under the claimed key closes that gap without exposing the
blind cbcl-pairing ceremony to the hub. Giving a verified device grant priority
in both the writer and per-frame reader makes accepted authority monotonic and
removes the post-commit TOCTOU. Rejected: two pre-commit absence checks leave an
unauthenticated third-party writer live afterwards. Deletion-only repair races
and makes authority depend on cleanup. The existing outer signature is not
claimed-key proof because those keys differ by design.

## Test specification

All effective TEST-1011 through TEST-1101 remain release-blocking except where
CON-968 replaces an expectation. cbcl-pairing TEST-030 through TEST-055 and
cbcl-bus TEST-043 through the new 0.16.3 tests are mandatory.

### TEST-1102 — Credential/v2 verifies against the unchanged live profile

Use the exact pinned profile-v1 octets and digest. Require v1 enrollment to
accept only evidence version 1 and credential/v2 to accept only evidence
version 2 while both require profile version 1. Mutate either version, profile
digest, member set, or builder dispatch and require a closed refusal. Prove the
HTTP v1 signer cannot reach the v2 recogniser, builder, policy, or signer.

### TEST-1103 — Claimed-key possession and nonce binding are load-bearing

Against the real WebSocket path, request a pairaccount challenge then prove
with the exact claimed key. Mutate hub ID, verified handle bytes, outer current
wire key, claimed key, nonce, connection nonce, request sequence, signature, or
expiry. Replay across sockets, handles, sequences, and restart. Require zero
claim writes. Prove mismatch burns the nonce. A public-key-only browser fixture must
surface `pairaccount-possession-required` and must not submit a proof.

### TEST-1104 — Verified device authority cannot be made inert

Race pairaccount challenge/proof against every device-grant writer before,
during, and after credential/v2 commit, including arbitrary TOFU handles and a
public granted key. Require exactly one sound ordering. An earlier unratified
claim blocks grant finalization without migration. A committed verified grant
blocks or overrides the claim and passes ordinary per-frame resolution.
After grant commit, restart and replay all proofs; the granted key remains live
and status recovery succeeds. Repeat against a legacy enrolled wire key.
Require its authoritative member standing to dominate every old or new claim.

### TEST-1105 — Every grant writer is capped under the same absent-row locks

At the last account slot, race distinct keys through the existing Path-B binder
and credential/v2 finalizer. Require one commit, indexed count parity with a
reference scan, exact lock-order evidence, and no exported cap-free writer.
Exercise accounts with and without stored authority rows, absent guard rows,
startup/restart/index migration, and injected index/table failure. Race new
member enrollment against a pairaccount proof for the same public key.

### TEST-1106 — Bounds, canonical primitives, and privacy claims are exact

Generate legal maxima and require the exact 56,000/4,000/2,000 subtotals,
62,000 credential-member total, and recorded residual. Mutate every cap and
require manifest failure. Reproduce deterministic-CBOR, RFC-8785 JWK, room,
snapshot, and confirmation vectors in Rust, independent JavaScript, and BEAM
through the NIF; reject generic/caller-canonical bytes. Measure the relay-
visible ciphertext length correlation and require the privacy gate to accept
that residual explicitly.

### TEST-1107 — Effective text and shared-kind ownership do not regress

Static/spec/code checks require every CON-968 replacement. They reject:

- `profileVersion = 2`;
- v1 round-trips through shared `build`;
- a verifier-returned transition or an 8,192-byte v2 budget;
- an uncapped grant writer or pairaccount claim without inner possession proof;
- pairaccount authority outranking a verified grant;
- an LFE canonicaliser; and
- `account-linked` counted among the six new v2 results.

Mutation tests remove each of the six new hub-result literals and make
the suite red without reclassifying the shared notice.

## Implementation and gate sequencing

The coordinated repository slices remain design-only until PASS. A PASS
authorizes an Elephant SPL/test-first plan, not production enablement.

1. Run a fresh-context Claude Opus/max Circus review over this amendment,
   cbcl-pairing 0.4.4, and cbcl-bus 0.16.3. Include the complete adopted set,
   the 0.4.9 review, and the exact four code baselines.
2. Launch the Claude driver with `ANTHROPIC_API_KEY` absent and verify
   subscription authentication before accepting reviewer output.
3. Only after PASS, encode every effective test as executable Elephant gates.
4. Land red schemas/types/vectors/pure reducers first; then wallet/hub/NIF/
   resolver effects and crash tests; then dormant production dispatch and the
   visibly test-only E2E wiring.
5. Keep production credential/v2 allocation closed. GATE-01, GATE-03, GATE-04,
   human cryptography/migration/privacy review, independent CPace evidence,
   both durability gates, owner approval, and separate deployment confirmation
   remain required.

## Changelog

- **0.4.10-draft — 2026-08-23 — review candidate, not approved.** Preserves
  standalone first contact and makes verified device authority monotonic
  against pairaccount. It separates evidence versions without changing the
  live profile. It closes the bound, display, writer, and canonicalisation
  seams and records the relay-length residual. No implementation, allocation, or
  deployment authority is claimed.
