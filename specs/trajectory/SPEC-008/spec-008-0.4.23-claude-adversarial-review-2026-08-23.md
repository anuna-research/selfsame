---
id: SPEC-008-review-0.4.23-claude-2026-08-23
title: SPEC-008 0.4.23 + cbcl-pairing 0.4.9 + cbcl-bus 0.16.16 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer-model-family: Anthropic Claude
reviewer-model-version: Claude Opus 5 (`claude-opus-5`)
reviewer-session: session_01Bu79Cuv8JmEGNnwHnDKtHa
circus-attempt: spec008-v0423-review/1
review-date: 2026-08-23
review-input: "[[spec-008-0.4.23-total-result-contracts-and-static-nesting]]"
prior-review: "[[spec-008-0.4.22-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.23 + cbcl-pairing 0.4.9 + cbcl-bus 0.16.16 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

Verdict: **REJECT**.

## Authentication, model, and independence evidence

The launch driver removed the paid-API credential before this session started.
The gate result was recorded first. No secret value was printed.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; else echo PRESENT; fi
ABSENT
$ env | grep -iE '^(ANTHROPIC|CLAUDE)[A-Z_]*=' | sed 's/=.*/=<redacted>/'
CLAUDE_CODE_ENTRYPOINT=<redacted>          # ten CLAUDE_* rows in total,
CLAUDE_CODE_SESSION_ID=<redacted>          # every value redacted before display
CLAUDE_EFFORT=<redacted>                   # and no ANTHROPIC_* row returned
$ claude auth status --json      # only the three permitted fields are recorded
  "authMethod": "claude.ai"
  "apiProvider": "firstParty"
  "subscriptionType": "max"
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai`. The reported `apiProvider` is `firstParty`. The reported
`subscriptionType` is `max`. That is the acceptable subscription path.

The command output also carries an electronic-mail address, an organisation
identifier, and an organisation name. Those three fields were read only to
reach the three permitted rows. None of them appears in this report.

### Reviewer identity

| Field | Value |
|---|---|
| Model family | Anthropic Claude |
| Model | Claude Opus 5, exact identifier `claude-opus-5` |
| Harness | Claude Code `2.1.241`, reasoning effort `max` |
| Session | `session_01Bu79Cuv8JmEGNnwHnDKtHa` |
| Runtime session identifier | `142d52a1-7732-480c-98e1-fede515ad14f` |
| Circus attempt | `spec008-v0423-review/1` |
| Circus worktree | `/Users/anuna-01/Code/selfsame-spec008-v0423-review-1` |
| Circus branch | `circus/spec008-v0423-review/1` |

The Circus attempt record is
`/Users/anuna-01/Code/selfsame/.git/circus/spec008-v0423-review/1/record.json`.
It carries `"attempt_id": "spec008-v0423-review/1"` and `"state": "prepared"`.
Its `prepared_at` value is `2026-08-23T09:23:50Z`.

### Distinctness from the generating record

| Property | Generation | This review | Distinct |
|---|---|---|---|
| Model family | OpenAI GPT-5 | Anthropic Claude | yes |
| Model version | `gpt-5.6-sol` | `claude-opus-5` | yes |
| Session | `01a029aa-9127-7c42-ad28-81512b91ded6` | `session_01Bu79Cuv8JmEGNnwHnDKtHa` | yes |
| Context | authored all three candidates | fresh, candidates and code only | yes |

All three candidates record the same generating family, version, and session.
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#Tier-1-generation-provenance]]
carries the Selfsame record. The cbcl-bus and cbcl-pairing records agree with
it. This reviewer belongs to a different model family and a different session.
[[PROTO-001-usdd-agent-protocol]] cross-model review is therefore satisfied on
its recorded evidence.

This session received the three candidates, the amendment chain, and the two
implementation baselines. It did not receive the generator's reasoning.

### Executed evidence beyond reading

Five classes of evidence were produced rather than reasoned about.

1. Mnesia terminals were re-derived at the local OTP 28.5 installation.
   `mnesia-4.25.3` source was read and then exercised in a clean store.
2. Erlang boolean and list operators were probed with non-boolean and
   non-list arguments at the same installation.
3. Every production `mnesia:transaction` call under `apps/` was enumerated at
   the declared cbcl-bus baseline.
4. Every production consumer of an in-lane public owner was enumerated by a
   cross-module scan of production LFE under `apps/`.
5. Every production Rust source was scanned for the version identifiers the
   candidate classifier names.

## Reviewed commits and baselines, resolved rather than assumed

| Repository | Role | Commit | Verified |
|---|---|---|---|
| selfsame | candidate | `c48e6d5a5cecd9eb1a457fa0155bd942892dd4d2` | `spec(pairing): make result consumers total` |
| selfsame | implementation baseline | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | `crates/` is byte-identical at the candidate |
| cbcl-bus | candidate | `b073072738ab740c24fb33f9b22a3d93e48c12eb` | `spec(identity): make retry results total` |
| cbcl-bus | implementation baseline | `ce894a7f2debc111b610296b1a3780de6084e37e` | `apps/` is byte-identical at the candidate |
| cbcl-pairing | candidate | `46fdf6c8660e924d5f9d1700d9e837a7cd45ad3f` | `spec(pairing): advance coordinated consumer pointer` |
| cbcl-pairing | implementation baseline | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | `spec(credential-v2): align deployed room admission` |
| did-crdt | coordinated set member | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | `fix(rendezvous): bound storage, amortise expiry, cap body` |

`git diff --stat` between each implementation baseline and its candidate is
empty for `crates/` and for `apps/`. Every code claim below therefore holds at
the declared baseline and at the candidate.

The reviewed candidate documents are
`specs/trajectory/SPEC-008/spec-008-0.4.23-total-result-contracts-and-static-nesting.md`,
`specs/trajectory/SPEC-053/spec-053-0.16.16-total-result-contracts-and-static-nesting.md`,
and
`specs/trajectory/SPEC-001/spec-001-0.4.9-coordinated-consumer-pointer.md`.

Effective authority was resolved from the whole amendment chain. CON-978,
CON-979, CON-980, CON-981, and CON-982 retirements were applied before any
current value was compared. Headings, changelogs, and gate labels were not
treated as authority.

## Verdict and severity counts

| Severity | Count | Identifiers |
|---|---|---|
| CRITICAL | 0 | none |
| HIGH | 0 | none |
| MEDIUM | 1 | [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-M-1]] |
| LOW | 6 | [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-1]], [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-2]], [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-3]], [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-4]], [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-5]], [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-6]] |

A PASS requires zero unresolved CRITICAL, HIGH, and MEDIUM defects. One
unresolved MEDIUM defect stands. The verdict is therefore REJECT.

This candidate is a large improvement on its predecessor. Every one of the
twelve 0.4.22 findings closes on code-backed evidence. The single MEDIUM
defect is a new gap that the result-shape change itself opened.

Verdict: **REJECT**.

## Findings

### N8-M-1 (MEDIUM, gate-blocking) — two production consumer classes of shape-changed owners have no declared busy boundary

**Repository owner: selfsame (NFR-925, TEST-1150) and cbcl-bus (NFR-023, TEST-109).**

`NFR-925` changes the public result of every collection and predicate owner to
`#(ok Value)` or `#(error authority-busy)`. It then declares where a busy value
terminates. The declared boundaries are exactly these six.

| Boundary | Declared terminal |
|---|---|
| WebSocket consumer | close code 1013, reason `try-again-later`, no frame |
| Application start | listener stays closed, no default-room claim |
| WebFinger read adapter | HTTP 503, empty body, `no-store`, never HTTP 404 |
| Transaction-based schema migration | `#(error schema-migration-busy)`, no listener |
| Name-squatting check | busy propagates to the 1013 boundary |
| Member enrollment | no write after busy |

The consumer surface is larger than those six classes. A cross-module scan of
production LFE under `apps/` at the cbcl-bus baseline returns 57 call sites.
They consume 37 distinct in-lane public owners across 10 consumer modules.

`NFR-925` names six of those sites. It names `enrol-commit` by function name.
The remaining sites rely on the total rule alone.

**The Path-B revocation poller is outside every declared boundary.**
`apps/cbcl_chat/src/shell/cbcl-chat-path-b-poller.lfe:17` reads:

```lfe
(lists:foreach (lambda (account) ...) (cbcl-chat-account-grants:accounts))
```

`accounts/0` is a named authority-lane collection owner. The module is a
supervised `gen_server`, registered at
`apps/cbcl_chat/src/cbcl-chat-sup.lfe:65`. Its own header names it the
periodic Path-B resolver revocation ingestion loop. It is neither a WebSocket
consumer, nor application start, nor WebFinger, nor schema migration.

The success shape reaches a list function. That construct was probed at OTP
28.5 rather than assumed:

```text
lists:foreach over {ok,[]}         -> {error,function_clause}
lists:map     over {ok,[]}         -> {error,case_clause}
true andalso {ok,false}            -> {ok,{ok,false}}
```

`accounts/0` at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:280-285`
currently maps every non-atomic transaction result to `'()`. `NFR-925` forbids
busy becoming an empty list. The poller therefore requires an explicit busy
branch, and the requirement names no terminal for it.

**The second poller edge discards a verified revocation union.**
`apply-verified-union` at
`apps/cbcl_chat/src/shell/cbcl-chat-path-b-poller.lfe:32` reads:

```lfe
(case (cbcl-chat-account-grants:revoke-observed account ids)
  (`#(ok ,keys) (lists:foreach (lambda (key) (catch (cbcl-chat-path-b-live:evict account key))) keys) 'ok)
  (_ 'ok))
```

`revoke-observed/2` is a named authority owner. `NFR-925` forbids the wildcard
default. An implementer satisfies the letter with an explicit clause that
returns `'ok`. The `ids` value is a quorum-verified revocation union from
`cbcl_selfsame_erl:verified_path_b_revocations`. Discarding it silently skips
one revocation cycle and its live-session eviction fan-out.

`NFR-925` prohibits busy becoming an empty list, `false`, `undefined`,
`exists`, absence, or non-membership. A silent `'ok` from a revocation
applier is in none of those classes. Nothing requires a retry, an alarm, or an
`OBS-###` signal for the discarded union.

**The webhook path is the second undeclared class.**
`apps/cbcl_chat/src/shell/cbcl-chat-rooms.lfe:54` and `:61` consume
`cbcl-chat-roomcfg:get/1`. Line `:60` calls `cbcl-chat-roomcfg:claim/3` and
discards its result. Line `:66` is the fail-closed wildcard whose own comment
records the SPEC-055 reason:

> an unreadable config is NOT a licence to fan an untrusted webhook payload
> into what may be a private channel — refuse

That function is reached from `apps/cbcl_router/src/cbcl-webhook-handler.lfe:76`,
an HTTP webhook handler inside a second OTP application. `NFR-925` forbids the
fail-closed wildcard and declares no HTTP webhook terminal.

**The test row cannot be discharged.** `TEST-1150` states:

> Exercise every production consumer under both forms. Require every success to
> be unwrapped before use and every busy value to reach its declared boundary.

For the poller and for the webhook path there is no declared boundary. Under
[[PROTO-001-usdd-agent-protocol]] evidence discipline, that row is recorded
`unverified` and blocks the phase, or it is falsely passed. Cbcl-bus
`TEST-109` carries the identical row and the identical gap.

The consequence is bounded rather than an authority bypass. An unadapted
poller crashes and restarts. A conforming but under-specified poller delays
one revocation cycle. The poll interval defaults to 60,000 milliseconds, so
the next tick re-derives the union. The severity is MEDIUM rather than HIGH
for that reason, and it is not LOW because a revocation control is the
affected path.

**Minimum sound correction.** Declare the busy terminal for a periodic
background consumer and for a non-WebSocket HTTP consumer. State that a
periodic consumer records the busy result, reports no success, and retries on
its next tick. State that the webhook consumer refuses the fan-out. Add
`cbcl-chat-path-b-poller.lfe:17` and `:32` to the consumer map, together with
`cbcl-chat-rooms.lfe:54`, `:60`, and `:61`. Add an `OBS-###` signal for a
discarded revocation union, or state that no verified revocation union is
discarded.

---

### N8-L-1 (LOW) — the consumer map names one collection consumer, and six exist

**Repository owner: selfsame (NFR-925) and cbcl-bus (NFR-023).**

`NFR-925` states a definite singular:

> It also includes the collection consumer at `cbcl-chat-session-ws.lfe:869`.

The production scan returns six collection consumers of shape-changed owners.

| Site | Owner | Sink |
|---|---|---|
| `cbcl-chat-session-ws.lfe:512` | `device-grants/1` | pure admission map field |
| `cbcl-chat-session-ws.lfe:869` | `rooms-for-account/1` | `lists:foreach` |
| `cbcl-chat-session-ws.lfe:965` | `list-public/0` | `cbcl-core-wire:channels` |
| `cbcl-chat-session-ws.lfe:1683` | `grants/1` | `lists:map` |
| `cbcl-chat-room.lfe:677` | `peer-grants/1` | `lists:map` |
| `cbcl-chat-path-b-poller.lfe:17` | `accounts/0` | `lists:foreach` |

The three named boolean contexts are correct and complete. `andalso` was the
only probed construct that accepts a non-boolean silently.
`cbcl-chat-room.lfe:263-270`, `cbcl-chat-pairgrant.lfe:78`, and
`cbcl-chat-session-ws.lfe:1050` are the three production sites of that class.

No obligation is lost, because the total rule and the total `TEST-1150` row
both bind. The defect is that a definite singular understates a six-site class
inside a normative inventory sentence.

**Minimum sound correction.** Replace the singular with the enumerated set, or
state that a production consumer scan is the completeness authority for the
consumer map.

---

### N8-L-2 (LOW) — the baseline witness set is defined by reference into a requirement the same contract replaces in full

**Repository owner: selfsame (REQ-1002, CON-982).**

`CON-982` states that `REQ-1002` replaces `REQ-1001` in full. `REQ-1002`
states its own scan rule:

> Retired text supplies no survivor, test, coordinated set, pointer, or
> replacement authority.

`REQ-1002` then defines its witness set by reaching into that replaced text:

> Named baseline witnesses include every site listed by REQ-1001.

The eighteen-line block lives only inside
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#REQ-1001]]. All
eighteen lines were verified at the declared baseline and each resolves to the
construct its label claims. An authority scan applying the retirement rule
literally has to decide whether that block remains readable.

No unsafe implementation follows, because `REQ-1002` also makes the production
scan the completeness authority. A witness list cannot limit the scan.

**Minimum sound correction.** Restate the eighteen witness lines inside
`REQ-1002`, or state that the block is incorporated by exact reference and is
exempt from the retirement rule.

---

### N8-L-3 (LOW) — the classifier names five site classes and the scan obligation names three

**Repository owner: selfsame (REQ-1002, TEST-1151).**

`REQ-1002` states the classifier:

> The classifier includes every version constant, literal, comparison, emitted
> member, and typed version-field assignment.

It then states the completeness authority over three classes only:

> The production scan is the completeness authority for v1 comparisons,
> substitutions, and ingress conversions.

`TEST-1151` narrows the same way:

> Scan all production Rust sources for every v1 comparison, substitution, and
> ingress conversion under REQ-1002's classifier.

A version constant definition is none of those three. Two production
definitions sit in that gap.

| Site | Construct |
|---|---|
| `crates/selfsame-app-identity/src/lib.rs:109` | `pub const PROFILE_VERSION: i64 = 1;` |
| `crates/selfsame-app-identity/src/profile.rs:60` | `pub const PROFILE_VERSION: i64 = 1;` |

Neither file carries a `#[cfg(test)]` boundary above those lines, so both are
production code. `REQ-1002` also states that credential/v2 reaches no
classified v1 site. A scan built from the three named classes lists every use
of the constant and neither definition.

The residual risk is small, because every production use of `PROFILE_VERSION`
is itself a comparison or a substitution and appears in the scan.

**Minimum sound correction.** Align the two sentences with the classifier, or
state that a version constant definition is evidence rather than a scanned
class.

---

### N8-L-4 (LOW) — the ingress-conversion witness set is a closed copula that omits a third production site

**Repository owner: selfsame (REQ-1002, TEST-1151).**

`REQ-1002` states a closed enumeration:

> The direct ingress-conversion witnesses are
> `crates/selfsame-app-identity/src/ceremony.rs:413` and
> `crates/selfsame-app-identity/src/provider_hint.rs:96`.

Both witnesses resolve at the baseline. `REQ-1002` defines the class as
extraction from untyped input into a typed version-bearing value. A third
production site matches that definition:

```rust
// crates/selfsame-app-identity/src/profile.rs:384
let version = integer(value, "profileVersion")?;
```

`value` is an untyped `Json`. `version` is a typed `i64`. The site is
production code, and the comparison one line below it is already a named
witness at `profile.rs:385`.

`TEST-1151` requires a newly classified production site to appear in the
generated scan output, so the audit still covers it. The defect is the closed
copula in a normative sentence.

**Minimum sound correction.** Add `profile.rs:384`, or replace `are` with
`include` so the sentence stops asserting closure.

---

### N8-L-5 (LOW) — the authority owner list attaches the tagged-success contract to owners that are neither collections nor predicates

**Repository owner: selfsame (NFR-925).**

`NFR-925` states the tagged-success rule for one class:

> Public collection and predicate owners use tagged success. They return
> `#(ok Value)` or `#(error authority-busy)`.

It then introduces the two owner lists asymmetrically:

> These authority owners expose that contract: …
> These ceremony-migration owners expose the same busy result: …

The nearest antecedent of `that contract` is tagged success. Read that way,
`enroll-or-check/2`, `revoke-device-grant/2`, and `store-verified-device-grant/6`
each require a `#(ok Value)` success. Read the other way, only the busy result
binds. The ceremony-migration sentence carries no such doubt.

Cbcl-bus `NFR-023` states the same obligation without the ambiguity. It writes
`The authority public owners are` and `The ceremony-migration public owners are`,
and keeps the tagged-success rule in one lane-independent sentence.

Both readings are safe, because each owner's consumers are bound by the same
totality rule. The two repositories nevertheless state one coordinated
obligation in two shapes.

**Minimum sound correction.** Adopt the cbcl-bus wording in `NFR-925`, or name
which authority owners adopt the tagged-success shape.

---

### N8-L-6 (LOW) — the withdrawal refusal branch names no terminal for its new account-side disagreement case

**Repository owner: selfsame (NFR-925, TEST-1150) and cbcl-bus (NFR-023, TEST-109).**

`NFR-925` replaces the first `NFR-921` range with a stricter withdrawal
design:

> A present account row SHALL name the requested alias and home DID. …
> A disagreeing account row refuses. Neither case deletes any row.

The deployed `tombstone/2` at
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:206` has one account-side
disagreement, a stale home DID, and answers `#(error wrong-generation)`.
Deriving `requestedAlias` from `homeDid` creates a second case: an account row
whose stored alias is not the requested alias. That case has no named
terminal.

`TEST-1150` drives three withdrawal cases. It drives the deleted alias row,
the alias row pointing at another account or DID, and the absent account row.
It drives no case where the account row disagrees on the alias.

The safety property is stated, so both plausible terminals refuse and delete
nothing. The case also requires a corrupt store, because `provision/3` writes
the account alias as the exact function of the home DID.

**Minimum sound correction.** Name the terminal for an account row whose
stored alias is not the requested alias. Add the matching zero-deletion row to
`TEST-1150` and `TEST-109`.

---

## Disposition of every 0.4.22 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N7-H-1** — the current test demands `authority-busy` from `lookup/1` | **CLOSED** | `NFR-925` states that `lookup/1` is exempt from the `authority-busy` result form and returns `unavailable`. `TEST-1150` restores the lane qualifier, and its WebFinger paragraph requires `unavailable`, HTTP 503, an empty `no-store` response, and no HTTP 404. `cbcl-chat-webfinger-store.lfe:314-320` already answers `'unavailable` for every non-atomic transaction result. `cbcl-chat-webfinger-http.lfe:36-37` already maps `'unavailable` to 503 and carries `cache-control: no-store` in `base-headers`. The three-clause circle is broken, because the requirement no longer asks a wildcard consumer to branch on a value its owner never returns. Cbcl-bus `NFR-023` and `TEST-109` carry the same repair. |
| **N7-M-1** — no consumer obligation for the success value | **CLOSED** | `NFR-925` states the total rule: every production consumer handles both forms, unwraps `#(ok Value)` before use, and handles busy explicitly rather than through a wildcard default. It adds the prohibition that tagged success cannot reach a boolean operator, list function, pure admission map, or fail-closed wildcard. It names the three production `andalso`, `not`, and `if` sites at `cbcl-chat-room.lfe:263-270`, `cbcl-chat-pairgrant.lfe:78`, and `cbcl-chat-session-ws.lfe:1050`. `TEST-1150` requires every production consumer under both forms and requires the admission decision to remain total. The `andalso` hazard was re-probed at OTP 28.5 and reproduces. Two residual points are [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-M-1]] and [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-1]]. |
| **N7-M-2** — two of three exhaustion classes are unreachable | **CLOSED** | `NFR-925` reduces the wrapper class to `{aborted, nomore}` and states that Mnesia converts an exhausted level-1 cyclic restart to that value. It states that proper nesting commits silently and therefore has no runtime terminal or classifier. `TEST-1150` states that a raw exhausted `#cyclic{}` and `nested_transaction` are not required. Independent probes at OTP 28.5 reproduce all three claims. Forty concurrent opposed-order transactions with `Retries` of 3 produced `#{atomic => 8, {aborted,nomore} => 32}` in one run and `#{atomic => 3, {aborted,nomore} => 37}` in another. A body opening a transaction produced `{atomic,{inner_returned,{atomic,inner_ok}}}` and the inner write committed. `mnesia_tm.erl:918-927` and `mnesia_lib:fix_error/1` were read and agree. |
| **N7-M-3** — one authority-lane transaction is missing from the owner list and the nesting repair | **CLOSED** | `NFR-925` adds `cbcl-chat-successor:accept/3` to the authority owner list. It requires a transaction-only device-grant read and forbids `account-accepted?/2` calling transaction-opening `device-grant/2`. It forbids the helpers at `cbcl-chat-successor.lfe:109` and `:134` catching an `{aborted, _}` exit. Both line numbers were verified exactly. A repository-wide scan of the ten in-lane modules found exactly two production nesting edges, `cbcl-chat-account-grants.lfe:180` and `cbcl-chat-successor.lfe:134`, and exactly two in-body catch sites able to swallow an abort, at `:109` and `:134`. Both are now named. The abort-swallowing mechanism was reproduced: a `catch` around an in-transaction read returned `{atomic,{guard,{'EXIT',{aborted,cyclic…}}}}` in 7 of 24 trials and the enclosing transaction committed a later write. `TEST-1150` instruments both edges. |
| **N7-M-4** — the eighteen-site v1 inventory omits one production comparison | **CLOSED** | `REQ-1002` deletes the word `complete` and the numeral. It states that the production scan is the completeness authority and that a fixed list cannot limit it. It adds `crates/selfsame-app-identity/src/enrollment.rs:592`, classifies the three `selfsame-web-device/src/lib.rs` ingest copies as substitutions, and names two direct ingress conversions. All twenty-four named witnesses were verified line by line at the declared baseline and each resolves to the claimed construct. An independent scan of every production Rust source found the remaining candidates to be `#[cfg(test)]` code, type declarations, member-name constants, format templates, or comments. Three production sites remain outside the witness list. They are recorded as [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-3]] and [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-4]]. |
| **N7-M-5** — the replacement test drops four inline cases | **CLOSED** | `TEST-1150` restates all four groups inline. It restates the profile-digest silent re-pin with the authority and issuer prompt arm, the handle-change refusal with the unlink confirm and cancel, the fresh ceremony with the retained key after grant deletion, and the restored acquisition point with the invite-before-room order. `TEST-1151` replaces the open-ended phrase with a named reference to [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-995]]. Cbcl-bus `TEST-109` restates the same four groups and `TEST-110` names Selfsame `REQ-995`. `REQ-995` was read clause by clause and covers every restated case. |
| **N7-L-1** — two enumerated authority owners are ceremony-migration rows | **CLOSED** | `NFR-925` moves `account-binding/1` and `bind-account/2` into the ceremony-migration list. `account-binding/1` at `cbcl-chat-account-grants.lfe:70` reads only `cbcl-account-binding` inside its transaction, and `bind-account/2` at `:84` reads and writes only that table. The lists were then verified against module exports for all ten in-lane modules. Every exported function that opens a transaction appears, and every omitted export is either `ensure-table`, a transaction-only helper, a `dirty_read` accessor, a pure function, or the `transform_table` migration. |
| **N7-L-2** — replacing REQ-954 drops the parent REQ-903 and REQ-904 dispositions | **CLOSED** | `REQ-1002` restates both. It states that parent `REQ-903` remains effective with live approve and decline under the amended SPEC-007 `REQ-804` ordering. It states that parent `REQ-904` remains effective, that scan and paste decode into the same recognised type, and that the separate PAIR1 presence field remains a scan-only co-presence constraint. Both parent requirements exist at `specs/SPEC-008-production-pairing-claimant.md:163` and `:173`. |
| **N7-L-3** — the replacement pointer paragraph drops the open-gate half | **CLOSED** | `CON-982` restores the clause: the Selfsame parent points here and names this candidate's open review gate. It also drops the non-conventional title anchor and uses the plain document wikilink. The parent satisfies the obligation at `specs/SPEC-008-production-pairing-claimant.md:8` and `:17-18`, which name `CON-982` and state that the coordinated Tier-1 review gate is open. |
| **N7-L-4** — the name-squatting sentence is unscoped | **CLOSED** | `NFR-925` scopes the sentence: for the busy class, name-squatting checks propagate busy to the 1013 boundary and cannot report a free name. It records the `cbcl-provenance` wildcard in `addagent-target-free?/3` as an accepted out-of-lane residual for non-retryable failures, and disclaims any safety property outside the busy class. `TEST-1150` keeps the residual recorded and states that it repairs no non-retryable behaviour. The check at `cbcl-chat-session-ws.lfe:1047-1052` still has its two inputs, and `cbcl-provenance` still matches no lane rule. |
| **N7-L-5** — the recorded trajectory is a summary line | **CLOSED** | All three candidates now enumerate the trajectory. The Selfsame field names the initial draft, the failing-test artefact, the localised correction table, and this candidate. The provenance section maps each to its [[PROTO-001-usdd-agent-protocol]] role and states that all twelve repairs are applied. The twelve-row N7 disposition table supplies the adversarial dispositions. Cbcl-bus records a four-element trajectory and a nine-row hub disposition table. Cbcl-pairing records a four-element trajectory and states that no protocol-body change follows. |
| **N7-L-6** — the schema-migration lane names one site and a second exists | **CLOSED** | `NFR-925` scopes the universal to the named site and records the second migration. It states that `legacy-rows/0` does not overlap the runtime lanes, and that `cbcl-chat-roommember:migrate-since/0` is a second schema migration using `mnesia:transform_table/4` outside the transaction-call inventory. Both claims were verified. `cbcl-store-migrate.lfe:78-90` is the only production transaction over `cbcl-msg`, and `cbcl-chat-roommember.lfe:64` reaches `mnesia:transform_table/4` at `:78` and produces no inventory row. |

Twelve findings were raised and twelve close on code-backed evidence. Two
close with residual points that are recorded above as new LOW defects.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-982` names nothing inside `REQ-981`, which remains effective at `spec-008-0.4.11-standalone-tofu-first-contact.md:51`. Policy stays keyed on the exact `(applicationId, relayOrigin)` pair. The cbcl-bus GATE-04 box requiring the exact v2 policy pair to prompt is one of the 41 boxes that survive byte-for-byte. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched at the declared baseline, which was verified by an empty `crates/` diff. `crates/selfsame-pairing/src/live.rs:479-488` still commits one explicit local decision only from `ClaimantPhase::Endpoint`, and its own doc comment binds that call to `LiveEffect::DisplayIntent`. Cbcl-pairing 0.4.9 adds one file and changes no protocol language, which was verified by diffing the candidate against its predecessor. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-982` names nothing inside `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202`. Every busy path this amendment adds creates nothing. The WebSocket boundary emits no acknowledgement, refusal, invite, grant, or enrollment frame, and no partial effect survives an exhausted transaction. The four `REQ-995` regression groups that carry the local-unlink and fresh-ceremony obligations are restated inline in `TEST-1150`. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-982` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:902-908` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically or the browser displays no carrier. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-026` states that every `CON-025` survivor remains effective, and `CON-025` preserves the no-relay-field property of `CON-024`. The 0.4.9 diff adds one file and touches no protocol asset. The size-correlation residual stays bounded above the 62,016-octet sealed cap body. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:330-331` states that mismatch, protocol violation, cancellation, relay closure, and expiry produce no accepted credential and no identity side effect before approved payload delivery. Every negative path this amendment keeps or adds creates nothing. The 503 response carries an empty body and reveals no binding. The busy close emits no frame, so no pending ceremony exists to resume. [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-M-1]] is a hub storage-contention gap rather than an endpoint failure, so `REQ-812` is not the clause it touches. |

`CON-982` restates every F-A through F-E correction and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] as hard stops.

## Repository assessments

### selfsame SPEC-008 0.4.23-draft (`c48e6d5`)

The amendment applies exactly the replacements `CON-982` declares. `REQ-1002`
replaces `REQ-1001` in full. `NFR-925` replaces `NFR-924` in full and replaces
three `NFR-921` ranges. `ADR-960` replaces `ADR-959`. `TEST-1150` replaces
`TEST-1148`. `TEST-1151` replaces `TEST-1149`.

The three `NFR-921` range boundaries were verified against the `NFR-921` text
rather than against the `NFR-924` replacement text. Each opening and closing
phrase matches exactly. The paragraph that follows the first range survives,
so the already-withdrawn branch keeps its stated lock behaviour.

The successor chain resolves to one requirement. `REQ-954` and `REQ-963` were
replaced by `REQ-1001` and `REQ-1000`. `REQ-1000` replaced `REQ-999` and
`REQ-963`. `REQ-1001` replaced `REQ-1000` and `REQ-954`. `REQ-1002` replaces
`REQ-1001`. `TEST-1151` names four of those five predecessors, and `REQ-999`
reaches `REQ-1002` transitively.

The Selfsame test set resolves. Every effective test through `TEST-1108`
survives. `TEST-1134`, `TEST-1135`, `TEST-1139`, `TEST-1150`, and `TEST-1151`
are current. The five collectively replace `TEST-1129` through `TEST-1133`,
and every intermediate number reaches one of the five through a recorded
replacement.

Four defects are recorded against this repository alone:
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-2]],
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-3]], and
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-4]] concern the v1
scan and its witness set. [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-5]]
concerns the owner-list wording.

### cbcl-bus SPEC-053 0.16.16-draft (`b073072`)

`REQ-078` supplements `REQ-077` and retires its current-value range. `NFR-023`
replaces `NFR-022` in full and replaces three `NFR-019` ranges. The third
`NFR-019` range ends with `unmeasured threshold claim`, which was verified in
`spec-053-0.16.12-singular-authority-and-webfinger-order.md`. `NFR-023`
restates the load-evidence sentence that the longer range removed.

GATE-04 arithmetic is exact. The effective gate in
`spec-053-0.16.15-explicit-busy-contracts-and-complete-traces.md` carries 42
boxes, counted mechanically. The retired first box begins with
`- [ ] coordinated Selfsame 0.4.22` and ends with
`record generation provenance and receive fresh cross-model PASS;`, which
matches the file byte for byte. One box replaces one box, so 42 boxes remain
and 41 survive unchanged. Selfsame `TEST-1151` and cbcl-bus `TEST-110` agree
on 42.

The cbcl-bus test set agrees across the two repositories. `CON-982` and
`REQ-078` both state `TEST-043` through `TEST-065`, `TEST-068`, `TEST-070`,
`TEST-093`, `TEST-094`, `TEST-098`, `TEST-109`, and `TEST-110`.

The hub parent points here. `specs/SPEC-053-key-root-identity.md:15` and
`:688` name `REQ-078` at 0.16.16. The parent still points GATE-04 at the
0.16.15 gate, which is correct under the exact-fragment replacement `REQ-078`
declares.

The public owner enumeration was verified against module exports. It is
complete for all ten in-lane modules.
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-M-1]] and
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-1]] are shared with
selfsame. [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-6]] is
also shared.

### cbcl-pairing SPEC-001 0.4.9-draft (`46fdf6c`)

The candidate adds one file and changes nothing else, which was verified by
diffing `46fdf6c` against `9522269`. `REQ-031` states that the protocol body
remains `REQ-030`, `NFR-022`, and `ADR-023` from 0.4.8. `CON-026` retires only
the `consumer-design` pointer and states that every `CON-025` survivor remains
effective.

The survivor set is singular in content. `CON-025` and Selfsame `CON-978`
state the identical set. It contains `TEST-030` through `TEST-049`. It contains
`TEST-050` without its replaced control portion and its retired positive
witness. It contains `TEST-051` through `TEST-056`, and `TEST-059`. Both state
that `TEST-057` and `TEST-058` do not survive.

The frontmatter `consumer-design` value names Selfsame SPEC-008 0.4.23-draft.
Selfsame `CON-982` and cbcl-bus `REQ-078` both assert that pointer. The three
records agree.

No defect is recorded against this repository.

### did-crdt (`1f409a4`)

The commit exists and is `fix(rendezvous): bound storage, amortise expiry, cap
body before buffering`. It is a coordinated set member rather than a candidate.
Its 4,096-octet rendezvous cap remains deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. That non-conformance is a
recorded open gate rather than a defect.

## Attack surfaces examined and found sound

**Storage contention as false authority.** Every named owner's busy path was
traced. `NFR-925` forbids busy becoming an empty list, `false`, `undefined`,
`exists`, absence, or non-membership. The three prior fail-open mechanisms at
`cbcl-chat-members.lfe:28-30`, `cbcl-chat-account-roommember.lfe:94-97`, and
`cbcl-chat-successor.lfe:134` are all covered by a named repair.

**Nested transactions.** A cross-module call scan over the ten in-lane modules
found exactly two production edges where a transaction body reaches a
transaction opener. Both are named. Every other in-body cross-module call
reaches a transaction-only helper or a pure core function.

**Abort swallowing.** Every `catch` inside an in-lane transaction body was
enumerated. Three exist. Two are named and forbidden. The third, at
`cbcl-chat-successor.lfe:90`, wraps `base64:decode` and touches no Mnesia
operation, so it cannot swallow an abort.

**Out-of-scope lane leakage.** The seven out-of-lane modules were checked for
cross-module calls inside their transaction bodies. `cbcl-chat-groupinfo` and
`cbcl-chat-epochclaim` call each other, and both tables are out of scope.
`cbcl-chat-pairgrant.lfe:78` calls an in-lane owner outside its transaction.
No out-of-lane transaction body reaches a lane table.

**WebFinger false absence.** The read path was re-traced end to end.
`lookup-binding/1` answers `'missing` only for a committed absence or a
disagreeing reverse row. `lookup/1` answers `'unavailable` for every non-atomic
transaction result. `cbcl-chat-webfinger-http.lfe:36-37` maps `'unavailable`
to 503 with an empty body and `no-store`, and never to 404.

**Cross-account withdrawal deletion.** The reciprocal condition was re-run
against the specified branch. The inputs are alias row `{U, B, D}` and account
row `{A, U, D}`. The account row agrees on the requested alias and DID. The
alias row names a different account. The branch refuses and deletes nothing.
The deployed `alias-agrees?` predicate at
`cbcl-chat-webfinger-store.lfe:232-236` already implements the reciprocal
check, and the amendment strengthens the alias derivation.

**Reinstatement lock order.** `NFR-925` removes the account lock from
`reinstate/2` and requires one whole withdrawal write lock before the first
`index_read`. A single-lock operation cannot participate in a lock cycle, so
the change removes a cycle rather than adding one.

**Nonce collision retry.** `cbcl-chat-grant-nonce.lfe:65-89` recurses from
outside its transaction, in the `#(atomic collision)` branch. Each attempt is a
fresh non-nested transaction. `NFR-925` classifies it correctly as a semantic
uniqueness retry.

**Schema migration mistaken for absence.** `cbcl-store-migrate.lfe:78-113`
already separates a genuine `no_exists` from a transient failure and answers
`#(error ...)` for the second. `NFR-925` types the contention case as
`schema-migration-busy` and requires a closed listener.
`cbcl-chat-app.lfe:88-90` and `:109` already refuse to open the listener on a
failed migration.

**Version freeze.** Every production Rust source was scanned. Every named
witness resolves. Every unnamed match falls into one of six classes. Those
classes are `#[cfg(test)]` code, a type declaration, a member-name constant, a
format template, a comment, and the three sites recorded as LOW defects. The
pairing, link-code, and QR
version identifiers belong to different namespaces and are outside the
credential/v2 classifier.

**Boolean-context smuggling.** The three production sites where a non-boolean
passes silently were re-derived rather than inherited. `andalso` and `orelse`
return their second operand unchecked. `not`, `if`, and `cond` raise. Only
`andalso` appears in a production admission path, and all three of its sites
are named.

## Residual assumptions and open gates, separated from defects

These are not defects. Each is a recorded assumption or a gate that stays
open by design.

1. **The coordinated Tier-1 gate is open.** All three candidates carry
   `review-gate: not-approved`. `CON-982` states that a fresh PASS authorises
   only the Elephant SPL and test-first plan.
2. **GATE-04 stays 42 boxes open.** This amendment closes none of them. The
   cbcl-bus text states that directly.
3. **The PROTO-002 rendezvous gate stays open.** Both reviewed rendezvous
   baselines cap at 4,096 octets and are deliberately non-conforming to
   [[PROTO-002-selfsame-rendezvous-v1#REQ-305]].
4. **The `cbcl-provenance` wildcard is an accepted out-of-lane residual.**
   `NFR-925` records it and disclaims any repair of non-retryable behaviour.
   `cbcl-chat-app.lfe` waits for the table at boot, so the window stays narrow.
5. **`cbcl-chat-successor:accept/3` has no production caller.** A scan of
   non-test callers reaches `ensure-table/0` at `cbcl-chat-app.lfe:28`,
   `spent?/1` at `cbcl-chat-session-ws.lfe:343` and `:381`, and
   `for-account-handle/2` with `bare/1` at `cbcl-chat-room.lfe:528-529`. The
   row is still production code and the amendment now covers it.
6. **The cbcl-pairing parent carries no amendment pointer.**
   `specs/SPEC-001-reusable-blind-pairing.md` is version 0.2.0 and references
   no trajectory amendment. No candidate requires it to. This is an inherited
   repository property rather than a pointer-set defect.
7. **The 1013 close code and the browser generation are new behaviour.**
   Neither `1013` nor `try-again-later` appears in the cbcl-bus source or web
   assets today. Both are specified obligations rather than observed
   behaviour.
8. **The transaction-inventory non-conformance sentence is Selfsame-only.**
   `NFR-925` states that omitted, unclassified, or unsupported out-of-scope
   rows are non-conforming. Cbcl-bus `NFR-023` states no equivalent sentence,
   as `NFR-022` did not. Cbcl-bus `TEST-109` and `TEST-110` carry the
   completeness obligation instead.
9. **Lane rule 5 names a function rather than a table.** The other four lane
   rules are table-derived. `cbcl-msg` is reachable from one production
   transaction only, so nothing is misclassified.
10. **`enroll-or-check/2` is two transactions today.** `cbcl-chat-members.lfe:42-54`
    performs a read transaction and a separate write transaction, and
    discards the second result. `NFR-925` requires one transaction and a
    result that reaches `enrol-commit`. That is a specified repair.
11. **The consumer inventory has no named derivation mechanism.** The
    transaction inventory and the v1 inventory each name a scan as their
    completeness authority. The consumer inventory relies on the total
    `TEST-1150` row instead.

## What this verdict authorizes and does not authorize

This verdict is REJECT. It authorizes nothing.

It does not authorize implementation, an Elephant SPL theory, a test-first
plan, code, production allocation, a release, or a deployment. It does not
close any GATE-04 box. It does not alter DEC-012's production deferral. It
does not change the PROTO-002 Tier-1 gate.

Under `CON-982`, a later PASS on a corrected candidate authorizes only the
Elephant SPL and the test-first plan. Even that PASS authorizes no code until
the primary agent independently verifies and merges the review evidence.

This report is one reviewer's evidence. It is not a merge decision and not a
gate closure.

## Corrections and owners, collected

| Finding | Owner repositories | Minimum sound correction |
|---|---|---|
| N8-M-1 | selfsame, cbcl-bus | Declare the busy terminal for a periodic background consumer and for a non-WebSocket HTTP consumer. State that a periodic consumer records the busy result, reports no success, and retries on its next tick. State that the webhook consumer refuses the fan-out. Add `cbcl-chat-path-b-poller.lfe:17` and `:32`, and `cbcl-chat-rooms.lfe:54`, `:60`, and `:61`, to the consumer map. Add an `OBS-###` signal for a discarded revocation union, or state that none is discarded. |
| N8-L-1 | selfsame, cbcl-bus | Replace the definite singular with the six-site enumeration, or state that a production consumer scan is the completeness authority for the consumer map. |
| N8-L-2 | selfsame | Restate the eighteen witness lines inside `REQ-1002`, or state that the block is incorporated by exact reference and is exempt from the retirement rule. |
| N8-L-3 | selfsame | Align the completeness-authority sentence and the `TEST-1151` scan row with the five-class classifier, or state that a version constant definition is evidence rather than a scanned class. |
| N8-L-4 | selfsame | Add `crates/selfsame-app-identity/src/profile.rs:384` to the ingress-conversion witnesses, or replace `are` with `include`. |
| N8-L-5 | selfsame | Adopt the cbcl-bus `NFR-023` wording in `NFR-925`, or name which authority owners adopt the tagged-success shape. |
| N8-L-6 | selfsame, cbcl-bus | Name the terminal for an account row whose stored alias is not the requested alias, and add the matching zero-deletion row to `TEST-1150` and `TEST-109`. |

## Report self-audit

| Property | Value | Source |
|---|---|---|
| Findings raised | 7 | this report |
| CRITICAL findings | 0 | severity table |
| HIGH findings | 0 | severity table |
| MEDIUM findings | 1 | severity table |
| LOW findings | 6 | severity table |
| 0.4.22 findings dispositioned | 12 | disposition table |
| Architecture hard stops dispositioned | 6 | hard-stop table |
| Production transaction sites enumerated | 70 | `grep -rn 'mnesia:transaction' apps/` less 2 comment lines |
| Production consumer call sites enumerated | 57 | cross-module scan of production LFE under `apps/` |
| Named v1 witness sites verified | 24 | line-by-line read at the baseline |
| cbcl-bus GATE-04 boxes counted | 42 | `grep -c '^- \[ \]'` over the effective gate |

The severity table, the corrections table, and this audit agree on one MEDIUM
and six LOW findings.

## Changelog

- **2026-08-23 — coordinated Tier-1 adversarial review of SPEC-008 0.4.23,
  cbcl-pairing 0.4.9, and cbcl-bus 0.16.16 — REJECT.** All twelve 0.4.22
  findings close. One new MEDIUM defect and six new LOW defects stand.
