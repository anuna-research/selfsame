---
id: SPEC-008-amendment-0.4.25
title: SPEC-008 0.4.25-draft — Fail-closed room start and singular authority
status: draft
version: 0.4.25-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#CON-983]] only where [[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]] says so"
adopts: "All effective 0.4.4 through 0.4.24 clauses except the exact replacements in CON-984"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.11-draft; cbcl-bus SPEC-053 0.16.18-draft; [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]]"
generation-model-family: OpenAI GPT-5
generation-model-version: gpt-5.6-sol
generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6
generation-synthesis-trajectory: "[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#Tier-1-generation-provenance]] -> [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#Findings]] -> [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]] -> this candidate"
last-updated: 2026-08-23
---

# SPEC-008 0.4.25-draft — Fail-closed room start and singular authority

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** This amendment addresses every 0.4.24
> review finding. It authorizes no planning, implementation, allocation,
> release, or deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. The authenticated application,
exact `(applicationId, relayOrigin)` consent, authenticated intent display,
fresh-scope bootstrap, and post-consent effect order remain unchanged.

## Tier-1 generation provenance

OpenAI GPT-5 model `gpt-5.6-sol` generated this coordinated synthesis. Its
Codex session is `01a029aa-9127-7c42-ad28-81512b91ded6`.

The prior candidate is
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#Tier-1-generation-provenance]].
Its failing evidence and attribution map are in
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#Findings]].

The local correction input is
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]].
This candidate applies its one HIGH, one MEDIUM, and six LOW corrections.

A qualifying reviewer SHALL use another model family and a fresh session. The
report SHALL record its model, authentication path, session, and Circus
attempt. It SHALL verify distinctness from this generation record.

The reviewer SHALL execute strict controlled-language lint before deciding.
The reviewer SHALL trace supervised room start through the webhook boundary.

## Requirement

### REQ-1004 — Credential v1 has one closed scan and one companion target

This requirement replaces
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#REQ-1003]] in
full.

It also replaces this current sentence in
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#REQ-1002]]:

```text
Credential/v2 SHALL conform to cbcl-pairing SPEC-001 0.4.9-draft.
```

The replacement sentence is:

```text
Credential/v2 SHALL conform to cbcl-pairing SPEC-001 0.4.11-draft.
```

The production scan is the completeness authority for every classified
credential-v1 site. Its closed identifier set is `PROFILE_VERSION`,
`profileVersion`, `evidenceVersion`, and `payloadVersion`.

Its classes are version-constant definition, version literal, comparison,
emitted member, typed version-field assignment, ingest substitution, and
untyped ingress conversion.

A field-to-field copy on an ingest path is a substitution. Extraction from
untyped input into a typed version-bearing value is an ingress conversion.

The scan distinguishes production code from `#[cfg(test)]` code,
member-name constants, type declarations, format templates, and comments.

Three other version namespaces are outside this closed credential-v1 set:

- `successionVersion` belongs to identity succession under
  [[SPEC-004-application-scoped-identity#REQ-231]] and
  [[SPEC-004-application-scoped-identity#CON-225]];
- `WIRE_VERSION` belongs to the Selfsame core invitation-wire namespace under
  Selfsame core SPEC-001 REQ-016; and
- `CODE_VERSION` belongs to the Selfsame core link-code namespace under
  Selfsame core SPEC-001 CON-001.

The following retired-list witnesses are incorporated as current evidence.
The class column below governs and replaces every retired vocabulary label.

| Production site | Current class |
|---|---|
| `crates/selfsame-web-device/src/lib.rs:600` | emitted member |
| `crates/selfsame-web-device/src/lib.rs:1346` | version literal |
| `crates/selfsame-web-device/src/lib.rs:1360` | version literal |
| `crates/selfsame-web-device/src/lib.rs:1369` | typed version-field assignment |
| `crates/selfsame-app-identity/src/ceremony.rs:185` | emitted member |
| `crates/selfsame-app-identity/src/ceremony.rs:190` | emitted member |
| `crates/selfsame-app-identity/src/ceremony.rs:308` | comparison |
| `crates/selfsame-app-identity/src/ceremony.rs:460` | emitted member |
| `crates/selfsame-app-identity/src/ceremony.rs:510` | comparison |
| `crates/selfsame-app-identity/src/enrollment.rs:373` | comparison |
| `crates/selfsame-app-identity/src/enrollment.rs:394` | comparison |
| `crates/selfsame-app-identity/src/enrollment.rs:452` | typed version-field assignment |
| `crates/selfsame-app-identity/src/enrollment.rs:552` | comparison |
| `crates/selfsame-app-identity/src/enrollment.rs:662` | emitted member |
| `crates/selfsame-app-identity/src/enrollment.rs:666` | emitted member |
| `crates/selfsame-app-identity/src/profile.rs:385` | comparison |
| `crates/selfsame-app-identity/src/provider_hint.rs:67` | emitted member |
| `crates/selfsame-app-identity/src/provider_hint.rs:115` | comparison |

Additional comparison evidence is
`crates/selfsame-app-identity/src/enrollment.rs:592`.

Ingest-substitution evidence is `crates/selfsame-web-device/src/lib.rs:831`,
`:847`, and `:861`.

Untyped ingress-conversion evidence includes
`crates/selfsame-app-identity/src/ceremony.rs:413`,
`crates/selfsame-app-identity/src/provider_hint.rs:96`, and
`crates/selfsame-app-identity/src/profile.rs:384`.

Production version-constant evidence includes
`crates/selfsame-app-identity/src/lib.rs:109` and
`crates/selfsame-app-identity/src/profile.rs:60`.

Named sites are evidence only. They cannot close or limit the production
scan.

Credential/v2 reaches no classified credential-v1 site. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.

## Non-functional requirement

### NFR-927 — Unavailability never selects a channel policy

This requirement replaces
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#NFR-926]] in
full. It supplements
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]].

It applies the four NFR-925 replacements declared by NFR-926, with two
corrected range boundaries.

The first range starts with "A present account row SHALL name". It ends with
"both live deletes under the held locks." The replacement is:

A present account row whose home DID differs returns
`#(error wrong-generation)`. A matching home DID with another stored alias
returns `#(error inconsistent-binding)`.

The write-locked alias row SHALL name that same account and home DID. An absent
or disagreeing alias row returns `#(error inconsistent-binding)`.

Every refusal deletes zero alias, account, and withdrawal rows. Only reciprocal
agreement continues to the withdrawal lock and both live deletes.

The second range is the sentence "These authority owners expose that
contract:". Its replacement is "The authority public owners are:".

The third fragment is the sentence "These ceremony-migration owners expose
the same busy result:". Its replacement is "The ceremony-migration public
owners are:".

Only collection and predicate owners adopt tagged success. Every other public
owner retains its declared success shape and adds its lane's typed busy shape.

The fourth range starts with "Every production consumer handles both tagged
result forms". It ends with "fail-closed wildcard." The replacement is:

A repository-wide production consumer scan is the completeness authority.
Every row names owner, consumer path and line, success sink, busy branch, and
terminal boundary.

Every consumer handles the owner's success shape and
`#(error authority-busy)`. It unwraps `#(ok Value)` before use and never
handles busy through a wildcard default.

Known collection sinks include:

- `cbcl-chat-session-ws.lfe:512`, consuming `device-grants/1` into an
  admission map;
- `cbcl-chat-session-ws.lfe:869`, consuming `rooms-for-account/1` through
  `lists:foreach`;
- `cbcl-chat-session-ws.lfe:965`, consuming `list-public/0` through the wire
  channel encoder;
- `cbcl-chat-session-ws.lfe:1683`, consuming `grants/1` through `lists:map`;
- `cbcl-chat-room.lfe:677`, consuming `peer-grants/1` through `lists:map`;
- `cbcl-chat-path-b-poller.lfe:17`, consuming `accounts/0` through
  `lists:foreach`; and
- `cbcl-chat-room.lfe:221` and `:550`, consuming `get-dialects/1` into room
  state or a room-configuration frame.

The boolean contexts are `cbcl-chat-room.lfe:263-270`,
`cbcl-chat-pairgrant.lfe:78`, and `cbcl-chat-session-ws.lfe:1050`.

The inventory includes `unratified-device?/1` at
`cbcl-chat-session-ws.lfe:347` and `:385`. It includes
`cbcl-chat-path-b-poller.lfe:17` and `:32`.

It includes the discarded `claim/5` result at
`cbcl-chat-session-ws.lfe:1898`. Busy reaches the existing WebSocket 1013
terminal and never becomes `'ok`.

The webhook inventory includes `cbcl-chat-rooms.lfe:54`, `:60`, and `:61`.
It includes `cbcl-chat-room.lfe:201` and `:221` during supervised room start.

It includes `cbcl-chat-rooms.lfe:49` and `:99-104`, plus the HTTP edge at
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76`.

Tagged success cannot enter a boolean operator, list function, admission map,
wire encoder, channel-policy default, or refusal wildcard.

Retry-exhausted transaction contention returns
`#(error authority-busy)`. Other transaction failure from
`cbcl-chat-roomcfg:get/1` remains `#(error store-unavailable)`.

The two results remain distinct through every consumer. Neither result SHALL
become `#(error unknown-room)`, a private-channel decision, or a channel
configuration.

Only `#(error unknown-room)` MAY select the ADR-007
`#(public undefined false)` default.

At supervised room start, `authority-busy` and `store-unavailable` both stop
the child. They create no ETS registration, room process, room state, or
fan-out.

Busy from `get-dialects/1` during room start also stops the child. Busy during
`refresh-dialects` retains the prior dialect set and emits no frame.

`cbcl-chat-rooms:get-or-start/1` preserves an `authority-busy` child-start
failure. `cbcl-chat-rooms:ingest/3` returns that value without calling
`cbcl-chat-room:ingest/3`.

For a periodic Path-B tick, busy from `accounts/0` emits OBS-905 with phase
`account-scan`. The tick reports no success, calls no resolver, and leaves the
supervised poller alive.

Busy from `revoke-observed/2` emits OBS-905 with phase `revocation-apply`. It
reports no applied union, performs no eviction, and defers that account to the
next scheduled tick.

The poller MAY continue other accounts after that result. It MUST NOT report
the discarded verified union as applied.

Busy from the webhook's room-configuration calls propagates as
`#(error authority-busy)`. The same value propagates from supervised room
start.

The webhook maps `authority-busy` to HTTP 503 with status `authority-busy` and
`cache-control: no-store`. It maps `store-unavailable` to a retryable HTTP 503
with its distinct status and the same cache policy.

Either 503 releases any reserved blob slot. It performs no room start, room
ingest, persistence, or channel fan-out.

HTTP 409 remains only for a committed non-public-channel decision.
Unavailability never becomes that policy refusal.

## Observation

### OBS-906 — Room-start unavailability is visible without room data

The hub records `room-start-authority-busy` when supervised room start refuses
retry-exhausted contention. It records the terminal boundary, not the room
name.

The event contains no room name, channel capability, account DID, credential,
profile digest, or grant key. A counter MAY aggregate by terminal boundary.

OBS-905 remains the Path-B poller signal. Neither observation claims a
successful operation.

## Contract

### CON-984 — N9 corrections replace their current authority once

This contract replaces
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#CON-983]] in
full. Every earlier replacement remains effective unless this contract states
otherwise.

REQ-1004 replaces REQ-1003 in full and updates REQ-1002's companion sentence.
NFR-927 replaces NFR-926 in full and applies its corrected NFR-925 fragments.

OBS-906 supplements OBS-905. ADR-962 supplements ADR-961.

TEST-1154 replaces TEST-1152 in full. TEST-1155 replaces TEST-1151 and
TEST-1153 in full. Every transitive predecessor relationship remains intact.

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, and TEST-109 through TEST-114.

Every effective Selfsame test through TEST-1108 also survives. TEST-1134,
TEST-1135, TEST-1139, TEST-1150, TEST-1154, and TEST-1155 are current.

TEST-1134, TEST-1135, TEST-1139, TEST-1154, and TEST-1155 collectively replace
TEST-1129 through TEST-1133. No superseded portion or open range supplies
authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.11, and
cbcl-bus 0.16.18. It contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

The Selfsame parent [[SPEC-008-production-pairing-claimant]] points here and
names this candidate's open review gate.

The cbcl-bus SPEC-053 parent at DEC-012 points to cbcl-bus 0.16.18.
Cbcl-pairing 0.4.11 points its `consumer-design` to this candidate.

Cbcl-pairing's recorded generation provenance participates in the coordinated
gate.

Every F-A through F-E correction remains a hard stop.
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] also remains a hard stop.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It
authorizes no code, allocation, release, or deployment.

## Decision

### ADR-962 — Typed unavailability fails closed before channel policy

`authority-busy` identifies retry-exhausted contention. `store-unavailable`
identifies other room-configuration transaction failure.

Both values refuse supervised room start. Only committed absence MAY select
an unconfigured public channel.

The HTTP consumer owns its retry terminal. The poller owns its next-tick
terminal. The room process owns its start refusal and refresh-state boundary.

The production consumer scan remains authoritative. Named witnesses support
that scan and never limit it.

Authenticated display, post-consent effects, shared grammar, cap arithmetic,
and the sealed-grant graph remain unchanged.

## Test specification

### TEST-1154 — Every asynchronous and room-start terminal is fail-closed

This test replaces
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#TEST-1152]] in
full.

Generate the production consumer inventory from every public owner. Require
owner, consumer, success sink, busy branch, and terminal boundary for every
row.

Drive every known collection sink and every newly discovered collection sink.
No tagged value reaches its sink before unwrap.

Drive `cbcl-chat-room.lfe:221` and `:550` under success and busy. Room start
stops on busy. Refresh retains prior dialects and emits no frame.

Drive `cbcl-chat-session-ws.lfe:1898` under success and busy. Busy reaches
WebSocket 1013 and never becomes `'ok`.

Force retry exhaustion and another transaction failure at
`cbcl-chat-roomcfg:get/1`. Require distinct `authority-busy` and
`store-unavailable` results.

Force each result during supervised room start. Require a refused child,
zero ETS registration, zero default configuration, and zero fan-out.

Force `unknown-room` separately. Require only that result to select the
ADR-007 public and cleartext default.

Force busy at the webhook's initial `get/1`, `claim/3`, and post-claim
`get/1`. Force it again at supervised room-process start.

Require HTTP 503 `authority-busy`, `no-store`, released reservation, and zero
room ingest, persistence, or fan-out for each forcing.

Force `get-dialects/1` busy during that room start. Require the same HTTP
terminal and zero room registration or fan-out.

Force `store-unavailable` on the webhook route. Require its distinct retryable
503 status, `no-store`, and zero effect.

Drive a committed private-channel decision. Require HTTP 409 and prove that
neither unavailability branch reaches that terminal.

Force `accounts/0` busy during a scheduled Path-B tick. Require OBS-905 phase
`account-scan`, no resolver call, no success report, and a live poller.

Force `revoke-observed/2` busy after a verified union. Require OBS-905 phase
`revocation-apply`, no eviction, and a fresh resolver pass next tick.

Give withdrawal a matching home DID and a different stored alias. Require
`#(error inconsistent-binding)` and zero deletion from all three WebFinger
tables.

Give withdrawal another home DID. Require `#(error wrong-generation)` and
zero deletion.

Every existing TEST-1150 regression remains REQUIRED. This includes its four
inline REQ-995 groups and WebFinger read case.

### TEST-1155 — Current authority, pointers, and version scan are singular

This test replaces
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#TEST-1151]] and
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#TEST-1153]] in
full.

Resolve every effective clause from 0.4.4 through CON-984. Require one
protocol survivor set, hub set, Selfsame correction set, review set, and
pointer set.

Require REQ-1004 as the current successor of REQ-1003. Require cbcl-pairing
0.4.11 as the sole companion target.

Require the retained dispositions of parent
[[SPEC-008-production-pairing-claimant#REQ-903]] and
[[SPEC-008-production-pairing-claimant#REQ-904]].

Scan every production Rust source under REQ-1004's closed four-identifier set
and seven-class classifier.

Generate path, line, namespace, class, and production-or-test disposition for
every match. Require every current witness and exclusion to resolve.

Require `successionVersion`, `WIRE_VERSION`, and `CODE_VERSION` to resolve to
their named out-of-scope namespaces and governing contracts.

Require credential/v2 to reach no classified credential-v1 site. Require
frozen v1 bytes to remain byte-identical.

Require the Selfsame parent here and its open review gate. Require the
cbcl-bus DEC-012 pointer at 0.16.18.

Require cbcl-pairing 0.4.11's `consumer-design` here. Require generation
family, version, session, and synthesis trajectory in all three candidates.

Require another reviewer family and a fresh reviewer session. Require the
review to record its subscription or API authentication path.

Require the transaction inventory to cover every production call under
`apps/`. Require table-derived classifications and exact public-owner results.

Require all 42 cbcl-bus GATE-04 boxes. Require both non-conforming rendezvous
baselines and sealed cap arithmetic.

Require every installed-state clause in
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-995]]. Require
the four inline regression groups from TEST-1150.

Require every N9 finding to have a code-backed disposition. Re-test every N8
and N7 disposition and every F-A through F-E hard stop.

Run `tools/usdd-lint.sh --type descriptive --strict` against all three
candidates. Any error or warning fails this test.

Removing any current obligation fails.

## N9 disposition

| Finding | Disposition in this candidate |
|---|---|
| N9-H-1 | NFR-927 preserves both failure atoms, closes room start, forbids policy defaults, and tests the fourth webhook forcing. |
| N9-M-1 | REQ-1004 updates the companion sentence. TEST-1155 replaces both stale authority tests and names only current pointers. |
| N9-L-1 | NFR-927 drops the numeral and adds both dialect sinks plus the discarded `claim/5` result. |
| N9-L-2 | REQ-1004 relabels every incorporated witness under the current seven-class classifier. |
| N9-L-3 | REQ-1004 closes the four-identifier set and names three governed exclusions. |
| N9-L-4 | NFR-927 and REQ-1004 replace their predecessors in full, so no duplicate survivor text remains. |
| N9-L-5 | CON-984 restates the five-test collective replacement and every transitive predecessor relationship. |
| N9-L-6 | Both coordinated candidates use uppercase BCP 14 keywords. TEST-1155 requires strict lint. |

## Gate and changelog

Owner direction selects standalone first contact. A fresh-context cross-model
Tier-1 PASS remains mandatory before the Elephant SPL or test-first plan.

- **0.4.25-draft — 2026-08-23 — review candidate, not approved.** Closes the
  fail-open room-start gap and makes current authority singular. No code,
  planning, release, or deployment authority is claimed.
