---
id: SPEC-008-amendment-0.4.4
title: SPEC-008 0.4.4-draft — Standalone pair-scoped TOFU with co-present CPace
status: draft
version: 0.4.4-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-804]]; [[SPEC-007-cbcl-pairing-cutover#REQ-805]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-007-cbcl-pairing-cutover#REQ-813]]; [[SPEC-007-cbcl-pairing-cutover#NFR-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; [[SPEC-007-cbcl-pairing-cutover#CON-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-806]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-204]]; [[SPEC-004-application-scoped-identity#CON-206]]; [[SPEC-004-application-scoped-identity#CON-211]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-221]]; cbcl-bus SPEC-075 REQ-007; did-crdt HTTP service contract; cbcl-pairing SPEC-001 REQ-003/REQ-005 through coordinated 0.3.0-draft"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; cbcl-pairing SPEC-001 REQ-002/NFR-002 and retained hard stops"
supersedes: "[[spec-008-0.4.3-standalone-tofu-first-contact]]"
review-inputs: "[[spec-008-0.4.3-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md at 7ddea48"
last-updated: 2026-08-23
---

# SPEC-008 0.4.4-draft — Standalone pair-scoped TOFU with co-present CPace

> **Design target, not implementation authority.** The owner selected standalone
> first-contact pairing and person-owned relay TOFU. The independent Claude/Opus
> Circus review rejected 0.4.3. This revision disposes every finding in that
> review. No production source change is authorised until a new fresh-context,
> different-model Tier-1 review returns PASS over this exact document, the
> coordinated cbcl-pairing SPEC-001 0.3.0 draft, and the named source revisions.
> PASS authorises test-first implementation planning; it is not production or
> deployment approval.

## Outcome and security boundary

A person starts from a signed-in application page. That page displays two
separate values:

1. a machine-readable **public carrier** containing the cbcl-pairing v2 route,
   application context, ceremony binding, profile version, and a commitment to
   an independent relay claim token, but no claim token or CPace secret; and
2. a visibly non-mnemonic `PAIR1-…` **presence code** encoding independent
   128-bit values `C` and `T`. `C` is the CPace password input; `T` authorises
   the relay claim before the mailbox can be crowded.

The wallet authenticates the application profile live, obtains explicit consent
before contacting a new origin, and asks whether that exact application may use
that exact relay. CPace then proves that the allocator and claimant share the
separately conveyed `C`. The authenticated application backend supplies a
closed, account-bound CON-219 offer. The wallet shows only values with truthful
provenance, obtains exact-intent consent, derives only an ephemeral issuer DID
preview, and asks the person to compare its fingerprint with the application
screen. Only the final release approval may cause signatures, resolver
publication, issuance-state writes, or a grant payload. The application accepts
the grant against a ceremony-pending reciprocal record, installs the connection,
atomically finalizes alias plus pairaccount authority at the hub, obtains the
real correlated acknowledgement, and only then sends a receipt.

This remains one cbcl-pairing protocol ceremony. It does not run SPEC-004
rendezvous enrolment and does not write or consume a held pairing-trust record.
It reuses the stronger CON-214/219 signed offer and the CON-221 human comparison
instead of inventing weaker account or grant authority.

The protocol establishes these bounded properties:

- the profile served by canonical application `A` declares relay `R`;
- the person accepted the exact pair `(A, R)` or had already accepted it;
- both cbcl-pairing endpoints held the 128-bit presence secret displayed on the
  signed-in application surface and transcribed into the wallet;
- the application backend asserts that one live application session, account
  label, account principal, device key, scope, and permission request belong to
  the signed offer; and
- the person approved the exact offer and, when required, confirmed the issuer
  fingerprint on both surfaces.

It does **not** prove legal ownership of the displayed account label, relay
operatorship, wallet provenance, or that a person who deliberately forwards both
the public carrier and presence code is co-present. The UI SHALL say to enter the
code only from the application page the person opened and SHALL never label the
application-asserted account as a civil or independently verified identity.

```text
 signed-in browser / allocator                         Selfsame / claimant
 ─────────────────────────────                         ───────────────────
 hub binds ceremony to live account
 show public QR (no C or T) + PAIR1 code(C,T)
 public carrier ─────────────────────────────────────▶ recognise without I/O
                                                       ask before contacting origin
                                                       live CON-220 profile fetch
                                                       exact (app,relay) policy gate
 PAIR1 code (person transcribes) ───────────────────▶ relay claim T + CPace C

 CPace + Finished  ◀════════ blind relay ═══════════▶ CPace + Finished

 signed account/device CON-219 offer ───────────────▶ authorise + consume request ID
                                                       truthful exact-intent display
                                                       preliminary consent
                                                       ephemeral DID preview only
 preparation(issuer DID/fingerprint) ◀────────────────
 independently resolve authority; display fingerprint
 comparison/binding status ──────────────────────────▶ final release approval
                                                       ───── final hard stop ─────
                                                       sign + publish + persist
grant bundle ◀────────────────────────────────────────
 pending alias record + CON-206/207 + atomic local install
 atomic alias/pairaccount finalize + real acknowledgement
receipt(bundle/grant/ack digests) ──────────────────▶ report linked
```

## Reviewed baselines

The rejected review verified these baselines; this revision's review SHALL trace
them again and SHALL record any intervening revision explicitly:

| Repository | Reviewed revision | Load-bearing observed behaviour |
|---|---|---|
| `selfsame` | `59db90ba027c` review/spec history over code parent `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | current pairing displays wire claims and derives before the socket; `HomeKey::home_did` can compute a DID without `issuer::create`; current issuance signs before CON-221; no issuer publication function exists |
| `cbcl-pairing` | `7ddea48` coordinated SPEC-001 draft over code parent `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | v1 has a closed admission grammar, unprotected claim/crowding, a seven-field store record, four credential/v1 performatives, and one-way payload; the 0.3.0 draft specifies but does not implement v2 admission |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | browser allocator uses a deterministic fixture; the signing route signs caller octets; webfinger `provision/3` has no caller; pairaccount acknowledgement is asynchronous and uncorrelated |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | public resolution routes have no CORS; `POST /dids` can overwrite an existing in-memory DID; delta submission cannot bootstrap an unknown DID |

## Disposition of the 0.4.3 review

| Finding | 0.4.4 disposition |
|---|---|
| **H-1 — public v1 claim still crowds v2** | [[spec-008-0.4.4-standalone-tofu-first-contact#REQ-944]] adopts the coordinated cbcl-pairing SPEC-001 0.3.0 admission profile. Bare v1 `claim`, wrong `claim-v2`, and every surplus claim against a v2 mailbox return `Error(404)` with no mailbox-domain state change. |
| **H-2 — offer expires during the pre-relay human path** | [[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]] moves offer construction and the start of its 120-second window after both Finished values, using a second authenticated hub prepare/finalize exchange outside the relay frame budget. |
| **H-3 — cbcl-pairing hard stop silently amended** | The companion cbcl-pairing SPEC-001 0.3.0 draft at `7ddea48` explicitly amends REQ-003/REQ-005, retains every other hard stop, specifies wire/store compatibility, names affected owners, and is a mandatory input to the same fresh Tier-1 review. |
| **M-1 — encoder witnesses are false** | [[spec-008-0.4.4-standalone-tofu-first-contact#NFR-906]] distinguishes the 64,558 short-object size from the 64,560 maximum and accounts for the five-octet CBOR byte-string head above 65,535, yielding a 66,696-octet large relay body. |
| **M-2 — `operatorId` grammar disagrees with code** | [[spec-008-0.4.4-standalone-tofu-first-contact#CON-933]] pins ASCII `[a-z0-9][a-z0-9-]{0,62}`, 1..63 octets. |
| **M-3 — profile-version split strands production** | [[spec-008-0.4.4-standalone-tofu-first-contact#CON-933]] owns the dispatcher, declares the shipped v1-labelled object nonconforming, coordinates cbcl-bus SPEC-075 REQ-007, makes credential/v1 relay selection recognise version 2, and declares tier-3 `pairingRecordRelays` absent in version 2. |
| **M-4 — tombstone re-key breaks relinking** | [[spec-008-0.4.4-standalone-tofu-first-contact#CON-941]] preserves every legacy primary key, moves only authority indexes to stable account ID, preserves multi-generation URI-keyed tombstones, and defines same-account reinstatement plus exact rollback. |
| **M-5 — unsupported policy CAS/crash claim** | [[spec-008-0.4.4-standalone-tofu-first-contact#CON-935]] defines two fixed authenticated slots, highest-valid-revision reads, locked overwrite of only the older slot, read-back verification, and a passcode/presence-gated reset. |
| **M-6 — commitment has no allocation transport** | [[spec-008-0.4.4-standalone-tofu-first-contact#REQ-944]] uses the companion's closed `allocate-v2` and `claim-v2` grammar, client-generated direct mailbox ID, and explicit store-v2 admission tag. |
| **L-1 through L-7** | [[spec-008-0.4.4-standalone-tofu-first-contact#NFR-906]] names the real peer-address-keyed per-operation limiter; frontmatter separates amendments from conformance; CON-934 ASCII-normalises PAIR1 input and permits local retry; CON-935 states the mobile/desktop presence split; REQ-952 records same-origin control; and TEST-1046/TEST-1047 cover the missing release, operation, and raw-locator obligations. |

Two additional defects found while re-tracing code are closed here. CON-939
excludes `devicePossessionProof` from `OfferCoreV2`, eliminating the circular
request to sign a digest containing its own signature. CON-941 replaces
permanent alias provisioning followed by fictional rollback with one
ceremony-pending hub transaction whose exact prior rows can be restored and
whose alias plus pairaccount effects finalize atomically.

## Inherited hard-stop disposition

This amendment is a coordinated revision request; it does not pretend a child
spec can silently waive its parents.

| Existing stop | Disposition in 0.4.4 |
|---|---|
| SPEC-008 REQ-902 | **Amended for the reverse direction.** Fixture/demo material remains prohibited. Claimant intent context comes from the live profile, signed offer, CPace transcript, and local constants; allocator acceptance context comes from raw resolver closure replay, signed offer, retained device key, and hub authority state. Neither side may accept caller-asserted verification facts. |
| SPEC-008 REQ-905 | **Amended.** The public carrier remains non-navigable and contains neither `C` nor `T`; the visibly prefixed pairing code is a distinct human channel and never enters QR, clipboard, URL, log, or analytics. |
| SPEC-008 REQ-906 | **Replaced by REQ-943.** Held enrolment trust and compiled digest admission are replaced by live profile authentication plus sealed exact-pair person policy. |
| SPEC-008 REQ-907 | **Retained.** This design and its implementation do not themselves open production allocation. |
| SPEC-008 REQ-908 | **Retained.** Loopback is development-only and every UI terminal is truthful. |
| SPEC-008 CON-903 | **Retired from claimant admission.** No compiled conformance-digest allowlist decides a person's relay policy. |
| SPEC-007 REQ-803 | **Retained.** Selfsame alone derives the application account key and signs the grant. |
| SPEC-007 REQ-804 | **Amended for v2.** Preliminary intent consent permits only the issuer preview; final release consent precedes every identity effect and the reverse grant payload. |
| SPEC-007 REQ-805 | **Narrowly amended for anti-crowding.** The relay receives one random 128-bit claim bearer `T` and its commitment solely to authorise claimant membership. It receives no `C`, application identifier, identity/intent/decision/grant content, verifier evidence, or value used to derive pairing keys; it never interprets opaque bodies. `T` is zeroized after claim and is not retained. |
| SPEC-007 REQ-806 | **Retained.** Credential/v2 is an explicit profile with no negotiation, capability exchange, downgrade, or fallback. |
| SPEC-007 REQ-807 | **Retained.** Every legacy or v1 carrier is inert at a v2 entry point and vice versa. |
| SPEC-007 REQ-809 | **Retained without exception.** Production allocation remains false until the complete production gate has durable human and independent-vector evidence. |
| SPEC-007 REQ-811 | **Retained.** Invitation consumption precedes the first peer CPace message and every terminal burns the attempt. |
| SPEC-007 REQ-812 | **Narrowly amended for reverse issuance.** After final approval, the credential authority may perform the signatures, publication, and issuance writes causally required to construct the approved claimant-to-allocator payload. Before final approval, no such effect is permitted. Post-final failures authorise nothing and enter CON-942 compensation. |
| SPEC-007 REQ-813 | **Amended by REQ-943/CON-933.** The application still selects a declared relay and the person never enters an origin; claimant eligibility is canonical non-loopback profile membership plus exact-pair person policy, not a vendor digest registry. |
| SPEC-007 REQ-814 / CON-801 / TEST-818 | **Retained.** Both v2 credential objects are at most 62,000 octets; 62,001 refuses before allocation or send. |
| SPEC-007 NFR-802 / CON-802 | **Amended to pin cbcl-pairing SPEC-001 0.3.0.** Existing byte, frame, lifetime, recognition-work, and unrelated-work bounds remain exact. Relay state additionally permits one waiting-v2 commitment and transient `T`; store-v2 and the two additive commands are governed by the companion Tier-1 amendment. |
| SPEC-007 CON-806 | **Amended by CON-933.** Grammar, selection, uniqueness, release-build loopback refusal, and availability exception remain. Compiled evidence eligibility is removed from both allocator selection and claimant admission; descriptor digests remain signed comparison facts only. |
| SPEC-007 NFR-801 and production gate | **Retained.** Human cryptography, independent CPace/endpoint vectors, privacy review, security review, and all test classes remain mandatory. |
| SPEC-002 REQ-103 | **Retained.** Hex remains the normative cross-screen comparison; LifeHash is displayed only as its deterministic recognition aid. |
| cbcl-pairing SPEC-001 REQ-002 | **Retained.** The relay derives no pairing key, verifies no Finished value, retains no plaintext bearer, and interprets no opaque application body. |
| cbcl-pairing SPEC-001 REQ-003 | **Amended by its 0.3.0 REQ-017.** The generic v2 admission profile adds `allocate-v2`, `claim-v2`, one admission tag, and store-v2; it carries no application semantic and future applications using that exact profile require no relay change. |
| cbcl-pairing SPEC-001 REQ-005 | **Amended for v2 only by its 0.3.0 REQ-016.** V1 third-distinct-claim crowding remains; commitment-protected v2 mailboxes install one authenticated claimant and refuse every other claim without a mailbox transition. This hard-stop change requires the companion version and the same Tier-1 PASS. |
| cbcl-pairing SPEC-001 REQ-008/009/013/014/015 | **Retained.** Claim admission neither verifies CPace nor bypasses Finished, exact intent, consent, invitation consumption, authorization separation, or CBCL effect verdicts. |
| cbcl-pairing SPEC-001 NFR-002 and NFR-009 through NFR-012 | **Retained.** Frame counts and all anonymous-work, unrelated-work, clock, and limiter-diversity bounds are unchanged and tested by the companion amendment. |
| cbcl-pairing SPEC-001 production gates | **Retained in full.** Neither document opens conformance allocation or substitutes for human cryptography, independent endpoints/operators, profile policy, or owner enablement. |

## Requirements

### REQ-943 — Consent-bounded live origin authentication and exact-pair TOFU

The claimant SHALL parse the public v2 carrier under CON-934 without network
I/O. Before dereferencing its untrusted `application-context`, the wallet SHALL
display the exact unverified HTTPS origin and ask whether to contact it. Reject
causes no network I/O and no custody/passcode request. Accept permits at most one
live fetch for that one carrier. The wallet SHALL consume the carrier's sole
contact capability after that attempt and use CON-935's installation-keyed
origin ledger to enforce at most five distinct never-seen application origins
in a rolling 24-hour window;
exceeding it refuses before network I/O.

Burning the contact attempt prohibits a second fetch or rescan. The recognised
carrier may remain only in the current in-memory pre-network screen so CON-934
can accept corrected local code entry; cancellation, deadline, relay start, or
leaving that screen burns it completely.

The live fetch SHALL use CON-220 with no redirect, no caller profile, and no
stale first-contact cache. This path has no PROTO-003 CON-409 record, so CON-220
step 6 is amended as follows: the wallet computes the live profile digest; the
allocator independently computes the digest of its release-embedded recognised
profile; CON-934 binds those locally computed digests into CPace/Finished; and
the wallet later requires equality with the digest in the signed CON-214 offer
before display. Until both cross-checks succeed, the profile is usable only to
decide whether to contact a relay, never to derive or display an authenticated
intent.

Before opening the relay socket, the claimant SHALL require exactly one CON-933
descriptor whose `relayOrigin` equals the carrier origin byte-for-byte. Ordinary
builds reject loopback, IP-literal, non-HTTPS, userinfo, path, query, fragment,
and non-canonical origins. The wallet then admits only an existing sealed exact
pair `(applicationId, relayOrigin)` or explicit person acceptance of that new
pair. The wallet requests passcode and OS presence once for this policy access,
after descriptor recognition and before the relay socket, and zeroizes the
derived policy key before opening the socket. A new application on a familiar
relay and a familiar application on a new relay both prompt. A descriptor
removed from the live profile refuses despite a local entry. Policy
unavailability, authentication failure, or two-slot commit failure opens no socket.

### REQ-944 — Presence code separates CPace from relay claim and recovery

The signed-in application surface SHALL generate independent fresh values `C`
and `T`, each sixteen CSPRNG octets, and render only their canonical CON-934
`PAIR1-…` encoding. `C` is the v2 CPace password input. `T` is a one-use relay
claim bearer and SHALL NOT enter CPace. Neither is a wallet seed, BIP-39
mnemonic, application credential, or account key.

Before relay allocation, the allocator generates a fresh direct mailbox ID `M`
of 32 CSPRNG octets and computes the companion SPEC-001 value:

```text
claimCommitment = SHA-256(
  UTF8("cbcl-pairing claim-v2 commitment\u0000") || M || T
)
```

`M` and `T` are raw octets. The allocator sends exactly
`allocate-v2(M, claimCommitment, ttlSeconds)` and accepts only
`allocated-v2(M, membershipToken, expiresAt)`. The carrier contains `M` and the
commitment but neither `C` nor `T`. A claimant presents `M,T` over TLS in the
closed `claim-v2` command and accepts only `claimed-v2` for that `M`.

The relay's constant-time commitment check and atomic store-v2 transition occur
before claimant membership is created. A well-formed wrong `T`, bare v1
`claim`, or any v1/v2 claim after the v2 claimant exists returns the same
canonical `Error(404)` as an unknown mailbox and changes no mailbox status,
membership, expiry, body, sequence, or attached-peer route. A malformed token
fails recognition as `Error(400)` before mailbox lookup. Successful verification
erases the commitment, persists only the claimant membership hash plus the v2
admission tag, and zeroizes `T` before the next command. V1 command encodings and
v1 mailbox crowding remain byte-for-byte unchanged; v1 `claim` is changed only
in that a v2-tagged mailbox is deliberately invisible to it.

Selfsame SHALL accept the code only through a visually distinct pairing field
after REQ-943. The field has no paste, autofill, password-manager, accessibility
injection, deep-link, notification, QR, or peer-data path. It rejects every
valid twelve-word English BIP-39 mnemonic before decoding pairing input. The
restore field conversely rejects the `PAIR1-` prefix. Neither surface reuses the
other's parser, label, component, telemetry event, or retained buffer.

The CPace public context binds the canonical public carrier digest, each
endpoint's independently computed profile digest, `application-context`, relay
origin, locator, ceremony ID, claim commitment, profile
`anuna.io/credential/v2`, and both fixed roles. A wrong code, wrong `T`, public-
carrier transplant, forwarded QR without the code, profile mismatch, or any
context mismatch fails before intent. Every terminal zeroizes `C` and burns the
invitation, offer, request, and ceremony.

The browser SHALL label the code: "Type this PAIR1 code only into your Selfsame
wallet while this chat page is open. It is not your recovery phrase. Do not send
it to anyone." The wallet SHALL name the live authenticated application origin
beside the field and repeat that warning. Deliberate remote forwarding of both
values remains an explicit social-engineering residual.

### REQ-945 — Signed offer authorisation and truthful exact-intent display

After both Finished values, the browser SHALL complete CON-939's authenticated
off-relay offer-core prepare/device-proof finalize exchange. Only then may the
allocator send exactly one encrypted, transcript-bound CON-219 v2 offer. Before
any intent display, the claimant SHALL run the complete CON-214 predicate,
recompute the proof-free `offerCoreDigest`, verify the device proof and hub
evidence, and atomically consume the valid `requestId` whether the person later
approves or rejects. The consumed-ID tombstone is the sole permitted pre-intent
mutation beyond separately accepted origin/policy/rate-limit state.

The claimant SHALL require exact equality among the invitation, live profile,
descriptor, signed offer, CPace transcript, and display inputs for application,
relay, profile version/digests, descriptor digest, ceremony/request IDs,
carrier nonce, intent nonce, stable account-principal digest, application-session account
label, scope, distinct device DID/JWK/digest/proof, permissions, platform
binding, and validity window. A mismatch emits zero display effects and zero
consent affordances.

The display provenance type is closed: `LiveApplicationProfile`,
`SignedApplicationSession`, `SignedApplicationOffer`, or `LocalProtocolConstant`.
There is no `claimed_by_secret_holder` boolean and no generic peer-claimed value
can be promoted. The account row SHALL read "Application account asserted by
<origin>" and SHALL NOT say or imply that Selfsame authenticated the person's
ownership of that label.

### REQ-946 — Preview, final approval, and effect order

The exact-intent approval is preliminary: it authorises a new passcode and
custody-presence operation, one hierarchy derivation inside a zeroizing closure,
pure `HomeKey::home_did`,
fingerprint rendering, and transmission of the resulting issuer DID in a
preparation bound to the intent digest. Before final release approval the wallet
SHALL NOT call `issuer::create`, sign any delta or grant, publish a DID, write
issuance or grant state, build a bundle, provision an alias, or send a credential
payload. `assemble_claimant` SHALL NOT be reused.

Sending `preparation` irreversibly discloses that pairwise preview DID to the
authenticated application even if the person later declines; the preliminary
screen SHALL state that disclosure and approval authorises it. It authorises no
signature, publication, or durable identity state.

The application determines CON-221 applicability independently through
CON-941's authenticated account-authority query. `Unknown` refuses. `NoBinding` requires the application to
display the prepared DID fingerprint and record an explicit comparison action;
`BoundSameDid` may report the existing binding; `BoundDifferentDid` refuses. The
claimant supplies no authoritative applicability bit. The wallet displays its
locally recomputed fingerprint and the application result, then asks for final
release. Final decline or timeout zeroizes preview state and leaves hierarchy,
DID resolver, grant, issuance, alias, and application connection state unchanged;
only the mandatory request tombstone and separately consented policy/rate state
remain.

After final approval, and only then, the wallet requests a new passcode/custody-
presence operation and re-derives the same home key,
requires the DID to equal the preview byte-for-byte, calls `issuer::create`,
signs the grant, publishes and resolves the issuer under CON-940, writes pending
issuance state, builds the bundle, and sends it as the v2 reverse payload. Those
steps form one causally ordered post-consent operation. Any failure enters the
terminal/compensation rules in REQ-951 and never reports linked.

### REQ-947 — Stable application account principal and scope ownership

The hub SHALL assign each chat account a stable CSPRNG 32-octet
`applicationAccountId` and durably bind every authorised chat session signing
key to that identifier. The identifier, not a handle or key fingerprint, keys
logical account authority, pairing scope, replay, device registration, and
ceremony state and is the authoritative join key for alias, withdrawal, and
pairaccount projections. Legacy public/index tables may retain their natural
primary keys only as CON-941 specifies. The signed offer carries only
`accountPrincipalDigest = SHA-256("cbcl-chat account principal v1" ||
applicationId || applicationAccountId)`. The human-selected handle is mutable
display metadata and has no authority role.

Existing accounts are migrated transactionally: allocate one ID; create the
account-authority/scope/replay/ceremony rows and handle/session-key indexes;
annotate the URI-keyed alias rows, handle-keyed account projection, URI-keyed
withdrawal rows, and composite `(Handle, DeviceKey)` pairaccount rows with that
ID and their required secondary indexes; and commit all changes or none. A
duplicate mapping, conflicting alias, lost tombstone generation, or partial
migration fails closed and keeps the old record readable but not v2-eligible.
There is no inference from handle equality and no legacy primary-key rewrite.

The hub SHALL atomically create one CSPRNG 32-octet CON-211 scope per
`(applicationId, applicationAccountId)` and reuse it for retries and every
authorised device. Account-key recovery or rotation retains the ID and scope
only through a separately specified authenticated account-recovery transaction;
loss without that transaction creates a new account ID and the UI says so.

The offered application device key is distinct from every chat session signing
key. The browser generates it once per `(applicationId, applicationAccountId,
browser installation)`, stores its seed with strict IndexedDB durability before
allocation, and reuses it across retries on that installation. After Finished,
the browser verifies the hub-built proof-free `OfferCoreV2` and signs exactly:

```text
SHA-256(
  UTF8("selfsame credential/v2 device proof\u0000") ||
  offerCoreDigest || transcriptHash
)
```

where both digest values are raw octets and `transcriptHash` is the exact
64-octet CPace transcript hash. `devicePossessionProof` is excluded from
`OfferCoreV2` and therefore cannot be an input to its own signed digest. Two
installations of the same account share the scope but have different device
keys; a ceremony never relabels a session key as the device key.

The hub constructs the complete offer and CON-214 statement from authenticated
session state under CON-939. It SHALL NOT sign arbitrary caller-supplied
statement octets. Scope creation plus one-use ceremony allocation SHALL commit
atomically or return no carrier. Device-key registration plus offer
finalization SHALL commit atomically after proof verification or return no
offer.

### REQ-948 — Credential/v2 is an explicit reverse-grant protocol

`anuna.io/credential/v2` SHALL be an additive, non-negotiated profile with
distinct CDDL, public-context domain, CPace/Finished/HKDF labels, CBCL dialect,
role projections, application body types, and independent vectors. A v2 public
carrier cannot start v1, and v1 cannot recognise any v2 value.

The projected application session is exactly:

```text
             [after Finished: authenticated hub prepare/finalize, off relay]
Allocator  -> offer
Claimant   -> intent-approve | intent-decline
Claimant   -> preparation
Allocator  -> comparison-confirmed | binding-confirmed | refusal
Claimant   -> final-approve | final-decline
Claimant   -> payload
Allocator  -> receipt | refusal
terminal
```

The bracketed hub exchange is not a cbcl-pairing session performative and does
not consume a relay frame. It is nevertheless ceremony-bound by the live signed
chat session, carrier digest, ceremony ID, transcript hash, account principal,
device key, and retained pending-offer state. Failure burns the ceremony with no
offer performative and no v1 fallback.

Every performative cites the predecessor content hash, the intent digest,
ceremony ID, and role required by the dialect. `preparation` contains only the
preview issuer DID, its fingerprint rendering inputs, and binding digests; it
contains no key, signature, closure, grant, scope, or applicability decision.
The allocator's comparison status is bound to the preparation hash and records
its independently observed authority result. `payload` contains the CON-219
bundle claimant-to-allocator. `receipt` binds the exact bundle digest, grant ID,
issuer DID, account-principal digest, device-key fingerprint, local-install
digest, and matching hub-acknowledgement digest.

Wrong sender, skipped predecessor, reordering, duplicate with different bytes,
digest mismatch, refusal, mailbox close, or silence past the fixed deadline is
terminal. Exact retransmission is idempotent. There is no capability list,
profile negotiation, payload-direction inference, or v1 fallback.

### REQ-949 — Application acceptance precedes a truthful receipt

On receiving the reverse payload, the browser SHALL perform these steps in
order:

1. recognise the v2 envelope and CON-219 bundle;
2. require request, ceremony, intent, bundle, and prepared-issuer bindings;
3. require the grant `issuer` to equal the prepared issuer DID byte-for-byte;
4. recompute the reciprocal `acct_uri` from that issuer;
5. fetch raw issuer closure bytes through CON-940 and create its opaque WASM
   acceptance object, which internally replays the closure and runs CON-206
   steps 1 through 8;
6. invoke CON-941 `selfsamepairaccept`, require its exact idempotent
   ceremony-pending reply, and treat only that opaque signed reply as the fresh
   reciprocal-record input;
7. complete CON-206 steps 9 through 13 and CON-207 using that reply and the
   actual retained browser device key;
8. atomically install the accepted connection in IndexedDB with strict
   durability, or restore the byte-identical prior local state;
9. submit the actual device key in the signed, ceremony-extended
   `pairaccountgrant`; require the hub to atomically finalize the pending alias
   projection plus pairaccount authority and await matching
   `pairaccountgranted` for the same account principal ID, device fingerprint,
   ceremony ID, pending-transaction digest, and durable record digest; and
10. emit the v2 receipt only after steps 1 through 9 succeed.

`sendCBCL` write completion is not acknowledgement. A hub error, unrelated
acknowledgement, timeout, browser persistence failure, resolver failure, or alias
failure emits `refusal`, invokes compensation, and never emits `receipt`. The
wallet reports `linked` only after verifying the receipt. The browser reports
linked only after emitting it.

### REQ-950 — Issuer publication and browser-resolvable state

After final approval, the wallet SHALL publish an issuer through a new
`selfsame-app-identity-net` operation:

1. `POST /dids` with the derived public key;
2. reproduce the canonical unsigned bootstrap genesis locally, require its
   content hash and returned DID to equal the resolver's genesis, the preview,
   and the locally derived DID;
3. submit the alias and assertion-method deltas in causal order to each
   profile-declared resolver;
4. require at least one resolver intended for application acceptance to
   acknowledge and then return a verified closure containing the complete
   frontier; and
5. retain failed resolver submissions for bounded retry without claiming that
   an acknowledgement proves global publication.

The did-crdt create operation SHALL be idempotent and non-destructive: creating
an already-held DID derived from the same public key returns the existing state
and SHALL NOT replace, truncate, or reset its DAG. Its canonical genesis may use
the existing empty-signature bootstrap exception because no verification method
exists yet; every post-genesis delta SHALL carry a signature that verifies under
a materialised method. A different genesis content hash, invalid post-genesis
signature, or DID mismatch refuses. The wallet SHALL NOT claim that submitting
its same-content signed genesis replaces the resolver's bootstrap genesis.

Public `GET /:did?includeClosure=true` and `GET /dids/:did/closure` responses
SHALL carry read-only CORS permitting HTTPS browser origins without credentials;
`OPTIONS` and `HEAD` SHALL have equivalent policy. No write route gains CORS.
The browser SHALL fetch raw closure bytes directly from a declared resolver and
pass them without caller-produced fact fields to CON-940's opaque WASM
acceptance object. The module replays the bootstrap and every post-genesis
signature internally and sets resolver provenance only after successful replay.
A projected DID document, hub proxy, bundle closure while a resolver is
reachable, fabricated fact set, or permissive CORS on POST is a refusal.

### REQ-951 — Terminal silence, refusal, and compensation

One absolute deadline of allocation time plus 600 seconds bounds the ceremony.
Each state also has the following monotonic idle deadline measured from entry:

| State | Idle deadline |
|---|---:|
| await authenticated `claim-v2` | 180 s |
| CPace/Finished bootstrap | 30 s |
| authenticated hub offer prepare/finalize and delivery | 30 s |
| await preliminary intent decision | 120 s |
| claimant preview and `preparation` | 45 s |
| authority query and comparison result | 90 s |
| await final release decision | 120 s |
| commit/publication and reverse payload | 90 s |
| application acceptance, hub acknowledgement, and receipt | 90 s |

The effective deadline is the earlier of the state idle deadline and the
absolute deadline. No offer or evidence timestamp exists during public-carrier,
policy, claim, or CPace work. CON-939 starts the unchanged 120-second
CON-214/CON-219 window when it constructs `OfferCoreV2` after both Finished
values. The wallet evaluates that window exactly once, immediately before
REQ-945 authorisation and request-ID consumption. The complete 30-second hub
prepare/finalize/delivery budget therefore fits inside the signed window; later
human steps do not re-evaluate it. All post-authorisation freshness comes from
the table and transcript-bound absolute expiry. Silence, disconnect without
successful resumption, and an application `refusal` are terminal. Before final
approval they leave no identity effect beyond the REQ-945 request tombstone,
disclosed pairwise preview after preliminary approval, and separately consented
local policy/rate state.

After final approval, a failure before payload transmission SHALL retain no
grant bundle and SHALL mark any published but unaccepted issuer as an approved
orphan eligible for reuse only for the same `(applicationId, accountScopeId)`;
it SHALL NOT mint another scope or DID on retry. A failure after payload
transmission SHALL:

- durably queue a CON-210 revocation of the released grant ID and submit it to
  every declared resolver;
- ask the hub to delete pending state or exactly restore any finalized
  ceremony-owned alias/pairaccount rows;
- remove or quarantine any locally staged browser connection; and
- report `not linked` until revocation/rollback standing is known.

The application SHALL monitor the released grant's status and disable any
connection whose compensation revocation becomes visible. Compensation is
idempotent and keyed by ceremony/grant ID. A failed compensation remains
`pending`; it never turns the ceremony into success. No new DID is created for a
retry with the same stable scope.

### REQ-952 — Relay operatorship remains an explicit residual

A valid application profile proves only that application `A` declares use of
relay `R`. `operatorId`, privacy-policy digest, and conformance-evidence digest
are application assertions. They SHALL NOT be rendered as independently
verified operatorship and SHALL NOT bypass exact-pair person policy. Trusting
`(A, R)` does not trust another application on `R`.

Global relay ownership, cross-application operator revocation, and third-party
conformance certification require a future signed operator credential. Their
absence is not disguised as a property of v2. Release notes and the first-use
prompt SHALL state this residual.

The profile-digest equality is also not independent of the authenticated
application origin: an attacker controlling that origin and its release can
replace both the live profile and the browser-embedded profile so their digests
agree. The signed chat session, exact-pair prompt, human code, and hub offer
still bind the ceremony, but they do not recover a previously honest origin.
Release notes and the first-use prompt SHALL state this same-origin-control
residual beside the operatorship residual.

### REQ-953 — Production allocation stays closed until its evidence exists

Implementing or passing this amendment SHALL NOT flip
`PRODUCTION_ALLOCATION_ENABLED`, remove the rollback hold, or deploy an allocator.
Production enablement requires every item in the SPEC-007 production gate,
including human CPace/Finished/AEAD review, independent CPace and complete
endpoint vectors, relay/privacy review, application-identity security review,
the operator availability exception or second operator, and all release tests.

The local and staging implementations MAY exercise credential/v2 behind an
explicit non-production gate. A later reviewed gate-evidence record, owner
approval, and separate deployment approval are required before chat.anuna.io
emits production v2 carriers.

### NFR-906 — Fixed v2 size classes within unchanged v1 outer bounds

The credential logical object bound is exactly 62,000 octets. A v2 application
object uses deterministic CBOR integer keys `{0: version, 1: kind, 2:
intentDigest, 3: logicalLength, 4: paddedBody}`. `offer` and `payload` pad field 4
to exactly 64,512 octets; all control objects pad it to exactly 4,096 octets.
Padding is authenticated, not interpreted, and generated without account-derived
data. A large object with `logicalLength = 1` is 64,558 octets; the large-class
maximum is 64,560 once `logicalLength` needs a three-octet unsigned-integer
encoding. The control-class maximum is 4,144 octets. These totals comprise one
map byte, two two-byte uint pairs, one 35-byte digest pair, a two- or four-byte
length pair, and one key plus the three-byte byte-string head plus fixed body.

The existing shared v1 limits remain byte-for-byte unchanged:

| Layer | Existing maximum retained |
|---|---:|
| v2 credential object | 62,000 |
| application body | 64,512 |
| sealed-plaintext body | 65,536 |
| sealed plaintext | 69,556 |
| sealed ciphertext | 69,572 |
| relay frame body | 69,632 |
| WebSocket message | 70,000 |

With a 2,048-octet control value, existing string-key `sealed-plaintext` adds
2,068 octets: the large and control plaintext maxima are 66,628 and 6,212.
AES-GCM adds 16: ciphertext maxima are 66,644 and 6,228. At counter `0..15`, the
existing deterministic `sealed-frame` map adds 52 octets to the large value
because a 66,644-octet ciphertext requires a five-octet CBOR byte-string head;
it adds 50 to the control value, whose head remains three octets. Complete relay
bodies are therefore at most **66,696** and **6,278**. These figures are
executable encoder witnesses, not estimates. A 62,001-octet credential refuses
before allocation/send. No shared CDDL bound, v1 rejection boundary, v1 vector,
or relay queue constant changes.

The v2 encoder additionally caps the complete relay body at 67,000 octets for a
large application frame, 7,000 for a control-class frame, and 8,192 for each
CPace/Finished frame. A successful ceremony has exactly two large, five control,
and four bootstrap frames: a 201,768-octet worst-case queue budget. Exact retries
reuse a sequence and do not add a body. The existing 512 MiB queue therefore
admits at most 2,660 simultaneously fully queued worst-case ceremonies before it
fails closed; the 10,000-mailbox cap remains independently enforced. No
membership reaches the sixteen-frame ceiling and the relay performs no
application parsing.

A v2 mailbox adds exactly one 32-octet claim commitment until successful claim
or terminal closure/expiry: at the unchanged 10,000-mailbox cap this is 320,000
octets of bounded metadata. Each `claim-v2` attempt performs one bounded command
parse, one addressed-mailbox lookup, one SHA-256, and one constant-time
comparison under the existing peer-address-keyed per-operation limiter. A
mismatch stores nothing, queues nothing, does not extend TTL, and does not reset
a limiter. Success deletes the commitment. V2 adds `allocate-v2`, `claim-v2`, an
admission-profile tag, and store-v2 exactly as the companion SPEC-001 0.3.0
draft defines. V1 encodings, v1 mailbox behaviour, shared bounds, and v1
vectors remain unchanged; v1 `claim` merely receives unknown-mailbox behaviour
when its addressed mailbox is tagged v2.

## Contracts

### CON-933 — Ratified pairing-relay profile member and TOFU eligibility

CON-201 profile version 1 is not amended. Profile version 2 is a new closed
grammar with exactly these nine required top-level names and no others:

```text
profileVersion = 2
applicationId
accountAuthority
verifierAudience
allowedPermissions
enrollment
cbclPairingRelays
stateResolvers
revocation
```

Version 2 intentionally removes version 1 `rendezvous` and
`pairingRecordRelays`; an older version-1 recogniser therefore cannot accept a
version-2 profile accidentally. `cbclPairingRelays` is an array of one through
sixteen closed descriptors. Each descriptor has exactly:

```text
operatorId                  ASCII [a-z0-9][a-z0-9-]{0,62}, 1..63 octets
relayOrigin                 canonical HTTPS origin
priority                    uint 0..65535
weight                      uint 1..65535
privacyPolicyDigest         base64url-no-pad SHA-256
conformanceEvidenceDigest   base64url-no-pad SHA-256
```

Every shared member keeps its CON-201 version-1 grammar byte-for-byte. A new
dispatcher performs UTF-8/size/duplicate/depth recognition, reads only the
integer version discriminator, then applies the complete closed grammar for
that version before any semantic action. Version 1 therefore recognises its
nine required names including `rendezvous` and optional tenth
`pairingRecordRelays`; version 2 recognises only the nine names above. The
reviewed single-grammar `PROFILE_VERSION = 1` implementation and generator must
be split; relabelling their current nine-name output without dispatch is not
conformance. The currently shipped chat.anuna.io object — version 1 with
`cbclPairingRelays` and without `rendezvous` — conforms to neither grammar. The
cbcl-bus generator SHALL be coordinated with SPEC-075 REQ-007 and re-mint that
release profile as version 2 before any v2 allocation. Its fixture, served JCS
bytes, embedded browser bytes, digest, and wallet vectors SHALL all come from
that one version-2 generator output.

`operatorId` and `relayOrigin` are each unique in one profile. An ordinary build
rejects loopback and IP literals; a development capability may admit loopback
without changing ordinary code. The member participates in RFC 8785 profile
digesting and complete CON-220 recognition. Until this amendment is ratified, no
profile containing the member is conforming and REQ-943 remains unimplementable.

For allocator selection, priority/weight and the existing approved availability
exception remain. For both allocator selection and claimant admission,
`eligible` means a completely recognised canonical non-loopback descriptor; the
claimant additionally requires exact-pair policy acceptance. Neither role uses
membership in a compiled digest registry. Descriptor and conformance-evidence
digests remain signed comparison inputs, not vendor trust roots. A production v2
allocator SHALL embed and recognise the exact version-2 profile it serves before
allocation; the browser may not assemble a profile from runtime JSON fields.

SPEC-007 CON-806 step 1 is amended so the credential/v1 relay-selection path
also accepts a completely recognised version-2 profile and selects only its
`cbclPairingRelays`; this is profile recognition, not a credential/v2 fallback
or negotiation. CON-220 tier 3 has no `pairingRecordRelays` carrier in version
2 and SHALL be unavailable rather than inferred from `cbclPairingRelays`.
Version 1 retains its exact `rendezvous` and optional `pairingRecordRelays`
branches for application contexts that continue to publish a conforming v1
profile.

### CON-934 — Credential/v2 public carrier and presence code

The public carrier is deterministic CBOR encoded as base64url-no-pad and has the
closed fields:

```text
version                 2
profile                 "anuna.io/credential/v2"
application-context     canonical ApplicationId ASCII, 1..2048 octets
profile-version         2
relay-origin            canonical HTTPS origin, 1..2048 octets
locator                 direct 32-octet mailbox id
ceremony-id             32 octets
carrier-nonce           32 octets
claim-commitment        32 octets
expected-allocator-key  optional 32-octet digest
```

It contains neither `C` nor `T`. The v2 endpoint constructor therefore takes two
typed inputs: a recognised `PublicInvitationV2` and
`PresenceCode { c: [u8;16], claim_token: [u8;16] }`.

The canonical human representation is `PAIR1-` followed by exactly eleven
hyphen-separated groups of five Crockford Base32 characters. The 55 characters
decode as one big-endian bit string containing `C || T || checksum16 || 000`,
where the final three bits are zero and `checksum16` is the first two octets of
`SHA-256("selfsame credential/v2 presence code" || C || T)`. The fixed alphabet
is `0123456789ABCDEFGHJKMNPQRSTVWXYZ`; `I`, `L`, `O`, `U`, whitespace, Unicode
lookalikes, missing separators, nonzero pad bits, and checksum failure are
rejected. Before grammar recognition, the input component converts ASCII
`a`..`z` to uppercase without applying Unicode case folding; lower or mixed
ASCII case is therefore accepted only when the normalized result is otherwise
canonical. A local grammar, checksum, or recovery-collision refusal clears the
field, zeroizes its decoded buffer, and permits re-entry within REQ-951's
current idle/absolute deadline without consuming the invitation. Network start,
successful relay claim, peer input, cancellation, or deadline remains terminal.
The string is never serialised into a machine carrier.

`carrierDigest = SHA-256(canonical carrier bytes)`. The allocator's context uses
the RFC-8785 digest of its release-embedded recognised profile; the claimant's
uses the digest of the one live CON-220 response. The v2 PairingContext and all
role-separated CPace/Finished associated data bind both local profile digest and
`carrierDigest`, every carrier field, the resolved mailbox ID, and the v2
domain. The endpoints can reach Finished only if the digests agree. A v1
constructor cannot take these types. Carrier, profile, code, commitment, or
cross-ceremony mutation fails before intent.

### CON-935 — One sealed person-owned application/relay policy

One closed versioned policy document is stored redundantly in the fixed keychain
slots `selfsame-pairing-relay-policy-a` and
`selfsame-pairing-relay-policy-b`; `SealedRoot` bytes and version are never
rewritten by policy operations. On mobile, a successful operation requires the
wallet passcode plus the platform OS-presence gate. On desktop, the existing
passcode-mediated custody opening is the presence gate; no nonexistent desktop
biometric callback is claimed. The wallet then derives
`policyKey = HKDF-Expand(custodyWrappingKey, "selfsame pairing relay policy key
v2", 32)` inside a zeroizing closure. The document is sealed with a fresh nonce;
associated data binds the literal domain, wallet root public fingerprint, policy
schema version, slot name, and revision. It contains:

```text
version = 1
revision = monotonic u64 for locked local serialization only
entries = sorted unique array of {
  applicationId,
  relayOrigin,
  acceptedAt
}
```

Each slot is fully recognised and AEAD-opened independently. Read chooses the
valid document with the greatest revision. Equal revisions are accepted only
when their canonical plaintext documents are identical. Both slots absent means
empty only after a valid root opening; a present record that yields no valid
slot, or equal revisions with different plaintext, refuses.

Every read-modify-write holds one installation-scoped exclusive process/OS
lock. Under that lock it re-reads both slots, computes `revision + 1`, seals with
a fresh nonce, overwrites only the absent, invalid, or lower-revision slot, then
reads back, recognises, opens, and byte-compares that slot before reporting the
update committed. The previously highest valid slot remains untouched. A crash
or failed write therefore leaves at least the prior complete authenticated
document; a competing process cannot pass the revision check outside the lock.
There is no reliance on destructive single-entry replace being atomic.

If at least one policy slot exists but neither is valid, policy management SHALL
offer `reset relay policy` only after the same passcode/platform-presence gate.
Reset writes and verifies an authenticated empty document at the next revision
to the older/invalid slot first, then writes and verifies the other slot at the
following revision. A crash after the first verified write already makes empty
the highest valid policy. Reset affects neither `SealedRoot` nor identities and
causes the next relay pair to prompt again. Presence failure, lock failure,
revision overflow, read-back mismatch, or write failure refuses.

The pre-network origin-attempt ledger is a separate closed record sealed under a
random installation key held by the OS keychain, not a custody/root key. It
stores only origin digest, first/last-attempt time, count, and carrier digest.
Its failure refuses before network I/O. Origin rejection can update this ledger
but requests no passcode. The ledger is an availability/SSRF throttle, not
identity authority and not recovery state.

Policy is device-local and deliberately omitted from recovery export; restoring
the wallet creates an empty policy and prompts again. This contract authenticates
current state but does not claim a rollback-resistant hardware counter. Policy
management may list and remove exact pairs; there is no wildcard, relay-only
entry, vendor seed, or silent migration from a held pairing-trust record. Policy
access, issuer preview, and issuance commit are three distinct key-derivation
operations. Each uses passcode plus OS presence on mobile and passcode custody
presence on desktop; no derived wrapping/policy/root key survives between them.

### CON-936 — Claimant-authenticated intent verifier and display provenance

Credential/v2 injects an `IntentVerifier` beside the grant verifier. It receives
the recognised offer, authenticated live profile, selected descriptor, public
carrier, CPace transcript/exporter binding, local observation, and local clock.
It delegates offer grammar to `ceremony::recognise_offer` and authorisation to
`authorise::authorise`; it does not duplicate either parser. It returns an
`AuthenticatedIntent` only after REQ-945 equality and replay consumption.

`AuthenticatedIntent` contains only bounded typed fields and the closed
provenance enum from REQ-945. The generic cbcl-pairing reducer can emit
`DisplayIntent` only from that object. Any field retaining `PeerClaimed`, any
unknown provenance, or any attempted display before verification is a terminal
profile error.

### CON-937 — Credential/v2 reducer and terminal grammar

The v2 reducer implements exactly the projected session in REQ-948. Each local
constructor is role-specific; the allocator API has no payload constructor and
the claimant API has no offer/receipt constructor. The reducer records one
content hash per state and accepts only an exact byte retransmission for that
state. A conflicting duplicate, predecessor not in the unique causal chain, or
message after terminal closes the mailbox and zeroizes session secrets.

The effect surface is closed:

- `DisplayAuthenticatedIntent` only after CON-936;
- `IntentApproved` permits CON-938 preview but no identity effect;
- `DisplayIssuerComparison` only from the locally computed preview plus the
  allocator's bound authority status;
- `FinalApproved` permits the post-consent issuance adapter;
- `DeliverBundleForAcceptance` occurs only at the allocator after final
  approval and reverse payload recognition;
- `Linked` occurs only at the claimant after receipt verification; and
- every refusal/timeout emits `Compensate(stage)` when and only when a released
  grant may exist.

Timeout is a reducer input supplied by the shell's monotonic clock, not an
absence the UI invents. Every nonterminal state has an explicit timeout
transition. Receipt and refusal are mutually exclusive terminals.

### CON-938 — Unsigned issuer preview and post-final issuance commit

The wallet adapter is split into two APIs which cannot share a pending output
type:

`preview_issuer(authorised_offer, passcode)`:

- requires backup confirmation and custody presence;
- derives the account home key inside `Custody::use_hierarchy_root`;
- calls only `HomeKey::home_did` and fingerprint rendering;
- copies no seed or signing key out of the closure;
- zeroizes the derived key before returning `IssuerPreview` containing DID,
  fingerprint, ceremony ID, intent digest, and the REQ-951 absolute deadline; and
- performs no signature, resolver call, grant/issuance write, or random grant
  token generation.

`commit_issuance(final_approval, held_preview, authorised_offer, passcode)`:

- takes and consumes one held preview; checks ceremony, intent, final-decision
  hashes, and REQ-951 absolute/current-state deadlines;
- re-derives and requires the home DID to equal the preview;
- calls `issuer::create`, creates the grant token, signs the grant, publishes
  through CON-940, writes pending issuance, and builds one CON-219 bundle;
- returns the bundle once and retains the grant ID for compensation; and
- cannot be called from preliminary approval, preparation, comparison display,
  decline, timeout, or a second final decision.

The request ID was already consumed by CON-936 before display and is not
consumed a second time. The exact effect table is:

| State | request tombstone | derive for preview | sign | publish | issuance write | payload |
|---|---:|---:|---:|---:|---:|---:|
| before authenticated display | no | no | no | no | no | no |
| authenticated display | yes | no | no | no | no | no |
| preliminary decline | yes | no | no | no | no | no |
| preliminary approve / preparation | yes | yes, zeroized | no | no | no | no |
| final decline/timeout | yes | preview dropped | no | no | no | no |
| final approve | yes | re-derived | yes | yes | pending | one reverse payload |

### CON-939 — Authenticated hub offer allocation

The browser persists its distinct device seed before beginning, then uses three
authenticated commands on its existing signed chat session.

**1. Allocate ceremony, before the public carrier is displayed.**
`selfsamepairallocate(rawCarrier, requestId, intentNonce, requestedPermissions,
deviceKeyJwk)` accepts the exact canonical CON-934 carrier bytes; it does not
accept `C` or `T`. The hub recognises the carrier and its release-embedded
version-2 profile, recomputes `carrierDigest` and the selected descriptor digest,
requires the canonical application/relay/ceremony/mailbox/commitment values,
resolves the session signing key to REQ-947's stable account ID, validates the
permission subset and distinct device JWK, and loads or atomically creates the
stable scope. It stores one pending ceremony containing those exact inputs. The
scope plus pending ceremony commit atomically or the browser displays no
carrier. No signed offer, `issuedAt`, or `expiresAt` exists in this phase.

**2. Prepare offer core, after both Finished values.**
`selfsamepairofferprepare(ceremonyId, transcriptHash)` requires the same live
session and pending ceremony and a 64-octet transcript hash. At that moment the
hub starts the unchanged 120-second CON-214/CON-219 window and constructs the
closed canonical `OfferCoreV2` with exactly:

```text
payloadVersion = 2             role = "offer"
ceremonyId                     requestId
applicationId                  profileVersion = 2
profileDigest                  descriptorDigest
relayOrigin                    carrierDigest
transcriptHash                 accountPrincipalDigest
applicationAccountDisplay      accountScopeId
deviceDid                      deviceKeyJwk
requestedPermissions           intentNonce
platformBindingId              returnUri
issuedAt                       expiresAt
```

Binary IDs, nonces, and SHA-256 values in this JSON object use canonical
base64url-no-pad and decode to 32 octets; `transcriptHash` decodes to 64 octets.
The inherited application ID, JWK, DID, permission, timestamp, platform, and
return-URI grammars remain exact. No listed member is optional.

`applicationAccountDisplay` is 1..128 Unicode scalar values after NFC and is
asserted only by the authenticated application session. `expiresAt` is after
`issuedAt` by at most 120 seconds. `offerCoreDigest` is the raw SHA-256 of the
RFC-8785 serialization of that exact object. The hub durably holds the exact
core and returns it with the digest. The browser checks every carrier,
transcript, profile, descriptor, permission, platform, device-key, ceremony,
and request value it knows and treats the account/scope values only as signed
application-session assertions. It refuses rather than signing a mismatch.

**3. Finalize with non-circular device proof.** The browser signs REQ-947's
domain-separated digest and sends
`selfsamepairofferfinalize(ceremonyId, offerCoreDigest,
devicePossessionProof)`. The hub requires byte identity with the held core,
verifies the Ed25519 proof under that core's distinct `deviceKeyJwk`, and only
then registers the device key. `devicePossessionProof` is not a member of
`OfferCoreV2`; it is canonical base64url-no-pad of the exact 64-octet Ed25519
signature and is a member of the complete offer and the signed CON-214 v2
evidence. That evidence carries the parent fields plus
`accountPrincipalDigest`, `applicationAccountDisplay`, `carrierDigest`,
`transcriptHash`, and `devicePossessionProof`, all exact-equality inputs. The
provider hint carries the same carrier/core/descriptor/transcript bindings. The
hub signature therefore covers the proof without making the proof sign itself.
Device registration, evidence signature, and one finalized full offer commit
atomically; retry returns only the byte-identical offer.

The hub signs only canonical state it constructed. Caller-supplied JWS octets,
application ID, profile digest, scope, account principal, account label,
permission outside the profile, timestamps, return URI, provider hint, altered
core, or second proof are rejected. One ceremony produces at most one finalized
offer and burns on failure or terminal outcome. These three authenticated HTTP/
WebSocket commands are outside cbcl-pairing relay frames and do not alter the
two-large/five-control budget. The prototype `/selfsame/enrolment/sign` route is
not callable by credential/v2 and may not be treated as an implementation
shortcut.

### CON-940 — Non-destructive DID publication and direct browser resolution

The native publication adapter uses the profile's declared resolver URLs and
the existing TLS client. It first invokes the idempotent `POST /dids` bootstrap,
then submits the two post-genesis deltas in causal order. It compares every
returned DID and resolved closure to the locally held preview, locally verifies
all deltas, and records per-resolver acknowledgement/pending state. Logs contain
only closed outcome tokens.

The did-crdt service serializes concurrent creates of one DID. When the DID is
present it verifies that the existing genesis derives from the supplied public
key and returns the existing resolution without replacing the DAG. It never
turns a repeat create into a reset. Public read CORS is `Access-Control-Allow-
Origin: *`, permits only `GET`, `HEAD`, and `OPTIONS`, sends no credentials, and
does not expose the rendezvous or write routes.

The browser fetches raw resolution/closure bytes with `includeClosure=true` and
passes them to a new wasm-bindgen class whose Rust fields and constructors are
private. `PathBPeerAcceptance::recognise(raw_profile, raw_bundle,
raw_resolver_closure, expected_offer_bindings)` performs parsing, genesis-content
verification, post-genesis signature replay, freshness, and CON-206 steps 1
through 8 inside WASM and returns an opaque live object. Its
`finish(raw_hub_pending_acceptance, retained_device_signer)` recognises the
correlated signed CON-941 pending reciprocal record and performs steps 9 through
13 and CON-207. The object is single-use and cannot be serialized, cloned, or
constructed from booleans or a `ClosureSource` enum.

The existing JSON facade may remain for its existing non-v2 callers, but no v2
symbol or call edge reaches it. A NIF result, hub assertion of resolver facts,
unsigned projection, fabricated valid-looking fact set, bundle-supplied closure
while a resolver is reachable, or caller-supplied provenance cannot enter this
boundary.

### CON-941 — Account-authority alias command and browser acceptance transaction

Before comparison, the cbcl-bus hub exposes authenticated session command
`selfsamepairauthority(ceremonyId)`. It resolves the live session to the stable
account ID, requires that ceremony's pending signed offer, and reads the account-
keyed authority store. Its correlated signed reply is exactly `Unknown`,
`NoBinding`, or `Bound(did)`. `Unknown` covers unavailable, corrupt, ambiguous,
or unmigrated state and fails closed. The browser maps `Bound(preparedDid)` to
`BoundSameDid`, every other `Bound(did)` to `BoundDifferentDid`, and never queries
webfinger by the prepared DID to infer absence.

After raw grant verification, the hub exposes authenticated session command
`selfsamepairaccept(ceremonyId, issuerDid, grantId, deviceFingerprint)`. It
recomputes the stable account ID and account-principal digest from the live
session, recomputes `acctUri` from `issuerDid`, and requires the ceremony's
signed offer and accepted-bundle bindings. It then creates one
**ceremony-pending alias transaction** rather than publishing an alias. Under a
single Mnesia transaction it checks conflicts, captures the exact prior
alias/account/tombstone/pairaccount rows and revisions, and records the proposed
reciprocal binding. Its correlated signed reply contains that proposed record
and is a fresh authenticated-cache input for CON-206 step 9. No public
WebFinger row or pairaccount authority changes yet.

The browser passes that opaque reply to CON-940 `finish`, runs CON-206 steps 9
through 13 and CON-207, then installs its local connection with strict IndexedDB
durability. It next sends the actual retained device key through
`pairaccountgrant` extended with ceremony ID and stable-account commitment. In
one hub transaction, `pairaccountgrant` rechecks the held pending transaction,
atomically finalizes its reciprocal alias/account projection and the exact
`(accountId, deviceKey)` pairaccount authority, marks the ceremony finalized,
and only then emits the correlated `pairaccountgranted`. Retry returns the same
acknowledgement and record digest. Receipt remains prohibited until that reply
and the local install both succeed.

Rollback is an explicit transaction, not a call to permanent withdrawal.
Before finalization it deletes only the pending record. After finalization it
uses the captured versions to restore the exact prior alias, account,
tombstone, and pairaccount rows and removes the ceremony's rows; an intervening
conflicting writer fails closed and keeps compensation pending. It SHALL NOT
create a withdrawal tombstone as compensation. A same-account relink of the
same tombstoned URI may perform exact-account reinstatement at finalization; a
different-DID relink leaves prior URI tombstones intact. Both cases preserve
unrelated historical generations and are rolled back byte-for-byte on failure.

Legacy store primary keys are preserved rather than falsely re-keyed:

| Table | Primary key retained | Stable-ID treatment for v2 |
|---|---|---|
| `cbcl-webfinger-alias` | full normalized alias URI | account reference and secondary index use stable account ID |
| `cbcl-webfinger-account` | handle | remains a mutable lookup projection to stable account ID, never authority |
| `cbcl-webfinger-withdrawn` | alias URI | secondary handle index migrates to stable account ID; multiple URI-keyed generations remain possible |
| `cbcl-chat-pairaccount` | composite `(Handle, DeviceKey)` | retains the wire-compatible key and adds stable-account/ceremony indexes; authority is checked through stable ID |

New account-authority, scope, pending-ceremony, device-registration, and
compensation tables are keyed by stable account ID plus their natural ceremony
or device component. Handle/session-key tables are indexes only. Handle changes
transactionally rewrite mutable projection/index rows without changing scope or
authority. CON-939 migration SHALL complete before either v2 command is
reachable; no v2 command calls the reviewed unauthenticated `provision/3`
directly.

The browser's opaque acceptance object records resolver source, freshness,
grant ID, issuer, account URI, permissions, device proof, pending-transaction
digest, and ceremony bindings. IndexedDB replacement uses one strict-durability
transaction whose failure restores the prior record. Pending hub transaction,
browser local commit, hub finalization, and acknowledgement are separately
recorded compensatable stages rather than misrepresented as one cross-system
atomic transaction.

### CON-942 — Receipt binding and compensation ledger

The hub acknowledgement is extended or wrapped with ceremony ID,
account-principal digest, stable account ID commitment, device fingerprint, and a digest of its durable
pairaccount record. The browser correlates all fields and rejects an earlier,
later, or unrelated `pairaccountgranted`. Only then may it construct `receipt`.

The wallet verifies every receipt field against held issuance and writes its
local linked record atomically before reporting linked. A receipt is idempotent
for the exact held ceremony. A second receipt with changed bytes is terminal.

Both endpoints retain a bounded compensation ledger keyed by ceremony/grant ID.
It records which of resolver publication, grant release, hub pending-alias
creation, browser install, hub alias/pairaccount finalization, and correlated
acknowledgement occurred. On refusal or timeout the shell executes only the
inverse operations applicable to recorded stages, including CON-941's
version-checked exact-row restoration, and retries pending revocation/rollback
without issuing a second grant. This ledger contains no `C`, account scope,
private key, grant body, or relay token and is sealed at rest on the wallet.

## Decisions

### ADR-922 — Relay trust is exact-pair, sealed, and person-owned

The wallet has no vendor relay allowlist. It authenticates the application's
profile live, then asks the person whether that application may use that relay.
The stored decision is exactly `(applicationId, relayOrigin)`, protected in a
separate custody-derived policy record without mutating the identity root.
Relay-only trust recreates F-A; a plaintext JSON policy makes a forged entry
equivalent to consent. Both are rejected.

### ADR-923 — Standalone first contact requires a human CPace channel

An opaque invitation can authenticate an application backend but cannot show
that the carrier came from the person's own signed-in browser session. v2
therefore requires the public QR and the separately transcribed presence code.
The code carries independent 128-bit CPace and anti-crowding values and is
deliberately unlike BIP-39 recovery. Its 55-character entry cost is the explicit
cost of choosing standalone pairing without a prior enrolment trust record.

### ADR-924 — Application account facts come from a signed server-built offer

CPace proves shared code knowledge, not application or account authority. The
hub authenticates its live session and constructs the CON-214/219 offer itself.
The wallet treats the account label as an application-session assertion even
after signature verification. Arbitrary-octet signing and browser-authored
scope/account claims remain prohibited.

### ADR-925 — One preliminary consent, one final effect gate

The first grant decision lets the person inspect what the application asks and
permits only an unsigned deterministic issuer preview. The final decision is
the unique gate before signatures, publication, persistence, and payload. This
keeps the CON-221 comparison possible without relocating irreversible effects
ahead of it.

### ADR-926 — Application-declared relay use is not operatorship proof

Pair-scoped consent mitigates reuse of a popular relay by a new application; it
does not bind DNS/service operatorship. Version 2 states that residual and
defers global operator credentials rather than treating `operatorId` as proof.

### ADR-927 — Add v2 inside existing transport bounds

Changing shared frame bounds would alter v1 recognition and its rejection
vectors. v2 uses the already admitted 64,512 and 4,096 size classes, preserves
62,000 as the credential limit, and adds a distinct dialect/profile/domain. No
v1 byte or boundary moves.

## Test specification

Every row is release-blocking for the component it names. Positive tests alone
do not satisfy a prohibited-action or mutation row.

### TEST-1011 — Public carrier recognition performs zero I/O

Recognise valid and malformed v2 carriers while network, store, custody, clock,
and relay doubles panic on access. Malformed/noncanonical application context,
relay, locator, size, version, profile, duplicate field, or embedded secret
refuses before any double is called.

### TEST-1012 — Origin contact is prompted, burned, and rate-bounded

An unseen attacker origin causes one unverified-origin prompt and zero fetches
until accept. Reject, a second use of the carrier, and a sixth distinct unseen
origin within 24 hours cause zero network actions. One accept permits one
no-redirect fetch; redirect, TLS failure, stale cache, or second fetch refuses.

### TEST-1013 — Pair scope prevents the trusted-relay substitution

Accept `(A,R)`. Present live valid profiles for `(B,R)`, `(A,S)`, and `(A,R)`
after R is removed from A's profile. The first two prompt as new pairs and the
third refuses. Only the exact unchanged pair passes without prompt. Release
builds refuse loopback before policy lookup.

### TEST-1014 — The relay policy rejects valid-looking forgery

Round-trip the two policy slots, then independently mutate a well-formed entry,
nonce, tag, root fingerprint, slot name, revision, and timestamp. Each invalid
slot is ignored when the other is valid; no valid slot refuses and opens no
socket. Crash before write, during destructive slot replacement, before and
after read-back verification: the highest valid policy is exactly old or new
and the sealed root remains byte-identical. Race two processes through the named
lock and prove no lost update. Exercise passcode/presence-gated reset from two
corrupt slots and prove no ungated call edge reaches it. A recovery restore
imports no policy and prompts again. Corrupt the installation-keyed origin
ledger and require zero network I/O without a custody prompt. Record the absence
of a rollback-resistant hardware counter and the mobile/desktop presence split.

### TEST-1015 — Pairing code is distinct from recovery and stays human-only

Generate fixed and random v2 invitations. Decode the public carrier, QR bytes,
logs, analytics events, browser storage records, relay frames before CPace, and
hub allocation request; none contains `C`, `T`, or the PAIR1 string. The relay
sees only the commitment before authenticated claim. The wallet has no QR/paste
command that can populate `PresenceCode`. Every valid twelve-word English
BIP-39 recovery phrase is rejected by the pairing parser, every PAIR1 code is
rejected by restore, and the two surfaces share no parser or DOM input. Upper,
lower, and mixed ASCII case normalize to the same code; Unicode lookalikes do
not. A local grammar/checksum error permits corrected re-entry inside the same
idle deadline without allocating, claiming, or burning the ceremony.

### TEST-1016 — Co-presence and wrong-account adversarial corpus

Exercise: forwarded public QR without the code; correct QR with one wrong code
character; code from a different ceremony; a valid signed offer for another legitimate
chat account; and a remote attacker who can supply only machine-carried values.
All fail before intent or show the exact application-session asserted account
and require the independent human code plus explicit consent. No account label
is rendered as `AuthenticatedPerson`.

### TEST-1017 — CPace context transplant matrix

Mutate each carrier field, claim commitment/token, resolved mailbox ID,
application context, relay, ceremony ID, either endpoint's locally computed
profile digest, role, code, and v2 domain one at a time. Also replay v1
frames into v2 and v2 into v1. Every case fails Finished or recognition and
burns the invitation with zero display and identity effects.

### TEST-1018 — One server-built signed offer reaches display

From a real authenticated chat-session fixture, allocate a scope and pending
ceremony with no offer timestamps. After both Finished values, execute the
prepare/finalize round trip, verify the browser checked the returned core before
signing, and obtain one server-constructed CON-219 v2 offer. Verify its signature
from the live profile, proof-free core digest, transcript, carrier/session/stable-
account/distinct-device/proof/provider bindings, request consumption, and closed
display provenance. Exactly one authenticated display effect results.

### TEST-1019 — Intent mutation corpus emits no display

Independently mutate application ID/origin, account principal/display, scope,
device DID/JWK/digest, permission, profile/descriptor/provider/carrier/offer
digest, ceremony/request ID, nonce, time window, platform binding, action, and
authority summary. Re-sign only where the attacker model holds the relevant
non-authoritative key. Every mutation terminates with zero display/consent.

### TEST-1020 — Prohibited display construction is structurally absent

Compile-time/API tests prove credential/v2 cannot construct display fields from
generic peer strings, cannot set a provenance boolean, and cannot render unknown
provenance. Source/symbol tests reject the old `claimed_by_secret_holder` path
from the v2 dependency graph.

### TEST-1021 — CON-214 replay consumption is complete

For approve, preliminary decline, final decline, timeout, and malformed evidence
that fails before step 5, snapshot the request ledger. Every syntactically valid
authorised request is consumed once before display; failures before step 5 are
not; every replay refuses before display or key access.

### TEST-1022 — Preliminary consent cannot reach an identity effect

Instrument hierarchy, issuer creation, randomness, JWS signing, resolver POST,
issuance/grant stores, bundle construction, alias command, and payload send.
Carrier parsing, profile/policy admission, CPace, offer display, and preliminary
decline call none. Preliminary approve may call only the zeroized derivation and
pure DID/fingerprint functions.

### TEST-1023 — Preview is deterministic, unsigned, and zeroized

For fixed root/application/scope, preview and later commit derive the same DID.
Preview emits no delta, JWS, grant token, closure, store write, or network call;
its key material cannot escape its custody closure and is zeroized on success,
error, decline, panic-unwind, and timeout.

### TEST-1024 — Final decline has no identity residue

Snapshot hierarchy-visible state, resolver state, issuance/grant stores, alias,
browser connection, hub pairaccount, and outbound frames immediately before
preliminary approval. After preparation followed by final decline or timeout,
all durable identity/application states are byte-identical. Only the request
tombstone and separately accepted policy/rate state may differ; assert that the
application did receive the preview DID and that preliminary consent disclosed
that exact residual.

### TEST-1025 — Final approval is the unique effect gate

Record a total order of calls. `issuer::create`, grant token generation, every
signature, DID bootstrap/delta publication, pending issuance write, bundle build,
and payload send all follow the exact final-decision hash. Mutate the preview DID
between preview and commit and require refusal before the first signature.

### TEST-1026 — Stable scope follows account ID and device keys stay distinct

Two retries and two installations mapped to one stable account ID reuse one
scope and have two different retained device keys, each distinct from every
session key. A replacement session key authorised through account recovery keeps
the ID/scope; an unlinked key using the same handle obtains neither. Device proof
substitution, per-ceremony device-key minting, session-key-as-device, proof over
a different core/transcript, and proof supplied before Finished all refuse
before offer signature.

### TEST-1027 — The arbitrary signing oracle is unreachable

Attempt to vary every CON-214 field through the browser and through direct HTTP
calls. The hub either derives the canonical value from its signed session/profile
or refuses. Credential/v2 has no call edge to `/selfsame/enrolment/sign`, and an
unauthenticated caller obtains no signature.

### TEST-1028 — Independent v2 state-machine and cryptographic vectors

Two independent implementations reproduce every public carrier, context,
CPace/Finished value, key schedule, AEAD frame, CBCL control hash, fixed body,
predecessor hash, intent digest, and terminal classification. Existing v1
vectors and boundary rejections remain byte-identical. This row remains red
until the independent implementation includes CPace group math.

### TEST-1029 — Reducer directions, duplicates, silence, and terminals

For every v2 state, test the valid next sender and all invalid senders,
reordering, skipped predecessor, conflicting duplicate, exact retry, refusal,
mailbox close, expiry, and silence. Exact retry is idempotent; every other
invalid path has one terminal, closes the mailbox, and emits compensation only
when a grant may have been released.

### TEST-1030 — Exact payload and enclosing bounds

Credential logical lengths 1, 61,999, and 62,000 pad into a 64,512-byte field
and encode respectively to 64,558, 64,560, and 64,560-byte application objects;
62,001 refuses before allocation/send.
Every control type pads to 4,096 and encodes to 4,144. Assert exact large/control
maxima 66,628/6,212 plaintext, 66,644/6,228 ciphertext, and 66,696/6,278 relay
body, plus the encoded WebSocket maximum below 70,000. Re-run all v1
62,000/62,001 and 69,632 boundary vectors unchanged.

### TEST-1031 — Relay work remains bounded

Drive the maximum legal v2 frame sequence across 10,000 mailbox attempts and at
the configured limiter boundary. Derive exact per-ceremony queued bytes, enforce
the existing 512 MiB cap, delete acknowledged/terminal bodies, and prove the
relay never parses application bodies or grows work with claimed logical length.
An observer holding only the public carrier cannot change, claim, crowd, or
terminate the mailbox. Wrong `T` and unknown locator are observationally equal;
only the committed `T` creates claimant membership once. Submit the exact
canonical bare-v1-`claim` bytes against waiting, paired, and closed v2 mailboxes
and require `Error(404)`, no peer route, and byte-identical mailbox snapshots.

### TEST-1032 — DID creation is idempotent and cannot reset state

Create a DID, add alias/assertion/revocation deltas, then repeat `POST /dids`
concurrently and sequentially with the same public key. The bootstrap genesis
content hash and complete resolver DAG remain byte-identical, including its
documented empty-signature bootstrap; every post-genesis delta verifies. A
different key/DID/genesis-hash mismatch refuses. Crash/retry between each
publication step converges without deleting or duplicating accepted deltas.

### TEST-1033 — Browser resolver path is direct, CORS-safe, and verified

From an HTTPS browser-origin harness, preflight and fetch both public closure
routes and feed the raw bytes to the opaque WASM object. It internally verifies
genesis content, replays signed post-genesis deltas, and reaches the split
CON-206 boundary. Prove JS cannot construct/clone/serialize the object or pass a
valid-looking boolean fact set. Assert no credentials and no write-method CORS.
Unsigned projection, hub proxy, 404/410, malformed/stale closure, and bundle
fallback while any resolver answered all refuse CON-206.

### TEST-1034 — Application decides CON-221 and grant issuer is bound

Exercise `NoBinding`, `BoundSameDid`, `BoundDifferentDid`, and `Unknown` through
the authenticated stable-account authority command, including an account bound
to a different DID before this ceremony. Only the application result controls
applicability; claimant/peer flags are absent. Mutate the bundle grant issuer away from the
prepared DID and refuse before alias provisioning and CON-206. For `NoBinding`,
both screens render identical hex and LifeHash and record the comparison action.

### TEST-1035 — Only the account authority provisions aliases

The browser cannot call the webfinger store directly. Its ceremony-bound hub
command creates one idempotent pending transaction for the recomputed
alias/stable account ID and refuses wrong account, wrong ceremony, supplied
noncanonical alias, other DID, conflicting authority, and a tombstone owned by a
different account. No refusal changes an existing row. Assert the exact legacy
primary keys and stable-ID secondary indexes from CON-941. Withdraw, then relink
the same stable account under the same DID and a different DID: same-alias
reinstatement is explicit, other URI-keyed tombstone generations remain, and
neither legitimate relink is permanently refused. Change the display handle and
prove scope/authority are unchanged while mutable projections move cleanly.

### TEST-1036 — Full browser CON-206/207 acceptance succeeds

Using a real published issuer closure and retained browser device key, execute
CON-206 steps 1..8, pending hub acceptance, steps 9..13, CON-207 proof, strict
IndexedDB install, and actual ceremony-extended pairaccount submission. Require
the last hub transaction to finalize alias plus pairaccount before its correlated
acknowledgement. Record the resolver source and acceptance facts; no fixture or
NIF-authored acceptance facts enter.

### TEST-1037 — Acceptance mutation and rollback corpus

Mutate every bundle/grant/closure/device/permission/audience/account/alias,
prepared-issuer, ceremony, request, and freshness input. Inject failure after
each browser/hub stage. Before finalization, hub rollback deletes only pending
state. After finalization, it restores byte-identical prior alias, account,
tombstone, and pairaccount rows without creating a withdrawal. Each mutation or
failure emits no receipt, restores or quarantines local state, invokes only
applicable compensation, and never reports linked.

### TEST-1038 — Receipt waits for the matching real hub acknowledgement

Delay, drop, duplicate, and substitute `pairaccountgranted`; return
pairaccount-limit/store-failed; and acknowledge a different account ID, principal,
device, ceremony, pending-transaction digest, or finalized record digest. No
receipt appears. Crash at each Mnesia write boundary and prove alias plus
pairaccount are both old or both finalized, never split. Only the matching
durable hub acknowledgement plus local install produces a receipt whose bound
digests the wallet verifies before linked.

### TEST-1039 — Post-payload failure revokes and rolls back

After a released grant, inject allocator refusal, alias failure, CON-206 failure,
browser-store failure, hub failure, relay close, and receipt timeout. The wallet
queues one idempotent CON-210 revocation, the application rolls back pending or
finalized hub stages with exact prior-row restoration, and neither reports
linked. Retry reuses the stable issuer DID and scope and never releases the same
grant ID twice.

### TEST-1040 — Fixture and prohibited-production paths are absent

Ordinary wallet/WASM/hub artifacts contain no `local_demo`, local credential
fixture, deterministic recipient/bundle, caller-octet signer call, compiled relay
admission registry, v1 fallback, or loopback mapping. Production allocation
remains false and the old fixture cannot be reached through the visible link
affordance.

### TEST-1041 — Local end-to-end standalone link

With production allocation still held, run the explicit local/staging gate:
signed-in cbcl_chat account → public QR plus PAIR1 code → new-relay policy
accept → `allocate-v2`/`claim-v2` → CPace → post-Finished hub offer
prepare/finalize → exact-intent consent → unsigned preview → fingerprint
comparison → final approve → resolver publication → reverse grant → pending
alias + CON-206/207 → strict browser install → atomic alias/pairaccount
finalization → real acknowledgement → receipt. Assert the wallet and browser
show linked, the hub holds the distinct actual device key, the resolver holds
the issuer closure, and relay logs contain only bounded opaque frames.

### TEST-1042 — Profile version 2 has one exact cross-role grammar

Feed allocator and claimant the nine-name version-2 profile and every one-field
addition/removal, including legacy `rendezvous`, `pairingRecordRelays`, version 1,
invalid `operatorId`, duplicate relay origin/operator, and unknown field. Both
roles agree exactly. Version-1 profile recognisers continue to accept/reject
their ratified grammar, while credential/v1 relay selection accepts a fully
recognised version-2 profile without protocol fallback. CON-220 tier 3 is absent
for v2. The cbcl-bus generator, live fixture, served bytes, browser embedding,
and wallet vector all say `profileVersion: 2` and hash identically. Source and
artifact scans prove neither v2 role consults a compiled digest registry.

### TEST-1043 — Offer and state deadlines have one interpretation

At each REQ-951 state, advance the monotonic clock to one tick before, at, and
after its idle and absolute deadlines. Exactly the earlier deadline terminates.
Consume the full 180-second claim and 30-second CPace budgets and prove no offer
timestamp exists yet; then complete hub prepare/finalize and authorise inside its
30-second state. Let the 120-second CON-214 window expire after successful
authorisation while later human decisions remain inside state/absolute
deadlines: no later code re-evaluates it. Delay offer delivery/authorisation past
120 seconds from `OfferCoreV2` construction and require refusal before request
consumption or display.

### TEST-1044 — Device proof has no self-reference or pre-Finished oracle

Recompute `offerCoreDigest` from an exact core and prove the core contains no
`devicePossessionProof`, evidence, or provider hint. Verify the proof over the
domain, core digest, and exact CPace transcript, then verify hub evidence over
the proof and core. Attempt finalize with a proof over the complete offer, a
different transcript/core, caller-changed account field, before Finished, and
after one finalized offer; all refuse without a signature oracle or second
offer.

### TEST-1045 — Pending alias transaction restores exact prior generations

Snapshot every relevant Mnesia row, create a pending acceptance, and prove
public WebFinger and pairaccount state are unchanged while its signed reply
satisfies only the ceremony's CON-206 step 9. Crash or refuse before and after
local install and final hub commit. Rollback restores exact prior rows, including
all URI-keyed tombstone generations, and creates no withdrawal. Exact retry is
idempotent; a concurrent changed row leaves compensation pending rather than
overwriting it.

### TEST-1046 — Companion SPEC-001 hard stops and raw locator binding hold

Run the companion TEST-030 through TEST-037 suite against the exact
`allocate-v2`/`claim-v2` CDDL and store-v2 fixtures. Recompute the commitment
from raw 32-byte `M` and raw 16-byte `T`; encoded locator/text substitutions
fail. Prove v1 crowding remains, v2 surplus claims preserve state, the relay
derives no pairing key or application semantic, all shared bounds remain, and
production allocation remains disabled.

### TEST-1047 — Person-visible residuals and three custody operations are exact

Snapshot release notes and first-use UI and require both relay-operatorship and
same-origin-control residuals without claiming independent certification. On
mobile, policy access, preview, and commit each require a separate passcode plus
OS-presence operation; on desktop each requires a separate passcode custody
opening and no nonexistent biometric callback. Cancel or fail each operation
and prove no wrapping, policy, hierarchy, or signing key survives into the next.

## Cross-repository implementation boundary

No implementation starts before review PASS. After PASS, the executable SPL
theory SHALL decompose at least these independently evidenced units:

| Repository | Required slice |
|---|---|
| `cbcl-pairing` | companion SPEC-001 0.3.0 `allocate-v2`/`claim-v2`, admission state/store-v2, v1 compatibility; v2 public invitation and PAIR1 types; context/profile-digest domains/CDDL/dialect; injected authenticated intent verifier; preparation/comparison/final/reverse-payload/receipt reducer; fixed body classes; independent vectors |
| `selfsame` pure core | exact v1/v2 profile dispatcher including v1 rendezvous grammar; v2 offer/evidence extensions and authoriser; proof-free core digest; closed display provenance; issuer preview; bundle/preparation/receipt binding; two-slot policy grammar; compensation state |
| `selfsame` Tauri wallet | two-slot locked policy plus gated reset and installation-keyed origin ledger; origin prefetch/rate limit; v2 transport; split preview/commit issuance; non-destructive resolver publication; compensation; truthful terminals |
| `selfsame` wallet UI | public-carrier scan; mandatory PAIR1 entry without paste/autofill and mutually exclusive recovery parser; unverified-origin prompt; exact-pair policy manage/remove; application-asserted account display; preliminary disclosure/final consent; comparison and receipt states |
| `selfsame` browser WASM | v2 allocator ABI without pre-staged grant; opaque raw-closure acceptance object; pending-hub-record split for full CON-206/207; fixed envelope handling |
| `cbcl-bus` hub | stable account-ID migration/indexes with legacy primary keys preserved; initial ceremony allocation; post-Finished offer prepare/finalize; distinct device registration/proof; account-ID/scope/ceremony tables; authority query; pending alias transaction; atomic alias/pairaccount finalization; exact rollback/compensation |
| `cbcl_chat` browser | version-2 production-profile generator/embedding under amended SPEC-075 REQ-007; PAIR1/QR split; non-fixture allocator; retained device key and non-circular proof; offer prepare/finalize; comparison; direct resolver fetch; opaque pending acceptance; acknowledgement-gated receipt |
| `cbcl-bus` relay | expose the companion `allocate-v2`/`claim-v2` library/NIF path and store-v2; unchanged application-blind bounds; v1/v2 work, opacity, restart, and terminal tests; no application parser |
| `did-crdt` | serialized idempotent non-resetting create; read-only CORS for public resolution/closure; publication/resolution integration tests |

## Gate and sequencing

1. A new fresh-context Claude-driver Circus review of this exact revision, the
   companion cbcl-pairing SPEC-001 0.3.0 draft at `7ddea48`, and all four code
   baselines, with a defect-finding mandate and no access to the authoring
   conversation. Owner approval alone is insufficient. Both documents must PASS
   in one coordinated verdict.
2. If and only if PASS, encode the approved design as an executable SPL
   Elephant theory. Every TEST-1011–TEST-1047 result and every upstream amendment
   approval is an explicit gate; no theory fact may be asserted from prose
   alone.
3. Commit failing pure-core tests and independent vectors before production
   code. Implement `cbcl-pairing` v2 and pure Selfsame types before effectful
   shells.
4. Implement custody/policy and resolver contracts before wallet issuance;
   implement hub scope/offer/alias/account acknowledgement before browser
   allocator and acceptance.
5. Run unit, depth, red, prohibited-action, mutation, work-bound, CORS,
   reproducible-WASM, Android, browser, and local real-relay E2E gates.
6. Keep production allocation disabled until REQ-953's independent human and
   vector evidence is durable. Ask the owner separately before any cbcl-bus or
   did-crdt production deploy; both are outward-facing and have no assumed
   canary.

## Explicit open production gates

- A human cryptography reviewer has not yet approved credential/v2 CPace inputs,
  Finished labels, key separation, counters, AEAD nonces, or fixed-class padding.
- An independent implementation currently does not duplicate CPace group math;
  TEST-1028 and the SPEC-007 vector gate remain red until one does.
- The coordinated SPEC-004 CON-201/204/206/211/214/219/220/221, SPEC-007,
  cbcl-pairing SPEC-001 0.3.0, cbcl-bus SPEC-075 REQ-007, and did-crdt HTTP
  amendments require their `selfsame`, `cbcl-pairing`, `cbcl-bus`, and
  `did-crdt` human owners' approval after this design review. SPEC-004 REQ-215
  and SPEC-002 REQ-103 are conformed to, not amended.
- The relay operator availability exception or a second independent operator,
  and privacy/security reviews, remain required by the existing production gate.
- Global relay operatorship proof remains out of scope under ADR-926.

## Changelog

- **0.4.4-draft — 2026-08-23 — review candidate, not approved.** Responds to the
  independent 0.4.3 REJECT: coordinates the cbcl-pairing REQ-005 hard-stop
  amendment and exact v2 allocation/store wire; moves offer minting after
  Finished with a non-circular device proof; corrects size/grammar/limiter
  claims; makes production profile-version migration explicit; implements a
  two-slot person policy with gated repair; preserves alias/tombstone primary
  keys and adds a pending/finalized hub transaction with exact rollback; and
  adds tests for every new obligation. No implementation exists under this
  amendment.

- **0.4.3-draft — 2026-08-23 — independently reviewed REJECT, not approved.** Responds to the
  independent 0.4.2 REJECT: mints profile version 2 with the actual nine-name
  grammar; replaces BIP-39 with a domain-distinct PAIR1 code and an independent
  anti-crowding token; binds independently computed profile digests into CPace;
  introduces stable hub account IDs and distinct device keys; adds account-
  keyed authority lookup and storage migration; moves policy out of SealedRoot;
  requires an opaque raw-closure WASM boundary; defines exact deadlines and
  genesis semantics; reconciles inherited stops and exact encoder arithmetic.
  No implementation exists under this amendment.

- **0.4.2-draft — 2026-08-23 — review candidate, not approved.** Responds to the
  independent 0.4.1 REJECT: adds a public-carrier/128-bit-human-code split;
  truthful application-session account provenance; unsigned zeroized issuer
  preview and a final effect gate; ratified relay profile grammar; sealed policy;
  exact retained size bounds; direct browser resolver CORS; non-destructive DID
  creation; authority-owned alias provisioning; correlated hub acknowledgement;
  stable account-key scope; explicit timeouts/compensation; and a complete
  inherited-hard-stop table. No implementation exists under this amendment.
