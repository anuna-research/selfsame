---
id: spec-008-0.5.13-implementation-claude-adversarial-review-2026-08-25
title: SPEC-008 0.5.13 implementation — Tier-1 adversarial review (REJECT)
status: complete
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
reviewer-model: claude-opus-5
date: 2026-08-25
verdict: REJECT
review-of: "[[spec-008-0.5.13-production-boundary-and-reproducibility-remediation-2026-08-25]]"
parent: "[[SPEC-008-production-pairing-claimant]]"
---

# SPEC-008 0.5.13 implementation — Tier-1 adversarial review

## Reviewer identity, subscription authentication path, Circus attempt, and independence

| Field | Value |
|---|---|
| Reviewing family | Anthropic Claude |
| Reviewing model | `claude-opus-5` |
| Generating family | OpenAI GPT-5 (`gpt-5.6-sol`) |
| Review worktree | `/Users/anuna-01/Code/selfsame-spec008-v0513-review-2` (Circus ref `circus/spec008-v0513-review/2`) |
| Driver | interactive Claude CLI on the person's Max subscription |

`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` were checked for presence
without printing any value; both are **absent** in this session's environment.
`env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` reported:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

No email, organization identifier, token, key, or account identifier is
recorded here. The API-billed `circus-driver-claude` did not run.

**Independence statement.** This session received no generating-session
reasoning, transcript, or intermediate artefact. Every conclusion below was
reached by reading the governing parents and the committed candidate sources
and by executing tests and mutations in clean detached clones created from the
exact revisions. The prior review reports were opened only after the
independent pass was complete, exactly as the review prompt directs. The
person's dirty working trees, output APKs, and patch scripts were not read as
evidence, and were not modified or deleted.

## Exact commits reviewed

| Repository | Candidate | Prior baseline | Governing parent |
|---|---|---|---|
| Selfsame | `ff2f1394813a38ce9d0409726892b6e0c63d00ea` | pre-remediation `0dbfb3c4323aec568ac2803d58c0aa71c301edda`; coordinated `0220cec2dec44cd95d4f411ea4814d790b6716d2` | `specs/SPEC-008-production-pairing-claimant.md`, `specs/SPEC-007-cbcl-pairing-cutover.md` |
| cbcl-bus | `22fcc012ff4d4746f251b497c11495b1b26c0f29` | WASM vendor `eaacb080304c57730a1e82be483e78aa8b8bb99e`; coordinated `ce894a7f2debc111b610296b1a3780de6084e37e` | `specs/SPEC-053-key-root-identity.md` (authority commit `1b145cbb4b3771a43abb18d3b3893fa1927e679a`) |
| cbcl-pairing | `62ef4a968b46b4836374fcee1d78c410f730a7a7` | coordinated `c22f2d0526432c01c56d64be35a8d4f87516ec92` | `specs/SPEC-001-reusable-blind-pairing.md` |
| did-crdt | pin `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | service HEAD `94db956d7d5d5edf0f42afad073ab42f265a1752` | — |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | — | — |

The 0.5.13 remediation authority is Selfsame `bd8bcff790750c9fe3672be72fffa694131a30ed`.
The reviewer prompt commit `ddc769cfac1e74d12fc52c2566d4532fffc8e034` is not an
implementation candidate; `git diff ff2f139..ddc769c` touches only
`specs/`, so the code reviewed here is byte-identical to the candidate.

**did-crdt participation.** Selfsame's `did-crdt.sha`, cbcl-bus's `did-crdt.sha`,
the browser provenance record, and the runtime path dependency all name
`1f409a4`. Service HEAD `94db956` ("fix(service): make DID creation idempotent")
touches only the did-crdt service, is *ahead of* the pin, and is refused by the
strict witness (executed below). It therefore does **not** participate in the
reviewed runtime closure.

All work below ran in clean detached clones under
`…/scratchpad/closure/{selfsame,cbcl-pairing,cbcl-rs,did-crdt,cbcl-bus}`,
verified at the exact revisions with `git status --porcelain --untracked-files=all`
empty for every repository.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 3 |
| LOW | 3 (two carried-forward residuals, one new, each with explicit consequence and treatment) |

The four 0.5.12 MEDIUM findings are **closed**, and I reproduced the generating
agent's key claims independently: the arm-after-persist and disarm-before-replace
mutants both fail the isolated TEST-1162 case, the restored candidate passes with
the exact `1 passed; 0 failed; 0 ignored` summary, and the Linux `linux/amd64`
rebuild reproduces the recorded WASM digest. Every one of the seventeen required
mutations that has an executable gate is killed, with the one exception recorded
as M-2 below.

The rejection rests on three **verification-gate** defects, not on a protocol,
consent, binding, or effect-order exploit. I found no sequence in which a
person's approval, identity, scope, grant, or capability can be widened, and no
sequence in which a pre-payload failure strands an application slot.

Two of the three share one shape and are the reason the rejection is not
cosmetic: **both continuous jobs that TEST-1162 names as its gate abort before
reaching it.** Selfsame's `rust` job fails at `Clippy (deny warnings)`, its
first cargo step, so the isolated TEST-1162 invocation and `cargo test
--workspace` never run. Selfsame's `screens` job fails at `Render every
screen`, its first browser step, so `e2e:wallet-pairing` — the wallet pairing
lifecycle suite TEST-1162 explicitly requires — never runs either. I executed
both mandated gates by hand and both pass; what does not exist is the
continuous execution the parent requires.

## Findings

### M-1 — the Selfsame CI Rust job cannot reach the mandated TEST-1162 gate: `cargo clippy --workspace --all-targets --locked -- -D warnings` fails on the exact candidate closure

**Severity: MEDIUM.** Owner: Selfsame. Carried in from `0dbfb3c` (not
introduced by `ff2f139`), and recorded as passing by the prior review.

Affected requirements:
[[SPEC-008-production-pairing-claimant#TEST-1162]] ("The continuous gate SHALL
execute this process-global keyring test in an isolated test process… Removing
either explicit invocation SHALL fail review evidence for this test"),
[[SPEC-008-production-pairing-claimant#CON-985]] ("Exact pins form one
buildable closure"), and
[[SPEC-008-production-pairing-claimant#TEST-1166]] (whose CI execution sits in
the same job).

**Code evidence.** `crates/selfsame-web-device/src/lib.rs:568` declares

```rust
    pub fn restore_offer_context(
        &mut self,
        raw_carrier: &[u8],
        offer_core: &[u8],
        offer_core_digest: &[u8],
        pending_expires_at: u64,
        signed_offer: &[u8],
        authority_response: &[u8],
        authority_digest: &[u8],
        now: u64,
    ) -> Result<(), JsError> {
```

That is nine arguments against clippy's default `too_many_arguments` threshold
of seven. The sibling constructor in the same `#[wasm_bindgen] impl
CredentialV2BrowserAllocatorSession` block — `restore` at
`crates/selfsame-web-device/src/lib.rs:446` — carries
`#[allow(clippy::too_many_arguments)]`; `restore_offer_context` does not. There
is no `clippy.toml`, no `[lints]` table in the workspace or crate manifest, and
`reload_verified_view` at `src-tauri/src/cbcl_v2_commands.rs:1041` shows the
same allow being applied deliberately elsewhere, so this is an omission rather
than a policy.

**Failure sequence (executed).** In the clean exact-pin closure, with
rustc/clippy 1.96.0 — the `stable` toolchain `.forgejo/workflows/ci.yml:125-139`
installs:

```
$ cargo clippy --workspace --all-targets --locked -- -D warnings
error: this function has too many arguments (9/7)
   --> crates/selfsame-web-device/src/lib.rs:568:5
    = note: `-D clippy::too-many-arguments` implied by `-D warnings`
error: could not compile `selfsame-web-device` (lib) due to 1 previous error
error: could not compile `selfsame-web-device` (lib test) due to 1 previous error
```

`cargo clippy -p selfsame-web-device --locked -- -D warnings` fails identically,
so `--all-targets` is not the trigger — the crate's own library fails.

`.forgejo/workflows/ci.yml:154-158` runs that exact command with
`set -o pipefail` as the **first** cargo step of the `rust` job. Steps abort a
job on failure, so with this defect present:

1. `Clippy (deny warnings)` fails;
2. `Test` (`cargo test --workspace --locked`, line 163) never runs;
3. `Test credential/v2 interrupted-link compensation` (the isolated TEST-1162
   invocation and its new `grep -Fq "test result: ok. 1 passed; 0 failed; 0
   ignored"` assertion, lines 170-178) **never runs**;
4. `Build the core for wasm32` never runs.

The 0.5.13 remediation's whole M-1 claim is that CON-990 now has "executable
closure… not a table of synthetic labels". That closure is executable locally —
I reproduced it — but the *continuous* gate TEST-1162 requires cannot execute at
all on this candidate, and the CI diagnostic step would report the clippy line,
not a compensation failure. A load-bearing obligation is therefore missing even
though every runtime predicate fails closed.

**Measurable correction.** Add `#[allow(clippy::too_many_arguments)]` to
`restore_offer_context` (matching `restore` two hundred lines above) or reduce
its arity to seven, and require
`cargo clippy --workspace --all-targets --locked -- -D warnings` to exit 0 from
a clean exact-pin closure. Because the prior review recorded this command as
"exit 0, no warnings" while the same defect was already present at `0dbfb3c`,
the correction SHOULD also record the command's captured transcript rather than
its claimed exit status.

### M-2 — the hub's CI-selection guard does not refuse a rebuild step marked ignored, so CI can report success with zero browser rebuilds

**Severity: MEDIUM.** Owner: cbcl-bus.

Affected requirements:
[[SPEC-053-key-root-identity#REQ-083]] ("The CI Linux runner SHALL execute that
rebuild") and
[[SPEC-053-key-root-identity#TEST-120]] ("Rename the CI rebuild test selector
**or arrange for it to execute no check**. Require CI to fail rather than report
an empty success"), coordinated into
[[SPEC-008-production-pairing-claimant#TEST-1165]].

**Code evidence.**
`apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs:25-34`:

```js
const recogniseCiRebuild = (source) => {
  const steps = [...source.matchAll(
    /- name: Rebuild and verify the pinned Selfsame browser package[\s\S]*?(?=\n\s+- name:|\n\s{2}[a-zA-Z0-9_-]+:|$)/g,
  )];
  assert.equal(steps.length, 1, …);
  assert.match(steps[0][0], /cargo install wasm-bindgen-cli --version 0\.2\.126 --locked/);
  assert.match(steps[0][0], /scripts\/check-selfsame-wasm-rebuild\.sh/);
  assert.doesNotMatch(steps[0][0], /continue-on-error:\s*true/);
};
```

The recogniser inspects the step's *name*, its two commands, and
`continue-on-error`. It never inspects the step's `if:` condition. A step-level
`if: false` is not an ignored *failure* — it is a skipped *step*: Actions
evaluates the condition, does not run the step, and the job continues and can
succeed.

**Failure sequence (executed).** In the clean cbcl-bus checkout at `22fcc01`,
inserting one line after the step name in `.github/workflows/ci.yml`:

```yaml
      - name: Rebuild and verify the pinned Selfsame browser package
        if: false
        run: |
          set -eux
          cargo install wasm-bindgen-cli --version 0.2.126 --locked
          scripts/check-selfsame-wasm-rebuild.sh
```

```
node --test apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs
→ 6 pass, 0 fail   (VENDOR SUITE: PASS — mutant survived)
```

The comparison mutants the test *does* carry — renaming the step, replacing the
script invocation with `:`, and adding `continue-on-error: true` — were all
killed in the same run, so the guard is real; it simply does not cover the case
its own title ("CI cannot silently select zero browser-package rebuilds") and
REQ-083's own trajectory name. The 0.17.9 authority states the stronger property
as already implemented: "an executable guard that requires exactly one
**non-ignored** CI rebuild step" and "deletes the rebuild command, renames its
step, **and marks it ignored** in separate mutants. Its recognizer refuses every
mutant." The `if:` form is not refused.

**Why the consequence is wider than a missing mutant.** The vendor suite alone
does not check the recorded host triple or the exact tool versions — it only
regex-shapes them (`/^0\.2\.\d+$/`, `/^1\.\d+\.\d+$/`) and never reads
`buildHost`. I verified this by mutation: `buildHost →
aarch64-apple-darwin`, `rustcVersion → 1.95.0`, and `wasmBindgenVersion →
0.2.125` all **survive** the vendor suite and are caught **only** by
`scripts/check-selfsame-wasm-rebuild.sh`. With the rebuild step skipped by
`if: false`, the host, compiler, and generator provenance of the served browser
artifact becomes entirely unchecked while CI stays green — which is precisely
the class of failure the rejected 0.5.12 M-3 finding was about.

**Measurable correction.** Extend `recogniseCiRebuild` to require that the
matched step carries no `if:` key (or only an `if:` the test whitelists), and
add an `if: false` mutant to TEST-120's executable mutant list beside the three
present ones. Additionally, assert the exact `buildHost`, `rustcVersion`, and
`wasmBindgenVersion` values in the vendor suite so those three provenance
fields have a gate that does not depend on the rebuild step running.

### M-3 — the Selfsame `screens` CI job cannot reach the mandated wallet-pairing browser gate: `npm run screens` fails on the exact candidate, and one of its two failures is a regression the credential/v2 front-end introduced

**Severity: MEDIUM.** Owner: Selfsame. One half is new in the coordinated
increment; the other predates it.

Affected requirement:
[[SPEC-008-production-pairing-claimant#TEST-1162]] ("The continuous browser
gate SHALL execute the wallet pairing lifecycle suite that exposes and abandons
interrupted pending rows. Removing either explicit invocation SHALL fail review
evidence for this test.").

**Failure sequence (executed).** From the clean exact-pin closure with
`npm ci` at the tracked lockfile (103 packages, exit 0):

```
$ CI=true npm run screens
…
ERRORS:
  31-pairing-wait: missing required text "Secure pairing started"
  31-pairing-wait: missing required text "wss://pairing-relay.example"
  31-pairing-wait: missing required text "Nothing is authorised until you see and approve the verified request."
  31-pairing-wait: reached pairing-enter, expected pairing-wait
  invoke-surface: src/app.js calls "cbcl_enrol_prepare", which lib.rs does not register — …
  invoke-surface: src/app.js, src/app.js calls "cbcl_enrol_confirm", which lib.rs does not register — …
exit 1
```

`.forgejo/workflows/ci.yml:310-321` runs, in this order, `Render every screen`
(`npm run screens`), `Run the cbcl-pairing browser ceremony` (`npm run
e2e:pairing`), and `Run the wallet pairing lifecycle` (`npm run
e2e:wallet-pairing`). The first step exits 1, so the job aborts and **the
wallet pairing lifecycle suite never runs on CI.**

**Code evidence, failure 1 — a real regression.** `tests/screens.mjs:313` drives
screen 31 with `['to-applications', 'to-pairing', 'fill-cbcl-invitation',
'start-cbcl-pairing']` against a stub bridge whose only pairing command is
`cbcl_pairing_start` (`tests/screens.mjs:256`). At the coordinated baseline
`0220cec`, `src/pairing.js` called exactly `cbcl_pairing_start`,
`cbcl_pairing_cancel`, and `cbcl_pairing_capability`. At the candidate it calls
`cbcl_v2_recognise`, `cbcl_v2_relay_decide`, `cbcl_v2_preliminary_decide`,
`cbcl_v2_final_decide`, `cbcl_v2_finish`, and eight more — and no longer calls
`cbcl_pairing_start` at all. `git diff --name-only 0220cec ff2f139 --
tests/screens.mjs` is empty: the harness was never updated for the credential/v2
front-end (its last touch is `062ffc3`, before the increment). The UI therefore
stays on `pairing-enter` and the three required strings on `pairing-wait` are
never painted.

I confirmed the direction of the regression by executing the baseline
front-end against the same harness: checking out `0220cec`'s `src/` and
`tests/` into the closure and re-running `npm run screens` reproduces **only**
the two `invoke-surface` errors — screens 30 and 31 pass there.

**Code evidence, failure 2 — a pre-existing checker gap, not a dangling call.**
`tests/screens.mjs:996` builds the registered-command set with
`/(?:commands|app_identity)::(\w+)/g`. `src-tauri/src/lib.rs:115-117` registers
`app_grant::cbcl_enrol_start`, `app_grant::cbcl_enrol_prepare`, and
`app_grant::cbcl_enrol_confirm` — under a third module prefix the regex does not
match. The two commands *are* registered; the checker cannot see them. This
half is present at `0220cec` too, so it is inherited rather than introduced, but
it is equally load-bearing: it alone is enough to fail the job.

**Consequence.** TEST-1162's browser clause is the one gate that proves an
interrupted pre-payload link is visible after restart and abandonable by the
person. I ran it by hand and it passes —
`node --test tests/spec-007-wallet-pairing.mjs` reports 5 tests, 0 failures,
including `TEST-1162 interrupted pre-payload links are visible and locally
abandonable`. But no CI run of this candidate can reach it, so the parent's
"continuous browser gate" obligation is unmet, and a future regression in that
suite would go unnoticed for exactly the same reason this one has since
`0220cec`.

**Measurable correction.** Update `tests/screens.mjs` for the credential/v2
front-end — teach the stub bridge `cbcl_v2_recognise`/`cbcl_v2_relay_decide`
and drive screen 31 through the presence-code entry the new flow requires —
and widen the invoke-surface regex to every module that contributes to
`generate_handler!` (or derive the prefix set from the block instead of hard-
coding two). Then require `npm run screens` to exit 0 from a clean exact-pin
closure, and require the three browser steps to run in a job whose earlier
steps pass.

### L-1 — a mid-chain post-approval failure still leaves an orphaned issuer DID publication with no compensating retraction

**Severity: LOW (accepted residual, unchanged in kind from 0.5.8 L-1 through
0.5.12 L-1).** Owner: Selfsame.
Affected requirement: [[SPEC-007-cbcl-pairing-cutover#REQ-812]] ("Compensation
SHALL remove only effects created by this ceremony").

**Code evidence.** `src-tauri/src/cbcl_v2_commands.rs:569-578` publishes the
issuer identity and then verifies the resolver closure. Every later failure —
resolver refusal, grant construction, payload construction, payload checkpoint —
drops `PrePayloadPendingTransaction`, which best-effort removes the local
pending slot only (`cbcl_v2_completion.rs:909-915` → `:1697-1736`). Nothing
retracts or deactivates the already-published issuer DID document.

**Failure sequence.** The person approves finally; the wallet creates the
issuer, publishes its DID document, and the resolver acknowledges. The next
step — `resolve_closure`, grant construction, payload construction, or the
payload checkpoint — fails (network, custody, or store). The transaction drops,
`remove_pre_payload_attempt` deletes the local pending slot, and the command
returns the original protocol error. The published DID document remains
resolvable with no local record referring to it and no ceremony that will ever
consume it.

**Consequence and operational treatment (explicit).** The orphan is a
self-signed DID document naming a key the person's own root derived. It is
non-authoritative: no WebFinger binding exists for it (the hub provisions that
only on grant consumption), `verify_reload` requires the reciprocal binding
before granting capability, and no local slot survives to reference it. It
cannot be presented as an accepted credential, and a fresh ceremony for the
same application derives the same DID from the same hub-minted scope, so the
orphan does not consume a name. The residual cost is resolver-side storage and
a discoverable-but-unusable publication.

**Measurable correction (deferred, not gating).** Either publish the issuer
document only after the grant is durable, or record a retraction intent in the
pending slot before publishing and deactivate the document during compensation.
Until then the existing acceptance in
[[SPEC-008-production-pairing-claimant]] stands. I re-confirmed the effect is
genuinely non-authorizing before retaining the LOW classification.

### L-2 — the CON-036 anonymous recovery limiter is keyed on the TCP peer address, so a reverse-proxied deployment shares one bucket

**Severity: LOW (accepted residual, unchanged from 0.5.9 L-2 through 0.5.12 L-2).**
Owner: cbcl-bus.

**Failure sequence.** Any client reaches the hub through the Fly proxy, so
every anonymous CON-036 recovery request presents the same TCP peer address.
One noisy or hostile client exhausts the shared bucket; a different person whose
relay receipt was lost then receives typed unavailability from the recovery
endpoint until the window resets.

**Consequence and operational treatment (explicit).** The effect is
availability-shaped, not authority-shaped: the endpoint issues no signed
negative on an exhausted or ambiguous read, the wallet repeats
[[SPEC-004-application-scoped-identity#CON-220]] steps 1 through 5 against the
authenticated application origin regardless, and no capability is granted or
withheld by the limiter. The affected person's `PayloadPrepared` slot is
retained, so recovery succeeds on a later attempt.

**Measurable correction (deferred, not gating).** Key the anonymous limiter on
the proxy-supplied client address with an explicit trusted-proxy list, and add
a hub test that two distinct forwarded clients get distinct buckets. This is a
hub deployment change outside this candidate; the acceptance is unchanged.

### L-3 — TEST-1162's runtime injection still does not execute the production final-decision function; hook presence and order are proved by a source-text scan

**Severity: LOW (new in 0.5.13).** Owner: Selfsame.
Affected requirement: [[SPEC-008-production-pairing-claimant#TEST-1162]] ("The
injected steps SHALL use the exact typed hooks in `cbcl_v2_final_decide`. Each
hook SHALL occur once and in causal order.").

**Code evidence.** The 0.5.13 test has two halves. The static half
(`src-tauri/src/cbcl_v2_completion.rs:2468-2488`) `include_str!`s
`cbcl_v2_commands.rs`, slices the region between `async fn
cbcl_v2_final_decide_with_faults` and `pub async fn cbcl_v2_finish`, and asserts
each `PrePayloadBoundary::{Variant}` marker occurs exactly once and in
non-decreasing position. The dynamic half (`:2490-2506`) runs

```rust
let mut transaction = PrePayloadPendingTransaction::begin(pending.clone()).unwrap();
overwrite_test_slot(&CredentialV2LinkSlot::Pending(stored));
let error = transaction.try_step(&mut InjectAt(boundary), boundary, || Ok(())).unwrap_err();
```

— that is, the fault sink is driven against `try_step` directly, with a no-op
closure and a boundary value supplied by the test. `cbcl_v2_final_decide_with_faults`
is never called by any test in the workspace (`grep -rn
"cbcl_v2_final_decide_with_faults"` returns only its definition and the
`cbcl_v2_final_decide` wrapper). Two of the fifteen hooks —
`PrePayloadBoundary::IssuerPublication` and `::ResolverVerification`
(`cbcl_v2_commands.rs:562` and `:570`) — use `before_async`, which fires the
sink and returns without wrapping the `await` that follows.

**Failure sequence.** A later change moves one hook inside an `if` (say, an
early-return fast path that skips the resolver when the closure is cached), or
into a helper the command calls conditionally. `function.matches(&marker).count()`
is still 1, `remainder.split_once(&marker)` still succeeds in order, and every
row of the fifteen-row matrix still passes, because none of them executes the
production function. The gate stays green while a production boundary no longer
executes on every path.

**Consequence (explicit).** The property itself holds today, and I verified it by
reading: compensation is `Drop`-based on `PrePayloadPendingCleanup`, so *every*
early return between `begin` and `commit_payload` compensates whether or not it
passed through a hook, and the production body is straight-line with no branch,
loop, or closure that could reorder execution relative to source. The two
`before_async` sites inject at the causally correct point — before the operation
— which is exactly what TEST-1162's "inject one terminal error … before each
possible successful `PayloadPrepared` replacement" asks for. What the scan
cannot see is a future edit that moves a hook into a conditional, a loop, or
dead code: the marker would still occur once, in order, and the test would stay
green while runtime behaviour changed.

**Operational treatment.** Either drive `cbcl_v2_final_decide_with_faults` once
per boundary behind a test double for `AppSession`/socket/custody, or add a
recording sink (`Vec<PrePayloadBoundary>`) asserted against the exact fifteen-
element sequence from one production run. Until then the source scan is a
genuine, mutation-killed guard — I confirmed it refuses hook removal, hook
rename, and adjacent-hook reordering — and the arm/disarm placement mutants that
motivated 0.5.12 M-1 are killed by real store-failure injection through
`begin` and `commit_payload`, not by labels.

## Dispositions for all twenty-three required checks

| # | Check | Disposition |
|---|---|---|
| 1 | Live CON-220 application authentication and exact `(applicationId, relayOrigin)` consent precede relay socket creation; a relay trusted for one application never authorizes another | **PASS.** `cbcl_v2_recognise` (`src-tauri/src/cbcl_v2_commands.rs:152-175`) decodes the carrier, binds the separate PAIR1 presence code to it, and fetches the named application over authenticated HTTPS (`cbcl_v2_claimant.rs:133-154`) before anything else. `recognise_profile_candidate` (`:156-180`) requires the fetched profile's `application_id` to equal the carrier's application context and requires **exactly one** `cbclPairingRelays` descriptor whose `relay_origin` equals the carrier relay, then computes exact-pair state. The doc comment's claim is true in code: no socket is opened. `cbcl_transport::connect_wss` is reached only from `cbcl_v2_relay_decide` (`:180-218`) after `authorise_claimant_relay` consumes the single-use `RelaySocketCapability`. The policy key is the full tuple in every operation (`cbcl_v2_policy.rs:75-123`) and `exact_pair_never_authorises_another_application_on_the_same_relay` passes. |
| 2 | The claimant binds the locally authenticated profile digest into the CPace public context and checks the peer-authenticated digest before policy seal, exact-intent display, or identity work | **PASS.** `prepare_claimant` (`cbcl_v2_claimant.rs:205-231`) passes `profile_digest: *profile.digest()` — the digest of the *locally fetched* profile — into `CredentialV2ClaimantSessionInput`. cbcl-pairing's `CredentialV2Context::derive` (`src/credential_v2/context.rs:22-79`) places that digest into the channel identifier, both roles' associated data, and the public context, so a divergent digest cannot produce a matching Finished. `bind_finished_profile` (`cbcl_v2_claimant.rs:279-308`) runs only once the core is `is_awaiting_profile_authorisation`, seals a newly approved row there and re-checks an existing row rather than assuming it, and only then installs the verifier. The explicit equality check follows in `verify_offer` (`crates/selfsame-pairing/src/credential_v2.rs:358`): `recognised.profile_digest != *self.profile.digest()` refuses. |
| 3 | The display shows the locally authenticated application identity and cross-checks the wire claim before approval, closing historical F-B | **PASS.** `CredentialV2WalletOfferVerifier::verify_offer` (`credential_v2.rs:346-382`) recognises the signed offer under the independently fetched profile and refuses unless profile digest, descriptor digest, carrier digest, transcript hash, expiry, application id, relay origin, carrier ceremony id, and the expected allocator key all agree. The display authority is built from those verified claims; `AuthenticatedOfferVerdict::verify` (`:404-420`) then requires the peer's claimed `application_id`, `https_origin`, `relay_origin`, and `carrier_ceremony_id` to equal the authority's. `intent_view` (`cbcl_v2_commands.rs:1739-1771`) projects only verifier-produced values — no field is copied from the wire object. F-B remains closed. |
| 4 | No identity key, scope, DID, signature, publication, alias, grant, or durable identity record occurs before its consent boundary, including [[SPEC-007-cbcl-pairing-cutover#REQ-812]] | **PASS.** Traced statement by statement across `cbcl_v2_commands.rs:152-682`. Before preliminary approval only the intent-decision object is built and sent (`:229-256`). Preliminary approval authorises exactly one custody call, `preview_identity` (`:1721-1737`), which derives the home DID from the hub-minted scope inside `Custody::use_hierarchy_root` and performs no signature, publication, resolver call, alias operation, or durable write. Before final approval nothing signs, publishes, resolves, issues, persists, or stages. Final approval writes one sealed non-authorising checkpoint through `PrePayloadPendingTransaction::begin` (`:456`) and only then follows the declared causal order issuer → publish → resolve-and-verify-closure → grant → payload, with a durable phase replacement between stages. The exact-pair policy row is written in `bind_finished_profile`, which is a consent record, not an identity effect. |
| 5 | Fresh account scope is hub-minted, exclusive, and ceremony-bound; used only after consent, with no prior enrolment record | **PASS.** The scope is `recognised.claims.account_provenance().account_scope_id()` — a field of the hub-signed offer core, which also carries `carrierCeremonyId` and is refused unless that ceremony equals the carrier's (`credential_v2.rs:365`). The wallet mints nothing. It is consumed twice, both times inside `Custody::use_hierarchy_root`: at `preview_identity` after preliminary approval, and at `cbcl_v2_commands.rs:392-407` after final approval, where the re-derived DID and fingerprint must equal the displayed preview or the command returns `PairingPreviewChanged`. No enrolment statement is constructed on this path. |
| 6 | Relay operatorship ambiguity is not represented as authentication; the actual property is per-application exact-pair consent plus cryptographic application binding | **PASS.** `RelayConsentView` exposes only `application_id`, `relay_origin`, and `requires_approval` (`cbcl_v2_claimant.rs:21-27`); nothing in the pre-socket surface asserts who operates the relay. The binding that does carry weight is cryptographic and per-application: the profile digest and application context inside the CPace channel identifier and public context, plus the single matching `cbclPairingRelays` descriptor. F-E's honest residual is unchanged and is not restated as an authentication claim anywhere in the candidate. |
| 7 | Every credential/v2 body is closed, canonical, bounded, versioned, context-separated, and recognised before action on both sides | **PASS.** `crates/selfsame-pairing/src/credential_v2/bodies.rs` builds the eleven Selfsame logical bodies through one `CredentialV2BodyAuthority`/`CredentialV2BodyVerifier` pair; every constructor takes a predecessor object and emits deterministic CBOR under a distinct kind, and the verifier recognises before the endpoint advances. Bounds are enforced at decode (`decode_bounded`, `MAX_PROFILE_OCTETS`, cbcl-pairing's `MAX_INNER_BYTES`/`MAX_FRAME_BYTES`). `the_slot_is_a_canonical_exclusive_tagged_union` and the cbcl-pairing recognition suite (all `--all-features` targets, 29 binaries) pass. |
| 8 | Allocator, relay, claimant, reverse issuance, final status, receipt, and installation form one end-to-end cbcl-pairing protocol; no parallel enrolment transport substitutes for it | **PASS.** `crates/selfsame-pairing/tests/shell_cutover.rs` (TEST-801) asserts every ordinary shell starts the CBCL endpoint with no protocol option and no fallback selector, and that the Tauri shell keeps endpoint state out of the webview; it passes. The credential/v2 path never constructs an enrolment statement: `build_enrollment_json` exists only on the browser vendor surface, and the wallet's `cbcl_v2_*` commands reach the hub only through the relay socket and the two authenticated CON-220/CON-036 HTTPS calls the parents name. The `enrollment_keys` references in `cbcl_v2_commands.rs` and `cbcl_v2_completion.rs` are the profile's signature key set, not a transport. The wallet does still register `app_grant::cbcl_enrol_{start,prepare,confirm}` (`src-tauri/src/lib.rs:115-117`), but that module is SPEC-004 CON-219's separate device-grant path; it contains no credential/v2 reference and is not reachable from the credential/v2 ceremony. |
| 9 | Browser checkpoints and recovery reconcile every restart phase without duplicating allocation, identity, grant, or acceptance effects | **PASS.** cbcl-pairing `CredentialV2AllocatorSession::reopen` (`src/credential_v2/allocator.rs:410-430`) rebuilds relay membership from the sealed checkpoint and resends only a genuinely cached outbound frame; `credential_v2_allocator_session.rs` walks every phase including the acknowledged-`FinishedSent` case (see check 11). On the browser side `CredentialV2BrowserAllocatorSession::restore` and `restore_offer_context` re-validate carrier, ceremony, offer core, digests, request id, intent nonce, expiry, and authority response before exposing any restored fact. The wallet's `prepare_received_receipt` (`cbcl_v2_commands.rs:1500-1571`) drains cached frames idempotently and replaces the durable record per acknowledgement. |
| 10 | Wallet pending state, endpoint checkpoints, deadlines, recovery, and installation CAS cannot widen authority after failure or wrong order | **PASS.** `persist_pending` occupies an empty slot only, is idempotent for the exact retry, and refuses any other pending or installed value (`cbcl_v2_completion.rs:1396-1415`). `replace_pending`/`replace_installed` are exact compare-and-set with an idempotent re-application arm and an immutable-field projection check (`:1588-1617`). Both offer deadlines are re-checked (`:368-370` and inside `PayloadCheckpointPreparation`). `pending_application_ids` exposes a slot as *recoverable* only in `PayloadPrepared` and only after the relay window closes, while `pending_links` exposes pre-payload phases as *abandonable* — the separation is explicit and tested. |
| 11 | Acknowledged `FinishedSent` allocator restoration emits `Open` without a duplicate `Finished` | **PASS, mutation-killed.** cbcl-pairing `bootstrap.rs:131-141`: `cached_bootstrap_sequence` now returns `Ok(None)` when `!awaiting_ack` instead of `Err(Phase)`, and `allocator.rs:415-423` maps that to no cached frame. `assert_restored_reopens_acknowledged_bootstrap_frame` requires the restored session to emit `Bind` then exactly `[Open { mailbox_id, membership_token }]`. Reverting the predicate to `Err(CredentialV2Error::Phase)` fails the test (mutation 17). |
| 12 | [[SPEC-008-production-pairing-claimant#CON-990]] arms before the initial pending persistence; its typed transaction owns that persistence and every later transition; it disarms only after durable `PayloadPrepared` | **PASS, mutation-killed.** `PrePayloadPendingTransaction::begin` (`cbcl_v2_completion.rs:852-859`) constructs `PrePayloadPendingCleanup` *before* calling `persist_pending`. `PrePayloadPendingCleanup` and `disarm_after_payload` are private to the module; the only disarm path is `commit_payload` (`:895-906`), which performs the durable `PayloadPrepared` replacement first and disarms last. `cbcl_v2_commands.rs` holds no `PrePayloadPendingCleanup` and calls no `persist_pending` on this path. Mutations 1, 2, and 5 are all killed. |
| 13 | Each of the fifteen exact `PrePayloadBoundary` values wraps the real production operation it names; no operation duplicated, skipped, or represented only by a synthetic phase label | **PASS with observation (see L-3).** Thirteen of fifteen wrap the named operation in the injected closure (`FinalApprovalRelease`, `AcknowledgementRead`, `AcknowledgementRecognition`, `AcknowledgementCheckpointCommit`, `PlanConstruction`, `IssuerCustody`, `GrantConstruction`, `PayloadConstruction`, `PayloadCheckpointPreparation`, and the four `replace_at` stage replacements, each of which performs the real `replace_pending`). `IssuerPublication` and `ResolverVerification` use `before_async`, which fires the sink at the causally correct point immediately before the real `await` rather than wrapping it; the operations themselves are present and their failures still compensate through `Drop`. No boundary is duplicated: the in-test source scan asserts exactly one occurrence of each marker inside the production function, and I confirmed by reading that the region between the two markers contains no other function. |
| 14 | Commit-then-error at the initial persistence and each replacement still compensates the exact attempt; a committed `PayloadPrepared` survives | **PASS, mutation-killed.** The in-memory keyring double injects `SetFailure::BeforeCommit` and `SetFailure::AfterCommit`. `begin` with a post-commit failure returns `PairingCheckpointUnavailable` and leaves no pending link. `commit_payload` with a pre-commit failure removes the `Provisioned` slot and preserves the storage error; with a post-commit failure it preserves the committed `PayloadPrepared` value, because `remove_pre_payload_attempt` (`:1727-1729`) returns `Ok(())` for a payload-prepared current value rather than deleting it. Mutations 5 and 6 are killed. |
| 15 | Compensation refuses another attempt, root generation, installed record, and sibling application, and never deletes their state | **PASS, mutation-killed.** `remove_pre_payload_attempt` (`:1697-1736`) reads only `slot_name(&anchor.application_id)` — no sibling is reachable — refuses any `Installed` slot with `PairingCheckpointRefused`, and compares twelve immutable attempt fields including `root_generation` before deleting. Mutations 7 (both the root-generation-only and the whole-comparison variants) and 8 are killed. |
| 16 | Confirmed unlink compares the current slot and the exact-pair state sampled at selection; absence sampled at selection remains removable | **PASS, mutation-killed.** `load_local_link` (`:1541-1560`) now samples the slot *and* `cbcl_v2_policy::state(application_id, relay_origin)` together. `unlink_local` (`:1620-1662`) refuses on either mismatch, removes policy only when the sampled state was `TrustedPair`, and restores it best-effort only if it removed it. A slot selected while its policy was already absent is deleted, which is the permanent-stranding escape 0.5.12 M-2 required. Mutations 9, 10, and 11 are killed. |
| 17 | TEST-1162 injects failure through every real production hook and observes the actual causal order; the CI command proves exactly one isolated test ran | **PARTIAL — second clause PASS, first clause see L-3.** The CI clause is closed and mutation-killed: `.forgejo/workflows/ci.yml:170-178` now tees to `/tmp/test-1162.log` and asserts `grep -Fq "test result: ok. 1 passed; 0 failed; 0 ignored"`. Drifting the selector (renaming the test) makes `cargo test` exit 0 with `0 passed; 0 failed; 0 ignored; 67 filtered out` and the `grep` fails the step (mutation 13, killed). The injection clause is discharged by a static source-order scan plus real store-failure injection rather than by driving the production function; see L-3. **Separately, M-1 means this CI step cannot be reached at all on this candidate, and M-3 means the same for TEST-1162's browser clause.** |
| 18 | Pending links are visible with bounded public summaries; abandonment and a fresh same-application retry work after restart and partial unlink failure | **PASS.** `PendingCredentialV2LinkSummary` carries only `application_id`, `relay_origin`, and a `&'static str` phase (`cbcl_v2_completion.rs:319-323`, `:539-545`) — no checkpoint, token, grant, scope, or digest. `pending_links` enumerates every recognised pending phase and prunes stale index rows. TEST-1162's tail exercises restart-visible rows, confirmed abandonment, refusal of a stale slot selection, refusal of a stale policy selection, and the absent-policy retry, then re-persists the same application to prove the slot was released. Executed end to end in the browser as well: `node --test tests/spec-007-wallet-pairing.mjs` reports 5 passed, 0 failed, including `TEST-1162 interrupted pre-payload links are visible and locally abandonable`, which renders the interrupted row, opens it, passes an axe audit, and confirms the unlink. **That suite is unreachable on CI — see M-3.** |
| 19 | Installed reload reauthenticates reciprocal authority before exposing capability; rotation, handle change, oversized input, unavailability, and revocation remain distinct and fail closed | **PASS.** `cbcl_v2_reload_verify` (`cbcl_v2_commands.rs:791-1006`) requires the caller to name the account handle, refetches the profile, re-resolves the closure, re-verifies WebFinger, and re-checks the revocation set before `verify_reload` decides. `reload_view` (`:1076-1103`) maps the closed outcome set with `capability: true` only for `Usable` and `ProfileRefresh`; `Unavailable`, `Revoked`, `HandleChanged`, `HubDeleted`, `AuthorityRotation`, `IssuerRotation`, and `FreshPairingRequired` all grant nothing and retain the record. `classify_webfinger_failure` keeps an oversized bounded body in `unavailable` rather than `revoked`, and there is no wildcard arm. |
| 20 | Exactly one `pub const PROFILE_VERSION` exists in the workspace, owned by `profile.rs`, re-exported at the crate root, and source-gated | **PASS, mutation-killed.** `crates/selfsame-app-identity/src/lib.rs:109` is now `pub use profile::PROFILE_VERSION;`. `crates/selfsame-app-identity/tests/profile_version_source.rs` recursively scans every `.rs` file under the workspace root (excluding `target`), requires exactly one `pub const PROFILE_VERSION` definition, requires it to live in `selfsame-app-identity/src/profile.rs`, and asserts the crate-root export equals it. Adding a second definition in a sibling crate file or in `src-tauri` fails the test (mutation 12, both variants). |
| 21 | Exact pins form one buildable closure across wallet, hub, CI, Android, tests, and browser provenance; the strict witness rejects every tracked-dirty or wrong-HEAD sibling despite the development override | **PASS, mutation-killed.** `cbcl-pairing.sha`, `cbcl-rs.sha`, and `did-crdt.sha` name the candidate revisions; cbcl-bus's four pin files, `Dockerfile` `ARG SELFSAME_SHA`, and the browser provenance record name the same closure; `cargo test --workspace --locked` builds and passes from the clean closure. With `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` set, `crates/selfsame-pairing/tests/dependency.rs` (TEST-701/TEST-1164) still refuses all six drift cases I injected — tracked-dirty cbcl-pairing, cbcl-rs, and did-crdt, and wrong-HEAD cbcl-pairing, cbcl-rs, and did-crdt (including did-crdt at service HEAD `94db956`) — and accepts only the restored exact-pin closure. |
| 22 | Hub Linux CI rebuilds the four browser artifacts byte-identically from the exact closure and cannot report success after zero rebuilds or an ignored failure | **PARTIAL — rebuild PASS, "zero rebuilds" see M-2.** I executed `scripts/check-selfsame-wasm-rebuild.sh` inside a real `linux/amd64` container (host `x86_64-unknown-linux-gnu`, rustc 1.96.0, wasm-bindgen 0.2.126 installed from source with `--locked`) against clean detached checkouts at all five revisions. It reported "Selfsame WASM rebuild matches the exact vendored package", and the served `selfsame_web_device_bg.wasm` digests to `23e14bf658321b49f759e43d002797e2f3cb3184fbe460c99b0b8f580ab5e781`. A second fresh rebuild reproduced it. The ignored-failure case (`continue-on-error: true`) is killed; the zero-rebuild case via `if: false` is not — M-2. |
| 23 | Production allocation remains closed; the review cannot authorize release, push, or deployment | **PASS, and affirmed.** [[SPEC-008-production-pairing-claimant#CON-985]] and [[SPEC-007-cbcl-pairing-cutover#REQ-809]] hold production allocation closed; [[SPEC-053-key-root-identity#GATE-04]] remains open with every box unchecked. Nothing in the candidate opens an allocation path, and nothing in this review authorizes release, push, production allocation, or deployment. |

## Dispositions for all seventeen required mutations

Every mutation was applied to a clean detached clone, forced to recompile
(`touch` on the mutated crate sources; the Rust gates rebuilt in every case, as
the logs show), gated, and then reverted with `git checkout -- .`. "Killed"
means the named executable gate returned non-zero **for the mutation's own
reason**, quoted below.

| # | Mutation | Gate | Disposition |
|---|---|---|---|
| 1 | Move transaction arming after the initial `persist_pending` (`begin`) | isolated TEST-1162 | **KILLED.** `panicked at src-tauri/src/cbcl_v2_completion.rs:2520` — the post-commit `begin` failure leaves a pending link. `test result: FAILED. 0 passed; 1 failed`. |
| 2 | Move transaction disarming before the final durable replacement (`commit_payload`) | isolated TEST-1162 | **KILLED.** `panicked at …:2562` — the pre-commit `commit_payload` failure leaves the `Provisioned` slot durable. `0 passed; 1 failed`. |
| 3 | Remove one `PrePayloadBoundary` hook (deleted the `IssuerPublication` `before_async` call) | isolated TEST-1162 | **KILLED.** `panicked at …:2479` — "production boundary hook must occur exactly once". |
| 3b | Rename one hook (`PrePayloadBoundary::PlanConstruction` → `::PlannedStageReplacement` in production) | isolated TEST-1162 | **KILLED.** `panicked at …:2479` — the renamed target now occurs twice and the original zero times. |
| 4 | Reorder two adjacent production hooks (swapped `IssuerPublication` and `ResolverVerification`) | isolated TEST-1162 | **KILLED.** `panicked at …:2485` — "production boundary hook is absent or out of order". |
| 5 | Bypass initial commit-then-error compensation (`std::mem::forget(cleanup)` on the `persist_pending` error path) | isolated TEST-1162 | **KILLED.** `panicked at …:2523` — the committed initial slot survives. |
| 6 | Delete `PayloadPrepared` retention (removed `if current.is_payload_prepared() { return Ok(()); }`) | isolated TEST-1162 | **KILLED.** `panicked at …:2571` — the committed `PayloadPrepared` value is deleted by cleanup. |
| 7a | Remove the root-generation comparison from `remove_pre_payload_attempt` | isolated TEST-1162 | **KILLED.** `panicked at …:2618` — a different root generation's slot is deleted. |
| 7b | Remove the whole immutable same-attempt comparison (kept only `application_id`) | isolated TEST-1162 | **KILLED.** `panicked at …:2592` — another attempt's slot is deleted. |
| 8 | Allow compensation to delete an installed slot | isolated TEST-1162 | **KILLED.** `panicked at …:2633` — the installed record is destroyed by a stale pre-payload guard. (A sibling-application deletion is unreachable by construction: the anchor's own `slot_name` is the only entry touched.) |
| 9 | Remove unlink's slot comparison | isolated TEST-1162 | **KILLED.** `panicked at …:2659` — a stale selection deletes the replacement slot. |
| 10 | Remove unlink's selected-policy-state comparison | isolated TEST-1162 | **KILLED.** `panicked at …:2676` — a slot selected under a since-changed policy is deleted. |
| 11 | Make absent-at-selection unlink refuse (restore the 0.5.12 `!= TrustedPair` guard) | isolated TEST-1162 | **KILLED.** `panicked at …:2688` — the absent-policy retry is refused and the slot is stranded. |
| 12 | Add a second `pub const PROFILE_VERSION` anywhere in the workspace | `cargo test -p selfsame-app-identity --locked --test profile_version_source` | **KILLED, both placements.** Added to `crates/selfsame-app-identity/src/uri.rs` and, separately, to `src-tauri/src/cbcl_v2_policy.rs`: `panicked at crates/selfsame-app-identity/tests/profile_version_source.rs:39` in each case. |
| 13 | Drift the isolated TEST-1162 selector so it runs zero tests (renamed the test function) | the exact CI two-command step | **KILLED.** `cargo test` exits 0 with `test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 67 filtered out`, and the new `grep -Fq "test result: ok. 1 passed; 0 failed; 0 ignored"` exits 1, failing the step. Baseline: `cargo exit=0  grep exit=0`. |
| 14 | Delete, rename, or mark ignored the hub CI WASM rebuild step | `node --test …/selfsame-vendor.test.mjs` | **PARTIAL.** Renamed step → **KILLED** ("CI must contain exactly one pinned Selfsame browser-package rebuild step"). Script invocation replaced with `:` → **KILLED**. `continue-on-error: true` → **KILLED**. Step marked ignored with `if: false` → **SURVIVED (6 pass, 0 fail)**. See M-2. |
| 15 | Mutate each WASM source pin or provenance source revision | `scripts/check-selfsame-wasm-rebuild.sh` on `linux/amd64`, and the vendor suite | **KILLED, all eight.** Script: "Selfsame / cbcl-pairing / cbcl-rs / did-crdt revision does not match <pin>". Vendor suite: each of the four `provenance.*Sha` fields and each of the four pin files refuses with "the browser artifact must name …". |
| 16 | Mutate the recorded host, compiler, wasm-bindgen version, digest, or one served browser byte | rebuild script and vendor suite | **KILLED, all five.** `rustcVersion=1.95.0` → "rustc 1.96.0 does not match provenance 1.95.0". `wasmBindgenVersion=0.2.125` → "wasm-bindgen 0.2.126 does not match provenance 0.2.125". `buildHost=aarch64-apple-darwin` → "host x86_64-unknown-linux-gnu cannot reproduce the aarch64-apple-darwin artifact". `wasmSha256` mutated → vendor suite: "the served WASM bytes must match the reviewed provenance digest". One served byte flipped → vendor suite refuses on the digest ("the served WASM bytes must match the reviewed provenance digest") and the Linux rebuild script refuses on `cmp` ("rebuilt selfsame_web_device_bg.wasm differs from the vendored artifact"); appending one byte to the generated `selfsame_web_device.js` is refused the same way. **Recorded division of labour:** the *host*, *rustc*, and *wasm-bindgen* mutations survive the vendor suite alone (it only regex-shapes those fields and never reads `buildHost`) and are caught **only** by the rebuild script — which is why M-2's skippable step widens the consequence. |
| 17 | Revert the acknowledged-`FinishedSent` allocator predicate | `cargo test --locked --all-features --test credential_v2_allocator_session` (cbcl-pairing) | **KILLED.** Restoring `Err(CredentialV2Error::Phase)` for `!awaiting_ack`: `panicked at tests/credential_v2_allocator_session.rs:152`, `test result: FAILED. 2 passed; 1 failed`. |

### Additional mutations run beyond the required seventeen

| Mutation | Gate | Disposition |
|---|---|---|
| Tracked-dirty cbcl-pairing / cbcl-rs / did-crdt, with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` set | `cargo test -p selfsame-pairing --locked --test dependency` | **KILLED ×3** — "`<label>` pinned checkout has tracked drift". |
| Wrong-HEAD cbcl-pairing / cbcl-rs / did-crdt (did-crdt at service HEAD `94db956`), same override | same | **KILLED ×3** — "`<label>` HEAD differs from its pin". |
| Untracked file in the staged Selfsame tree | `scripts/check-selfsame-wasm-rebuild.sh` | **KILLED** — "Selfsame checkout is not clean" (`--untracked-files=all`). |

## Dispositions for every 0.5.12 and earlier finding, F-A through F-E, and REQ-812

No disposition below is inherited. Each was re-derived from the candidate
sources and, where an executable gate exists, re-run.

### 0.5.12 findings

| Finding | 0.5.12 severity | This review |
|---|---|---|
| **M-1** — TEST-1162's fifteen named boundaries are five synthetic guard drops; the production arm/disarm placement survives mutation | MEDIUM | **CLOSED for the blocking part; residual recorded as L-3.** The exact mutant the prior review reported as surviving — moving `disarm_after_payload` ahead of the durable `PayloadPrepared` write — is now impossible to express in the command (`PrePayloadPendingCleanup` and its disarm are private to `cbcl_v2_completion`), and the equivalent mutation inside `commit_payload` is killed by TEST-1162 (mutation 2). Arm-after-persist is killed (mutation 1). The fifteen rows are no longer five duplicated bodies: each carries its own typed boundary, the source scan requires exactly one occurrence in causal order, and hook removal, rename, and reordering are all killed (mutations 3, 3b, 4). The remaining gap — the test still does not drive `cbcl_v2_final_decide_with_faults` — is recorded as L-3 with its consequence and treatment. |
| **M-2** — the still-trusted-policy refusal makes a transient store failure permanently strand the application slot | MEDIUM | **CLOSED, code-confirmed and mutation-killed.** `load_local_link` samples the policy state with the slot; `unlink_local` compares against the sample rather than against `TrustedPair`, removes policy only when the sample was `TrustedPair`, and restores it best-effort only if it removed it. The exact stranding sequence — prior unlink removed policy, failed to delete the slot, failed to restore — is now recoverable: a fresh selection sees `NewPair` and deletes the slot. Restoring the 0.5.12 predicate fails the test (mutation 11). Refusal on a changed slot and on a changed policy are both still required (mutations 9, 10). |
| **M-3** — the coordinated hub's continuous gate cannot execute: the browser WASM provenance check hard-fails on CI's host triple and blocks every later step | MEDIUM | **CLOSED, independently reproduced.** The package is re-vendored from `x86_64-unknown-linux-gnu`. I executed `scripts/check-selfsame-wasm-rebuild.sh` in a real `linux/amd64` container against clean checkouts at all five revisions: "Selfsame WASM rebuild matches the exact vendored package", digest `23e14bf6…5e781`, and a second fresh rebuild reproduced it. The host check now passes on CI's platform instead of hard-failing. **A different, narrower gate defect at the same site is opened as M-2 of this review** (`if: false` skips the step). |
| **M-4** — REQ-1005's sole-definition obligation for `PROFILE_VERSION` is unimplemented and the consumers are split | MEDIUM | **CLOSED, mutation-killed.** One definition (`profile.rs`), one public re-export (`lib.rs:109`), and an executable recursive workspace scan that also asserts equality through the export. Adding a second definition in either crate fails the test (mutation 12). The named consumers now resolve to the same item through ordinary imports. |
| **L-1** — orphaned issuer DID publication | LOW residual | **RETAINED as L-1** (see above). Re-confirmed non-authorizing. |
| **L-2** — anonymous recovery limiter keyed on the TCP peer | LOW residual | **RETAINED as L-2** (see above). |
| **L-3** — the CI TEST-1162 step reports success when it selects no test | LOW | **CLOSED, mutation-killed.** `.forgejo/workflows/ci.yml:177` now asserts the exact `1 passed; 0 failed; 0 ignored` summary; renaming the test fails the step (mutation 13). |
| **L-4** — the credential/v1 conformance registry cites SPEC-008 CON-903 instead of SPEC-007 CON-806 | LOW | **CLOSED.** `src-tauri/src/cbcl_registry.rs:1` now reads "SPEC-007 `CON-806` — the compiled approved-conformance registry", and CON-806 is the clause that actually defines `conformanceEvidenceDigest` and its release semantics through CON-805. |

### 0.5.11 and earlier named findings

| Finding | Origin | This review |
|---|---|---|
| **0.5.11 M-1** — CON-990's compensation and confirmed-unlink guards have no executable verification; four predicates survive mutation; the only TEST-1162 test runs in no automated gate | 0.5.11 | **CLOSED.** All four predicates are now mutation-killed (payload retention 6, attempt/root identity 7a/7b, installed-slot protection 8, exact-slot compare-and-delete 9) and the test runs in a dedicated CI step with a result assertion. **Caveat: M-1 of this review means that step is currently unreachable on CI.** |
| **0.5.10 H-1** — a restored browser allocator is driven to `Terminal` at the acknowledged `FinishedSent` checkpoint | 0.5.10 | **CLOSED, mutation-killed.** See check 11 and mutation 17. |
| **0.5.10 H-2** — a terminal refusal after final approval leaves an unremovable pending slot | 0.5.10 | **CLOSED.** Compensation is `Drop`-armed before the first store write and covers every early return; every pre-payload phase is listed by `pending_links` and removable by confirmed unlink; and TEST-1162 proves a fresh same-application ceremony occupies the released slot. |
| **0.5.10 L-3** — `test_701` verifies only the cbcl-pairing sibling, so cbcl-rs and did-crdt drift survives with the override set | 0.5.10 | **CLOSED, mutation-killed.** The witness now covers all three siblings; I refused six independent drift cases with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` set. |
| **0.5.10 L-4** — an oversized WebFinger response is reported as `revoked` rather than unavailable | 0.5.10 | **CLOSED.** `classify_webfinger_failure` (`cbcl_v2_commands.rs:1803-…`) maps oversized bodies to unavailability; `reload_view` keeps `revoked`, `unavailable`, `hub-deleted`, and `account-device-handle-change-refused` distinct, and there is no wildcard arm. TEST-1163's mutation (oversized → revoked) is required by the parent and the mapping is explicit rather than defaulted. |
| **0.5.9 H-1** — a restored browser allocator cannot reopen the relay at either cached-frame bootstrap checkpoint | 0.5.9 | **CLOSED.** cbcl-pairing `3d01330` plus the candidate `62ef4a9`; `assert_restored_reopens_cached_bootstrap_frame` and `assert_restored_reopens_acknowledged_bootstrap_frame` both pass. |
| **0.5.9 H-2** — REQ-1006's installed-state lifecycle is unimplemented | 0.5.9 | **CLOSED.** `cbcl_v2_reload_verify`, `cbcl_v2_installed_links`, `cbcl_v2_unlink`, handle-change refusal, rotation prompts, and `replace_installed` CAS are all present and exercised. |
| **0.5.9 M-1/M-2** — hub signs `not-finalized` without authenticating the recovery token; the accept transaction has no expiry gate | 0.5.9 | **CLOSED in the coordinated parent and hub.** [[SPEC-053-key-root-identity#REQ-083]]'s increment does not reopen them; CON-989's commitment is computed by the hub in transient memory from the raw token and the token is zeroized. Not re-exploited here: the hub runtime gates were not executed (see "Gates not run"). |
| **0.5.9 M-3** — the coordinated set carries disagreeing cbcl-rs and did-crdt pins and Selfsame's CI cannot build the candidate | 0.5.9 | **CLOSED.** All five repositories, the Dockerfile arg, and the browser provenance name one closure; `cargo test --workspace --locked` builds and passes from it; mutating any pin refuses (mutation 15). |
| **0.5.8 H-1/H-2** — no `selfsamepairstatus`/`selfsamepairabort`; CON-989 signed-status HTTPS recovery unimplemented | 0.5.8 | **CLOSED in design and wallet code.** `recover_claimant_completion` (`cbcl_v2_commands.rs:1162-1348`) implements the CON-989 POST against the retained application origin and repeats CON-220 steps 1–5 before trusting the response. Hub-side execution not re-run here. |
| **0.5.8 M-1** — cbcl-pairing's dependency pin is unenforced and inconsistent | 0.5.8 | **CLOSED, mutation-killed.** `build.rs` plus `tests/dependency.rs`; see check 21. |
| **0.5.7 N-1, L-1…L-7** | 0.5.7 (specification-level) | **CLOSED or superseded by the 0.5.8→0.5.13 reissues.** These were wording, bound, and citation defects in the parent text. The 0.5.13 parent states the socket-generation encoding and bounds, the frame ceiling, one member-naming convention, and one citation form directly; I found no current-value conflict while reading CON-985 through CON-990, REQ-1005, REQ-1006, TEST-1162 through TEST-1166. Not blocking. |
| **REQ-909 F-1** — the cbcl-pairing invitation's `application` member is a constant, so live-fetch had no wire source | 2026-08-22 | **CLOSED by the coordinated wire amendment.** The credential/v2 carrier carries `application-context`, validated by `recognise_application_id` (cbcl-pairing `src/credential_v2/display.rs:718-734`) as a canonical HTTPS origin **with a non-empty, non-traversing path** — the CON-201 grammar, not `anuna.io/credential/v1`. `recognise_claimant_invitation` parses it with `ApplicationId::parse` and requires the fetched profile's `application_id` to equal it. |
| **REQ-909 F-2** — pre-consent closure publication and alias provisioning violate REQ-812 | 2026-08-22 | **CLOSED.** Nothing publishes, resolves, or provisions before final approval; a declining person reaches no identity effect at all. See check 4. |
| **REQ-909 F-3/F-4** — the registry digest certifies the operator, not the application; repeated first contacts mint unbounded scopes | 2026-08-22 | **CLOSED in mechanism.** Application-level anchoring is now the live CON-220 profile fetch plus the profile digest bound into the CPace context, not the relay registry. Scope is hub-minted and consumed only after two person decisions, and the wallet holds one `pending_cbcl_v2` at a time (`PairingAlreadyActive`), with `MAX_LINKS`-bounded slots. |
| **REQ-909 F-5/F-6** — step order contradicted ADR-914; the held-profile invariant was relaxed without reconciliation | 2026-08-22 | **CLOSED.** The 0.5.13 parent states one direct order; the withdrawn amendment is not resurrected. |

### F-A through F-E

| Hard stop | This review |
|---|---|
| **F-A** — exact application+relay, never relay-only | **CLOSED, code-confirmed.** `cbcl_v2_policy.rs` read in full: the row key is the full `(applicationId, relayOrigin)` tuple in `state`, `insert`, and `remove`; rows are canonical, strictly ordered, re-encode-checked, and `MAX_ROWS`-bounded; malformed state refuses rather than becoming a fresh TOFU prompt. The row is written only in `bind_finished_profile`, after both Finished values. `exact_pair_never_authorises_another_application_on_the_same_relay` passes. |
| **F-B** — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed.** See check 3. `intent_view` reads only `CredentialV2Display` accessors; `AuthenticatedOfferVerdict::verify` refuses any peer claim that differs from the locally authenticated authority. |
| **F-C** — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed.** `recognise_claimant_invitation` → `authorise_claimant_relay` (single-use `RelaySocketCapability`) → `prepare_claimant` → `preview_identity` → `cbcl_v2_final_decide_with_faults` is the only construction path. `recover_claimant_completion` is terminal: it cannot construct, derive, sign, publish, or resend. |
| **F-D** — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed.** See check 5. The scope is a signed-offer field verified under the independently fetched profile, and the profile digest that gates the offer is inside the CPace channel identifier, both associated-data values, and the public context. |
| **F-E** — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged).** Nothing in the candidate represents operatorship as authentication; the containment is per-application exact-pair consent plus the cryptographic application binding. See check 6. |

### [[SPEC-007-cbcl-pairing-cutover#REQ-812]]

**CLOSED, code-confirmed, re-derived.** Traced clause by clause against
`src-tauri/src/cbcl_v2_commands.rs:152-682`:

- *"Before credential/v2 preliminary approval, the endpoint SHALL produce no
  application identity effect."* The pre-approval path builds and sends only
  the intent-decision object; the passcode is not even taken unless `approve`.
- *"Preliminary approval authorizes one zeroizing derivation, pure DID and
  fingerprint computation, and the stated authenticated preview disclosure."*
  Exactly one `Custody::use_hierarchy_root` call in `preview_identity`, which
  returns a DID string and nothing else; the fingerprint is computed from it;
  the preparation body carries the preview inside the protected channel.
- *"Before credential/v2 final approval, the endpoint SHALL NOT sign, publish,
  resolve, issue, persist, stage capability, or construct a credential
  payload."* No such call exists before `:456`.
- *"Final approval authorizes only causal work required to construct the
  approved reverse payload."* The post-approval sequence is exactly
  re-derivation and preview equality → checkpoint → release → acknowledgement →
  plan → issuer → publish → resolve/verify → grant → payload → payload
  checkpoint. Nothing else.
- *"Final approval also authorizes one encrypted non-authorizing completion
  checkpoint before the first causal effect."* `PrePayloadPendingTransaction::begin`
  persists exactly one sealed record before `FinalApprovalRelease`.
- *"Compensation SHALL remove only effects created by this ceremony. It SHALL
  NOT change pre-existing identity state or operator withdrawal state."*
  `remove_pre_payload_attempt` touches one application slot, requires twelve
  immutable fields to match, refuses installed records, and never touches the
  root, the policy record, or any sibling. Mutations 7a, 7b, and 8 confirm.
- *"Failure before immutable final acceptance yields no accepted capability."*
  Installation happens only in `cbcl_v2_finish` from the authenticated receipt
  and the hub's immutable final status; `verify_reload` re-authenticates before
  any later capability.

The one acknowledged deviation is L-1: the issuer DID publication is a real
effect that compensation does not retract. It grants no capability, and it is
carried as an accepted LOW residual by the parent.

## Exact test commands and outcomes

### Environment and closure construction

```
$ [ -z "${ANTHROPIC_API_KEY+x}" ] && echo ABSENT        → ABSENT
$ [ -z "${ANTHROPIC_AUTH_TOKEN+x}" ] && echo ABSENT     → ABSENT
$ env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status
    authMethod=claude.ai  apiProvider=firstParty  subscriptionType=max

$ git clone <repo> …/closure/<name>          # selfsame, cbcl-pairing, cbcl-rs, did-crdt, cbcl-bus
$ git -C …/closure/selfsame     checkout --detach ff2f1394813a38ce9d0409726892b6e0c63d00ea
$ git -C …/closure/cbcl-pairing checkout --detach 62ef4a968b46b4836374fcee1d78c410f730a7a7
$ git -C …/closure/cbcl-rs      checkout --detach febc6691e6dd2d5f7116b1a4d84c984b64717564
$ git -C …/closure/did-crdt     checkout --detach 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
$ git -C …/closure/cbcl-bus     checkout --detach 22fcc012ff4d4746f251b497c11495b1b26c0f29
    → every HEAD exact; `git status --porcelain --untracked-files=no` empty for all five
$ rustc --version   → rustc 1.96.0 (ac68faa20 2026-05-25)     (CI installs `stable`)
$ cargo clippy --version → clippy 0.1.96 (ac68faa20c 2026-05-25)
```

### Selfsame

The final Rust runs used `CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0`
to fit the review host's remaining disk. That drops debug info only; it changes
no lint result and no test outcome, and the earlier full-debuginfo run of the
same commands produced identical counts.

| Command | Outcome |
|---|---|
| `cargo test --workspace --locked --no-run` | **exit 0** — the strict `build.rs` pin check accepted the closure with no development override set |
| `cargo test --workspace --locked` | **exit 0** — 74 test binaries, **806 passed, 0 failed, 9 ignored**, 0 warnings. Re-run a second time from a pristine tree after every mutation was reverted, with byte-identical counts. |
| `cargo test -p selfsame --locked --lib -- --ignored --exact cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot` | **exit 0** — `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 66 filtered out` |
| the exact CI step, `… | tee /tmp/test-1162.log …` then `grep -Fq "test result: ok. 1 passed; 0 failed; 0 ignored"` | **cargo exit=0, grep exit=0** |
| `cargo test -p selfsame-app-identity --locked --test profile_version_source` | **exit 0** — `1 passed` (`req_1005_profile_version_has_one_workspace_definition`) |
| `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1 cargo test -p selfsame-pairing --locked --test dependency` | **exit 0** on the clean closure; **exit 101** on each of six injected drift cases |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | **FAILS (exit 101) — see M-1.** `error: this function has too many arguments (9/7) --> crates/selfsame-web-device/src/lib.rs:568:5`; `error: could not compile 'selfsame-web-device' (lib)` and `(lib test)`. Reproduced twice, the second time from a pristine `git reset --hard` tree at `ff2f139` with `git status --porcelain --untracked-files=no` empty. |
| `cargo clippy -p selfsame-web-device --locked -- -D warnings` | **FAILS** identically — the crate's own library, not `--all-targets` |

### Selfsame browser suites (node v22.12.0, puppeteer 23.11.1 from the tracked lockfile)

| Command | Outcome |
|---|---|
| `npm ci --no-audit --no-fund` | **exit 0** — 103 packages from `package-lock.json` |
| `CI=true npm run e2e:wallet-pairing` | **exit 0** — `# tests 5 # pass 5 # fail 0`, including `TEST-1162 interrupted pre-payload links are visible and locally abandonable`, `TEST-1159 installed links reload, prompt on rotation, and unlink locally`, and `TEST-1161 pending completion retains the authenticated offer profile for restart recovery` |
| `CI=true npm run screens` | **FAILS (exit 1) — see M-3.** Four `31-pairing-wait` assertions and two `invoke-surface` errors. With `0220cec`'s `src/` and `tests/` checked into the same closure the same command reproduces only the two `invoke-surface` errors, isolating the `31-pairing-wait` half as a regression of the credential/v2 front-end. |

### cbcl-pairing

| Command | Outcome |
|---|---|
| `cargo test --locked --all-features --workspace` | **exit 0** — 29 test binaries, every one `test result: ok`, 0 failed |
| `cargo test --locked --all-features --test credential_v2_allocator_session` (mutant 17) | **exit 101** — `panicked at tests/credential_v2_allocator_session.rs:152` |

### cbcl-bus — browser vendor suite (host node v22.12.0)

| Command | Outcome |
|---|---|
| `node --test apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs` | **exit 0** — `# tests 6 # pass 6 # fail 0`, reproducing the generating agent's six-test claim |

### cbcl-bus — Linux `linux/amd64` browser WASM rebuild (TEST-1165 / TEST-120)

Run inside a real `linux/amd64` container (`rust:1.96.0`, Rosetta-backed
emulation on the review host), with `wasm-bindgen-cli 0.2.126` installed **from
source** with `cargo install … --locked` in this review's own volumes — not
reusing any generating-run toolchain artefact. Sources were streamed into a
Docker volume from the clean detached clones, so no host path or dirty tree
participates.

```
$ rustc -vV | grep -E '^(rustc|host)'
rustc 1.96.0 (ac68faa20 2026-05-25)
host: x86_64-unknown-linux-gnu
$ wasm-bindgen --version
wasm-bindgen 0.2.126

$ cd /src/cbcl-bus && bash scripts/check-selfsame-wasm-rebuild.sh
    Finished `release` profile [optimized] target(s) in 2m 05s
Selfsame WASM rebuild matches the exact vendored package
REBUILD-1 exit=0
$ sha256sum apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm
23e14bf658321b49f759e43d002797e2f3cb3184fbe460c99b0b8f580ab5e781

# second fresh rebuild (the script wipes and rebuilds its whole staging root)
$ bash scripts/check-selfsame-wasm-rebuild.sh
Selfsame WASM rebuild matches the exact vendored package
```

Both rebuilds byte-compared all four generated files
(`selfsame_web_device.js`, `.d.ts`, `_bg.wasm`, `_bg.wasm.d.ts`) against the
served package. The digest equals
[[SPEC-053-key-root-identity#REQ-083]] and
[[SPEC-008-production-pairing-claimant#CON-985]].

Vendor mode on a temporary copy, after corrupting all four provenance source
revisions, the host, and the digest:

```
$ bash scripts/check-selfsame-wasm-rebuild.sh --vendor
Selfsame WASM package re-vendored; provenance digest 23e14bf6…0ab5e781
→ cbclPairingSha / cbclRsSha / didCrdtSha / selfsameSha restored from the pin files
→ buildHost recorded as the measured x86_64-unknown-linux-gnu
→ all four generated files byte-identical to the reviewed package
```

## Gates not run, and why

| Gate | Status |
|---|---|
| cbcl-bus `rebar3 compile` / EUnit / purity boundaries (×3) / SPEC-053 mutation harnesses / NIF parser conformance | **Not run.** The review host reached a hard disk-space floor (under 1 GiB free at the tightest point) while holding the Selfsame and Linux-WASM build trees; fetching the hub's Erlang dependency closure and building three OTP applications was not possible alongside them. These gates are unchanged by this candidate — `22fcc01` touches exactly one file, `selfsame-vendor.test.mjs`, and `eaacb08` touches the Dockerfile, one pin file, the rebuild script, and the vendored artifacts. No hub Erlang source changed in the reviewed increment. |
| cbcl-bus browser-core Node suites other than the vendored Selfsame suite (`Run browser-core Node tests (discovered)`), and the three CDP evidence suites | **Not run**, same reason; they require the compiled hub and a headless Chromium. The one suite the increment actually changes — `selfsame-vendor.test.mjs` — was run in full and mutated. |
| Selfsame `npm run e2e:pairing` | **Not run.** It spawns `cargo` to build the pairing demo server (`tests/cbcl-pairing-demo.mjs:40`, `:495`), and the review host had no disk headroom for a third large Rust build tree. `npm run screens` and `npm run e2e:wallet-pairing` **were** run — see the table above and M-3. |
| Android (`.forgejo/workflows/android.yml`) | **Not run** — no Android SDK/NDK on the review host, and the increment does not change the Android job beyond the pin propagation already checked. |
| Live credential/v2 gates against `chat.anuna.io` | **Not run, and deliberately not attempted.** Production allocation is closed by [[SPEC-007-cbcl-pairing-cutover#REQ-809]] and [[SPEC-053-key-root-identity#GATE-04]]; a live ceremony would be an unauthorized production action. |
| Hub-side 0.5.9 M-1/M-2 re-exploitation | **Not re-executed** for the same reason as the hub gates above; dispositioned from the parents and the coordinated increment's scope. |

The Selfsame workspace suite, the isolated TEST-1162 process, the profile-version
source test, the strict dependency witness, the complete cbcl-pairing workspace
with all features, the wallet pairing lifecycle browser suite, the screen-render
suite, the browser vendor suite, and the Linux WASM rebuild all ran
to completion. No gate reported a collection failure, dependency failure, host
mismatch, or empty selector; where a selector *could* have been empty (the
isolated TEST-1162 step) I proved the new assertion catches it.

## Release and deployment authority

**PASS does not authorize release, push, production allocation, or deployment —
and this review returns REJECT, which authorizes even less.** Those actions
remain prohibited by
[[SPEC-008-production-pairing-claimant#CON-985]],
[[SPEC-007-cbcl-pairing-cutover#REQ-809]], and
[[SPEC-053-key-root-identity#GATE-04]], each of which requires a fresh
cross-model Tier-1 PASS **and** separate owner approval. Release and deployment
controls are outside this review's authority. Nothing was pushed, deployed, or
allocated in the course of this review; every mutation was applied to a
throwaway clone and reverted, and the person's working trees, output APKs, and
patch scripts were neither read as evidence nor modified.
