---
id: spec-008-0.4.11-claude-adversarial-review-2026-08-23
title: Tier-1 adversarial review — SPEC-008 0.4.11 coordinated standalone pairing
status: final
tier: 1
kind: review
verdict: REJECT
reviews: "[[spec-008-0.4.11-standalone-tofu-first-contact]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.4-draft and its amended parent SPEC-053-key-root-identity"
severity-counts: "0 critical, 2 high, 3 medium, 4 low"
driver: Claude Opus 5 (claude-opus-5), fresh-context cross-model Circus reviewer, defect-finding mandate
date: 2026-08-23
---

# Tier-1 adversarial review — SPEC-008 0.4.11 coordinated standalone pairing

## Model, driver and authentication context

- **Driver model:** Claude Opus 5 (`claude-opus-5`), Claude Code TUI, Circus
  fresh-context Tier-1 reviewer. Defect-finding mandate only: no drafting, no
  implementation, no gate-closing authority.
- **Authentication:** Claude Max subscription authentication through the Claude
  Code TUI. `ANTHROPIC_API_KEY` was verified **absent** from the review process
  environment (`echo "ANTHROPIC_API_KEY=[${ANTHROPIC_API_KEY:-UNSET}]"` printed
  `UNSET`). No API key, external paid API, or account-billed Anthropic API call
  was used.
- **Working tree:** `/Users/anuna-01/Code/selfsame-spec008-v0411-review-2`
  (worktree of `selfsame`, branch `circus/spec008-v0411-review/2`).
- **Sibling repositories read at their own checkouts:**
  `/Users/anuna-01/Code/cbcl-pairing`, `/Users/anuna-01/Code/cbcl-bus`,
  `/Users/anuna-01/Code/did-crdt`.
- **Method:** every clause naming a module, function, record, index, constant,
  route, member or octet count was located in source, or shown absent, at the
  declared baselines. Prose, comments, disposition tables and prior review
  conclusions were treated as unverified assertions. Every stated size was
  re-derived independently from the declared constants rather than checked
  against the amendment's own sum. Where Mnesia lock semantics are load-bearing
  the OTP 28.5 / mnesia 4.25.3 source was read
  (`/opt/homebrew/Cellar/erlang/28.5/lib/erlang/lib/mnesia-4.25.3/src`), not
  recalled.

### Reviewed commits

| Repository | Candidate commit | Declared implementation baseline | Verified relationship |
|---|---|---|---|
| selfsame | `bbc7a4b05e47ad32cfe2c83e68d2a55da60bdfc7` | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | ancestor; candidate delta touches only `specs/` |
| cbcl-pairing | `f4dbb0c28b35dff5eafff9371bbe0483b1e9ef7c` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | ancestor; delta is 6 files, all under `specs/trajectory/SPEC-001/`; delta from the 0.4.10-reviewed commit `c9c7d5c` is **two frontmatter lines** |
| cbcl-bus | `6e368b0151fabd4753a0975ac966db90a250fb8f` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | ancestor; delta is 5 files, all under `specs/`; adds `spec-053-0.16.4-distinct-v2-authority-ordering.md` plus a 13-line parent pointer bump |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | same | candidate **is** the baseline; the uncommitted `specs/did-crdt-spec.md` and untracked `docs/adr/ADR-007-*` in that tree were not reviewed |

Every candidate is spec-only relative to its declared code baseline, so the
checked-out worktrees hold exactly the code the amendments claim to be written
against. All code citations below are at those baselines.

### Documents read

Selfsame SPEC-008 0.4.0 through 0.4.11 and every intervening adversarial
review; `pairing-completion-runbook.md`;
`req-909-adversarial-review-2026-08-22.md`; `HANDOFF-pairing-integration.md`;
SPEC-004 (CON-201/211/214/219/220/226, REQ-215), SPEC-002 REQ-103, PROTO-004
CON-502/503, SPEC-007 REQ-810/REQ-812/REQ-813/CON-806, SPEC-008 parent (0.2.2),
IMPL-008. cbcl-pairing SPEC-001 0.3.0 and 0.4.0–0.4.4. cbcl-bus
`SPEC-053-key-root-identity`, SPEC-053 0.16.1/0.16.2/0.16.3/0.16.4,
`SPEC-076-pairing-account-grant`, SPEC-075 REQ-007, GATE-00/01/03/04.

---

# Verdict: REJECT

**0 critical, 2 high, 3 medium, 4 low.**

The standalone first-contact architecture remains sound and is not the reason
for this verdict. The two 0.4.10 HIGH findings are genuinely closed, and closed
the right way. `CON-970` clauses 2–5 restore the distinct v2 ApplicationId,
keep `profileVersion = 2` on a *new* document rather than re-ratifying the live
one, and re-point the statement discriminator at `evidenceVersion` — this is
exactly 0.4.10 `H-1`'s Option A. cbcl-bus `REQ-051` replaces "supersedes in
full" with an explicit survivor/replacement ledger that restores `REQ-041`,
`REQ-042`, `REQ-044`, `CON-013`, `CON-014`, `CON-015` and `TEST-048`–`TEST-056`
by name, closing 0.4.10 `H-2`. `NFR-913`'s two load-bearing Mnesia claims are
**correct against OTP source** — I verified both rather than accepting them. The
statement-side arithmetic is not merely plausible, it reproduces **exactly**:
2,304, 187, 250/3,072/86/2 and 3,410 all fall out of the declared constants when
recomputed from scratch.

The verdict turns on two design defects that the repairs create or leave
standing, and on three bounded but release-blocking gaps:

1. **`H-1`: `REQ-982`'s "29 members are the complete closed core" contradicts
   the adopted, unreplaced 0.4.8 `CON-960`, which gives `OfferCoreV2` one
   nested `accountTransition` object over a closed `NoTransition | PathAToB`
   sum.** The offer core is the RFC-8785 preimage of `offerCoreDigest`, the
   single value hub, browser and wallet each recompute independently; it now has
   two incompatible member sets. `accountDid` appears in no adopted core
   definition, and a flat 29-member core in which "no listed member is optional"
   cannot represent `NoTransition` at all — which `CON-960` says is the valid
   shape for a session already on open Path-B standing. The entire
   release-blocking size derivation is computed over the shape no adopted clause
   defines.

2. **`H-2`: `REQ-983`/cbcl-bus `REQ-054` assert "neither ordering locks out the
   enrolled identity", and the code says the opposite.** `cbcl-core-enrol`
   refuses on `pairing-unratified` *before* it reaches any binding branch, so a
   key carrying an unratified pairaccount row can never reach
   `enroll-or-check/2` — and `cbcl-chat-pairaccount` exports no delete or
   ratification path. The claim-first ordering is a permanent lockout, not a
   recoverable race, and `TEST-1110`/cbcl-bus `TEST-067` mandate an outcome that
   cannot be produced without reopening SPEC-076 `REQ-004`.

Both are bounded; neither is an architectural objection to standalone first
contact.

---

# Findings

## H-1 (HIGH) — `REQ-982`'s flat 29-member `OfferCoreV2` contradicts adopted 0.4.8 `CON-960`'s single nested `accountTransition` object, introduces an unsourced `accountDid`, makes `NoTransition` unrepresentable, and derives the release-blocking bound over a member set the effective text does not define

**Owner: Selfsame** (`REQ-982`, `CON-970`), coordinated into **cbcl-bus**
(`NFR-011`, `CON-020`, `TEST-066`) and **cbcl-pairing** (`CON-021`'s
`AccountTransition`).

### What 0.4.11 says

`REQ-982`:

> The maximum `OfferCoreV2` has the 22 members from
> [[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]]. It adds
> `accountDid`, `legacyHandle`, `migrationRooms`, three migration digests, and
> `snapshotNonce`. **Those 29 members are the complete closed core.**

cbcl-bus `NFR-011` repeats it: "The closed core has 29 members. Its non-room
members total at most 4,067 canonical octets."

### What the adopted set says

0.4.11 `adopts:` is "All 0.4.4 through 0.4.10 clauses except the exact
replacements and corrections named by this amendment." `CON-970`'s ten-item
ledger names `REQ-977`, `CON-968` clause 1, `REQ-979`, `NFR-912`, `REQ-978`,
`CON-966`'s possession sentence and the browser-JavaScript claim. It names
**nothing** in `CON-960`.

0.4.8 `CON-960` — the only clause in the whole trajectory that adds migration
members to the core (verified by grepping 0.4.4–0.4.11 for `OfferCoreV2` and
`accountTransition`; `CON-960` is the sole hit) — says:

> `OfferCoreV2` gains **one required `accountTransition` object**:
>
> ```text
> NoTransition
> PathAToB {
>   legacyHandle, legacyKeyDigest, migrationRooms,
>   roomSetDigest, migrationSnapshotDigest, snapshotNonce
> }
> ```
>
> `NoTransition` is valid only for a session already authenticated through open
> Path-B standing.

The only sentence of `CON-960` any ledger has ever replaced is 0.4.10 `CON-968`
clause 3, which replaced the `IntentVerifier`-returns-`AccountTransition`
sentence and nothing else. cbcl-pairing 0.4.4 `REQ-026`/`CON-021` — unchanged in
this candidate — carry the same closed sum:
`AccountTransition = NoTransition | PathAToB(PathAToBDisplay)`.

### Why this is substantive, not editorial

**(a) The digest preimage has two definitions.** `offerCoreDigest` is
`SHA-256(RFC8785(OfferCoreV2))` (0.4.5, and `ceremony::offer_digest`). 0.4.8
`CON-960` requires every transition member to be "part of RFC-8785
`offerCoreDigest`". Under `CON-960` the preimage nests them one level down;
under `REQ-982` it flattens them to the top level. RFC-8785 of
`{"accountTransition":{"legacyHandle":...}}` and of `{"legacyHandle":...}` are
different octet strings, so hub, browser and wallet — which `CON-013` requires
to recompute this value independently — cannot agree.

**(b) `NoTransition` becomes unrepresentable.** `CON-939` states of the core:
"No listed member is optional." A flat core with mandatory `legacyHandle`,
`migrationRooms` and three migration digests has no encoding for a ceremony that
is not a migration, which `CON-960` explicitly says is a live branch. The nested
form has one, which is why it is a sum type.

**(c) `accountDid` has no source.** Grepping 0.4.4–0.4.11, `accountDid` appears
only in 0.4.7's membership-classification result `Bound(accountDid)` and in
0.4.8 `REQ-972`'s `migrationConfirmationDigest` preimage — never as an
`OfferCoreV2` member. `REQ-982` adds it to the signed core with no grammar, no
declared relationship to `accountScopeId`/`accountPrincipalDigest`, and no
ledger entry.

**(d) The release-blocking arithmetic is computed over the undefined shape.**
`NFR-011` makes the generator "fail when any legal maximum exceeds its component
or total", and `TEST-1109`/`TEST-066` are mandatory. I reconstructed both shapes
from the declared maxima:

```text
flat 29-member core   (REQ-982)     non-room = 4,070 octets
nested 23-member core (CON-960)     non-room = 3,564 octets
declared                            non-room = 4,067 octets
```

Neither matches. (The 3-octet gap on the flat shape is a separate defect — see
`M-1`.)

### Minimum sound correction

Selfsame `CON-970` must ledger this explicitly. Either

- **retain `CON-960`'s nested `accountTransition`** (the shape cbcl-pairing
  `CON-021` already carries and the only one with a `NoTransition` encoding),
  restate the core as 23 members, drop `accountDid` or give it a grammar and a
  home inside `PathAToB`, and re-derive the non-room total (3,564 by my
  reconstruction) from that shape; or
- **explicitly replace `CON-960`'s member paragraph**, state how `NoTransition`
  is encoded in a core where every member is required, give `accountDid` a
  grammar and a cap, and re-point cbcl-pairing `CON-021`'s `AccountTransition`
  at the flat form.

cbcl-bus `NFR-011`/`CON-020`/`TEST-066` and cbcl-pairing `CON-021` must move
with whichever is chosen.

---

## H-2 (HIGH) — `REQ-983`/`REQ-054`'s "neither ordering locks out the enrolled identity" is false at the baseline: `cbcl-core-enrol` refuses on `pairing-unratified` before the TOFU branch, so `enroll-or-check/2` is unreachable for a claimed key, and no delete or ratification path exists

**Owner: cbcl-bus** (`REQ-054`, `CON-021`, `TEST-067`), mirrored in **Selfsame**
(`REQ-983`, `TEST-1110`).

### What 0.4.11 says

`REQ-983`:

> If member enrollment wins the device guard, a later pairaccount claim sees the
> member row and refuses. **If a valid pairaccount claim wins first, later member
> enrollment can create the stronger member authority. That member row makes the
> claim inert. Neither ordering locks out the enrolled identity.**

cbcl-bus `REQ-054`/`CON-021` and `TEST-067` say the same: "If claim wins, later
member authority makes it inert. The public transaction verdict must reach
`enrol-commit` in both outcomes." `TEST-1110` makes it mandatory: "Race member
enrollment, pairaccount proof, both grant creators, and both grant deleters for
one key. Require one documented ordering with no partial state."

### What the code does

`enroll-or-check/2` is reached from exactly one place, and only after
`cbcl-core-enrol:resolve` has already accepted the frame:

```lfe
;; cbcl-chat-session-ws.lfe:282-296
(defun authorise-frame (handle pk kind msg binding state)
  (case (enrol-facts binding handle pk kind msg now state)
    (`#(ok ,handle-facts ,key-facts ,permission ,state2)
     (case (cbcl-core-enrol:resolve handle-facts key-facts permission now)
       (`#(accept ,accepted) (enrol-commit accepted handle pk state2))
       ...
;; cbcl-chat-session-ws.lfe:454-462
(defun enrol-commit (accepted handle pk state)
  (case (maps:get 'bind accepted 'undefined)
    (`#(account ,account) ...)
    (`#(key ,_bound)
     (case (cbcl-chat-members:enroll-or-check handle pk) ...
```

and `resolve` refuses on `pairing-unratified` **before** it inspects the binding
at all — i.e. before the `unbound -> tofu` branch that produces `#(key ...)`:

```lfe
;; cbcl-core-enrol.lfe:125-131
(if (not (=:= 'false (maps:get 'pairing-unratified frame-key 'unknown)))
  #(reject pairing-unratified)
  (case (maps:get 'binding handle 'undefined)
    ('unbound (tofu handle frame-key))
    ...
```

`pairing-unratified` is `cbcl-chat-pairaccount:unratified-device?/1`
(`cbcl-chat-session-ws.lfe:347` in `selector-facts`, `:385` in
`account-standing-facts`), which under `REQ-054` returns literal `false` only
when a **grant or member row already exists**. For a key whose only authority
row is the pairaccount claim, it returns `true`, so:

- `resolve` returns `#(reject pairing-unratified)`;
- `enrol-commit` is never called;
- `enroll-or-check/2` is never called;
- **no member row can ever be created for that key**.

And there is no escape: `cbcl-chat-pairaccount` exports exactly
`(ensure-table 0) (claim 2) (grants 1) (unratified-device? 1)`
(`cbcl-chat-pairaccount.lfe:15-20`). There is no `revoke`, no `ratify`, and
`state` is stamped `'unratified` "until a ratification surface exists". The row
is permanent.

### Why this matters, and why it is not merely editorial

The lockout is *intended* by SPEC-076 `REQ-004` ("a paired-but-unratified
Selfsame device key may not enrol as a chat identity"), which `REQ-054` does not
amend. So the design has two effective statements that cannot both be true: the
inherited one that the claim is a permanent inertness, and the new one that
member enrolment can override it. The consequence chain is concrete:

- `TEST-1110`/`TEST-067`'s claim-wins arm cannot pass without changing
  `cbcl-core-enrol`'s fail-closed clause — a change no clause authorizes, and
  one that would let a paired-but-unratified device TOFU-enrol, which is the
  exact hazard SPEC-076 `REQ-004` exists to prevent.
- `GATE-04`'s "authority dominates only while its row exists, and authorized
  revocation has the documented closed outcome" box cannot be closed against a
  requirement sentence that is false as written.
- `REQ-053`'s possession proof narrows *who* can create the lockout (only the
  holder of the claimed key), which is why this is HIGH rather than the CRITICAL
  third-party brick 0.4.9 filed — but it does not remove it. A person who pairs
  a device key through SPEC-076 and later wants that same device to be a chat
  identity is permanently refused, with no operator remedy.

### Minimum sound correction

cbcl-bus owns it, and it is a design decision, not a wording fix. Either

- **state the truth**: a key with an unratified pairaccount row is permanently
  ineligible for member enrolment (SPEC-076 `REQ-004`), delete the
  "later member enrollment can create the stronger member authority" and
  "neither ordering locks out the enrolled identity" sentences from `REQ-983`
  and `REQ-054`, and rewrite `TEST-1110`/`TEST-067`'s claim-wins arm to require
  a closed refusal; or
- **specify the ratification/revocation surface**: add an authorized deleter for
  `cbcl-pairaccount-grant` with its own guard order, index maintenance and
  restart evidence, and state exactly which authority may invoke it — which is a
  SPEC-076 amendment, and would need its own review round.

Selfsame `REQ-983`/`TEST-1110` must move with the choice.

---

## M-1 (MEDIUM, release-blocking) — `REQ-982`/`NFR-011`'s 4,067-octet non-room core is not reproducible from its own declared maxima; the auxiliary budget double-counts `deviceKeyJwk` and uses a different value for it than the core does; and only one member has a declared escaped-form cap while every total is stated in RFC-8785 octets

**Owner: Selfsame `REQ-982`**, jointly with **cbcl-bus `NFR-011`/`CON-020`**.

`NFR-011` makes this release-blocking: "The generator records actual maxima and
residuals. **It fails when any legal maximum exceeds its component or total.**"
`TEST-1109`/`TEST-066` are mandatory.

### What reproduces exactly (recorded so the repair does not disturb it)

I reconstructed the 21-member statement from the declared constants, RFC-8785
(no whitespace, minimal escaping), with `issuedAt`/`expiresAt` as strings —
verified correct against `enrollment::build`, which emits
`Json::text(time::format_date_time_stamp(...))` (`enrollment.rs:659-681`,
`time.rs:99-105`). Per-member `"name":value` octets:

```text
accountPrincipalDigest     70   deviceKeyDigest          63   profileDigest    61
accountScopeId             62   devicePossessionProof   112   profileVersion   18
applicationAccountDisplay 286   evidenceVersion          19   providerId       78
applicationId             114   expiresAt                46   requestId        57
carrierDigest              61   issuedAt                 45   requestedPerms  548
ceremonyId                 58   offerDigest              59   returnUri       174
descriptorDigest           64   platformBindingId       182   transcriptHash  105
                      members 2,282  +  2 braces + 20 commas = 22
                      -------------------------------------------
                      payload                                 2,304   exact
```

The protected header, with `alg` = `EdDSA` (`jws.rs:46`), `typ` =
`selfsame-credential-v2+jws`, `kid` at the declared 128:
`1 + 13 + 1 + 136 + 1 + 34 + 1 = 187` exact. The base64url-no-pad expansions
`187 -> 250`, `2304 -> 3072`, `64 -> 86`, plus two separators, give `3,410`
exact, leaving `590` under the 4,000 policy. **All four figures reproduce.**

### What does not reproduce

**(a) The device JWK is 93 canonical octets, not 96, and the two derivations
disagree.** `DEVICE_JWK_MEMBERS = ["kty","crv","alg","x"]`
(`ceremony.rs:278`), Ed25519, so the RFC-8785 form is fixed:

```text
{"alg":"EdDSA","crv":"Ed25519","kty":"OKP","x":"<43>"}
 1 + 13 + 1 + 15 + 1 + 11 + 1 + 49 + 1  =  93 octets
```

`REQ-982` declares 96 in the auxiliary budget ("The four-member device JWK has
maximum 96 canonical octets", `512 + 96 + 86 + 512 = 1,206`), but the non-room
core total of 4,067 is only reachable with **93**. Recomputing the flat
29-member core (H-1's shape) with the *declared* 96 gives **4,070**, three
octets above the declared `V2_MAX_OFFER_CORE_NON_ROOM_OCTETS = 4,067`. The
generator gate `NFR-011` mandates therefore fails on the design's own constants.

**(b) `deviceKeyJwk` is counted twice.** It is a member of `OfferCoreV2`
(`CON-939`), already inside the 56,000-octet core, and is then charged again as
a line item in the 2,000-octet auxiliary budget. The budget is conservative so
the 62,000 total still holds, but the line item is not a real contribution —
which is how the same quantity came to have two values in one requirement.

**(c) The provider hint's exact maximum is 507, not 512.** 0.4.5 `CON-945`
(adopted, unreplaced) declares exactly seven required members. At the declared
`applicationId` at most 96 and `providerId` at most 63:

```text
applicationId 114, carrierDigest 61, descriptorDigest 64, offerDigest 59,
profileVersion 18, providerId 78, transcriptHash 105  = 499
+ 2 braces + 6 commas = 8                             -----
                                                        507
```

512 is a safe over-declaration, but `REQ-982` states it as "the provider hint
has maximum 512 canonical octets" without naming `CON-945` as the member source,
so the number cannot be checked from `REQ-982` alone. `REQ-982` likewise gives
no member list for the complete credential member beyond a total.

**(d) The caps are declared over UTF-8, the totals over RFC-8785, and only one
member closes the gap.** `REQ-982` says "Every octet count is over UTF-8 before
cloning", then states every total in canonical (escaped) octets.
`applicationAccountDisplay` is the only member with both caps declared
(`..._OCTETS = 256` **and** `..._JCS = 256`). `legacyHandle` is capped at 256
UTF-8 octets with no escaped-form cap and no stated character grammar; if a
handle can carry a double quote, a backslash or a C0 control, RFC-8785 escaping
expands it by up to six times (a C0 octet becomes a six-character `\u00XX`
escape), which alone can add 1,280 octets to the "4,067" non-room total.
`deviceDid`/`accountDid` (512) and `platformBindingId` (160) are in the same
position; their grammars happen to exclude escapable characters, but the
requirement does not say so.

**(e) `V2_MAX_MIGRATION_ROOMS_JCS_OCTETS = 48,000` and adopted 0.4.8 `REQ-972`'s
"48,000 encoded UTF-8 octets" for the same array are stated in different units**,
and neither declares whether room identifiers reach JSON as text or as
base64url of raw bytes — while `REQ-972`'s `roomSetDigest` is defined over "the
complete unique **raw room-byte** array".

### Minimum sound correction

Selfsame `REQ-982` and cbcl-bus `NFR-011` must publish one derivation whose
inputs are all declared: add `V2_MAX_DEVICE_JWK_OCTETS` to the constant block
and use the same value in both the core and the auxiliary derivations; remove
`deviceKeyJwk` from the auxiliary line or say explicitly that it is a deliberate
double charge; cite `CON-945` as the provider hint's member source and state
507 or justify 512; declare an escaped-form cap (or an explicit
no-escapable-character grammar) for every string member that participates in a
canonical total, exactly as `applicationAccountDisplay` already does; and state
whether `migrationRooms` entries are text or base64url so the 48,000 figure has
one unit.

---

## M-2 (MEDIUM, release-blocking) — the cbcl-bus `GATE-04` replacement chain deletes eight mandatory boxes, and 0.16.4's "All prior surviving GATE-04 boxes remain open" is inert because `REQ-051` item 10 leaves no prior `GATE-04` surviving

**Owner: cbcl-bus** (`REQ-051` item 10, `GATE-04`).

### The chain

- 0.16.2 `GATE-04` header: "replaces 0.16.1 GATE-04".
- 0.16.3 `GATE-04` header: "replaces 0.16.2 GATE-04".
- 0.16.4 `REQ-051` item 10: "This amendment's GATE-04 **replaces** 0.16.3
  GATE-04."

So the only effective `GATE-04` is 0.16.4's. Its opening sentence — "All prior
surviving `GATE-04` boxes remain open" — therefore quantifies over the empty
set, and the boxes it does not restate have no effective text.

### What is lost

Comparing 0.16.3's sixteen boxes against 0.16.4's fourteen, these are dropped
and appear nowhere in 0.16.4:

| Dropped box (0.16.3 / 0.16.2 wording) | Why it matters |
|---|---|
| `GATE-01` and `GATE-03` close without a credential/v2 bypass or exception | GATE-01's `CON-002` is what makes `maxDevicesPerAccount` exist at all (see `R-1`) |
| human security/privacy review accepts compromised-Path-A takeover, irreversible migration, exact room-list consent, relay-visible ciphertext length correlation, terminal `MigrationPlanTooLarge` | the only human check on the accepted residual in `F-E` and 0.4.10 `L-2` |
| hub identity-authority off-machine readiness and did-crdt durable readiness remain continuously true | `R-4`: post-commit standing has no legacy fallback |
| production closure artefact and two-gate-open fixture pass distinct CI controls with no release crossover | 0.4.8 `H-3`'s repair |
| dispatch-before-authorisation zero-write/fan-out evidence for the six client and six hub kinds | 0.4.8 `H-1`'s repair |
| frozen v1 and disjoint evidence-v2 recogniser/builder/signer call graphs | 0.4.8 `H-4`'s repair |
| guard/index migration, transaction mutation/crash/race suite, immutable status recovery, authority-continuity restore drill | `CON-014`'s recoverability |
| credential/v2 has no pairaccount writer/call edge; migration leaves the new key live with recoverable immutable status | 0.4.8 `C-1`'s repair |

Selfsame's side is unaffected: 0.4.11 adopts 0.4.10's "Implementation and gate
sequencing" section, whose clause 5 still names GATE-01, GATE-03, GATE-04, human
cryptography/migration/privacy review, independent CPace evidence, both
durability gates and separate deployment confirmation. But the cbcl-bus
repository's own release gate — the one an operator would read before enabling
the hub — is now materially weaker than the round that preceded it, and
`TEST-071`'s "static checks require every `REQ-051` survivor and replacement"
will not detect it, because the deletion *is* a declared replacement.

### Minimum sound correction

cbcl-bus `GATE-04` must either restate every surviving box explicitly (the
0.16.2/0.16.3 pattern), or `REQ-051` item 10 must say "this amendment's
`GATE-04` **supplements** 0.16.3's; all 0.16.1–0.16.3 boxes remain open" and the
`supersedes:`/`adopts:` frontmatter must make the prior box lists effective.

---

## M-3 (MEDIUM) — the enumerated v1/v2 disjointness boundary covers only the statement recogniser/builder/signer, while three further `crate::PROFILE_VERSION = 1` comparison sites sit on the credential/v2 path, including the shared five-member `provider_hint` recogniser that `CON-945` requires v2 to reach with seven members and `profileVersion = 2`

**Owner: Selfsame `REQ-981`/`CON-970`/`TEST-1112`**, mirrored in cbcl-bus
`REQ-052`/`CON-020`.

`REQ-981` states the rule generically — "App identity performs total closed
dispatch before member recognition. The version-2 branch does not compare
against the crate-wide version-1 constant" — but the only *enumerated* boundary
in the effective text is adopted `REQ-977`'s six names
(`recognise_/build_/sign_enrollment_v1`, `recognise_/build_/sign_credential_v2`),
all statement-scoped. At the baseline `crate::PROFILE_VERSION` is compared in
four places, and three of them are outside that enumeration:

```text
profile.rs:60,385      PROFILE_VERSION = 1; recognise() hard-refuses any other
                       ("must be exactly 1 in this profile version")
enrollment.rs:394,452,552   statement-scoped — covered by REQ-977
provider_hint.rs:115   if self.profile_version != crate::PROFILE_VERSION
                       => HintError::UnsupportedProfileVersion
```

`provider_hint.rs` is the concrete one. 0.4.5 `CON-945` (adopted) declares the
credential/v2 hint as **seven** required members carrying `profileVersion = 2`;
the deployed recogniser has `HINT_MEMBERS` closed at **five**
(`applicationId`, `profileVersion`, `providerId`, `descriptorDigest`,
`offerDigest`) and rejects anything else with `HintError::UnknownMember`, then
rejects `profileVersion = 2` with `UnsupportedProfileVersion`. So the v2 hint
needs its own recogniser branch, and `TEST-1112`'s static effective-text check —
which enumerates what it rejects ("a live-v1 v2 trust anchor, unconditional
post-revocation authority, a per-record description of `index_read`") — names no
hint site and would not fail on a shared `provider_hint::verify`.

`profile.rs:385` is the per-branch profile recogniser `CON-951` already owns, so
it is named; but `REQ-981`'s "does not compare against the crate-wide
version-1 constant" is the sentence that has to survive translation into a
static test, and the test does not enumerate the sites.

### Minimum sound correction

`REQ-981` should extend `REQ-977`'s enumeration to the non-statement v2 surfaces
— the profile recogniser branch (`CON-951`), the seven-member provider-hint
recogniser (`CON-945`), and the offer recogniser (`OFFER_CORE_MEMBERS`,
`OFFER_EXCLUDED_MEMBERS`, `recognise_offer`'s `payloadVersion == 1` literal) —
and `TEST-1112` should reject a `crate::PROFILE_VERSION` comparison reachable
from any v2 entry point, by site, rather than by prose.

---

## L-1 (LOW) — `NFR-913`'s matrix contains at least two cross-operation lock-order inversions it does not name, and neither it nor `ADR-947` records that Mnesia's locker is wait-die, which is what actually makes the design deadlock-free

**Owner: Selfsame `NFR-913`/`ADR-947`**, mirrored in cbcl-bus
`NFR-011`/`CON-021`.

The two load-bearing claims are **correct**, and I verified both in OTP source
rather than accepting them:

```erlang
%% mnesia_locker.erl:1086-1101  — index_read takes {Tab, ?ALL}
ixrlock(Tid, Store, Tab, IxKey, Pos) ->
    ... case need_lock(Store, Tab, ?ALL, read) of ...
%% mnesia_locker.erl:326-333
grant_lock(Tid, {ix_read,IxKey,Pos}, Lock, Oid = {Tab,_}) ->
    Res = ix_read_res(Tab, IxKey, Pos), set_lock(Tid, Oid, Lock), {granted, Res, [?ALL]};
%% mnesia.erl init_iteration/4 — foldl locks the table
init_iteration(ActivityId, Opaque, Tab, LockKind) ->
    lock(ActivityId, Opaque, {table, Tab}, LockKind), ...
%% mnesia_locker.erl rlock_table/3
rlock_table(Tid, Store, Tab) -> rlock(Tid, Store, {Tab, ?ALL}).
```

The absent-row guard reasoning is also sound: `wlock/4` inserts
`{{locks,Tab,Key}, write}` and requests the lock whether or not a record exists,
so locking a key with no row is real exclusion.

What is missing is the reason the matrix is safe. `mnesia_locker:can_queue/4`
grants a wait slot only when the holder is *younger* than the requester and
otherwise refuses with `cyclic`; `can_queue(Tid, Tid, _, Res) -> Res` makes a
transaction's own locks non-conflicting. That is **wait-die**: no transaction
ever waits on an older one, so the lock manager cannot deadlock regardless of
acquisition order. The stated order is therefore abort-rate and evidence
discipline, not a deadlock proof — which is worth saying, because the matrix as
written contains inversions the amendment does not predict:

| Pair | Inversion |
|---|---|
| grant creation vs observed revocation | creation takes `device guard` then `grant ?ALL`; observed revocation takes `grant ?ALL` then sorted `device guards` |
| credential/v2 finalizer vs new member enrollment | the finalizer takes exact `cbcl-member` (step 5) then `grant ?ALL` (step 8); member enrollment takes `grant ?ALL` then its exact `cbcl-member` write |

Both produce additional `{aborted, cyclic}` retries under load. `NFR-913`
predicts them only for "different-account grant writers ... Mnesia can abort one
transaction as cyclic". Correctness is preserved by wait-die and bounded retry,
so this is LOW — but `TEST-1111`/`TEST-069`'s "require the exact guard, `?ALL`,
and record lock sequence" and the `GATE-04` load box should be told which pairs
to expect aborts from, or the measurement will read a predicted-clean trace as a
failure.

**Minimum sound correction:** state in `NFR-913`/`ADR-947` that Mnesia prevents
deadlock by wait-die preemption (`mnesia_locker:can_queue/4`,
`allowed_to_be_queued/2`), name the two cross-operation inversions above as
expected cyclic-abort sources alongside the different-account writers, and have
`TEST-1111` assert on them.

---

## L-2 (LOW) — `REQ-983` rule 3 silently removes the deployed handle-change affordance for any browser that has completed the credential/v2 migration, while the same requirement makes preserving it for plain keys an explicit goal

**Owner: cbcl-bus `REQ-054`**, mirrored in Selfsame `REQ-983`.

`REQ-983` sets out to preserve multi-handle key sharing — "The deployed behavior
where one browser key enrolls under successive handles remains supported" — and
rule 3 refuses "For an absent handle, any device-grant row for the key".

After a credential/v2 migration the browser reconnects on Path-B with the
granted device key as its wire key (`CON-962`: "browser verifies and installs
the distinct device key locally ... new-key Path-B reconnect"). The deployed
handle affordance is still live:

```js
// app.js
$('handle').addEventListener('change', (e) => { ... id.handle = h; ... })
```

A changed handle has no `cbcl-account-binding` row, so `dispatch/3` resolves it
as `unbound`, reaches `tofu`, and calls `enroll-or-check`, which rule 3 refuses
because the key now carries a `cbcl-device-grant` row. So a migrated person can
no longer change their chat handle. That is probably the right authority answer
— a Path-B device key should not become a Path-A chat identity — but it is an
unstated consequence for a deployed, user-reachable control in the same clause
that makes preserving that control a named goal, and `TEST-1110`/`TEST-067` do
not cover it.

**Minimum sound correction:** state the post-migration case explicitly in
`REQ-983`/`REQ-054` (either as an accepted consequence with the person-visible
error, or with a stated re-binding path), and add it to `TEST-1110`/`TEST-067`.

---

## L-3 (LOW) — `V2_MAX_PAYLOAD_OCTETS` collides with the deployed `MAX_PAYLOAD_OCTETS`, which bounds the *complete* offer payload rather than the core, and no constant names the v2 complete-payload recogniser bound

**Owner: Selfsame `REQ-982`.**

At the baseline `ceremony.rs:64,283-287` uses `MAX_PAYLOAD_OCTETS = 69_607` as
both the JSON `Limits.max_bytes` and the length pre-check for the **whole**
offer payload — core members plus
`OFFER_EXCLUDED_MEMBERS = ["enrollmentEvidence","providerHint"]`. `REQ-982`
defines `V2_MAX_PAYLOAD_OCTETS = 56,000`, equal to `V2_MAX_OFFER_CORE_OCTETS`,
and applies it to the **core** ("The generator constructs that maximum legal
`OfferCoreV2`. It refuses above `V2_MAX_PAYLOAD_OCTETS`"). A v2 recogniser
written by analogy with the deployed one would cap the whole payload at 56,000
and refuse a legal member of roughly 56,140 octets (52,067 core + 3,433 evidence
with its member name + 522 hint with its member name + 112 proof with its member
name + delimiters), while `V2_MAX_CREDENTIAL_MEMBER_OCTETS = 62,000` permits it.

**Minimum sound correction:** rename the core constant (or drop it in favour of
`V2_MAX_OFFER_CORE_OCTETS`) and declare the v2 complete-payload recogniser bound
by name, so the two levels the deployed crate conflates are separated in the
specification rather than in the implementer's head.

---

## L-4 (LOW) — `REQ-982` narrows adopted `CON-939`'s `applicationAccountDisplay` grammar without a ledger entry, making a legal 128-scalar non-Latin display name illegal

**Owner: Selfsame `REQ-982`/`CON-970`.**

`CON-939` (adopted, unreplaced): "`applicationAccountDisplay` is 1..128 Unicode
scalar values after NFC" — up to 512 UTF-8 octets. `REQ-982` adds "Application
display text is NFC, has 1 through 128 Unicode scalars, and excludes C0 and C1
controls. Both its UTF-8 bytes and its RFC-8785 string-content bytes fit the
stated caps", with both caps at 256. The conjunction excludes any 128-scalar
string averaging more than two UTF-8 octets per scalar — i.e. most CJK, all
emoji, and much Indic text. `CON-970` clause 6 says `REQ-982` replaces
"`REQ-979`'s undeclared maxima and `NFR-912`'s bound arithmetic"; it does not
name `CON-939`'s display grammar, so the two are simultaneously effective and
inconsistent.

**Minimum sound correction:** ledger it in `CON-970` as an explicit replacement
of `CON-939`'s display sentence, and state the scalar/octet conjunction (and its
internationalization consequence) in one place.

---

# Disposition of the original `F-A` – `F-E`

| Finding | Disposition | Evidence |
|---|---|---|
| **F-A** — trust is application+relay scoped; a new application prompts | **HOLDS; 0.4.10's disturbance is repaired** | `REQ-981` restores the distinct canonical ApplicationId `https://chat.anuna.io/selfsame/application/v2` and keys person-owned policy on the exact pair `(.../application/v2, https://chat.anuna.io:9443)`. Because the id is new, the first v2 contact prompts by construction, and `REQ-981` states it: "A v1 policy entry cannot suppress this first v2 prompt." `relayOrigin` provenance stays authentic, not wire-claimed: it is a member of the CON-220 profile's `cbclPairingRelays`, present in the live ratified 1,235-octet document (`cbcl-chat-selfsame-application-gate.lfe` decodes to `"relayOrigin":"https://chat.anuna.io:9443"`, digest pinned at `6ac06f62...` by `cbcl-chat-gate-closed-tests.lfe`). `TEST-1108`/`TEST-065` require the preload-v1-then-v2 prompt case. |
| **F-B** — approval displays the CON-220-authenticated application/transition; cross-check precedes display | **HOLDS** | Adopted `CON-967` and cbcl-pairing `CON-021` still agree word for word: the `IntentVerifier` "can return only `Ok(())` or a closed profile error", and only after success may the shared profile construct `CredentialV2Display`. cbcl-pairing 0.4.4 is byte-identical to the reviewed 0.4.10 candidate apart from two frontmatter lines, and remains constructible at `8e55160` (`DisplayIntent`/`DisplayField` are public-field structs, every construction is in-crate, `recognise_intent(&mut self)` type-checks at both call sites and preserves `dyn` object safety). **Caveat:** `H-1` makes the *content* of the transition ambiguous — cbcl-pairing's closed `AccountTransition` sum no longer matches Selfsame's flat core — so what is displayed is well-owned but not yet well-defined. |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias provisioning or grant precedes the appropriate explicit consent | **HOLDS** | Adopted `CON-962`'s sixteen-step order is intact and untouched by 0.4.11 (verified: 0.4.11 replaces `REQ-977`, `REQ-978`, `REQ-979`, `NFR-912`, `CON-968` clause 1 and `CON-966`'s possession sentence, and nothing in `CON-962`). Nothing wallet-side precedes preliminary approval; no hub binding/grant/alias/membership/room-deletion precedes signed final approval, verified publication/grant and browser install. `CON-969`'s new browser handler adds no mutation: it either signs the challenge with an already-retained seed or terminates locally. `REQ-981`'s "Both explicit migration consent screens remain mandatory on every migration" is preserved. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS, and is v2-namespaced again** | Adopted `REQ-970`/`CON-939`: the gated allocation command mints one pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId` before the public carrier; they become durable only at the final transaction. 0.4.10 `H-1(d)`'s side effect — the scope being minted inside the *live v1* application's namespace — is undone by `REQ-981`'s distinct v2 ApplicationId. |
| **F-E** — per-app consent is a mitigation, not a relay-operatorship binding claim | **HOLDS** | 0.4.11's review-disposition section restates it as such and claims nothing further ("Relay operatorship remains an accepted residual, not an inferred fact"). Adopted `REQ-979`'s relay-visible ciphertext-length residual survives (`MAX_ADJACENT_BODY_OCTETS = 65_536`, `cbcl-pairing/src/cbcl_protocol.rs:34`), but see `M-2`: the human privacy-acceptance box that gated it has been dropped from the effective cbcl-bus `GATE-04`. |

---

# Disposition of every 0.4.8 finding (1C / 4H / 3M / 7L)

Re-tested against 0.4.11 and code.

| 0.4.8 finding | Re-test verdict | Evidence |
|---|---|---|
| **C-1** (CRITICAL) — the transaction writes a `cbcl-pairaccount-grant` for the key the person must reconnect with; both keys inert | **CLOSED** | Two independent mechanisms survive. (i) Credential/v2 writes no pairaccount row and `REQ-983`/`REQ-054` make `unratified-device?` return literal `false` on any grant or member row, so the granted key passes the unchanged `cbcl-core-enrol.lfe:125-131` clause. (ii) `REQ-053`/`CON-016` require an inner Ed25519 proof by the exact claimed key, removing the third-party writer — the gap is real at the baseline: `do-pairaccountgrant` (`cbcl-chat-session-ws.lfe:1667-1678`) takes `devkey` verbatim from the frame with no possession check. |
| **H-1** (HIGH) — WS gate runs after TOFU and default fan-out | **CLOSED** | Structurally exact against the code: `handle-frame` (`cbcl-chat-session-ws.lfe:204-211`) performs only decode and parse before `dispatch/3`, and `dispatch/3` (`:212-233`) has `grant`/`grantproof` first clauses that precede the `account-binding` read, `resolve-key`, `verify-and-route` and TOFU. The fall-through hazard is real (`route/8`'s default is `do-publish` at `:960`). The owning clause, cbcl-bus `REQ-042`, is now explicitly restored by `REQ-051`'s 0.16.2 survivor list. |
| **H-2** (HIGH) — `fingerprintCanonicalBytes` undefined; hub has no BLAKE3 | **CLOSED** | `enrollment::device_key_digest` (`enrollment.rs:265-274`) is exactly `SHA-256(canonicalise(deviceKeyJwk))` over the closed four-member object `DEVICE_JWK_MEMBERS` (`ceremony.rs:278`). That definition is what let me compute its exact 93-octet canonical size in `M-1`. |
| **H-3** (HIGH) — the positive fixture opens an unnamed second Tier-1 gate | **CLOSED** | Both gates verified: `cbcl-chat-path-b-gate:profile/0` returns `#(error tier-1-unapproved)` as a source constant; the shell holds only `cbcl-chat-path-b-gate.lfe`, `cbcl-chat-selfsame-application-gate.lfe`, `cbcl-chat-webfinger-gate.lfe`, so `cbcl-chat-credential-v2-gate` is genuinely absent. cbcl-bus `REQ-044`/`CON-015` survive per `REQ-051`. **But see `M-2`:** the `GATE-04` box that made the two-artefact CI separation a release condition is no longer in the effective gate. |
| **H-4** (HIGH) — the live `/selfsame/enrolment/sign` is an unauthenticated widening oracle | **CLOSED** | The hazard is exactly as described — `sign_enrollment_profile_bound` (`selfsame-beam/src/enrollment.rs:129-152`) takes caller-supplied `statement` octets. The repair is verified free: `build()` hard-codes `("evidenceVersion", Json::int(1))` (`enrollment.rs:661`), `EnrollmentStatement` carries no evidence-version field, and `recognise_payload` compares `evidenceVersion` to a literal `Some(1)` — so today's `build` **is** a v1-only builder. `REQ-052`/`CON-020` keep the route v1-only. |
| **M-1** (MEDIUM) — wallet cannot recompute two of three digests | **CLOSED** | Adopted `REQ-975`/`CON-013` state the per-party split honestly; `CON-013` survives per `REQ-051`. |
| **M-2** (MEDIUM) — no credential/v2 reply performative is in `hub-originated?` | **CLOSED** | `hub-originated?/1` (`cbcl-core-dialect.lfe:79-85`) is a closed `lists:member` literal already containing `account-linked` and `pairaccountgranted`, and is the only thing between a client-authored hub verb and `do-publish`. `REQ-050` survives unchanged per `REQ-051`. |
| **M-3** (MEDIUM) — the typed `AccountTransition` has no carrier | **CLOSED in the carrier, REOPENED in the content** | cbcl-pairing 0.4.4 `REQ-026`/`CON-021` supply the carrier and are verified implementable at `8e55160`. But `H-1` means the flat 29-member core and the closed `NoTransition \| PathAToB` sum no longer describe the same value. |
| **L-1** (LOW) — the lock order names a nonexistent "device count index" | **CLOSED** | `ensure-device-table` (`cbcl-chat-account-grants.lfe:53-62`) still declares no index and `account-device-count/1` (`:204-207`) is a whole-table `mnesia:foldl`, exactly as filed. `NFR-913`/`NFR-011` now add and name `account`/`pubkey` on `cbcl-device-grant` and `pubkey` on `cbcl-member` (`cbcl-chat-members.lfe:16-24` confirms the member index is a genuine new migration). |
| **L-2** (LOW) — `CON-941`'s `cbcl-webfinger-account` primary key | **CLOSED** | Unchanged since 0.4.9; not re-opened by this amendment. |
| **L-3** (LOW) — a nonexistent gate stated as a present-tense fact | **CLOSED** | Effective text says "On landing"; the module is absent at the baseline (verified above). |
| **L-4** (LOW) — mixed NUL notation / field-vector overclaim | **CLOSED** | One rule and an exact member vector; `cbcl-member` is `(handle pubkey enrolled)` (`cbcl-chat-members.lfe:13`), so excluding the wall-clock `enrolled` from the snapshot vector is correct. |
| **L-5** (LOW) — the offer bound lacks arithmetic | **PARTLY CLOSED, becomes `M-1` (this review)** | The statement half now reproduces exactly (2,304 / 187 / 3,410 / 590). The core half does not: 4,067 is unreachable from the declared constants, and `H-1` means the member set it sums over is undefined. |
| **L-6** (LOW) — oversize silently excludes the person | **CLOSED** | Adopted `NFR-911`'s terminal-exclusion statement retained. |
| **L-7** (LOW) — no allocator-observable no-offer test | **CLOSED** | cbcl-pairing's relay still distinguishes control frames from adjacent bodies (`src/cbcl_protocol.rs:32-34`), so "zero `offer` objects" is observable. |

---

# Disposition of every 0.4.9 finding (1C / 1H / 3M / 4L)

| 0.4.9 finding | Re-test verdict | Evidence |
|---|---|---|
| **C-1** (CRITICAL) — an arbitrary pairaccount claim can brick a granted key after commit | **CLOSED for the third-party brick; a self-inflicted enrolment lockout remains, becoming `H-2`** | Possession proof plus grant/member dominance removes the third-party writer entirely. What `REQ-983`/`REQ-054` additionally claim — that the claim-first ordering is recoverable through later member enrolment — is false against `cbcl-core-enrol.lfe:125-131` plus `cbcl-chat-session-ws.lfe:282-296,454-462`. |
| **H-1** (HIGH) — `profileVersion = 2` cannot verify against the live version-1 profile | **CLOSED** | `REQ-981` and `CON-970` clauses 2–5 take 0.4.10 `H-1`'s Option A: a *distinct* document at `.../application/v2` carrying `profileVersion = 2`, a statement carrying `evidenceVersion = 2` plus `profileVersion = 2` plus that document's digest, and the live v1 bytes frozen. Verified compatible with the code's shape: `profile.rs:385` refuses any version but 1, which `CON-951`'s three-branch dispatcher already owns; `enrollment.rs:394` compares `profileVersion` to `crate::PROFILE_VERSION`, which `REQ-981` requires the v2 branch not to use. See `M-3` for the sites the enumeration misses. |
| **M-1** (MEDIUM) — 8,192-byte v1 evidence cannot fit a 6,000-byte non-core budget | **CLOSED for the v1 collision; NOT CLOSED for the fixed point, becoming `M-1` (this review)** | `ENROLLMENT_JWS.max_octets = 8_192` (`enrollment.rs:58`) stays outside the v2 arithmetic, and the v2-specific 4,000-octet policy is correctly separate. 0.4.10 `M-1`'s "no fixed point" is now largely closed — every variable member has a declared cap — but the arithmetic still does not close. |
| **M-2** (MEDIUM) — adopted `CON-960` contradicts the verdict-only display API | **CLOSED** | 0.4.10 `CON-968` clause 3 named the exact sentence and replacement; `CON-970` leaves it effective. |
| **M-3** (MEDIUM) — the lock and cap rule does not cover both real grant writers | **CLOSED** | Verified at the baseline: `store-device-tx/7` (`cbcl-chat-account-grants.lfe:189-202`) is absent from the `defmodule` export list and already enforces the cap; `store-verified-device-grant/6` (`:216-237`) is exported, uncapped, and has no production caller, so `REQ-054`'s removal is safe. The account guard key is exact and real — `cbcl-device-grant.account` holds the same account DID binary `cbcl-account-binding.account` holds. |
| **L-1** (LOW) — `account-linked` is not a credential/v2 kind | **CLOSED** | `account-linked` is in `custom-kind` and in `hub-originated?/1` (`cbcl-core-dialect.lfe:82-84`) and is emitted post-commit under REQ-033; `REQ-050` survives per `REQ-051`. |
| **L-2** (LOW) — ciphertext length leaks coarse migration size | **CLOSED in text; weakened in gate** | The residual is correctly stated and bounded. But `M-2` removes the human privacy-acceptance box that made it a release condition on the cbcl-bus side. |
| **L-3** (LOW) — the hub lacks CBOR/JCS primitives | **CLOSED** | Adopted `REQ-980`'s closed typed NIF boundary survives; `CON-020` restates "LFE supplies typed values and raw binaries, never caller-selected canonical preimages", and `REQ-982` correctly demotes BEAM-through-NIF to "a marshalling and integration lane, not a third canonical algorithm". |
| **L-4** (LOW) — the shared `build()` can silently widen v1 | **CLOSED** | `REQ-977`'s four boundaries survive; verified free at the baseline (see 0.4.8 `H-4` row). |

---

# Disposition of every 0.4.10 finding (0C / 2H / 4M / 3L)

| 0.4.10 finding | 0.4.11 claim | Re-test verdict | Evidence |
|---|---|---|---|
| **H-1** (HIGH) — v2 has two incompatible trust anchors | `REQ-981` retains the distinct v2 ApplicationId and profile; only the statement discriminator becomes `evidenceVersion = 2` | **CLOSED** | `CON-970` clause 2 replaces 0.4.10 `CON-968` clause 1; clauses 3–5 explicitly keep 0.4.5 `REQ-957`/`CON-947`, 0.4.6 `REQ-962`/`CON-951` and 0.4.7 `CON-957` effective — I read all five and they now agree with `REQ-981`. `REQ-981` gives the v2 profile its own four-permission namespace at `.../application/v2#...`, which is legal under `profile.rs` `permissions()` (entries must share the applicationId **origin**, not its path — `https://chat.anuna.io` in both cases). The live document's four `.../application#...` permissions and its 1,235 octets are untouched. Per-branch profile recognition is implementable: `profile.rs:385`'s single-version refusal is exactly the site `CON-951`'s dispatcher replaces. First v2 contact prompts by construction. **Residual:** `M-3` — the enumeration of "does not compare against the crate-wide version-1 constant" omits three real sites. |
| **H-2** (HIGH) — cbcl-bus 0.16.3 deleted adopted authority clauses | `CON-970` adopts 0.16.4's explicit survivor and replacement ledger | **CLOSED** | `REQ-051` is a real ledger, and I checked it against the actual clause inventory of all three prior amendments. 0.16.1 (`REQ-040`, `NFR-008`, `CON-012`, `ADR-017`, `DEC-015`, `TEST-043`–`047`) and 0.16.2 (`REQ-041`, `REQ-042`, `REQ-044`, `CON-013`, `CON-014`, `CON-015`, `ADR-018`, `TEST-048`–`056`) survivors are enumerated exactly; the ten replacements map onto real clause numbers with no orphan and no double-replacement; partial replacements (`REQ-045`'s possession sentence, `REQ-049`'s independence sentence, `ADR-019`'s version portion, `DEC-017`'s browser sentence) leave determinate remainders under `adopts:`. Selfsame `CON-970` mirrors it and its test spec now enumerates `TEST-043`–`047`, `048`–`056`, `057`–`064`, `065`–`071` instead of an open range. **Residual:** `M-2` — the one clause class the ledger does *not* preserve is `GATE-04` itself. |
| **M-1** (MEDIUM) — size arithmetic has no fixed point | `REQ-982` declares every v2 variable-field maximum and derives the compact JWS bound | **PARTLY CLOSED, becomes `M-1` (this review)** | Genuine progress: every variable member now has a declared cap, the inherited `MAX_PERMISSIONS = 64` and the unbounded URI path/fragment are superseded for v2 by `V2_MAX_REQUESTED_PERMISSIONS = 4` and per-member octet caps, and the statement/header/JWS chain reproduces exactly. Not closed: the non-room core total is unreachable from the declared constants, the JWK is valued at 93 in one derivation and 96 in another, the hint's 512 has no declared member source, and escaped-form caps are declared for one member out of eight. |
| **M-2** (MEDIUM) — member collision rule breaks handle changes | `REQ-983` preserves multi-handle use of one plain enrolled key | **CLOSED for multi-handle; the transaction-verdict repair is right; NOT CLOSED for the claim-first ordering, becoming `H-2`** | Rule 4 ("another member row using the same key does not refuse") preserves the deployed `app.js` handle-change flow, which is a real state: one wire seed per browser, handle editable at will. Rule 3 supplies the invariant `REQ-046` needs by refusing a key that already has a `cbcl-device-grant` row. Making `enroll-or-check/2` one transaction whose verdict reaches `enrol-commit` is necessary and implementable — the current shape runs `pubkey/1` in one transaction, writes in another, and returns `'ok` unconditionally (`cbcl-chat-members.lfe:41-53`); `enrol-commit` already maps any non-`ok` to `#(error rejected)`. See `L-2` for the post-migration case rule 3 silently removes. |
| **M-3** (MEDIUM) — grant deleters escape the guard discipline | `REQ-983` covers both revocation writers and states the exact post-revocation result | **CLOSED** | Both deleters are named and given implementable guard orders. Verified against the code they must replace: `revoke-observed-tx` (`cbcl-chat-account-grants.lfe:297-305`) currently `mnesia:foldl`s the whole table then deletes, and `revoke-device-grant/2` (`:309-313`) deletes with no guard at all. Candidate discovery through the account index, raw-byte-sorted device guards and an exact recheck before delete is a faithful replacement, and Mnesia maintains indexes on delete so index parity is ordinary. `REQ-983`'s replacement of the "for all time" sentence with row-conditional dominance is correct and now matches the mechanism: the enrolled-member arm is genuinely permanent (`cbcl-chat-members` exports no delete), the grant arm is not, and the amendment now says so. |
| **M-4** (MEDIUM) — the lock order omits Mnesia table locks | `NFR-913` names every `?ALL` table lock and accepts measured contention as an open gate | **CLOSED** | Both claims verified in OTP 28.5 source rather than accepted (`mnesia_locker:ixrlock/5` takes `{Tab, ?ALL}`; `mnesia:init_iteration/4` calls `rlock_table/3`, which is `rlock(Tid, Store, {Tab, ?ALL})`). The shared grant core does read grant and member authority tables after its account and device guards, as required. The credential/v2 finalizer does take its two outer guards before the inherited `CON-014` migration locks and does read grant, member and pairaccount before its exact grant write. The per-frame contention is correctly recorded as an accepted operational residual with `GATE-04` open and no unmeasured performance claim, and `ADR-947`'s rejection of a cache or dirty read for this increment is well argued. **Residual:** `L-1` — the matrix contains two unnamed cross-operation inversions, and neither `NFR-913` nor `ADR-947` records that Mnesia is deadlock-free by wait-die rather than by the stated order. |
| **L-1** (LOW) — local possession error has no wire carrier | `CON-969` makes it browser-local and requires a control-frame handler | **CLOSED** | `CON-969` and cbcl-bus `REQ-053`/`CON-022` agree: four wire messages, no fifth error slug, hub never sends it. The browser deliverable is real and needed — at the baseline `app.js` has handlers only for `grantchallenge` and `pairaccountgranted` (`onFrame`, `:2264-2282`), and any unrecognised hub frame reaches `renderIncoming(text)` at `:2281`. `CON-022`'s "consumes `pairaccountchallenge` before generic room rendering" is the right placement. The negative fixture claim is accurate: `local-pairing-fixture.json` holds exactly `account`, `bundleB64u`, `devicePublicKeyB64u`, `profileB64u`, `relayOrigin`, `transfer` — no seed. |
| **L-2** (LOW) — browser differential lane reuses Rust WASM | `REQ-982` names one source-disjoint JavaScript canonical implementation | **CLOSED** | The premise is confirmed: `selfsame-application-profile.mjs` states "The Selfsame WASM recogniser remains the authority for CON-201 recognition", and `apps/cbcl_chat/priv/web/selfsame/` is the vendored `selfsame_web_device*` build. `credential-v2-canonical.mjs` does **not** exist at the baseline, so "the browser implementation is ..." is correctly future-tensed, and the in-repo precedent for an honest hand-written JS canonicaliser plus a static vendor check exists (`mls-ds-canonical.mjs`, `selfsame/selfsame-vendor.test.mjs`). `DEC-018` correctly declines to make it a second hub authority. |
| **L-3** (LOW) — coordination metadata is stale | This document, cbcl-bus 0.16.4 and cbcl-pairing 0.4.4 name this review unit | **CLOSED** | cbcl-pairing's whole delta from the 0.4.10-reviewed commit is the two frontmatter lines (`consumer-design: selfsame SPEC-008 0.4.11-draft`, `review-input: ... 0.4.10 Claude adversarial review ...`). cbcl-bus 0.16.4 `conforms-to: Selfsame SPEC-008 0.4.11-draft`, and its `supersedes:` no longer says "in full". Selfsame's test spec enumerates the cbcl-bus tests by number. |

---

# Verification of the coordinated amendments

## cbcl-pairing SPEC-001 0.4.4 — passes on its own terms, unchanged

The candidate differs from the 0.4.10-reviewed commit by exactly two frontmatter
lines. Re-verified against `8e55160`:

- `DisplayIntent`/`DisplayField` are public-field structs and every construction
  is in-crate, so making fields private breaks no external producer;
- `recognise_intent(&self)` becoming `(&mut self)` type-checks at both call
  sites, preserves `dyn` object safety and needs no interior mutability;
- `TEST-055`'s control witnesses are grounded in the real constant
  `MAX_CONTROL_OCTETS = 2_048` (`src/cbcl_protocol.rs:32`), and the 62,000-octet
  credential ceiling is real and enforced in both directions
  (`src/profile.rs:334` on encode, `:920` on decode), comfortably inside
  `MAX_ADJACENT_BODY_OCTETS = 65_536`;
- `CON-021`'s verdict-only verifier still agrees word for word with Selfsame
  `CON-967`.

**No finding in this review is owned by cbcl-pairing**, with one coordination
consequence: `H-1` means Selfsame's flat 29-member core and cbcl-pairing's
closed `AccountTransition` sum have to be reconciled, and if the flat form wins
it is `CON-021` that changes.

## cbcl-bus SPEC-053 0.16.4 and its amended parent — does not pass

Verified sound and implementable against `fdbe414`:

- **`REQ-051`.** A real, checkable ledger; closes 0.4.10 `H-2` (see the
  disposition table). Its one hole is `GATE-04` (**`M-2`**).
- **`REQ-052`.** Correct, and the correct half of 0.4.10 `H-1`. The v1 route and
  bytes stay frozen; the v2 identity is distinct; "No v2 check compares
  `profileVersion` against a global version-1 constant" is the right rule but is
  not carried into a site enumeration (**`M-3`**).
- **`REQ-053`.** Correct. The hub genuinely cannot know a client lacks a key, and
  the four-message wire needs no fifth slug.
- **`REQ-054`.** Correct on multi-handle preservation, on the device-grant
  collision predicate, on making `enroll-or-check/2` return its verdict, on both
  creators, and on both deleters. Wrong on the claim-first ordering (**`H-2`**),
  and silent on the post-migration handle case (**`L-2`**).
- **`NFR-011`.** Lock claims verified correct against OTP source; contention
  correctly declared an open gate. Arithmetic inherits **`M-1`** and **`H-1`**.
- **`CON-020`/`CON-021`/`CON-022`.** Implementable. `CON-021`'s conditional
  dominance statement is the right shape; `CON-022`'s static import test has an
  in-repo precedent.
- **`ADR-020`/`ADR-021`/`DEC-018`.** Sound, and `ADR-021` states the revocation
  transition honestly instead of promising permanence.
- **`GATE-04`.** Defective as an effective gate (**`M-2`**). It correctly closes
  none of its own boxes.

The parent `SPEC-053-key-root-identity` change is a candidate-amendment pointer
bump and introduces nothing further.

---

# Residual assumptions and open gates — *not* design blockers

Recorded so they are not mistaken for findings and so `GATE-04`'s reviewers have
them in one place.

- **R-1 — `maxDevicesPerAccount` still does not exist.**
  `ratified-max-devices-per-account/0` returns the atom `'unratified`, so
  `path-b-max-devices/0` returns `#(error unratified-device-limit)` and the
  capped binder is unreachable in production. `NFR-913`'s "the recognized
  `maxDevicesPerAccount`" is inert until GATE-01's CON-002 box closes — and
  `M-2` has removed the GATE-01 linkage from the effective cbcl-bus gate.
- **R-2 — the compiled conformance allowlist is still in the code.**
  `RelayPolicy.approved_conformance: &'a [[u8; 32]]` and
  `cbcl_relay::eligible` require descriptor membership in a build-time digest
  list (`cbcl_relay.rs:5-13,85-92`). Adopted `CON-933` forbids exactly this
  ("Neither role uses membership in a compiled digest registry"), so its removal
  is implementation work the coordinated text requires but nowhere schedules.
- **R-3 — challenge-table aggregate RAM is still bounded per connection only.**
  `REQ-045` caps two live challenges per (connection, verified handle) and
  expires within 120 s, but TOFU makes connections free and no clause states a
  global ceiling or a restart reaper. `cbcl-chat-grant-nonce` has the same shape
  *plus* `cbcl-chat-grant-nonce-reaper`, which is the pattern to copy. Unchanged
  from 0.4.10; not closed by 0.4.11.
- **R-4 — post-commit authority depends on continuous resolver availability.**
  After the irreversible commit the legacy key is dead and standing runs through
  `cbcl-chat-path-b-resolver`, which fails closed. A did-crdt outage is a total
  lockout with no legacy fallback, by design.
- **R-5 — `authority-busy` has no declared surface.** `NFR-913`/`NFR-011` make
  retry exhaustion "a closed `authority-busy` result", but no clause says what a
  per-frame authority read does with it. The existing consumer
  (`cbcl-core-enrol`) proceeds only on literal `'false`, so any non-`false` value
  refuses the frame. That is correct fail-closed behaviour and should be stated
  as the intended one.
- **R-6 — `mnesia:transaction/1` defaults to unbounded restarts.** Bounded
  retries require `mnesia:transaction/2,3`; the amendment's "bounded retries" is
  therefore a real code change at every listed operation, not a description of
  current behaviour.
- **R-7 — CPace and the blind-relay bounds were re-checked and are unchanged.**
  `cpace.rs` is draft-21 `CPace255`; `x25519_scalar_mult_vfy` rejects the neutral
  element in constant time (`ct_eq`), and `calculate_generator` clears the high
  bit before elligator2. `MAX_CONTROL_OCTETS = 2_048` and
  `MAX_ADJACENT_BODY_OCTETS = 65_536` are unchanged and the 62,000-octet
  credential member fits both. No new contact with the CPace transcript, the
  PROTO-004 envelope, or SPEC-075 relay opacity was introduced by 0.4.11; they
  were checked for absence of new contact, not re-audited.
- **R-8 — the compromised-Path-A-key takeover remains an accepted, gated risk**,
  and `M-2` removes the human-review box that gated it on the cbcl-bus side.
- **R-9 — removing `store-verified-device-grant/6` breaks roughly ten existing
  test call sites** in `cbcl-chat-account-grants-tests.lfe` and
  `cbcl-chat-account-roommember-tests.lfe`. Bounded work, named so it is not a
  surprise.
- **R-10 — `credential-v2-canonical.mjs`, the v2 profile route, the v2 provider
  hint recogniser, the `cbcl-member` `pubkey` index, both guard tables and the
  browser `pairaccountchallenge` handler are all absent at the baselines.** That
  is correct for a design-only increment; it is recorded so no reader mistakes
  the specification's present tense for deployed behaviour.

### SPEC-007 REQ-812 audit

`REQ-812` ("Mismatch, protocol violation, cancellation, relay closure, and
expiry SHALL produce no accepted credential or identity side effect before
approved payload delivery. After approved delivery, only the standing CON-204
provisioning and compensation effects are permitted before CON-206 returns
acceptance") is an inherited unwaivable hard stop and is **not breached** by
0.4.11. The three effects the amendment adds before final approval are the
pending scope/ceremony pair (deleted on decline, expiry or pre-commit failure,
adopted `REQ-970`), the RAM-only pairaccount challenge (`CON-016`, no durable
authority), and a browser-local device seed (`CON-966`/`CON-969`, neither a hub
nor a wallet mutation). `CON-962`'s order places every binding, grant, alias,
membership and room deletion after signed final approval and verified
publication. `H-2`'s lockout arises from the *adjacent* SPEC-076 claim path, not
from a credential/v2 pre-approval effect, so it is not a `REQ-812` breach either.

---

# What would make this pass

1. **Selfsame `REQ-982`/`CON-970`** — close `H-1`: decide whether the offer core
   is 23 members with a nested `accountTransition` (cbcl-pairing `CON-021`'s
   shape, and the only one with a `NoTransition` encoding) or 29 flat members;
   ledger the disposition of 0.4.8 `CON-960` explicitly; give `accountDid` a
   source, a grammar and a cap or remove it; and re-derive the core total over
   the chosen shape.
2. **cbcl-bus `REQ-054`/`CON-021`/`TEST-067` plus Selfsame `REQ-983`/`TEST-1110`**
   — close `H-2`: either state that a key with an unratified pairaccount row is
   permanently ineligible for member enrolment and delete the claim-first
   recovery sentence and its test arm, or specify the ratification/revocation
   surface for `cbcl-pairaccount-grant` as a SPEC-076 amendment.
3. **Selfsame `REQ-982` plus cbcl-bus `NFR-011`** — close `M-1`: one JWK value
   used in both derivations, `deviceKeyJwk` charged once, `CON-945` cited as the
   hint's member source, escaped-form caps (or an explicit
   no-escapable-character grammar) for every string member in a canonical total,
   and one unit for `migrationRooms`.
4. **cbcl-bus `GATE-04`/`REQ-051` item 10** — close `M-2`: restate every
   surviving box, or make this `GATE-04` supplement rather than replace.
5. **Selfsame `REQ-981`/`TEST-1112`** — close `M-3`: enumerate the non-statement
   v2 surfaces (`profile.rs:385`, `provider_hint.rs:115`, `ceremony.rs`'s
   `payloadVersion` literal) and make the static test reject a
   `crate::PROFILE_VERSION` comparison reachable from any v2 entry point.
6. **The four lows** — `L-1` (name wait-die and the two inversions), `L-2`
   (post-migration handle change), `L-3` (`V2_MAX_PAYLOAD_OCTETS` naming), `L-4`
   (ledger the display-grammar narrowing). Each is a bounded clause change with a
   single named owner.

None of these is architectural. `H-1` and `H-2` would each block on their own;
`M-1` and `M-2` block because the amendments make their own generator gate and
release gate normative.

---

# Authority statement

**This review confers no implementation, allocation, enablement, release or
deployment authority whatsoever, and a PASS from a future review would not
either.** Specifically:

- The verdict is **REJECT**; the coordinated set is not approved.
- Even on a later PASS, the adopted 0.4.10 sequencing holds: a PASS authorizes an
  Elephant SPL/test-first plan and nothing else.
- `GATE-04` remains no-go with every box open — including the boxes `M-2` shows
  are currently missing from the cbcl-bus text and which must be restored before
  they can be assessed at all. `GATE-01` and `GATE-03` remain required and are
  not addressed here; note `R-1`, which makes GATE-01's CON-002 box a
  precondition of the device-grant path working at all. Production credential/v2
  allocation stays closed, both Tier-1 source constants stay closed, and human
  cryptography/migration/privacy review, independent CPace evidence, both
  durability gates, owner approval and separate deployment confirmation all
  remain outstanding.
- This review changed no implementation code, candidate spec, test, manifest,
  configuration, or sibling repository. It adds exactly this one file.
