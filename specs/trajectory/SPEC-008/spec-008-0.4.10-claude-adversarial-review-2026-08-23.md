---
id: spec-008-0.4.10-claude-adversarial-review-2026-08-23
title: Tier-1 adversarial review — SPEC-008 0.4.10 coordinated standalone pairing
status: final
tier: 1
kind: review
verdict: REJECT
reviews: "[[spec-008-0.4.10-standalone-tofu-first-contact]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.3-draft and its amended parent SPEC-053-key-root-identity"
severity-counts: "0 critical, 2 high, 4 medium, 3 low"
driver: Claude Opus 5 (claude-opus-5), fresh-context cross-model Circus reviewer, defect-finding mandate
date: 2026-08-23
---

# Tier-1 adversarial review — SPEC-008 0.4.10 coordinated standalone pairing

## Model and driver context

- **Driver model:** Claude Opus 5 (`claude-opus-5`), Circus fresh-context Tier-1
  reviewer, defect-finding mandate only. No drafting, no implementation, no
  gate-closing authority.
- **Working tree:** `/Users/anuna-01/Code/selfsame-spec008-v0410-review-3`
  (worktree of `selfsame`, branch `circus/spec008-v0410-review/3`).
- **Sibling repositories read at their own checkouts:**
  `/Users/anuna-01/Code/cbcl-pairing`, `/Users/anuna-01/Code/cbcl-bus`,
  `/Users/anuna-01/Code/did-crdt`.
- **Method:** every clause that names a module, function, record, index,
  constant, route or wire member was located in source (or shown absent) at the
  declared baselines. Prose, comments and disposition tables were treated as
  unverified assertions. Where a Mnesia or Rust semantic is load-bearing, the
  OTP/crate source was read rather than recalled.

### Reviewed commits

| Repository | Candidate commit | Declared implementation baseline | Verified relationship |
|---|---|---|---|
| selfsame | `e3479b664cd90c7427994f3e3d7d52c63dba1b92` | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | ancestor; diff touches only `specs/` |
| cbcl-pairing | `c9c7d5cfe27eed6252072b3b2c99b436978860eb` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | ancestor; diff is 6 files, all under `specs/trajectory/SPEC-001/`; **byte-identical to the commit reviewed at 0.4.9** |
| cbcl-bus | `2eb526c4b853824b8a1b0d62c71ea8713d118203` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | ancestor; diff is 4 files, all under `specs/`; adds `spec-053-0.16.3-monotonic-device-authority.md` and a 2-line parent pointer bump |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | same | candidate *is* the baseline (uncommitted `specs/did-crdt-spec.md` edits in that tree were **not** reviewed) |

Every candidate is spec-only relative to its declared code baseline, so the
checked-out worktrees hold exactly the code the amendments claim to be written
against. All code citations below are at those baselines.

### Documents read

Selfsame SPEC-008 0.4.0 through 0.4.10 and every intervening adversarial
review; `pairing-completion-runbook.md`; `req-909-adversarial-review-2026-08-22.md`;
`HANDOFF-pairing-integration.md`; SPEC-004 (CON-201/206/209/211/214/219/220/221/226,
REQ-215), SPEC-002 REQ-103, PROTO-004 CON-502/503, SPEC-007 CON-806.
cbcl-pairing SPEC-001 0.3.0, 0.4.0–0.4.4. cbcl-bus `SPEC-053-key-root-identity`
(amended parent), SPEC-053 0.16.1/0.16.2/0.16.3, `SPEC-076-pairing-account-grant`,
SPEC-075 REQ-007, GATE-00/01/03/04.

---

# Verdict: REJECT

**0 critical, 2 high, 4 medium, 3 low.**

The standalone first-contact architecture remains sound and is not the reason
for this verdict. The consent ordering in adopted `CON-962` is intact. The
`C-1` repair is the right shape and, on the code, actually works: requiring an
inner Ed25519 proof by the claimed key removes the third-party brick entirely,
and making a verified grant or enrolled member dominate `unratified-device?`
un-bricks the live production exposure the 0.4.9 review flagged. `M-2` is
closed cleanly. `M-3`'s hardest part — naming the exact lock key — is closed
exactly right: `cbcl-device-grant.account` is a real binary field holding the
same account DID `cbcl-account-binding` holds, and `store-device-tx/7` is
already private while `store-verified-device-grant/6` genuinely has no
production caller.

The verdict turns on two effective-text defects that the repairs create, and on
four bounded but release-blocking implementability gaps:

1. **`H-1`: the evidence-version repair silently re-anchors credential/v2 to
   the live version-1 profile and applicationId.** The adopted, unreplaced
   0.4.5 `REQ-957`/`CON-947`, 0.4.6 `REQ-962`/`CON-951` and 0.4.7 `CON-957`
   require credential/v2 to fetch a *separate* profile document at the distinct
   ApplicationId `https://chat.anuna.io/selfsame/application/v2`. `REQ-977` and
   cbcl-bus `REQ-048` now bind credential/v2 evidence to the live v1 document
   and its pinned digest. `CON-968` clause 1's blanket rewrite of "every
   `profileVersion = 2` discriminator" additionally converts a *profile
   document* version into a statement member, which `CON-951`'s branch table
   cannot express. The set now gives two incompatible answers about the
   ceremony's trust anchor.

2. **`H-2`: cbcl-bus 0.16.3 supersedes 0.16.2 "in full" and does not restate
   the clauses the coordinated design depends on** — `REQ-041`, `REQ-042`,
   `REQ-044`, `CON-013`, `CON-014`, `CON-015`, `NFR-009`'s continuity list, and
   `TEST-048`–`TEST-056` — while its own `CON-018`/`NFR-010` and Selfsame's
   effective `REQ-974`/`REQ-975`/`CON-965` cite them by name. On its face this
   deletes the dispatch-before-authorization gate and the atomic migration
   contract that closed 0.4.8 `H-1` and `C-1`.

Both are ledger defects with bounded corrections. Neither is an architectural
objection.

---

# Findings

## H-1 (HIGH) — `REQ-977`/`REQ-048` re-anchor credential/v2 to the live version-1 profile and applicationId, but 0.4.5 `REQ-957`/`CON-947`, 0.4.6 `REQ-962`/`CON-951` and 0.4.7 `CON-957` are adopted, unreplaced, and require the opposite; `CON-968` clause 1's blanket rewrite makes `CON-951`'s credential-v2 branch inexpressible

**Owner: Selfsame** (`CON-968`, `REQ-977`), coordinated into **cbcl-bus**
(`REQ-048`, the `CON-947`/`CON-957` generator outputs).

### What 0.4.10 says

`REQ-977`:

> The live CON-220 application profile, its `profileVersion = 1`, its canonical
> octets, and its pinned digest remain byte-for-byte unchanged. Credential/v2 is
> identified by the statement-local `evidenceVersion = 2`; its statement carries
> `profileVersion = 1` **and the exact live profile digest**.

`CON-968` clause 1:

> Every `profileVersion = 2` credential/v2 discriminator in adopted text is
> replaced by `evidenceVersion = 2`; `profileVersion` and the live profile stay 1.

cbcl-bus `REQ-048` repeats it: "Credential/v2 evidence carries
`evidenceVersion = 2`, `profileVersion = 1`, and that exact digest… No profile
re-ratification, digest change, `PROFILE_VERSION` widening, or deployed CON-219
migration is part of this increment."

### What the adopted set still says

0.4.10's `adopts:` is "All 0.4.4 through 0.4.9 clauses except the exact
replacements and corrections named by this amendment." Nothing in `CON-968`
names any of the following, and no 0.4.7/0.4.8/0.4.9 ledger item replaces them
(verified by grep across 0.4.7–0.4.10 for `CON-947`, `CON-951`, `REQ-957`,
`REQ-962`, `CON-957`, `application/v2` — 0.4.9 and 0.4.10 have **zero** hits):

- **0.4.5 `REQ-957`:** "Credential/v2 SHALL publish its conforming version-2
  profile at the distinct canonical ApplicationId
  `https://chat.anuna.io/selfsame/application/v2`. The carrier, live fetch,
  embedded browser bytes, permissions, offer, policy pair, and vectors all name
  that exact v2 identifier."
- **0.4.5 `CON-947`:** two named immutable generator outputs — the frozen v1
  compatibility bytes and "one conforming version-2 output for
  `https://chat.anuna.io/selfsame/application/v2`, used only by credential/v2."
- **0.4.6 `CON-951`** branch table, row 3: "credential-v2 | the exact nine-member
  v2 grammar in 0.4.4 `CON-933` at the distinct ApplicationId
  `https://chat.anuna.io/selfsame/application/v2`" and "The v2 branch requires
  `profileVersion = 2` and all 0.4.4 `CON-933` nested rules."
- **0.4.6 `REQ-962`:** "Credential/v1 SHALL NOT fetch or use the distinct
  credential/v2 ApplicationId. The v2 branch remains exclusive to credential/v2."
- **0.4.7 `CON-957`:** "the cbcl-bus deployment generator owns only the frozen
  cutover-v1 **and distinct credential/v2 outputs**."

### Why this is substantive, not editorial

**(a) `CON-968` clause 1 cannot be applied.** `CON-951`'s and `CON-933`'s
`profileVersion = 2` is a member of the *profile document*, not of a statement.
Rewriting it to `evidenceVersion = 2` produces a profile-document branch keyed
on a member no profile has. The v2 branch of the dispatcher — and with it
`CON-957`'s deterministic branch-attribution precedence and its CON-226 corpus
fixtures — becomes unrecognisable. The 0.4.9 review's `H-1` was filed on the
premise that "one application cannot serve both" documents; that premise does
not hold for two applicationIds, which is exactly what the adopted set
specified.

**(b) The trust anchor now has two values.** `enrollment::verify`
(`crates/selfsame-app-identity/src/enrollment.rs:551-556, 588-595`) compares
`statement.application_id` and `statement.profile_digest` against **both** the
fetched profile and the offer. Under `REQ-977` the wallet must fetch
`https://chat.anuna.io/selfsame/application`; under `REQ-957`/`CON-947` it must
fetch `.../application/v2`. An implementer cannot pick one from the effective
text, and the choice determines what `F-A`'s exact `(applicationId,
relayOrigin)` TOFU record keys on, what `F-B` displays, and which
`accountScopeId` namespace `CON-211` derives in.

**(c) The `F-A` prompt behaviour changes without being re-tested.** Under
0.4.6's distinct v2 identity, credential/v2's first contact is by construction a
*new* application and always prompts. Under `REQ-977` it is the same
applicationId the deployed credential/v1 cbcl-pairing path already uses with the
same relay: the live profile at
`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-gate.lfe` decodes to
`"applicationId":"https://chat.anuna.io/selfsame/application"` with
`"cbclPairingRelays":[{…,"relayOrigin":"https://chat.anuna.io:9443",…}]`. A
person who already TOFU'd that exact pair gets **no new-application prompt** for
credential/v2. The two explicit migration decisions still stand, so this is a
change in the design's own stated behaviour rather than a consent hole — but
0.4.10 neither states it nor re-tests `F-A` against it.

**(d) The v2 permission namespace silently collapses.** `enrollment::verify`
requires `statement.requested_permissions` to be an exact subset of
`observed.profile.allowed_permissions`. The live document's
`allowedPermissions` is exactly four strings —
`https://chat.anuna.io/selfsame/application#{channel-join,chat-read,chat-send,mls-commit}`.
`REQ-977` forbids changing those bytes, so credential/v2 may request only those
four. `CON-933`'s v2 grammar gave v2 its own `allowedPermissions`,
`accountAuthority`, `verifierAudience`, `stateResolvers` and `revocation`; all
of that is now the live v1 application's, unstated.

### Minimum sound correction

Selfsame `CON-968` must ledger this explicitly, one way or the other:

- **Option A (smaller change to the effective set):** retain the distinct v2
  profile at `.../application/v2` with document `profileVersion = 2`, and scope
  `CON-968` clause 1 to *statement* discriminators only — the v2 statement then
  carries `evidenceVersion = 2` and the v2 document's `profileVersion`/digest.
  This is available because a second applicationId is a new document, never a
  re-ratification of the live one, so `REQ-976`/`ADR-944`'s deployed-CON-219
  protection is untouched. `enrollment::verify`'s `crate::PROFILE_VERSION`
  comparison must then become a per-branch value, which `CON-951` already owns.
- **Option B:** explicitly replace `REQ-957`, `CON-947`, `CON-951`'s
  credential-v2 row, `REQ-962`'s v2-exclusivity sentence and `CON-957`'s v2
  generator output; state that credential/v2 authenticates against the live v1
  applicationId and pinned digest; restate `F-A`'s prompt behaviour for an
  already-TOFU'd pair; constrain v2 `requestedPermissions` to the live four; and
  reduce `CON-951` to two branches with the corresponding CON-226 corpus change.

Either way the corresponding cbcl-bus generator clause (`CON-947`/`CON-957`) and
`REQ-048` must move with it.

---

## H-2 (HIGH) — cbcl-bus 0.16.3 declares 0.16.2 superseded "in full" but restates neither the dispatch-before-authorization gate nor the atomic migration contract, while its own body and Selfsame's effective text depend on them by name

**Owner: cbcl-bus** (`spec-053-0.16.3-monotonic-device-authority` frontmatter and
body), consequentially Selfsame `REQ-974`/`REQ-975`/`CON-965`/test spec.

### The frontmatter

```yaml
supersedes: "[[spec-053-0.16.2-credential-v2-authority-corrections]] in full;
             adopts its architecture only through the corrected clauses below"
```

That is the same phrase 0.16.2 used about 0.16.1 — and 0.16.2 honoured it: it
restated the *whole* design as `REQ-041`–`REQ-044`, `NFR-009`, `CON-013`–`CON-015`,
`ADR-018`, `DEC-016`, `GATE-04` and `TEST-048`–`TEST-056`, so nothing was left
stranded in 0.16.1. The phrase therefore carries a settled meaning in this
trajectory: the prior document is gone.

### What 0.16.3 restates, and what it drops

0.16.3 contains `REQ-045`–`REQ-050`, `NFR-010`, `CON-016`–`CON-019`, `ADR-019`,
`DEC-017`, `GATE-04` and `TEST-057`–`TEST-064`. It restates **none** of:

| 0.16.2 clause | What it carries | Restated in 0.16.3? |
|---|---|---|
| `REQ-041` | credential/v2 authority is a device grant; the migration/finalizer | no |
| `REQ-042` | **credential/v2 WS kinds gate before verification and TOFU**; the six client + six hub kinds; `hub-originated?` membership | no |
| `REQ-044` | the two named source-constant gate overlays and two-artefact CI separation | no |
| `CON-013` | the canonical digest graph and per-party recomputation table | no |
| `CON-014` | the one atomic device-grant migration transaction and recoverable status | no |
| `CON-015` | closed dispatch classes and the two-artefact build boundary | no |
| `NFR-009` | bounds, indexes, continuity list, invite/provenance/epoch exclusions | partially, by *reference* only |
| `TEST-048`–`TEST-056` | every credential/v2 gate test | no |

### Why this is a defect and not a reading preference

0.16.3's own body depends on the document it deletes:

- `NFR-010`: "…and the **exact 0.16.2 continuity list**."
- `CON-018`: "The credential/v2 finalizer retains the **complete 0.16.2 atomic
  migration order** inside these outer guards."

And Selfsame's effective text depends on it by clause number:

- adopted 0.4.9 `REQ-974`: "The wallet adopts **cbcl-bus 0.16.2 REQ-042/REQ-044
  and CON-015**." That is the 0.4.8 `H-1` repair — the single most important
  structural correction of the last two rounds.
- adopted 0.4.9 `REQ-975`: "The wallet and browser adopt **cbcl-bus 0.16.2
  CON-013** byte-for-byte."
- adopted 0.4.9 `CON-965` clause 1: "…replaced by REQ-973/CON-962 and **cbcl-bus
  REQ-041/CON-014**."
- 0.4.10 test spec: "cbcl-bus **TEST-043 through the new 0.16.3 tests** are
  mandatory" — a range that spans 0.16.1 (`TEST-043`–`TEST-047`) and 0.16.2
  (`TEST-048`–`TEST-056`), both declared superseded in full.

So the coordinated cbcl-bus half is not determinate: on one reading the
pre-authorization dispatch gate, the closed kind sets, the two-artefact
production/fixture separation, the digest graph and the atomic migration
transaction are all effective; on the other they have no text at all. This is
precisely the class of defect the 0.4.8 `L-2` and 0.4.9 `M-2` findings
established the correction ledgers to prevent.

### Minimum sound correction

cbcl-bus owns it, and it is a frontmatter plus one-paragraph change: replace the
"in full" clause with the Selfsame-style pair that 0.4.10 uses —
`supersedes: 0.16.2` plus `adopts: "all 0.16.1 and 0.16.2 clauses except the
exact replacements named below"` — and add an explicit effective-text ledger
naming which 0.16.2 clauses this amendment replaces (`REQ-043`'s v1 freeze is
replaced by `REQ-048`; `NFR-009`'s bound arithmetic by `NFR-010`; `GATE-04` by
the new `GATE-04`) and which survive unchanged (`REQ-041`, `REQ-042`, `REQ-044`,
`CON-013`, `CON-014`, `CON-015`, `TEST-048`–`TEST-056`). Selfsame's citations
should then be re-pointed at the surviving clause numbers rather than at "0.16.2".

---

## M-1 (MEDIUM, release-blocking) — `CREDENTIAL_V2_EVIDENCE_JWS = 4,000` and the 56,000-octet core cap have no fixed point: the v2 statement inherits `MAX_PERMISSIONS = 64` and a URI grammar with **no length bound**, so `REQ-979`'s mandated "every legal field at its declared maximum" generator gate cannot pass

**Owner: Selfsame `REQ-979`/`NFR-912`, jointly with cbcl-bus `NFR-010`.**

### What is required

`REQ-979`:

> ```
> 56,000  canonical OfferCoreV2
>  4,000  complete compact JWS under CREDENTIAL_V2_EVIDENCE_JWS
>  2,000  aggregate of every other encoded contribution
> ```
> The generator encodes every legal field at its declared maximum and records the
> exact subtotal and residual. **If any legal maximum makes either subtotal or the
> 62,000-octet total fail, the manifest fails; prose estimates cannot waive it.**

cbcl-bus `NFR-010` repeats the arithmetic verbatim, and `TEST-1106`/`TEST-061`
make it release-blocking.

### The declared maxima the v2 statement inherits

0.4.4 `CON-939` — adopted and unreplaced — says of the v2 evidence: "That
evidence carries the parent fields plus `accountPrincipalDigest`,
`applicationAccountDisplay`, `carrierDigest`, `transcriptHash`, and
`devicePossessionProof`" and "The inherited application ID, JWK, DID,
**permission**, timestamp, platform, and **return-URI** grammars remain exact."
Those inherited grammars are:

- `crates/selfsame-app-identity/src/profile.rs:76` —
  `pub(crate) const MAX_PERMISSIONS: usize = 64;`, applied to statements at
  `enrollment.rs`: `if permission_items.is_empty() || permission_items.len() >
  crate::profile::MAX_PERMISSIONS { … }`.
- `crates/selfsame-app-identity/src/uri.rs` — `recognise_host` bounds the host at
  253 octets and `recognise_port` at 5 digits, but `recognise_path` and
  `recognise_pchars` apply **no length bound at all** to the path or the
  fragment. A single `requestedPermissions` entry, `applicationId` or `returnUri`
  is therefore bounded only by the enclosing JSON `Limits.max_bytes`.
- `applicationAccountDisplay` is "1..128 Unicode scalar values after NFC"
  (0.4.4 `CON-939`) — up to 512 UTF-8 octets.

### Why the gate cannot pass

`STATEMENT_LIMITS` is `Limits { max_bytes: ENROLLMENT_JWS.max_octets, … }`
(`enrollment.rs:705-706`), i.e. the JSON bound *is* the JWS bound. Declaring
`CREDENTIAL_V2_EVIDENCE_JWS.max_octets = 4,000` makes the v2 statement's only
per-member ceiling the 4,000-octet total itself. "Encode every legal field at its
declared maximum" then has no solution: the maximum of each variable member is
whatever is left after the others, and 64 permissions at *any* realistic length
(the live profile's own permission strings are ~55 octets each — 64 of them is
~3,650 octets) already exceeds the ~2,830-octet payload budget that a
4,000-octet compact JWS leaves after the protected header, the base64url
expansion and the 64-octet signature.

The same defect is present one level up: `recognise_offer` bounds an offer at
`MAX_PAYLOAD_OCTETS = 69_607` (`ceremony.rs:64,283`), above the design's own
56,000 core cap, and `OfferCoreV2` carries the same unbounded-length URI members
plus `migrationRooms`. 56,000 is likewise not derivable from declared maxima.

This is the 0.4.9 `M-1` recurring one level down: the arithmetic moved from
"contradicted by a deployed constant" to "unsatisfiable because the constants it
budgets against do not exist."

### Minimum sound correction

Declare the v2-specific maxima the arithmetic needs, in the coordinated set,
before the generator is written: an explicit v2 permission-count ceiling and a
per-permission octet ceiling; explicit octet ceilings for `applicationId`,
`returnUri`, `providerId`, `platformBindingId` and
`applicationAccountDisplay`; and a v2 `MAX_PAYLOAD_OCTETS` equal to the 56,000
core cap. Then re-derive 4,000 and 2,000 from those numbers and show the sum, so
the assertion is checkable before implementation rather than at manifest time.
`REQ-979` must cite each constant it budgets against by name, as the 0.4.9
review already asked of `ENROLLMENT_JWS`.

---

## M-2 (MEDIUM) — `REQ-047`'s new `enroll-or-check/2` rule is ambiguous; the global-key-uniqueness reading breaks the deployed handle-change flow, and the handle-scoped reading makes the mandated member-pubkey-index check vacuous

**Owner: cbcl-bus `REQ-047`/`CON-017`**, mirrored in Selfsame `REQ-978`
("New `cbcl-member` enrollment writes also take the device guard and check both
authority indexes").

`REQ-047`:

> `cbcl-chat-members:enroll-or-check/2` takes the same device-authority guard
> before an absent-row enrollment write. It checks **both authoritative pubkey
> indexes** under that lock. Existing exact handle/key enrollment remains
> idempotent; **an incompatible device grant or enrolled handle refuses**.

"an incompatible … enrolled handle" has two readings, and the amendment picks
neither.

**Global-key-uniqueness reading** (the one "checks both authoritative pubkey
indexes" implies): a TOFU enrolment refuses when the key already appears in the
`cbcl-member` pubkey index under a different handle. That is a **behaviour
change to a deployed, user-reachable flow**. `apps/cbcl_chat/priv/web/app.js`
carries one wire seed per browser and lets the person edit their handle at will:

```js
$('handle').addEventListener('change', (e) => {
  …
  id.handle = h; e.target.value = h;         // same id.pubB64, new handle
  localStorage.setItem('cbcl_handle', h);
```

The next `hello` TOFU-enrols the new handle with the *same* public key
(`app.js:1066` sets `id = { handle, priv, pubB64, … }`, and the key is only
rotated when the seed changes, `:1050`). Two `cbcl-member` rows sharing a pubkey
is therefore a normal deployed state, and refusing it silently breaks handle
changes.

**Handle-scoped reading** (today's `#(error handle-key-mismatch)`): then the
`cbcl-member` pubkey index contributes nothing to the check, and the clause's own
justification — `GATE-04`'s "every new member enrollment serializes under the
device guard and cannot race a pairaccount claim into an authoritative-key
lockout" — is served entirely by the device guard, not by the index read.

Two further facts the clause has to survive:

- `cbcl-member` has **no** `pubkey` index at the baseline
  (`cbcl-chat-members.lfe:17-24` declares `attributes`, `type`, `record_name`
  and the storage kind only), so the index is a real new migration, correctly
  named in `REQ-047`/`NFR-912`.
- `enroll-or-check/2` today **discards its write transaction's result** and
  returns `'ok` unconditionally:

  ```lfe
  (#(error not-enrolled)
   (mnesia:transaction (lambda () (mnesia:write 'cbcl-member … 'write)))
   'ok)
  ```

  so "refuses" requires restructuring it into one transaction whose verdict
  reaches `enrol-commit`. The amendment should say so, because the current shape
  would satisfy a literal reading of "takes the guard and checks the indexes"
  while still accepting.

### Minimum sound correction

State which key-collision predicate refuses. The invariant `REQ-046` needs is
served by refusing an enrolment whose key already has a **`cbcl-device-grant`**
row (an authority confusion between a Path-B device key and a Path-A chat
identity) while leaving multi-handle sharing of a plain enrolled key permitted —
and say explicitly that the existing multi-handle behaviour is preserved.
Additionally require `enroll-or-check/2` to become a single transaction that
returns its verdict, and add the deployed handle-change case to `TEST-059`.

---

## M-3 (MEDIUM) — the guard/cap/index discipline covers both grant *creators* but neither grant *deleter*, and `REQ-978`'s "for all time" monotonicity rests on a row `revoke-observed/2` and `revoke-device-grant/2` delete

**Owner: cbcl-bus `REQ-046`/`REQ-047`/`NFR-010`**, mirrored in Selfsame
`REQ-978`/`NFR-912`.

`REQ-978` states the invariant as unconditional:

> Once a raw device key has verified grant or enrolled-member authority, no new
> pairaccount claim can target it. An old claim can never make that key fail
> ordinary per-frame resolution. … Device authority obeys this invariant **for
> all time**, not merely at two pre-commit instants.

`REQ-047` names "every device-grant **creation** path" and disposes of the two
creators correctly. `cbcl-chat-account-grants.lfe` has two more writers of the
same table, and neither amendment mentions them:

```lfe
(defun revoke-observed-tx (account ids)
  (let ((rows (mnesia:foldl … '() 'cbcl-device-grant)))   ; whole-table read lock
    (lists:foreach (lambda (rec) (mnesia:delete 'cbcl-device-grant …)) rows)
    …))

(defun revoke-device-grant (account pubkey)
  (mnesia:transaction (lambda () (mnesia:delete 'cbcl-device-grant (device-key account pubkey) 'write))))
```

Three consequences:

1. **The invariant is conditional on the row.** After `revoke-observed/2`
   applies a resolver-derived revocation union, `unratified-device?` falls
   through to the pairaccount index again. For a key with a pre-amendment
   pairaccount row this re-activates the inert state. The *observable* outcome
   is unchanged — a revoked key is refused by `account-bound` anyway, because
   `path-b-issued?` reads the same deleted row — but "for all time" is not what
   the mechanism provides, and `GATE-04`'s "no other party, old pairaccount row,
   race, restart, or replay can change any device-grant or enrolled-member
   standing" box cannot be closed against a statement that is false as written.
   (`cbcl-member` has **no** delete path — `cbcl-chat-members` exports only
   `ensure-table/0`, `enroll-or-check/2`, `pubkey/1`, `pubkey-tx/1` — so the
   enrolled-member arm *is* permanent. The two arms differ and the amendment
   treats them identically.)
2. **Index maintenance is unstated for the deleters.** `NFR-912` requires
   `account` and `pubkey` indexes on `cbcl-device-grant`; Mnesia maintains them
   on delete, but `TEST-1105`/`TEST-059`'s "index parity with a reference scan"
   must exercise the revocation paths, and neither test names them.
3. **They are outside the stated lock order** (see `M-4`).

### Minimum sound correction

Restate the invariant as "while a verified device grant or enrolled-member row
exists", state the post-revocation consequence explicitly (the key is refused by
ordinary resolution regardless, and an old pairaccount row becomes visible
again), bring `revoke-observed/2` and `revoke-device-grant/2` into the device
guard and the stated lock order, and add revocation to the index-parity and
authority-continuity tests.

---

## M-4 (MEDIUM) — the "global acquisition order" omits the whole-table read lock that `mnesia:index_read` actually takes, so the order it states is not the order that conflicts

**Owner: cbcl-bus `REQ-047`/`NFR-010`**, mirrored in Selfsame `NFR-912`.

`NFR-912`/`REQ-047` state the order as: account guard → pairaccount handle guard
→ device authority guard → member/device-grant pubkey indexes, grant account
index, exact row → pairaccount indexes, and require "exact lock-order evidence"
in `TEST-1105`/`TEST-059`.

At OTP 28.5 / mnesia 4.25.3, `mnesia:index_read/3` inside a transaction does not
take record locks — it takes a **read lock on the whole table**:

```erlang
%% mnesia_index.erl
read(Tid, Store, Tab, IxKey, Pos) ->
    ResList = mnesia_locker:ixrlock(Tid, Store, Tab, IxKey, Pos),
%% mnesia_locker.erl
ixrlock(Tid, Store, Tab, IxKey, Pos) ->
    … case need_lock(Store, Tab, ?ALL, read) of …
grant_lock(Tid, {ix_read,IxKey,Pos}, Lock, Oid = {Tab, _}) ->
    Res = ix_read_res(Tab, IxKey, Pos), set_lock(Tid, Oid, Lock), {granted, Res, [?ALL]};
```

Three consequences the design does not account for:

1. **The per-key guards are not what excludes distinct-key writers.** Two grant
   writers for *different* accounts pass different account guards, then both take
   the `?ALL` read lock on `cbcl-device-grant` for the indexed count, then both
   want a record write lock on that table — a lock upgrade in both directions, so
   Mnesia detects the cycle and aborts one with `{aborted, cyclic}`. Correctness
   is preserved (`mnesia:transaction/1` restarts), but the stated order neither
   predicts nor prevents this, and the "phantom insert" protection the amendment
   attributes to the guards is in fact supplied by the table lock.
2. **`revoke-observed-tx`'s `mnesia:foldl` (table read lock) plus record deletes
   is the same upgrade shape**, against a writer the order does not name (`M-3`).
3. **`unratified-device?` becomes a hub-wide serialiser.** `REQ-046` makes it
   read the device-grant and enrolled-member pubkey indexes *inside its
   transaction*, on every authenticated frame — `selector-facts`
   (`cbcl-chat-session-ws.lfe:347`) and `account-standing-facts` (`:385`) both
   call it. Every frame from every connection therefore takes `?ALL` read locks
   on `cbcl-device-grant` and `cbcl-member`, blocking every writer of those
   tables — including the TOFU enrolment that runs on each new handle's first
   frame. The pattern pre-exists for `cbcl-pairaccount-grant`; `REQ-046` extends
   it to two more tables, one of which is hot, and neither amendment says so.

### Minimum sound correction

Name the `?ALL` table read lock explicitly in the stated order, so
`TEST-1105`/`TEST-059`'s "exact lock-order evidence" measures the lock that
conflicts; state that cyclic aborts and automatic restart are the expected
outcome of distinct-account concurrent grant writes rather than a defect; and
either bound the per-frame authority lookup (for example a per-key positive
cache invalidated by the writers, or `dirty_index_read` with a documented
staleness argument reconciled inside the guarded writer) or record the
contention as an accepted operational residual for `GATE-04`.

---

## L-1 (LOW) — `pairaccount-possession-required` has no carrier in `CON-016`'s four-message wire, and `REQ-045` ("receives") contradicts `CON-966`/`TEST-057`/`TEST-1103` ("surfaces"/"stops at")

**Owner: cbcl-bus `REQ-045`/`CON-016`**, mirrored in Selfsame `CON-966`.

`REQ-045`: "A client without the claimed private key **receives**
`pairaccount-possession-required` and cannot claim the device." But the hub
cannot know that a client lacks a private key — it issues a challenge and the
client simply never answers. `CON-016`'s wire table has exactly four messages
(`pairaccountgrant`, `pairaccountchallenge`, `pairaccountgrantproof`,
`pairaccountgranted`) and no error carrier. `CON-966` and `TEST-1103` say the
browser "surfaces"/"stops at" it; cbcl-bus `TEST-057` says it "must stop at
`pairaccount-possession-required` without a proof frame". So the symbol is a
client-side terminal state in three clauses and a hub reply in one.

The concrete consequence at the baseline: `app.js` has no
`pairaccountchallenge` handler, and an unrecognised hub frame falls through to
`renderIncoming(text)` (`app.js:2276-2281`) — it would be painted into the room
as a message. The shipped `local-pairing-fixture.json` does hold only
`devicePublicKeyB64u` (verified: keys are `account`, `bundleB64u`,
`devicePublicKeyB64u`, `profileB64u`, `relayOrigin`, `transfer`), so `CON-966`'s
factual claim about it is accurate; what is missing is that the browser change
is an owned deliverable of this increment, not an optional follow-up.

### Minimum sound correction

State that `pairaccount-possession-required` is a client-side terminal state
with no wire carrier (or add it to `CON-016` as a hub error slug with a stated
trigger), and name the browser handler for `pairaccountchallenge` as an owned
deliverable so the deployed link affordance cannot regress into painting a
control frame into the room.

---

## L-2 (LOW) — `REQ-980`/`REQ-049`'s "independent browser JavaScript" lane is, at the baseline, the vendored Selfsame WASM — the same Rust authority — so the three-stack differential control is circular unless a source-disjoint JS module is named

**Owner: Selfsame `REQ-980`**, coordinated with cbcl-bus `REQ-049`/`DEC-017`.

`REQ-980` routes all canonicalisation through the Rust NIF and offers the
three-stack corpus as the compensating control: "Rust and **independent browser
JavaScript** reproduce the corpus; BEAM invokes the same Rust authority through
the NIF." cbcl-bus `REQ-049` is explicit: "the JS lane is the **independent**
canonical/digest implementation".

At the cbcl-bus baseline the browser's Selfsame lane is the vendored WASM build
of the same crate:

- `apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm` (+ `.js`,
  `.d.ts`, `provenance.json`, `selfsame-vendor.test.mjs`);
- `apps/cbcl_chat/priv/web/selfsame-application-profile.mjs`: "recognised by the
  vendored WASM at selfsame pin 17b0bf5. **The Selfsame WASM recogniser remains
  the authority for CON-201 recognition.**"

If the credential/v2 browser digests are computed through that WASM, all three
lanes are one implementation and the corpus proves marshalling, not agreement.
The repository does contain the right precedent for the honest version —
`apps/cbcl_chat/priv/web/mls-ds-canonical.mjs` is a hand-written JS
canonicaliser cross-checked against Rust-emitted vectors — so this is a naming
gap, not an impossibility.

### Minimum sound correction

Name the browser canonical/digest module as an owned new deliverable, require it
to be source-disjoint from `selfsame_web_device*` (an import/source check in the
same shape as `selfsame-vendor.test.mjs`), or drop the word "independent" and
state honestly that the corpus is an interop/marshalling control over a single
authority implementation and route the residual parser-differential risk to
`GATE-04`'s human cryptography review.

---

## L-3 (LOW) — coordination metadata across the three candidate documents still points at the superseded revision

**Owner: all three repositories.**

- cbcl-pairing `spec-001-0.4.4`: `consumer-design: selfsame SPEC-008 0.4.9-draft`
  and `review-input: … SPEC-008 0.4.8 … M-3`. The document is byte-identical to
  the commit reviewed at 0.4.9 (verified: `git diff` between the two candidate
  commits is empty for cbcl-pairing), so this is stale, not wrong.
- cbcl-bus `spec-053-0.16.3`: `conforms-to: Selfsame SPEC-008 0.4.10-draft` is
  correct, but `amends:` still lists 0.16.2 clauses that its own `supersedes:`
  line deletes (see `H-2`).
- Selfsame 0.4.10 test spec: "cbcl-bus **TEST-043** through the new 0.16.3
  tests" spans two documents 0.16.3 declares superseded in full, and 0.16.3
  itself starts at `TEST-057`.

### Minimum sound correction

Re-point `consumer-design`/`review-input` at 0.4.10 and this review; enumerate
the mandatory cbcl-bus tests by the clause numbers that survive `H-2`'s ledger
rather than by an open range.

---

# Disposition of the original `F-A` – `F-E`

| Finding | Disposition | Evidence |
|---|---|---|
| **F-A** — trust is application+relay scoped; a new application prompts | **HOLDS for the pair; the "new application" half is disturbed by `H-1`** | Exact `(applicationId, relayOrigin)` pair TOFU is retained unchanged and `relayOrigin` provenance is authentic, not wire-claimed: it is a member of the CON-220 profile's `cbclPairingRelays`, present in the live ratified document (`cbcl-chat-selfsame-application-gate.lfe` decodes to `"relayOrigin":"https://chat.anuna.io:9443"`), and its descriptor digest is fixed by 0.4.7 `REQ-968`/`CON-957`. `REQ-977`'s re-anchoring to the live applicationId means an already-TOFU'd pair does not re-prompt — see **H-1(c)**. |
| **F-B** — approval displays the CON-220-authenticated application/transition; cross-check precedes display | **HOLDS** | `CON-967` is now unambiguous and matches cbcl-pairing `CON-021` exactly: the verifier "receives immutable parsed authenticated input and returns only `Ok(())` or a closed profile error… Only after success does the shared cbcl-pairing profile construct `CredentialV2Display`." Adopted `CON-963`'s "`IntentVerifier` recomputes the digest before it can return success" keeps the room-set cross-check ahead of display. `CON-968` clause 3 ledgers the replacement of `CON-960`'s contradicting sentence. Verified constructible: all three `impl ApplicationProfile` and every `DisplayIntent`/`DisplayField` construction are in-crate (`cbcl-pairing/src/profile.rs:490,571,648,784-795`). |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias provisioning or grant precedes the appropriate explicit consent | **HOLDS** | Adopted `CON-962`'s order is intact and unaltered by 0.4.10. Nothing wallet-side precedes preliminary approval; no hub binding/grant/alias/membership/room-deletion precedes signed final approval, verified publication/grant and browser install. The new pairaccount challenge is RAM-only and writes no durable authority (`CON-016`), so it adds no pre-consent mutation. `CON-966` requires the credential/v2 browser to create and durably retain its device seed **before allocation**, which is a browser-local secret, not a hub or wallet mutation; the in-repo precedent is `installEnrolledConnection` (`app.js`), which writes seed, grant and issuer under one strict IndexedDB transaction. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS** | Adopted `REQ-970`/`CON-939`: the gated allocation command allocates one pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId` before the public carrier; they become durable only at the final transaction; decline/expiry/pre-commit failure deletes them. `H-1(d)` notes that under `REQ-977`'s reading the scope is minted inside the *live* application's namespace rather than a v2-specific one — an unstated consequence, not a missing bootstrap. |
| **F-E** — per-app consent is a mitigation, not a relay-operatorship binding claim | **HOLDS** | 0.4.10 restates it as such and claims nothing further. `REQ-979` now records the one residual the mitigation does not cover — relay-visible ciphertext length correlating with encoded migration size and coarse room count — as an explicit accepted residual gated on human privacy review (`TEST-1106`/`TEST-063`). |

---

# Disposition of every 0.4.8 finding (1C / 4H / 3M / 7L)

Re-tested against v0.4.10 and code, not against a disposition table.

| 0.4.8 finding | Re-test verdict | Evidence |
|---|---|---|
| **C-1** (CRITICAL) — the transaction writes a `cbcl-pairaccount-grant` for the key the person must reconnect with; both keys inert | **CLOSED** | Two independent mechanisms now, both verified implementable. (i) `REQ-973` removed every credential/v2 pairaccount write, and `REQ-978`/cbcl-bus `REQ-046` add the standing rule: `unratified-device?` reads the device-grant and enrolled-member pubkey indexes first and returns literal `false` on any authoritative hit, so the granted key passes the unchanged `cbcl-core-enrol:resolve` clause at `cbcl-core-enrol.lfe:125-131`. (ii) `REQ-045`/`CON-016` make a claim require an inner Ed25519 proof by the exact claimed key, which removes the third-party writer entirely. |
| **H-1** (HIGH) — WS gate runs after TOFU and default fan-out | **CLOSED on the design, at risk from `H-2`** | Structurally exact against the code: `handle-frame` (`cbcl-chat-session-ws.lfe:205-211`) performs only `rearm-ping`, `cbcl-signed-frame:decode` and `cbcl-parser:parse-message` before `dispatch/3`; `dispatch/3` (`:213-235`) reads the binding only in its `_` branch, so a first clause genuinely precedes `resolve-key`, `verify-frame`, `authorise-frame`, TOFU and `route`. The fall-through hazard is real (`custom-kind` defaults to `'other`, `route-member`'s default is `do-publish` at `:960`). The clause that owns this is cbcl-bus `REQ-042`, which 0.16.3 supersedes without restating — see **H-2**. |
| **H-2** (HIGH) — `fingerprintCanonicalBytes` undefined; hub has no BLAKE3 | **CLOSED** | The replacement lands on an already-implemented definition: `enrollment::device_key_digest` (`enrollment.rs:265-274`) is exactly `SHA-256(canonicalise(deviceKeyJwk))` over the closed four-member object `DEVICE_JWK_MEMBERS = ["kty","crv","alg","x"]` (`ceremony.rs:278`), already in RFC-8785 order. `Fingerprint::hex()` (`selfsame-core/src/fingerprint.rs:77`) is correctly demoted to a wallet/UI rendering under REQ-103. |
| **H-3** (HIGH) — the positive fixture opens an unnamed second Tier-1 gate | **CLOSED on the design, at risk from `H-2`** | Both gates are real and correctly named: `cbcl-chat-path-b-gate:profile/0` returns `#(error tier-1-unapproved)` as a source constant and is consulted on every account-bound frame; `cbcl-chat-credential-v2-gate` does not exist at the baseline (only `cbcl-chat-path-b-gate.lfe`, `cbcl-chat-selfsame-application-gate.lfe`, `cbcl-chat-webfinger-gate.lfe`), so "on landing" is the right tense. `cbcl-chat-gate-closed-tests.lfe:18-33` pins both the closed admission gate and the 1,235-octet/`6ac06f62…` serving octets. The owning clause is cbcl-bus `REQ-044`/`CON-015` — see **H-2**. |
| **H-4** (HIGH) — the live `/selfsame/enrolment/sign` is an unauthenticated widening oracle | **CLOSED** | The hazard is exactly as described: `sign_enrollment_profile_bound` takes caller-supplied `Statement` octets (`cbcl_selfsame_erl.erl:68-70` stub; `selfsame-beam/src/enrollment.rs:130-153`), so widening `recognise_unsigned` would make the deployed route a v2 oracle. `REQ-977`'s four boundaries close it and are verified free: `build()` hard-codes `("evidenceVersion", Json::int(1))` (`enrollment.rs:662`) and `EnrollmentStatement` carries no evidence-version field, so today's `build` **is** a v1-only builder and naming it `build_enrollment_v1` is a rename, not a refactor. `recognise_payload` compares `evidenceVersion` to a literal `Some(1)` (`:373`) and never compares it to the profile or the offer, so a total v1/v2 dispatch is available. |
| **M-1** (MEDIUM) — wallet cannot recompute two of three digests | **CLOSED** | Adopted `REQ-975`/`CON-013` state the per-party split honestly and require the wallet to recompute `roomSetDigest` from the exact array it will display before any approval affordance. |
| **M-2** (MEDIUM) — no credential/v2 reply performative is in `hub-originated?` | **CLOSED** | `hub-originated?/1` (`cbcl-core-dialect.lfe:79-84`) is a closed `lists:member` literal list and is the only thing between a client-authored hub verb and `do-publish` (`route/8`, `:928-931`). `REQ-050`/`CON-968` clause 7 correct the set to the exact six and return `account-linked` to REQ-033's ownership. `CON-016` correctly adds `pairaccountchallenge` to the hub-originated class and `pairaccountgrantproof` as an explicitly handled client kind. |
| **M-3** (MEDIUM) — the typed `AccountTransition` has no carrier | **CLOSED** | cbcl-pairing 0.4.4 `REQ-026`/`CON-021` supply it, and it is verified implementable against `8e55160`: `profile: Box<dyn ApplicationProfile>` is an owned field (`src/endpoint.rs:295`), both `recognise_intent` call sites are already inside `&mut self` methods, `&mut self` preserves object safety, and every display construction is in-crate. |
| **L-1** (LOW) — the lock order names a nonexistent "device count index" | **CLOSED for the index; see `M-3`/`M-4` for the discipline** | `ensure-device-table` (`cbcl-chat-account-grants.lfe:53-61`) declares no index and `account-device-count/1` (`:204-207`) is a whole-table `mnesia:foldl`, exactly as filed. `NFR-912`/`REQ-047` now add and verify `account` + `pubkey` on `cbcl-device-grant` and `pubkey` on `cbcl-member`, with the in-repo idempotent precedent in `cbcl-chat-webfinger-store.lfe:97-110`. |
| **L-2** (LOW) — `CON-941`'s `cbcl-webfinger-account` primary key | **CLOSED** | `cbcl-chat-webfinger-store.lfe:54,70` — `(defrecord cbcl-webfinger-account account alias home-did)` created with `attributes '(account alias home-did)`; the primary key is `account`, as 0.4.9 `CON-965` clause 5 states. |
| **L-3** (LOW) — a nonexistent gate stated as a present-tense fact | **CLOSED** | Effective text says "On landing"; the module is absent at the baseline. |
| **L-4** (LOW) — mixed NUL notation / field-vector overclaim | **CLOSED** | One rule (`0x00`, once per domain) and an exact member vector. Verified against the records: `cbcl-member` is `(handle pubkey enrolled)` (`cbcl-chat-members.lfe:14`), so excluding the wall-clock `enrolled` is correct. `CON-016`'s new possession preimage follows the same discipline: one trailing `0x00` after the domain, `u32be` length prefixes on both variable members, fixed-size members undelimited. |
| **L-5** (LOW) — the offer bound lacks arithmetic | **NOT CLOSED → M-1 (this review)** | The 62,000 target is verified real (`cbcl-pairing/src/profile.rs:334,920`) and the 0.4.9 `ENROLLMENT_JWS` contradiction is gone. But the replacement arithmetic has no fixed point — see **M-1**. |
| **L-6** (LOW) — oversize silently excludes the person | **CLOSED** | Adopted `NFR-911`'s terminal-exclusion statement is retained and unaltered. |
| **L-7** (LOW) — no allocator-observable no-offer test | **CLOSED** | Verified implementable: cbcl-pairing's relay distinguishes control frames from adjacent bodies (`src/cbcl_protocol.rs:32-34`; `src/mailbox.rs`), and `grep -rn "offer" src/*.rs` in cbcl-pairing returns nothing, so "zero `offer` objects" is observable as zero application-payload frames after Finished. |

---

# Disposition of every 0.4.9 finding (1C / 1H / 3M / 4L)

| 0.4.9 finding | 0.4.10 claim | Re-test verdict | Evidence |
|---|---|---|---|
| **C-1** (CRITICAL) — an arbitrary pairaccount claim can brick a granted key after commit | `REQ-978` adopts possession proof and grant-dominant monotonic authority | **CLOSED, with `M-2`/`M-3`/`M-4` on the mechanism** | The possession preimage is fully constructible at the handler: `hub_id` is in the connection context (`cbcl-chat-session-ws.lfe:49,59`), `conn_nonce` is the 16-octet per-connection value, the verified sequence is `(maps:get 'seq frame)` as already used for `cbcl-chat-grant-nonce:issue` (`:712`), `outerVerifiedKey32` is `(maps:get 'pubkey nctx)`, and `handleBytes` is the parsed `:from` with its sigil. `cbcl-chat-grant-nonce` is an exact in-repo precedent for the RAM-only, burn-before-compare, socket/sequence-bound ledger (`consume-tx` marks `used` before comparing any field). Possession precedes the authority read, so `device-already-authoritative` is not an oracle for a key the caller does not hold. Restart dominance holds: `cbcl-device-grant`, `cbcl-member` and `cbcl-pairaccount-grant` are all on the disc storage kind and `mnesia:add_table_index` persists. The live pre-existing exposure is genuinely repaired: a key with any `cbcl-member` row now reads `false`, so an old third-party claim can no longer disable an enrolled member. Residual scope: **M-2** (the `enroll-or-check` rule), **M-3** (the two deleters, and "for all time"), **M-4** (the real lock that conflicts). |
| **H-1** (HIGH) — `profileVersion = 2` cannot verify against the live version-1 profile | `REQ-977` uses statement-local `evidenceVersion = 2` | **NOT CLOSED → H-1 (this review)** | The *statement*-level repair is correct and verified free (see 0.4.8 `H-4` row). But 0.4.9's `H-1` was filed on the premise that one application must serve both documents, and the adopted set already gave credential/v2 a **separate** applicationId (0.4.5 `REQ-957`, 0.4.6 `CON-951`). 0.4.10 does not ledger those clauses, and `REQ-977`/`REQ-048` now bind v2 evidence to the live v1 digest. Two effective answers for the trust anchor — see **H-1**. |
| **M-1** (MEDIUM) — 8,192-byte v1 evidence cannot fit a 6,000-byte non-core budget | `NFR-912` defines a 4,000-octet v2 policy and a `56,000 + 4,000 + 2,000` proof | **NOT CLOSED → M-1 (this review)** | `ENROLLMENT_JWS.max_octets = 8_192` is correctly left alone and outside the v2 arithmetic. But the new budget is unsatisfiable against the inherited grammars: `MAX_PERMISSIONS = 64` and `uri::recognise` places no length bound on a path or fragment (`uri.rs` `recognise_path`/`recognise_pchars`), while `STATEMENT_LIMITS.max_bytes` is defined *as* the JWS cap (`enrollment.rs:705-706`). See **M-1**. |
| **M-2** (MEDIUM) — adopted `CON-960` contradicts the verdict-only display API | `CON-968` explicitly replaces that sentence | **CLOSED** | `CON-968` clause 3 names the exact sentence, the exact replacement (`CON-967`), and the single owner. `CON-967` and cbcl-pairing `CON-021`/`ADR-019` now agree word for word on verdict-only verification and shared-profile construction, and `TEST-1107` rejects "a verifier-returned transition" statically. |
| **M-3** (MEDIUM) — the lock and cap rule does not cover both real grant writers | `REQ-978` and 0.16.3 name absent-row guards, indexes, one private write core, and removal of the uncapped export | **CLOSED for the creators; NOT CLOSED for the deleters and the real lock → M-3, M-4** | The hardest part is closed exactly right. `store-device-tx/7` is already private (`cbcl-chat-account-grants.lfe:189`, absent from the `defmodule` export list) and already enforces the cap; `store-verified-device-grant/6` is exported, uncapped (`:216-237`), and has **no production caller** (verified: only `apps/cbcl_chat/test/…` call sites), so removal is safe. The account guard key is exact and real: `cbcl-device-grant.account` holds the same account DID binary that `cbcl-account-binding.account` holds (`path-b-durable-write` passes `(maps:get 'account_did facts)`, `:856`), so "the exact raw canonical bytes stored in `cbcl-device-grant.account`" has one referent for accounts created by either writer, present row or not. Mnesia does lock an absent key, so the absent-row guard reasoning is sound. What remains: `revoke-observed/2` and `revoke-device-grant/2` are unnamed `cbcl-device-grant` writers (**M-3**), and the stated order omits `index_read`'s `?ALL` table lock (**M-4**). |
| **L-1** (LOW) — `account-linked` is not a credential/v2 kind | `CON-968` clause 7 separates the six from the shared notice | **CLOSED** | Verified: `account-linked` is already in `cbcl-core-msg:custom-kind` and already in `hub-originated?/1` (`cbcl-core-dialect.lfe:82`), and is emitted post-commit by `fan-account-linked-notices/3` (`cbcl-chat-account-grants.lfe:138`) under REQ-033. `REQ-050` scopes the mutation criterion to the six. |
| **L-2** (LOW) — ciphertext length leaks coarse migration size | `REQ-979` records it as an accepted residual and makes privacy acceptance a gate | **CLOSED** | The leak is real and correctly bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` (`cbcl-pairing/src/cbcl_protocol.rs:34`) with the length on the wire. `REQ-979` states the granularity (256 rooms / 48,000 encoded room octets), says exact identifiers stay encrypted, claims no padding property, and `TEST-1106`/`TEST-063` require measurement plus an explicit human privacy acceptance before enablement. |
| **L-3** (LOW) — the hub lacks CBOR/JCS primitives | `REQ-980` routes closed typed digest construction through the Rust NIF | **CLOSED in shape → L-2 (this review) on the differential claim** | The boundary is real and the drift hazard is already understood in-repo: `cbcl_selfsame_erl.erl:52-60` documents that `rustler::init!` registers every NIF and `load_nif` answers `bad_lib` when the library exports one the module does not declare, which is exactly why `REQ-049`'s "source NIF, Erlang facade/stub, and reproducibly vendored release NIF move together" is the right requirement. `catch_unwind` at the NIF boundary is the existing pattern (`selfsame-beam/src/enrollment.rs`). Residual: the browser lane's independence — see **L-2**. |
| **L-4** (LOW) — the shared `build()` can silently widen v1 | `REQ-977` requires disjoint recognisers, builders, signers, and call graphs | **CLOSED** | `REQ-977` names the builder explicitly (`build_enrollment_v1` / `build_credential_v2`), states the four boundaries, and `TEST-1107` statically rejects "v1 round-trips through shared `build`". Verified free at the baseline: `build` emits `evidenceVersion` as a literal `1` and the statement struct has no version field, so the round-trip checks at `enrollment.rs:318-320` and `:352-354` are already v1-specific. |

---

# Verification of the coordinated amendments

## cbcl-pairing SPEC-001 0.4.4 — passes on its own terms, unchanged

The candidate commit is byte-identical to the one reviewed at 0.4.9 (empty diff
between `c9c7d5c` and itself; the six spec files are the whole delta from
`8e55160`). Re-verified against `8e55160`:

- `DisplayIntent`/`DisplayField` are public-field structs at the baseline
  (`src/profile.rs:81-101`) and every construction is in-crate (`:784-795`), so
  making fields private breaks no external producer.
- `recognise_intent(&self)` → `(&mut self)` type-checks at both call sites
  (`send_intent`, `apply_intent`), keeps `dyn` object safety, and needs no
  interior mutability — exactly what `TEST-053` demands.
- `TEST-055`'s control witnesses are grounded in the real constant:
  `MAX_CONTROL_OCTETS = 2_048` (`src/cbcl_protocol.rs:32`).
- `CON-021`'s verdict-only verifier now agrees with Selfsame `CON-967` word for
  word; the 0.4.9 `M-2` caveat is discharged by `CON-968` clause 3.

**Only defect:** `L-3` (stale `consumer-design`/`review-input` metadata). No
finding in this review is owned by cbcl-pairing.

## cbcl-bus SPEC-053 0.16.3 and its amended parent — does not pass

Verified sound and implementable against `fdbe414`:

- **`REQ-045`/`CON-016`.** Every preimage member is available at the handler
  (see the `C-1` row above), `cbcl-chat-grant-nonce` is the exact precedent for
  the RAM-only burn-before-compare ledger, and `cbcl-core-pairaccount:decode-device-key`
  already enforces "strict standard base64 of exactly 32 octets". The claimed
  correction to `SPEC-076` `REQ-002`/`ADR-001` is warranted: the module's own
  header asserts the outer envelope "already covered the canonical claim bytes",
  which authenticates the sender and never the key named in `:device`, and
  `do-pairaccountgrant` (`cbcl-chat-session-ws.lfe:1667-1678`) takes `devkey`
  verbatim from the frame with no membership check and no gate.
- **`REQ-046`/`CON-017`.** The ordering is implementable and monotone; the
  enrolled-member arm is genuinely permanent (no delete path in
  `cbcl-chat-members`); the grant arm is not (**M-3**). The fail-closed
  `unknown` contract at `cbcl-core-enrol.lfe:125-131` is preserved.
- **`REQ-047`.** Correct on the two creators and the exact guard key
  (see the 0.4.9 `M-3` row). Incomplete on the deleters (**M-3**), the
  `enroll-or-check` predicate (**M-2**), and the real lock (**M-4**).
- **`REQ-048`.** The statement-level version split is correct; the profile-level
  consequence is not owned (**H-1**).
- **`REQ-049`/`DEC-017`.** Right boundary, right drift requirement; the
  independence claim about the browser lane is unsupported (**L-2**).
- **`REQ-050`.** Correct and verified against `custom-kind`/`hub-originated?`.
- **`NFR-010`.** Arithmetic unsatisfiable as written (**M-1**); privacy residual
  correctly stated.
- **`GATE-04`.** The box list is complete for what this amendment introduces and
  the amendment correctly closes none of them.

**Defective:** the frontmatter's supersession semantics (**H-2**), which is the
larger of the two cbcl-bus problems because it puts the previous two rounds'
structural repairs outside the effective text.

The parent `SPEC-053-key-root-identity` change is a two-line
`candidate-amendment` pointer bump from 0.16.2 to 0.16.3 plus the version field;
it introduces nothing further.

---

# Residual assumptions and open gates — *not* design blockers

Recorded so they are not mistaken for findings and so `GATE-04`'s reviewers have
them in one place.

- **R-1 — `maxDevicesPerAccount` does not exist yet, and the credential/v2
  finalizer inherits that refusal.** `ratified-max-devices-per-account/0`
  returns the atom `'unratified` (`cbcl-chat-session-ws.lfe:898`), so
  `path-b-max-devices/0` returns `#(error unratified-device-limit)` and
  `bind-and-store-verified-device-grant/8` is unreachable in production today.
  `REQ-047`'s "Every path supplies the recognized `maxDevicesPerAccount`" is
  therefore inert until GATE-01's CON-002 box closes with an owner ratification
  in 1..8. Both amendments already list GATE-01 as required and open; this is
  consistent, but the coordinated text nowhere says the constant is absent.
- **R-2 — the two-gate fixture is still an unbuilt build-system seam.** The
  `test` profile adds `{erl_opts, [{src_dirs, ["src","test"]}]}`
  (`rebar.config:128`) with no per-profile source *exclusion*, so two same-named
  modules would collide; `REQ-044`'s "explicit source manifest" acknowledges it.
  Real release-guard tooling exists to build on
  (`scripts/check-retired-pairing-absence.sh`, run as a `prod` release
  post-hook). Effort, not impossibility.
- **R-3 — challenge-table aggregate RAM is bounded per connection, not in
  aggregate.** `REQ-045` caps two live challenges per (connection, verified
  handle) and expires within 120 seconds, but TOFU makes connections free and no
  clause states a global ceiling or a restart-crash reaper. The existing
  `cbcl-chat-grant-nonce` has the same shape *plus* a reaper
  (`cbcl-chat-grant-nonce-reaper`), which is the pattern to copy.
- **R-4 — post-commit authority depends on continuous resolver availability.**
  After the irreversible commit the legacy key is dead and the granted key's
  standing runs through `path-b-establish-standing` → `path-b-sidecar` →
  `cbcl-chat-path-b-resolver:resolve`, which fails closed. A did-crdt outage is a
  transient total lockout with no legacy fallback, by design. `GATE-04` already
  requires did-crdt `durable-ready` continuously true.
- **R-5 — production kind recognition is a mild existence oracle.** Adding the
  head symbols to `cbcl-core-msg:custom-kind` in production makes a gated verb
  answer `bad-signature` while an unknown verb is still published. The kind set
  is public in the specs; `GATE-00` condition 3 is not breached.
- **R-6 — the `CredentialV2GateWitness` unforgeability mechanism is still
  unspecified.** Achievable in BEAM (a closure or `make_ref/0`, neither
  producible by `cbcl-parser` from wire bytes); an implementation choice, not a
  defect, provided the chosen term is one the wire parser demonstrably cannot
  construct.
- **R-7 — unchanged surfaces not re-opened.** CPace transcript/role/application
  binding, the PROTO-004 envelope, the rendezvous carrier and SPEC-075 relay
  opacity are untouched by 0.4.10 and were checked only for the absence of new
  contact, not re-audited. did-crdt is unchanged from its own baseline.
- **R-8 — the compromised-Path-A-key takeover remains an accepted, gated risk.**
  Whoever holds the legacy enrolled key can drive the whole ceremony and
  irreversibly bind the handle. `GATE-04` routes this to human security review;
  0.4.10 does not change it and this review does not re-litigate it.
- **R-9 — device-key loss after migration is recoverable and is not a finding.**
  The wallet retains account root authority and can issue a fresh Path-B device
  grant to a new browser.
- **R-10 — removing `store-verified-device-grant/6` from the source will break
  roughly ten existing test call sites** in `cbcl-chat-account-grants-tests.lfe`
  and `cbcl-chat-account-roommember-tests.lfe`. Bounded implementation work,
  named here so it is not discovered as a surprise.

---

# What would make this pass

1. **Selfsame `CON-968`/`REQ-977`** — close `H-1`: ledger the disposition of
   0.4.5 `REQ-957`/`CON-947`, 0.4.6 `REQ-962`/`CON-951` and 0.4.7 `CON-957`, and
   scope clause 1 to *statement* discriminators. State which applicationId and
   which profile digest credential/v2 evidence binds to, and re-state `F-A`'s
   prompt behaviour and the v2 permission set for the choice made.
2. **cbcl-bus 0.16.3 frontmatter** — close `H-2`: replace "supersedes … in full"
   with an explicit adopts-except ledger, so `REQ-041`, `REQ-042`, `REQ-044`,
   `CON-013`, `CON-014`, `CON-015` and `TEST-048`–`TEST-056` have determinate
   standing.
3. **Selfsame `REQ-979`/`NFR-912` + cbcl-bus `NFR-010`** — close `M-1`: declare
   the v2-specific maxima (permission count and per-permission octets;
   `applicationId`, `returnUri`, `providerId`, `platformBindingId`,
   `applicationAccountDisplay`; a v2 payload ceiling) and re-derive 4,000 /
   2,000 / 56,000 from them, showing the sum.
4. **cbcl-bus `REQ-047`/`CON-017`** — close `M-2`: name the exact key-collision
   predicate for `enroll-or-check/2`, preserve multi-handle key sharing
   explicitly, and require the function to become one transaction that returns
   its verdict.
5. **cbcl-bus `REQ-046`/`REQ-047`/`NFR-010`** — close `M-3`: bring
   `revoke-observed/2` and `revoke-device-grant/2` into the guard discipline,
   restate the monotonicity claim as row-conditional, and state the
   post-revocation consequence.
6. **cbcl-bus `NFR-010` + Selfsame `NFR-912`** — close `M-4`: name the `?ALL`
   table read lock `index_read` takes, so the stated order is the order that
   conflicts and the mandated lock-order evidence measures something real.
7. **The three lows** — `L-1`, `L-2`, `L-3`, each a bounded clause change with a
   single named owner.

None of these is architectural. `H-1` and `H-2` are the two that would block on
their own; `M-1` blocks because `REQ-979` makes its own generator gate
release-blocking.

---

# Authority statement

This review confers **no implementation, allocation, enablement, release or
deployment authority whatsoever**, and a PASS from a future review would not
either. Specifically:

- The verdict is **REJECT**; the coordinated set is not approved.
- Even on a later PASS, SPEC-008 0.4.10's own sequencing holds: a PASS
  authorizes an Elephant SPL/test-first plan and nothing else.
- `GATE-04` remains no-go with every box open. `GATE-01` and `GATE-03` remain
  required and are not addressed here; note `R-1`, which makes GATE-01's CON-002
  box a precondition of the device-grant path working at all. Production
  credential/v2 allocation stays closed, both Tier-1 source constants stay
  closed, and human cryptography/migration/privacy review, independent CPace
  evidence, both durability gates, owner approval and separate deployment
  confirmation all remain outstanding.
- This review changed no implementation code, candidate spec, test, manifest,
  configuration, or sibling repository. It adds exactly this one file.
