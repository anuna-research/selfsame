---
id: spec-008-0.5.16-fail-closed-invoke-discovery-2026-08-25
title: SPEC-008 0.5.16 — fail-closed invoke caller discovery
status: owner-authorized-test-first-candidate
tier: 1
mode: descriptive
author: Anuna Research (drafted with OpenAI GPT-5.6 Codex)
last-updated: 2026-08-25
parent: "[[SPEC-008-production-pairing-claimant]]"
---

# SPEC-008 0.5.16 — fail-closed invoke caller discovery

## Authority and scope

The owner authorized test-first implementation before review PASS. This
trajectory records the sole blocking finding from the 0.5.15 implementation
review and its measured correction.

It authorizes no push, production allocation, release, or deployment.

## Rejected-review finding

The review passed every protocol and security hard stop. It also passed the
Linux artefact reproduction, release-source authentication, and both complete
local pairing capstones.

One mutation survived. The screen harness kept a manual list of three
JavaScript invoke callers. Removing `src/pairing.js` from that list left every
screen green because the comparison saw a smaller caller set.

The review report is
[[spec-008-0.5.15-implementation-claude-adversarial-review-2026-08-25#M-1 — the screen gate's invoke-surface caller list is hand-maintained, so excluding `src/pairing.js` from invoke discovery leaves the gate green; TEST-1167's third named mutation is not killed]].

## Red-first measurement and correction

The uncorrected mutation rendered all 33 screens and exited zero. This
reproduced the review finding against the committed candidate.

The corrected harness recursively enumerates `src` JavaScript modules. It
derives invoke callers from source, scans the complete derived set, and
ratchets the current caller count.

The same exclusion mutation now exits one. Its `invoke-surface` error reports
two discovered callers where three are required. The restored candidate
renders all 33 screens and exits zero.

[[SPEC-008-production-pairing-claimant#TEST-1169 — Invoke caller discovery fails closed]]
records this executable gate in the consolidated parent.

## Coordinated citation correction

SPEC-007 already carried version 0.3.7 in front matter. Its orientation and
the coordinated review-set citations still named 0.3.6.

The current references now name Selfsame SPEC-007 0.3.7, Selfsame SPEC-008
0.5.16, and cbcl-bus SPEC-053 0.17.12. Historical 0.3.6 evidence and changelog
entries remain unchanged.

## Exact implementation closure

- Selfsame implementation:
  `60abb8b25e858be8005151e47ea2a8044d130e80`
- cbcl-bus implementation:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-bus coordinated authority:
  `a4f46c47b7cf21c3d210d9998107ba5c27ba0838`
- cbcl-pairing:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`

## Review gate

The focused review SHALL independently rerun the formerly surviving mutation.
It SHALL verify the unmutated screen gate and strict controlled-language
lint. It SHALL regression-check the 0.5.15 protocol PASS.

The review uses Circus and the interactive Claude subscription driver.
`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent.

PASS still authorizes no push, production allocation, release, or deployment.
