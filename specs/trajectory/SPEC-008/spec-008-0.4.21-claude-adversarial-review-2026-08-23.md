---
id: SPEC-008-review-0.4.21-claude-2026-08-23
title: SPEC-008 0.4.21 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.14 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.21-singular-ledgers-and-whole-graph-retries]]"
prior-review: "[[spec-008-0.4.20-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.21 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.14 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session started. I
recorded the gate result first, and printed no secret value.

```text
$ if [ -n "${ANTHROPIC_API_KEY+x}" ]; then echo PRESENT; else echo ABSENT; fi
ABSENT
$ env | grep -c ANTHROPIC_API_KEY
0
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "email": "<redacted>",
  "orgId": "<redacted>",
  "orgName": "<redacted>",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai` and the reported `subscriptionType` is `max`. Account identifiers are
redacted above and appear nowhere in this report.

The reviewing model is Claude Opus 5 in a fresh context. This session called no
Anthropic API and no external web service. Every input came from the four local
repositories, the local cbcl-rs checkout, and the local OTP install. This session
read the candidates and the source, and never the generator reasoning.

One half of the independence claim is unverifiable from the artefacts. No candidate
records its generating model family, version, or session. I searched all three for
such a record and found none, and the authoring commits carry only a human
identity. So this report establishes reviewer independence and cannot establish the
cross-model property. That gap is
`[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-M-3]]`.

## Reviewed commits and baselines, resolved rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `94d9c293e96379b99ed5a2c25df8c249b0c8a307` | candidate 0.4.21-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `91e14c98881826ad9ff52454fdc66915c8619d2d` | candidate 0.4.8-draft |
| cbcl-bus | `f4d8151a3235dd656de361e7bf9c2365dd053056` | candidate 0.16.14-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The sibling working trees carry user-owned uncommitted
paths. I read those repositories only through `git show COMMIT:PATH`, and I
changed nothing in them. The local OTP install is `28.5` with `mnesia-4.25.3`.

I read the complete effective amendment chain from 0.4.4 forward. I also read the
0.4.20, 0.4.19, 0.4.18, 0.4.17, 0.4.16, and 0.4.15 reviews and both coordinated
candidates. I read [[PROTO-002-selfsame-rendezvous-v1]] and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Cbcl-pairing 0.4.8 differs from the
revision the prior review read by one line, which is its `consumer-design`
pointer.

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 2 | N6-H-1, N6-H-2 |
| MEDIUM | 3 | N6-M-1, N6-M-2, N6-M-3 |
| LOW | 2 | N6-L-1, N6-L-2 |

Verdict: **REJECT**.

The ledger work closes completely. Every retirement boundary in
[[spec-008-0.4.21-singular-ledgers-and-whole-graph-retries#CON-980]] resolves to
exactly one paragraph or list item. The five declared roles each resolve to one
current value across the three repositories. The whole-graph retry scope returns,
and the WebFinger busy terminal verifies leg by leg against real code.

The rejection rests on the two corrections themselves. The withdrawal rewrite
drops a parent-mandated reciprocal check, so a live-branch delete can destroy
another account's binding. The restored retry mandate states a consumer contract
the reviewed code contradicts, and the bound it imposes makes two fail-open
consumers reachable. A third defect is inherited rather than introduced: no
candidate records its generating model, so the cross-model gate they demand cannot
be discharged.

---

## Findings

### N6-H-1 (HIGH, release-blocking) — the withdrawal rewrite drops the reciprocal alias check that the cbcl-bus parent mandates, so a live delete can destroy a third account's binding

**Repository owner: selfsame (NFR-923) and cbcl-bus (NFR-021).**

`NFR-923` at
`specs/trajectory/SPEC-008/spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md:85-89`
states the new live branch:

> An absent account row enters `already-withdrawn/2`. A present account row whose
> stored account, alias, or home DID disagrees refuses without taking a second
> alias lock. Only a present row that agrees on all three values continues on the
> live branch and takes the exact `cbcl-webfinger-withdrawn` write lock.

The predecessor at
`spec-008-0.4.20-complete-authority-retirement-and-observable-busy.md:69-70` read
differently:

> Both rows MUST agree on the account, alias, and home DID.

The subject moved from **both rows** to **a present account row**. Cbcl-bus
`NFR-021` carries the identical narrowed text at
`specs/trajectory/SPEC-053/spec-053-0.16.14-singular-ledgers-and-whole-graph-retries.md:112-116`.

**The dropped check is a parent SHALL.** `specs/SPEC-053-key-root-identity.md:1991-1996`
at the pinned cbcl-bus commit reads:

> **Withdrawal SHALL verify the reciprocal row before destroying anything.**
> Lookup already treats a disagreeing pair as corruption, but it only declines to
> publish; withdrawal deletes. Acting on an account row without checking that the
> alias row names the same account would let a single stray row make withdrawing
> one account permanently destroy another's binding — and a damaged store is
> precisely when this lever gets reached for.

The deployed code implements exactly that. `alias-agrees?/3` at
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:232-237` reads the alias
row and compares its account and home DID. `tombstone/2` at `:220` answers
`#(error inconsistent-binding)` when the comparison fails, and the module states
that outcome at `:173-176`.

**Failure scenario.** A damaged store holds alias row `{U, B, D}` and account row
`{A, U, D}`. An operator withdraws account `A` naming home DID `D`. The requested
alias is `U`, so the live branch takes the `U` write lock. The account row agrees
on its own key, on `U`, and on `D`. NFR-923 therefore admits the live branch, and
the branch deletes alias `U`, which belongs to account `B`. The call answers
`#(ok #(webfinger-withdrawn U D))`.

**One of the three named checks is vacuous.** The account table is keyed on the
account, and `mnesia:read` selects by that key. So `account_row.account` always
equals the requested account. Under the literal reading the clause therefore names
three checks and performs two. Under the charitable reading it means the pairwise
agreement, and it says so nowhere.

Either reading is gate-blocking. `#(error inconsistent-binding)` becomes
unreachable, and the clause that decides when a delete is permitted does not say
which row it compares.
[[spec-008-0.4.21-singular-ledgers-and-whole-graph-retries#TEST-1146]] at `:285-286`
requires only "a present disagreeing row" to refuse, so the test does not catch
the case either.

**Minimum sound correction.** Restore the reciprocal predicate. State that the
requested alias row MUST name the same account and home DID, and that a
disagreeing or absent alias row refuses.

---

### N6-H-2 (HIGH, release-blocking) — the restored whole-graph mandate states a consumer contract the code contradicts, and the bound it imposes makes two fail-open guards reachable

**Repository owner: selfsame (NFR-923) and cbcl-bus (NFR-021).**

The scope correction itself lands. `NFR-923` at `:105-109` restores the whole-graph
obligation, and `:125-127` scopes the four WebFinger owners to their own lane. I
re-derived the retry mechanics at the local install rather than from prose.
`mnesia:transaction/2` accepts an integer at `mnesia.erl:729`. `try_again/1` at
`mnesia_tm.erl:929-931` retries only while the count exceeds one, so `Retries >= 2`
is the true minimum. `maybe_restart/6` at `:918-927` returns `{aborted, nomore}` on
exhaustion. `transaction/6` at `:811-824` returns `{aborted, nested_transaction}` on
bad nesting. The three mapped terminals are real.

The inventory rule is also sound in shape. I enumerated every production
`mnesia:transaction` call site under `apps/` at the declared cbcl-bus baseline and
found 70 sites in 20 modules. Two further textual hits at
`cbcl-chat-invite.lfe:55` and `:74` are comments.

**The consumer sentence is false about the reviewed code.** `NFR-923` at `:119-123`
reads:

> The shared retry wrapper maps `nomore`, exhausted `#cyclic{}`, and
> `nested_transaction` to `authority-busy`. Every authority-lane and
> migration-lane public consumer returns its previously specified closed
> `authority-busy` result.

No authority-lane or migration-lane public consumer has a previously specified
`authority-busy` result. A whole-chain search returns `authority-busy` only in
prose, and the exact result form only for the WebFinger owners. The deployed
consumers answer `#(error grant-write-failed)`, `#(error revoke-failed)`,
`#(error txn-failed)`, `#(error not-enrolled)`, and `#(error nonce-write-failed)`.

**Three in-scope sites have no result channel at all.** Each discards its
transaction result and returns a fixed value.

| Site | Lane and table | Public value |
|---|---|---|
| `cbcl-chat-members.lfe:47-53` in `enroll-or-check/2` | authority, `cbcl-member` | always `'ok` |
| `cbcl-chat-invite.lfe:34-40` in `mint/3` | migration, `cbcl-invite` | always the token |
| `cbcl-chat-roomcfg.lfe:91-98` in `ensure-room/4` | migration, room | always `'ok` |

`NFR-921` at `spec-008-0.4.19-singular-authority-and-webfinger-order.md:73-74`
places `cbcl-member` in the authority graph, and `:84-85` places every room and
`cbcl-invite` lock in the migration graph. All three sites are production, and
their callers are `cbcl-chat-session-ws.lfe:460`, `:982`, and `:1023`. The
mandated terminal mapping cannot exist at these sites without a signature change
that no clause states.

**Two guards fail open once the bound is imposed, and the bound is what makes them
reachable.** Today these sites use `mnesia:transaction/1`, which is
`transaction(Fun, [], infinity)` at `mnesia.erl:720-721`. An infinite retry count
never surfaces a cyclic abort. An explicit `Retries >= 2` surfaces
`{aborted, nomore}` under exactly the contention `NFR-913` at
`spec-008-0.4.11-standalone-tofu-first-contact.md:264-267` calls expected.

The first guard is the name-squatting backstop.
`cbcl-chat-account-roommember.lfe:94-97` maps every non-atomic result to `'false`,
and `cbcl-chat-roommember.lfe:222-225` falls through to it.
`addagent-target-free?/3` at `cbcl-chat-session-ws.lfe:1047-1052` implements
SPEC-055 REQ-009 on that predicate. An exhausted retry therefore answers that the
name is free, and the agent name is granted over an existing principal.
`cbcl-chat-pairgrant.lfe:78` uses the same predicate as its only membership bound.

The second guard is member enrolment. `pubkey/1` at `cbcl-chat-members.lfe:27-30`
maps every non-atomic result to `#(error not-enrolled)`. `enroll-or-check/2` at
`:42-54` treats that answer as "enrol" and writes the presented key. The table is a
`set`, so the write replaces any enrolled key. `enrol-commit/4` at
`cbcl-chat-session-ws.lfe:454-463` then reports success. An exhausted retry rebinds
the handle's enrolled key and says nothing.

**A production nested transaction already sits on the same predicate, and it guards
SPEC-053 REQ-033.** `bind-and-store-verified-device-grant/8` opens its transaction at
`cbcl-chat-account-grants.lfe:166`. Its body reaches `bind-and-store-unbound/8` at
`:169`, which calls the transaction-opening `pubkey/1` at `:180`. That inner call
opens a second transaction at `cbcl-chat-members.lfe:28`.

The module knows the distinction. `confirmed-migrate-path-a-handle/3` uses the
transaction-only `pubkey-tx/1` at `cbcl-chat-account-grants.lfe:126`, and only `:180`
uses the outer form.

The guard at `:180-181` exists to refuse an implicit Path-A upgrade. Its own comment
at `:178-179` states the rule:

> Existing Path-A membership is a deliberate upgrade flow (REQ-033), never
> an implicit consequence of presenting a grant on the wire.

Nesting makes that guard fail open. `maybe_restart/6` at `mnesia_tm.erl:921-924`
restarts only at level one and otherwise returns the bare `#cyclic{}`. The inner
transaction runs at level two, so a cyclic abort reaches `pubkey/1`, which answers
`#(error not-enrolled)`. `bind-and-store-unbound/8` at `:182-186` then writes the
device grant and the account binding for an enrolled Path-A handle.

`NFR-923` at `:108-109` declares nested transactions non-conforming, and `TEST-1146`
at `:270-273` fails an inventory containing one. Neither names this site, its inner
mapping, or the repair its REQ-033 guard needs. The GATE-04 box "shared
`account-linked` remains protected by [[SPEC-053-key-root-identity#REQ-033]]" depends
on that guard.

**No effective clause forbids this.** `NFR-923` at `:134` forbids contention
becoming absence inside the WebFinger lane only:

> Contention SHALL NOT become `missing` or HTTP 404.

The authority and migration lanes get no analogue, and `enrol-commit/4` at `:462`
collapses every non-`ok` value into `#(error rejected)`. The cbcl-bus GATE-04 box
"level-1 integer retry exhaustion is closed and has zero partial effect" cannot
hold at these consumers as specified.

**Minimum sound correction.** Name the authority-lane and migration-lane public
owners and give each an exact busy result. State the lane-wide rule that a busy
terminal SHALL NOT be observed as absence, non-membership, or a terminal refusal.
Name the three result-discarding sites and the signature change each needs. Name the
`cbcl-chat-account-grants.lfe:180` nesting and require the transaction-only read
there.

---

### N6-M-1 (MEDIUM, gate-blocking) — the "complete" v1 version trace omits three production substitution sites

**Repository owner: selfsame (REQ-996, TEST-1147).**

`REQ-996` at
`spec-008-0.4.17-complete-consent-and-executable-ledgers.md:158-176` introduces a
fifteen-line block with the words "The complete production v1 comparison and
substitution trace is". That block sits outside the `REQ-997` replacement range and
remains effective. `TEST-1147` at
`spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md:327` requires "all
fifteen version sites".

I re-read all fifteen lines at the declared implementation baseline. Each resolves
to the stated construct. I then scanned every `crates/**/*.rs` file for the four
version spellings and separated production code from tests. Three production
substitution sites are absent from the trace.

| Site | Construct |
|---|---|
| `crates/selfsame-app-identity/src/ceremony.rs:190` | `("profileVersion", Json::int(self.profile_version))` |
| `crates/selfsame-app-identity/src/enrollment.rs:666` | `("profileVersion", Json::int(statement.profile_version))` |
| `crates/selfsame-app-identity/src/provider_hint.rs:67` | `("profileVersion", Json::int(self.profile_version))` |

Each emits a version value into a serialized object. Each is the same construct the
trace labels "substitution" at `ceremony.rs:185`, at `enrollment.rs:662`, and at
`web-device/src/lib.rs:1346`. Two of them sit five lines and four lines from a
traced entry inside the same function.

Each substitutes a struct field rather than the `PROFILE_VERSION` constant, so the
emitted value is whatever the caller supplied. Four traced comparisons pin the
value on the verifying side. They are `enrollment.rs:394`, `enrollment.rs:552`,
`profile.rs:385`, and `provider_hint.rs:115`. That is why this is MEDIUM rather
than higher.

One cbcl-bus GATE-04 box is driven by this trace. It requires that every
comparison and substitution site passes against pinned v1 bytes. A checker built
from fifteen lines under-covers the v1-freeze audit by three sites.

The prior review closed `N-M-1` on a narrower claim. It held that every omitted
production hit is a member-name constant, a field read, a constant definition, a
format template, or a comment. These three are none of those.

**Minimum sound correction.** Add the three sites and restate the count, or drop the
word "complete" and make the scan rather than the list the obligation.

---

### N6-M-2 (MEDIUM, gate-blocking) — one of the two superseded conformance SHALLs survives, so the companion conformance target is not singular

**Repository owner: selfsame (REQ-954, CON-980).**

`CON-980` at
`spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md:188-190` states:

> Replacing REQ-963 retires its superseded conformance and TEST-030 through
> TEST-050 sentences.

`REQ-1000` at `:53-54` supplies the current target:

> Credential/v2 SHALL conform to the current cbcl-pairing companion candidate
> named by CON-980.

`REQ-954` at `spec-008-0.4.5-standalone-tofu-first-contact.md:80-86` is never
replaced. I searched the whole chain for its identifier and found its own heading
and two disposition rows. It reads:

> `anuna.io/credential/v2` SHALL conform to cbcl-pairing SPEC-001 0.4.0 at
> `47424b7`, including its typed carrier selection, exact CPace inputs, v2
> CI/AD/public-context domains, distinct HKDF/Finished labels, sealed AAD version,
> fixed application envelope, `intentDigest`, role-projected CBCL dialects,
> shared/profile authority split, store grammar correction, asset manifest, and
> independent vectors.

That is a normative SHALL over a superseded cbcl-pairing revision with a superseded
revision pin. It is the same construct the prior review named inside `REQ-963`, and
`REQ-963` was the only one retired. Two effective requirements now name two
companion revisions.

`REQ-998` at `spec-008-0.4.19-singular-authority-and-webfinger-order.md:50-53` makes
a stale pointer non-conforming. The candidate frontmatter names cbcl-pairing 0.3.0
through 0.4.8-draft, which disagrees with `REQ-954` as well.

No value conflict follows, which is why this is MEDIUM rather than HIGH. Cbcl-pairing
0.4.8 adopts 0.3.0 through 0.4.7, so 0.4.0 is inside the adopted chain. The
2,048-octet control bound already appears in 0.4.0 CON-015, so the stale pin revives
no superseded cap.

**Minimum sound correction.** Retire or restate the `REQ-954` conformance sentence,
so one effective requirement names the companion revision.

---

### N6-M-3 (MEDIUM, gate-blocking) — no candidate records its generating model, so the cross-model gate the candidates demand cannot be discharged

**Repository owner: selfsame, cbcl-bus, and cbcl-pairing.**

All three candidates carry the same frontmatter field:

> review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review

Cbcl-bus GATE-04 repeats the demand in its first box, which requires that the three
candidates "receive fresh-context cross-model PASS".

[[PROTO-001-usdd-agent-protocol]] places this work at review tier 1 and states the
protocol for it. Step 1 of the cross-model review protocol is to record the
generating model family and version. Its conformance registry states the same
obligation for Constitutional Principle 12:

> Reviewer identity + session id recorded per AI Trust Boundaries; presence and
> *distinctness from the generating session* are both checkable

Neither is recorded. I searched
`spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md`,
`spec-053-0.16.14-singular-ledgers-and-whole-graph-retries.md`, and
`spec-001-0.4.8-split-room-shape-and-length.md` for a model, family, version, or
session record and found none. The authoring commits `94d9c29` and `f4d8151` carry
a human author identity only. The protocol also requires a recorded synthesis
trajectory for tier 1 work, and no candidate carries one.

The consequence is exact. Distinctness of the reviewing session from the generating
session is checkable only against a recorded generating session, and no such record
exists. A reader of this report therefore cannot tell a cross-model review from a
same-model fresh-context review, which the protocol grades at tier 3. The gate the
candidates name for themselves cannot be closed by any review while the record is
absent.

This is not a defect in the corrected text. It is a missing tier-1 dependency of the
gate the candidates invoke, and it is inherited across the whole 0.4.x chain rather
than introduced here.

**Minimum sound correction.** Record the generating model family, version, and
session in each candidate, together with the tier-1 synthesis trajectory the
protocol requires.

---

### N6-L-1 (LOW) — the lane names do not decide every site, and one production transaction sits on the ambiguity

`NFR-923` at `:111-117` requires each inventory row to classify its site as
authority, migration, WebFinger, or out-of-scope. It supplies no membership
criterion. The criterion exists in the unreplaced `NFR-921` paragraphs, which define
each graph by its tables at `:73-74`, `:84-88`, and `:105-121`. A reader of
`NFR-923` alone cannot classify a site.

The word "migration" carries two meanings in the same sentence.
`apps/cbcl_bus/src/store/cbcl-store-migrate.lfe:90` is a boot-time legacy migration
over `cbcl-msg`, and `NFR-921`'s migration graph contains rooms, finalizer-tail
locks, `cbcl-invite`, and `cbcl-sealedgrant` only. The module name and the lane name
agree while the table membership disagrees. An in-scope classification demands a
busy terminal from a boot path that must fail closed at `:113`.

The nesting rule has the same edge. `issue-attempt/5` at
`cbcl-chat-grant-nonce.lfe:65-89` runs a bounded collision retry around a fresh
transaction, which is not contention retry. The prohibition on nested retry loops
flags it under an in-scope classification. The `cbcl-grant-nonce` table belongs to
no named graph.

**Minimum sound correction.** State the classification criterion inside `NFR-923`,
and name the schema-migration lane separately from the ceremony migration graph.

---

### N6-L-2 (LOW) — the corrected control row omits one refusal the companion states

`TEST-1146` at
`spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md:300-303` states:

> Run the current cbcl-pairing TEST-050 exception. The control recognizer accepts
> lengths 1, 23, 24, 255, 256, 2,047, and 2,048 and refuses 0, 2,049, and 4,096.

The companion's effective control witnesses live in cbcl-pairing `TEST-052` at
`specs/trajectory/SPEC-001/spec-001-0.4.3-migration-display-and-test-replacement.md:112-115`:

> Run the effective TEST-050/051 control witnesses at 1, 23, 24, 255, 256, 2,047,
> and 2,048. Refuse 0, 2,049, 4,095, and 4,096 before dialect transition.

The accepted lengths agree exactly. The refusal row omits `4,095`. The omission is
not a contradiction. The first sentence requires the companion exception in full,
and `TEST-1082` at `spec-008-0.4.7-standalone-tofu-first-contact.md:341-344`
already carried the same shorter list. Cbcl-bus `TEST-105` repeats the Selfsame
row verbatim, so both coordinated candidates carry it.

**Minimum sound correction.** Add `4,095` to the refusal row, or drop the row and
rely on the companion exception alone.

---

## Disposition of every 0.4.20 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N5-H-1** — the retirement reaches four of seven lists, so ten clauses state competing ledgers | **CLOSED** | `CON-980` at `:161-186` retires item 1 of the 0.4.4, 0.4.5, and 0.4.6 lists and item 2 of the 0.4.5, 0.4.6, 0.4.7, and 0.4.8 lists. I unwrapped every document and counted each boundary string. All fourteen strings occur exactly once in their named document, and every start is item-initial and every end item-final. `REQ-1000` replaces `REQ-963` in full and `TEST-1146` replaces `TEST-1073` in full. I then re-ran the whole traversal from 0.4.4 forward. Exactly one cbcl-pairing survivor set survives, at `CON-978:242-245`. Exactly one cbcl-bus set, Selfsame correction set, coordinated review set, and pointer set survive, all in `CON-980`. No effective text states TEST-050 in full, and no effective text revives a positive 4,096-octet control witness. |
| **N5-H-2** — bounded retry narrowed from the whole graph to four WebFinger functions | **CLOSED IN SCOPE, REOPENED ON CONTRACT** | `NFR-923` at `:105-109` restores the whole-graph rule and `:125-127` makes the four owners WebFinger-lane. The inventory rule at `:111-117` covers all 70 production sites I enumerated. The scope defect closes. The replacement introduces a consumer contract the code contradicts and a bound that makes two fail-open guards reachable. Refiled as `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-H-2]]`. |
| **N5-M-1** — six earlier cbcl-bus paragraphs still claim an exact survivor set | **CLOSED** | Cbcl-bus `REQ-076` at `:39-59` retires the six early paragraphs and the 0.16.13 paragraph. I resolved all seven boundaries at the pinned commit. Each start string and each end string occurs exactly once in its document, each match is paragraph-initial and paragraph-final, and each paragraph precedes the named heading. `REQ-076` also retires the three-paragraph `REQ-075` range, whose start is paragraph-initial at `0.16.13:63` and whose end is paragraph-final at `:72`. One exact current hub set remains, at `0.16.14:75-77`. The historical survivor chain in `REQ-072`, `REQ-074`, and `REQ-075` stays intact. |
| **N5-L-1** — one replacement boundary named a paragraph and ended outside it | **CLOSED** | `CON-980` at `:192-195` supersedes the singular-paragraph instruction and retires a named two-paragraph range in `CON-978`. I counted the paragraphs. The `CON-978` range from "The exact cbcl-bus set" to the did-crdt commit is exactly two paragraphs, and the `CON-979` range is exactly four. Both start strings occur twice inside `CON-979`, and the qualifier "the paragraph whose first text is" selects the replacement text rather than the quoted instruction. The pointer retirement adds a paragraph-final qualifier, which the instruction paragraph fails. |
| **N5-L-2** — the reordered withdrawal collapsed an absent row into "a mismatch" | **CLOSED, WITH A NEW REGRESSION** | `NFR-923` at `:85` now states that an absent account row enters `already-withdrawn/2`. The branch verifies at `cbcl-chat-webfinger-store.lfe:230` and `:252-262`, and the idempotent repeat is preserved. The rewrite that achieved this narrowed the agreement predicate to one row, which is `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-H-1]]`. |

Three closed cleanly, one closed in scope and reopened on contract, one closed with
a new regression.

## Disposition of every 0.4.19 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N4-H-1** — the ledger retirement stops at 0.4.12 | **CLOSED** | The remaining ten sites retire or are replaced. See the `N5-H-1` row. |
| **N4-H-2** — the cbcl-bus twin left REQ-072 effective | **CLOSED** | `REQ-075` retires two `REQ-072` fragments and two `REQ-074` fragments, and `REQ-076` retires the residual six paragraphs. A whole-chain search now returns one effective coordinated-set sentence and one hub set. Ancestry survives in the unretired opening sentences. |
| **N4-M-1** — the busy terminal reached the read path with no stated status | **CLOSED** | `NFR-923` at `:129-135` states the mutation terminal, the `lookup/1` atom, and HTTP 503. Every leg verifies at the baseline. `cbcl-chat-webfinger-http.lfe:36` answers 503 for `unavailable`, `:40-42` sends `no-store`, and `:44-45` sends an empty body. `:38` keeps 400 for invalid syntax and `:37` keeps 404 for absence. `provision/3`, `withdraw/2`, and `reinstate/2` have no production caller; the only non-test consumers are `cbcl-chat-app.lfe:24` and `cbcl-chat-webfinger-http.lfe:27`. |
| **N4-L-1** — the pre-derived alias forced a new arity | **CLOSED** | `NFR-923` at `:78-80` names `tombstone/3(account, homeDid, requestedAlias)` and requires derivation before the transaction opens. `TEST-1146` at `:283-286` and cbcl-bus `TEST-105` both name `tombstone/3`. The derivation stays total: `cbcl-core-webfinger:account-uri/1` answers `'undefined` outside its octet range. |
| **N4-L-2** — the reinstatement account lock named no table | **CLOSED** | `NFR-923` at `:94-100` keeps the whole withdrawal write lock before the first `index_read` and takes no alias or account lock. `lift-tombstone/2` at `cbcl-chat-webfinger-store.lfe:302-308` performs the indexed lookup and exact delete, which that table lock covers. Provision and withdrawal serialize through their own withdrawal write locks at `:140` and `:222`. |
| **N4-L-3** — the cbcl-bus amendment stated two hub test sets | **CLOSED** | `REQ-076` retires the 0.16.13 paragraph and the six earlier ones. See the `N5-M-1` row. |

Six closed.

## Disposition of every 0.4.18 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N3-H-1** — four survivor ledgers and three coordinated review sets stay effective | **CLOSED** | The contradiction set is empty. See the `N5-H-1` row. |
| **N3-H-2** — `CON-976` replaced with no clause preserving its four replacements | **CLOSED** | `CON-980` at `:147-149` keeps every `CON-979` replacement and retirement effective except the named fragments. It restates ancestry at `:155-159`. I attempted to revive `REQ-963`, `REQ-994`, `NFR-918`, `ADR-953`, `TEST-1073`, and `TEST-1129` through `TEST-1145`, and each has exactly one current successor. I also traced the test chain from `TEST-1109` forward through `CON-971` item 8, `CON-972` item 6, `CON-973` item 6, `CON-974` item 5, `CON-975` item 5, and `CON-976` item 4. No link is missing. |
| **N3-M-1** — the reinstatement order created a cross-table cycle | **CLOSED** | Every named WebFinger operation is an ordered subsequence of alias, account, withdrawal. Reinstatement touches one table. The already-withdrawn branch takes a whole withdrawal read lock last, and reinstatement takes a whole withdrawal write lock only. |
| **N3-L-1** — the two withdrawal lock claims had no named test operation | **CLOSED** | `TEST-1146` at `:279-286` names `withdraw/2`, `tombstone/3`, `already-withdrawn/2`, `reinstate/2`, and `lift-tombstone/2`, and requires alias-before-account ordering. Both branches verify at `cbcl-chat-webfinger-store.lfe:222-229` and `:252-262`. |
| **N3-L-2** — one leg of the pointer graph named the superseded candidate | **CLOSED** | All three legs point at the current candidates. `specs/SPEC-008-production-pairing-claimant.md:8` and `:17` name 0.4.21. `specs/SPEC-053-key-root-identity.md:15` and `:688-691` name 0.16.14. `spec-001-0.4.8-split-room-shape-and-length.md:10` reads `consumer-design: selfsame SPEC-008 0.4.21-draft`. |
| **N3-L-3** — one effective CON-975 sentence required a maximum sealed record | **CLOSED** | `CON-978` retires that sentence and `CON-980` does not disturb the retirement. The current cap obligation at `CON-978:257-261` sits outside the retired two-paragraph range. |

Six closed.

## Disposition of every 0.4.17 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N2-H-1** — three Selfsame ledger clauses contradict each other | **CLOSED** | Ten clauses were open at 0.4.20 and none is open now. See the `N5-H-1` row. |
| **N2-M-1** — reinstatement performs an unnamed upgrade | **CLOSED** | `NFR-923` keeps the whole write lock before the first `index_read` and drops the account lock. I re-derived the lock semantics at the local OTP 28.5 install. `ixrlock/5` at `mnesia_locker.erl:1086-1102` requests a whole-table read, and `can_queue` returns the requester's own write lock as compatible. |
| **N2-M-2** — the fresh-ceremony same-key re-grant test disappeared | **CLOSED** | `TEST-1146` at `:262-264` restates the case inline and requires current eligibility, capacity, both consent decisions, and a new verified grant. Cbcl-bus `TEST-105` carries the identical paragraph. |
| **N2-M-3** — two recognizer caps were relabelled as maxima | **CLOSED** | `REQ-997` stays effective and every figure re-derives. The sealed cap bodies are 57,016 and 62,016 octets, and the transit residuals against 69,632 octets are 12,616 and 7,616. The AEAD premise verifies at `crates/selfsame-core/src/seal.rs:143-165`, where `seal_offer` and `seal_bundle` pass a fixed role nonce and authenticated associated data. |
| **N2-L-1** — one `cbcl-sealedgrant` holder fell outside the declared scope | **CLOSED** | `NFR-921` at `:105-112` names both holders with exact modes, and `NFR-923` leaves that paragraph effective. Both halves verify: `put-tx` at `cbcl-chat-sealedgrant.lfe:67-79` reaches `cbcl-chat-invite:peek-tx` at `cbcl-chat-invite.lfe:75-78`, and `get-tx` at `:94-101` reads then deletes. |

Five closed.

## Disposition of every 0.4.16 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N-H-1** — the consent-surface completeness rule is inverted | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:117-119` states that no filter, paging, collapse, or lazy rendering SHALL hide a room. `CON-980` adds no replacement over `REQ-996`. The shipped target exists at `apps/cbcl_chat/priv/web/app.js:602-606`. |
| **N-M-1** — the v1 trace omitted four version sites | **REOPENED** | The fifteen lines resolve, and my independent scan found three production substitution sites the trace omits. Refiled as `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-M-1]]`. |
| **N-M-2** — the acquisition point and the `revoke-handle-tx` upgrade were dropped | **CLOSED** | `NFR-921` at `:100-103` keeps both sentences and `NFR-923` replaces neither. `TEST-1146` at `:266-268` requires the restored acquisition point and the invite-before-room order. `revoke-handle-tx/2` exists at `cbcl-chat-account-roommember.lfe:201`. |
| **N-M-3** — the twelve cycles used symbols whose legend was deleted | **CLOSED** | `NFR-921` at `:68-79` carries the legend and the cycle block, and `NFR-923` replaces neither paragraph. I counted twelve cycles and eight symbols. The binding to `NFR-913` stays bijective, because that matrix at `spec-008-0.4.11-standalone-tofu-first-contact.md:245-253` has seven rows and one read-only row. |
| **N-M-4** — the parent pointer had no target | **CLOSED** | `specs/SPEC-008-production-pairing-claimant.md:8` names the 0.4.21 candidate and `:9` carries the open candidate gate. The implemented 0.2.2 baseline stays distinct at `:4-5`. |
| **N-L-1** — `socketGeneration` had no definition | **CLOSED** | `REQ-996` at `:83-96` states creation, capture, cancellation, and reconnect semantics. That block is outside every replacement range. |
| **N-L-2** — two different definitions of one shared grammar production | **CLOSED** | The alphabet in `spec-001-0.4.8-split-room-shape-and-length.md:33-40` matches `is_symbol_char` at `crates/cbcl-parser/src/parser.rs:328-342` in cbcl-rs `febc669`. I compared all fourteen branches character for character. |
| **N-L-3** — the transit ledger omitted the in-repository baseline | **CLOSED** | `CON-978` at `:257-261` names both baselines and `CON-980` leaves that paragraph effective. `crates/selfsame-rendezvous/src/lib.rs:48` reads `pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`. Did-crdt `src/service/rendezvous.rs:65` carries the same constant at `1f409a4`. |
| **N-L-4** — the sealed-record maximum was never derived | **CLOSED** | `REQ-997` derives every figure and residual, and the derivation re-computes exactly. See the `N2-M-3` row. |
| **N-L-5** — one live `cbcl-invite` lock fell outside both graph scopes | **CLOSED** | `NFR-921` at `:84` and `:105-116` own every `cbcl-invite` lock and place that table first. |

Nine closed, one reopened.

## Disposition of every 0.4.15 finding

| ID | Disposition | Evidence |
|---|---|---|
| **H-1** — unsigned advancing signal with no defined correlation | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:72-101` states the correlation member, its 32 random octets, its encoding, and its one-use consumption. That block is outside every replacement range. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **CLOSED** | Both sites resolve at `crates/selfsame-app-identity/src/enrollment.rs:373` and `:662`, and `:452` carries its label. The separate trace-completeness defect is `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-M-1]]`. |
| **M-2** — `write-grant` read-then-write upgrades | **CLOSED** | `NFR-921` at `:90-92` requires the three-argument read with `write` on the first read in both paths, and `NFR-923` leaves that paragraph effective. Both deployed sites carry the upgrade today at `cbcl-chat-roommember.lfe:155` and `cbcl-chat-account-roommember.lfe:67`. |
| **M-3** — maxima never checked against the implemented rendezvous | **CLOSED** | `CON-978` at `:257-261` states the dependency. `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:310-314` sets 69,632 bytes, and `CON-304` at `:709-733` sets a `1..69632` body with `413` above it. The reference implementation's own non-conformance list at `:1016` records the 4,096-byte body. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` remains effective and states the terminal at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:185-187`. `TEST-1146` at `:258-259` requires the terminal and zero enrollment frames. The independent hub-side guard is real at `cbcl-chat-account-grants.lfe:180-181`. |
| **L-1** — survivor set revives `TEST-057` | **CLOSED** | `CON-978` at `:242-245` states that TEST-057 and TEST-058 do not survive. Cbcl-pairing `CON-025` at `spec-001-0.4.8-split-room-shape-and-length.md:102-105` states the identical set. I compared the two sentences word for word. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED** | `REQ-995` restates the unlink action in full at `:194-198`. `TEST-1146` at `:259-260` states the case inline, and cbcl-bus `TEST-106` requires that inlining. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-996` at `:53-63` orders the four classes and defers length classification until `RoomShape` passes. Cbcl-pairing `REQ-030` at `spec-001-0.4.8-split-room-shape-and-length.md:47-50` carries the same order. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` remains effective and splits the triggers at `:174-183`. `TEST-1146` at `:254-256` keeps both arms. The silent path cannot widen consent, because `REQ-981` keys the policy on the exact pair. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-996` at `:126-135` keeps `migration-display-state-changed` non-terminal and `migration-plan-invalid` terminal. Both remain implementable against the shipped reducer, whose terminal branch returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:171-174`. |

Ten closed.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-980` names nothing in `REQ-981`, which still fixes the policy key as the exact application and relay pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-95`. It still states that a v1 policy entry cannot suppress the first v2 prompt. Cbcl-bus GATE-04 retains the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched. `crates/selfsame-pairing/src/live.rs:479-488` commits one explicit local decision only after `LiveEffect::DisplayIntent`. The five pre-consent checks in `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:108-114` are outside every replacement range and remain ordered. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS AS SPECIFIED** | `CON-980` names nothing in `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202-231`. `REQ-995` still states that local unlink cannot re-grant authority. `TEST-1146` and cbcl-bus `TEST-105` state the re-grant case inline. The specified order is intact. The hub-side REQ-033 upgrade guard that `ADR-939` relies on is separately bypassable under contention, which is `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-H-2]]`. That is a repair this candidate owes rather than a change to the specified order. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-980` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:902-908` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-025` at `spec-001-0.4.8-split-room-shape-and-length.md:96-99` preserves the no-relay-field property of `CON-024`. The size-correlation residual stays bounded above the 62,016-octet sealed cap body. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-331` states that mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery. Every negative path this amendment keeps or adds creates nothing. The 503 result carries an empty body and reveals no binding. The `N6-H-2` failures are storage-contention failures inside the hub, not endpoint failures, so REQ-812 is not the clause they breach. |

## Repository assessments

### selfsame SPEC-008 0.4.21-draft (`94d9c29`)

The ledger half is finished work. Fourteen retirement boundaries, two paragraph
ranges, and one pointer range all resolve uniquely, and the paragraph-initial and
paragraph-final qualifiers do real disambiguation work. `REQ-1000` states its own
resolver scope correctly. I attempted to construct a surviving competing value for
each of the five declared roles, and every attempt failed.

The requirement half is not ready. `NFR-923` recovers the scope the prior candidate
lost and then states a consumer contract that no reviewed consumer satisfies. Its
withdrawal rewrite narrows a predicate a parent SHALL requires. Both defects sit in
the same clause the amendment exists to correct.

Frontmatter resolves. `supersedes`, `adopts`, `conforms-to`, `implementation-baseline`,
and `review-input` all name existing targets. The cross-repository wikilink
`[[SPEC-053-key-root-identity#DEC-012]]` is dead inside this vault, which is an
inherited property of the amendment form rather than a defect this candidate
introduces.

### cbcl-bus SPEC-053 0.16.14-draft (`f4d8151`)

`REQ-076` is exact. Seven paragraph retirements and one three-paragraph range all
resolve uniquely at the pinned commit. The historical replacement chain through
`REQ-072`, `REQ-074`, and `REQ-075` is preserved sentence by sentence. GATE-04
carries 42 boxes, which I counted mechanically, and every box stays open.

`NFR-021` carries the Selfsame narrowing verbatim, so `N6-H-1` and `N6-H-2` are
joint defects. `ADR-032` restates the whole-graph claim, and `TEST-105` and `TEST-106`
mirror the Selfsame tests. The parent `DEC-012` pointer is current at
`specs/SPEC-053-key-root-identity.md:15` and `:688-691`.

### cbcl-pairing SPEC-001 0.4.8-draft (`91e14c9`)

Unchanged from the reviewed revision except the `consumer-design` pointer, which now
names 0.4.21. The split grammar still accepts the same bounded language, and its
alphabet still matches the pinned cbcl-rs recogniser. `CON-025` still states the
survivor set that `CON-978` mirrors. The two typed display types remain absent from
`src/`, which both candidates and every prior review record.

### did-crdt (`1f409a4`)

Unchanged and still deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]] at a 4,096-octet cap.

## Attack surfaces examined and found sound

1. **Reviving a retired ledger through a heading.** `REQ-1000` at `:44-46` adds
   "earlier gate label" to the list of things that do not make conflicting text
   historical. I attempted to source a survivor set, a hub set, a correction set, a
   review set, or a pointer set from every retired region. None supplies one.
2. **Breaking a replacement chain.** I attempted to revive every clause any
   contract replaced, from `TEST-1109` forward and from `ADR-953` forward. Each has
   exactly one current successor.
3. **Reviving the 4,096-octet positive control witness.** I read every occurrence of
   `4,096` in the effective chain. Each is a padded field size, a rendezvous cap, a
   retirement statement, or a negative obligation.
4. **Making a boundary phrase ambiguous.** Two `CON-979` start strings occur twice
   inside their own contract, once as a quoted instruction. The paragraph-initial
   qualifier resolves both. The `CON-978` end string occurs once.
5. **Turning WebFinger contention into false absence.** `NFR-923` at `:134` forbids
   it, and the deployed adapter already separates the three statuses at
   `cbcl-chat-webfinger-http.lfe:36-38`.
6. **Widening consent through the silent re-pin path.** `REQ-981` keys the policy on
   the exact pair, and `REQ-995` splits the silent and prompting triggers.
7. **Nesting a transaction to escape the bound.** Proper nesting increments the
   level and yields a bare `#cyclic{}` at `mnesia_tm.erl:921-924`. Bad nesting yields
   `nested_transaction` at `:822-823`. The mapped terminal set covers both. One
   production nesting exists and is unnamed, which is
   `[[spec-008-0.4.21-claude-adversarial-review-2026-08-23#N6-H-2]]`.
8. **Reaching a WebFinger mutation from production.** The three mutation owners have
   no production caller, so the result change breaks nothing.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later gate
owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and credential/v2
   transport depends on it. Its own non-conformance list at
   `specs/PROTO-002-selfsame-rendezvous-v1.md:1016` records the reference
   implementation's 4,096-byte body.
2. **The whole credential/v2 lane is planned rather than shipped.**
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt durable
   readiness, and the separate owner approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration.**
5. **The unary transaction call sites remain unbounded today.** `NFR-923` mandates a
   change for all of them rather than describing current behaviour.
6. **The typed cbcl-pairing display is a design.** Its two named types are absent
   from `src/`.
7. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** Several artefact references in the amendment
   bodies are plain text rather than wikilinks. The parent
   [[SPEC-008-production-pairing-claimant]] carries both blocks. This is an inherited
   condition of the amendment form.
8. **The exact result form of `authority-busy` is stated for the WebFinger lane
   only.** Even after `N6-H-2` is repaired, an implementer needs the tuple shape for
   each other lane.
9. **Tier 1 also requires a human domain expert** under
   [[PROTO-001-usdd-agent-protocol]]. The cbcl-bus GATE-04 tracks four human review
   boxes, and every one stays open.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation plan.
It authorizes no code, no public allocation, no gate opening, no release, no
deployment, and no production enablement. Every GATE-04, `GATE-00`, `GATE-01`, and
`GATE-03` box stays open. Every human review, every durability obligation, and the
separate owner approval for production deployment also stay open and unaffected.

This review changed no candidate specification, no source, no test, no fixture, no
generated file, and nothing in any sibling repository. It added exactly one file,
which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N6-H-1 | selfsame, cbcl-bus | Restore the reciprocal predicate. Require the requested alias row to name the same account and home DID, and refuse a disagreeing or absent alias row. |
| N6-H-2 | selfsame, cbcl-bus | Name the authority-lane and migration-lane public owners with exact busy results. Add the lane-wide rule that a busy terminal SHALL NOT present as absence, non-membership, or a terminal refusal. Name the three result-discarding sites and their signature changes. Name the `cbcl-chat-account-grants.lfe:180` nesting and require the transaction-only read. |
| N6-M-1 | selfsame | Add `ceremony.rs:190`, `enrollment.rs:666`, and `provider_hint.rs:67` and restate the count, or make the scan rather than the list the obligation. |
| N6-M-2 | selfsame | Retire or restate the `REQ-954` conformance sentence, so one effective requirement names the companion revision. |
| N6-M-3 | selfsame, cbcl-bus, cbcl-pairing | Record the generating model family, version, and session in each candidate, with the tier-1 synthesis trajectory. |
| N6-L-1 | selfsame, cbcl-bus | State the classification criterion inside the retry clause, and separate the schema-migration lane from the ceremony migration graph. |
| N6-L-2 | selfsame, cbcl-bus | Add `4,095` to the refusal row, or rely on the companion exception alone. |
