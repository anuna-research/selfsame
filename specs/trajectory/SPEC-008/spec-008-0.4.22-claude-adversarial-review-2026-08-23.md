---
id: SPEC-008-review-0.4.22-claude-2026-08-23
title: SPEC-008 0.4.22 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.15 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer-model-family: Anthropic Claude
reviewer-model-version: Claude Opus 5 (`claude-opus-5`)
reviewer-session: session_01THgunaUmteqUBa32FS9Qi9
circus-attempt: spec008-v0422-review/1
review-date: 2026-08-23
review-input: "[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces]]"
prior-review: "[[spec-008-0.4.21-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.22 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.15 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Authentication, model, and independence evidence

The launch driver removed the paid-API credential before this session started.
The gate result was recorded first, and no secret value was printed.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; else echo PRESENT; fi
ABSENT
$ env | grep -c '^ANTHROPIC_API_KEY='
0
$ claude auth status --json      # only the three permitted fields are recorded
  "authMethod": "claude.ai"
  "apiProvider": "firstParty"
  "subscriptionType": "max"
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai`, the reported `apiProvider` is `firstParty`, and the reported
`subscriptionType` is `max`. That is the acceptable path.

Account identifiers, electronic-mail addresses, organisation identifiers, and
tokens were read only to check the gate. None appears in this report.

### Reviewer identity

| Field | Value |
|---|---|
| Model family | Anthropic Claude |
| Model | Claude Opus 5, exact identifier `claude-opus-5` |
| Session | `session_01THgunaUmteqUBa32FS9Qi9` |
| Circus attempt | `spec008-v0422-review/1` |
| Circus worktree | `/Users/anuna-01/Code/selfsame-spec008-v0422-review-1` |
| Circus branch | `circus/spec008-v0422-review/1` |

The Circus attempt record is `/Users/anuna-01/Code/selfsame/.git/circus/spec008-v0422-review/1/record.json`.
It carries `"attempt_id": "spec008-v0422-review/1"` and `"state": "prepared"`.

### Distinctness from the generating record

All three candidates now record their generation provenance. The recorded
generating model family is OpenAI GPT-5. The recorded model version is
`gpt-5.6-sol`. The recorded generating session is
`01a029aa-9127-7c42-ad28-81512b91ded6`.

| Property | Generator | Reviewer | Distinct |
|---|---|---|---|
| Model family | OpenAI GPT-5 | Anthropic Claude | yes |
| Model version | `gpt-5.6-sol` | `claude-opus-5` | yes |
| Session | `01a029aa-9127-7c42-ad28-81512b91ded6` | `session_01THgunaUmteqUBa32FS9Qi9` | yes |

Both halves of the [[PROTO-001-usdd-agent-protocol]] cross-model gate are now
checkable. This review is therefore a cross-model review rather than a
same-model fresh-context review.

This session read the candidates, the four repositories, the local OTP
installation, and the pinned dependency sources. It never read the generator
reasoning. It called no Anthropic API and no external web service.

### Executed evidence beyond reading

Four claims in this report rest on execution rather than on source reading.
Each was reproduced in a clean store on the local installation.

The local installation is OTP `28`, `mnesia-4.25.3`, `cowboy-2.12.0`,
`cowlib-2.13.0`, and `lfe-2.1.3`. The probe sources are named inline with each
finding.

## Reviewed commits and baselines, resolved rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `7f0e756cc860cb527479843db6a8ddabc504ffc7` | candidate 0.4.22-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-bus | `2fe615a1651097c5d5b74df4060116e120961c45` | candidate 0.16.15-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| cbcl-pairing | `952226990c3e2473f0e85a99ff664ae6a97cbaa2` | candidate 0.4.8-draft |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |

Every commit resolves to a commit object. The sibling working trees carry
user-owned uncommitted paths. Those repositories were read only through
`git show COMMIT:PATH`, and nothing in them was changed.

The complete effective amendment chain from 0.4.4 forward was read. The 0.4.21,
0.4.20, 0.4.19, 0.4.18, 0.4.17, 0.4.16, and 0.4.15 reviews were read. Both
companion candidates, [[PROTO-002-selfsame-rendezvous-v1]], and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] were read.

Effective authority was resolved by applying every retirement in CON-978,
CON-979, CON-980, and
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#CON-981]] before
current values were compared. Headings, changelogs, and review prose were
treated as supplying no authority.

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | N7-H-1 |
| MEDIUM | 5 | N7-M-1, N7-M-2, N7-M-3, N7-M-4, N7-M-5 |
| LOW | 6 | N7-L-1, N7-L-2, N7-L-3, N7-L-4, N7-L-5, N7-L-6 |

Verdict: **REJECT**.

The ledger work stays closed and the withdrawal repair lands. The reciprocal
alias predicate returns, and a third account's row is now unreachable from any
withdrawal branch. The companion conformance target is singular for the first
time in the chain, and all three candidates record their generating model.

The rejection rests on the new busy contract itself. The current test text
demands a terminal from `lookup/1` that the requirement forbids, and the
deployed adapter turns that terminal into HTTP 404. The tagged-success change
carries a consumer obligation for the failure value and none for the success
value. Two of the three named exhaustion classes cannot be produced at a
conforming wrapper. The owner enumeration and the named nesting repair both
miss one production site.

---

## Findings

### N7-H-1 (HIGH, release-blocking) — the current test demands `authority-busy` from `lookup/1`, which the same requirement forbids, and the deployed adapter answers HTTP 404

**Repository owner: selfsame (TEST-1148) and cbcl-bus (TEST-107).**

`NFR-924` at
`specs/trajectory/SPEC-008/spec-008-0.4.22-explicit-busy-contracts-and-complete-traces.md:259-268`
names the WebFinger public owners and fixes their terminals:

> The WebFinger public owners remain `provision/3`, `withdraw/2`, `reinstate/2`,
> and `lookup/1`. … WebFinger mutations return `#(error authority-busy)` on retry
> exhaustion. `lookup/1` returns `unavailable` for that class.
>
> `cbcl-chat-webfinger-http` maps `unavailable` to HTTP 503. … Contention never
> becomes `missing` or HTTP 404.

`TEST-1148` at `:379-383` states the opposing obligation:

> Exercise every public owner named by NFR-924. Force `nomore`, exhausted
> `#cyclic{}`, and prohibited nesting.
>
> Require `#(error authority-busy)`, zero partial effect, and unchanged
> non-contention meanings.

`lookup/1` is a public owner named by `NFR-924`. The test therefore requires
`#(error authority-busy)` from a function the requirement requires to return
`unavailable`. No implementation satisfies both clauses.

**The predecessor did not have this conflict.** `TEST-1146` at
`specs/trajectory/SPEC-008/spec-008-0.4.21-singular-ledgers-and-whole-graph-retries.md:292-293`
qualified the terminal by lane:

> Require **the lane's exact** `authority-busy` terminal, no partial effect,
> bounded progress, and unchanged non-contention results.

`TEST-1146` then carried a separate WebFinger paragraph at `:296-298`:

> Force the WebFinger terminal through `lookup/1`. Require `unavailable`, HTTP
> 503, an empty `no-store` response, and no HTTP 404. Successful and missing
> lookups retain their existing responses.

`TEST-1148` drops the lane qualifier and drops that paragraph. A search of the
whole candidate returns no occurrence of `503`, `no-store`, or `404` inside any
test. Cbcl-bus `TEST-107` carries the identical regression, and its
`TEST-108` carries no WebFinger read row either.

**The wrong resolution is the deployed default.** `cbcl-chat-webfinger-http:get/2`
at `apps/cbcl_chat/src/shell/cbcl-chat-webfinger-http.lfe:36-37` reads:

```lfe
('unavailable (empty 503 req state))
(_            (empty 404 req state))
```

An implementer who follows `TEST-1148` makes `lookup/1` answer
`#(error authority-busy)`. The wildcard at `:37` maps that value to HTTP 404.
Storage contention then publishes a false absence about an identity binding.
`NFR-924` at `:268` forbids exactly that outcome. The 0.4.19 finding `N4-M-1`
closed exactly that outcome.

`NFR-924` at `:243-244` adds a rule that cannot be satisfied here either:

> All production consumers handle `#(error authority-busy)` explicitly. A
> wildcard branch cannot reinterpret it as a non-busy result.

Applied to `cbcl-chat-webfinger-http`, that rule requires an explicit
`authority-busy` branch in a consumer whose owner the same requirement says
never returns `authority-busy`. The three clauses close a circle.

[[PROTO-001-usdd-agent-protocol]] Error Response states that a conflicting
specification terminates the task rather than resolving it. This conflict sits
at a public read boundary whose whole purpose is to avoid asserting absence.

**Minimum sound correction.** Restore the lane qualifier in `TEST-1148` and
`TEST-107`, so the required terminal is the lane's terminal. Restore the
`lookup/1` row requiring `unavailable`, HTTP 503, an empty `no-store` body, and
no HTTP 404. State that the WebFinger read owner is exempt from the
`authority-busy` result form.

---

### N7-M-1 (MEDIUM, gate-blocking) — the tagged-success change states a consumer obligation for the failure value and none for the success value

**Repository owner: selfsame (NFR-924) and cbcl-bus (NFR-022).**

`NFR-924` at `:200-202` changes the result form of every collection and
predicate owner:

> Public collection and predicate owners use tagged success. They return
> `#(ok Value)` or `#(error authority-busy)`. Busy never becomes an empty list,
> `false`, `undefined`, `exists`, absence, or non-membership.

The clause names fifteen such owners across five modules. It then states one
consumer obligation, at `:243-244`, and that obligation covers the failure value
alone:

> All production consumers handle `#(error authority-busy)` explicitly. A
> wildcard branch cannot reinterpret it as a non-busy result.

Nothing requires a consumer to be adapted to the `#(ok Value)` **success**
shape. An implementer who satisfies the letter adds an `authority-busy` clause
to each consumer and leaves the success path untouched.

**The success shape then reaches boolean contexts.** LFE 2.1.3 on OTP 28.5 was
probed in a clean module rather than reasoned about:

| Construct | Argument `#(ok false)` | Result |
|---|---|---|
| `(if X …)` | non-boolean | `case_clause` error |
| `(cond (X …))` | non-boolean | `case_clause` error |
| `(andalso 'true X)` | non-boolean | returns `#(ok false)`, no error |
| `(orelse X 'sentinel)` | non-boolean | `badarg` error |
| `(not X)` | non-boolean | `badarg` error |

`andalso` is the dangerous row, because it carries the wrong shape past the
point of the change. `cbcl-chat-room.lfe:263-270` builds the admission channel
map with exactly that construct:

```lfe
'account-grant (andalso (=:= vis 'private)
                        (andalso (is_binary account)
                                 (cbcl-chat-account-roommember:re-admit? room account handle)))
'key-grant     (andalso (=:= vis 'private)
                        (cbcl-chat-roommember:re-admit? room handle pk))
```

Both owners are named ceremony-migration owners. A `#(ok true)` value therefore
enters the map silently. `cbcl-core-admission:branch-a` at
`apps/cbcl_chat/src/core/cbcl-core-admission.lfe:103` reads `key-grant` as a
`cond` test, and `account-admitted?` at `:161` reads `account-grant` through
`orelse`. Both raise rather than refuse.

The module states the invariant that breaks. Its container guard comment
records the consequence of a non-map channel:

> ABORT rather than refuse, which is an availability failure at the auth
> boundary

The signature change reintroduces that availability failure through the map's
values instead of its shape.

The same shape reaches four further production consumers.
`cbcl-chat-pairgrant.lfe:78` applies `not` to `member-grant?`.
`cbcl-chat-session-ws.lfe:1050` uses `member-grant?` as an `if` test.
`cbcl-chat-session-ws.lfe:869` passes `rooms-for-account` to `lists:foreach`.
`cbcl-chat-session-ws.lfe:347` and `:385` feed `unratified-device?` to the pure
core, whose fail-closed clause then refuses every frame.

No path admits, so this is not an authority bypass. Every path either crashes
the socket process or refuses, which makes private-channel join and Path-B
admission unavailable. That is the SPEC-055 outage class the surrounding
comments exist to prevent.

`TEST-1148` does not catch it. It exercises owners and the busy consumers, and
it states no success-shape consumer row.

**Minimum sound correction.** State that every production consumer of a
tagged-success owner is adapted to the `#(ok Value)` form as well as to the
busy form. Name the boolean-context sites that accept a non-boolean without
raising, beginning with `cbcl-chat-room.lfe:263-270`. Add a test row requiring
the admission decision to remain total under both result forms.

---

### N7-M-2 (MEDIUM, gate-blocking) — two of the three named exhaustion classes cannot be produced at a conforming wrapper, and the nesting classification cannot fire

**Repository owner: selfsame (NFR-924, TEST-1148) and cbcl-bus (NFR-022, TEST-107).**

`NFR-924` at `:196-197` states the classification:

> The shared wrapper classifies `nomore`, exhausted `#cyclic{}`, and prohibited
> nesting as retry exhaustion.

`TEST-1148` at `:379-380` requires all three to be forced at every public owner:

> Exercise every public owner named by NFR-924. Force `nomore`, exhausted
> `#cyclic{}`, and prohibited nesting.

The classes were re-derived at the local OTP 28.5 installation and then
reproduced in a clean Mnesia store.

**`#(aborted #cyclic{})` never reaches a level-1 caller.** `check_exit/7` at
`mnesia_tm.erl:906-916` routes every `#cyclic{}` into `maybe_restart/6`.
`maybe_restart/6` at `:918-927` restarts while `try_again/1` answers `yes` and
the level is one, and otherwise returns `{aborted, nomore}`. Forty concurrent
transactions with opposed lock order and `Retries` of `3` produced exactly two
outcomes:

```text
outer tally = #{atomic => 5, {aborted,nomore} => 35}
```

`#(aborted #cyclic{})` did not occur. Under the requirement's own rule that
every affected transaction runs at level one, it cannot occur.

**A body that opens a transaction does not abort.** The prohibited condition
`NFR-924` names at `:163` is a transaction body that opens a transaction. OTP
treats that as proper nesting:

```text
1 proper-nesting outer = {atomic,{inner_returned,{atomic,[]}}}
```

The wrapper sees `{atomic, …}`. The classification cannot fire for the
condition it is named after.

**`{aborted, nested_transaction}` requires a corrupted activity state.**
`transaction/6` at `mnesia_tm.erl:811-823` returns that value only from its
final clause, which matches when `mnesia_activity_state` is neither `undefined`
nor a three-tuple. Nothing in Mnesia writes such a value. The probe reached it
only by writing the process dictionary directly:

```text
2 bad-nesting = {aborted,nested_transaction}
```

So the classification names three classes and only one is observable. The
second is a restatement of the first. The third is unreachable without
corrupting a third-party library's process state. `TEST-1148` therefore requires
two unrunnable forcings at every public owner.

Under [[PROTO-001-usdd-agent-protocol]] evidence discipline, an unrunnable gate
row is recorded `unverified` and blocks the phase, or it is falsely passed. The
runtime control against nesting is separately ineffective. The static inventory
is therefore the only real control, and the requirement presents it as one of
two.

**Minimum sound correction.** Reduce the wrapper's exhaustion class to
`{aborted, nomore}` and say so. State that prohibited nesting is a static
inventory obligation with no runtime terminal, because proper nesting commits
silently. Remove the two unrunnable forcings from `TEST-1148` and `TEST-107`.

---

### N7-M-3 (MEDIUM, gate-blocking) — one production transaction is authority-lane, opens a nested transaction, and appears in neither the owner list nor the nesting repair

**Repository owner: selfsame (NFR-924, TEST-1148) and cbcl-bus (NFR-022, TEST-107).**

`NFR-924` at `:173-191` derives lane membership from transitive
transaction-body table access, and at `:165-171` makes an omitted or
unsupported out-of-scope row non-conforming. Every production
`mnesia:transaction` call under `apps/` was enumerated at the declared cbcl-bus
baseline. There are 70 call sites in 20 modules.

`apps/cbcl_chat/src/shell/cbcl-chat-successor.lfe:49` is one of them. Its body
reaches two authority tables:

| Edge | Target | Table |
|---|---|---|
| `held-the-handle?/2` at `:109` | `cbcl-chat-members:pubkey-tx/1` | `cbcl-member` |
| `account-accepted?/2` at `:134` | `cbcl-chat-account-grants:device-grant/2` | `cbcl-device-grant` |

By the requirement's own rule the row is authority-lane. `NFR-924` lists
fifteen authority owners at `:204-215` and `cbcl-chat-successor:accept/3` is not
among them. Cbcl-bus `NFR-022` carries the same omission.

**The second edge is a nested transaction.** `device-grant/2` at
`cbcl-chat-account-grants.lfe:236` opens its own `mnesia:transaction`. `NFR-924`
at `:240-241` names exactly one nesting to repair:

> `bind-and-store-unbound/8` calls `cbcl-chat-members:pubkey-tx/1`. It SHALL NOT
> call transaction-opening `pubkey/1`.

`TEST-1148` at `:400-401` instruments the same single site. A cross-module call
scan over every in-lane module found no other nesting, so this is the one
production nesting edge the candidate does not name.

**Both guards catch the abort the wrapper depends on.** `mnesia.erl:800-801`
states the rule directly:

> a transaction needs to be restarted, thus a `Fun` must not catch `exit`
> exceptions with reason `{aborted, term()}`.

`held-the-handle?/2` wraps `pubkey-tx/1` in `catch`, and `account-accepted?/2`
wraps `device-grant/2` in `catch`. A probe reproduced the consequence. A `catch`
around an in-transaction read swallowed the cyclic abort, and the enclosing
transaction committed a later write:

```text
catch-bare-read-in-txn -> {atomic,{guard,{'EXIT',{aborted,{cyclic, …}}}}}
z row after = [{t,z,caught}]
```

So lock contention makes `held-the-handle?/2` answer `'false`, and `accept/3`
commits `#(error not-the-handles-key)`. Contention becomes a false
non-membership answer, which `ADR-959` at `:353-355` forbids by name.

`accept/3` has no production caller today. Only `ensure-table/0`, `spent?/1`,
`for-account-handle/2`, and `bare/1` are reached from production, so the runtime
consequence is currently nil. The row is still production code under `apps/`,
and the candidate's inventory cannot be produced from the candidate's own list.

**Minimum sound correction.** Add `cbcl-chat-successor:accept/3` to the
authority owner list with its exact busy result. Name its
`account-accepted?/2` nesting edge and require the transaction-only read
there. Require that no transaction body catch an `{aborted, _}` exit, and name
`:109` and `:134`.

---

### N7-M-4 (MEDIUM, gate-blocking) — the "complete" eighteen-site v1 inventory omits one production comparison

**Repository owner: selfsame (REQ-1001, TEST-1149).**

`REQ-1001` at `:75-76` introduces the block with the word "complete":

> The complete current v1 comparison and substitution inventory has these
> eighteen baseline sites:

All eighteen lines were checked at the declared implementation baseline. Each
resolves to the construct its label claims. The three sites the 0.4.21 finding
`N6-M-1` named are now present, at `ceremony.rs:190`, `enrollment.rs:666`, and
`provider_hint.rs:67`.

An independent scan of every production Rust source found one comparison the
list omits.

| Site | Construct |
|---|---|
| `crates/selfsame-app-identity/src/enrollment.rs:592` | `|| statement.profile_version != observed.offer.profile_version` |

The site sits in `verify/2`, forty lines below the listed comparison at
`enrollment.rs:552`, and its own comment at `:581` names the member:

> *Every* one. `applicationId`, `profileVersion`, and `profileDigest` are shared
> members too, and checking them against the profile above is a different check.

It is not a member-name constant, a field read, a constant definition, a format
template, or a comment. It is the exact construct the inventory labels
"comparison" at `:552`.

The candidate also states the scan rule at `:99-100`:

> That inventory is evidence at the named baseline. It is not finite permission.
> A repository-wide production scan remains the completeness authority.

Those two sentences pull against each other, and `TEST-1149` at `:438-442`
pulls a third way. It requires "all eighteen named v1 sites" and then requires
the scan to decide completeness. A checker built from eighteen lines
under-covers the v1-freeze audit that a cbcl-bus GATE-04 box depends on.

Three production field substitutions are the same construct as the listed
`web-device/src/lib.rs:1369` and are also absent:
`crates/selfsame-web-device/src/lib.rs:831`, `:847`, and `:861`. Each copies a
parsed browser version field into a typed struct. They are recorded here as
class-boundary cases rather than as certain omissions.

**Minimum sound correction.** Add `enrollment.rs:592` and restate the count, or
delete the word "complete" and the numeral so the scan is the sole obligation.
State whether a field-to-field copy on an ingest path is a substitution.

---

### N7-M-5 (MEDIUM, gate-blocking) — the replacement test drops four inline cases that closed three prior findings

**Repository owner: selfsame (TEST-1148, TEST-1149) and cbcl-bus (TEST-107, TEST-108).**

`CON-981` at `:295` replaces `TEST-1146` in full. `TEST-1146` opened with three
paragraphs that `TEST-1148` does not restate.

| Dropped case | `TEST-1146` lines | Finding it closed |
|---|---|---|
| Profile-digest silent re-pin, and the authority or issuer prompt arm | `:254-256` | 0.4.15 `L-4` |
| Handle change while installed, and unlink confirm and cancel | `:258-260` | 0.4.15 `M-4` and `L-2` |
| Fresh ceremony with the retained key after grant deletion | `:262-264` | 0.4.17 `N2-M-2` |
| Restored acquisition point and invite-before-room order | `:266-268` | 0.4.16 `N-M-2` |

Each of those findings was closed on the ground that the current test restates
the case **inline**. The 0.4.17 review recorded the reason for that standard:
It raised `N2-M-2` precisely because an open-ended reference then carried the
case.

`TEST-1149` at `:454-455` now carries the only successor, and it is an open-ended
phrase:

> Require all 42 cbcl-bus GATE-04 boxes. Require both non-conforming rendezvous
> baselines, sealed cap arithmetic, and every installed-state case.

"Every installed-state case" has no current enumerating artefact. `TEST-1132`,
whose title was "Installed state refreshes, rotates, unlinks, and re-grants", is
inside the `TEST-1129` through `TEST-1133` range that `CON-981` at `:318`
records as replaced.

The obligations themselves survive. `REQ-995` at
`spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:174-205` states
the silent re-pin, the authority-change prompt, the
`account-device-handle-change-refused` terminal, the confirmed unlink, and the
fresh-ceremony re-grant. `NFR-921` still owns the acquisition point. So this is
a verification regression rather than a requirement loss.

Cbcl-bus `TEST-107` drops the identical four paragraphs from `TEST-105`, and
`TEST-108` carries the same open-ended phrase.

**Minimum sound correction.** Restate the four dropped cases inline in
`TEST-1148` and `TEST-107`, as every predecessor since 0.4.16 did. Replace
"every installed-state case" with a reference naming `REQ-995` and its clauses.

---

### N7-L-1 (LOW) — two enumerated authority owners are ceremony-migration rows under the requirement's own classifier

`NFR-924` at `:173` derives lane membership from transitive transaction-body
table access. Rule 1 puts `cbcl-device-grant`, `cbcl-member`, and
`cbcl-pairaccount-grant` in authority. Rule 2 puts `cbcl-account-binding` in
ceremony migration.

`account-binding/1` at `cbcl-chat-account-grants.lfe:70` reads only
`cbcl-account-binding` inside its transaction. `bind-account/2` at `:84` reads
and writes only that table. Both are ceremony-migration rows by the rule, and
both appear in the authority owner list at `:206-208`.

No behaviour follows, because `NFR-924` at `:197-198` maps authority and
ceremony migration to the same `#(error authority-busy)` result. `TEST-1149`
nevertheless requires "table-derived classifications", so a conforming inventory
disagrees with the requirement's own prose for two rows.

**Minimum sound correction.** Move both owners to the ceremony-migration list,
or state that the enumerated grouping is by module rather than by lane.

---

### N7-L-2 (LOW) — replacing REQ-954 in full drops the parent REQ-903 and REQ-904 dispositions

`REQ-1001` at `:53-54` replaces
[[spec-008-0.4.5-standalone-tofu-first-contact#REQ-954]] in full. `REQ-954`
carried a second paragraph at `:88-93` that `REQ-1001` does not restate:

> SPEC-008 REQ-903 is retained: the wallet has live approve/decline … SPEC-008
> REQ-904 is retained for the machine carrier: scan and paste decode to the same
> recognized carrier type. Its scope does not include the separate PAIR1 field,
> whose no-paste rule is an intentional co-presence constraint.

The 0.4.4 inherited-stop table at `:140-165` was read row by row. It records
SPEC-007 `REQ-806` and `REQ-807`, and it records no SPEC-008 `REQ-903` or
`REQ-904` row. `REQ-954` was the only chain record of those two dispositions,
so the 0.4.5 finding `L-9` reopens.

No obligation is lost in substance. The parent requirements remain effective in
[[SPEC-008-production-pairing-claimant]], and the PAIR1 no-paste rule stands on
its own at `spec-008-0.4.4-standalone-tofu-first-contact.md:245-249`.

**Minimum sound correction.** Restate the two parent dispositions in `REQ-1001`,
or add them to the 0.4.4 inherited-stop table by an exact supplement.

---

### N7-L-3 (LOW) — the replacement pointer paragraph drops the open-gate half

`CON-980` at `:219-220` required the parent pointer to do two things:

> The Selfsame parent [[SPEC-008-production-pairing-claimant]] points to this
> candidate **and names its open review gate.**

`CON-981` at `:325-334` replaces that paragraph with "points here" and drops the
gate clause. Every predecessor from 0.4.17 through 0.4.21 carried it.

The parent satisfies the dropped obligation today. `specs/SPEC-008-production-pairing-claimant.md:18`
reads "Its coordinated Tier-1 review gate is open." So the obligation is
unstated rather than violated.

The replacement also introduces an unusual anchor,
`[[SPEC-008-production-pairing-claimant#SPEC-008--Production-Pairing-Claimant-Transport-Real-Credential-and-Origin-Trust]]`.
The parent heading is `# SPEC-008 — Production Pairing Claimant: Transport, Real Credential, and Origin Trust`.
The anchor is neither the literal heading nor a conventional slug, and `zetl`
0.9.3 validates no anchor, so nothing catches its rot.

**Minimum sound correction.** Restore the open-gate clause and use an artefact
anchor rather than a title anchor.

---

### N7-L-4 (LOW) — the name-squatting sentence is unscoped, and the check's other input stays out of lane

`NFR-924` at `:256-257` states an unqualified property:

> Name-squatting checks propagate busy to the 1013 boundary. They cannot report
> that a name is free.

The check is `addagent-target-free?/3` at
`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:1047-1052`. It has two
inputs. The membership half is `cbcl-chat-roommember:member-grant?/2`, which the
candidate brings in lane. The ownership half is
`cbcl-chat-provenance:added-by/2`, whose table `cbcl-provenance` matches no lane
rule and is therefore out of scope by rule 6.

The provenance branch keeps its wildcard:

```lfe
(case (cbcl-chat-provenance:added-by room name)
  (`#(ok ,adder) (if (=:= adder requester) 'ok #(error #"name-taken")))
  (_ (if (cbcl-chat-roommember:member-grant? room name) …)))
```

A provenance read that fails falls through to the narrower membership question.
A name owned by a different adder can then be reported free. The trigger is a
non-retryable abort rather than contention, and `cbcl-chat-app.lfe:67` waits for
`cbcl-provenance` at boot, so the window is narrow.

Under the charitable reading the sentence means "busy cannot be reported as
free", and there is no defect. The sentence does not say so.

**Minimum sound correction.** Scope the sentence to the busy class, and record
the provenance wildcard as an accepted out-of-lane residual.

---

### N7-L-5 (LOW) — the recorded Tier-1 synthesis trajectory is a summary line rather than the enumerated trajectory

[[PROTO-001-usdd-agent-protocol]] requires five things in a Tier-1 record:

1. the initial draft;
2. the failing tests and attribution map at each iteration;
3. the localised feedback at each repair step;
4. the successive drafts with the precise change at each step; and
5. the adversarial tests with their disposition.

It states that end-to-end supervision without the intermediate trajectory is
insufficient.

All three candidates record a single line. The Selfsame value is
`"0.4.21 REJECT; production-code re-trace; coordinated Selfsame, cbcl-bus, and cbcl-pairing correction"`.
That names an input, a method, and an output, and it supports no inspection of
how the artefact was derived.

The severity is LOW rather than higher because the amendment chain itself
supplies the substance at version granularity. `specs/trajectory/SPEC-008/`
holds 23 successive drafts and 22 adversarial reviews. Those reviews carry the
attribution and the localised feedback in their disposition tables. What is
absent is the intra-session record for this generation.

The blocking half of the 0.4.21 finding `N6-M-3` closes. Model family, model
version, and session are recorded in all three candidates, so distinctness is
now checkable.

**Minimum sound correction.** Point the trajectory field at the chain artefacts
that constitute it, or record the intra-session drafts and repairs.

---

### N7-L-6 (LOW) — the schema-migration lane names one site, and a second schema migration exists over a ceremony-migration table

`NFR-924` rule 5 at `:185-186` defines the schema-migration lane by a single
function:

> Schema migration contains `cbcl-store-migrate:legacy-rows/0` over `cbcl-msg`.

Rule 5 names a function rather than a table, which breaks the table-derived form
of the other four rules. `NFR-924` at `:190-191` then states a universal:

> Schema migration never overlaps those runtime lanes.

`cbcl-chat-roommember:migrate-since/0` at
`apps/cbcl_chat/src/shell/cbcl-chat-roommember.lfe:64-70` is a second schema
migration, and it transforms `cbcl-roommember`, a ceremony-migration table. The
universal is therefore false for the repository, and true only for the one named
site.

Nothing is misclassified today, because `migrate-since/0` uses
`mnesia:transform_table/4` rather than `mnesia:transaction`, so it produces no
inventory row. It already fails closed: `cbcl-chat-app.lfe:87-90` refuses to
open the listener when the migration cannot be confirmed.

**Minimum sound correction.** Scope the universal to the named site, and record
`migrate-since/0` as a second schema migration outside the transaction
inventory.

---

## Disposition of every 0.4.21 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N6-H-1** — the withdrawal rewrite drops the reciprocal alias check | **CLOSED** | `NFR-924` at `:133-140` restores the reciprocal predicate. The write-locked alias row SHALL name the same account and home DID, and an absent or disagreeing alias row returns `#(error inconsistent-binding)`. The prior attack was re-run on the specified branch. With alias row `{U, B, D}` and account row `{A, U, D}`, the account row agrees on the requested alias and DID, the alias row names `B`, and the branch refuses without deleting. The narrower design also refuses a store whose account row names an alias that is not `f(homeDid)`, which the deployed `tombstone/2` at `cbcl-chat-webfinger-store.lfe:206-229` would have deleted. `TEST-1148` at `:409-416` drives the deleted-alias case, the wrong-account case, and the wrong-DID case, and requires zero deletion in each. Cbcl-bus `NFR-022` at `:104-108` carries the identical text. |
| **N6-H-2** — the retry mandate stated a consumer contract the code contradicts | **CLOSED IN SUBSTANCE, REOPENED IN PARTS** | `NFR-924` at `:204-244` names every authority-lane and ceremony-migration public owner with an exact busy result, states the lane-wide rule that busy never becomes absence or non-membership, names the three result-discarding sites, and names the `cbcl-chat-account-grants.lfe:180` nesting with its transaction-only repair. Every leg was checked against the baseline and the enumeration matches the module exports for nine of ten in-lane modules. Three defects remain in the replacement: `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-1]]`, `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-2]]`, and `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-3]]`. One prior mechanism claim was also incorrect and is corrected below. |
| **N6-M-1** — the "complete" v1 trace omits three production substitution sites | **CLOSED AS RAISED, REOPENED ON COUNT** | `ceremony.rs:190`, `enrollment.rs:666`, and `provider_hint.rs:67` are all present in the eighteen-line block, and each resolves at the baseline. An independent scan found `enrollment.rs:592`, which is `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-4]]`. |
| **N6-M-2** — one of two superseded conformance SHALLs survives | **CLOSED** | `REQ-1001` at `:53-54` replaces `REQ-954` in full, and `CON-981` at `:292` records the replacement. A whole-chain search for "SHALL conform" returns four hits. `0.4.5:80` is inside the replaced `REQ-954`. `0.4.6:91` is inside `REQ-963`, which `REQ-1000` replaced and `REQ-1001` succeeds. `0.4.21:53` is inside the replaced `REQ-1000`. Exactly one effective sentence survives, at `0.4.22:68`, and it names cbcl-pairing SPEC-001 0.4.8-draft. The frontmatter `conforms-to` value agrees. |
| **N6-M-3** — no candidate records its generating model | **CLOSED ON THE GATE, PARTIAL ON THE TRAJECTORY** | All three candidates carry `generation-model-family`, `generation-model-version`, `generation-session`, and `generation-synthesis-trajectory`, and each repeats the record in a Tier-1 provenance section. The recorded values agree across the three repositories. Distinctness from this reviewer is now checkable and is recorded above. The trajectory field is a summary line, which is `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-L-5]]`. |
| **N6-L-1** — the lane names do not decide every site | **CLOSED** | `NFR-924` at `:173-191` states a table-derived criterion inside the requirement, separates the schema-migration lane from ceremony migration, fixes terminal precedence, and states that the nonce allocator's collision loop is a semantic uniqueness retry rather than a contention retry. The criterion was applied to all 70 production sites. It decides every site, including the five `cbcl_router` sites over `cbcl_mnemonic_store` and `cbcl_agent_registry`, which are out of scope. Two residual classification points are `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-L-1]]` and `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-L-6]]`. |
| **N6-L-2** — the control row omits one refusal | **CLOSED** | `TEST-1148` at `:421-425` now refuses 0, 2,049, 4,095, and 4,096 octets, and accepts 1, 23, 24, 255, 256, 2,047, and 2,048. That matches cbcl-pairing `TEST-052` exactly. Cbcl-bus `TEST-107` at `:355-358` carries the same row. |

Two closed cleanly. Two closed as raised and reopened on a narrower point. One
closed in substance with three new defects in the replacement. One closed on its
blocking half. One closed with two residual classification points.

### A prior mechanism claim corrected

The 0.4.21 review held that the nested `pubkey/1` call at
`cbcl-chat-account-grants.lfe:180` fails open. Its stated mechanism was a cyclic
abort reaching `pubkey/1` and answering `#(error not-enrolled)`. That mechanism
is incorrect at OTP 28.5.

`return_abort/4` at `mnesia_tm.erl:1021-1050` exits rather than returns when the
level exceeds one and the reason is `#cyclic{}`. A probe confirmed it. The inner
call produced `{'EXIT', {aborted, {cyclic, …}}}`, which propagates because
`bind-and-store-unbound/8` installs no `catch`. The outer transaction restarts
at level one instead.

The repair `NFR-924` mandates is still correct, for the level-one rule and for
the write-lock strength `pubkey-tx/1` takes. The two level-one fail-open
mechanisms the 0.4.21 review found are real. `pubkey/1` at
`cbcl-chat-members.lfe:28-30` and `member-grant?/2` at
`cbcl-chat-account-roommember.lfe:94-97` both run at level one, where
`{aborted, nomore}` reaches their wildcard branches.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-981` names nothing in `REQ-981`, which remains effective at `spec-008-0.4.11-standalone-tofu-first-contact.md:51`. Policy stays keyed on the exact `(applicationId, relayOrigin)` pair, and a v1 entry cannot suppress the first v2 prompt. Cbcl-bus GATE-04 retains the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched at the declared baseline. `crates/selfsame-pairing/src/live.rs:479-488` commits one explicit local decision only after `LiveEffect::DisplayIntent`. Cbcl-pairing 0.4.8 changed only its `consumer-design` pointer and its provenance block, which was checked by diffing the candidate against its own predecessor. `REQ-030` and `NFR-022` keep the verdict-only verifier and the private typed display. The five pre-consent checks in `REQ-996` remain outside every replacement range. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS AS SPECIFIED** | `CON-981` names nothing in `CON-962`, whose effect order stands. Every busy path the candidate adds creates nothing: the WebSocket boundary emits no acknowledgement, refusal, invite, grant, or enrollment frame, and no partial effect survives an exhausted transaction. `REQ-995` still states that local unlink cannot re-grant authority. The inline test for that case is dropped, which is `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-5]]`. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-981` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:902-908` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-025` preserves the no-relay-field property of `CON-024`, and its protocol body is byte-identical to the reviewed revision. The size-correlation residual stays bounded above the 62,016-octet sealed cap body. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause states that mismatch, protocol violation, cancellation, relay closure, and expiry produce no accepted credential and no identity side effect before approved payload delivery. Every negative path this amendment keeps or adds creates nothing. The 503 response carries an empty body and reveals no binding. The busy close emits no frame, so no pending ceremony exists to be resumed. `N7-H-1` and `N7-M-3` are hub storage failures rather than endpoint failures, so REQ-812 is not the clause they breach. |

`CON-981` at `:339-340` restates every F-A through F-E correction and
`[[SPEC-007-cbcl-pairing-cutover#REQ-812]]` as hard stops.

## Repository assessments

### selfsame SPEC-008 0.4.22-draft (`7f0e756`)

The withdrawal repair is finished work. The reciprocal predicate is stated over
both rows, the refusing branches delete nothing, and the idempotent
absent-account branch stays non-mutating. Three separate damaged-store shapes
were constructed against the specified branch and none produced a delete.

The authority resolver is finished work. Every retirement boundary in `CON-981`
resolves uniquely. The `CON-980` current-value range has three occurrences of
its start string, and only the paragraph at `:201` is paragraph-initial and a
current value. The end string has three occurrences, and only `:213` follows the
start. The pointer range resolves the same way. The five declared roles each
resolve to one current value.

The owner enumeration is close to complete. It matches the module exports for
nine of ten in-lane modules, and the tenth omission is
`[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-3]]`.

The busy contract is not ready. Its test text contradicts its requirement text
at the WebFinger read boundary. Its consumer obligation covers one of two result
forms. Two of its three exhaustion classes cannot be produced.

The test half regresses. `TEST-1148` is shorter than `TEST-1146` by four inline
cases and one WebFinger paragraph, and each of those closed a prior finding.

Frontmatter resolves. `supersedes`, `adopts`, `conforms-to`,
`implementation-baseline`, `review-input`, and the four generation fields all
name existing targets or recorded values. The cross-repository wikilink
`[[SPEC-053-key-root-identity#DEC-012]]` is dead inside this vault, which is an
inherited property of the amendment form.

### cbcl-bus SPEC-053 0.16.15-draft (`2fe615a`)

`REQ-077` is exact. Its single retirement boundary resolves uniquely at the
pinned commit, and the historical chain through `REQ-072`, `REQ-074`, `REQ-075`,
and `REQ-076` is preserved.

GATE-04 carries 42 boxes, counted mechanically, and every box stays open. The
box list restates every earlier obligation and adds a generation-provenance
clause to the first box.

The hub test set agrees with the Selfsame ledger word for word. Both name
TEST-043 through TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, TEST-098,
TEST-107, and TEST-108.

`NFR-022` carries the Selfsame busy contract almost verbatim, so `N7-M-1`,
`N7-M-2`, and `N7-M-3` are joint defects. `TEST-107` carries the Selfsame test
regressions, so `N7-H-1` and `N7-M-5` are joint defects.

The parent `DEC-012` pointer is current at
`specs/SPEC-053-key-root-identity.md`, and `REQ-077` requires the Selfsame
parent to point at 0.4.22.

### cbcl-pairing SPEC-001 0.4.8-draft (`9522269`)

The protocol body is unchanged from the reviewed revision. The candidate diff
against its predecessor adds only the four generation fields, the Tier-1
provenance section, and the `consumer-design` pointer at 0.4.22-draft.

The split grammar still accepts the same bounded language. `CON-025` still
states the survivor set that `CON-978` mirrors. The 33-octet handle, 129-octet
room, 256-room count, and 33,793-octet array are unchanged, and the array
arithmetic recomputes exactly.

The two typed display types remain absent from `src/`, which both candidates and
every prior review record.

### did-crdt (`1f409a4`)

Unchanged. `src/service/rendezvous.rs:65` still reads
`pub const MAX_SLOT_BYTES: usize = 4096;`, so it remains deliberately
non-conforming to [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. The in-repository
baseline at `crates/selfsame-rendezvous/src/lib.rs:48` carries the same value.

## Attack surfaces examined and found sound

1. **Deleting a third account's alias row through withdrawal.** Three damaged
   stores were constructed against the specified branch. An alias row naming a
   different account refuses. An alias row naming a different DID refuses. An
   absent alias row refuses. None deletes.

2. **Deleting a row through the idempotent branch.** The absent-account branch
   reaches `already-withdrawn/2`, which performs an indexed read and returns.
   `NFR-921` keeps that branch free of any later write lock, and `NFR-924`
   restates it as non-mutating.

3. **Inverting the WebFinger lock order.** `NFR-921` at `:123-126` already
   requires `provision/3` and `create-or-return/3` to reorder their current
   withdrawn-first reads to alias, account, withdrawal. `NFR-924` puts the
   withdrawal lock last on the live branch. Every named operation is an ordered
   subsequence of alias, account, withdrawal, so no cross-table cycle exists.

4. **Escaping the bound by nesting a transaction.** Proper nesting was probed
   rather than reasoned about. It commits at level two and returns
   `{atomic, …}`, and a level-two cyclic abort exits to the outer transaction.
   No nesting escapes a level-one bound. The classification of that condition is
   separately ineffective, which is `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-2]]`.

5. **Emitting close code 1013 from Cowboy.** `cow_ws:frame/2` at
   `cowlib/src/cow_ws.erl:641-644` packs the status code with no validation, and
   `cowboy_websocket:commands/3` at `:606-613` sends the frame and stops. A
   17-octet close payload is inside the 125-octet control limit. The server half
   is implementable. The receive-side `validate_close_code/1` at `:504-511`
   rejects 1013, and the `close_code()` type at `:32` excludes it. A Cowboy peer
   therefore refuses the code on receipt. Neither fact affects the browser
   path.

6. **Duplicate durable effects across a busy reconnect.** `NFR-924` requires the
   busy close to emit no acknowledgement, so no pending ceremony exists.
   `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:93-97`
   cancels every live entry for the old generation on close and increments the
   generation. A late callback from the old socket cannot match a new attempt.
   The shipped `app.js:4992-4995` already reconnects on every close.

7. **Making the busy close a hidden terminal refusal.** No effective clause makes
   a socket close terminal for a ceremony. `REQ-996` makes only
   `migration-plan-invalid` terminal, and `REQ-812` makes relay closure
   effect-free rather than unrepeatable.

8. **Reviving a retired ledger through a heading or a changelog.** `REQ-1001` at
   `:56-62` keeps the resolver scope and adds the earlier gate label to the list
   of things that do not make conflicting text historical. Every retired region
   was searched for a survivor set, a hub set, a correction set, a review set,
   and a pointer set. None supplies one.

9. **Breaking a replacement chain.** `CON-981` at `:287-290` keeps every earlier
   replacement effective unless it replaces the exact current fragment, and at
   `:296-297` keeps every transitive predecessor relationship intact. Each replaced
   clause was traced forward and each has exactly one current successor.

10. **Reviving the 4,096-octet positive control witness.** Every occurrence of
    `4,096` in the effective chain is a padded field size, a rendezvous cap, a
    retirement statement, or a negative obligation.

11. **Classifying an out-of-scope module into a lane.** Every out-of-scope
    module was searched for cross-module calls inside its transaction bodies.
    `cbcl-chat-pairgrant:put/3` calls `member-grant?` before opening its
    transaction, so its body touches only `cbcl-pairgrant`. The one genuine
    exception is `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-3]]`.

12. **Turning a nonce collision retry into a contention retry.** The nonce
    allocator's loop wraps a fresh transaction rather than sitting inside one,
    and `cbcl-grant-nonce` matches no lane rule. `NFR-924` names the distinction
    directly.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns, or a limit on what this session executed.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and
   credential/v2 transport depends on it. Both rendezvous implementations carry a
   4,096-octet cap against its 69,632-octet requirement.

2. **The whole credential/v2 lane is planned rather than shipped.** No
   `cbcl-selfsame-*` table exists at the declared baseline, so lane rule 3 has no
   current members.

3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt
   durable readiness, and the separate owner approval for production enablement.

4. **`DEC-012` still defers the Path-A to Path-B migration.**

5. **Every production transaction site remains unbounded today.** All 70 sites
   use `mnesia:transaction/1` or `mnesia:transaction/2` with no integer count.
   `NFR-924` mandates a change rather than describing current behaviour.

6. **The typed cbcl-pairing display is a design.** Its two named types are absent
   from `src/`.

7. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** Several artefact references in the amendment
   bodies are plain text rather than wikilinks. The parent
   [[SPEC-008-production-pairing-claimant]] carries both blocks. This is an
   inherited condition of the amendment form.

8. **Browser handling of close code 1013 was not executed.** The server half was
   verified against cowlib source. The browser half rests on
   `app.js:4992-4995`, which reconnects on every close without reading
   `event.code`. Whether a browser surfaces 1013 rather than 1006 is recorded
   here as unverified.

9. **The `cbcl-provenance` wildcard at `cbcl-chat-session-ws.lfe:1050` is
   out of lane** and keeps its existing fall-through. See
   `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-L-4]]`.

10. **`cbcl-chat-successor:accept/3` has no production caller.** Its defects are
    recorded because the row is production code under `apps/`, not because a
    live path reaches it.

11. **Tier 1 also requires a human domain expert** under
    [[PROTO-001-usdd-agent-protocol]]. The cbcl-bus GATE-04 tracks four human
    review boxes, and every one stays open.

12. **The intra-session synthesis trajectory was not available to this session.**
    The generating session identifier is recorded, and its transcript was not
    read. See `[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-L-5]]`.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation
plan. It authorizes no code, no public allocation, no gate opening, no release,
no deployment, and no production enablement.

Every GATE-04, `GATE-00`, `GATE-01`, and `GATE-03` box stays open. Every human
review, every durability obligation, and the separate owner approval for
production deployment also stay open and unaffected.

A PASS on a later revision still authorizes no code. The primary agent
independently verifies this review's evidence and merges it first.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report. Its executable probes ran in a scratch directory
outside every repository.

## Report self-audit

Every count this report states about itself or about the reviewed set comes
from a tool. The outputs are pasted rather than restated.

```text
$ tools/usdd-count.sh spec-008-0.4.22-claude-adversarial-review-2026-08-23.md
artefacts   REQ=19 NFR=3 CON=8 TEST=14 ADR=1 SPEC=5 PROTO=2
gate rows   0  (pass=0 fail=0 unverified=0)
wikilinks   35  (anchored=21)

$ tools/usdd-lint.sh --type descriptive --strict <this file>
          longest_sentence       25 (limit 25, mode descriptive)
          scanned                411 sentences, 4798 words
          TOTAL                  0 errors, 0 warnings
```

The strict descriptive lint is clean. This file contributes seven dead links to
the vault, and every one targets [[PROTO-001-usdd-agent-protocol]], which has no
page here. That is visible debt in the sense
[[PROTO-001-usdd-agent-protocol]] intends, and it is left in place.

Two counts about the reviewed set were derived rather than read.

```text
$ git grep -c '^- \[ \]'   # GATE-04 block, cbcl-bus 0.16.15 candidate
42

$ git grep mnesia:transaction ce894a7f -- apps/ | production filter
sites: 70
modules: 20
```

The 70 production call sites divide into 43 in-lane rows and 27 out-of-scope
rows. The 27 cover `cbcl-epoch-claim`, `cbcl-grant-nonce`, `cbcl-groupinfo`,
`cbcl-keypkg`, `cbcl-pairgrant`, `cbcl-pairing`, `cbcl-provenance`,
`cbcl-successor-pointer`, `cbcl_mnemonic_store`, and `cbcl_agent_registry`.

One of those 27 rows is misclassified by its own body, which is
`[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#N7-M-3]]`.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N7-H-1 | selfsame, cbcl-bus | Restore the lane qualifier on the required terminal, and restore the `lookup/1` row requiring `unavailable`, HTTP 503, an empty `no-store` body, and no HTTP 404. Exempt the WebFinger read owner from the `authority-busy` result form. |
| N7-M-1 | selfsame, cbcl-bus | Require every production consumer of a tagged-success owner to be adapted to the `#(ok Value)` form as well as the busy form. Name the boolean-context sites that accept a non-boolean without raising. Add a totality test for the admission decision. |
| N7-M-2 | selfsame, cbcl-bus | Reduce the wrapper's exhaustion class to `{aborted, nomore}`. State that prohibited nesting is a static inventory obligation with no runtime terminal. Remove the two unrunnable forcings from the tests. |
| N7-M-3 | selfsame, cbcl-bus | Add `cbcl-chat-successor:accept/3` to the authority owner list. Name its `device-grant/2` nesting edge and require the transaction-only read. Forbid a transaction body from catching an `{aborted, _}` exit, naming `:109` and `:134`. |
| N7-M-4 | selfsame | Add `crates/selfsame-app-identity/src/enrollment.rs:592` and restate the count, or delete "complete" and the numeral so the scan is the sole obligation. Decide the class of `web-device/src/lib.rs:831`, `:847`, and `:861`. |
| N7-M-5 | selfsame, cbcl-bus | Restate the four dropped installed-state and graph cases inline, as every predecessor since 0.4.16 did. Replace "every installed-state case" with a reference naming `REQ-995`. |
| N7-L-1 | selfsame, cbcl-bus | Move `account-binding/1` and `bind-account/2` to the ceremony-migration list, or state that the grouping is by module. |
| N7-L-2 | selfsame | Restate the parent `REQ-903` and `REQ-904` dispositions in `REQ-1001`, or add them to the 0.4.4 inherited-stop table. |
| N7-L-3 | selfsame | Restore "and names its open review gate" in the pointer paragraph, and use an artefact anchor rather than a title anchor. |
| N7-L-4 | selfsame, cbcl-bus | Scope the name-squatting sentence to the busy class, and record the `cbcl-provenance` wildcard as an accepted out-of-lane residual. |
| N7-L-5 | selfsame, cbcl-bus, cbcl-pairing | Point the trajectory field at the chain artefacts that constitute it, or record the intra-session drafts and repairs. |
| N7-L-6 | selfsame, cbcl-bus | Scope "schema migration never overlaps those runtime lanes" to the named site, and record `cbcl-chat-roommember:migrate-since/0` as a second schema migration. |
