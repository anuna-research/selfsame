---
id: SPEC-008-review-0.4.13-claude-2026-08-23
title: Tier-1 adversarial review — SPEC-008 0.4.13 coordinated standalone pairing
status: complete
tier: 1
kind: adversarial-review
verdict: REJECT
reviewed-candidates: "selfsame cb6fff61fb65851f7b28214fafd65220962cb6fd; cbcl-pairing 08f8c1f05911e9920337c289888dea90ecc90a3e; cbcl-bus 874d69806f1a4f98564402d8dac2a11562547b71; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
code-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
severity-counts: "1 critical, 1 high, 2 medium, 3 low"
last-updated: 2026-08-23
---

# Tier-1 adversarial review — SPEC-008 0.4.13 coordinated standalone pairing

## Model, driver and authentication context

| Field | Value |
|---|---|
| Model | Claude Opus 5 (`claude-opus-5`) |
| Driver | Claude Code TUI, Circus worktree `circus/spec008-v0413-review/1` |
| Authentication | Claude Max subscription (OAuth). `ANTHROPIC_API_KEY` absent from the process environment |
| Paid/API calls | None. No external paid API, no account-billed Anthropic API call, no web access |
| Context | Fresh. No prior turn of this conversation preceded the review prompt |
| Working directory | `/Users/anuna-01/Code/selfsame-spec008-v0413-review-1` (isolated worktree) |
| Sibling repositories read | `/Users/anuna-01/Code/cbcl-pairing`, `/Users/anuna-01/Code/cbcl-bus`, `/Users/anuna-01/Code/did-crdt`, and — read-only, for grammar agreement only — `/Users/anuna-01/Code/cbcl-rs` |
| OTP source read | `/opt/homebrew/Cellar/erlang/28.5/lib/erlang/lib/mnesia-4.25.3/src/{mnesia.erl,mnesia_locker.erl}` (OTP 28.5, Mnesia 4.25.3) |

The mandate was defect-finding, not drafting. No implementation code, candidate
spec, test, manifest, configuration, or sibling repository was edited. This
review file is the only change.

### Reviewed commits, verified rather than assumed

```text
selfsame       HEAD = cb6fff61fb65851f7b28214fafd65220962cb6fd   (matches candidate)
cbcl-pairing   HEAD = 08f8c1f05911e9920337c289888dea90ecc90a3e   (matches candidate)
cbcl-bus       HEAD = 874d69806f1a4f98564402d8dac2a11562547b71   (matches candidate)
did-crdt       HEAD = 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1   (matches candidate = baseline)
```

Every declared implementation baseline was diffed against its candidate:

```text
git diff --stat c6af6df..cb6fff6   ->  25 files, ALL under specs/trajectory/SPEC-008/
git diff --stat 8e55160..08f8c1f   ->   7 files, ALL under specs/trajectory/SPEC-001/
git diff --stat fdbe414..874d698   ->   7 files, ALL under specs/ (SPEC-053 parent + trajectory)
did-crdt candidate == did-crdt baseline
```

**No product code changed in any repository between the declared baseline and
the candidate.** Every code claim below is therefore a claim about the
baselines, and every code citation was read through `git show <baseline>:<path>`
or `git grep <baseline>` rather than through a working tree. `cbcl-rs` is *not*
a declared baseline and its working tree is dirty; it was read only at its
committed revision `febc669` and only to answer the mandate's explicit question
about room-grammar agreement. Sibling dirt is attributed to nothing.

### Documents read to determine effective text

Selfsame SPEC-008 0.4.0 through 0.4.13 and every adversarial review 0.4.1
through 0.4.12; `SPEC-007-cbcl-pairing-cutover` (`REQ-812`, `REQ-813`);
`SPEC-008-production-pairing-claimant`; `IMPL-008-production-pairing-claimant`;
`pairing-completion-runbook`; `req-909-adversarial-review-2026-08-22`;
`HANDOFF-pairing-integration`. cbcl-pairing SPEC-001 0.3.0 through 0.4.5.
cbcl-bus SPEC-053 0.16.1 through 0.16.6, `SPEC-053-key-root-identity` (parent,
including its DEC-012 paragraph), and `SPEC-076-pairing-account-grant` as cited.
Code as cited per finding.

---

# Verdict: REJECT

**1 critical · 1 high · 2 medium · 3 low.**

The standalone architecture is still sound, and this amendment is a real
advance. It closes 0.4.12's `H-1` completely and correctly: `deviceDid` at 56,
the timestamps at 20, the handle at 33, and a derived rather than declared room
array make every published figure constructible, and **I re-derived all
fourteen totals and four residuals independently, member by member, and every
one is exact.** `M-1`'s rule now says "comparison **or substitution**" and names
the cross-crate spelling. `M-2`'s room element finally has a grammar and both
units. `L-2`, `L-4` and `L-5` are fully closed; `L-1` and `L-3` are substantially
improved. cbcl-bus 0.16.6 carries all thirty-nine GATE-04 boxes forward with six
of them *strengthened* and none removed or weakened.

It fails on two things, and both are cases where a repair introduced a new
normative claim about behaviour that the deployed code cannot supply.

`REQ-989` gives the browser's account-bound terminal a durable state source —
which was exactly right, and the IndexedDB primitives to do it already exist.
But it then makes the *clearing* transition turn on "an authenticated hub
`not-issued` refusal for a frame signed by that exact retained device key". **No
such object exists on the wire, and none of the three candidates creates one.**
Every hub-originated frame is `(error @room "slug")` signed with sixty-four zero
octets, and the browser's own definition of "hub control" is literally "the
signature is all zeros". The refusal carries no signature, no account, no key,
and no frame correlation, and `not-issued` is a predicate on the *handle's*
account rather than on the browser's own. So the clause is either unreachable —
returning 0.4.12 `L-3`'s stale permanent over-refusal — or implementable only by
letting one injected unsigned frame atomically destroy a person's installed
credential/v2 grant. That is `C-1`, and it blocks on its own.

`REQ-987`'s `CanonicalRoomText` is a **narrower** language than the room
identifiers the deployed system actually mints. `bareName` performs no
validation at all, the CBCL recipient symbol admits `/ ! ? + * < > = @` with no
length bound, and no hub module validates a room name. Adopted `REQ-972` requires
`roomSetDigest` to bind "the complete unique raw room-byte array" and forbids
"truncation, paging, hidden remainder, or partial migration". Those two
sentences cannot both hold for an account that is a member of `@team/eng`. That
is `H-1`.

Nothing here reopens `F-A` through `F-E`, the sixteen-step consent order, the
custody boundary, or SPEC-007 `REQ-812`. All five original findings hold.

---

# Findings

## C-1 (CRITICAL, release-blocking) — `REQ-989`'s revocation trigger names a wire object that does not exist: every hub refusal is an unsigned `(error @room "slug")` with a sixty-four-zero signature, carries no account, key or frame correlation, and `not-issued` fires on the *handle's* account rather than the browser's own — so the transition is either unreachable or is a remotely forgeable primitive for destroying a person's installed grant

**Owner: cbcl-bus** (a new authenticated, correlated, account-scoped hub refusal
is the only sound fix, and cbcl-bus owns the wire), coordinated into **Selfsame
`REQ-989`/`TEST-1119`** and **cbcl-bus `REQ-060`/`CON-026`/`TEST-077`**.

### What the candidate says

Selfsame `REQ-989`:

> An **authenticated** hub `not-issued` refusal **for a frame signed by that
> exact retained device key** proves the installed grant no longer has hub
> standing. The browser atomically removes the installed grant and account-bound
> projection, retains the device seed under the existing recovery policy, and
> restores the handle control. A disconnected socket, timeout, generic error,
> **unauthenticated text**, cached age, or refusal for another key SHALL NOT
> clear the record.

cbcl-bus `REQ-060` mirrors it ("An authenticated hub `not-issued` refusal applies
only to a frame signed by the exact retained key"), `CON-026` makes it the sole
clearing trigger, `TEST-1119`/`TEST-077` make it a mandatory arm, and `GATE-04`
box 36 makes it a release condition ("clears only on authenticated exact-key
`not-issued`").

### (a) There is no authenticated hub channel at all

The hub's refusal is constructed in one place:

```lfe
;; cbcl-bus apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:2093-2097
(defun reply-error (state room slug)
  (let ((payload (cbcl-core-wire:err (or-room room) slug)))
    (tuple (list (tuple 'binary
                        (cbcl-core-frame:build-frame payload (cbcl-core-wire:zero-sig))))
           state)))
```

and the two pieces it uses are:

```lfe
;; cbcl-bus apps/cbcl_chat/src/core/cbcl-core-wire.lfe:31-36
(defun zero-sig ()
  (binary (0 (size 64) (unit 8))))

;; err(Room, Slug) -> canonical CBCL bytes: (error @room "slug")
(defun err (room slug)
  (iolist_to_binary (list "(error " room " \"" slug "\")")))
```

`zero-sig` is used at **twenty** call sites in `cbcl-chat-session-ws.lfe`, and
**every single `build-frame` in that module passes it**. There is no signed
hub-originated frame in the deployed protocol. The browser agrees, explicitly:

```js
// cbcl-bus apps/cbcl_chat/priv/web/app.js:1507-1509
function isHubControlSignature(sig) {
  return sig.length === 64 && sig.every((b) => b === 0);
}
```

The browser's entire notion of "this came from the hub" is *the absence of a
signature*. The adjacent comment — "Require the zero hub-control signature …
a member cannot manufacture a hold for itself or anyone else" — is true only of
*members*, whose frames must carry a real Ed25519 signature over the
domain-separated envelope. It is false of anything that can put bytes on the
socket: a network position, a TLS-terminating proxy, a compromised relay hop, or
any injection into the page's WebSocket. All of those can emit a byte-identical
"hub control" frame.

So `REQ-989`'s own guard is self-defeating. "Unauthenticated text … SHALL NOT
clear the record" excludes *every* refusal the hub can currently send. Read
strictly, the clearing transition never fires and 0.4.12 `L-3`'s stale permanent
over-refusal is back — the exact failure `ADR-950` says this design avoids
("This avoids both reload under-refusal and a stale permanent over-refusal after
authorized grant deletion"). Read loosely enough to fire, one injected frame
atomically deletes the installed grant and the account-bound projection.

### (b) The refusal cannot be correlated to a frame

`REQ-989` conditions clearing on a refusal **"for a frame signed by that exact
retained device key"**. The refusal is `(error @room "not-issued")`. It carries a
room and a slug and nothing else — no sequence number, no request id, no
ceremony id, no key digest. The connection is pipelined (`connSeq` is a
per-connection monotonic counter, `app.js:1536`), so several frames may be in
flight. The browser cannot determine which frame a refusal answers, and
therefore cannot evaluate `REQ-989`'s condition at all. `TEST-1119`'s arm
"Timeouts, generic errors, forged text, another key, and stale cache state do
not clear" is not decidable against this wire: "another key" is precisely the
distinction the wire does not carry.

### (c) `not-issued` is a predicate on the *handle's* account, not the browser's

This is the part that would still be wrong even with a perfect authenticated
channel. The slug is emitted at exactly one place:

```lfe
;; cbcl-bus apps/cbcl_chat/src/core/cbcl-core-enrol.lfe:241  (in account-bound/5)
((not (=:= 'true (maps:get 'issued frame-key 'false))) #(reject not-issued))
```

and `issued` is established by a durable read keyed on the account the **frame's
handle** resolved to:

```lfe
;; cbcl-bus apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:393-396
(defun path-b-issued? (account pk)
  (case (cbcl-chat-account-grants:device-grant account pk)
    (`#(ok ,_stored) 'true)
    (_ 'false)))
```

with `account` threaded from `dispatch/3`'s single binding read:

```lfe
;; cbcl-bus apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:214-233
(handle (cbcl-core-msg:from-handle msg))
…
(let ((binding (cbcl-chat-account-grants:account-binding handle)))
```

So `not-issued` means "no grant row exists for `(account_of(handle), frame_key)`".
It fires whenever the frame's handle resolves to an account the device has no
grant under — including when the browser's **own** grant, under its **own**
account, is entirely valid. 0.4.12's own `L-3` recorded this branch ("changing to
another account's handle refuses on `not-issued`"), and `REQ-989` then adopted
that same slug as proof of the opposite proposition.

`REQ-989` is careful about the key and silent about the account. Its own record
"contains the retained device seed and accepted installed grant for the current
application **and account scope**" — so the browser knows its account scope and
could check it. The wire gives it nothing to check against.

### Why this is critical rather than medium

1. **It is not fail-closed.** Every other defect in this trajectory has been a
   refusal. This one is a *destructive* local effect — atomic removal of the
   installed grant and the account-bound projection — triggered by an input that
   is, by construction, unauthenticated. Seed retention limits the blast radius
   to "the person must re-enrol", but re-enrolment after an irreversible Path-A→B
   migration runs through `cbcl-chat-path-b-resolver` and did-crdt (adopted
   `R-4`), with no legacy fallback.
2. **It is reachable without an attacker.** Branch (c) fires on ordinary hub
   behaviour. Any frame whose handle resolves elsewhere produces `not-issued`
   while the grant is good.
3. **Three documents and a release box are normative over it.** `REQ-989`,
   `REQ-060`, `CON-026`, `TEST-1119`, `TEST-077` and `GATE-04` box 36 all assume
   the object exists. Box 36 cannot be closed honestly either way.
4. **`ADR-950` claims the property this defect removes.** "Authenticated
   `not-issued` is the narrow transition that removes that standing" — it is
   neither authenticated nor narrow.

### Minimum sound correction

**cbcl-bus** must define the wire kind, because cbcl-bus owns the socket. A
signed hub control performative carrying, at minimum, the `accountScopeId`, the
device-key digest, the refused frame's `seq`, and a signature under the key the
recognised application profile declares — verified by the browser against that
profile, not against "the signature is zero". Then **Selfsame `REQ-989`** and
**cbcl-bus `REQ-060`/`CON-026`** must require the browser to match *all three* of
its own account scope, its own device key, and its own installed grant reference
before clearing, and `TEST-1119`/`TEST-077` must add a negative arm for a
genuine `not-issued` that names a different account.

If that wire kind is out of scope for this increment, the sound alternative is
to say so: **no hub error clears the record**, the account-bound projection is
removed only by an explicit local person-initiated action (the `CON-969` seed
precedent), and `GATE-04` box 36 is restated to that effect. That leaves a
knowingly accepted over-refusal instead of an unowned destructive primitive.

---

## H-1 (HIGH, release-blocking) — `CanonicalRoomText` is strictly narrower than the room identifiers the deployed system mints: `bareName` validates nothing, the CBCL recipient symbol admits `/ ! ? + * < > = @` unbounded, and no hub module checks a room name — so adopted `REQ-972`'s "complete unique raw room-byte array" and the new grammar cannot both hold, and no clause says which gives way

**Owner: Selfsame `REQ-987`/`REQ-988`**, mirrored in **cbcl-bus `REQ-059`** and
**cbcl-pairing `REQ-027`/`NFR-019`**.

### What the candidate says

`REQ-987`:

```text
CanonicalRoomText = "@" ( ALPHA / DIGIT )
                    *127( ALPHA / DIGIT / "." / "_" / "-" )
```

cbcl-bus `REQ-059` adds the binding sentence: room values are "**identical to the
raw `roomBytes` used by the room-set digest**". cbcl-pairing `REQ-027` adds the
enforcement: "**Unknown text**, duplicates, unsorted values, an oversized element,
or an oversized count refuses before `CredentialV2Display` construction."

### What the deployed system actually mints

A room name is produced by the browser with no validation whatsoever:

```js
// cbcl-bus apps/cbcl_chat/priv/web/app.js:4834
const bareName = (s) => String(s || '').trim().replace(/^[#@]+/, '');
```

`bareName` strips leading `#`/`@` and trims whitespace. It checks no character
class and no length. Both room-entry paths use it directly:

```js
// app.js:5362 (the "go to" box)          // app.js:5386 (create a channel)
const room = '@' + b;                      const room = '@' + name;
```

and the result is interpolated raw into the wire text:

```js
// app.js:5142
const ok = await sendCBCL(`(hello ${room} :from ${id.handle} …)`);
```

The authoritative recogniser for that text admits far more than
`CanonicalRoomText`. At the committed cbcl-rs revision `febc669` — the grammar
`cbcl_erl:parse_message` wraps, read read-only and at a commit, not from the
dirty tree:

```rust
// cbcl-rs crates/cbcl-parser/src/parser.rs:328-342
fn is_symbol_char(ch: char) -> bool {
    ch.is_ascii_alphanumeric()
        || ch == '_' || ch == '-' || ch == '.' || ch == '/' || ch == '!'
        || ch == '?' || ch == '+' || ch == '*' || ch == '<' || ch == '>'
        || ch == '=' || ch == '@'
}
```

and `docs/cbcl-grammar.ebnf` at the same revision states the production:

```text
recipient = "@" , symbol-char , { symbol-char }
```

with **no length bound** — the only ceiling is the frame size. On the hub side
nothing narrows it: `cbcl-chat-rooms:get-or-start/1` and
`cbcl-chat-roomcfg:ensure-room/3,4` take the name as an opaque binary and write
it (`cbcl-chat-roomcfg.lfe:86-95`); the only name validator in that module is
`valid-dialect-name?`, which is about dialects.

So `@team/eng`, `@2024.q1/plan`, `@a+b`, `@x=y`, `@_draft`, `@-wip` and any name
longer than 128 characters after the `@` are all **reachable today, by ordinary
user input, through the shipped UI**, and none of them is a `CanonicalRoomText`.

### Why it matters

**(a) It contradicts an adopted clause that this amendment does not replace.**
`CON-972` leaves `REQ-972` intact, and `REQ-972` says:

> `roomSetDigest` hashes its exact domain plus deterministic CBOR of the
> **complete** unique raw room-byte array sorted by unsigned-octet order

and

> Oversize is `MigrationPlanTooLarge` before carrier allocation; there is **no
> truncation, paging, hidden remainder, or partial migration** in this version.

`REQ-059` requires the `migrationRooms` values to be *identical to* those
`roomBytes`. If one of the account's rooms is not a `CanonicalRoomText`, the
array cannot carry the complete set. Either the digest binds a set the array
cannot represent — a direct contradiction — or the migration refuses.

**(b) The refusal has no name and no remediation.** `REQ-972` names
`MigrationPlanTooLarge` for the oversize case. There is no
`MigrationRoomUnrepresentable`, no clause telling the person which room blocks
them, and no path to fix it (leaving the room is not offered, and the room set is
read from durable `cbcl-roommember` state). A reachable class of real accounts
becomes permanently unable to migrate, and the design says nothing about it.

**(c) It is the one input `REQ-988` does not derive.** `ADR-950`'s method is that
"a release-blocking equality witness must be constructible through the same
grammar the recognizer enforces". For `deviceDid` and the timestamps the
amendment correctly *adopted* the code's grammar. For rooms it *invented* one and
imposed it on legacy data that predates it. The 129-octet maximum and the
33,793-octet array are constructible **within the declared grammar** — I checked,
and there are `62 × 65^127` distinct 129-octet values, so 256 sorted unique
witnesses exist with room to spare — but constructibility inside an invented
grammar is not the property `ADR-950` asks for when the values come from
elsewhere.

**(d) The consent screen is affected, not just the wire.** `GATE-04` box 27 makes
"exact room-list consent" a human-review condition. A list that silently cannot
represent one of the person's rooms is not an exact room list.

### What is *not* wrong here

`M-2`'s two other halves are genuinely closed and should not be re-opened.
`V2_MAX_MIGRATION_ROOM_OCTETS = 129` is declared in **both** units;
`V2_MAX_MIGRATION_ROOMS_JCS_OCTETS` is now **derived** (`256 * (129 + 2) + 255 + 2`)
rather than declared independently, so the two caps cannot disagree; and
cbcl-pairing `NFR-019` puts every element and aggregate check **before** the
clone ("It then checks room count, uniqueness, ordering, and the 33,793-octet
aggregate **before cloning or allocating** the owned `CredentialV2Display`"),
which is exactly the allocation boundary 0.4.12 asked for and which `ADR-020`
correctly frames as resource safety rather than a transfer of room policy.

### Minimum sound correction

**Selfsame `REQ-987`/`REQ-988`** must either

- **widen the grammar to the deployed one** — `"@" 1*128( ALPHA / DIGIT / "_" /
  "-" / "." / "/" / "!" / "?" / "+" / "*" / "<" / ">" / "=" / "@" )` — citing
  `is_symbol_char`, keep the 129-octet element cap as a *v2 recogniser cap* on an
  otherwise unbounded wire symbol (correctly labelled a cap, per `ADR-950`), and
  re-derive nothing, because the octet counts do not change; **or**
- **keep the narrow grammar and state the consequence**: declare a
  `MigrationRoomUnrepresentable` closed error, require it before carrier
  allocation, require the display to name the offending room, and record in
  `CON-972` that `REQ-972`'s "complete" is thereby conditioned on the room set
  being grammar-legal — with a `GATE-04` box for the human review that accepts
  the resulting migration exclusion, alongside box 28's oversize exclusion.

Either way, one of the two must move. cbcl-bus `REQ-059` and cbcl-pairing
`REQ-027`/`TEST-056` move in lockstep, and `TEST-1117` gains a negative arm for a
deployed-legal, grammar-illegal room.

---

## M-1 (MEDIUM) — `NFR-915`'s "complete expected cyclic-source set" is not complete: derived from the amendment's own lock matrix and OTP's `can_lock/4`, at least five further pairs are cyclic (grant-create × finalize, finalize × finalize, finalize × observed-revoke, observed-revoke × observed-revoke, claim × claim), while listed source 5 appears not to be cyclic at all — and the clause forbids the ordinary amendment path by asserting closure

**Owner: Selfsame `NFR-915`/`TEST-1120`**, mirrored in **cbcl-bus
`NFR-013`/`TEST-078`** and **`GATE-04` box 32**.

### The two load-bearing OTP facts, re-verified

Both are correct, and I read them rather than accepting them.

```erlang
%% mnesia_locker.erl:357-360 — a record WRITE checks table locks of ANY mode
can_lock(Tid, write, Oid = {Tab, Key}, AlreadyQ) when Key /= ?ALL ->
    ObjLocks = ets:select(mnesia_held_locks, [{ {{Oid,  '_', '$1'}}, [], ['$1'] }]),
    TabLocks = ets:select(mnesia_held_locks, [{ {{{Tab, ?ALL}, '_', '$1'}}, [], ['$1'] }]),

%% mnesia_locker.erl:362-365 — a whole-table READ checks record WRITE locks
can_lock(Tid, read, Oid, AlreadyQ) -> % Whole tab
    Tab = element(1, Oid),
    ObjLocks = ets:select(mnesia_held_locks, [{ {{{Tab, '_'}, write, '$1'}}, [], ['$1'] }]),
```

So a held `{Tab, ?ALL}` **read** blocks a record write, **and** a held record
write blocks a `{Tab, ?ALL}` read. Both directions are needed below and both
hold. `index_read` and `foldl` do take `?ALL`:

```erlang
%% mnesia_locker.erl:1083-1084, 1095, 326-331
rlock_table(Tid, Store, Tab) -> rlock(Tid, Store, {Tab, ?ALL}).
ixrlock(…) -> … need_lock(Store, Tab, ?ALL, read) …
grant_lock(Tid, {ix_read,IxKey,Pos}, Lock, Oid = {Tab,_}) -> … {granted, Res, [?ALL]};
%% mnesia.erl:1953-1954
init_iteration(ActivityId, Opaque, Tab, LockKind) ->
    lock(ActivityId, Opaque, {table, Tab}, LockKind),
```

and wait-die is where `NFR-915` says it is (`can_queue/4` at
`mnesia_locker.erl:410-414`, `allowed_to_be_queued/2` at `:430-436`, with the
module's own statement of the rule at `:341-343`). `NFR-915`'s "It is not a
deadlock proof" remains the right characterisation.

### The derivation the list does not survive

Write `G` = grant create, `F` = credential/v2 finalize, `XR` = exact revoke,
`OR` = observed revoke, `ME` = member enroll, `PC` = pairaccount claim, `AR` =
per-frame authority read, and read the sequences straight off cbcl-bus
`NFR-013`'s table. Three of them take `grant ?ALL` **and then write
`cbcl-device-grant` by exact key**: `G`, `F`, `OR`. By `can_lock/4` above, any
two of them mutually block — each holds a `?ALL` read the other's exact write
must clear. Worked through for the pair the list omits, with distinct accounts
`a1 ≠ a2` and devices `d1 ≠ d2`:

```text
T_G : AG(a1)w, DG(d1)w, {grant,?ALL}r, {member,?ALL}r,           then {grant,k1}w
T_F : AG(a2)w, DG(d2)w, …, {member,m2}w, …, {grant,?ALL}r,
      {member,?ALL}r, {pairaccount,?ALL}r,                        then {grant,k2}w

T_F's {grant,k2}w  -> TabLocks match {grant,?ALL} held by T_G   -> blocked
T_G's {grant,k1}w  -> TabLocks match {grant,?ALL} held by T_F   -> blocked
```

That is the same shape as listed source 1, between two operations the list never
pairs. The full clique `{G, F, OR}` yields six pairs; `NFR-915` names two of
them (`G×G` as source 1, `G×OR` as source 2). Missing:

1. **`G × F`** — grant creation against credential/v2 finalization (above).
2. **`F × F`** — two finalizations. `F` takes `exact member` **before**
   `member ?ALL`, so each holds a record write the other's whole-table read must
   clear; the same reversal also makes them cyclic on `cbcl-device-grant`.
3. **`F × OR`** — `F` takes the device guard *before* `grant ?ALL`; `OR` takes
   `grant ?ALL` *before* its sorted device guards. Opposite order on
   `{device guard, grant ?ALL}`, plus the exact-write-versus-`?ALL` block. This
   is structurally identical to listed source 2 with `F` in place of `G`.
4. **`OR × OR`** — two observed revocations. The matrix's "sorted device guards"
   correctly removes the guard-ordering cycle, but not the `?ALL`-versus-exact
   -write one.
5. **`PC × PC`** — two pairaccount claims. Both hold `pairaccount ?ALL` and both
   then write `cbcl-pairaccount-grant` by exact key. Worth noting because the
   *current* code has the reverse order — `claim-tx` does
   `(mnesia:read 'cbcl-pairaccount-grant k 'write)` at
   `cbcl-chat-pairaccount.lfe:54` **before** `unratified-count-tx`'s
   `mnesia:index_read` at `:70-75` — so this cycle is *created* by the matrix's
   corrected ordering and cannot be inherited from load experience.

Conversely, listed **source 5** ("pairaccount claim against grant creation and
against credential/v2 finalization") does not appear to be cyclic under the
matrix as written: `PC`'s only exact write is on `cbcl-pairaccount-grant`, a
table neither `G` nor `F` writes; its `?ALL` reads are compatible with theirs;
and a shared device guard serialises the pair before either acquires anything
else. Naming a non-cyclic pair costs only test effort, but combined with the five
omissions it indicates the list was assembled by inspection rather than derived
from the matrix — which is the actual defect.

`XR × OR` is a further candidate (`XR` holds the device guard and wants an exact
grant write blocked by `OR`'s `grant ?ALL`; `OR` holds `grant ?ALL` and wants
that device guard) and is also unlisted.

### Why this is worse than 0.4.12's open-ended list

0.4.12 `L-1` was LOW because cbcl-bus's "Expected cyclic sources **include** …"
merely failed to enumerate. 0.4.13 replaces it with an assertion:

> This list is closed. … No phrase such as "include" or "for example" widens or
> weakens this test set. **Finding another cyclic source requires an amendment to
> this closed list and its load evidence.**

`TEST-1120` says "Trigger every pair in `NFR-915` and no open-ended substitute",
and `GATE-04` box 32 says "each closed `NFR-013` cyclic pair appears in trace
evidence". A closed list that is demonstrably not closed lets box 32 be closed on
incomplete evidence, and the closure sentence makes adding the missing pairs
during implementation a *specification violation* rather than ordinary work.

### Minimum sound correction

**Selfsame `NFR-915`** and **cbcl-bus `NFR-013`**: replace the hand-listed set
with the derivation, which is short and complete — "for each authority table,
every pair of matrix operations that holds a `{Table, ?ALL}` lock and later
writes that table by exact key, in either order, is a cyclic source; plus every
pair whose guard and `?ALL` acquisitions are in opposite order; plus the
per-frame authority read against every writer." Then enumerate the resulting
pairs and keep the closure sentence, which is only sound over a derived set.
Retest source 5 and drop it if it does not derive. `TEST-1120`/`TEST-078` follow.

---

## M-2 (MEDIUM) — `LegacyHandle` is described as "the deployed maximum 33-octet ASCII form", but it is a browser change-handler convention: the hub validates no handle at all, and `cbcl-core-msg:sym->handle/1` accepts any symbol or any CBCL string as a handle, so a legacy handle outside the subset is unrepresentable in the transition and blocks migration with no named error

**Owner: Selfsame `REQ-987`**, mirrored in **cbcl-bus `REQ-059`** and
**cbcl-pairing `REQ-027`**.

### What the candidate says

```text
LegacyHandle = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )
```

and `REQ-988` calls it a reachable maximum at 33 octets, `TEST-1117` requires
constructing one, and `REQ-059` says "The maximum lengths are 33 and 129 in both
UTF-8 and RFC-8785 string-content units."

### What the code enforces

The grammar matches the browser's own production and validation exactly, which is
why this is MEDIUM rather than a second `H-1`:

```js
// app.js:909 — every browser-minted handle is inside the subset
const freshHandle = () => '@user-' + Math.random().toString(36).slice(2, 7);

// app.js:1074 — the change handler is the one place a person can widen it
if (!/^@[a-z0-9_-]{1,32}$/.test(h)) { … sysLine('handle must be @ plus 1–32 lowercase letters, digits, _ or -'); return; }
```

But that regex is client-side UI, not an invariant. The hub takes the handle
straight off the parsed frame:

```lfe
;; cbcl-bus apps/cbcl_chat/src/core/cbcl-core-msg.lfe:39-47
(defun sym->handle (val)
  (cond
    ((andalso (is_tuple val) (andalso (=:= (tuple_size val) 2) (=:= (element 1 val) 'symbol)))
     (element 2 val))
    ((is_binary val) val)
    ('true 'undefined)))
(defun from-handle (msg) (sym->handle (kw #"from" msg)))
```

Any CBCL **symbol** (the `is_symbol_char` set from `H-1`, unbounded) or any CBCL
**string** (any Unicode except `"` and `\`, unbounded) is accepted as a handle,
and neither `cbcl-core-enrol:tofu/2` (`:153-162`) nor
`cbcl-chat-session-ws:dispatch/3` (`:214-233`) applies a character class or a
length check anywhere on the path to `cbcl-chat-account-grants:account-binding`.
A non-browser client, an agent, or a person who edits
`localStorage['cbcl_handle']` can enrol a handle no `LegacyHandle` can express.
The consequence is the same as `H-1`(a): the transition is unconstructible for
that account, with no named error.

The octet arithmetic is unaffected — 33 remains the right number for the
grammar as written — so no total moves.

### Minimum sound correction

**Selfsame `REQ-987`**: say what the grammar *is*. Either (i) declare it a
**v2 admission precondition** — "credential/v2 migrates only an account whose
legacy handle matches `LegacyHandle`; any other handle refuses with a named
closed error before carrier allocation" — and add the `GATE-04` human-review
acceptance for that exclusion, or (ii) widen it to the enrolment path's actual
language and cap it as a v2 recogniser cap. Do not describe a client regex as
"the deployed maximum". cbcl-bus `REQ-059` and cbcl-pairing `REQ-027` move with
it; `TEST-1117` gains the negative arm.

---

## L-1 (LOW) — three further production v1 substitutions live in `selfsame-web-device`, on the offer, hint and statement builder surfaces, and one more in `selfsame-app-identity`'s statement builder; `REQ-987`, `TEST-1118` and cbcl-bus `REQ-059`/`TEST-078` name only `profile_facts`

**Owner: Selfsame `REQ-987`/`TEST-1118`**, mirrored in **cbcl-bus `REQ-059`/`TEST-078`**.

0.4.12's `M-1` is genuinely closed at the level that mattered: the rule now reads
"compare with **or substitute**", it names `selfsame_app_identity::PROFILE_VERSION`,
and it adds "the integer literal 1 as a profile or statement version". I swept
every version-constant site at the baseline. There are eight `PROFILE_VERSION`
occurrences in `crates/`:

```text
selfsame-app-identity/src/lib.rs:109             pub const PROFILE_VERSION: i64 = 1;   (definition)
selfsame-app-identity/src/profile.rs:60          pub const PROFILE_VERSION: i64 = 1;   (definition)
selfsame-app-identity/src/profile.rs:385         comparison   (named in 0.4.12)
selfsame-app-identity/src/enrollment.rs:394      comparison   (named in 0.4.12)
selfsame-app-identity/src/enrollment.rs:552      comparison   (named in 0.4.12)
selfsame-app-identity/src/provider_hint.rs:115   comparison   (named in 0.4.12)
selfsame-app-identity/src/enrollment.rs:452      SUBSTITUTION  <- not named anywhere
selfsame-web-device/src/lib.rs:600               SUBSTITUTION  (the one REQ-987 names)
```

plus these literal-1 profile-version substitutions, all **before** the test
module (`#[cfg(test)] mod tests` begins at `selfsame-web-device/src/lib.rs:1521`,
so they are production code, not fixtures):

```rust
// selfsame-web-device/src/lib.rs:1343-1361, in EnrolmentAllocator::new_native
let core = OfferCore { … profile_version: 1, … };          // :1346
let hint = ProviderHint { … profile_version: 1, … };        // :1360
let statement = EnrollmentStatement { … profile_version: core.profile_version, … };
```

`EnrolmentAllocator::new_native` is the browser's own offer, hint **and**
statement builder — three of the eight surfaces `REQ-987` enumerates — and it
hard-codes the version in all three rather than taking it from the profile it
just recognised two lines earlier. `enrollment.rs:452` is the same pattern inside
app identity's statement builder.

`REQ-987`'s static-boundary list *does* textually reach all four ("the integer
literal 1 as a profile or statement version"; `crate::PROFILE_VERSION`), which is
why this is LOW and not a reopening of `M-1`. What is short is the **named-site
enumeration**: `REQ-987` names one cross-crate site, `TEST-1118` names one
("including the current `selfsame-web-device::profile_facts` constant site"), and
cbcl-bus `REQ-059` names one ("The trace crosses into
`selfsame-web-device::profile_facts`"). A static check bootstrapped from the
named sites misses the builder that credential/v2's browser must not reuse.
cbcl-bus `CON-025`'s disjointness sentence ("Credential/v2 held state reaches
only v2 profile, hint, offer, statement, signer, and profile-facts branches")
is the clause that forbids the reuse; it just is not connected to these sites.

Two nearby literals are **not** defects and are recorded so a later sweep does
not refile them: `discovery.rs:202` (`version != Some(1)`) is the two-member
application-*enumeration* document, shared by v1 and v2 and not a profile or
statement version; `succession.rs:195,262` are `successionVersion`, outside
credential/v2.

**Minimum sound correction:** add the three `selfsame-web-device` sites and
`enrollment.rs:452` to `REQ-987`'s named cross-crate trace and to `TEST-1118`'s
enumerated site list, by file and line, and state explicitly that the v2 browser
does not reach `EnrolmentAllocator`. Owner: Selfsame `REQ-987`/`TEST-1118`;
cbcl-bus `REQ-059`/`TEST-078` follow.

---

## L-2 (LOW) — the cbcl-pairing side of the test ledger is still a range, and it names `TEST-050`, whose control-object portion and 4,096-octet positive witness cbcl-pairing `CON-020` explicitly replaces and retires

**Owner: Selfsame 0.4.13 test-specification preamble.**

0.4.12 `L-5`(b) is closed on the cbcl-bus side — 0.4.13 correctly replaces the
open "TEST-043 through TEST-075" with the enumerated survivor set "TEST-043
through TEST-065, TEST-068, TEST-070, and TEST-076 through TEST-079", which
matches cbcl-bus `REQ-058`'s ledger exactly. `L-5`(a) is closed too: the parent
`SPEC-053-key-root-identity` now carries
`candidate-amendment: "[[spec-053-0.16.6-reachable-bounds-and-durable-authority]]"`
and a DEC-012 paragraph pointing at 0.16.6's GATE-04 (verified by
`git diff fdbe414..874d698 -- specs/SPEC-053-key-root-identity.md`).

The cbcl-pairing half was not updated with it. 0.4.13 says "Cbcl-pairing
TEST-030 through TEST-056 remains mandatory", and cbcl-pairing `CON-020` says:

> The control-object portion of [[…#TEST-050]] is **replaced, not
> supplemented**, by [[…#TEST-051]] as corrected by TEST-052 below. … Its
> `logicalLength = 4,096` positive control witness is **retired and is a
> required negative witness**.

So a range naming `TEST-050` as mandatory-in-full asserts a positive witness that
is now required to be negative. `TEST-1120` makes "the exact Selfsame,
cbcl-pairing, and cbcl-bus test survivor lists" a static check; a range is not a
survivor list. This is narrower than the cbcl-bus case (only part of one test is
superseded), hence LOW.

**Minimum sound correction:** one sentence — "Cbcl-pairing TEST-030 through
TEST-049, TEST-050 except its replaced control-object portion and retired 4,096
positive witness, and TEST-051 through TEST-056."

---

## L-3 (LOW) — `REQ-989` requires the installation record's seed to re-derive the recorded device key but never requires the installed grant itself to be verified on reload, so a locally authored record reconstructs account-bound state

**Owner: Selfsame `REQ-989`**, mirrored in **cbcl-bus `REQ-060`/`CON-026`**.

The durability half of 0.4.12 `L-3` is genuinely and well closed, and every
primitive `REQ-989` needs already exists at the baseline — which is worth
recording, because it makes the requirement implementable rather than
aspirational:

```js
// app.js:943-948  — multi-key atomicity, with the rationale already written down
//   "observing `installed` must also observe the target seed … Keep related
//    recovery rows in one IndexedDB transaction."
const idbPutMany = (db, entries, strict = false) => …
// app.js:929-931  — durability: 'strict' for recovery checkpoints
// app.js:993-999  — publicKeyFromSeed(seed) re-derives the public key
// app.js:958-963  — idbClearOwnedExcept(db, keep) clears selectively, keeping 'seed'
// app.js:1054     — the seed-retaining clear is already the established pattern
```

What `REQ-989` does not say is that the *grant* must be checked. Its
reconstruction condition is "the record is fully recognized and its seed
re-derives the recorded device key" — which proves this browser holds the private
key for the device DID in the record, and nothing about the grant having been
issued. Since the record is same-origin local state and the only consequence is
*self*-refusal of the handle control, the consequence is a self-inflicted denial
of a control, not an escalation — hence LOW. It is still a gap in a requirement
whose whole subject is "durable truth".

Note also that `isCbclIdentityStoreKey` (`app.js:955-957`) does not yet recognise
a credential/v2 installation key, so the record would be invisible to
`idbClearOwnedExcept`'s recovery sweep until added. That is ordinary
implementation work for a design-only increment (`R-10` class), recorded so it is
not a surprise.

**Minimum sound correction:** one clause in `REQ-989` — "reconstruction
additionally requires the installed grant to verify under the authenticated
application profile's issuer and to name the record's account scope and device
key" — and an arm in `TEST-1119`.

---

# Independent verification of `REQ-988`'s arithmetic

Recorded in full because it is the part of the amendment that 0.4.12 rejected,
because it is now correct, and because the next round should not have to redo it.
I rebuilt every level from the declared constants under RFC 8785 (no whitespace,
minimal escaping, members sorted — sorting does not change octet counts) **before**
testing reachability, and every published figure reproduces exactly.

### The 23 core members, `"name":value` octets

```text
payloadVersion            18   accountPrincipalDigest    70   platformBindingId  182
role                      14   applicationAccountDisplay 542  returnUri          174
ceremonyId                58   accountScopeId            62   issuedAt            33
requestId                 57   deviceDid                  70  expiresAt           34
applicationId            114   deviceKeyJwk             108   accountTransition  371
profileVersion            18   requestedPermissions     548        (non-room part)
profileDigest             61   intentNonce               59
descriptorDigest          64   relayOrigin              288
carrierDigest             61   transcriptHash           105
                                            members    3,111
                     + 2 braces + 22 commas               24
                     -------------------------------------------
                     non-room core                     3,135   <- REQ-988 exact
                     + complete migrationRooms array   33,793
                     -------------------------------------------
                     complete core                     36,928   <- REQ-988 exact
```

The nested transition's non-room part, independently:

```text
"kind":"path-a-to-b"          20   "migrationSnapshotDigest":".."  71
"legacyHandle":"<33>"         50   "roomSetDigest":".."            61
"legacyKeyDigest":"<43>"      63   "snapshotNonce":"<43>"          61
"migrationRooms":             17   + 2 braces + 6 commas            8
                                                   ----------------
                                   transition object            351
                                   + "accountTransition":        20
                                                                371
```

The three grammar-forced reductions from 0.4.12 are exactly as the amendment
states: `deviceDid` `526 -> 70` (**-456**), the two timestamps `45+46 -> 33+34`
(**-24**), `legacyHandle` `273 -> 50` (**-223**); `3,838 - 703 = 3,135`.

### The 21-member statement, header, and JWS chain

```text
evidenceVersion  19   requestedPermissions 548   platformBindingId        182
requestId        57   providerId            78   returnUri                174
ceremonyId       58   descriptorDigest      64   issuedAt                  33
applicationId   114   offerDigest           59   expiresAt                 34
profileVersion   18   deviceKeyDigest       63   accountPrincipalDigest    70
profileDigest    61   accountScopeId        62   applicationAccountDisplay 542
carrierDigest    61   transcriptHash       105   devicePossessionProof    112
                                        members  2,514
                     + 2 braces + 20 commas         22
                     ------------------------------------
                     statement payload           2,536   <- REQ-988 exact

{"alg":"EdDSA","kid":"<128>","typ":"selfsame-credential-v2+jws"}
  1 + 13 + 1 + 136 + 1 + 34 + 1                 =   187   <- exact
b64url-no-pad(187)=250 · b64url-no-pad(2536)=3,382 · b64url-no-pad(64)=86 · 2 seps
                                                = 3,720   <- exact
policy 4,000 - 3,720                            =   280   <- exact
```

### Hint, JWK, array, offer, residuals

```text
CON-945 seven-member hint  114+18+78+64+59+61+105 = 499, +2 braces +6 commas =   507  exact
{"alg":"EdDSA","crv":"Ed25519","kty":"OKP","x":"<43>"}  1+13+1+15+1+11+1+49+1 =    93  exact
migrationRooms  256*(129+2) = 33,536; +255 commas; +2 brackets              = 33,793  exact
"devicePossessionProof":"<86>"                                              =    112  exact
"enrollmentEvidence":"<3720>"                                               =  3,743  exact
"providerHint":{<507>}                                                      =    522  exact
complete addition 112+3,743+522+3                                           =  4,380  exact
complete offer 36,928 + 4,380                                               = 41,308  exact
residuals: 52,000-36,928 = 15,072 · 57,000-41,308 = 15,692 · 62,000-41,308 = 20,692  exact
```

### Reachability of each input, checked against code rather than prose

- **`deviceDid` = 56.** `didkey::decode` admits one shape only — `did:key:z` then
  base58btc of `0xED 0x01 || key32`, with `octets.len() == 34` enforced
  (`didkey.rs:76-98`), and `recognise_offer` requires it to encode the same key
  as `deviceKeyJwk` (`ceremony.rs:330,373-376`). base58btc of 34 octets whose
  leading byte is `0xED` is always 47 characters: `237·256³³ > 58⁴⁶` and
  `256³⁴ < 58⁴⁷`, so the count is pinned from both sides. `8 + 1 + 47 = 56`.
- **Timestamps = 20.** `STAMP_CHARS: usize = 20` and `if b.len() != STAMP_CHARS`
  (`time.rs:53,56-59`) — an equality, with punctuation pinned at indices
  4/7/10/13/16/19 and digits elsewhere; `format_date_time_stamp` (`:98-108`)
  emits the same twenty, and `recognise_unsigned` requires the build round-trip
  (`enrollment.rs:317`).
- **`kid` = 128 is reachable.** `UriPolicy::FRAGMENT_ID` is `path: Any,
  fragment: RequiredNonEmpty` (`uri.rs:136-139`) with no length rule of its own,
  and `recognise_kid_for_application` only pins the origin
  (`enrollment.rs:468-477`). With the 44-octet v2 ApplicationId, an 83-character
  fragment reaches exactly 128, so the 187-octet header is constructible.
- **Display 512/512 is exactly tight.** With 1..128 NFC scalars and C0/C1
  excluded, RFC 8785 escapes only `"` and `\`. For `k` escapable one-octet
  scalars and `128-k` four-octet scalars: UTF-8 `= 512-3k ≤ 512`, JCS
  `= 512-2k ≤ 512`, equality at `k = 0`. No legal 128-scalar display is excluded
  in either unit, and the maximum is reached by 128 four-octet scalars. (C1
  controls are not JCS-escaped, which would have broken the equality; the
  grammar excludes them, so it does not arise.)
- **256 distinct maximum rooms exist** *within the declared grammar*: `62 × 65¹²⁷`
  values of exactly 129 octets, trivially sortable and unique. The grammar
  excludes quote, backslash and controls, so UTF-8 and JCS string-content lengths
  agree and no escaping expansion is possible. Whether that grammar is the right
  one is `H-1`.
- **No double charging.** `deviceKeyJwk` is charged once (108) and
  `devicePossessionProof` appears once in the statement (112) and once in the
  offer's excluded members (112) — two different objects, correctly counted
  separately. The 0.4.11 auxiliary budget is still gone.
- **Cap layering is consistent at the relay.** `MAX_ADJACENT_BODY_OCTETS =
  65_536` (`cbcl-pairing/src/cbcl_protocol.rs:34`), outer `grant` bounded at
  63,000 (`profile.rs:271,384`), `credential` bounded at 1..62,000
  (`profile.rs:334,920`), `MAX_CONTROL_OCTETS = 2_048` (`:32`). 41,308 sits far
  inside all three, with 1,000 and 2,536 octets of CBOR/enclosure margin above
  the credential cap.

The one label discipline `ADR-950` introduces is honoured throughout: 52,000,
57,000 and 62,000 are called caps and their gaps residuals; only the derived
figures are called maxima.

---

# Disposition of the original `F-A` – `F-E`

| Finding | Disposition | Evidence |
|---|---|---|
| **F-A** — trust is application+relay scoped; a new application prompts | **HOLDS** | `CON-972`'s six replacements touch `REQ-984` (partly), `REQ-985`, `REQ-986` (partly), `NFR-914`, one `CON-960` sentence and four tests — **not `REQ-981`**, which retains the distinct canonical ApplicationId `https://chat.anuna.io/selfsame/application/v2`, the person-owned policy key `(…/application/v2, https://chat.anuna.io:9443)`, and "A v1 policy entry cannot suppress this first v2 prompt." cbcl-bus `REQ-059` restates the distinct id in its first paragraph and `GATE-04` box 14 mirrors the preload-v1-then-v2 case. The v2 permission namespace remains legal at the baseline: `profile.rs:530-556` constrains permissions only by origin, and both ids share `https://chat.anuna.io`. |
| **F-B** — approval displays the CON-220-authenticated application/transition; cross-check precedes display | **HOLDS, and is strengthened** | cbcl-pairing `CON-022` explicitly preserves every `CON-021` property ("Every private-construction, verdict-only verifier, borrowed-accessor, v1-compatibility, and no-relay-field property … remains exact"), and new `NFR-019` moves every element and aggregate check *ahead of* the clone: "It then checks room count, uniqueness, ordering, and the 33,793-octet aggregate **before cloning or allocating** the owned `CredentialV2Display`." `ADR-020` frames this correctly as allocation safety, not a transfer of room policy — the consumer still owns membership, migration policy, application authority, DID derivation and grant issuance. The `H-1` grammar mismatch can make the display *refuse* a legitimate room; it cannot make it *fabricate* one. |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias provisioning or grant precedes the appropriate explicit consent | **HOLDS** | `CON-972` names nothing in `CON-962`, whose sixteen-step order is therefore intact, and the 0.4.13 preamble restates "Both explicit migration consent screens remain mandatory." I checked each new clause for a pre-consent effect: `REQ-987`/`REQ-988` are grammar and bounds only; `NFR-915` is lock and retry evidence; `REQ-989`'s browser terminal sends no frame and its clearing path *removes* rather than creates standing, retaining the seed. The deployed ordering is real, not merely asserted — `recognise_unsigned` completes the whole closed grammar and the compact-JWS bound "before a private key is constructed" (`enrollment.rs:300-332`), and `cbcl-chat-enrolment-signer` refuses "before reading key material, before opening the seed" (`:43`). **`C-1` is adjacent but does not reopen `F-C`:** an unconsented *destructive* local effect is not one of the seven effects `F-C` enumerates, and it is filed on its own terms. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS** | Untouched by 0.4.13. Adopted `REQ-970`/`CON-939` still mint one pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId` before the public carrier, durable only at the final transaction, inside the distinct v2 namespace; 0.4.12's removal of `accountDid` from the offer core (a value the hub could not know at step 2) is preserved by cbcl-bus `REQ-059`'s "`accountDid` is not an offer-core member. It enters only the later migration-confirmation digest." |
| **F-E** — per-app consent is a mitigation, not a relay-operatorship binding claim | **HOLDS** | 0.4.13 restates it in its preamble ("Relay operatorship remains an accepted residual rather than a claimed binding") and claims nothing further. The relay-visible ciphertext-length residual survives (`MAX_ADJACENT_BODY_OCTETS = 65_536`), and the human privacy-acceptance box is present in the effective gate as `GATE-04` box 28, verbatim from 0.16.5. |

---

# Disposition of every 0.4.12 finding

| 0.4.12 finding | Disposition | Basis |
|---|---|---|
| **H-1** — `REQ-985`'s fixed point is unreachable (`V2_MAX_DID_OCTETS = 512`, `V2_MAX_TIMESTAMP_OCTETS = 32`) | **CLOSED** | `REQ-988` takes the first branch of the offered correction: `V2_DEVICE_DID_OCTETS = 56`, `V2_TIMESTAMP_OCTETS = 20`, `V2_MAX_LEGACY_HANDLE_OCTETS = 33`, and all fourteen figures and four residuals re-derived. I reproduced every one independently (table above) and every one is exact. `ADR-950` states the maxima-versus-caps rule the finding asked for, and `REQ-988` labels 52,000/57,000/62,000 as caps and their gaps as residuals. `CON-972` item 2 ledgers the replacement. cbcl-bus `NFR-013` carries the identical table and `GATE-04` box 15 is *strengthened* from "reproduces" to "is constructed". |
| **M-1** — the dispatch rule reaches comparisons only, missing `selfsame-web-device`'s `PROFILE_VERSION` substitution | **CLOSED for the rule; see `L-1` for the enumeration** | `REQ-987` now says "SHALL NOT compare with **or substitute**", lists all three constant spellings including the cross-crate one, adds "the integer literal 1 as a profile or statement version", adds profile-facts to the enumerated surfaces, and names the `selfsame-web-device::profile_facts` branch. `TEST-1118` requires the trace to cross crate boundaries. cbcl-bus `REQ-059` and `GATE-04` box 12 both gained "and every comparison or substitution site, including profile facts". Three further production sites remain unnamed — `L-1`. |
| **M-2** — `migrationRooms` element has no grammar or cap; the 48,000 witness may be unconstructible | **PARTLY CLOSED; the grammar-agreement half becomes `H-1`** | Closed: the element cap is declared in **both** units (129/129); the array cap is **derived** from it rather than declared independently, so the two cannot disagree; cbcl-pairing `NFR-019` supplies the pre-clone per-element bound the consent display needed; and the array is now 33,793, constructible within the declared grammar. Not closed: the declared grammar is narrower than the deployed room language, which is the defect the finding's "cite the clause that owns it" was pointing at. Filed as `H-1`. |
| **L-1** — `NFR-914`'s cyclic list omits the per-frame reader and create-vs-enroll; cbcl-bus's list is open-ended | **PARTLY CLOSED; the closure claim becomes `M-1`** | Closed: source 6 adds "the per-frame authority read against every matrix writer" and enumerates all six writers; source 4 adds grant creation against member enrollment; cbcl-bus `NFR-013` replaces "include" with "This list is closed"; `GATE-04` box 32 is strengthened to "each closed NFR-013 cyclic pair". Not closed: the list asserts closure it does not have. Filed as `M-1`. |
| **L-2** — "`transaction/2`" is bounded in only one of two overloads | **CLOSED** | `NFR-915` now states the arity **and** the argument: "`mnesia:transaction/2` with an explicit non-negative integer `Retries` second argument, or `mnesia:transaction/3` with `Args` and an explicit non-negative integer `Retries`. `transaction/1`, `transaction(Fun, infinity)`, and `transaction(Fun, Args)` are unbounded and non-conforming." Verified against OTP: `mnesia.erl:720-721` (`/1` → infinity), `:729-736` (three `/2` clauses, only the integer one bounded), `:803-807` (`/3` whose *spec* still admits `infinity`, which is why the "explicit non-negative integer" wording matters and is correct). `TEST-1120` rejects all three forms. |
| **L-3** — the account-bound terminal has no declared state source; a durable flag becomes a stale over-refusal | **PARTLY CLOSED; the clearing trigger becomes `C-1`** | Closed and well done: `REQ-989` names the source (retained seed + accepted installed grant for the current application and account scope), requires the seed to re-derive the recorded key, requires atomicity, and fails closed on corruption or partial state — and every primitive already exists (`idbPutMany`, `durability: 'strict'`, `publicKeyFromSeed`, `idbClearOwnedExcept`). Not closed: the revocation transition it introduces to avoid the stale over-refusal names a wire object that does not exist. Filed as `C-1`, with a residual gap at `L-3`. |
| **L-4** — `CON-960`'s membership sentence is reconciled only by "as applicable" and is unledgered | **CLOSED** | `CON-972` item 5 is exactly the correction requested. I recomputed the member sets: the transition lives in the offer core alone (23 members) and binds directly into `offerCoreDigest` and `intentDigest`; the 21-member CON-214 statement and the exact seven-member `CON-945` hint each carry `offerDigest` and no transition member, so both bind transitively; the final confirmation binds through `offerCoreDigest` **and**, for `migrationSnapshotDigest`, `legacyKeyDigest`, `roomSetDigest` and `snapshotNonce`, directly through `REQ-972`'s `migrationConfirmationDigest` preimage — which is precisely what the item says, and it matches `REQ-972` verbatim. `legacyHandle` and `migrationRooms` bind through `offerCoreDigest` only, and the item's wording covers that correctly. No digest edge is missing and no adopted sentence now contradicts the 4,000-octet JWS policy. |
| **L-5** — the parent's DEC-012 paragraph points at 0.16.2; Selfsame reverts to an open cbcl-bus range | **CLOSED; a narrower cbcl-pairing instance becomes `L-2`** | (a) `git diff fdbe414..874d698 -- specs/SPEC-053-key-root-identity.md` shows the parent at `version: 0.16.6-draft`, `candidate-amendment` pointing to 0.16.6, and the DEC-012 paragraph naming 0.16.6's GATE-04. (b) Selfsame's preamble is now the enumerated "TEST-043 through TEST-065, TEST-068, TEST-070, and TEST-076 through TEST-079", matching cbcl-bus `REQ-058` exactly. The cbcl-pairing half is still a range containing a partly-replaced test — `L-2`. |

---

# Verification of the coordinated amendments

### cbcl-pairing 0.4.5 (`08f8c1f`)

**Sound, and the cleanest of the three.** `REQ-027` states the same two grammars
as Selfsame `REQ-987`, character for character, and the same 33/129 both-unit
bounds, 256-value count, unsigned-UTF-8 ordering and 33,793 aggregate — I
diffed the two grammar blocks and they are identical. `NFR-019` is the real
contribution: it puts *every* check — text grammar, both per-element units, count,
uniqueness, ordering, aggregate — **before** the clone, and names the bypasses it
forbids ("No unbounded iterator collection, extension map, lossy normalization,
escaped surrogate, or post-clone validation"). `CON-022` replaces exactly one
sentence of `NFR-018` (the one that delegated all transition bounds to an unnamed
effective offer limit) and explicitly preserves every `CON-021` property, so
`F-B`'s verdict-only verifier and private construction survive untouched.
`ADR-020` draws the ownership line correctly: allocation safety is the shared
type's, room policy is the consumer's. `TEST-056` mutates prefix, alphabet, case,
element length, count, uniqueness, ordering, unit, aggregate and offer limits by
one and requires a closed profile error **before display construction and before
an unbounded clone**, and re-runs `TEST-053`–`TEST-055` unchanged. The candidate
differs from its baseline only in `specs/trajectory/SPEC-001/`. It inherits
`H-1` because it inherits the grammar, and it correctly does *not* become a room
authority.

### cbcl-bus 0.16.6 (`874d698`)

**Sound on the ledger, and the GATE-04 audit passes.** `REQ-058`'s replacement
list is exact and matches Selfsame `CON-972` item for item. `NFR-013`'s figures
are identical to `REQ-988`'s — I compared all fourteen results and four residuals
and there is no drift. The operation matrix is unchanged from `NFR-913` and its
`?ALL` claims verify against OTP source.

I compared **every** GATE-04 box against **all** prior amendments. 0.16.5's
thirty-nine boxes map one-to-one onto 0.16.6's thirty-nine; nothing is dropped
and six are strengthened:

```text
box 1  pointer   0.4.12/0.4.4  ->  0.4.13/0.4.5
box 7  pointer   REQ-057       ->  REQ-060
box 12 STRONGER  "+ and every comparison or substitution site, including profile facts"
box 15 STRONGER  "reproduces in all three lanes" -> "is constructed in all three lanes"
box 22 STRONGER  "exact closed outcome" -> "exact closed BROWSER AND HUB outcome"
box 32 STRONGER  "expected cyclic source" -> "each closed NFR-013 cyclic pair"
box 33 STRONGER  "bounded retry" -> "bounded INTEGER-retry"
box 36 STRONGER  "truthful, zero enrollment frames" -> "survives reload, sends zero
                 enrollment frames while installed, and clears only on authenticated
                 exact-key not-issued"
```

Every box from 0.16.1 (7), 0.16.2 (11), 0.16.3 (16) and 0.16.4 (14) has a
counterpart in the effective thirty-nine — I walked all forty-eight. One phrase
has no successor and is recorded as a residual rather than a finding: 0.16.2's
box "the frozen v1 signer rejects every v2/mixed statement **before key access**"
survives in 0.16.6 as box 12's disjointness claim without the ordering clause.
The property itself is enforced in code (`enrollment.rs:300-332`;
`cbcl-chat-enrolment-signer.lfe:43`) and by `CON-962`, so nothing is lost in
substance — but a gate box that named an ordering now names a graph.

`REQ-060` and `CON-026` restate the authority outcomes without change and add the
durable-browser paragraph; they inherit `C-1` from Selfsame `REQ-989` and would
move with it. `CON-025`'s NIF boundary is correct as a design constraint — the
browser module `credential-v2-canonical.mjs` does not exist at the baseline, and
the production signer path is already profile-bound rather than caller-selected
(`cbcl-chat-enrolment-signer:sign/1` routes through
`cbcl_selfsame_erl:sign_enrollment_profile_bound/4`, and the statement is
recognised inside the NIF, not in LFE). `TEST-079` requires the parent DEC-012
pointer, which now resolves.

### did-crdt (`1f409a4`)

Candidate equals baseline; nothing to verify beyond the pointer, which
`CON-972` states correctly. Its working tree carries unrelated dirt, ignored per
the mandate.

---

# Independent attack pass — what I looked for and did not find

Recorded so the next round knows what has been swept.

- **Profile downgrade / v1-v2 trust confusion.** The two ApplicationIds share an
  origin, and `profile.rs:530-556` constrains permissions only by origin, so the
  namespace separation is a property of the two frozen generated byte strings and
  of the wallet comparing the requested set against the authenticated document —
  not of the recogniser (carried forward as `R-11`). Nothing in 0.4.13 changes
  this, and `REQ-981`'s distinct-prompt rule still holds.
- **TOFU suppression.** `REQ-981`'s "A v1 policy entry cannot suppress this first
  v2 prompt" is unreplaced; `GATE-04` box 14 gates it.
- **Dispatch before TOFU.** Verified in code: `dispatch/3` routes `grant` and
  `grantproof` before the `missing-from` check and before
  `cbcl-chat-account-grants:account-binding`, so a control verb never falls
  through to `do-publish` or TOFU-enrols a key first
  (`cbcl-chat-session-ws.lfe:214-233`).
- **Display fabrication / consent reordering.** cbcl-pairing `CON-021`'s
  verdict-only verifier and private construction are explicitly preserved by
  `CON-022`; `NFR-019` only *adds* checks ahead of the clone. `CON-962`'s order
  is untouched.
- **Size undercount / relay-body mismatch.** Every level re-derived above; the
  62,000 / 63,000 / 65,536 layering is consistent and 41,308 sits far inside.
- **NIF caller-selected bytes.** `cbcl_selfsame_erl`'s exported surface is
  `verify_path_b/2`, `verify_path_b_sealed/2`, `verify_path_b_standing/3`,
  `rehydrate_path_b/2`, `verified_path_b_revocations/2`,
  `path_b_standing_cache_expiry/1`, `operation_permission/2`,
  `sign_enrollment/4`, `sign_enrollment_profile_bound/4`. Production signing goes
  through the profile-bound entry point, which looks the `kid` up in the
  recognised profile, so configuration cannot select a key the profile does not
  declare.
- **Production gate leakage.** Both Tier-1 source constants remain closed at the
  baselines: `cbcl-chat-path-b-gate:profile/0` returns
  `#(error tier-1-unapproved)` and there is no `cbcl-chat-credential-v2-gate`
  module. `GATE-04` boxes 24 and 38 keep the closure and fixture controls
  physically separate.
- **CPace and blind-relay bounds.** Re-checked and unchanged from 0.4.12:
  `MAX_CONTROL_OCTETS = 2_048`, `MAX_CONTROL_DEPTH = 8`,
  `MAX_ADJACENT_BODY_OCTETS = 65_536` (`cbcl_protocol.rs:32-34`), with the
  bounded ASCII s-expression check applied at both control sites (`:676,761-765`)
  and the body cap applied before buffering at both adjacency sites (`:834,886`).
  The relay-visible size-correlation residual is unchanged and `GATE-04` box 28
  still accepts it.
- **Handle authority confusion / pairaccount substitution.** `REQ-060`'s outcomes
  are unchanged from 0.16.5 and `REQ-053`'s possession proof still gates the
  claim; `unratified-count-tx` counts on the sender's own handle index
  (`cbcl-chat-pairaccount.lfe:70-75`), so slot exhaustion stays self-inflicted
  (`R-13`).
- **Acknowledgement loss / recovery divergence / partial migration.** `REQ-972`'s
  "no truncation, paging, hidden remainder, or partial migration" is unreplaced;
  `GATE-04` boxes 9, 23 and 24 gate the drills. `H-1` is the one place where
  "complete" and the new grammar disagree.

---

# Residual assumptions and open gates — *not* design blockers

`R-1` through `R-14` carry forward from 0.4.12; re-verified at these baselines
except where noted. `R-15` and `R-16` are new.

- **R-1 — `maxDevicesPerAccount` still does not exist.**
  `ratified-max-devices-per-account/0` returns `'unratified`
  (`cbcl-chat-session-ws.lfe:898`), so the capped binder is unreachable until
  GATE-01's CON-002 box closes. `GATE-04` box 25 re-links it.
- **R-2 — the compiled conformance allowlist is still in the code.**
  `RelayPolicy.approved_conformance: &'a [[u8; 32]]`
  (`crates/selfsame-app-identity/src/cbcl_relay.rs:11,90`) requires descriptor
  membership in a build-time digest list. Adopted `CON-933` forbids exactly this;
  its removal is implementation work the coordinated text requires and nowhere
  schedules. Unchanged.
- **R-3 — challenge-table aggregate RAM is bounded per connection only.**
  Unchanged by 0.4.13.
- **R-4 — post-commit authority depends on continuous resolver availability.**
  A did-crdt outage is a total lockout with no legacy fallback, by design.
  `GATE-04` box 30 gates it. This is what makes `C-1`'s blast radius larger than
  "re-enrol".
- **R-5 — `authority-busy` still has no clause naming its per-frame surface.**
  `NFR-915` makes it "refuses closed at every consumer"; no clause states that
  the per-frame authority read's `authority-busy` refuses the *frame*.
- **R-6 — every one of the 66 `mnesia:transaction` call sites in
  `apps/cbcl_chat/src/` is the unbounded `/1` form** at the baseline. `NFR-915`
  is a design requirement, not a description; bounded retries are a real code
  change at every listed operation. No `mnesia:activity` is used, so the
  static check has one function family to cover.
- **R-7 — CPace and blind-relay bounds re-checked, unchanged.** See the attack
  pass.
- **R-8 — the compromised-Path-A-key takeover remains an accepted, gated risk;**
  `GATE-04` box 26 carries it.
- **R-9 — removing `store-verified-device-grant/6` breaks roughly ten existing
  test call sites.** Bounded work, unchanged.
- **R-10 — every new artefact is absent at the baselines**, correctly for a
  design-only increment: `credential-v2-canonical.mjs`, the v2 profile route, the
  v2 provider-hint recogniser, the `cbcl-member` `pubkey` index, both
  `cbcl-device-grant` indexes, both guard tables, the browser `pairaccountchallenge`
  and account-bound handlers, and — new this round — the credential/v2
  installation record and its key in `isCbclIdentityStoreKey`.
- **R-11 — v1/v2 permission separation is a document property, not a grammar
  property.** The generator's two immutable outputs are the control.
- **R-12 — the v2 TOFU prompt is structurally distinct but visually close.**
  One path segment separates the two ApplicationIds; worth a sentence in the
  consent copy the human review accepts.
- **R-13 — the pairaccount per-handle cap is evaluated on the sender's own
  handle**; its protection comes from the possession proof.
- **R-14 — the per-connection Path-B standing cache is a real, bounded
  staleness.** It caches standing and permissions, never issuance;
  `path-b-issued?` re-reads the durable grant row every frame
  (`cbcl-chat-session-ws.lfe:393-396`), so revocation is not delayed by it.
- **R-15 (new) — no GATE-04 box now names the "before key access" ordering.**
  0.16.2's box said the frozen v1 signer "rejects every v2/mixed statement before
  key access"; the effective box 12 asserts frozen bytes and disjoint call
  graphs. The property is enforced in code and by `CON-962`, so nothing is lost
  in substance — recorded because the gate is where a reviewer looks for it.
- **R-16 (new) — the room and handle grammars are *v2 admission preconditions*,
  whichever way `H-1` and `M-2` are resolved.** Even the widened option leaves
  a 256-room ceiling and a 62,000-octet credential cap, so some accounts remain
  unmigratable by size alone. `REQ-972` already names `MigrationPlanTooLarge` for
  that; the grammar cases need the same treatment, and the human review that
  accepts irreversible migration should be shown the resulting exclusion set.

---

# What would make this pass

1. **cbcl-bus wire, with Selfsame `REQ-989` and cbcl-bus `REQ-060`/`CON-026`** —
   close `C-1`. Either define an authenticated, frame-correlated,
   account-scoped hub refusal and require the browser to match all three of its
   own account scope, device key and installed grant before clearing, or state
   that no hub error clears the record and restate `GATE-04` box 36 accordingly.
2. **Selfsame `REQ-987`/`REQ-988`, cbcl-bus `REQ-059`, cbcl-pairing `REQ-027`** —
   close `H-1`. Either widen `CanonicalRoomText` to the deployed recipient
   language and keep 129 as a labelled v2 cap, or keep the narrow grammar and
   declare the closed `MigrationRoomUnrepresentable` refusal, its display, and a
   `GATE-04` human-review acceptance for the exclusion. Reconcile with
   `REQ-972`'s "complete unique raw room-byte array" either way.
3. **Selfsame `NFR-915`, cbcl-bus `NFR-013`** — close `M-1`. Replace the
   hand-listed set with the two-line derivation, enumerate what it yields
   (adding at least `G×F`, `F×F`, `F×OR`, `OR×OR`, `PC×PC`, and retesting
   source 5), and keep the closure sentence over the derived set.
4. **Selfsame `REQ-987`** — close `M-2`. Say whether `LegacyHandle` is an
   admission precondition or a widened grammar; do not describe a client regex as
   a deployed maximum.
5. **The three lows** — `L-1` (name the three `selfsame-web-device` builder sites
   and `enrollment.rs:452`), `L-2` (enumerate the cbcl-pairing survivors around
   `TEST-050`), `L-3` (require the installed grant to verify on reload).

`C-1` and `H-1` each block independently. Neither is architectural: the
standalone design, the consent order, the custody boundary and the digest graph
all survive intact, and `REQ-988`'s arithmetic — the thing 0.4.12 rejected — is
now correct to the octet. Both blockers are the same species of error: a repair
that asserted a property of deployed behaviour without checking the deployed
behaviour.

---

# Authority statement

**This review confers no implementation, allocation, enablement, release or
deployment authority whatsoever, and a PASS from a future review would not
either.** Specifically:

- The verdict is **REJECT**; the coordinated set — Selfsame 0.4.13, cbcl-pairing
  0.4.5, cbcl-bus 0.16.6, did-crdt at `1f409a4` — is not approved.
- Even on a later PASS, `CON-972`'s own sentence governs: "A fresh PASS
  authorizes only the Elephant SPL and test-first implementation plan. It
  authorizes no code or deployment." The adopted 0.4.10 sequencing says the same.
- `GATE-04` remains no-go with all thirty-nine boxes open, and this amendment
  correctly says it closes none of them. `GATE-01` and `GATE-03` remain required
  and are not addressed here; note `R-1`, which makes GATE-01's CON-002 box a
  precondition of the device-grant path working at all.
- Production credential/v2 allocation stays closed. Both Tier-1 source constants
  stay closed at the baselines (`cbcl-chat-path-b-gate:profile/0` returns
  `#(error tier-1-unapproved)`; no `cbcl-chat-credential-v2-gate` module exists).
- Human cryptography, migration and privacy review, independent CPace and
  endpoint vectors, both durability gates, the representative load report and its
  human operational acceptance, owner approval, and separate deployment
  confirmation all remain outstanding and are not substituted for by this review.
- SPEC-007 `REQ-812`, `REQ-809`, `REQ-813`/`REQ-814`/`CON-801` and the
  cbcl-pairing production gates are inherited hard stops that this review neither
  waives nor weakens. `REQ-812` was re-audited against every new clause and holds.
