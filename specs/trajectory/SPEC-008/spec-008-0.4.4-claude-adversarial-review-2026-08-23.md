---
id: SPEC-008-0.4.4-claude-adversarial-review
title: Tier-1 fresh-context cross-model adversarial review — SPEC-008 0.4.4-draft and cbcl-pairing SPEC-001 0.3.0-draft
status: complete
tier: 1
verdict: REJECT
verdict-selfsame-spec-008-0.4.4: REJECT
verdict-cbcl-pairing-spec-001-0.3.0: REJECT
reviewer: independent Circus adversarial reviewer, fresh context, no access to the authoring conversation
model: Claude Opus 5 (claude-opus-5)
review-date: 2026-08-23
candidates:
  - "[[spec-008-0.4.4-standalone-tofu-first-contact]] — specs/trajectory/SPEC-008/spec-008-0.4.4-standalone-tofu-first-contact.md at selfsame 670becb98c7210c4a6914530085a5b58aa022009"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md at cbcl-pairing 7ddea48dbab842ca0db48aac5b59df9582097531"
reviewed-revisions:
  selfsame-integration-baseline: 670becb98c7210c4a6914530085a5b58aa022009
  selfsame-code-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
  cbcl-pairing-spec: 7ddea48dbab842ca0db48aac5b59df9582097531
  cbcl-pairing-code: 8e55160a3234127bd60ce723fd93ba6b2b67a06b
  cbcl-bus: fdbe4142261923f48cced037a7f609e48a9fdd5a
  did-crdt: 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
prior-review: "[[spec-008-0.4.3-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# Verdict: REJECT

Two HIGH and six MEDIUM findings remain. Both coordinated candidates fail.

- **Selfsame `SPEC-008` 0.4.4-draft — does not pass.** H-1, H-2, M-1 through M-6.
- **cbcl-pairing `SPEC-001` 0.3.0-draft — does not pass.** Every requirement it
  states was verified sound against `8e55160`, and it closes the prior review's
  H-1 and M-6 completely. It fails as the coordinated vehicle: H-1 (it is the
  only cbcl-pairing amendment in the pair, it asserts a complete existing-stop
  disposition and a complete approval list, and both omit the parent contracts
  that credential/v2 actually changes) plus L-6.

The 0.4.3 REJECT is substantially answered. H-1, H-2, M-1, M-2, M-4, M-6 and
L-1 through L-7 are closed; M-3 and M-5 are closed in substance with residue.
The two new HIGHs are not regressions of those findings — they are defects the
0.4.4 rewrite introduced or left standing beneath them.

---

## Revision verification

| Repository | Reviewed revision | Verified |
|---|---|---|
| `selfsame` (this worktree, `circus/spec008-v044-review/2`) | `670becb98c7210c4a6914530085a5b58aa022009` | `git status` clean at session start. `git diff --name-only c6af6df75d033d2e44ae367457c1a754ba2c4c3a 670becb` lists **only** seven files under `specs/trajectory/SPEC-008/`, so the non-spec code baseline `c6af6df` holds exactly. `670becb` adds the candidate and nothing else (1,580 insertions, one file). |
| `selfsame` code parent | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | `docs(spec): handoff prompt for the cbcl-pairing + selfsame + cbcl_chat integration`. |
| `selfsame` prior-review baseline | `59db90ba027c11d39681996a250d12f7133a04a4` | Candidate's "Reviewed baselines" table cites `59db90ba027c`; resolves to the merge of `circus/spec008-v043-review/1`. Intervening revision to `670becb` is spec-only, as recorded above. |
| `cbcl-pairing` spec | `7ddea48dbab842ca0db48aac5b59df9582097531` (`spec: define commitment-authenticated v2 mailboxes`) | Working tree carries untracked `.DS_Store`, `.zetl/`, `fuzz/.DS_Store`, `specs/.zetl/`, `src/.DS_Store` only. No tracked modification. |
| `cbcl-pairing` code parent | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` (`docs: restore aligned pairing swimlane`) | Confirmed as `7ddea48^`. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | Clean. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | **Docs-only dirt recorded, not edited:** modified `specs/did-crdt-spec.md` (+127 lines; `git diff --stat` shows one file, zero source files); untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md`. No source, test, or asset file is dirty, so every code claim below is read against committed `1f409a4`. |

### Candidate baseline table re-traced

All four observed-behaviour claims in the candidate's own "Reviewed baselines"
table hold:

- **selfsame.** `src-tauri/src/cbcl_pairing.rs:35,291` and
  `src-tauri/src/cbcl_context.rs:244` reach `local_demo::credential`;
  `crates/selfsame-app-identity/src/hierarchy.rs:178-182` `HomeKey::home_did`
  calls `selfsame_core::identity::derive_did` and nothing else — no signature,
  no `issuer::create`; `crates/selfsame-app-identity/src/issuer.rs:134-164`
  signs three deltas; `crates/selfsame-app-identity-net/src/` exports
  `submit_revocation` and `confirm_revocation` but **no** DID-creation or
  alias/assertion publication operation.
- **cbcl-pairing.** `schemas/pairing-v1.cddl:16` closed union
  `bind / allocate / claim / open / put / ack / close / ping`;
  `src/wire.rs:44-79` projects `Allocate{locator_mode,ttl_seconds}` and
  `Claim(Locator)`; `src/relay.rs:341-410` takes `randomness.mailbox_id` from
  the shell; `src/relay.rs:413-428` resolves the locator before the pure
  command; `src/mailbox.rs:552-577` `Some(_) => Ok(next.terminate(Crowded))`;
  `src/storage.rs:23,225-241,251-256` encodes exactly seven array fields under
  `cbcl-pairing-mailbox-store/v1` with no version dispatcher.
- **cbcl-bus.** `apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-http.lfe:56-78`
  is a self-declared "PROTOTYPE POSTURE … signs a caller-supplied statement";
  `cbcl-chat-webfinger-store.lfe:123` `provision/3` has no production caller
  (grep across `apps/**/src` finds tests only);
  `cbcl-chat-session-ws.lfe:1667-1696` with `cbcl-core-wire.lfe:311-323` show
  `pairaccountgranted` carrying only handle/count/devices/new — no ceremony,
  principal, or record digest.
- **did-crdt.** `src/service/server.rs:141-155` — no CORS layer on `/:did` or
  `/dids/:did/closure` (only `src/service/rendezvous.rs:47-61` has
  `cors_headers`); `src/service/handlers.rs:77-99` `create_did` unconditionally
  `state.docs.lock().insert(did, doc)` over any existing document;
  `handlers.rs:292-295` returns `404` for a delta against an unknown DID.

---

## Executive rationale

The 0.4.4 draft is a materially better document than 0.4.3. The two HIGHs it
was rejected for are genuinely closed, and I verified both by reconstruction
rather than by reading the disposition table:

- **Anti-crowding is real now.** `REQ-944` plus companion `REQ-016` make a bare
  v1 `claim` against a v2-tagged mailbox return the same canonical `Error(404)`
  as an unknown mailbox with byte-identical mailbox state, and `TEST-1031`
  names the exact canonical v1 `claim` byte sequence against waiting, paired
  and closed v2 mailboxes. Against `mailbox.rs:552-577` and `relay.rs:413-470`
  this is implementable as a pure-core admission-profile branch taken after
  `resolve` and before `transition`.
- **The offer window fits.** `CON-939` mints `OfferCoreV2` only after both
  Finished values, and the whole prepare/finalize/deliver/authorise path is
  inside one 30-second state against a 120-second signed window
  (`ceremony.rs:76,402`, `authorise.rs:170`). The pre-Finished human path no
  longer poisons the next state.
- **The device proof is acyclic.** I reconstructed the graph:
  `carrierDigest -> OfferCoreV2 -> offerCoreDigest`,
  `transcriptHash = 64-octet CPace TH` (`channel.rs:64,115`),
  `devicePossessionProof = Ed25519(SHA-256(domain || offerCoreDigest || transcriptHash))`,
  then hub evidence over parent fields **plus** the proof.
  `devicePossessionProof` is not a member of `OfferCoreV2`, so nothing signs
  itself. The prepare/finalize pair is not a signing oracle: the only
  caller-chosen input that reaches signed bytes is a 64-octet `transcriptHash`
  inside a fixed canonical JSON shape, and it is worthless to the caller because
  the wallet requires equality with its own transcript.
- **The alias story matches the store.** `CON-941`'s primary-key table now
  agrees with `cbcl-chat-webfinger-store.lfe:53-75` (alias-keyed alias table,
  alias-keyed tombstone with an `account` index, multi-generation tombstones at
  `:245-268`), and the fictional "roll back by withdrawing" is replaced by a
  version-checked exact-row restoration.
- **The arithmetic is right.** I recomputed every CBOR figure from the CDDL
  member sets and RFC 8949 §3 head rules. 64,558 / 64,560 / 4,144 / 66,628 /
  6,212 / 66,644 / 6,228 / **66,696** / 6,278 / 66,721 / 201,768 / 2,660 all
  hold. The 0.4.3 off-by-two is fixed.

What blocks PASS is not those. It is two things the rewrite did not reach.

**H-1.** The prior review's H-3 said: a cross-repository parent's contracts are
amended in substance and dispositioned nowhere. 0.4.4 answers that for
cbcl-pairing `REQ-003`/`REQ-005` with a proper companion amendment — and then
does exactly the same thing to cbcl-pairing `CON-001`, `CON-002`, `CON-004` and
`CON-005`. Credential/v2 replaces the invitation carrier grammar, adds a second
CPace public-context domain and a second HKDF/Finished key-schedule label set,
and replaces the intent/decision/payload choreography, in a repository whose
three existing profiles all share exactly one of each. Neither candidate's
`amends:` names them; neither disposition table has a row; the cbcl-pairing
owner is asked to approve only the relay-admission change.

**H-2.** `intentDigest` is a mandatory member of every v2 application object,
the value every performative cites, a field of `IssuerPreview`, a check inside
`commit_issuance`, and a required cross-implementation vector — and it is
defined nowhere. Worse, the object that must carry it first is `offer`, and v2
deleted the `intent` performative whose canonical bytes defined it in v1. There
is no reading under which a conforming encoder can be written.

The six MEDIUMs are: a reachable recovery path that bricks v2 pairing while the
document says it does not (M-1); the origin ledger reproducing the exact
no-repair defect M-5 was raised for and closed only for the policy record
(M-2); `CON-209` amended and named nowhere (M-3); the `CON-226` normative
conformance corpus, whose completeness rule covers almost every contract this
amendment touches, named nowhere (M-4); no deployment rollback for three
outward-facing one-way changes, with SPEC-007 `REQ-810` missing from the
inherited table (M-5); and a new normative permission that lets a ceremony lift
an operator-only withdrawal tombstone (M-6).

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct.

`candidate:NNN` references
`specs/trajectory/SPEC-008/spec-008-0.4.4-standalone-tofu-first-contact.md` at
`670becb`. `companion:` references
`/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md`
at `7ddea48`. All other `file:NNN` references are to the confirmed baselines.

**There are no CRITICAL findings.**

---

### H-1 (HIGH) — Credential/v2 replaces cbcl-pairing's carrier grammar, CPace public context, key-schedule labels and application choreography; four parent contracts are amended in substance, named in no `amends:`, dispositioned in no table, and put to no owner

**Exact candidate clauses.**

`candidate:384-386` (`REQ-948`):

> "`anuna.io/credential/v2` SHALL be an additive, non-negotiated profile with
> **distinct CDDL, public-context domain, CPace/Finished/HKDF labels, CBCL
> dialect, role projections, application body types, and independent vectors.**"

`candidate:746-750` (`CON-934`):

> "The **v2 PairingContext** and all role-separated CPace/Finished associated
> data bind both local profile digest and `carrierDigest`, every carrier field,
> the resolved mailbox ID, and the v2 domain. … **A v1 constructor cannot take
> these types.**"

`candidate:706-724` (`CON-934`) gives the v2 carrier: `version 2`, `profile`,
`application-context`, `profile-version`, `relay-origin`, `locator`,
`ceremony-id`, `carrier-nonce`, `claim-commitment`, optional
`expected-allocator-key` — and **no `secret`**.

`candidate:8` (`amends:`) says of cbcl-pairing only: "cbcl-pairing SPEC-001
REQ-003/REQ-005 through coordinated 0.3.0-draft". `candidate:9`
(`conforms-to:`): "cbcl-pairing SPEC-001 REQ-002/NFR-002 and retained hard
stops". `candidate:1540-1544` (approval list): "… **cbcl-pairing SPEC-001
0.3.0**, cbcl-bus SPEC-075 REQ-007, and did-crdt HTTP amendments require their
… human owners' approval".

The companion's frontmatter `amends:` names `REQ-003`, `REQ-005`, `CON-002`,
`CON-003` and nothing else; its "Existing-stop disposition" table has rows for
REQ-002/003/005/008/009/013/014/015, NFR-002–006, NFR-009–012 and production
gates. Its "Outcome and boundary" scopes it to relay admission: "This amendment
adds a generic, application-unaware **v2 admission profile**".

**Governing clauses.**
`cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:1486-1496` §Amendment
Channels:

> "Amendable by: the `cbcl-pairing` specification owner and affected application
> owners. **Through: a reviewed, merged revision of this specification with
> updated assets, tests, evidence, and compatibility disposition.** Not
> amendable by: … implementation behaviour, test fixtures, or dependency
> updates."

- `:289-293` **REQ-003** (not a hard stop): "Adding an application SHALL require
  **only an endpoint-side profile.**"
- `:681-699` **CON-001**: "The exact grammar is `schemas/pairing-v1.cddl` plus
  `schemas/pairing-v1.abnf`. … **An invitation contains version 1, suite
  `CPACE25519-SHA512-D21`, application identifier, canonical relay origin,
  locator, secret, and optional ceremony-key digests.**"
- `:708-712` **CON-002**: "The exact client and server grammar is
  `schemas/pairing-v1.cddl`."
- `:784-833` **CON-004**, verbatim pinned shapes:

  ```cddl
  pairing-ci = [ "cbcl-pairing-ci/v1", 1, "CPACE25519-SHA512-D21",
    application-id, relay-origin, mailbox-id, ["allocator","claimant"] ]
  pairing-public-context = [ "cbcl-pairing-public-context/v1", 1,
    "CPACE25519-SHA512-D21", application-id, relay-origin, mailbox-id,
    bstr .size 32 / null, bstr .size 32 / null ]
  ```

  with `PRS` = "Exact invitation-secret octets" and the fixed label set
  `pairing-v1 kc A/B`, `pairing-v1 key A-B/B-A`, `pairing-v1 iv A-B/B-A`,
  `pairing-v1 exporter`, `pairing-v1 finished A/B`.
- `:858-880` **CON-005**: "The exact deterministic CBOR grammar is
  `schemas/pairing-v1.cddl`. … The intent contains application, action, both
  claim bodies, authority summary, and a 32-octet nonce."

**Code evidence that these are protocol-wide, not per-profile.**

- `cbcl-pairing/src/context.rs:7-9`:

  ```rust
  const CI_DOMAIN: &str = "cbcl-pairing-ci/v1";
  const AD_DOMAIN: &str = "cbcl-pairing-ad/v1";
  const CHANNEL_CONTEXT_DOMAIN: &str = "cbcl-pairing-public-context/v1";
  ```

  and `:25` `pub fn derive(invitation: &Invitation, mailbox_id: [u8; 32])` —
  one constructor, one shape, taking the v1 `Invitation` type.
- `cbcl-pairing/src/channel.rs:118-147` hard-codes
  `b"pairing-v1 kc A"`, `b"pairing-v1 kc B"`, `b"pairing-v1 finished A"`,
  `b"pairing-v1 finished B"`, `b"pairing-v1 key A-B"`, `b"pairing-v1 key B-A"`,
  `b"pairing-v1 iv A-B"`, `b"pairing-v1 iv B-A"`, `b"pairing-v1 exporter"`.
  Nothing about them is parameterised by profile.
- `cbcl-pairing/src/profile.rs:508,586,663` — the agent, credential and
  synthetic profiles differ only in claim decoding and display. All three share
  `context.rs` and `channel.rs` unchanged. The repository's own architecture
  therefore treats context and key schedule as CON-004's property, not the
  profile's.
- `cbcl-pairing/schemas/pairing-v1.cddl:10-11`: `application-id = tstr .size
  (1..96)`, `relay-origin = tstr .size (1..255)`. `candidate:714,716` set
  `application-context` to **1..2048** and `relay-origin` to **1..2048**. Even
  if the v2 context reused the v1 rules it could not carry the v2 carrier's own
  declared field widths.
- `cbcl-pairing/schemas/pairing-v1.cddl:138-158` `pairing-intent` /
  `pairing-decision` / `application-payload` are the CON-005 grammar that
  `candidate:394-404`'s projected session replaces wholesale (offer →
  intent-approve → preparation → comparison → final-approve → payload →
  receipt, two decisions, no `intent` object).

**Failure trace.**

1. This review returns PASS. `candidate:1519-1527` step 2 encodes the design as
   an SPL theory with "every upstream amendment approval" as a generic gate.
2. The operative approval list is `candidate:1540-1544`. For cbcl-pairing it
   names exactly one artefact: SPEC-001 0.3.0. The cbcl-pairing owner reads
   that document. It is titled "Commitment-authenticated v2 mailboxes", its
   boundary paragraph says "application-unaware v2 admission profile", and its
   `REQ-017` promises "adding another application that uses its exact semantics
   SHALL require only endpoint-side profile work". The owner approves an
   admission change.
3. Implementation proceeds under `candidate:1497` — the cbcl-pairing slice —
   which quietly contains "context/profile-digest domains/CDDL/dialect". A
   second `CHANNEL_CONTEXT_DOMAIN`, a second `pairing-ci` shape, a second
   HKDF/Finished label family in `channel.rs`, a carrier type with no `secret`,
   and replacement intent/payload grammars all land in `cbcl-pairing`.
4. `CON-001`, `CON-002`, `CON-004` and `CON-005` are thereby amended by
   implementation and by new assets — a channel §Amendment Channels names as
   **not** amendable — with no revision of SPEC-001 covering them, no
   compatibility disposition, and no NFR-007/CON-009 vector or
   NFR-008/CON-010 dialect disposition.
5. `candidate:130-131`'s own opening sentence — "This amendment is a
   coordinated revision request; it does not pretend a child spec can silently
   waive its parents" — is false of the document, in exactly the way the 0.4.3
   review's H-3 said it was.

**Why REQ-003 does not cover it.** REQ-003 permits an application to be added
with "only an endpoint-side profile". A second CPace public-context domain and
a second key schedule are not a profile; they are the protocol's cryptographic
core. `context.rs` and `channel.rs` are shared by every profile, contain no
profile parameter, and are the implementation of CON-004, which is a contract
of the parent specification. The companion's own REQ-003 disposition concedes
the boundary: "A reviewed protocol version **may add one reusable,
application-unaware admission profile**; it cannot add application identifiers
or phases to the relay" — a sentence about relay admission that says nothing
about the endpoint key schedule.

**Minimum correction.** Either (a) extend the companion (or add a second,
named cbcl-pairing amendment) with explicit dispositions for **CON-001** (v2
carrier grammar without `secret`, widened `application-context` and
`relay-origin`), **CON-002** (the new normative rules added to
`schemas/pairing-v1.cddl`), **CON-004** (second public-context/CI/AD domain
set, second HKDF/Finished label family, `PRS` sourced from a presence code
rather than an invitation `secret`), **CON-005** (v2 intent/decision/payload
replacement and the two-decision choreography), **NFR-007/CON-009** (v2 vector
corpus) and **NFR-008/CON-010** (v2 dialect); and name that amendment in
`candidate:8` and in `candidate:1540-1544`; or (b) constrain credential/v2 to
reuse CON-004's exact context shape and labels, and say so, in which case
`candidate:384-386` and `candidate:746-750` must be rewritten and the
`application-context`/`relay-origin` widening dropped. Add a `TEST-1042`-class
row asserting that a v1 endpoint and a v2 endpoint cannot be made to agree on a
transcript, and a companion test row asserting that no v1 vector's transcript
value changes.

---

### H-2 (HIGH) — `intentDigest` is a mandatory member of every v2 application object and a normative binding in five clauses, is defined nowhere, and cannot be defined for the first object because v2 deleted the performative that produced it in v1

**Exact candidate clauses.**

`candidate:579-582` (`NFR-906`):

> "A v2 application object uses deterministic CBOR integer keys `{0: version,
> 1: kind, 2: **intentDigest**, 3: logicalLength, 4: paddedBody}`. `offer` and
> `payload` pad field 4 to exactly 64,512 octets; all control objects pad it to
> exactly 4,096 octets."

`candidate:585-587`: the encoder witness accounts for field 2 as "one 35-byte
digest pair", i.e. a 32-octet byte string.

`candidate:408-409` (`REQ-948`): "Every performative cites the predecessor
content hash, **the intent digest**, ceremony ID, and role required by the
dialect."

`candidate:297-299` (`REQ-946`): "… transmission of the resulting issuer DID in
a preparation **bound to the intent digest**."

`candidate:868-871` (`CON-938`): `preview_issuer` "zeroizes the derived key
before returning `IssuerPreview` containing DID, fingerprint, ceremony ID,
**intent digest**, and the REQ-951 absolute deadline"; `commit_issuance`
"checks ceremony, **intent**, and final-decision hashes".

`candidate:1299-1301` (`TEST-1028`): "Two independent implementations reproduce
every public carrier, context, CPace/Finished value, key schedule, AEAD frame,
CBCL control hash, fixed body, predecessor hash, **intent digest**, and
terminal classification."

**Nothing defines it.** A grep of the complete candidate for `intentDigest` and
"intent digest" returns exactly the six sites above plus `candidate:280`
("intent nonce", a different value), `candidate:903` (`intentNonce`, an
argument of `selfsamepairallocate`) and `candidate:929` (`intentNonce`, an
`OfferCoreV2` member). No clause states a preimage, a domain string, or a hash
function for `intentDigest`.

**The two candidate readings both fail.**

1. *Inherit v1's definition.* `cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:861-864`
   (CON-005): "The intent contains application, action, both claim bodies,
   authority summary, and a 32-octet nonce. **Its digest is SHA-256 over exact
   canonical bytes.**" The object is `pairing-intent`
   (`schemas/pairing-v1.cddl:138-146`). `candidate:394-404`'s projected v2
   session contains **no `intent` performative** — it begins `Allocator ->
   offer`. `candidate:384-386` further declares v2's "application body types"
   distinct. There is no v2 `pairing-intent` and therefore no v1 digest to
   inherit.
2. *Substitute `offerCoreDigest`.* `candidate:963` defines `offerCoreDigest` as
   "the raw SHA-256 of the RFC-8785 serialization of that exact object" and
   `CON-939`/`REQ-947` use that name consistently. `candidate:870-871` lists
   "ceremony, intent, and final-decision hashes" as three separate checks
   alongside a separate `offerCoreDigest` recomputation in `REQ-945:233`. Two
   distinctly named normative values cannot be silently identified, and doing
   so would make the `offer` object's field 2 a digest of content carried
   inside its own field 4 without saying so.

**Failure trace.**

1. An implementer writes the v2 encoder against `NFR-906`. The first object on
   the wire is `offer`. Field 2 must be a 32-octet `intentDigest`. There is no
   preceding message, no defined preimage, and no clause permitting a
   placeholder.
2. `CON-937`'s reducer requires each state to record "one content hash per
   state" and each performative to cite "the intent digest". Without a
   definition the reducer's binding cannot be written, so `TEST-1029`
   ("For every v2 state, test the valid next sender and all invalid senders …")
   cannot distinguish a conforming from a non-conforming implementation on that
   dimension.
3. `TEST-1028` is release-blocking and demands two independent implementations
   reproduce the intent digest. Two implementations that each invent a
   different preimage both pass their own suites and fail each other; the row
   cannot be authored at all.
4. `CON-938`'s `commit_issuance` "checks ceremony, intent, and final-decision
   hashes" before `issuer::create`. The final effect gate — the single most
   load-bearing check in the design — is conditioned on an undefined value.
5. Inherited SPEC-007 `REQ-804` (a hard stop:
   `specs/SPEC-007-cbcl-pairing-cutover.md:227-231`) reads "approves **the exact
   recognised intent digest** through an explicit person action", and
   `candidate:141` amends it to "Preliminary intent consent permits only the
   issuer preview". The amendment retains the term and still does not define it,
   so the hard stop cannot be shown to be satisfied in either its original or
   its amended form.

**Minimum correction.** Define `intentDigest` normatively in `CON-936` or
`CON-937`: name the exact object it digests (the `AuthenticatedIntent`, the
`OfferCoreV2`, or a new canonical v2 intent record), its domain separation, and
its hash; state what the `offer` object carries in field 2 given that it is the
first performative; and reconcile it with `offerCoreDigest` so the two names are
either identified explicitly or shown to have different preimages. Add the
preimage to `TEST-1028`'s vector list and add a `TEST-1030` sub-row asserting
the exact field-2 bytes for a fixed `offer`.

---

### M-1 (MEDIUM) — CON-935 says a wallet restore "creates an empty policy and prompts again"; on the reachable same-device restore path its own read rule refuses every v2 ceremony instead, and TEST-1014 asserts the behaviour the contract forbids

**Exact candidate clauses.** `candidate:779-782` (`CON-935`):

> "Both slots absent means empty only after a valid root opening; **a present
> record that yields no valid slot**, or equal revisions with different
> plaintext, **refuses**."

`candidate:808-810`: "Policy is device-local and deliberately omitted from
recovery export; **restoring the wallet creates an empty policy and prompts
again.**"

`candidate:1181` (`TEST-1014`): "A recovery restore **imports no policy and
prompts again**."

`candidate:187-188` (`REQ-943`): "Policy unavailability, authentication
failure, or two-slot commit failure **opens no socket**."

**Code evidence.**

- `src-tauri/src/custody.rs:190-197` — `restore` refuses when an identity
  already exists, so the same-device path is `forget()` then `restore()`.
- `src-tauri/src/custody.rs:344-346` — `forget` calls `store::delete(ROOT_ENTRY)`
  and nothing else. `ROOT_ENTRY` is `"root-v1"` (`src-tauri/src/store.rs:41`);
  the two policy slots named at `candidate:770-772`
  (`selfsame-pairing-relay-policy-a` / `-b`) are separate keychain entries and
  survive.
- `src-tauri/src/custody.rs:200-232` — `store` draws a **fresh random 16-octet
  salt** on every seal, and `derive_wrapping_key(passcode, &salt)`
  (`:359-361`) is Argon2id over passcode **and salt**. A restore therefore
  produces a different `custodyWrappingKey` for the same phrase and the same
  passcode, so `policyKey = HKDF-Expand(custodyWrappingKey, …)`
  (`candidate:774-776`) differs and neither slot AEAD-opens.

**Failure trace.**

1. A person uses the first-run "start again" affordance, or reinstalls over the
   same OS keychain, then restores from their twelve words on the same device.
2. `ROOT_ENTRY` is rewritten under a fresh salt. Slots `-a` and `-b` are still
   present and still authenticated under the **old** `policyKey`.
3. The next v2 pairing reads policy. Both slots are present; neither opens.
   `candidate:779-782` requires **refuse**.
4. `candidate:187-188` then opens no socket. Every v2 ceremony on that
   installation refuses, permanently, until the person finds
   `candidate:790-796`'s "reset relay policy" in policy management — an
   affordance whose stated trigger is a corrupt record, not a recovery.
5. `candidate:808-810`'s "restoring the wallet creates an empty policy and
   prompts again" is false on this path, and `TEST-1014:1181` asserts it. A
   harness that runs the row on a fresh install passes while the contract is
   violated on the device where restore actually happens.

This is the availability half of the M-5 defect the two-slot scheme was written
to close, recurring one layer up. It is fail-closed, so it is not a security
finding — but it is a normative claim that is false in a reachable ordinary
path, and a release-blocking test row that asserts it.

**Minimum correction.** State in `CON-935` that identity creation, restore, and
`forget` SHALL delete both policy slots and the origin ledger in the same
operation (they are device-local and deliberately not recovery state, so
deletion is exactly the specified semantics), or state that an unopenable slot
whose associated data names a *different* wallet root fingerprint or a
superseded salt generation is treated as absent rather than as a present
invalid record. Split `TEST-1014`'s restore row into fresh-device and
same-device-after-`forget` cases and assert the same outcome for both.

---

### M-2 (MEDIUM) — The installation-keyed origin ledger is mandatory on the only v2 entry path, refuses on any failure, and has no repair — the exact property M-5 was raised for and closed only for the policy record

**Exact candidate clauses.** `candidate:801-806` (`CON-935`):

> "The pre-network origin-attempt ledger is a separate closed record sealed
> under a random installation key held by the OS keychain, not a custody/root
> key. It stores only origin digest, first/last-attempt time, count, and carrier
> digest. **Its failure refuses before network I/O.** … The ledger is an
> availability/SSRF throttle, **not identity authority and not recovery
> state**."

`candidate:160-165` (`REQ-943`) makes it mandatory: "… and use CON-935's
installation-keyed origin ledger to enforce at most five distinct never-seen
application origins in a rolling 24-hour window; exceeding it refuses before
network I/O." There is no v2 path that skips it — every v2 ceremony
dereferences an `application-context`.

`candidate:1181-1182` (`TEST-1014`): "Corrupt the installation-keyed origin
ledger and require zero network I/O without a custody prompt."

`candidate:790-796` specifies a passcode/presence-gated reset for the **policy
slots** only. No clause specifies a rebuild, reset, or absent-treatment for the
ledger or for a lost installation key.

**Failure trace.**

1. The random installation key is an ordinary keychain entry
   (`src-tauri/src/store.rs:38-48` shows the service and entry model; there is
   no CAS and no durability guarantee beyond `assert_durable`'s probe of a
   *different* entry at `:26-31`). A keychain migration, an OS account move, a
   partial write, or an Android TEE key invalidation loses it.
2. The ledger record is still present and no longer opens.
3. `candidate:801-806` requires refusal before network I/O. `REQ-943` reaches
   the ledger on every v2 attempt.
4. Every v2 pairing on that installation refuses, permanently. Unlike the
   policy record there is no gated reset, and the document itself argues the
   record is worth nothing — "not identity authority and not recovery state" —
   so the fail-closed posture buys nothing it does not already have from a
   fresh empty ledger.
5. `TEST-1014:1181-1182` locks the behaviour in as release-blocking, so an
   implementer who does the sensible thing (discard and rebuild empty) fails
   the suite.

**Minimum correction.** Say what repairs the ledger. The natural rule, and the
one consistent with the record's own declared status, is: an unopenable or
missing ledger is rebuilt empty on the next attempt, the rebuild is recorded in
the local audit log, and the rebuild does not require passcode or presence —
because the record is a throttle, and the worst case of an attacker forcing a
rebuild is five extra prompted origins. Update `TEST-1014` to assert the
rebuild path and to assert that a rebuild cannot be triggered without clearing
the counter honestly.

---

### M-3 (MEDIUM) — `CON-209`'s closed five-member provider hint gains carrier and transcript bindings; `CON-209` is in neither `amends:` nor the coordinated-approval list, and the change also moves `CON-214`'s recogniser

**Exact candidate clause.** `candidate:958-960` (`CON-939` step 3): "That
evidence carries the parent fields plus `accountPrincipalDigest`,
`applicationAccountDisplay`, `carrierDigest`, `transcriptHash`, and
`devicePossessionProof`, all exact-equality inputs. **The provider hint carries
the same carrier/core/descriptor/transcript bindings.**"

**Governing clauses.** `specs/SPEC-004-application-scoped-identity.md:3278-3288`
(`CON-209`) fixes the bound logical value at exactly five members:
`applicationId`, `profileVersion`, `providerId`, `descriptorDigest`,
`offerDigest`. `:3298-3301`: the hint "is authenticated by that envelope's tag
over additional authenticated data containing the **PROTO-003 `binding_hash`**"
— a value credential/v2 does not have. `:3639-3642` (`CON-214`): the evidence
recognizer "rejects … any value outside the inherited CON-201, CON-205,
**CON-209**, CON-211, and CON-219 grammars".

**Code evidence.** `crates/selfsame-app-identity/src/provider_hint.rs:55-61`:

```rust
const HINT_MEMBERS: &[&str] = &[
    "applicationId", "profileVersion", "providerId",
    "descriptorDigest", "offerDigest",
];
```

with `HintError::UnknownMember` — "The hint carries a member the credential
contract does not define" (`:39-41`) — and `authorise.rs:174-186` refusing on
any hint mismatch. This is a closed grammar with unknown-member rejection: the
CON-201 hazard class.

**Failure trace.**

1. `candidate:8` enumerates the SPEC-004 amendments as
   `CON-201/204/206/211/214/219/220/221`; `candidate:1538-1542` repeats exactly
   that list for owner approval. `CON-209` is in neither.
2. The Selfsame owner approves eight named contract amendments. The hint is
   changed anyway, because `CON-939` requires it.
3. Two conforming recognisers now disagree: one built to `CON-209` as ratified
   rejects the v2 hint with `UnknownMember`; one built to `candidate:958-960`
   accepts it. Both compute the same `offerDigest`.
4. `CON-214`'s own recogniser inherits the CON-209 grammar, so the change
   silently moves a second contract that *is* on the approval list, in a way the
   list does not describe.
5. `TEST-1018:1245-1248` asserts "carrier/session/stable-account/distinct-
   device/proof/provider bindings" — it will pass against whichever grammar the
   implementer chose, so the corpus cannot see the divergence.

**Minimum correction.** Add `SPEC-004 CON-209` to `candidate:8` and to
`candidate:1538-1542`, state the v2 hint's exact closed member set in
`CON-939`, state that the PROTO-003 `binding_hash` authentication clause is
replaced for v2 by the CPace transcript binding, and add a `TEST-1019` sub-case
mutating each added hint member independently.

---

### M-4 (MEDIUM) — `CON-226`'s normative conformance corpus is a completeness *gate* over almost every contract this amendment changes, and no requirement, slice row, test row, or gate step names it

**Governing clauses.**
`specs/SPEC-004-application-scoped-identity.md:4655-4660` (`CON-226`
Completeness rule):

> "For every closed error token defined by **CON-204, CON-211, CON-212,
> CON-214, CON-215, CON-219, CON-220, CON-221**, CON-222, CON-223, and CON-225,
> and for each of **CON-206's thirteen numbered steps**, the corpus SHALL
> contain at least one case whose `expect.reject` names it. **A token or step
> with no case is a gate failure, not a documentation gap.**"

`:4663-4666` Required group 2 is "profile recognition under **CON-201** and
discovery under **CON-220**". `:4676-4678`: "The corpus SHALL NOT be edited to
match an implementation … **The file's SHA-256 is recorded in the changelog
entry of every Tier-1 amendment that changes it.**"
`:6586-6588` (Amendment Channels): "A change to the CON-226 corpus is such an
amendment, and its new SHA-256 is recorded in the changelog entry."

**Overlap with the candidate.** `candidate:8` amends
`CON-201/204/206/211/214/219/220/221`. Six of those eight are named in
CON-226's completeness rule, and CON-206 is named step-by-step. `REQ-949`
splits CON-206 into steps 1–8 and 9–13 across a WASM boundary and changes what
satisfies step 9; `CON-933` mints a second profile grammar and removes CON-220
tier 3; `CON-939` adds five members to CON-214's statement; `CON-941` changes
what CON-204 provisioning means and when it happens.

**Failure trace.**

1. The document is implemented. `TEST-1011`–`TEST-1047` all pass.
2. `test-vectors/spec-004-v1.json` still carries the ratified CON-201 v1
   profile-recognition group, no version-2 cases, no case for the amended
   CON-206 step 9, and no case for the new CON-214 members.
3. Under `CON-226:4658-4660` that is "a gate failure, not a documentation gap"
   — and the SPEC-004 Tier-1 gate is the standing gate SPEC-007 `REQ-809`
   inherits in full (`specs/SPEC-007-cbcl-pairing-cutover.md:291-292`: "The hold
   includes the complete standing SPEC-004 Tier-1 gate").
4. Nothing in `candidate:1495-1506` (slices), `candidate:1141-1400` (tests) or
   `candidate:1519-1531` (gate and sequencing) tells anyone to touch the
   corpus, and `candidate:1547-1580` (changelog) records no corpus SHA-256.
5. The corpus is normative and "SHALL NOT be edited to match an
   implementation", so discovering the gap after implementation is the
   expensive order.

**Minimum correction.** Add a normative sentence to `CON-933`/`CON-939`/
`CON-941` (or one clause covering all of them) requiring the CON-226 corpus to
gain cases for every added or changed closed error token and for the amended
CON-206 steps, add a `TEST-1042`-adjacent row asserting CON-226 completeness
against the amended contracts, add the corpus to the `selfsame` pure-core slice,
and record the requirement to publish its new SHA-256 in the changelog.

---

### M-5 (MEDIUM) — Three outward-facing one-way changes and no deployment rollback; SPEC-007 `REQ-810` is missing from the inherited table, and the live ratified profile is re-minted with no ordering against any deployed recogniser

**What is one-way.**

1. **The ratified profile.**
   `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-gate.lfe:7,16`
   — "`document/0` is the ONE place the ratified octets live server-side … The
   octets are the exact RFC 8785 canonical document **minted 2026-08-22**".
   Decoding `:28-55` gives a live `profileVersion: 1` object with one
   `cbclPairingRelays` descriptor (`operatorId: "anuna-1"`, `relayOrigin:
   "https://chat.anuna.io:9443"`), no `rendezvous`, no `pairingRecordRelays`.
   `candidate:679-684` (`CON-933`) requires that exact object to be re-minted as
   version 2 and requires "Its fixture, served JCS bytes, embedded browser
   bytes, digest, and wallet vectors SHALL **all** come from that one version-2
   generator output." Re-minting changes `SHA-256(RFC8785(profile))`, which is
   the value `CON-220` step 6, `CON-214`, and every pinned wallet vector
   compare against. Any recogniser built before the dispatcher
   (`crates/selfsame-app-identity/src/profile.rs:60,80-91`, `:384-387` refuses
   `profileVersion != 1`) refuses the new object outright.
2. **The hub store.** `CON-941:1050-1064` requires the four legacy tables to be
   "annotated" with a stable account ID plus new secondary indexes. In Mnesia
   that is `transform_table` plus `add_table_index`, both schema transactions
   that cannot be composed with the data transaction `REQ-947:298-306`
   describes, and neither is reversed by redeploying an older release.
3. **did-crdt.** `REQ-950:456-462` and `CON-940:995-999` change `POST /dids`
   semantics and add public CORS to two routes on an outward-facing service.

**Governing clause not dispositioned.**
`specs/SPEC-007-cbcl-pairing-cutover.md:299-311` **REQ-810 Release rollback**:

> "When a prior reviewed `cbcl-pairing` release exists, rollback SHALL deploy
> it. Rollback SHALL invalidate every unfinished pairing created by the
> reverted release."

`candidate:134-158`'s inherited hard-stop table has rows for SPEC-007 REQ-803,
804, 805, 806, 807, 809, 811, 812, 813, 814/CON-801/TEST-818, NFR-802/CON-802,
CON-806 and NFR-801. **REQ-810 has no row.** (SPEC-008 parent REQ-903 and
REQ-904 are likewise absent, though neither is contradicted; see L-9.)

**Contrast within the coordinated pair.** The companion does this correctly:
its "Rollback and retained residuals" section gives an ordered operational
sequence — disable new allocation, close live v2 mailboxes, wait through the
600-second expiry, verify no v2 records remain, install the v1-only binary —
and states that an older binary "SHALL fail startup rather than discard or
reinterpret" store v2. I verified that claim against
`cbcl-pairing/src/storage.rs:130-152`: `load` propagates `decode(&bytes)?`, and
`decode:255-256` refuses any domain other than
`cbcl-pairing-mailbox-store/v1`, so a v1-only binary does fail startup rather
than silently drop a v2 record. The candidate has no equivalent section for any
of its three outward-facing changes.

**Failure trace.** The v2 slices ship to staging. The profile is re-minted to
version 2 so the allocator can embed and recognise it (`CON-933:696-698`
requires exactly that before allocation). Any wallet build predating the
dispatcher now fails CON-220 step 4 against the live `.well-known` object and
the deployed CON-219 enrolment path stops. A defect is found. `REQ-810` says
deploy the prior reviewed release — but the prior release serves the version-1
object whose digest the new browser bundle and wallet vectors no longer pin,
and the hub tables have been transformed. There is no specified sequence for
getting back, and `candidate:1531` asks the owner only "before any cbcl-bus or
did-crdt production **deploy**".

**Minimum correction.** Add a "Rollback and deployment sequencing" section
covering: the ordering of the profile re-mint against wallet and browser
releases (and whether a compatibility window serving both versions is required
or forbidden); a SPEC-007 `REQ-810` row in the inherited table stating how
rollback works once the Mnesia transform has run; the reverse migration or the
explicit statement that the transform is forward-only and rollback is by
feature gate rather than by binary; and did-crdt's revert posture for the CORS
and create-idempotence changes. Add a test row for the rollback rehearsal.

---

### M-6 (MEDIUM) — `CON-941` lets a ceremony lift a withdrawal tombstone at finalization, converting an operator-only revocation lever into one the constrained party can trigger

**Exact candidate clause.** `candidate:1046-1049` (`CON-941`):

> "It SHALL NOT create a withdrawal tombstone as compensation. **A same-account
> relink of the same tombstoned URI may perform exact-account reinstatement at
> finalization**; a different-DID relink leaves prior URI tombstones intact."

**Governing clauses and code.**
`cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:139-149`:
`create-or-return` reads the tombstone by URI under a write lock and returns
`#(error withdrawn)` — the comment at `:144-148` says "an operator asking why a
provision will not take is owed the one answer **no amount of retrying will
change**".

`:271-300`, the `reinstate/2` doc comment, verbatim:

> "The repair for **a withdrawal aimed at the wrong account**. … **IT LIFTS THE
> BLOCK; IT DOES NOT RESTORE THE BINDING.** Forging the two rows back would mean
> asserting an alias-to-DID binding on no better evidence than a deleted row,
> **and this table is the thing verifiers trust.** Re-provisioning is the
> enrolment path's job and will now succeed …"

`reinstate/2` and `withdraw/2` have no HTTP projection: a grep across
`cbcl-bus/apps/**/src` finds callers only in tests, and `:145-146` states
"Nothing untrusted reaches this function — the HTTP projection never calls it".
They are operator runbook levers.

`specs/SPEC-004-application-scoped-identity.md:2900-2906` (CON-204) makes the
tombstone the mechanism by which a withdrawn binding stays unusable: "Between
issuance and provisioning the grant exists but is unusable, because CON-206
step 9 fails closed on the missing binding. **A verifier SHALL NOT relax step 9**
…".

**Failure trace.**

1. An operator withdraws account `P`'s binding to `did:crdt:AAA` — because a
   wallet was reported compromised, or a support incident required it. One
   tombstone exists at `acct_uri(AAA)`, and `create-or-return` refuses forever
   until an operator explicitly lifts it naming that exact DID.
2. The holder of the compromised wallet runs a v2 ceremony from the same
   signed-in chat session. The scope is stable (`REQ-947:307-309`), so the same
   `AAA` and the same `acct_uri` are derived.
3. `selfsamepairaccept` creates the pending transaction. At finalization,
   `candidate:1046-1048` permits "exact-account reinstatement".
4. The revocation is lifted by the party it was aimed at, with no operator
   action and no second authority. The design's own account-authority query
   (`CON-941:1017-1023`) is not consulted for this — it reports `Bound`,
   `NoBinding` or `Unknown`, not "withdrawn".
5. `TEST-1035:1370-1373` asserts only that "same-alias reinstatement is
   explicit" and that "neither legitimate relink is permanently refused". No row
   asserts that a withdrawal intended as a revocation survives a ceremony by the
   withdrawn party.

The prior review's M-4 asked only that a legitimate relink not be *permanently*
refused. Automatic self-service reinstatement over-corrects past that into a
security-relevant permission, and it is dispositioned against neither CON-204
nor the store's stated authority model.

**Minimum correction.** Either drop the automatic reinstatement and state that a
tombstoned URI refuses the ceremony with a distinct terminal that names operator
reinstatement as the repair (which is the existing, reviewed behaviour and
costs the person one support interaction), or keep it and add the missing
authority: a withdrawal record field distinguishing an operator revocation from
an ordinary unlink, with only the latter self-liftable, plus a `TEST-1035` row
proving a revocation-class tombstone is not liftable by the ceremony.

---

### L-1 (LOW) — `CON-941`'s primary-key table names a module where it means a table, and calls an opaque provider identifier a handle

`candidate:1058` names the table `cbcl-chat-pairaccount`. That is the **module**
(`cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-pairaccount.lfe`); the Mnesia table
is `cbcl-pairaccount-grant` with attributes `(key handle devkey granted state)`,
`type set`, and `(index (handle devkey))` (`:22-38`). The composite key claim is
right. Separately, `candidate:1056` calls `cbcl-webfinger-account`
"handle"-keyed; its key is the `account` attribute, which
`cbcl-chat-webfinger-store.lfe:339-341` `valid-account?` bounds only as a
1..4096-octet binary — "the private provider-side identifier" (`:180-183`).
Since `provision/3` has no production caller, no production rows exist and the
migration for the three webfinger tables is vacuous today; say so.

### L-2 (LOW) — `selfsamepairofferprepare` repeat semantics are unspecified

`candidate:906-940` says the hub "durably holds the exact core" and that "One
ceremony produces at most one **finalized** offer", but never says whether
`prepare` may be called twice. A second `prepare` with a different
`transcriptHash` would replace the held core and restart the 120-second window
from a fresh `issuedAt`. That is harmless to the wallet (the transcript binding
still refuses a wrong core) but it makes the freshness bound the window exists
for self-renewable by the signer. State that `prepare` is once-per-ceremony,
idempotent for a byte-identical `(ceremonyId, transcriptHash)`, and terminal on
a second distinct call. `TEST-1044:1345-1350` covers "after one finalized
offer"; it does not cover repeated prepare.

### L-3 (LOW) — `CON-941` attributes the migration to the wrong artefact

`candidate:1064-1065`: "**CON-939 migration** SHALL complete before either v2
command is reachable". The migration is specified in `REQ-947:296-306`, not in
`CON-939`. Rename the reference.

### L-4 (LOW) — `CON-935`'s reset revision and its both-slots-equal case are undefined

`candidate:790-793`: reset "writes and verifies an authenticated empty document
**at the next revision**" — but reset is reachable exactly when "at least one
policy slot exists but neither is valid" (`:790`), i.e. when no revision is
readable, so "next" has no referent. Separately, `candidate:786-787` targets
"only the absent, invalid, or lower-revision slot"; if both slots were ever
valid at equal revision with identical plaintext (`:781` permits reading that
state) no slot qualifies and no update can proceed. State the reset's starting
revision explicitly and give the equal-revision write rule a deterministic
target.

### L-5 (LOW) — The nine idle deadlines sum past the absolute deadline, and no row asserts the achievable envelope

`candidate:493-508`: the state idle deadlines are 180 + 30 + 30 + 120 + 45 + 90
+ 120 + 90 + 90 = **795 s** against "allocation time plus **600** seconds".
`candidate:510-511`'s "earlier of" rule makes this coherent rather than
contradictory — the 0.4.3 H-2 defect is genuinely gone, because no state's
budget now guarantees the *next* state's failure. But several budgets are
unreachable in a successful ceremony, and `TEST-1043:1330-1340` exercises each
state independently without ever asserting an end-to-end envelope that fits
600 s. Add one row.

### L-6 (LOW, companion) — `mailbox-store-v2` is presented as CDDL but references undefined rules

The companion's `CON-012` `mailbox-store-v2` block uses `status` and `sequence`
as rule names. Neither exists in `cbcl-pairing/schemas/pairing-v1.cddl` (the
store record is Rust-only, `src/storage.rs:196-241`), and the companion does not
define them. Either define both inline or mark the block as illustrative and
name `storage.rs` as the normative encoder. The eight-element shape itself is
correct against `storage.rs:225-241`'s seven elements plus one.

### L-7 (LOW) — `NFR-906`'s component list omits the three-byte `logicalLength` pair

`candidate:585-587` decomposes the totals as "… **a two- or four-byte length
pair** …". A `logicalLength` in 24..255 encodes as `18 xx`, giving a three-byte
pair and a **64,559**-octet object. Both stated totals (64,558 and 64,560) and
all three `TEST-1030:1288-1290` assertions (1 gives 64,558; 61,999 gives 64,560;
62,000 gives 64,560) are correct; only the enumeration is incomplete. Say "two-,
three-, or four-byte".

### L-8 (LOW) — "allocation time" is ambiguous between two distinct events

`candidate:493` anchors the absolute deadline at "allocation time plus 600
seconds". `CON-939:900-914` (hub `selfsamepairallocate`) and `REQ-944:225-234`
(relay `allocate-v2`) are separate events whose order the document does not fix,
and `allocated-v2` returns its own `expiresAt`. Pin the anchor to the relay's
returned `expiresAt` minus the requested TTL, or say the two SHALL be the same
transaction.

### L-9 (LOW) — Two parent requirements have no disposition row

SPEC-008 parent `REQ-903` (live approve/decline, "SPEC-007 REQ-804 consent
ordering unchanged") and `REQ-904` (scan and paste converge) have no row in
`candidate:134-158`. Neither is a hard stop
(`specs/SPEC-008-production-pairing-claimant.md:92-94` lists REQ-902, REQ-905,
REQ-906, REQ-907), and neither is contradicted — v2 keeps scan and paste for the
carrier while `REQ-944:245-248` deliberately gives the PAIR1 field no paste
path, which is a narrowing of REQ-904's scope rather than a conflict. Record
both. (SPEC-007 `REQ-810` is the third missing row; it is blocking and is
folded into M-5.)

### L-10 (LOW) — Two domain-separation inconsistencies

`candidate:336-337` fixes `accountPrincipalDigest = SHA-256("cbcl-chat account
principal v1" || applicationId || applicationAccountId)`, hard-coding one
application's name into a contract the rest of the document keeps
application-generic. And `candidate:731-733`'s presence-code checksum domain
`"selfsame credential/v2 presence code"` carries no `\u0000` terminator, unlike
`candidate:227-230` and `candidate:322-325`, which both do. Neither is
exploitable — every trailing input is fixed-length in all three preimages, so
all three are unambiguous — but the pattern should be uniform and the domain
string should not name one deployment.

---

## Disposition of every 0.4.3 finding

| 0.4.3 finding | 0.4.4's claim | Verified disposition |
|---|---|---|
| **H-1** — v1 `claim` still crowds a v2 mailbox; TEST-998 asserts the contrary | `REQ-944` adopts the companion admission profile; bare v1 `claim`, wrong `claim-v2`, and every surplus claim return `Error(404)` with no mailbox-domain state change | **CLOSED.** `candidate:236-243` and companion `REQ-016` both state it; companion `CON-012`'s v2 state machine has `paired-v2 / closed-v2 --any claim--> unchanged`. Implementable against `relay.rs:413-470` as a branch after `resolve` and before `transition`, and `relay.rs:428` already returns `Error(404)` for an unresolvable locator, so the responses really are identical. `TEST-1031:1315-1318` names the exact canonical bare-v1-`claim` bytes against waiting, paired **and** closed v2 mailboxes and requires byte-identical snapshots — the sub-row the prior review asked for. Companion `TEST-031` independently requires three distinct v1 claims plus three wrong bearers, then a successful bearer. The enumeration half is closed too: an unknown locator and a v2 mailbox now return the same code. |
| **H-2** — the 120 s CON-214 window and the pre-offer deadlines cannot both hold | `CON-939` moves offer construction and the start of the window after both Finished values, on an off-relay hub exchange | **CLOSED.** `candidate:912-914`: the hub "starts the unchanged 120-second CON-214/CON-219 window" at `OfferCoreV2` construction, after `transcriptHash` exists. `candidate:512-518`: the whole prepare/finalize/deliver/authorise path is one 30-second state and the wallet evaluates the window once. Against `ceremony.rs:76,402` (`MAX_OFFER_WINDOW_SECONDS = 120`, grammar-enforced) and `authorise.rs:170` (single `now >= expires_at` check) 30 s is far inside 120 s with generous clock-skew margin. `candidate:906` and `candidate:497-499` both confirm no offer timestamp exists during claim or CPace work, and `TEST-1043:1334-1337` asserts exactly that. The chosen fix is the review's option (a), implemented completely: the exchange is excluded from the relay frame budget (`candidate:406-407`, `candidate:975-977`) and given its own deadline row. |
| **H-3** — cbcl-pairing hard stops amended in substance and dispositioned nowhere; two owners omitted | The companion at `7ddea48` amends REQ-003/REQ-005, retains the rest, names owners; did-crdt owner added | **PARTIALLY CLOSED — see H-1.** The REQ-003/REQ-005 half is fully closed: the companion is a versioned SPEC-001 amendment, its existing-stop table has rows for REQ-002 (retained, with the four-clause argument), REQ-003 (amended, correctly noting it is not a hard stop), REQ-005 (amended for v2 only, correctly flagged as a hard stop needing a new version and Tier-1 review), REQ-008/009/013/014/015, NFR-002–006 and NFR-009–012, and its approval section names all four required reviewers. `candidate:8` now lists REQ-003/REQ-005 and moves REQ-002/NFR-002 to `conforms-to:`. `candidate:1540-1544` names the cbcl-pairing **and** did-crdt owners. SPEC-007 NFR-802 has a row (`candidate:150`). What is not closed is the set of cbcl-pairing contracts the *endpoint* profile changes — CON-001, CON-002, CON-004, CON-005, NFR-007/CON-009, NFR-008/CON-010 — which are named in no `amends:`, have no row, and reach no owner. |
| **M-1** — encoder witness off by two; `logicalLength = 1` is not 64,560 | `NFR-906` distinguishes 64,558 from 64,560 and accounts for the five-octet head | **CLOSED.** Recomputed independently from `pairing-v1.cddl:125-136` and `wire.rs:877-888`: application object 1 + 2 + 2 + 35 + 2 + 64,516 = **64,558** at `logicalLength = 1`, and + 4 = **64,560** at three-octet encoding; control **4,144**; `sealed-plaintext` overhead with a 2,048-octet control = 1 + 8 + 2,051 + 5 + 3 = **2,068**; plaintext **66,628** / **6,212**; ciphertext **66,644** / **6,228**; `sealed-frame` non-ciphertext overhead **47**, plus a five-octet head above 65,535 giving **52** large and **50** control; relay body **66,696** / **6,278**; enclosing `put` **66,721**, under 70,000 (`bin/cbcl-pairing-relay-ws.rs:32`). Every figure in `candidate:576-604` and `TEST-1030:1288-1295` matches. Only the component enumeration is incomplete (L-7). |
| **M-2** — minted `operatorId` grammar contradicts the recogniser | `CON-933` pins ASCII `[a-z0-9][a-z0-9-]{0,62}`, 1..63 octets | **CLOSED.** `candidate:672` matches `crates/selfsame-app-identity/src/profile.rs:803-820` exactly and matches `cbcl-bus/scripts/gen-production-profile.mjs:243,293-294`'s `PROVIDER_ID` regex. The other five descriptor members also match (`MAX_RELAYS = 16` at `profile.rs:79`; `priority 0..65535`; `weight 1..65535`; two base64url-no-pad SHA-256 digests). |
| **M-3** — profile-version split strands production; no slice owns the dispatcher | `CON-933` owns the dispatcher, declares the shipped object nonconforming, coordinates SPEC-075 REQ-007, amends CON-806 step 1, declares tier 3 absent | **CLOSED IN SUBSTANCE; residue in M-5.** All four sub-items are answered: `candidate:678-680` declares the shipped object conforming to neither grammar — verified true against `SPEC-004:2469-2481` (ten names including `rendezvous`) and the live object at `cbcl-chat-selfsame-application-gate.lfe:28-55` (`profileVersion: 1` with `cbclPairingRelays`, no `rendezvous`); `candidate:681-684` coordinates SPEC-075 REQ-007, whose text (`cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md:229-236`) does forbid `rendezvous`; `candidate:691-694` amends CON-806 step 1 so credential/v1 selects from a version-2 profile; `candidate:694-696` declares CON-220 tier 3 unavailable — correct, since `CON-220:4148-4155`'s tier-3 branch runs on `pairingRecordRelays`, which version 2 removes. `candidate:1497-1498` gives the pure-core slice "exact v1/v2 profile dispatcher **including v1 rendezvous grammar**" and `candidate:1504` gives cbcl_chat the "version-2 production-profile generator/embedding". What remains is not the split but the deployment ordering of the re-mint against a live ratified artefact — M-5. |
| **M-4** — re-keying the tombstone misdescribes the baseline and blocks relinking | `CON-941` preserves every legacy primary key, moves only indexes, preserves multi-generation tombstones, defines reinstatement plus rollback | **CLOSED, with a new defect — M-6.** The corrected table at `candidate:1053-1058` matches `cbcl-chat-webfinger-store.lfe:53-77`: alias-keyed alias table; account-keyed account projection; alias-keyed `cbcl-webfinger-withdrawn` with `#(index (account))`; composite pairaccount key. Multi-generation tombstones are preserved (`candidate:1049-1051` against `:245-268`'s "ONE ACCOUNT CAN HOLD SEVERAL TOMBSTONES"). `TEST-1035:1368-1374` adds the withdraw-then-relink row the prior review asked for. The over-correction is the new self-service reinstatement permission (M-6), and the naming imprecision is L-1. |
| **M-5** — unsupported crash-atomic CAS with no repair path | `CON-935` defines two fixed slots, highest-valid-revision reads, locked overwrite of the older slot, read-back, gated reset | **CLOSED FOR THE POLICY RECORD; residue in M-1 and M-2.** The scheme is sound against `src-tauri/src/store.rs:184-188`'s destructive `set_password`: the write never targets the unique highest-valid slot, so a torn write leaves the prior complete authenticated document readable; associated data binding the slot name defeats cross-slot ciphertext copying; the process/OS lock closes the concurrent-writer race; read-back closes the silent-discard case. The named mechanism and the gated reset are exactly what the prior review asked for. Two things it does not reach: the reachable same-device restore (M-1) and the origin ledger, which reproduces the original no-repair property (M-2). L-4 records two undefined edges. |
| **M-6** — no transport for `claimCommitment`; the only admissible one is forbidden | `REQ-944` uses the companion's closed `allocate-v2`/`claim-v2`, client-generated mailbox ID, and store-v2 tag | **CLOSED.** Companion `CON-011` gives both commands closed deterministic-CBOR grammars with distinct `allocated-v2`/`claimed-v2` responses; companion `ADR-012` explains the client-supplied `M` and confirms the relay still owns membership tokens, capacity, and duplicate refusal — implementable against `relay.rs:360-362`, which already refuses a duplicate `mailbox_id` with `Error(503)`. Companion `REQ-018`/`CON-012` add the eighth store field under a new domain. `candidate:602-604` restates the change honestly: "V2 adds `allocate-v2`, `claim-v2`, an admission-profile tag, and store-v2 … v1 `claim` merely receives unknown-mailbox behaviour when its addressed mailbox is tagged v2." The relay slice at `candidate:1505` names the library/NIF path and store-v2. |
| **L-1** — limiter mis-described | `NFR-906` names the real peer-address-keyed per-operation limiter | **CLOSED.** `candidate:596-598` matches `src/limiter.rs:1,62-70` and `src/relay.rs:214`. |
| **L-2** — `amends:` over-inclusive | frontmatter separates amendments from conformance | **CLOSED.** `candidate:9` `conforms-to:` now carries SPEC-004 REQ-215, SPEC-002 REQ-103, and cbcl-pairing REQ-002/NFR-002. (Two new omissions in `amends:` are H-1 and M-3.) |
| **L-3** — PAIR1 rejects the common honest input; local rejection burn unclear | `CON-934` ASCII-normalises and permits local retry | **CLOSED.** `candidate:734-741`: uppercase conversion without Unicode case folding, and a local grammar/checksum/recovery-collision refusal "permits re-entry within REQ-951's current idle/absolute deadline without consuming the invitation". `TEST-1015:1218-1221` tests upper, lower and mixed case and the corrected-re-entry path. Bit arithmetic re-verified: 128 + 128 + 16 + 3 = 275 = 55 × 5. |
| **L-4** — desktop has no OS presence | `CON-935` states the mobile/desktop split | **CLOSED.** `candidate:766-770` matches `src-tauri/src/store.rs:200-203`'s `require_presence` returning `Ok(())` with its own comment; `TEST-1047:1394-1397` asserts "no nonexistent biometric callback". |
| **L-5** — same-origin-control residual undeclared | `REQ-952` records it | **CLOSED.** `candidate:540-547`. |
| **L-6** — two normative obligations untested | `TEST-1047` covers residuals and the three custody operations | **CLOSED.** `candidate:1390-1398`. |
| **L-7** — commitment preimage does not pin the locator encoding | both documents pin raw octets | **CLOSED.** `candidate:231-232` "`M` and `T` are raw octets"; companion `REQ-016` "not a CBOR locator, text, hexadecimal, or base-encoded value"; `TEST-1046:1383-1385` requires encoded substitutions to fail. |

**0.4.2 and 0.4.1 findings** (chained through the 0.4.3 review's own disposition
tables) remain closed. I re-checked the two that the 0.4.3 review marked
"PARTIALLY CLOSED": the v2 nine-name grammar at `candidate:657-666` is still
byte-for-byte `profile.rs:80-91` in the same order, and the BIP-39/PAIR1
separation is unchanged and complete.

---

## Inherited-hard-stop audit

| Stop | Owner spec | Hard stop? | Candidate's row | Verified |
|---|---|---|---|---|
| SPEC-008 REQ-902 | SPEC-008 0.2.2 | yes (`:92-94`) | amended for the reverse direction; fixture prohibition retained | **OK.** `TEST-1040:1355-1359` keeps the symbol-absence gate; `local_demo` reachable only from `crates/selfsame-pairing/{tests,examples}` and `src-tauri/src/cbcl_pairing.rs:291`. |
| SPEC-008 REQ-905 | SPEC-008 0.2.2 | yes | amended; carrier non-navigable, code never in QR/clipboard/URL/log | **OK**, except the carrier grammar it points at (cbcl-pairing CON-001) is amended without disposition — H-1. |
| SPEC-008 REQ-906 | SPEC-008 0.2.2 | yes | replaced by REQ-943 | **OK.** Compiled-digest admission removed; `CON-903` retired at `candidate:139`. |
| SPEC-008 REQ-907 | SPEC-008 0.2.2 | yes | retained | **OK.** `REQ-953:559-563`. |
| SPEC-008 REQ-908 | SPEC-008 0.2.2 | no | retained | OK. |
| SPEC-008 REQ-903 / REQ-904 | SPEC-008 0.2.2 | no | **absent** | L-9. |
| SPEC-008 CON-903 | SPEC-008 0.2.2 | — | retired from claimant admission | OK. |
| SPEC-007 REQ-803 | SPEC-007 | yes (`:1072`) | retained | **OK.** CON-206 still gates acceptance (`REQ-949`). |
| SPEC-007 REQ-804 | SPEC-007 | yes | amended for v2 | **OK as an amendment**, but its operative term "the exact recognised intent digest" is undefined — H-2. |
| SPEC-007 REQ-805 | SPEC-007 | yes | narrowly amended: relay receives `T` and its commitment only | **OK.** `T` is independent of `C`, never enters CPace (`candidate:220-222`), is not persisted (companion `NFR-013`), and is zeroized. TLS observation of `T` is declared in the companion's residuals. |
| SPEC-007 REQ-806 | SPEC-007 | yes | retained — no negotiation or fallback | **OK.** Companion `REQ-017` mirrors it on the relay side. |
| SPEC-007 REQ-807 | SPEC-007 | yes | retained — legacy input inert both ways | **OK.** `TEST-1017:1236-1237` replays v1 into v2 and v2 into v1. |
| SPEC-007 REQ-809 | SPEC-007 | yes | retained without exception | **OK.** `REQ-953`, `TEST-1040`, gate step 6. |
| SPEC-007 REQ-811 | SPEC-007 | yes | retained | **OK.** `candidate:222-223`; companion REQ-013 disposition. |
| SPEC-007 NFR-801 and production gate | SPEC-007 | yes | retained | OK. |
| SPEC-007 REQ-810 | SPEC-007 | no | **absent** | **M-5.** |
| SPEC-007 REQ-812 / REQ-813 / NFR-802 / CON-801 / CON-802 / CON-806 | SPEC-007 | no | amended / narrowly amended | OK; ordinary channel; dispositions accurate against `:328-354,645-678`. |
| SPEC-002 REQ-103 | SPEC-002 | — | retained (hex normative, LifeHash an aid) | **OK.** Matches `CON-221:4196-4204`. |
| cbcl-pairing REQ-002 | SPEC-001 0.2.0 | yes (`:1497`) | retained | **OK.** Relay derives no key, verifies no Finished, retains no plaintext, interprets no body. Comparing and erasing an opaque bearer is not semantic interpretation. |
| cbcl-pairing REQ-003 | SPEC-001 0.2.0 | no | amended by companion REQ-017 | **OK for relay admission.** Not sufficient to cover the endpoint context/key-schedule change — H-1. |
| cbcl-pairing REQ-005 | SPEC-001 0.2.0 | **yes** | amended for v2 only by companion REQ-016 | **OK.** Properly routed: new SPEC-001 version, four named reviewers, Tier-1 review required. V1 third-distinct-claim crowding is retained byte-for-byte. |
| cbcl-pairing REQ-008/009/013/014/015 | SPEC-001 0.2.0 | yes | retained | **OK.** The bearer gates membership only; CPace, Finished, intent, consent and CBCL verdicts are untouched. |
| cbcl-pairing NFR-009 / 010 / 011 / 012 | SPEC-001 0.2.0 | yes | retained | **OK.** Companion `NFR-013` bounds v2 work to the addressed command or mailbox; companion `TEST-037` re-runs the 100 ms ceiling, the sub-64 KiB unrelated-allocation bound, single-lookup, and limiter eviction. |
| cbcl-pairing NFR-002 – NFR-006 | SPEC-001 0.2.0 | no | retained | **OK.** Per-membership counts are 5 (allocator) and 6 (claimant) against `MAX_FRAMES_PER_MEMBERSHIP = 16` (`mailbox.rs:13`). NFR-006 holds because every terminal v2 transition is `V2Closed` with the commitment erased. |
| cbcl-pairing CON-002 / CON-003 | SPEC-001 0.2.0 | no | amended by companion (client-supplied `M`, new state and store) | **OK for admission.** CON-002's "Allocation creates a CSPRNG mailbox identifier" is correctly superseded by `ADR-012`. Not sufficient for the v2 endpoint grammar additions — H-1. |
| cbcl-pairing CON-001 / CON-004 / CON-005 | SPEC-001 0.2.0 | no | **absent from both documents** | **H-1.** |
| cbcl-pairing NFR-007/CON-009, NFR-008/CON-010 | SPEC-001 0.2.0 | no | **absent** | **H-1** (v2 vectors and v2 dialect are asserted by `REQ-948` and slice-listed, but neither contract is dispositioned). |
| cbcl-pairing production gates | SPEC-001 0.2.0 | yes | retained in full | OK. |
| SPEC-004 CON-201/204/206/211/214/219/220/221 | SPEC-004 | Tier-1 normative | amended, owner approval required | OK as declared; see M-3 (CON-209 missing) and M-4 (CON-226 missing). |
| SPEC-004 REQ-215 | SPEC-004 | — | conformed to, not amended | **OK.** `REQ-947:317-325` gives the device key a per-`(applicationId, applicationAccountId, browser installation)` lifecycle. |

---

## State and effect-order audit

I traced the claimed order against the APIs that must express it.

**Pre-network.** `REQ-943` parses the carrier with no I/O (`TEST-1011`), then
prompts for the origin, then consumes the carrier's single contact capability.
No custody call, no passcode. Expressible: recognition is pure.

**Policy.** One passcode plus presence operation after descriptor recognition
and before the socket, with the key zeroized before the socket opens.
Expressible as a new `Custody::use_policy_key`-shaped API alongside
`custody.rs:312-331`'s `use_hierarchy_root`. One wrinkle worth recording: on a
new-pair acceptance the derived key must survive the human prompt inside the
same closure, since the write follows the decision. The document permits this
(`candidate:184-186` says "once for this policy access") but does not say the
key is held across an unbounded human wait.

**Offer authorisation.** `REQ-945` runs the CON-214 predicate, recomputes the
proof-free core digest, verifies the device proof and hub evidence, and consumes
the request ID before display. This matches `CON-214`'s own step order
(`SPEC-004:3690-3697`: step 5 consumption, step 6 consent rendering) and
`:3712-3713`'s "The consumed-ID record is the sole permitted mutation after
syntactically valid evidence reaches step 5". `authorise.rs:131-215` performs no
consumption today, so the ledger is new work — correctly slice-listed.

**Preliminary consent to preview.** `CON-938`'s `preview_issuer` requires backup
confirmation and custody presence, derives inside
`Custody::use_hierarchy_root`, and calls only `HomeKey::home_did` plus
fingerprint rendering. Verified expressible: `hierarchy.rs:267-283`
(`application_node`, `account_node`, `home_key`) then `:178-182` `home_did`,
which calls `selfsame_core::identity::derive_did` and returns a `String`. No
signature, no randomness, no store write, no network. The scope it needs comes
from the authorised offer. The closure returns before the key is zeroized at
`custody.rs:329`.

**Preview disclosure.** `candidate:301-304` states plainly that sending
`preparation` irreversibly discloses the pairwise preview DID even on a later
decline, and requires the preliminary screen to say so. `TEST-1024:1266-1268`
asserts the disclosure happened. This is the honest treatment of the one
residual the split creates.

**CON-221 applicability.** Decided by the application from
`selfsamepairauthority`, a read; `Unknown` fails closed. Matches
`CON-221:4188-4191` ("SHALL determine this from authority state, not from local
cache, and SHALL treat an unreachable authority as 'unknown' and fail closed").
The claimant supplies no applicability bit. `BoundDifferentDid` refuses, which
is `CON-221:4225-4228`'s subsequent-enrollment rule.

**Final consent to commit.** `CON-938`'s effect table is complete and correct
across six states, and `TEST-1025:1273-1277` requires a total order of calls
with every signature, publication, issuance write, bundle build and payload
send after the final-decision hash. One expressibility note: `commit_issuance`
lists "publishes through CON-940" inside the same bulleted sequence as the
derivation, and `use_hierarchy_root` is synchronous while publication is async
network. The sequence is still expressible — `issuer::create`
(`issuer.rs:134-190`) signs all three deltas locally, so the key can be zeroized
before any HTTP call — but the document should not read as if the network call
sits inside the custody closure.

**Post-payload compensation.** `REQ-951` requires a CON-210 revocation, which
needs a home-key signature and therefore a **fourth** custody operation after
`CON-935`'s stated three. The document handles the failure case honestly ("A
failed compensation remains `pending`") but does not name the extra
authentication.

**No leak found across the wrong gate.** I looked specifically for: a signature
before final approval (none — the only pre-final key use is
`HomeKey::home_did`, which is `derive_did` over a public key); a scope mint at
the wallet (none — the scope is hub-created, `REQ-947:311-313`); a publish
before final approval (none — `REQ-950` is entirely post-final); an alias write
before final approval (none — `selfsamepairaccept` runs after the reverse
payload); a grant payload before final approval (none — `CON-937`'s
`DeliverBundleForAcceptance` occurs "only at the allocator after final approval
and reverse payload recognition"); an identity write on decline (none — only
the request tombstone and the separately consented policy/rate state, asserted
byte-for-byte by `TEST-1024:1262-1266`).

---

## Implementability audit per repository

### `cbcl-pairing` (`8e55160`)

- **Admission — implementable.** `MailboxCommand` gains a `ClaimV2` arm and the
  pure state gains a four-value admission profile. `relay.rs:413-470`'s `claim`
  already returns `Error(404)` from two places, so the v2 refusal is a branch
  after `resolve` and before `transition`. `relay.rs:341-410`'s `allocate`
  already refuses a duplicate `mailbox_id`, so `allocate-v2` taking `M` from the
  client is a substitution of one field. `commit` runs before `attach` and
  before the reply is built, so "commits the successful transition before
  emitting `claimed-v2`" is the existing order.
- **Store — implementable.** `storage.rs:196-241` gains a domain constant and
  an eighth element; `decode:251-256` gains a two-domain dispatch. The
  companion's rollback claim holds: `load:130-152` propagates `decode`'s error,
  so a v1-only binary fails startup on a v2 record rather than dropping it.
- **Zeroization — partially.** Companion `NFR-013` requires the recognised `T`
  and comparison input to be zeroized. `wire.rs` recognises through a
  `ciborium::Value` intermediate that is not zeroizing; companion `TEST-032`'s
  wording ("heap-visible **retained** command state") is scoped narrowly enough
  to be honest, but the intermediate copy should be acknowledged.
- **Endpoint v2 — blocked.** `context.rs` and `channel.rs` are single-shape and
  single-label; the v2 profile requires a second of each. That work is
  slice-listed and undispositioned — H-1.

### `selfsame` (code tree `c6af6df`)

- **Profile dispatcher — implementable, large.** `profile.rs:60,80-91,384-387`
  must be split into a version reader plus two complete grammars, and the v1
  branch's `rendezvous` and `pairingRecordRelays` grammars have never existed.
  Correctly slice-listed at `candidate:1497-1498`.
- **Offer and evidence extensions — implementable.** `ceremony.rs:150-258`
  (`OfferCore`, thirteen members) and `enrollment.rs:78-95` (`STATEMENT_MEMBERS`,
  sixteen names) both extend cleanly. `platformBindingId` and `returnUri` are
  already mandatory in the parent statement, so making them mandatory in
  `OfferCoreV2` is inheritance, not invention; the live profile's `web:` binding
  at `cbcl-chat-selfsame-application-gate.lfe:42-44` and the `/selfsame/return`
  route make them satisfiable for a browser allocator.
- **Provider hint — blocked on M-3.** `provider_hint.rs:55-61` is a closed
  five-member set with `UnknownMember` refusal.
- **Preview and commit split — implementable.** Verified above.
- **Policy — implementable.** Two keychain entries plus an installation lock over
  `store.rs:184-188`. M-1 and M-2 are contract defects, not implementability
  ones.
- **Publication — implementable, and the genesis hazard is real but handled.**
  I checked the one thing that could have broken `REQ-950`: `issuer::create`
  builds on a **signed** genesis (`issuer.rs:140-141` calling
  `selfsame-core/src/identity.rs:125-135` `sign_genesis`), while the resolver's
  `POST /dids` writes an **unsigned** one. They are interchangeable because
  `did-crdt/src/core/delta.rs:339-386` computes `content_hash` over
  `{did, op, parents, timestamp}` with the proof deliberately excluded, and
  `selfsame-core/src/identity.rs:109-111` `bootstrap` calls the same
  `Document::new` the service calls, with the same `HlcTimestamp::default()`.
  The alias delta's parents therefore match the resolver's frontier and
  `submit_delta` will not answer `delta_pending`. `candidate:466-471`'s "the
  wallet SHALL NOT claim that submitting its same-content signed genesis
  replaces the resolver's bootstrap genesis" is exactly right.
- **`intentDigest` — blocked.** H-2.

### `cbcl-bus` (`fdbe414`)

- **Stable account ID and migration — implementable with a caveat.** The tables
  exist (`cbcl-member` handle to pubkey; `cbcl-account-binding` handle to
  account; `cbcl-device-grant` key to record; the three webfinger tables;
  `cbcl-pairaccount-grant`). `REQ-947:296-306`'s "commit all changes or none"
  cannot span Mnesia schema operations — `transform_table` and
  `add_table_index` are their own schema transactions and cannot nest inside
  `mnesia:transaction`; the existing code reflects this by doing `add-index` at
  startup (`cbcl-chat-webfinger-store.lfe:97-115`). Read as "schema evolution at
  startup, then a per-account data transaction", the requirement is satisfiable;
  read literally it is not. Say which.
- **Prepare and finalize — implementable.** New authenticated session commands
  over the existing signed chat session, plus RFC 8785 canonicalisation and
  SHA-256 in LFE. The hub must also recognise the CON-934 CBOR carrier — new
  work, slice-listed.
- **`provision/3` bypass — correctly forbidden.** `candidate:1064-1066` says no
  v2 command calls it directly; it has no production caller today.
- **Signing oracle — correctly fenced.** `candidate:975-977` bars credential/v2
  from `/selfsame/enrolment/sign`, whose own source comment
  (`cbcl-chat-selfsame-application-http.lfe:60-64`) admits it "still signs a
  caller-supplied statement". `TEST-1027:1281-1284` asserts the absence of a
  call edge.
- **Acknowledgement — implementable.** `cbcl-core-wire.lfe:311-323`'s
  `pairaccountgranted` must be extended or wrapped; `CON-942:1079-1082` names
  the exact correlators.

### `did-crdt` (`1f409a4`)

- **Idempotent create — implementable.** `handlers.rs:77-99` already holds a
  global `state.docs.lock()`, so serialization exists; the change is to branch
  on presence, verify the existing genesis derives from the supplied key, and
  return the existing resolution instead of `insert`.
- **CORS — implementable.** `server.rs:141-155` registers the two public read
  routes with `get(...)` only; `OPTIONS` currently 405s, so explicit handlers or
  a scoped layer are needed. `rendezvous.rs:47-61` shows the house pattern. The
  candidate correctly forbids CORS on write routes.
- **Docs-only dirt recorded, not edited**, as above.

---

## Bounds and work audit

All figures recomputed independently; none taken from the candidate.

| Quantity | Recomputed | Candidate | Verdict |
|---|---:|---:|---|
| v2 application object, `logicalLength = 1` | 64,558 | 64,558 | correct |
| v2 application object, `logicalLength` 24..255 | 64,559 | not enumerated | L-7 |
| v2 application object, maximum | 64,560 | 64,560 | correct |
| control object, maximum | 4,144 | 4,144 | correct |
| `sealed-plaintext` overhead (2,048-octet control) | 2,068 | 2,068 | correct |
| plaintext, large / control | 66,628 / 6,212 | same | correct |
| ciphertext, large / control | 66,644 / 6,228 | same | correct |
| `sealed-frame` overhead excluding ciphertext head | 47 | implied | correct |
| ciphertext head, large / control | 5 / 3 | 5 / 3 | correct |
| complete relay body, large / control | **66,696** / 6,278 | 66,696 / 6,278 | correct (0.4.3's 66,694 fixed) |
| enclosing `put` message | 66,721, under 70,000 | below 70,000 | correct |
| frames per ceremony | 2 large + 5 control + 4 bootstrap | same | correct |
| frames per membership | allocator 5, claimant 6, both under 16 | "no membership reaches the sixteen-frame ceiling" | correct |
| queue budget per ceremony | 2·67,000 + 5·7,000 + 4·8,192 = 201,768 | 201,768 | correct |
| ceremonies within 512 MiB | floor(536,870,912 / 201,768) = 2,660 | 2,660 | correct |
| commitment metadata at cap | 10,000 × 32 = 320,000 | 320,000 | correct |
| PAIR1 bits | 128 + 128 + 16 + 3 = 275 = 55 × 5 | same | correct |

Constants confirmed in code: `MAX_FRAME_BODY_BYTES = 69_632`
(`mailbox.rs:15`), `MAX_FRAMES_PER_MEMBERSHIP = 16` (`:13`),
`MAX_WIRE_MESSAGE = 70_000` (`bin/cbcl-pairing-relay-ws.rs:32`),
`DEFAULT_MAILBOX_CAP = 10_000` and `DEFAULT_QUEUE_CAP = 512 * 1024 * 1024`
(`:36-37`), `ciphertext: bstr .size (17..69572)` and `body: bstr .size
(1..69632)` (`pairing-v1.cddl:125-131,44-47`), `transcript_hash: [u8; 64]`
(`channel.rs:64`).

CPace and Finished frame cap: a `cpace-frame` is at most about 2,200 octets and
a `finished-frame` about 2,150 against the 8,192 cap — comfortable.

**Relay work.** Companion `NFR-013` bounds `allocate-v2`, `claim-v2`, v1 `claim`
against v2, and v2 snapshot recognition to the addressed command or mailbox. One
SHA-256 plus one constant-time comparison per attempt under the existing
peer-address-keyed limiter is correct against `limiter.rs:62-70` and
`relay.rs:214`. `T` is 128 bits, so online guessing is not the threat; the
threat the v2 rule closes is free destruction, and it is closed.

---

## Tests and gates audit

**Strong rows.** `TEST-1031` (exact bare-v1-`claim` bytes against three v2
mailbox states with byte-identical snapshots), `TEST-1044` (proof-free core, no
self-reference, no pre-Finished oracle, no second offer), `TEST-1043` (offer
timestamps absent through claim and CPace; the 120 s window not re-evaluated by
later human steps), `TEST-1024` (byte-identical durable state after final
decline, and an explicit assertion that the preview DID *was* disclosed),
`TEST-1030` (all three `logicalLength` cases now correct), `TEST-1046`
(companion suite plus raw-octet commitment), `TEST-1038` (crash at each Mnesia
write boundary, alias and pairaccount both old or both finalized). The
companion's `TEST-030` through `TEST-037` are well-formed and directly
executable against the reviewed baseline.

**Rows that cannot pass as written.**

- `TEST-1028` — requires two independent implementations to reproduce the
  "intent digest" (H-2).
- `TEST-1014` restore row — asserts prompting where `CON-935`'s read rule
  requires refusal on the same-device path (M-1).
- `TEST-1014` ledger row — locks in a permanent refusal with no repair (M-2).

**Missing coverage.**

- No row for the `CON-226` corpus completeness gate (M-4).
- No row and no section for deployment rollback (M-5).
- No row proving a revocation-class withdrawal survives a same-account ceremony
  (M-6).
- No row for repeated `selfsamepairofferprepare` (L-2).
- No row asserting an achievable end-to-end deadline envelope inside 600 s
  (L-5).

**Owner approvals.** `candidate:1538-1544` names the SPEC-004, SPEC-007,
cbcl-pairing, cbcl-bus and did-crdt owners — the two the 0.4.3 review said were
missing are now present. Omitted: `CON-209` (M-3), the `CON-226` corpus
(M-4), and the cbcl-pairing endpoint contracts (H-1).

**Gate and sequencing.** Six forward steps, correctly ordered (pure core and
vectors before shells; custody and policy and resolver before wallet issuance;
hub before browser). No rollback (M-5). `candidate:1531` correctly requires a
separate owner ask before any cbcl-bus or did-crdt production deploy and states
that neither has an assumed canary.

**"No PASS may depend on unspecified implementation."** Two violations: the
`intentDigest` preimage (H-2) and the v2 CPace context and label set, which
`REQ-948` asserts exists and no clause defines (H-1).

---

## Residual risks (accurately declared by the candidates, and correct)

- Deliberate forwarding of both the carrier and the presence code is a
  social-engineering residual (`candidate:96-98`, `candidate:263-265`). Bounded
  in practice: `BoundDifferentDid` refuses on a re-enrolment, so the exposure is
  first-enrolment TOFU behind the CON-221 human hex comparison.
- Relay operatorship is not proved (`REQ-952`, `ADR-926`).
- A compromised application origin can serve both the substituted profile and
  the browser bundle that embeds it, so the two digests agree
  (`candidate:540-547`). Correctly named as not recovering a previously honest
  origin.
- No rollback-resistant hardware counter for the policy record
  (`candidate:809-810`). The unsafe direction is bounded: forging an *added*
  pair needs the custody-derived key; a restored older ciphertext can only
  re-add a pair the person removed.
- The relay observes `T` at TLS termination on the successful claim (companion
  residuals).
- A lost `claimed-v2` response strands the mailbox until expiry (companion
  `TEST-036` accepts this explicitly). Strictly better than v1, where the same
  retry crowds.
- CON-207 in the reverse direction is a self-challenge: the browser both holds
  and verifies the device key. It still binds a grant naming a *different* key,
  which is what `REQ-949` step 3 needs, but it is not third-party proof of
  possession and the document does not say so.
- The `preparation` DID disclosure survives a final decline. Declared.
- `CON-206` step 9 is satisfied in v2 by a hub-signed *pending* record, so the
  alias is not publicly resolvable at acceptance time — a declared amendment of
  `CON-204`'s explicit ordering ("provisions … and **only then** runs CON-206").
  Sound only because the verifier and the account authority are the same trust
  domain and finalization gates the receipt; worth stating in the document as
  the residual it is.

---

## Method

Fresh context, no access to the authoring conversation, defect-finding mandate.

1. Confirmed all six revisions and the spec-only delta between the code baseline
   and the integration baseline; recorded did-crdt's docs-only dirt without
   touching it.
2. Read both candidates end to end (1,580 and 509 lines).
3. Read the complete 0.4.3 review and audited each of its sixteen findings
   against the 0.4.4 text **and** against the code, rather than against the
   candidate's disposition table.
4. Re-traced every load-bearing code claim: `cbcl-pairing`
   `schemas/pairing-v1.cddl`, `wire.rs`, `relay.rs`, `mailbox.rs`,
   `storage.rs`, `limiter.rs`, `context.rs`, `channel.rs`, `profile.rs`,
   `bin/cbcl-pairing-relay-ws.rs`; `selfsame`
   `profile.rs`, `ceremony.rs`, `authorise.rs`, `enrollment.rs`,
   `provider_hint.rs`, `hierarchy.rs`, `issuer.rs`, `path_b.rs`,
   `selfsame-core/identity.rs`, `selfsame-app-identity-net/`,
   `src-tauri/{custody,store,cbcl_pairing,cbcl_context}.rs`; `cbcl-bus`
   `cbcl-chat-webfinger-store.lfe`, `cbcl-chat-pairaccount.lfe`,
   `cbcl-chat-session-ws.lfe`, `cbcl-core-wire.lfe`,
   `cbcl-chat-selfsame-application-{http,gate}.lfe`,
   `cbcl-chat-account-grants.lfe`, `cbcl-chat-members.lfe`,
   `scripts/gen-production-profile.mjs`, `specs/SPEC-075-…`; `did-crdt`
   `service/{server,handlers,rendezvous}.rs`, `core/{document,delta,causal}.rs`.
   Base64-decoded the live ratified profile to read its actual member set.
5. Read every parent artefact named by either candidate: SPEC-004
   CON-201/204/206/207/209/211/214/219/220/221/226 and Amendment Channels;
   SPEC-007 REQ-803 through REQ-814, NFR-801/802, CON-806, Amendment
   disposition and hard stops; SPEC-008 0.2.2 REQ-902 through REQ-908, CON-903
   and hard stops; cbcl-pairing SPEC-001 0.2.0 REQ-001 through REQ-015,
   NFR-001 through NFR-012, CON-001 through CON-006 and Amendment Channels;
   cbcl-bus SPEC-075 REQ-007.
6. Recomputed every CBOR, frame, queue and bit figure from the CDDL member sets
   and RFC 8949 §3 head rules; recomputed the deadline sums.
7. Reconstructed the OfferCoreV2, evidence, device-proof, carrier and transcript
   hash graph and enumerated each value's authoritative source and each
   party's independent recomputation.
8. Probed adversarially for: pre-consent identity effects; a signing oracle in
   prepare/finalize; circular proofs; v1 and v2 crowding and enumeration; policy
   crash, concurrency, rollback and restore paths; migration atomicity against
   real Mnesia semantics; withdrawal and reinstatement authority; genesis
   content-hash agreement between the wallet and the resolver; and undefined
   normative values.
9. No candidate, parent spec, code, fixture or sibling repository was edited.
   The sole change in this worktree is this review artefact.
