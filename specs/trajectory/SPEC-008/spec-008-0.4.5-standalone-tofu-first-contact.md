---
id: SPEC-008-amendment-0.4.5
title: SPEC-008 0.4.5-draft — Standalone pair-scoped TOFU, versioned endpoint, and reversible release
status: draft
version: 0.4.5-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-804]]; [[SPEC-007-cbcl-pairing-cutover#REQ-805]]; [[SPEC-007-cbcl-pairing-cutover#REQ-810]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-007-cbcl-pairing-cutover#REQ-813]]; [[SPEC-007-cbcl-pairing-cutover#NFR-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; [[SPEC-007-cbcl-pairing-cutover#CON-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-806]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-204]]; [[SPEC-004-application-scoped-identity#CON-206]]; [[SPEC-004-application-scoped-identity#CON-209]]; [[SPEC-004-application-scoped-identity#CON-211]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-221]]; [[SPEC-004-application-scoped-identity#CON-226]]; cbcl-bus SPEC-075 REQ-007; did-crdt HTTP service contract; cbcl-pairing SPEC-001 through coordinated 0.3.0 and 0.4.0 drafts"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; cbcl-pairing SPEC-001 REQ-002 and retained hard stops"
supersedes: "[[spec-008-0.4.4-standalone-tofu-first-contact]]"
adopts: "All 0.4.4 clauses except the exact replacements and corrections named by this amendment"
review-inputs: "[[spec-008-0.4.4-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.3.0 at 7ddea48; cbcl-pairing SPEC-001 0.4.0 at 47424b7"
last-updated: 2026-08-23
---

# SPEC-008 0.4.5-draft — Standalone pair-scoped TOFU, versioned endpoint, and reversible release

> **Design target, not implementation authority.** The owner selected Option 1:
> standalone first-contact pairing, person-owned exact-pair relay TOFU, and full
> cross-repository implementation. The fresh Claude/Opus Circus review rejected
> 0.4.4 with zero critical, two high, six medium, and ten low findings. This
> amendment closes each finding. It adopts the otherwise unchanged 0.4.4 design
> and replaces only the clauses named below. No source, schema, fixture, vector,
> release, or deploy change is authorized until another fresh-context
> Claude-driver Circus review returns PASS over the complete coordinated set.

## Effective candidate and unchanged security boundary

The effective candidate is this document plus:

1. [[spec-008-0.4.4-standalone-tofu-first-contact]], except where this document
   says `replaces` or `corrects`;
2. cbcl-pairing SPEC-001 0.3.0, which owns protected blind-mailbox admission;
   and
3. cbcl-pairing SPEC-001 0.4.0, which owns the distinct credential/v2 endpoint,
   crypto schedule, application envelope, dialect, and independent vectors.

The security outcome remains standalone pairing with no enrolment prerequisite
and no compiled relay allowlist. The person consents to the exact pair
`(applicationId, relayOrigin)`. Live CON-220 authentication, co-present CPace,
authenticated account-bound offer evidence, truthful display provenance, a
preliminary DID-preview consent, a final identity-effect consent, direct
resolver verification, and acknowledgement-gated browser acceptance remain
mandatory. Relay operatorship and same-origin compromise remain explicitly
declared residuals rather than silently upgraded claims.

## Disposition of the 0.4.4 review

| Finding | 0.4.5 disposition |
|---|---|
| **H-1 — endpoint protocol outside companion authority** | [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-954]] adopts cbcl-pairing SPEC-001 0.4.0, which explicitly amends CON-001/002/004/005/009/010 and NFR-007/008 and defines exact assets and vectors. |
| **H-2 — undefined `intentDigest`** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-943]] defines one NUL-domain-separated digest of the proof-free offer-core digest, requires it in the first object, and makes the authenticated core the only display source. |
| **M-1 — same-device restore strands stale policy** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-944]] defines a crash-recoverable installation-lifecycle purge before root deletion/restoration. |
| **M-2 — origin-ledger loss bricks v2 forever** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-944]] rebuilds the non-authoritative throttle empty under lock, records a bounded audit event, and never ignores a valid ledger. |
| **M-3 — CON-209 changed without amendment** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-945]] adds CON-209 to amendment authority, pins the exact seven-member v2 hint, and replaces PROTO-003 `binding_hash` with credential/v2 transcript authentication. |
| **M-4 — CON-226 corpus omitted** | [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-956]] makes the amended corpus, completeness check, two-stack result, and changelog SHA-256 a release gate and implementation slice. |
| **M-5 — no safe deployment/rollback order** | [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-957]], [[spec-008-0.4.5-standalone-tofu-first-contact#CON-947]], and [[spec-008-0.4.5-standalone-tofu-first-contact#CON-949]] use a distinct v2 ApplicationId, additive side tables, feature gates, drain rules, and an explicit forward-fix boundary for did-crdt. SPEC-007 REQ-810 is dispositioned. |
| **M-6 — ceremony could lift an operator withdrawal** | [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-958]] removes ceremony reinstatement. A tombstone requires the existing operator-only `reinstate/2` repair before a new ceremony. |
| **L-1 — table/module and handle/account confusion** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-946]] names the actual `cbcl-pairaccount-grant` table and the opaque provider account key, and uses new stable-ID side indexes without changing legacy tuple schemas. |
| **L-2 — repeated offer prepare undefined** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-948]] makes byte-identical retry idempotent and a distinct retry terminal. |
| **L-3 — migration attributed to wrong contract** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-946]] attributes migration to REQ-957 and CON-946 only. |
| **L-4 — reset revision and equal-slot edge undefined** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-944]] resets by verified deletion, defines the one-slot crash state, and chooses slot A deterministically on a valid equal-revision tie. |
| **L-5 — idle budgets exceed 600 seconds** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-948]] replaces composable idle budgets with feasible cumulative checkpoints ending at 590 seconds. |
| **L-6 — store-v2 CDDL references undefined rules** | cbcl-pairing SPEC-001 0.4.0 CON-016 defines `store-status` and `store-sequence` completely. |
| **L-7 — size explanation omitted a three-byte length pair** | [[spec-008-0.4.5-standalone-tofu-first-contact#NFR-907]] corrects the enumeration while retaining every reviewed total. |
| **L-8 — allocation time ambiguous** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-948]] anchors `t0` to the relay's committed `allocated-v2` response and binds its `expiresAt` into the carrier. |
| **L-9 — parent REQ-903/904 and REQ-810 absent** | [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-954]] records REQ-903/904; [[spec-008-0.4.5-standalone-tofu-first-contact#REQ-957]] records REQ-810. |
| **L-10 — two inconsistent domains** | [[spec-008-0.4.5-standalone-tofu-first-contact#CON-943]] uses a generic canonical-CBOR account-principal preimage and a NUL-terminated presence-code domain. |

The review also noted two non-finding implementation edges. CON-944 bounds the
zeroizing policy-key lifetime across a new-pair prompt. REQ-960 distinguishes
the three successful-path custody operations from a possible fourth,
separately authorized post-payload compensation signing operation.

## Requirements

### REQ-954 — The coordinated cbcl-pairing endpoint amendment is authoritative

This requirement replaces the first paragraph of 0.4.4 REQ-948 and supplements
its inherited-stop table. `anuna.io/credential/v2` SHALL conform to cbcl-pairing
SPEC-001 0.4.0 at `47424b7`, including its typed carrier selection, exact CPace
inputs, v2 CI/AD/public-context domains, distinct HKDF/Finished labels, sealed
AAD version, fixed application envelope, `intentDigest`, role-projected CBCL
dialects, shared/profile authority split, store grammar correction, asset
manifest, and independent vectors. The companion and this consumer are one
review unit; neither can PASS or be implemented independently.

SPEC-008 REQ-903 is retained: the wallet has live approve/decline and the
two-stage credential/v2 decision sequence remains under the amended SPEC-007
REQ-804 ordering. SPEC-008 REQ-904 is retained for the machine carrier: scan and
paste decode to the same recognized carrier type. Its scope does not include the
separate PAIR1 field, whose no-paste rule is an intentional co-presence
constraint. SPEC-007 REQ-806/807 still prohibit negotiation and fallback.

### REQ-955 — Device-local safety state has an explicit identity lifecycle

This requirement replaces the reset, ledger-failure, and recovery paragraphs of
0.4.4 CON-935. Relay policy and the origin-attempt ledger are device-local
installation safety state. Wallet forget, new-root creation, and recovery
restore SHALL execute CON-944's installation-lifecycle purge. A restored or new
root therefore sees both policy slots absent and a fresh origin ledger; stale
ciphertext under the prior wrapping root can never become the new root's
permanent refusal state.

An unreadable or keyless origin ledger SHALL be rebuilt empty under the
installation lock, with one bounded local `OriginLedgerRebuilt` audit event, and
the current attempt may then apply the ordinary unseen-origin prompt and limit.
No passcode or custody root is requested because the ledger is not authority.
A completely valid ledger is never reset, skipped, or replaced merely because
its counter reached the limit. Corruption can at worst restore five prompted
unseen origins in the next rolling window; it cannot accept an origin, relay, or
application pair.

### REQ-956 — Amended evidence, provider hint, and CON-226 corpus move together

This requirement supplements 0.4.4 REQ-945 and CON-939 and explicitly amends
SPEC-004 CON-209 and CON-226. The v2 signed evidence statement keeps every
0.4.4 field and equality check. Its provider hint SHALL be exactly CON-945 and
SHALL be recognized before authenticated display. No version-1 hint parser may
accept it by ignoring added members.

The normative `test-vectors/spec-004-v1.json` corpus SHALL remain one corpus and
gain credential/v2 cases for the version-2 profile dispatcher, CON-209 hint,
CON-214 statement and error tokens, CON-219 offer/core equality, CON-220 live
profile branch, CON-206 amended step 9 and every other changed numbered step,
CON-221 applicability states, and each added or changed closed rejection token.
The CON-226 completeness rule remains literal: a missing token or changed step
case is a gate failure. Two independent stacks SHALL agree. The RFC-8785 corpus
SHA-256 SHALL be recorded in the eventual implementation changelog; a draft
cannot invent that future hash.

### REQ-957 — Profile publication, hub migration, and rollback are additive

This requirement replaces 0.4.4 CON-933's instruction to re-mint the existing
live ApplicationId and replaces REQ-947's legacy-table annotation transaction.
The existing compatibility profile at
`https://chat.anuna.io/selfsame/application` SHALL remain byte-identical during
credential/v2 rollout and is not claimed to satisfy the new v1/v2 dispatcher.
Credential/v2 SHALL publish its conforming version-2 profile at the distinct
canonical ApplicationId
`https://chat.anuna.io/selfsame/application/v2`. The carrier, live fetch,
embedded browser bytes, permissions, offer, policy pair, and vectors all name
that exact v2 identifier. No content negotiation serves different profile bytes
for one ApplicationId.

The hub SHALL add CON-946 side tables and indexes without transforming any
legacy tuple shape or primary key. Mnesia schema creation is an idempotent
startup phase. Per-account stable-ID allocation and side-index population are a
separate ordinary Mnesia data transaction; the specification no longer claims
that schema changes and data writes share one transaction. A failed account
migration leaves all legacy rows byte-identical and marks the account
v2-ineligible.

SPEC-007 REQ-810 is amended only for this additive release. Rollback follows
CON-947 and CON-949: disable allocation, prevent new offer preparation, drain or
terminally close live ceremonies, compensate released grants, and only then
change binaries or profile routes. Finalized grants and additive stable-ID rows
remain valid data. No rollback reinterprets store-v2 or returns to a destructive
did-crdt create handler.

### REQ-958 — Withdrawal tombstones remain operator-controlled

This requirement replaces the same-account reinstatement paragraph of 0.4.4
CON-941. A v2 ceremony SHALL NOT lift, delete, replace, or downgrade any
`cbcl-webfinger-withdrawn` tombstone. If the recomputed issuer URI is tombstoned,
`selfsamepairaccept` returns the distinct terminal
`AliasWithdrawnRequiresOperatorReinstatement` before creating pending state.

The existing non-HTTP operator function `reinstate/2` is the only repair. It
names the exact account and DID, lifts the mistaken withdrawal block, and does
not recreate a binding. After that independently authorized operator action, a
new ceremony may provision normally. A tombstone for another URI remains, and a
different-DID attempt still follows CON-221/account-authority conflict rules.
This makes legitimate relink repairable without granting the constrained party
the revocation authority aimed at it.

### REQ-959 — Allocation, offer idempotency, and one achievable deadline agree

This requirement replaces the deadline table in 0.4.4 REQ-951 and clarifies the
first two phases of CON-939. The browser SHALL complete the adopted relay
`allocate-v2` first. The relay's committed success and returned `expiresAt`
define `t0`; the exact expiry is a mandatory CON-934/companion CON-013 carrier
field. The browser then submits the complete carrier to
`selfsamepairallocate`. If the hub allocation fails, the browser closes the
mailbox and displays no carrier. Hub allocation is not a second definition of
`t0`.

All successful application progress SHALL meet CON-948's cumulative
checkpoints, each strictly before the relay's original expiry. A claimant also
caps its local ceremony at 600 monotonic seconds from first carrier recognition;
the earlier relay close, signed checkpoint, or local cap wins. An attacker may
shorten a public carrier's apparent expiry before CPace but cannot extend the
relay record, and the authenticated hub offer later requires byte equality.

`selfsamepairofferprepare` is once-per-ceremony. A byte-identical retry of
`(ceremonyId, transcriptHash)` returns the held core, digest, `issuedAt`, and
`expiresAt` byte-for-byte. A second distinct transcript, session, or argument
is terminal, burns offer preparation, and cannot restart the 120-second window.

### REQ-960 — Domain separation and custody-operation accounting are exact

The account-principal and PAIR1 checksum preimages are CON-943. The successful
path has exactly three independent custody openings: policy access, issuer
preview, and final issuance commit. The policy key may remain only inside one
zeroizing policy-access closure while the new-pair prompt is visible, for at
most 120 monotonic seconds; socket open, cancel, timeout, backgrounding, or any
error ends the closure and zeroizes it.

Post-payload CON-210 compensation may require a fourth, separately explained
passcode/presence operation because it signs a revocation with the home key. A
failure or refusal to authorize that operation leaves the compensation ledger
`pending`, reports `not linked`, disables the application connection when
possible, and never reuses the successful-path issuance key or claims automatic
revocation.

### REQ-961 — Resolver acknowledgement means durable causal state

This requirement strengthens 0.4.4 REQ-950 against the reviewed production
service. A resolver declared for credential/v2 SHALL run with configured durable
storage. `POST /dids` and `POST /dids/:did/deltas` SHALL acknowledge success
only after the complete resulting document snapshot is durably committed. A
save failure leaves the in-memory document byte-identical to its prior state and
returns a closed service failure; it is not a best-effort metric-only success.

When durable storage is configured, failure to enumerate or decode the complete
snapshot set at startup SHALL fail startup rather than serve an empty store. The
wallet's immediate resolve-and-verify step remains required but no longer stands
in for durability. A profile generator and production gate SHALL refuse to
declare a resolver whose health evidence does not report durable storage ready.

### NFR-907 — The fixed-class arithmetic explanation includes every CBOR head

This requirement corrects one sentence in 0.4.4 NFR-906. The reviewed totals
remain unchanged. A large object total comprises one map byte, two two-byte uint
pairs, one 35-byte digest pair, a **two-, three-, or four-byte** key-plus-value
`logicalLength` pair, and one key plus the three- or five-byte byte-string head
plus the fixed body. Thus logical length 1 encodes to 64,558, lengths 24 through
255 encode to 64,559, and the large maximum encodes to 64,560. All outer totals,
including the 66,696-octet large relay body, remain as 0.4.4 states.

## Contracts

### CON-943 — Exact intent, account-principal, and presence-code digests

For the exact proof-free `OfferCoreV2` from 0.4.4 CON-939:

```text
offerCoreDigest = SHA-256(RFC8785(OfferCoreV2))
offerDigest = BASE64URL-NO-PAD(offerCoreDigest)
intentDigest = SHA-256(
  UTF8("selfsame credential/v2 intent\u0000") || offerCoreDigest
)
```

The NUL terminates the literal domain and the digest input is raw 32-octet
bytes. The credential/v2 offer application object's integer field 2 contains
raw `intentDigest`; every successor carries the same bytes. The offer body
contains the complete finalized offer whose core recomputes the digest. Before
display, CON-936 recomputes all three values, verifies the complete offer and
evidence, and constructs `AuthenticatedIntent` only from typed fields in that
verified core/evidence. Display does not consume a peer-provided application ID
or separate intent record.

The stable account-principal digest is deployment-generic:

```text
accountPrincipalDigest = SHA-256(DETERMINISTIC-CBOR([
  "selfsame-application-account-principal/v1",
  applicationId,
  applicationAccountId
]))
```

`applicationId` is the exact canonical text and `applicationAccountId` is raw
32-octet bytes. The PAIR1 checksum is the first two octets of:

```text
SHA-256(UTF8("selfsame credential/v2 presence code\u0000") || C || T)
```

where `C` and `T` are raw sixteen-octet values. These formulas replace the two
inconsistent 0.4.4 domains.

### CON-944 — Two-slot policy, lifecycle purge, and repairable origin ledger

The 0.4.4 two-slot format and highest-valid-revision rule remain. When both
slots are valid at equal revision with identical canonical plaintext, slot A is
the deterministic current slot and the next ordinary update overwrites slot B.
When no slot is valid, gated `reset relay policy` acquires the installation
lock, deletes slot A and verifies absence, deletes slot B and verifies absence,
then returns the ordinary both-absent empty state after a valid root opening. A
crash after one deletion leaves the other old valid slot authoritative; retry
completes reset. Reset never invents an unreadable `next revision`.

The fixed keychain marker `selfsame-installation-lifecycle-v1` has closed states
`purging` and absent. Forget, restore, and new-root creation acquire the same
installation lock, write and read-verify `purging`, delete and verify both
policy slots, delete and verify the origin-ledger record and installation key,
then delete the old `SealedRoot` last. After verifying all old state absent, the
operation deletes and verifies absence of the marker; only then may it write a
new/restored root. Startup seeing `purging` completes the deletions and marker
removal before opening or creating a root. A crash before root deletion leaves
the old root with empty safety state; a crash after root deletion leaves no root
and safely retries, never a new root under a stale `purging` marker.

The origin ledger uses its own exclusive installation lock and two objects: a
random keychain key and an authenticated record. If both open, the valid record
is always applied. If either is missing or the record fails complete grammar,
canonicalization, nonce, or tag verification, the wallet creates a fresh random
key, writes/read-verifies an authenticated empty ledger, emits a bounded local
`OriginLedgerRebuilt` event containing reason token and time but no origin, and
then processes the current origin as unseen. Failure to commit the rebuilt
ledger refuses before network I/O. There is no passcode/custody call edge.

### CON-945 — Credential/v2 evidence, provider hint, and corpus profile

The credential/v2 provider hint has exactly these seven required members and no
others:

```text
applicationId
profileVersion = 2
providerId
descriptorDigest
offerDigest
carrierDigest
transcriptHash
```

The four digest fields use canonical base64url-no-pad; the first three decode to
32 octets and `transcriptHash` to 64. `providerId` is the selected descriptor's
exact `operatorId`. `offerDigest` is CON-943's proof-free core digest. The hint
contains no account scope, account label, device proof, or caller display text.

For v2, the parent CON-209 PROTO-003 `binding_hash` sentence is replaced. The
hint is confidential and authenticated inside the post-Finished credential/v2
AEAD channel whose AAD binds `TH`; its carrier, profile, descriptor, core, and
transcript values are exact-equality checked against the locally recognized
carrier/profile and the hub-signed CON-214 v2 statement before display. The
statement carries 0.4.4's added account-principal, account-display, carrier,
transcript, and device-proof members. The v1 five-member hint and PROTO-003
authentication remain byte-for-byte unchanged for v1.

The CON-226 corpus file keeps its current top-level corpus profile and gains an
explicit `credential_v2` group. Its cases include every changed closed grammar,
error token, and numbered-step disposition in REQ-956. The same corpus remains
normative and cannot be rewritten to agree with implementation output.

### CON-946 — Stable account side tables and authority transactions

No legacy Mnesia table changes record arity or primary key:

| Actual table | Existing key/meaning retained | Additive v2 side index |
|---|---|---|
| `cbcl-webfinger-alias` | full normalized alias URI | `cbcl-selfsame-alias-index`: stable account ID to alias URI and revision |
| `cbcl-webfinger-account` | opaque provider-side account identifier, not asserted to be a handle | `cbcl-selfsame-account-index`: stable account ID to provider account key and revision |
| `cbcl-webfinger-withdrawn` | alias URI; existing secondary opaque-account index unchanged | `cbcl-selfsame-withdrawal-index`: stable account ID to URI/revision generations |
| `cbcl-pairaccount-grant` | composite `(Handle, DeviceKey)` | `cbcl-selfsame-pairaccount-index`: stable account ID, ceremony, legacy key, and revision |

New `cbcl-selfsame-account`, `cbcl-selfsame-scope`,
`cbcl-selfsame-ceremony`, `cbcl-selfsame-device`, and
`cbcl-selfsame-compensation` tables use stable account ID plus their natural
component. Table creation/index addition is idempotent schema setup before the
v2 feature gate. REQ-957's per-account migration populates only new tables and
side indexes in one data transaction, referring to legacy keys/revisions rather
than annotating legacy tuples.

`selfsamepairaccept` and final `pairaccountgrant` may update legacy data rows and
their side-index rows inside one ordinary Mnesia transaction after every schema
object exists. The pending transaction captures exact old legacy and side-index
rows. Rollback restores them byte-for-byte or stays pending on a revision
conflict. A tombstone causes REQ-958's terminal before pending creation. No v2
command calls `provision/3` or `reinstate/2`.

### CON-947 — Dual profile publication and hub release switch

The cbcl_chat generator produces two named immutable outputs rather than
mutating one:

- the existing compatibility bytes for
  `https://chat.anuna.io/selfsame/application`, served unchanged throughout the
  rollout; and
- one conforming version-2 output for
  `https://chat.anuna.io/selfsame/application/v2`, used only by credential/v2.

The v2 route, browser embedding, wallet vector, hub recognizer, and signed offer
must share one generated byte string and digest. The allocator gate defaults
off independently of serving the v2 profile. Release order is: ship additive
cbcl-pairing and Selfsame recognition with allocation off; ship did-crdt safe
create/CORS; ship hub side tables and v2 profile route with allocation off; run
corpus, migration, rollback, and staging E2E; only a later owner-approved
production gate may enable allocation.

Hub rollback is a feature-gate rollback, not a schema rollback. Disable v2,
drain CON-948 ceremonies, compensate released grants, verify no pending
ceremony, retain finalized grants and all additive tables, then an older hub
binary may run only if it ignores extra tables and reads unchanged legacy tuple
shapes. The legacy profile never moved, so no profile reversal is needed. The
v2 route may be withdrawn only after allocation is disabled and its live
carriers have expired.

### CON-948 — Offer preparation and cumulative ceremony checkpoints

`t0` is the allocator's monotonic observation of the relay's durably committed
`allocated-v2` success. The relay's returned `expiresAt` and original 600-second
TTL are recorded without repair in the public carrier and hub pending ceremony.
All endpoint progress must occur before both that expiry and these cumulative
deadlines:

| Required milestone | Deadline from `t0` |
|---|---:|
| authenticated claimant installed | 180 s |
| both Finished values verified | 210 s |
| offer prepare/finalize, delivery, authorization, request consumption | 240 s |
| preliminary intent decision | 330 s |
| preview and preparation delivered | 375 s |
| authority comparison/binding result delivered | 435 s |
| final release decision | 510 s |
| issuance/publication and reverse payload delivered | 555 s |
| browser acceptance, durable hub acknowledgement, and receipt | 590 s |

The reducer uses the earliest applicable checkpoint and expiry. This is one
feasible successful envelope; state budgets are not independently composable to
more than 600 seconds. Offer timestamps still begin only after Finished. The
unchanged 120-second evidence window is evaluated once before display as 0.4.4
states.

Offer prepare is a compare-and-set on an empty held core. The first valid
`(ceremonyId, transcriptHash)` constructs and persists one core and timestamps.
An exact retry reads and returns those bytes. Any changed transcript, session
account, ceremony input, or second core request records terminal
`OfferPrepareConflict`, closes pending preparation, and cannot mint new
timestamps or evidence.

### CON-949 — Cross-service rollback boundary

The cbcl-pairing relay rollback is exactly the coordinated companion's
disable/close-or-expire/verify-empty/install sequence. The hub rollback is
CON-947. did-crdt's idempotent non-destructive `POST /dids` and public read-only
CORS are additive service behavior, but once any credential/v2 issuer has been
created the deployment SHALL NOT roll back to a binary whose repeat-create path
can replace a DID DAG. Disable v2 allocation and forward-fix that service. CORS
may be disabled as an availability rollback only after v2 allocation is off;
doing so never changes DID state.

Every outward-facing step records the prior Fly release number and feature-gate
state before deployment. This document does not authorize a deploy. cbcl-bus is
single-machine `immediate`; did-crdt has no assumed canary. Separate owner
confirmation remains mandatory before either production deployment.

### CON-950 — Durable did-crdt write boundary

The production did-crdt `AppState` SHALL expose a keyed asynchronous mutation
lock per DID and a fallible durable-save adapter. Under that lock a write handler
clones the prior document, applies creation or one verified delta to the clone,
persists and read-verifies the complete candidate snapshot, and only then swaps
the in-memory document and emits success. A failed validation or save changes
neither copy. A crash after durable save but before the memory swap recovers the
candidate at startup; a crash before durable save recovers the prior snapshot.

Idempotent create under the same lock returns the existing document only when
its genesis public key/content hash matches; it does not rewrite the snapshot.
Announcement and DHT publication occur only after durable commit and memory
swap. With `STORAGE_PATH` configured, open failure, `load_all` failure, corrupt
snapshot, incomplete enumeration, or read-back mismatch fails startup. The
production health response distinguishes `durable-ready` from memory-only; the
credential/v2 generator admits only the former.

## Decisions

### ADR-928 — The pairing library owns a versioned endpoint, not only a relay

Adopt cbcl-pairing SPEC-001 0.4.0 so the protocol's context, key schedule,
dialect, and reducer are reviewed and reusable library contracts. Selfsame owns
account and grant semantics behind the typed profile adapter.

### ADR-929 — Credential/v2 uses a distinct ApplicationId

A profile is an immutable application identity. Serving version 2 at a new
canonical ApplicationId avoids content negotiation and lets the current live
compatibility artifact remain unchanged during rollout and rollback. The person
still sees the same authenticated chat.anuna.io origin; exact-pair TOFU records
the new canonical v2 application ID.

### ADR-930 — Device-local policy is purged before root replacement

Recovery intentionally does not restore relay policy. A durable purge marker
and delete-old-root-last order make that property true across same-device crash
and restore paths instead of relying on unreadable stale ciphertext.

### ADR-931 — Pairing never has withdrawal-reinstatement authority

A withdrawal is an operator safety lever. Automatic same-account reinstatement
would let the constrained party undo it. The existing operator-only repair is
retained even though it costs a support interaction.

### ADR-932 — Stable authority uses additive side indexes

Changing legacy Mnesia record arity creates an unnecessary one-way binary
migration. Side indexes preserve every reviewed key/tuple while allowing stable
account authority and one-transaction data updates.

### ADR-933 — One cumulative schedule owns the 600-second ceremony

Absolute milestones make the end-to-end budget executable and prevent a list of
individually plausible idle timers from promising an impossible successful
path.

### ADR-934 — Production resolver writes are not best-effort

A grant whose issuer vanishes on resolver restart is not browser-verifiable
authority. Production acknowledgement therefore moves behind durable snapshot
commit, while announcement and DHT publication remain best-effort discovery
effects after that authority boundary.

## Test specification

All 0.4.4 TEST-1011 through TEST-1047 remain release-blocking except where the
replacement rows below change an expected outcome. Companion TEST-030 through
TEST-047 are also mandatory.

### TEST-1048 — Coordinated endpoint assets and vectors are complete

Run cbcl-pairing SPEC-001 0.4.0 TEST-038 through TEST-047 and adopted TEST-030
through TEST-037. Require exact manifest hashes, independent CPace math, no
cross-version recognition, and production allocation disabled.

### TEST-1049 — Intent digest is exact before truthful display

Two independent implementations reproduce CON-943's core, offer, and intent
digests. Mutate every core/evidence/provider field, digest encoding, first object
field 2, and successor reference. Every mutation emits zero display and zero
consent. Source/API tests prove the v2 display can be built only from the
authenticated core.

### TEST-1050 — Forget and same-device restore cannot inherit stale safety state

Inject a crash before/after marker write, each slot/ledger/key deletion, old-root
deletion, new-root write, and marker removal. Startup converges to either the old
root with empty safety state or the new root with empty safety state, never a
new root with stale/unopenable records. Recovery export contains no policy.

### TEST-1051 — Origin ledger rebuild is safe and bounded

Lose the installation key, corrupt each grammar/nonce/tag field, and race two
rebuilders. Exactly one authenticated empty ledger commits, one bounded audit
event is recorded, no custody prompt occurs, and the current origin is processed
as unseen. A valid rate-limited ledger cannot be reset through the rebuild path.

### TEST-1052 — Policy reset and equal revisions are deterministic

Create identical valid A/B slots at equal revision and require A selection plus
B overwrite on update. Crash after deleting either reset slot and require the
remaining valid old policy; retry deletes both and reaches empty only after root
opening. No unreadable revision is used.

### TEST-1053 — Provider-hint mutation and v1 isolation are complete

Round-trip the exact seven-member v2 hint. Independently remove, add, or mutate
every member and transcript/carrier/core/descriptor equality; refuse before
display. V1 accepts only its exact five-member hint and rejects every v2 added
member. No v2 path requests a PROTO-003 `binding_hash`.

### TEST-1054 — CON-226 corpus completeness remains a hard gate

Regenerate the one corpus with every REQ-956 group and closed token, record its
RFC-8785 SHA-256, run two independent stacks, and run the parent completeness
checker. Delete each required case in turn and require a gate failure.

### TEST-1055 — Migration uses side tables and preserves legacy bytes

Create each legacy row shape plus multiple tombstone generations. Run schema
setup and per-account migration with failure at each boundary. Every legacy
tuple remains byte-identical; new authority/side-index rows commit all-or-none.
An older hub fixture ignores extra tables and reads the unchanged legacy tuples.

### TEST-1056 — Withdrawal survives every ceremony by the withdrawn party

Withdraw a same-account same-DID binding, then attempt complete v2 ceremonies
with the old and rotated session/device keys. Each returns
`AliasWithdrawnRequiresOperatorReinstatement` before pending state and leaves the
tombstone byte-identical. Invoke the operator-only `reinstate/2` outside HTTP,
then prove a fresh ceremony can provision while unrelated tombstones remain.

### TEST-1057 — Offer prepare cannot refresh its own window

Repeat byte-identical prepare and require identical core/timestamps/digest.
Change transcript, session, ceremony, or any argument and require terminal
`OfferPrepareConflict`, no second signature, and no refreshed expiry.

### TEST-1058 — The cumulative schedule is achievable and enforced

Run one successful ceremony reaching milestones at 170, 200, 230, 320, 365,
425, 500, 545, and 585 seconds after `t0`. Then cross each checkpoint and relay
expiry independently by one tick and require the correct terminal. No offer
timestamp exists before Finished and later human steps do not re-evaluate its
already-passed 120-second authorization window.

### TEST-1059 — Allocation time and carrier expiry have one source

Require relay allocation before hub pending allocation and exact equality among
`allocated-v2.expiresAt`, carrier expiry, hub held expiry, and later signed
offer. Hub failure closes the relay mailbox and emits no carrier. A public expiry
extension cannot extend relay state or pass the signed offer equality check.

### TEST-1060 — Fixed-class arithmetic includes every integer-head boundary

Assert exact application-object sizes for logical lengths 1, 23, 24, 255, 256,
61,999, and 62,000, including 64,559 for 24 through 255, plus all unchanged
plaintext, ciphertext, relay-body, queue, and v1 witnesses.

### TEST-1061 — Dual profile rollout and rollback do not strand the legacy route

Serve the existing compatibility bytes and the distinct v2 bytes concurrently.
Old-client fixtures observe byte-identical legacy bytes; v2 roles accept only
the new ApplicationId and one shared generated digest. Disable allocation,
drain/compensate, withdraw the v2 route, and prove legacy service remains
unchanged. No content negotiation changes bytes at one identifier.

### TEST-1062 — Cross-service rollback rehearsal is executable

Rehearse relay drain/store-v2 removal, hub feature-gate rollback with additive
tables retained, profile-route withdrawal, and did-crdt forward-fix-only state
after a created issuer. Assert no unfinished ceremony survives a binary revert,
no finalized grant is reinterpreted, and no DID DAG can be reset.

### TEST-1063 — Successful and compensation custody operations are truthful

On mobile and desktop, record exactly three separate successful-path custody
operations with the platform split from 0.4.4. Hold the policy key only inside
its 120-second zeroizing prompt closure. After payload failure, require a fourth
separately labelled revocation authorization or a durable pending compensation;
never claim automatic revocation after denial.

### TEST-1064 — Resolver success survives every persistence boundary

Run create and each post-genesis delta with failure before candidate build,
during durable save, after save before memory swap, after swap before response,
and during read-back. Success always resolves the complete state after process
restart; failure leaves the prior state. Corrupt or fail `load_all` under
configured storage and require startup failure. Memory-only health is rejected
by the v2 profile generator and production gate.

## Cross-repository implementation boundary

The 0.4.4 slice table remains, with these corrections and additions:

| Repository | Added/replaced slice |
|---|---|
| `cbcl-pairing` | Complete SPEC-001 0.3.0 plus 0.4.0 schemas, context/channel version dispatch, v2 envelope/dialects/reducer/profile boundary, store grammar, manifest, and two-stack vectors. |
| `selfsame` pure core | CON-943 digests; seven-member v2 provider hint with v1 isolation; versioned profile dispatcher; amended CON-226 corpus and completeness gate. |
| `selfsame` Tauri wallet | CON-944 lifecycle marker/purge, deterministic policy reset, origin-ledger rebuild/audit, bounded policy-key prompt, and separately authorized compensation signing. |
| `cbcl-bus` / `cbcl_chat` | Distinct v2 profile route/generator; relay-first allocation; idempotent offer prepare; additive stable-account/side-index tables; operator-only tombstone behavior; feature gates and rollback rehearsal. |
| `did-crdt` | Idempotent create/CORS; keyed mutation serialization; acknowledge-after-durable-save and startup fail-closed; plus a release guard and runbook forbidding rollback to destructive create after first v2 issuer. |

## Gate and sequencing

1. Run a fresh-context Claude-driver Circus Tier-1 adversarial review of the
   exact 0.4.4 + 0.4.5 Selfsame set, cbcl-pairing 0.3.0 + 0.4.0 set, and all
   four code baselines. Its mandate is defect finding. It must explicitly
   re-audit every 0.4.4 finding and every inherited hard stop. Owner direction
   is necessary but cannot substitute for PASS.
2. Only after coordinated PASS, encode the design as an executable SPL Elephant
   theory. Every TEST-1011 through TEST-1064 and companion TEST-030 through
   TEST-047 becomes a named gate, including CON-226 completeness and rollback.
3. Commit failing schemas/vectors/pure-core tests before production code.
   Implement cbcl-pairing and pure Selfsame contracts before effectful shells.
4. Implement and validate device-local custody/lifecycle, resolver safety, hub
   additive schema, and rollback gates before browser/wallet release wiring.
5. Run unit, mutation, prohibited-action, work-bound, corpus, reproducible-WASM,
   Android, browser, restart, rollback, and local real-relay E2E gates.
6. Keep production allocation disabled. Obtain separate owner confirmation
   before any cbcl-bus or did-crdt production deploy. There is no canary and the
   prior Fly release number must be recorded first.

## Explicit open production gates

- Human cryptography review of credential/v2 CPace inputs, HKDF/Finished labels,
  AEAD separation, counters, and fixed padding remains open.
- Independent endpoint vectors must include independent CPace group math.
- The amended SPEC-004 CON-201/204/206/209/211/214/219/220/221/226,
  SPEC-007 including REQ-810, both cbcl-pairing amendments, cbcl-bus SPEC-075,
  and did-crdt service contract require their named human owners' approval and
  recorded conformance evidence.
- The operator availability exception or a second operator, privacy/security
  reviews, global operatorship residual, and all existing production gates
  remain open.

## Changelog

- **0.4.5-draft — 2026-08-23 — review candidate, not approved.** Closes the
  0.4.4 REJECT by adopting a complete versioned cbcl-pairing endpoint amendment;
  defining `intentDigest`; repairing policy/root and origin-ledger lifecycle;
  amending CON-209 and CON-226; replacing legacy Mnesia transforms with additive
  side indexes; publishing v2 at a distinct ApplicationId; removing ceremony
  tombstone reinstatement; defining idempotent offer preparation, feasible
  cumulative deadlines, exact domains and size enumeration; and specifying
  cross-service rollback; and moves resolver acknowledgement behind durable
  causal storage. No implementation or deployment authority is claimed.
