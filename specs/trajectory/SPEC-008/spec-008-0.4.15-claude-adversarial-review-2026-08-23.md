---
id: SPEC-008-review-0.4.15-claude-2026-08-23
title: SPEC-008 0.4.15 + cbcl-pairing 0.4.7 + cbcl-bus 0.16.8 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.15-authenticated-allocation-and-consent]]"
prior-review: "[[spec-008-0.4.14-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.15 + cbcl-pairing 0.4.7 + cbcl-bus 0.16.8 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Authentication and billing evidence

The driver removed the paid-API credential before this session started. I
recorded both facts at the start of the review.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo UNSET; fi
UNSET
$ env | grep -i anthropic
(no ANTHROPIC* variables in environment)
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "email": "hugo.oconnor@gmail.com",
  "orgName": "hugo.oconnor@gmail.com's Organization",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The session runs on Claude Max
subscription OAuth. I called no paid API and no external web service. Every
input came from the four local repositories and from the local OTP install.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `5878eb19aa03955544032b03dbf0ed9c502bcd1a` | candidate 0.4.15-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `83ac3d35c263a0a429524eade863ddbd3383862d` | candidate 0.4.7-draft |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | declared implementation baseline |
| cbcl-bus | `0ca5e3c792aa23b1546dd8548d30243b746df5ba` | candidate 0.16.8-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit exists and every declared baseline resolves. The pinned cbcl-rs
revision is the current `HEAD` of that repository. The local OTP install is
`28` with `mnesia-4.25.3`, which matches the versions the candidates cite.

I read the complete effective amendment chain each candidate adopts. I read
the full 0.4.14 review. I read source at the declared baselines rather than
accepting prose about it.

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | H-1 |
| MEDIUM | 4 | M-1 … M-4 |
| LOW | 5 | L-1 … L-5 |

Verdict: **REJECT**.

`H-1` is release-blocking on its own. `M-2` and `M-3` are separately
disqualifying under this review's mandate, because each makes a mandatory test
unsatisfiable against real code. `M-1` and `M-4` are evidence and effective-text
defects that a Tier-1 ledger cannot carry.

This amendment set is much stronger than 0.4.14. Every one of that review's
four HIGH findings is closed, and eleven of its fifteen findings are closed
outright. The room grammar, the arithmetic, the retry contract, the twelve
authority cycles, the gate restoration, and the installed-state semantics all
re-derive exactly against code. The rejection rests on one new load-bearing
carrier that no clause defines, and on three derivations that a faithful
generator cannot reproduce.

---

## Findings

### H-1 (HIGH, release-blocking) — the advancing signal is an unsigned, origin-less frame whose "exact correlated" property no clause defines, and no test forges or replays a success

**Repository owner: cbcl-bus (primary), selfsame (coordination).**

The whole fail-closed argument of this amendment set rests on one sentence.
`ADR-026` states it: *"the absence of correlated success is fail closed, while
an opaque diagnostic can only deny service."* `REQ-065` makes it operational:

> The browser waits for the exact correlated `selfsamepairallocated` success and
> maps every refusal, unrelated frame, timeout, or suppressed result to
> `credential-v2-allocation-refused`.

Selfsame `REQ-992` carries the same load: *"The browser advances solely on the
matching `selfsamepairallocated` success for its signed request."*

Three code facts break the premise.

First, the deployed hub authenticates no frame it sends. At the cbcl-bus
baseline, `apps/cbcl_chat/src/core/cbcl-core-wire.lfe:185` states the rule
outright:

```text
;; NO `:from`. Every hub-originated frame is unsigned and origin-less, and every
;; member-originated one carries `:from` because the hub requires it
```

`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:14-16` states the same
design from the transport side: *"Server->client frames are NOT enveloped (the
hub does not sign; TLS secures the channel; clients trust the hub's
attestation…)"*. The one server frame the file builds directly,
`conn-nonce-frame` at `:143-149`, uses `cbcl-core-frame:build-frame` with
`cbcl-core-wire:zero-sig`.

Second, no clause anywhere defines the correlation. I searched the complete
effective cbcl-bus chain: the words `correlated` and `correlation`, applied to
`selfsamepairallocated`, occur only inside 0.16.8 itself. The tokens
`requestId` and `ceremonyId` occur nowhere in the SPEC-053 chain. The closed
hub-minted kind set in adopted 0.16.2 lists `selfsamepairallocated` with no
members. So "the exact correlated success" and "the matching success for its
signed request" name a predicate with no field, no grammar, and no owner.

Third, the amendment removed the clause that recorded the gap. 0.16.7 `ADR-025`
stated a true fact: *"The deployed hub has no authenticated, correlated,
account-scoped control frame."* `REQ-064` item 6 replaces `ADR-025` with
`ADR-026`, which now depends on exactly the frame `ADR-025` said does not
exist. Nothing in the replacement creates it.

The consequence is asymmetric, and only half of it is safe. Suppression is
safe: a withheld success leaves the browser waiting, which denies service and
allocates nothing. Forgery and replay are not. The tests cover only the safe
half. `TEST-1125` requires *"Suppress and forge refusals and prove neither can
advance."* `TEST-084` requires *"Forge, suppress, and reorder refusal traffic"*
and then asserts, without a test, *"Only exact correlated success does."*
Neither ledger forges a success, replays a stale success, or swaps a success
between two concurrent ceremonies for one account.

That last case is the one correlation exists to stop. Two ceremonies for the
same account produce two hub-valid snapshots. Without a correlation member the
browser cannot tell them apart, so ceremony A displays and approves ceremony
B's room set. Both sets pass every later signature check, because both are
genuine hub output.

The downstream signature graph limits the damage but does not close the hole.
A forged transition fails later, because the hub signs CON-214 evidence over
`offerCoreDigest`, and adopted `CON-960` makes `PathAToB` *"valid only for the
exact signed legacy session and snapshot"*. A **genuine** transition from
another live ceremony carries no such contradiction.

**Minimum sound correction.** Name the exact correlation member that
`selfsamepairallocate` sends and `selfsamepairallocated` echoes. State that the
browser discards any `selfsamepairallocated` whose correlation member is
absent, unequal, or already consumed. State plainly that hub-originated frames
are unsigned, so the member is anti-confusion rather than authentication, and
that authentication of the plan rests on the later hub-signed evidence.
Extend `TEST-1125` and `TEST-084` to forge a success, replay a stale success,
and swap successes between two concurrent same-account ceremonies.

---

### M-1 (MEDIUM) — the block labelled "the complete production v1 comparison and substitution trace" omits both production evidence-version sites, and mislabels the recognizer as the builder

**Repository owner: selfsame (REQ-992) and cbcl-bus (REQ-065), identical block.**

The nine-line trace is a real improvement, and every line in it verifies. I
checked all nine at the candidate commit:

```text
selfsame-web-device/src/lib.rs:600       selfsame_app_identity::PROFILE_VERSION,
selfsame-web-device/src/lib.rs:1346      profile_version: 1,
selfsame-web-device/src/lib.rs:1360      profile_version: 1,
selfsame-web-device/src/lib.rs:1369      profile_version: core.profile_version,
selfsame-app-identity/src/enrollment.rs:452  profile_version: crate::PROFILE_VERSION,
selfsame-app-identity/src/profile.rs:385     if version != PROFILE_VERSION {
selfsame-app-identity/src/provider_hint.rs:115  if self.profile_version != crate::PROFILE_VERSION {
selfsame-app-identity/src/enrollment.rs:394  if payload.get("profileVersion")... != Some(crate::PROFILE_VERSION) {
selfsame-app-identity/src/enrollment.rs:552  || statement.profile_version != crate::PROFILE_VERSION
```

An independent sweep of every `PROFILE_VERSION` occurrence in `crates/`
produces eight hits. Two are the constant definitions at `profile.rs:60` and
`lib.rs:109`. The other six are all in the trace. Every `profile_version: 1`
literal outside the trace sits below `#[cfg(test)]` — `lib.rs:1664/1676/1685`
below `lib.rs:1521`, and `selfsame-beam/src/enrollment.rs:236` below its
`#[cfg(test)]` at `:199`. So the profile-version half of the inventory is
complete and correct.

The evidence-version half is missing entirely. `crates/selfsame-app-identity/src/enrollment.rs`
contains no `#[cfg(test)]` anywhere in its 706 lines, so every site in it is
production. Two of them compare and substitute a version-1 constant:

| Site | Code | Surface |
|---|---|---|
| `enrollment.rs:373` | `if payload.get("evidenceVersion").and_then(Json::as_i64) != Some(1)` | **comparison**, in `recognise_payload` (declared `:360`) |
| `enrollment.rs:662` | `("evidenceVersion", Json::int(1))` | **substitution**, in `pub fn build` (declared `:660`) |

This is load-bearing rather than pedantic. cbcl-bus `REQ-065` opens by stating
that credential/v2 retains *"profile version 2, evidence version 2"*. Evidence
version is therefore a v1/v2 discriminant of exactly the same standing as
profile version, and both of its production sites are outside a block whose own
first word is "complete". `:373` sits twenty-one lines above `:394`, which the
trace does name, inside the same function.

The mislabel compounds it. The trace calls `enrollment.rs:452`
*"statement-builder substitution"*. `:452` is inside `recognise_payload`, the
recognizer. The real statement builder is `pub fn build` at `:660`, and its
first emitted member is the unnamed `evidenceVersion: 1`. GATE-04 box 12 asks
that *"both builders"* pass. One of the two builders it names is not a builder,
and the builder it misses is the one that hardcodes the version.

What is not wrong: the normative rule is universal — *"Credential/v2 SHALL NOT
reach any production v1 comparison or substitution site, including one added
later"* — and `TEST-1126` requires *"every site found by a complete source
scan"*. A conforming implementation stays safe. The defect is that the
inventory labelled complete is not complete, one revision after the 0.4.14
review filed the identical class as `M-1`.

**Minimum sound correction.** Add `enrollment.rs:373` as an evidence-version
comparison and `enrollment.rs:662` as an evidence-version substitution to the
shared block. Relabel `:452` as a recognizer substitution. Extend the scan
obligation in `TEST-1126` and `TEST-086` to name every version constant, not
only `PROFILE_VERSION`.

---

### M-2 (MEDIUM, gate-blocking) — the migration graph asserts zero additional cycles and no upgrade, while both deployed single-table room grants perform an exact-key read-then-write upgrade

**Repository owner: selfsame (NFR-917) and cbcl-bus (NFR-015).**

The split into two graphs is the right correction, and most of it verifies.
`NFR-917` states that the finalizer's whole room locks are write locks. That is
exactly true at `apps/cbcl_chat/src/shell/cbcl-chat-account-roommember.lfe:139-140`:

```lfe
(mnesia:lock (tuple 'table 'cbcl-roommember) 'write)
(mnesia:lock (tuple 'table 'cbcl-account-roommember) 'write)
```

The stated table order matches the code order. The removal of the
`index_read`-then-exact-write upgrade from `revoke-handle-tx` (`:201-209`) is
correct and needed. The twelve authority cycles re-derive exactly, and I
re-derived them independently from the NFR-913 matrix and from `can_lock/4`.

One sentence is contradicted by code. `NFR-917` says:

> A single-table room grant takes only its exact record lock.

Both deployed single-table room grants take two locks on that record, in the
order that generates a cycle. `cbcl-chat-roommember.lfe:153-161`:

```lfe
(defun write-grant (room handle pk via head)
  (let* ((key   (pkey room handle))
         (prior (mnesia:read 'cbcl-roommember key))
    ...
    (mnesia:write 'cbcl-roommember ... 'write)))
```

`cbcl-chat-account-roommember.lfe:65-80` has the identical shape on
`cbcl-account-roommember`. `mnesia:read/2` takes a read lock on the exact
`{Tab,Key}`. `mnesia:write` then requests a write lock on that same object.

`mnesia_locker.erl:358-361` at OTP 28.5 makes the consequence exact:

```erlang
can_lock(Tid, write, Oid = {Tab, Key}, AlreadyQ) when Key /= ?ALL ->
    ObjLocks = ets:select(mnesia_held_locks, [{ {{Oid,  '_', '$1'}}, [], ['$1'] }]),
```

The wildcard `'_'` in the lock-mode position means **any** lock held on that
object by another transaction blocks the write. Two concurrent grants on one
`{room, handle}` key therefore each hold a read lock and each request a write
lock. That is a cycle, and wait-die kills the younger with `#cyclic{}`.

The cycle is not a projection of any of the twelve authority pairs, because
those cover `cbcl-device-grant`, `cbcl-member`, and `cbcl-pairaccount-grant`
only. So `NFR-917`'s two closing claims both fail against the trace it
mandates: *"It SHALL generate zero additional cycles"*, and *"A missing
operation, unstated mode, **upgrade**, opposite order, or additional cycle is
non-conforming."*

The callers are ordinary and live: `cbcl-chat-room.lfe:329-330` and `:343-344`
call both grant paths on the normal admission route.

The asymmetry is what makes this a defect rather than a wording slip. The same
paragraph identifies and removes one upgrade by name in `revoke-handle-tx`, so
the author enumerated upgrades and stopped at one. The two `write-grant`
upgrades have the identical shape in the same subsystem.

`TEST-1128` and `TEST-086` require a generator over *"every operation and exact
lock mode"* to produce *"no additional cycle, upgrade, or omitted operation"*.
A faithful generator over this code produces both. The test cannot pass, and
the GATE-04 room-graph box cannot close.

A related operation is also unscoped. `cbcl-chat-account-roommember.lfe:53-63`
`redeem-and-grant` joins an invite-table read to an `cbcl-account-roommember`
write inside one transaction. It touches two tables and one room table, so it
falls outside both the "both room tables" rule and the finalizer-tail rule.

**Minimum sound correction.** Require both `write-grant` paths to take the write
lock on first read, through `mnesia:read/3` with `write`, and state that
removal as explicitly as the `revoke-handle-tx` removal. Alternatively, admit
the room-grant self-pair into the migration graph and delete "zero additional
cycles". Either way, name `redeem-and-grant` and place its invite table in the
generated total order.

---

### M-3 (MEDIUM, gate-blocking) — the declared credential/v2 maxima are never checked against the only implemented rendezvous, and the coordinated set pins a baseline that refuses them

**Repository owner: selfsame (CON-974 and the REQ-988 maxima), with PROTO-002
and did-crdt as the implementing owners.**

The shipped browser carries the sealed credential/v2 offer and the reverse
bundle through the rendezvous named by the application profile descriptor.
`apps/cbcl_chat/priv/web/app.js:2112-2126` at the cbcl-bus baseline:

```js
const slots = JSON.parse(wasm.mailbox_slots_json(secret));
const base = mailboxBase.replace(/\/+$/, '');
const put = await fetch(`${base}/rendezvous/${slots.offer}`, {
  method: 'PUT', ... body: wasm.seal_offer_bytes(secret, offer) });
...
const got = await fetch(`${base}/rendezvous/${slots.bundle}`);
```

Both implementations of that route cap the body at 4,096 octets, and both
refuse above it before storing anything:

- selfsame `crates/selfsame-rendezvous/src/lib.rs:48` — `pub const MAX_SLOT_BYTES: usize = 4096;`
- did-crdt `src/service/rendezvous.rs:65` at the declared coordinated baseline — the same constant, enforced at `:158` with `StatusCode::BAD_REQUEST`

The maxima this amendment carries forward are far above that. `REQ-992` says
*"The exact maxima and residuals in [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]]
remain unchanged."* Those include:

```text
V2_MAX_MIGRATION_ROOMS_JCS_OCTETS = 33,793
V2_MAX_OFFER_CORE_OCTETS          = 52,000
V2_MAX_OFFER_PAYLOAD_OCTETS       = 57,000
V2_MAX_CREDENTIAL_MEMBER_OCTETS   = 62,000
```

The maximum credential member is about fifteen times the carrier's body cap.
Sealing adds AEAD overhead on top. A maximum 256-room migration plan cannot
reach the wallet through the only rendezvous either repository implements.

`PROTO-002` already owns this gap honestly, at `:1015-1016`, in its list of
reference-implementation non-conformances: *"its maximum body is 4,096 rather
than 69,632 bytes"*. `PROTO-002` `:392` and `:729` set the conforming figure at
69,632, above the 62,000-octet member. So the design is coherent once
`PROTO-002` closes, and that gate is entirely open.

The defect is the missing linkage, not a hidden impossibility. No clause in any
of the three candidates names the rendezvous, the body cap, or `PROTO-002`.
`CON-974` names did-crdt `1f409a4` as a coordinated review-set member without
stating that its rendezvous refuses the declared maxima. No GATE-04 box ties
credential/v2 bounds to a carrier, and no test transits a maximum object. The
arithmetic is proven constructible and never proven deliverable. `REQ-988`'s own
title — *"Every credential/v2 maximum is reachable from its grammar"* — is
about construction, and nothing extends it to transport.

**Minimum sound correction.** State in `CON-974` that the declared did-crdt
baseline does not yet meet `PROTO-002` `CON-002`, and that credential/v2 depends
on that gate. Add a GATE-04 box requiring the maximum credential/v2 object to
transit the declared rendezvous at the conforming body cap. Add a test that
performs that transit rather than only constructing the object.

---

### M-4 (MEDIUM) — the named handle-change terminal and its test obligation are lost from every effective ledger at once

**Repository owner: selfsame (REQ-993 and TEST-1127), cbcl-bus (REQ-066 and
TEST-085) in coordination.**

`account-device-handle-change-refused` was a named terminal across four
Selfsame amendments and three cbcl-bus amendments. Selfsame 0.4.14 `REQ-991`
carried it at `:123`, and 0.4.13 `REQ-989` at `:210`. cbcl-bus carried it at
0.16.5 `:128`, 0.16.6 `:151`, and 0.16.7 `:147`.

`CON-974` item 2 replaces `REQ-991` in full. `REQ-993` does not restate the
terminal, the property, or the zero-frame rule attached to it. A search of the
Selfsame trajectory finds the name in no effective clause.

The test obligation goes with it. 0.4.14 `TEST-1123` carried one sentence:
*"While installed, require local handle refusal and zero enrollment frames."*
`TEST-1127` replaces `TEST-1123` and carries no handle-change case. cbcl-bus
`TEST-085` replaces `TEST-081` and carries none either. No GATE-04 box among
the forty-one names it.

One sentence survives, in cbcl-bus `REQ-066`: *"While verified installation
exists, handle changes remain locally refused and send no enrollment frame."*
That keeps the property and drops the name, and it has no test behind it.

The hub side stays independent, so this is not an authority hole. The upgrade
guard is real at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`,
which aborts with `account-upgrade-required` when an account-bound handle
already holds a member row. The defect is a coordinated ledger that drops a
named obligation and its only test in one revision, inside a contract that
calls its ledgers exact, and beside a test that requires failure when any
obligation is removed.

**Minimum sound correction.** Restate `account-device-handle-change-refused` in
`REQ-993`, with the zero-enrollment-frame rule. Restore the handle-refusal case
to `TEST-1127` and add it to `TEST-085`.

---

### L-1 (LOW) — the Selfsame ledger names a cbcl-pairing test that cbcl-pairing replaces, and drops the guard sentence against it

**Repository owner: selfsame (CON-974).**

`CON-974` states: *"TEST-051 through TEST-058 also survive."* cbcl-pairing
`CON-024` states: *"TEST-058 replaces
[[spec-001-0.4.6-deployed-room-admission#TEST-057]]."* A replaced test cannot
also survive, so the two coordinated ledgers disagree about `TEST-057`.

The correct Selfsame range is `TEST-051` through `TEST-056`, plus `TEST-058`.

0.4.14 closed its ledger with *"No superseded portion or open-ended range
supplies authority."* `CON-974` drops that sentence. cbcl-bus `REQ-064` keeps
its own version, and `TEST-087` still rejects open-ended survivor ranges, so the
obligation survives on the cbcl-bus side only.

Substance is not lost: `TEST-058` covers everything `TEST-057` covered and adds
the bare-`@` negative, the slash-escaping fixed point, and the prohibition on
inventing a hub admission terminal. The ledger is wrong rather than the design.

**Minimum sound correction.** Write the cbcl-pairing survivor set as `TEST-030`
through `TEST-049`, `TEST-050` in part, `TEST-051` through `TEST-056`, and
`TEST-058`. Restore the superseded-portion guard sentence.

---

### L-2 (LOW) — REQ-993 sources the unlink definition from the clause CON-974 replaces in full

**Repository owner: selfsame (REQ-993 and CON-974).**

`CON-974` item 2 replaces `REQ-991` in full. `REQ-993` then states: *"The only
ordinary clear remains the `person-confirmed` `unlink Selfsame wallet` action from
[[spec-008-0.4.14-deployed-admission-and-local-unlink#REQ-991]]."*

A clause replaced in full supplies no effective text, so the pointer resolves to
a document and to no obligation. `REQ-993` restates two of the replaced clause's
properties, namely seed retention and unchanged hub state. It does not restate
the exact-acceptance screen, the zero-frame rule, or the atomic removal of the
grant and its projection. cbcl-bus `REQ-066` carries all three, so the
coordinated design keeps them.

**Minimum sound correction.** Restate the unlink action's full definition inside
`REQ-993`, and delete the pointer to the replaced clause.

---

### L-3 (LOW) — an over-length room satisfies both admission names in the same paragraph

**Repository owner: selfsame (REQ-992), cbcl-bus (REQ-065), same wording.**

`REQ-992` states two rules in adjacent sentences:

> A room is `MigrationRoomUnrepresentable` whenever its raw value fails
> `CanonicalRoomText`, including bare `@`, a non-symbol value, or any other
> grammar failure. Length, count, or aggregate overflow is
> `MigrationPlanTooLarge`.

`CanonicalRoomText = "@" 1*128( CbclSymbolChar )` bounds length, so a 130-octet
room fails the grammar and is also a length overflow. The two sentences assign
it two different names.

The names never reach the wire, because all negative results collapse to one
opaque refusal, so no security property depends on the answer. The tests do.
`TEST-1125` requires *"every one-over boundary"*, and `TEST-084` requires the
same in five lanes, with no deterministic expected cause for an over-length
room.

**Minimum sound correction.** Reserve `MigrationRoomUnrepresentable` for a room
that fails the alphabet or shape, and `MigrationPlanTooLarge` for any length,
count, or aggregate overflow, including a single over-length room.

---

### L-4 (LOW) — an ordinary profile edit presents to the person as an authority rotation

**Repository owner: selfsame (REQ-993) and cbcl-bus (REQ-066).**

`REQ-993` states: *"A newly authenticated profile with a different digest or
account authority is `credential-v2-application-authority-changed`"*, and
requires the person to compare the old and new profile and authority
fingerprints and then explicitly accept the change.

The profile digest changes whenever the application publishes any profile edit,
including a new relay descriptor or a rotated enrolment signer. Every such edit
raises the same person-facing comparison as a hostile `accountAuthority`
substitution, and in the common case the authority fingerprint is unchanged and
the profile fingerprint carries nothing a person evaluates.

Habituation is the exact mechanism that defeats the mitigation this clause
implements, which the 0.4.14 review filed as `L-2`.

**Minimum sound correction.** Split the two triggers. A digest change with an
unchanged `accountAuthority` re-pins silently after the grant re-verifies under
the same issuer. An `accountAuthority` change keeps the person-facing
acceptance.

---

### L-5 (LOW) — the two new browser state names have no stated status class

**Repository owner: cbcl-bus (REQ-065) and selfsame (REQ-992).**

`REQ-992` introduces `migration-plan-invalid` and `migration-display-state-changed`.
It states that a display difference `disables confirmation`, which places the
second name outside the terminal states. It states only that a grammar failure
*"is `migration-plan-invalid`, never `burned('invalid-grant-observation')`"*,
which fixes the reason string and leaves the status open.

That matters because of how the shipped reducer works. At
`apps/cbcl_chat/priv/web/path-b-ceremony.mjs`, `burned/1` returns
`CeremonyStatus.Burned`, and `reduce` returns state unchanged for `Burned`, so
every refusal on that path is terminal today. The 0.4.14 `H-1` finding was
about a refusal that was both misleading and unrecoverable. The candidate fixes
the misleading half and leaves the recoverable half unstated.

**Minimum sound correction.** State whether `migration-plan-invalid` burns the
ceremony or leaves it retryable, and state the same for
`migration-display-state-changed`.

---

## Disposition of every v0.4.14 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **H-1** — shipped `CHANNEL` regex unnamed and terminal | **CLOSED** | Both `REQ-992` and `REQ-065` now name `apps/cbcl_chat/priv/web/path-b-ceremony.mjs` and its `CHANNEL` expression as a current shipped, load-bearing admission gate, and require replacement by a `CanonicalRoomText` recognizer generated from one source with the wallet, hub, and cbcl-pairing. Both forbid `burned('invalid-grant-observation')` for a grammar failure. cbcl-pairing `ADR-022` replaces `ADR-021` and says *"a shipped browser regex"* rather than *"a future browser regex"*. GATE-04 gains a box requiring one generated grammar. I re-verified the regex at `:118`, `affectedChannels` at `:150-157`, and the terminal burn at `:192-195`. Residual filed as `L-5`. |
| **H-2** — three admission terminals, three owners, no carrier | **CLOSED for ownership; the carriage half re-opens as `H-1`** | All three candidates now make the hub the sole evaluator. cbcl-pairing `REQ-029` states *"Cbcl-pairing does not own, deliver, or infer"* them. `REQ-992` makes each *"a hub-local typed negative result, not a browser predicate and not a new chat performative"*. Refusal is the absence of success, and adopted `REQ-040` already places carrier allocation after the snapshot inside the hub. cbcl-bus `CON-030` adds *"Only exact success MAY lead to carrier allocation."* The term "browser-local terminal" is gone. What replaces it depends on a correlation no clause defines, which is `H-1`. |
| **H-3** — DOM-sourced consent list | **CLOSED** | Both candidates name `apps/cbcl_chat/priv/web/app.js:602-606` and `affectedChannelsNow()`, forbid `.clog[data-room]` values as the source of `CeremonyObservation.GrantVerified.affectedChannels`, require the surface to render exactly the authenticated `migrationRooms`, and define `migration-display-state-changed` for any difference. GATE-04 gains a matching box. I verified the seam is implementable: the reducer's closed action shape `['accountDid','affectedChannels','grantId','type']` accepts the plan's array unchanged, and the two DOM call sites at `:6409` and the default parameter at `:6443` both fall inside the universal prohibition. |
| **H-4** — `(error @room "not-issued")` named as a wire object | **CLOSED** | Both candidates now name `(error @room "bad-signature")`. The claim *"Six distinct hub authorization causes collapse to that one wire value"* verifies exactly: `apps/cbcl_chat/src/core/cbcl-core-enrol.lfe` `account-bound` at `:212-250` has exactly six reject clauses — `no-verified-key`, `key-selector-required`, `revoked`, `not-issued`, `no-standing`, `permission-undeclared`. The collapse is real at `cbcl-chat-session-ws.lfe:264` and `:2093-2097`, over `cbcl-core-wire:err/2` with `zero-sig` (`cbcl-core-wire.lfe:31-36`). The browser MAY display only cause-neutral `hub-frame-refused`. `TEST-1127` and `TEST-085` require every deployed and unknown slug instead of a genuine `not-issued`. |
| **M-1** — comparison sites omitted; GATE-04 box 12 weakened | **PARTLY CLOSED** | All four `PROFILE_VERSION` comparison sites are added and each verifies at its exact line. A fifth substitution site at `lib.rs:1369` is added. GATE-04 box 12 is restored to *"every comparison or substitution site"* and regains *"pass against pinned v1 bytes"*. Still open: both production `evidenceVersion` sites are missing, and `:452` is mislabelled. Filed as the new `M-1`. |
| **M-2** — contention set not closed over room tables | **PARTLY CLOSED** | The 0.4.14 review's option (b) is taken and strengthened. The twelve-cycle graph is explicitly scoped to three authority tables and *"makes no claim about room or finalizer-tail tables"*. A separate migration graph is defined with fixed table order. The finalizer's whole room locks are stated `write`, which matches `cbcl-chat-account-roommember.lfe:139-140` exactly, and its exact `cbcl-member` lock is stated `write`. The `revoke-handle-tx` upgrade is removed. A GATE-04 box is added. Still open: *"zero additional cycles"* is contradicted by both `write-grant` upgrades. Filed as the new `M-2`. |
| **M-3** — retry values admitting no retry | **CLOSED** | Both candidates now require *"explicit integer `Retries >= 2`"*, cite `mnesia_tm:try_again/1` retrying only above one, require transaction level 1, prohibit nesting, and map three abort shapes to `authority-busy`. I verified all four facts against the local OTP 28.5 `mnesia-4.25.3` source: `mnesia_tm.erl:929-931` for `try_again/1`, `:1013-1015` for `decr/1`, `:918-923` for the `level == 1` restart condition, and `:823` for `{aborted, nested_transaction}`. `return_abort/4` at `:1021-1057` returns `{aborted, nomore}` at level 1 and exits with the `#cyclic{}` record when nested, which matches the three named shapes. |
| **M-4** — GATE-04 dropped human acceptance of irreversible migration | **CLOSED** | The box reads *"human review accepts irreversible migration, exact room-list consent, and every handle, room, count, and aggregate admission exclusion"*. I diffed all boxes 0.16.7 to 0.16.8: the count moves 39 to 41, and every other change is a pointer update, a restoration, or a strengthening. |
| **M-5** — offline reload collapses into a recovery terminal | **CLOSED** | `credential-v2-verification-unavailable` is added as a non-terminal state that *"retains the complete record"*, sends no frame, claims no authority, and retries in session without a page reload. `credential-v2-recovery-required` is reserved for corruption, partial state, an invalid signature, or a wrong binding. The issuer-rotation outcome is stated through `credential-v2-application-authority-changed`. Residual filed as `L-4`. |
| **L-1** — "four named sites" and the excluded statement line | **CLOSED** | The count is gone. Nine sites are enumerated individually, and `lib.rs:1369` is cited on its own line. I verified `:1369` reads `profile_version: core.profile_version`. |
| **L-2** — record binds no profile digest | **CLOSED** | The installation record now contains *"the authenticated profile digest and exact `accountAuthority`"*, and reload clause 3 matches ApplicationId, digest, and authority. Residual filed as `L-4`. |
| **L-3** — bare `@` outside both triggers | **CLOSED** | `REQ-992` covers *"any other grammar failure"*, naming bare `@` explicitly. I verified the premise at the pinned cbcl-rs revision: `parse_symbol` requires only one consumed character, so `@` alone parses to a symbol. Residual overlap filed as `L-3`. |
| **L-4** — slash escaping unstated | **CLOSED** | *"The serializer SHALL NOT escape `/`"* appears in all three candidates, and a maximum-length `/`-bearing room is a mandatory generated fixed point in each test ledger. |
| **L-5** — re-grant after grant deletion unstated | **CLOSED** | `REQ-993` states that deletion creates no permanent device-key tombstone and that the same key takes a new grant only through a fresh ceremony. Both halves verify: `revoke-device-grant` at `cbcl-chat-account-grants.lfe:309-311` performs a plain exact-key delete and writes no tombstone, and `store-device-tx` at `:189-202` re-admits an absent key subject to `max-devices`. GATE-04 box 35 now requires human acceptance of *"fresh-ceremony same-key re-grant"*. |
| **L-6** — TEST-050 negative witness omitted | **CLOSED** | `CON-974` appends the sentence `That witness remains a required negative.` |

Eleven closed, two partly closed, and two closed with a residual filed at lower
severity. No 0.4.14 finding regressed.

---

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is exact application and relay scoped; a new application prompts | **HOLDS** | `CON-974` names five replacements, and `REQ-981` is not among them, so the distinct ApplicationId `https://chat.anuna.io/selfsame/application/v2` and the person-owned exact policy pair survive. cbcl-bus `REQ-065` restates the distinct ApplicationId in its first sentence. GATE-04 box 14 is unchanged. |
| **F-B** — approval displays the authenticated application and transition; every cross-check precedes display and approval | **HOLDS**, and the 0.4.14 failure is repaired | The browser room list now comes from the authenticated array rather than the DOM, which was the 0.4.14 breach. The wallet half is untouched and remains sound. Browser account identity stays a human comparison rather than a local verification, which is the design: `crates/selfsame-web-device/src/lib.rs:529-534` states that the application derives the fingerprint itself from the announced issuer *"so a substituted issuer produces a visibly different fingerprint"*, and adopted `CON-221` plus 0.4.2 `REQ-926` place the authoritative equality check on the application before CON-206. |
| **F-C** — no custody access, derivation, mint, signing, publication, alias, or grant precedes the applicable explicit consent | **HOLDS** | `CON-974` names nothing in `CON-962`, so its sixteen-step order stands. Every new clause is a grammar, admission, display, lock, or local-state rule. Each negative result *"emits no correlated `selfsamepairallocated` success, no carrier, no pairing room, no digest, and no pending partial object"*. The deployed ordering evidence is unchanged at `crates/selfsame-app-identity/src/enrollment.rs:291-300`, where the kid bound precedes every parse, clone, and canonicalisation. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS** | Untouched by 0.4.15. Adopted `REQ-970` and `CON-939` still mint the pending account identifier and scope before the public carrier. `REQ-065` preserves the exact 23-member core, and 0.4.12 `REQ-984` still excludes `accountDid` from it. |
| **F-E** — per-application consent is mitigation, not a relay-operatorship binding claim | **HOLDS** | Neither candidate claims a binding property for the relay. cbcl-pairing `CON-024` preserves the no-relay-field property. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at cbcl-pairing `src/cbcl_protocol.rs:34`, above the 57,000-octet offer and the 62,000-octet member. GATE-04 box 35 still requires human acceptance of relay-visible size correlation. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause reads *"Mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery"* at `specs/SPEC-007-cbcl-pairing-cutover.md:328-334`. Every negative path added or kept by 0.4.15 creates nothing. The three admission results allocate nothing. `credential-v2-allocation-refused`, `credential-v2-verification-unavailable`, and `hub-frame-refused` each send no frame and reconstruct no state. `credential-v2-application-authority-changed` mutates the record only inside one transaction that first re-verifies the grant. The one mutating local path is subtractive and person-initiated. `CON-974` restates the stop. `H-1` bears on which signal advances a ceremony, not on whether a failure creates an effect. |

---

## Repository assessments

### cbcl-pairing 0.4.7-draft (`83ac3d3`)

The strongest document in the set, and the only one I found no defect in.

`REQ-029` retracts the consumer-ownership claim that produced 0.4.14 `H-2`:
*"The hub owns the earlier durable-state admission decisions. Cbcl-pairing does
not own, deliver, or infer"* the three names. It keeps cbcl-pairing as a closed
independent recognizer before private typed-display construction, which is the
correct defence-in-depth posture.

The grammar verifies character for character against `is_symbol_char` at the
pinned cbcl-rs revision `febc669`. The alphabet is ASCII alphanumeric plus
`_ - . / ! ? + * < > = @`, and `CbclSymbolChar` lists exactly those. The
33,793-octet arithmetic re-derives: `256 * 131 = 33,536`, plus 255 commas, plus
2 brackets.

`ADR-022` corrects `ADR-021`'s factual error from *"a future browser regex"* to
*"a shipped browser regex"*. `NFR-021` keeps every authenticated byte ahead of
the owned allocation, and keeps the verifier verdict-only. `TEST-058` adds the
bare-`@` negative, the maximum `/`-bearing room, and a prohibition on inventing
a hub admission terminal.

`CON-024`'s preservation of the *"consumer-ownership property"* in `CON-022`
does not conflict with the retraction. `CON-022`'s second paragraph is about
enforcing the bounded type rather than deciding membership, which `REQ-029`
restates.

The typed display remains a design rather than code: `PathAToBDisplay` and
`CredentialV2Display` are absent from `src/`. The repository does carry the
real surfaces they attach to, at `src/profile.rs` with `DisplayField`,
`DisplayIntent`, `ProfileIntent`, and `CredentialProfile`, so the seam is
concrete.

### cbcl-bus 0.16.8-draft (`0ca5e3c`)

Carries the largest correction and the two heaviest defects.

Closed well: hub-sole admission evaluation, the authenticated consent source
naming `app.js:602-606`, the shipped regex naming `path-b-ceremony.mjs:118`,
the `bad-signature` collapse with a verified six-cause count, the
unavailability and rotation split, the tombstone-free re-grant rule, the
restored GATE-04 box 12 and irreversible-migration box, the split lock graphs,
and the level-1 retry contract.

GATE-04 holds exactly 41 boxes, which matches `TEST-087`'s *"all forty-one
GATE-04 boxes"*. The two additions are the room-graph box and the consent-surface
box. Every box remains open, and the amendment states that it closes none.

The parent pointer resolves: `specs/SPEC-053-key-root-identity.md:15` carries
`candidate-amendment` to `spec-053-0.16.8-authenticated-allocation-and-consent`,
and `DEC-012` is at `:635`.

Open against it: `H-1` on correlation, `M-2` on the room-grant upgrades, and
shares of `M-1`, `M-3`, `M-4`, `L-3`, `L-4`, and `L-5`.

The survivor set `TEST-043` through `TEST-065`, `TEST-068`, `TEST-070`, and
`TEST-084` through `TEST-087` agrees with the Selfsame ledger exactly, and
correctly excludes the replaced `TEST-080` through `TEST-083`.

### selfsame SPEC-008 0.4.15-draft (`5878eb1`)

Every wikilink resolves, and I checked each of the twelve against its target
file and heading. Artefact numbers are sequential: `TEST-1125` through
`TEST-1128` follow `TEST-1124` with no gap or reuse, and `REQ-992`, `REQ-993`,
`NFR-917`, `CON-974`, and `ADR-952` each advance their series by one.

The nine-line v1 trace verifies line for line. The five pre-consent checks are
correctly ordered and each is concrete. The installed-state clause is the
clearest version yet, and its three-way split between invalid, unavailable, and
changed is right.

Open against it: `M-1`, `M-3`, `M-4`, `L-1`, `L-2`, `L-3`, `L-4`, and shares of
`H-1`, `M-2`, and `L-5`.

`NFR-917` scopes its inheritance precisely: it adopts the `NFR-913` *operation
matrix* rather than the whole clause, which correctly leaves 0.4.11's superseded
48,000-octet arithmetic behind. `REQ-992` separately pins the current maxima to
`REQ-988`.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged by this amendment set. I read the
service rather than accepting the pointer.

The rendezvous at `src/service/rendezvous.rs` implements `PUT` and
`GET /rendezvous/{slot}` with CORS, a 4,096-octet body cap at `:65`, a
600-second TTL at `:68`, a 65,536-slot ceiling at `:79`, amortised expiry, and a
diagnostic-free refusal. The bounds work in the head commit is sound and
matches its own commit message.

The 4,096-octet cap is the substance of `M-3`. `PROTO-002` already records it as
a known non-conformance and gates it, so this is a disclosed dependency rather
than a discovery. Nothing in the coordinated set states the dependency.

The working tree carries an uncommitted `specs/did-crdt-spec.md` modification
and two untracked paths. I assessed the declared commit and ignored the working
tree, which is the correct baseline.

---

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Refusal suppression.** A withheld or dropped refusal leaves the browser
  waiting, which denies service and allocates nothing. The design is correct on
  this half, and it is the half the tests cover.
- **Forged refusal.** A refusal's only permitted effect is denial, so forging
  one grants nothing. `REQ-992` states this and it holds.
- **Browser carrier substitution.** A hostile carrier address cannot redirect
  the ceremony, because `pinnedEndpoint` in `app.js:1934-1941` refuses any
  relay or mailbox origin that disagrees with the profile descriptor.
- **`EnrolmentAllocator::new_native` reachability.** The unreachability argument
  is structural rather than a denylist. The only JavaScript-exposed entry is the
  `#[wasm_bindgen(constructor)]` at `crates/selfsame-web-device/src/lib.rs:1417-1430`,
  and `CON-029` forbids the v2 browser module from importing the vendored WASM.
  The vendored bundle is real at `apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm`,
  so the import check is meaningful.
- **Twelve authority cycles.** I re-derived the set independently from the
  `NFR-913` matrix and `mnesia_locker:can_lock/4`, and reproduced all twelve
  with no extra pair over the three authority tables. `index_read` taking a
  whole-table read lock verifies through `ixrlock/5` at `mnesia_locker.erl:1086`,
  and `foldl` taking one through the `init_iteration` call at `mnesia.erl:1867-1868`,
  whose `lock/4` on `{table, Tab}` is at `mnesia.erl:1953-1954`.
- **Two grant creators and two deleters.** GATE-04's claim is exact.
  `store-device-tx` and `store-verified-device-grant` create;
  `revoke-observed-tx` and `revoke-device-grant` delete. `store-device-tx` takes
  the write lock on first read at `:190`, so it carries no upgrade.
- **Consent-surface seam.** The authenticated array reaches the display through
  the reducer's existing closed action shape and the painter at
  `app.js:652-670`, with no new transition needed.
- **Arithmetic.** Every figure re-derives: the room array at 33,793; the compact
  JWS at 250 plus 3,382 plus 86 plus 2, giving 3,720 against a 4,000 cap with a
  280 residual; and each base64url expansion.
- **Relay body cap against offer size.** cbcl-pairing's own mailbox is sized
  correctly at `MAX_FRAME_BODY_BYTES = 69_632` in `src/mailbox.rs:15`, above
  both the 57,000-octet offer and the 62,000-octet member. Only the rendezvous
  is undersized, which is `M-3`.

---

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns.

1. **`PROTO-002` `selfsame-rendezvous-v1` is entirely ungated.** Its Tier-1 gate
   has eight open boxes, including two independent implementations and a
   cross-model adversarial review. Credential/v2 transport depends on it, which
   `M-3` asks the coordinated set to state.
2. **The whole credential/v2 lane is planned rather than shipped.**
   `selfsamepairallocate`, `selfsamepairallocated`, and
   `credential-v2-canonical.mjs` exist in specifications only. That is honest
   planned work rather than a contradicted premise, and I graded it as such.
3. **All 41 GATE-04 boxes stay open,** including `GATE-01`, `GATE-03`, and
   `GATE-00`, the human security, privacy, and operational reviews, hub
   off-machine readiness, did-crdt durable readiness, and the separate owner
   approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration** as a later
   increment, and the parent specification records that deferral as in force.
5. **Browser account identity remains a human comparison.** `CON-221` and 0.4.2
   `REQ-926` own it, and the application performs the authoritative equality
   check. This amendment does not restate the obligation, which is acceptable
   because it adopts it.
6. **Relay-visible size correlation is unchanged and bounded,** and GATE-04
   still requires human acceptance of it.
7. **The 75 unary `mnesia:transaction` call sites** in `apps/` remain unbounded
   today. `NFR-917` mandates a change to bounded retries rather than describing
   current behaviour, and the change surfaces `authority-busy` on writes that
   previously blocked.

---

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state
it. Such a PASS authorizes only the Elephant SPL and the test-first
implementation plan. It authorizes no code, no public allocation, no gate
opening, no release, no deployment, and no production enablement. Every
GATE-04, `GATE-01`, `GATE-03`, and `GATE-00` box stays open. Every human
review, every durability obligation, and the separate owner approval for
production deployment also stay open and unaffected.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| H-1 | cbcl-bus, selfsame | Name the correlation member, state that hub frames are unsigned, and test a forged, stale, and cross-ceremony success. |
| M-1 | selfsame, cbcl-bus | Add `enrollment.rs:373` and `:662` as evidence-version sites, relabel `:452` as a recognizer, and widen the scan to every version constant. |
| M-2 | selfsame, cbcl-bus | Take the write lock on first read in both `write-grant` paths, or admit the room-grant self-pair; place `redeem-and-grant` in the total order. |
| M-3 | selfsame, PROTO-002 | State the `PROTO-002` dependency in `CON-974`, add a GATE-04 transit box, and test a maximum object through the declared rendezvous. |
| M-4 | selfsame, cbcl-bus | Restate `account-device-handle-change-refused` in `REQ-993` and restore its case to `TEST-1127` and `TEST-085`. |
| L-1 | selfsame | Write the cbcl-pairing survivor set without `TEST-057`, and restore the superseded-portion guard sentence. |
| L-2 | selfsame | Restate the unlink definition inside `REQ-993` and delete the pointer to the replaced clause. |
| L-3 | selfsame, cbcl-bus | Reserve one name per failure class, with length overflow going to `MigrationPlanTooLarge`. |
| L-4 | selfsame, cbcl-bus | Split the digest trigger from the authority trigger. |
| L-5 | cbcl-bus, selfsame | State the status class of both new browser names. |
