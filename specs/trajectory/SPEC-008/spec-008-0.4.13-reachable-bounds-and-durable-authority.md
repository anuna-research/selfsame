---
id: SPEC-008-amendment-0.4.13
title: SPEC-008 0.4.13-draft — Reachable bounds and durable browser authority
status: draft
version: 0.4.13-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.12-tagged-transition-closed-authority]] only where [[spec-008-0.4.13-reachable-bounds-and-durable-authority#CON-972]] says so"
adopts: "All effective 0.4.4 through 0.4.12 clauses except the exact replacements in CON-972"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.5-draft; cbcl-bus SPEC-053 0.16.6-draft"
implementation-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
review-input: "[[spec-008-0.4.12-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.13-draft — Reachable bounds and durable browser authority

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.12 review found no architectural
> failure. It found one unreachable maximum, two incomplete coverage rules, and
> five coordination or operational defects. This amendment corrects only those
> findings. It authorizes no implementation, allocation, release, or deployment
> before a fresh coordinated Tier-1 PASS.

The standalone architecture remains the target. Relay trust is person-owned and
scoped to the exact `(applicationId, relayOrigin)` pair. The authenticated
application constructs the typed display only after the verifier succeeds. Both
explicit migration consent screens remain mandatory. Wallet custody, derivation,
signing, publication, and grant issuance remain after the applicable consent.
Relay operatorship remains an accepted residual rather than a claimed binding.

## Requirements

### REQ-987 — V2 grammar and dispatch have constructible closed boundaries

The tagged `accountTransition` shapes in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] remain exact.
Their two human identifiers use these v2-only canonical text grammars:

```text
LegacyHandle      = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )
CanonicalRoomText = "@" ( ALPHA / DIGIT )
                    *127( ALPHA / DIGIT / "." / "_" / "-" )
```

Both are ASCII. `LegacyHandle` is therefore at most 33 UTF-8 and RFC-8785
string-content octets. `CanonicalRoomText` is at most 129 in both units. Room
values are unique and sorted by unsigned UTF-8 octets. The complete
`migrationRooms` array has at most 256 values.

The credential/v2 `deviceDid` uses the exact recognised Ed25519 `did:key` form.
It is `did:key:z` followed by base58btc of multicodec bytes `0xED 0x01` and a
32-octet public key. Every conforming value is exactly 56 ASCII octets.
`issuedAt` and `expiresAt` use the existing exact 20-octet UTC grammar.
No wider recognizer ceiling is described as a constructible maximum.

App identity performs closed version dispatch before member recognition. A v2
path SHALL NOT compare with or substitute any v1 constant or literal. The
closed v2 call graph contains distinct profile, provider-hint, offer, statement,
builder, recognizer, signer, and profile-facts surfaces. The profile-facts
surface includes the branch currently owned by
`selfsame-web-device::profile_facts`; it emits the version selected by the
recognized profile rather than substituting
`selfsame_app_identity::PROFILE_VERSION`.

The static boundary rejects a v2-reachable comparison or substitution of any of:

```text
crate::PROFILE_VERSION
profile::PROFILE_VERSION
selfsame_app_identity::PROFILE_VERSION
the integer literal 1 as a profile or statement version
```

Exact function names MAY change mechanically. The named semantic surfaces and
cross-crate trace SHALL remain complete. The live v1 profile-facts, profile,
hint, statement, builder, signer, bytes, and digest remain byte-identical.

### REQ-988 — Every credential/v2 maximum is reachable from its grammar

Every textual cap limits both UTF-8 content and RFC-8785 escaped string content.
A value satisfies both limits before cloning. The effective constants are:

```text
V2_MAX_APPLICATION_ID_OCTETS                 = 96
V2_MAX_RELAY_ORIGIN_OCTETS                  = 272
V2_MAX_REQUESTED_PERMISSIONS                = 4
V2_MAX_PERMISSION_OCTETS                    = 128
V2_MAX_RETURN_URI_OCTETS                    = 160
V2_MAX_PROVIDER_ID_OCTETS                   = 63
V2_MAX_PLATFORM_BINDING_ID_OCTETS           = 160
V2_TIMESTAMP_OCTETS                         = 20
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_OCTETS   = 512
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_JCS      = 512
V2_MAX_CREDENTIAL_KID_OCTETS                = 128
V2_DEVICE_DID_OCTETS                        = 56
V2_MAX_LEGACY_HANDLE_OCTETS                 = 33
V2_MAX_MIGRATION_ROOMS                      = 256
V2_MAX_MIGRATION_ROOM_OCTETS                = 129
V2_MAX_MIGRATION_ROOM_JCS_OCTETS            = 129
V2_MAX_MIGRATION_ROOMS_JCS_OCTETS           = 33,793
V2_MAX_DEVICE_JWK_JCS_OCTETS                = 93
V2_MAX_PROVIDER_HINT_JCS_OCTETS             = 507
V2_MAX_OFFER_CORE_NON_ROOM_OCTETS           = 3,135
V2_MAX_OFFER_CORE_OCTETS                    = 52,000
V2_MAX_OFFER_PAYLOAD_OCTETS                 = 57,000
V2_MAX_CREDENTIAL_MEMBER_OCTETS             = 62,000
```

Application display text remains NFC, contains 1 through 128 Unicode scalars,
and excludes C0 and C1 controls. Its two 512-octet caps admit 128 legal
four-octet scalars. Quotes and backslashes remain inside the JCS cap.

The complete 21-member statement has a reachable maximum of 2,536 RFC-8785
octets. The protected header has a reachable maximum of 187 octets. The fixed
type is `selfsame-credential-v2+jws`, and `kid` uses the closed HTTPS-fragment
grammar.

```text
base64url-no-pad(187)  = 250
base64url-no-pad(2536) = 3,382
base64url-no-pad(64)   = 86
two separators         = 2
--------------------------------
maximum compact JWS     = 3,720
policy cap              = 4,000
residual                 = 280
```

The maximum transition is `path-a-to-b`. Its legacy handle is a reachable
33-octet value. The JWK is the 93-octet canonical object with `alg`, `crv`,
`kty`, and `x`. The provider hint is the exact seven-member object in
[[spec-008-0.4.5-standalone-tofu-first-contact#CON-945]] and is 507 octets.

The room-array maximum is derived rather than selected:

```text
256 values * (129 content + 2 quotes) = 33,536
255 separating commas                  =    255
two array brackets                     =      2
------------------------------------------------
complete migrationRooms array          = 33,793
```

There are at least 256 distinct maximum-length values under the room grammar,
so one sorted unique witness reaches the total. No escaping expansion is
possible because the grammar is ASCII and excludes quote, backslash, and
control characters.

The prior non-room result is corrected only by grammar-forced reductions:

```text
0.4.12 non-room core                    = 3,838
deviceDid: 512 -> 56                    =  -456
two timestamps: 32 -> 20               =   -24
legacyHandle: 256 -> 33                 =  -223
------------------------------------------------
reachable non-room core                 = 3,135
complete migrationRooms array           = 33,793
------------------------------------------------
reachable complete core                 = 36,928
52,000-octet core cap residual          = 15,072
```

The complete offer adds exactly three excluded members and their separators:

```text
devicePossessionProof member             =    112
enrollmentEvidence member with JWS       =  3,743
providerHint member                      =    522
three separating commas                  =      3
------------------------------------------------
complete addition                        =  4,380
reachable complete core                  = 36,928
------------------------------------------------
reachable complete offer                 = 41,308
57,000-octet offer residual              = 15,692
62,000-octet CBCL member residual        = 20,692
```

The generator constructs every maximum statement, header, hint, transition,
room array, core, offer, and CBCL member. It records the exact result and
residual. A recognizer ceiling that is intentionally above a reachable maximum
is labelled a cap, never a maximum. Any legal input above a component cap fails
the generator and recognizer gates.

The browser implementation remains source-disjoint in
`apps/cbcl_chat/priv/web/credential-v2-canonical.mjs`. Rust remains hub authority
through the NIF. BEAM-through-NIF remains a marshalling lane.

### REQ-989 — The browser account-bound terminal derives from durable truth

The account-device role rules in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-986]] remain effective.
The browser is account-bound only from its atomic credential/v2 installation
record. That record contains the retained device seed and accepted installed
grant for the current application and account scope. Its seed also re-derives
the recorded device public key. In-memory `pathBBinding`, `heldGrant`, a cache
bit, or handle equality alone is not authority.

The successful install transaction required by
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-919]] creates this durable
state. Reload reconstructs account-bound state only after the record is fully
recognized and its seed re-derives the recorded device key. Corruption,
partial state, or mismatch fails closed and reports recovery required.

While that installed grant exists, a handle change displays the local terminal
`account-device-handle-change-refused` and sends no enrollment frame. The hub
independently refuses a forged or stale attempt.

An authenticated hub `not-issued` refusal for a frame signed by that exact
retained device key proves the installed grant no longer has hub standing. The
browser atomically removes the installed grant and account-bound projection,
retains the device seed under the existing recovery policy, and restores the
handle control. A disconnected socket, timeout, generic error, unauthenticated
text, cached age, or refusal for another key SHALL NOT clear the record. After
the clear, ordinary hub enrollment authority decides any later attempt; this
requirement promises no automatic rebinding or legacy enrollment.

## Non-functional requirement

### NFR-915 — Cyclic contention and bounded retries form a closed set

The complete operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
Mnesia wait-die preemption in `mnesia_locker:can_queue/4` and
`allowed_to_be_queued/2` prevents deadlock. The matrix controls abort rate and
evidence; it is not a deadlock proof.

The complete expected cyclic-source set for this increment is:

1. different-account grant creation against grant creation;
2. grant creation against observed revocation;
3. credential/v2 finalization against member enrollment;
4. grant creation against member enrollment;
5. pairaccount claim against grant creation and against credential/v2
   finalization; and
6. the per-frame authority read against every matrix writer of grant, member,
   or pairaccount state. Those writers are grant creation, credential/v2
   finalization, exact revocation, observed revocation, member enrollment, and
   pairaccount claim.

No phrase such as “include” or “for example” widens or weakens this test set.
Finding another cyclic source requires an amendment to this closed list and its
load evidence.

Every affected operation uses either `mnesia:transaction/2` with an explicit
non-negative integer `Retries` second argument, or `mnesia:transaction/3` with
`Args` and an explicit non-negative integer `Retries`. `transaction/1`,
`transaction(Fun, infinity)`, and `transaction(Fun, Args)` are unbounded and
non-conforming. Exhaustion returns `authority-busy`, produces no partial effect,
and refuses closed at every consumer.

The representative load gate triggers every closed-list pair and records
latency, throughput, retries, writer progress, and decision correctness. The
candidate claims no unmeasured performance property.

## Contract

### CON-972 — The effective text and coordination ledger are exact

The effective set applies only these replacements and clarifications:

1. REQ-987 replaces the room-text and version-dispatch paragraphs in
   [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]]. Its tagged
   transition shapes and member sets remain unchanged.
2. REQ-988 replaces
   [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-985]] in full.
3. REQ-989 replaces only the account-bound browser paragraph in
   [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-986]]. Every member,
   pairaccount, and grant authority outcome remains unchanged.
4. NFR-915 replaces
   [[spec-008-0.4.12-tagged-transition-closed-authority#NFR-914]] in full and
   supplements the matrix in
   [[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]].
5. The membership sentence in
   [[spec-008-0.4.8-standalone-tofu-first-contact#CON-960]] is clarified:
   transition members are members of the offer core alone. They bind directly
   into `offerCoreDigest` and `intentDigest`. CON-214 evidence and the provider
   hint bind them transitively through `offerCoreDigest`. Final confirmation
   binds them through that digest and, for the three transition digests plus
   `snapshotNonce`, through the exact
   [[spec-008-0.4.8-standalone-tofu-first-contact#REQ-972]]
   `migrationConfirmationDigest` preimage.
6. TEST-1117 through TEST-1120 replace
   [[spec-008-0.4.12-tagged-transition-closed-authority#TEST-1113]] through
   [[spec-008-0.4.12-tagged-transition-closed-authority#TEST-1116]].

All other 0.4.12 corrections survive. The exact coordinated set is this
amendment, cbcl-pairing 0.4.5, cbcl-bus 0.16.6, and did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`. A fresh PASS authorizes only the
Elephant SPL and test-first implementation plan. It authorizes no code or
deployment.

## Decision

### ADR-950 — Protocol maxima are constructive, while larger limits are caps

A release-blocking equality witness must be constructible through the same
grammar the recognizer enforces. Calling a larger defensive ceiling a maximum
made an impossible generator normative. This amendment instead chooses the
deployed handle, room, DID, and timestamp grammars, constructs each exact
maximum, and keeps larger aggregate caps explicitly labelled as caps.

The browser authority terminal follows the same rule. It derives from the
durable seed-plus-grant state the protocol installs, not an ephemeral or stale
boolean. Authenticated `not-issued` is the narrow transition that removes that
standing.

## Test specification

Every effective Selfsame test through TEST-1108 remains mandatory.
Cbcl-pairing TEST-030 through TEST-056 remains mandatory. The exact cbcl-bus
set is TEST-043 through TEST-065, TEST-068, TEST-070, and TEST-076 through
TEST-079. No superseded or open-ended test range supplies authority.

### TEST-1117 — Every grammar maximum and aggregate is reachable

Generate both exact transition variants. Construct a 33-octet legacy handle,
256 distinct sorted 129-octet room values, a 56-octet device DID, and exact
20-octet timestamps. Require a 33,793-octet room array.

Require statement 2,536, header 187, compact JWS 3,720, JWK 93, hint 507,
non-room core 3,135, core 36,928, complete offer 41,308, and every stated
residual. Mutate each grammar, count, unit, cap, escaping rule, and member by
one. Require refusal. Rust and source-disjoint JavaScript reproduce every
vector; BEAM-through-NIF reproduces marshalling only.

### TEST-1118 — Every v2 comparison and substitution is source-disjoint

Trace profile, provider hint, offer, statement, builder, recognizer, signer, and
profile-facts call graphs across crate boundaries. Require the exact v2 members
and version constants. Reject every listed v1 comparison or substitution from a
v2-reachable path, including the current `selfsame-web-device::profile_facts`
constant site.

Keep live v1 bytes, digest, route, builder, signer, and profile-facts output
exact. Mutate one dispatch or substitution edge and require static and vector
suites to fail.

### TEST-1119 — Account-bound browser state survives reload and clears narrowly

Install credential/v2 atomically, reload, re-derive the device key, change the
handle, and require `account-device-handle-change-refused` with zero enrollment
frames. Corrupt or partially remove the installation record and require closed
recovery rather than inferred authority.

Revoke the exact installed grant. Deliver the authenticated `not-issued` result
for a frame signed by that device key. Require atomic grant/projection removal,
seed retention, and restored handle control. Timeouts, generic errors, forged
text, another key, and stale cache state do not clear. A forged enrollment frame
still receives the hub's closed authority result.

### TEST-1120 — Every cyclic source, retry form, gate, and ledger is closed

Instrument every matrix lock. Trigger every pair in NFR-915 and no open-ended
substitute. Require bounded retry or `authority-busy`, zero partial effects, and
closed consumers. Static checks reject `transaction/1`, infinity, and the
two-argument `(Fun, Args)` overload.

Require the cbcl-bus 0.16.6 GATE-04 text byte-for-byte in gate evidence. Require
the exact Selfsame, cbcl-pairing, and cbcl-bus test survivor lists. Remove one
gate box, replacement, CON-960 clarification, or parent DEC-012 pointer and
require the effective-text check to fail.

## Changelog

- **0.4.13-draft — 2026-08-23 — review candidate, not approved.** Makes every
  maximum reachable, closes cross-crate version substitution, derives browser
  standing from durable installation state, and closes contention and
  coordination ledgers. No implementation or deployment authority is claimed.
