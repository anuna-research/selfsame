---
id: spec-008-0.5.1-claude-adversarial-review
title: SPEC-008 0.5.1-draft — Tier-1 adversarial review of the standalone credential/v2 pairing architecture
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
review-set: "selfsame `2ba6422329d80ab1af1bd458312c5f9b0a19e4e2` SPEC-008 0.5.1-draft; cbcl-bus `4be2e0bf7faff0a739e8f36f73ea5064a063bb66` SPEC-053 0.17.1-draft; cbcl-pairing `7c6b9ef5666b05763a8b9a298a44404b6235ebe5` SPEC-001 0.5.0-draft; did-crdt `829855c22f254192fa56fd2a69ecc85d11711683` SPEC-037 0.1.0-draft"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`"
circus-attempt: spec008-consolidated-review attempt 2
last-updated: 2026-08-24
---

# SPEC-008 0.5.1-draft — Tier-1 adversarial review of the standalone credential/v2 pairing architecture

Verdict: **REJECT**.

The reissue repairs almost every defect the 0.5.0 review recorded. The hub half
now exists. The typed display, the PAIR1 separation, and the exact-pair policy
now carry parent-level obligations. Twelve new defects survive verification.

One defect is CRITICAL. Seven are HIGH. The declared hub test set names
twenty-eight artefacts that exist in no parent. The credential/v2 command
sequence cannot execute as written.

## Orientation

Intent: decide whether the four parents state one self-contained, implementable
standalone first-contact architecture. The architecture needs no prior SPEC-004
enrolment and no compiled relay allowlist.

Result: the architecture is close and, in several places, better than its
predecessor. It is not yet executable. Four defects block the wire, two block
the state machine, and two block authority singularity.

Controls:   this review changes no candidate parent, no code, and no sibling
            repository file
            this review commits only this report
            a REJECT verdict authorizes no implementation, allocation,
            release, or deployment
Open:       the corrections in
            [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#Collected corrections and owners]]
            every GATE-04 box (owner: named gate owners)
Detail:     [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#C-1 The declared hub test set exists only in trajectory artefacts]],
            [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#H-1 The ceremony identifier is unreachable and ambiguous]],
            [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#H-2 Reconnect recovery has no command and the concurrency rule blocks the retry]]

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL in this document are to be interpreted as
described in BCP 14 (RFC 2119, RFC 8174) when, and only when, they appear in
all capitals.

## Reviewer identity, authentication, and independence

The generating family is OpenAI GPT-5. This reviewer is Anthropic Claude,
runtime model `claude-opus-5`. The families differ, so the cross-model
condition holds.

`ANTHROPIC_API_KEY` was absent from the environment. The reviewer confirmed
absence through a parameter-set test and an exact-prefix environment count.
The reviewer printed no value and recorded no derivative.

The command `/usr/bin/env -u ANTHROPIC_API_KEY claude auth status --json`
returned these three fields:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

The three values match the gate exactly, so the review proceeded. This report
records no email, organization, account identifier, token, or key. It records
no redacted derivative of one.

This session is fresh. The reviewer imported no conclusion from any earlier
Claude conversation. The reviewer read the 0.5.0 report only to enumerate its
finding identifiers, and re-derived every disposition from the candidates and
the pinned code.

Circus attempt: `spec008-consolidated-review` attempt 2, worktree
`/Users/anuna-01/Code/selfsame-spec008-consolidated-review-2`, branch
`circus/spec008-consolidated-review/2`.

## Exact review set, baselines, and pre-implementation status

The reviewer verified each HEAD before reading. All four match the mandate.

| Repository | Candidate commit | Parent and version |
|---|---|---|
| selfsame | `2ba6422329d80ab1af1bd458312c5f9b0a19e4e2` | SPEC-008 0.5.1-draft |
| cbcl-bus | `4be2e0bf7faff0a739e8f36f73ea5064a063bb66` | SPEC-053 0.17.1-draft |
| cbcl-pairing | `7c6b9ef5666b05763a8b9a298a44404b6235ebe5` | SPEC-001 0.5.0-draft |
| did-crdt | `829855c22f254192fa56fd2a69ecc85d11711683` | SPEC-037 0.1.0-draft |

The cbcl-pairing and did-crdt checkouts carry user-owned working-tree files.
The reviewer read every sibling artefact through `git show COMMIT:path`. The
reviewer modified and cleaned nothing outside this report.

Changes since the pinned baselines are specification-only in all four
repositories. A path filter over each baseline-to-candidate diff returned no
file outside `specs/`, `docs/`, and `evidence/`.

The code is intentionally pre-implementation. No `selfsamepair` command,
`cbcl-selfsame-v2` table, `credential-v2` type, `CredentialV2` symbol, or
`PAIR1` recogniser exists at any baseline. This report separates a correctly
specified future change from a false claim about present code.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 1 | yes |
| HIGH | 7 | yes |
| MEDIUM | 4 | four break test decidability, authority singularity, or a live path |
| LOW | 4 | no |

## Findings

### C-1 The declared hub test set exists only in trajectory artefacts

Severity: CRITICAL. Owner: cbcl-bus repository owner, with the selfsame owner
for the mirrored clause.

cbcl-bus REQ-081 states that the exact current test set contains TEST-043
through TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, TEST-098, and
TEST-115 through TEST-117. Selfsame [[SPEC-008-production-pairing-claimant#CON-985]]
repeats that set verbatim.

The same REQ-081 paragraph states that trajectory documents and review reports
supply evidence only. Twenty-eight of the thirty-one named tests exist in no
part of the cbcl-bus parent.

An exhaustive identifier scan of `specs/SPEC-053-key-root-identity.md` at the
candidate commit returns TEST-001 through TEST-042d, then TEST-115, TEST-116,
and TEST-117. TEST-043, TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, and
TEST-098 appear only inside the declaring sentence itself.

Every missing member exists only under `specs/trajectory/SPEC-053/`. TEST-043
sits in `spec-053-0.16.1-credential-v2-confirmed-migration.md:269`. TEST-093
sits in `spec-053-0.16.10-complete-consent-and-executable-ledgers.md:404`.
TEST-098 sits in `spec-053-0.16.11-closed-ledgers-and-upgrade-free-operator-graph.md:312`.

The 0.17.0 and 0.17.1 reissues removed the amendment ledger that carried those
tests forward. The reissues carried the name list across without carrying the
content.

Two current tests fail as a direct consequence. cbcl-bus TEST-116 requires
every current value to resolve from a direct clause in the parent. The same
test then requires the exact current test set in REQ-081.

Those two requirements contradict each other at the candidate commit, so
TEST-116 cannot pass. Selfsame
[[SPEC-008-production-pairing-claimant#TEST-1157]] carries the same
contradiction through its hub-set row.

cbcl-pairing SPEC-001 shows the correct pattern. Its TEST-063 declares a
current set of TEST-001 through TEST-029 and TEST-060 through TEST-063. Every
one of those thirty-three tests exists as a heading in that parent.

This defect breaks current-authority singularity, pointer resolution, and test
decidability at once. A release engineer cannot run the hub test set, because
twenty-eight members carry no stated content.

### H-1 The ceremony identifier is unreachable and ambiguous

Severity: HIGH. Owner: cbcl-bus repository owner, with the cbcl-pairing owner
for the carrier member.

Two distinct thirty-two-octet ceremony identifiers exist, and both carry the
same name. cbcl-pairing CON-027 places `ceremony-id` inside the
credential/v2 carrier, which the browser builds before any hub call.

cbcl-bus REQ-082 states that the hub allocates fresh independent
thirty-two-octet CSPRNG values for `applicationAccountId`, `accountScopeId`,
`ceremonyId`, and `snapshotNonce`. That allocation happens inside the hub
transaction, after the browser sends the finished carrier.

No clause relates the two values. cbcl-bus CON-012 gives `OfferCoreV2` exactly
one `ceremonyId` member alongside a separate `carrierDigest`, and states no
rule for which identifier that member carries.

The browser also cannot obtain the hub value. REQ-082 states that the hub
returns `selfsamepairallocated(requestId)`, and CON-012 declares that exact
arity. The acknowledgement carries the request identifier and nothing else.

The next four commands all take `ceremonyId` as their first argument. They are
`selfsamepairofferprepare`, `selfsamepairofferfinalize`,
`selfsamepairauthority`, and `selfsamepairaccept`. `selfsamepairabort` takes it
too.

Either reading fails. If the commands carry the carrier value, then REQ-082's
hub allocation of `ceremonyId` is unreachable. If they carry the hub value,
then the browser never receives it and the sequence stops after allocation.

The wallet side fails as well. cbcl-pairing CON-029 requires byte equality
between every overlapping peer value and the separate authority input. The
wallet knows only the carrier identifier, so it cannot decide the offer's
`ceremonyId` row.

This breaks the fresh-scope bootstrap, wire interoperability, and the
non-circularity the mandate requires. An implementer needs to invent the
missing binding rule.

### H-2 Reconnect recovery has no command and the concurrency rule blocks the retry

Severity: HIGH. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 declares exactly six commands. They are
`selfsamepairallocate`, `selfsamepairofferprepare`,
`selfsamepairofferfinalize`, `selfsamepairauthority`, `selfsamepairaccept`, and
`selfsamepairabort`.

The same contract then states that status recovery returns the exact finalized
state, or the exact phase and original expiry, or one closed refusal. No
command in the closed set requests status.

REQ-082 states that the new device key recovers the immutable status and the
byte-identical acknowledgement. Selfsame
[[SPEC-008-production-pairing-claimant#CON-986]] states that crash or loss
before the wallet commit recovers from hub status. Neither names the surface.

The retry path contradicts itself as well. Selfsame
[[SPEC-008-production-pairing-claimant#NFR-928]] states that the browser treats
WebSocket 1013 as non-terminal, cancels the old socket generation, reconnects,
and starts a fresh correlated attempt.

REQ-082 permits a byte-identical answer only for an exact retry on the same
socket generation and request identifier. A reconnect changes the generation,
so that branch closes.

REQ-082 then refuses a concurrent request for the same application, handle, and
enrolled key with a different request or carrier bytes. The fresh correlated
attempt carries a new request identifier, so that branch refuses.

No clause aborts or releases the pending record on socket loss. The abort
command needs the `ceremonyId` that H-1 shows the browser never receives.

The result is a lockout. After any socket loss between allocation and
finalization, the person cannot retry and cannot abort until the pending record
expires. Selfsame TEST-1156 asks for exactly the retry this rule refuses.

### H-3 Four mandatory offer-core members carry no grammar, bound, or consumer

Severity: HIGH. Owner: cbcl-bus repository owner.

cbcl-bus CON-012 gives `OfferCoreV2` exactly twenty-three top-level members and
states that no member is optional. The reviewer counted twenty-three, so the
list is closed and exact.

Four members appear exactly once across all four parents, inside that list and
nowhere else. They are `applicationAccountDisplay`, `platformBindingId`,
`returnUri`, and `deviceDid`.

CON-012 bounds only some shapes. It states that binary identifiers, nonces, and
SHA-256 values use canonical base64url without padding and decode to
thirty-two octets. None of the four members takes that shape.

`returnUri` is the sharpest case. An unconstrained URI inside a signed consent
object, with no scheme restriction and no length bound, is a confused-deputy
and open-redirect surface.

`applicationAccountDisplay` reads as human-facing text. cbcl-pairing CON-029
lists the display contents and excludes it, so no clause states whether a
consent surface renders it.

The digest rule makes the gap load-bearing. CON-012 states that
`offerCoreDigest` is SHA-256 over the RFC-8785 encoding, and that the wallet,
browser, and hub each recompute it independently.

Three independent implementations cannot agree on canonical bytes for members
with no declared type. This breaks wire interoperability and leaves four
unbounded inputs inside an authenticated structure.

### H-4 The rendezvous dependency has no consumer and its two witnesses derive from nothing

Severity: HIGH. Owner: selfsame repository owner, with the did-crdt owner.

Selfsame [[SPEC-008-production-pairing-claimant#CON-986]] requires the Selfsame
rendezvous to accept bodies of one through 69,632 octets. It names a
57,016-octet sealed offer witness and a 62,016-octet sealed reverse-payload
witness.

cbcl-bus GATE-04 box forty makes both witnesses a release blocker. did-crdt
REQ-370 and TEST-370 carry the matching obligation for `did.anuna.io`.

No clause in any of the four parents states that the credential/v2 ceremony
writes to or reads from a rendezvous slot. A full identifier sweep of the
cbcl-pairing parent returns one hit, in frontmatter.

The credential/v2 transport is the cbcl-pairing relay, not a rendezvous.
cbcl-pairing NFR-004 caps a mailbox frame body at 69,632 octets, and its relay
binaries cap a wire message at 70,000 octets.

Neither witness matches a cbcl-pairing object either. CON-028 pads the large
arm to exactly 64,512 octets, so every large plaintext encodes to one constant
size near 64,556 octets before sealing.

Neither number derives from any stated encoding. cbcl-bus CON-012 caps the
offer core at 56,000 octets and every other logical member at 62,000 octets.
No parent states the sealing overhead that reaches 57,016 or 62,016.

Selfsame [[SPEC-008-production-pairing-claimant#TEST-1157]] then requires the
reviewer to check sealed cap arithmetic. No parent states that arithmetic, so
the test row is undecidable.

The witnesses themselves are satisfiable and separately owned. A server can
store a body of either length, selfsame CON-986 owns
`crates/selfsame-rendezvous`, and did-crdt CON-370 owns
`src/service/rendezvous.rs`.

### H-5 The Selfsame rendezvous has no capacity bound and no CORS behavior to preserve

Severity: HIGH. Owner: selfsame repository owner.

Selfsame [[SPEC-008-production-pairing-claimant#CON-986]] requires the Selfsame
rendezvous to preserve its current slot grammar, first-write, read-once,
lifetime, capacity, CORS, and blind-content behavior.
[[SPEC-008-production-pairing-claimant#TEST-1159]] requires a re-run of the
capacity and CORS tests.

`crates/selfsame-rendezvous/src/lib.rs` contains neither property. The file
declares `MAX_SLOT_BYTES` at line 48 and `SLOT_TTL_SECONDS` at line 51, and
declares no slot-count constant.

`put_slot` at lines 123 to 148 inserts into an unbounded `HashMap` after the
expiry sweep. It emits 400, 409, or 201 and sets no `Access-Control` header on
any path.

The did-crdt implementation differs. Its handler declares `MAX_SLOTS` at
`src/service/rendezvous.rs:79`, refuses a full store with 507, and attaches
`cors_headers()` to every response.

So CON-986 names two properties that its own named implementation lacks. An
implementer needs to invent a capacity bound and a CORS policy before TEST-1159
can run.

The omission is also a live availability defect. The change raises the
unauthenticated per-slot ceiling from 4,096 to 69,632 octets. That
seventeen-fold rise lands on a store with no slot cap and a 600-second
lifetime.

did-crdt REQ-370 states the corresponding control directly. It preserves the
65,536-slot capacity and requires bounded request buffering. Selfsame CON-986
states no equivalent.

### H-6 The SPEC-007 REQ-812 amendment uses a channel SPEC-007 does not provide

Severity: HIGH. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#CON-986]] states that one paragraph
amends [[SPEC-007-cbcl-pairing-cutover#REQ-812]] for credential/v2 reverse
issuance only. The frontmatter `amends` field repeats that claim.

The amendment is necessary. REQ-812 forbids an identity side effect before
approved payload delivery, and reverse issuance needs signing and publication
in order to build the payload it delivers.

SPEC-007 states its own amendment channel. Amendment happens through a
reviewed, merged, versioned revision with updated tests, vectors, traceability,
changelog, and Gate Evidence Record.

SPEC-008 supplies none of those for SPEC-007. SPEC-007 remains `status:
approved` at version 0.2.1 with `last-updated: 2026-08-17`, and its REQ-812
text carries no narrowing.

SPEC-007 also contains no reference to SPEC-008 anywhere in the file. A reader
of the approved parent sees the unamended rule and no pointer to the draft that
narrows it.

The `amends` frontmatter field is novel. SPEC-008 is the only specification in
the selfsame vault that carries it, so no established vault channel supports
the claim.

REQ-812 is not in the SPEC-007 hard-stop list, so no hard stop is waived. The
defect is authority singularity: two documents now state different rules for
one requirement identifier.

The consequence is concrete. An implementer following SPEC-007 as written fails
[[SPEC-008-production-pairing-claimant#TEST-1158]], and one following CON-986
violates an approved Tier-1 requirement.

### H-7 The hub names a cbcl-pairing test that does not exist

Severity: HIGH. Owner: cbcl-bus repository owner.

cbcl-bus TEST-115 requires a run of the current cbcl-pairing TEST-050
control-body bounds, with every exact positive and negative boundary row.

cbcl-pairing SPEC-001 0.5.0-draft contains no TEST-050. Its declared current
set is TEST-001 through TEST-029 and TEST-060 through TEST-063, and the
control-body bounds sit in TEST-063.

The retired TEST-050 lives in
`specs/trajectory/SPEC-001/spec-001-0.4.1-credential-v2-profile-authority.md:227`.
The 0.4.3 trajectory document replaced it because it retained a stale
4,096-octet positive witness.

The 0.5.0 reissue inlined the surviving witnesses under TEST-063 and renumbered
them. The hub cross-reference did not follow.

The two coordinating parents now disagree. Selfsame
[[SPEC-008-production-pairing-claimant#TEST-1156]] names cbcl-pairing TEST-063
for the same control-body rows, and the hub names TEST-050.

An identifier collision worsens the confusion. The cbcl-bus vault carries its
own retired TEST-050 about credential/v1 signing widening, in
`specs/trajectory/SPEC-053/spec-053-0.16.2-credential-v2-authority-corrections.md:388`.

### M-1 The `record_pairing_trust` prohibition reaches a live SPEC-004 path

Severity: MEDIUM. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#CON-986]] states that the wallet
ordinary build contains no compiled conformance registry, relay allowlist,
held-enrolment precondition, `record_pairing_trust` call edge, or fallback to
`assemble_claimant`.

Three of those five terms belong only to the replaced path. `cbcl_registry`
reaches production code only through `src-tauri/src/cbcl_pairing.rs:30` and
`:152`. `assemble_claimant` reaches it only through `src-tauri/src/cbcl_pairing.rs:170`.

The fourth term does not. `record_pairing_trust` has one production caller, and
it is `src-tauri/src/app_grant.rs:539` inside `confirm_issuance`. That call
belongs to the SPEC-004 grant-issuance path, not to pairing.

A literal reading of CON-986 therefore deletes a call edge that the deployed
SPEC-004 enrolment path owns. Nothing in credential/v2 consumes that edge.

The verifying test states a narrower rule.
[[SPEC-008-production-pairing-claimant#TEST-1159]] fails every match on the
credential/v2 path, which leaves the SPEC-004 caller untouched.

The contract and its test therefore disagree about scope. That disagreement
needs a decision before either can be implemented.

### M-2 The exact-pair consent boundary has no owning function

Severity: MEDIUM. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#REQ-906]] requires one new-relay
decision before socket creation. Approval seals the tuple into the person's
policy, and rejection creates no policy entry or socket.

[[SPEC-008-production-pairing-claimant#CON-986]] states that `prepare_claimant`
performs pure recognition and authenticated public network reads only. A
consent prompt and a durable sealed write are neither.

The same clause states that the split functions are the only credential/v2
construction path. Those functions are `prepare_claimant`,
`preview_claimant_identity`, and `complete_claimant`.

The first decision capability authorizes exactly one zeroizing custody call, so
it cannot own the earlier policy write either.
[[SPEC-008-production-pairing-claimant#CON-903]] describes the store interface
and names no caller.

CON-986 also gives `prepare_claimant` work on both sides of that boundary. It
recognises the carrier and verifies the profile before the socket, then
completes CPace and both Finished values after it.

So the third consent surface has no owner, and one specified function spans a
consent boundary its own purity clause excludes. An implementer needs to invent
the placement.

### M-3 WIRE_VERSION names half of its actual namespace

Severity: MEDIUM. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#REQ-1005]] states that `WIRE_VERSION`
belongs to the link-offer wire namespace, governed by `anuna-ssi` SPEC-001
CON-001 at commit `c7d462029841ea1884bb6f089732058d8838728d`.

The reviewer resolved that pin independently. CON-001 sits at line 1073 of
`specs/SPEC-001-device-key-provisioning.md`, and it defines both an
`extend link-offer` form and an `extend link-grant` form. Both carry `:v`.

The pinned selfsame code matches the wider reading. `WIRE_VERSION` is emitted
at `crates/selfsame-core/src/record.rs:329` for the offer and at `:462` for the
grant, and compared at `:396` and `:490`.

So two of four production sites are grant-record sites.
[[SPEC-008-production-pairing-claimant#TEST-1157]] requires `WIRE_VERSION` to
resolve to its named namespace, and that row fails against the pinned code.

Nothing load-bearing depends on the clause, because `WIRE_VERSION` matches none
of the five closed credential-v1 identifiers. The failing test row is the
defect.

`CODE_VERSION` resolves correctly. `crates/selfsame-core/src/code.rs:78`
declares it, and CON-001's code grammar is
`version(1) || application-id(1) || secret(16)`.

### M-4 The did-crdt worst-case store rises seventeen-fold behind a weak assurance

Severity: MEDIUM. Owner: did-crdt owner.

did-crdt REQ-370 preserves the 65,536-slot capacity and the 600-second
lifetime. It requires bounded request buffering so the larger ceiling creates
no unbounded request or store.

The arithmetic still moves. The worst-case resident store rises from about 268
megabytes to about 4.56 gigabytes, on an endpoint that takes unauthenticated
opaque bodies.

The clause is literally satisfied, because 4.56 gigabytes is a bound. The
parent records no operational acceptance of the new figure and no memory
ceiling separate from the slot count.

CON-370 recognition order also buffers up to 69,633 octets per request before
the store lock. TEST-370 exercises one stream, and no clause bounds concurrent
in-flight requests.

### L-1 The executor order appears in three places with two shapes

Severity: LOW. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#CON-902]] gives the executor this order:
re-derive, check preview, create and sign issuer, publish, verify the live
WebFinger JRD, construct the grant last. It omits closure resolution.

[[SPEC-008-production-pairing-claimant#CON-986]] adds closure resolution, and
[[SPEC-008-production-pairing-claimant#TEST-1158]] declares the full order as
issuer creation, publication, closure verification, WebFinger verification,
grant construction, and one reverse payload.

The three statements do not contradict each other. CON-902's omission still
leaves the sequence stated twice with different completeness.

### L-2 The schema-migration enumeration misses a third site

Severity: LOW. Owner: cbcl-bus repository owner.

[[SPEC-008-production-pairing-claimant#NFR-928]] names
`cbcl-store-migrate:legacy-rows/0` as the transaction-based schema-migration
site. It then names `cbcl-chat-roommember:migrate-since/0` as a second schema
migration outside the transaction inventory.

A third `mnesia:transform_table` site exists at
`apps/cbcl_chat/src/shell/cbcl-chat-roomcfg.lfe:82`, inside
`transform-room/1`, which rewrites the `cbcl-room` table across two attribute
shapes.

Both excluded sites sit outside the transaction inventory, so nothing
load-bearing breaks. The same requirement states that omitted or unclassified
rows are non-conforming, which makes completeness normative.

### L-3 Two named failure modes no longer describe the pinned code

Severity: LOW. Owner: selfsame repository owner.

The parent dates its four named failure modes to 2026-08-18, and the reviewer
treats that dating as honest. Two of the four now read as false claims about
the pinned baseline.

FM-1 cites `src-tauri/src/cbcl_pairing.rs:106-153` for a claimant that opens no
socket. Those lines now hold `cbcl_pairing_capability` and the head of
`cbcl_pairing_start`, and the non-demo branch opens a `wss://` socket at
`:176`.

FM-2 states that `tungstenite` compiles without TLS and cannot speak
`wss://`. `src-tauri/Cargo.toml:46` adds `rustls-tls-webpki-roots`, and
`crates/selfsame-pairing/Cargo.toml:41` keeps the narrow feature set.

[[SPEC-008-production-pairing-claimant#REQ-903]] and
[[SPEC-008-production-pairing-claimant#TEST-905]] also require the
`PairingUnavailable` stubs to be absent. No approve or decline stub exists at
the baseline; the one remaining use at `:448` is `take_pending`'s legitimate
refusal.

### L-4 The registry module comment still cites the contract that forbids it

Severity: LOW. Owner: selfsame repository owner.

`src-tauri/src/cbcl_registry.rs:1` describes the file as
"SPEC-008 `CON-903` — the compiled approved-conformance registry".
[[SPEC-008-production-pairing-claimant#CON-903]] now excludes a
conformance-digest registry from the policy.

The mandate treats a stale code comment as implementation work rather than
normative truth. The comment disappears with the module that
[[SPEC-008-production-pairing-claimant#CON-986]] removes.

## Disposition of the 0.5.0 findings

| 0.5.0 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 hub half exists in no cbcl-bus specification | **CLOSED** | cbcl-bus REQ-082, CON-012, and TEST-117 now state the allocation command, its authenticated precondition, the bounded pending record, the offer core, the final atomic transaction, and every deletion trigger. |
| H-1 both parents fail their own singularity tests | **PARTLY CLOSED** | No retirement list, partial-range replacement, or historical precedence rule survives in either current band. The hub set still fails to resolve; see [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#C-1 The declared hub test set exists only in trajectory artefacts]]. |
| H-2 each parent declares its own anchor historical | **CLOSED** | Both parents carry `authority-form: consolidated-direct-current-authority` and no historical range. No anchor clause sits inside a retired band. |
| H-3 the claimed DEC-012 pointer does not exist | **CLOSED** | The identifier `DEC-012` appears nowhere in the selfsame parent. The hub retains it only inside its legacy decision band, where it defers production and claims no coordinated pointer. |
| H-4 GATE-04 sealed-cap box unsatisfiable | **PARTLY CLOSED** | did-crdt SPEC-037 now owns its half, GATE-04 box forty names both owning parents, and both witnesses fit 69,632 octets. The consumer and the arithmetic remain absent; see [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#H-4 The rendezvous dependency has no consumer and its two witnesses derive from nothing]]. |
| H-5 CON-902 contradicts itself on issuer and WebFinger placement | **CLOSED** | The plan table drops the `issuer` and `jrd` rows and carries `issuer_constraints` instead. Both boundaries place issuer creation, publication, and WebFinger verification after final approval. |
| H-6 the pending-scope contract dropped retry and concurrency | **CLOSED** | CON-902 and cbcl-bus REQ-082 both restore exact retry, concurrency refusal, expiry deletion, and four crash boundaries. The reconnect gap is a new defect, not this one. |
| H-7 TEST-1157 requires a non-normative trajectory clause | **CLOSED** | `REQ-995` appears nowhere. Every installed-state obligation now sits directly in [[SPEC-008-production-pairing-claimant#REQ-1006]]. |
| M-1 cbcl-pairing version collides | **CLOSED** | The parent is 0.5.0-draft, above every trajectory amendment, whose maximum is 0.4.12. All four parents and GATE-04 box one name that exact version. |
| M-2 authenticated typed display appears in no parent | **CLOSED** | cbcl-pairing CON-029 makes `DisplayIntent` and `DisplayField` private, forbids the wire `authority_summary`, and gives the profile sole construction of `CredentialV2Display`. Selfsame [[SPEC-008-production-pairing-claimant#REQ-1006]] mirrors it. |
| M-3 PAIR1 carries no parent-level obligation | **CLOSED** | cbcl-pairing REQ-031 and selfsame [[SPEC-008-production-pairing-claimant#REQ-1006]] both state separation, non-substitutability, and the exact code grammar. |
| M-4 frontmatter disagrees about the historical band | **CLOSED** | Neither parent declares a historical range now. |
| L-1 `WIRE_VERSION` named for the wrong wire | **PARTLY CLOSED** | The wire family is right and the namespace is still narrow; see [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#M-3 WIRE_VERSION names half of its actual namespace]]. |
| L-2 anuna-ssi references are plain prose | **CLOSED** | [[SPEC-008-production-pairing-claimant#REQ-1005]] and [[SPEC-008-production-pairing-claimant#CON-985]] both record an explicit cross-vault deferral and state why a local wikilink cannot represent it. |
| L-3 compiled registry has no stated disposition | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] removes it and [[SPEC-008-production-pairing-claimant#TEST-1159]] scans the ordinary binary and source graph for it. Scope needs the M-1 repair. |
| L-4 stale `cbcl_registry.rs` comment | **OPEN, NON-BLOCKING** | The comment survives at the baseline and remains implementation work; see [[spec-008-0.5.1-claude-adversarial-review-2026-08-24#L-4 The registry module comment still cites the contract that forbids it]]. |

## Hard-stop disposition, F-A through F-E

Each row is re-derived from the candidate text and the pinned code.

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-903]] keys the sealed set on `(applicationId, relayOrigin)` and excludes a relay-only key, a global allowlist, a digest registry, and a wildcard. The replaced code takes the excluded path: `cbcl_relay.rs`'s `eligible` requires `approved_conformance.contains(...)`. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input and none from the peer input. It forbids generic `DisplayField` values and the wire `authority_summary`, which `src/profile.rs:98` still exposes today. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | The pinned `assemble_claimant` derives the key, calls `issuer::create`, then `webfinger::fetch_and_verify` and `resolve_closure`, all before the socket. CON-902 and CON-986 move every one of those after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end, no prior-enrolment cycle | **NOT CLOSED** | The cycle does break: cbcl-bus REQ-082 supplies the hub-owned allocation that replaces the `record.account_scope_id` read at `cbcl_context.rs:279`. The trace still stops, because the ceremony identifier is unreachable and reconnect recovery has no command. See H-1 and H-2. |
| F-E what does and does not prove relay operatorship | **CLOSED** | [[SPEC-008-production-pairing-claimant#ADR-902]] states plainly that the application profile does not prove relay operatorship, and that exact-pair consent contains the residual without claiming to solve it. No parent contradicts it. |

## N11 disposition

Each row re-tests the disposition recorded in the 0.5.0 report against this
candidate.

| N11 row | Disposition | Re-derived evidence |
|---|---|---|
| N11-C-1 retirement chain revives itself | **CLOSED** | No retirement list, survivor partition, or historical precedence clause survives in either current band. Both parents state current law directly. |
| N11-H-1 GATE-04 carries 42 direct boxes | **CLOSED** | A count of `- [ ]` items between the GATE-04 heading and the following contract returns exactly 42. Box one names all four parents and their exact versions. |
| N11-M-1 the anuna-ssi pin resolves | **CLOSED** | Commit `c7d462029841ea1884bb6f089732058d8838728d` exists, and CON-001 sits at line 1073 of `specs/SPEC-001-device-key-provisioning.md`. It supplies grammar only, and the namespace claim is now partly wrong; see M-3. |
| N11-M-2 both alias cases appear | **CLOSED** | Selfsame [[SPEC-008-production-pairing-claimant#NFR-928]] and cbcl-bus NFR-026 both carry the absent-alias and the disagreeing-alias case, each requiring `inconsistent-binding` and zero deletion. Both test bands repeat them. |
| N11-M-3 only collection and predicate owners take tagged success | **CLOSED** | Both parents state that rule verbatim, and both add that every other public owner retains its declared success shape. `mint/3` returns `#(ok Token)` and `ensure-room/4` returns `ok`, each after commit. |
| N11-L-1 anuna-ssi references remain non-wikilinked prose | **CLOSED** | Both parents now record the deferral explicitly, so the non-wikilinked form is a stated decision rather than an omission. |
| N11-L-2 no raw exhausted `#cyclic{}` terminal | **CLOSED** | Selfsame [[SPEC-008-production-pairing-claimant#NFR-928]] and [[SPEC-008-production-pairing-claimant#TEST-1156]] carry the prohibition, and cbcl-bus NFR-026 and TEST-115 carry the matching pair. |
| N11-L-3 the three lint commands report clean | **CLOSED** | The reviewer re-ran strict descriptive lint on the three complete parents and on the hub current-authority band. All four report zero errors and zero warnings. |

## Authority, pointer, and test-set assessment

Authority form is now direct in all four parents. No current clause depends on
a retirement ledger, a partial-range replacement, or a historical precedence
rule. That repair is real and complete.

Version agreement is exact across the coordinated set. All four parents name
SPEC-008 0.5.1-draft, SPEC-053 0.17.1-draft, SPEC-001 0.5.0-draft, and SPEC-037
0.1.0-draft, and GATE-04 box one repeats the same four.

Generation provenance agrees in all four. Each records OpenAI GPT-5,
`gpt-5.6-sol`, session `01a029aa-9127-7c42-ad28-81512b91ded6`, and a synthesis
trajectory line.

Consumer pointers close their loop. cbcl-pairing frontmatter names
`consumer-design: selfsame SPEC-008 0.5.1-draft`, and
[[SPEC-008-production-pairing-claimant#REQ-1005]] names cbcl-pairing SPEC-001
0.5.0-draft.

Cross-vault identifiers are explicit and honest.
[[SPEC-008-production-pairing-claimant#CON-985]] states that the cbcl-pairing,
cbcl-bus, did-crdt, and `anuna-ssi` identifiers are deliberately not local
wikilinks.

Test-set singularity holds for two parents and fails for one. The selfsame set
of TEST-901 through TEST-915 and TEST-1156 through TEST-1159 resolves
completely, and so does the cbcl-pairing set. The hub set does not; see C-1.

The declared hub set also mixes two vaults. cbcl-bus TEST-115 sends the reader
to a cbcl-pairing test identifier that no current cbcl-pairing clause defines;
see H-7.

## Wire assessment

The PAIR1 grammar is exact and closes cleanly. Eleven groups of five Crockford
characters give 275 bits, and `C || T || checksum16 || 000` consumes
128 + 128 + 16 + 3 bits.

The alphabet `0123456789ABCDEFGHJKMNPQRSTVWXYZ` holds exactly 32 symbols and
omits `I`, `L`, `O`, and `U`. The checksum construction names its exact
domain separator, so an implementer can build it without invention.

The cbcl-pairing wire is internally consistent. Control bodies of 1 through
2,048 octets and the exactly 4,096-octet control padding match
`MAX_CONTROL_OCTETS` at `src/cbcl_protocol.rs:32`.

Large bodies of 1 through 62,000 octets and the exactly 64,512-octet padding
sit under `MAX_FRAME_BODY_BYTES` at `src/mailbox.rs:15`. They also sit under
the 70,000-octet ceiling in both relay binaries.

The v2 admission wire is closed and exact. `allocate-v2`, `claim-v2`,
`allocated-v2`, and `claimed-v2` each name their member set, and the commitment
construction states its domain separator.

The offer core is closed at twenty-three members and the reviewer counted
twenty-three. Four of those members carry no type; see H-3.

The credential/v2 carrier does carry an application identifier, so the wallet
authenticates a real application rather than a constant. That repairs the
0.3.0 defect, where `application` was the constant `anuna.io/credential/v1`.

## State-machine assessment

The pure projections are stated and closed. cbcl-bus CON-012 gives
`absent -> allocated -> prepared -> offered -> authority-clear -> finalized`
with abort and expiry arms, and cbcl-pairing CON-028 gives the session
projection.

Lock orders are stated for both hub transactions and read as non-circular.
Allocation and final acceptance each name an ordered list, and CON-012 requires
one commit with no nested transaction and no abort catching.

The account principal is well formed. It hashes the application identifier with
a thirty-two-octet random `applicationAccountId`, and CON-012 excludes the raw
identifier from `OfferCoreV2`.

That construction satisfies the mandate directly. The raw value stays
hub-private through preimage resistance, and the wallet installs the digest and
scope under [[SPEC-008-production-pairing-claimant#REQ-1006]].

The machine still fails to execute. The ceremony identifier is unreachable, and
socket loss produces a lockout with no status command; see H-1 and H-2.

## Consent assessment

Three consent surfaces exist. They are the exact-pair decision, the preliminary
decision, and the final decision.

The exact-pair surface names the authenticated application and canonical relay
origin, both from the profile authenticated under
[[SPEC-004-application-scoped-identity#CON-220]]. No peer text reaches it. Its
owning function is unstated; see M-2.

The preliminary surface takes every value from the authority input.
cbcl-pairing CON-029 requires byte equality between overlapping peer and
authority values before any allocation, and returns a verdict-only verifier
result.

Only the cbcl-pairing profile allocates `CredentialV2Display`, and only after
that equality check. No caller verdict, wire string, or DOM room value can
enter the surface.

The final surface adds only the locally derived DID and fingerprint. That
material never leaves custody as a key, because the preview call zeroizes the
hierarchy root before returning.

Ordering against [[SPEC-007-cbcl-pairing-cutover#REQ-812]] is coherent under
the CON-986 amendment and incoherent without it. The amendment channel is the
defect; see H-6.

Every effect list is enumerated. [[SPEC-008-production-pairing-claimant#TEST-1158]]
instruments custody, derivation, DID computation, issuer creation, signing,
publication, closure, WebFinger, grant, persistence, alias, migration, payload,
and installation.

## Persistence assessment

The busy inventory is accurate against the pinned hub code. The reviewer
resolved every cited line and found each one in the claimed role.

Examples include `cbcl-chat-session-ws.lfe:512` consuming `device-grants/1`
into an admission map, `:869` consuming `rooms-for-account/1` through
`lists:foreach`, and `:965` consuming `list-public/0` through the wire encoder.

Both `get-or-start/1` consumers resolve. The removal path sits at
`cbcl-chat-session-ws.lfe:1076` and the hello path at `:1788`, and each carries
the terminal the parents assign it.

The successor edges resolve too. `cbcl-chat-successor.lfe:109` catches around
`pubkey-tx/1` and `:134` catches around the transaction-opening
`device-grant/2`, which is exactly the pair the parents forbid.

The transaction inventory is decidable. Seventy-five `mnesia:transaction` call
sites exist under `apps/`, and the lane rule derives membership from table
access.

The WebFinger owners resolve at their stated arities. `provision/3`,
`withdraw/2`, `reinstate/2`, `lookup/1`, `create-or-return/3`,
`lookup-binding/1`, `already-withdrawn/2`, and `lift-tombstone/2` all match
`cbcl-chat-webfinger-store.lfe`.

`tombstone` is currently arity two and the parents name `tombstone/3`. The
parents state that as the intended shape, so this reviewer reads it as a
specified change rather than a false claim.

The installed-state rules are complete and decidable.
[[SPEC-008-production-pairing-claimant#REQ-1006]] names every stored member and
every reload outcome, and separates unavailability from revocation and absence.

### The transaction and consumer surfaces found sound

The Mnesia claim about level-one exhaustion is correct. A retry-exhausted
cyclic restart under `mnesia:transaction(Fun, [], 3)` surfaces as
`{aborted, nomore}`, so the single observable terminal is real.

The lane precedence rule is total. Authority takes precedence, then ceremony
migration, then WebFinger, and the schema-migration site sits outside every
runtime lane.

The tagged-success scope is narrow and stated twice. Only collection and
predicate owners adopt `#(ok Value)`, and every other owner keeps its declared
shape.

## Rendezvous assessment

Both baselines are non-conforming, exactly as the parents assume. Each declares
`MAX_SLOT_BYTES` at 4,096 and each refuses an oversize body with 400 rather
than 413.

Both owning clauses exist and are separate.
[[SPEC-008-production-pairing-claimant#CON-986]] owns
`crates/selfsame-rendezvous/src/lib.rs`, and did-crdt CON-370 owns
`src/service/rendezvous.rs`.

Both parents state that neither implementation's result proves the other, and
both test lists carry the same four witness lengths plus the 69,633-octet
refusal.

did-crdt SPEC-037 is the cleanest artefact in the set. It states its slot
grammar, its outcome table, its streaming bound, its capacity and lifetime
preservation, and its exclusion of the other PROTO-002 changes.

The Selfsame half is weaker. It preserves two properties its named
implementation lacks, and it states no buffering bound; see H-5.

The dependency itself lacks a consumer, and the two witness sizes derive from
no stated encoding; see H-4.

## Mechanical evidence

### Controlled-language lint

The reviewer ran
`/Users/anuna-01/.agents/skills/anuna-dev/tools/usdd-lint.sh --type descriptive
--strict FILE` on each artefact below.

| Artefact | Errors | Warnings | Longest sentence |
|---|---:|---:|---:|
| selfsame SPEC-008 0.5.1-draft, complete parent | 0 | 0 | 24 |
| cbcl-pairing SPEC-001 0.5.0-draft, complete parent | 0 | 0 | 25 |
| did-crdt SPEC-037 0.1.0-draft, complete parent | 0 | 0 | 20 |
| cbcl-bus current-authority band | 0 | 0 | 24 |

The hub band runs from `## Tier-1 generation provenance` up to but excluding
`## Changelog`, extracted by exact heading boundaries. It holds 896 lines and
479 sentences.

Legacy hub debt sits outside that band and predates this candidate. The band
before the provenance heading reports 895 errors, and the band from the
changelog onward reports 445. This reviewer attributes neither to this
candidate.

### Vault and structural checks

`git diff --check` on the Circus worktree reports no whitespace or conflict
error.

No candidate carries a duplicate artefact heading. The reviewer scanned
headings for eight identifier prefixes: `REQ`, `CON`, `ADR`, `TEST`, `NFR`,
`OBS`, `GATE`, and `DEC`. No value repeats in any of the four.

No candidate carries an unqualified same-note anchor link. A literal search
for a wikilink opening followed immediately by a hash returns zero hits in all
four parents.

The selfsame candidate carries 58 distinct wikilink targets and no dead link. A
resolver over the candidate commit matched every note and every anchor.

The hub current-authority band carries five wikilink targets and no dead link.
The whole hub parent carries sixteen dead anchors and four dead notes, all
outside the band and all pre-existing.

GATE-04 carries exactly 42 unchecked boxes between its heading and the
following contract heading.

### Code-claim checks

Every one of the 27 production version-site witnesses in
[[SPEC-008-production-pairing-claimant#REQ-1005]] resolves to the exact line.
Each carries one of the five closed identifiers in its declared class.

Every hub line number cited by
[[SPEC-008-production-pairing-claimant#NFR-928]] resolves to the claimed
consumer, sink, or boundary. The reviewer checked 33 cited lines across seven
modules.

The `anuna-ssi` pin resolves to a real commit, a real file, and a real
CON-001. That contract defines both the link code and the link records.

## Residual assumptions

The reviewer read the `anuna-ssi` vault from a local checkout that the mandate
did not list. The pin resolves there. A reviewer without that checkout cannot
verify it.

The reviewer verified specification text and pinned code. The reviewer executed
no build, no test suite, and no relay, so every runtime claim rests on source
reading.

The reviewer accepts the parents' dating of the four named failure modes. Two
of them no longer describe the pinned baseline; see L-3.

The reviewer treats normative present tense in a pre-implementation parent as a
specified future shape. `tombstone/3` and the six new hub commands read that
way.

The Mnesia retry-exhaustion claim rests on OTP behavior rather than on a test
in this repository. No pinned code exercises it.

## What this verdict authorizes

This verdict authorizes nothing beyond further specification work.

It authorizes no implementation, no Elephant SPL, no test-first plan, no
production allocation, no release, and no deployment. It closes no GATE-04 box
and no SPEC-007 production gate.

It does not reopen any surface this report marks CLOSED. The C-1 repair, the
typed display, the PAIR1 separation, the exact-pair policy, and the two consent
boundaries are sound as written.

It records that the architecture is one round of correction away from
implementable. Eight of twelve findings are local text repairs rather than
design changes.

The next review needs another fresh session and a different model family from
the generating one. This report's findings form the set that review needs to
re-test.

## Collected corrections and owners

| Finding | Minimal correction | Owner |
|---|---|---|
| C-1 | Inline TEST-043 through TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, and TEST-098 into SPEC-053 as direct clauses, or shrink the declared set in REQ-081 and CON-985 to the tests the parent states. | cbcl-bus owner, with the selfsame owner for CON-985 |
| H-1 | Give the two ceremony identifiers distinct names, state which one `OfferCoreV2` carries, and add the hub value to the `selfsamepairallocated` acknowledgement. | cbcl-bus owner, with the cbcl-pairing owner |
| H-2 | Add one status-recovery command to CON-012's closed set, and state that a socket-generation change releases or rebinds the active pending record. | cbcl-bus owner |
| H-3 | Give `applicationAccountDisplay`, `platformBindingId`, `returnUri`, and `deviceDid` an exact grammar, an octet bound, and a stated consumer, or remove them from the closed member list. | cbcl-bus owner |
| H-4 | State which credential/v2 object crosses a rendezvous slot, who writes it, and how the slot is addressed. Derive 57,016 and 62,016 from that encoding, or drop both witnesses and the arithmetic row in TEST-1157. | selfsame owner, with the did-crdt owner |
| H-5 | State an explicit slot capacity, a request-buffering bound, and a CORS rule for `crates/selfsame-rendezvous`, rather than preserving properties it lacks. | selfsame owner |
| H-6 | Issue a versioned SPEC-007 revision that carries the credential/v2 narrowing of REQ-812 with its tests, traceability, changelog, and Gate Evidence Record. | selfsame owner |
| H-7 | Replace the cbcl-pairing TEST-050 reference in cbcl-bus TEST-115 with TEST-063, matching selfsame TEST-1156. | cbcl-bus owner |
| M-1 | Scope CON-986's `record_pairing_trust` prohibition to the credential/v2 path, matching TEST-1159, so the SPEC-004 caller at `app_grant.rs:539` survives. | selfsame owner |
| M-2 | Name the function that shows the exact-pair decision and writes the CON-903 row, and place it before `prepare_claimant`'s socket work. | selfsame owner |
| M-3 | Name `WIRE_VERSION`'s namespace as the link record wire, covering both the offer and the grant form of `anuna-ssi` SPEC-001 CON-001. | selfsame owner |
| M-4 | Record the new worst-case resident store in did-crdt REQ-370, and state a concurrent in-flight request bound. | did-crdt owner |
| L-1 | Add closure resolution to CON-902's executor sentence, matching CON-986 and TEST-1158. | selfsame owner |
| L-2 | Record `cbcl-chat-roomcfg:transform-room/1` as the third schema migration outside the transaction inventory. | cbcl-bus owner |
| L-3 | Re-date or restate FM-1 and FM-2 against the pinned baseline, and drop the `PairingUnavailable` stub clause from REQ-903 and TEST-905. | selfsame owner |
| L-4 | Repair the `cbcl_registry.rs` module comment in the change that satisfies CON-903 and CON-986. | selfsame owner |

## Changelog

- **2026-08-24 — Tier-1 adversarial review of the 0.5.1-draft coordinated set.
  REJECT.** One CRITICAL, seven HIGH, four MEDIUM, and four LOW findings. Every
  0.5.0 CRITICAL closes and eleven of sixteen 0.5.0 findings close outright.
  Three of five hard stops close, and F-D stays open on two new defects.
