---
id: spec-008-0.5.8-implementation-reviewer-prompt-2026-08-24
title: SPEC-008 0.5.8 implementation — Tier-1 adversarial reviewer prompt
status: active
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-24
---

# SPEC-008 0.5.8 implementation — Tier-1 adversarial reviewer prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
approve work because it is extensive or because focused tests pass.

Review the coordinated standalone credential/v2 architecture and its actual
test-first implementation. Do not implement or repair the design. Write only the
review report, commit that report in the Circus worktree, and complete through
the appended Circus instruction.

## Authentication and isolation

This review runs through Circus with `ANTHROPIC_API_KEY` removed from the child
environment. Check that the variable is absent without printing a value. Run
`env -u ANTHROPIC_API_KEY claude auth status` and record only these fields:

- `authMethod`
- `apiProvider`
- `subscriptionType`

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier. A different result
is a review-process blocker.

The generating family is OpenAI GPT-5. The reviewing family is Anthropic
Claude. This session receives no generating-session reasoning.

## Exact candidate set

Use `git show COMMIT:path` and `git diff BASELINE..COMMIT`. Do not substitute
uncommitted files.

- Selfsame candidate `ada42dbdf38900977edcf79aab5d7ad05d7e70d4`
  - implementation baseline `0220cec2dec44cd95d4f411ea4814d790b6716d2`
  - `specs/SPEC-008-production-pairing-claimant.md` version `0.5.8-draft`
  - `specs/SPEC-007-cbcl-pairing-cutover.md` version `0.3.6-draft`
- cbcl-bus candidate `bc952995ed65b6b9f004205d0bb844ac80028c8c`
  - implementation baseline `ce894a7f2debc111b610296b1a3780de6084e37e`
  - `specs/SPEC-053-key-root-identity.md` version `0.17.8-draft`
- cbcl-pairing candidate `c1746a5705b3a6a58c4cd079cb5364003a733519`
  - implementation baseline `c22f2d0526432c01c56d64be35a8d4f87516ec92`
  - `specs/SPEC-001-reusable-blind-pairing.md` version `0.5.7-draft`
- did-crdt candidate `94db956d7d5d5edf0f42afad073ab42f265a1752`
  - implementation baseline `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
  - `specs/SPEC-037-pairing-rendezvous-body-bound.md` remains deprecated

The candidate code is local test-first implementation. Production allocation,
release, dependency-pin movement, and deployment remain prohibited.

## Required independent pass

Read the four coordinated parents and the code diffs before any earlier review.
Trace the executable browser, hub, pairing-library, and wallet paths. Treat every
prose claim and test name as untrusted until code or a runnable test supports it.

Check at least these properties:

1. The policy key is exactly `(applicationId, relayOrigin)`. A new application
   prompts even when its relay is already trusted for another application.
2. Every consent screen displays CON-220-authenticated application facts. The
   claimed wire value cannot reach a decision surface without an earlier exact
   comparison.
3. No application key derivation, scope mint, DID publication, grant, alias,
   bundle, or durable identity write occurs before its specified approval.
4. Standalone first contact receives an authenticated fresh scope without prior
   enrolment. No attacker-selected value becomes the derivation authority.
5. The design claims no relay operatorship proof. Per-application consent is the
   explicit mitigation for a profile that names another operator's relay.
6. CPace credential/v2 uses one closed wire grammar and one key schedule across
   independent implementations. Verify every cryptographic preimage byte.
7. The browser proves installation-device possession before the hub signs an
   offer. Verify socket-generation typing, bounds, replay resistance, and order.
8. Caller-carried resolver closure data never becomes resolver authority. The
   hub observes live authority independently and compares the carried replay.
9. Hub final acceptance is one atomic transition. Retries remain byte-identical
   across Mnesia transaction retries and client reconnects.
10. The browser activates only after signed final acceptance. The wallet ACKs
    Receipt only after live resolver and reciprocal WebFinger checks and an exact
    durable pending-to-installed compare-and-swap.
11. Crash, disconnect, timeout, retry, wrong-order, replay, and recovery paths do
    not strand an accepted authority or create a second identity effect.
12. Bounded inputs, counts, expiries, device ceilings, and websocket frame limits
    are executable at their declared maxima and refuse one unit beyond them.
13. The ordinary production surface stays closed. Loopback and fixture paths do
    not leak into ordinary builds. Existing enrolment remains distinct.
14. Tests actually reach each named invariant. Mutate load-bearing predicates
    mentally or experimentally and identify any surviving mutant.

Run the focused suites and any narrower adversarial tests needed. Record exact
commands and outcomes. A collection failure or dependency-resolution failure is
not evidence that a requirement passed.

## Historical re-test

Only after the independent pass, read:

- `specs/trajectory/SPEC-008/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`

Independently re-test 0.5.7 N-1, F-A through F-E, and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Do not inherit any disposition.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`.
The report SHALL contain:

- reviewer identity, auth path, Circus attempt, exact commits, and independence;
- verdict `PASS` or `REJECT`;
- severity counts;
- every finding with severity, code evidence, exploit or failure sequence,
  affected requirement, and measurable correction;
- explicit dispositions for all fourteen required checks;
- explicit dispositions for 0.5.7 N-1, F-A through F-E, and REQ-812;
- test commands and exact results;
- a statement that PASS does not authorize production release or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Missing implementation of
a load-bearing obligation is a finding, even when the candidate fails closed.
Release and deployment controls are outside this review's authority.
