---
id: spec-008-0.5.14-continuous-gate-reachability-remediation-2026-08-25
title: SPEC-008 0.5.14 — continuous-gate reachability remediation
status: owner-authorized-test-first-candidate
tier: 1
date: 2026-08-25
parent: "[[SPEC-008-production-pairing-claimant]]"
review-of: "[[spec-008-0.5.13-implementation-claude-adversarial-review-2026-08-25#Findings]]"
---

# SPEC-008 0.5.14 — continuous-gate reachability remediation

## Decision and authority

The repository owner authorized test-first implementation before review PASS.
The owner also authorized this consolidated reissue.

The selected architecture remains standalone first-contact credential/v2
pairing through cbcl-pairing. It uses live application authentication and
person-owned exact-pair TOFU. It does not reintroduce enrolment or a compiled
relay allowlist.

No part of this artefact authorizes push, production allocation, release, or
deployment. Those actions remain gated by
[[SPEC-008-production-pairing-claimant#CON-985 — Consolidated current authority and gate]].

## Review disposition

The 0.5.13 fresh-context review found zero CRITICAL and zero HIGH defects. It
found no protocol, consent, binding, effect-order, or installation exploit.

The review rejected three unreachable verification gates. Two continuous jobs
stopped before their named credential/v2 checks. The hub selector also allowed
a conditionally skipped rebuild.

This reissue changes verification reachability only. It does not change the
credential/v2 wire, cryptographic context, consent order, scope source, or
installed authority.

## Rust gate correction

`CredentialV2BrowserAllocatorSession::restore_offer_context` has one explicit
Clippy arity disposition. Its sibling restore constructor already uses the
same disposition.

The exact package Clippy command failed before this correction. It passes after
the correction. The clean-closure workspace command remains the release
evidence required by
[[SPEC-008-production-pairing-claimant#TEST-1167 — Continuous Rust and browser jobs reach credential/v2 gates]].

## Browser gate correction

The screen harness now drives the current credential/v2 frontend. It enters an
invitation and a well-formed PAIR1 code before starting the ceremony.

The harness renders three separate states. These are pre-socket application
authentication, new exact-pair relay consent, and authenticated exact-intent
consent.

The Tauri invoke scanner now reads every module prefix from the actual handler
block. It also scans `src/pairing.js`, the current credential/v2 caller.

The corrected screen command reaches a clean result. The continuous browser
job can therefore reach the wallet lifecycle required by
[[SPEC-008-production-pairing-claimant#TEST-1162 — Pre-payload failure and confirmed abandonment release one exact slot]].

## Hub rebuild-selector correction

The hub vendor suite now rejects any step-level `if:` condition on the exact
browser rebuild step. Its mutation list includes `if: false`.

The same suite directly requires `x86_64-unknown-linux-gnu`, rustc 1.96.0, and
wasm-bindgen 0.2.126. A skipped rebuild cannot leave those provenance fields
unchecked.

The coordinated current law is cbcl-bus `SPEC-053#TEST-120`.

## Exact implementation closure

- Selfsame implementation:
  `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`
- cbcl-bus implementation:
  `c9e0838cf39e05cac00c292ed8077bf1069c5685`
- cbcl-pairing:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `23e14bf658321b49f759e43d002797e2f3cb3184fbe460c99b0b8f580ab5e781`

## Retained LOW findings

Three LOW findings remain explicit. An orphan issuer publication remains
non-authorizing. The recovery limiter remains proxy-shared and affects
availability only.

The runtime fault matrix still combines production source-order checks with
transaction-level failure injection. Arm and disarm mutations fail executable
tests. A later test-double refactor can drive the complete production command.

These residuals do not widen authority. They do not block a Tier-1 PASS under
the accepted zero-CRITICAL, zero-HIGH, and zero-MEDIUM threshold.

## Executed evidence and next gate

The red phase reproduced all three MEDIUM findings. The corrected package
Clippy command passes. The corrected screen suite renders all credential/v2
states and reports no dangling invoke.

The hub vendor suite passes six tests. Its `if: false` mutant fails before the
correction and is refused after it.

A fresh cross-model Tier-1 review SHALL verify the exact closure above. It
SHALL use Circus and the interactive Claude subscription driver.

`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent. PASS still
authorizes no push, production allocation, release, or deployment.
