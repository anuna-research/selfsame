---
id: spec-008-0.5.9-implementation-claude-adversarial-review-2026-08-24
title: SPEC-008 0.5.9 remediation — Tier-1 adversarial implementation review
status: complete
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
generating-family: OpenAI GPT-5
date: 2026-08-24
verdict: REJECT
---

# SPEC-008 0.5.9 remediation — Tier-1 adversarial implementation review

## Reviewer identity, authentication path, Circus attempt, and independence

**Reviewer family:** Anthropic Claude (model `claude-opus-5`). The generating
family for the candidate implementation is OpenAI GPT-5. This session received
no generating-session reasoning, no generating-session transcript, and no
hand-off notes.

**Authentication path.** `ANTHROPIC_API_KEY` was verified absent from the
child environment without printing any value (`if [ -z "${ANTHROPIC_API_KEY+x}" ]`
reported `unset in parent env`). `env -u ANTHROPIC_API_KEY claude auth status`
returned:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

All three match the required values. No email, organization identifier, token,
key, or account identifier is recorded here. This is not a review-process
blocker.

**Circus attempt.** `spec008-remediation-review/1`, worktree
`/Users/anuna-01/Code/selfsame-spec008-remediation-review-1`, branch
`circus/spec008-remediation-review/1`, integration ref `48dd5b5`. The worktree
HEAD differs from the Selfsame candidate `d01959f` by exactly one added file
(this review's own prompt, 157 lines of markdown), verified with
`git diff --stat d01959f..48dd5b5`; no code differs.

**Independence.** The four coordinated parents and all four candidate diffs
were read before any earlier review report was opened. The
0.5.8/0.5.7/REQ-909 reports were read only after the independent pass, solely
to produce the historical re-test dispositions in the section so titled. No
disposition below is inherited: every one was re-derived from the candidate
source, and two of the four 0.5.8 findings are re-opened on new evidence.

## Exact commits reviewed

Every read used `git show COMMIT:path` or `git diff BASELINE..COMMIT` against
committed objects. No uncommitted file in any repository was substituted.

| Repository | Baseline | Candidate | Prior review candidate |
|---|---|---|---|
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | `d01959f877d77d50a5c32f830664bd223ae9ebd8` | `ada42dbdf38900977edcf79aab5d7ad05d7e70d4` |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | `79423c433e636ccf9c9cf83f6574c6e40c481715` | `bc952995ed65b6b9f004205d0bb844ac80028c8c` |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | `aedbc4ca07068c3cf6356d95333f4c2ec0d374d9` | `c1746a5705b3a6a58c4cd079cb5364003a733519` |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | `94db956d7d5d5edf0f42afad073ab42f265a1752`* | — |

*The did-crdt candidate changes only `src/service/handlers.rs`,
`src/service/server.rs`, and the deprecated `SPEC-037` document (174 insertions,
28 deletions, verified with `git diff --stat 1f409a42..94db956d`). `service` is
a non-default cargo feature and Selfsame consumes did-crdt with
`default-features = false`, so this candidate contributes no compiled byte to
the wallet, the browser WASM, or the hub. `SPEC-037` remains deprecated, as the
candidate set requires.

Parent specification versions confirmed at the candidates: SPEC-008
`0.5.8-draft`, SPEC-007 `0.3.7-draft`, cbcl-bus SPEC-053 `0.17.8-draft`,
cbcl-pairing SPEC-001 `0.5.7-draft`.

**Concurrent uncommitted work observed (disclosure).** At session start the
main `selfsame` checkout carried tracked modifications under
`crates/selfsame-app-identity*`, and `did-crdt` carried tracked modifications
under `src/service`, `src/sync`, and `tests/`. `cbcl-rs` carried tracked
modifications under `crates/cbcl-core` and elsewhere. None of that work was
read as candidate material; every build and test below ran in clean detached
clones staged in a scratch directory outside all four repositories. No
candidate repository was modified by this review.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 2 |
| MEDIUM | 3 |
| LOW | 2 |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Two HIGH and three
MEDIUM findings survive verification.

The remediation is substantial and much of it is correct: the credential/v2 key
schedule and wire grammars are closed and independently oracle-checked, the
five-function claimant split holds, exact-pair relay policy is exactly keyed,
the browser device-possession proof is type-enforced ahead of offer signing,
`selfsamepairstatus`/`selfsamepairabort` now exist end to end, the CON-989
HTTPS recovery route now exists on all three sides, the cbcl-pairing pin guard
is executable, and the vendored WASM rebuilds byte-identically. The findings
below are specific defects inside that work, not a judgement on its extent.

---

## Findings

### H-1 — A restored browser allocator cannot reopen the relay at either bootstrap checkpoint that holds a cached frame; restart recovery is broken at two of the four pre-Finished crash windows

**Severity: HIGH.** Owner: cbcl-pairing repository owner (defect), cbcl-bus
repository owner (unreached client path).

**Affected requirements.** [[SPEC-008-production-pairing-claimant#CON-986]];
[[SPEC-008-production-pairing-claimant#TEST-1156]] ("After credential/v2
allocation socket loss, reconnect with a new socket generation. Recover the
original request and carrier ceremony under the persisted installation key.
Require one correlated result and no duplicate durable effect.");
[[SPEC-008-production-pairing-claimant#TEST-1158]] ("Restart from each later
boundary and require only the cached frame, exact final capability, and one
installed replacement."); cbcl-bus SPEC-053 CON-012 / REQ-082 reconnect clause.
This is the exact obligation the 0.5.8 H-1 finding named, and the exact
mechanism this candidate adds to satisfy it.

**Code evidence.** `cbcl-pairing` keeps two distinct cached-frame slots.
`CredentialV2RelayState.cached_outbound` (`src/credential_v2/bootstrap.rs:35`)
is written only by `cache_application_frame`
(`src/credential_v2/bootstrap.rs:106-119`), which refuses anything that is not
a `CredentialV2Frame::Sealed`. Pre-Finished bootstrap frames — the allocator
CPace share and the allocator Finished value — go through
`queue_bootstrap_frame` (`src/credential_v2/bootstrap.rs:74-86`), which
increments the local sequence and sets `awaiting_ack` but never populates
`relay.cached_outbound`. The frame itself is retained in a *second* field,
`CredentialV2AllocatorBootstrap.cached_outbound`
(`src/credential_v2/bootstrap.rs:168`, set at `:268` for the share and `:332`
for Finished) and is read back through
`CredentialV2AllocatorBootstrap::cached_outbound_frame`
(`src/credential_v2/bootstrap.rs:242`).

The new `reopen_effects` (`src/credential_v2/allocator.rs:414-445`) mixes the
two. For the `Bootstrap` arm it takes the frame from
`bootstrap.cached_outbound_frame()` (`:419`) but takes the sequence number from
`relay.cached_application_sequence()` (`:439`), and
`cached_application_sequence` (`src/credential_v2/bootstrap.rs:122-129`)
returns `Err(CredentialV2Error::Phase)` whenever
`self.cached_outbound.is_none()` — which, for every bootstrap frame, is always.

Both affected checkpoints are real production states. `peer_frame`'s
`Allocated` arm queues the CPace share and immediately seals a checkpoint
(`src/credential_v2/allocator.rs:496-522`, generation 2, bootstrap phase
`ShareSent`), and `local_acknowledged`'s `ShareSent` arm queues Finished and
seals another (`:552-573`, generation 3, phase `FinishedSent`).

**Failure sequence (executed, not argued).** A clean detached clone of
cbcl-pairing at `aedbc4c` was given a new integration test that drives the
allocator to each bootstrap checkpoint, restores from the exact sealed
checkpoint with the exact carrier, generation, profile digest and wrapping key,
and then delivers the relay `Welcome` that recovery depends on:

```
ShareSent    bootstrap_phase = Some(ShareSent)     reopen = Err(Phase)
FinishedSent bootstrap_phase = Some(FinishedSent)  reopen = Err(Phase)
```

`CredentialV2AllocatorSession::restore` succeeds; `start()` correctly emits
`Bind`; the `Welcome` that should produce `Open` plus the exact cached `Put`
returns `Err(Phase)` instead, at both phases. The session is left unusable.

This reaches the browser unmitigated. `selfsame-web-device`'s
`restored_phase()` (`crates/selfsame-web-device/src/lib.rs`, added in this
candidate) maps these states to the strings `"share-sent"` and
`"finished-sent"`, and the browser controller explicitly admits both
(`apps/cbcl_chat/priv/web/credential-v2-allocator.mjs:804-805`:
`['allocated', 'claimed', 'share-sent', 'finished-sent', 'begin']`). `app.js`
restores from whatever generation was last persisted
(`apps/cbcl_chat/priv/web/app.js:1915-1927`). The first relay message after
restart therefore reaches `protocol.receive` (`app.js:1984`), the WASM raises
`JsError`, and `onRelayMessage` throws out of the controller. A person whose
tab reloaded, laptop slept, or network flipped during the ~2 relay round trips
between mailbox allocation and Finished is permanently stranded for that
ceremony: the hub pending row still holds the old socket generation, the WASM
refuses to resume, and `persist_pending`-equivalent browser state still names
the ceremony.

It fails closed — no identity effect, no capability, no duplicate — which is
why this is HIGH and not CRITICAL.

**Why the candidate's own tests miss it.** `tests/credential_v2_allocator_session.rs`
exercises exactly two restore points: bootstrap phase `Allocated`
(generation 1, where `cached_outbound_frame()` is `None`, so the broken branch
is never taken) and the established endpoint with a cached *sealed* offer
(where `relay.cached_outbound` genuinely is `Some`). The browser test
`credential-v2-allocator.test.mjs` restores with `restoredProtocolPhase:
'prepared'` only. No test in any repository restores at `share-sent` or
`finished-sent`. This is a live surviving mutant in the exact area the
remediation was written for.

**Measurable correction.** In `reopen_effects`, take the sequence from the same
owner as the frame: for the `Bootstrap` arm use the bootstrap's own retained
frame together with `relay.next_local_sequence - 1` while `relay.awaiting_ack`
is true (or add a `cached_bootstrap_sequence` accessor with that rule), and
keep `cached_application_sequence` for the `Established` arm only. Add
integration tests that restore and reopen at bootstrap phases `Allocated`,
`Claimed`, `ShareSent`, and `FinishedSent`, asserting `Open` plus a `Put` whose
body is byte-identical to the frame originally released at that phase and whose
`seq` equals the original, and add the corresponding browser-level test with
`restoredProtocolPhase: 'share-sent'`.

---

### H-2 — REQ-1006's entire installed-state lifecycle is unimplemented: no reload verification, no unlink, no handle-change refusal, no rotation prompt, and no consumer of the installed record at all

**Severity: HIGH.** Owner: selfsame repository owner.

**Affected requirements.** [[SPEC-008-production-pairing-claimant#REQ-1006]]
(fourth through sixth paragraphs);
[[SPEC-008-production-pairing-claimant#TEST-1159]] ("Reload an installed link
under unchanged, profile-refresh, authority-rotation, issuer-rotation,
unavailable, revoked, handle-change, confirmed-unlink, and hub-deleted states.
Require every REQ-1006 outcome.");
[[SPEC-008-production-pairing-claimant#TEST-1156]] (its first four inline
regression groups); [[SPEC-008-production-pairing-claimant#CON-989]] ("Only
verified `not-finalized`, explicit unlink, or root purge clears a post-payload
pending slot"; "Only explicit unlink or root purge can then clear it").

**Code evidence.** REQ-1006 states five separate obligations over the installed
record. Every one is absent from the candidate:

1. *"Reload SHALL verify the installed grant, current profile, authority,
   issuer, and hub status before granting application capability."*
   `InstalledCredentialV2Link` (`src-tauri/src/cbcl_v2_completion.rs:604-830`)
   exposes exactly four methods: `from_authenticated_receipt` (`:613`),
   `account()` (`:718`), `issuer_did()` (`:722`), and a private `validate()`
   (`:726`). There is no reload, revalidation, or capability-granting method.
   `git grep "InstalledCredentialV2Link::"` over `src-tauri/src` and `crates`
   returns exactly two call sites, both `from_authenticated_receipt`
   (`cbcl_v2_commands.rs:620` in `cbcl_v2_finish`, and `:807` in
   `recover_claimant_completion`). The installed record is written by
   `install()` (`cbcl_v2_completion.rs:1119`) and **never read back** by any
   code path.
2. *"Hub or resolver unavailability SHALL be distinct from revocation or
   absence and SHALL NOT clear the installed record."* Unreachable: no reload
   path exists to classify.
3. *"A profile-only digest refresh ... MAY re-pin silently ... Authority or
   issuer rotation SHALL prompt the person. A changed installed handle SHALL
   refuse."* `accepted_rotation`/`negative_rotation`
   (`cbcl_v2_commands.rs:895-925`) implement the rotation prompt **only** for
   the CON-989 recovery adapter, never for reload. No re-pin path exists. The
   handle-change terminal named by TEST-1156
   (`account-device-handle-change-refused`) does not appear anywhere in the
   Selfsame candidate: `git grep account-device-handle-change-refused
   d01959f -- src-tauri crates src` returns nothing.
4. *"Unlink SHALL require explicit confirmation and remove only the application
   record, exact-pair relay policy selected by the person, and application-owned
   cached material."* There is no credential/v2 unlink command. The registered
   command set (`src-tauri/src/lib.rs:100-107`) is `cbcl_v2_recognise`,
   `cbcl_v2_relay_decide`, `cbcl_v2_preliminary_decide`, `cbcl_v2_final_decide`,
   `cbcl_v2_finish`, `cbcl_v2_pending_recoveries`, `cbcl_v2_recover`,
   `cbcl_v2_cancel`. `commands::unlink_device` is the credential/v1 device
   unlink and touches no credential/v2 slot. `cbcl_v2_policy::remove` (the
   exact-pair row remover, `src-tauri/src/cbcl_v2_policy.rs:158`) has no
   caller anywhere in the candidate.
5. *"Re-grant after hub-side deletion requires a complete fresh pairing
   ceremony."* Cannot be exercised, because the installed record is never
   consulted to decide whether a link exists.

**Failure sequence.** A person completes a full credential/v2 ceremony. The
record is installed. They then want to unlink the application, or the
application rotates its account authority, or the hub deletes the grant. In
every case the wallet has no command to reach that state: the record is inert
storage. Worse, `persist_pending` refuses to write a new pending record when
any slot already exists for that application
(`cbcl_v2_completion.rs:951-970`, returning `PairingApplicationAlreadyLinked`),
and the only two clear paths are `remove_pending` — reachable solely from a
verified `not-finalized` inside `recover_claimant_completion`
(`cbcl_v2_commands.rs:855-862`) — and `purge_all_links`, reachable only from
the whole-root purge at `src-tauri/src/commands.rs:617`. So the person's only
route out of *any* installed or wedged credential/v2 slot is to destroy their
entire Selfsame root. That directly contradicts REQ-1006's "It SHALL retain the
root seed."

The same gap makes CON-989's own terminal statement unimplementable: after the
hub's replay tombstone lapses and recovery returns `unknown`, CON-989 says
"Only explicit unlink or root purge can then clear it" — and explicit unlink
does not exist.

This fails closed in the capability direction (no capability is ever granted
from an installed record, because nothing reads it) and is therefore HIGH
rather than CRITICAL. It is nonetheless a load-bearing missing obligation with
a full verifying test that cannot be run, which the review contract requires be
reported as a finding regardless of failing closed.

**Measurable correction.** Add `cbcl_v2_installed_links`,
`cbcl_v2_reload_verify(application_id, ...)`, and
`cbcl_v2_unlink(application_id, confirmation, passcode)` Tauri commands;
implement `InstalledCredentialV2Link::verify_reload` covering the nine
TEST-1159 states with the exact REQ-1006 outcomes (silent atomic re-pin for a
profile-only digest change; person prompt for authority or issuer rotation;
`account-device-handle-change-refused` with zero enrollment frames for a handle
change; typed unavailability that does not clear the record; complete fresh
ceremony after hub deletion); make unlink remove the slot, its index row, the
person-selected `(applicationId, relayOrigin)` row via
`cbcl_v2_policy::remove`, and application-owned cached material only, while
retaining the root seed and claiming no remote revocation. Add TEST-1159 and
the four TEST-1156 inline regression groups as executable tests.

---

### M-1 — The hub signs a `not-finalized` recovery status without authenticating the recovery token; the commitment it signs is echoed from the caller's own request and compared against nothing

**Severity: MEDIUM.** Owner: cbcl-bus repository owner.

**Affected requirements.** cbcl-bus SPEC-053 CON-036 ("The token is the raw
Selfsame SPEC-008 0.5.8-draft CON-989 value... A wrong token receives the same
status, body size class, and timing class as an unknown ceremony... The
endpoint accepts no carrier, profile, offer, grant, status JWS, digest,
account, scope, device, issuer, or caller-selected result");
[[SPEC-008-production-pairing-claimant#CON-989]];
[[SPEC-008-production-pairing-claimant#TEST-1161]] ("Mutate the token,
commitment, ... Require no installation ... and preservation of the sealed
pending slot").

**Code evidence.** Two branches of `recovery-status-tx`
(`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-v2-store.lfe:871-881`) treat the
caller's derived commitment very differently.

`recovery-with-final` (`:883-896`), the *accepted* branch, does the right
thing: it requires application-ID equality and a constant-time comparison of
the caller's commitment against the stored
`cbcl-selfsame-v2-final-recovery-commitment`, returning `unknown` on mismatch.

`recovery-without-final` (`:898-923`), the branch that produces `in-progress`
and the signed `not-finalized`, checks only

```lfe
(if (=/= (maps:get 'application-id facts)
         (cbcl-selfsame-v2-pending-application-id pending))
  'unknown
  ...)
```

and then, for the negative, places
`'receipt-recovery-commitment (maps:get 'receipt-recovery-commitment facts)` —
the caller's own value — into the map that
`cbcl-chat-enrolment-signer:recover-credential-v2`
(`apps/cbcl_chat/src/shell/cbcl-chat-enrolment-signer.lfe:210-226`) signs under
a current live profile key. Nothing in that path compares the commitment to any
stored value, and the pending record has no commitment column at all
(`pending-attributes`, `:124-135`) — the commitment only becomes durable at
acceptance, in `new-final-row` (`:686-712`).

**Exploit sequence.** `carrierCeremonyId` is not a secret: it is a plain member
of the public machine carrier (cbcl-pairing `encode_carrier`,
`src/credential_v2/carrier.rs:167-193`), so anyone who photographs or scans the
QR, or who operates the relay, knows it. Such a party generates any 32-octet
value as a "token", POSTs the closed CON-036 request to
`/selfsame/pairing/v2/status`, and — for a ceremony that reached
`allocated`/`prepared`/`offered`/`authority-clear` and passed its
`pendingExpiresAt` — receives HTTP 200 with a live-profile-signed
`recovery-status` object binding `applicationId`, `carrierCeremonyId`, their
own commitment, and `observedAt`. Two consequences follow. First, the hub is an
unauthenticated signing oracle for a bounded authority object whose 32-octet
commitment field the caller chooses (within the SHA-256 image). Second, and
concretely, the response class is a distinguisher on ceremony outcome from a
public identifier alone: `202 in-progress` (live), `200 not-finalized`
(expired without finalizing), `404 unknown` (finalized-but-wrong-token, or
never existed). That is exactly the observable CON-036's "A wrong token
receives the same status, body size class, and timing class as an unknown
ceremony" exists to suppress; the sentence is scoped in CON-036 to the
final-status case, but the property it protects is defeated in the pre-final
case.

It does not let an attacker install anything in, or clear anything from, a
victim wallet: `recognise_recovery_not_finalized_status`
(`crates/selfsame-pairing/src/credential_v2/recovery.rs:378-434`) reconstructs
the exact signed core with the wallet's *own* locally recomputed commitment
(`recovery_negative_core`, `:437-471`) and refuses any other. That containment
is why this is MEDIUM and not HIGH — the missing check is on the hub's side of
a boundary the wallet independently re-establishes.

**Measurable correction.** Store the wallet's `receiptRecoveryCommitment` on
the pending row at the point the browser first supplies it (it already crosses
the wire in `selfsamepairaccept`, and is validated in
`projection-matches-pending?` at `:592-624`; carry it earlier, or add a
dedicated column set at `finalize`), and require constant-time equality in
`recovery-without-final` exactly as `recovery-with-final` does, returning the
byte-identical `unknown` body on mismatch. Add store tests that drive a wrong
token at each of `allocated`, `prepared`, `offered`, and `authority-clear`,
before and after `pendingExpiresAt`, and require `unknown` in every case.

---

### M-2 — The hub's mutating accept transaction has no expiry gate, so a signed `not-finalized` can be issued while a live pending transaction is still able to finalize

**Severity: MEDIUM.** Owner: cbcl-bus repository owner.

**Affected requirements.** cbcl-bus SPEC-053 CON-036 ("The hub signs a negative
only when final status is absent and the locked pending state cannot finalize.
That state is absent or terminal after its finalization window");
[[SPEC-008-production-pairing-claimant#CON-989]] ("The hub SHALL also prove
under one lock that no final status exists. No live pending transaction can
remain able to finalize");
[[SPEC-008-production-pairing-claimant#TEST-1161]] ("Return a correctly signed
`not-finalized` only after the hub's locked terminal-absence predicate").

**Code evidence.** Expiry is enforced in `accept-context-tx`
(`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-v2-store.lfe:497-518`):

```lfe
('authority-clear
 (if (>= now (cbcl-selfsame-v2-pending-pending-expires-at rec))
   (mnesia:abort 'expired)
   (accept-context-map rec now)))
```

The mutating transaction is a *separate* one. `accept-with` (`:534-556`) runs
`accept-context` (one transaction), then the out-of-transaction Rust verifier,
then `mnesia:transaction(accept-tx)`. `accept-tx` (`:570-584`) and `accept-new`
(`:625-660`) contain no comparison of `now` against `pending-expires-at` at
all: reaching phase `authority-clear` is sufficient to write the account row,
the immutable final row, the WebFinger provision, and the device-grant
migration.

Meanwhile `recovery-without-final` (`:898-923`) signs the negative on exactly
the complementary predicate:

```lfe
((andalso (>= now expires) (recoverable-live-phase? phase)) (tuple 'not-finalized ...))
```

with `recoverable-live-phase?` (`:925-926`) including `authority-clear`.

The two are not serialised against each other. `recovery-status-tx` takes a
whole-table write lock on `cbcl-selfsame-v2-pending` (`:872`), which serialises
it against `accept-tx` — but not against the earlier `accept-context-tx`, which
takes only a record-level `'read` lock. And the WebSocket handler puts a
*network* operation between them: `do-selfsamepairaccept`
(`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:1908-1929`) calls
`path-b-sidecar` (`:826-829` → `cbcl-chat-path-b-resolver:resolve`) between
`accept-context` and `accept`, which spawns an external resolver executable
bounded only by that module's 5-second port timeout.

**Failure sequence.** The hub's `pendingExpiresAt` is
`min(carrierExpiry, now + 600)` (`cbcl-core-selfsame-v2-pending.lfe:38-40`),
and the wallet's HTTPS recovery refuses until `now >= carrier.relay_expires_at()`
(`src-tauri/src/cbcl_v2_commands.rs:700-707`). With the relay's fixed v2 TTL of
900 seconds (`cbcl-pairing/src/mailbox.rs:14,363-373`) the two instants are
normally ~300 seconds apart, which is what keeps the window shut in the common
case. But the gap is `300 - Δ` where `Δ` is the elapsed time (plus hub/relay
clock skew) between the relay's `AllocatedV2` and the hub's `selfsamepairallocate`;
`decide-allocation` only rejects `Δ > 300`. With `Δ` approaching 300 — a stalled
client, or clock skew between the relay host and the hub host — the gap
collapses toward zero, and the ~5-second resolver window straddles it. Then:
the browser's `accept-context` passes at `t < expires`; the resolver runs; the
wallet's recovery POST lands at `t' >= expires` and takes the pending write
lock, sees phase `authority-clear`, and returns a validly signed
`not-finalized`; the wallet verifies it, applies the rotation prompt if needed,
and calls `remove_pending` (`cbcl_v2_commands.rs:861`), erasing the sealed
post-payload slot; then `accept-tx` commits, creating the account row, the
final row, the WebFinger binding and the migrated device grant.

The result is an asymmetric install with no remedy: the person's account,
published DID, and grant exist at the hub, and the wallet has irreversibly
deleted the only state from which it could install. Because H-2 leaves no
unlink and no reload path, the wallet cannot even represent the situation.

The narrowness of the timing coincidence is why this is MEDIUM. The missing
obligation is not narrow: CON-036 and CON-989 both require the terminal-absence
predicate to be *proved under lock*, and the implementation instead relies on
an incidental 300-second margin established in a different module.

**Measurable correction.** Move the expiry check inside `accept-tx` — re-read
`pending-expires-at` under the same write lock that performs the writes and
abort with `expired` when `now >= expires` — so the mutating transaction and
the recovery transaction decide the same predicate under one lock. Add a store
test that interleaves a delayed `accept` (context taken before the deadline,
transaction attempted after it) with `recovery-status`, and require that
exactly one of "accepted" and "not-finalized" is reachable.

---

### M-3 — The pin remediation closes cbcl-pairing only; the coordinated set now carries two disagreeing `cbcl-rs` pins and three disagreeing `did-crdt` pins, and Selfsame's own CI cannot build the candidate

**Severity: MEDIUM.** Owner: selfsame repository owner. This is the residual of
0.5.8 M-1, partially remediated.

**Affected requirements.** Required check 12 of this review ("Dependency pins
name the exact cbcl-pairing candidate in Selfsame, cbcl-bus, SPEC-007 ADR-802,
and the reproducible WASM provenance. Ordinary builds must reject a mismatched
or dirty sibling"); [[SPEC-007-cbcl-pairing-cutover#ADR-802]];
[[SPEC-008-production-pairing-claimant#TEST-1157]] (pointer resolution).

**What was fixed, and verified.** The new `crates/selfsame-pairing/build.rs`
compares `git -C ../cbcl-pairing rev-parse HEAD` against `cbcl-pairing.sha` and
requires a clean tracked tree. Both halves are executable — mutation-tested in
a clone:

```
# dirty sibling
cbcl-pairing source integrity check failed: the pinned checkout has tracked modifications.
# mismatched revision
cbcl-pairing source integrity check failed: expected aedbc4ca…, found c1746a57….
```

All four cbcl-pairing pointers now agree on `aedbc4ca07068c3cf6356d95333f4c2ec0d374d9`:
Selfsame `cbcl-pairing.sha`, cbcl-bus `cbcl-pairing.sha`, SPEC-007 ADR-802
(`specs/SPEC-007-cbcl-pairing-cutover.md:481`), and the WASM provenance
`cbclPairingSha`.

**What is not fixed.** The same class of drift now sits on the two remaining
path-dependency siblings, which are equally compiled into the wallet and the
vendored WASM.

| Sibling | Selfsame's pin | cbcl-bus's pin | Review candidate set | Build guard in Selfsame |
|---|---|---|---|---|
| cbcl-pairing | `aedbc4ca…` | `aedbc4ca…` | `aedbc4ca…` | yes (verified) |
| cbcl-rs | `cbcl-rs.sha` = `1c6fa8f5…` | `cbcl-rs.sha` = `febc6691…` | — | **none** |
| did-crdt | `crates/selfsame-core/tests/vectors.rs` → `fbccfd58…` | `did-crdt.sha` = `1f409a42…` | `94db956…` | **none** |

`cargo tree -p selfsame-web-device --target wasm32-unknown-unknown` shows the
WASM links **two** copies of `cbcl-core`: one from the git pin `febc6691` (via
`selfsame-pairing`'s explicit `cbcl-pairing-core` dependency,
`crates/selfsame-pairing/Cargo.toml:17`) and one from the *path* sibling
`../cbcl-rs/crates/cbcl-core` (workspace dependency, `Cargo.toml:51`). The path
copy's revision is whatever is checked out. Between `1c6fa8f5` and `febc6691`
that tree changes by 14,476 insertions across 42 files
(`git diff --stat 1c6fa8f5..febc6691 -- crates/cbcl-core crates/cbcl-parser`).

`.forgejo/workflows/ci.yml` clones `../cbcl-rs` at `cbcl-rs.sha` (`:59,73-79`)
and `../did-crdt` at the revision grepped out of `vectors.rs` (`:57,65-71`),
then runs `cargo clippy --workspace --all-targets --locked` (`:161`),
`cargo test --workspace --locked` (`:170`), and
`cargo build -p selfsame-core --target wasm32-unknown-unknown --locked` (`:178`).
With `../cbcl-rs` at Selfsame's own declared pin `1c6fa8f5`, the committed
`Cargo.lock` no longer describes the graph — `cbcl-core` loses its `sha2 0.10.9`
dependency — and cargo refuses:

```
error: cannot update the lock file … because --locked was passed to prevent this
```

**Selfsame CI at candidate `d01959f` therefore fails at its first cargo step.**
Without `--locked` the same build succeeds silently and rewrites `Cargo.lock`,
producing a *different* WASM binary from the same Selfsame commit — verified:
`cargo build -q -p selfsame-web-device --target wasm32-unknown-unknown --release`
with `../cbcl-rs` at `1c6fa8f5` exits 0 with no warning.

The vendored WASM itself is sound: cbcl-bus's
`scripts/check-selfsame-wasm-rebuild.sh` stages all four trees at *its own*
pins at a canonical absolute path with a pinned `CARGO_HOME`, and this reviewer
ran it end to end against clean clones — `Selfsame WASM rebuild matches the
exact vendored package`, all four vendored files byte-identical, exit 0. That
script also checks all four siblings for pin match and cleanliness
(`:39-56, 273-291`). The defect is that Selfsame's own repository disagrees
with those pins and enforces none of them.

**Measurable correction.** Add `did-crdt.sha` to Selfsame; set both
`cbcl-rs.sha` and `did-crdt.sha` to the exact revisions the coordinated release
is built from (`febc6691…` and `1f409a42…`, or the intended did-crdt candidate
if `94db956…` is meant to be in the closure — in which case cbcl-bus's
`did-crdt.sha` and the WASM provenance must move too); extend the
`selfsame-pairing/build.rs` guard, or add equivalent guards, to `../cbcl-rs`
and `../did-crdt`; make `crates/selfsame-core/tests/vectors.rs` read the pin
file rather than being a second source of truth; and re-run
`cargo test --workspace --locked` in CI against those pins to prove the lock
file and the pins agree.

---

### L-1 — A mid-chain post-approval failure still leaves an orphaned DID publication, and now also wedges the application's only wallet slot

**Severity: LOW (residual, unchanged in kind; worsened in consequence).**
Owner: selfsame repository owner.

**Affected requirement.** [[SPEC-008-production-pairing-claimant#CON-986]]
("Compensation is limited to effects created by this ceremony");
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] final paragraph.

`cbcl_v2_final_decide` (`src-tauri/src/cbcl_v2_commands.rs:479-520`) publishes
the issuer DID via `publish_issuer_identity` and only then resolves and
verifies the closure. A refusal at `resolve_closure` or the closure checks
leaves a published, unused `did:crdt` document with no compensating unpublish.
The 0.5.8 disposition of this residual is unchanged by the candidate.

What is new is that the wedged pending slot now has no bounded remedy.
`pending_application_ids` (`src-tauri/src/cbcl_v2_completion.rs:978-1022`)
lists only slots in `PendingCredentialV2Stage::PayloadPrepared` whose relay
window has closed, so a slot stuck at `IssuerCreated` or `Provisioned` is never
offered for recovery; and even if `cbcl_v2_recover` were invoked for it,
`with_receipt_recovery_token` (`cbcl-pairing/src/credential_v2/claimant.rs:347-360`)
requires endpoint phase `PayloadSent` and refuses. Because `persist_pending`
(`:951-970`) rejects a fresh ceremony while any slot exists, and no unlink
command exists (H-2), the application becomes permanently unpairable in that
wallet short of a full root purge.

**Consequence and operational treatment (explicit, as required for a LOW
residual to remain).** No capability is granted, no pre-existing identity or
operator withdrawal state changes, and no second identity effect occurs. The
published DID is an unreferenced resource-and-privacy residual. The wedged slot
is an availability residual whose only current operator remedy is root purge;
implementing H-2's unlink command reduces it to "one explicit person-confirmed
unlink." Until then this residual must be disclosed in release notes as a
known non-recoverable-without-purge state.

**Correction (non-blocking).** Add a bounded compensation for pre-payload
pending stages: expose them through `pending_application_ids` (or a sibling
listing) with a distinct outcome class, allow explicit person-confirmed
abandonment, and record the orphaned DID for a later tombstone pass.

---

### L-2 — The CON-036 anonymous limiter is keyed on the TCP peer address, so a reverse-proxied deployment shares one bucket across every wallet

**Severity: LOW.** Owner: cbcl-bus repository owner.

`cbcl-chat-selfsame-application-http:peer-ip/1` uses `cowboy_req:peer`, and
`cbcl-chat-selfsame-v2-recovery-limit:decide/6` keys its fixed 60-second /
30-request window on that binary
(`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-v2-recovery-limit.lfe:70-101`).
If the hub sits behind a TLS-terminating proxy, every wallet in the world
shares one key, and 30 recovery attempts per minute exhaust the route for
everyone; each refusal is an empty `no-store` 503, which the wallet correctly
maps to the non-destructive `unavailable` outcome
(`crates/selfsame-app-identity-net/src/pairing_status.rs:76-88`;
`src-tauri/src/cbcl_v2_commands.rs:778-780`).

**Consequence and operational treatment.** Fail-closed and non-destructive: no
pending slot is cleared and no capability is granted, so this is availability
only. Operationally, a deployment behind a proxy must either terminate TLS on
the hub, or configure the limiter's `selfsame_v2_recovery_rl_limit` to account
for the shared key, or derive the key from a trusted forwarded-for header.
Recorded as accepted only with that treatment stated.

---

## Dispositions for all fifteen required checks

**1. Relay consent keyed exactly by `(applicationId, relayOrigin)`; displayed
application facts come from the live authenticated profile.** **PASS.**
`src-tauri/src/cbcl_v2_policy.rs` read in full: `ExactPairRow` carries both
fields; `state`/`insert`/`remove` binary-search the exact tuple; the encoding
is canonical, strictly ordered, bounded at 256 rows, and re-serialisation
equality is required on load. No relay-only lookup, wildcard, or fallback
exists. `RelayConsentView` (`src-tauri/src/cbcl_v2_claimant.rs:22-27`) projects
only `fetched.profile.application_id` and `carrier.relay_origin()` — the live
CON-220-fetched profile and the carrier value that
`recognise_profile_candidate` (`:152-176`) has just required to match exactly
one profile descriptor. The consent display itself is built only by
cbcl-pairing's private `CredentialV2Display`, whose construction and mutation
are compile-fail-guarded (`src/credential_v2/display.rs:575,585`), and the
Selfsame verifier returns a closed verdict only
(`CredentialV2WalletOfferVerifier`, `crates/selfsame-pairing/src/credential_v2.rs:309-420`).

**2. No derivation, scope mint, publication, grant, alias, bundle, or durable
identity effect before its specified person approval; fresh first-contact scope
has authenticated non-attacker-selected authority.** **PASS.**
`cbcl_v2_final_decide` (`src-tauri/src/cbcl_v2_commands.rs:261-571`) was traced
line by line against CON-986's declared order: decline short-circuits before
any custody call; approval takes passcode + backup confirmation, re-reads the
clock against the signed `expiresAt`, re-derives and byte-compares the preview
DID and fingerprint inside custody, seals the endpoint checkpoint, persists the
sealed `PendingCredentialV2Completion` (`:415`) *before* any signature, seals
again on the relay acknowledgement, and only then creates the issuer,
publishes, resolves and verifies the closure, and builds the grant and payload.
The preliminary path performs one zeroizing derivation and pure DID/fingerprint
computation. Scope comes from the hub-signed offer's `accountScopeId`, which
`write-new` in the hub mints from `crypto:strong_rand_bytes(32)`.

**3. The design claims no relay-operatorship proof; exact per-application
consent is the explicit mitigation.** **PASS (honest residual).** SPEC-007
ADR-902 and SPEC-008 CON-903/REQ-1006 state it; nothing in the candidate claims
otherwise, and the exact-pair prompt is unconditional for a new tuple even when
the relay origin is already trusted under a different application
(`cbcl_v2_policy.rs` unit test
`exact_pair_never_authorises_another_application_on_the_same_relay`).

**4. CPace credential/v2 has one closed wire grammar and key schedule;
cryptographic preimages, Finished order, and checkpoint key separation.**
**PASS.** `cbcl-pairing/src/credential_v2/channel.rs` read in full. One
HKDF-SHA512 schedule over `ISK` with `info = label || TH`, distinct labels for
`kc A`/`kc B`/`key A-B`/`key B-A`/`iv A-B`/`iv B-A`/`exporter`;
`TH = SHA-512(canonical CBOR [publicContext, allocatorFrame, claimantFrame])`;
Finished is HMAC-SHA512 under the role's `kc` over `label || TH`, compared in
constant time with an explicit `peer_side == local_side` rejection. AEAD AAD is
closed canonical CBOR over `{v, direction, counter, th}`. Checkpoint keys are a
separate HKDF-SHA512 with salt = carrier ceremony and a side-bound CBOR info
(`checkpoint.rs:353-369`), and the outer AAD binds domain, version, role,
profile, ceremony, generation, and expiry. CON-989's
`receiptRecoveryToken = HMAC-SHA256(EXPORTER, domain‖TH)` and
`receiptRecoveryCommitment = SHA-256(domain‖token‖ceremony‖applicationId)`
match the contract byte for byte (`channel.rs:322-352`). All of this is checked
against an independent Python oracle in TEST-066
(`tests/support/credential_v2_oracle.py`), which passes.

**5. Browser installation-device possession proved before offer signing; exact
socket-generation typing, bounds, replay resistance.** **PASS.**
`finalize_verified_offer` (`crates/selfsame-pairing/src/credential_v2.rs:605-612`)
accepts only a `VerifiedPreparedCredentialV2Offer`, constructible solely by
`verify_prepared_offer_device_proof` (`:569-602`), which re-recognises the
offer core, requires the device DID and JWK digest to match the offer's device
binding, and verifies Ed25519 over
`SHA-256(domain ‖ CBOR[socketGenerationDigest, carrierCeremonyId, offerCoreDigest])`.
The hub reaches it only through `decide-finalize`
(`cbcl-core-selfsame-v2-pending.lfe:82-86`), which requires
`socket-generation` 32 octets, `offer-core-digest` 32 octets, and
`device-possession-proof` 64 octets, refuses a generation that differs from the
stored row, refuses at or past the deadline, and replays a prior `finalized-ack`
only under `retry-proof` for the exact same digest.

**6. Resolver closure replay cannot become resolver authority; hub and wallet
independently obtain and compare live authority.** **PASS.**
`selfsame-app-identity/src/path_b.rs:323` (`replay_resolver_closure`) projects
facts only from signed `did:crdt` history — `did_recomputed_ok`,
`deltas_verified`, `locally_closed`, `deactivated` are derived, never asserted
— and `agree_closures` (`:464`) requires agreement between independently
obtained observations. The new `tests/path_b_closure_replay.rs` exercises it.
On the hub side, `do-selfsamepairaccept` obtains its own evidence through
`path-b-sidecar` rather than trusting the browser's supplied closure; on the
wallet side, `cbcl_v2_final_decide` resolves and verifies the closure itself
(`cbcl_v2_commands.rs:494-520`).

**7. Hub final acceptance atomic; retries byte-identical; browser activation
follows signed final acceptance; wallet installation follows authenticated
receipt, live reciprocal binding, durable compare-and-swap.** **PASS.**
`accept-new` (`cbcl-chat-selfsame-v2-store.lfe:625-660`) performs the account
row, migration, WebFinger provision, final row, and phase transition inside one
Mnesia transaction with uniqueness locks on principal, application-account,
scope, and DID; any refusal aborts the whole transaction. `accept-retry`
(`:715-722`) returns the stored `acceptance-ack` bytes unchanged after
`final-matches?`. The browser writes an inactive staging record before hub
finalization and activates only after verifying `finalStatusJws` under the
profile key that signed the offer. `cbcl_v2_finish`
(`cbcl_v2_commands.rs:582-668`) verifies the receipt, then
`verify_live_installation` performs the live WebFinger fetch-and-verify, and
`install()` (`cbcl_v2_completion.rs:1119-1160`) is a locked compare-and-swap on
the exact pending predecessor. (What happens to that installed record
afterwards is H-2.)

**8. Browser process restart works at every pre-terminal checkpoint; restored
allocator authenticates the replacement socket, queries hub status, reopens the
exact membership, resends only the exact cached frame; crash windows including
an `attempt: null` durable record.** **FAIL — see H-1.** The machinery is
present and correct for the `Allocated` bootstrap checkpoint and for every
established-endpoint checkpoint, and the `attempt: null` case is handled
correctly (`app.js:1929-1943` synthesises the default attempt, and the
controller's guard at `credential-v2-allocator.mjs:808-815` admits it only in
combination with `restoredProtocolPhase === 'allocated'`). It is broken at the
`ShareSent` and `FinishedSent` checkpoints, which are exactly the crash windows
between checkpoint persistence and the CPace-share and Finished relay effects.

**9. `selfsamepairstatus` and `selfsamepairabort` have closed grammars,
current-generation authentication, exact binding, bounded state, no
unauthorized advance; explicit cancel and transport failure are distinct.**
**PASS.** Both are now in the dialect's closed set
(`cbcl-core-dialect.lfe:40-41, 93-94, 144-160`) with exact keyword sets and no
positional or content values, and both are dispatched in session-ws.
`decode-credential-v2-status` requires the recovery nonce to equal the
connection's held `conn_nonce` and carries a 64-octet installation-device
proof; `decide-status` (`cbcl-core-selfsame-v2-pending.lfe:97-131`) requires
both `authenticated` and `recovery-authenticated`, and returns only
`finalized` / `absent` / `rebind` / `close expired` — it never advances a
phase. `decide-abort` (`:134-158`) requires the current generation, restricts
`terminal-class` to `aborted | expired`, and refuses `expired` before the
exclusive deadline, so a client cannot relabel a live attempt. On the browser
side `abort()` (`credential-v2-allocator.mjs:1302-1316`) refuses at every
post-payload phase, and `onHubStatusClosed` distinguishes `aborted` from
`expired`, `corrupt`, `unavailable`, `ambiguous`, and `busy`, while a transport
failure follows the separate `recoverSocket` path.

**10. Post-relay-window HTTPS recovery authenticates token, application,
ceremony, offer, receipt commitment, current live profile, and authority
rotation; installs once; HTTP is POST-only, CBOR-only, bounded,
non-redirecting, uncacheable, rate-limited before parsing or store lookup with
fail-closed limiter failure.** **PARTIAL — the transport half PASSES; the
authentication half FAILS on the hub (M-1) and its terminal-absence predicate
is unsound (M-2).** Transport: the route is `dispatch-post` only, returns 405
otherwise; `recovery-request?` requires an empty query string, no
`content-encoding`, and exactly `application/cbor` with no parameters; the body
is capped at 2,304 octets before parsing; responses carry `no-store` and are
capped at 9,216 octets; the limiter runs first and any non-`ok` result — a
crash included, via `catch` — yields an empty `no-store` 503. The wallet client
(`crates/selfsame-app-identity-net/src/pairing_status.rs`) refuses redirects,
content-encoding, cookies, a non-`no-store` cacheable response, a wrong media
type, any status outside {200, 202, 404, 503}, a body on a 503, and an
over-length body. Adapter discipline: `recover_claimant_completion` derives,
publishes, and resends nothing — it reconstructs the CON-028 receipt from the
returned JWS plus retained state, authenticates it through
`authenticate_recovered_receipt_object`, and installs at most once through the
same locked compare-and-swap. Rotation is prompted before installation and
before pending removal, and cancellation preserves pending.

**11. Every restored display and decision authority survives checkpointing
without trusting duplicated JavaScript state or oversized checkpoint state.**
**PASS.** `restore_offer_context` (`selfsame-web-device/src/lib.rs`) re-verifies
the signed offer against the live profile, requires the supplied offer core,
digest, request ID, intent nonce, and `expiresAt` to match the signed
recognition, recomputes the authority-response digest, re-recognises the signed
authority status under the same `kid`, and refuses a restored offer at or past
its deadline. Nothing display-bearing is taken from the persisted JSON on
trust. Checkpoint size is bounded at 69,632 octets at the JS boundary and at
`MAX_CIPHERTEXT` in `checkpoint.rs`, and the endpoint verifier state added in
this candidate is length-prefixed and bounded by `MAX_PLAINTEXT`.

**12. Dependency pins name the exact cbcl-pairing candidate in all four places;
ordinary builds reject a mismatched or dirty sibling; the vendored WASM rebuilds
byte-identically from clean exact pins and exposes the restored allocator ABI.**
**PARTIAL — cbcl-pairing PASSES, the WASM rebuild PASSES, the sibling closure
FAILS (M-3).** All four cbcl-pairing pointers agree on `aedbc4ca…`; the
`selfsame-pairing/build.rs` guard was mutation-tested and fires on both a dirty
and a mismatched sibling; `scripts/check-selfsame-wasm-rebuild.sh` reproduced
all four vendored files byte-identically (vendored
`selfsame_web_device_bg.wasm` SHA-256 `9b79c5ed…`, matching
`selfsame_web_device.provenance.json`); and the vendored `.d.ts` exposes
`restore`, `restore_offer_context`, `restored_phase`, and
`restored_presence_code`. Selfsame's `cbcl-rs` and `did-crdt` pins are
unguarded and disagree with cbcl-bus's, and Selfsame's CI cannot build the
candidate.

**13. Crash, disconnect, timeout, replay, wrong order, cancel, expiry, and
recovery cannot create a second identity effect or accepted authority; the
orphan-DID residual evaluated separately.** **PARTIAL.** No path in the
candidate produces a second accepted authority: the hub's uniqueness locks and
`accept-retry` make finalization idempotent, the wallet's slot is a tagged
pending-or-installed union with locked compare-and-swap on every transition,
and both recovery adapters install at most once. Two exceptions are recorded
above: M-2 can produce a durable hub identity effect concurrently with the
wallet's destruction of its pending slot (an asymmetric, not a duplicate,
effect), and H-1 makes two crash windows unrecoverable rather than
double-effecting. The orphan-DID residual is L-1 and is **not blocking** on its
own; its wedged-slot consequence is folded into H-2.

**14. Bounds and one-over-limit refusals are executable; the ordinary
production entry remains closed; loopback staging and existing enrolment remain
distinct.** **PASS.** cbcl-pairing TEST-063's control-body bounds (1, 23, 24,
255, 256, 2,047, 2,048 accepted; 0, 2,049, 4,095, 4,096 refused) run green in
the full suite; the recovery request/response bounds are enforced on both
sides and tested (`test_1161_only_closed_bounded_uncacheable_responses_cross_the_boundary`);
the limiter's window, limit, and source-capacity boundaries are exercised
through the exported pure `decide/6`. Credential/v2 allocation is reachable
only behind `cbcl-chat-selfsame-application-gate:document`
(`cbcl-chat-session-ws.lfe:1750`), the Path-B admission gate remains closed,
and the loopback staging path retains its own separate receipt oracle
(`crates/selfsame-pairing/tests/credential_v2_staging_receipt.rs` and
`tests/support/credential_v2_staging_oracle.py`). Credential/v1 enrolment
remains a distinct command set and its frozen bytes are unchanged.

**15. Tests reach the named invariants; surviving mutants identified.**
**FAIL.** Three surviving mutants were found and two were executed:
(a) *browser durable-state recognition / phase reconciliation* — no test in any
repository restores an allocator at bootstrap phase `share-sent` or
`finished-sent`; mutating that path is unnecessary because it is already broken
(H-1), which the suite does not detect; (b) *hub recovery authentication* —
deleting the commitment comparison from `recovery-with-final` would be caught,
but the symmetric check is simply absent from `recovery-without-final` and no
test drives a wrong token against a pre-final pending row (M-1); (c) *installed
state* — every REQ-1006 reload/unlink predicate could be deleted with no test
failing, because none exists (H-2). Rate limiting and the pin guard are
genuinely covered: the limiter's pure `decide/6` is directly tested at its
boundaries, and the pin guard was mutation-tested live and fired on both
mutations.

---

## Dispositions for the prior-review findings

Read only after the independent pass, from
`specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`,
`…/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`, and
`…/req-909-adversarial-review-2026-08-22.md`. Each was independently re-tested
against the candidate; no disposition is inherited.

### 0.5.8 H-1 — `selfsamepairstatus`/`selfsamepairabort` absent

**PARTIALLY CLOSED; RE-OPENED as this review's H-1.** The two commands now
exist end to end: dialect validators (`cbcl-core-dialect.lfe:40-41,144-160`),
response types (`:93-94`), session-ws handlers, the pure decision core
`cbcl-core-selfsame-v2-pending:decide-status/4` and `decide-abort/3`, store
transactions with device-proof verification, browser send/receive paths
(`credential-v2-allocator.mjs:530-556, 1196-1316`), and new tests
(`cbcl-core-selfsame-v2-pending-tests.lfe`, +93 lines;
`cbcl-chat-selfsame-v2-ws-tests.lfe`). The socket-rebind semantics the 0.5.8
finding demanded are implemented and correct. What is not closed is the other
half of the same reconnect obligation: the browser cannot resume its
*cbcl-pairing relay* leg after restart at two of the four bootstrap
checkpoints, so a socket loss in that window still strands the ceremony —
now for a different reason, inside the code written to fix the first one.

### 0.5.8 H-2 — CON-989 HTTPS recovery unimplemented

**CLOSED, code-confirmed, with two new defects inside the new mechanism.** All
three sides now exist: cbcl-pairing's `with_receipt_recovery_token`,
`authenticate_recovered_receipt_object`, and the `peer_seq: Option<u8>` split
that lets a recovered receipt commit without inventing a relay acknowledgement
(`src/credential_v2/claimant.rs:337-360, 580-640`); Selfsame's closed CBOR
grammar and negative-status verifier
(`crates/selfsame-pairing/src/credential_v2/recovery.rs`, 555 lines), strict
HTTP client (`crates/selfsame-app-identity-net/src/pairing_status.rs`), and
`recover_claimant_completion` plus the `cbcl_v2_pending_recoveries` /
`cbcl_v2_recover` commands; and cbcl-bus's
`POST /selfsame/pairing/v2/status` route, `recovery-status` store transaction,
signer, and bounded anonymous limiter. The 0.5.8 finding is therefore closed.
The new mechanism carries M-1 (no token authentication on the negative branch)
and M-2 (unsound terminal-absence predicate), both reported above.

### 0.5.8 M-1 — dependency-pin drift

**PARTIALLY CLOSED; re-opened as this review's M-3.** cbcl-pairing is now
pinned consistently in all four declared places and enforced by an executable,
mutation-tested build guard. `cbcl-rs` and `did-crdt` are not, they disagree
across the coordinated repositories, and Selfsame's CI cannot build the
candidate with its own declared pins.

### 0.5.8 L-1 — orphaned DID publication

**OPEN, unchanged in kind.** Re-traced directly: `publish_issuer_identity`
still precedes `resolve_closure` with no compensating unpublish
(`cbcl_v2_commands.rs:494-520`). Recorded as this review's L-1, with its
consequence and operational treatment made explicit as the contract requires,
and with its new wedged-slot consequence attributed to H-2.

### 0.5.7 N-1 — socket-generation digest typing/bound ambiguity

**CLOSED, code-confirmed independently.** The construction was re-read in three
places rather than taken from the spec: the browser
(`credential-v2-allocator.mjs:66-88`), the Rust NIF the hub calls
(`crates/cbcl-pairing-erl/src/lib.rs:67-85`, reached via
`credential-v2-socket-generation` at `cbcl-chat-session-ws.lfe:1865-1870`), and
SPEC-053's CDDL. All three agree on the label bytes, on byte-string (not
text-string) CBOR types for `hubId`, `recoveryNonce`, and `frameKeyDigest`, and
on the 1–64 / 16 / 32 octet bounds, and both code sites refuse out-of-bound
input. The digest also now binds the recovery path: `decode-credential-v2-status`
requires the request's `recovery-nonce` to equal the connection's live
`conn_nonce`, so a status query cannot be replayed onto a different socket
generation.

### 0.5.7 F-A through F-E

| Hard stop | Disposition | Evidence re-derived this session |
|---|---|---|
| F-A — exact application+relay, never relay-only | **CLOSED, code-confirmed** | `src-tauri/src/cbcl_v2_policy.rs` read in full; both unit tests re-run green |
| F-B — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed** | `cbcl-pairing/src/credential_v2/display.rs` compile-fail doctests at `:151,575,585` execute and pass; `CredentialV2WalletOfferVerifier` returns a closed verdict and cannot emit display text |
| F-C — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed** | `recognise_claimant_invitation` / `authorise_claimant_relay` / `prepare_claimant` / `preview_claimant_identity` / `complete_claimant` traced against CON-986; `recognise_claimant_invitation` opens no socket; the policy write happens in `bind_finished_profile` only after both Finished values, per CON-988 step 6 |
| F-D — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed** | scope, principal digest, and snapshot nonce are hub-minted with `crypto:strong_rand_bytes(32)` in `write-new`; the wallet derives its home key from that hub-signed scope and never accepts a peer-supplied one |
| F-E — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged)** | ADR-902 text; nothing in the candidate contradicts it |

### [[SPEC-007-cbcl-pairing-cutover#REQ-812]]

| Clause | Disposition |
|---|---|
| No accepted credential/identity effect before approved payload delivery (v1 profiles) | **Holds.** v1 paths untouched; frozen bytes unchanged. |
| No application identity effect before preliminary approval | **Holds.** Traced through `cbcl_v2_preliminary_decide`. |
| Preliminary approval authorises one zeroizing derivation and pure preview only | **Holds.** Traced. |
| No signing/publish/resolve/issue/persist/stage/payload before final approval | **Holds.** Traced; the sealed checkpoint precedes every signature. |
| Final approval authorises only the declared causal order | **Holds.** Exact order re-verified line by line at `cbcl_v2_commands.rs:463-540`. |
| One encrypted non-authorising completion checkpoint before the first causal effect | **Holds.** `persist_pending` at `:415` precedes `build_issuer_artifacts` at `:485`. |
| Browser staging crash-safe and non-authorising until immutable hub acceptance | **Holds** for the in-band path, and now also for the post-window path — the CON-989 adapter exists and installs only from an authenticated receipt. |
| Compensation touches only ceremony-owned effects | **Holds, with the L-1 residual**, unchanged: a mid-chain failure can leave a published-but-unused DID. No pre-existing identity or operator-withdrawal state is changed. |

### REQ-909 (2026-08-22 withdrawn first-contact amendment) — recurrence check

Re-tested independently. F-1 does not recur: the carrier carries a real
`application-context` application ID, recognised by
`recognise_application_id` before any use. F-2 does not recur: every closure,
publication, alias, and grant effect sits behind final approval. F-3/F-4's
residual (a relay descriptor certifies its operator only to the extent the
profile does) persists and is the same honest residual as F-E. F-5/F-6 were
internal contradictions of the withdrawn text and have no analogue in the
current architecture.

---

## Exact test commands and results

All builds and tests ran in clean detached clones under the session scratch
directory. No candidate repository was modified.

| # | Command | Result |
|---|---|---|
| 1 | `if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo unset; fi` | `unset in parent env` |
| 2 | `env -u ANTHROPIC_API_KEY claude auth status` | `authMethod=claude.ai`, `apiProvider=firstParty`, `subscriptionType=max` |
| 3 | `git diff --stat d01959f..48dd5b5` (worktree vs candidate) | 1 file, 157 insertions, markdown only |
| 4 | cbcl-pairing @ `aedbc4c` clean clone: `cargo test --all-features` | **exit 0** — 29 suites, all `test result: ok`, 0 failed (includes TEST-063 bounds and the TEST-066 Python schedule oracle) |
| 5 | Same clone + added `tests/probe_bootstrap_restart.rs`: `cargo test --test probe_bootstrap_restart` | **FAILED (intended)** — `ShareSent bootstrap_phase = Some(ShareSent)` / `reopen = Err(Phase)`; `FinishedSent bootstrap_phase = Some(FinishedSent)` / `reopen = Err(Phase)`. Evidence for H-1 |
| 6 | selfsame @ `d01959f` clean clone, siblings cbcl-pairing@`aedbc4c` / cbcl-rs@`febc6691` / did-crdt@`1f409a4`: `cargo test --locked -p selfsame-pairing -p selfsame-web-device -p selfsame-app-identity -p selfsame-app-identity-net` | **exit 0** — 549 passed, 0 failed, 1 ignored |
| 7 | cbcl-bus @ `79423c4` clean clone: `rebar3 compile` | **exit 0** — `boundary-check PASSED` for `cbcl_bus`, `cbcl_chat`, `cbcl_router` |
| 8 | Same clone: `rebar3 eunit` | **exit 0** — `All 974 tests passed.` |
| 9 | `node --test apps/cbcl_chat/priv/web/credential-v2-allocator.test.mjs` | **18 pass, 0 fail** |
| 10 | cbcl-bus: `bash scripts/check-selfsame-wasm-rebuild.sh` with all four siblings clean at the cbcl-bus pins | **exit 0** — `Selfsame WASM rebuild matches the exact vendored package` (all four vendored files `cmp`-identical) |
| 11 | `git show 79423c4:…/selfsame_web_device_bg.wasm \| shasum -a 256` | `9b79c5ed72593d83916b9ea9007fae3a454ee6e4cf5e7e6fb2127a3a8cd5cbb1` — matches `provenance.wasmSha256` |
| 12 | Pin-guard mutation: append a line to `../cbcl-pairing/src/lib.rs`, then `cargo build -q -p selfsame-pairing` | **build.rs panics** — "the pinned checkout has tracked modifications" |
| 13 | Pin-guard mutation: `git -C ../cbcl-pairing checkout c1746a57`, then `cargo build -q -p selfsame-pairing` | **build.rs panics** — "expected aedbc4ca…, found c1746a57…" |
| 14 | `git -C ../cbcl-rs checkout 1c6fa8f5` (Selfsame's own `cbcl-rs.sha`), then `cargo build -q -p selfsame-web-device --target wasm32-unknown-unknown --release` | **exit 0, no warning**, and `Cargo.lock` silently rewritten (`cbcl-core` loses `sha2 0.10.9`). Evidence for M-3 |
| 15 | Same, with `--locked` (what CI runs) | **refused** — "cannot update the lock file … because --locked was passed". Selfsame CI cannot build the candidate at its own pin |
| 16 | `cargo tree -p selfsame-web-device --target wasm32-unknown-unknown` | Two `cbcl-core` copies in the WASM: git `febc6691` and the unguarded path sibling |
| 17 | `git diff --stat 1c6fa8f5..febc6691 -- crates/cbcl-core crates/cbcl-parser` (cbcl-rs) | 42 files, 14,476 insertions — the two pins are materially different |

A note on evidence hygiene: run 8 was first attempted while this reviewer's own
pin-guard mutation (runs 12–13) was still applied to the shared clone, and
failed for that reason. It was re-run after restoring the clone to
`aedbc4c` clean, and the result recorded above is from that clean re-run. One
earlier `cargo test --workspace` attempt aborted with `No space left on device`
on the host volume (an environmental condition, 100% full at the time); the
scratch tree was pruned and run 6 completed cleanly. Neither aborted run is
counted as evidence for or against any requirement.

---

## Surfaces examined and found sound

Recorded so a later reader can tell what was checked from what was not.

- The complete credential/v2 key schedule, transcript hash, Finished
  construction, AEAD framing, and both recovery-secret derivations, against an
  independent Python oracle.
- The eleven logical-body grammars, the closed carrier grammar, the
  272-octet relay-origin bound, and canonical-CBOR round-trip equality
  enforcement on decode.
- The endpoint and bootstrap checkpoint formats, including their distinct inner
  domains, AEAD AAD binding, monotonic generation and nonce-reuse refusal, and
  the new verifier-state field.
- The five-function claimant split, its capability types, and the
  socket-capability consumption order.
- The hub's seven-command WebSocket surface, its pure decision core, and its
  Mnesia lock ordering.
- The hub's HTTP recovery route policy and the wallet's HTTP client policy.
- The exact-pair relay policy store and its canonical encoding.
- The reproducible WASM build pipeline and the vendored package's ABI.

Surfaces deliberately **not** exercised: no live network ceremony was run
against a real relay, hub, or resolver; no Tauri application was built or
launched (the desktop shell's UI wiring in `src/pairing.js` and
`src/index.html` was read but not executed); and the NFR-928 transaction and
consumer inventories were spot-checked against the code rather than
regenerated in full.

---

## What this verdict authorizes

This is a **REJECT**. Nothing here authorizes production allocation, public
allocation, release, push, or deployment.

For completeness, and as the output contract requires this statement in either
direction: **a PASS verdict would not authorize release, push, or deployment
either.** Those actions require their own separate gates, which lie outside
this review's authority. The repository owner's 2026-08-24 waiver authorizes
local test-first implementation before PASS and nothing more.

The two HIGH findings (H-1 restart recovery at the two bootstrap checkpoints;
H-2 the absent installed-state lifecycle) and the three MEDIUM findings (M-1
unauthenticated negative-status signing; M-2 the unsound terminal-absence
predicate; M-3 the unguarded and mutually inconsistent sibling pins) must be
corrected and re-reviewed. The two LOW residuals may remain only with the
consequence and operational treatment stated above carried into the release
notes.
