---
id: spec-008-0.5.15-authenticated-relay-release-source-2026-08-25
title: SPEC-008 0.5.15 — authenticated relay release source
status: owner-authorized-test-first-candidate
tier: 1
mode: descriptive
author: Anuna Research (drafted with OpenAI GPT-5.6 Codex)
last-updated: 2026-08-25
parent: "[[SPEC-008-production-pairing-claimant]]"
---

# SPEC-008 0.5.15 — authenticated relay release source

## Authority and scope

The owner authorized test-first implementation before review PASS. This
trajectory records a release-boundary defect found by executing the complete
hub relay matrix after the 0.5.14 verification corrections.

It authorizes no push, production allocation, release, or deployment.

## Red-first finding

The hub pinned cbcl-pairing
`62ef4a968b46b4836374fcee1d78c410f730a7a7`, which contains the standalone
credential/v2 protocol. Its release builder still authenticated executable
source against pre-v2 revision
`197d4cb3d1560ab5328df28fc984269799c510f9`.

The agent SPAKE2 boundary passed, then the release-target isolation harness
stopped when the builder reported executable drift. A direct release-builder
run produced the same refusal. The hub compiled its NIF and passed browser
tests. The pinned production relay did not build.

## Correction

The builder's executable baseline is now the exact pinned credential/v2
revision. Future descendant pins remain acceptable only when executable
source, protocol assets, build hooks, dependency declarations, and the lock
graph do not drift. Executable drift requires another explicit integration
review and baseline update.

[[SPEC-008-production-pairing-claimant#TEST-1168 — Release relay authenticates the credential/v2 source closure]]
requires red-first regression evidence and the poisoned-cache isolation
harness. It also requires both relay feature tests, deny-warnings Clippy,
injected-feature rejection, and a successful normal release build.

## Reproducible artefact correction

The first clean Linux comparison disproved the 0.5.14 byte-stability
assumption. The Clippy disposition changes an embedded Rust source location,
so the generated WASM differs even though its behavior does not.

Vendor mode regenerated the artefact on `x86_64-unknown-linux-gnu` with rustc
1.96.0 and wasm-bindgen 0.2.126. Its measured SHA-256 is
`a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`.
An independent fresh comparison SHALL reproduce those bytes before review.

## Exact implementation closure

- Selfsame implementation:
  `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`
- cbcl-bus implementation:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-pairing:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`

## Review gate

The coordinated review set is Selfsame SPEC-008 0.5.15, Selfsame SPEC-007
0.3.6, cbcl-pairing SPEC-001 0.5.7, and cbcl-bus SPEC-053 0.17.11.

A fresh Tier-1 review SHALL use Circus and the interactive Claude subscription
driver. `ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent.

PASS still authorizes no push, production allocation, release, or deployment.
