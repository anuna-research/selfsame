---
id: spec-008-0.5.3-claude-adversarial-review
title: SPEC-008 0.5.3-draft — Tier-1 adversarial review of the self-contained standalone credential/v2 reissue
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 4
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `01ac5e76c9bff553ce9cf034e6d0988b723f5c37` SPEC-008 0.5.3-draft and SPEC-007 0.3.1-draft; cbcl-bus `ac963b2b686beb1e91a2798e93832a60d512845c` SPEC-053 0.17.3-draft; cbcl-pairing `9942a72c2aace0c329e99ad22413570b84057649` SPEC-001 0.5.2-draft; did-crdt `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 4. It imported no conclusion from any earlier Claude conversation. It read the 0.5.2 report only after completing an independent first pass over the four current parents, the deprecated did-crdt disposition, and the five pinned code baselines."
date: 2026-08-24
---

# SPEC-008 0.5.3-draft — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the coordinated 0.5.3 reissue is singular, safe,
bounded, and implementable without invention.

Metaphor: a bridge inspection before the first vehicle. The deck is now
almost complete. Four spans still carry no stated load path.

Structure:

```text
independent pass over four parents + five code baselines
        -> nine new findings
        -> prior C/H/M/L re-test
        -> F-A through F-E re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; one HIGH; four MEDIUM; six LOW
Load-bearing: the hub authority response has no grammar in any current parent
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         eleven owner corrections before the next reissue
Detail:       the finding sections and the assessment sections below

This report uses descriptive controlled language. Its wikilink anchors are
fully qualified.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-opus-5`. The generating author
is OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model
requirement holds.

The launcher removed `ANTHROPIC_API_KEY` before starting the session. The
reviewer confirmed absence by testing the variable's presence in the process
environment. The variable is unset. No value reached this report.

`claude auth status --json` ran with that variable removed. It reported
`authMethod = claude.ai`, `apiProvider = firstParty`, and
`subscriptionType = max`. This report records no email, organization
identifier, access token, API key, or account identifier.

The driver SHA-256 is
`4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

The session is fresh. The reviewer completed an independent pass over the four
current parents, the deprecated did-crdt disposition, and every code baseline
before opening the 0.5.2 report. Every prior finding below carries an
independently re-derived disposition.

## Exact review set, baselines, and pre-implementation status

Each candidate object exists and carries the declared version.

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `01ac5e76c9bff553ce9cf034e6d0988b723f5c37` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.3-draft | yes |
| selfsame | `01ac5e76c9bff553ce9cf034e6d0988b723f5c37` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.1-draft | yes |
| cbcl-bus | `ac963b2b686beb1e91a2798e93832a60d512845c` | `specs/SPEC-053-key-root-identity.md` | 0.17.3-draft | yes |
| cbcl-pairing | `9942a72c2aace0c329e99ad22413570b84057649` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.2-draft | yes |
| did-crdt | `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

The SPEC-053 blob is `728cd141c4c6b377815140e82d6168555c505b8d`. The SPEC-001
blob is `609b8b37a5004b4f1ce7b74900b37786790c90f6`. The SPEC-037 blob is
`5ff881b24a37e2ed3cd142f963769fa9617f379e`.

Every implementation baseline resolves as a commit. They are selfsame
`0220cec2`, cbcl-bus `ce894a7f`, cbcl-pairing `c22f2d05`, did-crdt `1f409a42`,
and the anuna-ssi namespace pin `c7d46202`.

The candidate is intentionally unimplemented. The reviewer treated absent
candidate code as expected and judged the prose against the pinned baselines.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 1 | yes |
| MEDIUM | 4 | yes |
| LOW | 6 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Five blocking findings
survive, so the verdict is REJECT.

## Findings

### N-1 The hub authority response has no grammar and a Selfsame body hashes it

Severity: HIGH. Owner: cbcl-bus repository owner, with the selfsame owner for
the consuming body.

cbcl-bus CON-012 declares seven commands on the authenticated chat WebSocket.
One is `selfsamepairauthority(carrierCeremonyId)`, at SPEC-053 line 6810.

The same contract then declares its response set. Line 6828 states that the
closed set is the allocation, recovery, and final response set. It contains
`selfsamepairallocated`, `selfsamepairstatuspending`, `selfsamepairaccepted`,
`selfsamepairstatusabsent`, and `selfsamepairstatusclosed`.

No authority response appears in that set. A repository-wide identifier search
over SPEC-053 returns `selfsamepairauthority` at exactly two lines, 6693 and
6810. Neither line states a response shape, member set, encoding, or
authentication rule.

Three further commands share the gap. `selfsamepairofferprepare`,
`selfsamepairofferfinalize`, and `selfsamepairabort` also carry no response in
the closed set. Line 6845 still requires the response recognizer to reject
every extra argument, unknown class, wrong length, duplicate field, and
trailing term.

The Selfsame parent then hashes the missing object. SPEC-008 CON-987 declares
`credential-v2-binding-confirmed-body` at line 1288. Its member
`authorityStatusDigest` is mandatory and carries `bstr .size 32`.

Line 1342 states the construction. It states that `authorityStatusDigest` is
SHA-256 over the exact authenticated hub authority response that proved
`BoundSameDid`.

The outcome tokens exist in one parent only. `BoundSameDid` and `NoBinding`
appear in SPEC-008 at lines 1343 and 1344. They appear zero times in SPEC-053
and zero times in cbcl-pairing SPEC-001.

The hub parent describes a different vocabulary. SPEC-053 REQ-082 line 6696
names unknown, corrupt, unavailable, ambiguous, changed snapshot, changed
session, and a different reciprocal DID. It maps none of them to a wire value.

The consequence reaches the consent path. cbcl-pairing CON-028 places
`binding-confirmed` between preparation and final approval. cbcl-pairing
CON-031 hashes the exact canonical object bytes into `objectContentHash`, and
every successor carries that value as `predecessorDigest`.

Two independent implementations therefore cannot agree. They cannot produce the
same `authorityStatusDigest`, the same binding-confirmed object, the same
content hash, or the same final-approve predecessor.

SPEC-008 TEST-1160 requires exactly that agreement. It requires independent
encoders and recognisers for all eleven logical bodies and byte-identical
vectors. cbcl-pairing TEST-066 requires the same property for content hashes.
Both tests are undecidable.

The wallet also cannot check the value. SPEC-008 states no wallet
recomputation, signature, or equality rule for `authorityStatusDigest`. It
contrasts with `migrationConfirmationDigest` at line 1348, which the wallet
recomputes from the authenticated offer and its local `previewIssuerDid`.

This defect has the shape the closed 0.5.2 C-2 finding had. The reissue
promoted nine body grammars and left one member's preimage outside current
authority.

### N-2 An ordinary-build registry prohibition contradicts two current tests

Severity: MEDIUM. Owner: selfsame repository owner.

SPEC-008 REQ-1006 states a build-scoped prohibition at lines 736 to 740. It
states that the wallet contains no compiled relay allowlist,
conformance-digest registry, relay wildcard, or relay-only trust fallback in an
ordinary build.

The subject is the wallet and the scope is the build. The sentence carries no
credential/v2 qualifier.

SPEC-007 TEST-817 requires the opposite artefact to exist. Lines 1100 and 1101
state that the reviewer installs an empty or hostile compiled relay registry
for credential/v2. The same lines require that registry to have no selection,
prompt, socket, or acceptance effect.

Two further clauses agree with the test rather than the requirement. SPEC-008
CON-986 at lines 1230 to 1233 scopes the same list to the credential/v2 path.
SPEC-008 TEST-1159 at lines 1789 to 1792 scans the ordinary binary and fails
only a match on the credential/v2 path.

The pinned code makes the strict reading destructive.
`src-tauri/src/cbcl_registry.rs:17` declares `APPROVED_CONFORMANCE` with one
ratified entry. `src-tauri/src/cbcl_registry.rs:35` returns the ordinary-build
`RelayPolicy` from it.

`src-tauri/src/cbcl_pairing.rs:152` consumes that policy on the non-demo
claimant path. `src-tauri/src/cbcl_context.rs:36` passes it to
`gate_invitation_origin`, which calls `verify_invitation_origin`.

`crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93` define
eligibility. Line 90 requires `approved_conformance.contains(...)`, so an
absent registry makes every descriptor ineligible.

Removing the registry therefore does not weaken the credential/v1 claimant. It
refuses every non-loopback origin and stops the deployed SPEC-004 enrolment
path.

SPEC-007 CON-806 keeps a build policy for that version. Its selection step 2
applies the standing build policy only for credential/v1. No clause in the
coordinated set replaces the claimant-side control the strict reading deletes.

An implementer must therefore choose between a current requirement and two
current tests. That choice changes the security posture of a live path, so the
parents leave a security decision to the implementer.

### N-3 The offer window has no stated terminal effect

Severity: MEDIUM. Owner: cbcl-bus repository owner, with the selfsame owner for
the wallet check.

cbcl-bus REQ-082 opens a bounded window at line 6668. It states that the hub
starts the 120-second offer window and constructs the proof-free
RFC-8785 `OfferCoreV2`.

CON-012 bounds the signed member at lines 6998 to 7000. It states that
`expiresAt` follows `issuedAt` by 1 through 120 seconds and never exceeds
`pendingExpiresAt`.

A second and wider bound exists. REQ-082 line 6633 sets `pendingExpiresAt` to
the earlier carrier relay expiry or allocation time plus 600 seconds.

No clause states which bound terminates acceptance. CON-012's state projection
at line 6895 sends four phases to `aborted` or `expired` and names no clock.
REQ-082's final-transaction clause requires a recheck of the held snapshot,
recovery commitment, and every pending binding, and names no expiry.

The wallet side is equally open. SPEC-008 CON-902 lists `now` and
`clock_skew_seconds` as authenticated plan inputs. SPEC-008 CON-986 lines 1155
to 1157 state that the final effect capability binds expiry. They name no
value.

The window spans work the hub cannot bound. It spans the preliminary consent
screen, an out-of-band fingerprint comparison, and the final consent screen.
It also spans one hierarchy derivation, one DID publication, one closure
resolution, one live WebFinger fetch, and one grant construction.

The narrow reading makes the ceremony unreachable in practice. The wide reading
makes the 120-second member decorative. Two implementations diverge, and the
divergence lands after the wallet has published a DID.

The baseline already records the lesson for credential/v1.
`src-tauri/src/cbcl_pairing.rs:323` to `:327` call `refresh_now` before the
decision, with the comment that the consent screen is a human pause of
unbounded length.

### N-4 A post-payload refusal strands the claimant recovery precondition

Severity: MEDIUM. Owner: cbcl-pairing repository owner, with the selfsame
owner for the pending slot.

cbcl-pairing CON-028 permits an allocator refusal after the payload. Its
projection at line 1774 states `payload -> receipt | refusal`. Line 1775 sends
`refusal` to terminal.

cbcl-pairing CON-032 then requires one exact state. Lines 2020 and 2021 state
that the endpoint exposes `recover_receipt` only while a recognised claimant
checkpoint is in the `payload -> receipt` state.

CON-030 keeps the checkpoint alive and states no state disposition. Its erasure
list at line 1921 names terminal decline, pre-payload refusal, pre-payload
expiry, and verified receipt. A post-payload refusal appears in no list.

SPEC-008 CON-986 keeps the wallet slot alive in the same case. Lines 1219 and
1220 state that decline, terminal refusal before payload, authenticated signed
`not-finalized`, explicit unlink, or root purge removes the pending value.

SPEC-008 CON-989 offers the only remaining route. It states that verified
accepted status produces the receipt body and that CON-032 then runs the
ordinary receipt transition.

The route closes when the hub has already finalized. The allocator can send a
post-payload refusal after losing the hub response, because cbcl-bus CON-012
returns `busy`, `unavailable`, or `ambiguous` without an answer. The hub record
stays `finalized`, so recovery returns accepted status.

The wallet then holds a pending slot it can neither install nor clear. CON-032
refuses the terminal checkpoint, and CON-989 supplies no signed `not-finalized`
for a finalized ceremony.

SPEC-008 TEST-1158 asks for the property this gap denies. It requires the
reviewer to restart each component at every boundary and to require exact
recovery. The explicit unlink escape leaves the person a second ceremony and a
second hub account.

### N-5 The eleven object kinds carry no integer assignment

Severity: MEDIUM. Owner: cbcl-pairing repository owner.

cbcl-pairing CON-028 declares two envelope arms at lines 1704 to 1720. The
control arm at line 1706 fixes `1: 1..8`. The large arm at line 1714 fixes
`1: 0 / 9 / 10`.

Line 1723 then names eleven kinds in prose. Line 1727 states that only `offer`,
`payload`, and `receipt` use the large arm.

No clause maps a kind name to an integer. The arm partition fixes the two sets
and leaves the assignment inside each set open.

The remaining freedom is real. A conforming implementation can place `payload`
at 10 and `receipt` at 9. It can also permute the eight control kinds across 1
through 8 in any order.

Field 1 enters the object hash. CON-031 line 2007 defines `objectContentHash`
as SHA-256 over the exact canonical `credential-v2-object` bytes. Line 2010
states that those bytes include fields 0 through 4.

Divergent assignments therefore break every predecessor equality check.
cbcl-pairing TEST-061 and TEST-066 require two independent implementations to
reproduce the same content hashes, so both become undecidable.

Two prose orderings do converge on one answer. The kind list and the session
projection both give offer, intent-approve, intent-decline, preparation,
comparison-confirmed, binding-confirmed, refusal, final-approve, final-decline,
payload, and receipt. A zero-indexed reading of either satisfies both arms.

CON-031 forbids that inference in its own words. Line 2015 states that no
trajectory document, version-1 rule, implicit library default, prose label, or
caller-selected algorithm supplies any value in that contract.

The normative local assets do not close it either. SPEC-001's asset table at
line 162 names `schemas/pairing-v1.cddl` and three version-1 companions. That
file at `c22f2d05` contains zero `credential-v2` rules.

### N-6 The claimant checkpoint wrapping key has no stated info encoding

Severity: LOW. Owner: selfsame repository owner.

SPEC-008 CON-986 states the derivation at lines 1168 to 1171. It states that
the wrapping key is a distinct HKDF-SHA512 child of the hierarchy root. It
states that the salt is the raw carrier ceremony ID. It states that the info
contains the label `selfsame credential/v2 claimant checkpoint wrapping v1`
and the application ID.

The word "contains" leaves the combination open. The clause states no
concatenation rule, length prefix, separator, or CBOR encoding for the two
values.

The pinned code uses a different and exact rule.
`crates/selfsame-app-identity/src/hierarchy.rs:328` to `:331` build
`info = LP(label) || LP(context)` under a fixed salt, and the module header
explains why the separator makes `info` injective.

The scope is local, because only the wallet seals and opens this checkpoint.
The clause still states a cryptographic construction that two builds of the
same wallet can implement differently.

cbcl-bus REQ-082 states the allocator equivalent exactly. It states that the
key info is `cbcl-chat credential/v2 allocator checkpoint wrapping v1`, with
one value and no combination.

### N-7 One checkpoint member carries no name

Severity: LOW. Owner: cbcl-pairing repository owner.

cbcl-pairing CON-030 declares `credential-v2-checkpoint` at lines 1871 to 1881.
Its fifth member is `bstr .size 32` and carries no comment.

The prose from line 1884 names the sixth, seventh, and eighth members. It names
the checkpoint generation, the absolute expiry or `null`, and the AES-256-GCM
nonce. It skips the fifth.

The value is inferable. The same contract requires refusal on a wrong ceremony,
and the checkpoint key salt is the raw carrier ceremony ID.

The neighbouring contracts label every member. CON-031 states that every
comment in the public context names the semantic value in that exact position.

### N-8 The declared Selfsame test set omits the other Selfsame parent

Severity: LOW. Owner: selfsame repository owner.

SPEC-008 CON-985 states the set at lines 1085 and 1086. It states that the
current Selfsame test set is TEST-901 through TEST-915 and TEST-1156 through
TEST-1161. It states that every test is stated in this parent.

SPEC-007 0.3.1-draft is a current Selfsame parent in the same coordinated set.
It declares TEST-801 through TEST-821, and all twenty-one headings exist.

Two of those tests are load-bearing here. TEST-821 is the only verifier named
by SPEC-007 CON-807 and REQ-812, and REQ-812 is a declared hard stop. TEST-817
is the only test that exercises registry inertness for credential/v2.

The hub parent shows the missing sentence. cbcl-bus REQ-081 states that the
hub's base-parent tests remain current outside this coordinated increment set.
SPEC-008 states no equivalent carve-out.

A plan built from CON-985 alone therefore drops both tests.

### N-9 One construction count cannot be resolved

Severity: LOW. Owner: selfsame repository owner.

SPEC-008 CON-988 states at line 1395 that this seven-step construction replaces
CON-220 step 6 for credential/v2.

The contract numbers no steps. It states the CON-220 prefix, one digest
computation, two equality checks, one prompt, one capability, one CPace
binding, two Finished checks, and one policy promotion.

At least two groupings reach seven. Nothing depends on the count, so the clause
is imprecise rather than ambiguous.

SPEC-004 CON-220 numbers its own six obligations, so the neighbouring style is
explicit.

### N-10 The registry module comment cites the contract that excludes it

Severity: LOW. Owner: selfsame repository owner.

`src-tauri/src/cbcl_registry.rs:1` describes the file as
"SPEC-008 `CON-903` — the compiled approved-conformance registry".

SPEC-008 CON-903 line 388 now excludes a conformance-digest registry from the
policy. The comment therefore cites a contract that disowns it.

This is implementation work at the baseline rather than normative truth. It
carries forward the 0.5.2 L-3 observation unchanged, and N-2 must settle the
module's fate before the comment can be corrected.

### N-11 One code citation names the wrong line range

Severity: LOW. Owner: selfsame repository owner.

SPEC-008's named failure modes state that the camera scan is built and wired.
The clause cites `src/pairing.js:104-148` and
`src-tauri/capabilities/mobile-scanner.json`.

The substance holds. `src/pairing.js:159` at `0220cec2` defines
`async function scan()`. It checks the barcode-scanner permission, requests it,
runs a windowed QR scan, and hands the exact content to `start`. The capability
file grants `allow-check-permissions`, `allow-request-permissions`, and
`allow-scan`.

The cited range names other code. Lines 98 to 115 render intent fields, lines
117 to 142 define `decide`, and lines 144 to 148 define `cancel`.

This is the only code citation in the four parents that misses its target. The
reviewer resolved every other named path and line exactly.

## Disposition of the 0.5.2 findings

Each row is re-derived from the current candidates and the pinned code.

| 0.5.2 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 credential/v2 key schedule exists in no current parent | **CLOSED** | cbcl-pairing CON-031 now states `credential-v2-public-context` inline at SPEC-001 line 1935. It carries thirteen members and a per-position comment. It states `TH` as SHA-512 over deterministic CBOR of the public context and both CPace frames. It states `PRK = HKDF-Extract-SHA512(empty-octet-string, ISK)`, seven `HKDF-Expand-SHA512` outputs at 32, 32, 32, 32, 12, 12, and 32 octets, both `HMAC-SHA-512` Finished values at 64 octets, the counter rule, the 12-octet nonce as direction IV XOR a 96-bit big-endian counter, and the exact `credential-v2-aad` map. Line 1958 states the thirteen-member count. CON-027 now names all five `credential-v2-ad` members. The public context represents the absent claimant key as a literal `null`, so the 0.5.2 shape contradiction is gone. |
| C-2 nine of eleven logical body grammars exist in no current parent | **CLOSED** | SPEC-008 CON-987 now declares nine CDDL bodies at lines 1258 to 1334. They are intent-approve, intent-decline, preparation, comparison-confirmed, binding-confirmed, refusal, final-approve, final-decline, and payload. The offer body resolves to cbcl-bus CON-012 `signed-offer-v2` and the receipt body to cbcl-pairing CON-028. cbcl-pairing CON-031 now defines `objectContentHash`, and CON-987 binds every `predecessorDigest` to that raw value. The residual member `authorityStatusDigest` becomes [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-1 The hub authority response has no grammar and a Selfsame body hashes it]]. |
| H-1 pre-socket CON-220 gate loses its binding step | **CLOSED** | SPEC-008 CON-988 now states the substitute directly. Line 1395 states that the construction replaces CON-220 step 6 for credential/v2 and amends no credential/v1 caller. Both endpoints place the recognised `profileDigest` into `ci`, `ad`, and the public context, and the claimant requires both Finished values before treating the candidate as bound. REQ-906 orders a peer digest mismatch and a policy-write failure after Finished and before display or identity work. `crates/selfsame-app-identity-net/src/profile.rs:69` implements steps 1 to 5 already, and `crates/selfsame-app-identity/src/discovery.rs:48` sets `MAX_BODY_OCTETS` to 65,536. |
| H-2 signed offer-core member has no defined input | **CLOSED** | SPEC-007 CON-806 now states `descriptorDigest = SHA-256(RFC8785(D))` over the complete six-member descriptor. cbcl-bus CON-035 requires the unique descriptor whose canonical `relayOrigin` equals the v2 carrier relay origin, and refuses duplicate origins, an origin-only projection, another equal-priority descriptor, and a caller digest. CON-806 also requires unique operator identifiers and relay origins within one profile, so selection is total. |
| M-1 safety parent contradicts the offer-core source rule | **CLOSED** | SPEC-007 CON-806 now version-qualifies both paragraphs. One begins "For credential/v1 only". The next states that for credential/v2 the application identifier and HTTPS origin come only from the live profile bound by CON-988, and that no merely channel-authenticated peer string supplies any of them. |
| M-2 two parents disagree about the relay-origin bound | **CLOSED** | cbcl-pairing CON-027 now declares a separate `relay-origin-v2 = tstr .size (1..272)`. It states that the version-1 rule stays at 1 through 255 octets and cannot recognise a version-2 carrier. `schemas/pairing-v1.cddl:13` at `c22f2d05` is unchanged. cbcl-bus TEST-118 exercises 255, 256, 267, 272, and 273 octets and re-runs the v1 vectors. |
| M-3 post-final wallet recovery has no path after the window | **CLOSED** | SPEC-008 CON-989 adds the signed post-window route with an exporter-derived token and a commitment. cbcl-bus CON-036 adds `POST /selfsame/pairing/v2/status` with a closed CBOR request and a four-arm response union. cbcl-pairing CON-032 adds `recover_receipt` and the private recovered-receipt authority. cbcl-bus CON-012 binds the commitment into `FinalStatusCoreV2`. A narrower residual survives as [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-4 A post-payload refusal strands the claimant recovery precondition]]. |
| L-1 two parents decompose one claimant differently | **CLOSED** | SPEC-007 CON-807 now states five ordered owned functions and names all five. They match SPEC-008 CON-986's list exactly. |
| L-2 profile grammar attributed to an absent SPEC-004 amendment | **CLOSED** | SPEC-007 CON-806 now states that this contract defines `cbclPairingRelays` and that no SPEC-004 amendment supplies the member. |
| L-3 registry module comment cites the excluding contract | **OPEN, NON-BLOCKING** | The comment survives unchanged at `0220cec2`; see [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-10 The registry module comment cites the contract that excludes it]]. |
| L-4 one version constant declared twice under one name | **CLOSED** | SPEC-008 REQ-1005 now states that the two baseline sites are one recorded duplication, that `profile::PROFILE_VERSION` stays the sole definition, and that the crate root may re-export but not redefine it. `crates/selfsame-app-identity/src/profile.rs:60` and `crates/selfsame-app-identity/src/lib.rs:109` still both read `pub const PROFILE_VERSION: i64 = 1;`. |

## Disposition of the 0.5.1 findings

Each row is independently re-derived at the current candidates.

| 0.5.1 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 declared hub test set exists only in trajectory | **CLOSED** | cbcl-bus REQ-081 declares TEST-115 through TEST-119, and all five headings exist. SPEC-008 CON-985 names the same range. Every Selfsame and cbcl-pairing test the parents declare resolves as a heading. |
| H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | cbcl-pairing REQ-031 names the carrier ceremony ID the sole credential/v2 ceremony identifier. It reaches the carrier, `ci`, the public context, every logical body, the seven hub commands, five hub responses, the checkpoint salt, the receipt, the recovery request, `FinalStatusCoreV2`, and the installed record. |
| H-2 reconnect recovery has no command | **CLOSED** | cbcl-bus CON-012 carries `selfsamepairstatus` with a 16-octet recovery nonce, a socket-generation digest, and a 64-octet device recovery proof. REQ-082 rebinds the pending record atomically and advances no phase. |
| H-3 four offer-core members carry no grammar | **CLOSED** | `OfferCoreV2` closes at twenty members and the reviewer counted twenty. `deviceDid` carries an exact 56-octet `did:key` grammar with a multicodec check. SPEC-053 line 7881 keeps `applicationAccountDisplay`, `platformBindingId`, and `returnUri` in a negative row only. |
| H-4 rendezvous dependency has no consumer | **CLOSED** | SPEC-008 CON-986 forbids every Selfsame and did-crdt rendezvous edge on the credential/v2 path. GATE-04 carries the matching box. did-crdt SPEC-037 0.1.2 keeps the deprecation and burns REQ-370, CON-370, ADR-370, and TEST-370. |
| H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The credential/v2 path reads and writes no rendezvous slot. did-crdt SPEC-037 0.1.2 states that it changes nothing in `src/service/rendezvous.rs`, its capacity, its buffering, its CORS behavior, or its deployed routes. |
| H-6 REQ-812 amendment uses an unavailable channel | **CLOSED** | SPEC-007 0.3.1-draft states the credential/v2 boundary directly in REQ-812 and CON-807. SPEC-008 carries no `amends` frontmatter member. All four parents name the same coordinated set. |
| H-7 hub names a cbcl-pairing test that does not exist | **CLOSED** | cbcl-bus TEST-115 and SPEC-008 TEST-1156 both name cbcl-pairing TEST-063 with the same accepted and refused control-body rows. TEST-063 exists at SPEC-001 line 2106. |
| M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | SPEC-008 CON-986 scopes the prohibition to the credential/v2 path and states that the standing SPEC-004 caller remains unchanged. That caller is `src-tauri/src/app_grant.rs:539` inside `commit_pairing_trust`. |
| M-2 exact-pair consent boundary has no owning function | **CLOSED** | SPEC-008 CON-986 gives `authorise_claimant_relay` the prompt and the provisional capability, and gives `prepare_claimant` the CON-903 write after CPace binding. `cbcl_pairing_relay_approve` and `cbcl_pairing_relay_decline` are the only UI entry points. |
| M-3 `WIRE_VERSION` names half of its namespace | **CLOSED** | SPEC-008 REQ-1005 states that `WIRE_VERSION` governs both link-offer and link-grant records. `crates/selfsame-core/src/record.rs:329`, `:396`, `:462`, and `:490` carry the offer and grant sites. |
| M-4 did-crdt worst-case store rises seventeen-fold | **CLOSED** | SPEC-037 0.1.2 makes no change to the rendezvous service. `src/service/rendezvous.rs` at `1f409a42` keeps `MAX_SLOT_BYTES` at 4,096, `SLOT_TTL_SECONDS` at 600, and `MAX_SLOTS` at 65,536. |
| L-1 executor order stated in three places with two shapes | **CLOSED** | SPEC-008 CON-902, CON-986, and TEST-1158 state one order. It is re-derivation, preview equality, issuer creation, signing, publication, closure verification, WebFinger verification, grant construction, and payload construction. |
| L-2 schema-migration enumeration misses a third site | **CLOSED** | SPEC-008 NFR-928 names `cbcl-chat-roomcfg:transform-room/1` as the third schema migration. The `mnesia:transform_table` call sits at `apps/cbcl_chat/src/shell/cbcl-chat-roomcfg.lfe:82`. |
| L-3 two failure modes misdescribe the pinned code | **CLOSED** | SPEC-008 labels FM-1 and FM-2 historical and repaired. `src-tauri/src/cbcl_pairing.rs:20` imports `MaybeTlsStream`, and the non-demo branch reaches `cbcl_transport::connect_wss` at `:177`. |
| L-4 stale `cbcl_registry.rs` comment | **OPEN, NON-BLOCKING** | See [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-10 The registry module comment cites the contract that excludes it]]. |

## Hard-stop disposition, F-A through F-E

Each row is re-derived from the candidate text and the pinned code.

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | SPEC-008 CON-903 keys the sealed set on `(applicationId, relayOrigin)`. Line 388 excludes a relay-only key, a global allowlist, a conformance-digest registry, and an application wildcard. REQ-906 states that relay-only trust never authorizes a new application, and REQ-1006 requires a new prompt when a profile names a previously accepted relay under another application. The replaced control is `crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93`. [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-2 An ordinary-build registry prohibition contradicts two current tests]] concerns the removal scope, not the exact-pair rule. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input and none from the peer input. It forbids generic `DisplayField` values and the wire `authority_summary`, and it requires byte equality on every overlapping value before display allocation. The pinned surface still carries both prohibited shapes: `src-tauri/src/cbcl_pairing.rs:416` passes `authority_summary`, and `:420` to `:424` map generic label and value fields; `src/pairing.js:104` to `:112` render them. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | `assemble_claimant` derives the scope at `src-tauri/src/cbcl_context.rs:279`, derives the identity at `:285`, creates the issuer at `:288`, verifies WebFinger at `:306`, and resolves the closure at `:317`. All precede `connect_wss` at `src-tauri/src/cbcl_pairing.rs:177`. SPEC-008 CON-902, CON-986, and SPEC-007 REQ-812 move every one of those after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end | **CLOSED** | cbcl-bus REQ-082 allocates `applicationAccountId`, `accountScopeId`, and `snapshotNonce` inside one authenticated level-1 transaction. It replaces the `record.account_scope_id` read at `src-tauri/src/cbcl_context.rs:279`. The stable principal commits the raw account identifier through a SHA-256 preimage, and CON-012 excludes the raw identifier from `OfferCoreV2`. The derivation stays namespaced, because `crates/selfsame-app-identity/src/hierarchy.rs:267` and `:272` place the account node under the application node. |
| F-E what does and does not prove relay operatorship | **CLOSED** | SPEC-008 ADR-902 states that the application profile does not prove relay operatorship, and that exact-pair consent contains the residual without claiming to solve it. SPEC-007 CON-806 keeps the operator and privacy policy display-only. TEST-915 keeps the two-operator depth test open with a named owner. No parent contradicts the statement. |

## Inherited SPEC-007 REQ-812 boundary

REQ-812 remains a hard stop and now states the credential/v2 boundary directly.
The reviewer re-tested it against SPEC-008 and cbcl-bus.

| REQ-812 clause | Coordinated agreement |
|---|---|
| no application identity effect before preliminary approval | SPEC-008 REQ-902, CON-902, CON-986, and TEST-1158 agree. |
| preliminary approval authorizes one zeroizing derivation and a pure preview | SPEC-008 CON-986 authorizes one custody call and forbids signature, publication, grant, alias, bundle, and durable write. `src-tauri/src/custody.rs:312` already exposes `use_hierarchy_root`. |
| no signing, publication, resolution, issuance, persistence, staging, or payload before final approval | SPEC-008 CON-986 and cbcl-bus REQ-082 agree. The hub performs no account, alias, grant, membership, room, or successor write at preliminary approval. |
| final approval authorizes only the declared causal construction | SPEC-008 CON-986 lists re-derivation, preview equality, issuer creation, signing, publication, closure resolution, WebFinger verification, grant construction, and payload construction in that order. |
| one encrypted non-authorizing completion checkpoint before the first causal effect | SPEC-008 CON-986 requires the sealed `PendingCredentialV2Completion` first, and forbids a hierarchy root, derived key, issuer key, grant key, signature, publication result, or application capability inside it. |
| browser staging is crash-safe and non-authorizing until immutable acceptance | cbcl-bus CON-012 defines `browser-staging-receipt-v2` with fourteen members, thirteen signed, and a 4,608-octet bound. The hub verifies it before its acceptance witness. |
| compensation touches only ceremony-owned effects | SPEC-008 CON-986 routes compensation through SPEC-004 CON-204 and forbids a change to operator withdrawal state. SPEC-004 CON-204 confers no authority on the earlier publication order. |

The boundary holds. [[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-3 The offer window has no stated terminal effect]] does not breach it. That finding leaves the window for the authorized work undetermined.

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact `(applicationId, relayOrigin)` TOFU resists a new application; operatorship residual honest | **YES.** CON-903 keys on the exact pair, REQ-1006 requires a fresh prompt for a new application on an accepted relay, and ADR-902 states the residual plainly. |
| 2 CON-220 steps 1 to 5 authenticate only the origin candidate; CPace binds the same digest first | **YES.** CON-988 returns `OriginRecognisedProfileCandidate`, denies it verifier, display, policy, and key authority, and requires both Finished values before the durable row, the display, or profile-key use. |
| 3 no peer, carrier, DOM, verifier, route, or caller value reaches a consent screen early | **YES, with one unverifiable member.** CON-029 builds the display only from the authority input after byte equality, and the verifier returns a verdict only. `authorityStatusDigest` has no wallet check and no defined preimage; see N-1. |
| 4 every effect follows its exact consent boundary | **YES.** REQ-902, CON-902, CON-986, SPEC-007 REQ-812 and CON-807, cbcl-bus REQ-082, and TEST-1158 state one ordering with no gap. |
| 5 hub fresh scope breaks the enrolment cycle without leaking the account identifier | **YES.** REQ-082 allocates inside the authenticated transaction, the principal commits a 32-octet CSPRNG preimage, and `OfferCoreV2` carries no raw account identifier. The derivation stays under the application node. |
| 6 one carrier ceremony identifier throughout | **YES.** The value reaches the carrier, `ci`, the public context, every body, every command, five responses, the checkpoint salt, the receipt, the recovery request, the final status, and the installed record. One checkpoint member carries no label; see N-7. |
| 7 two implementations reproduce context, transcript, schedule, Finished, nonces, AAD, hashes, predecessors | **NO.** CON-031 now states the thirteen-member context, `TH`, `PRK`, all seven expands, both Finished values, the counter and nonce rule, and the AAD map. The object hash still lacks a kind assignment, and one body member hashes an undefined object; see N-5 and N-1. |
| 8 eleven body grammars exist; 50,221 recomputes; padding and frames agree | **YES for the arithmetic.** The reviewer recomputed the payload body at its maxima as 1 + 201 + 50,019 = 50,221 deterministic-CBOR octets. Nine grammars sit in CON-987, the offer in CON-012, and the receipt in CON-028. |
| 9 `descriptorDigest` binds one uniquely selected six-member descriptor; v1 and v2 origin rules disjoint | **YES.** CON-806 fixes six members and unique origins, CON-035 selects by exact origin equality and refuses duplicates, and CON-027 separates `relay-origin-v2` from the unchanged v1 rule. |
| 10 migration digest graph and staging receipt exact, bounded, recomputable, free of caller booleans | **YES for both objects.** Every preimage carries a domain string, a length prefix where needed, and a stated order. The reviewer computed the staging receipt maximum as 3,049 octets against a 4,608-octet bound. |
| 11 retries, rebinding, checkpoints, status, and crash boundaries free of duplicate effects | **PARTLY.** No duplicate identity effect exists. A post-payload allocator refusal strands the recovery precondition; see N-4. |
| 12 recovery cannot cause false installation or unsafe pending deletion | **YES for false installation.** Only verified accepted status installs, and every other class leaves pending unchanged. A signed `not-finalized` needs locked terminal absence, and rotation needs an explicit prompt. Service data erasure of an accepted status permits one signed negative for a ceremony that did finalize; the wallet then fails closed with no capability. |
| 13 the nine declared bounds agree arithmetically | **YES.** Large object 64,560 plaintext plus a 16-octet tag fits 69,632 and 70,000. Offer 56,625, payload 50,221, and receipt 8,373 fit 62,000. Padding 64,512 exceeds 62,000, and 4,096 exceeds 2,048. Recovery request 2,187 fits 2,304, and the largest response 8,294 fits 9,216. |
| 14 activation cannot precede immutable signed acceptance | **YES.** CON-986, SPEC-007 CON-807, and cbcl-bus REQ-082 order inactive staging, the single final transaction, JWS verification under the offer `kid`, activation, receipt, and installation. |
| 15 hub authentication, locks, capacity, expiry, migration, tombstone, response grammar, limiter, retention, staging total | **NO.** Both lock orders, the capacity bounds, the phase projection, the terminal classes, the limiter, and the retention rule are stated. Four of seven commands carry no response grammar, and the expiry rule is open; see N-1 and N-3. |
| 16 did-crdt rendezvous absent; the sole HTTPS exception is signed status recovery | **YES.** CON-986, GATE-04, cbcl-bus TEST-117, and did-crdt SPEC-037 0.1.2 agree. No credential/v2 offer, decision, preparation, payload, or ordinary receipt leaves the cbcl-pairing relay. |
| 17 credential/v1 bytes and production behavior frozen | **YES in the wire rules.** REQ-031, TEST-063, and REQ-1005 require byte-identical v1 output and no v2 reach into a classified v1 site. One requirement can be read to remove a live v1 control; see N-2. |

## Repository and code-claim assessment

Every testable code claim resolved exactly at its stated baseline. This remains
the strongest part of the reissue.

**Selfsame version sites.** All eighteen witness rows in SPEC-008 REQ-1005
resolve at `0220cec2` with the stated class.
`crates/selfsame-web-device/src/lib.rs:600` reads
`selfsame_app_identity::PROFILE_VERSION`. Lines `:1346` and `:1360` read
`profile_version: 1`, and `:1369` reads `profile_version: core.profile_version`.
`crates/selfsame-app-identity/src/ceremony.rs:185` and `:190` emit members,
`:308` and `:510` compare, and `:460` emits.
`crates/selfsame-app-identity/src/enrollment.rs:373`, `:394`, `:552`, and
`:592` compare; `:452` assigns; `:662` and `:666` emit.
`crates/selfsame-app-identity/src/profile.rs:385`,
`crates/selfsame-app-identity/src/provider_hint.rs:115` compare, and `:67`
emits.

**The three ingest substitutions.** `crates/selfsame-web-device/src/lib.rs:831`,
`:847`, and `:861` each read `profile_version: value.profile_version`. The
reviewer ran the closed five-identifier scan over every production Rust file in
`crates` and `src-tauri/src`. It returned 69 matches. Every remaining match is
a `#[cfg(test)]` line, a member-name constant, a type declaration, a format
template, a comment, or a continuation of a named witness. No fourth production
ingest substitution exists, so TEST-1157's three-substitution row holds.

**The recorded duplication.** `crates/selfsame-app-identity/src/profile.rs:60`
and `crates/selfsame-app-identity/src/lib.rs:109` both read
`pub const PROFILE_VERSION: i64 = 1;`. REQ-1005's disposition matches.

**Excluded namespaces.** `WIRE_VERSION` resolves at
`crates/selfsame-core/src/record.rs:329`, `:396`, `:462`, and `:490`, covering
both link-offer and link-grant records. `CODE_VERSION` resolves at
`crates/selfsame-core/src/code.rs:78`. Neither identifier appears in the
five-identifier scan set, so the exclusions are consistent.

**cbcl-bus transaction and consumer claims.** Every line SPEC-008 NFR-928 names
resolves at `ce894a7f` under `apps/cbcl_chat/src/shell/`.
`cbcl-chat-session-ws.lfe:347` and `:385` read `unratified-device?`. `:512`
reads `device-grants`, `:869` reads `rooms-for-account`, and `:965` reads
`list-public`. `:1050` reads `member-grant?`, `:1076` reads the eviction path,
and `:1683` reads `grants`. `:1788` reads the hello `get-or-start`, and `:1898`
reads the discarded `claim/5`. `cbcl-chat-room.lfe:201` and `:221` read room
configuration, and `:263` to `:270` are the boolean contexts. `:550` reads
`get-dialects`, and `:677` reads `peer-grants`.
`cbcl-chat-pairgrant.lfe:78` reads `member-grant?`.
`cbcl-chat-rooms.lfe:49`, `:54`, `:60`, `:61`, and `:99` to `:104` match.
`cbcl-chat-path-b-poller.lfe:17` and `:32` match.
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76` matches.

**Two identified defects in the pinned hub.** `cbcl-chat-successor.lfe:109` and
`:134` both wrap their calls in `catch`, and `:134` calls transaction-opening
`cbcl-chat-account-grants:device-grant`. `cbcl-chat-account-grants.lfe:180`
calls transaction-opening `cbcl-chat-members:pubkey` from inside
`bind-and-store-unbound/8`. NFR-928 states the corrected shape for all three.
Its present-tense sentence about `pubkey-tx/1` reads as the required state, and
the following prohibition removes the ambiguity.

**Scale of the declared inventory.** The reviewer counted 72 production
`mnesia:transaction` call sites under `apps/`, excluding test directories.
NFR-928's inventory is therefore large, bounded, and enumerable.

**Baseline claims about the claimant.** The camera-scan claim holds in
substance and misses in its line range; see
[[spec-008-0.5.3-claude-adversarial-review-2026-08-24#N-11 One code citation names the wrong line range]].
`src/pairing.js:159` defines `async function scan()`, and
`src-tauri/capabilities/mobile-scanner.json` grants exactly three
barcode-scanner permissions. FM-3 holds: the demo fixture at
`crates/selfsame-pairing/src/local_demo.rs:15` declares
`LOCAL_CONFORMANCE_DIGEST: [u8; 32] = [19; 32]`, and
`src-tauri/src/cbcl_pairing.rs:292` is its only non-test consumer. FM-4 holds:
`src-tauri/src/cbcl_registry.rs:17` carries one ratified entry.

## Consent assessment

The two-boundary design is coherent and its ordering is total.

The preliminary boundary opens after the wallet has authenticated the live
profile, verified the signed offer under the CON-988-bound key, and checked
every overlapping equality. It authorizes one zeroizing derivation, a pure home
DID, a fingerprint, and one disclosure the screen must state.

The final boundary opens after the wallet displays its own recomputed DID and
fingerprint plus the complete authenticated transition. It authorizes exactly
the declared provisioning work.

Display authority is closed. cbcl-pairing CON-029 keeps `DisplayIntent` and
`DisplayField` private, forbids generic v2 fields and the wire
`authority_summary`, and copies every displayed value from the authority input.
The verifier returns a verdict and cannot append, replace, or mutate display
data.

The 0.3.0 rejection reason does not return. A declined ceremony hands the
application a preview DID only after an explicit screen that states the
disclosure. That DID is unique to one hub-allocated scope, so it correlates
with nothing else.

One member escapes the discipline. `authorityStatusDigest` reaches a Selfsame
body with no defined preimage, no wallet recomputation, and no signature; see
N-1.

## Wire and bounds assessment

The reviewer recomputed every declared bound.

| Object | Computed maximum | Declared bound | Verdict |
|---|---:|---:|---|
| `credential-v2-payload-body` | 50,221 | exactly 50,221 | exact |
| `signed-offer-v2` | 56,625 | at most 56,640 | inside |
| `credential-v2-receipt-body` | 8,373 | 62,000 large body | inside |
| `credential-v2-large-object` | 64,560 plaintext | 69,632 frame body | inside |
| large object plus GCM tag | 64,576 | 70,000 relay ceiling | inside |
| `credential-v2-control-object` | 4,144 plaintext | 69,632 frame body | inside |
| `browser-staging-receipt-v2` | 3,049 | at most 4,608 | inside |
| `receipt-recovery-request-v2` | 2,187 | at most 2,304 | inside |
| `receipt-recovery-not-finalized-v2` | 8,294 | at most 9,216 | inside |
| control padding | 4,096 fixed | 2,048 body maximum | consistent |
| large padding | 64,512 fixed | 62,000 body maximum | consistent |
| compact `finalStatusJws` | 8,192 | receipt body member | consistent |
| checkpoint ciphertext | 69,632 | store sequence bound | consistent |

The payload computation is the map header of one octet, 201 octets of keys, and
50,019 octets of values. The value total is nine 34-octet byte strings, one
515-octet DID, one 58-octet device DID, one 19-octet media type, and one
49,155-octet grant.

Every control body the parents declare fits the 2,048-octet control bound. The
largest is `credential-v2-binding-confirmed-body` at 775 octets when the preview
DID reaches 512 octets.

The version-1 and version-2 carriers stay disjoint. `schemas/pairing-v1.cddl`
at `c22f2d05` fixes `"version": 1`, `application-id = tstr .size (1..96)`, and
`relay-origin = tstr .size (1..255)`. CON-027 fixes `"version": 2`,
`application-id-v2 = tstr .size (1..2048)`, and
`relay-origin-v2 = tstr .size (1..272)`.

One wire value is unassigned. Field 1 of `credential-v2-object` carries a range
per arm and no kind mapping; see N-5.

## Cryptography assessment

The credential/v2 schedule is now complete inside cbcl-pairing CON-031, and the
reviewer reproduced every construction from the parent alone.

The public context contains exactly thirteen members. The first five are the
context tag, the version 2, the suite string, the profile string, and the
application identifier. The next three are the profile digest, the carrier
digest, and the relay origin. The next three are the mailbox identifier, the
carrier ceremony identifier, and the claim commitment. The last two are a
nullable expected allocator key and a literal `null` for the absent claimant
key. Every position carries a naming comment, and line 1958 states the count.

`TH` is SHA-512 over deterministic CBOR of the public context and both CPace
frames as byte strings. `PRK` is HKDF-Extract-SHA512 with an empty salt over
`ISK`. The seven expands carry distinct ASCII labels concatenated with `TH` and
produce 32, 32, 32, 32, 12, 12, and 32 octets in the stated order.

Both Finished values are HMAC-SHA-512 under their key-confirmation keys and
contain 64 octets. The nonce is the direction IV exclusive-ored with a 96-bit
big-endian counter carrying four leading zero octets. The AAD is a four-member
map with `v`, `direction`, `counter`, and `th`.

The recovery secrets are exact. `receiptRecoveryToken` is
HMAC-SHA-256 over the exporter with a null-terminated label and `TH`.
`receiptRecoveryCommitment` is SHA-256 over a null-terminated label, the token,
the ceremony, and the application identifier.

The migration digest graph is exact. Every preimage carries a null-terminated
domain string, and `rawPrimaryKey` carries a four-octet big-endian length
prefix before the room bytes.

Two gaps remain. The kind integers are unassigned, and one Selfsame body member
hashes an object no parent defines.

## Recovery and atomicity assessment

The post-window route closes the 0.5.2 M-3 defect.

The wallet seals `PendingCredentialV2Completion` before the first causal
effect. The browser writes an inactive staging record before the hub command.
The hub commits one level-1 transaction that writes every row and deletes the
legacy rows together.

`selfsamepairstatus` recovers a lost allocation response under the persisted
installation key, rebinds the socket generation atomically, and advances no
phase. Only authenticated absent status permits a fresh request.

`POST /selfsame/pairing/v2/status` recovers a lost receipt after the relay
window. It refuses redirects, content encoding, credentials in a URI, an
oversized request, and an oversized response. A wrong token receives the same
status, body size class, and timing class as an unknown ceremony.

A signed negative needs locked terminal absence. An unavailable, corrupt,
ambiguous, busy, or lock-exhausted read never becomes `not-finalized`.

Two residuals stand. A post-payload allocator refusal strands CON-032's
precondition; see N-4. Service data erasure of an accepted status permits one
signed negative for a ceremony that finalized, and the wallet then fails closed
rather than installing.

## Persistence assessment

The wallet slot is one tagged pending-or-installed union, never two records.
The installed record carries thirteen named values plus the immutable hub
status and digest.

Reload verification is total. It distinguishes unavailability from revocation
and absence. It allows a silent re-pin only for a profile-only digest refresh.
It prompts on authority or issuer rotation and refuses a changed handle.

Unlink removes the application record, the person-selected exact-pair policy,
and application-owned cached material. It retains the root seed, claims no
remote revocation, and forbids a local re-grant shortcut.

The hub side is bounded. The pending record encodes to at most 69,632 octets,
and the active table holds at most 65,536 records. One active record exists per
exact application, handle, and enrolled key.

`src-tauri/src/store.rs` already carries the durability probe the design needs.
`src-tauri/src/custody.rs:312` already exposes the zeroizing hierarchy-root
closure.

One derivation is imprecise. The claimant checkpoint wrapping-key info states
no combination rule; see N-6.

## Rendezvous assessment

The credential/v2 call graph carries no rendezvous edge.

SPEC-008 CON-986 forbids every Selfsame and did-crdt rendezvous read, write,
route, and client edge on that path. GATE-04 carries the matching box, and
cbcl-bus TEST-117 traces every object.

did-crdt SPEC-037 0.1.2 retains the deprecation and records the new HTTPS
exception. It changes nothing in `src/service/rendezvous.rs`, whose bounds at
`1f409a42` remain 4,096 slot octets, 600 seconds, and 65,536 slots.

The credential/v1 rendezvous edge in the browser survives by design.
`apps/cbcl_chat/priv/web/app.js:1697` still declares
`PAIRING_RELAY = 'http://localhost:8080/selfsame'` for the version-1 path.

## Compatibility assessment

Credential/v1 wire behavior stays frozen in the wire rules. cbcl-pairing
REQ-031 keeps the v1 carrier secret, CPace schedule, display, bytes, and
decisions. TEST-063 re-runs every base outcome byte-identically.

SPEC-008 REQ-1005 forbids credential/v2 from reaching a classified
credential-v1 site and from calling `EnrolmentAllocator::new_native`.

The `anuna-ssi` pin resolves two namespaces only. The parents state the
cross-vault deferral explicitly and claim no design authority from it.

One requirement can be read to remove a live credential/v1 control; see N-2.

## Authority, pointer, and singularity assessment

The four parents agree on provenance. Each records
`generation-model-family: OpenAI GPT-5`, `generation-model-version: gpt-5.6-sol`,
and `generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6`. did-crdt
SPEC-037 records the same session.

The version pointers are reciprocal and correct. SPEC-007, SPEC-001, and
SPEC-053 each name SPEC-008 0.5.3-draft, and SPEC-008 CON-985 names the other
three at their exact versions.

The declared sets resolve. cbcl-bus REQ-081 declares TEST-115 through TEST-119
and all five headings exist. cbcl-pairing TEST-063 declares TEST-001 through
TEST-029 and TEST-060 through TEST-067, and all thirty-seven headings exist.
SPEC-008 declares TEST-901 through TEST-915 and TEST-1156 through TEST-1161,
and all twenty-one headings exist.

GATE-04 contains exactly 42 open boxes. Its first box names all four parents at
their exact versions, matching cbcl-bus TEST-116.

Counts verify. `OfferCoreV2` carries 20 members. `FinalStatusCoreV2` carries 15
members. CON-987 carries 9 Selfsame body grammars. `browser-staging-receipt-v2`
carries 14 members with 13 signed.

Artefact numbering is fresh. CON-987, CON-988, CON-989, TEST-1160, TEST-1161,
and OBS-907 appear only in SPEC-008 and, where appropriate, SPEC-007. REQ-909,
TEST-916, and TEST-917 appear only in the changelog burn record, and REQ-370,
CON-370, ADR-370, and TEST-370 stay burned in did-crdt.

Anchors are fully qualified. SPEC-008 uses 43 distinct self-anchors and all 43
resolve among its 50 headings. Every SPEC-007 and SPEC-004 anchor it cites
resolves. No bare `[[#...]]` anchor exists in either Selfsame parent. Every
wikilink target resolves to a file in `specs/` at the candidate commit.

Controlled language passes. `usdd-lint.sh --type descriptive --strict` returns
0 errors and 0 warnings for SPEC-008, SPEC-007, and the complete cbcl-pairing
parent. The cbcl-bus coordinated sections from REQ-081 through TEST-119 return
0 errors and 0 warnings, which is the extraction cbcl-bus TEST-116 requires.

Two authority defects survive. One requirement contradicts two current tests;
see N-2. One declared set omits the other Selfsame parent's tests; see N-8.

## Stopping-rule assessment

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. It also requires no
design correction before the Elephant plan.

One HIGH and four MEDIUM findings survive. Each names an unresolved
implementability, consent, cryptographic, ordering, or recovery defect.

N-1 leaves a mandatory wire member hashing an undefined object, so two
implementations cannot interoperate and two declared tests cannot decide.

N-2 leaves an implementer choosing between a current requirement and two
current tests, and the strict choice stops a deployed path.

N-3 leaves the window for authorized post-consent work undetermined on both
sides.

N-4 leaves one reachable state in which the declared recovery adapter refuses
and the pending slot cannot resolve.

N-5 leaves one envelope field without an assignment, and the field enters every
object hash.

The verdict is REJECT.

## Surfaces found sound

The reviewer states these surfaces sound at this revision.

The credential/v2 key schedule is complete and reproducible from cbcl-pairing
CON-031 alone.

The pre-socket authentication and CPace profile binding are ordered correctly.
No durable policy row, consent display, or profile-key use precedes both
Finished values.

The exact-pair TOFU rule is sound. A new application on an accepted relay
prompts, and the operatorship residual is stated rather than claimed away.

The two consent boundaries and their effect lists are total and agree across
four parents.

The hub allocation and final migration are atomic, bounded, and lock-ordered.
The account principal keeps the raw account identifier hub-private.

The descriptor digest, the migration digest graph, and the browser staging
receipt are exact, bounded, and independently recomputable.

Every declared numeric bound agrees arithmetically, and the payload maximum
recomputes to exactly 50,221 octets.

The rendezvous removal is complete and the single HTTPS exception is narrow and
signed.

Every code claim in every parent resolves exactly at its stated baseline. The
production version scan is complete under its own classifier.

Controlled language, anchors, wikilinks, counts, provenance, and artefact
numbering all pass.

## Residual assumptions

This review assumes the pinned baselines are the true implementation substrate.
It ran no build, no test suite, and no relay.

It assumes RFC 8785 and RFC 8949 deterministic encoding behave as their
specifications state.

It assumes the human cryptography review that cbcl-pairing OQ-001 holds open
will examine the CPace group choice and the exporter construction.

It accepts the published orphan DID as an existing residual. SPEC-004 CON-204
states that no authority follows the earlier publication order, so a failed
post-approval ceremony leaves an unusable published identifier and nothing more.

It accepts the relay-visible frame size variation as a disclosed residual. The
control and large arms vary by up to two octets with the encoded body length,
and GATE-04 sends relay-visible size correlation to human review.

It does not evaluate whether the 42 GATE-04 boxes are individually achievable.
It confirms only that 42 exist and stay open.

## Collected corrections and owners

| Finding | Correction | Owner |
|---|---|---|
| N-1 | State the `selfsamepairauthority` response grammar, its outcome tokens, and its authentication in cbcl-bus CON-012. State the response grammar for `selfsamepairofferprepare`, `selfsamepairofferfinalize`, and `selfsamepairabort`. Then state the wallet's check for `authorityStatusDigest`, or remove the member. | cbcl-bus owner, with selfsame owner |
| N-2 | Scope SPEC-008 REQ-1005's registry sentence to the credential/v2 path, matching CON-986, TEST-1159, and SPEC-007 TEST-817. Or state the replacement control for the credential/v1 claimant. | selfsame owner |
| N-3 | State which clock terminates `selfsamepairaccept`, what the wallet checks `OfferCoreV2.expiresAt` against, and which expiry the final effect capability binds. | cbcl-bus owner, with selfsame owner |
| N-4 | State the claimant checkpoint disposition for a post-payload refusal, and reconcile it with cbcl-pairing CON-032's `payload -> receipt` precondition. | cbcl-pairing owner, with selfsame owner |
| N-5 | Assign each of the eleven kind names its exact integer in cbcl-pairing CON-028. | cbcl-pairing owner |
| N-6 | State the exact info encoding for the claimant checkpoint wrapping key. | selfsame owner |
| N-7 | Name the fifth `credential-v2-checkpoint` member. | cbcl-pairing owner |
| N-8 | Add the carve-out sentence to SPEC-008 CON-985 for SPEC-007 TEST-801 through TEST-821. | selfsame owner |
| N-9 | Number the CON-988 construction, or drop the count. | selfsame owner |
| N-10 | Correct `src-tauri/src/cbcl_registry.rs:1` when N-2 settles the module's fate. | selfsame owner |
| N-11 | Change the camera-scan citation to `src/pairing.js:159`. | selfsame owner |

## What this verdict authorizes

This verdict authorizes one correction pass over the five blocking findings and
the six non-blocking observations.

It authorizes a further coordinated reissue and one fresh cross-model Tier-1
review of that reissue.

## What this verdict does not authorize

This verdict authorizes no implementation, no Elephant SPL, and no test-first
plan.

It authorizes no production invitation allocation, no release, and no
deployment.

It closes no GATE-04 box, no cbcl-pairing production gate, and no SPEC-007
production gate.

It changes no current requirement, contract, decision, test, or observation in
any parent.
