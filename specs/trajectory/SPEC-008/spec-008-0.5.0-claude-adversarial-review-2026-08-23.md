---
id: spec-008-0.5.0-claude-adversarial-review
title: SPEC-008 0.5.0-draft — Tier-1 adversarial review of the consolidated standalone first-contact authority
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
review-set: "selfsame `b2e787812bf46a891eec32101aff27ebba05f94c`; cbcl-bus `5ef5d399c9d3f32e0c0b7ab46f8e75fc7763490d`; cbcl-pairing `f10c328ca9588d613da8117922c7b1e76769180c`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`"
last-updated: 2026-08-23
---

# SPEC-008 0.5.0-draft — Tier-1 adversarial review of the consolidated standalone first-contact authority

Verdict: **REJECT**.

The coordinated reissue removes the amendment ledger that caused the N11
rejection. It does not make the three parents self-contained. The hub half of
the standalone architecture exists in no specification in the `cbcl-bus` vault.
Both consolidated parents also fail their own singularity tests.

## Orientation

Intent: decide whether the three coordinated parent specifications state one
implementable, internally consistent standalone first-contact design. The
mandate treats every prose claim as untrusted until code or a named current
clause supports it.

Result: ten blocking defects survive verification. One is CRITICAL, seven are
HIGH, and two are MEDIUM. Seven earlier N11 obligations close. One does not.

Controls:   the review changes no candidate, parent, code, or sibling
            repository file
            the review commits only this report
            a REJECT verdict authorizes no implementation, allocation,
            release, or deployment
Open:       every GATE-04 box (owner: named gate owners)
            the corrections in [[spec-008-0.5.0-claude-adversarial-review-2026-08-23#Collected-corrections-and-owners]]
Detail:     [[spec-008-0.5.0-claude-adversarial-review-2026-08-23#C-1--The-hub-half-of-the-design-exists-in-no-cbcl-bus-specification]],
            [[spec-008-0.5.0-claude-adversarial-review-2026-08-23#H-1--Both-parents-fail-their-own-singularity-tests]],
            [[spec-008-0.5.0-claude-adversarial-review-2026-08-23#H-4--GATE-04s-sealed-cap-box-is-unsatisfiable-at-both-pinned-rendezvous-baselines]]

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL in this document are to be interpreted as
described in BCP 14 (RFC 2119, RFC 8174) when, and only when, they appear in
all capitals.

## Reviewer identity, authentication, and independence

| Field | Value |
|---|---|
| Reviewer model family | Anthropic Claude |
| Reviewer model | `claude-opus-5` |
| Generating model family | OpenAI GPT-5 |
| Generating model version | `gpt-5.6-sol` |
| Generating session | `01a029aa-9127-7c42-ad28-81512b91ded6` |
| Cross-model requirement | satisfied; the families differ |

The launch driver removed `ANTHROPIC_API_KEY`. The reviewer verified its
absence without printing any value. The shell reported `ABSENT (unset)`.

`env -u ANTHROPIC_API_KEY claude auth status` returned these three fields:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

The gate passes. This review runs on the Claude.ai Max subscription and never
on Anthropic API billing. Email, organization, and account identifiers stay
redacted and appear nowhere in this report.

Fresh-session statement: this session began with no prior context on this
increment. The reviewer read the three parents, the named current clauses, and
the pinned source directly. The reviewer read the 0.4.26 review only to
enumerate the N11 identifiers. Every conclusion below rests on an independent
re-derivation against code or a named clause.

Circus attempt: `circus/spec008-consolidated-review/1`, in the worktree
`/Users/anuna-01/Code/selfsame-spec008-consolidated-review-1`. Completion
signals through the attempt sentinel.

## Exact review set and baselines

| Repository | Reviewed commit | Document | Version |
|---|---|---|---|
| selfsame | `b2e787812bf46a891eec32101aff27ebba05f94c` | `specs/SPEC-008-production-pairing-claimant.md` | `0.5.0-draft` |
| cbcl-bus | `5ef5d399c9d3f32e0c0b7ab46f8e75fc7763490d` | `specs/SPEC-053-key-root-identity.md` | `0.17.0-draft` |
| cbcl-pairing | `f10c328ca9588d613da8117922c7b1e76769180c` | `specs/SPEC-001-reusable-blind-pairing.md` | `0.4.0-draft` |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | baseline only | — |
| anuna-ssi | `c7d462029841ea1884bb6f089732058d8838728d` | namespace pin only | — |

Every HEAD matched the named commit before use. The reviewer verified each with
`git rev-parse HEAD`.

Implementation baselines:

| Repository | Baseline | Non-specification files changed |
|---|---|---|
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | 0 of 25 changed paths |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | 0 of 13 changed paths |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | 0 of 7 changed paths |

Every difference from every implementation baseline is specification-only. The
reviewer derived this from `git diff --name-only BASELINE HEAD` filtered
against `specs/`.

Worktree status at review time:

| Repository | Status |
|---|---|
| selfsame worktree | clean |
| cbcl-bus | clean; `.zetl/` is ignored |
| cbcl-pairing | user-owned untracked `.DS_Store`, `fuzz/.DS_Store`, `src/.DS_Store`, `.zetl/`, `specs/.zetl/` |
| did-crdt | user-owned modified `specs/did-crdt-spec.md`; untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md` |

The reviewer modified, cleaned, staged, committed, and removed nothing in any
sibling checkout. The cbcl-pairing dirt matched the session-start snapshot
exactly at the end of the review.

## Severity counts

| Severity | Count |
|---|---|
| CRITICAL | 1 |
| HIGH | 7 |
| MEDIUM | 4 |
| LOW | 4 |

Every CRITICAL and HIGH finding blocks the gate. Two MEDIUM findings also
block. M-1 blocks because TEST-1157 requires one pointer set. M-2 blocks
because F-B is a hard stop. M-3, M-4, and all four LOW findings do not block.

## Findings

### C-1 — The hub half of the design exists in no cbcl-bus specification

Class: implementability. Severity: CRITICAL. Blocking.

The architecture requires hub allocation of a pending `applicationAccountId`
and `accountScopeId` before carrier release. Durability arrives only at the
final migration transaction.

The Selfsame parent states that obligation in
[[SPEC-008-production-pairing-claimant#CON-902]] at lines 283 to 286:

> The hub allocates the pending identifiers before public-carrier release and
> binds them into the signed offer. They become durable account identity only
> in the final hub migration transaction.

The token `applicationAccountId` appears nowhere in the `cbcl-bus` vault. A
recursive search over `/Users/anuna-01/Code/cbcl-bus/specs/` returned zero
files. That search covered the parent and every trajectory document.

The token `accountScopeId` appears three times in the `cbcl-bus` vault. One
occurrence sits in the parent's threat-model prose at line 4306. Two sit in
review dispositions. None allocates a value.

`[[SPEC-053-key-root-identity#REQ-081]]` states no allocation obligation.
`[[SPEC-053-key-root-identity#GATE-04]]` states acceptance boxes and no
mechanism. A gate box is an acceptance criterion, never a specification of the
mechanism it accepts.

The pinned hub code names the gap in its own comment.
`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-http.lfe:59-64` reads:

> PROTOTYPE POSTURE … this still signs a caller-supplied statement with the
> deployment's enrolment key … It does NOT yet construct the statement from
> authenticated, single-use server-side ceremony state — that is the Tier-1
> release work.

The browser allocator generates the scope itself today.
`apps/cbcl_chat/priv/web/enrol-allocator.mjs:61-68` passes
`randomBytes(32)` as the account scope into `wasm.EnrolmentAllocator`. No hub
call allocates it, and no `applicationAccountId` exists on that path.

The hub declares its competent channels at
`specs/SPEC-053-key-root-identity.md:229-239`. That table names the repository
owner, selfsame SPEC-004, selfsame SPEC-007, cbcl-pairing SPEC-001, anuna-ssi
SPEC-001, SPEC-065, and an accepted issue. It never names selfsame SPEC-008.

A Selfsame clause therefore cannot land a hub obligation.
[[PROTO-001]] treats an amendment outside a declared channel as a request, not
an instruction. The obligation stays unlanded in the repository that must
implement it.

Consequence: F-D cannot close. An implementer reading the coordinated review
set finds no hub requirement, contract, test, or grammar for pending
allocation, offer signing, offer verification, or pending deletion.

Minimum correction: author the hub mechanism in
`[[SPEC-053-key-root-identity]]` as `REQ`, `CON`, and `TEST` artefacts under a
versioned owner edit. The contract SHALL state the allocation command, its
authenticated precondition, the signing key, the wallet-side verification rule,
the bounded pending record, and every deletion trigger. Owner: cbcl-bus
repository owner.

### H-1 — Both parents fail their own singularity tests

Class: authority. Severity: HIGH. Blocking.

[[SPEC-008-production-pairing-claimant#TEST-1157]] at lines 1089 to 1091 states:

> Within the consolidated current-law section, require no `adopts`,
> `supersedes`, exact-phrase replacement, partial-range replacement,
> retirement chain, or historical precedence rule.

[[SPEC-008-production-pairing-claimant#CON-985]] sits inside that section at
line 859. Its first paragraph, lines 861 to 863, states:

> Every mechanism retirement and replacement recorded through CON-981 remains
> effective unless this parent states otherwise. Pointer, survivor-set,
> review-set, and test-set values from CON-981 do not survive.

That paragraph is three of the six prohibited forms at once. Sentence one is a
retirement chain and a historical precedence rule. Sentence two is a
partial-range replacement over CON-981's value ranges.

TEST-1157 therefore fails against CON-985 by construction. A current test in
the current-law section contradicts a current contract in the same section.

The hub repeats the defect exactly.
[[SPEC-053-key-root-identity#TEST-116]] at lines 7112 to 7114 carries the same
prohibition. [[SPEC-053-key-root-identity#REQ-081]] at lines 6563 to 6565
carries the same three forms.

This is not a wording preference. The consolidation's stated purpose is a
parent that states current law directly.
[[SPEC-008-production-pairing-claimant#ADR-963]] at line 901 asserts that
purpose. The purpose fails on the parent's own test.

Minimum correction: replace both anchor paragraphs with direct statements of
the surviving mechanism. State the mechanism in the parent instead of naming a
trajectory clause that recorded it. Owner: repository owner for each parent.

### H-2 — Each parent declares its own authority anchor historical

Class: authority. Severity: HIGH. Blocking.

[[SPEC-008-production-pairing-claimant#CON-985]] makes the retirements recorded
through CON-981 effective at line 861. Its second paragraph, at lines 866 to
870, states:

> REQ-998 through REQ-1004, NFR-921 through NFR-927, OBS-905 and OBS-906,
> CON-978 through CON-984, ADR-956 through ADR-962, and TEST-1142 through
> TEST-1155 supply historical evidence only.

CON-981 lies inside `CON-978 through CON-984`. The same contract therefore
makes CON-981 effective and historical in adjacent paragraphs.

The hub repeats it. [[SPEC-053-key-root-identity#REQ-081]] preserves the
retirements recorded through REQ-077 at line 6563. It then places
`REQ-074 through REQ-080` in the historical class at line 6568. REQ-077 lies
inside that range.

A third sentence deepens the conflict. Both parents state that no unlisted
trajectory authority value supplies normative precedence. The whole surviving
mechanism lives in unlisted trajectory clauses. Selfsame 0.4.0 through 0.4.18
and cbcl-bus 0.16.1 through 0.16.11 carry it.

Two readings follow, and they produce different implementations. Under the
first reading the trajectory mechanism binds and the design is buildable. Under
the second reading it does not bind and the design has no mechanism at all.

The stopping rule names hard-stop ambiguity as a REJECT condition. This is that
ambiguity, on the authority anchor of a Tier-1 no-go area.

The reviewer verified that the two retirement lists are arithmetically exact
for their declared bands. Enumeration of every candidate heading gives these
totals.

| Band | Artefacts enumerated | Covered by the parent's two lists |
|---|---|---|
| Selfsame 0.4.19 through 0.4.26 | REQ-998–1005, NFR-921–928, OBS-905–907, CON-978–985, ADR-956–963, TEST-1142–1157 | all |
| cbcl-bus 0.16.12 through 0.16.19 | REQ-074–081, NFR-019–026, OBS-013–015, ADR-030–037, TEST-101–116 | all |

Both lists partition their bands with no gap and no overlap.

The arithmetic is correct. The construction is not.

Minimum correction: remove the anchor clause from each historical range, or
state the surviving mechanism directly and delete the anchor sentence. Owner:
repository owner for each parent.

### H-3 — The claimed DEC-012 pointer does not exist

Class: authority. Severity: HIGH. Blocking.

[[SPEC-008-production-pairing-claimant#CON-985]] at line 888 states:

> The cbcl-bus SPEC-053 DEC-012 parent clause names this parent.

[[SPEC-008-production-pairing-claimant#TEST-1157]] at lines 1112 to 1113
states:

> Require this Selfsame parent and its open review gate. Require the cbcl-bus
> DEC-012 pointer at parent version 0.17.0-draft.

`[[SPEC-053-key-root-identity#DEC-012]]` spans lines 640 to 699 of the hub
parent. A search of that span for `SPEC-008`, `Selfsame`, `selfsame`, `0.5.0`,
and `0.17.0` returns nothing.

The whole hub parent mentions `SPEC-008` exactly three times, at lines 6580,
6597, and 7118. All three sit in REQ-081, GATE-04, and TEST-116. None sits in
DEC-012.

The hub's own text agrees with the code and disagrees with Selfsame.
[[SPEC-053-key-root-identity#REQ-081]] at line 6584 states that DEC-012 and
GATE-04 state the hub's deferral and release conditions. It never claims that
DEC-012 points at Selfsame.

A current Selfsame contract therefore states a false fact about the coordinated
hub parent. A current Selfsame test then requires that false fact. TEST-1157
cannot pass against the pinned hub commit.

Minimum correction: delete the DEC-012 pointer claim from CON-985 and
TEST-1157, or add an explicit Selfsame parent pointer to DEC-012 through the
hub's owner channel. Owner: repository owner for both parents.

### H-4 — GATE-04's sealed-cap box is unsatisfiable at both pinned rendezvous baselines

Class: implementability. Severity: HIGH. Blocking.

[[SPEC-053-key-root-identity#GATE-04]] box 39, at lines 6648 to 6649, requires:

> opaque sealed cap bodies of 57,016 and 62,016 octets transit both rendezvous
> implementations, and 69,633 octets is rejected;

Both pinned rendezvous implementations refuse a body above 4,096 octets.
`did-crdt/src/service/rendezvous.rs:65` declares
`pub const MAX_SLOT_BYTES: usize = 4096`. Line 158 refuses when
`body.len() > MAX_SLOT_BYTES` and answers `400 Bad Request`.

`selfsame/crates/selfsame-rendezvous/src/lib.rs:48` declares the same constant
with the same value.

`[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]` requires acceptance of
`1..69,632` octets. `[[PROTO-002-selfsame-rendezvous-v1#CON-304]]` requires
`413 Content Too Large` above that bound. Neither implementation meets either
obligation.

The rendezvous carries the credential path today.
`apps/cbcl_chat/priv/web/enrol-allocator.mjs:16` sets
`const RENDEZVOUS = 'https://did.anuna.io'`. Line 90 writes the sealed offer to
`${RENDEZVOUS}/rendezvous/${allocator.offer_slot}`.

No parent specifies the change that closes the box. The Selfsame parent, the
hub parent, and the cbcl-pairing parent contain no occurrence of `69,632`
outside the cbcl-pairing mailbox clause at line 512. The did-crdt commit enters
the review set as an unmodified baseline with no change authorized.

[[SPEC-008-production-pairing-claimant#TEST-1157]] at line 1127 requires the
reviewer to check both non-conforming rendezvous baselines and the sealed cap
arithmetic. Neither the non-conformance nor the arithmetic appears in any
parent.

Both statements exist only in `cbcl-bus` trajectory
`specs/trajectory/SPEC-053/spec-053-0.16.10-complete-consent-and-executable-ledgers.md`
at lines 177 to 190. That document states the residual plainly:

> The current Selfsame and did-crdt rendezvous implementations accept only
> 4,096 octets. They cannot carry maximum credential/v2 records.

The reviewer verified the arithmetic independently. A 57,000-octet offer
plaintext plus a 16-octet tag gives 57,016 octets. A 62,000-octet reverse
plaintext plus the same tag gives 62,016 octets. Both sit under the
69,632-octet PROTO-002 bound and above the 4,096-octet implemented bound.

Consequence: a Tier-1 no-go gate box requires a change that no parent
specifies, in a repository the review set pins as unchanged.

Minimum correction: state the rendezvous conformance obligation in a parent as
a requirement with its cap, status codes, and test. Name the owner of the
did-crdt change and its release path. Owner: repository owner, with the
did-crdt owner.

### H-5 — CON-902 contradicts itself on issuer and WebFinger placement

Class: safety. Severity: HIGH. Blocking.

[[SPEC-008-production-pairing-claimant#CON-902]] lists the authenticated plan's
authoritative sources at lines 272 to 281. Two rows read:

| Field | Authoritative source |
|---|---|
| `issuer` / `jrd` | verified WebFinger resolution |

The same contract, at lines 294 to 295, assigns those effects to the final
executor:

> The executor re-derives the wallet application home key. It creates the
> issuer state, proof, DID publication, and grant in the declared final-consent
> order.

[[SPEC-008-production-pairing-claimant#REQ-902]] at lines 127 to 130 requires
the plan to precede the preliminary decision and to contain no derived
application key.

The pinned code settles which reading is possible.
`crates/selfsame-app-identity/src/issuer.rs:134-142` takes a
`home_key: &ed25519_dalek::SigningKey` and commits the DID to its public key.
`crates/selfsame-app-identity-net/src/webfinger.rs:98-106` takes `home_did`
and verifies a reciprocal binding.
`crates/selfsame-app-identity-net/src/state.rs:431-439` takes `did` and
recognises it before any request.

A verified `jrd` therefore requires the home DID. The home DID requires
`issuer::create`. That call requires the derived home key, which REQ-902
authorizes only after preliminary approval.

Under the table reading, `prepare_claimant` derives a key the same requirement
forbids. It then makes two network requests that disclose the person's own
account and home DID to the authority before any consent.

`[[SPEC-007-cbcl-pairing-cutover#REQ-812]]` forbids an identity side effect
before approved payload delivery. A pre-consent WebFinger lookup on the
person's own account is such an effect for a declined ceremony.

Under the executor reading, the plan carries no `issuer` and no `jrd`, and the
contract's own table is wrong. The parent does not say which reading governs.

The current code confirms the ordering the design must replace.
`src-tauri/src/cbcl_context.rs:288-325` derives the hierarchy home key, creates
the issuer identity, verifies WebFinger, and resolves the closure. All of that
runs before `src-tauri/src/cbcl_pairing.rs:176` calls
`cbcl_transport::connect_wss`, and long before any display.

Consequence: F-C does not close. The split plan does not fully replace the
current call order, because the parent leaves two authority round trips
unplaced.

Minimum correction: delete the `issuer` and `jrd` rows from the plan table, and
state in CON-902 exactly which stage performs WebFinger verification and
closure resolution. State whether either disclosure precedes the preliminary
decision. Owner: repository owner.

### H-6 — The pending-scope contract dropped its retry and concurrency controls

Class: implementability. Severity: HIGH. Blocking.

The reviewed 0.4.8 candidate carried the full pending-allocation rule.
`specs/trajectory/SPEC-008/spec-008-0.4.8-standalone-tofu-first-contact.md`
at lines 85 to 93 states:

> … allocates one pending CSPRNG `applicationAccountId` and CON-211
> `accountScopeId`, and stores one bounded pending ceremony. Those values
> become durable account identity only at the final authority transaction.
> Decline, expiry, or pre-commit failure deletes them; a retry may derive a
> different preview DID and must display it again. Concurrent first-link
> allocation for the same `(applicationId, handle, enrolledKey)` refuses or
> returns the byte-identical active ceremony; it never creates two candidate
> accounts.

[[SPEC-008-production-pairing-claimant#CON-902]] at lines 283 to 286 keeps
decline, expiry, and pre-commit failure. It drops four controls:

- the gated hub allocation command and its lock-and-snapshot;
- the bounded pending ceremony record;
- the retry rule that a different preview DID requires a fresh display;
- the concurrency rule that never creates two candidate accounts.

The concurrency rule is a hard stop. Without it, two simultaneous first-link
ceremonies for one `(applicationId, handle, enrolledKey)` create two candidate
accounts. The person then approves one preview DID while a second account
exists under the same handle.

The retry rule is a display-integrity control. Without it, a retry after a
transport failure derives a different DID, and the person compares a stale
fingerprint.

[[PROTO-001]] states that a sentence limit never justifies deleting a control.
It names a dropped control as a defect of the same standing as a surviving
mutant. This reissue dropped four while shortening the clause.

Consequence: F-D's retry and crash legs stay unspecified even under the most
generous reading of H-2.

Minimum correction: restore the four controls into CON-902, and add the hub
crash behaviour between allocation and migration. Owner: repository owner.

### H-7 — TEST-1157 requires a trajectory clause the parent declares non-normative

Class: authority. Severity: HIGH. Blocking.

[[SPEC-008-production-pairing-claimant#REQ-1005]] at lines 456 to 457 states:

> This parent specification is the sole current Selfsame authority for this
> increment. Trajectory documents provide evidence and no normative precedence.

[[SPEC-008-production-pairing-claimant#TEST-1157]] at lines 1130 to 1132
states:

> Require every installed-state clause in
> `[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-995]]`.

`REQ-995` sits in a Selfsame trajectory document at
`specs/trajectory/SPEC-008/spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:153`.
CON-985 lists neither REQ-995 nor its document in any class.

A current test therefore requires satisfaction of a trajectory clause that a
current requirement declares non-normative.
[[SPEC-008-production-pairing-claimant#ADR-963]] at line 902 states that
historical amendments never determine precedence. TEST-1157 refutes that
statement inside the same section.

Minimum correction: restate the installed-state obligations directly in the
parent, or list REQ-995 explicitly as a surviving current clause. Owner:
repository owner.

### M-1 — The cbcl-pairing parent version collides and the review set names a different one

Class: authority. Severity: MEDIUM. Blocking, because TEST-1157 requires one
pointer set.

The cbcl-pairing parent carries `version: 0.4.0-draft` in its frontmatter. The
previous commit carried `version: 0.2.0`. The reissue raised the number and
changed no protocol clause.

A different document already owns that version. The trajectory amendment
`specs/trajectory/SPEC-001/spec-001-0.4.0-credential-v2-endpoint-profile.md`
carries `id: SPEC-001-amendment-0.4.0` and `version: 0.4.0-draft`.

Two documents in one vault now claim cbcl-pairing SPEC-001 version
`0.4.0-draft`, and they state different things.

The other two parents name the third member differently.
[[SPEC-008-production-pairing-claimant#CON-985]] at lines 880 to 881 names the
unchanged cbcl-pairing 0.4.8 protocol body.
[[SPEC-053-key-root-identity#REQ-081]] at lines 6580 to 6581 names the same. GATE-04 box
one names it again at line 6597.

No cbcl-pairing parent file carries version `0.4.8-draft`. Only the trajectory
amendment `spec-001-0.4.8-split-room-shape-and-length.md` does.

The pointer set therefore resolves to three different labels for one artefact.
The parent calls itself `0.4.0-draft`. Selfsame and the hub call it `0.4.8`.
The vault also holds a distinct `0.4.0-draft`.

Minimum correction: give the cbcl-pairing parent a version above every
trajectory amendment, such as `0.5.0-draft`. Update the two coordinating
parents and GATE-04 box one to that exact version. Owner: repository owner for
all three parents.

### M-2 — The authenticated typed display appears in no parent

Class: safety. Severity: MEDIUM. Blocking, because F-B is a hard stop.

F-B requires that the displayed application and transition come from
authenticated typed values before any decision.

The current display promotes untrusted peer text.
`cbcl-pairing/src/profile.rs:784-791` builds `DisplayIntent` from the wire
`PairingIntent`. It copies `intent.authority_summary` with no descriptor
equality check.

`cbcl-pairing/src/profile.rs:585-598` builds the credential display fields from
`decode_credential_claims(&intent.allocator_claim, ...)`. The field named
`applicationId` therefore carries the allocator's own claim.

`cbcl-pairing/src/profile.rs:793-799` marks every such field
`claimed_by_secret_holder: true`. The Selfsame view copies all of it at
`src-tauri/src/cbcl_pairing.rs:413-429`.

Two of the four displayed values resist substitution.
`cbcl-pairing/src/endpoint.rs:987` refuses when
`value.application != self.application`. `cbcl-pairing/src/profile.rs:764-771`
refuses a wrong application or action against the descriptor.

`authority_summary` receives no such check. It reaches the person's consent
surface as attacker-chosen free text.

No parent forbids that. [[SPEC-008-production-pairing-claimant#REQ-902]]
requires a display of the authenticated transition and names no field.
[[SPEC-008-production-pairing-claimant#ADR-963]] at line 925 states only that
authenticated display remains unchanged.

The mechanism exists in cbcl-pairing trajectory
`spec-001-0.4.4-opaque-typed-credential-v2-display.md` and
`spec-001-0.4.7-authenticated-admission-display.md`. Neither document sits in
the cbcl-pairing parent.

Minimum correction: state the typed display obligation in a parent. Name the
authenticated source of every displayed value, and prohibit any unchecked wire
string on the consent surface. Owner: repository owner for cbcl-pairing and
selfsame.

### M-3 — PAIR1 carries no parent-level obligation

Class: implementability. Severity: MEDIUM. Not blocking on its own.

The architecture requires a human PAIR1 co-presence input distinct from the
machine carrier.

[[SPEC-008-production-pairing-claimant#REQ-1005]] mentions PAIR1 twice, at
lines 462 and 470. Both mentions describe cbcl-pairing's enforcement. Neither
places an obligation on the wallet.

The Selfsame parent contains no `REQ`, `CON`, or `TEST` for PAIR1. Nothing
requires the wallet to capture the value. Nothing keeps it out of the carrier
path. Nothing refuses a substituted one.

The token appears zero times in the cbcl-pairing parent and zero times in the
hub parent. It appears in cbcl-pairing trajectory
`spec-001-0.4.0-credential-v2-endpoint-profile.md:95` and in
`spec-001-0.4.1-credential-v2-profile-authority.md`.

Minimum correction: add one Selfsame requirement and one test for PAIR1
capture, separation, and non-substitutability. Owner: repository owner.

### M-4 — The Selfsame frontmatter disagrees with CON-985 about the historical band

Class: authority. Severity: MEDIUM. Not blocking.

The Selfsame frontmatter at line 8 states that `0.4.23 through 0.4.26`
authority instruments are historical only.
[[SPEC-008-production-pairing-claimant#CON-985]] places `0.4.19` through
`0.4.25` in the historical class and `0.4.26` in the current class.

The two ranges disagree at both ends. The frontmatter omits 0.4.19 through
0.4.22 and includes 0.4.26.

The hub carries the same shape at its line 15, naming `0.16.16 through
0.16.19`. Its REQ-081 names `0.16.12` through `0.16.18` as historical.

Minimum correction: make each frontmatter range equal its parent's declared
historical class. Owner: repository owner for each parent.

### L-1 — WIRE_VERSION is named for the wrong wire

Class: style. Severity: LOW.

[[SPEC-008-production-pairing-claimant#REQ-1005]] at line 534 states that
`WIRE_VERSION` belongs to the invitation-wire namespace.

The pinned anuna-ssi CON-001 governs a link code and a signed offer, not a
cbcl-pairing invitation. `crates/selfsame-core/src/record.rs:329` and `:462`
emit `WIRE_VERSION` as the `:v` member of the `offer` and `grant` forms.

The namespace exclusion still holds. The label is wrong.

Minimum correction: rename the namespace to the link-offer wire. Owner:
repository owner.

### L-2 — The anuna-ssi references are plain prose

Class: style. Severity: LOW.

[[PROTO-001]] requires every cross-reference in an authored artefact to be a
wikilink. The two `anuna-ssi` references at lines 535 and 539 use backticks
only.

The references resolve. The reviewer verified the pin, the file, and the
clause.

Minimum correction: wikilink the target or record an explicit cross-vault
deferral, as the hub does at its line 6531. Owner: repository owner.

### L-3 — The compiled conformance registry has no stated disposition

Class: implementability. Severity: LOW.

[[SPEC-008-production-pairing-claimant#CON-903]] states that no
conformance-digest registry participates in the new policy.

`src-tauri/src/cbcl_registry.rs:18-31` still declares `APPROVED_CONFORMANCE`
with one ratified entry. `src-tauri/src/cbcl_pairing.rs:152` still consults
`cbcl_registry::production_relay_policy()`.

No requirement, contract, or test names the removal. A competent implementer
infers it from CON-903. The inference stays unstated.

Minimum correction: add one prohibited-action test that the ordinary binary
contains no approved-conformance registry. Owner: repository owner.

### L-4 — A stale module comment attributes the opposite meaning to CON-903

Class: style. Severity: LOW.

`src-tauri/src/cbcl_registry.rs:1` reads
`SPEC-008 CON-903 — the compiled approved-conformance registry`. The parent's
CON-903 now prohibits exactly that structure.

Code changes stay prohibited before a PASS. The reviewer records the collision
so the implementer repairs it with the rest.

Minimum correction: repair the comment in the same change that satisfies
CON-903. Owner: repository owner.

## N11 disposition

| Finding | Disposition | Evidence |
|---|---|---|
| N11-C-1 | **NOT CLOSED** | The two retirement lists partition their bands exactly. The anchor clauses still revive a retirement chain and contradict themselves. See C-1, H-1, H-2. |
| N11-H-1 | **CLOSED** | GATE-04 carries exactly 42 direct boxes at lines 6597 to 6654. Box one names all three parents. REQ-081 line 6590 states that no trajectory gate text participates in the box count or coordinates. No phrase-boundary replacement is load-bearing. |
| N11-M-1 | **CLOSED** | The pin `c7d462029841ea1884bb6f089732058d8838728d` resolves to a real commit and to `specs/SPEC-001-device-key-provisioning.md`. CON-001 sits at line 1073 of that file. Its code grammar is `version(1) ‖ application-id(1) ‖ secret(16)`, matching `CODE_VERSION` at `crates/selfsame-core/src/code.rs:78`. Its offer form carries `:v`, matching `WIRE_VERSION` at `crates/selfsame-core/src/lib.rs:58` and its emission at `record.rs:329` and `:462`. The pin supplies a grammar in another repository and no coordinated design authority. |
| N11-M-2 | **CLOSED** | Selfsame TEST-1156 lines 1060 to 1064 carry both the absent-alias and the disagreeing-alias case. Hub TEST-115 lines 7087 to 7091 carry the same two cases. Both require `inconsistent-binding` and zero deletion. |
| N11-M-3 | **CLOSED** | NFR-928 line 639 and NFR-026 line 6736 both state that only collection and predicate owners adopt tagged success. Both add that every other public owner retains its declared success shape. Both then trace `mint/3` to `#(ok Token)` and `ensure-room/4` to `ok`, each after commit. |
| N11-L-1 | **CLOSED** | The pin, the file, and CON-001 all resolve. The references remain non-wikilinked prose. See L-2. |
| N11-L-2 | **CLOSED** | Selfsame TEST-1156 line 964 and hub TEST-115 line 6999 both prohibit requiring a raw exhausted `#cyclic{}` or `nested_transaction` terminal. |
| N11-L-3 | **CLOSED** | The three commands ran exactly as specified. All three report zero errors and zero warnings. See §Mechanical-check evidence. |

## Hard-stop disposition

| Hard stop | Disposition | Evidence |
|---|---|---|
| F-A — exact application plus relay, never relay-only | **CLOSED** | CON-903 keys the policy on the exact `(applicationId, relayOrigin)` tuple. It excludes a relay-only key, a global allowlist, a conformance-digest registry, and an application wildcard. REQ-906 requires a fresh prompt for a changed application or origin. The current code takes the excluded path at `src-tauri/src/cbcl_registry.rs:18-31` and `src-tauri/src/cbcl_context.rs:36`, so the change is real and specified. |
| F-B — authenticated display, never the peer's claimed application string | **NOT CLOSED** | See M-2. The design constrains `application` and `action` against the descriptor. It leaves `authority_summary` as unchecked wire text on the consent surface, and states no typed display obligation in any parent. |
| F-C — split plan, preview, and executor fully replace the current call order | **NOT CLOSED** | See H-5. The current order derives, creates the issuer, and makes two authority calls before the socket. CON-902 places `issuer` and `jrd` in the pre-consent plan and in the final executor at once. |
| F-D — pending fresh scope traced end to end, with no prior-enrolment cycle | **NOT CLOSED** | See C-1 and H-6. The hub half exists in no cbcl-bus specification. The retry and concurrency controls left the contract. The design does break the enrolment cycle in principle: it removes the `record.account_scope_id` read at `src-tauri/src/cbcl_context.rs:279`. It supplies no replacement the hub owns. |
| F-E — what does and does not prove relay operatorship | **CLOSED** | ADR-902 states plainly that the application profile does not prove relay operatorship. It states that exact-pair consent contains the residual without claiming to solve it. The reviewer found no contrary claim in any parent. |
| SPEC-007 REQ-812 — zero authority on every failure and negative decision | **NOT CLOSED** | See H-5. Under CON-902's table reading, a declined ceremony still discloses the person's own account and home DID to the authority. The parent permits one preview disclosure, a request tombstone, TOFU policy, and bounded rate state. It does not permit two pre-consent authority round trips, and it does not exclude them either. |

## Repository assessments and surfaces found sound

The reviewer re-tested many surfaces and found no defect in them. Recording
them separates the eight blocking defects from the rest of the work.

### The busy inventory is accurate against the pinned hub code

NFR-928 and NFR-026 name many exact hub sites. Every named line resolves to the
construct the clause describes. The reviewer checked each one.

| Named site | Pinned code at that line |
|---|---|
| `cbcl-chat-session-ws.lfe:347`, `:385` | `cbcl-chat-pairaccount:unratified-device?` in a boolean position |
| `cbcl-chat-session-ws.lfe:512` | `device-grants` into an admission map |
| `cbcl-chat-session-ws.lfe:869` | `rooms-for-account` through `lists:foreach` |
| `cbcl-chat-session-ws.lfe:965` | `list-public` through the wire channel encoder |
| `cbcl-chat-session-ws.lfe:1050` | `member-grant?` in a boolean position |
| `cbcl-chat-session-ws.lfe:1076` | the removal path's `get-or-start` consumer |
| `cbcl-chat-session-ws.lfe:1683` | `grants` through `lists:map` |
| `cbcl-chat-session-ws.lfe:1788` | the hello path's `get-or-start` consumer |
| `cbcl-chat-session-ws.lfe:1898` | a discarded `roomcfg:claim` result becoming `'ok` |
| `cbcl-chat-room.lfe:201`, `:221`, `:550`, `:677` | `roomcfg:get`, `get-dialects`, and `peer-grants` |
| `cbcl-chat-room.lfe:263-270` | the two boolean re-admission contexts |
| `cbcl-chat-rooms.lfe:49`, `:54`, `:60`, `:61`, `:99-104` | ingest, webhook room configuration, and child start |
| `cbcl-chat-path-b-poller.lfe:17`, `:32` | the account scan and the revocation apply |
| `cbcl-chat-pairgrant.lfe:78` | `member-grant?` in a boolean position |
| `cbcl-chat-successor.lfe:109`, `:134` | two `(catch ...)` helper edges |
| `cbcl-webhook-handler.lfe:76` | the HTTP edge into `rooms:ingest` |

The two WebSocket `get-or-start` consumers are exactly the two named. A
repository search over `apps/` returns call sites at
`cbcl-chat-session-ws.lfe:1076` and `:1788` plus the module's own use at
`cbcl-chat-rooms.lfe:49`.

The successor claim is accurate and adverse.
`cbcl-chat-successor.lfe:134` calls transaction-opening `device-grant` inside a
`catch`, which is exactly what NFR-026 declares non-conforming.

The wrapper obligation is real work. A repository search finds 72 production
`mnesia:transaction` call sites under `apps/`, every one a bare unary call. No
shared wrapper exists, and `authority-busy` appears nowhere in the pinned code.

The schema-migration site resolves.
`apps/cbcl_bus/src/store/cbcl-store-migrate.lfe:78` defines `legacy-rows/0`.

### The TEST-050 exception matches the pinned recogniser

TEST-1156 requires acceptance at 1, 23, 24, 255, 256, 2,047, and 2,048 octets.
It requires refusal at 0, 2,049, 4,095, and 4,096 octets.

`cbcl-pairing/src/cbcl_protocol.rs:32` declares
`const MAX_CONTROL_OCTETS: usize = 2_048`. Lines 676 and 761 refuse above that
bound and on an empty body. Every boundary row matches.

The cbcl-pairing parent states the same exception at lines 1474 to 1477. It
retires the 4,096-octet positive witness and keeps the negative obligation.

### The transport and carrier surfaces stay sound

`src-tauri/src/cbcl_transport.rs:55-76` maps a canonical origin to
`wss://host[:port]/relay` and offers one TLS path with bundled roots. It
exposes no plaintext fallback and no root-injection parameter on that path.
NFR-901 matches the code.

`src-tauri/src/cbcl_pairing.rs:451-461` recognises the carrier as unpadded
base64url with no scheme, prefix, or padding. The inline tests at lines 475
to 504 exercise the wrapped, padded, and legacy cases. REQ-905 matches.

### The credential/v2 carrier does carry an application identifier

REQ-906 requires live CON-220 authentication of the invitation's application
before any socket. The credential/v1 carrier cannot support that, because
`cbcl-pairing/src/profile.rs:21` fixes the application string to the constant
`anuna.io/credential/v1`.

The credential/v2 carrier supplies the missing member. The `cbcl-pairing`
trajectory clause `spec-001-0.4.0-credential-v2-endpoint-profile.md#CON-013`
declares `"application-context": application-id-v2` and requires it to satisfy
the canonical HTTPS ApplicationId grammar.

REQ-906 therefore has a wire source, unlike the withdrawn 0.3.0 REQ-909. The
source sits outside the cbcl-pairing parent file, which is the pointer defect
of M-1 rather than a missing mechanism.

### The did-crdt and PROTO-002 baselines match the design's own account of them

`[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]` bounds a record at 69,632 bytes
and fixes a 600-second lifetime. `[[PROTO-002-selfsame-rendezvous-v1#CON-304]]`
fixes the status table.

The two implementations diverge from it identically, at 4,096 octets and a
`400` refusal. The design's own account of that divergence is correct. Its
placement is the defect, not its content. See H-4.

## Authority, test, and pointer singularity

The reviewer counted each authority value in each parent.

| Value | Selfsame parent | Hub parent | cbcl-pairing parent |
|---|---|---|---|
| Selfsame test set | 1 | — | — |
| Hub test set | 1 | 1 | — |
| Protocol body reference | 1 | 1 | 1 |
| Coordinated review set | 1 | 1 | — |
| Consumer pointer | — | — | 1 |
| Hub pointer | — | — | 1 |
| did-crdt pin | 1 | 1 | — |

Each parent states each value once. The two hub test sets agree exactly. The
two coordinated review sets agree exactly.

The pointer set is not singular across the review set. Selfsame and the hub
name cbcl-pairing `0.4.8`. The cbcl-pairing parent names itself `0.4.0-draft`,
which a trajectory amendment already owns. See M-1.

The DEC-012 pointer claim resolves to nothing. See H-3.

GATE-04 carries exactly 42 direct boxes. An extraction bounded by the GATE-04
heading and the next second-level heading counts 42 lines beginning `- [ ]`.
Box one names Selfsame SPEC-008 `0.5.0-draft`, the unchanged cbcl-pairing
SPEC-001 `0.4.8` protocol body, and the hub parent.

The TEST-050 exception resolves against the pinned recogniser. See
§The-TEST-050-exception-matches-the-pinned-recogniser.

## Mechanical-check evidence

### Controlled-language lint

The hub parent predates the controlled-language rules of [[PROTO-001]]. Its
legacy body carries 69,218 words written before those rules existed. The
authored lint unit is therefore the consolidated current-law section, extracted
by its exact heading boundaries.

| Command | Errors | Warnings | Longest sentence |
|---|---|---|---|
| `usdd-lint.sh --type descriptive --strict specs/SPEC-008-production-pairing-claimant.md` | 0 | 0 | 25 |
| `awk '/^## Tier-1 generation provenance/{f=1} /^## Changelog/{if(f) exit} f' SPEC-053 \| usdd-lint.sh --type descriptive --strict -` | 0 | 0 | 24 |
| `usdd-lint.sh --type descriptive --strict SPEC-001-reusable-blind-pairing.md` | 0 | 0 | 24 |

The Selfsame parent passes whole-file lint. The cbcl-pairing parent passes
whole-file lint. The consolidated hub section passes with zero errors and zero
warnings, so no new-section lint defect blocks.

The legacy hub body does not pass whole-file lint, and this review does not
claim that it does. The same command against the whole hub parent reports
1,340 errors and a longest sentence of 152 words.

| Rule | Count in the whole hub parent |
|---|---|
| `lowercase_bcp14` | 296 |
| `perfect_tense` | 28 |
| `progressive_passive` | 4 |
| `deprecated_term` | 3 |
| `sentence_over_limit` | 1,009 |

That legacy count is pre-existing debt. It falls outside this increment.

### Vault checks

`zetl 0.9.3` ran in all three vaults. Attribution uses each row's `source`
field, which names the document that carries the link.

| Vault | Dead links | Orphans | Syntax errors | SPL errors |
|---|---|---|---|---|
| selfsame `specs/` | 261 before this report | 31 | 1 | 0 |
| cbcl-bus `specs/` | 567 | 35 | 0 | 0 |
| cbcl-pairing `specs/` | 0 | 0 | 0 | 0 |

Per-document attribution for the three edited pages:

| Page | Dead links attributed to it |
|---|---|
| `SPEC-008-production-pairing-claimant` | 0 |
| `SPEC-001-reusable-blind-pairing` | 0 |
| `SPEC-053-key-root-identity`, whole file | 130 |
| `SPEC-053-key-root-identity`, lines 6542 to 7135 | 0 |

The edited Selfsame parent introduces no dead link. The edited cbcl-pairing
parent introduces no dead link. The hub consolidated current-law section
introduces no dead link.

The hub's 130 rows sit in its legacy body and are pre-existing debt. The 261
and 567 vault totals are pre-existing debt across many documents. The reviewer
attributes none of them to this increment.

The single selfsame syntax error is pre-existing debt in
`EXP-002-e2e-linking-harness.md:263`, an unclosed `[[Cargo` wikilink. It sits
outside this increment.

### Deferral: this report's own cross-vault links

This report carries 10 dead links, and the selfsame vault total rises from 261
to 271. Four target `[[PROTO-001]]` and six target
`[[SPEC-053-key-root-identity]]`. Both targets live outside the selfsame vault.

[[PROTO-001]] requires a wikilink for every cross-reference and forbids
deleting a dead link to tidy a report. It requires an explicit deferral with a
rationale and an owner instead.

Rationale: the hub parent records the same cross-vault boundary at its line
6531, where it names the selfsame specifications it adopts and never restates.
A concept page in this vault for either target forks a Tier-1 document.

Owner: repository owner, at the next vault-hygiene pass.

## Residual assumptions and open gates

These items are not defects. They record what a PASS still leaves open.

- Every one of the 42 GATE-04 boxes stays open. The hub states that the reissue
  closes none of them.
- `[[SPEC-053-key-root-identity#GATE-01]]` and
  `[[SPEC-053-key-root-identity#GATE-03]]` stay open and gate GATE-04.
- `[[SPEC-007-cbcl-pairing-cutover#REQ-809]]` and the SPEC-007 production gate
  stay closed. [[SPEC-008-production-pairing-claimant#REQ-907]] alters neither.
- The depth tests TEST-913, TEST-914, and TEST-915 stay open with named owners.
- The hub sign endpoint keeps its recorded prototype posture until the Tier-1
  release work lands.
- The two rendezvous implementations stay non-conforming to
  `[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]` until an owner changes them.
- Production deployment requires separate owner approval beyond any review
  verdict.

## Stopping-rule assessment

The mandate returns PASS only under six conditions. The design is safe,
implementable as written, internally consistent, and mechanically singular. It
also closes every N11 obligation and every F-A through F-E hard stop.

| Test | Result |
|---|---|
| Safe | fails; see H-5 and M-2 |
| Implementable as written | fails; see C-1, H-4, and H-6 |
| Internally consistent | fails; see H-1, H-2, H-5, and H-7 |
| Mechanically singular | fails; see H-1, H-3, and M-1 |
| N11 closed | seven of eight close; N11-C-1 stays open |
| F-A through F-E closed | two of five close; F-B, F-C, and F-D stay open |
| SPEC-007 REQ-812 closed | fails; see H-5 |

The mandate also directs a REJECT on any false code claim. H-3 records a false
claim about the pinned hub parent. H-4 records a gate box that contradicts both
pinned rendezvous implementations.

The mandate directs no rejection merely because implementation is absent. This
review rejects nothing on that ground. Every blocking finding names a
specification gap, a contradiction, or a false claim.

The mandate warns against promoting style or optional precision. This review
classifies four LOW findings and two non-blocking MEDIUM findings as
non-blocking, and blocks on none of them.

Owner preference did not enter any disposition. The reviewer re-derived every
conclusion from a pinned file.

### Does another specification correction suffice, or does the architecture return to the owner?

Seven of the eight blocking findings are specification corrections. H-1, H-2,
H-3, H-5, H-6, H-7, and M-1 each name a bounded edit to a parent.

C-1 and H-4 are different. Each names work that no parent owns yet.

C-1 requires an entire hub mechanism that the `cbcl-bus` vault never recorded.
That is new specification authorship in another repository, under that
repository's own channel, not a correction to this increment.

H-4 requires a change to a third repository that this review set pins as an
unmodified baseline. No parent authorizes it and no owner is named.

The architecture therefore returns to the owner for two decisions. The owner
decides who authors the hub mechanism and under which channel. The owner
decides whether the rendezvous conformance work enters this increment or
becomes its own increment with its own gate.

The remaining seven corrections proceed as ordinary specification edits once
those two decisions land.

## What this verdict authorizes and does not authorize

This verdict authorizes:

- reading this report;
- authoring the corrections listed below, through each repository's declared
  amendment channel;
- a further coordinated fresh-context Tier-1 review of the corrected parents.

This verdict does not authorize:

- any implementation, in any of the four repositories;
- any Elephant SPL theory or test-first plan for this increment;
- closing any GATE-04 box;
- production invitation allocation;
- any release, deployment, or production enablement;
- treating any trajectory clause as current law.

## Collected corrections and owners

| Id | Correction | Owner |
|---|---|---|
| C-1 | Author the hub pending-allocation mechanism in `[[SPEC-053-key-root-identity]]` as `REQ`, `CON`, and `TEST` artefacts. Cover the allocation command, its authenticated precondition, the signing key, the wallet verification rule, the bounded pending record, and every deletion trigger. | cbcl-bus repository owner |
| H-1 | Replace both anchor paragraphs with direct statements of the surviving mechanism. Remove every retirement chain, partial-range replacement, and historical precedence rule from both current-law sections. | repository owner, each parent |
| H-2 | Remove each anchor clause from its own historical range, or delete the anchor sentence and state the mechanism directly. | repository owner, each parent |
| H-3 | Delete the DEC-012 pointer claim from CON-985 and TEST-1157, or add an explicit Selfsame parent pointer to DEC-012 through the hub channel. | repository owner, both parents |
| H-4 | State the rendezvous conformance obligation in a parent, with its cap, status codes, and test. Name the did-crdt owner and the release path. | repository owner, with the did-crdt owner |
| H-5 | Delete the `issuer` and `jrd` rows from CON-902's plan table. State which stage performs WebFinger verification and closure resolution, and whether either precedes the preliminary decision. | repository owner |
| H-6 | Restore the four dropped pending-scope controls into CON-902. Add the hub crash behaviour between allocation and migration. | repository owner |
| H-7 | Restate the installed-state obligations directly in the parent, or list REQ-995 as a surviving current clause. | repository owner |
| M-1 | Version the cbcl-pairing parent above every trajectory amendment. Update both coordinating parents and GATE-04 box one to that exact version. | repository owner, all three parents |
| M-2 | State the typed display obligation in a parent. Name the authenticated source of every displayed value and prohibit unchecked wire strings on the consent surface. | repository owner, cbcl-pairing and selfsame |
| M-3 | Add one Selfsame requirement and one test for PAIR1 capture, separation, and non-substitutability. | repository owner |
| M-4 | Make each frontmatter historical range equal its parent's declared historical class. | repository owner, each parent |
| L-1 | Rename `WIRE_VERSION`'s namespace to the link-offer wire. | repository owner |
| L-2 | Wikilink the anuna-ssi references or record a cross-vault deferral. | repository owner |
| L-3 | Add one prohibited-action test for the absence of the approved-conformance registry. | repository owner |
| L-4 | Repair the stale `cbcl_registry.rs` module comment in the change that satisfies CON-903. | repository owner |

## Changelog

- **2026-08-23 — Tier-1 adversarial review of the coordinated 0.5.0 consolidated
  reissue. REJECT.** Cross-model review by Anthropic `claude-opus-5` of an
  OpenAI GPT-5 `gpt-5.6-sol` synthesis, from a fresh session under Circus
  attempt `circus/spec008-consolidated-review/1`. One CRITICAL, seven HIGH,
  four MEDIUM, and four LOW findings. Seven N11 obligations close. Two hard
  stops close. The review changed no candidate, parent, code, or sibling
  repository file.
