---
id: spec-008-0.5.11-implementation-claude-adversarial-review-2026-08-24
title: SPEC-008 0.5.11 implementation — Tier-1 cross-model adversarial review
status: complete
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
generating-family: OpenAI GPT-5
verdict: REJECT
date: 2026-08-24
---

# SPEC-008 0.5.11 implementation — Tier-1 adversarial review

## Reviewer identity, subscription authentication path, Circus attempt, and independence

Reviewing family: Anthropic Claude, fresh context, driven through Circus with
the interactive Claude CLI.

`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` were both checked for presence
without printing any value; both are **absent** from this environment. The
API-billed `circus-driver-claude` was not invoked.

`env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` recorded:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

All three are the required values, so the review-process precondition holds. No
email, organization identifier, token, key, or account identifier is recorded.

**Independence.** This session received no generating-session reasoning, plan,
or transcript. The independent pass was performed first, entirely against
`git show`/`git diff` of the named commits and against executable behaviour of
clean detached clones. The four prior reports and the 0.5.11 remediation note
were opened only *after* that pass was complete, and no disposition in them was
inherited: every 0.5.10, 0.5.9, 0.5.8 and 0.5.7 item below was re-derived from
code or re-executed here.

**Candidate hygiene.** No candidate repository was modified. Every build, test
and mutation ran in scratch clones or scratch worktrees created from the exact
commit objects. `/Users/anuna-01/Code/{cbcl-bus,cbcl-pairing}` finished this
session with zero tracked modifications and unchanged HEADs; `did-crdt` and
`cbcl-rs` retain the pre-existing uncommitted work they had at session start,
which was never read as candidate evidence.

## Exact commits reviewed

| Repository | Candidate | Pre-0.5.11 | Original baseline |
|---|---|---|---|
| Selfsame | `0c5801259802b767bd8f9230ff6cd32dff6fffa5` | `f054bad1840ffb5ce3840bba87c79a8f21bd3740` | `0220cec2dec44cd95d4f411ea4814d790b6716d2` |
| cbcl-bus | `965d6cc44cfdf0f8d00a562bb587b7785396ee97` | `bc1cf5d23029e36543df37d32f3234a2f6bb20c5` | `ce894a7f2debc111b610296b1a3780de6084e37e` |
| cbcl-pairing | `62ef4a968b46b4836374fcee1d78c410f730a7a7` | `3d013306cab68c9e258e149387e420058b57e80f` | `c22f2d0526432c01c56d64be35a8d4f87516ec92` |
| did-crdt | pin `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` (service HEAD `94db956d7d5d5edf0f42afad073ab42f265a1752`) | — | — |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | — | — |

Governing parents read in full for the checked areas:
`specs/SPEC-008-production-pairing-claimant.md`,
`specs/SPEC-007-cbcl-pairing-cutover.md`,
cbcl-bus `specs/SPEC-053-key-root-identity.md`,
cbcl-pairing `specs/SPEC-001-reusable-blind-pairing.md`.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 1 |
| LOW | 2 (both carried-forward residuals with explicit treatment) |

The 0.5.11 increment is a real and well-made remediation. Both 0.5.10 HIGH
findings are closed in code and I killed the corresponding mutants
experimentally: the acknowledged-`FinishedSent` reopen now emits `Open` alone,
and a pre-payload wallet failure no longer strands an application slot. L-3 and
L-4 are closed. The exact-pin WASM rebuild reproduced the vendored package
byte-for-byte at the claimed digest. Every other required check passes.

The single blocking finding is that the increment's own central new obligation,
[[SPEC-008-production-pairing-claimant#CON-990]], has no verification that any
automated gate executes, and the test artefact that the parent names as its
verifier — [[SPEC-008-production-pairing-claimant#TEST-1162]] — implements only
part of its own specified content. Four load-bearing predicates in the
compensation and confirmed-unlink mechanism survive deliberate mutation, and
each corresponds to a clause TEST-1162 states in terms. Under check 21 and the
brief's rule that a load-bearing missing obligation is a finding even where the
candidate fails closed, that is MEDIUM, and MEDIUM blocks PASS.

Nothing I found is exploitable in the shipped code. The correction is bounded
and mechanical.

## Findings

### M-1 — CON-990's compensation and confirmed-unlink guards have no executable verification: four load-bearing predicates survive mutation, and the only test naming TEST-1162 runs in no automated gate

**Severity: MEDIUM.** Owner: Selfsame.
Affected requirements: [[SPEC-008-production-pairing-claimant#CON-990]],
[[SPEC-008-production-pairing-claimant#TEST-1162]],
[[SPEC-008-production-pairing-claimant#REQ-1006]].

**What the parent requires.** CON-990 (`SPEC-008` lines 1569–1603) states four
prohibitions on the armed guard's compensation path: it "SHALL compare the
immutable attempt identity and remove the recognised current pre-payload
phase"; it "SHALL NOT remove another attempt, an installed record, or a sibling
application"; if an ambiguous storage result exposes a `PayloadPrepared` value,
compensation "SHALL retain it for CON-989 recovery"; and REQ-1006's confirmed
unlink "SHALL remove the exact local slot". TEST-1162 (`SPEC-008` lines
2025–2051) is named as the verifier and prescribes the mutations that must fail
it, including "Persist a correct `PayloadPrepared` value and inject failures in
payload release and receipt handling. Require preservation for CON-989
recovery" and "Substitute another attempt, an installed value, a changed root
generation, or a changed exact-pair policy and require compensation or unlink to
refuse rather than delete it."

**Code evidence — the predicates are present and correct.**
`src-tauri/src/cbcl_v2_completion.rs:1589-1628` (`remove_pre_payload_attempt`)
holds three of them: the `CredentialV2LinkSlot::Pending(current)` let-else at
`:1600` refuses an installed slot, `same_attempt` at `:1604-1615` compares the
twelve immutable attempt fields, and `:1619-1621` returns `Ok(())` without
deleting when the stored value is already `PayloadPrepared`.
`unlink_local` at `:1511-1554` holds the fourth: `:1537` re-reads the slot under
`SLOT_LOCK` and refuses unless it equals the value `load_local_link`
(`:1432-1451`) recognised before presence was requested. The shipped behaviour
is right; I could not construct an in-code path that violates any of the four.

**Failure sequence — what is unverified.** All four predicates were deleted in
turn, each with a forced recompile confirmed by cargo's `Compiling selfsame
v0.1.0` line, and in each case both the ignored TEST-1162 test and the complete
66-test `--lib` suite still passed:

| Mutant | Result | CON-990 clause left unprotected |
|---|---|---|
| `if current.is_payload_prepared() { return Ok(()); }` → `if false` | 1 passed / 66 passed | "SHALL retain it for CON-989 recovery" |
| `if !same_attempt { return Err(…) }` → `let _ = same_attempt;` | 1 passed / 66 passed | "SHALL NOT remove another attempt" |
| Installed arm of `recognise_slot` deletes the entry instead of refusing | 1 passed / 66 passed | "SHALL NOT remove … an installed record" |
| `if recognise_slot(&encoded)? != expected_slot { return Err(…) }` removed from `unlink_local` | 1 passed / 66 passed | REQ-1006 "remove the exact local slot" |

The third of those is the one with a real consequence if it ever regresses:
`remove_pre_payload_attempt` runs from `Drop`, on an error path, with no caller
inspecting its result (`:803` discards it). A future edit that lets it fall
through on an `Installed` slot silently deletes the durable grant, receipt
evidence and `finalStatusJws` for an already-installed application, with no
local recovery path — and no test anywhere would notice.

**Second, independent half of the same finding.** The only Rust test that names
TEST-1162 — `cbcl_v2_completion.rs:2207-2247` — carries
`#[ignore = "installs the process-global in-memory keyring; run this test
alone"]`. Selfsame CI runs `cargo test --workspace --locked`
(`.forgejo/workflows/ci.yml:166`), which never executes ignored tests, and a
repository-wide search finds no `--ignored`/`--include-ignored` invocation for
it in any workflow, script, manifest or README. The browser half of the same
requirement lives in `tests/spec-007-wallet-pairing.mjs`, and the CI `screens`
job runs only `npm run screens` and `npm run e2e:pairing` — `e2e:wallet-pairing`
is invoked nowhere. So CON-990 and REQ-1006's pending-slot lifecycle have, today,
zero continuous verification on either side.

Independently of the mutants, the implemented test also covers only one of the
error edges TEST-1162 enumerates: it injects a single failure after one
`replace_pending`, where the parent requires injection "after each successful
final-approval persistence and before each possible successful `PayloadPrepared`
replacement … final approval release, acknowledgement read and recognition,
every checkpoint and stage replacement, custody, publication, resolver, grant,
payload construction, checkpoint preparation."

**What does pass.** Two TEST-1162 clauses are genuinely executable and I killed
their mutants: deleting the guard's `Drop` body fails the test
(`assertion failed: pending_links().unwrap().is_empty()` at `:2222`), and the
committed-but-reported-failed replacement path is exercised. TEST-1163 and
TEST-1164 are fully executable — see the test table.

**Measurable correction.**

1. Make TEST-1162's Rust half run in the default suite. Move it into its own
   integration binary under `src-tauri/tests/` (a separate process removes the
   `keyring::set_default_credential_builder` collision that motivated
   `#[ignore]`), or add an explicit CI step that runs it by name with
   `--ignored`. Add `npm run e2e:wallet-pairing` to the CI `screens` job.
2. Add four executable cases, one per surviving mutant, such that each mutation
   above fails at least one of them: (a) a durable `PayloadPrepared` slot plus a
   post-payload failure, asserting the slot survives and stays recoverable;
   (b) a guard anchored on attempt A against a stored attempt B, asserting
   refusal and no deletion; (c) a guard anchored on a pending attempt against a
   stored `Installed` value, asserting refusal and no deletion; (d) an
   `unlink_local` whose slot changed between `load_local_link` and the call,
   asserting `PairingApplicationNotLinked` and no deletion.
3. Extend the injection matrix to each named stage boundary between durable
   final approval and durable `PayloadPrepared`, as TEST-1162 states.

### L-1 — A mid-chain post-approval failure still leaves an orphaned issuer DID publication with no compensating retraction

**Severity: LOW (residual, unchanged in kind from 0.5.8 L-1 / 0.5.9 L-1 /
0.5.10 L-1).** Owner: Selfsame.
Affected requirement: [[SPEC-007-cbcl-pairing-cutover#REQ-812]] compensation
clause; bounded by [[SPEC-004-application-scoped-identity#CON-204]].

`cbcl_v2_commands.rs:524-530` publishes the issuer identity to the profile's
declared state resolver. Any failure after that point — resolver refusal at
`:539-548`, grant construction, payload construction, the payload checkpoint, or
the guard's own compensation — returns without retracting the publication. I
searched `cbcl_v2_commands.rs` and `cbcl_v2_completion.rs` for any deactivation
or retraction edge and found none; the only `is_deactivated()` uses are
*reads* of a resolved document (`:540`, `:1535`).

**Consequence.** A published-but-unused `did:crdt:` document remains resolvable.
It carries no account, no scope binding, no alias, no WebFinger row and no
grant, and it cannot become authority: a fresh ceremony derives a different DID
from a fresh hub-minted `accountScopeId`, and both hub acceptance
(`cbcl-chat-selfsame-v2-store.lfe` `new-account-row`, four uniqueness reads) and
wallet installation (`install`, exact CAS plus live reciprocal WebFinger)
require evidence the orphan does not have.

**Operational treatment, explicit.** The 0.5.11 remediation note records this
as "the bounded compensation limitation already admitted by
CON-204" and does not represent it as closed. Two facts moderate it further and
are recorded here rather than assumed: `did-crdt` HEAD `94db956d` makes service
DID creation idempotent, so a retried publication of the same identity does not
multiply orphans; and the pinned runtime compiles `did-crdt` with
`default-features = false`, so that fix reaches the deployed resolver rather
than the wallet build. Operators should expect orphan documents proportional to
mid-chain failure rate and should not treat their presence as evidence of an
issued credential.

### L-2 — The CON-036 anonymous recovery limiter is keyed on the TCP peer address, so a reverse-proxied deployment shares one bucket

**Severity: LOW (residual, unchanged from 0.5.9 L-2 / 0.5.10 L-2).** Owner:
cbcl-bus / deployment.
Affected requirement: [[SPEC-008-production-pairing-claimant#CON-989]] recovery
availability; SPEC-053 CON-036.

`cbcl-chat-selfsame-application-http.lfe:138-141` derives the limiter key from
`cowboy_req:peer`, i.e. the TCP peer. `cbcl-chat-selfsame-v2-recovery-limit.lfe`
then applies a 60 s fixed window with a 30-request budget per key
(`:41-48`). `config/router.sys.config:5-8` records that this deployment sits
behind the Fly proxy, which terminates TLS and "supplies the sole trusted
client-IP header"; nothing in `apps/cbcl_chat` reads that header.

**Consequence.** Behind the proxy every wallet in the world shares one 30/60 s
bucket for `/selfsame/pairing/v2/status`. Any unauthenticated client can
exhaust it and make post-relay-window recovery answer `503` for everyone. The
failure is availability-only and fails closed: the wallet maps `503` to
`unavailable` (`pairing_status.rs:76`, `cbcl_v2_commands.rs:1190-1192`), retains
the pending slot, discloses nothing, and can retry. No authority, identity or
capability effect is reachable through it.

**Operational treatment, explicit.** The 0.5.11 remediation note records this as
"an operational deployment gate", and GATE-04's final box requires separate
owner approval of production enablement. The bounded correction, when that gate
is approached, is to key the limiter on the trusted proxy client-IP header when
and only when the listener is configured as proxied, and to keep
`cowboy_req:peer` as the key otherwise so an unproxied deployment cannot be
spoofed by a caller-supplied header.

## Dispositions for all twenty-one required checks

| # | Check | Disposition |
|---|---|---|
| 1 | Relay consent keyed exactly by `(applicationId, relayOrigin)`; displayed application facts from the live CON-220 profile | **PASS.** `cbcl_v2_policy.rs` stores a canonical, strictly-ascending, deny-unknown-fields row set of exactly `(application_id, relay_origin)` pairs, bounded at 256, with round-trip canonicality enforced on read (`:52-73`); `state`/`insert`/`remove` all binary-search the full tuple. `RelayConsentView` (`cbcl_v2_claimant.rs:56-62`) takes `application_id` from `fetched.profile.application_id` — the live HTTPS fetch — never from the carrier, and `recognise_profile_candidate` (`:156-180`) refuses unless the fetched profile's own `applicationId` equals `carrier.application_context()` exactly. The relay origin is the carrier's, admitted only when the authenticated profile declares exactly one matching descriptor (`:165-171`). The policy row is written in `bind_finished_profile` after both Finished values, and only when the person newly approved. |
| 2 | No derivation, scope mint, publication, grant, alias, bundle or durable identity effect before its approval; fresh first-contact scope has authenticated, non-attacker-selected authority | **PASS.** `cbcl_v2_recognise` opens no socket; `cbcl_v2_relay_decide` opens WSS only after `authorise_claimant_relay` consumes an approval. `cbcl_v2_preliminary_decide` performs exactly one custody call, `preview_identity` (`cbcl_v2_commands.rs:1665-1681`), which returns a `String` home DID and signs, publishes and persists nothing. Its `AccountScopeId` comes from `recognise_signed_offer(...).claims.account_provenance().account_scope_id()` — verified under the profile's declared `enrollment_keys` with an Ed25519 signature over a domain-separated digest (`selfsame-pairing/src/credential_v2.rs:1108-1154`) — so the scope is hub-minted and signed, never peer-chosen. Every issuer, publication, resolver, grant and payload effect sits after `cbcl_v2_final_decide`'s durable `persist_pending` at `:443`. |
| 3 | Design claims no relay-operator proof; exact per-application consent is the explicit mitigation | **PASS (honest residual).** ADR-902 states it; the code matches. A profile may name another operator's relay and the carrier may select it; the mitigation is that the exact `(applicationId, relayOrigin)` tuple must be approved once by the person, and `authorise_claimant_relay` refuses any decision/state combination other than `TrustedPair`+`ExistingTrust` or `NewPair`+`Approve`. |
| 4 | One closed CPace credential/v2 wire grammar and key schedule; preimages, Finished order, checkpoint-key separation | **PASS.** `credential_v2/channel.rs:49-109`: one `Hkdf::<Sha512>` over the ISK, transcript hash = SHA-512 over canonical CBOR `[public_context, allocator_frame, claimant_frame]`, with distinct labels for `kc A`/`kc B`, `key A-B`/`key B-A`, `iv A-B`/`iv B-A` and `exporter`. `confirm` (`:127-138`) rejects a Finished frame whose side equals the local side and compares in constant time via `subtle`. `sealed_aad` binds `v=2`, direction, counter and `th`. Checkpoint keys are a separate HKDF child, `derive_key` (`checkpoint.rs:355-366`) with salt = ceremony, info = `["cbcl-pairing checkpoint key/v2", side, "anuna.io/credential/v2"]`, and are pinned by an exact hex vector at `checkpoint.rs:851-865`. `decode_canonical` (`mod.rs`) enforces one complete CBOR value, no trailing bytes, no duplicate map keys and byte-exact canonical re-encoding. The independent Python oracle test `test_066_python_oracle_reproduces_complete_v2_schedule_and_first_frame` reproduces the whole schedule, both Finished values, the AAD, the nonce, the first ciphertext, the recovery token and the recovery commitment; it passed here. |
| 5 | Browser installation-device possession proved before offer signing; exact socket-generation typing, bounds, replay resistance | **PASS.** `verify_prepared_offer_device_proof` (`selfsame-pairing/src/credential_v2.rs:569-602`) re-recognises the prepared offer, checks `carrier_ceremony_id`, `device_did` and `device_key_digest` against the offer claims, and verifies an Ed25519 signature over `device_possession_proof_input(socket_generation_digest, carrier_ceremony_id, offer_core_digest)`. Only a `VerifiedPreparedCredentialV2Offer` — constructible solely by that function — can reach `finalize_verified_offer`, and the hub reads its seed only below that hard stop (`cbcl-chat-enrolment-signer.lfe:124-138`). Socket generation is `SHA-256` over canonical CBOR `[domain, hubId, recoveryNonce, SHA-256(frameKey)]` with `1..=64 / ==16 / ==32` bounds in `cbcl-pairing-erl/src/lib.rs:69-87`, independently re-implemented with the same types and bounds in `credential-v2-allocator.mjs`. Binding the socket generation makes a proof from an older socket unusable. |
| 6 | Resolver-closure replay cannot become resolver authority; hub and wallet independently obtain and compare live authority | **PASS.** The hub obtains its own evidence: `do-selfsamepairaccept` calls `path-b-sidecar` after `accept-context` and passes the result as `resolver_evidence` (`cbcl-chat-session-ws.lfe:1913-1919`). In the NIF (`selfsame-beam/src/credential_v2.rs:634-656`) the caller-carried `raw_resolver_closure` is replayed by `replay_carried_resolver_closure` against the profile's first declared resolver id and must resolve to the expected DID, but the value fed to `verify_inactive_grant_pure` is `decode_closures(env, resolver_evidence)` — the hub's own observation. The wallet resolves independently in `verify_live_installation` (`cbcl_v2_commands.rs:1520-1547`) and again on reload through `resolve_path_b_quorum` + `agree_closures`. |
| 7 | Hub final acceptance atomic, retries byte-identical; browser activation follows signed final acceptance; wallet installation follows authenticated receipt, live reciprocal binding and durable CAS | **PASS.** `accept-new` (`cbcl-chat-selfsame-v2-store.lfe:690-724`) writes account, final and pending rows in one Mnesia transaction behind a pending-table write lock, with `new-account-row` taking a whole-table write lock and four uniqueness reads and `new-final-row` refusing a duplicate ceremony or request id. `accept-retry` (`:779-786`) returns the stored `acceptance-ack` verbatim after `final-matches?` compares fourteen fields, so a retry is byte-identical by construction rather than by re-derivation. Browser activation runs through `verify_final_status` on the signed acceptance. Wallet `install` (`cbcl_v2_completion.rs:1702-1740`) verifies the reciprocal binding against a freshly fetched JRD and then compare-and-swaps the exact pending value, treating an identical installed value as idempotent success. |
| 8 | Browser restart works at every pre-terminal checkpoint; restored allocator authenticates the replacement socket, queries status, reopens exact membership, resends only an outstanding exact cached frame; drive all four bootstrap checkpoints including acknowledged `FinishedSent` | **PASS — 0.5.10 H-1 closed.** `cached_bootstrap_sequence` (`bootstrap.rs:131-142`) now returns `Ok(None)` when nothing is outstanding, and `reopen_effects` (`allocator.rs:414-457`) consults it only when the bootstrap actually holds a cached frame. I drove all four checkpoints: generation 1 (`Allocated`) and generation 4 (acknowledged `FinishedSent`) reopen with `Open` alone; generations 2 (`ShareSent`) and 3 (`FinishedSent`, ack pending) reopen with `Open` plus exactly the cached frame at the exact sequence. Generation 1 was not covered by the candidate's own assertions, so I added a probe (`tests/zz_review_probe.rs`, reviewer-authored, not committed to any candidate) and it passes. Mutating `cached_bootstrap_sequence` back to an unconditional resend fails the candidate's own test at `credential_v2_allocator_session.rs:154`. |
| 9 | A wallet failure after durable final approval but before durable `PayloadPrepared` cannot strand a slot; cleanup armed with no post-persist gap, preserves the original error, handles committed-but-reported-failed writes, never deletes a real `PayloadPrepared` slot; fresh same-application ceremony can start | **PASS in code; see M-1 for verification.** `PrePayloadPendingCleanup` is armed at `cbcl_v2_commands.rs:444-445` on the statement immediately after `persist_pending`, advanced at `:485`, `:511`, `:522`, `:567`, and disarmed at `:611` — before the payload frame is released at `:617`, so no ordering exists in which a released payload is followed by an armed guard. `Drop` (`cbcl_v2_completion.rs:800-806`) discards the compensation result, so the original protocol error is never replaced. `remove_pre_payload_attempt` compares the immutable attempt identity rather than the guard's phase, which is what makes a committed-but-reported-failed `replace_pending` compensable, and retains a `PayloadPrepared` value. `persist_pending` refuses a differing pending with `PairingApplicationAlreadyLinked`, so releasing the slot is what lets a fresh same-application ceremony start; TEST-1162 exercises exactly that. The residual gap is that the guard runs on unwind, not on process kill; check 10's abandonment path is the stated answer and it works. |
| 10 | Every pending phase visible as non-secret local state with a confirmed, current-root-gated abandonment path removing only the exact slot, exact policy and slot-owned cache | **PASS.** `pending_links` (`cbcl_v2_completion.rs:1395-1427`) enumerates every pending slot regardless of recoverability and projects only `applicationId`, `relayOrigin` and a phase string from the closed set `final-approval | planned | issuer-created | provisioned | payload-prepared`; it is deliberately separate from `pending_application_ids`, which lists only post-window `PayloadPrepared` recovery candidates. `cbcl_v2_unlink` (`cbcl_v2_commands.rs:957-986`) requires `confirmation`, requires a non-empty passcode, checks `require_root_generation` against the *current* root public key, authenticates presence through `Custody::use_hierarchy_root`, then calls `unlink_local`, which removes the exact slot, the exact `(applicationId, relayOrigin)` policy row (rolling the policy removal back if the slot delete fails) and that one index entry. The hierarchy root, sibling applications and all remote records are untouched, and the view returns `remote_revocation_claimed: false`. The browser half is exercised by `TEST-1162 interrupted pre-payload links are visible and locally abandonable`, which passed here. |
| 11 | `selfsamepairstatus` and `selfsamepairabort` have closed grammars, current-generation authentication, exact binding, bounded state, no unauthorized advance; cancel and transport failure not confused | **PASS.** `validate-selfsame-v2-keywords` (`cbcl-core-dialect.lfe:153-167`) requires room `@hub`, an authenticated `:from`, empty content, empty positional args, an exact keyword set and binary values for every non-`from` key. `decode-credential-v2-status` (`cbcl-chat-session-ws.lfe:2030-2061`) requires exact 32/32/16/64-octet canonical base64url values and refuses unless the request's `recovery-nonce` equals the live connection nonce, then derives the socket generation from the connection context rather than from the frame. The store verifies the installation-device Ed25519 proof under the pending and final table locks (`status-tx`/`status-held`). `decide-status` can only replay an immutable final, rebind the socket generation while preserving phase, report authenticated absence, or close on expiry — it never advances a phase. `decide-abort` accepts only `aborted` or `expired`, and refuses `expired` while `now < expires`, so a caller cannot relabel a live attempt as a timeout. |
| 12 | Post-window HTTPS recovery authenticates token, application, ceremony, offer, receipt commitment, live profile and authority rotation; derives, publishes, issues, resends and installs nothing twice; HTTP POST-only, CBOR-only, bounded, non-redirecting, uncacheable, rate-limited before parsing or lookup, limiter failure fails closed | **PASS.** Wallet side (`cbcl_v2_commands.rs:1106-1291`): the recovery token never leaves Rust (`with_receipt_recovery_token`), the request body is `Zeroizing`, the historical profile re-recognises the signed offer, the accepted branch authenticates the receipt object through the restored claimant before building the installed record, rotation prompts before any effect, `verify_live_installation` re-resolves and re-fetches WebFinger, and `install` is an exact CAS. The `NotFinalized` branch verifies the signed status under the *current* profile bound to application, ceremony, commitment and observed time, then only removes the pending record. Nothing on this path derives, publishes, issues or resends a payload. Transport (`pairing_status.rs`): POST to the application origin, `application/cbor` in and out, request ≤ 2 304 and response ≤ 9 216 octets, `Policy::none()` plus an explicit `is_redirection()` refusal, `https_only`, mandatory `cache-control: no-store`, cookie and content-encoding refusal, and a 503 that must carry no body. Hub side (`cbcl-chat-selfsame-application-http.lfe:120-136`): the limiter is consulted before the content-type check, before the body read and before any store call, and every non-`ok` limiter result — including a raised exception, since the branch is a `catch` with a catch-all — yields an empty no-store 503. Executable: 405/415/400/413/query-string-refusal and the rate-limit 503 are all asserted at the real Cowboy boundary in `cbcl-chat-selfsame-application-http-tests.lfe:202-241`. |
| 13 | Recovery commitment binding is server-owned; first correct use bound atomically under the pending-row write lock; wrong or absent commitments indistinguishable from unknown; signed `not-finalized` never echoes an unauthenticated caller value; pending and finalized retries | **PASS.** `bind-recovery-commitment` (`cbcl-chat-selfsame-v2-store.lfe:535-547`) runs inside `accept-context-tx` after a `'write` read of the pending row: an `undefined` held value is replaced and written; a held value must match under `crypto:hash_equals` on two 32-octet binaries or the transaction aborts. `recovery-without-final` (`:963-989`) returns the atom `unknown` when the application id differs *or* when the commitment does not match — and `pending-recovery-commitment-matches?` also yields false when the stored value is still `undefined`, so absent and wrong are the same answer as unknown. The signed negative's fields all come from the pending record (`profile-bytes`, `application-id`, `carrier-ceremony-id`, `receipt-recovery-commitment`, `kid`) plus the server clock; no caller value is echoed. `recovery-with-final` compares the caller's commitment against the immutable final row before returning the accepted JWS. Mutating the held-value comparison to accept any rebind fails the store suite. |
| 14 | Expiry sampled and enforced at the mutating accept transaction's linearization point after the row lock; a verifier delay crossing expiry cannot project a witness or create durable acceptance effects; accepted retries stay idempotent | **PASS.** `accept-tx` (`cbcl-chat-selfsame-v2-store.lfe:612-634`) takes the `'write` lock on the pending row, *then* samples `commit-now` from the injected clock, rejects a non-integer sample, and aborts `expired` before `accept-projected` is reached — so the opaque witness is never projected and no account, migration, WebFinger or final row is written. Because the sample is inside the transaction body, an automatic Mnesia retry re-samples. The `accepted` phase skips the expiry gate and goes to `accept-retry`, which returns the stored ack, so an accepted retry stays idempotent past expiry as specified. Mutating the sample to read the caller's `now` fails TEST-119. |
| 15 | Every restored display and decision authority survives checkpointing without trusting duplicated JavaScript state or admitting oversized state | **PASS.** `recogniseRecoveryAttempt` (`credential-v2-allocator.mjs:646-706`) requires an exact key set, a phase from a closed set, fixed 32/64-octet values, bounded carrier ≤ 69 632, offer core ≤ 56 000, signed offer ≤ 56 640 and authority response ≤ 768, plus paired presence of the optional digests. The JavaScript snapshot is then treated as untrusted input: `restore_offer_context` (`selfsame-web-device/src/lib.rs:568-628`) re-verifies the offer signature under the profile, re-derives `SHA-256(offer_core)` and `SHA-256(authority_response)`, and re-checks ceremony, application, request id, intent nonce and exclusive expiry before any restored fact is adopted; the signed authority status is re-recognised under the offer's own `kid`. The sealed endpoint checkpoint independently binds side, carrier, generation and nonce, and `decode_inner` re-decodes and re-checks the retained object against its recorded kind, intent digest and content hash. |
| 16 | Installed-link reload verifies stored grant plus current profile, resolver authority, issuer, reciprocal WebFinger binding and hub status before exposing capability; profile-only refresh atomic; rotation prompts; handle change refuses; unavailable and revoked distinct and non-destructive; oversized bounded WebFinger is unavailable | **PASS — 0.5.10 L-4 closed.** `cbcl_v2_reload_verify` (`cbcl_v2_commands.rs:739-951`) checks root generation, then the caller-named handle, then fetches the live profile, then checks account authority and offer-key retention, then WebFinger, then the Path-B quorum, revocation and reciprocal binding, then rehydrates the stored grant and compares issuer, grant id and device key. `classify_reload` orders root/application → handle → authority → issuer → revoked → profile-refresh → usable, and `reload_view` grants `capability: true` only for `Usable` and `ProfileRefresh`; `record_retained` is `true` on every branch, so nothing is erased. Profile-only refresh is an exact CAS through `replace_installed` that keeps the immutable offer profile and hub acknowledgement. `classify_webfinger_failure` (`:1747-1760`) now maps `TooLarge` alongside `Timeout`, `Transport` and `Refused` to `Unavailable`; `NotFound` alone is `HubDeleted`; only `Recognition` is `InvalidBinding`. `bounded_body` produces `TooLarge` both from a `Content-Length` above the bound and from the streaming loop, so an oversized body is genuinely reachable. Mutating the `TooLarge` arm to `InvalidBinding` fails `test_1163`. |
| 17 | Confirmed unlink applies to pending and installed slots; removes only the selected slot, exact policy and application-owned cache; neither purges the root nor claims remote revocation; re-grant after hub deletion needs a fresh ceremony | **PASS.** `LocalCredentialV2Link` covers both halves and `unlink_local` handles them symmetrically, reporting `abandoned` for a pending slot and `unlinked` for an installed one. The application-owned cached material is the profile bytes carried inside the slot value, which the delete removes with it. No shortcut re-grant path exists: the five split construction functions in `cbcl_v2_completion.rs` are the only way to build issuer or grant artifacts, and all of them are reachable only from `cbcl_v2_final_decide` after fresh approval. `purge_all_links` is called only from the root purge. |
| 18 | Dependency pins name one exact buildable closure across Selfsame, cbcl-bus, ADR-802, CI, Android, tests and WASM provenance; the explicit witness checks HEAD and tracked status for all three siblings even under the labelled override; the vendored WASM rebuilds byte-identically and exposes the restored allocator ABI | **PASS — 0.5.10 L-3 closed.** Selfsame `cbcl-pairing.sha` = `62ef4a9`, `cbcl-rs.sha` = `febc669`, `did-crdt.sha` = `1f409a42`; cbcl-bus's four pin files, its Dockerfile build args and the WASM provenance all agree, and CI's cargo cache key hashes all three pin files. `crates/selfsame-pairing/build.rs` guards all three siblings and honours the labelled override only as a warning; `tests/dependency.rs:32-54` has **no** override escape, so the witness is the strict layer. I ran the full TEST-1164 matrix in a clean exact-pin closure with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` set: a tracked-byte change in `cbcl-rs`, in `cbcl-pairing` and in `did-crdt`, and a HEAD change in `cbcl-pairing` and in `did-crdt`, each fails the witness with a message naming the sibling; restoring the clean closure passes. `scripts/check-selfsame-wasm-rebuild.sh` reproduced the vendored package byte-identically from clean clones at the exact four revisions with rustc 1.96.0 / wasm-bindgen 0.2.126 / aarch64-apple-darwin, and the vendored `.wasm` hashes to `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`, matching the claim exactly. The regenerated `.d.ts` declares `CredentialV2BrowserAllocatorSession.restore`, `restore_offer_context`, `restored_phase` and `restored_presence_code`, so the restored allocator ABI is present. On the did-crdt question the brief asks me not to assume: `service` is a non-default feature (`did-crdt/Cargo.toml:12-16`), `pub mod service` is `#[cfg(feature = "service")]`, every Selfsame crate consumes `did-crdt` through the workspace entry with `default-features = false`, and no crate enables `service` — and the four post-pin commits touch only `src/service/*` and a spec. They therefore participate in no credential/v2 runtime build. |
| 19 | Crash, disconnect, timeout, replay, wrong order, cancel, expiry and recovery cannot create a second identity effect or accepted authority; orphan-DID residual evaluated separately | **PASS on the duplication property; the orphan is L-1 and is NOT blocking.** Hub-side singularity comes from the whole-table write lock plus four account-index reads and the final-row conflict check inside one transaction, with retries replaying stored bytes. Wallet-side, the slot is a `deny_unknown_fields` tagged union under a process mutex with an exact compare-and-swap on every transition, `persist_pending` refuses a differing pending, and the recovery adapter can neither rebuild a grant nor re-sign, republish or resend a payload. Channel-level replay is closed by exact per-direction counters and a terminal flag; relay-level replay by exact sequence acknowledgement; checkpoint-level replay by mandatory generation and nonce bindings that refuse a reused nonce and a non-successor generation. The orphan DID creates no account, no capability and no authority, and cannot be promoted to one. |
| 20 | Bounds and one-over-limit refusals executable; the ordinary production entry remains closed; loopback staging and existing enrolment remain distinct | **PASS, with a deployment observation.** One-over-limit refusals are executable at the relay wire (`recognition.rs:318-324`, a 69 633-octet body refused against the 69 632 ceiling), at the recovery HTTP boundary (2 305 octets → 413), at the wallet transport (`MAX_RESPONSE_OCTETS + 1` refused), and at the anonymous limiter (`limit + 1` refused — I confirmed by mutation that widening the window budget by one fails two of the four limiter tests). The ordinary production entry is closed: `startCredentialV2Staging` and `restoreCredentialV2Staging` throw `credential/v2 release gate is closed` off loopback (`app.js:1852`, `:2257`), and `window.cbclCredentialV2` is installed only on loopback (`:2268`). Loopback staging and CON-214 enrolment are distinct surfaces with distinct routes and distinct signer entry points. **Observation, not a finding:** the hub's `selfsamepairallocate` route and `/selfsame/pairing/v2/status` are compiled into the release and gated by the *serving* gate, which the 2026-08-22 ratification opened; only `cbcl-chat-path-b-gate` (the WebSocket Path-B admission half) is hard-closed, and `do-selfsamepairallocate` does not consult it. `require-path-a-tx` restricts allocation to an unbound Path-A handle whose enrolled key matches the frame key, so this is not an unauthenticated surface. Whether that hub half should be reachable before GATE-04 closes is a deployment decision, explicitly outside this review's authority and covered by GATE-04's final box. |
| 21 | Tests reach the named invariants; identify surviving mutants | **FAIL — M-1.** Mutants experimentally **killed**: acknowledged-`FinishedSent` reopen (cbcl-pairing allocator suite); recovery-commitment rebinding and post-lock expiry re-sampling (cbcl-bus store suite, TEST-119); the anonymous limiter's one-over-limit boundary; the oversized-WebFinger classification (TEST-1163); all five sibling pin-guard cases (TEST-1164); and deleting the compensation guard's `Drop` body (TEST-1162). Phase reconciliation, browser durable-state recognition and rate limiting are adequately covered. **Four mutants survive**, all inside the CON-990/REQ-1006 removal mechanism and all corresponding to clauses TEST-1162 states in terms: the `PayloadPrepared` retention, the `same_attempt` identity comparison, the installed-slot refusal in compensation, and the exact compare-and-delete in `unlink_local`. Compounding this, the one Rust test that names TEST-1162 is `#[ignore]`d and runs in no automated gate, and the browser suite carrying the same requirement is not invoked by CI either. |

## Dispositions for every named prior-review item

### 0.5.10 findings

| Item | Disposition |
|---|---|
| **H-1** — restored allocator driven to `Terminal` at the acknowledged-`FinishedSent` checkpoint | **CLOSED, code- and mutation-confirmed.** `cached_bootstrap_sequence` returns `Ok(None)` when `awaiting_ack` is false, and `reopen_effects` consults it only when a cached frame exists. I restored a real allocator at generation 4 and observed `Bind` then `Open` alone, no `Put`, no `Terminal`; and at generation 1, 2 and 3 the specified behaviour. Reverting the fix fails the candidate's own assertion at `credential_v2_allocator_session.rs:154`. |
| **H-2** — a terminal refusal after final approval but before payload send leaves an unremovable pending slot | **CLOSED in code; verification incomplete — see M-1.** `PrePayloadPendingCleanup`, `remove_pre_payload_attempt`, `pending_links`, `load_local_link`, `unlink_local`, `cbcl_v2_pending_links`, the `interrupted-link` UI and TEST-1162 all exist, and the behaviour the 0.5.10 report demanded holds: the slot is released on the failure path, is visible if it survives, is abandonable under current-root presence, and a fresh same-application ceremony can start. What is not closed is the verification obligation the same remediation wrote into TEST-1162; that is this review's M-1. |
| **L-1** — orphan DID publication with no compensating unpublish | **OPEN, unchanged in kind.** Re-derived from code here; re-recorded as this review's L-1 with consequence and operational treatment. |
| **L-2** — CON-036 limiter keyed on the TCP peer address | **OPEN, unchanged.** Re-derived from code here; re-recorded as this review's L-2 with consequence and operational treatment. |
| **L-3** — `test_701` verified only the cbcl-pairing sibling | **CLOSED, executed.** The witness now iterates all three siblings and has no override escape. Full TEST-1164 matrix run with the override set: five distinct drift cases, five failures naming the right sibling; clean closure passes. |
| **L-4** — an oversized WebFinger response reported as `revoked` | **CLOSED, executed.** `TooLarge → Unavailable`, asserted by `test_1163`, and mutating the arm back to `InvalidBinding` fails that test. `bounded_body` genuinely produces `TooLarge` on both the advertised-length and streaming paths. |

### 0.5.9 findings

| Item | Disposition |
|---|---|
| **H-1** — restored allocator cannot reopen at either bootstrap checkpoint holding a cached frame | **CLOSED.** Both states re-tested here; see 0.5.10 H-1 for the fourth checkpoint, also now closed. |
| **H-2** — REQ-1006 installed-state lifecycle unimplemented | **CLOSED.** `cbcl_v2_installed_links`, `cbcl_v2_reload_verify`, `cbcl_v2_unlink`, `classify_reload`, `verify_reload`, `replace_installed`, `unlink_local` and `test_1159_reload_lifecycle_is_closed_over_every_required_state` all exist and pass (the reload test runs in the default suite). Handle change refuses, rotation prompts, unavailable/revoked/hub-deleted are distinct and the record is never cleared. |
| **M-1** — hub signed `not-finalized` without authenticating the recovery token; commitment echoed from the caller | **CLOSED, mutation-confirmed.** Server-owned first-use binding under the pending-row write lock; the signed negative's fields come from the stored row; wrong and absent are both `unknown`. Removing the held-value comparison fails the store suite. |
| **M-2** — mutating accept transaction had no expiry gate | **CLOSED, mutation-confirmed.** Post-lock sampling in `accept-tx`; replacing the sample with the caller's `now` fails TEST-119. |
| **M-3** — coordinated pins disagreed; Selfsame CI could not build the candidate | **CLOSED, build-confirmed.** A clean exact-pin closure builds and passes `cargo test --workspace --locked` (805 tests) with no override, and the coordinated pin files agree across both repositories, the Dockerfile and the WASM provenance. |
| **L-1** — orphan DID publication and wedged slot | **SPLIT, as 0.5.10 recorded.** The wedged slot is now closed (0.5.10 H-2). The orphan publication remains open as this review's L-1. |
| **L-2** — limiter keyed on TCP peer address | **OPEN, unchanged.** This review's L-2. |

### 0.5.8 findings

| Item | Disposition |
|---|---|
| **H-1** — `selfsamepairstatus`/`selfsamepairabort` absent | **CLOSED.** Independently re-derived this session: closed dialect validators, hub-originated response class, session-ws handlers, pure `decide-status`/`decide-abort`, store transactions with device-proof verification under lock, and the browser send/receive path. Re-tested against check 11's whole property list. |
| **H-2** — CON-989 HTTPS recovery unimplemented | **CLOSED.** All three sides traced end to end (check 12), with the two defects 0.5.9 found inside the new mechanism (its M-1, M-2) also closed and mutation-confirmed here. |
| **M-1** — cbcl-pairing's pin unenforced and inconsistent across three sources | **CLOSED.** Superseded by the three-sibling guard and witness; see check 18. |
| **L-1** — orphaned DID publication | **OPEN, unchanged in kind.** This review's L-1. |

### 0.5.7 findings

| Item | Disposition |
|---|---|
| **N-1** — socket-generation digest carried no reproducible encoding or bound | **CLOSED, re-derived in two implementations.** `cbcl-pairing-erl/src/lib.rs:69-87` and `credential-v2-allocator.mjs` independently produce the same canonical-CBOR preimage with the same `1..=64 / ==16 / ==32` bounds, and both refuse out-of-bound input. |
| **L-1** — one headroom assertion proved arithmetic rather than frame admission | **CLOSED.** Frame admission is now executable end to end: `recognition.rs` admits a 69 632-octet body and refuses 69 633; `relay_work_bounds.rs` exercises the queue bound. |
| **L-2** — the global frame ceiling rested on an untested compatibility claim | **CLOSED.** The 69 632 ceiling is now asserted at the relay recogniser, in the endpoint checkpoint bound (`credential_v2_endpoint.rs:521`), in the browser's `boundedB64u` limits and in the relay service queue configuration, so all four sites are executable rather than asserted in prose. |
| **L-3** — two member-naming conventions with no recorded split | **CLOSED (editorial).** SPEC-008 0.5.11 states current law directly in the parent, with historical amendments recorded as evidence only (ADR-963); the naming is consistent within the current parent. |
| **L-4** — two citation forms for same-document and historical identifiers | **CLOSED (editorial).** Same mechanism as L-3; the 0.5.11 text uses one wikilink form throughout the sections read. |
| **L-5** — the v2 lifetime range admits carriers the coordinated hub always refuses | **OPEN as an honest, fail-closed residual, unchanged.** `decide-allocation` rejects `carrier-window` when `carrier-expiry - now < 600` (`cbcl-core-selfsame-v2-pending.lfe:34-35`), while the carrier grammar itself admits any `relay_expires_at`. The coordinated allocator always requests 900 s, so the coordinated path cannot produce such a carrier; a hand-made one is refused at allocation with no effect. Consequence is a refusal, never an acceptance. |
| **L-6** — one checkpoint key derivation stated less exactly than its sibling | **CLOSED, vector-confirmed.** Both derivations now have exact executable vectors: `checkpoint.rs:852-865` pins `checkpoint_key_info` and `derive_key` in hex, and `checkpoint_info_is_length_prefixed_and_application_separated` pins the Selfsame-side wrapping key to a fixed base64url value and proves application separation. |
| **L-7** — the finalize retry after a socket rebind needs a re-signed proof | **CLOSED, executed.** The browser suite drives exactly this: after `recoverSocket` the allocator signs a fresh possession proof over the new socket generation and retries finalize rather than re-registering or re-signing the offer (`credential-v2-allocator.test.mjs`, "CPace establishment drives proof-gated offer signing…"). The hub re-verifies the proof on the `offered` retry path before touching the seed. |

### F-A through F-E

| Hard stop | Disposition |
|---|---|
| **F-A** — exact application+relay, never relay-only | **CLOSED, code-confirmed.** `cbcl_v2_policy.rs` read in full; the row key is the full tuple at every call site; `bind_finished_profile` writes the row only after both Finished values and re-checks an existing pair rather than assuming it. `exact_pair_never_authorises_another_application_on_the_same_relay` passes. |
| **F-B** — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed and executed.** The seven `compile_fail` doctests in `cbcl_pairing` (including `profile::DisplayField`, `profile::DisplayIntent`, `CredentialV2Display`, `PathAToBDisplay`, `EndpointCheckpointV2`, `CredentialV2RecoveredReceiptAuthority`) all pass. `RelayConsentView` and `intent_view` project only verifier-produced values. |
| **F-C** — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed.** `recognise_profile_candidate` → `authorise_claimant_relay` → `prepare_claimant` → `bind_finished_profile` is the only path to a live claimant; `recover_claimant_completion` is a terminal adapter that cannot derive, sign, publish or resend. |
| **F-D** — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed.** The scope, account-principal digest and snapshot nonce are hub-minted per allocation and reach the wallet only inside the Ed25519-signed offer; `preview_identity` and `cbcl_v2_final_decide` both read the scope from the signed claims. `CredentialV2Context::derive` binds the wallet's own profile digest into the public context, so Finished covers it. |
| **F-E** — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged).** ADR-902 states it; nothing in the candidate contradicts it, and check 3's exact-pair consent is the stated mitigation. |

### [[SPEC-007-cbcl-pairing-cutover#REQ-812]]

| Clause | Disposition |
|---|---|
| No accepted credential/identity effect before approved payload delivery (v1 profiles) | **Holds.** v1 paths untouched by the 0.5.11 delta; `test-vectors/spec-00{1,4}-v1.json` changed only in the recorded revision string and `pinned_derivation.rs` proves the derivation is unchanged. |
| No application identity effect before preliminary approval | **Holds.** The intent decision object is built and sent before any custody call. |
| Preliminary approval authorises one zeroizing derivation and pure preview only | **Holds.** `preview_identity` computes only `home_did` inside `Custody::use_hierarchy_root`. |
| No signing/publish/resolve/issue/persist/stage/payload before final approval | **Holds.** Re-verified line by line at `cbcl_v2_commands.rs:308-560`. |
| Final approval authorises only the declared causal order | **Holds.** issuer → publish → resolve and verify closure → grant → payload, with a durable `replace_pending` between stages. |
| One encrypted non-authorising completion checkpoint before the first causal effect | **Holds.** `persist_pending` at `:443` precedes `build_issuer_artifacts` at `:516`, and the sealed `EndpointCheckpointV2` is inside it. |
| Browser staging crash-safe and non-authorising until immutable hub acceptance | **Holds** for the in-band path and for the CON-989 post-window path. |
| Compensation touches only ceremony-owned effects | **Holds in its prohibitive half; now largely satisfied in its positive half.** No operator withdrawal or pre-existing identity is changed, and the pending slot — the gap 0.5.10 named — is now compensated automatically and abandonable manually. One ceremony-owned effect remains uncompensated: the published issuer DID (L-1). |

### REQ-909 (2026-08-22 withdrawn first-contact amendment) — recurrence check

Independently re-tested rather than inherited. **F-1** does not recur: the
carrier carries a real `application-context` application identifier parsed by
`ApplicationId::parse` before use, and the fetched profile's own
`applicationId` must equal it byte for byte. **F-2** does not recur: every
closure, publication, alias and grant effect sits behind explicit final
approval, with the durable non-authorising checkpoint written first. **F-3/F-4**
persist as the same honest residual as F-E: a relay descriptor certifies its
operator only to the extent the signed profile does, which is why exact-pair
consent exists. **F-5/F-6** were internal contradictions of the withdrawn text
and have no analogue in the current architecture.

## Exact test commands and results

All runs used clean detached clones or worktrees at the exact candidate
commits, laid out as siblings of one another. Toolchain observed:
rustc/cargo 1.96.0, wasm-bindgen 0.2.126, wasm-pack 0.14.0, host
`aarch64-apple-darwin`, Erlang/OTP 28, rebar3, Node 22.12.0.

| # | Command | Result |
|---|---|---|
| 1 | `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` | `authMethod=claude.ai`, `apiProvider=firstParty`, `subscriptionType=max` |
| 2 | cbcl-pairing @ `62ef4a9`: `cargo test --workspace --locked` | **143 passed, 0 failed** |
| 3 | cbcl-pairing: reviewer probe `cargo test --test zz_review_probe` (generation-1 reopen, expired restore, mandatory restore bindings) | **3 passed** |
| 4 | cbcl-pairing mutation: `cached_bootstrap_sequence` always resends | **FAILED (intended)** at `credential_v2_allocator_session.rs:154` — mutant killed |
| 5 | Clean exact-pin closure, Selfsame @ `0c58012`: `cargo test --workspace --locked`, no override | **805 passed, 0 failed** |
| 6 | Selfsame: `cargo test -p selfsame-pairing --locked --test dependency` in the clean closure | **1 passed** |
| 7 | Selfsame TEST-1164 matrix with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1`: tracked byte in cbcl-rs / cbcl-pairing / did-crdt; HEAD change in cbcl-pairing / did-crdt | **FAILED in all five (intended)**, each naming the right sibling; clean closure then **passed** |
| 8 | Selfsame: `cargo test -p selfsame --locked --lib` | **66 passed, 0 failed, 1 ignored** |
| 9 | Selfsame: `cargo test -p selfsame --locked --lib -- --ignored --exact …test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot` | **1 passed** |
| 10 | Selfsame mutation: delete the `PrePayloadPendingCleanup::drop` body | **FAILED (intended)** — mutant killed |
| 11 | Selfsame mutation: `if current.is_payload_prepared()` → `if false` | **PASSED — mutant SURVIVES** (M-1) |
| 12 | Selfsame mutation: remove the `same_attempt` guard | **PASSED — mutant SURVIVES** (M-1) |
| 13 | Selfsame mutation: compensation deletes an `Installed` slot | **PASSED — mutant SURVIVES** (M-1) |
| 14 | Selfsame mutation: remove `unlink_local`'s exact compare-and-delete | **PASSED — mutant SURVIVES** (M-1) |
| 15 | Selfsame mutation: `NetError::TooLarge → InvalidBinding` | **FAILED (intended)** at `test_1163` — mutant killed |
| 16 | Selfsame: `node --test tests/spec-007-wallet-pairing.mjs` | **5 passed, 0 failed** (includes TEST-1159 and TEST-1162) |
| 17 | cbcl-bus @ `965d6cc`, clean exact-pin siblings: `rebar3 eunit` | **All 975 tests passed** |
| 18 | cbcl-bus browser set: `node --test *.test.mjs` in `apps/cbcl_chat/priv/web` | **394 passed, 0 failed** |
| 19 | cbcl-bus mutation: `accept-tx` samples expiry from caller facts | **FAILED (intended)** at TEST-119 — mutant killed |
| 20 | cbcl-bus mutation: `bind-recovery-commitment` accepts any rebind | **FAILED (intended)** at TEST-119 — mutant killed |
| 21 | cbcl-bus mutation: limiter budget widened by one (`>= limit + 1`) | **FAILED (intended)**, 2 of 4 limiter tests — mutant killed |
| 22 | cbcl-bus: `bash scripts/check-selfsame-wasm-rebuild.sh` from clean clones at all four exact revisions | **"Selfsame WASM rebuild matches the exact vendored package"**; vendored `.wasm` SHA-256 `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`, equal to the provenance record and to the generating agent's claim |
| 23 | CI coverage audit: `grep` for `--ignored` / `e2e:wallet-pairing` across `.forgejo/workflows`, scripts, manifests | Neither TEST-1162's Rust half nor the wallet browser suite is invoked by any workflow — evidence for M-1 |

Every mutation above was applied with an explicit source rewrite plus `touch`,
and the rebuild was confirmed from the toolchain's own compile line before the
result was recorded; one earlier limiter run that reported a false survivor was
discarded once a stale build was identified, and the corrected run is what
appears in row 21. Every mutated file was restored, and all clones and
worktrees finished clean.

The generating agent's reported results were confirmed where they overlap:
cbcl-pairing workspace PASS, cbcl-bus 975 eunit PASS, cbcl-bus browser set PASS,
Selfsame focused regressions PASS, wallet browser regression PASS, clean
exact-pin dependency test PASS without override, deliberate witness failure
against a tracked-dirty sibling under the override, and the byte-identical WASM
rebuild at the exact claimed digest.

## What this verdict authorizes

Nothing beyond the review gate. This report records a **REJECT**; even a PASS
would not authorize release, public allocation, `git push`, production
enablement, or deployment. Those controls are outside this review's authority
and remain governed by GATE-04 and by separate owner approval. The owner's
authorization of test-first implementation before review PASS is unchanged by
this verdict and does not relax any of them.
