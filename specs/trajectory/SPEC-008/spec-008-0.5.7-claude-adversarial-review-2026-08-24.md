---
id: spec-008-0.5.7-claude-adversarial-review
title: SPEC-008 0.5.7-draft — Tier-1 adversarial review of the possession-proof and lifetime reissue
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 8
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `9145c9bf12ecc273c1afd7df2de4a5598bec0f24` SPEC-008 0.5.7-draft and SPEC-007 0.3.5-draft; cbcl-bus `854121e2461a1a476c69a879fa2b3da2abe75814` SPEC-053 0.17.7-draft; cbcl-pairing `30cadcb9168ff22042f723d8456fa8327ea2dd41` SPEC-001 0.5.6-draft; did-crdt `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 8. It imported no conclusion from an earlier Claude conversation. It read the 0.5.6 report only after an independent first pass over the four coordinated parents, the deprecated did-crdt disposition, and the five pinned code baselines."
date: 2026-08-24
---

# SPEC-008 0.5.7-draft — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the coordinated 0.5.7 reissue is singular, safe, bounded,
and implementable without invention.

Metaphor: a bridge inspection before the first vehicle. The new bolt now carries a
specified thread. The anchor that bolt threads into carries none.

Structure:

```text
independent pass over four parents + five code baselines
        -> one new finding + seven observations
        -> attempt-7 N-1, N-2, and L-1 through L-6 re-test
        -> earlier N, C, H, M, L, F-A through F-E, and REQ-812 re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; zero HIGH; one MEDIUM; seven LOW
Load-bearing: the new possession proof depends on an untyped, unbounded preimage
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         one blocking correction and seven recorded observations
Detail:       the finding sections and the assessment sections below

This report uses descriptive controlled language. Its wikilink anchors are fully
qualified. Cross-vault identifiers stay unlinked, matching the parents.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-opus-5`. The generating author is
OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model requirement
holds.

The launcher removed `ANTHROPIC_API_KEY` before this session started. The reviewer
tested that variable in the process environment. The variable is unset, and a
counting scan of the environment matched zero entries. No value reached this
report.

`claude auth status --json` ran with that variable removed. It reported
`authMethod = claude.ai`, `apiProvider = firstParty`, and
`subscriptionType = max`. This report records no email, organization identifier,
access token, API key, or account identifier.

The driver SHA-256 is
`4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

The session is fresh. The reviewer completed the independent pass before it opened
the 0.5.6 report. Every prior finding below carries an independently re-derived
disposition.

## Exact review set, baselines, and pre-implementation status

Each candidate object exists and carries the declared version.

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `9145c9bf12ecc273c1afd7df2de4a5598bec0f24` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.7-draft | yes |
| selfsame | `9145c9bf12ecc273c1afd7df2de4a5598bec0f24` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.5-draft | yes |
| cbcl-bus | `854121e2461a1a476c69a879fa2b3da2abe75814` | `specs/SPEC-053-key-root-identity.md` | 0.17.7-draft | yes |
| cbcl-pairing | `30cadcb9168ff22042f723d8456fa8327ea2dd41` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.6-draft | yes |
| did-crdt | `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

The SPEC-008 blob is `f7e3525fa0ad8c505cdd4dd9affbcc9f71a54528`. The SPEC-007 blob
is `f607b23dd099361230e7b4ab12b9fd39e64cd29a`. The SPEC-053 blob is
`4de1859c590731a939ca68e995c2d887e5fc3662`. The SPEC-001 blob is
`0dc15de03cc7e227a17285a801995524c3c30207`. The SPEC-037 blob is
`5ff881b24a37e2ed3cd142f963769fa9617f379e`.

Every implementation baseline resolves as a commit. They are selfsame `0220cec2`,
cbcl-bus `ce894a7f`, cbcl-pairing `c22f2d05`, did-crdt `1f409a42`, and the
anuna-ssi namespace pin `c7d46202`.

The reviewer read every candidate through `git show COMMIT:path`. No working-tree
substitute entered this review.

Each repository changes no code between its implementation baseline and its
candidate commit. The reviewer ran a path-excluding diff in all four repositories
and found only specification and trajectory files. The candidate is therefore
intentionally unimplemented, and the reviewer judged the prose against the pinned
baselines.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 | yes |
| LOW | 7 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. One blocking finding
survives, so the verdict is REJECT.

## Findings

### N-1 The socket generation digest carries no reproducible encoding or bound

Severity: MEDIUM. Owner: cbcl-bus repository owner.

The 0.5.7 reissue defines `devicePossessionProofInput` exactly. Its label is
`cbcl-chat credential/v2 device possession proof v1` with one trailing zero octet.
Its deterministic-CBOR array holds three members. The contract states that each
array value is its raw 32-octet byte string.

The reviewer reproduced that construction. The label contributes 51 octets. The
array header contributes one octet. Each 32-octet byte string contributes 34
octets. The hash preimage therefore contains exactly 154 octets.

The first array member is `socketGenerationDigest`. cbcl-bus CON-012 defines that
value as:

```text
frameKeyDigest = SHA-256(raw 32-octet Ed25519 frame key)
socketGenerationDigest = SHA-256(DETERMINISTIC-CBOR([
  "selfsame credential/v2 socket generation v1",
  hubId,
  recoveryNonce,
  frameKeyDigest
]))
```

**No member of that array carries a declared CBOR type.** The contract names
`recoveryNonce` as the existing 16-octet connection nonce. It derives
`frameKeyDigest` from a SHA-256. It supplies `hubId` neither a type, nor a length
bound, nor a grammar. Its only locating sentence places `hubId` and the nonce in
the existing connection bootstrap frame.

An implementer therefore chooses whether each member is a CBOR text string or a
CBOR byte string. Two independent implementations can choose differently and
compute different digests. The reviewer found no global typing convention in any
of the four parents. A search for such a rule matched only two local sentences,
and each one scopes to its own adjacent block.

The omission is inconsistent with the rest of the increment. Every other
deterministic-CBOR preimage in cbcl-bus CON-012 types its members. The authority
status input mirrors a complete CDDL. The staging receipt input mirrors a complete
CDDL. The migration digests write `bstr(UTF8(...))` and `u32be(len(...))`
explicitly. The new possession proof types its own three members in one sentence.

The gap has three consequences.

**The new test cannot execute as written.** cbcl-bus TEST-117 requires an
independent reproduction of `devicePossessionProofInput` from its exact label and
three deterministic-CBOR members. The first of those three members is not
independently reproducible.

**The two-implementation requirement fails for this value.**
[[SPEC-008-production-pairing-claimant#TEST-1160]] and cbcl-pairing TEST-066
require independent implementations to reproduce every cryptographic byte. The
browser signer and the LFE hub share no stated encoding for this digest.

**One preimage member carries no bound.** The hub identifier is operator
configuration. At `ce894a7f`, `apps/cbcl_chat/src/shell/cbcl-chat-pairing-ws.lfe:31`
reads `(application:get_env 'cbcl_chat 'hub_id #"cbcl-chat")`. That value is an
Erlang binary of unstated length. Every other member of every other preimage in
this increment carries an explicit size.

A second instance sits in the same contract. The `deviceRecoveryProof` preimage is
a four-member deterministic-CBOR array. It also declares no member types. Its
three non-label members do carry stated lengths, so that instance is narrower.

The value itself reaches the browser. At `ce894a7f`,
`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:112` sends
`conn-nonce-frame(conn_nonce, hub_id)` as the bootstrap frame. Line `:62` defines
`gen-conn-nonce` as 16 strong random octets. The browser therefore holds the bytes
and lacks only their encoding.

The defect is fail-closed. A disagreement makes every possession proof and every
recovery proof fail verification. No identity effect follows, which keeps this
finding below HIGH. It remains an unresolved implementability defect in the exact
construction this reissue exists to define.

Correction: give `socketGenerationDigest` a closed CDDL. Bound `hubId` and declare
its type. Declare `recoveryNonce` as a 16-octet byte string and `frameKeyDigest` as
a 32-octet byte string. Apply the same treatment to the `deviceRecoveryProof`
array. Require both reproductions in cbcl-bus TEST-117.

## Low observations

### L-1 One headroom assertion proves arithmetic rather than frame admission

cbcl-bus CON-012 states that the final command's encoded argument values contain
exactly 1,470,509 ASCII octets. It states that the frame bound leaves 29,491 octets
for the closed command syntax.

The reviewer recomputed both numbers and matched each exactly. Five 32-octet
digests encode to 43 unpadded base64url octets each. A 512-octet `issuerDid`
contributes 512. A 4,608-octet `stagingReceipt` encodes to 6,144. That sum is
6,871. Adding 65,536 and 1,398,102 yields 1,470,509, and 1,500,000 less that total
yields 29,491.

cbcl-bus TEST-117 requires exactly 29,491 octets of frame headroom after all
encoded values. That quantity is the difference of two declared constants. The
increment states no octet count for the closed command syntax or the outer signed
envelope. The test therefore does not prove that the maximum real frame fits.

The residual is small. A nine-argument command name, its argument keywords, and its
envelope occupy a few hundred octets by inspection.

### L-2 The global frame ceiling rests on an untested compatibility claim

cbcl-bus CON-012 sets Cowboy `max_frame_size` to 1,500,000 octets on the
authenticated chat WebSocket. It states that every existing valid credential/v1 and
chat object remains below that ceiling.

At `ce894a7f` the chat handler sets only `idle_timeout`, so the socket inherits the
unbounded default. The reviewer found no byte bound on a published room payload and
none on a dialect `:def` upload. The largest bounded field is a group-info value at
262,144 decoded octets.

cbcl-bus TEST-117 exercises 1,500,000 and 1,500,001 octets. It requires no
enumeration of existing object maxima. The compatibility claim therefore rests on
prose.

The change tightens an unbounded surface, so its direction improves safety. A plan
can settle the residual by measurement.

### L-3 The increment carries two member-naming conventions and records no split

cbcl-pairing CON-028 now spells the receipt body members `carrierCeremonyId`,
`predecessorDigest`, `finalStatusJws`, and `finalStatusDigest`. Those names match
the nine consumer bodies in [[SPEC-008-production-pairing-claimant#CON-987]].

cbcl-bus CON-036 keeps kebab case for its recovery request and response. Its
members include `carrier-ceremony-id`, `receipt-recovery-token`,
`final-status-jws`, and `recovery-status-digest`. cbcl-pairing CON-027 keeps
`carrier-ceremony-id` in the carrier.

Each grammar is closed and internally consistent, so recognition stays
deterministic. No clause records the partition between the two conventions.

### L-4 Same-document and historical identifiers keep two citation forms

[[SPEC-008-production-pairing-claimant]] now cites
[[SPEC-004-application-scoped-identity#CON-220]],
[[SPEC-004-application-scoped-identity#CON-201]],
[[SPEC-004-application-scoped-identity#CON-219]], and
[[PROTO-003-selfsame-pairing-v1#CON-409]] as fully qualified wikilinks.

Bare same-document identifiers survive at lines 318, 386, 753, 1141, 1453, 1465,
1694, and 1697. They name CON-903, CON-988, and OBS-907 in adjectival positions.
The changelog cites bare `CON-207` and `ADR-912` at lines 2034 and 2036.

Every one of those identifiers appears elsewhere in the same parent as a fully
qualified anchor. The inconsistency is presentational and creates no ambiguity.

### L-5 The v2 lifetime range admits carriers the coordinated hub always refuses

cbcl-pairing NFR-023 sets the credential/v2 mailbox lifetime between 600 and 900
seconds. cbcl-bus REQ-082 refuses a carrier with fewer than 600 remaining seconds
at hub allocation.

A 600-second v2 mailbox therefore leaves no transport time. The hub accepts it only
when zero whole seconds elapse between relay allocation and hub allocation. Every
lifetime below 900 shrinks that transport window by the same difference.

cbcl-bus REQ-082 requires the browser allocator to request exactly 900 seconds, so
the coordinated path never meets this case. cbcl-pairing is a reusable library, and
a second allocator can select the unusable end of the range.

### L-6 One checkpoint key derivation is stated less exactly than its sibling

[[SPEC-008-production-pairing-claimant#CON-986]] states the claimant checkpoint
wrapping key exactly. It names the label bytes, the application bytes, both
big-endian length prefixes, the raw salt, the extract order, and the 32-octet
output.

cbcl-pairing CON-030 states that the checkpoint key is HKDF-SHA512 over that
wrapping key. It names the salt as the raw carrier ceremony identifier. It
describes the info as deterministic CBOR containing a label, an endpoint role, and
an application context. It declares no array shape, no member types, and no output
length.

A checkpoint never crosses between implementations, and cbcl-pairing TEST-066
validates CON-027, CON-028, and CON-031 rather than CON-030. The looseness
therefore blocks no interoperability.

### L-7 The finalize retry after a socket rebind needs a re-signed proof

cbcl-bus CON-012 binds `devicePossessionProofInput` to the held socket generation.
Pending recovery atomically replaces that held generation digest.

A browser that loses the finalize response reconnects and recovers phase `offered`.
The pending status response repeats no signed offer, so the browser reissues
`selfsamepairofferfinalize` to obtain it. That command carries a proof bound to the
new generation, so its bytes differ from the first attempt.

CON-012 states that an exact retry in the same phase returns the byte-identical
prior response. It also states that a wrong proof is terminal. The clauses resolve
correctly together, because the hub recomputes the input from the generation it
currently holds.

No clause states that reading in one place. Two implementations can read command
byte inequality as a wrong proof and end the ceremony. The outcome stays
fail-closed, and a fresh ceremony recovers the person.

## Disposition of the 0.5.6 findings

Each row is independently re-derived at the current candidates and the pinned code.

| 0.5.6 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 the credential/v2 device-possession proof carries no defined signing input | **CLOSED for the proof itself; its first member REOPENED** | cbcl-bus CON-012 now states the label, the three-member deterministic-CBOR array, the raw 32-octet member rule, the SHA-256 boundary, the held `deviceKeyJwk`, and verification before pending-device registration or offer signing. The reviewer reproduced the 154-octet preimage. TEST-117 adds label, generation, ceremony, offer, key, signature, and replay mutations. The proof no longer enters the offer bytes, which removes the earlier circularity. See [[spec-008-0.5.7-claude-adversarial-review-2026-08-24#N-1 The socket generation digest carries no reproducible encoding or bound]]. |
| N-2 one named failure mode still misdescribes the pinned baseline | **CLOSED** | FM-3 now records that the UI reaches `cbcl_enrol_prepare` and `cbcl_enrol_confirm` from `app.js`. It records the other four commands as unreachable. At `0220cec2`, `src/app.js:580`, `:615`, and `:621` carry those two invocations. An exhaustive scan of `invoke("…")` names under `src/` returns no other `app_grant.rs` command. `src/index.html:951` loads `app.js`, and `app.js:64` imports `pairing.js`. [[SPEC-008-production-pairing-claimant#TEST-1157]] now requires that witness. |
| L-1 one refusal clause names a boundary the design crosses by intent | **CLOSED** | [[SPEC-008-production-pairing-claimant#REQ-906]] now ends the attempt before relay socket creation. A search of the parent for a network-boundary phrase returns no match. The `Controls` list, CON-902, CON-986, and CON-988 state the same boundary. |
| L-2 the shared receipt body mixes two member-naming conventions | **CLOSED for the receipt body** | cbcl-pairing CON-028 spells all four receipt members in camel case. CON-027 keeps the pre-existing carrier member `carrier-ceremony-id`. TEST-064 requires the exact camel-case names and refuses every kebab-case substitute. See [[spec-008-0.5.7-claude-adversarial-review-2026-08-24#L-3 The increment carries two member-naming conventions and records no split]]. |
| L-3 one declared frame sum is a valid bound rather than the exact maximum | **CLOSED** | cbcl-bus CON-012 now declares 6,871, 1,470,509, and 29,491. The reviewer recomputed each value independently and matched all three. TEST-117 now requires exactly 29,491 octets of headroom. See [[spec-008-0.5.7-claude-adversarial-review-2026-08-24#L-1 One headroom assertion proves arithmetic rather than frame admission]]. |
| L-4 a carrier can pass allocation and still lose its whole preparation window | **CLOSED** | cbcl-pairing NFR-023 sets the v2 lifetime between 600 and 900 seconds with a 900-second default. cbcl-bus REQ-082 requires at least 600 remaining carrier seconds and requires the browser to request 900. `pendingExpiresAt` therefore always equals allocation time plus 600 seconds. Preparation requires 300 through 600 remaining seconds, which leaves a full 300-second window. See [[spec-008-0.5.7-claude-adversarial-review-2026-08-24#L-5 The v2 lifetime range admits carriers the coordinated hub always refuses]]. |
| L-5 one consolidated contract carries no inbound requirement edge | **CLOSED** | [[SPEC-008-production-pairing-claimant#REQ-902]], [[SPEC-008-production-pairing-claimant#REQ-906]], and [[SPEC-008-production-pairing-claimant#REQ-1006]] each name CON-986 in their trace lines. |
| L-6 local contract references appear in two citation forms | **CLOSED for the four named identifiers** | All four now resolve as fully qualified wikilinks. A masked scan of the parent finds no remaining bare `CON-220`, `CON-201`, `CON-219`, or `CON-409`. See [[spec-008-0.5.7-claude-adversarial-review-2026-08-24#L-4 Same-document and historical identifiers keep two citation forms]]. |

## Disposition of the earlier blockers

The reviewer re-derived each earlier row against the current candidates and the
pinned baselines. Every row below stays closed.

| Earlier finding | Disposition | Re-derived evidence |
|---|---|---|
| 0.5.5 N-1 two named failure modes contradict the pinned baseline | **CLOSED** | FM-1 through FM-4 now resolve. `src-tauri/src/cbcl_pairing.rs:171` calls `assemble_claimant`, `src-tauri/src/cbcl_context.rs:342` and `crates/selfsame-web-device/src/lib.rs:205` construct the two production contexts, and `src-tauri/src/cbcl_registry.rs:17` carries the `anuna-1` digest with `allow_loopback: false`. |
| 0.5.5 N-2 provenance names the wrong correction input | **CLOSED** | The provenance section names the 0.5.6 report and its collected corrections. cbcl-bus names the same report. All five documents record one generation family, version, and session. |
| 0.5.5 N-3 the signed negative depends on an optional expiring tombstone | **CLOSED** | cbcl-bus CON-036 returns `unknown` for an expired terminal ceremony and an unheld ceremony alike. CON-989 preserves pending until explicit unlink or root purge. |
| 0.5.5 N-4 one account-scope disposition is narrower than the value's travel | **CLOSED** | CON-986 names the protected channel, the signed final status, and the private installed record. The raw account identifier stays hub-private, and no offer member carries it. |
| 0.5.5 N-5 one capacity counter mixes transient and permanent records | **CLOSED** | cbcl-bus REQ-082 and CON-036 count active pending records and immutable accepted statuses inside one 65,536-record cap. Only explicit service data erasure returns a slot. |
| 0.5.5 N-6 one narrow relay lifetime admits an allocation that cannot prepare | **CLOSED** | The 900-second lifetime and the 600-second remaining requirement now give a deterministic 600-second pending window and a 300-second preparation window. |
| 0.5.5 N-7 two consolidated contracts carry no trace lines | **CLOSED** | CON-988 and CON-989 name their requirements and verifying tests, and both inbound edges exist. |
| 0.5.4 N-1 two hub authority-command arguments carry no size bound | **CLOSED** | `rawGrant` bounds at 49,152 and `resolverClosure` at 1,048,576. `crates/selfsame-app-identity-net/src/state.rs:342` reads `MAX_CLOSURE_OCTETS` as `1_048_576`. |
| 0.5.4 N-3 one signed staging-receipt member has no stated meaning | **CLOSED** | The receipt declares thirteen members and signs the preceding twelve. The reviewer recomputed the grammar maximum as 3,015 octets. |
| 0.5.4 N-5 two signature clauses name their input less exactly than the third | **CLOSED** | All three inputs now state an exact 32-octet SHA-256 result. The residual moves to the first member of the third input. |
| 0.5.4 N-6 one current hub clause fails strict controlled-language lint | **CLOSED** | The reviewer extracted all fifteen SPEC-053 coordinated sections by their exact heading boundaries. Every extraction returns zero errors and zero warnings. |
| 0.5.4 N-8 a backwards hub clock step yields a non-conforming offer core | **CLOSED** | cbcl-bus REQ-082 returns closed `unavailable` above 600 remaining seconds, preserves the pending record, and permits an authenticated resample. |
| 0.5.4 N-9 the post-payload checkpoint expiry rule names only one role | **CLOSED** | cbcl-pairing CON-030 permits a null expiry only for a claimant checkpoint after durable payload send. An allocator checkpoint always carries its numeric expiry. |
| 0.5.3 N-1 hub authority response has no grammar | **CLOSED** | The reviewer recomputed the maximum bound-DID response as 1 + 45 + 34 + 34 + 6 + 515 + 66, which equals 701 octets against a 768-octet bound. |
| 0.5.3 N-5 eleven object kinds carry no integer assignment | **CLOSED** | cbcl-pairing CON-028 assigns `0 offer` through `10 receipt`. The control arm fixes `1: 1..8` and the large arm fixes `1: 0 / 9 / 10`. |
| 0.5.3 N-6 claimant checkpoint wrapping key has no info encoding | **CLOSED** | CON-986 states `checkpointInfo` with explicit big-endian length prefixes, the raw salt, and the extract order. |
| 0.5.3 N-8 declared Selfsame test set omits the other Selfsame parent | **CLOSED** | The reviewer counted 21 SPEC-008 test headings and 21 SPEC-007 test headings. CON-985 names both sets. |
| 0.5.3 N-10 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | REQ-1006 requires the comment to cite [[SPEC-007-cbcl-pairing-cutover#CON-806]]. `src-tauri/src/cbcl_registry.rs:1` still reads the old citation at `0220cec2`, which is declared implementation work. |
| 0.5.2 C-1 credential/v2 key schedule exists in no current parent | **CLOSED** | cbcl-pairing CON-031 states a thirteen-member public context, the transcript, one extract, seven expands, both Finished values, the counter rule, the nonce rule, and the AAD map. The reviewer counted thirteen members. |
| 0.5.2 C-2 nine of eleven logical body grammars exist in no current parent | **CLOSED** | CON-987 declares nine grammar blocks. The reviewer counted nine. The offer resolves to cbcl-bus `signed-offer-v2` and the receipt to cbcl-pairing `credential-v2-receipt-body`. |
| 0.5.2 H-1 pre-socket CON-220 gate loses its binding step | **CLOSED** | CON-988 restates steps 1 through 5 and numbers seven ordered substitute steps for step 6. |
| 0.5.2 H-2 signed offer-core member has no defined input | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-806]] fixes six descriptor members and unique origins, and cbcl-bus CON-035 selects by exact relay-origin equality. |
| 0.5.2 M-3 post-final wallet recovery has no path after the window | **CLOSED** | The reviewer recomputed the request maximum as 2,187 octets against 2,304. It recomputed the largest response arm, `not-finalized`, as 8,294 octets against 9,216. |
| 0.5.1 H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | One carrier ceremony identifier travels through carrier, CPace context, every body, every command, every response, both checkpoints, recovery, receipt, and installed state. |
| 0.5.1 H-3 four offer-core members carry no grammar | **CLOSED** | The reviewer counted exactly 20 `OfferCoreV2` members and 15 `FinalStatusCoreV2` members. |
| 0.5.1 H-4 rendezvous dependency has no consumer | **CLOSED** | CON-986 forbids every Selfsame and did-crdt rendezvous edge. did-crdt SPEC-037 0.1.2 keeps the deprecation and burns its four former identifiers. |
| 0.5.1 H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The did-crdt diff from `1f409a42` to `6d1cdbeb` changes no code. `src/service/rendezvous.rs` stays byte-identical. |
| 0.5.1 M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | CON-986 scopes the prohibition to credential/v2. The standing caller stays `src-tauri/src/app_grant.rs:539`, and `src-tauri/src/cbcl_context.rs:98` declares the function. |
| 0.5.1 L-4 one version constant declared twice under one name | **CLOSED** | `crates/selfsame-app-identity/src/lib.rs:109` and `crates/selfsame-app-identity/src/profile.rs:60` each declare `PROFILE_VERSION` at `0220cec2`. REQ-1005 records one duplication and requires one definition. |

## Hard-stop disposition, F-A through F-E

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-903]] keys the sealed set on the exact pair. It excludes a relay-only key, a global allowlist, a conformance-digest registry, and an application wildcard. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 builds the display from the authority input after byte equality. REQ-1006 excludes peer labels, caller booleans, and DOM room values. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | At `0220cec2` the scope read, derivation, issuer creation, WebFinger call, and closure resolution all precede `connect_wss` in `src-tauri/src/cbcl_pairing.rs`. CON-902, CON-986, and REQ-812 move every one after final approval. |
| F-D pending fresh scope traced end to end | **CLOSED** | cbcl-bus REQ-082 allocates the account identifier, scope, and snapshot nonce inside one level-1 transaction. The stable principal commits the raw identifier through a domain-separated SHA-256 preimage. |
| F-E what does and does not prove relay operatorship | **CLOSED** | [[SPEC-008-production-pairing-claimant#ADR-902]] states that the application profile does not prove relay operatorship. TEST-915 keeps the two-operator depth test open. |

## Inherited SPEC-007 REQ-812 boundary

[[SPEC-007-cbcl-pairing-cutover#REQ-812]] remains a hard stop and states the
credential/v2 boundary directly.

| REQ-812 clause | Coordinated agreement |
|---|---|
| no application identity effect before preliminary approval | REQ-902, CON-902, CON-986, and TEST-1158 agree. |
| preliminary approval authorizes one zeroizing derivation and a pure preview | CON-986 authorizes one custody call and excludes signature, publication, grant, alias, bundle, and durable write. |
| no signing, publication, resolution, issuance, persistence, staging, or payload before final approval | CON-986 and cbcl-bus REQ-082 agree. The hub writes no account, alias, grant, membership, room, or successor row at preliminary approval. |
| final approval authorizes only the declared causal construction | CON-986 lists one order, and TEST-1158 requires it. |
| one encrypted non-authorizing completion checkpoint before the first causal effect | CON-986 requires the sealed pending completion first and states its wrapping-key derivation. |
| browser staging is crash-safe and non-authorizing until immutable acceptance | cbcl-bus CON-012 defines a thirteen-member staging receipt with a 3,015-octet grammar maximum. |
| compensation touches only ceremony-owned effects | CON-986 routes compensation through [[SPEC-004-application-scoped-identity#CON-204]] and excludes operator withdrawal state. |

The boundary holds. The new finding does not breach it. The hub registers only
ceremony-pending device state at finalize, and decline or expiry deletes every
ceremony-owned pending row.

## Attempt-7 correction re-test

| Required correction | Result |
|---|---|
| 1 reproduce `devicePossessionProofInput` byte for byte | **PARTLY.** The label, array, member rule, hash boundary, signature boundary, held key, and verification ordering all resolve. Mutation and replay cases all refuse. The first array member is not independently reproducible. See N-1. |
| 2 FM-3 matches Selfsame baseline `0220cec2` | **YES.** The two reachable commands and the four unreachable commands both resolve. The standing SPEC-004 enrolment path stays distinct from credential/v2 and supplies it no bootstrap. |
| 3 REQ-906 gates before relay socket creation, not before all network I/O | **YES.** The clause now names relay socket creation. CON-988 keeps the live profile fetch before consent by design. |
| 4 receipt logical-body members use the camel-case names | **YES.** All four members match. The carrier keeps its pre-existing `carrier-ceremony-id`, and TEST-064 separates the two. |
| 5 recompute the final hub command frame | **YES.** The reviewer matched 6,871, 1,470,509, and 29,491 exactly. Base64url, ASCII, key order, and fragmentation all check out. See L-1. |
| 6 credential/v1 and credential/v2 lifetimes | **YES.** Version 1 keeps 60 through 600 with a 600-second default at `c22f2d05`. Version 2 uses 600 through 900 with a 900-second default. A full 300-second preparation window remains. |
| 7 CON-986 inbound edges and fully qualified local anchors | **YES for the edges and the four named identifiers.** See L-4 for the presentational residual. |
| 8 versions, pointers, provenance, changelog, and correction input synchronize | **YES.** All five documents record one generation family, version, and session. did-crdt stays unchanged and deprecated. |

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact application-relay TOFU resists a new application; operatorship residual honest | **YES.** CON-903 keys on the exact pair, REQ-1006 requires a fresh prompt for a new application on an accepted relay, and ADR-902 states the residual plainly. |
| 2 CPace binds the origin-authenticated profile before durable policy, display, custody, or effect | **YES.** CON-988 returns an origin-recognised candidate and denies it verifier, display, policy, and key authority. Both Finished values precede the row. |
| 3 no peer, carrier, DOM, route, verifier, or caller value reaches either consent screen early | **YES.** cbcl-pairing CON-029 builds the display only from the authority input after byte equality. The verifier returns a closed verdict. |
| 4 every effect follows its consent gate | **YES.** REQ-902, CON-902, CON-986, REQ-812, CON-807, cbcl-bus REQ-082, and TEST-1158 state one ordering with no gap. |
| 5 fresh scope bootstrap removes the enrolment cycle without leaking the account identifier | **YES.** The hub allocates fresh CSPRNG values inside its authenticated transaction, and no offer member carries the raw identifier. |
| 6 one carrier ceremony identifier throughout | **YES.** The reviewer traced the value through carrier, CPace context, eleven bodies, seven commands, eight responses, both checkpoints, recovery, receipt, and installed state. |
| 7 two implementations reproduce the schedule, kinds, bodies, predecessors, and checkpoint KDF | **PARTLY.** CON-031 states the thirteen-member context and the complete schedule. CON-028 assigns all eleven kinds. CON-987 states nine bodies, and CON-986 states the claimant checkpoint KDF. The socket generation digest is not reproducible. See N-1 and L-6. |
| 8 every declared maximum recomputes | **YES.** The reviewer recomputed 50,221, 1,583, 1,570, 701, 3,015, 6,871, 1,470,509, and 29,491 by hand and matched each exactly. The signed offer recomputes as 56,625 against a declared 56,640. The 49,152, 1,048,576, 65,536, and 1,500,000 values match the code and the transport. |
| 9 Cowboy refuses complete and fragmented oversize before the parser | **YES.** Cowboy 2.12.0 `cowboy_websocket.erl:450` refuses a declared frame length above `max_frame_size` at header parse. Line `:494` refuses the cumulative fragment buffer inside `dispatch_frame`. Line `:666` sends close code 1009, and no handler callback runs first. Decoded argument bounds refuse before lookup, mutation, resolver recognition, or NIF entry. |
| 10 the closure limit matches the resolver client; the grant limit matches payload and recognizer | **YES.** `MAX_CLOSURE_OCTETS` reads `1_048_576` at `crates/selfsame-app-identity-net/src/state.rs:342`. The 49,152 grant bound equals CON-987's payload member and sits below the `65_536` recognizer at `crates/selfsame-app-identity/src/grant.rs:55`. |
| 11 authority and staging Ed25519 inputs are exact 32-octet results; every signer fixed | **YES for those two.** Both mirror a complete adjacent CDDL, and every signer and key is fixed. |
| 12 the possession proof is reproducible, non-circular, and bound to its context | **PARTLY.** The proof is non-circular, because the hub verifies it before it signs the offer and the proof never enters the offer bytes. It binds generation, ceremony, offer, held key, and command authorization context. Its first member is not reproducible. See N-1. |
| 13 removing the staging generation leaves stale substitution prevented | **YES.** The receipt binds application, ceremony, account, scope, device, offer, payload, grant, issuer, profile, and recovery commitment under a deterministic signature. |
| 14 signed `bound` status is a safe fail-safe and cannot authorize a mismatched preview | **YES.** cbcl-bus CON-012 records the fail-safe scope. CON-987 requires a bound DID byte-identical to `previewIssuerDid` and permits only refusal otherwise. |
| 15 one exclusive deadline with exact outcomes at every boundary | **YES.** `expiresAt` equals `pendingExpiresAt` with no skew allowance. Below 300 remaining seconds expires, above 600 returns closed `unavailable`, equality refuses, and post-payload expiry preserves the slot for recovery only. |
| 16 null checkpoint expiry cannot be misused; allocator expiry composes with recovery | **YES.** Only a claimant checkpoint takes a null expiry, and only after durable payload send. After allocator expiry the browser's hub-pending and staging state drive signed status recovery. |
| 17 no retry, rebind, crash, replay, rotation, erasure, or expiry duplicates an effect | **YES for every stated path.** Exact retransmission is the only idempotence, and recovery cannot install without the retained pending slot. See L-7 for one unstated reading. |
| 18 hub locks, capacity, migration, terminals, grammar, limiter, retention, and staging total | **YES.** Three ordered lock sequences, both capacity bounds, six terminal classes, eight closed responses, the anonymous limiter, and the retention rule are all stated. |
| 19 credential/v1 bytes and valid production behavior stay frozen | **YES for the bytes; the behavior claim is untested.** `schemas/pairing-v1.cddl` stays byte-identical from `c22f2d05` to the candidate, and version 1 keeps its 60-through-600 range. See L-2. |
| 20 did-crdt absent from every credential/v2 edge; the sole HTTPS exception is status recovery | **YES.** CON-986 excludes every rendezvous edge, TEST-1159 permits only the CON-989 route, and the did-crdt candidate changes no code. |
| 21 every current clause traceable and strict-lint clean without trajectory authority | **YES with two observations.** The complete SPEC-008, SPEC-007, SPEC-001, and SPEC-037 documents return zero errors and zero warnings. All fifteen extracted SPEC-053 coordinated sections return the same. Every self-anchor resolves. See L-3 and L-4. |

## Repository and code-claim assessment

Every testable code claim resolves exactly at its stated baseline.

**Selfsame version sites.** All eighteen witness rows in
[[SPEC-008-production-pairing-claimant#REQ-1005]] resolve at `0220cec2` with the
stated class. The reviewer read each cited line. The additional comparison row at
`crates/selfsame-app-identity/src/enrollment.rs:592` resolves. The three ingest
substitutions at `crates/selfsame-web-device/src/lib.rs:831`, `:847`, and `:861`
sit inside three parser functions. Those are `parse_offer_core`,
`parse_provider_hint`, and `parse_enrollment_statement`. Each one deserializes
untrusted browser input. Line
`:1369` copies from a locally constructed core, so its separate class is correct.

**The failure-mode witnesses.** `src-tauri/src/cbcl_context.rs:244` declares
`assemble_claimant`, and `:342` constructs the production verification context.
`crates/selfsame-web-device/src/lib.rs:205` is the second production site.
`src-tauri/src/cbcl_pairing.rs:171` is the production caller. `src/pairing.js:159`
opens the scan function, and `src-tauri/capabilities/mobile-scanner.json` carries
the three barcode permissions.

**The hold and the fixture.** `crates/selfsame-pairing/src/release.rs:11` reads
`PRODUCTION_ALLOCATION_ENABLED: bool = false`, and `:28` declares
`require_production_allocation()`. `crates/selfsame-pairing/src/lib.rs:16` gates
`local_demo` behind `local-pairing-demo`. The `[19; 32]` digest appears only under
`tests/`.

**cbcl-bus consumer sites.** Every line
[[SPEC-008-production-pairing-claimant#NFR-928]] names resolves with the stated
consumer at `ce894a7f`. The reviewer checked `cbcl-chat-session-ws.lfe:347`, `:385`,
`:512`, `:869`, `:965`, `:1050`, `:1076`, `:1683`, `:1788`, and `:1898`.

**The Cowboy pin and bound hold.** The build tree carries `cowboy` version
`2.12.0`. No handler sets `max_frame_size` today, so the chat socket inherits an
unbounded default. The reviewer read `cowboy_websocket.erl` and confirmed the
header check, the fragment check, and the 1009 close.

**The upstream bounds exist.** cbcl-pairing `src/mailbox.rs:15` sets
`MAX_FRAME_BODY_BYTES` to `69_632`. `src/channel.rs:21` sets `MAX_SEALED_PLAINTEXT`
to `69_556`. Both relay binaries set `MAX_WIRE_MESSAGE` to `70_000`.
`src/mailbox.rs:7` and `:11` set the version-1 default and maximum lifetime to
`600`.

**The hub identifier is configuration.**
`apps/cbcl_chat/src/shell/cbcl-chat-pairing-ws.lfe:31` reads it from application
environment with a binary default. See
[[spec-008-0.5.7-claude-adversarial-review-2026-08-24#N-1 The socket generation digest carries no reproducible encoding or bound]].

## Consent assessment

Two decisions gate every identity effect. The exact-pair decision precedes the
socket, and the two-stage ceremony decision precedes derivation and delivery.

Rejection at the pair prompt creates no policy entry, socket, wallet scope, key,
alias, DID, grant, or publication. The durable row waits for the CPace-bound
profile digest.

Preliminary approval authorizes one zeroizing derivation, a pure DID and
fingerprint, and one disclosure the screen states. Final approval authorizes the
declared construction and nothing earlier.

Every displayed value comes from the authenticated authority input. No peer string,
DOM value, caller boolean, or verifier return reaches either screen.

## Wire and bounds assessment

Eleven object kinds map to integers 0 through 10. Only the offer, payload, and
receipt use the large arm.

Every declared maximum recomputes as an exact value or a correct bound. Control
bodies stay within 2,048 octets and large bodies within 62,000. The reviewer
recomputed the comparison body at 1,583 octets and the binding body at 1,570. It
recomputed the payload body at 50,221, the authority response at 701, and the
staging receipt at 3,015.

The final hub command sums to exactly 1,470,509 encoded argument octets. It leaves
29,491 octets under a 1,500,000-octet frame. Cowboy refuses a larger complete or
fragmented message with close code 1009.

## Cryptography assessment

The public context contains thirteen enumerated members. The transcript, extract,
seven expands, both Finished values, the counter rule, the nonce rule, and the AAD
map are exact.

Every concatenated preimage places its variable-length value last or gives it an
explicit length prefix. The recovery commitment, the checkpoint info, and the
migration confirmation digest each satisfy that rule.

The installation device seed signs three distinct messages. The possession proof is
a 32-octet hash, the recovery proof is a 146-octet array, and a post-acceptance
frame signature carries the length-prefixed wire envelope. Their lengths and
structures differ, so no signature transfers between them.

The possession proof and the staging receipt both sign a 32-octet hash under one
key. Their domain labels differ inside the hash, so a transfer requires a SHA-256
collision.

One preimage carries no member typing and one member carries no bound. See
[[spec-008-0.5.7-claude-adversarial-review-2026-08-24#N-1 The socket generation digest carries no reproducible encoding or bound]].

## Recovery and atomicity assessment

The claimant checkpoint precedes the first identity effect. The browser writes one
inactive staging record before hub finalization.

The hub commits one final transaction or none. The browser activates only after it
verifies the immutable status, and the wallet installs only after it verifies the
same bytes.

Signed status recovery reproduces the ordinary receipt transition. It rebuilds no
grant, repeats no signature, republishes no DID, and resends no payload.

## Persistence assessment

The wallet slot is one tagged pending-or-installed union. The hub holds one pending
record per ceremony and one immutable status per finalized account.

Every crash boundary recovers exactly one of absent, the complete pending record, or
the complete finalized state.

Capacity is bounded at 69,632 octets per pending record and 65,536 records in total.
Accepted statuses consume that budget until explicit service data erasure.

## Rendezvous assessment

No credential/v2 object reaches a Selfsame or did-crdt rendezvous slot. The only
non-relay route is the signed post-window status recovery to the authenticated
application origin.

did-crdt SPEC-037 0.1.2 records the deprecation and burns its former identifiers.
The candidate commit changes no did-crdt code.

## Compatibility assessment

Credential/v1 keeps its carrier, schedule, display, wire bytes, and decisions. The
version-1 schema stays byte-identical between the pinned baseline and the candidate.
The version-1 recogniser cannot read a version-2 carrier.

Version 1 keeps its 60-through-600 lifetime range and its 600-second default. Only
credential/v2 takes the 600-through-900 range. cbcl-pairing TEST-060 separates the
two ranges and re-runs the version-1 wire vectors.

The standing registry and selection control stay compiled and unchanged, with one
corrected implementation comment. The new WebSocket ceiling carries an untested
compatibility claim. See
[[spec-008-0.5.7-claude-adversarial-review-2026-08-24#L-2 The global frame ceiling rests on an untested compatibility claim]].

## Authority assessment

Each parent states its own current law and names the same coordinated set. All five
documents record the same generation family, version, and session.

Every version pointer between parents matches the reviewed object. No duplicate
artefact identifier exists in any parent, and no burned identifier returns to use.
`REQ-909`, `TEST-916`, and `TEST-917` appear only in the changelog.

The reviewer counted 42 GATE-04 boxes, 20 `OfferCoreV2` members, and 15
`FinalStatusCoreV2` members. It counted nine Selfsame successor body grammars. It
counted 21 SPEC-008 tests, 21 SPEC-007 tests, five hub tests, and eight
cbcl-pairing credential/v2 tests.

## Stopping-rule assessment

The reviewer stopped after the finding rate reached one blocking defect and seven
observations across a complete pass.

The pass covered four parents, one deprecated disposition, five pinned baselines,
one Cowboy release, and the shipped wallet surface. It recomputed every declared
maximum by hand rather than by trusting the text.

Further search does not change the verdict, because one MEDIUM finding already
blocks PASS.

## Surfaces found sound

- The two-stage consent boundary and its instrumented prohibition list.
- The seven-step CON-220 substitute and its explicit CPace digest binding.
- The complete credential/v2 channel schedule and its version-1 disjointness.
- All eleven logical bodies, their predecessors, and their recomputed maxima.
- The new possession proof's own array, ordering, held key, and non-circularity.
- The corrected final-command arithmetic and its exact headroom.
- The 900-second mailbox and its deterministic 300-second preparation window.
- The Cowboy frame ceiling, including its cumulative fragment guard.
- The Mnesia lane model, its level-1 requirement, and its static nesting proof.
- The signed final status, its recovery token, and its commitment binding.
- The indistinguishable `unknown` result and its pending-slot retention rule.
- The exact-pair policy contract and its honest operatorship residual.
- The corrected FM-3 witness and its separation from credential/v2.
- Strict controlled-language lint across every extracted section and four parents.

## Residual assumptions

- The possession proof protects the hub rather than the wallet. The wallet trusts
  the hub's offer signature as evidence that the hub verified device possession.
- The application profile does not prove relay operatorship, and exact-pair consent
  contains that residual without solving it.
- The wallet fetches the candidate profile over HTTPS before the exact-pair prompt,
  so a scanned carrier reaches its named origin before consent.
- The repository's own domain-separation suite proves separation by distinct
  leading bytes. The two new device-key contexts achieve it by collision resistance
  instead, because each signs a bare 32-octet hash.
- Explicit service data erasure of an accepted status leaves the wallet fail-closed
  with no capability and the hub with an orphaned account.
- The browser's comparison assertion stays a browser-side claim. The wallet's own
  final screen and its local DID remain the wallet's protection.
- The raw recovery token stays outside page script only while the browser allocator
  endpoint runs outside page script.
- The relay supplies the carrier expiry on its own clock, and the hub compares that
  value against its own whole UTC seconds.
- The chat WebSocket carries no frame ceiling today, so the new bound tightens an
  unbounded surface rather than relaxing a stated one.
- Two depth tests stay open with named owners, covering a real relay ceremony and
  two independently operated relays.

## What this verdict authorizes and does not authorize

This verdict authorizes nothing. It records one blocking correction and seven
non-blocking observations for the next coordinated reissue.

It does not authorize the Elephant plan, a test-first plan, implementation,
production allocation, release, or deployment.

It closes no GATE-04 box and changes no production gate. It changes no requirement,
contract, test, or bound in any parent.

## Collected corrections and owners

| Finding | Severity | Owner | Correction |
|---|---|---|---|
| N-1 | MEDIUM | cbcl-bus | Give `socketGenerationDigest` a closed CDDL. Bound `hubId` and declare its type. Type `recoveryNonce` and `frameKeyDigest` as 16-octet and 32-octet byte strings. Apply the same treatment to the `deviceRecoveryProof` array. Require both reproductions in TEST-117. |
| L-1 | LOW | cbcl-bus | State the closed command syntax and envelope octet count, or require TEST-117 to measure the generated frame against 1,500,000. |
| L-2 | LOW | cbcl-bus | Enumerate the existing chat and credential/v1 object maxima, or require a test that measures them under the new ceiling. |
| L-3 | LOW | cbcl-pairing | Record the member-naming partition between relay and HTTPS object families. |
| L-4 | LOW | selfsame | Cite same-document and changelog identifiers as fully qualified wikilinks. |
| L-5 | LOW | cbcl-pairing | Narrow the v2 lifetime range to values a 600-second consumer requirement can admit, or record the unusable end. |
| L-6 | LOW | cbcl-pairing | State the CON-030 checkpoint key info as an exact encoding with an output length. |
| L-7 | LOW | cbcl-bus | State that a finalize retry after a generation rebind carries a re-signed proof and still returns the byte-identical response. |
