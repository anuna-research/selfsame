---
id: spec-008-0.5.17-final-reviewer-prompt-2026-08-25
title: SPEC-008 0.5.17 — final Tier-1 adversarial review prompt
status: active
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-25
---

# SPEC-008 0.5.17 — final Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
inherit any earlier verdict.

Review the completed standalone credential/v2 implementation, the fail-closed
invoke-discovery correction, and the four-parent authority alignment.

Do not repair candidate code or specifications. Write only the review report,
commit it in the Circus worktree, and complete through Circus's appended
instruction.

## Authentication

This review runs through Circus and the interactive Claude CLI. It SHALL use
the person's Claude Max subscription. It SHALL NOT use the Anthropic API.

Both `ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent. Check
absence without printing either value.

Run `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status`.
Record only `authMethod`, `apiProvider`, and `subscriptionType`.

The required values are `claude.ai`, `firstParty`, and `max`. Record no secret
or account identifier.

## Exact candidate set

- Selfsame executable candidate:
  `60abb8b25e858be8005151e47ea2a8044d130e80`
- Selfsame governing authority:
  `9337fa9660ee47c01182dafc5f17b0ade9d1c963`
- Selfsame preceding executable:
  `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`
- cbcl-bus executable candidate:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-bus governing authority:
  `d35cdacfcd379d54771c655aea87f6f42a1db596`
- cbcl-pairing executable candidate:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-pairing governing authority:
  `6f841595d0127279d0d6c1cb850c68900d379ade`
- cbcl-rs dependency:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt runtime dependency:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`

Use committed objects and clean scratch copies. The person's dirty working
trees are not evidence.

Create any scratch directory with `mktemp -d`. Leave review scratch in place;
do not delete or overwrite any directory during this review.

Do not modify the person's output APKs, patch script, or dirty files.

## Current authority

Read these exact current parents before deciding:

- Selfsame SPEC-008 0.5.17 at `9337fa9`;
- Selfsame SPEC-007 0.3.8 at `9337fa9`;
- cbcl-pairing SPEC-001 0.5.8 at `6f84159`; and
- cbcl-bus SPEC-053 0.17.13 at `d35cdac`.

Verify that all four name the same review set and generation provenance.
Verify that historical changelog and evidence versions remain historical.

Verify that cbcl-pairing records credential/v2 as locally complete at
executable baseline `62ef4a9`. Diff its authority from the executable candidate
and require documentation-only changes.

Run strict descriptive controlled-language lint on both Selfsame parents, the
0.5.17 trajectory, and the cbcl-pairing parent. Lint changed cbcl-bus parent
lines independently because unrelated full-parent debt predates this work.

## Invoke-discovery mandate

Independently verify all of these claims:

1. The harness recursively discovers JavaScript modules below `src`.
2. It derives invoke callers from discovered source rather than a module list.
3. It scans every member of the derived caller set.
4. The reviewed caller-count ratchet fails when discovery loses one caller.
5. The unmutated `CI=true npm run screens` command exits zero.
6. Excluding `src/pairing.js` from discovery exits one.
7. That refusal is an `invoke-surface` caller-count error.
8. Restoring the candidate returns the screen gate to green.

Require an applied diff before counting the mutation. Verify byte restoration
afterward. Do not count a no-op mutation.

## Protocol regression mandate

Verify that `48a0c749..60abb8b` changes only `tests/screens.mjs`. Verify that
all production tree objects are unchanged.

Read the 0.5.15 rejected review at preserved commit `32217de`. Independently
regression-check its PASS evidence for F-A through F-E, authenticated intent
display, consent ordering, transaction cleanup, source authentication,
reproducible WASM, and both complete local capstones.

Run the vendor suite and wallet pairing lifecycle capstone. A valid preceding
measurement can guide commands but does not replace this review's result.

Production allocation, push, release, and deployment remain prohibited.

## Incomplete attempt

Circus attempt `spec008-v0516-review/1` ended with transport exit 130 before a
report or sentinel. It supplies no verdict.

Do not treat that infrastructure interruption as a candidate finding. Its
transcript can identify commands, but this review measures its own evidence.

## Verdict and report

Return PASS only with zero CRITICAL, HIGH, and MEDIUM findings. An unmeasured
required mutation is at least MEDIUM.

Write the report to
`specs/trajectory/SPEC-008/spec-008-0.5.17-final-claude-adversarial-review-2026-08-25.md`.

Record exact revisions, commands, exit results, mutation restoration,
authentication path, Circus attempt, findings, and final verdict. Record no
secret or account identifier.
