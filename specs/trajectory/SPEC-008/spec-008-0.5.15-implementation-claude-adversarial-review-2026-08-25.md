---
id: spec-008-0.5.15-implementation-claude-adversarial-review-2026-08-25
title: SPEC-008 0.5.15 implementation — Tier-1 cross-model adversarial review (REJECT)
status: complete
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
reviewer-model: claude-opus-5
generating-family: OpenAI GPT-5
verdict: REJECT
date: 2026-08-25
---

# SPEC-008 0.5.15 implementation — Tier-1 cross-model adversarial review

## Reviewer identity, subscription authentication path, Circus attempt, and independence

**Reviewer family.** Anthropic Claude (`claude-opus-5`), running in the
interactive Claude CLI. The generating family is OpenAI GPT-5
(`gpt-5.6-sol`, per SPEC-008 0.5.15 front matter). The two families are
distinct, satisfying the cross-model requirement.

**Subscription path.** The review ran on the person's Claude Max subscription,
not the Anthropic API. Verified before any other work:

```
$ env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status
authMethod       : claude.ai
apiProvider      : firstParty
subscriptionType : max
```

Those are the three required values and the only three recorded. No email,
organization identifier, account identifier, token, or key is reproduced here.
Both `ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` were confirmed **absent**
in the review process by testing emptiness without printing either value.

**Circus attempt.** Branch `circus/spec008-v0515-review/1`, worktree
`/Users/anuna-01/Code/selfsame-spec008-v0515-review-1`, attempt 1.

**Commits reviewed.**

| Repository | Role | Revision |
|---|---|---|
| selfsame | implementation candidate | `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7` |
| selfsame | governing authority | `8f5249026e6e3352a78f365638be3225c0cc3fc6` |
| selfsame | preceding implementation | `ff2f1394813a38ce9d0409726892b6e0c63d00ea` |
| cbcl-bus | implementation candidate | `14f26065cb147633416def15d0a7d8603bce2e09` |
| cbcl-bus | governing authority | `9ae1313ab8f9a95fe61f92c6f272010d795df871` |
| cbcl-bus | preceding browser-gate implementation | `22fcc012ff4d4746f251b497c11495b1b26c0f29` |
| cbcl-pairing | candidate | `62ef4a968b46b4836374fcee1d78c410f730a7a7` |
| cbcl-rs | dependency | `febc6691e6dd2d5f7116b1a4d84c984b64717564` |
| did-crdt | runtime dependency | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` |

`git diff --stat 48a0c749 HEAD` over the Circus worktree touches only
`specs/`, so the executable tree in the worktree is byte-identical to the
implementation candidate.

**Independence statement.** Every measurement below was taken from clean
detached checkouts I created myself under the session scratchpad, at the exact
five candidate revisions, with `git status --porcelain --untracked-files=no`
empty in each. The person's working trees were **not** used as evidence:
`cbcl-rs` carried tracked modifications, and `did-crdt`'s HEAD was
`94db956d5d5eafd7...` (`94db956d7d5d5edf0f42afad073ab42f265a1752`) — the service revision the parent explicitly places
outside the runtime closure — rather than the pinned `1f409a42`. No output APK,
patch script, or dirty file of the person's was modified or deleted. I did the
independent protocol pass and every mutation before reading the rejected 0.5.13
review, and I did not inherit its dispositions: each of its three MEDIUM and
three LOW findings was re-tested from code and from executed commands.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 1 |
| LOW | 4 |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. One MEDIUM stands:
**M-1**. [[SPEC-008-production-pairing-claimant#TEST-1167]] names four
mutations of the screen gate and requires the gate to fail for each. Three do
fail it. The third — "exclude the pairing module" — leaves the gate exiting 0.

The three MEDIUM findings of the 0.5.13 review are genuinely closed, and I
confirmed each by executing the gate that previously failed. Seventeen of the
eighteen required mutations are killed, and I measured every one. The exact
Linux WASM reproduction, the release-source authentication, the transaction
boundary behaviour, and both consent boundaries all hold under adversarial
testing. The single blocking finding is narrow, and its correction is small.

## Findings

### M-1 — the screen gate's invoke-surface caller list is hand-maintained, so excluding `src/pairing.js` from invoke discovery leaves the gate green; TEST-1167's third named mutation is not killed

**Severity: MEDIUM.** Owner: Selfsame. New in the coordinated increment —
0.5.13 had no credential/v2 caller in the list at all, so the mutation could
not previously be posed.

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#TEST-1167]] — "The invoke-surface check
SHALL scan every JavaScript module that calls Tauri. It SHALL recognise every
module prefix inside the actual `generate_handler!` block. Restore the legacy
credential/v1 stub, omit the PAIR1 entry, **exclude the pairing module**, or
hard-code a prefix set that excludes `app_grant`. **Require the screen gate to
fail for each mutation.**"

**Code evidence.** `tests/screens.mjs:1046-1067` (candidate `48a0c749`):

```js
const registered = new Set([...block.matchAll(/\b[A-Za-z_]\w*::([A-Za-z_]\w*)\b/g)].map((m) => m[1]));

const called = new Map();
for (const file of ['src/app.js', 'src/app-identity.js', 'src/pairing.js']) {
  for (const m of src(file).matchAll(/invoke\(\s*["'](\w+)["']/g)) {
    called.set(m[1], (called.get(m[1]) ?? []).concat(file));
  }
}
...
for (const [command, files] of called) {
  if (!registered.has(command)) { errors.push(...); }        // <- the only error
}
const unreached = [...registered].filter((c) => !called.has(c)).sort();
if (!errors.some((e) => e.startsWith('invoke-surface'))) {
  console.log(`invoke-surface every invoke resolves; ${unreached.length} registered and unreached (...)`);
}
```

The 0.5.15 remediation fixed the *prefix* half correctly: the registered set is
now derived from the `generate_handler!` block itself rather than a hard-coded
two-prefix regex, and I confirmed that kills the `app_grant` mutation. The
*caller* half was fixed by appending one more string to a hand-kept list.

The check's only failure mode is "called but not registered". Removing a file
from the caller list removes callers, never adds an unregistered one, so it
cannot raise an error. The registered-but-unreached set is `console.log`ged and
never pushed to `errors`.

**Failure sequence (executed, not argued).** From the clean exact-pin closure:

```
$ perl -0pi -e "s/\['src\/app\.js', 'src\/app-identity\.js', 'src\/pairing\.js'\]/['src\/app.js', 'src\/app-identity.js']/" tests/screens.mjs
$ CI=true npm run screens ; echo EXIT=$?
...
invoke-surface every invoke resolves; 10 registered and unreached (...)

all screens rendered clean
EXIT=0
```

The other three mutations TEST-1167 names in the same sentence all fail the
gate as required — legacy v1 stub `EXIT=1`, omitted PAIR1 entry `EXIT=1`,
`app_grant`-excluding prefix set `EXIT=1`. This one exits 0.

In service: `src/pairing.js` is split or renamed — the obvious next edit, since
CON-989 recovery and CON-990 abandonment already give it thirteen invokes and
645 lines. Discovery silently narrows to two files. A later `cbcl_v2_*` command
added to `pairing.js` and forgotten in `generate_handler!` then rejects at
runtime with the "command not found" this check's own header comment
(`tests/screens.mjs:1022-1030`) records as the defect that "produced most of
version 0.7.0" — and under
[[SPEC-008-production-pairing-claimant#CON-986]] the whole credential/v2
ceremony, both consent boundaries included, is behind those invokes.

This is the failure mode the check was built to prevent, reintroduced one level
up: the list of *commands* is derived, the list of *files* is not. The hub
already solved the same problem the right way — cbcl-bus
`.github/workflows/ci.yml:262-276` discovers its browser suites with `find` and
guards the discovery with an exact ratchet (`test "${#suites[@]}" -ge 40`),
with a comment recording that a hand-maintained list "named 17 files; the tree
held 36".

**Measurable correction.** Derive the caller set instead of listing it, and
ratchet it: enumerate `src/*.js`, select every module whose source contains
`invoke(`, and fail the gate if that computed set differs from the set actually
scanned (or, minimally, assert an exact module count the same way the hub
asserts an exact suite count). Then re-run the mutation and require
`CI=true npm run screens` to exit 1 with an `invoke-surface` error naming the
dropped module.

### L-1 — a mid-chain post-approval failure still leaves an orphaned issuer DID publication with no compensating retraction

**Severity: LOW (accepted residual, unchanged in kind from 0.5.8 L-1 through
0.5.13 L-1).** Owner: Selfsame.

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#CON-986]] — "Compensation is limited to
effects created by this ceremony and follows
[[SPEC-004-application-scoped-identity#CON-204]]."

**Code evidence.** `src-tauri/src/cbcl_v2_commands.rs:562-566` publishes the
issuer identity (`publish_issuer_identity`) and the transaction continues
through `ResolverVerification`, `GrantConstruction`,
`ProvisionedStageReplacement`, `PayloadConstruction`, and
`PayloadCheckpointPreparation`. If any of those fails, the `Drop` guard on
`PrePayloadPendingCleanup` (`cbcl_v2_completion.rs:909-915`) runs
`remove_pre_payload_attempt`, which touches only the local secure-store slot
and the local index (`:1697-1736`). `grep -rn "retract\|deactivate\|tombstone"
src-tauri/src/cbcl_v2_completion.rs` returns nothing.

**Failure sequence.** Grant construction fails after publication. The local slot
is released, a fresh ceremony succeeds, and the resolver retains a published
issuer DID that no credential references.

**Consequence (explicit).** The orphan grants no capability: it is a published
DID document with no grant chain, and
[[SPEC-008-production-pairing-claimant#CON-986]]'s installation path verifies
the grant chain, installation key, account, scope, issuer, profile, and device
proof before any capability is exposed. The cost is resolver litter and a
weak correlation signal, not authority.

**Operational treatment.** Accept as a residual, or add a best-effort
deactivation on the compensation path bounded by the same attempt identity that
`remove_pre_payload_attempt` already compares. Retraction must never be made
load-bearing: a failed retraction cannot be allowed to strand the local slot.

### L-2 — the CON-036 anonymous recovery limiter is keyed on the peer address, so a reverse-proxied deployment shares one bucket

**Severity: LOW (accepted residual, unchanged from 0.5.9 L-2 through 0.5.13
L-2).** Owner: cbcl-pairing.

**Code evidence.** `cbcl-pairing/src/limiter.rs:235-250` takes
`canonical_peer_address: &[u8]` and derives `peer_key` from it
(`:353-377`, an operator-keyed HMAC over that address). Behind a reverse proxy
that does not preserve the client address, every anonymous recovery request
canonicalizes to the proxy's address.

**Failure sequence.** The hub is deployed behind a terminating proxy. One
client exhausts the shared anonymous bucket and every other client's signed
final-status recovery is throttled.

**Consequence (explicit).** Availability only, and only on the CON-989 recovery
path after the relay window has closed. It cannot forge, replace, or expose a
receipt: the recovery response is still verified under the live authenticated
profile request-signing key. The ordinary in-window path does not use this
limiter.

**Operational treatment.** A deployment note is sufficient: either preserve the
client address to the relay, or run the relay without an address-rewriting
proxy in front of it. Release and deployment controls are outside this review's
authority.

### L-3 — TEST-1162's runtime injection still does not execute the production final-decision function; hook presence and order are proved by a source-text scan

**Severity: LOW (accepted residual, unchanged from 0.5.13 L-3).** Owner:
Selfsame. Independently re-derived before reading the 0.5.13 review, and
independently re-measured.

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#TEST-1162]] — "The injected steps SHALL
use the exact typed hooks in `cbcl_v2_final_decide`. Each hook SHALL occur once
and in causal order. They SHALL NOT be labels over synthetic guard drops."

**Code evidence.** The static half
(`src-tauri/src/cbcl_v2_completion.rs:2464-2487`) `include_str!`s
`cbcl_v2_commands.rs`, slices between `async fn cbcl_v2_final_decide_with_faults`
and `pub async fn cbcl_v2_finish`, and asserts each
`PrePayloadBoundary::{Variant}` marker occurs exactly once and in
non-decreasing position. The dynamic half (`:2489-2505`) drives
`transaction.try_step(&mut InjectAt(boundary), boundary, || Ok(()))` — a
supplied boundary and a no-op closure — then `drop(transaction)`.
`cbcl_v2_final_decide_with_faults` is never called by any test in the workspace.

**Failure sequence.** A later edit moves one hook inside a conditional — an
early-return fast path that skips resolver verification when a closure is
cached, say. `matches(&marker).count()` is still 1, the order scan still
succeeds, and all fifteen matrix rows still pass, because none of them executes
the production function.

**Consequence (explicit).** The property holds today, and I verified it by
reading rather than by trusting the scan: compensation is `Drop`-based on
`PrePayloadPendingCleanup`, so *every* early return between `begin` and
`commit_payload` compensates whether or not it passed a hook; and the
production body (`cbcl_v2_commands.rs:320-682`) is straight-line — no branch,
loop, or closure can reorder execution relative to source order. The two
`before_async` sites inject before the operation they name, which is the
causally correct point. What the scan cannot see is a future edit that moves a
hook into a conditional or dead code.

I also confirmed the scan is a real, mutation-killed guard rather than a
formality: removing `PrePayloadBoundary::ResolverVerification` and swapping
`IssuerPublication` with `ResolverVerification` both fail the test (exit 101,
"production boundary hook must occur exactly once" and the order panic
respectively). The arm/disarm placement mutants are killed by real store-failure
injection through `begin` and `commit_payload`, not by labels.

**Operational treatment.** Drive `cbcl_v2_final_decide_with_faults` once per
boundary behind test doubles for `AppSession`/socket/custody, or add a recording
sink (`Vec<PrePayloadBoundary>`) asserted against the exact fifteen-element
sequence from one production run. Until then, the guard is genuine but static.

### L-4 — the coordinated review set names SPEC-007 `0.3.6-draft`, but SPEC-007 is at `0.3.7-draft`, and 0.3.7 is the revision that governs the cbcl-pairing baseline this increment implements

**Severity: LOW (inherited; present since 0.5.12, not previously reported).**
Owner: Selfsame.

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#CON-985]] — "The coordinated review set
contains this parent, [[SPEC-007-cbcl-pairing-cutover]] 0.3.6-draft,
cbcl-pairing SPEC-001 0.5.7-draft, and cbcl-bus SPEC-053 0.17.11-draft. …
No plan or review can omit either set."

**Code evidence.** At the governing authority commit `8f52490`:

```
$ git show 8f52490:specs/SPEC-007-cbcl-pairing-cutover.md | grep -m1 '^version:'
version: 0.3.7-draft
```

CON-985 names `0.3.6-draft`. cbcl-bus SPEC-053 0.17.11 front matter repeats the
same stale value (`coordinated-safety-design: selfsame SPEC-007 0.3.6-draft`).
SPEC-007's own orientation paragraph is internally inconsistent with its own
front matter: "> **Current draft safety revision.** Version **0.3.6** states the
credential/v2 consent and effect boundary directly." The citation has been
stale since `ff2f139` (0.5.12); SPEC-007 reached 0.3.7 at `0c58012`, during the
0.5.11 increment.

**Failure sequence.** 0.3.7's changelog entry is not cosmetic — it "reconciles
ADR-802, the root pin, the compiled dependency baseline, and the fail-closed
build-time source-integrity check to reviewed cbcl-pairing revision
`62ef4a968b46b4836374fcee1d78c410f730a7a7`." That is exactly the subject of
TEST-1164's pin witness and of TEST-1168's release baseline, both of which this
candidate implements. A reviewer or planner who honours CON-985 literally reads
a superseded revision and reviews the increment against the authority it
replaced.

**Consequence (explicit).** No executable effect. Both the pin witness
(`crates/selfsame-pairing/build.rs`) and the release baseline
(cbcl-bus `scripts/build-cbcl-pairing-relay.sh:81`) implement 0.3.7's rule, and
both are mutation-killed here. The risk is procedural: the parent's own
definition of the review set points at the wrong revision of a coordinated
member.

**Measurable correction.** Advance the three citations — SPEC-008 CON-985,
cbcl-bus SPEC-053 front matter, and SPEC-007's own orientation paragraph — to
`0.3.7-draft`, or state explicitly why the coordinated set pins a superseded
revision. A cheap durable guard: a test asserting that every `X.Y.Z-draft`
version cited for a coordinated member equals that member's front-matter
`version:`.

## Dispositions for all nineteen protocol checks

Determined independently from code, before reading any prior review.

| # | Check | Disposition |
|---|---|---|
| 1 | Live CON-220 authentication and exact-pair consent precede socket creation | **TRUE.** `cbcl_v2_claimant.rs:127-152` — `recognise_claimant_invitation` decodes the carrier, binds the PAIR1 code to it, checks relay expiry and expected allocator key, then `selfsame_app_identity_net::profile::fetch` over authenticated HTTPS, then `recognise_profile_candidate`. It returns a `RelayConsentPlan` and opens nothing. The socket is created only in `cbcl_v2_commands.rs:200-207`, inside `cbcl_v2_relay_decide`, after the plan is consumed by `authorise_claimant_relay`. |
| 2 | Consent is scoped to exact `(applicationId, relayOrigin)` | **TRUE.** `cbcl_v2_policy.rs:75-128` keys `state`/`insert`/`remove` on the full tuple, sorted, `MAX_ROWS = 256`-bounded, re-encode-checked, with `recognise_pair` validating both halves (`:130-140`). `recognise_profile_candidate` evaluates `state(profile.application_id, carrier.relay_origin())` only. |
| 3 | The claimant binds the locally authenticated profile digest into CPace | **TRUE.** `prepare_claimant` passes `profile_digest: *profile.digest()` from the *fetched* profile into `CredentialV2ClaimantSessionInput` (`cbcl_v2_claimant.rs:200-214`). cbcl-pairing `credential_v2/context.rs:23-72` folds that digest into the CPace channel identifier, both associated-data values, and the public context. |
| 4 | Peer digest equality precedes policy sealing, intent display, and identity work | **TRUE.** `bind_finished_profile` runs only on `CredentialV2ClaimantEffect::Established { transcript_hash }` (`cbcl_v2_commands.rs:1462-1464`), i.e. after both Finished values — which cannot verify unless the peer used the same digest, since it is inside the channel identifier. It refuses unless `is_awaiting_profile_authorisation()`, then inserts (new pair) or re-checks (trusted pair) the policy row, then installs the verifier. `DisplayIntent` can only arrive afterwards. |
| 5 | The display names the locally authenticated application, not a wire claim | **TRUE.** `intent_view` (`cbcl_v2_commands.rs:1739-1771`) reads only `CredentialV2Display` accessors. That display comes from `CredentialV2WalletOfferVerifier::verify_offer` (`crates/selfsame-pairing/src/credential_v2.rs:346-383`), which recognises the signed offer under `self.profile` — the independently fetched profile — and refuses unless `recognised.profile_digest == *self.profile.digest()`. |
| 6 | Wire and local application identities cross-check before approval | **TRUE.** Same site: `recognised.claims.application_id() != self.carrier.application_context()`, `relay_origin`, `carrier_ceremony_id`, `descriptor_digest`, `carrier_digest`, `transcript_hash`, and `expected_allocator_key` are all compared, and `AuthenticatedOfferVerdict::verify` (`:405-419`) re-compares the peer's own claimed `application_id`, `https_origin`, `relay_origin`, and `carrier_ceremony_id` against the authority. `recognise_profile_candidate` already required `profile.application_id == carrier.application_context()`. |
| 7 | No key, scope, DID, signature, publication, alias, grant, or durable identity record before its consent boundary | **TRUE.** Pre-relay-consent: no custody call at all. Pre-preliminary-approval: the passcode is not even taken unless `approve` (`cbcl_v2_commands.rs:246-254`); only the intent-decision object is built and sent. Preliminary approval authorises exactly one `Custody::use_hierarchy_root` in `preview_identity`, which returns preview material only. The first durable record is written by `PrePayloadPendingTransaction::begin`, after final approval; the first signature is `IssuerCustody`, after that. |
| 8 | Fresh account scope is hub-minted, exclusive, ceremony-bound, used only after consent | **TRUE.** The scope is read from `recognised.claims.account_provenance().account_scope_id()` — a field of the hub-signed offer verified under the independently fetched profile — and is used first at `cbcl_v2_commands.rs:398-402`, inside the post-final-approval custody call. Nothing allocates or persists a scope before final status. |
| 9 | Relay operatorship is not represented as authentication | **TRUE (honest residual).** Nothing in the candidate claims an operatorship proof. `showRelayConsent` (`src/pairing.js:135-141`) is explicit that the guarantee is the person's own exact-pair decision: "The application's live authenticated profile declares this relay, but you have not approved this exact application–relay pair before" / "Your approval adds only this exact pair to your own relay policy." The actual security properties are exact-pair consent plus the cryptographic application binding of checks 3–6. |
| 10 | Every credential/v2 body is closed, canonical, bounded, versioned, context-separated before action | **TRUE.** cbcl-pairing `credential_v2/mod.rs:107-125` — `decode_canonical` refuses trailing bytes, duplicate map keys, and any input that does not re-encode canonically (which covers reordering and non-canonical encodings). `object.rs:9-10` bounds bodies at `MAX_CONTROL_BODY = 2_048` / `MAX_LARGE_BODY = 62_000`. `crates/selfsame-pairing/src/credential_v2/bodies.rs:1219` — `exact_fields` requires `entries.len() == names.len()` and every named key present, so an unknown or missing member refuses. Context separation is the `PROFILE`/`SUITE_ID`/domain-string prefixes in the channel identifier and public context. |
| 11 | Allocator, relay, claimant, issuance, status, receipt, installation form one cbcl-pairing protocol | **TRUE.** All of them route through `cbcl_pairing::credential_v2`: the browser allocator (`crates/selfsame-web-device/src/lib.rs` `CredentialV2BrowserAllocatorSession`), the relay (`crates/cbcl-pairing-relay`), the claimant (`CredentialV2ClaimantSession`), issuance and payload (`CredentialV2BodyAuthority`), signed status (`CON-989` recovery), the receipt, and installation. `grep -rn "rendezvous" src-tauri/src/cbcl_v2_*.rs crates/selfsame-pairing/src/credential_v2*` is empty — no Selfsame or did-crdt rendezvous edge on the v2 path. |
| 12 | No SPEC-004 enrolment transport substitutes for the standalone protocol | **TRUE.** `assemble_claimant` has exactly one caller, `cbcl_pairing.rs:171` — the standing credential/v1 command, not a v2 path. `record_pairing_trust` has exactly one caller, `app_grant.rs:539`, likewise v1. `cbcl_registry` (the compiled conformance registry) is referenced only from `cbcl_context.rs` and `cbcl_pairing.rs`, never from `cbcl_v2_*`. The v2 command set is the five split functions plus the terminal recovery adapter. |
| 13 | Browser and wallet recovery cannot duplicate allocation, identity, grant, acceptance, or acknowledgement effects | **TRUE.** `recover_claimant_completion` (`cbcl_v2_commands.rs:1162-1240`) refuses while the relay window is open, restores the sealed checkpoint under the re-derived wrapping key, re-authenticates the live profile, and posts a signed status-recovery request. It constructs no payload, derives no key beyond the checkpoint wrapping key, signs nothing, and publishes nothing — matching CON-986's "terminal recovery adapter only". On the browser side the staging record is written inactive and activated only after the hub's immutable `finalStatusJws` authenticates. |
| 14 | Transaction arming precedes initial pending persistence; only durable `PayloadPrepared` commit can disarm | **TRUE.** `PrePayloadPendingTransaction::begin` (`cbcl_v2_completion.rs:852-860`) constructs `PrePayloadPendingCleanup::new(initial.clone())` **before** `persist_pending(&initial)?`. Disarm lives only in `disarm_after_payload`, reachable only from `commit_payload` (`:896-906`), which refuses unless `payload_prepared.is_payload_prepared()` and performs `replace_pending` first. Neither the guard nor its disarm is exposed outside the type. |
| 15 | Each exact production boundary wraps the real operation it names once and in causal order | **TRUE, with L-3 as the recorded limit.** All fifteen `PrePayloadBoundary` variants appear exactly once in `cbcl_v2_final_decide_with_faults`, in causal order, each wrapping the operation it names (`try_step` for synchronous work, `replace_at` for durable phase replacement, `before_async` for the two awaited network steps). The proof of production reachability is a source-order scan rather than an execution of the production function; see L-3. |
| 16 | Compensation is attempt-scoped and preserves committed payloads, installed records, roots, sibling applications, and newer attempts | **TRUE.** `remove_pre_payload_attempt` (`:1697-1736`) compares twelve immutable ceremony fields including `root_generation`, refuses on any mismatch, returns `Ok(())` without deleting when `current.is_payload_prepared()`, refuses outright on an `Installed` slot, and deletes only `slot_name(&anchor.application_id)` plus that one index row. |
| 17 | Confirmed unlink compares the selected slot and selected exact-pair policy state against current values | **TRUE.** `unlink_local` (`:1622-1660`) refuses when `recognise_slot(&encoded)? != expected.slot` and again when `current_pair_state != expected.exact_pair_state`, and rolls the policy row back if the slot delete then fails. |
| 18 | Installed reload reauthenticates reciprocal authority before capability exposure | **TRUE.** `cbcl_v2_reload_verify` (`cbcl_v2_commands.rs:791-...`) checks the current root generation, the observed account handle, then re-fetches the live profile, then compares `account_authority` and offer-signing-key retention, then fetches the live WebFinger JRD, with typed unavailability mapped to `unavailable` rather than revoked. Capability is exposed only through `reload_verified_view` after all of it. |
| 19 | Production allocation, push, release, and deployment remain closed | **TRUE.** cbcl-pairing-relay `Cargo.toml:9-21` — `default = []`; the release hook compiles `production-allocation` but the binary allocates nothing unless the supervised command passes `--enable-allocation`, and `conformance-allocation` is never compiled into a release. `scripts/build-cbcl-pairing-relay.sh:17-38` refuses every `RUSTFLAGS` family for release, measured. SPEC-008 0.5.15's own gate line and CON-985 both hold release and deployment pending this review plus separate owner approval. Nothing was pushed, released, or deployed by this review. |

## Dispositions for all eleven rejected-review checks

Verified independently, from code and from executed commands.

| # | Correction | Disposition |
|---|---|---|
| 1 | Deny-warnings workspace Clippy reaches credential/v2 browser restoration; the lint disposition is narrow and justified | **CONFIRMED.** `cargo clippy --workspace --all-targets --locked -- -D warnings` exits 0 from the clean closure, and the log shows `Checking selfsame-web-device`. The candidate adds exactly one line: `#[allow(clippy::too_many_arguments)]` on the single item `CredentialV2BrowserAllocatorSession::restore_offer_context` (`crates/selfsame-web-device/src/lib.rs:568`) — one lint, one item, no crate-level or module-level suppression, and the same shape as the eight dispositions already in that file. Substantively justified: the nine arguments are independently authenticated restoration facts that a struct would only rebag. **Observation, not a finding:** the site carries no in-code rationale, unlike the sibling disposition at `src-tauri/src/cbcl_v2_commands.rs:1041-1043`, which explains itself in three lines. |
| 2 | Hub CI cannot skip the exact WASM rebuild with `if: false` or ignored failure | **CONFIRMED.** `selfsame-vendor.test.mjs:27-37` — `recogniseCiRebuild` requires exactly one rebuild step, requires both `cargo install wasm-bindgen-cli --version 0.2.126 --locked` and `scripts/check-selfsame-wasm-rebuild.sh` inside it, and `doesNotMatch` both `/continue-on-error:\s*true/` and `/\n\s+if\s*:/`. Both mutations measured and killed. An `if:` moved *above* the `- name:` line is also killed, because the literal step header then no longer matches and `steps.length === 0` fails. |
| 3 | The vendor suite directly checks the exact host, compiler, and wasm-bindgen versions | **CONFIRMED.** `selfsame-vendor.test.mjs:89-95` asserts `buildHost === 'x86_64-unknown-linux-gnu'`, `rustcVersion === '1.96.0'`, `wasmBindgenVersion === '0.2.126'` by equality, not by pattern. All three mutations measured and killed. |
| 4 | The screen harness enters current credential/v2 commands and a valid PAIR1 invitation | **CONFIRMED.** `tests/screens.mjs:254-282` — the stub bridge answers `cbcl_pairing_capability`, `cbcl_v2_recognise`, `cbcl_v2_relay_decide`, `cbcl_v2_cancel`; the legacy `cbcl_pairing_start`/`cbcl_pairing_cancel` cases are gone. `:633-639` types `PAIR1-00000-…-00000` into `#pairing-presence-code`, which satisfies `src/pairing.js:11`'s `presencePattern` (11 Crockford groups) and so enables the start button (`:47`). |
| 5 | It renders pre-socket authentication, exact-pair consent, and authenticated exact-intent consent | **CONFIRMED, executed.** `CI=true npm run screens` renders `31-pairing-wait`, `32-pairing-relay-consent`, and `33-pairing-intent-consent`, with `SCREEN_RULES` requiring "No relay connection yet" / "Checking the invitation and its declared relay before any socket opens." on 31, "Trust this new relay?" + the exact application and relay + "Your approval adds only this exact pair to your own relay policy." on 32, and "Review the exact request" + "Authenticated application" + the exact permission on 33. Exit 0. |
| 6 | The invoke scanner covers every calling module and every handler prefix | **PARTIALLY CONFIRMED — see M-1.** Prefix coverage is confirmed and enforced: the registered set is derived from the `generate_handler!` block, and the `app_grant`-excluding mutation is killed. Module coverage is *currently correct* — `grep -rl "invoke("` over `src/` returns exactly the three scanned files, and all thirty called commands are registered — but it is **not enforced**: dropping a module from the list leaves the gate green. |
| 7 | TEST-1162 reaches real transaction hooks in causal production order | **PARTIALLY CONFIRMED — see L-3.** The fifteen hooks are proved present, unique, and causally ordered in the production function by source scan, and I confirmed that scan kills both hook removal and adjacent-hook reordering. The transaction behaviour is proved by real store-failure injection through `begin` and `commit_payload`. The production function itself is still never executed by a test. |
| 8 | The release relay authenticates cbcl-pairing `62ef4a9` as its executable baseline before Cargo runs | **CONFIRMED.** `scripts/build-cbcl-pairing-relay.sh:81-92` sets `reviewed="62ef4a968b46b4836374fcee1d78c410f730a7a7"` and enforces ancestry plus an exact diff over `src dialects schemas vectors Cargo.lock build.rs .cargo/config .cargo/config.toml` and a license-line-excepted `Cargo.toml` diff — all of it above the `cargo_args=(…)` assembly at `:95` and the `cargo` invocation at `:120`. |
| 9 | A pre-v2 executable baseline makes the release builder refuse | **CONFIRMED, executed.** Reverting `reviewed` to `197d4cb3d1560ab5328df28fc984269799c510f9` makes both `scripts/build-cbcl-pairing-relay.sh release` and `scripts/check-release-target-isolation.sh` refuse with "latest pin has executable drift from Selfsame's reviewed baseline." |
| 10 | The Linux rebuild reproduces the committed `a8f82e98…` WASM bytes | **CONFIRMED, independently reproduced.** In a `linux/amd64` container on `x86_64-unknown-linux-gnu` with rustc 1.96.0 and wasm-bindgen 0.2.126, from my own clean checkouts at all five candidate revisions, `scripts/check-selfsame-wasm-rebuild.sh` reports "Selfsame WASM rebuild matches the exact vendored package" — that is `cmp` equality on all four generated artifacts, not the digest alone — and `sha256sum` of the served WASM is `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`. |
| 11 | The authority records that the lint disposition changed embedded source locations and invalidated the preceding digest | **CONFIRMED.** SPEC-008 0.5.15 changelog: "A Linux rebuild also disproved the preceding byte-stability assumption: the Clippy disposition changed embedded source locations. The exact regenerated WASM and measured digest now form the candidate." cbcl-bus SPEC-053 0.17.11 records the same: "A clean Linux rebuild disproves 0.17.10's byte-stability premise…". The provenance record moved `selfsameSha` `ff2f139` → `48a0c74` and `wasmSha256` `23e14bf6…` → `a8f82e98…` in the same commit, and `selfsame.sha` and the Dockerfile `ARG SELFSAME_SHA` moved with it (cross-checked by `scripts/check-cbcl-rs-pin.sh:53-69` and by the in-image `test` at `Dockerfile:92`). |

## Dispositions for all eighteen mutations

Every mutation was applied to a clean scratch copy and restored immediately
after measurement; `git status --porcelain` was confirmed empty after each.

| # | Mutation | Killing gate | Result |
|---|---|---|---|
| 1 | Remove the credential/v2 restoration lint disposition | `cargo clippy --workspace --all-targets --locked -- -D warnings` | **KILLED** — exit 101, "this function has too many arguments (9/7)". CI runs Clippy before the TEST-1162 step, so the job stops before it. |
| 2 | Add `if: false` to the exact hub WASM rebuild step | `selfsame-vendor.test.mjs` TEST-120 | **KILLED** — exit 1, 5 pass / 1 fail. |
| 3 | Add `continue-on-error: true` to that step | `selfsame-vendor.test.mjs` TEST-120 | **KILLED** — exit 1, 5 pass / 1 fail. |
| 4 | Change the recorded build host | `selfsame-vendor.test.mjs` | **KILLED** — exit 1. |
| 5 | Change the recorded rustc version | `selfsame-vendor.test.mjs` | **KILLED** — exit 1. |
| 6 | Change the recorded wasm-bindgen version | `selfsame-vendor.test.mjs` | **KILLED** — exit 1. |
| 7 | Restore credential/v1 screen stubs | `CI=true npm run screens` | **KILLED** — exit 1; screen 31 reached `pairing-enter`, expected `pairing-wait`. |
| 8 | Remove the valid PAIR1 screen input | `CI=true npm run screens` | **KILLED** — exit 1; the start button stays disabled, so screen 31 never leaves `pairing-enter`. |
| 9 | Exclude `src/pairing.js` from invoke discovery | *(none)* | **SURVIVED** — exit 0, "all screens rendered clean". See **M-1**. |
| 10 | Exclude the `app_grant` handler prefix | `CI=true npm run screens` | **KILLED** — exit 1; `cbcl_enrol_prepare` and `cbcl_enrol_confirm` reported as called-but-unregistered. |
| 11 | Revert the release relay baseline to `197d4cb3…` | `build-cbcl-pairing-relay.sh` and `check-release-target-isolation.sh` | **KILLED** — both refuse. |
| 12 | Inject `conformance-allocation` through `RUSTFLAGS` into a release build | `build-cbcl-pairing-relay.sh:17-30` | **KILLED** — "RUSTFLAGS is forbidden for release builds." |
| 13 | Change the recorded WASM digest | `selfsame-vendor.test.mjs` | **KILLED** — exit 1, digest equality fails. |
| 14 | Change one served WASM byte | `selfsame-vendor.test.mjs` | **KILLED** — exit 1 (byte flipped at offset 1884902). `check-selfsame-wasm-rebuild.sh`'s `cmp` is a second, independent killer. |
| 15 | Move transaction arming after initial persistence | TEST-1162 | **KILLED** — exit 101, `assertion failed: pending_links().unwrap().is_empty()` at `cbcl_v2_completion.rs:2520` (the post-commit store-failure case). |
| 16 | Disarm before durable `PayloadPrepared` replacement | TEST-1162 | **KILLED** — exit 101, same assertion at `:2561` (the pre-commit store-failure case). |
| 17 | Remove or reorder one production boundary hook | TEST-1162 source-order scan | **KILLED both ways** — removal: "production boundary hook must occur exactly once: PrePayloadBoundary::ResolverVerification"; reorder (`IssuerPublication` ↔ `ResolverVerification`): order panic at `:2485`. |
| 18 | Weaken unlink's slot or policy comparison | TEST-1162 | **KILLED both ways** — policy comparison removed: panic at `:2676`; slot comparison removed: panic at `:2659`. |

Seventeen of eighteen killed. The survivor is mutation 9.

## Dispositions for F-A through F-E and REQ-812

| Hard stop | Disposition |
|---|---|
| **F-A** — exact application+relay, never relay-only | **CLOSED, code-confirmed independently.** `cbcl_v2_policy.rs`: the row key is the full `(applicationId, relayOrigin)` tuple in `state`, `insert`, and `remove`; rows are canonical, strictly ordered, re-encode-checked, and `MAX_ROWS = 256`-bounded; malformed state refuses rather than degrading to a fresh TOFU prompt. The row is written only in `bind_finished_profile`, after both Finished values. |
| **F-B** — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed independently.** See checks 5 and 6. `intent_view` reads only `CredentialV2Display` accessors; the display is produced under the independently fetched profile; `AuthenticatedOfferVerdict::verify` refuses any peer claim that differs from the authenticated authority. |
| **F-C** — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed independently.** `recognise_claimant_invitation` → `authorise_claimant_relay` (single-use `RelaySocketCapability`, consumed by value) → `prepare_claimant` → `preview_claimant_identity` → `complete_claimant` is the only credential/v2 construction path; `recover_claimant_completion` is terminal and cannot construct, derive, sign, publish, or resend. |
| **F-D** — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed independently.** The scope is a signed-offer field verified under the independently fetched profile, and the profile digest that gates that offer is inside the CPace channel identifier, both associated-data values, and the public context. |
| **F-E** — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged).** Nothing represents operatorship as authentication; the containment is per-application exact-pair consent plus the cryptographic application binding. The UI copy says so in the person's own words. |
| **[[SPEC-007-cbcl-pairing-cutover#REQ-812]] — post-consent identity derivation hard stop** | **CLOSED, code-confirmed independently, traced against `src-tauri/src/cbcl_v2_commands.rs:152-682`.** Before preliminary approval the path builds and sends only the intent-decision object, and does not even take the passcode unless `approve`. Preliminary approval authorises exactly one `Custody::use_hierarchy_root` in `preview_identity`, returning zeroizable public preview material and performing no signature, issuer creation, resolver call, WebFinger call, grant construction, publication, alias operation, or durable identity write. The first durable record and the first signature both follow final approval, in that order, and no application capability is exposed before the hub's immutable `finalStatusJws` authenticates. |

## Exact executed commands and results

Host: `arm64` macOS (Darwin 25.3.0), rustc/cargo 1.96.0, wasm-bindgen 0.2.126,
node v22.12.0, puppeteer 23.11.1 from the tracked lockfile, Erlang/OTP 28,
rebar 3.27.0, Docker 28.3.3.

### Closure construction

```
$ git clone --quiet --no-checkout <each source> <scratch>/closure/<name>
$ git -C <scratch>/closure/<name> checkout --quiet --detach <pinned sha>
selfsame     -> 48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7 dirty=[0]
cbcl-pairing -> 62ef4a968b46b4836374fcee1d78c410f730a7a7 dirty=[0]
cbcl-rs      -> febc6691e6dd2d5f7116b1a4d84c984b64717564 dirty=[0]
did-crdt     -> 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1 dirty=[0]
cbcl-bus     -> 14f26065cb147633416def15d0a7d8603bce2e09 dirty=[0]
```

cbcl-bus's four pin files at the candidate name exactly the other four
revisions, so the five-repository closure is self-consistent.

### Selfsame

```
$ cargo clippy --workspace --all-targets --locked -- -D warnings
    Finished `dev` profile ... EXIT=0

$ cargo test -p selfsame --locked --lib -- --ignored --exact \
    cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot
running 1 test
test cbcl_v2_completion::tests::test_1162_... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 66 filtered out
CARGO_EXIT=0
$ grep -Fq "test result: ok. 1 passed; 0 failed; 0 ignored"   -> pass
```

The CI step's own `grep -Fq` assertion holds, so a renamed or unselected test
could not exit successfully.

```
$ CI=true npm run screens
... 31-pairing-wait screen=pairing-wait
    32-pairing-relay-consent screen=pairing-consent
    33-pairing-intent-consent screen=pairing-consent
    invoke-surface every invoke resolves; 10 registered and unreached (...)
all screens rendered clean                                  EXIT=0

$ CI=true npm run e2e:pairing
ok 1 - TEST-704 approved and declined ceremonies run in same-browser tabs
ok 2 - TEST-709 demo rejects a non-loopback bind
# tests 2  # pass 2  # fail 0                               EXIT=0

$ CI=true npm run e2e:wallet-pairing
ok 1 - TEST-1161 pending completion retains the authenticated offer profile for restart recovery
ok 2 - TEST-814 CBCL wallet states are keyboard complete and WCAG-clean
ok 3 - TEST-1161 restart recovery preserves pending through rotation consent
ok 4 - TEST-1159 installed links reload, prompt on rotation, and unlink locally
ok 5 - TEST-1162 interrupted pre-payload links are visible and locally abandonable
# tests 5  # pass 5  # fail 0                               EXIT=0
```

The three browser steps ran in the declared continuous-job order and each
passed, so the wallet pairing lifecycle suite is genuinely reachable.

### cbcl-bus — vendor and browser suites

```
$ node --test apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs
# pass 6  # fail 0                                          EXIT=0

$ find apps/cbcl_chat/priv/web -name '*.test.mjs' -not -path '*/node_modules/*' | sort
discovered 42 browser-core suites            (CI ratchet: >= 40, satisfied)
$ node --test "${suites[@]}"
# tests 400  # pass 399  # fail 1
```

The single failure is `chat-avatar.test.mjs` "NFR-001 — a cold hash costs at
most 10 ms of work": `fastest cold hash 10.10 ms over 9 keys (budget 10 ms)`.
This is a wall-clock budget assertion and the run was concurrent with a
container build and two cargo builds. Re-run twice on an otherwise idle
machine: `# pass 34 # fail 0` both times. **Local infrastructure limit, not a
code defect**, and unrelated to SPEC-008 — the generating agent's claim of 400
passing tests reproduces when the host is not saturated.

### cbcl-bus — relay release source, isolation, and feature matrices

```
$ ./scripts/check-agent-spake2-boundary.sh      agent SPAKE2 boundary unchanged     EXIT=0
$ ./scripts/check-release-target-isolation.sh
Release builders ignored poisoned caches and used fresh private targets           EXIT=0
$ cargo fmt --manifest-path crates/cbcl-pairing-relay/Cargo.toml -- --check        EXIT=0
$ cargo test --locked --manifest-path crates/cbcl-pairing-relay/Cargo.toml
test result: ok. 10 passed  /  test result: ok. 4 passed                          EXIT=0
$ cargo test --locked --manifest-path ... --features conformance-allocation
test result: ok. 10 passed  /  test result: ok. 6 passed                          EXIT=0
$ cargo clippy --locked --manifest-path ... --all-targets -- -D warnings          EXIT=0
$ cargo clippy --locked --manifest-path ... --all-targets --all-features -- -D warnings  EXIT=0
$ RUSTFLAGS='--cfg feature="conformance-allocation"' ./scripts/build-cbcl-pairing-relay.sh release
build-cbcl-pairing-relay: RUSTFLAGS is forbidden for release builds.             refused
$ ./scripts/build-cbcl-pairing-relay.sh release
    Finished `release` profile [optimized]                                        EXIT=0
```

Every relay count the generating agent reported (10+4 default, 10+6
conformance, both Clippy configurations, poisoned-cache isolation, injected-
feature refusal, successful production build) reproduces exactly.

### cbcl-bus — hub unit suite

```
$ rebar3 deps                                              EXIT=0
$ rebar3 eunit
=======================================================
  All 975 tests passed.                                    EXIT=0
```

Reproduces the reported 975 exactly.

### Linux `linux/amd64` exact WASM comparison

Docker Desktop could not bind-mount this session's sandbox path, so the closure
was transferred into the container with `docker cp` rather than a bind mount;
the container therefore built from a byte copy of the same clean checkouts.

```
$ docker run --platform linux/amd64 ... bash -c '...'
rustc 1.96.0 (ac68faa20 2026-05-25)
host: x86_64-unknown-linux-gnu
wasm-bindgen 0.2.126
selfsame     48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7 dirty=[0]
cbcl-pairing 62ef4a968b46b4836374fcee1d78c410f730a7a7 dirty=[0]
cbcl-rs      febc6691e6dd2d5f7116b1a4d84c984b64717564 dirty=[0]
did-crdt     1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1 dirty=[0]
cbcl-bus     14f26065cb147633416def15d0a7d8603bce2e09 dirty=[0]
+ ./scripts/check-selfsame-wasm-rebuild.sh
    Finished `release` profile [optimized] target(s) in 2m 24s
Selfsame WASM rebuild matches the exact vendored package
+ sha256sum apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm
a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07
```

This is an independent fresh Linux comparison: all four generated files match
by `cmp`, and the served digest equals CON-985's recorded value. The 0.5.15
premise that the Clippy disposition shifted embedded source locations and
invalidated `23e14bf6…` is consistent with the measured `a8f82e98…`.

### Live capstones

```
$ SELFSAME_PATH=<closure>/selfsame ./apps/cbcl_chat/test/e2e/run-cbcl-invite.sh
[wallet] Selfsame verifier: accepted all 13 checks
[wallet] Pairing outcome: accepted
[cdp] PASS delivered                                        EXIT=0
```

The browser ↔ wallet ↔ relay capstone reproduces the reported durable effect on
both ends (`delivered` and `accepted`) against a real headless Chrome
allocator, the real conformance-allocation relay process, and the Selfsame CLI
wallet claimant.

```
$ SELFSAME_PATH=<closure>/selfsame CBCL_E2E_SKIP_COMPILE=1 \
    ./apps/cbcl_chat/test/e2e/run-pairaccount.sh
[wallet] Selfsame verifier: accepted all 13 checks
[wallet] Pairing outcome: accepted
[e2e] ack rendered: "a Selfsame device was paired · ed4928c628d1c2c6 (unratified) · 1 device on this account"
PASS: ceremony delivered, wallet accepted, hub grant acknowledged as a system line   EXIT=0
```

The browser ↔ wallet ↔ hub capstone reproduces too: the hub's
`pairaccountgranted` frame renders as a system line in the real chat client
after wallet acceptance, which is the durable effect the step claims rather
than the absence of refusals. Reaching it needed a full `rebar3 compile` first
(the CI step that `CBCL_E2E_SKIP_COMPILE=1` assumes); all three per-app purity
gates passed on the way (`boundary-check PASSED` ×3).

Note for the record: both capstones drive the `anuna.io/credential/v1`
application context — they are the SPEC-075/SPEC-076 live ceremonies, and they
exercise the relay, the vendored WASM verifier, and the hub grant, not the
credential/v2 first-contact claimant. The credential/v2 claimant's own
end-to-end evidence is the Selfsame browser and wallet pairing suites above.

### Mutation commands

Each mutation was applied with `perl`/`python3` to a clean scratch copy, measured,
then reverted with `git checkout --`; `git status --porcelain` was verified empty
after every one. Representative:

```
$ perl -0pi -e 's/    #\[allow\(clippy::too_many_arguments\)\]\n    pub fn restore_offer_context\(/    pub fn restore_offer_context(/' \
    crates/selfsame-web-device/src/lib.rs
$ cargo clippy --workspace --all-targets --locked -- -D warnings
error: this function has too many arguments (9/7)
error: could not compile `selfsame-web-device` (lib) due to 1 previous error      EXIT=101

$ python3 -c "...swap arming and persist_pending in begin()..."
$ cargo test -p selfsame --locked --lib -- --ignored --exact cbcl_v2_completion::tests::test_1162_...
panicked at src-tauri/src/cbcl_v2_completion.rs:2520: assertion failed: pending_links().unwrap().is_empty()
                                                                                  EXIT=101
```

## Gates not run, and why

- **Selfsame `cargo test --workspace --locked`.** Not in the mandate's minimum
  list, and not run. The host volume was at 100 % capacity throughout (228 GiB
  total, 194–196 GiB used, 0.5–3.5 GiB available), which forced me to delete my
  own `target/` trees between phases. The workspace-wide suite would have needed
  several GiB of test binaries at once. The credential/v2 members of that suite
  that the mandate does name — the isolated TEST-1162 process, both browser
  pairing suites, the screen gate, and deny-warnings Clippy over `--all-targets`
  (which type-checks every test target) — all ran and passed.
- **`chat-avatar.test.mjs` NFR-001 timing assertion** failed once, at 10.10 ms
  against a 10 ms budget, while the host was running a container build and two
  cargo builds concurrently. It passed twice on re-run. Recorded above as a
  local infrastructure limit; it is a wall-clock budget in an unrelated
  specification, not a defect in this candidate.
- Everything else in the mandate's minimum list ran to completion and is
  reported above with its exact command and outcome.

**Distinguishing code defects from infrastructure.** The one blocking finding
(M-1) is a code defect: a gate that does not fail when the parent says it must,
demonstrated by an executed mutation with a recorded exit code. Every gate that
did not run, or ran red once, is attributable to host disk exhaustion or host
load, and none of it bears on M-1.

## Release and deployment authority

**A PASS from this review would not authorize push, release, or deployment —
and this review is not a PASS.** It is a REJECT.

Release and deployment controls are outside this review's authority. SPEC-008
0.5.15's own gate line, CON-985, and cbcl-bus SPEC-053 0.17.11 each keep
production allocation, release, and deployment prohibited pending a fresh
cross-model Tier-1 PASS **and** separate owner approval. Nothing in this review
relaxes either condition, and nothing was pushed, released, or deployed in the
course of it.

The four LOW residuals (L-1 through L-4) are permitted only with the explicit
consequences and operational treatments recorded against each above. They are
not blocking, and none of them is a licence to skip the correction it names.
