# SPEC-008 0.4.14 + cbcl-pairing 0.4.6 + cbcl-bus 0.16.7 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

Fresh-context, cross-model Tier-1 review of coordinated standalone first-contact
pairing. Defect-finding mandate; no drafting or implementation mandate.
Verdict: **REJECT**. Four HIGH, five MEDIUM, six LOW.

## Model, driver and authentication context

| Field | Value |
|---|---|
| Model | Claude Opus 5 (`claude-opus-5`) |
| Driver | Claude Code CLI, terminal TUI session, bypass-permissions mode |
| Authentication | Claude Max subscription OAuth |
| `ANTHROPIC_API_KEY` | **absent from the process environment** — verified with `env` filtered for `anthropic` (no match) and `${ANTHROPIC_API_KEY:+YES}` → empty. Unrelated third-party keys (OpenAI, Together, Firecrawl, …) are present in the environment and **none was used**. |
| Account-billed Anthropic API calls | none |
| External paid API | none |
| Web access | none used. Every claim below comes from local git object reads. |
| Sub-agents / workflows | none invoked |
| Date | 2026-08-23 |

Method note: every code claim was read from a read-only baseline view
(`git show <baseline>:<path>`, `git grep <baseline>`), never from a sibling
working tree. Sibling trees do contain unrelated dirt and were not trusted.

### Reviewed commits, verified rather than assumed

| Repository | Candidate | `HEAD` matched | Baseline | Diff is spec-only |
|---|---|---|---|---|
| selfsame | `9e4076c4390016556de0893b84e4c4a57db40419` | yes | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | yes — 27 files, all `specs/trajectory/SPEC-008/`, 24,201 insertions, 0 deletions |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | yes | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | yes — 8 files, all `specs/trajectory/SPEC-001/`, 1,940 insertions, 0 deletions |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | yes | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | yes — 8 files under `specs/`, 2,928 insertions, 2 deletions (both in `specs/SPEC-053-key-root-identity.md`) |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | yes | same commit | no delta |

Additional pinned inputs read read-only:

- cbcl-rs at `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- OTP 28.5 / Mnesia 4.25.3 source at
  `/opt/homebrew/Cellar/erlang/28.5/lib/erlang/lib/mnesia-4.25.3/src/`
  (`mnesia_locker.erl`, `mnesia_tm.erl`, `mnesia.erl`)

**No implementation code, test, manifest, or configuration differs between any
declared baseline and its candidate.** The candidates are specification-only, as
required.

### Documents read to determine effective text

Selfsame SPEC-008 0.4.1 through 0.4.14 and every adversarial review in the
trajectory (0.4.1–0.4.13), `req-909-adversarial-review-2026-08-22.md`,
`pairing-completion-runbook.md`, `SPEC-007-cbcl-pairing-cutover.md`,
`SPEC-004-application-scoped-identity.md`. cbcl-pairing SPEC-001 0.3.0 through
0.4.6. cbcl-bus SPEC-053 0.16.1 through 0.16.7, `SPEC-053-key-root-identity.md`
(parent, incl. DEC-012 and GATE-01/GATE-03), SPEC-076, SPEC-075.

---

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 4 | H-1, H-2, H-3, H-4 |
| MEDIUM | 5 | M-1 … M-5 |
| LOW | 6 | L-1 … L-6 |

**REJECT.** Four HIGH findings are release-blocking. Three of them
(`H-1`, `H-2`, `H-3`) are defects in the *new* text of this amendment, not
inherited residuals, and two of them (`H-1`, `H-4`) are direct recurrences of
the v0.4.13 findings this amendment claims to close.

What this amendment gets right is substantial and is recorded honestly in the
dispositions below: the room alphabet now matches deployed syntax
character-for-character; every arithmetic figure re-derives exactly; the twelve
contention pairs are correctly derived for the three authority tables and the
`G×PC` removal is correct; the destructive remote-clearing transition is gone;
all three test survivor ledgers are exact; and every coordination pointer
resolves. The rejection rests on defects that are specific and individually
correctable, not on the architecture.

---

## Findings

### H-1 (HIGH, release-blocking) — the widened room grammar is contradicted by a shipped, live, load-bearing browser regex that terminally burns the ceremony on exactly the newly admitted values, and no candidate names it

**Repository owner: cbcl-bus** (with a coordination note for selfsame and
cbcl-pairing).

REQ-990, cbcl-pairing REQ-028 and cbcl-bus REQ-062 all carry the byte-identical
grammar (verified: the three extracted grammar blocks share SHA-256
`d0cd93f71cbe6dee24f201ee14e21ca0227639abc1e585f63d35b37f774657fe`):

```text
CbclSymbolChar = ALPHA / DIGIT / "_" / "-" / "." / "/" / "!" /
                 "?" / "+" / "*" / "<" / ">" / "=" / "@"
CanonicalRoomText = "@" 1*128( CbclSymbolChar )
```

The alphabet is correct — see the H-1 disposition below; I derived it
independently and it matches character-for-character. TEST-1121 then requires
that rooms containing each of `/ ! ? + * < > = @` and a leading symbol such as
`@_draft` be **accepted**, and cbcl-pairing TEST-057 requires the same.

At the declared cbcl-bus baseline `fdbe414`,
`apps/cbcl_chat/priv/web/path-b-ceremony.mjs:118` reads:

```js
const CHANNEL = /^@[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/;
```

and `:150-157`:

```js
function affectedChannels(value) {
  if (!Array.isArray(value) || !value.every((channel) => typeof channel === 'string' && CHANNEL.test(channel))) return null;
  …
}
```

and `:192-195`:

```js
const channels = affectedChannels(action?.affectedChannels);
if (state.status !== CeremonyStatus.AwaitingGrant
  || !closed(action, ['accountDid', 'affectedChannels', 'grantId', 'type'])
  || !DID.test(action.accountDid) || !GRANT_ID.test(action.grantId) || !channels) state = burned('invalid-grant-observation');
```

`@team/eng`, `@_draft`, `@a+b`, and `@@x` all fail `CHANNEL` — `/`, `+` and a
leading `_` or `@` are outside `[A-Za-z0-9._-]`, and the first character class
is `[A-Za-z0-9]`. Every one of them therefore drives
`state = burned('invalid-grant-observation')`. `Burned` is terminal:
`reduce` returns the state unchanged for `Burned` at `:172`, so the person's
link attempt is unrecoverable within that ceremony, and the reason shown is
`invalid-grant-observation` — a claim that the **grant observation** was
invalid, when in fact a **room name** was outside a stale regex.

This module is not future or dormant. `apps/cbcl_chat/priv/web/app.js:42`
imports it directly (`from '/static/path-b-ceremony.mjs'`), `app.js:19` imports
its adapter, and `app.js:569` names it as the owner of every transition. It is
shipped production code on the Path-B consent path.

cbcl-pairing `ADR-021` states: *"The old room grammar copied a future browser
regex and excluded rooms the deployed parser and current UI already admit."*
The regex is not future — it is current, shipped, and terminal. The `{0,127}`
tail and the `129`-octet total match `CanonicalRoomText` exactly, which is
strong evidence the amendment authors were looking at this very regex and
mis-classified it. **No clause in any of the three candidates names
`path-b-ceremony.mjs`, `CHANNEL`, `affectedChannels`, or
`invalid-grant-observation`**, and no clause requires the shipped regex to be
widened. The specification therefore asserts an admission set that the shipped
consent path refuses, with an unnamed, misleading, terminal refusal.

This is the same defect class as the one this file documents about itself at
`:104-116` — a validator narrower than the values it would ever receive, making
a happy path unreachable, *"[f]ound by an adversarial review of the surface that
would have fed it"*.

**Minimum sound correction.** Add to cbcl-bus REQ-062 (and mirror the pointer in
selfsame REQ-990) an explicit statement that
`apps/cbcl_chat/priv/web/path-b-ceremony.mjs`'s `CHANNEL` recogniser is a
current shipped admission gate on the Path-B consent path; that it SHALL be
replaced by `CanonicalRoomText` byte-for-byte; that `affectedChannels`
non-conformance SHALL surface one of the three named migration terminals rather
than `burned('invalid-grant-observation')`; and add a GATE-04 box requiring the
browser recogniser and the credential/v2 grammar to be generated from one
source. Correct `ADR-021` to say *shipped*, not *future*.

---

### H-2 (HIGH, release-blocking) — the three admission terminals have no named carrier, and the three candidates disagree about which component owns them

**Repository owner: cbcl-bus (primary), selfsame and cbcl-pairing
(coordination).**

The amendment introduces three terminals — `MigrationPlanTooLarge`,
`MigrationRoomUnrepresentable`, `MigrationHandleUnrepresentable` — and makes all
of them load-bearing: each must fire *before* carrier, digest, pairing-room,
typed-display or migration allocation, and each must leave no partial state.
Three clauses assign them three different owners.

Selfsame REQ-990:

> **The hub reads the complete durable room set**, removes duplicate raw byte
> values, and sorts it by unsigned octets before allocating any public carrier.
> … returns the closed `MigrationPlanTooLarge` **browser terminal** before
> carrier allocation.

cbcl-bus REQ-062:

> Before public carrier allocation **the hub** takes a stable snapshot of the
> complete durable room set. … All are **browser-local terminals** before
> carrier, pairing-room, digest, display, or migration allocation.

cbcl-pairing REQ-028:

> **The consumer owns** the earlier `MigrationPlanTooLarge`,
> `MigrationRoomUnrepresentable`, and `MigrationHandleUnrepresentable` admission
> terminals

cbcl-pairing's frontmatter sets `consumer-design: selfsame SPEC-008
0.4.14-draft`, so "the consumer" is the **wallet**. So the hub evaluates, the
browser owns the terminal, and the wallet owns the terminal — three different
actors for one refusal.

The carriage is unspecified and the deployed system has no carrier that would
work. Within this specification family, "browser-local terminal" is a defined
term with a definite meaning, fixed by adopted CON-969: *"`pairaccount-possession-required`
is a browser-local terminal state. It has **no hub wire carrier**."* But
`MigrationPlanTooLarge` and `MigrationHandleUnrepresentable` are functions of
**durable hub state** — the complete room set and the current account's durable
handle — which the browser does not hold. At the bus baseline the browser's only
room source is `app.js:602-606`, a DOM query (see `H-3`). So the terminal cannot
be browser-local in CON-969's sense and simultaneously be a predicate on hub
durable state.

The remaining option is a wire carrier, and the amendment's own `ADR-025` closes
it: *"The deployed hub has no authenticated, correlated, account-scoped control
frame."* The only deployed error carrier is
`cbcl-chat-session-ws.lfe:2093-2097` → `cbcl-core-wire:err/2` built with
`cbcl-core-wire:zero-sig/0` (64 zero octets, `cbcl-core-wire.lfe:31-32`), which
REQ-991 itself correctly describes as unauthenticated and uncorrelated.
Delivering an allocation-blocking refusal over that carrier makes it forgeable
by anything that can inject a frame, and — more seriously — **suppressible**: a
withheld or dropped refusal would let carrier allocation proceed over a set that
was never admitted. A refusal whose absence is indistinguishable from success
cannot be the mechanism that guarantees "Neither result creates a partial array,
digest, carrier, pairing room, or migration."

**Minimum sound correction.** Name exactly one evaluating component and one
carriage for each of the three terminals. The sound shape, and the one that
requires no new signed protocol, is: the hub evaluates all three **inside the
same transaction that takes the room snapshot**, and a failure is expressed as
*refusal to allocate the carrier* — an absence of the allocation response the
browser is already waiting for — rather than as a delivered terminal. Then say
that the browser's rendering of the named terminal is a rendering of that
absence, and delete "browser-local" from clauses that describe a predicate on
hub durable state. Reconcile cbcl-pairing REQ-028's "the consumer owns" to
"defence-in-depth recognition only", which is what its next sentence already
says.

---

### H-3 (HIGH, release-blocking) — the browser's exact-room-list consent surface derives its displayed rooms from the DOM, not from the digest-bound authenticated set

**Repository owner: cbcl-bus.**

REQ-990 requires *"Exact room-list consent"* over the complete set covered by
`roomSetDigest` and `migrationRooms`, with *"no truncation, paging, hidden
remainder, or partial migration."* Adopted REQ-972 makes that binding real: the
wallet *"recomputes the confirmation from its local preview and authenticated
offer"*, and the hub *"recomputes it from held state"*. Both ends of the wallet
half are authenticated. I verified this half is sound.

The **browser** half is not. At the bus baseline:

```js
// app.js:602-606
function affectedChannelsNow() {
  return [...document.querySelectorAll('.clog[data-room]')]
    .map((n) => n.dataset.room)
    .filter((room) => room !== '');
}
```

```js
// app.js:6405-6410
linkReduce({
  type: 'grant-verified',
  accountDid: heldGrant.accountDid,
  grantId: heldGrant.grantId,
  affectedChannels: affectedChannelsNow(),
});
```

```js
// app.js:652-670  — the person-visible consent list
if (status === CeremonyStatus.AwaitingConfirmation) {
  const { accountDid, accountFingerprint, affectedChannels } = ceremony.state;
  …
  for (const room of affectedChannels) {
    const li = document.createElement('li');
    li.textContent = room;
    list.appendChild(li);
  }
}
```

and `path-b-ceremony.mjs:205-209` marks the next step: *"This is the
person-confirmed consent point."*

So the list the person reads before consenting is **whatever `.clog[data-room]`
elements happen to be in the DOM**. It is never compared with `migrationRooms`,
`roomSetDigest`, `migrationSnapshotDigest`, or any hub-authenticated value.
`path-b-ceremony.mjs:150-157` validates it only for shape, regex conformance and
uniqueness. The adapter deliberately refuses to supply it
(`path-b-ceremony-adapter.mjs:175`: *"`affectedChannels` IS REQUIRED AND IS NOT
SYNTHESISED HERE"*), and the shipped caller supplies the DOM.

Consequences, all within the prompt's display-provenance and consent-ordering
scope:

1. Any room absent from the DOM at that instant — not yet rendered, lazily
   loaded, filtered, collapsed, or removed by a concurrent update — is a room
   the person consents to migrate **without ever seeing it**. That is precisely
   the "hidden remainder" REQ-990 forbids, arriving through the display rather
   than through the protocol.
2. The displayed list has no authenticated provenance, so `F-B`'s "every
   cross-check precedes display and approval" is satisfied for the wallet
   surface and **not** for the browser surface.
3. The digest graph does not rescue this. `migrationSnapshotDigest` binds the
   hub's `cbcl-roommember` vectors and would catch a *state* change; it cannot
   catch a *display* that was drawn from a different source in the first place.

No clause in any of the three candidates names `affectedChannelsNow`, the DOM
provenance, or requires the browser confirmation list to equal `migrationRooms`.

**Minimum sound correction.** Require in cbcl-bus REQ-062/CON-028 that the
browser confirmation surface render **exactly** the authenticated
`migrationRooms` array bound by `roomSetDigest`, that `affectedChannelsNow()` be
removed as the source of `CeremonyObservation.GrantVerified.affectedChannels`,
and that a mismatch between any locally derived list and the authenticated array
be a named refusal rather than a silent preference for either. Add a GATE-04 box
requiring the two lists to be proven identical in the consent-surface test.

---

### H-4 (HIGH, release-blocking) — `(error @room "not-issued")` is still a claimed wire object that does not exist; the deployed refusal collapses to `bad-signature`, and `not-issued` never leaves the hub

**Repository owner: selfsame and cbcl-bus (identical text in both).**

This is the surviving half of v0.4.13's `C-1`. The candidate correctly removes
`not-issued` as a **clearing trigger** — that half is closed, see the
disposition table. It retains it as a **claimed wire object**, re-labelled from
"authenticated exact-key" to "deployed zero-signature":

REQ-991: *"In particular, the deployed zero-signature `(error @room
"not-issued")` frame is unauthenticated, uncorrelated, and account-ambiguous."*
cbcl-bus REQ-063: *"Every deployed hub-originated error, including `(error @room
"not-issued")`, has the zero control signature…"*

At the declared baseline `fdbe414` there is no such frame. The complete trace:

1. `apps/cbcl_chat/src/core/cbcl-core-enrol.lfe:241` produces the **atom**
   `#(reject not-issued)`. This is the only occurrence of `not-issued` anywhere
   in `apps/*/src/`.
2. `cbcl-core-enrol:resolve` has exactly one caller in the tree —
   `authorise-frame` in `apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:282-296`
   — which discards the reason to a log line and returns `#(error rejected)`:

   ```lfe
   (`#(reject ,reason)
    (logger:info "enrol_rejected handle=~ts kind=~s clause=~s"
                 (list handle kind reason))
    #(error rejected))
   ```

   Its own comment states the design: *"CON-008's reason enum is deliberately
   discriminating so the detail can be LOCAL while **the wire answer collapses
   to `bad-signature`**"*.
3. The caller of `authorise-frame`, `verify-and-route` at `:262-264`, matches
   every non-`ok` result with
   `(_ (reply-error state (or-room room) #"bad-signature"))`.
4. `reply-error` at `:2093-2097` emits
   `(error @room "bad-signature")` with `cbcl-core-wire:zero-sig`.

So the deployed frame for an unissued grant is `(error @room "bad-signature")`.
`not-issued` is a local log token that never reaches the wire.

Everything the amendment *needs* is nevertheless true of the real frame: it is
zero-signature (`cbcl-core-wire.lfe:31-32`), it carries only a room and a slug
(`cbcl-core-wire.lfe:35-36`, `err(Room, Slug) -> "(error " room " \"" slug "\")"`),
and it carries no account, device, grant, or request correlation. The *normative*
rule — "No hub error or chat frame clears the installation record" — is safe and
correct regardless. The defect is that a Tier-1 specification again names a wire
object that does not exist, and this time makes mandatory tests unsatisfiable:

- TEST-1123: *"Deliver **genuine** and forged `not-issued`"*
- cbcl-bus TEST-081: *"**Genuine** and forged `not-issued`, generic errors,
  timeout, disconnect, another account, and another key never clear the record."*

There is no producer of a genuine `not-issued` frame, so neither test can be
written as specified, and GATE-04 box 36 ("ignores remote clear") depends on
TEST-081.

The secondary hazard is display fabrication. REQ-991 permits the browser to show
`wallet-authorization-not-issued` on receipt of the frame. An implementer keying
that status on the string `"not-issued"` gets a status that never fires; an
implementer who "fixes" it by keying on the slug that actually arrives gets
*"authorization not issued"* rendered for **every** genuine signature failure,
malformed selector, revoked key, missing standing and undeclared permission —
because the deployed design collapses all six `cbcl-core-enrol` rejection
clauses into one wire slug.

**Minimum sound correction.** Replace both occurrences of `(error @room
"not-issued")` with the frame that exists,
`(error @room "bad-signature")`, and state the collapse explicitly: the hub's
authorisation refusals are indistinguishable on the wire, so no browser status
may claim a specific authorisation cause. Either delete the
`wallet-authorization-not-issued` status or redefine it as a cause-neutral
"the hub refused this frame" indication. Amend TEST-1123 and cbcl-bus TEST-081
to drop "genuine `not-issued`" and instead require that **no** value of the
deployed slug vocabulary clears the record.

---

### M-1 (MEDIUM) — the named v1 substitution trace omits every production version *comparison* site, and GATE-04 box 12 was weakened from "every … site" to "every **named** … site"

**Repository owner: selfsame (trace), cbcl-bus (gate box).**

REQ-990 requires that the v2 *"profile, provider hint, offer, statement, builder,
**recognizer**, signer, and profile-facts paths use only the version selected by
the recognized v2 profile"* and that *"No v2 path **compares with** or
substitutes a v1 constant"*. cbcl-bus REQ-062 calls its trace *"The **complete**
named v1 substitution trace"*. The block names three locations:

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts
crates/selfsame-web-device/src/lib.rs:1343-1361 EnrolmentAllocator::new_native
crates/selfsame-app-identity/src/enrollment.rs:452 statement builder
```

All three are real at the baseline and I confirmed each:
`lib.rs:600` is `selfsame_app_identity::PROFILE_VERSION` inside
`pub fn profile_facts` (declared `:582`); `lib.rs:1346` and `:1360` are the
offer and hint `profile_version: 1` literals inside `pub fn new_native`
(declared `:1318`); `enrollment.rs:452` is `profile_version: crate::PROFILE_VERSION`.

A full sweep of the baseline workspace (`git grep -n "PROFILE_VERSION"` and
`"profile_version: 1"` over `crates/*/src/*.rs`, then `#[cfg(test)]` boundaries
checked on every hit) gives the complete production set. Every hit at
`lib.rs:1664/1676/1685` is below `#[cfg(test)]` at `lib.rs:1521`, and
`selfsame-beam/src/enrollment.rs:236` is below `#[cfg(test)]` at `:199` — those
four are test fixtures and correctly excluded. The **four production comparison
sites are all unnamed**:

| Site | Code | Surface |
|---|---|---|
| `selfsame-app-identity/src/profile.rs:385` | `if version != PROFILE_VERSION { return Err(bad("profileVersion", "must be exactly 1 in this profile version")) }` | **profile recognizer** — the only production `ApplicationProfile::recognise` path, reached by `new_native` at `lib.rs:1334` |
| `selfsame-app-identity/src/provider_hint.rs:115` | `if self.profile_version != crate::PROFILE_VERSION { return Err(HintError::UnsupportedProfileVersion) }` | **provider-hint recognizer** |
| `selfsame-app-identity/src/enrollment.rs:394` | `if payload.get("profileVersion").and_then(Json::as_i64) != Some(crate::PROFILE_VERSION)` | **statement payload recognizer** |
| `selfsame-app-identity/src/enrollment.rs:552` | `\|\| statement.profile_version != crate::PROFILE_VERSION` | **statement verification** |

The amendment plainly knows these constants exist — cbcl-bus REQ-062 forbids
comparison with `crate::PROFILE_VERSION`, `profile::PROFILE_VERSION` **and**
`selfsame_app_identity::PROFILE_VERSION`, and both definitions are real
(`lib.rs:109` and `profile.rs:60`, each `= 1`). It names the symbols and none of
the sites that use them. The mandate for this review was explicit that a static
denylist is not a call-graph proof, and this is a denylist plus three
substitution sites.

The gate box that would have caught it moved the wrong way. Diffing all 39
GATE-04 boxes 0.16.6 → 0.16.7:

```text
- frozen v1 and disjoint v2 call graphs pass against pinned v1 bytes and
  every comparison or substitution site, including profile facts;
+ frozen v1 and disjoint v2 call graphs exclude every named comparison and
  substitution site, including both builders and profile facts;
```

0.16.6 quantified over **every** site; 0.16.7 quantifies over every **named**
site, and the named set omits all four recognizers. The box also drops "pass
against pinned v1 bytes". This is a mandatory box weakening in the exact place
the finding lands.

What is **not** wrong here: the normative rule itself is universal and correct,
so a conforming implementation is still forbidden from reaching these sites from
v2 — the defect is in the evidence obligation, not the requirement. And the
`new_native` unreachability proof is genuinely structural rather than a denylist:
`new_native` is exposed to JavaScript only through the `#[wasm_bindgen(js_class =
EnrolmentAllocator)]` constructor at `lib.rs:1417-1430`, and cbcl-bus CON-027
requires the v2 browser module `credential-v2-canonical.mjs` to import no
vendored Selfsame WASM. The vendored WASM is real at the bus baseline
(`apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm`), so the
module-import disjointness is a meaningful proof obligation and not a tautology.

**Minimum sound correction.** Add `profile.rs:385`, `provider_hint.rs:115`,
`enrollment.rs:394` and `enrollment.rs:552` to the shared trace block in
selfsame REQ-990 and cbcl-bus REQ-062, labelled as comparison sites. Restore
GATE-04 box 12's universal quantifier ("every comparison or substitution site")
and its "pinned v1 bytes" clause, keeping the added "both builders" text.

---

### M-2 (MEDIUM) — the derived contention set is closed over the three authority tables but not over the two room tables the finalizer write-locks, so "exactly the twelve pairs" is not reproducible from a faithful trace

**Repository owner: selfsame (NFR-916) and cbcl-bus (NFR-014).**

I reconstructed the pairs from the normative NFR-913 matrix and OTP 28.5 /
Mnesia 4.25.3 `can_lock/4` rather than accepting the list. Both load-bearing OTP
facts check out, and so does the derivation.

`mnesia_locker.erl:348-366`:

- `can_lock(Tid, read, {Tab,?ALL}, _)` selects `{{Tab,'_'}, write, _}` — a whole
  table read lock conflicts with an exact record write anywhere in the table;
- `can_lock(Tid, write, {Tab,Key}, _)` selects `{Oid,'_',_}` **and**
  `{{Tab,?ALL},'_',_}` — an exact record write conflicts with a held whole-table
  read lock.

NFR-913's two API claims are exact: `mnesia:index_read/3` →
`mnesia_locker:ixrlock/5` → `need_lock(Store, Tab, ?ALL, read)` and
`grant_lock(Tid, {ix_read,…}, Lock, Oid)` returning `{granted, Res, [?ALL]}`
(`:1086-1102`, `:326-333`); `mnesia:foldl/3` → `foldl(…, read)` →
`init_iteration` → `lock(ActivityId, Opaque, {table, Tab}, read)`
(`mnesia.erl:1842-1843`, `:1953-1954`). Deadlock prevention is wait-die exactly
as stated: `allowed_to_be_queued(WaitForTid, Tid) -> WaitForTid > Tid`
(`:430-437`), so an older holder makes the younger requester die with
`#cyclic{}` (`:294-297`), and an older requester queues behind a younger holder.
Progress is therefore guaranteed within a pair.

Applying the amendment's own two rules to the matrix reproduces the twelve pairs
exactly:

- **grant table**, rule (a) — the operations that hold grant `?ALL` and later
  take an exact grant write are `G`, `F`, `OR` (not `XR`, which takes no `?ALL`;
  not `ME` or `PC`, which take grant `?ALL` but write a different table). The six
  unordered pairs with repetition over `{G,F,OR}` are `G×G, G×F, G×OR, F×F,
  F×OR, OR×OR`. ✔
- **pairaccount table**, rule (a) — only `PC` both reads pairaccount `?ALL` and
  writes it (`F` explicitly *"performs no pairaccount write"*). `PC×PC`. ✔
- **member table**, rule (a) — empty, as the list implies. ✔
- **cross-table**, rule (b) — `G×ME` and `F×ME` (each side holds the other's
  target table's `?ALL` and wants an exact write in it); `F×PC` and `F×AR`
  (`F`'s exact member write blocks the other's member `?ALL`, while the other's
  grant `?ALL` blocks `F`'s exact grant write); `XR×OR` (`XR` holds a device
  guard `OR` wants, `OR` holds grant `?ALL` blocking `XR`'s exact grant write). ✔
- **`G×PC` correctly does not derive.** `G` never touches the pairaccount table,
  so `PC`'s exact claim write is not blocked by `G` — the contention is one-way
  (`G` waits on `PC`) and is not a cycle. Its removal is right. ✔

So the derivation is sound *for the three authority tables*. It is not closed
over the room tables, and NFR-916 and NFR-014 both scope their rule to "each
authority table" while TEST-1124 and cbcl-bus TEST-082 demand "**exactly** the
twelve … cyclic pairs" from "the implemented trace".

At the bus baseline the finalizer's room step is
`apps/cbcl_chat/src/shell/cbcl-chat-account-roommember.lfe:134-147`:

```lfe
(defun migrate-legacy-handle-tx (handle account enrolled-pubkey)
  (mnesia:lock (tuple 'table 'cbcl-roommember) 'write)
  (mnesia:lock (tuple 'table 'cbcl-account-roommember) 'write)
  …)
```

Two answers fall out, and one is a gap in the normative matrix itself:

1. **The whole room locks are `write`, not `read`.** NFR-913 says only "whole
   `cbcl-roommember`; whole `cbcl-account-roommember`" with no mode, and neither
   NFR-916 nor NFR-014 supplies it. The mode is load-bearing: under
   `can_lock(Tid, write, {Tab,?ALL}, _)` a whole-table write lock conflicts with
   *every* lock on that table, which is far broader than a read lock. A set
   claimed to be "mechanically derived from the traced acquisition sequence"
   cannot leave the mode of two of the finalizer's locks unstated. (The
   companion answer, which the amendment *does* supply and which is needed for
   `F×PC` and `F×AR`, is that `F`'s exact `cbcl-member` is a **write** — NFR-916
   states it in prose but NFR-913, which "remains normative", still does not.)

2. **Room-membership writers are an operation the matrix does not contain, and
   they satisfy the amendment's own rule (a).**
   `cbcl-chat-account-roommember.lfe:201-209`:

   ```lfe
   (defun revoke-handle-tx (room handle)
     (let ((keys (… (mnesia:index_read 'cbcl-account-roommember handle 'handle))))
       (lists:foreach (lambda (key) (mnesia:delete 'cbcl-account-roommember key 'write)) keys)
       'ok))
   ```

   This holds `{cbcl-account-roommember, ?ALL}` (index read) and then requests
   exact writes on the same table — textbook rule (a). Its callers are ordinary
   membership operations (`cbcl-chat-roommember.lfe`'s `grant/5`,
   `redeem-and-grant/5`, `write-grant/5`, `revoke/2` and the
   `account-roommember` equivalents), none of which appears in the
   seven-operation matrix. A faithful trace generator therefore yields at least
   `RM×RM` and `F×RM` in addition to the twelve, and TEST-1124's "exactly the
   twelve" fails — or the generator silently excludes the room tables, which no
   clause authorises and which is precisely the silent-cap failure mode.

3. `F`'s tail — *"alias, account, tombstone, finalization, status, and
   compensation exact keys"* — is six further tables with **no** companion
   operation analysis at all. GATE-04 retains boxes for *"immutable status
   recovery and authority-continuity restore drills"*, so a recovery operation
   exists and touches at least the status table; it is not in the matrix.

**Minimum sound correction.** Either (a) state the lock mode of every entry in
NFR-913 (both room tables `write`, exact `cbcl-member` `write`), add
room-membership grant/revoke and the recovery operation as named operations, and
re-derive the set — which will be larger than twelve; or (b) explicitly scope
the closed set to the three authority tables, say in TEST-1124/TEST-082 that the
generator is restricted to those three tables, and add a separate obligation
that the room and finalizer-tail tables have their own derived set. Option (b)
is smaller and honest; option (a) is stronger. Either way the "exactly twelve"
assertion and the trace generator's scope must agree.

---

### M-3 (MEDIUM) — the bounded-retry contract admits two values at which OTP performs no retry at all, and the exhaustion reason differs under nesting

**Repository owner: selfsame (NFR-916) and cbcl-bus (NFR-014).**

Both say: *"Every affected operation uses `mnesia:transaction/2` with an
explicit **non-negative integer** `Retries`, or `mnesia:transaction/3` with
`Args` and an explicit non-negative integer `Retries`. … Exhaustion returns
`authority-busy`, has no partial effect, and refuses closed at every consumer."*

The arity/overload half is correct — `mnesia.erl:729-734` shows the three `/2`
clauses, only the `is_integer(Retries), Retries >= 0` one being bounded, and
`transaction(Fun, Args)` falling through to `infinity`. But
`mnesia_tm.erl:929-931`:

```erlang
try_again(infinity) -> yes;
try_again(X) when is_number(X) , X > 1 -> yes;
try_again(_) -> no.
```

with `decr/1` at `:1013-1015` (`decr(X) when is_integer(X), X > 1 -> X - 1;
decr(_X) -> 0`). So the effective number of retries is `max(0, Retries - 1)`:

| `Retries` | retries actually performed |
|---|---|
| 0 | 0 |
| 1 | 0 |
| 2 | 1 |
| n ≥ 2 | n − 1 |

`0` and `1` are both "explicit non-negative integers" and therefore **conforming**,
and at both values the first `#cyclic{}` abort returns `{aborted, nomore}`
immediately. Under wait-die every one of the twelve derived pairs kills the
younger transaction on contact, so a conforming implementation at `Retries ∈
{0,1}` converts every derived cycle into an immediate `authority-busy` and can
never satisfy GATE-04's *"representative load evidence shows writer progress"*.
The specification names bounded retry as the mitigation for the closed cyclic
set while permitting values at which the mitigation does not exist.

Two smaller mismatches in the same clause:

- The exhaustion reason is `{aborted, nomore}` only at `Ts#tidstore.level == 1`.
  At any deeper level `maybe_restart/6` returns `return_abort(Fun, Args, Why)`
  with `Why` still the `#cyclic{}` record (`mnesia_tm.erl:920-927`), and a
  mis-nested call returns `{aborted, nested_transaction}` (`:823`). A consumer
  that maps only `nomore` to `authority-busy` fails open on nested paths. The
  clause says nothing about nesting.
- At the baseline **all 75** `mnesia:transaction` call sites in `apps/` use the
  unary form, i.e. `infinity`. The clause is a mandated change from unbounded to
  bounded, not a description of current behaviour; that is legitimate for a
  design but should be stated, because the change makes previously-succeeding
  contended writes surface `authority-busy`.

**Minimum sound correction.** Change "explicit non-negative integer" to
"explicit integer `Retries` ≥ 2" in both clauses, with a parenthetical citing
`mnesia_tm:try_again/1` (`X > 1`) so the bound is not re-broken later. Add that
every affected operation runs at transaction level 1, or that consumers map
`{aborted, nomore}`, `{aborted, #cyclic{}}` **and** `{aborted,
nested_transaction}` to `authority-busy`.

---

### M-4 (MEDIUM) — GATE-04 dropped the human acceptance of irreversible migration

**Repository owner: cbcl-bus.**

The gate holds at 39 boxes across 0.16.5, 0.16.6 and 0.16.7, and no box
disappears by count. One weakens by content. 0.16.6 box 27:

```text
- [ ] that review accepts irreversible migration and exact room-list consent;
```

0.16.7's replacement:

```text
- [ ] human review accepts exact room-list consent and every handle, room,
      count, and aggregate admission exclusion;
```

"irreversible migration" is gone from the human-acceptance box, and it does not
reappear in any other 0.16.7 box. The only surviving mention is the *technical*
box *"irreversible migration leaves the new key live with recoverable status"*
(`:289`), which asserts a property rather than requiring a human to accept the
irreversibility. Since REQ-062 and ADR-025 keep the migration irreversible, and
since GATE-04 box 9 is unchanged, the loss is exactly the sign-off.

The other 0.16.6 → 0.16.7 box deltas are pointer updates or strengthenings and
are fine: box 31 gains "all twelve derived cyclic pairs, and every AR-writer
contention case"; box 36 replaces "clears only on authenticated exact-key
`not-issued`" with "verifies its grant after reload, sends no enrollment frame,
ignores remote clear, and requires confirmed unlink". Box 12's weakening is
filed at `M-1`. Box 15's change from "every NFR-013 maximum" to "every unchanged
constructive maximum" loses a definite referent but stays resolvable through
NFR-014's "remain normative and unchanged" — noted, not filed.

**Minimum sound correction.** Restore "irreversible migration" to the human
acceptance box: *"human review accepts irreversible migration, exact room-list
consent, and every handle, room, count, and aggregate admission exclusion."*

---

### M-5 (MEDIUM) — an offline or unreachable-issuer reload is indistinguishable from an invalid grant, so transient unavailability collapses the installed state into a recovery terminal

**Repository owner: selfsame (REQ-991) and cbcl-bus (REQ-063).**

REQ-991's reload predicate is sound in its security direction and closes the
substantive half of v0.4.13 `L-3` — see the disposition. Clause 3 is:

> the installed grant verifies under the issuer selected by the current
> CON-220-authenticated application profile

and the failure rule is total:

> Corruption, a partial record, a profile mismatch, an invalid grant signature,
> or any binding mismatch fails closed as `credential-v2-recovery-required`.

Both inputs to clause 3 require the network. CON-220 authentication of the
current profile is a live fetch by construction. Issuer selection resolves
through the profile's `accountAuthority`, which at the baseline is a **DNS name**
(`profile.rs:409-410`, `uri::recognise_dns_name`), while the grant's `issuer` is
`did:crdt:<home>` (`grant.rs:124`, `:356-358`) — so the key must be resolved
off-machine.

Therefore an ordinary **offline reload** satisfies neither clause 3's precondition
nor its negation: the grant is not *invalid*, it is *unverifiable right now*. The
clause has no third outcome, so the only conforming behaviour is
`credential-v2-recovery-required`. The record itself survives (nothing in
REQ-991 deletes it, correctly), but the session shows a recovery terminal, and
REQ-991 supplies no in-session re-verification path — only "**Reload**
reconstructs installed account-device state". A person who reloads on a train
gets a recovery terminal until they reload again with connectivity. The same
happens on issuer rotation in did-crdt, on a slow `accountAuthority`, and on any
transient resolver failure.

This is the failure mode the codebase already knows to distinguish: the
`cbcl-core-enrol` revocation clause comment at `cbcl-chat-session-ws.lfe`
explicitly reasons about *"a verifier returning `#(error resolver-unavailable)`"*
and refuses it — but that is refusing *authority*, which is right, whereas here
the same conflation destroys *local usability* with no authority consequence.

**Minimum sound correction.** Split clause 3's failure into two named outcomes:
`credential-v2-recovery-required` for a record that is corrupt, partial, or
whose grant signature or bindings are **wrong**, and a distinct non-terminal
`credential-v2-verification-unavailable` for a profile or issuer that could not
be reached. Require the unavailable state to retain the record, send no
enrollment frame, make no authority claim, and re-attempt verification when
connectivity returns, without a reload. State the issuer-rotation outcome
explicitly.

---

### L-1 (LOW) — "all four named production substitution sites" has no resolvable referent, and the declared line range excludes the statement it claims to cover

**Repository owner: cbcl-bus (TEST-082) and selfsame (REQ-990 range).**

cbcl-bus TEST-082 requires a static check over *"all four named production
substitution sites."* The shared trace block names **three** file locations, and
its prose attributes **three** sites to the middle one ("offer, hint, and
statement"), giving five described sites. Neither three nor five is four, so the
check has no referent.

Separately, the declared range `crates/selfsame-web-device/src/lib.rs:1343-1361`
is labelled "offer, hint, and statement". The offer literal is at `:1346` and the
hint literal at `:1360`, both inside the range; the `EnrollmentStatement`
construction begins at `:1365` and takes `profile_version: core.profile_version`
at **`:1369`**, outside it. The value is inherited from `:1346`, so the trace is
not *wrong* — but a static check anchored on the stated range will not see the
statement site the label promises.

**Minimum sound correction.** Replace the count with the enumerated list, and
extend the range to `1342-1372` (or cite `:1346`, `:1360`, `:1369` individually).

---

### L-2 (LOW) — the installation record binds no profile digest, so an application that rotates `accountAuthority` redirects issuer selection without a new TOFU prompt

**Repository owner: selfsame (REQ-991) and cbcl-bus (REQ-063).**

The record contains *"the retained device seed, accepted installed grant, exact
ApplicationId, account scope, device public key, and grant reference"* — no
profile digest and no account authority. Person-owned TOFU is keyed on the exact
`(applicationId, relayOrigin)` pair. So an application that keeps its
ApplicationId and relay origin but publishes a new `accountAuthority` changes
which issuer clause 3 verifies against, with no new prompt. A hostile authority
could then mint a grant naming the recorded ApplicationId, account scope, device
key and grant reference, and clause 4 would pass for a credential the real
account revoked.

The blast radius is genuinely small, and the design already bounds it: REQ-991
states that the local terminal *"is evidence that a credential/v2 grant is
installed, not a claim that the hub currently retains that grant"*, and REQ-063
keeps hub authority independent (*"after authorized grant deletion, Path B
refuses the key immediately"*). So the resurrection is local display state only.
It is still a revocation-resurrection surface that costs one field to close.

**Minimum sound correction.** Add the observed profile digest and account
authority to the installation record, and require clause 3's issuer selection to
match the recorded values or else prompt.

---

### L-3 (LOW) — a bare `@` room is deployed-legal but falls outside both named refusal triggers

**Repository owner: selfsame (REQ-990) and cbcl-bus (REQ-062).**

`CanonicalRoomText = "@" 1*128( CbclSymbolChar )` has a minimum of two octets.
The deployed language does not: `parse_symbol` (`cbcl-parser/src/parser.rs:306-321`)
requires only one consumed character, so `@` alone parses to `Symbol("@")` and
satisfies the recipient predicate `sym.starts_with('@')`
(`cbcl-core/src/message.rs:573`). The hub applies no room-name validation at all
(`ensure-channel`/`create-channel` at `cbcl-chat-session-ws.lfe:1853-1898` pass
the name straight to `cbcl-chat-roomcfg:claim` with no length or alphabet check).

A durable room `@` is therefore neither "longer than 129 octets" nor "outside the
deployed recipient alphabet", so neither `MigrationPlanTooLarge` nor
`MigrationRoomUnrepresentable` is stated to fire.

**Minimum sound correction.** Restate `MigrationRoomUnrepresentable` as covering
any durable room that does not satisfy `CanonicalRoomText`, rather than only
values outside the alphabet.

---

### L-4 (LOW) — the non-expansion claim is correct under RFC 8785 but silent about the one character most likely to break it

**Repository owner: selfsame, cbcl-pairing, cbcl-bus (identical sentence).**

*"The alphabet excludes quote, backslash, and controls, so neither unit
expands."* This is right: RFC 8785 escapes only `"`, `\` and U+0000–U+001F, and
`/` is **not** escaped. The 33,793 total holds.

But `/` is newly admitted and is the character most likely to appear in a real
room (`@team/eng`), and several widely used JSON serializers escape `/` as `\/`
by default. Under such a serializer every `/` costs one extra octet, a 129-octet
room becomes up to 257 JCS octets, and the array cap is silently exceeded.

**Minimum sound correction.** Add one sentence: the serializer SHALL NOT escape
`/`, and the generator SHALL include a room containing `/` at maximum length as
a fixed point.

---

### L-5 (LOW) — the recovery route depends on an unstated fact about whether a revoked device key can be re-granted

**Repository owner: cbcl-bus (REQ-063).**

REQ-991 accepts a deliberate over-refusal on the promise that the person can
recover "through unlink and re-link", and unlink *"retains the device seed under
the existing recovery policy"*. But REQ-063 says that after authorized grant
deletion *"Path B refuses **the key** immediately."* Whether the hub will issue a
**new** grant for a key whose previous grant was deleted is not stated anywhere.
If it will not, the advertised recovery route fails for exactly the case it was
written for, because unlink deliberately retains that same key. GATE-04 box 29
asks humans to accept "local-unlink over-refusal" without disclosing this.

**Minimum sound correction.** State in REQ-063 whether a device key whose grant
was deleted may receive a new grant. If it may not, require unlink to offer a
new seed, and disclose the consequence in GATE-04 box 29.

---

### L-6 (LOW) — the TEST-050 survivor sentence omits that the retired witness is retained as a required negative

**Repository owner: selfsame (CON-973).**

The sentence *"TEST-050 except its replaced control-object portion and retired
4,096-octet positive witness"* matches cbcl-pairing 0.4.3 exactly on both
clauses, and is unambiguous about what is excluded — this closes v0.4.13 `L-2`.
0.4.3 goes one step further: *"Its `logicalLength = 4,096` positive control
witness is retired **and is a required negative witness**."* A reader excluding
it wholesale drops a mandatory negative. 0.4.3 remains adopted so the obligation
survives; the survivor sentence is merely lossy.

**Minimum sound correction.** Append "which remains a required negative
witness".

---

## Disposition of the original architecture hard stops F-A through F-E

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is exact application+relay scoped; a new application prompts | **HOLDS** | CON-973's six replacements touch REQ-987/989, NFR-915, CON-972, ADR-950 and four tests — **not** REQ-981, which retains the distinct ApplicationId `https://chat.anuna.io/selfsame/application/v2` and the person-owned exact policy pair. cbcl-bus REQ-062 restates the distinct ApplicationId in its first sentence and GATE-04 box 14 ("the exact v2 policy pair prompts when only the v1 pair was accepted") is unchanged from 0.16.5. Nothing in the widened room grammar or the unlink clause touches policy scoping. |
| **F-B** — approval displays the CON-220-authenticated application and transition; every cross-check precedes display and approval | **DOES NOT HOLD for the browser surface; holds for the wallet surface** | cbcl-pairing CON-023 preserves every private-construction and verdict-only property, and NFR-020 keeps every element and aggregate check ahead of the owned typed allocation — that half is sound and is strengthened. But the **browser** confirmation list is drawn from `document.querySelectorAll('.clog[data-room]')` and never cross-checked against the authenticated `migrationRooms` (`H-3`). A consent display with unauthenticated provenance is exactly what F-B forbids. `H-1` compounds it: the same surface refuses legitimate rooms terminally. |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias, or grant precedes the applicable explicit consent | **HOLDS** | CON-973 names nothing in CON-962, so its sixteen-step order is intact. Each new clause was checked for a pre-consent effect: REQ-990 is grammar, admission and bounds only and every terminal *"creates no pairing or migration state"*; NFR-916 is lock and retry evidence; REQ-991's terminals send no frame, and its only mutating path *removes* standing under person confirmation while retaining the seed. The deployed ordering is real: `recognise_unsigned` completes the closed grammar before a private key is constructed (`enrollment.rs:291-323`). `H-3` is a display-provenance defect, not a pre-consent effect, and is filed on its own terms. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS** | Untouched by 0.4.14. Adopted REQ-970/CON-939 still mint the pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId` before the public carrier, durable only at the final transaction, inside the distinct v2 namespace. cbcl-bus REQ-062 preserves the 23-member core and the exclusion of `accountDid` from it. |
| **F-E** — per-app consent is mitigation, not a relay-operatorship binding claim | **HOLDS** | Neither 0.4.14 nor cbcl-pairing 0.4.6 claims any binding property for the relay; CON-023 preserves the no-relay-field property verbatim. The size-correlation residual is unchanged and remains bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at cbcl-pairing `src/cbcl_protocol.rs:34`, above the 57,000-octet offer and 62,000-octet member caps. GATE-04 box 29 still requires human acceptance of relay-visible size correlation, and now also of local-unlink over-refusal. |

### SPEC-007 REQ-812 as an unwaivable hard stop

**HOLDS.** REQ-812 requires that *"[m]ismatch, protocol violation, cancellation,
relay closure, and expiry SHALL produce no accepted credential or identity side
effect before approved payload delivery"*
(`specs/SPEC-007-cbcl-pairing-cutover.md:328-334`). Every new refusal in 0.4.14
is non-creating: the three admission terminals fire before carrier allocation and
*"[create] no pairing or migration state"*; `credential-v2-recovery-required`
reconstructs nothing; `account-device-handle-change-refused` sends no frame;
`wallet-authorization-not-issued` is display-only. The one new mutating path,
`unlink Selfsame wallet`, is person-initiated and subtractive, which REQ-812 does
not reach. CON-973 restates it as a hard stop at `:218`. `H-2` bears on *how* a
refusal is delivered, not on whether it creates an effect.

---

## Disposition of every v0.4.13 finding

| v0.4.13 finding | Status | Basis |
|---|---|---|
| **C-1 (CRITICAL)** — destructive browser clearing on a wire object that does not exist | **PARTLY CLOSED — the destructive half is fully closed; the false code claim recurs as `H-4`** | Closed and well done: REQ-991 and cbcl-bus REQ-063 both state *"No hub error or chat frame clears the installation record"*; the only clearing transition is the person-confirmed `unlink Selfsame wallet`, which is atomic, seed-retaining, enrollment-frame-free, cancelable, and explicitly *"does not claim to revoke a hub grant or restore legacy authority"*. The revoked-but-installed state is named, bounded, and accepted as a reviewed tradeoff in ADR-025 with a GATE-04 box (29). I searched all adopted text and all 39 GATE-04 boxes for stale remote-clear claims and found **none** — box 36 was correctly rewritten and box 21 now reads *"cannot clear an installed browser credential"*. Not closed: `not-issued` survives as a claimed **wire object**, and the deployed slug is `bad-signature` (`H-4`). |
| **H-1 (HIGH)** — `CanonicalRoomText` narrower than the deployed room language | **PARTLY CLOSED — the grammar is now exactly right; the shipped consent-path recogniser is not, and is unnamed (`H-1` above)** | Closed: I derived the alphabet independently at cbcl-rs `febc669`. `parser.rs:328-342` is `is_ascii_alphanumeric()` plus `_ - . / ! ? + * < > = @` — a character-for-character match with `CbclSymbolChar`, independently corroborated by `cbcl-core/src/protocol.rs:32-35`. The recipient predicate is exactly `Symbol.starts_with('@')` (`message.rs:573`, `:584`, `:609`; `template.rs:93`), so the deployed room language is `"@" *(CbclSymbolChar)`, unbounded — the hub applies no room validation (`ensure-channel`/`create-channel`, `cbcl-chat-session-ws.lfe:1853-1898`). 129 is therefore correctly labelled *"a credential/v2 admission cap on an otherwise unbounded CBCL symbol, not a claimed deployed maximum"*, in both units, and `@team/eng`, `@_draft`, `@a+b` and `@@x` all satisfy the new grammar. The three grammars are byte-identical across the three repos. Not closed: the shipped browser recogniser still enforces the old grammar terminally (`H-1`), and the terminals have no carrier (`H-2`). |
| **M-1 (MEDIUM)** — the cyclic-source list asserts a closure it does not have | **PARTLY CLOSED — the authority-table derivation is now correct and complete; the room tables and finalizer tail are not (`M-2`)** | Closed: I reconstructed all twelve pairs from NFR-913 and OTP `can_lock/4` without accepting the list, and every one derives; `G×PC` correctly does not derive and is correctly removed; `F×PC`, `F×AR` and `XR×OR` each derive with the witnesses the clause names; wait-die is correctly identified (`allowed_to_be_queued: WaitForTid > Tid`); and NFR-913's `index_read`/`foldl` lock claims are exact at Mnesia 4.25.3. NFR-916 also correctly replaces a hand-written list with a stated derivation rule plus an amendment requirement. Not closed: the set is not closed over `cbcl-roommember`/`cbcl-account-roommember` or the six finalizer-tail tables, and two lock modes are unstated (`M-2`); the retry bound admits no-op values (`M-3`). |
| **M-2 (MEDIUM)** — `LegacyHandle` described as a deployed maximum | **CLOSED** | REQ-990 now says exactly the right thing: *"an explicit credential/v2 admission precondition, not a hub invariant or a claim about every historical client"*, and cbcl-bus REQ-062 mirrors it. The finding's factual basis is confirmed at the baseline: `cbcl-core-msg:sym->handle/1` (`cbcl-core-msg.lfe:39-45`) accepts **any** symbol payload or **any** binary as a handle with no grammar, length or encoding check, so symbol-valued, string-valued, Unicode and unbounded handles are all representable in hub state. The candidate supplies the named terminal (`MigrationHandleUnrepresentable`), forbids copying an unbounded value into the DOM while permitting a bounded escaped preview and digest, creates no state, and requires human review to own the exclusion (GATE-04 box 27). `LegacyHandle` itself matches the shipped browser convention exactly — `app.js:1074` and `identity-backup.mjs:14` are both `/^@[a-z0-9_-]{1,32}$/`, which is `"@" 1*32( %x61-7A / DIGIT / "_" / "-" )`. Carriage of the terminal is a separate defect (`H-2`). |
| **L-1 (LOW)** — three further production v1 substitutions unnamed | **NOT CLOSED, and the gate box regressed — refiled as `M-1` above** | The three `selfsame-web-device` sites the finding named (offer, hint, statement) and the `selfsame-app-identity` statement builder are now named. But an independent sweep finds four **comparison** sites still unnamed (`profile.rs:385`, `provider_hint.rs:115`, `enrollment.rs:394`, `enrollment.rs:552`) — every production recognizer, on a surface REQ-990 explicitly enumerates — and GATE-04 box 12 was weakened from "every comparison or substitution site" to "every **named**" one, which converts an omission into a closable gate. Severity raised from LOW to MEDIUM for that reason. |
| **L-2 (LOW)** — the cbcl-pairing test ledger is a range containing a partly-replaced test | **CLOSED** | CON-973's sentence — *"TEST-030 through TEST-049, TEST-050 except its replaced control-object portion and retired 4,096-octet positive witness, and TEST-051 through TEST-057"* — matches cbcl-pairing 0.4.3 clause-for-clause (*"The control-object portion of TEST-050 is replaced, not supplemented, by TEST-051 as corrected by TEST-052… Its `logicalLength = 4,096` positive control witness is retired"*). The cbcl-pairing inventory is TEST-030…TEST-057 with no gaps, so the survivor set is exact. I also verified the other two ledgers: cbcl-bus TEST-043…065 + 068 + 070 + 080…083 is exactly right — the replacement chain is 0.16.5 (072←066, 073←067, 074←069, 075←071), 0.16.6 (076…079←072…075), 0.16.7 (080…083←076…079), leaving 065/068/070 as the only 0.16.4 survivors — and it matches cbcl-bus REQ-061 and Selfsame CON-973 identically. Selfsame's "every effective test through TEST-1108, plus TEST-1121–1124" is exact given the chain 1109–1112 → 1113–1116 → 1117–1120 → 1121–1124. No open-ended range supplies authority in any of the three. One lossy phrase is filed as `L-6`. |
| **L-3 (LOW)** — the installed grant is never verified on reload | **CLOSED on the substance; two availability residuals filed as `M-5` and `L-2`** | REQ-991's four-clause predicate is the correction the finding asked for, and it is not circular, not locally forgeable, and not self-authenticating: the issuer is selected by the **current CON-220-authenticated application profile**, never from the record, so a locally authored record cannot manufacture a valid signature; clause 4 additionally binds the exact ApplicationId, account scope, device key and grant reference; and REQ-991 states directly that *"[i]n-memory state, a cache bit, handle equality, or seed possession alone is not authority."* cbcl-bus REQ-063 and CON-028 mirror it. The residuals are that "unverifiable" and "invalid" share one outcome (`M-5`) and that the record binds no profile digest (`L-2`). |

---

## Verification of the cbcl-pairing candidate (0.4.6, `c22f2d0`)

Baseline `8e55160`; the delta is specification-only (8 files, all under
`specs/trajectory/SPEC-001/`, 1,940 insertions, 0 deletions).

- **REQ-028 grammar** — byte-identical to the selfsame and cbcl-bus blocks
  (shared SHA-256 `d0cd93f7…`). The stated basis, *"matches `is_symbol_char` at
  pinned cbcl-rs revision `febc669`"*, is exact.
- **Array cap arithmetic** — `256 * (129 + 2) + 255 + 2 = 33,793` reproduces.
- **Constructibility** — "At least 256 distinct maximum-length values exist" is
  trivially true: the widened alphabet has 74 members, so `74^128 ≫ 256`. Both
  units agree because the alphabet excludes `"`, `\` and controls; RFC 8785 does
  not escape `/` (see `L-4` for the implementation hazard).
- **NFR-020 ordering** — every element and aggregate check precedes the owned
  typed allocation, and the clause explicitly forbids bypass via *"unbounded
  iterator collection, extension map, normalization, escaped surrogate,
  post-clone validation, or error-display copy"*. This is the correct shape and
  is strengthened relative to 0.4.5.
- **Scope discipline** — NFR-020 correctly disclaims room membership, migration
  eligibility, application authority, DID derivation, grant issuance and browser
  clearing. CON-023 preserves every private-construction, verdict-only,
  borrowed-accessor, v1-compatibility, no-relay-field and consumer-ownership
  property. No performative, frame, relay field, consent schedule, state
  transition, allocation, release or deployment authority changes.
- **Cap consistency with the consumer** — NFR-020's *"52,000-octet core cap and
  57,000-octet offer cap"* matches REQ-988's `V2_MAX_OFFER_CORE_OCTETS` and
  `V2_MAX_OFFER_PAYLOAD_OCTETS` exactly.
- **CPace and blind-relay bounds** — unchanged by this amendment and re-checked:
  `MAX_ADJACENT_BODY_OCTETS = 65_536` (`src/cbcl_protocol.rs:34`, enforced at
  `:834` and `:886`) still dominates the 57,000/62,000 caps.
- **TEST-057** — introduces no superseded authority and correctly re-runs
  TEST-053 through TEST-055 unchanged.
- `CredentialV2Display` and `PathAToBDisplay` do not exist at the baseline. That
  is expected for a design and is not a defect.

**Verdict for cbcl-pairing 0.4.6 in isolation: sound.** It carries one shared
sentence implicated in `H-2` (*"The consumer owns the earlier … admission
terminals"*, which conflicts with the other two candidates' "browser-local"),
and it inherits `L-4`. It contains no independent defect.

## Verification of the cbcl-bus candidate (0.16.7, `ce894a7`)

Baseline `fdbe414`; the delta is specification-only (8 files under `specs/`;
the only modified file is `specs/SPEC-053-key-root-identity.md`).

- **Parent pointers** — all three resolve to 0.16.7 and were verified in the
  diff: `version: 0.16.0 → 0.16.7-draft`, a new `candidate-amendment` frontmatter
  key naming `spec-053-0.16.7-deployed-admission-and-local-unlink`, and a new
  DEC-012 paragraph naming 0.16.7's GATE-04 and keeping the production deferral
  in force. Nothing else in the parent changed.
- **GATE-04** — 39 boxes, matching 0.16.6 and 0.16.5. No box disappears. Box 31
  and box 36 are strengthened. Box 12 weakens (`M-1`) and box 27 weakens
  (`M-4`).
- **REQ-061 survivor ledger** — exact and closed; verified against REQ-051 and
  the full 0.16.1→0.16.6 replacement chain. No open-ended range supplies
  authority.
- **REQ-062** — the grammar, the 23-member core, the distinct ApplicationId, the
  version-dispatch rule and the byte-frozen v1 output are all correctly stated.
  The named substitution trace is incomplete (`M-1`); the snapshot/terminal
  carriage is unresolved (`H-2`).
- **REQ-063 / CON-028** — the hub/browser truth-source split is clean and the
  clearing rules are correct. `not-issued` is misnamed (`H-4`).
- **NFR-014** — correct for the three authority tables, incomplete for the room
  tables (`M-2`), and its retry contract mismatches OTP (`M-3`).
- **CON-027** — the source-disjointness obligation is meaningful: the vendored
  Selfsame WASM really is present at the baseline
  (`apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm`), and
  `credential-v2-canonical.mjs` does not yet exist, so "imports no vendored
  Selfsame WASM" is a real and checkable constraint rather than a tautology.
  *"[A]ccepts typed values rather than caller-selected preimages"* correctly
  closes the NIF caller-selected-bytes surface.
- **ADR-025** — honest about both tradeoffs it makes.

**Verdict for cbcl-bus 0.16.7 in isolation: rejected**, as the owner repository
for `H-1`, `H-2`, `H-3`, `M-2`, `M-3`, `M-4` and (jointly) `H-4` and `M-1`.

## did-crdt

At `1f409a4` and unchanged from its declared baseline. Nothing in this
coordinated increment alters it. GATE-04's *"did-crdt durable readiness remains
continuously true"* box is unchanged and remains open.

---

## Independent attack surfaces examined and found sound

These were searched and produced no finding; they are recorded so a later
reviewer knows they were covered rather than skipped.

- **Profile downgrade / v1-v2 trust confusion** — the distinct v2 ApplicationId
  and the separate policy pair make first v2 contact prompt by construction;
  `profile.rs:385` hard-rejects any `profileVersion ≠ 1` on the v1 recognizer, so
  a v2 document cannot be accepted by the v1 path even by accident. (That same
  site is unnamed in the trace — `M-1` — but its behaviour is correct.)
- **TOFU suppression** — REQ-981 is untouched by CON-973 and still forbids a v1
  policy entry from suppressing the first v2 prompt; GATE-04 box 14 is unchanged.
- **Permission crossing** — the v2 permission namespace remains origin-constrained
  and both ApplicationIds share `https://chat.anuna.io`; unchanged by 0.4.14.
- **Consent reordering** — CON-962's sixteen-step order is not named in CON-973
  and is intact; both migration consent screens remain mandatory.
- **Size undercount** — every figure re-derived exactly (see below).
- **Relay/body mismatch** — `MAX_ADJACENT_BODY_OCTETS = 65_536` dominates every
  declared cap.
- **NIF caller-selected bytes** — CON-027 requires typed values, not preimages;
  Rust through `selfsame-beam` remains canonical and signing authority.
- **Pairaccount substitution and possession** — CON-969 is untouched;
  `pairaccount-possession-required` remains a browser-local terminal with no hub
  wire carrier, and GATE-04 box 35 ("challenges never render as chat") survives.
- **Control fallthrough** — dispatch-before-authorization is preserved (GATE-04
  box 10); the deployed `dispatch` already routes `grant`/`grantproof` before
  `do-publish` (`cbcl-chat-session-ws.lfe:222-223`).
- **Acknowledgement loss / reconnection** — REQ-991's *"Cancellation and every
  remote error leave the record unchanged"* covers disconnect and timeout, and
  TEST-1123 enumerates both.
- **Authority dominance and writers/deleters** — GATE-04 boxes 19–20 ("both grant
  creators and both deleters preserve every authority index", "verified authority
  dominates only while its durable row exists") are unchanged.
- **Production gate leakage** — GATE-00 production closure and fixture controls
  remain physically separate (box 38), and the owner's separate production
  approval remains (box 39).
- **Relay withholding** — bears on `H-2` and is filed there; it produces no
  separate finding while the carriage question is open.

### Arithmetic re-derived independently

Every REQ-988 figure the amendment claims is "unchanged" was recomputed from the
grammars rather than copied. All reproduce exactly, and the amendment's reasoning
for why widening the alphabet changes nothing is sound: both the old and new
alphabets are ASCII and exclude `"`, `\` and controls, so no length and no
escaping changes.

```text
room array   256 × (129 + 2) = 33,536; + 255 commas + 2 brackets = 33,793  ✔
non-room core       3,838 − 456 (deviceDid 512→56)
                          − 24 (two timestamps 32→20)
                          − 223 (legacyHandle 256→33)          =  3,135  ✔
complete core       3,135 + 33,793                             = 36,928  ✔
core residual      52,000 − 36,928                             = 15,072  ✔
offer addition    112 + 3,743 + 522 + 3                        =  4,380  ✔
complete offer     36,928 + 4,380                              = 41,308  ✔
offer residual     57,000 − 41,308                             = 15,692  ✔
member residual    62,000 − 41,308                             = 20,692  ✔
JWS   b64url(187)=250, b64url(2,536)=3,382, b64url(64)=86, +2  =  3,720  ✔
JWS residual        4,000 − 3,720                              =    280  ✔
JWK   {"alg":"EdDSA","crv":"Ed25519","kty":"OKP","x":"<43>"}   =     93  ✔
hint  114+61+64+59+18+78+105 members, +6 commas, +2 braces     =    507  ✔
```

The hint total was rebuilt member-by-member from CON-945's exact seven members in
JCS key order (`applicationId`, `carrierDigest`, `descriptorDigest`,
`offerDigest`, `profileVersion`, `providerId`, `transcriptHash`) with the
declared component maxima, and lands on 507 exactly. The statement 2,536 and
header 187 are consistent with the 0.4.12 → 0.4.13 delta (2,560 − 24 for the two
narrowed timestamps, and `enrollmentEvidence` 3,775 → 3,743, exactly the 32-octet
b64url difference). **At least 256 distinct maximum-length rooms remain
constructible under the widened grammar** (74 symbol characters, 128 positions),
and both units agree, so the fixed point is reachable and both repositories'
figures agree.

---

## Residual assumptions and open gates, separated from design blockers

These are **not** findings and do not contribute to the verdict. They are the
things a PASS would still be resting on.

1. **Every named terminal, the typed display, and the unlink control are design
   targets.** `MigrationPlanTooLarge`, `MigrationRoomUnrepresentable`,
   `MigrationHandleUnrepresentable`, `wallet-authorization-not-issued`,
   `credential-v2-recovery-required`, `account-device-handle-change-refused`,
   `unlink Selfsame wallet`, `credential-v2-canonical.mjs`,
   `CredentialV2Display` and `PathAToBDisplay` are all absent from their
   baselines. That is correct for a specification; it means every claim about
   their behaviour is unverifiable against code today and rests on the eventual
   test suite.
2. **All 39 GATE-04 boxes are open**, including GATE-01 and GATE-03, the human
   security/privacy review, the load-evidence review, and the owner's separate
   production approval. The amendment closes none of them and says so.
3. **The load gate claims no measured threshold**, correctly. Retry counts,
   latency and throughput under the derived cycles are unmeasured.
4. **did-crdt durable readiness and hub identity-authority off-machine
   readiness** are continuous-truth obligations asserted, not demonstrated here.
5. **The CON-226 corpus SHA-256** cannot exist until implementation, as prior
   amendments correctly note.
6. **The revoked-but-installed over-refusal** is a deliberate, disclosed
   tradeoff. I agree it is the right call for this increment — inventing an
   authenticated destructive frame to avoid it would be strictly worse — and it
   is properly gated on human acceptance. `L-5` asks only that its recovery route
   be made determinate.

---

## Minimum sound corrections and owners, collected

| ID | Sev | Owner repo | Minimum correction |
|---|---|---|---|
| H-1 | HIGH | cbcl-bus | Name `path-b-ceremony.mjs`'s `CHANNEL` as a shipped admission gate; require it to equal `CanonicalRoomText`; route non-conformance to a named migration terminal instead of `burned('invalid-grant-observation')`; correct ADR-021's "future". |
| H-2 | HIGH | cbcl-bus (+ selfsame, cbcl-pairing) | Name one evaluating component and one carriage per terminal; express refusal as refusal-to-allocate under the snapshot transaction; drop "browser-local" from predicates on hub durable state; reconcile cbcl-pairing REQ-028's "the consumer owns". |
| H-3 | HIGH | cbcl-bus | Require the browser confirmation list to be the authenticated `migrationRooms`; remove `affectedChannelsNow()` as its source; make any mismatch a named refusal; add a GATE-04 box. |
| H-4 | HIGH | selfsame + cbcl-bus | Replace `(error @room "not-issued")` with `(error @room "bad-signature")`; state the six-clause wire collapse; make the browser status cause-neutral or delete it; amend TEST-1123 and TEST-081 to drop "genuine `not-issued`". |
| M-1 | MED | selfsame + cbcl-bus | Add `profile.rs:385`, `provider_hint.rs:115`, `enrollment.rs:394`, `enrollment.rs:552` to the trace; restore GATE-04 box 12's universal quantifier and its "pinned v1 bytes" clause. |
| M-2 | MED | selfsame + cbcl-bus | State every NFR-913 lock mode (both room tables `write`, exact `cbcl-member` `write`); either add room-membership and recovery operations and re-derive, or explicitly scope the closed set and the trace generator to the three authority tables. |
| M-3 | MED | selfsame + cbcl-bus | Require `Retries ≥ 2` citing `mnesia_tm:try_again/1`; require level-1 execution or map `nomore`, `#cyclic{}` and `nested_transaction` all to `authority-busy`. |
| M-4 | MED | cbcl-bus | Restore "irreversible migration" to GATE-04's human acceptance box. |
| M-5 | MED | selfsame + cbcl-bus | Split unverifiable from invalid; add a non-terminal `credential-v2-verification-unavailable` that retains the record and re-verifies without a reload; state the issuer-rotation outcome. |
| L-1 | LOW | cbcl-bus + selfsame | Replace "all four named" with the enumerated list; extend the range to cover `:1369`. |
| L-2 | LOW | selfsame + cbcl-bus | Bind the observed profile digest and account authority into the installation record. |
| L-3 | LOW | selfsame + cbcl-bus | Make `MigrationRoomUnrepresentable` cover any value failing `CanonicalRoomText`. |
| L-4 | LOW | all three | Forbid escaping `/`; require a maximum-length `/`-bearing room in the generator. |
| L-5 | LOW | cbcl-bus | State whether a revoked device key may be re-granted; if not, have unlink offer a new seed and disclose it in GATE-04 box 29. |
| L-6 | LOW | selfsame | Append "which remains a required negative witness" to the TEST-050 sentence. |

---

## What a PASS would and would not authorize

Recorded because the amendment asks for it and the distinction must survive this
document: **this review is a REJECT, so nothing is authorized.** Had it been a
PASS, CON-973 and cbcl-bus REQ-061 are correct that a PASS would have authorized
**only** the Elephant SPL and the test-first implementation plan. A PASS would
grant **no implementation authority, no allocation authority, no release
authority, and no deployment authority**. All 39 GATE-04 boxes, GATE-01,
GATE-03, GATE-00 production closure, and the owner's separate approval of
production enablement and deployment would all remain independently required and
open. Review PASS and owner approval are distinct and neither substitutes for
the other.

---

*Review produced 2026-08-23 by Claude Opus 5 (`claude-opus-5`) under Claude Code
with Claude Max subscription OAuth, with `ANTHROPIC_API_KEY` absent from the
process environment, no account-billed Anthropic API call, no external paid API,
and no web access.*
