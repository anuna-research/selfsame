---
id: SPEC-008-0.4.6-claude-adversarial-review
title: Tier-1 fresh-context cross-model adversarial review — SPEC-008 0.4.6-draft (with adopted 0.4.4/0.4.5) and cbcl-pairing SPEC-001 0.3.0 + 0.4.0 + 0.4.1
status: complete
tier: 1
verdict: REJECT
verdict-selfsame-spec-008-0.4.6: REJECT
verdict-cbcl-pairing-spec-001-0.4.1: PASS-with-one-low
verdict-cbcl-pairing-spec-001-0.4.0: PASS-as-corrected-by-0.4.1
verdict-cbcl-pairing-spec-001-0.3.0: PASS-as-adopted
reviewer: independent Circus adversarial reviewer, fresh context, no access to the authoring conversation, no subagents
model: Claude Opus 5 (claude-opus-5)
review-date: 2026-08-23
candidates:
  - "[[spec-008-0.4.6-standalone-tofu-first-contact]] — specs/trajectory/SPEC-008/spec-008-0.4.6-standalone-tofu-first-contact.md at selfsame 6ac29ff7a910c12c7a3fe60a26010afa05322449"
  - "[[spec-008-0.4.5-standalone-tofu-first-contact]] — adopted except where 0.4.6 replaces or corrects"
  - "[[spec-008-0.4.4-standalone-tofu-first-contact]] — adopted except where 0.4.5 or 0.4.6 replaces or corrects"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.1-credential-v2-profile-authority.md at cbcl-pairing 9e8ba1badc92aefc1a235a2a7f6909ed2fc6f9d6"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.0-credential-v2-endpoint-profile.md (adopted, corrected by 0.4.1)"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md (adopted by 0.4.0)"
reviewed-revisions:
  selfsame-integration-baseline: 6ac29ff7a910c12c7a3fe60a26010afa05322449
  selfsame-code-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
  cbcl-pairing-spec-baseline: 9e8ba1badc92aefc1a235a2a7f6909ed2fc6f9d6
  cbcl-pairing-code-baseline: 8e55160a3234127bd60ce723fd93ba6b2b67a06b
  cbcl-bus: fdbe4142261923f48cced037a7f609e48a9fdd5a
  did-crdt: 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
prior-reviews:
  - "[[spec-008-0.4.5-claude-adversarial-review-2026-08-23]] (REJECT — 0 critical, 2 high, 4 medium, 8 low)"
  - "[[spec-008-0.4.4-claude-adversarial-review-2026-08-23]] (18 findings, carried by the 0.4.5 review)"
findings: 0 critical, 1 high, 3 medium, 7 low
last-updated: 2026-08-23
---

# Verdict: REJECT

One HIGH and three MEDIUM findings remain unresolved. The coordinated set does
not pass.

- **Selfsame `SPEC-008` 0.4.6-draft — does not pass.** `H-1`, `M-1`, `M-2`,
  `M-3`.
- **cbcl-pairing `SPEC-001` 0.4.1-draft — passes with one low.** It does exactly
  what the 0.4.5 review's `H-2` minimum correction asked: it names `CON-007` in
  `amends:`, scopes the parent table to `anuna.io/credential/v1`, supplies a
  complete six-row credential/v2 profile in `CON-017`, gives `CON-006` and
  `CON-008` retention rows, labels the public context, moves the allocator-only
  expiry rule off the claimant, replaces the loose envelope arm with two
  disjoint arms, and puts `CON-007` in the approval section with `TEST-048`
  through `TEST-050`. `L-4` below is the only residue.
- **cbcl-pairing `SPEC-001` 0.4.0-draft — passes as corrected by 0.4.1.**
- **cbcl-pairing `SPEC-001` 0.3.0-draft — sound as adopted.** Re-verified.

**Both 0.4.5 HIGH findings are closed and I could not reopen either.** `H-1` is
closed correctly and completely by the two-branch `CON-201` version-1
amendment: the deployed octets really do satisfy the `cutover-v1` member set,
which really is today's shipped recogniser byte-for-byte, so the frozen bytes,
`SPEC-007 CON-806`, `SPEC-075 REQ-007` and `SPEC-004 CON-201` now agree, and the
client-rollback direction the prior review said was unaddressed is addressed.
`H-2` is closed by cbcl-pairing 0.4.1. All four 0.4.5 MEDIUMs and all eight LOWs
are closed; I re-derived every number in `NFR-908` and `CON-018` independently
and they are exact.

The HIGH below is **not** a regression of a prior finding. It is a defect the
prior reviews could not have raised, because `CON-953` is the first clause in
the lineage to enumerate `cbcl-account-binding`, `cbcl-device-grant` and
`cbcl-member` as the v2 authority write set, and enumerating them is what makes
the real code's Path-A guard load-bearing. Its correction is bounded — three
sentences and two test rows — but it sits on an authority boundary the deployed
service protects with an explicit person confirmation, so it cannot be carried
as a residual.

---

## Revision verification

| Repository | Reviewed revision | Verified |
|---|---|---|
| `selfsame` (this worktree, `circus/spec008-v046-review/1`) | `6ac29ff7a910c12c7a3fe60a26010afa05322449` | `git status --porcelain` empty at session start. `git diff --name-status c6af6df75d033d2e44ae367457c1a754ba2c4c3a HEAD` lists **eleven files, every one under `specs/trajectory/SPEC-008/`, all `A` (added), zero `M`/`D`**. The non-spec code baseline `c6af6df` therefore holds byte-for-byte. |
| `selfsame` code parent | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | Confirmed the sole code ancestor of the candidate. |
| `cbcl-pairing` spec | `9e8ba1badc92aefc1a235a2a7f6909ed2fc6f9d6` | `git diff --name-status 8e55160a3234127bd60ce723fd93ba6b2b67a06b HEAD` lists **three files, all `A`, all under `specs/trajectory/SPEC-001/`**. Working tree carries untracked `.DS_Store`, `.zetl/`, `fuzz/.DS_Store`, `specs/.zetl/`, `src/.DS_Store` only; no tracked modification. Code baseline `8e55160` holds byte-for-byte. |
| `cbcl-pairing` code parent | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | Holds exactly. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | `git status --porcelain` empty. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | **Pre-existing dirt recorded, not edited:** modified `specs/did-crdt-spec.md`, untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md`. No source, test, fixture or asset file is dirty, so every code claim below reads committed `1f409a4`. |

No file outside
`specs/trajectory/SPEC-008/spec-008-0.4.6-claude-adversarial-review-2026-08-23.md`
was created or modified by this review. No candidate, parent spec, code,
fixture, vector or sibling-repository file was touched.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct.

`v046:NNN` references
`specs/trajectory/SPEC-008/spec-008-0.4.6-standalone-tofu-first-contact.md`.
`v045:` and `v044:` reference the 0.4.5 and 0.4.4 amendments in the same
directory. `companion041:`, `companion040:`, `companion030:` reference the three
cbcl-pairing trajectory documents at `9e8ba1b`. All `file:NNN` references are to
the confirmed baselines above.

**There are no CRITICAL findings.**

---

### H-1 (HIGH) — `CON-953` declares `cbcl-account-binding`, `cbcl-device-grant` and `cbcl-member` to be the complete v2 legacy authority write set, but omits the guard the deployed code puts between them: a handle with a legacy `cbcl-member` row cannot receive an account binding except through an explicit person-confirmed Path-A migration that also rewrites two room-membership tables `CON-953` never names. Either the v2 finalization fails after the identity effects are irreversible, or it moves standing chat authority without the confirmation `REQ-033` requires and strands every room grant.

**Exact candidate clauses.**

`v046:236-248` (`CON-953`):

> "This contract replaces 0.4.5 `CON-946`'s table and rollback paragraphs. No
> legacy record arity or primary key changes. **The complete legacy authority
> write set** and its additive stable-account index are:
>
> | Legacy table | Existing key retained | Additive index / v2 treatment |
> | `cbcl-account-binding` | handle | stable account ID to binding key + revision |
> | `cbcl-device-grant` | `(Account, PubKey)` | stable account ID to device-grant key + revision |
> | `cbcl-member` | handle | stable account ID to member key + revision |"

`v046:259-263` (`CON-953`): "Every authority/finalization transaction acquires
the stable-account row and **the exact legacy keys with write locks** before
re-reading authority and the computed withdrawal key. **It records only rows it
may write.**"

`v046:253-258` (`CON-953`): "Schema creation is idempotent and precedes the
feature gate; **per-account population is a separate all-or-none data
transaction.**"

`v046:130-136` (`REQ-965`) — the complete statement of v2 ineligibility:

> "Before enabling v2 allocation, the operator SHALL nevertheless audit actual
> Mnesia state rather than infer emptiness from code. **Any account with a v1
> binding is v2-ineligible and a v2 attempt returns `BoundDifferentDid` without
> mutation.**"

**Governing code evidence, `cbcl-bus` at `fdbe414`.**

- `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:178-181` — the only
  production writer of `cbcl-account-binding` for an unbound handle:

  ```lfe
  ;; Existing Path-A membership is a deliberate upgrade flow (REQ-033), never
  ;; an implicit consequence of presenting a grant on the wire.
  (case (cbcl-chat-members:pubkey handle)
    (`#(ok ,_) (mnesia:abort 'account-upgrade-required))
  ```

  `:161-176` `bind-and-store-verified-device-grant/8` routes every
  no-existing-binding case through this guard, and `:190-201` `store-device-tx`
  is the only writer of `cbcl-device-grant`. `cbcl-chat-session-ws.lfe:855-861`
  (`path-b-durable-write`) is the single production caller.
- `cbcl-chat-account-grants.lfe:118-140` — the *only* sanctioned way to clear
  that abort is `confirmed-migrate-path-a-handle/3`, which refuses unless
  `Confirmation` is literally `'confirmed`, and which calls
  `cbcl-chat-account-roommember:migrate-legacy-handle-tx`.
- `apps/cbcl_chat/src/shell/cbcl-chat-account-roommember.lfe:139-171` — that
  migration takes **whole-table write locks on `cbcl-roommember` and
  `cbcl-account-roommember`**, copies every legacy grant across, and only then
  deletes the legacy rows. Neither table appears anywhere in `CON-953`,
  `REQ-965`, `TEST-1069` or `TEST-1070`.
- `cbcl-chat-account-grants.lfe:65-74` — `account-binding/1` returns
  `#(ok Account)` once a binding row exists and `#(error key-bound)` while only
  a member row does. `cbcl-chat-session-ws.lfe:308-316` (`enrol-facts`) selects
  the CON-008 identity branch from exactly that value, so creating the binding
  row *is* the authority move.
- `apps/cbcl_chat/src/shell/cbcl-chat-members.lfe:42-54` +
  `cbcl-chat-session-ws.lfe:459-461` — `enroll-or-check/2` enrols a
  `cbcl-member` row on the first signed Path-A `hello`. **Every person who has
  ever used chat.anuna.io has one.** Unlike `provision/3` and
  `bind-and-store-verified-device-grant/8`, this writer is on the ordinary live
  path, so the "empty today" argument that bounds `M-3` of the 0.4.5 review
  does not bound this table.
- `apps/cbcl_chat/src/shell/cbcl-chat-path-b-gate.lfe:17-18` — `profile/0`
  returns `#(error tier-1-unapproved)`, so no `cbcl-account-binding` or
  `cbcl-device-grant` row exists in production yet. `confirmed-migrate-path-a-handle/3`
  has **no production caller at all** (grep across `apps/**` outside `test/`
  finds only its own definition and export).

**Failure trace.**

1. This review returns PASS. `v046:481-488` encodes the effective design as an
   SPL theory; step 3 commits failing schemas and pure-core tests; step 4
   implements "hub additive schema/transactions".
2. The implementer takes `CON-953:236-248` at its word — it says *complete* —
   and writes the finalization transaction over the six writable legacy tables
   with exact-key write locks, plus the six additive indexes at `v046:250-255`.
   `CON-953` contains no `cbcl-member` read and no membership tables, so nothing
   in the specified transaction reproduces `account-grants.lfe:178-181`.
3. A person who has used chat.anuna.io — so has a `cbcl-member` row and no
   `cbcl-account-binding` row — starts a v2 ceremony.
   `selfsamepairauthority(ceremonyId)` (`v044:1013-1021`) reads the
   account-keyed authority store, finds no binding, and answers `NoBinding`.
   `REQ-965:136`'s `BoundDifferentDid` branch does not fire — that branch is
   about `cbcl-webfinger-account`, and the person has no v1 webfinger binding
   either.
4. The ceremony proceeds through the whole irreversible half: `CON-938`
   `commit_issuance` opens custody, derives the home key, signs the grant,
   zeroizes the root, and `CON-955:340-341` publishes the signed bytes to the
   declared resolver. The browser runs `CON-206` steps 9–13 and installs its
   local connection.
5. `pairaccountgrant` finalizes and writes `cbcl-account-binding{handle}`.
   **Branch (a) — the implementer reused the existing writer:** the transaction
   aborts `account-upgrade-required`
   (`account-grants.lfe:181`). This is a post-consent, post-publication,
   post-local-commit terminal that no requirement names, no error token in the
   effective set covers, and `CON-953`'s compensation path was not written for.
   Its only repair, `confirmed-migrate-path-a-handle/3`, has no production
   caller and no operator runbook anywhere in the amendment. Every existing chat
   member is affected, so this is the normal case, not a corner.
   **Branch (b) — the implementer wrote the row directly, as `CON-953:259-263`'s
   exact-key locking model invites:** the write succeeds. The handle now has an
   account binding while every one of its room grants is still a legacy
   `cbcl-roommember` row, because nothing migrated them.
6. On the person's next `hello`, `enrol-facts`
   (`cbcl-chat-session-ws.lfe:308-316`) reads `account-binding/1`, now gets
   `#(ok Account)`, and selects the account branch of CON-008. Their room
   membership is unreachable from that branch: `cbcl-account-roommember` has no
   rows for them. `account-roommember.lfe:165-167` records the invariant the
   design has just broken — "no crash exposes one representation without the
   other or permits a legacy fallback after binding." The ceremony has performed
   the REQ-033 authority transfer whose entire reason for requiring
   `Confirmation = 'confirmed` is that "an account DID supplied in an ordinary
   frame [must not] move standing authority"
   (`account-grants.lfe:104-107`).

Branch (b) is the security-relevant one and branch (a) is the availability one.
The amendment picks neither, and both are reachable from its own text.

**Why this is not covered by what the amendment does say.** `REQ-965:130-132`
requires an operator preflight that audits "actual Mnesia state rather than
infer emptiness from code", and `TEST-1069:419-420` requires that "allocation
cannot enable with unclassified rows". That is a *release* gate on data
classification; it neither names `cbcl-member` as a class nor states what a
ceremony does when it meets one at finalization. `CON-953:263` "It records only
rows it may write" narrows the *capture* set, not the write set, and says
nothing about a precondition. `TEST-1070:423-428` populates "every CON-953
legacy table and side index" and fails each boundary — a fixture built from
`CON-953`'s table alone will never construct the member-row-without-binding
state, because `CON-953` does not say that state is significant.

**Minimum correction.** Three sentences and two rows.

1. In `CON-953`, add the precondition to the finalization transaction: it
   acquires the `cbcl-member` write lock for the handle and, when a legacy
   member row exists with no `cbcl-account-binding` row, refuses with a distinct
   terminal **before** the ceremony reaches any state that derives, signs or
   publishes — that is, the check belongs at `selfsamepairauthority`
   (`v044:1013-1021`) beside `Unknown`/`NoBinding`/`Bound(did)`, not at
   finalization. Add the third answer (`LegacyMemberRequiresConfirmedUpgrade` or
   equivalent) and map it in `REQ-946`'s browser table beside
   `BoundDifferentDid`.
2. In `CON-953`, add `cbcl-roommember` and `cbcl-account-roommember` to the
   table with their real keys, marked *not written by any v2 command*, and state
   that `confirmed-migrate-path-a-handle/3` is the only writer, is
   operator-invoked, requires the explicit person confirmation, and takes both
   table write locks — so its lock scope composes with `CON-953`'s exact-key
   model rather than silently contradicting it.
3. In `REQ-965`, add the second ineligibility class beside the v1-binding class,
   with the same operator-workflow treatment: identify the handle, run the
   confirmed Path-A upgrade (which needs a production entry point it does not
   have today, or an explicit runbook naming the `rpc`/remote-console lever),
   verify `cbcl-account-roommember` holds the migrated rows, and only then let
   the person start a v2 ceremony.
4. Extend `TEST-1069` with a legacy-member-row account attempting v2, requiring
   the pre-consent terminal, no derivation, no signature, no publication and no
   mutation; extend `TEST-1070` to prove no v2 write or rollback trace touches
   `cbcl-roommember` or `cbcl-account-roommember`.

---

### M-1 (MEDIUM) — `descriptorDigest` is a required member of the hub-signed `OfferCoreV2`, of the `CON-214` v2 evidence and of the seven-member provider hint, and the wallet is required to recompute it, but no clause in the effective set defines its preimage for a `cbclPairingRelays` descriptor; the parent rule it would inherit is scoped by its own words to a rendezvous descriptor that v2 profiles do not contain

**Exact candidate clauses.**

`v044:918-934` (`CON-939` step 2) makes `descriptorDigest` a member of the
closed canonical `OfferCoreV2`, and `v044:935-937`: "Binary IDs, nonces, and
SHA-256 values in this JSON object use canonical base64url-no-pad and decode to
32 octets". `v044:906` requires the browser to recompute it: "recomputes
`carrierDigest` and **the selected descriptor digest**".

`v045:316-330` (`CON-945`) puts `descriptorDigest` in the seven-member hint,
fixes its encoding, and `v045:333-337` requires that its "carrier, profile,
**descriptor**, core, and transcript values are exact-equality checked against
the locally recognized carrier/profile and the hub-signed `CON-214` v2
statement before display."

`v046:196-200` (`CON-951`) is the only 0.4.6 sentence about the descriptor and
it addresses grammar, not hashing: "The cutover-v1 `cbclPairingRelays` nested
grammar is exactly `SPEC-007 CON-806` and cbcl-bus `SPEC-075 REQ-007`".

**Governing evidence.**

- `specs/SPEC-004-application-scoped-identity.md:2634-2636` (`CON-201`) is the
  only definition of canonical descriptor bytes in the whole authority set, and
  it is explicitly about the other member: "For hashing in `CON-209` and
  PROTO-003, the canonical descriptor bytes are the RFC 8785 JSON
  Canonicalization Scheme serialization of the complete **rendezvous**
  descriptor, **including all three pairing fields**." A `cbclPairingRelays`
  descriptor has no `pairingUrl`, `pairingProtocol` or `pairingRoute`, so an
  implementer cannot apply this rule to it even by analogy without inventing the
  part the rule spends its words on.
- `specs/SPEC-004-application-scoped-identity.md:3283-3286` (`CON-209`) gives
  only the placeholder `"<base64url SHA-256 of canonical descriptor>"`.
- `specs/SPEC-007-cbcl-pairing-cutover.md:645-660` (`CON-806`) defines the
  descriptor member set and the selection algorithm and never mentions a digest
  of the descriptor.
- I grepped `RFC-8785|RFC 8785|RFC8785` across all three effective Selfsame
  amendments: five hits, none of them a descriptor rule
  (`v044:684`, `v044:745`, `v044:942`, `v045:128`, `v045:248`, `v046:69`,
  `v046:185`). `descriptorDigest` appears at `v044:279`, `v044:906`,
  `v044:924`, `v045:324` and nowhere in 0.4.6.

**Failure trace.**

1. The hub builds `OfferCoreV2` and must fill `descriptorDigest`. It has three
   defensible preimages: `SHA-256(RFC8785(descriptor object))`,
   `SHA-256(the exact octets of that descriptor as they appear inside the
   already-canonical served profile)`, or `SHA-256(UTF8(relayOrigin))`. Nothing
   selects one.
2. `NFR-014` (`companion040:155-160`) and `CON-226`
   (`SPEC-004:4643,4652-4658`) both require two implementations that share no
   protocol code to reproduce the same bytes. The second stack picks a different
   preimage.
3. `TEST-1049`/`TEST-041`-class vectors cannot be authored, because the vector
   file must contain one value.
4. At runtime the mismatch is a Finished-stage or display-stage refusal, so the
   ceremony fails closed — but the *silent* repair an implementer reaches for is
   the dangerous one: stop recomputing and copy the hub's value. The moment the
   wallet copies rather than recomputes, `CON-945:333-337`'s exact-equality check
   becomes vacuous and the binding that ties the authenticated offer to *which
   relay descriptor was selected* is gone, which is the one thing
   `descriptorDigest` is in the core to do.

**Minimum correction.** One sentence in `CON-951` or `CON-945`: for
`profileVersion = 1` cutover-v1 and `profileVersion = 2`, the canonical
descriptor bytes for `CON-209`/`CON-939` hashing are the RFC 8785 serialization
of the complete selected `cbclPairingRelays` descriptor object, all six members
included, and `descriptorDigest` is canonical base64url-no-pad of its SHA-256.
Add the preimage to the `CON-226` `credential_v2` corpus group and to
`TEST-1065`'s positive fixture set so the two stacks are pinned to it.

---

### M-2 (MEDIUM) — `REQ-965`'s mandated v1-to-v2 operator workflow requires `reinstate/2`, on a stated rationale that is false: the v2 alias URI is a different key from the tombstoned v1 URI, so no withdrawal row blocks the v2 ceremony. The step is unnecessary, and its only actual effect is to delete a revocation marker — which `ADR-937` and `REQ-964` exist to make impossible from every other direction

**Exact candidate clause.** `v046:138-142` (`REQ-965`):

> "A future v1-to-v2 transition is an explicit operator workflow: identify the
> exact existing account/home DID, run `withdraw/2`, **run `reinstate/2` for that
> same DID to remove the withdrawal block without restoring the binding**, verify
> the account is unbound, and only then let the person start a fresh v2
> ceremony. No ceremony performs or collapses those operator actions."

`v046:414-420` (`TEST-1069`) rehearses it verbatim: "Rehearse exact operator
withdraw plus same-DID reinstate, verify unbound state, then complete a fresh v2
ceremony."

**Governing evidence.**

- `v046:126-129` (`REQ-965`) itself: "The credential/v2 ApplicationId changes the
  hierarchy derivation input and therefore produces a distinct home DID, CON-211
  account scope, **reciprocal alias**, and exact-pair TOFU key from
  credential/v1." I verified the derivation:
  `crates/selfsame-app-identity/src/hierarchy.rs:267-269` derives
  `application_node` from `application.as_str()`, `:272-274` `account_node`,
  `:276-282` `home_key`, `:178-182` `home_did`; the crate's own test
  `hierarchy.rs:400-406` `a_one_octet_application_id_change_changes_the_home_key`
  asserts exactly this.
- `cbcl-bus/apps/cbcl_chat/src/core/cbcl-core-webfinger.lfe:35-40`:
  `account-uri(home-did) = "acct:ss-" || base32(SHA-256(home_did)) || "@" || authority`.
  So `did1 ≠ did2 ⇒ uri1 ≠ uri2`.
- `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:139-149`
  (`create-or-return`): the tombstone check is
  `(mnesia:read 'cbcl-webfinger-withdrawn uri 'write)` — keyed on **the alias URI
  being provisioned**, never on the account. `:60` confirms
  `cbcl-webfinger-withdrawn` is keyed on the alias; `:246-251` records in
  capitals that "ONE ACCOUNT CAN HOLD SEVERAL TOMBSTONES" precisely because the
  key is the URI and the URI is a function of the DID.
- `v045:163-166` (`REQ-958`, adopted): the terminal fires only "If **the
  recomputed issuer URI** is tombstoned". `v046:112-113` (`REQ-964`) is the same
  scoping: "the hub obtains the exact computed alias-key write lock and checks
  **the applicable** withdrawal row."
- `cbcl-chat-webfinger-store.lfe:206-227` (`tombstone`): `withdraw/2` deletes the
  alias and account rows and writes the tombstone at `uri1`.
  `:302-308` (`lift-tombstone`): `reinstate/2` deletes the tombstone at `uri1`.

**Failure trace.**

1. Operator runs `withdraw(account, did1)`. State: no `cbcl-webfinger-account`
   row for the account, no `cbcl-webfinger-alias` row at `uri1`, one tombstone at
   `uri1`.
2. The account is already unbound at this point. A v2 ceremony recomputes
   `uri2 = f(did2) ≠ uri1`. `REQ-964`'s check reads
   `cbcl-webfinger-withdrawn` at `uri2` — absent. `create-or-return:141` reads
   the same key — absent. **The v2 ceremony already succeeds.** The withdrawal
   block `REQ-965:140` says the reinstate exists to remove was never in the way.
3. Following `REQ-965` anyway, the operator runs `reinstate(account, did1)`,
   which deletes the only durable record that `did1` was withdrawn. A later
   `provision(account, uri1, did1)` would now pass `create-or-return:141-146`
   instead of returning `#(error withdrawn)`.
4. `TEST-1069:418-419` rehearses the sequence and asserts the end state
   ("verify unbound state, then complete a fresh v2 ceremony"), which holds with
   or without step 3, so the release-blocking test cannot detect that the step
   is both unnecessary and destructive.

The blast radius is bounded today — `provision/3` has no production caller
(verified: every reference outside `apps/cbcl_chat/test/**` is the definition
and export), and a v2-bound account would be refused a v1 re-provision by
`create-or-return-live:151-171`'s reciprocal-conflict rule anyway. But the
amendment's own `ADR-937:359-364` states the principle this violates —
"Withdrawal is an operator authority boundary… Excluding tombstones from capture
and restore preserves concurrent operator decisions" — and then the sole
normative operator runbook in the same document deletes one on a false premise.

**Minimum correction.** Delete `reinstate/2` from `REQ-965:139-140` and replace
the rationale with the true one: the v1 tombstone is keyed on the v1 alias URI,
the v2 ceremony provisions a different URI derived from a different home DID, so
the tombstone neither blocks the transition nor needs lifting and SHALL be left
in force as the durable record of the withdrawn v1 generation. If a same-DID
repair is separately intended, state it as its own case with its own
authorization. Rewrite `TEST-1069`'s rehearsal to assert that the v1 tombstone
is **still present** after a successful v2 ceremony.

---

### M-3 (MEDIUM) — Every claim about durable withdrawal authority, durable pending compensation and durable stable-account state is made over a `cbcl-bus` Mnesia store whose own configuration records that it is redeploy-durable only, with no off-machine replica; the amendment gates new ceremonies on did-crdt `durable-ready` while asserting nothing about the durability class of the store that actually holds the authority

**Exact candidate clauses.**

`v046:104-111` (`REQ-964`): "Every `cbcl-webfinger-withdrawn` row is read-only to
every credential/v2 command in both the forward and compensation directions…
**Their completed result wins**: a concurrent withdrawal blocks finalization; a
concurrent reinstatement remains lifted after any later compensation."

`v046:265-267` (`CON-953`): "Any mismatch preserves the intervening writer and
**leaves compensation pending**." `v045:355-358`/`v046:255-256` put the pending
compensation record in a Mnesia table (`cbcl-selfsame-compensation`).

`v046:150-155` (`REQ-966`), by contrast, for the *other* service:
"`durable-ready` must remain true **continuously**, not only at profile
generation. Loss of the storage adapter or read-back integrity makes health
fail, **blocks new v2 allocation/offer preparation**, and returns a closed
service failure for writes." `v046:439-445` (`TEST-1072`) makes that a
release-blocking runtime gate.

**Governing evidence, `cbcl-bus` at `fdbe414`.**

- `apps/cbcl_chat/src/shell/cbcl-chat-mnesia.lfe:10-13`: `storage-kind/0` returns
  `'disc_copies` on a named node. Every table in `CON-953` is created with that
  kind (`cbcl-chat-webfinger-store.lfe:80-84`,
  `cbcl-chat-account-grants.lfe:43-63`, `cbcl-chat-pairaccount.lfe:29-38`,
  `cbcl-chat-members.lfe:16-23`).
- `config/router.sys.config:47-58`, in the deployment's own words:

  > "SPEC-021 durability: keep the Mnesia control plane… on the durable /data
  > volume… **NOTE: redeploy-durable, not off-machine DR — a lost machine still
  > loses this (Litestream replicates only the SQLite stores).**"

  `{mnesia, [{dir, "/data/mnesia"}]}`. `fly.toml:38-45` shows only
  `CBCL_ROUTER_DB` and `CBCL_CHAT_DB` (SQLite) are the Litestream-replicated
  stores; `fly.toml:55-60` confirms the volume "is a read-latency optimisation,
  NOT the durability mechanism — a redeploy may land the machine elsewhere; the
  off-machine Litestream replica (ADR-002) is what makes history survive machine
  replacement."
- `fly.toml:22-31` is a hard single-machine invariant, so there is no second
  copy anywhere.

**Failure trace.**

1. Production allocation is eventually enabled and people complete v2
   ceremonies. `cbcl-webfinger-alias`/`-account` rows and, over time,
   `cbcl-webfinger-withdrawn` tombstones and `cbcl-selfsame-compensation`
   pending rows accumulate in `/data/mnesia`.
2. The Fly machine is lost (host failure, region incident, volume loss — the
   exact event `router.sys.config:55` says is unprotected). The SQLite stores
   come back from Litestream; **the Mnesia control plane does not**.
3. Every tombstone is gone. `create-or-return:141-146` no longer refuses a
   withdrawn alias, so `REQ-964:109-111`'s "their completed result wins" and
   `ADR-937:363-364`'s "makes 'ceremony cannot withdraw or reinstate' true in
   both directions" are false in the one direction that matters — the operator's
   revocation simply ceases to exist, with no signal.
4. Every pending compensation record is gone, so `CON-953:265-267`'s
   "leaves compensation pending" resolves to "compensation never happens", and
   `REQ-960`'s fourth-custody-operation ledger has nothing to point at.
5. Every reciprocal binding is gone, so `CON-206` step 9 fails closed for every
   issued grant — fail-closed and therefore not the security problem, but it
   silently un-links every person and the amendment never says so.

`REQ-966:150-155` shows the design knows how to state this obligation and how to
gate on it. Applying that standard to did-crdt and not to the store that holds
the account authority, the device grants and the revocation tombstones is the
asymmetry.

**Minimum correction.** Two sentences and one gate row. In `CON-953`, state the
hub authority store's actual durability class — single Fly volume at
`/data/mnesia`, redeploy-durable, no off-machine replica, unlike the
Litestream-replicated SQLite stores — and name the three consequences
(tombstone loss is fail-open, pending compensation loss is unrecoverable,
binding loss is fail-closed). Then either add a hub-side precondition to the
allocation gate symmetric to `REQ-966` (an off-machine replica or export for the
`cbcl-selfsame-*` and `cbcl-webfinger-*` tables before allocation may enable),
or move it verbatim into `v046:502-512`'s residual list as an accepted,
owner-visible production hold. Do not leave `REQ-964`/`ADR-937` asserting
permanence the deployment does not provide.

---

### L-1 (LOW) — The cutover-v1 descriptor grammar is pinned by pointer to two documents that give the member set but not the ranges, while the v2 branch of the same member is pinned to an exact table

`v046:196-200` (`CON-951`): "The cutover-v1 `cbclPairingRelays` nested grammar is
exactly `SPEC-007 CON-806` and cbcl-bus `SPEC-075 REQ-007`: one to sixteen closed
descriptors, with the existing operator/origin/digest/priority/weight checks."
`v046:200-202`: "The v2 branch requires `profileVersion = 2` and **all 0.4.4
`CON-933` nested rules**."

`SPEC-007:650-652` gives the six member names, uniqueness and "numeric priority,
numeric weight"; `:657-658` adds "rejects unknown members, duplicate members,
invalid digests, zero weight, and non-canonical origins".
`cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md:229-236` gives the same
six names. Neither gives `operatorId`'s grammar, the numeric ranges, or the
digest encoding. `v044:659-668` (`CON-933`) does:
`operatorId ASCII [a-z0-9][a-z0-9-]{0,62}, 1..63 octets`, `priority uint
0..65535`, `weight uint 1..65535`, both digests `base64url-no-pad SHA-256`.
`SPEC-007 CON-806:646-648` in fact points *back* — "the coordinated SPEC-004
amendment defines `cbclPairingRelays`" — so the cutover-v1 pointer closes a loop
rather than resolving it.

Practically the two readings coincide, because
`crates/selfsame-app-identity/src/profile.rs:823-890` already implements
`CON-933`'s exact table (`provider_id` at `:846`, `bounded_range` at `:854-855`,
positive weight at `:856-857`, `decode_b64url_32` at `:859-870`, uniqueness at
`:880-890`) and `CON-951:208-211` requires the live fixture and old wallet
fixtures to keep passing. But two stacks built from `CON-806` alone would
legitimately differ on `priority: 100000` or `operatorId: "Anuna_1"`, which is
what `CON-226`'s two-stack rule exists to catch. Say that the cutover-v1
`cbclPairingRelays` nested grammar is the same `CON-933` descriptor table as the
v2 branch.

### L-2 (LOW) — `CON-951` gives the two v1 branches no selection rule and no refusal identity for the shapes it says must refuse, which `CON-226` makes normative

`v046:203-206`: "The two v1 member sets are disjoint by `rendezvous` versus
`cbclPairingRelays`. **Both, neither**, `pairingRecordRelays` on cutover-v1, an
unknown member at any depth, or any nested mismatch refuses."
`v046:379-387` (`TEST-1065`) requires "refusal" without naming which.

`SPEC-004:4643,4652-4658` (`CON-226`): "Requiring the reason, not merely a failure, is
the point: **two stacks must agree on which check fired**, or they have not
implemented the same predicate", and cases carry `expect.reject` naming a closed
token or `con_<nnn>_step_<n>`. Today's recogniser answers with
`ProfileError::UnknownMember(String)` / `MissingMember(String)` /
`BadValue{path, reason}` (`profile.rs:104-119`), so "neither relay member
present" yields `MissingMember("rendezvous")` under one branch order and
`MissingMember("cbclPairingRelays")` under the other, and "both present" yields
the symmetric pair of `UnknownMember`s. `CON-951` supplies no rule for which
branch a version-1 document is attributed to before it fails, so the `path` two
stacks report is undetermined. Add the attribution rule, or a distinct
version-1-branch refusal token, and put the four ambiguous shapes in the
`CON-226` group-2 cases rather than only in `TEST-1065`.

### L-3 (LOW) — `CON-951`'s rendezvous-v1 branch has no stated semantic action in a SPEC-007-conforming release, and its only actionable content names the two engines `REQ-806` forbids

`v046:187-194` requires the recogniser to accept a version-1 profile whose
`rendezvous` descriptors carry, per `SPEC-004:2554-2560`, `protocol:
"selfsame-rendezvous-v1"`, `pairingProtocol: "selfsame-pairing-v1"`,
`pairingUrl` and `pairingRoute`. `SPEC-007:251-252` (`REQ-806`, an inherited hard
stop) forbids a Selfsame release to "advertise, negotiate, select, or fall back
to `selfsame-rendezvous-v1`, `selfsame-pairing-v1`, SSE1, or any
non-`cbcl-pairing` engine", and `:266-268` (`REQ-807`) says "The shell SHALL
reject every legacy carrier and **descriptor**". `v046:79-82` (`REQ-962`)
routes credential/v1 relay selection to the cutover-v1 branch only, so a
recognised rendezvous-v1 profile yields zero eligible `cbclPairingRelays`
descriptors and no ceremony can start. That is the right outcome and it is
nowhere written. Recognition is not selection, so there is no contradiction —
but add the sentence: a recognised rendezvous-v1 profile provides no
`cbclPairingRelays`, therefore no allocation, and recognising it never
advertises, negotiates or selects a legacy engine. `TEST-1065`'s rendezvous-v1
case should assert the refusal-to-allocate, not only the recognition.

### L-4 (LOW) — `CON-018`'s control arm admits a `logicalLength` that adopted `CON-015` forbids, so `CON-955` item 7's "no prose-only `logicalLength` constraint remains" is not yet true

`companion041:152-171` (`CON-018`) gives the control arm `3: 1..4096` beside
`4: bstr .size 4096`. `companion040:338` (`CON-015`, adopted and not
replaced) says "**The complete control is at most 2,048 octets** and depth 8" —
which can only mean the logical body, since the encoded object is 4,142–4,144
octets. `companion041:227-234` (`TEST-050`) and `v046:447-452` (`TEST-1073`)
then require a control witness at `logicalLength = 4,096`, which `CON-015`
forbids. No maximum moves — 2,048 still needs a three-octet uint, so the control
class still tops out at 4,144 and every derived plaintext/ciphertext/relay
figure is unaffected — but the grammar and the prose disagree about the
2,049..4,096 range, which is exactly the class `L-6` of the prior review raised
and `v046:328-330` declares closed. Either tighten the control arm to
`3: 1..2048` (and move the third size row to `256..2,048`), or state in
`CON-015` that the 2,048 figure is an application guidance and the grammar is
authoritative.

### L-5 (LOW) — `CON-952`'s "a later operation" leaves the non-crash restore and new-root paths ambiguous

`v046:222-229`: "Startup never writes a new or restored root. The person must
explicitly retry restore or new-root creation after recovery… **A new root is
written only in a later operation** after the marker and all old state are
absent." Read as written, the running `restore` operation may not write the root
it just purged for, making every restore a two-step user flow; 0.4.5
`CON-944:299-301`, which this paragraph replaces, said the opposite ("only then
may it write a new/restored root" — same operation). Both readings preserve the
security property (`v045:99-103`: no new root sees stale policy), because in
either the root write is strictly ordered after verified marker absence, and I
confirmed the mechanism against `src-tauri/src/custody.rs:190-193` (`restore`
refuses while an identity exists) and `:207-224` (a fresh 16-octet Argon2id salt
per seal, so no old slot can open under a new root). Say whether the purging
operation itself may complete the write, and reserve "a later operation" for the
crash-recovery case `TEST-1067:399-403` actually tests.

### L-6 (LOW) — `CON-951` requires "the generator" to pin a rendezvous-v1 positive fixture; the only generator in the coordinated set hard-refuses any object containing `rendezvous`

`v046:208-211`: "**The generator and corpus** SHALL pin three positive fixtures:
the parent rendezvous-v1 fixture, the exact live cutover-v1 octets and digest,
and the distinct v2 fixture/digest." `cbcl-bus/scripts/gen-production-profile.mjs:306-320`
walks every key at every depth and pushes a hard error —
"legacy profile member `rendezvous` is prohibited by SPEC-075 REQ-007" — and
`v046:9` moves `SPEC-075 REQ-007` to `conforms-to`, leaving that check
unamended. Selfsame has no profile generator (`scripts/` holds only
`check-spec008-fixture-absence.sh` and the PROTO-003 vector validators; the
corpus is `test-vectors/spec-004-v1.json`). Name which artefact carries which
fixture: the rendezvous-v1 positive belongs in the Selfsame recogniser corpus,
not in the cbcl-bus deployment generator, whose refusal is correct and must
stay.

### L-7 (LOW) — 0.4.6 amends `CON-201` version 1 a second time while the parent's standing justification for in-place version-1 amendment is no longer true at this baseline, and `CON-955` does not record that

`SPEC-004:2320-2326` (`ADR-224` rationale) justifies amending profile version 1
in place because "there are none of either: … **no ratified production profile
has ever been published by any origin**. The recogniser and the first ratified
profile ship together from pinned builds in this repository and cbcl-bus", and
`SPEC-004:6711-6712` repeats it. At `fdbe414` that is false:
`cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-gate.lfe:26`
has `routed?/0` return `'true`, `:16-21` records the octets as "the exact RFC
8785 canonical document minted 2026-08-22… recognised by the vendored Selfsame
WASM at pin 17b0bf5", and `cbcl-chat-app.lfe:221-223` compiles the route in. I
decoded the served bytes: 1,235 octets, `SHA-256` base64url-no-pad
`asBvYnvPMHVVU8zkFaFoz_FJg1DaZGSHQ5fxKxC8SZk`, top-level member set exactly
`accountAuthority, allowedPermissions, applicationId, cbclPairingRelays,
enrollment, profileVersion, revocation, stateResolvers, verifierAudience`.
0.4.6 does the compatibility work correctly — that is precisely why `H-1` of the
prior review is closed — but it does not retire the premise, so the next
amendment inherits "version 1 has zero compatibility surface" as if it were
still true. One `CON-955` row.

---

## Disposition of every 0.4.5 finding

Verified against the 0.4.6 and companion 0.4.1 text **and** against the code, not
against the candidate's own disposition table.

| 0.4.5 finding | 0.4.6 / companion claim | Verified disposition |
|---|---|---|
| **H-1** — the mandated dispatcher's version-1 branch provably rejects the one production profile also mandated frozen; credential/v1 left with no recognisable profile; SPEC-007 CON-806 and SPEC-075 REQ-007 in permanent contradiction with SPEC-004 CON-201 v1 | `REQ-962`/`CON-951` explicitly amend CON-201 v1 with two disjoint closed member-set branches | **CLOSED, and closed by the review's option (a).** `v046:64-66` reverses `v044:637` in as many words: "`SPEC-004 CON-201` version 1 is explicitly amended by `CON-951`." I decoded the live octets and the cutover-v1 row at `v046:192` is their exact nine-name member set. That row is also *byte-for-byte today's recogniser*: `profile.rs:80-91` `TOP_LEVEL_REQUIRED` is those nine names in that order with `TOP_LEVEL_OPTIONAL` empty, and `:384-390` pins `profileVersion == 1` — which is why `v046:84-87`'s client-rollback claim ("the current pre-dispatcher wallet baseline already recognizes the frozen live bytes") is true, and why `TEST-1066`/`CON-955` item 6 close the client half the prior review said was unaddressed. `v046:79-83` deletes `v044:697-701` (credential/v1 accepting a v2 profile), so `CON-806` step 1 now has one conforming profile at chat.anuna.io. `SPEC-075 REQ-007` moves to `conforms-to` (`v046:9`), which is right: its required shape is admitted unchanged. Residues are **L-1**, **L-2**, **L-3**, **L-6**, **L-7** — none of them reopens the finding. |
| **H-2** — credential/v2 reverses cbcl-pairing `CON-007` outside every amendment channel | `REQ-963` adopts companion 0.4.1, whose `REQ-023`/`CON-017` amend CON-007 explicitly | **CLOSED, item for item.** `companion041:8` `amends: CON-007`. `companion041:46` scopes the parent table to `anuna.io/credential/v1` and states the exact substance of the change (secret out of the machine carrier, typed presence field, second independent relay-claim bearer, two decisions, widened display). `companion041:45` and `:61` add the `CON-006`-retained and `CON-008`-retained rows the review asked for. `companion041:101-117` (`CON-017`) is a full six-row profile in the same shape as the parent's `SPEC-001:889-926` `CON-006`/`CON-007`, so the parent's profile catalogue is no longer left with two entries one of which contradicts the design. `companion041:209-216` (`TEST-048`) is the requested cross-version row in both directions ("Remove the v1 carrier secret and require v1 refusal. Add `secret`, `C`, `T`, PAIR1 text… and require v2 refusal before CPace"). `companion041:236-241` names CON-007 in the approval section and keeps owner direction subordinate to a fresh PASS. `companion041:78-97` (`NFR-015`) puts the profile table into the hashed asset manifest and makes the compatibility report name CON-006/007/008 separately — the "compatibility disposition" `SPEC-001:1486-1496` requires. |
| **M-1** — `CON-944`'s reset crash rule asserts a "remaining valid old slot" the precondition makes unreachable; `TEST-1052` unauthorable | `CON-952` requires ordinary read refusal while the remaining slot is invalid and retry to completion | **CLOSED, exactly as the correction asked.** `v046:215-219`: "A crash after the first deletion leaves the other slot **present and invalid**. Ordinary policy read still refuses; the gated reset path remains available and retry finishes deletion." `TEST-1067:396-399` restates it as a reachable fixture. Verified implementable against `src-tauri/src/store.rs:207-219`, where `get`/`set`/`delete` are plain backend operations with no presence edge, so a refusing read and an available gated reset can coexist. The equal-revision half of L-4 is carried unchanged at `v046:231-232`. |
| **M-2** — revisionless rollback can restore an operator-lifted tombstone | `REQ-964`/`CON-953` make withdrawal rows read-only in both directions and use exact locked-byte comparison for revisionless rows | **CLOSED.** `v046:106-108`: "The ceremony never captures a withdrawal row for restoration and never creates, deletes, lifts, replaces, or rewrites one." `v046:118-122` and `v046:263-267` split the conflict test exactly as asked — exact captured bytes against a write-locked re-read for revisionless legacy rows, revision comparison only for the additive side tables that carry one — and put the withdrawal row outside both comparisons and both write sets. I re-traced the concurrency against the real store: `cbcl-chat-webfinger-store.lfe:206-227` `withdraw/2` takes the account-key write lock, then the alias-key write lock via `alias-agrees?:229-235`, then writes the tombstone at the URI (a write lock on that oid); `:302-308` `reinstate/2` deletes at the same oid. Both therefore serialise against `REQ-964:112`'s alias-key write lock, and Mnesia's absent-key write locks — which this very module relies on at `:121-123` — make the ceremony's pre-pending check sound. I constructed the compensation-after-completed-withdrawal case and it converges correctly: captured absent/absent, re-read absent/absent, byte comparison passes, compensation is a no-op on those rows, tombstone untouched. The 0.4.5 `cbcl-selfsame-withdrawal-index` is dropped by `CON-953`'s wholesale table replacement, which is the right consequence of read-only tombstones. **M-3 above** is a *different* defect in the operator runbook, not a reopening of this one. |
| **M-3** — distinct ApplicationId makes v1/v2 account bindings mutually exclusive; stated nowhere | `REQ-965` states the derived DID/scope/alias change, the one-binding limit, preflight, refusal and operator transition | **CLOSED on the substance; the transition procedure is defective — M-2 above.** `v046:126-129` states every derived divergence and I verified each: `hierarchy.rs:267-269`/`:272-274`/`:276-282`/`:178-182` and the crate's own `:400-406` test for the ApplicationId sensitivity; `cbcl-core-webfinger.lfe:35-40` for the alias; `v044:307-309` for the CON-211 scope. `v046:129-131`'s one-binding claim matches `cbcl-chat-webfinger-store.lfe:54` + `:76-79` (`type set`, keyed on `account`, one alias and one home DID). `v046:132-136` states the empty-today fact and correctly refuses to rely on it ("audit actual Mnesia state rather than infer emptiness from code") — I re-confirmed `provision/3` has callers only under `apps/cbcl_chat/test/**`. `TEST-1069` covers the refusal, the divergence and the preflight. |
| **M-4** — did-crdt gossip bypasses `CON-950`'s per-DID lock | `REQ-966`/`CON-954` serialize every HTTP, gossip, sync and bootstrap writer through one durable mutation primitive | **CLOSED, and the writer enumeration is complete against the code.** I enumerated every mutation site at `1f409a4`: `service/handlers.rs:99` (create insert), `:291-292` (delta `get_mut` + merge), `sync/live.rs:351-352` (`merge_inbound`), `sync/gossip.rs:193` and `:479` (bootstrap reconstruct and merge, both reached only through `merge_inbound`), and `service/server.rs:209-211` (startup `load_all` recovery, which runs before the router serves and before the node joins the mesh). `resolve_did`'s `cold_start_bootstrap` (`handlers.rs:191-197`) is **not** an independent writer — `live.rs:261-265` only polls `docs` until the sync loop inserts — so `CON-954:274-277`'s list is exhaustive. The primitive composes: every operation touches exactly one DID, so there is no per-DID lock cycle, and the inner `std::sync::Mutex` (`lib.rs:32-38`) is taken only for the clone and the swap, never across an await, preserving the existing `CON-005` rule. `CON-954:285-289`'s crash semantics match `sync/store.rs:142-161`, where `set_tag(did_tag(did), hash)` is the per-DID commit point and is last-writer-wins — which is exactly why the lock was needed. `CON-954:295-297` correctly separates the reusable library's permissive `load_all` (`store.rs:182-215`, skip-and-continue) from production strict mode. `v046:148-153` states the single-Fly-machine premise honestly and `TEST-1072:444-445` gates scaling on a reviewed cross-process mechanism; `fly.toml:31-45` confirms one volume, `auto_stop_machines = "off"`, `min_machines_running = 1`. The health endpoint today reports `storage: "durable"` from `persistence.is_some()` alone (`handlers.rs:402-412`), so `CON-954:298-300`'s continuous readiness is real new work, correctly scoped. |
| **L-1** — `NFR-907` adds a five-byte head that never occurs | `NFR-908` gives six exact cases and adopts companion `CON-018` | **CLOSED, and I recomputed all six independently.** Deterministic CBOR, five-entry map: 1 (map head) + 2 (`0:2`) + 2 (`1:kind`) + 35 (`2:` + 32-octet bstr with a two-octet head) + the `3:` pair + 1 + 3 + body. Control: 4,142 / 4,143 / 4,144 at `logicalLength` 1..23 / 24..255 / 256..4,096. Large: 64,558 / 64,559 / 64,560. `v046:172-173`'s "the field-4 byte-string head is always three octets for both fixed application classes" is right (4,096 and 64,512 are both in 256..65,535), and `v046:174-175`'s "five-octet head only on the outer 66,644-octet ciphertext" is right (66,644 > 65,535). `companion041:181-190` states the identical table, so the two documents cannot drift. |
| **L-2** — `TEST-1030`'s "every control type encodes to 4,144" is false below 256 | `CON-955` item 5 replaces it with `NFR-908` | **CLOSED.** `v046:325-326`. `TEST-1073:447-452` gives the three control witnesses (1, 23, 24, 255, 256, 4,096) the prior review asked for, and `companion041:227-234` (`TEST-050`) mirrors them. Every derived maximum is unaffected because the class maxima (4,144 / 64,560) did not move — I re-derived 66,628 / 66,644 / 66,696 and 6,212 / 6,228 / 6,278 from `v044:597-606` and they are exact. |
| **L-3** — `CON-934`'s carrier field list never restated with `relay-expires-at` | `CON-955` item 1 makes companion `CON-013` the sole carrier grammar | **CLOSED.** `v046:305-309`. Verified that `companion040:178-190` is `CON-934`'s ten fields plus `"relay-expires-at": uint`, so declaring `CON-934` illustrative removes the two-digest hazard rather than papering over it. |
| **L-4** — `CON-013` puts an allocator construction rule in the claimant's recognition rules | `companion041 CON-017` splits the duties by what each role observes | **CLOSED, and correctly split.** `companion041:119-124` puts the exact-copy obligation on the allocator ("SHALL construct the exact CON-013 carrier only after a successful adopted `allocated-v2` response, and SHALL copy that response's exact `expiresAt`"). `companion041:125-131` gives the claimant only checks it can perform — canonical encoding, closed members, direct locator, absence of nameplates and secrets, local time and ceremony cap — and then names the two indirect authentications that actually detect substitution: `carrierDigest` at Finished and signed-offer equality. `:137-138` states plainly that the claimant "SHALL NOT claim to compare the carrier to a relay response it never received". |
| **L-5** — `CON-944`'s "completes the deletions" ambiguous about the old root; `TEST-1050`'s convergence set incomplete | `CON-952` defines one recovery result | **CLOSED.** `v046:221-226`: "recovery has exactly one result: no root and no safety state", with an explicit deletion order ending at the marker and an explicit statement that "Startup never writes a new or restored root". `TEST-1067:399-403` matches. Verified consistent with `custody.rs:190-193` and `:207-224`. **L-5 above** is a narrower residue about the non-crash path only. |
| **L-6** — `CON-014`'s CDDL admits a `logicalLength` its prose forbids | `companion041 CON-018` replaces the arm with two disjoint arms | **CLOSED for the large class, residue on the control class — L-4 above.** `companion041:152-171` gives `control {1: 1..8 / 10, 3: 1..4096, 4: bstr .size 4096}` and `large {1: 0 / 9, 3: 1..62000, 4: bstr .size 64512}`. The kind partition is exact and total against `companion040:276-281`: {0 offer, 9 payload} large, {1..8, 10} control, union {0..10}. The large arm's `1..62000` is now grammar-enforced against the 64,512 body and matches `SPEC-007:945-949` (`TEST-818`)'s 62,000 credential bound. `companion041:172-178` states the disjointness rule the review asked for. |
| **L-7** — three superseded sentences survive in the adopted 0.4.4 text | `CON-955` records every inline replacement | **CLOSED for all three.** `v046:314-316` (deadline table), `:317-318` (CON-939 migration attribution), `:319-321` (legacy-key table and the `cbcl-chat-pairaccount`/handle-keyed-account errors). `CON-955` goes further than asked, adding seven more rows. One un-listed stale sentence remains — `v044:637` "CON-201 profile version 1 is not amended" — but `v046:64-66` contradicts it explicitly by name in a superseding document, so the effective set is not ambiguous; a ledger row would still be tidier. |
| **L-8** — `SPEC-075 REQ-007` claimed as amended after the only clause changing it was removed | Moved to `conforms-to` | **CLOSED.** `v046:9` lists "cbcl-bus SPEC-075 REQ-007 unchanged" under `conforms-to`; `v046:8` no longer names it under `amends:`; `v046:59-60` explains why ("its required live cutover shape is admitted by CON-951 without changing its bytes or contract"). I verified that both the frozen v1 bytes and a conforming v2 output satisfy `SPEC-075:229-236` unchanged. |

---

## Disposition of every 0.4.4 finding, as carried by the 0.4.5 review

I re-checked each against 0.4.6 rather than assuming the prior PASS survives the
new amendment.

| 0.4.4 finding | Carried state at 0.4.5 | State at 0.4.6 |
|---|---|---|
| **H-1** — six cbcl-pairing contracts amended in substance, dispositioned nowhere | CLOSED for the six named | **Still closed, and the seventh (`CON-007`) is now closed too.** I re-verified the six against the code at `8e55160`: `src/context.rs:7-9`'s v1 domains are byte-disjoint from `companion040:207-247`'s `/credential-v2` variants; `src/channel.rs:115-147`'s `pairing-v1 …` labels are disjoint from `pairing-credential-v2 …` and no two `label‖TH` infos can collide (every label differs in length or content at a fixed 64-octet tail); `channel.rs:114` is `HKDF-Extract-SHA512("", ISK)`; `channel.rs:364-376` is `TH = SHA-512(cbor([bstr(pc),bstr(a),bstr(b)]))`; `channel.rs:341-362`'s AAD map is `{v,direction,counter,th}` with `v = 1`, so the v2 `v = 2` map is disjoint. |
| **H-2** — `intentDigest` undefined | CLOSED | **Still closed.** `v045:250-252` and `companion040:288-292` are byte-identical formulas; `companion040:294-296` reconciles `offerDigest` with `SPEC-004:3987-3990`. No fixed point: field 2 is an envelope member and the offer's field-4 body contains no field 2. Unaffected by 0.4.6. |
| **M-1** — same-device restore strands stale policy | CLOSED | **Still closed and improved.** `CON-952` replaces the recovery paragraph with a single result. |
| **M-2** — origin ledger has no repair | CLOSED | **Still closed.** `v045:306-314` is unreplaced by 0.4.6 and its no-custody-edge claim holds against `store.rs:207-219`. |
| **M-3** — `CON-209` hint changes without amendment authority | CLOSED | **Closed for authority; a preimage gap surfaces — M-1 above.** `CON-209` is on `v046:8` and in the approval list. The seven members and their 32/32/32/64-octet decodings are pinned. What is not pinned is what `descriptorDigest` hashes. |
| **M-4** — `CON-226` named in no requirement, slice, test or gate | CLOSED | **Still closed; the v1-branch cases are a residue — L-2 above.** `v045:114-130` (`REQ-956`) is unreplaced and `v046:208-211` adds the three positive profile fixtures. |
| **M-5** — three outward-facing one-way changes, no rollback ordering | PARTIALLY CLOSED; profile half replaced by H-1 | **Now fully closed.** Store half: `CON-953` keeps every legacy arity and key and separates idempotent schema creation from all-or-none per-account population. did-crdt half: `CON-949:437-444` forward-fix boundary retained, and `CON-954` strengthens it. Profile half: `REQ-962` + `CON-955` item 6 give both directions — server bytes never move, client rollback is an explicit release rule with `TEST-1066`. |
| **M-6** — a ceremony can lift a withdrawal tombstone | CLOSED, residue in CON-941's rollback | **Residue now closed by `REQ-964`/`ADR-937`.** See the 0.4.5 M-2 row. |
| **L-1** — table/module and handle/account confusion | CLOSED, one clause outstanding | **Outstanding clause now stated.** `v046:132-133`: "At the reviewed code baseline, `provision/3` has no production caller." Re-verified. |
| **L-2** — repeated offer prepare undefined | CLOSED | **Still closed.** `v045:428-433` unreplaced. |
| **L-3** — migration attributed to the wrong contract | CLOSED in new text; stale sentence survived | **Both halves closed.** `CON-955` item 3. |
| **L-4** — reset revision and equal-slot edge | PARTIALLY CLOSED → 0.4.5 M-1 | **Closed.** `CON-952`. |
| **L-5** — idle budgets exceed 600 s | CLOSED | **Still closed.** I re-added `v045:410-420`'s deltas: 180+30+30+90+45+60+75+45+35 = 590, strictly inside the relay maximum. `src/mailbox.rs:7-15` confirms `DEFAULT_TTL_SECONDS = MAX_TTL_SECONDS = 600`, so a `ttlSeconds: 600` allocation really yields the window the table assumes. `CON-955` item 2 removes the last competing sentence. |
| **L-6** — `mailbox-store-v2` CDDL references undefined rules | CLOSED and exact | **Still closed.** `companion040:360-379` against `src/storage.rs:196-241` and `src/mailbox.rs:13`, `:389-398`, `:571-577`. Unaffected by 0.4.1. |
| **L-7** — `NFR-906`'s component list omits the three-byte pair | PARTIALLY CLOSED → 0.4.5 L-1/L-2 | **Closed.** `NFR-908` + `CON-018`. |
| **L-8** — "allocation time" ambiguous | CLOSED; two residues | **Both residues closed.** `CON-955` item 1 (carrier list) and `companion041 CON-017` (misplaced rule). |
| **L-9** — parent REQ-903/904 and REQ-810 absent | CLOSED | **Still closed.** `v045:88-93` and `:154-159` unreplaced. |
| **L-10** — two inconsistent domain separations | CLOSED | **Still closed, and I re-checked uniformity.** All five v2 preimages are NUL-terminated with fixed-length raw-octet tails or length-prefixed CBOR: `intentDigest` (`companion040:288-292`), device proof (`v044:359-366`, `offerCoreDigest‖transcriptHash`), claim commitment (`v044:221-226`, `M‖T`), PAIR1 checksum (`v045:275-279`, `C‖T`), account principal (`v045:266-272`, canonical CBOR). None is ambiguous. |

**0.4.3, 0.4.2 and 0.4.1 findings** remain closed. I re-checked the two carried
as substantive: the v2 nine-name grammar at `v044:641-651` is still byte-for-byte
`profile.rs:80-91` in the same order, and the PAIR1 bit arithmetic
(`v044:726-733`: 128 + 128 + 16 + 3 = 275 = 55 × 5, eleven groups of five
Crockford Base32 characters) is unchanged and correct.

---

## Inherited-hard-stop audit

| Stop | Owner | Hard stop? | 0.4.6 position | Verified |
|---|---|---|---|---|
| SPEC-008 REQ-902 | SPEC-008 0.2.2 | yes | amended (0.4.4); fixture prohibition retained | OK. `TEST-1040` release-blocking and unchanged; `scripts/check-spec008-fixture-absence.sh` still present at the code baseline. |
| SPEC-008 REQ-905 | SPEC-008 0.2.2 | yes | amended; carrier non-navigable, no `C`/`T` | OK. Its carrier grammar is `CON-001`, explicitly amended at `companion040:8,61`; `companion041:59-62` re-states the no-secret rule for v2. |
| SPEC-008 REQ-906 / 907 / 908 / CON-903 | SPEC-008 0.2.2 | yes / yes / no / — | replaced / retained / retained / retired | OK. `v046:507-512` keeps production allocation closed and denies deploy authority. |
| SPEC-008 REQ-903 / REQ-904 | SPEC-008 0.2.2 | no | recorded at `v045:88-93` | OK, unreplaced. |
| SPEC-007 REQ-803 | SPEC-007 | yes | retained | OK. `CON-206` still gates acceptance; `CON-955` item 9 amends only step 9's evidence source, not the predicate. |
| SPEC-007 REQ-804 | SPEC-007 | yes | amended for v2 | OK. "The exact recognised intent digest" is defined by `CON-943` and `companion040 CON-014`. |
| SPEC-007 REQ-805 | SPEC-007 | yes | narrowly amended | OK. `T` never enters CPace (`companion040:94-95`, restated `companion041:60-62`), is not persisted, is zeroized. |
| SPEC-007 REQ-806 / REQ-807 | SPEC-007 | yes | retained | **OK with a gap — L-3.** No negotiation and no cross-version recognition are preserved at the endpoint (`companion040:81-85`, `CON-016:388-391`, `TEST-038`, `companion041 TEST-048`). But `CON-951`'s new rendezvous-v1 branch recognises descriptors naming both forbidden engines without stating that recognition yields no selection. Recognition is not selection, so this is not a breach; it is an unstated consequence. |
| SPEC-007 REQ-809 / NFR-801 / production gate | SPEC-007 | yes | retained without exception | OK. `v046:490-495`, `:507-512`. |
| SPEC-007 REQ-810 | SPEC-007 | no | amended for this additive release | OK. `v045:154-159` unreplaced; `REQ-962` adds the missing client direction. |
| SPEC-007 REQ-811 / 812 / 813 / NFR-802 / CON-801 / CON-802 | SPEC-007 | yes / no | retained / amended | OK. |
| **SPEC-007 CON-806** | SPEC-007 | no | amended by `CON-933` and re-pointed by `REQ-962` | **NOW OK — this was the prior review's blocking row.** `v046:79-83` replaces step 1 for credential/v1 so it recognises the legacy ApplicationId under `CON-951` and selects from the cutover-v1 branch only. The live profile satisfies that branch, so `CON-806` is satisfiable at chat.anuna.io. Its one-operator availability exception remains an open production gate (`SPEC-007:654-655`, `SPEC-008:66`), correctly listed as a residual at `v046:503-506` and warned about by `gen-production-profile.mjs:322-324`. |
| SPEC-002 REQ-103 | SPEC-002 | — | conformed to | OK. |
| cbcl-pairing REQ-002 | SPEC-001 | yes | retained | OK. The relay compares and erases an opaque bearer; `companion041:47` re-affirms no application identifier reaches relay logging. |
| cbcl-pairing REQ-003 / REQ-005 | SPEC-001 | no / **yes** | amended by adopted 0.3.0 only | OK. `companion041:48` explicitly re-routes REQ-005: "This is a reviewed SPEC-001 amendment, not an implementation-side exception." |
| cbcl-pairing REQ-008 / 009 / 013 / 014 / 015 | SPEC-001 | yes | retained; REQ-009 two-stage | OK. |
| cbcl-pairing NFR-001 – NFR-006, NFR-009 – NFR-012 | SPEC-001 | no / yes | retained | OK. NFR-002's 16-frame per-membership cap holds with margin — see the bounds audit. |
| cbcl-pairing CON-001 / 002 / 004 / 005 | SPEC-001 | no | amended by `companion040` | OK. |
| cbcl-pairing CON-003 | SPEC-001 | no | amended by adopted 0.3.0 | OK. |
| **cbcl-pairing CON-006** | SPEC-001 | no | **retention row added** | **NOW OK.** `companion041:45`: retained byte-for-byte and behaviour-for-behaviour; the agent profile's relay, nameplate, two BIP-39 words, entropy floor, PRS, display and approval path are "not shared with credential/v2". |
| **cbcl-pairing CON-007** | SPEC-001 | no | **explicitly amended** | **NOW OK — this was the prior review's H-2.** `companion041:8`, `:57-60`, `:71-90`, `:105-138`, `:243-247`. |
| **cbcl-pairing CON-008** | SPEC-001 | no | **retention row added** | **NOW OK.** `companion041:47`: limiter dimension `{operation, PeerKey}` unchanged; no application identifier in relay logging, no scan, no unbounded work, no operator-key authority. |
| cbcl-pairing NFR-007/CON-009, NFR-008/CON-010 | SPEC-001 | no | amended by `NFR-014`/`CON-016`/`CON-015`, extended by `NFR-015` | OK. |
| cbcl-pairing production gates | SPEC-001 | yes | retained in full | OK. `companion041:240-241`. |
| **SPEC-004 CON-201 version 1** | SPEC-004 | Tier-1 | **explicitly amended** | **NOW OK — this was the prior review's H-1.** Two disjoint closed member sets, `v046:64-66`, `:182-211`. Residues L-1, L-2, L-3, L-7. |
| SPEC-004 CON-204 / 206 / 209 / 211 / 214 / 219 / 220 / 221 / 226 | SPEC-004 | Tier-1 | amended; owner approval required | OK as declared. `CON-206` step 9's v2 disposition (`v046:331-335`) genuinely amends the parent's "sole gate on alias provisioning" sentence at `SPEC-004:3144-3147` — the pending record caches a binding that does not yet exist — but it is named in `amends:`, recorded in the ledger, compensated by finalization-gated receipt, and declared as a residual at `v046:503-506`. Accepted as a declared residual, not a defect. **`CON-209`'s `descriptorDigest` preimage is M-1.** |
| SPEC-004 REQ-215 | SPEC-004 | — | conformed to | OK. |
| PROTO-004 CON-502 / CON-503 | PROTO-004 | — | conformed to, non-use recorded | **NOW OK.** `v046:326-330` (`CON-955` item 8) records the non-use in one sentence, which is what the prior review recommended. |
| did-crdt HTTP and sync service contracts | did-crdt | — | amended | OK. `CON-954` changes `POST /dids` semantics, adds CORS (today only `rendezvous.rs:47-61`), fails startup closed, and changes `BlobStore::load_all`'s documented skip-and-continue (`sync/store.rs:182-215`). `v046:294-297` now separates the library's permissive mode from the production service explicitly — the library-contract change the prior review said was uncalled-out. |
| cbcl-bus SPEC-075 REQ-007 | cbcl-bus | — | conformed to, not amended | OK. |
| Owner substitution | — | — | forbidden | OK. `v046:474-479` and `companion041:238-240` both state that owner direction cannot substitute for a fresh-context PASS. Respected by this review. |

---

## Amendment-authority audit

- **cbcl-pairing.** `SPEC-001:1486-1496` permits amendment only "through a
  reviewed, merged revision of this specification with updated assets, tests,
  evidence, and compatibility disposition", and expressly not by "implementation
  behaviour, test fixtures, or dependency updates". `companion030` +
  `companion040` + `companion041` are now that vehicle for **twelve** contracts:
  CON-001, CON-002, CON-003, CON-004, CON-005, CON-007, CON-009, CON-010,
  NFR-007, NFR-008, with CON-006 and CON-008 given explicit retention rows.
  `companion041:78-97` supplies the compatibility disposition the channel
  demands. The gap the prior review found is closed.
- **Selfsame SPEC-004.** `v046:8` names CON-201, 204, 206, 209, 211, 214, 219,
  220, 221, 226; `v046:499-501` puts the same list to its owners. `CON-201`
  **version 1** is now inside that authority rather than excluded from it. The
  parent's own precedent supports in-place version-1 amendment
  (`SPEC-004:2315-2330`), though that precedent's factual premise has expired —
  **L-7**.
- **Selfsame SPEC-007.** REQ-804, 805, 810, 812, 813, NFR-802, CON-801, CON-802,
  CON-806 named. CON-806 is now satisfiable.
- **cbcl-bus SPEC-075 REQ-007.** Correctly demoted to `conforms-to`.
- **did-crdt.** Named in `amends:`; the `load_all` library-contract change is now
  explicitly scoped.
- **Fully qualified local anchors.** Every `[[spec-008-0.4.6-standalone-tofu-first-contact#X]]`
  target exists as a heading in the file (REQ-962…966, NFR-908, CON-951…955),
  and the prefix-anchor convention matches SPEC-004's own
  (`### CON-201: …` ← `[[…#CON-201]]`). No dangling local anchor.

---

## State and effect-order audit

- **Pre-network.** Unchanged and sound: carrier recognition with no I/O, the
  explicit origin prompt, the burned contact capability, then the ledger.
  Verified again that `store.rs:207-219`'s `get`/`set`/`delete` carry no presence
  edge, so `CON-952`'s refusing read and available gated reset coexist without a
  custody call.
- **Policy-key lifetime.** `v045:203-207` unreplaced: one zeroizing closure,
  alive only while the new-pair prompt is visible, capped at 120 monotonic
  seconds, ended by socket open, cancel, timeout, backgrounding or any error.
  Expressible beside `custody.rs:312-331`'s `use_hierarchy_root`.
- **Custody counts.** Exactly three successful-path openings (policy access,
  issuer preview, final issuance commit) plus a fourth, separately explained
  operation only for post-payload `CON-210` compensation. Verified necessary:
  `custody.rs:312-322` requires passcode plus presence for every
  `use_hierarchy_root`.
- **Custody closure / local signing boundary.** `CON-955` item 10
  (`v046:336-339`) is a genuine and necessary correction, not cosmetic:
  `custody.rs:312-315` types the callback as
  `impl FnOnce(&HierarchyRoot) -> T` — a **synchronous** closure — so
  `v044:884-886`'s ordering, which put "publishes through CON-940" inside the
  same list as the signing, was never implementable. Moving publication after
  zeroization, from signed bytes, matches the API exactly.
- **Effect gating.** `CON-938`'s six-row effect table is unchanged and remains
  the unique gate: no derivation before preliminary approval, no signature,
  publication, issuance write or payload before the final decision. `TEST-1025`
  still requires a total call order after the final-decision hash.
- **CON-221 applicability.** Decided by the application from
  `selfsamepairauthority`; `Unknown` fails closed; `BoundDifferentDid` refuses.
  **H-1 shows a third real answer is missing from that enumeration.**
- **Acyclicity.** I rebuilt the graph and found no cycle:
  `carrier → carrierDigest`; `profile → profileDigest` (each side locally);
  `{carrierDigest, profileDigest, carrier fields} → CI/AD/publicContext → TH`;
  `OfferCoreV2` contains `carrierDigest`, `transcriptHash`, `profileDigest`,
  `descriptorDigest`, `applicationId`, `accountPrincipalDigest` and yields
  `offerCoreDigest`, whence `offerDigest` and `intentDigest`;
  `devicePossessionProof = Ed25519(SHA-256(domain ‖ offerCoreDigest ‖
  transcriptHash))` and is excluded from the core, so it cannot sign itself; hub
  `CON-214` evidence signs the core fields plus the proof; the seven-member hint
  carries `offerDigest`, `carrierDigest`, `transcriptHash` and
  `descriptorDigest` and is excluded from the core by `SPEC-004:3987-3994`; each
  successor body carries `predecessorDigest = SHA-256(prior canonical object)`.
  Field 2 is an envelope member and the offer's field-4 body contains no field 2.
  **One node of this graph — `descriptorDigest` — has no defined preimage: M-1.**
- **One authoritative ApplicationId.** `companion040:236-238` requires the
  recognised profile's `applicationId` to equal `application-context`, so the two
  independently computed profile digests must agree for Finished to agree.
  `companion041:114-118` adds the matching prohibition: "The table does not make
  a caller or wire string authenticated." `CON-943:258-262` builds
  `AuthenticatedIntent` only from typed fields in the verified core and evidence;
  `companion040:116-118` forbids the shared reducer constructing display text
  from a generic peer claim; `REQ-945`'s provenance enum has no boolean arm. No
  caller or peer string is promoted before verification.

---

## Authority and provenance audit

- **Profile authority.** The wallet authenticates the profile from the claimed
  application origin under `CON-220`; the hub embeds and recognises the exact
  profile it serves; neither accepts runtime-assembled JSON. `CON-951` keeps that
  property across all three branches.
- **Account authority.** `selfsamepairauthority` is the sole source; `Unknown`
  fails closed. The hub-signed pending record is authenticated and correlated,
  not a caller assertion. `applicationAccountDisplay` is labelled as an
  application-session assertion and carries provenance
  (`v044:940-947`, `companion041:109` "account assertion/provenance").
- **Operator authority.** `withdraw/2` and `reinstate/2` remain non-HTTP operator
  levers (`webfinger-store.lfe:199-204`, `:295-300`; production callers: none).
  `REQ-964`/`ADR-937` make tombstones read-only to the ceremony in both
  directions. **M-2** is the one place the amendment itself instructs an operator
  to destroy that state, and **M-3** is the one place the platform can destroy
  it.
- **Confirmed-upgrade authority.** `confirmed-migrate-path-a-handle/3`'s
  `'confirmed` literal is a person-confirmation boundary the effective set does
  not mention. **H-1.**
- **Resolver authority.** Publication is a post-commit best-effort discovery
  effect that cannot reverse acknowledgement (`v046:300-301`), and the browser
  fetches raw resolution/closure bytes into a WASM recogniser whose fields and
  constructors are private (`v044:998-1005`). No hub assertion of resolver facts
  can enter that boundary.

---

## Implementability audit, per repository

| Repository | Verdict | Notes |
|---|---|---|
| `cbcl-pairing` (spec + code at `8e55160`) | **Implementable.** | v2 is strictly additive: new context domains, new label family, new AAD version, new carrier rule, new envelope, new dialect assets, new commands. Every v1 constant I checked is untouched (`context.rs:7-9`, `channel.rs:114-147`, `channel.rs:341-362`, `mailbox.rs:7-15`, `storage.rs:196-241`). `CON-018`'s two arms are mechanical. One low: **L-4**. |
| `selfsame` pure core | **Implementable.** | The dispatcher is the only structural change: today's `profile.rs:80-91` becomes the cutover-v1 branch unchanged, plus a new rendezvous-v1 branch (which needs `PROTO-002 CON-301` and `PROTO-003 CON-401` descriptor grammars the crate does not implement today) and the v2 branch. `discovery.rs:231-236`/`:262-268` already return the identifier's own path, so two ApplicationIds on one origin are servable. Lows **L-1**, **L-2**, **L-3**, **L-6**. |
| `selfsame` Tauri wallet | **Implementable.** | Policy slots, origin ledger and the purge marker are additional keychain entries through the existing `store.rs` interface; the four custody operations map onto `use_root_key`/`use_hierarchy_root`. `CON-955` item 10 makes the signing/publication split match the synchronous closure type. Low **L-5**. |
| `cbcl-bus` / `cbcl_chat` | **Not implementable as written.** | `CON-953`'s transaction model is complete for the webfinger and pairaccount tables and correct on locking (I traced every `mnesia:read … 'write` and `mnesia:write` in `webfinger-store.lfe`, `pairaccount.lfe` and `account-grants.lfe`). It is incomplete for the Path-B binding tables it newly claims — **H-1** — and silent on the store's durability class — **M-3**. One further observation, not a finding: `account-grants.lfe:203-207` (`account-device-count`) and `pairaccount.lfe:71-76` (`unratified-count-tx`) reach the device cap through `mnesia:foldl`/`index_read`, which take table-scope read locks, so `CON-953:259-263`'s "exact legacy keys with write locks" is a lower bound on the real lock set rather than a description of it. Mnesia's deadlock handling makes that safe but not free; worth one sentence. |
| `did-crdt` | **Implementable.** | `CON-954` composes with the existing `DocStore` mutex, covers every writer I could find, and its crash semantics match the tag-as-commit-point design. `BlobStore` needs one additive read-back-by-DID-tag method (today only `load(hash)` and `load_all()` exist). Health, strict startup, CORS and idempotent create are all new but bounded. |

---

## Bounds and work audit

Every number recomputed from the CDDL, not taken from the table.

- **Application objects.** Deterministic-CBOR five-entry map = 1 + 2 + 2 + 35 +
  (2\|3\|4) + (1 + 3 + body). Control (body 4,096): **4,142 / 4,143 / 4,144**.
  Large (body 64,512): **64,558 / 64,559 / 64,560**. Both field-4 heads are three
  octets. `NFR-908` and `companion041 CON-018` agree exactly.
- **Outer chain.** Control plaintext 4,144 + 2,068 = **6,212**; ciphertext
  **6,228**; relay body 6,228 + 50 = **6,278**. Large plaintext 64,560 + 2,068 =
  **66,628**; ciphertext **66,644**; relay body 66,644 + 52 = **66,696**. The
  50/52 difference is exactly the two extra head octets required once the
  ciphertext exceeds 65,535, which only the large value does.
- **Relay limits.** `src/mailbox.rs:15` `MAX_FRAME_BODY_BYTES = 69_632` >
  66,696 ✓. `companion040:372-377`'s `store-sequence` body bound `1..69632`
  matches. `mailbox.rs:13` `MAX_FRAMES_PER_MEMBERSHIP = 16`; a successful
  ceremony sends five frames from the allocator and six from the claimant ✓.
- **Frame count and queue.** `CON-015`'s successful projection is
  `offer → intent-approve → preparation → (comparison-confirmed | binding-confirmed)
  → final-approve → payload → receipt`: exactly **two large and five control**
  application objects, matching `v044:606-608`, plus four bootstrap frames
  (cpace-a/b, finished-a/b). 2 × 67,000 + 5 × 7,000 + 4 × 8,192 = **201,768**
  octets ✓ exact.
- **Deadlines.** `v045:410-420`'s cumulative checkpoints 180/210/240/330/375/435/
  510/555/590 have deltas 180/30/30/90/45/60/75/45/35 summing to **590**,
  strictly inside `MAX_TTL_SECONDS = 600` ✓, and `companion030:255` bounds
  `ttl-seconds` to 60..600 ✓. The achievable witness at `TEST-1058`
  (170/200/230/320/365/425/500/545/585) crosses each checkpoint. The 180-second
  first-human budget composes with `REQ-960`'s 120-second policy-key cap and is
  the tightest figure in the schedule; it is correctly listed as a residual
  (`v046:505-506`).
- **Presence code.** 128 + 128 + 16 + 3 = 275 bits = 55 Crockford Base32
  characters = eleven groups of five ✓.
- **Profile.** The live document is 1,235 octets, well inside `CON-201`'s 65,536
  ceiling; four permissions inside the 64 cap and correctly sorted by code point;
  one `cbclPairingRelays` descriptor inside 1..16; one `stateResolvers` entry
  inside 1..64; `revocation.projection` absent, which `SPEC-004:2629-2632` makes
  OPTIONAL; every ceiling (2,592,000 / 900 / 60) inside its bound.

---

## Tests and gates audit

`TEST-1065` through `TEST-1074` are authorable as written with three exceptions,
all named above: `TEST-1069` cannot construct the H-1 state because `CON-953`
does not say it exists; `TEST-1069`'s reinstate rehearsal cannot detect M-2;
`TEST-1070` cannot construct the machine-loss boundary of M-3. `TEST-1073`'s
control witness at `logicalLength = 4,096` is unauthorable against `CON-015`'s
2,048 cap (**L-4**). Everything else — the exact-live-octet branch test, the
independent client/server rollback test, the reachable invalid-slot and purge
crash matrix, the concurrent reinstate/withdraw compensation matrix, the
concurrent HTTP/gossip/restart matrix, the runtime readiness gate, the six size
boundaries and the static stale-branch conformance sweep — is a real, executable
row. `companion041`'s `TEST-048`/`049`/`050` are likewise concrete.
`v046:481-483` correctly makes `TEST-1011`–`TEST-1074` and companion
`TEST-030`–`TEST-050` named SPL gates, and `v046:484-486` puts failing schemas,
vectors, corpus and pure-core tests before production code.

---

## Deployment and rollback audit

- **Ordering.** `CON-947`'s release order is unreplaced and coherent: additive
  recognition with allocation off → did-crdt safe create/CORS → hub side tables
  and v2 route with allocation off → corpus/migration/rollback/staging → a later
  owner-approved gate.
- **Server rollback.** Feature-gate rollback, not schema rollback; drain,
  compensate, verify no pending ceremony, retain finalized grants and additive
  tables. `CON-949`'s did-crdt forward-fix boundary after the first v2 issuer is
  correct and its CORS-withdrawal carve-out is sound (CORS today exists only on
  `rendezvous.rs`, so removing it cannot touch DID state).
- **Client rollback.** Now explicit (`REQ-962:84-87`, `CON-955` item 6,
  `TEST-1066`) and *true*: `profile.rs:80-91` is the cutover-v1 branch, so
  reverting the wallet keeps chat.anuna.io working, and only an original
  rendezvous-v1 profile needs the dual-branch release.
- **Relay drain.** `CON-949` defers to the companion's
  disable/close-or-expire/verify-empty/install sequence. Note that the relay is
  not a separate deployment: `cbcl-bus/fly.toml:8,124-146` runs it in-process on
  18081 behind public 9443, so `v046:494-495`'s "separate owner confirmation
  before any cbcl-bus or did-crdt deploy" does cover the relay.
- **Machine strategy.** `cbcl-bus/fly.toml:22-31` is a hard one-machine
  invariant with `strategy = 'immediate'` and no canary — matching
  `CON-949:446-449`. `did-crdt/fly.toml:32-44` binds one volume with
  `auto_stop_machines = "off"`; `CON-954`/`TEST-1072` prohibit scaling past one
  writer until a reviewed cross-process mechanism exists.
- **No-deploy.** `v046:490-495` and `:507-512` keep production allocation
  disabled and deny deploy authority in all four repositories.

---

## Persistence audit

- **Wallet.** Two-slot policy with highest-valid-revision, deterministic slot-A
  tie-break, verified-deletion reset with a reachable crash state, and a
  crash-recoverable purge converging to exactly one result. All expressible
  through `store.rs`'s `get`/`set`/`delete` with no presence edge, and consistent
  with `custody.rs`'s `restore`-refuses-when-present and fresh-salt-per-seal
  behaviour. **L-5** is the only residue.
- **Hub.** Transaction and lock model correct for the tables it fully specifies;
  compensation conflict tests correctly split by whether a row carries a
  revision; tombstones outside both write sets and both comparisons; schema
  creation separated from all-or-none per-account population. **H-1** (missing
  precondition and two missing tables) and **M-3** (durability class) are the
  gaps.
- **Resolver.** One per-DID durable mutation primitive with clone → validate →
  save → read-back → swap → acknowledge, tag-as-commit-point crash semantics,
  strict production startup, and continuous readiness gating new ceremonies.
  Sound and complete against the code.
- **Browser.** Strict-durability IndexedDB with a restoring failure path;
  pending hub transaction, local commit, finalization and acknowledgement are
  separately recorded compensatable stages rather than a false cross-system
  atomic transaction (`v044:1085-1090`). Correct.

---

## Residual-risk audit

**Accepted declared residuals** — bounded, honestly stated, and not defects:

- Relay operatorship and the one-operator availability exception; same-origin
  compromise; deliberate forwarding of both inputs; relay observation of `T`;
  lost-claim mailbox stranding; device-local rollback counters; operator
  availability. Each is a threat-model boundary the design cannot close, and each
  is named at `v046:503-506`.
- The reverse device-possession check is a browser self-challenge, not
  third-party proof (`v046:339-343`). Correctly labelled and correctly not
  upgraded.
- `CON-206` v2 step 9 accepts a hub-signed pending authority cache before public
  alias finalization. This genuinely amends the parent's "sole gate on alias
  provisioning" sentence (`SPEC-004:3144-3147`), and I record that plainly — but
  the compensating controls are real: the receipt waits for finalization, the
  pending record is never displayed as a finalized public alias, the record is
  authenticated and correlated, and the party exposed to a not-yet-provisioned
  alias is the same party that signed the pending record. Bounded.
- The 180-second first-human checkpoint. Tight but achievable, and it composes
  with the 120-second policy-key cap.
- Human cryptography review and independent CPace group math remain open;
  production allocation stays closed.

**Not residuals — defects.** H-1, M-1, M-2 and M-3 are each stated somewhere in
the amendment as a settled fact rather than an open question, which is what
separates them from the list above.

---

## Methodology

Fresh context, no access to the authoring conversation, no subagents, no
delegation. I read the 0.4.6 candidate in full, then the 0.4.5 and 0.4.4
amendments it adopts, then the 0.4.5 review and the 0.4.4 dispositions it
carries, then the three cbcl-pairing companions, then the governing Selfsame
specs (`SPEC-008`, `SPEC-007`, `SPEC-004`, `SPEC-002`, `PROTO-003`, `PROTO-004`)
and `SPEC-001`.

Every claim about behaviour was checked against source at the four named
baselines rather than against the candidate's disposition prose. In particular I:
decoded the live `chat.anuna.io` profile from
`cbcl-chat-selfsame-application-gate.lfe`'s base64url and compared its exact
top-level member set and digest against `CON-951`'s cutover-v1 row and against
`profile.rs`'s `TOP_LEVEL_REQUIRED`; enumerated every Mnesia table and every
production caller of every authority writer in `cbcl-bus/apps/**` outside
`test/`; traced the lock acquisition of `provision/3`, `withdraw/2`,
`reinstate/2`, `bind-and-store-verified-device-grant/8`, `claim/2` and
`confirmed-migrate-path-a-handle/3` statement by statement; enumerated every
`DocStore` mutation site in `did-crdt` and checked each against `CON-954`'s
writer list, including the `resolve_did` cold-start path, which is not an
independent writer; read `BlobStore::save`/`load`/`load_all` to confirm the
tag-as-commit-point semantics the crash claims rest on; read both `fly.toml`
files and `config/router.sys.config` for the deployment premises; recomputed
every CBOR length, every derived plaintext/ciphertext/relay maximum, the frame
counts, the queue budget, the checkpoint deltas and the presence-code bit
arithmetic from the CDDL rather than from the tables; and re-derived the
v1/v2 domain, label, transcript, AAD and PRK disjointness from `context.rs` and
`channel.rs`.

I attempted to reopen both 0.4.5 HIGH findings and all four MEDIUMs and could
not; the closures are real and, in the case of H-1, structurally better than the
minimum correction asked for. The one HIGH I report is new, and I state
explicitly that it became visible only because `CON-953` is the first clause to
enumerate the Path-B binding tables.

No candidate, parent, code, fixture, vector or sibling-repository file was
modified.
