---
id: SPEC-008-0.4.7-claude-adversarial-review
title: Tier-1 fresh-context cross-model adversarial review — SPEC-008 0.4.7-draft (with adopted 0.4.4/0.4.5/0.4.6) and cbcl-pairing SPEC-001 0.3.0 + 0.4.0 + 0.4.1 + 0.4.2
status: complete
tier: 1
verdict: REJECT
verdict-selfsame-spec-008-0.4.7: REJECT
verdict-cbcl-pairing-spec-001-0.4.2: PASS-with-one-low
verdict-cbcl-pairing-spec-001-0.4.1: PASS-as-corrected-by-0.4.2
verdict-cbcl-pairing-spec-001-0.4.0: PASS-as-adopted
verdict-cbcl-pairing-spec-001-0.3.0: PASS-as-adopted
reviewer: independent Circus adversarial reviewer, fresh context, no access to the authoring conversation, no access to the failed attempt-1 transcript, no subagents
model: Claude Opus 5 (claude-opus-5)
review-date: 2026-08-23
attempt: circus/spec008-v047-review/2
candidates:
  - "[[spec-008-0.4.7-standalone-tofu-first-contact]] — specs/trajectory/SPEC-008/spec-008-0.4.7-standalone-tofu-first-contact.md at selfsame 19c0f2bf7e3fc772c0719cb97e270ef56ecb9d7f"
  - "[[spec-008-0.4.6-standalone-tofu-first-contact]] — adopted except where 0.4.7 replaces or corrects"
  - "[[spec-008-0.4.5-standalone-tofu-first-contact]] — adopted except where 0.4.6 or 0.4.7 replaces or corrects"
  - "[[spec-008-0.4.4-standalone-tofu-first-contact]] — adopted except where 0.4.5, 0.4.6 or 0.4.7 replaces or corrects"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.2-control-body-cap.md at cbcl-pairing 5b48e3e7f4b61be23fbc60fb6b418d4630f9d22a"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.1-credential-v2-profile-authority.md (adopted, corrected by 0.4.2)"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.0-credential-v2-endpoint-profile.md (adopted)"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md (adopted)"
reviewed-revisions:
  selfsame-integration-baseline: 19c0f2bf7e3fc772c0719cb97e270ef56ecb9d7f
  selfsame-code-baseline: c2c0e20b8d68602151572dc50bc159e0bb98ab32
  selfsame-code-ancestor-declared-by-candidate: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
  cbcl-pairing-spec-baseline: 5b48e3e7f4b61be23fbc60fb6b418d4630f9d22a
  cbcl-pairing-code-baseline: 9e8ba1badc92aefc1a235a2a7f6909ed2fc6f9d6
  cbcl-pairing-code-ancestor-declared-by-candidate: 8e55160a3234127bd60ce723fd93ba6b2b67a06b
  cbcl-bus: fdbe4142261923f48cced037a7f609e48a9fdd5a
  did-crdt: 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
governing-specs:
  - specs/SPEC-008-production-pairing-claimant.md
  - specs/SPEC-007-cbcl-pairing-cutover.md
  - specs/SPEC-004-application-scoped-identity.md
  - specs/SPEC-002-visual-key-fingerprint.md
  - specs/PROTO-003-selfsame-pairing-v1.md
  - specs/PROTO-004-selfsame-ceremony-envelope-v1.md
  - /Users/anuna-01/Code/cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md
  - /Users/anuna-01/Code/cbcl-bus/specs/SPEC-053-key-root-identity.md
  - /Users/anuna-01/Code/cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md
prior-reviews:
  - "[[spec-008-0.4.6-claude-adversarial-review-2026-08-23]] (REJECT — 0 critical, 1 high, 3 medium, 7 low)"
  - "[[spec-008-0.4.5-claude-adversarial-review-2026-08-23]] (REJECT — 0 critical, 2 high, 4 medium, 8 low)"
  - "[[spec-008-0.4.4-claude-adversarial-review-2026-08-23]] (18 findings, carried by the 0.4.5 and 0.4.6 reviews)"
findings: 0 critical, 3 high, 2 medium, 6 low
last-updated: 2026-08-23
---

# Verdict: REJECT

Three HIGH and two MEDIUM findings are unresolved. The coordinated set does not
pass.

- **Selfsame `SPEC-008` 0.4.7-draft — does not pass.** `H-1`, `H-2`, `H-3`,
  `M-1`, `M-2`.
- **cbcl-pairing `SPEC-001` 0.4.2-draft — passes with one low.** Its control-arm
  correction is exact; I recomputed every encoded length from the CDDL and all
  three totals reproduce. `L-3` is the only residue.
- **cbcl-pairing `SPEC-001` 0.4.1 / 0.4.0 / 0.3.0 — pass as adopted and as
  corrected by 0.4.2.** Re-verified; nothing in 0.4.2 reopens them.

**Every 0.4.6 finding is closed on its own terms, and I could not reopen any of
them.** `H-1` is closed by the correction the prior review asked for, and closed
better than the minimum: `REQ-967` puts the classification at
`selfsamepairauthority` *before* display rather than at finalization, `CON-956`
adds both room tables and reserves `confirmed-migrate-path-a-handle/3` as the
sole membership writer, and `TEST-1075`/`TEST-1076` are the two requested rows.
`M-1` is closed by `REQ-968`, which turns out to codify exactly what the shipped
recogniser already computes (`profile.rs:880`). `M-2` is closed by `REQ-969`.
`M-3` is closed on substance by `NFR-909`. All seven LOWs are closed. I
re-derived every numeric claim in the effective set independently and they are
exact, including the live profile's 1,235 octets and its digest.

**The three HIGH findings are new, and two of them are created by the H-1
correction itself.** `REQ-967` installs a legacy-member terminal whose
discriminator is the `cbcl-account-binding` row. At the reviewed `cbcl-bus`
baseline every handle that can hold an authenticated chat session either has a
`cbcl-member` row (and is therefore classified legacy and refused) or has a
`cbcl-account-binding` row (and is therefore refused authorisation entirely by
the closed Path-B gate). No account can reach a non-terminal answer, the local
end-to-end validation the amendment mandates in step 4 cannot be constructed,
and the one interim remedy the amendment names is destructive and irreversible.
The third HIGH is an inherited `cbcl-bus` hard stop — `SPEC-053` `GATE-00` —
that no amendment in this lineage and no prior review has ever named, and whose
clause 1 explicitly rejects the configurable allocator gate the release plan
depends on.

These are not the same class as the declared production holds. The amendment
declares two holds honestly — Path-A migration and off-machine hub durability —
and I accept both as bounded. What it does not declare is that with both holds
in force the ceremony has *no reachable claimant at all*, that the remedy it
offers for the first hold strands the person it is applied to, and that the
repository which must host the hub half forbids the surface as planned.

---

## Revision verification

| Repository | Reviewed revision | Verified |
|---|---|---|
| `selfsame` (this worktree, `circus/spec008-v047-review/2`) | `19c0f2bf7e3fc772c0719cb97e270ef56ecb9d7f` | `git status --porcelain` empty at session start. `git diff --name-status c2c0e20b8d68602151572dc50bc159e0bb98ab32 HEAD` lists **exactly one file, `A specs/trajectory/SPEC-008/spec-008-0.4.7-standalone-tofu-first-contact.md`, 376 insertions, zero `M`/`D`**. Non-spec code at `c2c0e20` therefore holds byte-for-byte. |
| `selfsame` declared code ancestor | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | `git diff --name-status c6af6df HEAD` lists **thirteen files, every one `A` and every one under `specs/trajectory/SPEC-008/`**. The candidate's `implementation-baselines:` claim (`v047:13`) is therefore true: no code has moved since `c6af6df`. |
| `cbcl-pairing` spec | `5b48e3e7f4b61be23fbc60fb6b418d4630f9d22a` | `git diff --name-status 9e8ba1b 5b48e3e` lists **one file, `A specs/trajectory/SPEC-001/spec-001-0.4.2-control-body-cap.md`**. |
| `cbcl-pairing` declared code ancestor | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | `git diff --name-status 8e55160 5b48e3e` lists **four files, all `A`, all under `specs/trajectory/SPEC-001/`**. Code holds byte-for-byte. Working tree carries untracked `.DS_Store`, `.zetl/`, `fuzz/.DS_Store`, `specs/.zetl/`, `src/.DS_Store` only. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | `git status --porcelain` empty. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | **Pre-existing dirt recorded, not edited:** modified `specs/did-crdt-spec.md`, untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md`. No source, test, fixture or asset file is dirty, so every code claim below reads committed `1f409a4`. |

No file outside
`specs/trajectory/SPEC-008/spec-008-0.4.7-claude-adversarial-review-2026-08-23.md`
was created or modified by this review. No candidate, parent spec, code,
fixture, vector or sibling-repository file was touched.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct.

`v047:NNN` references
`specs/trajectory/SPEC-008/spec-008-0.4.7-standalone-tofu-first-contact.md`.
`v046:`, `v045:`, `v044:` reference the 0.4.6, 0.4.5 and 0.4.4 amendments in the
same directory. `companion042:`, `companion041:`, `companion040:`,
`companion030:` reference the four cbcl-pairing trajectory documents at
`5b48e3e`. Bare paths under `apps/`, `config/`, `scripts/`, `fly.toml` and
`specs/SPEC-053`/`SPEC-075` are `cbcl-bus` at `fdbe414`. Bare paths under
`crates/`, `src-tauri/`, `test-vectors/` and `specs/SPEC-00*` are `selfsame` at
`c2c0e20`. did-crdt paths are called out explicitly.

**There are no CRITICAL findings.**

---

### H-1 (HIGH) — `REQ-967`'s legacy terminal has no reachable alternative: at the reviewed `cbcl-bus` baseline every handle that can hold an authenticated chat session is either a `cbcl-member` handle (classified `LegacyMemberRequiresConfirmedUpgrade` and refused) or a `cbcl-account-binding` handle (refused authorisation outright by the closed Path-B gate). `NoBinding` and `Bound(accountDid)` are unreachable, so no credential/v2 ceremony can pass the pre-preview gate, and the local real-relay E2E the amendment mandates in step 4 cannot be constructed without opening a distinct inherited hard stop

**Exact candidate clauses.**

`v047:51-55` (`REQ-967`):

> "This requirement replaces the account-authority state list in 0.4.4 REQ-946
> and CON-941 and supplements 0.4.6 CON-953. Immediately after authenticated
> offer recognition and before `DisplayAuthenticatedIntent`, preliminary
> approval, `preview_issuer`, custody access, hierarchy derivation, signature,
> publication, or durable identity state, the browser calls
> `selfsamepairauthority(ceremonyId)`."

`v047:66-67` (`REQ-967`) — the sole discriminator:

> "The hub returns the legacy state when the handle has a `cbcl-member` row and
> no `cbcl-account-binding` row."

`v047:73-76` (`REQ-967`): "`Unknown` also terminates pre-preview. `NoBinding`
and `Bound(accountDid)` may be held through preliminary preview".

`v047:364` (implementation sequencing step 4): "Validate local real-relay E2E
with an **eligible no-legacy-member account** and validate the legacy-member
refusal independently."

**Governing code evidence, `cbcl-bus` at `fdbe414`.**

The claim I verify is: *for every handle `H`, if a v2 browser holds an
authenticated chat session for `H`, then `H` is classified legacy.* It follows
from three facts, each traced statement by statement.

1. **Every ordinary authenticated frame writes a `cbcl-member` row.**
   `cbcl-chat-session-ws.lfe:213-235` (`dispatch`) routes every kind except
   `grant`/`grantproof` through `verify-and-route` (`:242-264`), which calls
   `authorise-frame` (`:282-302`) and then `enrol-facts` (`:308-320`). For a
   handle with no binding row, `enrol-facts` returns
   `handle-facts handle 'unbound` (`:317-319`) or the `key-bound` pair
   (`:311-316`); `handle-facts:328-331` hard-codes `'presentation 'false`, so
   `cbcl-core-enrol:tofu:153-162` takes its final clause and accepts
   `bind = #(key key)`. `enrol-commit:454-463` then calls
   `cbcl-chat-members:enroll-or-check/2` (`cbcl-chat-members.lfe:42-54`), which
   writes the `cbcl-member` row. This happens **before** `route` runs, so any new
   `selfsamepair*` verb dispatched as an ordinary frame is enrolled before its
   own handler executes.
2. **No path ever deletes a `cbcl-member` row.** I enumerated every
   `mnesia:delete` in `apps/cbcl_chat/src/`: nineteen call sites, covering
   `cbcl-roommember`, `cbcl-account-roommember`, `cbcl-grant-nonce`,
   `cbcl-device-grant`, `cbcl-pairing`, `cbcl-sealedgrant`, `cbcl-pairgrant`,
   `cbcl-provenance`, `cbcl-webfinger-alias`, `cbcl-webfinger-account`,
   `cbcl-webfinger-withdrawn`, `cbcl-invite` and `cbcl-epoch-claim`. Neither
   `cbcl-member` nor `cbcl-account-binding` appears. Enrolment is monotone and
   permanent.
3. **The only alternative — an account-bound handle — cannot authenticate at
   all while the Path-B gate is closed.** `enrol-facts:309-310` routes a handle
   with a binding row to `account-facts` (`:349-358`), whose **second
   statement** is `(case (path-b-profile) ... (_ #(error rejected)))`.
   `path-b-profile:803-804` is `cbcl-chat-path-b-gate:profile/0`, which at
   `cbcl-chat-path-b-gate.lfe:16-18` returns `#(error tier-1-unapproved)`
   unconditionally — a constant in source that reads no configuration, asserted
   by `cbcl-chat-gate-closed-tests.lfe:18-22` and `:37-40`. So every frame from
   an account-bound handle, including `hello`, is refused `#"bad-signature"` at
   `authorise-frame:300-302`.

The consequence is a closed loop. To answer anything other than
`LegacyMemberRequiresConfirmedUpgrade`, `H` needs a `cbcl-account-binding` row
(facts 1 and 2 make a `cbcl-member` row unavoidable for any other route). To
hold the authenticated session `CON-941` and `REQ-947` require
(`v044:1009-1011`: "resolves the live session to the stable account ID";
`v044:329-331`: "durably bind every authorised chat session signing key to that
identifier"), `H` must **not** have a `cbcl-account-binding` row while the gate
is closed. The two conditions are mutually exclusive.

Both routes to a binding row are behind hard stops:

- `bind-and-store-verified-device-grant/8`
  (`cbcl-chat-account-grants.lfe:161-187`), reached only from
  `path-b-durable-write:853-861` from `path-b-complete:778-801`, whose first
  statement is again the closed `path-b-profile` (`:780-781`), and whose entry
  `path-b-grant:683-689` refuses `#"path-b-unavailable"` before allocating a
  nonce.
- `confirmed-migrate-path-a-handle/3`
  (`cbcl-chat-account-grants.lfe:118-141`), which `v047:82-83` itself records
  "has no production caller at the reviewed baseline". I re-verified: every
  reference outside `apps/cbcl_chat/test/` and `_build/` is its own definition
  and export.

A third writer exists and is worth naming because it is what an implementer
under time pressure will reach for: **`bind-account/2`
(`cbcl-chat-account-grants.lfe:81-98`) is exported, writes
`cbcl-account-binding` directly, and contains no `cbcl-member` check at all** —
the guard at `:178-181` that `CON-956` builds on lives in
`bind-and-store-unbound`, not in `bind-account`. Its only callers today are
`cbcl-chat-account-grants-tests.lfe` and `cbcl-chat-path-b-ws-tests.lfe`.
`CON-956:179-180` says "The only sanctioned writer that moves legacy rows is
`confirmed-migrate-path-a-handle/3`" — true for the *room* tables, and silent
about the writer that produces the same authority move without touching them.

**Failure trace.**

1. This review returns PASS. Step 2 (`v047:359-360`) encodes
   `TEST-1011` through `TEST-1082` as SPL gates; step 4 (`v047:364`) requires a
   local real-relay E2E with "an eligible no-legacy-member account".
2. The implementer brings up a local hub and a browser. The browser opens a
   chat session for a fresh handle and sends `hello`. `enrol-commit:460` writes
   `cbcl-member{H}`. The ceremony allocates, both Finished values verify, the
   offer finalizes, and the browser calls `selfsamepairauthority`.
3. `H` has a `cbcl-member` row and no `cbcl-account-binding` row.
   `REQ-967:66-67` returns `LegacyMemberRequiresConfirmedUpgrade`. The ceremony
   closes pre-preview with zero wallet effects — correct behaviour, and the
   **only** behaviour.
4. The implementer tries to construct the eligible account. Path-B admission is
   `#(error tier-1-unapproved)` in source. `confirmed-migrate-path-a-handle/3`
   needs an account DID that does not exist yet (see **H-2**). What is left is
   `bind-account/2` — which writes the binding row with no member guard, i.e.
   exactly the branch-(b) state the 0.4.6 review's `H-1` identified as the
   security-relevant one, now performed by the test harness rather than the
   ceremony. And the moment it succeeds, fact 3 above locks that handle out of
   chat, so the session the ceremony needs is gone.
5. `TEST-1041` (0.4.4 local end-to-end standalone link), `TEST-1036` (full
   browser CON-206/207 acceptance), `TEST-1038`, `TEST-1069`'s "then complete a
   fresh v2 ceremony", `TEST-1079`'s "Complete v2 using the distinct DID/alias"
   and `TEST-1080`'s conforming-fixture restore drill all require a *successful*
   ceremony. None of them has a constructible fixture.
6. In production the same analysis says the feature has no audience: every
   chat.anuna.io account has a `cbcl-member` row, so after both declared holds
   clear, `REQ-967` still refuses every one of them until the Path-A migration
   surface exists — which the amendment lists as a required gate (`v047:366`)
   but never identifies as gating *every* user rather than a residual class.

**Why this is not covered by what the amendment does say.** `v047:83-86` frames
legacy ineligibility as a *hold*: "legacy members remain deliberately
v2-ineligible. This is a production eligibility hold, not an invitation to write
the binding row." A hold implies a non-held complement. `v047:364` asserts that
complement exists ("an eligible no-legacy-member account") and `TEST-1075` tests
only the refusal. Nothing in the effective set says how an eligible account
comes into being, and the two mechanisms that could produce one are both behind
gates the amendment does not name.

**Minimum correction.** Four items.

1. In `REQ-967`, state the reachability fact and its consequence in the
   amendment's own words: at the reviewed `cbcl-bus` baseline the eligible class
   is empty, because `enrol-commit`/`enroll-or-check` gives every ordinary
   authenticated handle a `cbcl-member` row, nothing deletes one, and an
   account-bound handle is refused authorisation by
   `cbcl-chat-path-b-gate:profile/0`. Say plainly that credential/v2 therefore
   has no reachable claimant until either the SPEC-053 `REQ-033` migration
   surface or SPEC-053 `GATE-01` admission exists.
2. Name the eligibility precondition explicitly: a v2-eligible account is one
   with a `cbcl-account-binding` row **and** an open Path-B admission gate, or a
   Path-A handle that has completed the confirmed `REQ-033` migration **and** an
   open Path-B admission gate. Both are `cbcl-bus` gates outside this design's
   authority; list them beside the two existing holds in `v047:365-367`.
3. Replace step 4 with an implementable statement: either (a) the local E2E runs
   against a development `cbcl-bus` build whose Path-B admission gate is opened
   by a named, reviewed change to `cbcl-chat-path-b-gate` — declared as such,
   with the resulting build marked non-production — or (b) the local E2E is
   deferred to the increment that opens that gate. Do not leave "an eligible
   no-legacy-member account" as an unqualified premise.
4. In `CON-956`, add `bind-account/2` to the writer enumeration and state that
   it is not a sanctioned credential/v2 writer, so the contract's "only
   sanctioned writer" claim covers the authority move (`cbcl-account-binding`)
   and not only the room rows.

---

### H-2 (HIGH) — the interim remedy `REQ-967` offers for the legacy hold — an owner-run `confirmed-migrate-path-a-handle/3` — locks the person out of chat.anuna.io entirely, is irreversible at the reviewed baseline, and cannot satisfy the `SPEC-053 REQ-033` confirmation content `REQ-967` requires of it, because the account DID it must name does not exist until the ceremony it is supposed to precede has already allocated

**Exact candidate clause.** `v047:83-86` (`REQ-967`):

> "Until a separately approved production surface **or an owner-run
> remote-console procedure** proves the same confirmation evidence and atomic
> room migration, legacy members remain deliberately v2-ineligible."

`v047:179-186` (`CON-956`) describes that procedure's durable half:
`confirmed-migrate-path-a-handle/3` "requires literal `confirmed`, takes
whole-table write locks on both membership tables, validates the legacy enrolled
key and all target absences, writes every account row, deletes every legacy row,
and commits the account binding in its one transaction."

**Governing evidence.**

- The description of the transaction is **exact**. I traced it statement by
  statement: `cbcl-chat-account-grants.lfe:119-121` requires
  `(=:= confirmation 'confirmed)`; `:124` takes the `cbcl-account-binding` write
  lock and aborts `already-account-bound` if a row exists; `:126` uses
  `cbcl-chat-members:pubkey-tx` (`cbcl-chat-members.lfe:36-39`, member-row write
  lock) and aborts `not-enrolled`; `:128-129` calls
  `cbcl-chat-account-roommember:migrate-legacy-handle-tx/3`, which at
  `cbcl-chat-account-roommember.lfe:139-140` takes whole-table write locks on
  `cbcl-roommember` **and** `cbcl-account-roommember`, at `:174-185` validates
  the enrolled-key match and every target absence, at `:152-164` writes every
  account row, at `:168-170` deletes every legacy row *after* the writes, and at
  `:130-133` the caller commits the binding last; `:137-139` fans the REQ-033
  `account-linked` notice per live room. `SPEC-053:2343-2348` (`REQ-033`) is
  quoted correctly.
- **But the post-migration state is a total chat lockout.** After the
  transaction, `H` has `cbcl-account-binding{H}`. Every subsequent frame reaches
  `dispatch:231`, `account-binding/1` returns `#(ok Account)`,
  `enrol-facts:309-310` routes to `account-facts:349-358`, whose second
  statement is `path-b-profile`, which returns `#(error tier-1-unapproved)`
  (`cbcl-chat-path-b-gate.lfe:16-18`), so `account-facts` answers
  `#(error rejected)` and `authorise-frame:300-302` replies
  `#"bad-signature"`. The person can no longer send `hello`, cannot read,
  cannot post, in any room.
- **It is irreversible.** No `mnesia:delete` anywhere in
  `apps/cbcl_chat/src/` targets `cbcl-account-binding` (enumerated above), and
  `bind-account/2:86-90` refuses a rebind (`account-rebind`). The legacy
  `cbcl-roommember` rows were deleted by `:168-170`, so even hand-deleting the
  binding row would leave the memberships in the account namespace with no
  account branch able to reach them. Only direct Mnesia surgery on a live
  production node repairs it.
- **The confirmation content is not constructible at the required time.**
  `REQ-033` (`SPEC-053:2345-2348`) requires a surface that "names the account
  [[DID]], displays its fingerprint, and enumerates the channels whose
  membership is being re-keyed". `confirmed-migrate-path-a-handle/3` takes that
  account DID as its second argument. For credential/v2 that DID is the wallet's
  home DID, and `hierarchy.rs:289-297` derives it as
  `home_key(account_node(application_node(root, applicationId), scope))` — a
  function of `accountScopeId`, which `v044:349-351` (`REQ-947`) has the hub
  mint as a CSPRNG 32-octet value *during* `CON-939` step 1 allocation, and
  which `hierarchy.rs:272-274` makes the account node's only differentiator.
  `REQ-967:52-55` requires the migration to have happened **before** the
  ceremony's pre-preview query. At that point no home DID exists for the
  operator to name.

**Failure trace.**

1. An owner reads `v047:83-86`, opens a remote console, and runs
   `cbcl-chat-account-grants:confirmed-migrate-path-a-handle(H, D, 'confirmed)`
   for a real chat member, choosing some `D`.
2. The transaction commits: rooms move to `cbcl-account-roommember`, legacy rows
   are deleted, the binding is written, `account-linked` notices fan out.
3. The person's next frame is refused `bad-signature` (`account-facts:352`
   meets the closed gate). They are out of every channel they were in, with a
   wire error that names no cause.
4. The v2 ceremony they were being prepared for cannot start either, because it
   needs the authenticated session that step 3 just destroyed.
5. If `D` was a placeholder, the state is also semantically wrong: when
   `GATE-01` later opens, `account-facts` will demand a device grant under `D`,
   which no wallet holds.

`ADR-939` (`v047:261-265`) states the principle correctly — "Writing only an
account binding either violates SPEC-053 REQ-033 or makes those rooms
unreachable" — and then `REQ-967` offers, as the interim path, the operation
that produces the second outcome by a different route: the rooms move, but the
person cannot reach the account branch that now holds them.

**Minimum correction.** Delete the "or an owner-run remote-console procedure"
alternative from `v047:84`, or qualify it exactly: the confirmed Path-A
migration SHALL NOT be run while `cbcl-chat-path-b-gate:profile/0` is closed,
because an account-bound handle is refused authorisation on every frame
(`cbcl-chat-session-ws.lfe:349-358`), the operation has no inverse at the
reviewed baseline (no `mnesia:delete` targets `cbcl-account-binding`), and the
account DID `REQ-033` requires the surface to name is not derivable until the
hub has minted the `CON-211` scope (`v044:349-351`, `hierarchy.rs:272-297`).
State that the ordering dependency — scope, then home DID, then REQ-033
confirmation, then migration, then v2 ceremony — is circular as written and is
part of what the separately approved migration surface must resolve.

---

### H-3 (HIGH) — the hub half of the design lands in `cbcl-bus`, where `SPEC-053 GATE-00` governs whether an authority-producing surface may exist on `main` at all; `GATE-00` clause 1 rejects exactly the "allocator gate defaults off" mechanism `CON-947` relies on, and clause 4 rejects "an inert account endpoint, or a configurable account gate" by name. No amendment in this lineage, and no prior review, names `GATE-00`

**Exact candidate clauses.**

`v045:388` (`CON-947`, adopted and unreplaced): "The allocator gate defaults off
independently of serving the v2 profile."

`v045:389-392` (`CON-947`) release order: "ship additive cbcl-pairing and
Selfsame recognition with allocation off; ship did-crdt safe create/CORS;
**ship hub side tables and v2 profile route with allocation off**".

`v047:350-353` (implementation and gate sequencing): "The 0.4.6 repository
slices remain, with these additions: **the hub authority query/offer path adds
the pre-preview legacy state**".

`v044:968-970` (`CON-939`): "These three authenticated HTTP/WebSocket commands
are outside cbcl-pairing relay frames"; `v044:1009` and `:1018-1019` add
`selfsamepairauthority` and `selfsamepairaccept`; `v044:1032-1034` extends
`pairaccountgrant`.

`v047:9` (`conforms-to:`) names "cbcl-bus SPEC-075 REQ-007 unchanged; cbcl-bus
SPEC-053 REQ-033 unchanged" and nothing else from `cbcl-bus`.

**Governing evidence, `cbcl-bus` at `fdbe414`.**

`specs/SPEC-053-key-root-identity.md:5223-5308` (`GATE-00` — "the dormancy
contract (applies to every gate above Tier 2)"):

- `:5225-5227`: "Code inside a still-open gate's enumeration may live on `main`
  when **all five** of these hold."
- `:5240-5248` clause 1: "**One enablement point, and it is a constant in
  source.** Exactly one function decides whether the gated capability is
  available, it returns a closed result unconditionally, and it reads **no**
  application environment value, database row, request field, header, build flag
  or deployment variable. **A gate that any configuration can open is a feature
  flag, and a feature flag is not a gate.**"
- `:5249-5253` clause 2: "**Every authority-producing path consults it first** —
  before any durable write, any allocation of a challenge or nonce, any call to
  a resolver or other network peer, and before any TOFU enrolment."
- `:5254-5257` clause 3: a prohibited-action test per gated surface, whose
  refusal must be opaque.
- `:5258-5262` clause 4: "**No authority-producing path in the enumeration is
  reachable from the network.** A route that can allocate an invitation, **start
  a ceremony, accept a grant, enrol an account, or change standing** is shipped,
  whatever it later answers."
- `:5282-5296` — the one owner amendment, DEC-014, admits only the pinned
  cbcl-pairing relay on its distinct origin, and closes with: "The exception does
  **not** admit the old PROTO-002/003 handlers, **an inert account endpoint, or
  a configurable account gate.** Changing any listed condition makes clause 4
  fail again."
- `:5297-5304` clause 5: closure asserted by `cbcl-chat-gate-closed-tests`,
  which "asserts the **compiled route table** the listener opens with carries no
  gated path".
- `:5306-5308`: "A surface that cannot meet all five, or the exact clause-4
  exception, does not land."

The repository implements this literally, and gives the exact shape a new gated
surface must take:

- `cbcl-chat-webfinger-gate.lfe:1-13` — "GATE-00 clause 4 says a route that
  answers is shipped whatever it answers, so **the refusal belongs in route
  compilation and not inside the handler.** This is the sole enablement point,
  is constant in source, and reads no application environment, request, or
  durable state."
- `cbcl-chat-path-b-gate.lfe:16-18` and
  `cbcl-chat-selfsame-application-gate.lfe:26` are the same pattern for the
  other two surfaces.
- `cbcl-chat-gate-closed-tests.lfe:18-73` asserts each constant, asserts the
  Path-B gate ignores a hostile `selfsame_path_b_profile` (`:37-40`), asserts
  route-table membership for the two opened surfaces (`:50-52`, `:62-64`), and
  asserts `cbcl-chat-app` exports no `dispatch/1..4` selector so "a console call
  cannot turn a test fixture into the live listener's dispatch table"
  (`:68-73`).
- `path-b-grant:683-689` is clause 2 in code: `path-b-profile` is consulted
  before a nonce is allocated, with the comment "GATE-01 is an authority and
  resource boundary, not merely a pre-verifier check: a closed Path B may not
  allocate even a short-lived challenge."

**Failure trace.**

1. This review returns PASS. Step 4 of the 0.4.6 sequencing (`v046:487-489`,
   unreplaced) implements "hub additive schema/transactions"; `CON-947:389-392`
   ships "hub side tables and v2 profile route with allocation off".
2. The implementer adds `selfsamepairallocate`, `selfsamepairofferprepare`,
   `selfsamepairofferfinalize`, `selfsamepairauthority`, `selfsamepairaccept`
   and the ceremony-extended `pairaccountgrant` to `cbcl-chat-session-ws`,
   behind a feature gate that "defaults off".
3. Every one of those verbs is a path that starts a ceremony, allocates a
   ceremony nonce, accepts a grant and changes standing — clause 4's enumeration
   verbatim. The gate is a configuration value — clause 1's "a feature flag is
   not a gate", and clause 4's "a configurable account gate", which DEC-014
   explicitly does not admit.
4. `cbcl-chat-gate-closed-tests` goes red — or, worse, is amended to accommodate
   the new surface, which is precisely the "opening it cannot arrive inside a
   change that reads as a refactor" failure clause 5 exists to prevent.
5. The `cbcl-bus` owner is asked to accept a change their own Tier-1 gate
   forbids, from a design that never told them it was touching that gate.

`SPEC-075 REQ-007` and `SPEC-053 REQ-033` are correctly identified as `cbcl-bus`
constraints and correctly placed in `conforms-to:` (`v047:9`). `GATE-00` — the
one clause that decides whether the hub slice may exist on `main` at all — is
absent from `amends:`, from `conforms-to:`, from the inherited-hard-stop
treatment, and from the four prior reviews.

**Minimum correction.** Three items.

1. Add `cbcl-bus SPEC-053 GATE-00` to the amendment's declared constraint set
   and to the inherited hard-stop list, with an explicit disposition row in the
   shape of `SPEC-053:5312-5316`.
2. State the conforming shape for the credential/v2 hub surface: one
   constant-in-source enablement point in the shape of
   `cbcl-chat-webfinger-gate:routed?/0`, consulted in route compilation and, for
   WebSocket verbs, as the first statement of every handler before any durable
   write, ceremony or nonce allocation, or resolver call; a prohibited-action
   test with an opaque refusal; and extensions to `cbcl-chat-gate-closed-tests`
   asserting both the constant and the compiled route table.
3. Correct `CON-947:388`: the "allocator gate defaults off" is an **additional**
   production control layered on top of the `GATE-00` enablement point, not the
   enablement point itself. A configuration value may not be the thing that
   decides whether an authority surface is available.

---

### M-1 (MEDIUM) — `REQ-967` introduces `legacyKeyDigest` and `roomSetDigest` as hub-computed, hub-signed, person-visible values and gives neither a preimage: no hash function, no encoding, no field selection, no sort order, no domain separation. `TEST-1075` requires "deterministic key/room digests" and cannot be authored. This is the exact defect class `REQ-968` exists to close, reintroduced in the same amendment

**Exact candidate clauses.**

`v047:63` (`REQ-967`), the fourth state:

> `LegacyMemberRequiresConfirmedUpgrade(handle, legacyKeyDigest, roomSetDigest)`

`v047:66-69` (`REQ-967`) — the whole definition:

> "It computes `legacyKeyDigest` from **the exact stored key** and
> `roomSetDigest` from the **deterministic-CBOR sorted complete legacy room
> membership rows**."

`v047:174-175` (`CON-956`): "It **signs** the classification and digests into
the ceremony reply."

`v047:284-286` (`TEST-1075`): "Require the exact signed legacy status and
**deterministic key/room digests** before authenticated-intent display."

**Governing evidence.**

- Compare with what the same amendment does for the value it *does* define.
  `v047:103` (`REQ-968`): "`descriptorDigest = BASE64URL-NO-PAD(SHA-256(canonical
  descriptor bytes))`", with `v047:90-101` fixing the exact six-member object
  and RFC-8785 as the serialisation, and `v047:109-112` putting the complete
  preimage, canonical bytes and independently reproduced output into the
  `CON-226` corpus. Every other digest in the effective set is pinned to that
  standard: `v045:248-252` (`offerCoreDigest`, `offerDigest`, `intentDigest`),
  `v045:266-272` (`accountPrincipalDigest`, with an explicit deterministic-CBOR
  array and a named domain string), `v045:275-279` (PAIR1 checksum),
  `v044:359-366` (device proof). The 0.4.6 review reports that a *single*
  undefined preimage among these was a MEDIUM.
- The rows the digest is taken over have no canonical form to fall back on.
  `cbcl-chat-roommember.lfe:28` is
  `(defrecord cbcl-roommember key room handle pubkey granted via since)`, where
  `granted` is a millisecond wall-clock stamp and `since` may be the atom
  `'undefined` for pre-`SPEC-059` rows (`:41-50` records that migration
  explicitly). Whether `granted`, `via` and `since` enter the digest changes the
  value; whether `'undefined` is encoded at all is undefined; and "sorted" does
  not say sorted by `room`, by `key`, or by the length-prefixed composite the
  table actually keys on.
- `legacyKeyDigest` "from the exact stored key" leaves the hash unnamed. The
  stored key is a 32-octet Ed25519 public key (`cbcl-chat-members.lfe:13`,
  written at `:49-53`); `SHA-256(pk)`, `SHA-512(pk)` and a `SPEC-002`
  fingerprint rendering are all defensible, and the amendment's own
  `SPEC-002 REQ-103` conformance claim (`v047:9`) makes the third a live
  possibility for a value that will be shown to a person.

**Failure trace.**

1. `TEST-1075` is encoded as an SPL gate (`v047:359-360`). Its assertion is
   "deterministic key/room digests". The test author must write an expected
   value and cannot: there is no rule that produces one.
2. Two implementations of the hub — or the hub and the corpus fixture the
   browser checks against — pick different field sets or sort orders. The signed
   classification differs across builds for identical Mnesia state, so the reply
   is not reproducible and `TEST-1075` cannot be red-first.
3. `CON-956:174-175` signs the digests into the ceremony reply, so the value is
   inside an authenticated statement whose bytes cannot be pinned by a vector.
   `v046:344-347` (`CON-945`, adopted) requires the `CON-226` corpus to contain
   "every changed closed grammar, error token, and numbered-step disposition",
   and this one cannot be added.
4. The browser displays a consequence of an unreproducible digest to the person
   (`v047:69-71`), which is the same "authenticated display of an unverifiable
   value" hazard `CON-936` and `v045:259-262` exist to exclude.

The blast radius is smaller than `M-1` of the prior review — no second party
recomputes these two values, so a mismatch is not a security failure — but the
requirement is untestable as written and the test row is unauthorable, which is
the stated MEDIUM bar.

**Minimum correction.** Two sentences in `REQ-967`, in `REQ-968`'s shape.
Define `legacyKeyDigest = BASE64URL-NO-PAD(SHA-256(UTF8("<domain>") ||
enrolledPublicKey))` over the raw 32 octets, and
`roomSetDigest = BASE64URL-NO-PAD(SHA-256(DETERMINISTIC-CBOR([...])))` over an
explicitly named field tuple per row — at minimum `room`, and explicitly *not*
`granted`/`since` if wall-clock and migration-`undefined` values are to be
excluded — with rows sorted by an explicitly named component under an explicitly
named byte comparison. Add both preimages, their canonical bytes and an
independently reproduced output to the `CON-226` group `REQ-968:109-112` already
requires, and put the corresponding expected values in `TEST-1075`.

---

### M-2 (MEDIUM) — `NFR-909`'s fail-open/fail-closed enumeration is stated as complete and is not, and the table list that defines the `hub-authority-off-machine-ready` gate omits a durable single-use authority ledger whose loss is fail-open: `cbcl-successor-pointer`. The gate as specified can report ready over a restore that silently un-spends every departed key

**Exact candidate clauses.**

`v047:135-143` (`NFR-909`):

> "Loss of the machine/volume **therefore**:
>
> - loses withdrawal tombstones and is fail-open for previously withdrawn alias
>   generations;
> - loses pending compensation and makes it unrecoverable; and
> - loses account/binding/grant authority and is fail-closed for existing
>   grants."

`v047:145-153` (`NFR-909`) — the gate's scope:

> "Production v2 allocation SHALL remain disabled until an owner-approved
> off-machine backup or replica **covers every** `cbcl-webfinger-*`,
> `cbcl-selfsame-*`, `cbcl-account-binding`, `cbcl-device-grant`, `cbcl-member`,
> `cbcl-roommember`, `cbcl-account-roommember`, and `cbcl-pairaccount-grant`
> table ... and passes a machine-loss restore drill that reproduces exact row
> bytes, indexes, tombstones, pending compensation, schema version, and
> **authority queries** before service readiness."

`v047:269-272` (`ADR-940`): "Withdrawal tombstones are fail-open on loss, so
accepting machine-loss as an availability residual would misstate a security
boundary."

**Governing evidence, `cbcl-bus` at `fdbe414`.**

The three stated consequences are all **correct**, and I verified each:

- tombstone loss is fail-open — `cbcl-chat-webfinger-store.lfe:139-149`
  (`create-or-return`) reads `cbcl-webfinger-withdrawn` at the alias URI and
  returns `#(error withdrawn)` only if the row is present; an empty table
  provisions;
- `cbcl-selfsame-compensation` is a Mnesia table (`v045:360-362`,
  `v046:253-256`) with no other copy;
- binding loss is fail-closed — `account-binding/1:69-74` degrades to
  `#(error key-bound)`/`#(error not-enrolled)` and `webfinger-store:314-335`
  (`lookup`) degrades to `'missing`.

The persistence model is also correct: `cbcl-chat-mnesia.lfe:10-13` returns
`'disc_copies` on a named node; `config/router.sys.config:48-60` sets
`{mnesia, [{dir, "/data/mnesia"}]}` and says in the deployment's own words
"redeploy-durable, not off-machine DR — a lost machine still loses this
(Litestream replicates only the SQLite stores)"; `fly.toml:22-31` is a hard
one-machine invariant with `strategy = 'immediate'`; `fly.toml:42-44` and
`:53-63` confirm only `CBCL_ROUTER_DB`/`CBCL_CHAT_DB` are the replicated stores.

**What is missing.** `cbcl-successor-pointer` (`cbcl-chat-successor.lfe:21-35`)
is a `disc_copies` Mnesia table on the same volume. Its purpose (`:8-12`) is the
SPEC-053 `at_most_one` property: "one key, one pointer, forever." It is
consulted on **every** frame: `cbcl-chat-session-ws.lfe:343` and `:381` set
`'spent (cbcl-chat-successor:spent? pk)` on both the selector and account
branches, and `cbcl-core-enrol.lfe:106-119` refuses any frame whose `spent` fact
is not literally `'false` — "A key that has an accepted CON-011 successor
pointer has handed its handle on, and every frame it signs is refused on EVERY
path."

`spent?/1` at `cbcl-chat-successor.lfe:150-154` is:

```lfe
(case (catch (mnesia:dirty_read 'cbcl-successor-pointer key))
  ('() 'false)
  (`(,_ . ,_) 'true)
  (_ 'true))
```

An **empty table is indistinguishable from "no pointer" and answers `'false`**.
The core's fail-closed discipline (`:113-117`) protects against an *unresolved*
answer, not against a resolved-but-empty one. So loss of the volume makes every
departed key spendable and acting again — a fail-open authority consequence of
exactly the class `ADR-940` says would "misstate a security boundary", and it is
in neither the consequence list nor the table list the gate is defined over.

A second, smaller instance: `cbcl-pairaccount-grant` *is* in the table list, but
its fail-open consequence is not in the consequence list.
`cbcl-chat-pairaccount.lfe:97-104` (`unratified-device?`) returns `'unknown`
only on a transaction failure; over an empty table it returns `'false`, and
`cbcl-core-enrol.lfe:125-126` then admits a key that SPEC-076 `REQ-004` makes
inert.

**Failure trace.**

1. Production allocation is eventually enabled after the `NFR-909` drill passes.
   The drill's scope is the eight table classes at `v047:147-149`;
   `TEST-1080:330-331` deletes "each table class from the snapshot and require
   restore/gate failure" — over that same list.
2. The Fly machine is lost. The restore reproduces every listed table exactly.
   `hub-authority-off-machine-ready` reports true and the service becomes ready.
3. `cbcl-successor-pointer` was not in the snapshot, so it comes back empty.
   Every key that had handed its handle on through a `CON-011` successor pointer
   now answers `spent = false` at `cbcl-chat-session-ws.lfe:343`, and
   `cbcl-core-enrol.lfe:118-119`'s clause-8 refusal never fires. Departed
   devices reconnect and act.
4. Nothing signals this: the gate the amendment defines has been satisfied
   exactly as written.

**Minimum correction.** Two edits. Add `cbcl-successor-pointer` to the
`v047:147-149` table list (its loss is fail-open for the `CON-011`
single-successor property and it is consulted on every frame at
`cbcl-chat-session-ws.lfe:343,381`), and add both fail-open consequences to the
`v047:140-143` list: loss of `cbcl-successor-pointer` un-spends every departed
key because `spent?/1:150-154` answers `'false` for an empty table, and loss of
`cbcl-pairaccount-grant` makes every paired-but-unratified device key eligible
to enrol because `unratified-device?/1:97-104` answers `'false` for an empty
table. If the intent is that the scope is credential/v2's own authority only,
say so and state that the wider `cbcl-bus` authority durability is a separate
`cbcl-bus` obligation — but do not present a partial list under "covers every".

---

### L-1 (LOW) — `REQ-967` renames `Bound(did)` to `Bound(accountDid)` in the same paragraph that makes `cbcl-account-binding` the legacy discriminator, and `cbcl-account-binding`'s value column is literally an account DID; the store the `Bound` answer actually reads is `cbcl-webfinger-account`

`v047:62-63` gives the state as `Bound(accountDid)`; `v047:66-67` makes the
legacy test "`cbcl-member` row and no `cbcl-account-binding` row"; `v047:73-75`
maps "the latter to `BoundSameDid` or `BoundDifferentDid`". `REQ-967:51` says it
"replaces the account-authority state list in 0.4.4 REQ-946 and CON-941", so it
is now the sole enumeration.

The correct store is determined by the adopted text, not by `REQ-967`.
`v044:1010-1012` (`CON-941`) says the command "reads the **account-keyed**
authority store" — `cbcl-webfinger-account` is keyed on the opaque provider
account (`cbcl-chat-webfinger-store.lfe:54`, `:70-71`), while
`cbcl-account-binding` is keyed on the handle (`cbcl-chat-account-grants.lfe:29`,
`:44-48`). `v046:129-136` (`REQ-965`) confirms the `BoundDifferentDid` branch is
about `cbcl-webfinger-account`. So the effective set resolves it — but the local
reading of `v047:62-75` alone points at `cbcl-account-binding.account`, which
`cbcl-chat-session-ws.lfe:857` shows is the verified `account_did` from a Path-B
grant.

The consequence of the wrong reading is not cosmetic: a handle with a Path-B
binding and no reciprocal alias would answer `Bound(did)` instead of
`NoBinding`, so `v044:308-311`'s `NoBinding` branch — "requires the application
to display the prepared DID fingerprint and record an explicit comparison
action", the `SPEC-004 CON-221` first-enrolment comparison — would be skipped on
a genuine first enrolment. Say in `REQ-967` that `Bound(accountDid)` is the
reciprocal home DID held in `cbcl-webfinger-account` for this stable account,
and is not `cbcl-account-binding`'s account value.

### L-2 (LOW) — `CON-958`'s non-crash root-completion rule contradicts adopted `0.4.6 CON-952:228-229`, and the amendment's own effective-text ledger does not record the replacement

`v047:235-238` (`CON-958`): "once the running operation has durably committed
and read-verified `purging`, deleted and verified all old safety state and the
old root, and deleted/read-verified the marker, **that same invocation MAY
write/read-verify the requested restored or new root**."

`v046:228-229` (`CON-952`, adopted): "**A new root is written only in a later
operation** after the marker and all old state are absent."

`v047:11` adopts "All 0.4.4, 0.4.5, and 0.4.6 clauses except the exact
replacements and corrections **named** by this amendment", and `v047:251-255`
names three replacements — `NFR-908`'s control range, the generator/corpus
split, and 0.4.4's "CON-201 profile version 1 is not amended" sentence — but not
this one. Every other correction in 0.4.7 is self-declaring (`v047:51`,
`v047:116`, `v047:164`), which makes the omission conspicuous.

The security property survives either reading — the root write is strictly
ordered after verified marker absence in both, and I re-confirmed the mechanism:
`custody.rs:190-193` refuses `restore` while an identity exists, and
`custody.rs:207-218` draws a fresh 16-octet salt and two fresh nonces per seal,
so no old slot opens under a new root. The test half is reconciled by
`v047:277-279`'s "except where the rows below replace an expectation" plus
`TEST-1081:335-339`. Only the contract sentence is left in direct contradiction.
One ledger row in `v047:251-255`.

### L-3 (LOW) — cbcl-pairing 0.4.2 tightens the control arm to `1..2048` but leaves adopted 0.4.1 `TEST-050`'s positive control witness at `logicalLength = 4,096` in force, and 0.4.7 asserts a replacement that 0.4.2 does not make and a Selfsame amendment cannot make

`companion042:62` (`CON-019`) is `3: 1..2048`; `companion042:32-33` (`REQ-024`):
"Logical lengths 2,049 through 4,096 are not padding capacity; they are terminal
envelope refusals before dialect transition."

`companion041:229-231` (`TEST-050`): "Assert encoded lengths 4,142, 4,143, and
4,144 at control logical lengths 1, 23, 24, 255, 256, **and 4,096**."

`companion042:8` amends only `CON-018`; `companion042:9` adopts 0.4.1 "except the
exact control-length replacement below"; `companion042:70` says "All other
CON-018 rules remain". `TEST-050` is nowhere addressed. Meanwhile `v047:279`
makes "Companion TEST-030 through TEST-051" mandatory — `TEST-050` included and
unmodified — and `v047:42` asserts "companion TEST-051 replaces the 4,096
witness", which is a claim about a cbcl-pairing document that the document does
not make, from a Selfsame amendment that has no authority over cbcl-pairing test
rows (`SPEC-001:1486-1496` permits amendment only through a reviewed merged
revision of SPEC-001).

Nothing numeric moves. I recomputed the control class from `CON-019` as
1 (map head) + 2 (`0:2`) + 2 (`1:kind`) + 35 (`2:` plus a 32-octet bstr with a
two-octet head) + (2 or 3 or 4) + 4,100 (`4:` plus a 4,096-octet bstr with a
three-octet head) = **4,142 / 4,143 / 4,144**, exactly `companion042:67-68`,
with the class maximum unchanged, so every derived plaintext, ciphertext, relay
and queue figure is unaffected. The correction belongs in cbcl-pairing 0.4.2:
one sentence saying `TEST-050`'s control-length list is replaced by `TEST-051`'s,
and `v047:42`/`v047:279` should cite that sentence rather than assert it.

### L-4 (LOW) — `CON-957` requires the recogniser to *completely accept* a profile whose descriptors name the two engines `SPEC-007 REQ-807` says to reject, and records the narrowing reading without naming `REQ-807` in `amends:` or `conforms-to:`

`v047:211` requires "only `rendezvous` present, then rendezvous-v1 complete
grammar", and `v046:195-196` makes that "the ratified parent CON-201 grammar" —
which at `SPEC-004:2554-2562` obliges every rendezvous descriptor to carry
`pairingUrl`, `pairingProtocol` and `pairingRoute` as defined by
`PROTO-003 CON-401`, i.e. `selfsame-pairing-v1`.

`SPEC-007:262-263` (`REQ-807`, an inherited hard stop): "The shell SHALL
**reject every legacy carrier and descriptor** before subsequent network access,
key derivation, profile fetch, or identity action."

`v047:221-226` states the true consequence — a recognised rendezvous-v1 profile
provides no `cbclPairingRelays`, so under `REQ-806`/`REQ-807` it cannot allocate
or select any engine, and "Recognition exists only for profile compatibility and
never advertises, negotiates, selects, or falls back" — and I confirmed it holds
mechanically: `SPEC-007:660-667` (`CON-806`) selects only from
`cbclPairingRelays`, and `v046:79-83` routes credential/v1 to the cutover-v1
branch only. The prior review reached the same conclusion (recognition is not
selection) and asked only for the sentence, which 0.4.7 supplies.

What remains is authority hygiene: the sentence is a narrowing *reading* of a
Tier-1 hard stop's plain text, and `REQ-807` appears in neither `v047:8`
(`amends:`) nor `v047:9` (`conforms-to:`). Either name it with the reading
recorded, or note the cost the branch actually carries: it requires implementing
`PROTO-002 CON-301` and `PROTO-003 CON-401` descriptor grammars that
`crates/selfsame-app-identity` does not contain today — I grepped the whole
crate and the only `rendezvous` occurrences are a comment at `hierarchy.rs:43`
and three `uri.rs` test hostnames — for a branch that is required to reach no
outcome.

### L-5 (LOW) — `REQ-967`'s ordering is expressed entirely in wallet-side terms, but the actor bound by it is the browser, and the one constraint the browser can actually enforce — do not send the `offer` performative — is unstated

`v047:52-55` orders the query "before `DisplayAuthenticatedIntent`, preliminary
approval, `preview_issuer`, custody access, hierarchy derivation, signature,
publication, or durable identity state". Every one of those is a wallet action.
The caller is the browser (`v047:55`), and `CON-941` requires the ceremony's
pending signed offer to exist first (`v044:1010-1011`), so the query sits after
`selfsamepairofferfinalize`.

The wallet displays as soon as it recognises `offer`: `companion040:321-322`
(`CON-015`) is `begin -> offer`, `offer -> intent-approve | intent-decline`, and
`v045:259-262` (`CON-943`) puts recognition-then-display immediately on receipt.
The allocator has no protocol-level refusal available before `offer` —
`companion040:320-329` reaches `refusal` only from `preparation` and `payload` —
so the sole way to satisfy `v047:52-55` is for the allocator to withhold the
`offer` performative and close or let the mailbox expire, which `v044:517`
(`REQ-951`) makes terminal for the claimant. `v047:70-71`'s "closes the v2
ceremony" implies it; nothing states it.

Add one clause to `REQ-967`: the allocator SHALL NOT send the `offer`
performative until `selfsamepairauthority` has returned a non-terminal
classification, and a terminal classification closes the ceremony without an
`offer`. Extend `TEST-1075:286-288` to assert that no `offer` object reaches the
relay in the legacy case, which is the only assertion that can be made from the
allocator's side.

### L-6 (LOW) — `CON-956` states an "Actual key" for `cbcl-account-roommember` that is not the actual key, and leaves `cbcl-roommember`'s unnamed, in a contract whose sibling rows give exact keys

`v047:166-169` (`CON-956`) gives `cbcl-roommember` as "existing legacy
room-membership composite key" and `cbcl-account-roommember` as
`(Room, AccountDID, Handle)`.

`cbcl-chat-account-roommember.lfe:234-239` shows the real key:

```lfe
(defun pkey (room account handle)
  (erlang:iolist_to_binary
    (list (size-prefix room) room (size-prefix account) account handle)))
(defun size-prefix (bin) (binary ((byte_size bin) (size 32) big unsigned integer)))
```

— a single length-prefixed binary, not a three-element tuple, and the module's
own comment (`:232-234`) explains why: "a CBCL string may contain arbitrary
non-quote bytes, so delimiter joining is not a recogniser."
`cbcl-chat-roommember.lfe:245` shows the legacy key is likewise
`pkey(room, handle)`. Both differ in kind from the genuine tuples the adopted
`CON-953:244-246` records — `(Handle, DeviceKey)` for `cbcl-pairaccount-grant`
(`cbcl-chat-pairaccount.lfe:23`, `:53`) and `(Account, PubKey)` for
`cbcl-device-grant` (`cbcl-chat-account-grants.lfe:322`).

The consequence is bounded — both rows are read-only for v2, and `roomSetDigest`
is taken over `cbcl-roommember` content rather than its key — but a contract
whose column is headed "Actual key" should carry the actual key, and **M-1**
above shows the digest work depends on knowing exactly what these rows are. Give
both keys their real shape.

**One related observation, verified and not a finding.** `CON-956:179-183` says
the confirmed migration "deletes every legacy row". It deletes every legacy
**room-membership** row (`account-roommember.lfe:168-170`) and leaves the
`cbcl-member` row in place — there is no delete of `cbcl-member` anywhere. That
is load-bearing in the design's favour: it means the post-migration state is
member-and-binding, which `REQ-967:66-67`'s rule correctly classifies as *not*
legacy.

---

## Disposition of every 0.4.6 finding

Verified against the 0.4.7 and companion 0.4.2 text **and** against source at
the four baselines, not against the candidate's own disposition table
(`v047:35-45`).

| 0.4.6 finding | 0.4.7 claim | Verified disposition |
|---|---|---|
| **H-1** — `CON-953` declares the Path-B binding tables the complete v2 write set but omits the deployed `account-upgrade-required` guard and the two room tables the confirmed migration rewrites | `REQ-967` plus `CON-956` add `LegacyMemberRequiresConfirmedUpgrade` before preview derivation, enumerate both membership tables, and reserve the migration writer | **CLOSED, and closed better than the minimum correction asked.** All four requested items are present and each is accurate against source. (1) The classification is at `selfsamepairauthority` before display, not at finalization — `v047:52-55`, which is exactly where the prior review said it belonged; the fourth answer exists at `v047:63` and is mapped beside the others at `v047:73-76`. (2) `CON-956:166-169` adds both room tables marked read-only with no v2 write or rollback, and `:179-186` states that `confirmed-migrate-path-a-handle/3` is the only sanctioned writer, requires literal `confirmed`, and takes both whole-table write locks — I re-traced the transaction statement by statement (`account-grants.lfe:118-141` plus `account-roommember.lfe:134-172`) and every clause of that description is exact, including the write-then-delete order at `:152-170` and the post-commit notice at `:137-139`. (3) `REQ-967:78-86` keeps SPEC-053 `REQ-033` as sole authority and re-verifies that `confirmed-migrate-path-a-handle/3` has no production caller — I re-confirmed. (4) `TEST-1075` and `TEST-1076` are the two requested rows, and `TEST-1076`'s "No production caller or gate is inferred from that test fixture" is the right guard. **The branch-(a) and branch-(b) failures of the prior review are both foreclosed:** no ceremony reaches finalization for a legacy member, so neither the post-consent `account-upgrade-required` abort nor the silent room-stranding write is reachable. I also checked the state machine for a TOCTOU between the pre-preview query and finalization and it is **closed by construction**, which the amendment does not argue but could: the classification is monotone. A `cbcl-member` row can never appear for a handle that already has a binding, because `enrol-facts:309-310` routes such a handle to `account-facts`, which never calls `enroll-or-check`; no `cbcl-account-binding` row is ever deleted; and `confirmed-migrate-path-a-handle/3` can only move a handle from legacy to eligible. **H-1, H-2 and H-3 above are consequences of this correction, not a reopening of it.** |
| **M-1** — `descriptorDigest` is a required member of `OfferCoreV2`, of the `CON-214` evidence and of the seven-member hint, and no clause defines its preimage for a `cbclPairingRelays` descriptor | `REQ-968` defines exact RFC-8785 descriptor bytes, SHA-256/base64url encoding, corpus cases, and independent recomputation | **CLOSED, and it turns out to codify the shipped implementation.** `v047:90-103` fixes the preimage as the RFC-8785 serialisation of the complete six-member selected descriptor and the digest as `BASE64URL-NO-PAD(SHA-256(...))`. `crates/selfsame-app-identity/src/profile.rs:880` already computes `digest: sha256(&json::canonicalise(item))` over the whole descriptor item, and `json.rs:257-261` is the RFC-8785 writer the same crate uses for the profile digest — so the two-branch rule matches the recogniser byte-for-byte and needs only the base64url encoding at the boundary. Scope is right and necessary: `SPEC-004:2634-2636` scopes the parent rule to "the complete **rendezvous** descriptor, including all three pairing fields", and `SPEC-004:3276-3287` (`CON-209`) puts `descriptorDigest` in the **v1** hint, so a cutover-v1 profile really did have an undefined preimage — `v047:90` covers cutover-v1 and credential-v2 and `v047:112` keeps the parent rule scoped to rendezvous-v1. No residual ambiguity: all six members are ASCII strings or uints in 0..65,535, so RFC-8785's number and string rules are unambiguous and no locale or Unicode-escape question arises. **I reproduced it in a second stack.** Decoding the live profile from `cbcl-chat-selfsame-application-gate.lfe:31-55` and canonicalising its sole descriptor gives 236 octets, `{"conformanceEvidenceDigest":"cCnyIp8IxUiyUjCUBbrZ5b3flzqMM8c-S_tUCPdCwUI","operatorId":"anuna-1","priority":10,"privacyPolicyDigest":"iMyDopkyqq32vQaPSubGHBzUHIZWSUXwjN8CbAmwKGs","relayOrigin":"https://chat.anuna.io:9443","weight":100}`, whose `descriptorDigest` is `AZGpsIW9xuv0M7I5bA6ZqZr151dQTQHAEaA7n4fMWEM`. `TEST-1077:302-306` is authorable exactly as written, including the "copy a hub value without local recomputation and require the API/type gate to fail" row that closes the dangerous repair the prior review named. |
| **M-2** — `REQ-965`'s operator workflow requires `reinstate/2` on a false rationale, and its only actual effect is to delete a revocation marker | `REQ-969` removes `reinstate/2`; the distinct v1 tombstone remains as the durable withdrawal record | **CLOSED, exactly as the minimum correction asked, with the true rationale.** `v047:121-124`: "Because credential/v2 derives a different home DID and alias URI, the v1 tombstone does not block the v2 alias check. The operator SHALL NOT run `reinstate/2`; the v1 tombstone remains in force as the durable record of the withdrawn v1 generation." I re-verified the whole chain: `hierarchy.rs:266-297` derives the home key from root, applicationId and accountScopeId, and the crate's own test `:400-406` asserts a one-octet ApplicationId change changes it; `cbcl-core-webfinger.lfe:35-40` gives `account-uri(home-did) = "acct:ss-" || base32(SHA-256(home_did)) || "@" || authority`, so distinct DIDs give distinct URIs; `webfinger-store.lfe:139-149` reads the tombstone at the URI being provisioned, so `uri2` is clear; and `:207-229` (`tombstone`) deletes the account row, so `create-or-return-live:152-162`'s both-absent branch fires. `v047:125-126` correctly keeps the same-DID mistaken-withdrawal repair as the separate `REQ-958` case, and `v047:128-131` correctly separates v1 alias state from Path-A membership eligibility. `TEST-1079:316-321` asserts the tombstone is byte-for-byte unchanged and that `reinstate/2` is absent from the transition trace, which is the assertion the prior review said `TEST-1069` could not make. |
| **M-3** — every durable-authority claim is made over a store the deployment's own configuration says is redeploy-durable only, while did-crdt is gated on continuous readiness | `NFR-909` records the actual fail-open/fail-closed consequences and makes off-machine backup/restore a production-allocation hold | **CLOSED on the substance; incomplete in one direction — M-2 above.** The prior review offered two options and 0.4.7 takes the stronger one: an enforceable hold plus a continuously evaluated gate, not a residual-list entry. `v047:135-138` states the persistence model exactly as source has it (`cbcl-chat-mnesia.lfe:10-13`, `config/router.sys.config:48-60`, `fly.toml:22-31,53-63`); `v047:140-143`'s three consequences are each correct and correctly classified; `v047:145-153` defines the backup scope, the authenticated/encrypted/versioned snapshot, the retention policy and a machine-loss restore drill reproducing exact row bytes, indexes, tombstones, pending compensation, schema version and authority queries; `v047:145-146` forbids any clause calling the state permanent or disaster-recoverable; `v047:155-158` marks dev/staging non-production and makes `hub-authority-off-machine-ready` continuous and independent of did-crdt `durable-ready` with both required; `TEST-1080:323-331` is a real executable row including the delete-each-table-class negative. `ADR-940:269-273` states the principle correctly. The residue is that the consequence list is presented as exhaustive and the table list as complete, and both omit a fail-open ledger. |
| **L-1** — cutover-v1 descriptor grammar pinned by pointer while the v2 branch is pinned to an exact table | `CON-957` pins the shared exact table | **CLOSED.** `v047:196-203` gives both branches the same `CON-933` table verbatim: `operatorId` 1..63 ASCII matching `[a-z0-9][a-z0-9-]{0,62}`, canonical HTTPS `relayOrigin`, base64url-no-pad 32-octet digests, `priority` uint 0..65,535, `weight` uint 1..65,535, and refusal of unknown/duplicate members, duplicate operators/origins, invalid digests/origins and out-of-range values. Every clause matches `profile.rs:822-892` exactly — `provider_id` at `:846`, `bounded_range` 0..=65,535 at `:895-901`, the positive-weight check at `:856-857`, `decode_b64url_32` at `:859-872`, duplicate operator and origin at `:883-891`, and the 1..16 array bound at `:824-828`. The two stacks can no longer legitimately differ on `priority: 100000` or `operatorId: "Anuna_1"`. |
| **L-2** — the two v1 branches have no selection rule and no refusal identity | `CON-957` pins deterministic precedence, tokens and reporting order | **CLOSED, and the precedence is total.** `v047:205-212` gives four ordered rules on top-level relay-family presence: both present gives `ProfileV1MixedRelayFamilies`; neither gives `ProfileV1MissingRelayFamily`; only `rendezvous` gives rendezvous-v1; only `cbclPairingRelays` gives cutover-v1. `v047:214-215` adds `ProfileV1PairingRecordRelaysForbidden` for the cutover branch. I enumerated the shapes and found no gap: `pairingRecordRelays` with neither family falls to rule 2 deterministically, and with `rendezvous` falls to rule 3 where it is the legitimate optional tenth member (`SPEC-004:2469-2472`). `v047:216-218`'s reporting order — missing common required members in `CON-201`'s fixed member order, then unknown members in code-point order, then nested value errors depth-first — is well-defined: `SPEC-004:2469-2472` lists the members in a fixed order and `profile.rs:80-90` implements that same order. `v047:218-219` puts every token and path in the `CON-226` corpus, which is what `SPEC-004:4643,4652-4658` demands. Implementation cost is real but bounded: `profile.rs:381-390` currently checks the closed member set *before* reading `profileVersion`, so the dispatcher inverts that order, and `ProfileError` (`:98-119`) gains the three closed tokens. |
| **L-3** — the rendezvous-v1 branch has no stated semantic action and names the two forbidden engines | `CON-957` states recognition-without-selection | **CLOSED on the substance; authority residue is L-4 above.** `v047:221-226` states it in the words the prior review asked for, and I confirmed it holds mechanically against `SPEC-007:249-252` (`REQ-806`), `:260-265` (`REQ-807`), `:660-667` (`CON-806` selection) and `v046:79-83`. `TEST-1078:313-314` adds the requested assert-refusal-to-allocate row. |
| **L-4** — `CON-018`'s control arm admits a `logicalLength` adopted `CON-015` forbids | companion 0.4.2 `CON-019` makes `1..2048` normative | **CLOSED for the grammar; test residue is L-3 above.** `companion042:52-70` replaces the arm with `3: 1..2048`, keeps `4: bstr .size 4096`, and restates the encoded lengths as 4,142/4,143/4,144 for 1..23/24..255/256..2,048. `companion040:338` ("The complete control is at most 2,048 octets and depth 8") is the bound being encoded, and `companion042:29-30` says so. `companion042:74-79` (`ADR-017`) gives the right reason: the fixed allocation exists to make ciphertext sizes less revealing, not to widen accepted input. `v047:251-253` propagates it to `NFR-908`'s third row and keeps 4,144. I recomputed every affected figure and none moves. |
| **L-5** — `CON-952`'s "a later operation" leaves the non-crash restore path ambiguous | `CON-958` allows the running operation to write its requested root after verified marker removal | **CLOSED on the substance; ledger residue is L-2 above.** `v047:235-241` answers the question the prior review asked, and separates the crash case ("A crash at or after marker commit loses the in-memory candidate; startup recovery converges to no root and never reconstructs one") from the non-crash case, with `TEST-1081:333-339` testing both distinctly. Verified expressible against `custody.rs:190-198` and `:200-232`, and against `store.rs:209-219`, whose `get`/`set`/`delete` are plain backend calls with no presence edge. |
| **L-6** — `CON-951` requires "the generator" to pin a rendezvous-v1 fixture; the only generator hard-refuses `rendezvous` | `CON-957` assigns rendezvous-v1 to the Selfsame corpus and keeps the generator refusing | **CLOSED, and the split is correct in both directions.** `v047:228-231`. I re-verified `scripts/gen-production-profile.mjs:306-320`: it walks every key at every depth and pushes a hard error for `rendezvous`, `pairingUrl`, `pairingProtocol` and `pairingRoute`, citing `SPEC-075 REQ-007` — whose text at `specs/SPEC-075-cbcl-pairing-relay-cutover.md:229-236` requires exactly the six-member `cbclPairingRelays` descriptor and forbids those four members. The Selfsame side has the corpus: `test-vectors/spec-004-v1.json` carries the `con_201_application_profile` through `con_226_corpus_self_description` groups and `scripts/` holds no profile generator. |
| **L-7** — 0.4.6 amends `CON-201` version 1 while the parent's zero-published-profile premise is no longer true | `CON-958` retires the premise and pins the live bytes | **CLOSED, and the record is exact.** `v047:243-249` states the retirement, names the URL, and pins "The 1,235-octet profile ... with SHA-256 base64url-no-pad `asBvYnvPMHVVU8zkFaFoz_FJg1DaZGSHQ5fxKxC8SZk`, was published and recognized on 2026-08-22", plus the forward rule that future in-place v1 amendments need their own compatibility proof. **I decoded and recomputed all of it.** The base64url in `cbcl-chat-selfsame-application-gate.lfe:31-55` decodes to exactly 1,235 octets; its SHA-256 is `asBvYnvPMHVVU8zkFaFoz_FJg1DaZGSHQ5fxKxC8SZk`, i.e. hex `6ac06f627bcf30755553cce415a168cff1498350da6464874397f12b10bc4999`, byte-identical to the constant asserted by `cbcl-chat-gate-closed-tests.lfe:27-32`; its top-level member set is exactly `accountAuthority, allowedPermissions, applicationId, cbclPairingRelays, enrollment, profileVersion, revocation, stateResolvers, verifierAudience`, which is `profile.rs:80-90`'s `TOP_LEVEL_REQUIRED` with `TOP_LEVEL_OPTIONAL` empty; `profileVersion` is 1; and re-serialising the parsed object in JCS form reproduces the input byte-for-byte. `v047:253-255` also closes the last un-listed stale sentence the prior review noted (`v044:637`). |

---

## Disposition of every carried prior finding

I re-checked each against 0.4.7 and `companion042` rather than assuming a
previous closure survives a new amendment.

### 0.4.5 findings, as carried by the 0.4.6 review

| 0.4.5 finding | State at 0.4.7 |
|---|---|
| **H-1** — the version-1 branch rejects the one frozen production profile | **Still closed, and further hardened.** `CON-957` adds the precedence and tokens the two-branch dispatcher was missing. I independently re-decoded the live octets and re-confirmed they satisfy the cutover-v1 member set, and that `profile.rs:80-91` is that member set in that order with no optional members — so `v046:84-87`'s client-rollback claim remains true and `TEST-1066` remains authorable. |
| **H-2** — credential/v2 reverses cbcl-pairing `CON-007` outside every amendment channel | **Still closed.** `companion042` touches only `CON-018`'s control arm; `companion041:8,45-61,101-138,209-247` are unchanged. `v047:8` carries the same `amends:` list plus 0.4.2. |
| **M-1** — the reset crash rule assumes an unreachable valid slot | **Still closed.** `v046:215-220` is unreplaced and `v047:235-241` touches only the purge/new-root path. |
| **M-2** — revisionless rollback can restore an operator-lifted tombstone | **Still closed, and `REQ-969` strengthens the operator side.** `v046:104-122` is unreplaced; `v047:121-124` removes the one place the effective set instructed an operator to delete a tombstone. I re-traced the concurrency: `withdraw/2` (`webfinger-store.lfe:199-229`) takes the account-key write lock, then the alias-key write lock through `alias-agrees?:232-237`, then writes at the URI oid; `reinstate/2` (`:295-308`) deletes at the same oid; both serialise against `REQ-964:112`'s alias-key write lock. |
| **M-3** — distinct ApplicationId makes v1/v2 bindings mutually exclusive | **Still closed on the substance, and the defective transition procedure is now repaired — see 0.4.6 M-2.** `v046:126-136` is unreplaced except for the workflow paragraph. `provision/3` still has no production caller. |
| **M-4** — did-crdt gossip bypasses the per-DID lock | **Still closed and still exhaustive.** 0.4.7 does not touch did-crdt. I re-enumerated every `docs.lock()` site at `1f409a4`: `src/service/handlers.rs:99` (create insert), `:193`/`:210`/`:444`/`:477` (reads), `:291` (delta merge), `src/sync/live.rs:161`/`:261`/`:340`/`:351`/`:363`/`:484`, `src/sync/dht.rs:372` (key list), `src/service/server.rs:209` (startup recovery) and `:301`. The only mutations are `handlers.rs:99`, `handlers.rs:291`, `live.rs:351` and `server.rs:209`, exactly `CON-954:274-277`'s list. `CON-954` remains real work in the right direction: `handlers.rs:99` swaps memory and only then calls `persist` at `:104`, and `persist:440-452` swallows a save failure with `eprintln!` plus a metric — the precise inversion `CON-954:284-292` forbids. `handlers.rs:401-412` still derives `storage: "durable"` from `persistence.is_some()` alone, so `CON-954:298-300`'s continuous readiness is new work, correctly scoped, and `NFR-909:157-158` correctly makes it independent of and additional to the hub gate. |
| **L-1 / L-2** — object-head and control-size equalities | **Still closed, and I re-derived all six cases from the CDDL.** With `companion042`'s tightened arm: control 4,142 / 4,143 / 4,144 at 1..23 / 24..255 / 256..2,048; large 64,558 / 64,559 / 64,560 at 1..23 / 24..255 / 256..62,000. `v047:251-253` moves `NFR-908`'s third row to match. |
| **L-3 / L-4** — carrier field list and misplaced expiry rule | **Still closed.** `v046:308-311` and `companion041:119-138` are unreplaced. |
| **L-5** — purge recovery convergence | **Still closed.** `v047:238-241` keeps the one recovery result and adds only the non-crash write permission. |
| **L-6** — `CON-014`'s CDDL admits a forbidden `logicalLength` | **Closed for the large class; the control class is now tightened by `companion042` — see 0.4.6 L-4 and L-3 above.** The kind partition remains exact and total: kinds 0 and 9 large, kinds 1..8 and 10 control, union 0..10 against `companion040:276-281`. |
| **L-7 / L-8** — `NFR-906`'s component list, `SPEC-075` naming | **Still closed.** `NFR-908` supersedes the component list; `v047:9` keeps `SPEC-075 REQ-007` under `conforms-to:`, which I re-verified is right — both the frozen v1 bytes and a conforming v2 output satisfy `SPEC-075:229-236` unchanged. |

### 0.4.4 findings, as carried by the 0.4.5 and 0.4.6 reviews

All eighteen remain closed. I re-checked the four that touch anything 0.4.7
moves:

- **M-3** (`CON-209` hint changed without a defined `descriptorDigest`
  preimage) — the preimage half is now closed by `REQ-968`; the authority half
  was already closed (`CON-209` is in `v047:8`).
- **M-5** (three outward-facing one-way changes without rollback ordering) —
  still closed; 0.4.7 adds no new outward-facing change. The store half gains
  `NFR-909`'s restore drill, which strengthens it.
- **L-1** (table/module and handle/account confusion) — still closed, and
  **L-6** above is the one remaining precision residue, now in `CON-956` rather
  than `CON-953`.
- **L-5** (idle budgets exceed 600 s) — still closed. `v045:410-420`'s deltas
  180+30+30+90+45+60+75+45+35 = **590**, strictly inside `MAX_TTL_SECONDS = 600`
  (`cbcl-pairing/src/mailbox.rs:11`). `REQ-967` does not add a checkpoint: the
  query fits inside the existing 240-second "offer prepare/finalize, delivery,
  authorization, request consumption" row, and `v045:417`'s "authority
  comparison/binding result **delivered**" row at 435 s is about the allocator's
  control frame, which `REQ-967:73-75` correctly still places after the preview.

**0.4.3, 0.4.2 and 0.4.1 findings** remain closed. I re-checked the two carried
as substantive: the v2 nine-name grammar at `v044:641-651` is still
byte-for-byte `profile.rs:80-91` in the same order, and the PAIR1 bit arithmetic
(`v044:726-733`: 128 + 128 + 16 + 3 = 275 = 55 x 5, eleven groups of five
Crockford Base32 characters) is unchanged and correct.

---

## Inherited-hard-stop audit

| Stop | Owner | Hard stop? | 0.4.7 position | Verified |
|---|---|---|---|---|
| SPEC-008 REQ-902 | SPEC-008 0.2.2 | yes | fixture prohibition retained | OK. `TEST-1040` release-blocking and unchanged; `scripts/check-spec008-fixture-absence.sh` present at the code baseline and still binary-level evidence for the compile-time `local-pairing-demo` boundary. |
| SPEC-008 REQ-905 | SPEC-008 0.2.2 | yes | carrier non-navigable, no `C`/`T` | OK. Unchanged by 0.4.7. |
| SPEC-008 REQ-906 / 907 / 908 / CON-903 | SPEC-008 0.2.2 | yes / yes / no / — | replaced / retained / retained / retired | OK. `v047:365-367` keeps production allocation closed and denies deploy authority. |
| SPEC-008 REQ-903 / REQ-904 | SPEC-008 0.2.2 | no | recorded at `v045:88-93` | OK, unreplaced. |
| SPEC-007 REQ-803 | SPEC-007 | yes | retained | OK. `CON-206` still gates acceptance. |
| SPEC-007 REQ-804 / 805 | SPEC-007 | yes | amended for v2 | OK. Unchanged by 0.4.7. |
| SPEC-007 REQ-806 | SPEC-007 | yes | retained; consequence now stated | OK. `v047:221-226` states that recognised rendezvous-v1 yields no eligible descriptor, verified against `SPEC-007:660-667`. |
| **SPEC-007 REQ-807** | SPEC-007 | yes | retained; narrowed by an unnamed reading | **OK with a residue — L-4.** `SPEC-007:262-263` says "reject every legacy carrier and **descriptor**"; `CON-957:211` requires complete acceptance of a profile whose descriptors are exactly those. The narrowing (recognition is inert) is stated and true; the authority for it is not recorded. |
| SPEC-007 REQ-809 / NFR-801 / production gate | SPEC-007 | yes | retained without exception | OK. `v047:365-367`. |
| SPEC-007 REQ-810 / 811 / 812 / 813 / NFR-802 / CON-801 / CON-802 | SPEC-007 | mixed | retained / amended | OK. Unchanged by 0.4.7. |
| SPEC-007 CON-806 | SPEC-007 | no | amended and re-pinned by `CON-957` | OK. The one-operator availability exception remains an open production gate (`SPEC-007:654-655`), correctly carried as a residual and warned about at `gen-production-profile.mjs:322-324` for the live one-relay profile. |
| SPEC-002 REQ-103 | SPEC-002 | — | conformed to | OK, though see **M-1**: an undefined `legacyKeyDigest` is the one place a person-visible key rendering is left unpinned. |
| cbcl-pairing REQ-002 / 003 / 005 / 008 / 009 / 013 / 014 / 015 | SPEC-001 | mixed | retained; amended only through the companion channel | OK. `companion042` touches only `CON-018`. |
| cbcl-pairing NFR-001 through NFR-012, CON-001 through CON-010 | SPEC-001 | mixed | retained or amended through the companion | OK. `NFR-002`'s 16-frame per-membership cap holds with margin — see the bounds audit. |
| cbcl-pairing production gates | SPEC-001 | yes | retained in full | OK. `companion042:93` re-states them. |
| SPEC-004 CON-201 version 1 | SPEC-004 | Tier-1 | amended; premise retired | OK. `v047:243-249`. |
| SPEC-004 CON-204 / 206 / 209 / 211 / 214 / 219 / 220 / 221 / 226 | SPEC-004 | Tier-1 | amended; owner approval required | OK as declared. `CON-221`'s applicability enumeration is now `REQ-967`'s, with the naming residue at **L-1**. |
| SPEC-004 REQ-215 | SPEC-004 | — | conformed to | OK. |
| PROTO-004 CON-502 / CON-503 | PROTO-004 | — | conformed to, non-use recorded | OK. `v046:325-327` unreplaced. |
| did-crdt HTTP and sync service contracts | did-crdt | — | amended | OK. Unchanged by 0.4.7; re-verified against `1f409a4`. |
| cbcl-bus SPEC-075 REQ-007 | cbcl-bus | — | conformed to, not amended | OK, and `CON-957:228-231` now depends on it staying that way. Verified at `gen-production-profile.mjs:306-320`. |
| cbcl-bus SPEC-053 REQ-033 | cbcl-bus | Tier-1 | conformed to, unchanged | OK. `REQ-967:78-86` and `CON-956:179-186` describe it accurately and reserve it. |
| **cbcl-bus SPEC-053 GATE-00** | cbcl-bus | **Tier-1 landing discipline** | **not named anywhere in the lineage** | **BREACHED AS PLANNED — H-3.** `SPEC-053:5240-5248` clause 1 and `:5282-5296` clause 4 forbid a configurable account gate and an inert account endpoint; `CON-947:388-392` ships exactly that. Not in `amends:`, not in `conforms-to:`, not in any prior review's table. |
| **cbcl-bus SPEC-053 GATE-01 (Path-B admission)** | cbcl-bus | **Tier-1, no-go** | **not named; silently required by the positive path** | **BREACHED AS PLANNED — H-1.** `cbcl-chat-path-b-gate:profile/0` returning `#(error tier-1-unapproved)` is what makes the v2-eligible class empty and makes an account-bound handle unauthenticable. The amendment's step-4 premise requires it open. |
| Owner substitution | — | — | forbidden | OK. `v047:357-358` and `companion042:93` both keep owner direction subordinate to a fresh-context PASS. Respected by this review. |

---

## Amendment-authority audit

- **Frontmatter integrity.** `v047:8` (`amends:`) reproduces 0.4.6's list and
  extends the cbcl-pairing clause to "0.3.0, 0.4.0, 0.4.1, and 0.4.2".
  `v047:10` supersedes 0.4.6; `v047:11` adopts 0.4.4/0.4.5/0.4.6 except named
  replacements; `v047:12` names the 0.4.6 review and the four companions;
  `v047:13`'s `implementation-baselines` are accurate — I confirmed
  `c6af6df` to `19c0f2b` is thirteen added spec files and `8e55160` to
  `5b48e3e` is four added spec files, so both declared code ancestors hold
  byte-for-byte.
- **Local anchors.** All seven
  `[[spec-008-0.4.7-standalone-tofu-first-contact#X]]` targets — `REQ-967`,
  `REQ-968`, `REQ-969`, `NFR-909`, `CON-956`, `CON-957`, `CON-958` — exist as
  `###` headings (`v047:49,88,114,133,162,194,233`), and the prefix-anchor
  convention matches `SPEC-004`'s. No dangling local anchor.
- **External anchors.** Every referenced target resolves:
  `SPEC-007 REQ-806`/`REQ-807` at `specs/SPEC-007-cbcl-pairing-cutover.md:249`
  and `:260`; `CON-806` at `:645`; the ten `SPEC-004` contracts;
  `PROTO-004 CON-502`/`CON-503`; `SPEC-002 REQ-103`.
- **cbcl-pairing channel.** `SPEC-001:1486-1496` permits amendment only through
  a reviewed merged revision with updated assets, tests, evidence and
  compatibility disposition. `companion042` is that vehicle for `CON-018`, and
  supplies `REQ-024`, `NFR-016`, `CON-019`, `ADR-017` and `TEST-051`. **The one
  gap is L-3**: it does not retire `companion041 TEST-050`'s now-ungrammatical
  witness, and `v047:42` asserts a replacement that only cbcl-pairing can make.
- **Selfsame channel.** `v047:8`'s SPEC-004 and SPEC-007 lists are unchanged
  from 0.4.6 and remain sufficient for what 0.4.7 actually changes: `REQ-968`
  narrows `CON-201:2634-2636`, which is named; `REQ-967` changes `CON-221`
  applicability, which is named; `CON-957` scopes `CON-806`, which is named.
- **cbcl-bus channel.** `v047:9` claims conformance to `SPEC-075 REQ-007` and
  `SPEC-053 REQ-033`, both verified accurate. **`SPEC-053 GATE-00` and
  `GATE-01` are absent — H-3 and H-1.** This is the one place 0.4.7's authority
  accounting is materially incomplete rather than merely terse: the design
  changes what may exist on `cbcl-bus`'s `main`, and the clause that governs
  that is unnamed.
- **Unnamed replacement.** `v047:251-255`'s effective-text ledger names three
  replacements and omits the `CON-952:228-229` one — **L-2**.

---

## State and effect-order audit

- **Pre-network.** Unchanged and sound: carrier recognition with no I/O, the
  explicit origin prompt, the burned contact capability, then the ledger.
  Re-verified that `store.rs:209-219`'s `get`/`set`/`delete` carry no presence
  edge, so `CON-952`'s refusing read and available gated reset coexist without a
  custody call.
- **The new pre-preview edge.** `REQ-967:52-55` inserts the account-authority
  query between authenticated offer recognition and every wallet effect. Traced
  against the call graph: `CON-939:902-912` allocates, `:914-946` prepares the
  core after both Finished, `:948-962` finalizes with the device proof, then the
  query, then `CON-936`/`v045:259-262` recognition-and-display, then
  `REQ-946:292-317`'s preliminary approval and `preview_issuer`, then
  `CON-938`'s six-row effect table. The ordering is correct and the wallet-side
  prohibitions are the right ones; **the actor mismatch is L-5**.
- **Classification stability.** The query is a point-in-time read whose locks
  release at transaction end (`CON-956:171-173`), and nothing re-checks at
  finalization. I looked for a TOCTOU and there is none, because the
  classification is **monotone**: a handle with a binding row never reaches
  `enroll-or-check` (`enrol-facts:309-310` routes to `account-facts`), no
  `cbcl-account-binding` or `cbcl-member` row is ever deleted, and
  `confirmed-migrate-path-a-handle/3` can only move legacy to eligible. The
  amendment does not make this argument and would be stronger for making it.
- **Effect gating.** `CON-938`'s six-row effect table is unchanged and remains
  the unique gate: no derivation before preliminary approval, no signature,
  publication, issuance write or payload before the final decision. `TEST-1025`
  still requires a total call order after the final-decision hash, and
  `TEST-1075:286-288` adds the pre-preview zero-effect assertions.
- **Custody counts and closure boundary.** Unchanged: three successful-path
  openings plus a fourth for post-payload `CON-210` compensation.
  `custody.rs:312-315` types the callback as a **synchronous**
  `impl FnOnce(&HierarchyRoot) -> T`, which is why `CON-955` item 10's
  signing/publication split is necessary rather than cosmetic; 0.4.7 does not
  disturb it.
- **Root lifecycle.** `CON-958:235-241` is expressible: `custody.rs:190-193`
  refuses `restore` while an identity exists, so the old root must be gone
  before the new one is written, and `custody.rs:207-218` draws a fresh
  16-octet Argon2id salt and two fresh nonces per seal, so no old slot opens
  under a new root. The contradiction with `CON-952` is **L-2**.
- **Acyclicity.** I rebuilt the graph with 0.4.7's additions and found no cycle.
  `descriptorDigest` is now a defined leaf: descriptor, RFC-8785, SHA-256,
  base64url — independent of `offerCoreDigest`, and it feeds `OfferCoreV2`, the
  `CON-214` evidence and the seven-member hint without any of them feeding back.
  `devicePossessionProof` is still excluded from `OfferCoreV2` and so cannot
  sign itself. Field 2 is an envelope member and the offer's field-4 body
  contains no field 2. The two new digests in `REQ-967` are leaves as well — but
  undefined ones (**M-1**).
- **One authoritative ApplicationId.** `companion040:236-238` requires the
  recognised profile's `applicationId` to equal `application-context`;
  `companion041:114-118` forbids the profile table making a caller or wire
  string authenticated; `CON-943:258-262` builds `AuthenticatedIntent` only from
  typed fields in the verified core and evidence. `REQ-967:75-76` adds the
  matching prohibition for the new state: "A claimant or caller cannot supply
  the legacy classification." Verified: the handle reaching the query is bound
  to the session's enrolled key by `verify-and-route:242-264` and
  `resolve-key:643-665`, and the classification is computed and signed by the
  hub (`CON-956:174-175`), not asserted on the wire.

---

## Authority and provenance audit

- **Profile authority.** Unchanged and sound. `CON-957` keeps the
  authenticate-then-recognise property across all three branches, and `REQ-968`
  makes the descriptor commitment locally recomputable on both sides rather than
  copied — `v047:106-107`: "Copying a peer or hub digest without recomputation
  is non-conforming."
- **Account authority.** `selfsamepairauthority` remains the sole source and
  `Unknown` still fails closed (`v047:73`, and `CON-956:175-177` correctly makes
  a corrupt, key-mismatched, partially migrated or mixed representation
  `Unknown` and never `NoBinding`). The new fourth state is computed from
  authoritative rows under write locks. **Which store feeds `Bound` is L-1.**
- **Membership authority.** `CON-956:179-186` is the strongest clause in the
  amendment and every word of it checks out against
  `account-grants.lfe:118-141` and `account-roommember.lfe:134-172`. **The one
  writer it does not name is `bind-account/2` — H-1 item 4.**
- **Operator authority.** `withdraw/2` and `reinstate/2` remain non-HTTP
  operator levers with no production callers, and `REQ-969` removes the one
  place the effective set told an operator to destroy a tombstone. `REQ-964`'s
  read-only-in-both-directions rule is unreplaced.
- **Confirmed-upgrade authority.** Now explicit, correct, and correctly
  reserved — and **the interim invocation the amendment offers is H-2.**
- **Resolver authority.** Unchanged. Publication remains a post-commit
  best-effort discovery effect that cannot reverse acknowledgement, and the
  browser still fetches raw resolution bytes into a WASM recogniser whose fields
  and constructors are private.
- **Durable authority survival.** `NFR-909` is the first clause in the lineage
  to make hub authority durability enforceable rather than assumed. **Its scope
  is M-2.**

---

## Implementability audit, per repository

| Repository | Verdict | Notes |
|---|---|---|
| `cbcl-pairing` (spec plus code at `8e55160`) | **Implementable.** | `companion042` is a two-character CDDL change plus a vector row. Every v1 constant remains untouched; `src/mailbox.rs:7-15` confirms the TTL and frame constants the bounds rest on. One low: **L-3**. |
| `selfsame` pure core | **Implementable.** | `REQ-968` is already computed at `profile.rs:880` and needs only base64url at the boundary. `CON-957` needs the dispatcher inversion (`profile.rs:381-390` currently checks the member set before the version), three new closed error tokens on `ProfileError` (`:98-119`), and a genuinely new rendezvous-v1 branch — `PROTO-002 CON-301` and `PROTO-003 CON-401` descriptor grammars are absent from the crate. Low **L-4**. |
| `selfsame` Tauri wallet | **Implementable.** | `CON-958`'s root rule maps onto `custody.rs:190-232` and `store.rs:209-219` unchanged. Low **L-2**. |
| `cbcl-bus` / `cbcl_chat` | **Not implementable as written.** | Three blocking defects, all in this repository. **H-1**: the ceremony has no reachable claimant, because `enrol-commit:454-463` gives every ordinary handle a `cbcl-member` row, nothing deletes one, and `account-facts:352` refuses every account-bound handle while `path-b-gate:profile/0` is closed. **H-2**: the stated interim remedy is destructive and its REQ-033 confirmation content is not derivable at the required time. **H-3**: `SPEC-053 GATE-00` forbids the surface as planned. Beyond those, `CON-956`'s transaction and lock model is correct — I traced every `mnesia:read ... 'write`, `mnesia:lock`, `mnesia:foldl` and `mnesia:index_read` in `webfinger-store.lfe`, `pairaccount.lfe`, `account-grants.lfe`, `members.lfe` and `account-roommember.lfe`, and the lock order it prescribes (binding, then member, then both membership tables) matches `confirmed-migrate-path-a-handle/3`'s actual order, so the two compose without a cycle. `CON-956:188-192`'s "exact-key write locks are a minimum, not the complete Mnesia lock set" is exactly right and closes the prior review's non-finding observation: `account-device-count:204-207` uses `mnesia:foldl` (whole-table read lock) and `unratified-count-tx:70-75` uses `mnesia:index_read` (index-scope read lock). |
| `did-crdt` | **Implementable.** | Unchanged by 0.4.7 and re-verified at `1f409a4`. `CON-954` composes with the existing `DocStore` mutex, covers every writer, and its crash semantics match `src/sync/store.rs:142-161`'s tag-as-commit-point. `BlobStore` still needs the additive read-back-by-DID-tag method. |

---

## Bounds and work audit

Every number recomputed from the CDDL and from source constants, not taken from
a table.

- **Application objects.** Deterministic-CBOR five-entry map =
  1 (map head) + 2 (`0:2`) + 2 (`1:kind`) + 35 (`2:` plus 32-octet bstr with a
  two-octet head) + (2 or 3 or 4) (`3:logicalLength`) + (1 + 3 + body) (`4:`
  plus a three-octet bstr head). Control (body 4,096): **4,142 / 4,143 /
  4,144**. Large (body 64,512): **64,558 / 64,559 / 64,560**. Matches
  `companion042:67-68`, `companion041:183-190` and `v046:167-174` with
  `v047:251-253`'s row correction. Both field-4 heads are three octets because
  4,096 and 64,512 are both in 256..65,535.
- **Outer chain.** Control plaintext 4,144 + 2,068 = **6,212**; ciphertext
  **6,228**; relay body 6,228 + 50 = **6,278**. Large plaintext
  64,560 + 2,068 = **66,628**; ciphertext **66,644**; relay body
  66,644 + 52 = **66,696**. The 50/52 difference is exactly the two extra head
  octets once the ciphertext exceeds 65,535, which only the large value does.
  **The 2,048 cap moves none of these**, because the fixed field-4 allocation is
  unchanged — `companion042:35-37` and `v047:253` both say so and both are
  right.
- **Relay limits.** `cbcl-pairing/src/mailbox.rs:15`
  `MAX_FRAME_BODY_BYTES = 69_632` exceeds 66,696. `mailbox.rs:13`
  `MAX_FRAMES_PER_MEMBERSHIP = 16`; a successful ceremony sends five frames from
  the allocator (cpace-a, finished-a, offer, comparison/binding-confirmed,
  receipt) and six from the claimant (cpace-b, finished-b, intent-approve,
  preparation, final-approve, payload).
- **Queue budget.** 2 x 67,000 + 5 x 7,000 + 4 x 8,192 = **201,768** octets,
  exact.
- **Deadlines.** `v045:410-420`'s cumulative checkpoints 180/210/240/330/375/
  435/510/555/590 have deltas 180/30/30/90/45/60/75/45/35 summing to **590**,
  strictly inside `MAX_TTL_SECONDS = 600` (`mailbox.rs:11`), and
  `companion030:255` bounds `ttl-seconds` to 60..600. `REQ-967` adds no
  checkpoint and needs none, as noted above.
- **Presence code.** 128 + 128 + 16 + 3 = 275 bits = 55 Crockford Base32
  characters = eleven groups of five.
- **Profile and descriptor.** The live document is **1,235** octets, inside
  `CON-201`'s 65,536 ceiling; four permissions inside the 64 cap, sorted by code
  point; one `cbclPairingRelays` descriptor inside 1..16 with `priority: 10` and
  `weight: 100`, both inside `bounded_range`'s 0..=65,535
  (`profile.rs:895-901`); one `stateResolvers` entry inside 1..64;
  `revocation.projection` absent, which `SPEC-004:2629-2630` makes OPTIONAL;
  ceilings 2,592,000 / 900 / 60 each inside their bound. Its canonical
  descriptor is **236** octets.
- **Unreachable frames.** `companion042`'s tightened control arm creates none. I
  checked the largest control body in the effective set — `receipt`, which
  `v044:414-416` binds to the bundle digest, grant ID, issuer DID,
  account-principal digest, device-key fingerprint, local-install digest and
  hub-acknowledgement digest — and it is far inside 2,048 octets. The new
  `LegacyMemberRequiresConfirmedUpgrade` reply is a hub-to-browser session
  reply, not a relay frame, so it does not enter the envelope at all.

---

## Tests and gates audit

`TEST-1075` through `TEST-1082` are authorable as written with three exceptions,
all named above:

- **`TEST-1075`** cannot assert "deterministic key/room digests" — **M-1** — and
  its allocator-side assertion is unstated — **L-5**. Its other clauses (create
  the rows, require the exact signed legacy status, assert zero
  approvals/custody/derivation/signature/publication/resolver writes, corrupt
  each representation and require `Unknown` not `NoBinding`) are concrete and
  correct.
- **`TEST-1076`** is authorable and is the right test. Its positive half
  requires "a SPEC-053 fixture that names the account DID/fingerprint and exact
  rooms", which is constructible as a *fixture*; that the same thing is not
  constructible as a *procedure* is **H-2**, not a defect in this row.
- **`TEST-1080`**'s negative half (current configuration gives
  `hub-authority-off-machine-ready = false`, blocking allocation and offer
  preparation) is authorable today and is real. Its positive half needs a
  successful ceremony to have produced "exact bindings, tombstones, pending
  compensation, grants, memberships, and authority answers" — which **H-1**
  makes unconstructible. Its per-table-class deletion matrix is scoped to a list
  that is incomplete — **M-2**.

`TEST-1077`, `TEST-1078`, `TEST-1079`, `TEST-1081` and `TEST-1082` are each
concrete, executable and red-first-able as written; I could author all five from
the amendment alone. `TEST-1077`'s "Copy a hub value without local recomputation
and require the API/type gate to fail before display" is the right structural
test for the repair `M-1` of the prior review warned about. `TEST-1082`'s static
checks ("the deployment generator never owns a rendezvous fixture, the corpus
does, and no effective clause relies on the retired zero-production-profile
premise") are both mechanisable.

`v047:277-279` correctly carries `TEST-1011` through `TEST-1074` and companion
`TEST-030` through `TEST-051` as release-blocking, with the "except where the
rows below replace an expectation" rule. That rule reconciles `TEST-1073`'s
4,096 control witness with `TEST-1082`'s refusal of it, and `TEST-1067`'s
later-retry requirement with `TEST-1081`'s same-invocation permission. It does
not reach `companion041 TEST-050` — **L-3** — because a Selfsame amendment
cannot replace a cbcl-pairing test row.

`v047:359-360` makes the SPL encoding conditional on PASS, and `v047:361-362`
keeps failing schemas/vectors/corpus/pure-core tests before code and effectful
tests before wiring. Both are right.

---

## Deployment and rollback audit

- **Ordering.** `CON-947`'s release order is unreplaced. Steps 1 and 2 (additive
  recognition with allocation off; did-crdt safe create/CORS) are unaffected by
  0.4.7. **Step 3 — "ship hub side tables and v2 profile route with allocation
  off" — is H-3.**
- **Server rollback.** Feature-gate rollback, not schema rollback; drain,
  compensate, verify no pending ceremony, retain finalized grants and additive
  tables. `NFR-909` adds a real forward precondition rather than a rollback
  hazard.
- **Client rollback.** Still explicit and still true: `profile.rs:80-91` is the
  cutover-v1 branch, so reverting the wallet keeps chat.anuna.io working, and
  only an original rendezvous-v1 profile needs the dual-branch release.
  `CON-957` does not disturb this.
- **Resolver forward-fix boundary.** `CON-949:437-444` retained; once any
  credential/v2 issuer exists the deployment may not roll back to a binary whose
  repeat-create path can replace a DID DAG. Unchanged.
- **Relay drain.** `CON-949` defers to the companion's
  disable/close-or-expire/verify-empty/install sequence; the relay runs
  in-process in `cbcl-bus` (`fly.toml:8`), so the inherited "separate owner
  confirmation before any cbcl-bus or did-crdt deploy" covers it.
- **Machine strategy.** `fly.toml:22-31` is a hard one-machine invariant with
  `strategy = 'immediate'` and no canary; `NFR-909:135-138` and `ADR-940` now
  state the durability consequence of that invariant honestly instead of leaving
  it implicit.
- **No-deploy.** `v047:365-367` keeps production allocation disabled and lists
  the outstanding gates. **The list is missing the two `cbcl-bus` gates the
  positive path depends on — H-1 and H-3.**

---

## Persistence audit

- **Wallet.** Two-slot policy with highest-valid-revision and deterministic
  slot-A tie-break; verified-deletion reset with a reachable crash state; a
  crash-recoverable purge converging to exactly one result; and now an explicit
  non-crash completion rule. All expressible through `store.rs:209-219` with no
  presence edge, and consistent with `custody.rs:190-232`. **L-2** is the only
  residue.
- **Hub.** Transaction and lock model correct and now complete for the
  membership tables; compensation conflict tests correctly split by whether a
  row carries a revision; tombstones outside both write sets and both
  comparisons and no longer destroyed by the operator runbook; schema creation
  separated from all-or-none per-account population; and, for the first time in
  the lineage, an enforceable durability precondition on the store that holds
  the authority. **M-2** is the scope gap; **H-1**, **H-2** and **H-3** are about
  reachability and landing, not about the transaction model.
- **Resolver.** One per-DID durable mutation primitive with clone, validate,
  save, read-back, swap, acknowledge; tag-as-commit-point crash semantics; strict
  production startup; and continuous readiness gating new ceremonies. Sound and
  complete against `1f409a4`; `NFR-909:157-158` correctly makes the two
  readiness gates independent and both required.
- **Browser.** Strict-durability IndexedDB with a restoring failure path;
  pending hub transaction, local commit, finalization and acknowledgement kept as
  separately recorded compensatable stages rather than a false cross-system
  atomic transaction. Correct and unchanged.

---

## Residual-risk audit

**Accepted declared residuals** — bounded, honestly stated, and not defects:

- Relay operatorship and the one-operator availability exception; same-origin
  compromise; deliberate forwarding of both inputs; relay observation of `T`;
  lost-claim mailbox stranding; device-local rollback counters; operator
  availability. Each is a threat-model boundary the design cannot close, and
  each is carried from `v046:502-506`.
- The reverse device-possession check is a browser self-challenge, not
  third-party proof. Correctly labelled and correctly not upgraded.
- `CON-206` v2 step 9 accepts a hub-signed pending authority cache before public
  alias finalization. It genuinely amends the parent's "sole gate on alias
  provisioning" sentence, is named, ledgered and compensated by a
  finalization-gated receipt. Bounded.
- The 180-second first-human checkpoint. Tight but achievable, and it composes
  with the 120-second policy-key cap.
- **The off-machine hub durability hold (`NFR-909`).** I accept this as a
  bounded declared hold. It is stated as a hold, gated continuously, tested by
  `TEST-1080`, and it blocks the thing it needs to block. Only its scope is
  defective (**M-2**), not its status.
- **The Path-A migration hold (`REQ-967:83-86`).** I accept the *hold* as
  bounded and correctly declared. What I do not accept is what surrounds it:
  that the complement class is asserted to exist without being reachable
  (**H-1**), and that the interim remedy offered is destructive (**H-2**).
  Distinguishing these is the point of mandate item 16 — the hold itself is not
  the defect.
- Human cryptography review and independent CPace group math remain open;
  production allocation stays closed.

**Not residuals — defects.** `H-1`, `H-2`, `H-3`, `M-1` and `M-2` are each
stated somewhere in the effective set as a settled fact rather than an open
question, which is what separates them from the list above. `H-1` and `H-2` rest
on asserted premises (`v047:364`, `v047:84`); `H-3` is an unexamined assumption
about another repository's landing rules; `M-1` is a required signed value with
no definition; `M-2` is an enumeration presented as complete.

---

## Methodology

Fresh context. No access to the authoring conversation, and no access to the
attempt-1 transcript — I inferred nothing about any protocol result from that
attempt's failure. No subagents, no delegation.

I read the 0.4.7 candidate in full, then 0.4.6, 0.4.5 and the relevant sections
of 0.4.4, then the 0.4.6 review in full including its 0.4.5 and 0.4.4
disposition tables, then the four cbcl-pairing companions, then the governing
Selfsame specs and the two governing `cbcl-bus` specs.

Every claim about behaviour was checked against source at the four named
baselines rather than against the candidate's disposition prose. In particular I:

- confirmed all four baselines and both declared code ancestors by
  `git diff --name-status`, establishing that no non-spec file has moved;
- traced the `cbcl-bus` session authorisation path statement by statement from
  `handle-frame:205` through `dispatch`, `verify-and-route`, `authorise-frame`,
  `enrol-facts`, `account-facts`, `cbcl-core-enrol:resolve` and `enrol-commit`,
  which is how `H-1` and `H-2` were found;
- enumerated every `mnesia:delete` and every `mnesia:create_table` in
  `apps/cbcl_chat/src/`, establishing that `cbcl-member` and
  `cbcl-account-binding` are never deleted and that the Mnesia table set is
  nineteen tables of which `NFR-909` names eight classes;
- enumerated every production caller of every authority writer
  (`bind-account/2`, `bind-and-store-verified-device-grant/8`,
  `confirmed-migrate-path-a-handle/3`, `store-verified-device-grant/6`,
  `migrate-legacy-handle-tx/3`, `provision/3`) outside `test/` and `_build/`;
- traced the lock acquisition of `provision/3`, `withdraw/2`, `reinstate/2`,
  `bind-and-store-verified-device-grant/8`, `claim/2`,
  `confirmed-migrate-path-a-handle/3` and `migrate-legacy-handle-tx/3`
  statement by statement, and checked the prescribed lock order for cycles;
- read `cbcl-chat-successor.lfe:150-154` and `cbcl-chat-pairaccount.lfe:97-104`
  against `cbcl-core-enrol.lfe:106-126` to classify their loss behaviour, which
  is how `M-2` was found;
- read `SPEC-053 GATE-00` in full (`:5223-5316`), `REQ-033` (`:2343-2363`), and
  `cbcl-chat-gate-closed-tests.lfe`, `cbcl-chat-webfinger-gate.lfe` and
  `cbcl-chat-path-b-gate.lfe`, which is how `H-3` was found;
- decoded the live `chat.anuna.io` profile from
  `cbcl-chat-selfsame-application-gate.lfe`'s base64url and independently
  recomputed its length (1,235), its SHA-256 in both base64url-no-pad and hex,
  its top-level member set, its JCS canonicality, and — in a second stack — the
  `REQ-968` descriptor digest of its sole descriptor;
- compared that member set against `profile.rs:80-91` and the digest against the
  constant in `cbcl-chat-gate-closed-tests.lfe:27-32`;
- read `profile.rs:822-901` and `json.rs:257-261` to confirm `REQ-968` matches
  the shipped recogniser, and `hierarchy.rs:266-297,400-406` plus
  `cbcl-core-webfinger.lfe:35-40` to confirm `REQ-969`'s premise;
- read `custody.rs:180-232,305-330` and `store.rs:195-230` for `CON-958`;
- re-enumerated every `DocStore` mutation site in did-crdt at `1f409a4` and
  re-read `src/service/handlers.rs:401-452`, `src/sync/store.rs:142-161` and
  `src/service/server.rs:209`;
- read both `fly.toml` files, `config/router.sys.config` and
  `cbcl-chat-mnesia.lfe` for the persistence premises;
- recomputed every CBOR length, every derived plaintext/ciphertext/relay
  maximum, the frame counts, the queue budget, the checkpoint deltas and the
  presence-code bit arithmetic from the CDDL rather than from the tables;
- read `gen-production-profile.mjs:295-335` and `SPEC-075:229-240` for the
  fixture-ownership split.

I attempted to reopen the 0.4.6 HIGH and all three MEDIUMs and could not; every
closure is real, and `H-1`'s closure is structurally better than the minimum
correction asked for. The three HIGH findings I report are new, and I state
explicitly that two of them exist **because** of that correction — a guard that
is correct in placement, in table coverage and in authority reservation, and
that at the reviewed baselines has no non-terminal branch.

No candidate, parent, code, fixture, vector or sibling-repository file was
modified.
