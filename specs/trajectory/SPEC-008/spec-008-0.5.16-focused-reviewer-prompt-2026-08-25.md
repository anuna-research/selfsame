---
id: spec-008-0.5.16-focused-reviewer-prompt-2026-08-25
title: SPEC-008 0.5.16 — focused Tier-1 adversarial review prompt
status: active
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-25
---

# SPEC-008 0.5.16 — focused Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Green
tests and a small diff do not establish correctness by themselves.

Review the 0.5.16 correction to the rejected 0.5.15 implementation. Regression
check the unchanged standalone credential/v2 protocol closure.

Do not repair candidate code or specifications. Write only the review report,
commit it in the Circus worktree, and complete through Circus's appended
instruction.

## Authentication and isolation

This review runs through Circus and the interactive Claude CLI. It SHALL use
the person's Claude Max subscription. It SHALL NOT use the Anthropic API.

Both `ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent in the
review process. Check absence without printing either value.

Run `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status`.
Record only `authMethod`, `apiProvider`, and `subscriptionType`.

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier.

Any other authentication result blocks the review. The generating family is
OpenAI GPT-5. The reviewing family is Anthropic Claude.

## Exact candidate set

Use committed files, exact diffs, and clean detached checkouts. Do not
substitute uncommitted working files.

- Selfsame implementation candidate:
  `60abb8b25e858be8005151e47ea2a8044d130e80`
- Selfsame governing authority:
  `cd8d3b5e55cb3581715c596972df9ab944d8fb9b`
- Selfsame preceding implementation:
  `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`
- Rejected review preserved in Selfsame:
  `32217de`
- cbcl-bus implementation candidate:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-bus governing authority:
  `a4f46c47b7cf21c3d210d9998107ba5c27ba0838`
- cbcl-pairing candidate:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs dependency:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt runtime dependency:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`

The person's working trees are not evidence. Do not modify or delete their
output APKs, patch script, or dirty files.

## Governing authority

Read these current parents before deciding:

- Selfsame `SPEC-008-production-pairing-claimant.md` at 0.5.16;
- Selfsame `SPEC-007-cbcl-pairing-cutover.md` at 0.3.7;
- cbcl-pairing `SPEC-001-reusable-blind-pairing.md` at 0.5.7; and
- cbcl-bus `SPEC-053-key-root-identity.md` at 0.17.12.

Verify that every coordinated parent names the same review set. Historical
0.3.6 evidence and changelog entries remain historical.

Run strict descriptive controlled-language lint on both Selfsame parents and
the 0.5.16 trajectory. Lint the changed cbcl-bus parent lines independently;
its unrelated pre-existing full-parent debt is outside this correction.

## Blocking-finding mandate

Read
[[spec-008-0.5.15-implementation-claude-adversarial-review-2026-08-25#M-1 — the screen gate's invoke-surface caller list is hand-maintained, so excluding `src/pairing.js` from invoke discovery leaves the gate green; TEST-1167's third named mutation is not killed]].

Independently verify all of these claims:

1. The harness recursively discovers JavaScript modules below `src`.
2. It derives invoke callers from discovered source rather than a module list.
3. It scans every member of the derived caller set.
4. The reviewed caller-count ratchet fails when discovery loses one caller.
5. The unmutated `CI=true npm run screens` command exits zero.
6. Excluding `src/pairing.js` from discovery exits one.
7. That refusal is an `invoke-surface` caller-count error.
8. Restoring the candidate returns the screen gate to green.

Use a clean scratch copy for the mutation. Restore every mutation after
measurement.

## Protocol regression mandate

Verify that `48a0c749..60abb8b` changes no production source. Verify that the
hub, pairing, dependency, and WASM implementation closure is unchanged from
the independently reviewed 0.5.15 closure.

Regression-check the prior report's independent PASS for F-A through F-E,
authenticated intent display, consent ordering, and transaction cleanup.
Also verify release source authentication, reproducible WASM, and both complete
local capstones.

Do not inherit the verdict mechanically. If a production claim changed or the
focused correction weakens another gate, inspect and test that boundary.

Production allocation, push, release, and deployment remain prohibited.

## Verdict and report

Return PASS only with zero CRITICAL, HIGH, and MEDIUM findings. A failed or
unmeasured required mutation is at least MEDIUM.

Write the report to
`specs/trajectory/SPEC-008/spec-008-0.5.16-focused-claude-adversarial-review-2026-08-25.md`.

Record the exact candidate revisions, commands, exit results, mutation
restoration, auth path, Circus attempt, findings, and final verdict. Record no
secret or account identifier.
