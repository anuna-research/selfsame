---
id: spec-008-0.4.1-claude-adversarial-review-2026-08-23
title: SPEC-008 0.4.1-draft — Tier-1 fresh-context cross-model adversarial review
status: complete
tier: 1
reviewer: independent fresh-context cross-model reviewer (defect-finding mandate)
reviewed-artifact: "[[spec-008-0.4.1-standalone-tofu-first-contact]]"
reviewed-baselines: "selfsame f2501d9 (code parent c6af6df); cbcl-pairing 8e55160; cbcl-bus fdbe414; did-crdt 1f409a4"
date: 2026-08-23
---

# Verdict: REJECT

## Executive reason

0.4.1 is a large and mostly honest improvement over 0.4.0. Its factual claims about
the *existing* code are, with one exception, accurate: I independently reproduced
F-F, F-G, F-H and F-I against the named revisions, and F-A/F-B/F-C/F-D are real
defects that the new requirements address in the right places. ADR-908's treatment
of F-E is the best-handled item in the document.

It is nevertheless not implementable as written, and it does not close the class of
defect that killed 0.3.0 and 0.4.0.

Two findings are CRITICAL.

1. **The hard stop is relocated, not established.** The candidate names three
   decisions and says "No decision substitutes for either later decision", then
   places request-ID consumption, key derivation, issuer creation, grant signature
   and **a new outward DID-closure publication to a public state resolver** after
   decision 2 and *before* decision 3 (`REQ-916:192`, `CON-911:487`). A person who
   reaches the CON-221 comparison and correctly refuses has already caused a
   permanent, publicly resolvable identity artefact at an attacker-chosen scope.
   Because each attempt uses a fresh scope, this is repeatable without bound — the
   same amplification primitive the 2026-08-22 review rejected as F-3/F-4, and an
   identity side effect before approved payload delivery under the inherited
   unwaivable SPEC-007 REQ-812. Publication is a *new* effect: the current wallet
   never publishes (`src-tauri/src/app_grant.rs:598-629`, `published: false`).

2. **Standalone first contact removes the only person-mediated channel binding and
   substitutes nothing.** SPEC-004 CON-227 states plainly that a web binding
   "authenticates … nothing about the carrier of the code", and names CON-221 as
   what closes that gap. In the SPEC-004 ceremony the person reads a link code off
   *their own signed-in browser*, so the attacker is never in the loop. 0.4.1
   replaces that with a cbcl-pairing carrier that SPEC-008 REQ-904/REQ-905
   explicitly admit by scan **or paste** from any source, and then hands the
   wallet's issuer fingerprint to the counterpart in `preparation` *before* the
   human compares (`CON-910:429`). An attacker who allocates from their own chat
   session and delivers the carrier to a victim learns the comparison value first
   and can display it. CON-221 therefore cannot serve as the backstop the design
   relies on, and the only remaining barrier is a self-chosen TOFU chat handle
   rendered with `AuthenticatedAccount` provenance.

Six HIGH findings compound these: the relay-admission anchor rests on a profile
member SPEC-004 does not define and CON-201 requires a recogniser to reject; the
payload bound is factually wrong for this transport and internally contradictory;
the browser cannot obtain the issuer state its own CON-206 facade demands; CON-204
alias provisioning is assigned to a party that structurally cannot perform it;
CON-221 is silently re-specified and left cryptographically unbound; and every
inherited hard stop except REQ-812 is left undispositioned while the design drives
production allocation.

Several load-bearing claims — "the complete CON-214 and CON-219 authorisation
predicate", "all thirteen CON-206 steps", "its normative 69,607-octet payload
bound", "Version 1 remains byte-stable" — are not supported by the cited
specifications or the cited code. Owner approval and the quality of the prose do
not substitute.

A PASS is not available. The corrections below are all local and specifiable; a
0.4.2 that makes them is a plausible candidate.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or implementability
defect; MEDIUM = must be fixed before implementation; LOW = record and correct.

Line references of the form `file:NNN` are to the reviewed baselines. Candidate
references are to
`specs/trajectory/SPEC-008/spec-008-0.4.1-standalone-tofu-first-contact.md`.

---

### C-1 (CRITICAL) — DID publication and request consumption occur before the final decision, making a correct human refusal produce a permanent public identity artefact

**Violated clauses.** `SPEC-007-cbcl-pairing-cutover#REQ-812` (inherited,
unwaivable: "Mismatch, protocol violation, cancellation, relay closure, and expiry
SHALL produce no accepted credential or identity side effect **before approved
payload delivery**", specs/SPEC-007-cbcl-pairing-cutover.md:328-341); the
candidate's own Orientation ("Three decisions, not one … No decision substitutes
for either later decision", candidate:41-50); `SPEC-004…#CON-221` (rejection
"SHALL NOT provision the alias, SHALL NOT accept the grant, SHALL create no
session, SHALL burn the ceremony", specs/SPEC-004-application-scoped-identity.md:4216-4221).

**What the candidate says.** `REQ-916:192-196` — "Only after the local
grant-consent event MAY the wallet consume the request ID, unlock custody, call
`hierarchy::derive`, call `issuer::create`, sign a grant, **publish DID state**,
prepare a CON-221 issuer comparison, or write issuance state." `CON-911:486-490` —
"After grant consent it consumes the request ID, derives/issues, **publishes the
issuer closure to declared state resolvers**, and produces preparation. After
final approval it builds the bundle …". `CON-910:412-416` puts `preparation` and
the person's `final-approve`/`final-decline` strictly after that publication.

**Exploit / failure sequence.**

1. Attacker signs in at chat.anuna.io under any handle, allocates a v2 invitation,
   obtains a hub-signed CON-219 offer for their own account, and delivers the
   carrier to the victim (paste in an email, a web page, a printed QR).
2. Victim's wallet authenticates `https://chat.anuna.io/selfsame/application` live,
   finds the relay descriptor, prompts once for `(chat.anuna.io, chat.anuna.io:9443)`,
   and the victim accepts — chat.anuna.io is a real, correctly authenticated
   application.
3. The signed offer authorises. The wallet displays the exact grant intent. The
   account label reads as a plausible chat handle. The victim approves — decision 2.
4. **The wallet now consumes the request ID, derives the home key for
   `(chat.anuna.io, attacker-chosen scope)`, creates the issuer, and publishes the
   signed closure to the profile's declared state resolvers.** `did.anuna.io` now
   serves a resolvable DID document controlled by the victim's recovery secret,
   permanently correlating that wallet with chat.anuna.io.
5. `preparation` carries the fingerprint. The victim cannot complete a comparison
   (or does and refuses) and sends `final-decline`.
6. The ceremony terminates with no grant — and with the published DID, the burned
   request ID and the derived hierarchy branch all intact. `REQ-916:201-203`
   guarantees byte-for-byte unchanged state only for a decline of *the exact grant
   intent*; it says nothing about `final-decline`, and `CON-910:438` merely says
   "No payload follows decline or final-decline."
7. Repeat. Each attempt uses a fresh `accountScopeId`, so each produces a distinct
   published DID. There is no attempt burn, no rate limit and no compensating
   action anywhere in the amendment.

**Source evidence.**

- The publication is *new*: the current wallet never publishes. `confirm_issuance`
  serialises the closure into the bundle and returns `published: false` —
  `src-tauri/src/app_grant.rs:598-629`.
- There is no publication capability in the net crate at all:
  `crates/selfsame-app-identity-net/src/state.rs` exports `resolve_closure`,
  `submit_revocation`, `confirm_revocation` and nothing else
  (`state.rs:431,695,829`).
- CON-206 does not require the closure to be published before the bundle is sent:
  step 4 admits "the encrypted bundle, local cache, or a profile-declared state
  resolver" (specs/SPEC-004-application-scoped-identity.md:3134-3136), so nothing
  forces publication earlier than immediately before payload transmission.
- `TEST-934:603-605` **entrenches** the defect: it kills mutations that move
  effects *before* grant consent, and then asserts "After consent, publication
  failure sends neither preparation nor payload" — i.e. it makes publication-before-
  preparation release-blocking behaviour. `TEST-933:598-602` snapshots only
  "exact-intent decline", never `final-decline`.

**Minimum design correction.**

- Move every outward and durable effect to after `final-approve`: the permitted
  post-decision-2 work is deriving the home key and computing the issuer DID *in
  memory* so `preparation` can carry the fingerprint. Publication, request-ID
  consumption and any issuance-state write happen only after `final-approve`, in
  the same causal step as building the bundle.
- State a normative post-condition for `final-decline` identical to the one
  `REQ-916:201-203` gives for `decline`, and name the compensating action if any
  effect did occur.
- Add a test row that snapshots resolver writes, the request ledger and issuance
  state across a `final-decline`, and a mutation row that kills moving publication
  earlier than `final-approve`.

---

### C-2 (CRITICAL) — No co-presence binding between the signed-in browser session and the wallet; the CON-221 comparison value is disclosed to the counterpart before the human compares

**Violated clauses.** `SPEC-004…#CON-227` ("A web binding authenticates strictly
less than CON-222 … nothing about the carrier of the code. What closes the gap is
… CON-221 first-enrollment confirmation", specs/SPEC-004-application-scoped-identity.md:4731-4740);
`SPEC-004…#CON-214` step 6 ("renders consent from the authenticated origin and
bound operation rather than caller-supplied presentation metadata",
specs/SPEC-004-application-scoped-identity.md:3699-3700); the candidate's
`REQ-915:170-174` and `CON-909:387-393` display-provenance contract.

**Why the substitution fails.**

SPEC-004's manual cross-device ceremony binds the two ends through a **link code
the person reads off their own authenticated browser and types into their own
wallet** (`selfsame_core::code::LinkCode`, driven by `cbcl_enrol_start(code)` at
`src-tauri/src/app_grant.rs:649`). The person's act of transcription *is* the
proof that the browser session and the wallet belong to the same person. It is the
reason CON-227 can be candid about the carrier being unauthenticated and still
call the design closed.

0.4.1 removes that. The cbcl-pairing carrier is an opaque base64url blob that
SPEC-008 REQ-904/REQ-905 deliberately make identical across camera scan and paste
(specs/SPEC-008-production-pairing-claimant.md:REQ-904/REQ-905), so a carrier can
arrive from any channel. The amendment never requires the carrier to originate on a
surface the person is authenticated to, and never names co-presence as a
precondition.

CON-221 cannot fill the gap here, because 0.4.1 changes who learns the comparison
value first. `CON-910:429` has the **claimant** send its issuer DID in
`preparation`; the allocator then renders the fingerprint. An attacker running the
allocator therefore *receives the exact hex the victim's wallet is about to
display*, in real time, before the victim is asked to compare. A phishing page can
show it. In SPEC-004's ordering — "the application, **after opening the bundle**
and before provisioning the alias, displays the same two values computed from the
grant's `issuer`" (specs/SPEC-004-application-scoped-identity.md:4198-4201) — the
value also reaches the application first, but the application is by construction
the person's own signed-in browser, because the ceremony could only have started
from a code that browser displayed.

**Exploit / failure sequence.**

1. Attacker registers `@a1ice` (or any lookalike) at chat.anuna.io. Handles are
   self-chosen and TOFU-bound: `apps/cbcl_chat/priv/web/app.js` `freshHandle()` and
   `HANDLE_KEY_PAIR`; the hub binds handle→wire key on first use.
2. Attacker allocates a v2 invitation and obtains a hub-signed offer. Under
   `REQ-917:224-227` the offer's `applicationAccountDisplay` is "obtained from the
   verified chat sender" — so it is genuinely signed, and genuinely `@a1ice`.
3. Attacker sends the victim the carrier plus a page that says "scan this to link
   your wallet, then check the code matches".
4. Victim's wallet performs the complete REQ-914 authentication of chat.anuna.io,
   the complete REQ-915 predicate over the signed offer, and every equality in
   `REQ-915:163-176` holds — because nothing in that list is a fact about *whose*
   chat account it is beyond a display string.
5. The wallet renders `Account: @a1ice` with `AuthenticatedAccount` provenance
   (`CON-909:387-390`). The victim approves.
6. `preparation` delivers the fingerprint to the attacker; the attacker's page
   displays it; the victim "compares", matches, and approves.
7. The wallet issues a device grant binding the victim's home DID to the
   **attacker's** chat account and device key, and publishes the alias. The attacker
   now holds a Selfsame-backed account grant at chat.anuna.io issued by the victim's
   wallet, and the victim's wallet believes it linked "their" chat account.

**Source evidence.**

- Provenance: `cbcl-pairing/src/profile.rs:784-798` is the current display path the
  candidate replaces — `display_intent` copies wire `application`, `action` and
  `authority_summary`, and `display()` marks every field
  `claimed_by_secret_holder: true` (`profile.rs:797`). The candidate correctly
  removes this for application ID and origin, and then re-introduces the same class
  of overstatement for the account label.
- The hub authenticates possession of a wire key bound to a handle; it does not
  authenticate that the handle names the person holding the wallet
  (`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:1667-1679`: the claim "binds
  to the VERIFIED sender (`handle`)" — verified as *a* sender, not as *this person*).
- No test row covers this. `TEST-944:634-637` covers a malicious *application*;
  `TEST-937:612-614` covers transplanting an offer to another invitation/account.
  Neither covers a valid offer from a legitimate application, correctly signed, for
  a chat account that is not the scanning person's.

**Minimum design correction.** Any one of the following, stated normatively:

- require the carrier to be obtained from a surface the person is authenticated to
  — i.e. re-introduce a short person-transcribed value that the wallet must accept
  and that enters the v2 CPace channel identifier alongside the invitation secret,
  so a relayed carrier alone cannot complete the ceremony; **or**
- require the CON-221 comparison value to be one the counterpart cannot learn first
  (for example, wallet-side display only, with the application deriving the same
  value independently from a *committed* wallet public key delivered before the
  wallet displays it) — and state the commitment scheme; **or**
- accept explicitly, as an ADR alongside ADR-908, that a first contact carried
  outside the person's own authenticated surface is out of the threat model, and
  make the wallet say so on the consent screen.

In every case: rename the `applicationAccountDisplay` provenance to something
honest (`ApplicationAsserted`), forbid rendering it as an authenticated identity,
and add a test row for the stranger's-session carrier.

---

### H-1 (HIGH) — The relay-admission anchor rests on a profile member SPEC-004 does not define and CON-201 requires a recogniser to reject

**Violated clauses.** `SPEC-004…#CON-201` ("the top-level value is an object whose
member set is exactly the ten names below … **An unknown member at any depth is a
rejection, not an extension point**", specs/SPEC-004-application-scoped-identity.md:2469-2481);
`SPEC-004…#CON-220` step 4 ("run the complete CON-201 recognition before any
semantic action", specs/SPEC-004-application-scoped-identity.md:4132);
`SPEC-007…#CON-806` ("the coordinated SPEC-004 amendment defines
`cbclPairingRelays`" — that amendment has not landed).

**Failure.** `REQ-914:135-136` requires "exactly one recognised `cbclPairingRelays`
descriptor whose `relayOrigin` equals the invitation origin", reached through
CON-220. `REQ-920:287-296` makes normative statements about `operatorId`.
`CON-908:353-360` keys the person-owned policy on it. Every one of the three
decisions the design rests on reads that member.

`grep -n "cbclPairingRelays\|operatorId" specs/SPEC-004-application-scoped-identity.md`
returns **zero matches** at the reviewed baseline. The member exists only in code
(`crates/selfsame-app-identity/src/profile.rs:340,419,822-890`), as a required
eleventh member with 1..16 closed descriptors and unique `operatorId`/`relayOrigin`.
CON-201 as ratified would require a conforming recogniser to reject any profile
containing it — including the ratified chat.anuna.io profile.

The candidate's `amends:` frontmatter lists CON-214, CON-219, CON-801 and
cbcl-pairing SPEC-001. Its "Open items" (candidate:690-694) name a CON-209/214/219
amendment and a cbcl-pairing amendment. **CON-201 and CON-806 appear nowhere.**

**Minimum design correction.** Add the CON-201 profile-grammar amendment
(eleventh member `cbclPairingRelays`, its descriptor grammar, and `operatorId`
semantics as application-asserted) to `amends:` and to the open-items gate, and
state that REQ-914 is unimplementable until it lands. Reconcile with SPEC-007
CON-806's eligibility definition (see H-6).

---

### H-2 (HIGH) — The payload bound is factually wrong for this transport, internally contradictory, and breaks the v1 byte-stability claim it asserts

**Violated clauses.** `SPEC-007…#REQ-814` ("The canonical CON-219 bundle payload
SHALL contain at most 62,000 octets", specs/SPEC-007-cbcl-pairing-cutover.md:359-365);
`SPEC-007…#CON-801` precondition ("The sender fully recognises a canonical CON-219
payload of at most 62,000 octets", specs/SPEC-007-cbcl-pairing-cutover.md:476);
`SPEC-004` cutover disposition ("CON-219 … narrows its cbcl payload bound to 62,000
octets", specs/SPEC-004-application-scoped-identity.md:30); `SPEC-007…#TEST-818`
(specs/SPEC-007-cbcl-pairing-cutover.md:943-947).

**Four distinct defects.**

1. **Wrong normative number.** `NFR-903:303-305` says "every valid CON-219 offer and
   bundle up to **its normative 69,607-octet payload bound**." 69,607 is the
   PROTO-004 bound; PROTO-004 is explicitly *not* on this path
   (`CON-911:461-463`). The normative bound for cbcl carriage is 62,000, set by
   SPEC-007 REQ-814, confirmed by SPEC-004's own cutover disposition, and
   implemented in both repos: `crates/selfsame-pairing/src/lib.rs:85`
   (`MAX_CREDENTIAL_PAYLOAD_OCTETS: usize = 62_000`, enforced at `live.rs:120` and
   `lib.rs:803`) and `cbcl-pairing/src/profile.rs:334`
   (`bounded_bytes(&self.credential, 1, 62_000)`). Raising it is a real amendment to
   REQ-814 and CON-801, neither of which the candidate lists, and it invalidates
   TEST-818's 62,000/62,001 vector.

2. **Contradictory maximum.** `CON-910:441-443` fixes the envelope's logical length
   at "`1..69,607`", and the same table (`CON-910:426`) says the `intent` envelope
   contains "one CON-219 offer **and fresh intent nonce**". A valid 69,607-octet
   offer plus a 32-octet nonce plus any framing cannot fit in 69,607 logical octets,
   so NFR-903's "SHALL admit every valid CON-219 offer" is unsatisfiable at the
   boundary it names. `TEST-945:638-641` tests exactly 69,607 and would fail on the
   intent branch.

3. **Named constant absent; nesting flattened.** NFR-903 says "All enclosing frame,
   relay, CBCL body, and WebSocket bounds SHALL be raised together from **one named
   constant**" and never gives its value. The five layers are not one object; today
   they differ by known CBOR/AEAD deltas and each must strictly exceed the next:

   | Layer | Constant | Today | Needed for a 69,611-octet envelope |
   |---|---|---|---|
   | WebSocket max message/frame | `MAX_WIRE_MESSAGE` | 70,000 (`cbcl-pairing/src/bin/cbcl-pairing-relay-ws.rs:32`) | ≈71,940 |
   | relay mailbox frame body | `MAX_FRAME_BODY_BYTES` | 69,632 (`cbcl-pairing/src/mailbox.rs:15`) | ≈71,910 |
   | CDDL `put`/`frame` body | — | 1..69,632 (`schemas/pairing-v1.cddl:44,78`) | ≈71,910 |
   | sealed-frame ciphertext | — | 17..69,572 (`schemas/pairing-v1.cddl:130`) | ≈71,865 |
   | sealed plaintext | `MAX_SEALED_PLAINTEXT` | 69,556 (`cbcl-pairing/src/channel.rs:21`) | ≈71,849 |
   | sealed-plaintext body | — | 1..65,536 (`schemas/pairing-v1.cddl:135`) | ≈69,781 |
   | application-payload body | — | 1..64,512 (`schemas/pairing-v1.cddl:158`) | ≈69,611 |

   Setting these "from one named constant" without stating the per-layer deltas
   either under-sizes an outer bound or makes an inner bound equal to its
   container — both are recognition failures at the maximum. Note that
   `MAX_WIRE_MESSAGE = 70,000` is already below the requirement.

4. **v1 byte-stability contradiction.** `put`, `frame`, `sealed-frame` and the
   mailbox body bound are shared, version-1-tagged objects. Raising them changes v1
   recognition, contradicting `CON-910:446` ("Version 1 remains byte-stable for its
   existing vectors") and `ADR-909:568-570` ("Add v2 alongside byte-stable v1 for
   vectors"). Concrete v1 assertions that move: `cbcl-pairing/tests/recognition.rs:131`
   (`("body", bytes(69_632, 3))`), `cbcl-pairing/tests/relay_work_bounds.rs:17`
   (`const BODY_BYTES: usize = 69_632`), `cbcl-pairing/tests/relay_service.rs:30`
   (`queue_bytes: 64 * 69_632`). The independent endpoint vector "also compares the
   closed rejection and terminal classifications" (`cbcl-pairing/vectors/README.md`),
   so the rejection boundary is itself a vector.

**Minimum design correction.** State the envelope's fixed size as a number; derive
and tabulate each enclosing bound from it with its explicit overhead; either keep
62,000 as the logical bound (and say so) or amend SPEC-007 REQ-814, CON-801 and
TEST-818 explicitly; give the `intent` envelope a logical bound of
`payload_bound + nonce + framing`; and either introduce a v2-only frame kind so v1
grammar is untouched, or withdraw the byte-stability claim and reissue the v1
boundary vectors.

---

### H-3 (HIGH) — The browser cannot obtain the issuer state its own CON-206 facade requires; "all thirteen steps" is not reachable at these baselines

**Violated clauses.** `REQ-919:264-270`; `CON-912:502-508`; `TEST-941:624-627`
("passes all thirteen CON-206 steps"); `SPEC-004…#CON-206` steps 4-5 and the
session-establishment freshness tier ("Where any declared `stateResolvers` entry is
reachable, the closure SHALL be resolved from one rather than taken from the
CON-219 bundle or a cache", specs/SPEC-004-application-scoped-identity.md:3174-3177).

**Failure.** The vendored browser CON-206 facade is
`verify_path_b_peer_json` / `verify_path_b_peer`
(`crates/selfsame-web-device/src/lib.rs:886,1001`). Its first act is:

```rust
if issuer.source != ClosureSource::StateResolver {
    return Err(DeviceError::Refused);
}
```
(`crates/selfsame-web-device/src/lib.rs:1012-1014`)

It refuses any closure that did not come from a state resolver — including a
bundle-supplied one. So `REQ-919:264`'s "resolve **or consume** sufficiently fresh
issuer state" has only one admissible branch in the code it must use.

The declared resolver cannot be reached from the browser. `did-crdt` applies CORS
headers **only** to the rendezvous routes: `cors_headers()` is defined at
`src/service/rendezvous.rs:47` and used only within that module (lines 61, 179,
188, 196). `GET /:did` (`src/service/server.rs:148`),
`POST /dids/:did/deltas` (`:150`) and `GET /dids/:did/closure` (`:155`) carry none,
and there is no `CorsLayer` anywhere in the crate. A page at `https://chat.anuna.io`
therefore cannot read `https://did.anuna.io/dids/{did}/closure`.

So at the reviewed baselines the browser has exactly zero admissible sources of
issuer state, and `REQ-919` step 4, `CON-912`'s `accept`, and `TEST-941` are
unreachable. The amendment names no CORS work, no resolver proxy, and no
alternative; its `did-crdt` disposition says only "No new pairing route or mailbox
is added there" (candidate:664).

Note also that a hub-proxied resolver is not a free substitute: the vendored
module's own contract says "resolver facts originate in the browser's own resolver
path, never a hub" (`selfsame_web_device.d.ts`, `verify_path_b_peer_json` doc).

**Minimum design correction.** Add a `did-crdt` slice to the cross-repository table:
CORS on `GET /dids/:did/closure` and `GET /:did` (and on `POST /dids/:did/deltas` if
the wallet ever publishes from a browser context), with its own review of what a
permissive read policy exposes. State which `ClosureSource` the browser will hold
and why it satisfies the session-establishment tier. If the bundle closure is to be
admissible, amend `verify_path_b_peer` explicitly and require the recording duty
CON-206 attaches to that fallback.

---

### H-4 (HIGH) — CON-204 alias provisioning is assigned to the browser, which structurally cannot perform it, and no hub command exists

**Violated clauses.** `SPEC-004…#CON-204` ("Before publishing `acct_uri`, the
**application account authority** SHALL create an account record … Provisioning
occurs through the application's authenticated active-account channel",
specs/SPEC-004-application-scoped-identity.md:2869-2877); `SPEC-004…#CON-206`
step 9 ("This step is the sole gate on alias provisioning", :3145-3149).

**Failure.** `REQ-919:263` makes step 2 of the *browser's* ordered list "provision
the recomputed reciprocal account binding under CON-204", and `TEST-941:624` asserts
"browser provisions the alias". The browser is not the account authority; the hub
is. `CON-912:500-513` describes only WASM acceptance and IndexedDB persistence and
names no hub call. The cross-repository table's `cbcl-bus hub` row (candidate:660)
lists "authenticated offer-allocation command; durable account scope and one-use
ceremony state; signed v2 CON-214 evidence; real device-key account grant" — **no
alias-provisioning command**. The `cbcl_chat browser` row lists "alias provisioning"
(candidate:661), i.e. the amendment assigns it to the wrong party in two places.

At the baseline the hub *has* the primitive and no caller:
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:124` defines
`provision/3`, and `grep -rn "cbcl-chat-webfinger-store:" apps/` outside tests
returns only `ensure-tables` (`cbcl-chat-app.lfe:24`) and `lookup`
(`cbcl-chat-webfinger-http.lfe:27`). Nothing provisions. Consequently every
`webfinger::fetch_and_verify` from the wallet returns `NotFound`, and CON-206 step 9
fails closed for every grant the design would produce.

**Minimum design correction.** Move alias provisioning to the `cbcl-bus hub` slice
as a named authenticated command bound to the verified chat session and to the
ceremony ID, taking the recomputed `acct_uri` and `home_did`; have the browser call
it and require its success before CON-206; and require that command to enforce
CON-204's own checks (recompute the localpart from `issuer`, reject an alias bound
elsewhere).

---

### H-5 (HIGH) — CON-221 is re-specified without amendment, left cryptographically unbound, and its applicability determination is inverted onto the peer

**Violated clauses.** `SPEC-004…#CON-221` in three places
(specs/SPEC-004-application-scoped-identity.md:4184-4236); the candidate's
`amends:` list, which does not include CON-221.

**(a) Ordering changed silently.** CON-221:4198-4201 requires the application to
display the fingerprint "**after opening the bundle** … computed from the grant's
`issuer`". `CON-910:429` and `REQ-918:243-245` move it to `preparation`, before the
bundle exists. This is defensible on its own — but it is an amendment to CON-221
and is not declared as one.

**(b) No binding between the compared value and the accepted grant.** Because the
fingerprint is now computed from a `preparation` field rather than from the grant's
`issuer`, the comparison establishes nothing unless equality is enforced.
`REQ-919:262-270` requires the browser to check "request and ceremony IDs" and to
run CON-206 — neither of which compares the accepted grant's `issuer` to the DID
that was displayed. On a *first* enrolment there is no authority binding to catch
the mismatch (CON-204's "a grant naming a different issuer is rejected" applies only
to *subsequent* enrolments, CON-221:4226-4229). The identical gap exists today:
`apps/cbcl_chat/priv/web/enrol-allocator.mjs` returns `issuer: announcedIssuer`
alongside `facts`, and `app.js:811-825` `installEnrolledConnection` stores both
without ever comparing them. The candidate reproduces it into normative text.

**(c) Applicability inverted onto the claimant.** CON-221:4190-4193: "The
**application** SHALL determine this from authority state, not from local cache, and
SHALL treat an unreachable authority as 'unknown' and fail closed." `CON-910:429`
puts "CON-221 applicability" in the **claimant's** `preparation` body, and
`CON-910:434-436` lets a "not required" assertion collapse the two person actions
into one. A peer-asserted applicability flag is strictly weaker than the local cache
CON-221 already forbids. (The wallet does compute applicability correctly today —
`src-tauri/src/app_grant.rs:443-444` via `authority_state`/`Applicability::decide`,
`crates/selfsame-app-identity/src/confirm.rs:109-119` — but that is the *wallet's*
determination for the *wallet's* prompt, not the application's.)

**Minimum design correction.** Add CON-221 to `amends:`. Require the accepted
bundle's grant `issuer` to equal, byte for byte, the issuer DID carried in
`preparation`, as a recognition step before CON-206 and as a mutation test row.
Require the **application** to determine applicability independently from its own
authority state and to fail closed on `Unknown`, and make the claimant's
`preparation` field advisory only — a claimant asserting "not required" must not
be able to suppress an application-side comparison.

---

### H-6 (HIGH) — Every inherited hard stop except REQ-812 is left undispositioned while the design drives production allocation

**Violated clauses.** `SPEC-008-production-pairing-claimant#Amendment Channels`
("Hard stops: REQ-902, REQ-905, **REQ-906**, REQ-907 — and every hard stop of
SPEC-007, which this document inherits and cannot waive",
specs/SPEC-008-production-pairing-claimant.md:92-94);
`SPEC-007…#REQ-809`, `#REQ-813`, `#CON-806`, `#Production gate`.

`grep` over the candidate for `REQ-907|REQ-908|REQ-809|REQ-813|REQ-803|REQ-804|
REQ-805|REQ-811|REQ-814|CON-903|loopback|PRODUCTION_ALLOCATION` returns **one
line** — the REQ-812 citation at candidate:204. Consequences:

- **REQ-906 is a declared SPEC-008 hard stop.** `REQ-914` replaces it. The
  amendment never says so as a hard-stop disposition; 0.4.0 at least stated the
  ADR-902 reversal openly.
- **CON-903 is retired by implication.** `REQ-914:145-146` says "The compiled
  conformance-digest allowlist … SHALL NOT participate in this decision." CON-903 is
  never named, and no changelog entry retires it.
- **REQ-813/CON-806 eligibility is dropped.** REQ-813 requires matching "exactly one
  **eligible** profile descriptor"; CON-806 defines eligible as "reject descriptors
  forbidden by build policy or missing approved conformance evidence"
  (specs/SPEC-007-cbcl-pairing-cutover.md:343-355, 645-680). `REQ-914:135-136`
  requires exactly one *recognised* descriptor and removes eligibility. Both are
  unamended and unlisted.
- **The non-loopback rule vanishes.** `eligible()` in
  `crates/selfsame-app-identity/src/cbcl_relay.rs:85-93` refuses loopback in
  ordinary builds, and SPEC-008 REQ-908 keeps loopback dev-only. 0.4.0 explicitly
  retained REQ-907 and REQ-908; 0.4.1 dropped that retention. As written, a person
  could accept `(A, https://127.0.0.1:9443)` in a release build.
- **Production allocation.** `REQ-918:239-247` and `CON-912:510-513` make the
  production Selfsame affordance emit v2 and remove the fixture — i.e. real
  production allocation. SPEC-007 REQ-809 is a hard stop until the whole Production
  gate has durable evidence, and SPEC-008 REQ-907 forbids "any enablement path" for
  it. The candidate's own open items concede that the human cryptography review, the
  independent vectors and the SPEC-004/cbcl-pairing owner approvals are still
  outstanding (candidate:684-694), and the code gate is still closed
  (`crates/selfsame-pairing/src/release.rs:11`, `PRODUCTION_ALLOCATION_ENABLED = false`,
  asserted by `crates/selfsame-pairing/tests/spec008_hold_invariance.rs:16` and
  `tests/rollback_hold.rs:13-16`). Nothing in the amendment says whether TEST-911's
  scope invariant survives.

**Minimum design correction.** Add a hard-stop disposition table: for each of
SPEC-008 REQ-902/905/906/907/908 and CON-903, and SPEC-007
REQ-803/804/805/809/811/812/813/814 and CON-806, state retained / amended / retired
with the replacing clause. Restate the non-loopback rule in REQ-914. State
explicitly that REQ-809 remains in force and that this amendment is not its
enablement path, or open a separate amendment for it.

---

### M-1 (MEDIUM) — An unbounded pre-consent HTTPS fetch to an attacker-named origin, blessed as "read-only"

`REQ-914:132-134` requires the wallet to dereference the invitation's
`application-context` "live … with no redirect or stale first-contact cache" before
anything is shown to the person, and `REQ-916:186-190` lists "live profile fetch"
among the work that "SHALL be read-only before the person accepts".

A network request to an attacker-chosen host is not a read-only local operation. Any
carrier — including one pasted from an email — causes the wallet to make an
authenticated-TLS request to an arbitrary HTTPS origin with no prompt, no attempt
burn and no rate limit, disclosing the wallet's IP address, TLS fingerprint and the
fact that the carrier was received. Forbidding the cache guarantees one fetch per
attempt. Under 0.2.2 this could only happen toward an application the person had
already enrolled with.

**Correction.** Bound it: a per-attempt burn, a rate limit on distinct first-contact
origins, and an explicit statement in ADR-908's residual list. Consider prompting on
the *origin* before the fetch rather than after it.

### M-2 (MEDIUM) — The person-owned policy document is recognised, not authenticated

`CON-908:351-370` requires "one closed, versioned document under one entry",
recognised completely before use, with an "unavailable/corrupt/write-failing store"
refusing admission. Recognition is not integrity. The backing store writes plaintext
JSON: `src-tauri/src/store.rs:110-114` → `SelfsameStore::set`
(`crates/tauri-plugin-selfsame-store/src/lib.rs:100`), and only the *root record* is
custody-sealed under an Argon2id key
(`crates/tauri-plugin-selfsame-store/src/lib.rs:30-36`). A well-formed forged
document — inserted by an attacker with app-process access, a restored backup, or a
desktop keychain compromise — silently pre-admits arbitrary `(applicationId,
relayOrigin)` pairs and removes the single first-contact prompt that F-A's fix
exists to produce. `TEST-929:584-586` tests a *corrupt* store, not a *valid forged*
one.

**Correction.** Seal or MAC the policy document under a custody-derived key, or bind
it to the root record; add a negative test for a well-formed document the wallet did
not write.

### M-3 (MEDIUM) — "The complete CON-214 predicate" is asserted while its only permitted pre-consent mutation is deferred, and a test makes the weaker behaviour release-blocking

`REQ-915:157-161` requires "the **complete** CON-214 and CON-219 authorisation
predicate" before display. CON-214's predicate is a six-step list introduced by
"Before showing consent, Selfsame:", whose step 5 is "atomically records `requestId`
as consumed before any home-key signature, delta publication, or grant-bundle write,
**whether the request is approved or rejected**"
(specs/SPEC-004-application-scoped-identity.md:3687-3701). `REQ-916:186-193`
explicitly moves consumption to after consent, and `TEST-933:598-602` asserts the
"request ledger" is unchanged on decline — making the weaker posture a
release-blocking requirement.

The residual is a decline-then-replay loop inside the offer's 120-second validity:
after a decline nothing is consumed, so the same signed offer can be re-presented in
a fresh ceremony until the person approves. (The current wallet consumes at
`src-tauri/src/app_grant.rs:381`, i.e. at prepare time, so 0.4.1 matches shipped
behaviour — but it claims completeness it does not have.)

**Correction.** Either say plainly that CON-214 step 5 is amended for this path and
name the compensating control (an attempt ledger keyed by `offerDigest` that refuses
a second display of the same offer), or consume at authorisation and accept the
pre-consent ledger write that CON-214 already sanctions.

### M-4 (MEDIUM) — The receipt is not bound to the hub acknowledgement, so `linked` can be reported when the hub holds nothing

`REQ-919:271-278` orders "6. **submit** the existing signed `pairaccountgrant` …
7. emit the v2 receipt", and states "Any failure through step 6 emits a refusal".
Submission is not acknowledgement. `sendCBCL` resolves when the frame is written
(`apps/cbcl_chat/priv/web/app.js:1591-1596`); the hub's answer arrives asynchronously
and `handlePairAccountGranted` (`app.js:1746-1760`) only renders a system line — it
correlates with no ceremony and gates nothing. The hub can refuse with
`pairaccount-limit` or `pairaccount-store-failed`
(`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:1671-1678`) after the receipt has
been emitted and the wallet has reported `linked`.

Only `TEST-946:642-650` mentions the acknowledgement; the core rows `TEST-941:624-627`
and `TEST-942:628-630` require only "submits the actual key, emits receipt".

**Correction.** Make step 6 "submit and **await** the matching `pairaccountgranted`
for this handle and device fingerprint", define its timeout as a refusal, and move
the acknowledgement assertion into TEST-941/942.

### M-5 (MEDIUM) — Fixed 69.6 KB envelopes against unchanged relay work bounds

NFR-903 raises size bounds and says nothing about the relay's work bounds. Each
ceremony would carry two ~72 KB frames instead of two ~2 KB frames — roughly a 35×
increase in per-ceremony queue cost — against `DEFAULT_QUEUE_CAP = 512 * 1024 * 1024`,
`DEFAULT_MAILBOX_CAP = 10_000`, `DEFAULT_LIMIT = 240` per 60 s
(`cbcl-pairing/src/bin/cbcl-pairing-relay-ws.rs:33-37`) and
`MAX_FRAMES_PER_MEMBERSHIP = 16` (`cbcl-pairing/src/mailbox.rs:13`). Concurrent
capacity falls from tens of thousands of ceremonies to a few thousand, and
`tests/relay_service.rs:30` sizes the queue as a multiple of the frame bound, so the
test rig moves with it. `TEST-945:638-641` tests opacity, not capacity.

**Correction.** State the new per-ceremony worst case, re-derive the queue and
mailbox caps from it, and add a work-bound row to the test list. Consider whether the
fixed envelope needs to be the full payload bound, or whether two size classes
(a small class sized to a real ~2 KB offer/bundle, and the maximum) give adequate
opacity at a fraction of the cost.

### M-6 (MEDIUM) — Unsized objects in the v2 body grammar

- `CON-910:429`: the `preparation` body is "fixed-size" but no size is given, and its
  contents (CON-221 applicability plus an issuer DID) have no stated bound; the
  amendment cites no DID grammar.
- `CON-910:441-443`: the envelope's *fixed size* is never stated — only the logical
  length range.
- `CON-907:325`: `application-context` is `bstr .size (1..2048)` while
  `ApplicationId::parse` (`crates/selfsame-app-identity/src/profile.rs:148`) is the
  sole recogniser; the relationship between the 2,048-octet carrier bound and
  CON-201's own length rule is asserted to be "additional resource policy" but not
  reconciled with CON-201's `applicationId` grammar.
- `CON-911:471-473`: `applicationAccountDisplay` is "bounded" with no bound.

### M-7 (MEDIUM) — CON-211 scope stability is keyed on an undefined notion of "chat account"

`REQ-917:214-218` requires "one CSPRNG 32-octet account scope for each authenticated
`(applicationId, chat account)` pair", committed atomically and reused. This is the
right shape and correctly replaces `randomBytes(32), // account scope`
(`apps/cbcl_chat/priv/web/enrol-allocator.mjs:65`), which mints a new identity on
every retry.

But a chat account at this baseline is a self-chosen handle TOFU-bound to a wire key
(`app.js` `freshHandle()`, `HANDLE_KEY_PAIR`, `HANDLE_KEY_PAIR_RECORD`), whose only
durable anchor is a seed in IndexedDB. A person who loses that seed gets a new handle
and therefore a new scope, a new home DID, a new alias, and an orphaned prior
identity — silently. CON-211 requires the value to be "immutable for the lifetime of
that account", which presupposes an account with a lifetime independent of browser
storage. `TEST-935:606-608` tests two retries within one session and a different
account; it does not test seed loss or handle change.

**Correction.** Define what the hub treats as the durable account identity for
scope purposes, state what happens on handle change or key rotation, and add a test
row for "same person, new browser storage".

### M-8 (MEDIUM) — The v2 state machine has no timeout terminal and no compensation on `refusal`

`CON-910:412-418` gives no transition for "allocator sends neither `receipt` nor
`refusal`", yet `REQ-919:276-277` makes the wallet's `linked` report conditional on a
receipt. The wallet's terminal state in that case is undefined. Separately,
`payload -> refusal -> terminal` leaves the wallet having published a DID, consumed a
request, and released a signed grant that the application refused; the amendment
prescribes no compensating action, where CON-221 for its own rejection path
"SHOULD revoke the grant ID under CON-210". `TEST-940:621-623` covers wrong
sender/reorder/duplicate but not silence.

### L-1 (LOW) — The cited 0.4.0 review record does not exist

`review-inputs` (candidate:10) cites
`[[req-909-adversarial-review-2026-08-22#Review of SPEC-008 0.4.0 draft (standalone TOFU first contact)]]`.
That heading is absent from
`specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`, which is
entirely about 0.3.0 / REQ-909 and enumerates F-1..F-6. F-A..F-E exist only as a
prose summary in `specs/trajectory/SPEC-008/HANDOFF-pairing-integration.md:105-136`.
There is no durable 0.4.0 review artefact, so the disposition table at
candidate:113-123 cannot be checked against a primary record — a reviewer must take
the handoff's paraphrase on trust. The vault's own discipline (candidate:687-688,
"Owner approval alone is insufficient") argues for the record existing.

### L-2 (LOW) — CON-220 step 6 has no counterpart and the deviation is uncited

CON-220 step 6 pins the fetched profile against a `profileDigest` in a PROTO-003
CON-409 record (specs/SPEC-004-application-scoped-identity.md:4130-4132). No such
record exists on this path; the pin is deferred to `REQ-915:167-176`'s equality list,
which is substantively equivalent to CON-220's own tier-3 ordering carve-out
(:4145-4152). Neither the deviation nor the carve-out is cited.

### L-3 (LOW) — The independent-vector production gate is farther from met than the open items imply

`TEST-939:618-620` asks two implementations to reproduce every v2 byte.
`cbcl-pairing/vectors/` contains only a README, which states that "An independent
CPace vector source and the required human cryptography review remain absent" and
that `tools/reference_endpoint.py` "deliberately does not duplicate CPace group
math". SPEC-007's Production gate requires "independent vectors cover the CPace
boundary **and** complete endpoint protocol". The candidate's open items name only
the human cryptography review.

---

## Disposition of F-A through F-I

| Finding | Candidate's claim | Verified disposition |
|---|---|---|
| **F-A** relay-scoped trust | REQ-914 + CON-908 key policy by exact `(applicationId, relayOrigin)` | **Closed in design.** The mechanism is right and the scope invariant TEST-928 is the correct test. **Two dependencies unclosed:** the descriptor it reads is not a ratified SPEC-004 member (H-1), and the policy document is unauthenticated (M-2). |
| **F-B** untrusted display | CON-909 verifier + closed provenance; `claimed_by_secret_holder` removed | **Partially closed.** Correctly diagnoses `cbcl-pairing/src/profile.rs:784-798` and fixes application ID, origin, action and authority summary. **Re-opened elsewhere:** the account label is a self-chosen TOFU handle rendered as `AuthenticatedAccount` (C-2). |
| **F-C** pre-consent derivation | REQ-916 splits preflight from issuance; `assemble_claimant` not reused | **Not closed — relocated.** Correctly drops `assemble_claimant` (`src-tauri/src/cbcl_context.rs:286`). Then places request consumption, derivation, issuance and a **new** DID publication after decision 2 and before decision 3 (C-1). The hard-stop question is moved, not settled. |
| **F-D** no fresh scope | REQ-917 puts CON-211 ownership on the authenticated application account | **Closed in design; two gaps.** Correctly replaces `randomBytes(32) // account scope` (`enrol-allocator.mjs:65`). "Chat account" is undefined as a durable identity (M-7); the hub has no scope store today (implementability, not a design defect). |
| **F-E** no operatorship binding | ADR-908 accepts the narrower property explicitly | **Closed as an honest residual.** REQ-920 and ADR-908 are accurate, correctly scoped, and correctly refuse to dress `operatorId` as verified. The best-handled item in the document. Contingent on H-1 (`operatorId` has no SPEC-004 grammar). |
| **F-F** pre-staged recipient/grant | Reversed by CON-910 | **Accurately named; closed in design.** Verified: `AllocatorRelaySession::new` requires a complete `CredentialTransfer` and calls `recognise_transfer_claims` before allocation (`crates/selfsame-pairing/src/live.rs:114-145`); the browser supplies it from `local-pairing-fixture.json` (`app.js:780-781`). |
| **F-G** wrong payload direction | CON-910 reverses payload flow and adds preparation/receipt | **Accurately named; design incomplete.** Verified: `apply_payload` is claimant-only (`cbcl-pairing/src/endpoint.rs:1043-1049`), `apply_decision` allocator-only (`:1010-1015`). The new machine lacks a timeout terminal and a `refusal` compensation (M-8), and its CON-221 placement is unbound (H-5). |
| **F-H** no application acceptance | CON-912 + REQ-919 add a subject-side acceptance object | **Accurately named but imprecise; not implementable as written.** `installEnrolledConnection` (`app.js:811-825`) does store without CON-206/207 — correct. But "does not expose a complete browser-side CON-206 acceptance facade" is wrong: `verify_path_b_peer_json` exists (`crates/selfsame-web-device/src/lib.rs:886`); it is unwired and refuses non-resolver closures (H-3). Alias provisioning is misassigned (H-4). |
| **F-I** unauthenticated signing oracle | REQ-917 forbids the oracle; hub builds the offer from session state | **Accurately named; closed in design.** Verified: `cbcl-chat-enrolment-signer:sign/1` signs caller-supplied octets (`apps/cbcl_chat/src/shell/cbcl-chat-enrolment-signer.lfe:68-88`) and the route's own comment records the posture (`cbcl-chat-selfsame-application-http.lfe:60-77`). REQ-917's replacement and TEST-936 are the right shape. |

---

## State and effect-order audit

Legend: **HS-2** = the grant-consent hard stop; **HS-3** = the CON-221 final approval.

| Effect | Candidate's placement | Should be | Verdict |
|---|---|---|---|
| Invitation recognition | pre-consent | pre-consent | OK |
| Live CON-220 profile fetch | pre-consent, unbounded, uncached (`REQ-914:132-134`) | pre-consent but bounded and burnable | **M-1** — an outward request to an attacker-named host, mislabelled "read-only" |
| Relay-policy read | pre-consent | pre-consent | OK |
| Relay-policy **write** on accept | pre-consent, explicitly permitted (`REQ-916:199-201`) | as stated | OK — a separately consented, non-identity effect; correctly argued |
| Socket open / CPace / Finished | pre-consent | pre-consent | OK |
| Signed-offer authorisation | pre-consent | pre-consent | OK |
| Replay **lookup** | pre-consent | pre-consent | OK |
| Display construction | pre-consent | pre-consent | OK |
| **Request-ID consumption** | after HS-2 (`REQ-916:192`) | at authorisation (CON-214 step 5) or after HS-3 | **M-3** — claims a complete predicate it does not run |
| Custody unlock | after HS-2 | after HS-2 | OK |
| `hierarchy::derive` | after HS-2 | after HS-2 | OK |
| `issuer::create` (local) | after HS-2 | after HS-2 | OK |
| **DID closure publication** | after HS-2, before HS-3 (`CON-911:487`) | **after HS-3** | **C-1** — outward, irreversible, before the final decision |
| Grant signature | after HS-2 (`REQ-916:193`) | after HS-3 | **C-1** (same class; a signed grant retained across a final-decline) |
| CON-221 preparation | after HS-2 | after HS-2 | OK |
| Issuance-state write | after HS-2 | after HS-3 | **C-1** |
| Bundle build + payload send | after HS-3 | after HS-3 | OK |
| CON-204 alias provisioning | application side, after bundle (`REQ-919:263`) | application side, after HS-3 and bundle | OK in ordering; **H-4** in role |
| CON-206/207 acceptance | after provisioning (`REQ-919:266-269`) | as stated | OK in ordering; **H-3** in feasibility |
| Atomic connection install | after CON-206 (`REQ-919:270`) | as stated | OK |
| `pairaccountgrant` | after install (`REQ-919:271`) | after install, **awaited** | **M-4** |
| Receipt | after step 6 (`REQ-919:273`) | after acknowledgement | **M-4** |
| Wallet reports `linked` | on receipt (`REQ-919:276`) | on receipt | OK given M-4 fixed |

**Conclusion.** There is **not** exactly one defensible hard stop before every listed
effect. There are two consent events with irreversible outward effects between them,
and the amendment defines a residue-free post-condition only for the first.

---

## Input-authority and display-provenance audit

| Displayed / decided value | Candidate's source | Real authority at the baseline | Verdict |
|---|---|---|---|
| Application ID | live authenticated profile (`REQ-915:173-174`) | CON-220 origin dereference — genuine | **Sound.** Correctly replaces the wire value that `cbcl-pairing/src/profile.rs:784-790` copies today |
| Application origin | live profile | same | **Sound** |
| Action | fixed profile constant (`REQ-915:175`) | removes peer control; today it is wire-supplied (`profile.rs:786`) | **Sound** |
| Authority summary | fixed profile constant | removes peer control; today wire-supplied (`profile.rs:787`) | **Sound** |
| Relay origin | invitation + live descriptor + signed hint | genuine, three-way equality | **Sound**, contingent on H-1 |
| Permission set | signed offer, ⊆ profile `allowedPermissions` | CON-214-signed; profile-bounded | **Sound** |
| Device DID / JWK / digest | signed offer | CON-214-signed | **Sound** |
| Offer / ceremony / request IDs, time window, digests | signed offer | CON-214-signed | **Sound** |
| **Account label** | signed offer `applicationAccountDisplay`, provenance `AuthenticatedAccount` (`CON-909:387-390`, `REQ-917:224-226`) | a self-chosen handle TOFU-bound to a wire key (`app.js` `freshHandle`, `HANDLE_KEY_PAIR`; `cbcl-chat-session-ws.lfe:1667`) | **UNSOUND — C-2.** The hub authenticates *a* sender; it does not authenticate that this account is the scanning person's. Rendering it as authenticated is the same defect class as F-B in a new location |
| **CON-221 fingerprint** | claimant `preparation`, rendered by the allocator (`CON-910:429`) | the counterpart learns it before the human compares | **UNSOUND — C-2/H-5.** No binding to the accepted grant's `issuer` |
| **CON-221 applicability** | claimant `preparation` (`CON-910:429`) | CON-221 assigns it to the application, from authority state, fail-closed | **UNSOUND — H-5.** Peer-asserted control over whether a comparison happens |
| Relay operator | application-asserted, never rendered as verified (`REQ-920:287-296`) | application-asserted | **Sound and honest** |

Positively: `CON-909:395-397`'s insistence that the verifier reuse
`ceremony::recognise_offer` and `authorise::authorise` rather than duplicate their
grammars is correct and both exist
(`crates/selfsame-app-identity/src/ceremony.rs:281`,
`crates/selfsame-app-identity/src/authorise.rs:131`). Replacing
`claimed_by_secret_holder: bool` with a closed provenance type is the right fix and
`TEST-932:594-597` is a well-formed prohibited-action row.

---

## Implementability audit

### cbcl-pairing (`8e55160`)

| Slice | Feasible? | Notes |
|---|---|---|
| v2 CDDL + context + dialect | Yes | `PairingContext::derive` (`src/context.rs:23-64`) already carries `application` and `relay_origin` into CI and the public context; a v2 domain and a `bstr` context are mechanical. The public context is a local transcript input, never transmitted (`src/channel.rs:95-104`), and the transmitted CPace associated data excludes the application (`src/context.rs:113-125`). **CON-907's opacity claim is verified, not merely inferred.** |
| Intent-verifier display boundary | Yes | `ApplicationProfile::recognise_intent` (`src/profile.rs:203`) is already an injected trait alongside `GrantVerifier` (`:192`); adding a second injected verifier is the same shape. |
| Preparation / final decision / reverse payload / receipt reducer | Yes, but larger than stated | The reducer hard-codes direction (`apply_intent` claimant-only `:982`, `apply_decision` allocator-only `:1010`, `apply_payload` claimant-only `:1043`) and `SessionPerformative` has exactly four variants (`src/cbcl_protocol.rs:97-106`). The CBCL dialect (`dialects/blind-pairing-session-v1.cbcl`) needs four new performatives with role casts and a new `(then …)` chain; its `body-length` requirement is compatible with a fixed envelope. Projection/admission semantics support the proposed chain. |
| Unified frame bound | **No, as specified** | H-2: no constant value, contradictory maximum, and v1 grammar objects are shared. |

### selfsame core / adapter (`c6af6df`)

| Slice | Feasible? | Notes |
|---|---|---|
| v2 invitation + credential offer authoriser | Yes | `decode_selfsame_invitation` (`crates/selfsame-pairing/src/lib.rs:773`) and `SelfsameEndpointBootstrap::start` (`:134`) pin `CREDENTIAL_APPLICATION`; a v2 branch is additive. |
| Post-consent issuer path | Yes | `prepare_issuance`/`confirm_issuance` (`src-tauri/src/app_grant.rs:365,556`) already separate review, consent and release, and correctly refuse an overlapping ceremony (`:471-478`). CON-911's reuse claim is accurate. |
| Resolver **publication** | **Not implemented and not specified** | No publish function exists (`crates/selfsame-app-identity-net/src/state.rs`: only `resolve_closure`, `submit_revocation`, `confirm_revocation`). `POST /dids/:did/deltas` exists (`did-crdt/src/service/server.rs:150`) so it is buildable — but it is a new capability the cross-repository table does not list, and C-1 puts it in the wrong place. |
| Browser CON-206/207 acceptance WASM | **Blocked** | H-3. |

### cbcl-bus hub and browser (`fdbe414`)

| Slice | Feasible? | Notes |
|---|---|---|
| Authenticated offer-allocation command | Yes | The session already verifies the sender's frame envelope; building the offer server-side needs the `build_offer_json` logic to move into the NIF or a hub-side Rust path. `ProviderHint::verify` **already** resolves `providerId` against `cbcl_pairing_relays` and compares the descriptor digest (`crates/selfsame-app-identity/src/provider_hint.rs:107-130`), so CON-911's "one explicit cbcl-pairing branch" for CON-209 is largely already the implemented behaviour. |
| Durable account scope + one-use ceremony state | Yes | New tables; no blocker found. M-7 is the definitional gap. |
| Signed v2 CON-214 evidence | Yes | `sign-under-open-gate` is already profile-bound (`cbcl-chat-enrolment-signer.lfe:100-107`) and refuses an undeclared `kid` inside the NIF. Closing F-I means constructing the statement hub-side rather than accepting octets. |
| **Alias provisioning** | **Missing** | H-4: `provision/3` exists with no caller and no route. |
| Non-fixture allocator + acceptance + atomic install | Partly | `CbclAllocatorSession`'s constructor takes `bundle: Uint8Array` (`selfsame_web_device.d.ts`), so the ABI itself must change — a published wasm-bindgen ABI break the amendment does not mention. IndexedDB strict-durability persistence already exists (`app.js:824`). |
| Receipt binding | Missing | M-4. |
| Relay bounds | Blocked | H-2, M-5. |

### did-crdt (`1f409a4`)

The amendment says "No new pairing route or mailbox is added there" (candidate:664).
That is true but insufficient: CORS on the closure/resolution routes is required
(H-3), and if the wallet publishes, the deltas route is exercised in a new way. The
amendment gives `did-crdt` no slice at all.

---

## Test and gate audit

| Row | Verdict |
|---|---|
| TEST-927 | Sound. Correctly places the refusal "before application-origin or relay I/O". |
| TEST-928 | Sound and load-bearing — the correct F-A invariant. |
| TEST-929 | **Incomplete.** Tests a corrupt store, not a well-formed forged one (M-2). |
| TEST-930 | Sound. |
| TEST-931 | Strong. The mutation list is the right one for F-B. |
| TEST-932 | Strong prohibited-action row. |
| TEST-933 | **Wrong boundary.** Asserts the request ledger is unchanged on decline, entrenching M-3; and it covers only decline at HS-2, never `final-decline` at HS-3 — so C-1's residue is untested by construction. |
| TEST-934 | **Actively entrenches C-1.** "After consent, publication failure sends neither preparation nor payload" makes publication-before-preparation the required behaviour. Must be inverted to kill any effect before `final-approve`. |
| TEST-935 | Sound but incomplete (M-7: no seed-loss / handle-change case). |
| TEST-936 | Strong. The right F-I row. |
| TEST-937 | Sound — but note it tests transplanting an offer to *another* invitation, not delivering a *valid* offer to a stranger's wallet (C-2 is untested). |
| TEST-938 | Sound. |
| TEST-939 | **Not achievable at the baseline.** The independent endpoint "deliberately does not duplicate CPace group math" (`cbcl-pairing/vectors/README.md`), and "every v1 vector remains unchanged" is falsified by H-2's bound raise. |
| TEST-940 | Sound but incomplete: no silence/timeout case (M-8). |
| TEST-941 | **Currently impossible.** "provisions the alias" (H-4) and "passes all thirteen CON-206 steps" (H-3) cannot run. |
| TEST-942 | Good shape; inherits TEST-941's impossibility. |
| TEST-943 | Strong prohibited-action row; the right answer to F-F/F-H. |
| TEST-944 | Sound for the threat it names. Does not cover C-2. |
| TEST-945 | **Contradictory.** "logical lengths 1 and 69,607 round-trip" cannot hold for the `intent` envelope, which must also carry the nonce (H-2). |
| TEST-946 | Correctly gated on owner approval, and the only row that mentions the hub acknowledgement — which is why M-4 matters: the core rows are weaker than the E2E row. |

**Missing rows.** No test for: a valid offer from a legitimate application for a chat
account that is not the scanning person's (C-2); the `final-decline` residue (C-1);
a well-formed forged policy document (M-2); the accepted bundle's `issuer` versus the
compared DID (H-5); a claimant-asserted "CON-221 not required" (H-5); loopback
admission in a release build (H-6); relay work bounds under fixed envelopes (M-5);
awaiting `pairaccountgranted` (M-4).

**Gates.** The candidate's sequencing (candidate:668-684) is disciplined and its
ordering — spec review, then failing tests and vectors, then core, then shells — is
correct. Two problems: step 5's list omits the relay work-bound and CORS gates the
design now needs, and the SPEC-007 Production gate is treated as a set of "open
items" (candidate:684-694) rather than as REQ-809's hard stop on the production
allocation the design requires (H-6). SPEC-007's human-cryptography and
independent-vector requirements are named and preserved, which is correct — but
TEST-939 asserts an independent-vector property the repository cannot currently
produce (L-3).

---

## What is right, and should survive into 0.4.2

Recorded so the next revision does not lose ground:

- The three-decision framing, and the refusal to let one decision stand for another.
  The design's failure is that it does not honour its own rule, not that the rule is
  wrong.
- ADR-906's exact-pair policy and its explicit rejection of relay-only trust and
  vendor seed entries.
- ADR-908. Naming F-E as a bounded residual, refusing to dress `operatorId` as
  verified, and putting operatorship in a future amendment is exactly right.
- CON-909's injected verifier, closed provenance type, and the deletion of
  `claimed_by_secret_holder` — with the caveat that provenance must then be *true*
  for every field, including the account label.
- ADR-907's insistence that first contact reuse CON-214/CON-219 rather than invent a
  weaker bootstrap.
- ADR-909's non-negotiated v2 cutover: distinct schema, domains, profile ID and
  dialect, with no capability list. This is the right anti-downgrade posture and I
  found no downgrade path in it.
- REQ-917's relocation of CON-211 ownership to the authenticated application account.
- The F-F..F-I trace itself. Four premises the previous drafts never examined were
  found and named accurately; that is the work the review process is supposed to
  produce.

---

## Residual risks

Not applicable — the verdict is REJECT. The items above are defects to be corrected,
not accepted residuals. ADR-908's F-E residual is the only residual in the document
I would accept as stated, and it remains contingent on H-1.

---

## Method

Every claim above was checked against the named revisions rather than against the
candidate's summary or source comments. Baselines confirmed at review time:
`selfsame` worktree HEAD `f2501d9` (spec-only commit over code parent `c6af6df`),
`cbcl-pairing` `8e55160`, `cbcl-bus` `fdbe414`, `did-crdt` `1f409a4`.

Paths traced end to end: the cbcl-pairing invitation CDDL, decoder, `PairingContext`,
CPace inputs, `PendingChannel`/`Channel`, the CBCL session dialect and projection, and
the endpoint reducer's intent/decision/payload arms; `CredentialProfile::recognise_intent`
and the display path; `decode_selfsame_invitation`, `SelfsameEndpoint`,
`AllocatorRelaySession`/`ClaimantRelaySession`, `cbcl_pairing_start`, `finish_live`
and `assemble_claimant`; `authorise::authorise`, `observation_for_enrolment_offer`,
`prepare_issuance`, `confirm_issuance`, `replay::consume`, `Applicability::decide`,
`ProviderHint::verify`, `verify_path_b_peer`; the vendored WASM ABI,
`cbcl-invite.mjs`, `enrol-allocator.mjs`, `app.js` install/submit/acknowledge, the
hub `pairaccountgrant` handler, the enrolment signer and its HTTP route, and the
webfinger store; the did-crdt router and CORS placement; and the relay mailbox,
limiter, storage and WebSocket bounds.

No implementation code and no candidate amendment text was modified by this review.
