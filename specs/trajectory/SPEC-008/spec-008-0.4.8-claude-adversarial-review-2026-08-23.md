---
id: SPEC-008-0.4.8-claude-adversarial-review
title: Tier-1 fresh-context cross-model adversarial review — SPEC-008 0.4.8-draft (with adopted 0.4.4–0.4.7), cbcl-pairing SPEC-001 0.4.3-draft, and cbcl-bus SPEC-053 0.16.1-draft
status: complete
tier: 1
verdict: REJECT
verdict-selfsame-spec-008-0.4.8: REJECT
verdict-cbcl-bus-spec-053-0.16.1: REJECT
verdict-cbcl-pairing-spec-001-0.4.3: PASS-on-its-narrower-amendment-with-one-medium
verdict-cbcl-pairing-spec-001-0.4.2: PASS-as-adopted-and-completed-by-0.4.3
verdict-cbcl-pairing-spec-001-0.4.1: PASS-as-corrected-by-0.4.2-and-0.4.3
verdict-cbcl-pairing-spec-001-0.4.0: PASS-as-adopted
verdict-cbcl-pairing-spec-001-0.3.0: PASS-as-adopted
reviewer: independent Circus adversarial reviewer, fresh context, no access to the authoring conversation, no subagents, no workflows
model: Claude Opus 5 (claude-opus-5), max reasoning
review-date: 2026-08-23
attempt: circus/spec008-v048-review/2
candidates:
  - "[[spec-008-0.4.8-standalone-tofu-first-contact]] — specs/trajectory/SPEC-008/spec-008-0.4.8-standalone-tofu-first-contact.md at selfsame 644a051310024de097e88a34560caefd2ab99a7a"
  - "[[spec-008-0.4.7-standalone-tofu-first-contact]] — adopted except where 0.4.8 replaces or corrects"
  - "[[spec-008-0.4.6-standalone-tofu-first-contact]], [[spec-008-0.4.5-standalone-tofu-first-contact]], [[spec-008-0.4.4-standalone-tofu-first-contact]] — adopted except where later amendments replace or correct"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.3-migration-display-and-test-replacement.md at cbcl-pairing a50edc784824bc7f26d3223b5e5575c9ba93cc03"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.2-control-body-cap.md, 0.4.1, 0.4.0, 0.3.0 (adopted)"
  - "/Users/anuna-01/Code/cbcl-bus/specs/trajectory/SPEC-053/spec-053-0.16.1-credential-v2-confirmed-migration.md at cbcl-bus 856b489f5352f123ee6d4f405a42edf35e9c3200"
reviewed-revisions:
  selfsame-candidate: 644a051310024de097e88a34560caefd2ab99a7a
  selfsame-implementation-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
  cbcl-pairing-candidate: a50edc784824bc7f26d3223b5e5575c9ba93cc03
  cbcl-pairing-implementation-baseline: 8e55160a3234127bd60ce723fd93ba6b2b67a06b
  cbcl-bus-candidate: 856b489f5352f123ee6d4f405a42edf35e9c3200
  cbcl-bus-implementation-baseline: fdbe4142261923f48cced037a7f609e48a9fdd5a
  did-crdt-implementation-baseline: 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
governing-specs:
  - specs/SPEC-008-production-pairing-claimant.md
  - specs/SPEC-007-cbcl-pairing-cutover.md
  - specs/SPEC-004-application-scoped-identity.md
  - specs/SPEC-002-visual-key-fingerprint.md
  - /Users/anuna-01/Code/cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md
  - /Users/anuna-01/Code/cbcl-bus/specs/SPEC-053-key-root-identity.md
  - /Users/anuna-01/Code/cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md
  - /Users/anuna-01/Code/cbcl-bus/specs/SPEC-076-pairing-account-grant.md
prior-reviews:
  - "[[spec-008-0.4.7-claude-adversarial-review-2026-08-23]] (REJECT — 0 critical, 3 high, 2 medium, 6 low)"
  - "[[spec-008-0.4.6-claude-adversarial-review-2026-08-23]] (REJECT — 0 critical, 1 high, 3 medium, 7 low)"
  - "[[spec-008-0.4.5-claude-adversarial-review-2026-08-23]] (REJECT)"
  - "[[spec-008-0.4.4-claude-adversarial-review-2026-08-23]] (18 findings)"
  - "[[req-909-adversarial-review-2026-08-22]] (REJECTED)"
findings: 1 critical, 4 high, 3 medium, 7 low
last-updated: 2026-08-23
---

# Verdict: REJECT

One CRITICAL and four HIGH findings are unresolved. The coordinated set does not
pass.

- **Selfsame `SPEC-008` 0.4.8-draft — does not pass.** `C-1`, `H-1`, `H-2`,
  `H-3`, `H-4`, `M-1`, `M-2`, `M-3`.
- **cbcl-bus `SPEC-053` 0.16.1-draft — does not pass.** It owns `C-1`, `H-1`,
  `H-2`, `H-3` and half of `M-1`. Its amendment hygiene is otherwise exact:
  `REQ-040`, `NFR-008`, `CON-012`, `ADR-017`, `DEC-015`, `GATE-04` and
  `TEST-043`–`TEST-047` are all the next free numbers above the baseline's
  `REQ-039`/`NFR-007`/`CON-011`/`ADR-016`/`DEC-014`/`TEST-042`, the main-text
  diff at `856b489` touches only the frontmatter and one `DEC-012` paragraph,
  and `GATE-04` is defined once, in the amendment, with the main text pointing
  at it.
- **cbcl-pairing `SPEC-001` 0.4.3-draft — passes its narrower owning-spec
  amendment, with one MEDIUM.** The `TEST-050` replacement the 0.4.7 review
  asked for is made correctly and through the only channel that can make it.
  `REQ-025`'s display clause carries `M-3`.

**The three 0.4.7 HIGH findings are substantively answered in design, and I
could not reopen `H-2` at all.** `REQ-970` and cbcl-bus `REQ-040` make an
existing Path-A member the positive branch rather than a terminal, which is the
right correction and is reachable at the authentication layer: I re-traced
`cbcl-chat-session-ws.lfe:213-320` and confirmed that a handle with a
`cbcl-member` row and no `cbcl-account-binding` row authenticates normally, so
the ceremony's *entry* is no longer circular. The destructive console remedy is
deleted by name in both repositories. `GATE-00` is named, quoted and given a
per-surface disposition. `M-1`'s digests are defined. `M-2`'s two fail-open
ledgers are added and both consequences I re-verified in code are stated
correctly.

**What the amendment did not do is check the *exit* of the ceremony against the
code it lands on.** The one irreversible authority transaction is specified to
write a `cbcl-pairaccount-grant` row for the browser's newly granted device key
(`bus0161:171`). The only writer of that table stamps `state = 'unratified`
(`cbcl-chat-pairaccount.lfe:66`), and `cbcl-core-enrol:resolve` refuses **every**
frame from **any** key holding an unratified row, on **every** binding path,
before it reaches the binding case (`cbcl-core-enrol.lfe:125-126`). The design's
own `NFR-910` states this property in the durability section and then walks into
it. So the commit that `CON-959` declares irreversible and uncompensatable
terminates in a state where the new device key cannot send a frame, the old key
is account-bound behind a shut Path-B gate, and the person is out of every
channel — the exact outcome 0.4.7's `H-2` identified, arrived at by a different
route. That is `C-1`.

Three further HIGH findings are of the same class: statements about the hub half
that the hub's code does not support. The WebSocket half cannot satisfy
`GATE-00` clause 2 or `TEST-1088`'s "zero writes" because TOFU enrolment
completes inside `authorise-frame` before `route` reaches any handler, and an
unrouted verb falls through to `do-publish`, which persists and fans it to the
room (`H-1`). The confirmation digest's `fingerprintCanonicalBytes` is defined
nowhere in any of the four repositories, and the renderer it must reproduce is
BLAKE3 over a Selfsame domain that cbcl-bus has no primitive for (`H-2`). The
mandated positive end-to-end fixture needs a *second* Tier-1 gate opened that no
amendment names (`H-3`). And adopted `TEST-1027`'s "an unauthenticated caller
obtains no signature" is false at the reviewed baseline, with no owning change
anywhere in the coordinated set (`H-4`).

I have not found a defect in the *architecture*. Standalone first contact, the
exact `(applicationId, relayOrigin)` scope, two-stage consent with a
locally-derived DID between the decisions, and using the final screen as the
`REQ-033` migration surface are all sound, and `ADR-941`'s circularity argument
is correct. The defects are in the seam between that design and the running
system it has to land in.

---

## Revision verification

| Repository | Reviewed revision | Verified |
|---|---|---|
| `selfsame` (this worktree, `circus/spec008-v048-review/2`) | `644a051` | `git status --porcelain` empty at session start. `git diff --name-status c6af6df 644a051` lists **only `A` entries, every one under `specs/trajectory/SPEC-008/`**. The candidate's `implementation-baselines:` claim holds: no `src`, `src-tauri` or `crates` file has moved since `c6af6df`. |
| `cbcl-pairing` | `a50edc7` | `git diff --name-status 8e55160 a50edc7` lists four files, all `A`, all under `specs/trajectory/SPEC-001/`. Code holds byte-for-byte at `8e55160`. Working tree carries untracked `.DS_Store`, `.zetl/`, `fuzz/.DS_Store`, `specs/.zetl/`, `src/.DS_Store` only; every code claim below reads `git show 8e55160:<path>`. |
| `cbcl-bus` | `856b489` | `git status --porcelain` empty. `git diff --stat fdbe414 856b489` is exactly two files: `specs/SPEC-053-key-root-identity.md` (+13/−2, frontmatter plus one `DEC-012` paragraph) and the new `spec-053-0.16.1-…md` (+315). No source, config, script or fixture file moved. |
| `did-crdt` | `1f409a4` | **Pre-existing dirt recorded, not edited:** modified `specs/did-crdt-spec.md`, untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md`. No source file is dirty; all claims read committed `1f409a4`. |

No file outside
`specs/trajectory/SPEC-008/spec-008-0.4.8-claude-adversarial-review-2026-08-23.md`
was created or modified. No candidate, parent spec, code, fixture, vector or
sibling-repository file was touched.

---

## Findings

Severity: CRITICAL = the accepted path terminates in an unsound state, or a
security property fails as designed; HIGH = blocking design or implementability
defect; MEDIUM = must be fixed before implementation; LOW = record and correct.

`v048:NNN` is
`specs/trajectory/SPEC-008/spec-008-0.4.8-standalone-tofu-first-contact.md`;
`v047:`, `v046:`, `v045:`, `v044:` the earlier amendments in that directory.
`bus0161:` is
`/Users/anuna-01/Code/cbcl-bus/specs/trajectory/SPEC-053/spec-053-0.16.1-credential-v2-confirmed-migration.md`.
`pair043:` is
`/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.3-migration-display-and-test-replacement.md`.
Bare paths under `apps/`, `config/`, `scripts/`, `specs/SPEC-0*` are `cbcl-bus`
at `fdbe414`. Bare paths under `crates/`, `src-tauri/`, `specs/SPEC-00*` are
`selfsame` at `c6af6df`. cbcl-pairing and did-crdt paths are called out.

---

### C-1 (CRITICAL) — the one irreversible authority transaction writes a `cbcl-pairaccount-grant` row for the very device key the person must reconnect with; the only writer of that table stamps `state = 'unratified`, and `cbcl-core-enrol:resolve` refuses every frame from an unratified key on every path before the binding case, so the accepted, uncompensatable commit terminates in a total chat lockout with both keys inert

**Exact candidate clauses.**

`bus0161:169-177` (`CON-012`), the transaction's staged writes:

> "4. stages the verified device grant, **pairaccount authority**, reciprocal
> alias/account projection, stable-account indexes, and binding;"

`bus0161:75-79` (`REQ-040`):

> "Only then does it emit `pairaccountgranted` and one `account-linked` notice
> per affected live room. The browser closes the legacy-key socket and
> **reconnects using the distinct granted device key**; it SHALL NOT send
> another ordinary frame under the legacy key."

`v048:118-121` (`REQ-970`): "The browser receives or later recovers the exact
acknowledgement under the new device key, closes the old-key socket, **reconnects
through Path B**, and only then can the wallet accept a receipt and report
linked."

`v044:1032-1036` (`CON-941`, adopted and unreplaced): the browser "next sends the
actual retained device key through `pairaccountgrant` extended with ceremony ID
and stable-account commitment", and the hub transaction "atomically finalizes …
the exact `(accountId, deviceKey)` pairaccount authority".

`bus0161:186-191` (`CON-012`): "After it, timeout or lost acknowledgement never
restores legacy rows, deletes the binding, or revokes the accepted grant
automatically." `v048:352-357` (`CON-959`) says the same and adds "A later unlink
is a new confirmed protocol."

**Governing code evidence, `cbcl-bus` at `fdbe414`.**

1. **Every write to `cbcl-pairaccount-grant` stamps `'unratified`.**
   `cbcl-chat-pairaccount.lfe:15-20` exports exactly `ensure-table/0`,
   `claim/2`, `grants/1`, `unratified-device?/1`. The single `mnesia:write` is in
   `claim-tx/2` at `:52-68`, and its `state` field is the literal
   `'unratified` (`:66`). There is no other writer, no state-transition
   function, and no ratification call site anywhere in `apps/cbcl_chat/src/`.
2. **SPEC-076 says that is deliberate and permanent for now.**
   `specs/SPEC-076-pairing-account-grant.md:127-132` (`REQ-004`): "A recorded
   grant in the `unratified` state SHALL NOT confer channel membership… **until
   then every grant stays `unratified` and inert.**" `:67` records the
   ratification UX as an **Open** item: "who ratifies an unratified device and
   on which surface".
3. **An unratified key is refused on every path, before the binding case.**
   `cbcl-core-enrol.lfe:87` is the single evaluation point. At `:125-126`, after
   the `spent` check and **before** the `case (maps:get 'binding handle …)` that
   selects unbound / key-bound / account-bound:

   ```lfe
   (if (not (=:= 'false (maps:get 'pairing-unratified frame-key 'unknown)))
     #(reject pairing-unratified)
     (case (maps:get 'binding handle 'undefined) …
   ```

   The shell supplies that fact on **both** branches —
   `cbcl-chat-session-ws.lfe:347` (`selector-facts`) and `:385`
   (`account-standing-facts`) — each calling
   `cbcl-chat-pairaccount:unratified-device?/1`, which at `:97-104` does
   `mnesia:index_read 'cbcl-pairaccount-grant Pk 'devkey` and answers `'true`
   if any matching row is `'unratified`. `authorise-frame:282-302` turns
   `#(reject …)` into `#(error rejected)` and `verify-and-route:262` replies
   `#"bad-signature"`.
4. **The design already knows this property.** `v048:224-226` (`NFR-910`):
   "pairaccount-grant loss is fail-open because an empty table makes every
   paired-but-unratified device appear **eligible for enrolment**". `TEST-1090`
   (`v048:443-446`) makes demonstrating it a release gate. The amendment states
   the rule in the durability section and then has the authority transaction
   write the row that triggers it against its own device key.
5. **The old key is simultaneously locked out.** After the commit the handle has
   a `cbcl-account-binding` row, so `dispatch:231` reads it,
   `enrol-facts:309-310` routes to `account-facts:349-358`, whose second
   statement is `path-b-profile` → `cbcl-chat-path-b-gate:profile/0` →
   `#(error tier-1-unapproved)` (`cbcl-chat-path-b-gate.lfe:16-18`). Even in a
   build where that constant is opened, the old key has no `cbcl-device-grant`
   row under the account (`path-b-issued?:393-397` is `'false`), so `CON-008`
   refuses it too.

**Failure trace.**

1. This review returns PASS. The implementer encodes `TEST-043`–`TEST-047` and
   `TEST-1083`–`TEST-1091`, then writes `finalize-credential-v2-migration/1`
   exactly as `CON-012` step 4 says, calling the sanctioned pairaccount writer
   for the new device key `D`.
2. The person completes the ceremony. Rooms migrate, the binding is written, the
   device grant is stored, `pairaccountgranted` is emitted. Per `CON-012` this
   is "the irreversible acceptance point".
3. The browser closes the legacy socket as `REQ-040` instructs and reconnects
   under `D`. Its first frame reaches `cbcl-core-enrol:resolve`;
   `unratified-device?(D)` is `'true`; the frame is refused `bad-signature`.
   Every subsequent frame under `D` is refused identically and opaquely.
4. The person retries under the legacy key `K`. `account-facts` meets the shut
   Path-B constant and refuses. They are out of every channel they were in.
5. `TEST-1083`'s "new-key Path-B reconnect", `TEST-1087`'s "The new key
   retrieves the immutable status", and `bus0161`'s `TEST-045` "the distinct
   granted key reconnects, passes Path-B standing" are unsatisfiable. Nothing
   recovers: `CON-959` and `CON-012` both forbid post-commit compensation, and
   no `mnesia:delete` in `apps/cbcl_chat/src/` targets `cbcl-account-binding` or
   `cbcl-pairaccount-grant`.
6. A second attempt does not help either. `unratified-count-tx:70-75` counts by
   the `handle` index and `cbcl-core-pairaccount:decide-claim` caps it, so
   `SPEC-076 REQ-005`'s cap of two aborts the third ceremony `cap-exceeded` —
   *inside the final transaction*, after the wallet has already published a DID
   and issued a grant.

**Why the effective set does not cover this.** `v048` and `bus0161` between them
mention `unratified` exactly three times, all in the durability sections
(`v048:225`, `v048:445`, and `bus0161` not at all). Neither `amends:` line names
`SPEC-076`; `bus0161:8` lists only `SPEC-053` clauses and `v048:8` lists
`SPEC-053 0.16.1-draft` as a whole. The `state` a credential/v2 pairaccount row
carries is never stated, no ratification surface is specified, and `SPEC-076`'s
own Open item is not listed beside the design's declared holds.

**Minimum correction.** One of three, stated explicitly and traced:

1. Do not write `cbcl-pairaccount-grant` at all in the credential/v2 transaction.
   State that `cbcl-device-grant` plus the binding is the complete authority for
   a credential/v2 device, that `SPEC-076`'s table is the *agent-pairing*
   ledger, and remove "pairaccount authority" from `CON-012` step 4,
   `v044:1032-1036`, `CON-959`'s writer list and `NFR-910`'s scope rationale; or
2. amend `SPEC-076 REQ-004` in `cbcl-bus`, through `SPEC-076`'s own amendment
   channel, to define a `ratified` state and to make a completed credential/v2
   confirmed migration the ratification event — and record that this changes the
   `SPEC-076:220-226` threat posture ("inert until ratified") for a new class of
   key, which is a `GATE-04` human-review input; or
3. keep the row and require the transaction to write it already-ratified, which
   still needs (2)'s amendment because the state and the enrol rule both live in
   `SPEC-076`'s scope.

In every case add a release-blocking row asserting that immediately after the
commit the granted device key passes `cbcl-core-enrol:resolve`'s
`pairing-unratified` clause, and re-state `REQ-005`'s two-unratified cap as a
pre-final refusal rather than a final-transaction abort.

---

### H-1 (HIGH) — the credential/v2 hub verbs are WebSocket chat frames on the already-shipped `/chat/v1` route, where TOFU enrolment completes inside `authorise-frame` *before* `route` reaches any handler, and an unrouted verb falls through to `do-publish`, which persists it and fans it to the room. `REQ-971`'s "handler consults it first… before TOFU-enrolling" is unsatisfiable, and `TEST-1088`'s "every request is opaque and produces zero writes" is false at the reviewed baseline

**Exact candidate clauses.**

`v048:133-138` (`REQ-971`): "Every allocation, prepare, finalize, authority,
accept, status, and migration handler consults it first, **before parsing
application bodies beyond route recognition, allocating nonce/scope/relay state,
writing, resolving, or TOFU-enrolling.**"

`v048:139-141` (`REQ-971`): "Production `cbcl-chat-app:dispatch/0` has no
credential/v2 authority route and the production CBCL grammar/WS dispatcher
recognizes no credential/v2 authority performative."

`v048:427-430` (`TEST-1088`): "Production route table, parser/dialect, WS
dispatch, static UI, relx manifest, and binary scan contain no credential/v2
authority selector or open test fixture; **every request is opaque and produces
zero writes/allocation.**"

`bus0161:245-248` (`GATE-00` disposition 2 and 3): "Every allocator/offer/
authority/accept/finalize/status handler consults it first, before nonce/scope
allocation, durable write, resolver call, relay allocation, or **legacy TOFU**.
Each handler has an opaque prohibited-action test with **zero writes**."

`SPEC-053:5249-5253` (`GATE-00` clause 2, quoted correctly by both): "'First' is
literal: a refusal that happens after a row is written has already shipped the
part that matters."

**Governing code evidence.**

The three hub commands are authenticated *session* commands
(`v044:968-969`: "These three authenticated HTTP/WebSocket commands are outside
cbcl-pairing relay frames"), and `cbcl-chat-app.lfe:204-238` shows the only
authenticated session surface is `(tuple "/chat/v1" 'cbcl-chat-session-ws '())`
at `:208` — a route that is already compiled in unconditionally and cannot be
gated by route compilation the way `cbcl-chat-webfinger-gate:routed?` gates
`/.well-known/webfinger` at `:216-220`. So the gate has to act inside the frame
path, and the frame path is fixed:

1. `handle-frame:205-211` decodes and calls `cbcl-parser:parse-message`, then
   `dispatch`.
2. `dispatch:213-235` special-cases only `grant` and `grantproof` (`:221-223`),
   with the comment "must not TOFU-enrol a key before `CON-206` accepts it".
   Everything else goes to `verify-and-route`.
3. `verify-and-route:242-264` verifies the signature, then calls
   `authorise-frame` (`:246`), which at `:282-302` resolves facts and, on
   `#(accept …)`, calls `enrol-commit` (`:288`).
4. `enrol-commit:454-463` on the `#(key …)` branch calls
   `cbcl-chat-members:enroll-or-check/2`, which at `cbcl-chat-members.lfe:42-54`
   **writes the `cbcl-member` row**.
5. Only then, at `:257`, does `route` run — and `route`/`route-member`
   (`:928-960`) is the first place a `selfsamepair*` clause could live.

So for any credential/v2 verb that the production dialect *does* parse, a
durable write and a TOFU enrolment have already happened before the handler
exists. `REQ-971`'s ordering and `GATE-00` clause 2 cannot both be honoured by a
handler-level check.

And for any verb the production dialect *does not* route, the fall-through is not
a refusal. `route-member:960`'s default is `do-publish`, and the module says why
in its own words at `:919-921`:

> "This is checked before the routing table, not inside it, because the hazard is
> the DEFAULT branch: `do-publish` **fans anything it does not recognise**, so an
> unlisted hub verb is not ignored — it is relayed to every member looking
> exactly like the hub's own."

`do-publish:1990-2018` reserves a blobstore slot (`:2006`) and calls
`cbcl-chat-room:publish` (`:2010`), i.e. **SPEC-021 persistence plus fan-out**,
returning `not-a-member` only when the sender has not joined that room. Neither
outcome is opaque, and the `cbcl-member` write at step 4 happened regardless.

**Failure trace.**

1. This review returns PASS. The implementer follows `v048:139-141` literally:
   the production `cbcl-parser`/`cbcl-core-wire` dialect carries no
   `selfsamepair*` verb, and no `route-member` clause exists.
2. An attacker (or a stale client, or an operator's smoke test) opens an ordinary
   authenticated chat session and sends
   `(selfsamepairallocate @general :carrier "…" :device "…")`.
3. `cbcl-parser` parses it — the CBCL grammar is open over performative names;
   `route`'s own comment is the proof. `dispatch` → `verify-and-route` →
   `authorise-frame` → `enrol-commit` → `cbcl-member` row written. One durable
   write, one TOFU enrolment, gate never consulted.
4. `route-member` falls through to `do-publish`, which persists the frame and
   fans it to every member of `@general`. The ceremony's carrier bytes, device
   JWK and (in the `PathAToB` case, if a client ever composes one) the legacy
   handle and room list are now chat history.
5. `TEST-1088` is run and fails on its own terms: the request was not opaque and
   it produced writes.

**Why "the dialect recognizes nothing" is not the answer either.** Making the
production parser reject the verb name outright is a change to a shared core
recogniser that must then differ between the production and the compiled-open
build — see `H-3`. And it still leaves `GATE-00` clause 3's "opaque
prohibited-action test with zero writes" unwritable, because in the production
build there is no gated surface to point the test at: the refusal is
`malformed-cbcl` from the parser, which is a different observable from the
refusal the compiled-open build produces.

**Minimum correction.** Three items.

1. State the conforming placement explicitly: for WebSocket verbs the
   credential/v2 gate is consulted **in `dispatch`, before `verify-and-route`**,
   in exactly the shape `dispatch:217-223` already uses for `grant`/`grantproof`
   and for the same stated reason. Cite that precedent. A handler-level check is
   non-conforming because `enrol-commit` runs first.
2. Require the production build to carry that dispatch-level clause returning one
   opaque refusal, rather than relying on parser absence — so that no
   credential/v2 verb can reach `verify-and-route`, `enrol-commit` or
   `do-publish` on any build, and so `GATE-00` clause 3's prohibited-action test
   has a surface with zero writes to assert against.
3. Correct `TEST-1088` and `bus0161`'s `TEST-047` to assert the two observable
   consequences separately: no `cbcl-member` row is created by a credential/v2
   verb, and no message is persisted or fanned for one.

---

### H-2 (HIGH) — `migrationConfirmationDigest`'s `fingerprintCanonicalBytes` input is defined nowhere in any of the four repositories, and the hub is required to reproduce it byte-for-byte from a Selfsame renderer that is BLAKE3 over a Selfsame domain string; `cbcl-bus` has no BLAKE3 primitive, and the value it must match has at least four defensible encodings in the shipped code

**Exact candidate clauses.**

`bus0161:147-153` (`CON-012`):

```text
migrationConfirmationDigest = SHA-256(
  UTF8("cbcl-chat credential/v2 migration confirmation v1<NUL>") ||
  offerCoreDigest || UTF8(accountDid) ||
  fingerprintCanonicalBytes || legacyKeyDigest || roomSetDigest ||
  migrationSnapshotDigest || snapshotNonce || deviceKeyDigest
)
```

(`<NUL>` here stands for the escape the candidate writes; see `L-4`.)

`bus0161:155-159`: "The hub derives every input except the wallet-rendered
fingerprint from held authenticated state, **independently renders the
fingerprint from `accountDid`**, and requires byte equality before constructing a
typed `ConfirmedCredentialV2Migration`."

`v048:168-171` (`REQ-972`): "`migrationConfirmationDigest` hashes its exact domain
plus raw `offerCoreDigest`, UTF-8 account DID, **canonical fingerprint bytes**,
the three snapshot/key/set digests, snapshot nonce, and device-key digest."

**Governing evidence.**

`fingerprintCanonicalBytes` and "canonical fingerprint bytes" occur **exactly
twice in the whole coordinated corpus** — `bus0161:151` and `v048:170` — and
nowhere else. I grepped `selfsame/specs/`, `cbcl-bus/specs/` and
`cbcl-pairing/specs/` for both spellings and for "fingerprint bytes";
`SPEC-002-visual-key-fingerprint.md` does not contain the phrase, and
`SPEC-002 REQ-103` (`:226-238`), which `v048:9` claims conformance to, says only
that `Fingerprint::hex` "remains the normative comparison value".

The shipped renderer offers four distinct byte strings for one fingerprint
(`crates/selfsame-core/src/fingerprint.rs`):

| Rendering | Bytes | Cited at |
|---|---|---|
| `Fingerprint::as_bytes()` | 6 raw digest octets | `:94-96` |
| `Fingerprint::hex()` | 17 ASCII octets, `"C0 7A 1E 42 9B 33"` | `:77-79` |
| `Fingerprint::label()` | `copper-lynx-42`, variable | `:84-90` |
| `LifeHash::rgb()` / `::base64()` | 3,072 raw / 4,096 ASCII | `:145-147`, `:172-175` |

Any of the first, second or fourth is a defensible reading of "canonical
fingerprint bytes"; `REQ-103` and `SPEC-002 REQ-105` push toward the hex, and
`v048:9`'s `conforms-to:` on `REQ-103` makes that reading live. Three of the four
produce different digests.

The primitive is also absent from the repository that must verify it.
`fingerprint_did` is

```rust
const DID_DOMAIN: &[u8] = b"anuna-ssi/v1/fp/did";
fn digest(domain, input) { blake3::Hasher::new(); update(domain); update(input);
                           finalize().as_bytes()[..6] }
```

(`fingerprint.rs:69`, `:203-215`). `cbcl-bus` at `fdbe414` has no BLAKE3: I
grepped `apps/` and `rebar.config` and the only `blake*` hits are libsodium's
`blake2b` headers pulled in transitively by `apps/cbcl_bus/c_src/cbcl_ristretto.d`.
OTP's `crypto:hash/2` — the hash every existing hub digest uses
(`cbcl-mls-hub-reducer.lfe:73-74`, `cbcl-mls-ds-wire.lfe:98`) — offers SHA-2,
SHA-3, BLAKE2b and BLAKE2s, and no BLAKE3. Rendering the fingerprint hub-side
therefore requires either a new NIF or a pure-Erlang BLAKE3, in the repository
whose own `GATE-00` posture makes adding cryptographic surface expensive.

**Failure trace.**

1. `TEST-1085` (`v048:397-404`) requires "two independent stacks" to reproduce
   the four digests and to "change the rendered fingerprint and require refusal".
   The test author must fix an expected `migrationConfirmationDigest` and cannot:
   the preimage has an undefined member.
2. The wallet implementer picks `hex().as_bytes()` (17 octets, the `REQ-103`
   reading). The hub implementer picks `as_bytes()` (6 octets, the "canonical"
   reading). Every confirmation digest mismatches.
3. `CON-012` says byte equality is required "before constructing a typed
   `ConfirmedCredentialV2Migration`", so **every** migration refuses at the final
   transaction — after DID publication, grant issuance and browser installation.
   Under `CON-959` those are all pre-commit, so the wallet compensates and the
   person sees an unexplained failure at the last step, every time.
4. If instead cbcl-bus never gains BLAKE3, the check cannot be implemented at
   all and the transaction has no conforming form.

**A second, smaller instance in the same preimage.** `deviceKeyDigest` is used
without a local definition or an anchor. It *is* definable — Selfsame
`SPEC-004:3617` gives "`base64url` SHA-256 of the canonical `cnf.jwk`" and
`:3658-3659` makes it "the SHA-256 of the canonical `deviceKeyJwk` in
`offer_core`" — but neither `bus0161` nor `v048:171` cites it, and `bus0161:129`
("raw 32-octet values in concatenated preimages") is the only thing that resolves
base64url-vs-raw. For a contract whose whole purpose is byte-exactness across
three languages, an uncited cross-repository input is a defect of the same
family.

**Minimum correction.** Two items.

1. Define `fingerprintCanonicalBytes` in `CON-012` in the shape `CON-012` already
   uses for `legacyKeyDigest`: name the function
   (`selfsame-core fingerprint_did`), the hash (BLAKE3), the exact domain octets
   (`anuna-ssi/v1/fp/did`, no separator), the truncation (first 6 octets), and
   the exact rendering that enters the preimage — and say which of the six raw
   octets, the seventeen ASCII octets of `Fingerprint::hex`, or something else it
   is. Add the vector to the `CON-226` `credential_v2` corpus that
   `v048:186-190` already mandates.
2. Either state that the hub must gain a BLAKE3 primitive and make that an
   explicit `GATE-04` implementation dependency, or — better — drop the hub-side
   re-render entirely. The hub already holds `accountDid`; re-deriving a
   *display* rendering from it adds no authentication the DID equality does not
   already provide, and buys a cross-language cryptographic parity obligation on
   the mandatory path. If the fingerprint stays in the preimage, cite
   `SPEC-004:3658-3659` for `deviceKeyDigest` in the same paragraph.

---

### H-3 (HIGH) — the mandated positive end-to-end fixture requires opening a *second* Tier-1 gate, `cbcl-chat-path-b-gate:profile/0`, which is `GATE-00` clause 1's named enablement point for `GATE-01` and which no amendment in the coordinated set names; and neither amendment says how the compiled-open build coexists with `cbcl-chat-gate-closed-tests`, whose whole job under clause 5 is to fail when a gate opens

**Exact candidate clauses.**

`v048:147-152` (`REQ-971`): "Test source may build an explicitly labelled
non-production route/profile and use the real local browser, relay, and wallet."
Singular, and the only gate named anywhere in `REQ-971` is
`cbcl-chat-credential-v2-gate:profile/0` (`v048:132`).

`v048:384-389` (`TEST-1083`): "Use the non-production compiled-open cbcl-bus
profile… Require the complete `CON-960` order, one migrated account, **a new-key
Path-B reconnect**, exact acknowledgement/receipt, and one linked wallet record."

`bus0161`'s `TEST-045` (`:291-297`): "the distinct granted key reconnects,
**passes Path-B standing in the compiled-open fixture**, and sees the migrated
rooms."

`bus0161:250-254` (`GATE-00` disposition 4 and 5): "Test source may install an
explicitly non-production route/profile… `cbcl-chat-gate-closed-tests` asserts
the constant, hostile-configuration indifference, compiled HTTP route table,
production dialect/dispatcher, and a gated-in test control."

**Governing evidence.**

Passing Path-B standing is not a property of the credential/v2 gate. It is
decided at `cbcl-chat-session-ws.lfe:349-358` (`account-facts`), whose second
statement is `path-b-profile` (`:803-804`) →
`cbcl-chat-path-b-gate:profile/0`, which at `cbcl-chat-path-b-gate.lfe:16-18` is
the constant `#(error tier-1-unapproved)`. `SPEC-053:5240-5248` names that exact
function as this repository's `GATE-00` clause-1 enablement point for `GATE-01`.
So `TEST-1083` and `TEST-045` require a build in which **two** Tier-1 constants
are opened, and the amendment set names one.

The repository has no existing mechanism to open it in a test. I enumerated every
reference: `cbcl-chat-gate-closed-tests.lfe:18-22` and `:37-40` assert it closed
(including under a hostile `selfsame_path_b_profile`);
`cbcl-chat-path-b-ws-tests.lfe:135,151` asserts the same;
`cbcl-chat-path-b-poller.lfe:15` and `cbcl-chat-enrolment-signer.lfe:42` consult
it; `cbcl-core-selfsame-application.lfe:6` records that it is "shut" so "a
boundary test can only ever" observe a refusal. No `meck`, no injection point, no
alternate module. Opening it means editing that source file for the fixture
build.

That is exactly what the 0.4.7 review's `H-1` minimum correction item 3 asked for
— "a development `cbcl-bus` build whose Path-B admission gate is opened by a
**named, reviewed change to `cbcl-chat-path-b-gate`** — declared as such, with the
resulting build marked non-production". `v048` and `bus0161` supply the
non-production label and the release-isolation evidence, but not the naming.

The coexistence problem is separate and is not addressed anywhere.
`cbcl-chat-gate-closed-tests` is `SPEC-053:5297-5304` clause 5 in code — its
stated purpose is that "opening it cannot arrive inside a change that reads as a
refactor". In the compiled-open build, `path-b-gate-is-closed` (`:18-22`) and any
new credential/v2 equivalent are red by construction. The only ways out are to
exclude that suite from the fixture profile — which removes clause 5's protection
from the one build where a gate is actually open — or to make the assertions
conditional on the profile, which is the "configuration decides" shape clause 1
forbids. `bus0161:254` asserts the suite covers "production dialect/dispatcher"
and "a gated-in test control" without saying which build runs which assertion.

**Failure trace.**

1. This review returns PASS. Step 4 of the sequencing (`v048:470-472`) says
   "Validate the real browser/relay/wallet path only in the visibly
   non-production compiled-open build."
2. The implementer builds a `credential-v2-open` rebar profile that swaps in an
   open `cbcl-chat-credential-v2-gate`, runs the ceremony, and reaches the
   migration commit. The browser reconnects under the granted key and is refused
   at `account-facts:352` because the Path-B constant is still shut.
3. They open `cbcl-chat-path-b-gate.lfe` too. `cbcl-chat-gate-closed-tests`
   goes red on `path-b-gate-is-closed`. The suite is excluded from the profile.
4. `GATE-00` clause 5's assertion now runs only in a build where nothing is open,
   and the build where two Tier-1 gates *are* open has no closure test at all.
   The `cbcl-bus` owner is asked to accept a fixture that disarms the mechanism
   their gate contract exists to arm — from a design that never told them it was
   opening `GATE-01`'s enablement point.
5. A worse repair is available and cheaper: make `account-facts` skip
   `path-b-profile` for a credential/v2-created binding. That ships a `GATE-01`
   bypass in production, and nothing in the amendment set forbids it by name.

**Minimum correction.** Three items.

1. Name `cbcl-chat-path-b-gate:profile/0` in `REQ-971` and in `bus0161`'s
   `GATE-00` disposition as a second constant the non-production fixture opens,
   with the resulting build declared non-production and excluded from every
   release manifest by the same evidence `v048:147-152` already requires.
2. State how `cbcl-chat-gate-closed-tests` runs in both builds: which assertions
   are production-artefact assertions (route table, dialect, relx manifest,
   binary scan) and are run against the production build only, and which
   constant assertions hold in every build. If the fixture build cannot run the
   constant assertions, say so and name the compensating control.
3. Add an explicit prohibition: no credential/v2 code path may bypass, cache,
   shortcut or special-case `path-b-profile` for an account created by a
   credential/v2 migration. Make it a `TEST-1088`/`TEST-047` row.

---

### H-4 (HIGH) — adopted `TEST-1027`'s "an unauthenticated caller obtains no signature" is false at the reviewed baseline: `/selfsame/enrolment/sign` is a live, routed production endpoint that signs caller-supplied `CON-214` statement octets under the enrolment key the wallet's whole display-authentication chain trusts, and no amendment in the coordinated set owns a change to it

**Exact candidate clauses.**

`v044:1289-1294` (`TEST-1027`, adopted and release-blocking per `v048:376`):

> "Attempt to vary every `CON-214` field through the browser and through direct
> HTTP calls. The hub either derives the canonical value from its signed
> session/profile or refuses. Credential/v2 has no call edge to
> `/selfsame/enrolment/sign`, and **an unauthenticated caller obtains no
> signature.**"

`v044:970-972` (`CON-939`): "The prototype `/selfsame/enrolment/sign` route is not
callable by credential/v2 and may not be treated as an implementation shortcut."

`v044:964-966` (`CON-939`): "The hub signs only canonical state it constructed.
Caller-supplied JWS octets … are rejected."

**Governing evidence, `cbcl-bus` at `fdbe414`.**

The route is compiled in and open. `cbcl-chat-app.lfe:221-227` installs
`/selfsame/enrolment/sign` whenever
`cbcl-chat-selfsame-application-gate:routed?` is true, and
`cbcl-chat-selfsame-application-gate.lfe:26` is `(defun routed? () 'true)` — the
2026-08-22 ratification. `cbcl-chat-gate-closed-tests.lfe:57-64` asserts that
route group is present.

The handler signs what it is given. `cbcl-chat-selfsame-application-http.lfe:78-88`
reads up to 16,384 octets of body and passes them straight to
`cbcl-chat-enrolment-signer:sign/1`. The module's own header says what that is,
at `:59-77`:

> "PROTOTYPE POSTURE (codex review, **'the hub is an unauthenticated
> application-signature oracle'**): this still signs a caller-supplied statement
> with the deployment's enrolment key… **A server-side caller (not a browser) can
> still POST a well-formed statement**; that residual is what the ceremony-state
> authentication above is for."

The two controls named there — requiring `application/json` and omitting
`access-control-allow-origin` — defeat only the cross-origin *browser* vector, as
the comment states. There is no authentication, no nonce, no session binding.

It is provisioned in production. `config/sys.config:12,14` and
`apps/cbcl_chat/src/cbcl_chat.app.src:23,25` set
`selfsame_enrolment_kid = https://chat.anuna.io/selfsame/application#enrollment-2026-08`
and `selfsame_enrolment_seed_path = /data/selfsame-enrolment-seed.hex`, and
`scripts/entrypoint.sh:246` writes that file from a Fly secret. The `kid` matches
the `requestSigningKeys` entry embedded in the ratified profile
(`cbcl-chat-selfsame-application-gate.lfe:45-48`) — i.e. the exact key a wallet
resolves through `CON-220` and trusts.

Today the blast radius is bounded by the *grammar*, not by the amendment.
`sign/1:68-72` routes through `cbcl_selfsame_erl:sign_enrollment_profile_bound`,
which applies upstream's closed `enrollment::recognise_unsigned`; `SPEC-004`
`CON-214` (`:3637-3641`) rejects unknown members, so a v2-shaped statement
carrying `accountPrincipalDigest`, `applicationAccountDisplay`, `carrierDigest`,
`transcriptHash` and `devicePossessionProof` is refused **as long as the v2
grammar does not exist upstream**. `CON-939` requires exactly that grammar to be
added.

**Failure trace.**

1. This review returns PASS. Step 3 (`v048:466-469`) has the implementer commit
   schemas and vectors first: the v2 `CON-214` grammar lands in
   `selfsame-app-identity::enrollment` so `sign_enrollment_profile_bound` can
   produce the evidence `selfsamepairofferfinalize` needs.
2. The shipped `/selfsame/enrolment/sign` route now accepts a v2 statement,
   because it does no recognition of its own and delegates to the one recogniser
   that just widened.
3. An attacker composes an `OfferCoreV2` naming the victim's handle, a fabricated
   `PathAToB` plan (any room names they like), their own `deviceKeyJwk` and their
   own relay, builds the matching v2 `CON-214` statement, and POSTs it with
   `content-type: application/json` from a server. They receive a compact JWS
   under `#enrollment-2026-08`.
4. They run the ceremony as allocator against a Selfsame wallet. The wallet's
   `IntentVerifier` (`CON-936`) does exactly what `CON-943` says: recomputes
   `offerCoreDigest`/`intentDigest`, verifies the evidence against the `CON-220`
   profile's declared signing key — which is the key that just signed the
   attacker's statement — and constructs `AuthenticatedIntent`.
5. The person is shown an application-authenticated first-contact screen for
   `chat.anuna.io` and, at the final screen, a migration plan for rooms they may
   or may not be in. They approve. The wallet derives, publishes a DID and issues
   a device grant to the attacker's device key. The real hub never migrates
   anything, so nothing on chat.anuna.io reveals what happened.
6. `TEST-1027` is run and fails on its last clause, and the ceremony's entire
   "live-`CON-220` authenticated, never a peer/caller string" property — the
   answer to 0.4.0's `F-B` — turns out to rest on an endpoint that authenticates
   nobody.

**Why the effective set does not cover it.** `v044:111` records the baseline fact
("the signing route signs caller octets") in a table of *observations*, and
`v044:970-972` forbids credential/v2 from *calling* it. Neither is a change to
the route, and a `cbcl-bus` route can only be changed by a `cbcl-bus` amendment.
`bus0161:8`'s `amends:` lists nine `SPEC-053` clauses and does not include
`REQ-031` or the enrolment-signing surface; `bus0161`'s `GATE-00` disposition
(`:243-259`) closes with "The already published application profile and blind
relay keep only their **existing narrow** `GATE-00` dispositions", which leaves
this route exactly as it is.

**Minimum correction.** Two items, one of them in `cbcl-bus`.

1. Add an owning clause to `SPEC-053` 0.16.1 (or a sibling amendment) that makes
   one of these true before any v2 `CON-214` grammar lands: the
   `/selfsame/enrolment/sign` route is removed; or it is bound to an
   authenticated session and a single-use server-side ceremony record; or it
   refuses any statement whose `evidenceVersion`/`profileVersion` is not exactly
   the v1 pair, with that refusal asserted by a test that is red-first against a
   v2 statement. Name it in `GATE-04`'s enablement boxes.
2. Correct `v048`'s effective-text ledger to state that `TEST-1027`'s
   unauthenticated-caller clause is a `cbcl-bus` obligation, not a Selfsame one,
   and cite the owning clause. As written, an adopted release-blocking Selfsame
   test asserts a property about a `cbcl-bus` endpoint that no document in the
   set has authority to make true.

---

### M-1 (MEDIUM) — the wallet cannot recompute two of the three offer digests it must sign over, and the one it *can* recompute — `roomSetDigest` from the displayed `migrationRooms` — is nowhere required to be recomputed; so the room list the person reads is not bound to the set the transaction migrates, and `REQ-972`'s "recompute … inputs independently" is impossible as written

**Exact candidate clauses.**

`v048:173-177` (`REQ-972`): "Both browser and wallet recompute offer/descriptor/
intent inputs independently. The wallet recomputes the confirmation from its
local preview and authenticated offer; the hub recomputes it from held state and
its own fingerprint renderer. **Copying a peer/caller digest** or accepting a
browser boolean **is non-conforming.**"

`bus0161:140-146` (`CON-012`): "The offer contains the handle, complete room
array, the three digests, and a 32-octet CSPRNG `snapshotNonce`."

`bus0161:169-170` (`CON-012` step 1): the hub "re-reads and recomputes the
complete snapshot and all confirmation inputs".

**Why it does not close.** Of the three digests in the confirmation preimage:

| Digest | Wallet inputs available? |
|---|---|
| `legacyKeyDigest` | **No.** Preimage is the raw 32-octet enrolled key (`bus0161:131-134`); the wallet never receives it — `REQ-040` item 3 gives it only the legacy *key fingerprint* for display. |
| `migrationSnapshotDigest` | **No.** Preimage is every `cbcl-roommember` row's `key`, `room`, `handle`, `pubkey`, `granted`, `via`, `since` (`bus0161:136-139`). None of `pubkey`/`granted`/`via`/`since`/`key` is in the offer. |
| `roomSetDigest` | **Yes.** Preimage is deterministic CBOR of the complete unique room byte array (`bus0161:126-135`) — precisely `migrationRooms`, which the offer carries and the final screen displays. |

So "recompute … inputs independently" is unsatisfiable for two of three, and
"copying a peer/caller digest is non-conforming" would make the wallet's own
mandated behaviour non-conforming, since copying is the only thing it can do
with them. Meanwhile the one recomputation that would carry weight is not
required anywhere: not in `REQ-972`, not in `CON-960`, not in `pair043`'s
`CON-020` or `TEST-052`, and not in `TEST-1085`, whose mutation list is "each
field/domain/encoding/sort/null tag" of the digests themselves.

**Failure trace (a buggy hub, not a hostile one).**

1. `selfsamepairallocate` builds `migrationRooms` from the snapshot and computes
   `roomSetDigest` from a second traversal — or from `cbcl-account-roommember`,
   or from an index read that misses a row inserted concurrently, or from a
   `usort` applied to one list and not the other.
2. The offer is signed as a whole, so the wallet authenticates it and displays a
   room list that is missing one channel.
3. The person approves "exactly these channels". The confirmation carries the
   hub's `roomSetDigest`, which covers the *other* set.
4. `CON-012` step 1 re-reads the snapshot and recomputes; the recomputation
   matches its own earlier digest; the transaction commits and migrates the
   channel the person never saw. `NFR-008`'s "No implementation silently widens
   consent" is violated by an implementation that satisfies every stated check.

The `MigrationSnapshotChanged` machinery does not catch this: it detects the rows
changing between snapshot and finalization, not the display disagreeing with the
digest at snapshot time.

**Minimum correction.** Two sentences in `REQ-972`, mirrored in `CON-012`.

1. Require the wallet to compute `roomSetDigest` itself from the exact
   `migrationRooms` array it is about to display, and to refuse before display if
   it differs from the offer's value. That is the only step that makes the
   displayed list load-bearing.
2. Replace "Both browser and wallet recompute offer/descriptor/intent inputs
   independently" with the truthful split: which inputs each party recomputes
   (`offerCoreDigest`, `intentDigest`, `roomSetDigest`, the fingerprint,
   `deviceKeyDigest`) and which are hub-only assertions carried inside the signed
   offer (`legacyKeyDigest`, `migrationSnapshotDigest`) and therefore bind the
   hub to its own state rather than cross-checking it. Say that explicitly, so
   the person's protection is not overstated. Add a `TEST-1085` row that mutates
   `migrationRooms` without mutating `roomSetDigest` and requires refusal before
   display.

---

### M-2 (MEDIUM) — `cbcl-core-dialect:hub-originated?` is a closed literal list and is the only thing that stops a client forging a hub-minted verb into `do-publish`'s fan-out; no credential/v2 reply performative is added to it by any amendment

**Exact candidate clauses.** `v044:1009-1034` introduces `selfsamepairauthority`,
`selfsamepairaccept` and the extended `pairaccountgrant`, each with a
"correlated signed reply". `bus0161:75` has the transaction "emit
`pairaccountgranted` and one `account-linked` notice per affected live room".
`v048:139-141` requires the production dialect to recognise no credential/v2
authority performative. Nothing anywhere names `cbcl-core-dialect`.

**Governing evidence.** `cbcl-chat-session-ws.lfe:928-931`:

```lfe
(defun route (kind msg payload sig sig-rec room handle state)
  (if (cbcl-core-dialect:hub-originated? kind)
    (reply-error state (or-room room) #"hub-only")
    (route-member …)))
```

with the comment at `:919-927` explaining that this is checked before the routing
table "because the hazard is the DEFAULT branch". `cbcl-core-dialect.lfe:79-84`
is a literal `lists:member` over fourteen atoms; `pairaccountgranted` and
`account-linked` are in it, which is exactly why the existing SPEC-076 and
REQ-033 replies cannot be forged.

Any new hub-minted credential/v2 reply that is *not* added to that list can be
sent by a client and, because `route-member`'s default fans anything it does not
recognise, is relayed to every member of the room "looking exactly like the hub's
own, save for the `:from` the hub never writes". A browser correlating a signed
reply by ceremony ID would still need the signature — but the design's own
`v044:1010-1019` describes these as *correlated signed* replies precisely because
correlation matters, and shipping a forgeable channel beside the authenticated
one is the class of hazard `:919-927` exists to close.

**Minimum correction.** One clause in `CON-959` or `bus0161`'s `CON-012`: every
credential/v2 performative the hub mints SHALL be a member of
`cbcl-core-dialect:hub-originated?` in every build that recognises it, and a
`TEST-1088`/`TEST-047` row SHALL assert that a client-sent instance of each is
refused `hub-only` and is neither persisted nor fanned.

---

### M-3 (MEDIUM) — the typed `AccountTransition` has no carrier: `DisplayIntent` and `DisplayField` are public structs with public fields in the shared crate, `CON-960` forbids the only mechanism that exists, and `pair043`'s `CON-020` — the one amendment with authority over that crate — changes prose rather than the type

**Exact candidate clauses.**

`v048:299-303` (`CON-960`): "The credential/v2 `IntentVerifier` returns a closed
typed `AccountTransition` only after … **Generic `DisplayField`, peer-claimed
values, caller booleans, and post-verification mutation cannot represent it.**
cbcl-pairing 0.4.3 `CON-020` is the only shared display-layer amendment."

`pair043:37-42` (`REQ-025`): "The shared endpoint accepts this display only as
part of the opaque profile-authenticated object returned by `IntentVerifier`. It
does not parse a room name, compute migration policy, accept a peer/caller
transition flag, or **expose a generic display-field constructor.**"

`pair043:47-49` (`REQ-025`): "No new shared performative, kind number, sender
rule, state transition, frame, fixed allocation, or relay-visible field is
introduced."

`pair043:60-70` (`CON-020`) replaces the `CON-017` `display` row with a prose
line containing "optional typed account transition", and nothing else.

**Governing evidence, cbcl-pairing at `8e55160` (`git show 8e55160:src/profile.rs`).**

```rust
pub struct DisplayField { pub label: &'static str, pub value: String,
                          pub claimed_by_secret_holder: bool }          // :81-88
pub struct DisplayIntent { pub application: String, pub action: String,
                           pub authority_summary: String,
                           pub fields: Vec<DisplayField> }              // :92-101
```

Every field is `pub` and neither type is `#[non_exhaustive]`, so a
"generic display-field constructor" is exposed today by construction — any crate
can write `DisplayField { .. }`. `ApplicationProfile::recognise_intent` (`:206`)
returns `ProfileIntent`, which wraps a `DisplayIntent`; the endpoint emits
`EndpointEffect::DisplayIntent(DisplayIntent)`.

Downstream, Selfsame flattens it into exactly that generic shape:
`src-tauri/src/cbcl_pairing.rs:413-428` (`intent_view`) maps
`intent.fields` into `Vec<CbclIntentFieldView>` — `{label, value,
claimed_by_secret_holder}` serialised to the front end at `:51-67`. There is no
other channel from the shared reducer to the person.

So `CON-960` forbids the only existing representation, and the amendment that
owns the crate does not create a replacement. Carrying a `PathAToB` plan — legacy
handle, up to 256 room names, three digests, a nonce, then a DID and fingerprint
— through `Vec<DisplayField>` is exactly what `CON-960` prohibits; carrying it
any other way requires a typed member on the shared `DisplayIntent` (making
`AccountTransition` a shared cbcl-pairing type) plus making `DisplayField`
non-constructible, which is a breaking change to a public type the agent profile
also uses. Neither is stated, neither is traced, and `REQ-025`'s "no new shared …
field" sentence reads as though no shared type changes at all.

This is an ownership-boundary defect as much as an implementability one:
`v048:299-303` is a Selfsame contract constraining a cbcl-pairing type, and
`SPEC-001:1486-1496` permits amendment of that crate's contracts only through a
reviewed merged revision of `SPEC-001`.

**Minimum correction.** In `pair043`'s `CON-020`, state the shared type change
exactly: `DisplayIntent` gains one optional typed `AccountTransition` member with
the closed variant set `NoTransition | PathAToB { … }`; `DisplayField` and
`DisplayIntent` become non-exhaustive with private construction so a consumer
cannot fabricate either; and the consumer adapter is the sole constructor. Add a
`TEST-052` row asserting that a consumer cannot construct a `DisplayIntent` or
`DisplayField` outside the adapter. Then have `v048`'s `CON-960` cite that clause
rather than assert the prohibition on its own authority.

---

### L-1 (LOW) — `CON-012`'s and `CON-959`'s lock order names a "device … count index" that does not exist; `cbcl-device-grant` has no index and its count is a whole-table `foldl`

`bus0161:163-166` locks "device/pairaccount count indexes and exact keys";
`v048:342-346` (`CON-959`) says "the device/pairaccount count/index and exact-key
locks". For pairaccount that is right: `cbcl-chat-pairaccount.lfe:29-38` creates
`(tuple 'index (list 'handle 'devkey))` and `unratified-count-tx:70-75` reads
through it. For devices it is not: `cbcl-chat-account-grants.lfe:53-63`
(`ensure-device-table`) declares no index, and `account-device-count/1:204-207`
is `mnesia:foldl` over the entire `cbcl-device-grant` table — a whole-table read
lock inside the one authority transaction, scaling with every grant for every
account, taken while whole-table write locks on both membership tables are
already held. Either add the index as a declared schema change (with its
`mnesia:add_table_index` migration and its place in `NFR-910`'s manifest), or say
the device cap is enforced by a full scan and account for the lock it takes.

### L-2 (LOW) — adopted `v044`'s `CON-941` still states `cbcl-webfinger-account`'s primary key is the handle, which `REQ-970` contradicts and `CON-961`'s ledger does not replace

`v044:1056` gives the table row "`cbcl-webfinger-account` | **handle** | remains a
mutable lookup projection". `v048:79-83` (`REQ-970`) says `ReciprocalAlias`
"comes only from the **account-keyed** `cbcl-webfinger-account` authority store
using the stable application account ID". The code agrees with `REQ-970`:
`cbcl-chat-webfinger-store.lfe:54` is
`(defrecord cbcl-webfinger-account account alias home-did)` and `:70` creates it
with `'(account alias home-did)`, so the primary key is the account.
`CON-961`'s ten replacements (`v048:310-336`) replace `CON-941`'s *state enum*
(item 3) and `v047`'s `CON-956` key cells (item 9), but not this row. One ledger
line. **Related, and worth stating rather than leaving to inference:**
`provision/3` still has no production caller — I re-grepped
`apps/cbcl_chat/src/` and every hit is the definition or a comment — so
`cbcl-webfinger-account` is empty in production and, because `REQ-970` mints a
fresh CSPRNG `applicationAccountId` per ceremony, `NoReciprocalAlias` is the only
reachable answer on the first-link branch. `BoundSameDid`/`BoundDifferentDid` are
relink-only. That is coherent, but `TEST-1089`'s cross-product should say which
cells are reachable in which build so it cannot pass vacuously.

### L-3 (LOW) — `REQ-971` states as a present-tense fact about the reviewed baseline a module that does not exist there

`v048:131-135`: "**At the reviewed baseline** `cbcl-chat-credential-v2-gate:profile/0`
is the sole enablement point, is constant in source, returns
`#(error tier-1-unapproved)` unconditionally…". At `fdbe414` the only gate
modules are `cbcl-chat-path-b-gate`, `cbcl-chat-webfinger-gate` and
`cbcl-chat-selfsame-application-gate` (`apps/cbcl_chat/src/shell/`). The clause is
a specification of the landing shape, and `bus0161:243-249` writes it correctly as
one ("The landing shape is mandatory: 1. `cbcl-chat-credential-v2-gate:profile/0`
is the sole enablement point and returns…"). Change "At the reviewed baseline" to
"On landing", so the amendment does not assert a false fact about a commit a
reviewer can check.

### L-4 (LOW) — `CON-012` uses two escape notations for the same NUL and never states the octet; `REQ-972` describes a "field vector" that `CON-012` does not supply

`bus0161:131-133` terminates the legacy-key domain with a six-character
`backslash-u-0-0-0-0` escape, and `:148-149` terminates the confirmation
domain the same way, while `:135` writes
`"cbcl-chat credential/v2 room set v1\0"` and `:141` writes
`"cbcl-chat credential/v2 migration snapshot v1\0"`. Both notations resolve to a
single NUL in Rust, JavaScript and Erlang, so nothing is ambiguous *if the reader
supplies a language*; in a contract whose stated purpose is byte-exactness across
three of them, the octet should be stated once ("a single `0x00` terminates each
literal domain; no other separator is present") and one notation used. Separately,
`v048:165-168` says `migrationSnapshotDigest` covers "the exact `cbcl-member` …
field vectors", while `bus0161:136-137` gives
`["cbcl-member", handle, enrolledPublicKey]` — three elements, omitting the
record's third attribute `enrolled` (`cbcl-chat-members.lfe:13`). That omission is
right (it is a wall-clock stamp), but "exact … field vectors" says otherwise.

### L-5 (LOW) — the 60,000-octet offer-core cap is asserted to fit the 62,000-octet logical bound without the arithmetic every other bound in this lineage carries

`v048:179-183` (`REQ-972`) sets the offer core at 60,000 RFC-8785 octets and says
"the enclosing credential/v2 large object **remains** at `logicalLength <=
62,000`". `v045:259-262` (`CON-943`) makes the offer body "the complete finalized
offer whose core recomputes the digest", and `v044:1023-1030` puts the `CON-214`
evidence and the provider hint in that body beside the core. Working from
`SPEC-004:3600-3630`'s member set, a v2 `CON-214` statement is roughly 900 octets
of canonical JSON, so its compact JWS is roughly 1,370; with the hub signature,
the provider hint and the enclosing structure the body lands near 61,800 against a
62,000 ceiling — under, but with about 200 octets of headroom and no margin
stated. Every other numeric claim in this lineage arrives with its encoded
lengths (`v045:236-240`, `v046:164-174`, cbcl-pairing `CON-019`'s
4,142/4,143/4,144, which I recomputed and reproduce exactly). Give this one the
same treatment: show the residual, or lower the core cap to leave a declared
margin.

### L-6 (LOW) — `MigrationPlanTooLarge` is a terminal refusal with no remaining route, and the consequence is not stated

`bus0161:88-93` (`NFR-008`) and `v048:179-184` refuse an account beyond 256 rooms
or 48,000 room octets "before carrier allocation", with "no paging, truncation,
ellipsis, hidden remainder, or partial-room migration … permitted by this
version". Because `v048:36-40` deletes the console alternative and `REQ-970`
makes `PathB` reachable only for a session that already has a binding — which only
this ceremony creates — such a person has no path to a linked wallet at all, in
this version or any build. That may well be the right call at 256 rooms, but the
refusal currently reads as a bound when it is an exclusion. One sentence: name the
consequence, and name the increment that will carry a bounded multi-batch or
scoped-subset consent.

### L-7 (LOW) — `REQ-970`'s no-`offer` rule is stated but no test row asserts it from the side that can observe it

`v048:98-101` and `CON-961` item 8 supply the 0.4.7 `L-5` correction exactly: "The
allocator SHALL NOT send `offer` until the result is non-terminal", and a terminal
result "closes or expires the mailbox without an offer object". The 0.4.7 review's
correction had a second half — "Extend `TEST-1075` to assert that **no `offer`
object reaches the relay** in the legacy case, which is the only assertion that can
be made from the allocator's side." `TEST-1089` (`v048:436-441`) asserts "the exact
`REQ-970` outcome" for the membership/alias cross-product but names no relay
observation, and `TEST-1084` (`v048:391-396`) lists "offer/authority mismatch"
among wallet-side refusals. Add the relay-side assertion: for every terminal
authority result, zero `offer` objects are observed at the mailbox.

---

## Disposition of every 0.4.7 finding

Verified against `v048`, `bus0161` and `pair043` **and** against source at the
four baselines, not against the candidate's own table at `v048:34-52`.

| 0.4.7 finding | Candidate's claim | My verdict |
|---|---|---|
| **H-1** (HIGH) — no reachable claimant; eligible class empty; step-4 E2E unconstructible | `REQ-970` makes the deployed Path-A member the positive branch and does not assert an empty complement | **Closed at the entry, not at the exit. Residual is HIGH (`H-3`, and `C-1`).** The reachability *statement* is now exact and matches code: `v048:59-63` says every ordinary authenticated handle has a `cbcl-member` row and an account-bound handle cannot authenticate while Path B is shut, both of which I re-verified (`cbcl-chat-session-ws.lfe:308-320`, `:349-358`, `cbcl-chat-members.lfe:42-54`, `cbcl-chat-path-b-gate.lfe:16-18`), and the eligibility precondition is named (`v048:122-126`, `bus0161:81-85`). Correction items 1, 2 and 4 are made. **Item 3 is half-made**: the E2E is placed in a non-production compiled-open build, but the specific change the item asked to be *named* — opening `cbcl-chat-path-b-gate` — is not named, and the reconnect the positive test ends on is blocked by `C-1` regardless. |
| **H-2** (HIGH) — console remedy locks the person out, is irreversible, cannot name the DID | `REQ-970` deletes the console alternative; hub mints scope first; final screen names DID/fingerprint/rooms | **Closed, and I could not reopen it.** `v048:126-128` and `bus0161:85-87` both forbid the console call while Path B is shut and state it has no sanctioned wrapper. `ADR-941` (`v048:263-271`) and `ADR-017` (`bus0161:200-208`) state the circularity argument correctly, and it is correct: `hierarchy.rs` derives the home DID from the scope the hub mints, so no pre-pairing surface can name it. `CON-959` keeps `confirmed-migrate-path-a-handle/3` non-sanctioned with no production call edge, which I re-verified. |
| **H-3** (HIGH) — `GATE-00` never named; clause 1 rejects the configurable allocator gate | `REQ-971` + `GATE-04` require a constant-in-source gate, physical route/dialect absence, opaque tests, non-production compiled-open build | **Closed on authority and naming; the landing shape does not survive contact with the code. Residual is HIGH (`H-1`, `H-3`).** Correction item 1 is made (`v048:9` `conforms-to:`, `bus0161:224-259`'s per-surface disposition). Item 3 is made verbatim (`v048:142-146`, `bus0161:256-258`: the throttle "is not the enablement point"). Item 2 is made for the HTTP half and **fails for the WebSocket half**: `REQ-971`'s "handler consults it first … before TOFU-enrolling" is unsatisfiable because `enrol-commit` runs inside `authorise-frame`, and "the dialect recognises no performative" routes to `do-publish`, which writes and fans. |
| **M-1** (MEDIUM) — `legacyKeyDigest`/`roomSetDigest` had no preimage | `REQ-972` adopts `CON-012`'s domains, CBOR tuples, sort order, null/atom rules and vectors | **Closed for the two digests named, and closed well. New defects introduced in the same clause: `H-2`, `M-1`, `L-4`.** `bus0161:126-146` is exact where it is exact: the domains are literal, the sort is "unsigned-octet lexicographic", the row tuple is enumerated, `since = undefined` maps to CBOR null, and `via` is "exactly text `standing` or `invite`" — which I verified is exhaustive at the baseline (`cbcl-chat-room.lfe:330` passes `'standing`, `:344` passes `'invite` through `redeem-and-grant`, and no other caller of `cbcl-chat-roommember:grant/5` exists). But the *confirmation* digest, which is the one the person's consent actually rides on, has an undefined member (`H-2`), an impossible recomputation instruction (`M-1`), and inconsistent escape notation (`L-4`). |
| **M-2** (MEDIUM) — durability list omits `cbcl-successor-pointer`; consequence list incomplete | `NFR-910` adds it, records both fail-open failures, scopes the gate | **Closed, and both consequences are correct.** I re-verified `cbcl-chat-successor.lfe:150-154` — `mnesia:dirty_read` on an empty table returns `'()` and answers `'false`, so an empty table un-spends every departed key — and `cbcl-chat-pairaccount.lfe:97-104` — `index_read` over an empty table makes `lists:any` false, so an unratified device looks eligible. `v048:220-228` states both in exactly those terms, and `v048:229-232` honestly disclaims chat/MLS/invitation continuity. `TEST-1090` is the requested row. |
| **L-1** (LOW) — `Bound(accountDid)` store ambiguous | `REQ-970` separates the axes, names `cbcl-webfinger-account` keyed by stable account | **Closed. Residual `L-2`**: adopted `v044:1056` still says that table is keyed on the handle, and `CON-961` does not replace the row. |
| **L-2** (LOW) — root correction not ledgered | `CON-961` item 5 replaces `v046`'s `CON-952` sentence | **Closed.** `v048:326-327` is the requested single ledger row. |
| **L-3** (LOW) — cbcl-pairing `TEST-050` still names 4,096 | `pair043`'s `CON-020` replaces it through the owning channel | **Closed.** See the cbcl-pairing section below. |
| **L-4** (LOW) — inert rendezvous recognition narrows `REQ-807` unnamed | `REQ-807` is in `amends:`; `CON-961` item 7 records the reading and the cost | **Closed.** `v048:8` now carries `[[SPEC-007-cbcl-pairing-cutover#REQ-807]]`, and `v048:331-336` records both halves the review asked for, including the `PROTO-002 CON-301` / `PROTO-003 CON-401` grammar cost for a branch that is required to reach no outcome. |
| **L-5** (LOW) — browser cannot enforce wallet-side ordering | `REQ-970` adds the allocator no-`offer` rule; `CON-961` item 8 | **Closed in text; the test half is not made. Residual `L-7`.** |
| **L-6** (LOW) — membership primary keys misstated | `CON-959` pins both raw length-prefixed keys and lists `bind-account/2` | **Closed, exactly.** `v048:339-346` matches `cbcl-chat-roommember.lfe:259-262` (`u32be(len(room)) || room || handle`) and `cbcl-chat-account-roommember.lfe:234-239` (`… || u32be(len(account)) || account || handle`) byte-for-byte, and `bus0161:180-184` repeats them. `v048:348-350` adds `bind-account/2` to the non-sanctioned list, which is the item-4 correction from 0.4.7's `H-1`. I re-enumerated the binding writers and `CON-959`'s list is complete: `enrol-commit:454-463` writes no binding on the account branch, only `cbcl-chat-path-b-live:observe`. |

### Carried findings from 0.4.4–0.4.6

Every 0.4.6 finding was verified closed by the 0.4.7 review, and I re-tested the
three that `v048` touches: the membership-writer enumeration (0.4.6 `H-1`) is
still closed and is now stronger; `REQ-968`'s descriptor digest (0.4.6 `M-1`) is
unchanged and I re-derived it; `NFR-909`'s replacement by `NFR-910` (0.4.6 `M-3`)
strengthens rather than reopens. The 0.4.5 and 0.4.4 carried sets are untouched by
`v048` except through `CON-961`, whose ten replacements I checked one by one
against the text they name; nine are exact and item 3's is incomplete (`L-2`).

---

## Original `F-A` – `F-E` regression matrix

Re-tested against the design *and* against the shipped code, which is unchanged
since `c6af6df` (correct for a design-only candidate; the code is expected to
fail these until implementation).

| Finding | Design status at 0.4.8 | Code status at `c6af6df` | Verdict |
|---|---|---|---|
| **F-A** (CRITICAL) — TOFU relay-scoped, not `(applicationId, relayOrigin)`-scoped | **Held.** `v044`'s `REQ-914`/`CON-908` key policy by the exact pair and `v048` does not touch it; `REQ-970`'s Path-A branch adds an authority axis, not a trust axis, and `CON-960`'s order still puts the TOFU decision inside the authenticated-intent display. `v048:307-308` keeps "separately accepted TOFU/policy/rate state" as the only pre-final wallet mutation. | Not implemented. | **No regression.** |
| **F-B** (CRITICAL) — consent screen shows the peer-claimed `applicationId`; cross-check runs after Approve | **Held in design, and `v048` extends it correctly to the new values.** `CON-936` (`v044:817-828`) puts `IntentVerifier` before display; `CON-960` (`v048:299-303`) requires the transition to come from that object and forbids `claimed_by_secret_holder` representation. | **Still exactly as `F-B` described.** `crates/selfsame-pairing/src/lib.rs:376-379` passes `EndpointEffect::DisplayIntent` straight through, and `recognise_transfer_claims` runs only in the `EndpointEffect::DeliverGrant` arm at `:393-395` — after `decide(Approve)`. `src-tauri/src/cbcl_pairing.rs:413-428` flattens the peer-claimed fields to the UI. | **No regression; `M-3` is the new obstacle to the fix** — the design forbids the generic field but no amendment supplies the typed replacement. |
| **F-C** (CRITICAL) — derivation before consent, contradicting `SPEC-007 REQ-812` | **Held.** `CON-938`'s split preview/commit APIs, `CON-937`'s effect surface, and `CON-960`'s order (`v048:305-309`: "No wallet custody access, derivation, signing, publication, policy write, grant write, or durable identity state precedes its existing consent edge"). The new pre-consent effects `v048:87-93` adds are all **hub-side** pending rows (scope, account id, ceremony), which `REQ-812` does not reach — it governs accepted-credential and identity side effects, and `SPEC-007`'s `REQ-812` is quoted correctly. | **Still exactly as `F-C` described.** `src-tauri/src/cbcl_context.rs:284-299` derives through `Custody::use_hierarchy_root` / `hierarchy::derive` and mints an issuer identity during `assemble_claimant`, before any socket. | **No regression.** |
| **F-D** (HIGH) — standalone first contact has no `account_scope_id` | **Closed in design, and this is where 0.4.8 does its best work.** `v044:349-351` has the hub mint the `CON-211` scope; `v048:87-90` makes it a *pending* CSPRNG allocation deleted on decline/expiry/pre-commit failure, with concurrent allocation for the same `(applicationId, handle, enrolledKey)` refusing or returning the byte-identical ceremony; `ADR-941` explains why the scope must precede the DID. | Not implemented — `assemble_claimant:279-281` still requires `record.account_scope_id` from a held `PairingTrustRecord`. | **No regression; the bootstrap is now specified.** |
| **F-E** (HIGH) — nothing binds relay operatorship to the application | **Held as the explicitly accepted per-application mitigation.** `v044`'s `CON-933` pins `operatorId`, `conformanceEvidenceDigest` and `privacyPolicyDigest`; `v047:197` re-states the grammar; the accepted position is that per-application TOFU plus `verify_invitation_origin` against the *current* profile bounds the practical attack without binding operatorship. `v048` does not disturb it, and the ratified production profile carries exactly one relay descriptor with all three fields (`cbcl-chat-selfsame-application-gate.lfe:37-42`). | `src-tauri/src/cbcl_pairing.rs:295-305` calls `cbcl_relay::verify_invitation_origin` against the fetched profile with an explicit `RelayPolicy`; `cbcl_context.rs:275-277` re-checks against the *current* profile, which is the `M-1` fix the comment records. | **No regression; the mitigation remains coherent and remains a mitigation.** |

---

## Reachability audit

**Question.** Is `REQ-970`'s Path-A branch reachable without circularly requiring
an account DID or an open `GATE-01`?

**Entry: yes, and this is a real correction.** For a handle `H` with a
`cbcl-member` row and no `cbcl-account-binding` row,
`cbcl-chat-account-grants:account-binding/1` (`:69-74`) returns
`#(error key-bound)`, `enrol-facts:311-316` produces `handle-facts H #(key K)`,
`cbcl-core-enrol:resolve` takes the `key-bound` branch, and the frame is
authorised. No gate is consulted on that path. A brand-new handle is even
simpler: `enrol-facts:317-319` yields `'unbound`, `tofu:157-162` accepts
`#(key K)`, and `enrol-commit:459-462` writes the member row — so the ceremony's
own `selfsamepairallocate` frame creates the `cbcl-member` row that `REQ-040`
requires, with a zero-row `MigrationSnapshot`. The eligible class is therefore
non-empty and constructible, which is precisely what 0.4.7's `H-1` said did not
exist. The amendment earns this.

**Exit: no.** Three independent blocks, in order of severity:

1. `C-1` — the granted device key is made inert by the pairaccount row the
   transaction itself writes, on every path, in every build.
2. `H-3` — even without `C-1`, `account-facts:349-358` meets the shut Path-B
   constant, so the reconnect requires opening `GATE-01`'s enablement point in
   the fixture build, which no amendment names.
3. Production enablement additionally requires `GATE-01` closed, which
   `bus0161:81-85` and `v048:122-126` now state explicitly and honestly. That is
   a declared dependency, not a defect.

**Mnesia tables, row shapes, keys and writers touched by the reachable path**
(all verified at `fdbe414`):

| Table | Record | Primary key | Writers |
|---|---|---|---|
| `cbcl-member` | `handle pubkey enrolled` | `handle` | `cbcl-chat-members:enroll-or-check/2` only (`:42-54`), reached from `enrol-commit:459` |
| `cbcl-account-binding` | `handle account bound` | `handle` | `bind-account/2` (`:81-98`), `bind-and-store-unbound` (`:177-187`), `confirmed-migrate-path-a-handle/3` (`:118-141`). No `mnesia:delete` targets it anywhere |
| `cbcl-roommember` | `key room handle pubkey granted via since` | `u32be(len(room))‖room‖handle` (`:259-262`) | `write-grant` (`:153-180`); deleted by `revoke/2` (`:242-255`) and by `validate-and-migrate-legacy` (`:168-170`) |
| `cbcl-account-roommember` | `key room account handle granted via since` — **no `pubkey`** | `u32be(len(room))‖room‖u32be(len(account))‖account‖handle` (`:234-239`) | `write-grant`, `migrate-legacy-handle-tx:152-164`; index on `handle` |
| `cbcl-device-grant` | `key account pubkey vc credential-id valid-until permissions stored` | `device-key(account, pubkey)` | `store-device-tx:189-202`; **no index** |
| `cbcl-pairaccount-grant` | `key handle devkey granted state` | tuple `#(Handle DeviceKey)` | `claim-tx:52-68` only, always `state = 'unratified`; indexes on `handle` and `devkey` |
| `cbcl-webfinger-account` | `account alias home-did` | **`account`** | `create-or-return-live:151-171`, reached only from `provision/3`, which has no production caller |
| `cbcl-successor-pointer` | `predecessor account handle sequence succ succfrom stored` | `predecessor` | `accept/3`; read by `spent?/1` via `dirty_read`, outside any transaction |

`CON-959`'s and `CON-012`'s statements about these are correct except for `L-1`
(the device count "index") and `L-2` (`cbcl-webfinger-account`'s key in the
adopted `v044` table).

---

## Display and authentication audit

**Claim under test (mandate item 3).** The application ID, origin, relay and
every displayed migration value are live-`CON-220`-authenticated consumer values
before display, never peer/caller/browser strings.

**In the design: true, with one broken link.** Every migration member —
`legacyHandle`, `legacyKeyDigest`, `migrationRooms`, `roomSetDigest`,
`migrationSnapshotDigest`, `snapshotNonce` — is a member of `OfferCoreV2`
(`CON-960`, `v048:277-291`) and therefore of `offerCoreDigest`, of the
domain-separated `intentDigest` (`v045:249-252`), and of the `CON-214` v2
evidence (`v044:1023-1030`). `CON-936` constructs `AuthenticatedIntent` only from
typed fields in the verified core and evidence, and `CON-943` forbids display from
a peer-provided application ID. The hub's construction is server-side
(`v044:964-966`) and `CON-939`'s three commands never accept a caller-supplied
digest or scope. `pair043`'s `REQ-025` keeps the shared endpoint out of room
parsing and migration policy, which is the right ownership split.

The broken link is `H-4`: the chain terminates at a signature by the profile's
declared enrolment key, and at the reviewed baseline that key signs whatever is
POSTed to `/selfsame/enrolment/sign`. Until a `cbcl-bus` clause owns that route,
"application-authenticated" reduces to "signed by an endpoint that authenticates
nobody", and the answer to `F-B` is defeated one layer below where `F-B` looked.

**Two secondary gaps.** `M-1`: the displayed room list is not required to be
checked against the digest that governs the migration, so an honest-but-buggy hub
can migrate a channel the person never saw while passing every stated check.
`M-3`: there is no typed carrier from `AuthenticatedIntent` to the person, and
the only existing one is prohibited by name.

**Ordering, re-verified in cbcl-pairing at `8e55160`.** The endpoint's
`begin -> offer -> intent-approve | intent-decline` (0.4.0 `CON-015`) means the
claimant displays on receipt of `offer`, and the allocator has no protocol-level
refusal available between `offer` and the decision. `v048:98-101`'s
allocator-withholds-`offer` rule is therefore the only implementable form of the
pre-display authority gate, and it is stated correctly.

---

## Consent and effect-order audit

`CON-960`'s thirteen-step order (`v048:288-302`) is internally consistent and I
found no effect that can occur before the consent edge authorising it, with these
observations:

- **Pre-consent hub effects are real but correctly classified.** `REQ-970` allows
  a snapshot, a pending `applicationAccountId`, a pending `accountScopeId` and one
  bounded pending ceremony before the carrier is displayed. None is a binding,
  grant, alias, membership or successor effect, and `CON-960:305-307` says so
  explicitly. `SPEC-007`'s `REQ-812` ("Mismatch, protocol violation,
  cancellation, relay closure, and expiry SHALL produce no accepted credential or
  identity side effect before approved payload delivery") is not breached: a
  pending hub row is neither an accepted credential nor a wallet identity effect,
  and `v048:88-90` requires decline/expiry/pre-commit failure to delete them.
- **The retry-derives-a-different-DID rule is right and is stated.**
  `v048:90-92`: "a retry may derive a different preview DID and must display it
  again." That closes the trap where a person compares a fingerprint from a
  previous attempt.
- **The one-transaction placement is right.** Resolver verification, grant
  verification and browser strict-durability installation all precede the hub
  commit (`v048:107-112`, `bus0161:70-74`), so a hub that commits cannot be ahead
  of a browser that failed to install.
- **The irreversibility declaration is honest and correctly bounded.**
  `CON-959:352-357` and `CON-012:186-192` both say the commit is the acceptance
  point, that `outcome-unknown` is retained by browser and wallet until the
  immutable status is recovered, and that a later unlink is a new person-confirmed
  protocol rather than compensation. That is the right shape — and it is exactly
  what makes `C-1` unrecoverable.
- **One unstated post-consent abort.** `SPEC-076`'s `REQ-005` two-unratified cap
  is enforced inside `claim-tx` by `cbcl-core-pairaccount:decide-claim`, i.e.
  inside the final transaction, after DID publication and grant issuance.
  `CON-012` and `CON-959` treat pre-commit failure as compensable, so this is
  survivable, but a cap that can only be discovered at the last step should be
  checked at allocation and refused before the carrier — the same treatment
  `NFR-008` gives `MigrationPlanTooLarge`.

---

## Digest audit (`CON-012` / `REQ-972`), byte by byte

| Element | Rust | JavaScript | LFE/Erlang | Verdict |
|---|---|---|---|---|
| `legacyKeyDigest` domain + raw 32-octet key | fine | fine | fine | **Implementable.** Domain literal, no length prefix, no separator beyond the NUL. |
| `roomSetCbor` — deterministic CBOR array of unique room byte strings, unsigned-octet lexicographic | fine | fine | fine | **Implementable.** "Unique" and "complete" are both stated; `lists:usort` on binaries is unsigned-octet order, matching. |
| `roomSetDigest` | fine | fine | fine | **Implementable**, but see `M-1` — nobody is required to recompute it from the display. |
| snapshot row tuple `["cbcl-roommember", key, room, handle, pubkey, granted, via, since]` | fine | fine | fine | **Implementable.** Sort by raw `key` under unsigned-octet order is well defined given the pinned key construction. `granted` is `erlang:system_time(millisecond)`, always a non-negative integer. `since` is an integer or `'undefined` → CBOR null. `via` is exhaustively `'standing`/`'invite` at the baseline. `pubkey` may in principle be `'undefined` for a pre-verification legacy row, which `bus0161:139` maps to `Unknown` — fail-closed, and correct. |
| `["cbcl-member", handle, enrolledPublicKey]` | fine | fine | fine | **Implementable**; `L-4` on the "field vectors" wording. |
| `migrationSnapshotDigest` | fine | fine | fine | **Implementable, hub-only.** No other party has the inputs; `M-1`. |
| `migrationConfirmationDigest` | — | — | — | **Not implementable.** `fingerprintCanonicalBytes` is undefined (`H-2`) and requires BLAKE3 in a runtime that has none. `deviceKeyDigest` is uncited but derivable from `SPEC-004:3617`. |
| Base64url-vs-raw discipline | | | | **Correct and stated once**: `bus0161:129` — base64url-no-pad in JSON, raw octets in concatenated preimages; `v048:172-173` repeats it. |
| Self-reference | | | | **None.** `migrationConfirmationDigest` lives in the reverse bundle, not in `OfferCoreV2`, so it does not cover itself. The `CON-214` exclusion rule at `SPEC-004:3651-3656` is respected. |
| Length caps | | | | 256 entries / 48,000 octets / 60,000 core / 62,000 logical; `L-5` on the residual arithmetic. |
| Duplicate handling | | | | Stated for rooms ("complete unique"); **not stated** for the snapshot row array, though the primary key makes duplicates impossible. |
| Signature coverage | | | | The confirmation is "a signed consumer claim inside the reverse bundle" (`v048:189-190`) and adds no shared performative — which `pair043:43-46` confirms from the owning side. Correct. |

---

## Transaction, race and recovery audit

**Lock order.** `CON-012:160-168` and `CON-959:342-346` declare: stable
account/pending → handle binding → `cbcl-member` → whole `cbcl-roommember` →
whole `cbcl-account-roommember` → device/pairaccount indexes and exact keys →
alias/account/tombstone → finalization/compensation. I checked it against every
existing writer:

- `confirmed-migrate-path-a-handle/3` takes binding (`:124`) → member
  (`:126`, `pubkey-tx`'s write lock) → `cbcl-roommember` table (`:139`) →
  `cbcl-account-roommember` table (`:140`) → binding write (`:130`). **Same
  direction**; no cycle.
- `cbcl-chat-roommember:revoke/2` takes a `cbcl-roommember` record lock then
  reads `cbcl-account-roommember` through its `handle` index. **Same direction**;
  a record-vs-table conflict is a wait, resolved by Mnesia's own abort, and
  `CON-959:346-348` correctly treats an abort as a closed refusal or idempotent
  retry rather than permission to skip a check.
- `bind-and-store-verified-device-grant/8` takes binding (`:164`) → device-grant
  record (`:190`) → whole `cbcl-device-grant` via `foldl` (`L-1`). **Same
  direction.**
- `cbcl-chat-pairaccount:claim-tx` takes an exact-key write lock then an
  `index_read`. Independent table.

The declared order is feasible and linearizable. The snapshot itself must be a
whole-table scan — `cbcl-roommember` has no `handle` index, so
`migrate-legacy-handle-tx:134-148` folds the entire table — which `CON-959`'s
"whole-table read locks over both membership tables" correctly anticipates but
whose cost (every join and evict hub-wide blocks for the duration, on a
single-machine deployment with `strategy = 'immediate'`) is not stated anywhere.

**Room metadata preservation.** `CON-012` step 3 requires `granted`, `via` and
`since` to be preserved, which matches `validate-and-migrate-legacy:151-164`
exactly, and correctly omits `pubkey` because the target record has no such
field. `since` semantics survive (`cbcl-chat-roommember.lfe:22-27` explains why
the floor must not be refreshed).

**TOCTOU and mutation.** `MigrationSnapshotChanged` at finalization, with the
complete legacy state preserved and the person restarting to see the new set
(`NFR-008:94-98`, `TEST-1086`, `TEST-044`), is the right answer and is stated for
add, delete, re-key and metadata mutation. `bus0161:170` step 1's "re-reads and
recomputes the complete snapshot and all confirmation inputs" is the mechanism
that makes it work.

**Concurrency.** One pending migration per `(applicationId, handle, enrolledKey)`
(`NFR-008:94-95`), and `v048:92-94` requires a concurrent first-link allocation to
refuse or return the byte-identical active ceremony. Sound.

**Recovery.** The immutable acknowledgement/status row plus new-key recovery
(`CON-959:353-357`, `TEST-1087`, `TEST-046`) is the right design and is exactly
what `C-1` renders unreachable: the recovery channel is a frame under the new
device key.

**Old-key compromise.** `DEC-012`'s takeover trace is correctly retained as a
live `GATE-04` human-review input (`bus0161:214-217`, `v048:157-158`). I confirm
it is live and is sharpened by this design: a compromised Path-A key can drive the
whole ceremony to an irreversible commit that binds the victim's handle and rooms
to the attacker's account, with no protocol reversal. The design's own
irreversibility clause is what makes this severe, and routing it to human review
rather than claiming a mitigation is the honest call.

**Browser/wallet/hub disagreement.** `Unknown`, changed pending state, gate
refusal and a different reciprocal DID all close the ceremony with no `offer`
(`v048:96-101`). Wallet-side, `CON-937`'s terminal grammar closes and zeroizes on
conflicting duplicates. The one disagreement not covered is `M-1`'s: display
versus digest, where nobody is required to compare.

---

## Durability audit

`NFR-910`'s manifest is the right shape and its scope disclaimer
(`v048:229-232`) is the correction 0.4.7's `M-2` asked for — it no longer says
"covers every" over a partial list. Both fail-open consequences are correct in
code, as recorded above. `did-crdt`'s `durable-ready` is a real signal:
`src/service/handlers.rs:379-410` at `1f409a4` serves `GET /health` reporting
`"durable"` when snapshots are written to `STORAGE_PATH` and `"ephemeral"`
otherwise, and `src/service/server.rs:147` routes it — so
`v048:236-238`'s "did-crdt `durable-ready` continuously true" is checkable.

**Two authority/continuity states are still outside the manifest:**

1. **`cbcl-invite`.** `cbcl-chat-roommember:redeem-and-grant/5` (`:137-146`)
   consumes an invite use and writes the grant in one transaction, so invite
   *spend* state is admission authority. An empty `cbcl-invite` after restore
   makes every previously-spent invitation unspendable rather than reusable —
   fail-closed, so not a security consequence, but it is admission state the
   migration reads through and the gate does not name. One line, either way.
2. **`cbcl-provenance` and `cbcl-epoch-claim`** are named as delete targets in the
   0.4.7 review's writer enumeration and are neither in `NFR-910`'s manifest nor
   in its explicit exclusion list (`v048:229-232` excludes "chat content, MLS
   state, invitations, or unrelated Mnesia tables"). "Invitations" arguably covers
   `cbcl-invite`; the other two are unclassified. `NFR-910`'s own rule — "Adding a
   table read by credential/v2 or per-frame identity without adding it to this
   manifest makes the gate false" — is a good rule and would be easier to apply if
   the exclusion list were an enumeration rather than a category.

Neither is a finding at the severity bar; both are one line each.

---

## Physical-gate audit (`GATE-00` applied literally)

`SPEC-053:5232-5310`, clause by clause, against `REQ-971` + `bus0161:243-259`:

| Clause | Requirement | Can the plan meet it? |
|---|---|---|
| **1 — one enablement point, constant in source** | Exactly one function, unconditional closed result, reads no env/db/request/header/build flag/deploy variable | **Yes for the HTTP half, as specified.** `bus0161:244-246` and `v048:131-135` are the right shape and match `cbcl-chat-webfinger-gate.lfe:1-13`'s precedent. Both correctly demote the allocator throttle to "an additional control" (`v048:142-146`), which is 0.4.7 `H-3` item 3 verbatim. **Complicated by `H-3`**: the compiled-open build introduces a second source constant for the same capability, and neither document says how "exactly one function" survives two builds. |
| **2 — consulted first, before any write, allocation, resolver call or TOFU enrolment** | "'First' is literal" | **No, for the WebSocket half.** `H-1`. `enrol-commit` writes `cbcl-member` inside `authorise-frame` before `route` exists. The repository's own conforming shape — `dispatch:217-223`'s pre-authorisation special-case for `grant`/`grantproof`, added for exactly this reason — is not named by either amendment. |
| **3 — prohibited-action test per surface, opaque refusal** | Must not distinguish which downstream condition would have failed | **Not as specified.** `H-1`: in production the refusal is `malformed-cbcl` from the parser, in the fixture build it is the gate — two different observables for the same request, and neither is a refusal from a gated surface with zero writes. |
| **4 — no authority-producing path reachable from the network** | "A route that can allocate an invitation, start a ceremony, accept a grant, enrol an account, or change standing is shipped, whatever it later answers" | **Partially.** The HTTP half can be held out of `cbcl-chat-app:dispatch/0` exactly as `:216-227` already does for two gates. The WebSocket half rides `/chat/v1`, which is shipped unconditionally at `:208`, so absence has to be achieved in the dialect — and the unrouted fall-through is `do-publish`, not a refusal (`H-1`). Note `DEC-014` (`SPEC-053:5282-5296`) does **not** help here: its exception is the blind relay on a distinct origin, and it explicitly does not admit "an inert account endpoint, or a configurable account gate". |
| **5 — closure asserted by a test** | So opening cannot arrive inside a change that reads as a refactor | **Unresolved.** `H-3`. `cbcl-chat-gate-closed-tests` is red by construction in the compiled-open build, and neither amendment says which build runs which assertion. |

**Release-manifest and binary evidence.** `v048:150-152` and `bus0161:250-252`
require relx manifests, release build scripts, binary scans and the closed-gate
suite to prove the fixture, the open constant, the authority routes, the
selectors and the conformance-allocation capability absent from the production
artefact. That is the right evidence class and has precedent: `SPEC-075`'s relay
exception already turns on "a source test proves the supervised release has no
branch that passes `--enable-conformance-allocation`" (`SPEC-053:5290-5292`). It
is achievable. It does not, by itself, resolve clauses 2, 3 or 5.

**Verdict on mandate item 10.** A configurable/default-off route is correctly
rejected by both documents, and neither proposes one. But the constant-in-source
closed gate, the first-in-handler checks, the physical absence of production
WS selectors, the release-manifest tests and the separately compiled
non-production positive fixture **cannot all coexist as written**: the
first-in-handler check is impossible on the frame path (`H-1`), physical absence
of the WS verb produces a fan-out rather than an opaque refusal (`H-1`), and the
positive fixture needs a second gate open in a build where clause 5's assertion
must fail (`H-3`).

---

## cbcl-pairing `SPEC-001` 0.4.3 — does it pass its narrower owning-spec amendment?

**On the `TEST-050` replacement: yes, cleanly, and it is the right instrument.**

- `NFR-017` states the ownership rule explicitly — "A consumer amendment cannot
  retire a cbcl-pairing test; this document performs that replacement through the
  `SPEC-001` amendment channel" — which is precisely the authority defect 0.4.7's
  `L-3` identified.
- `pair043:8` (`amends:`) names
  `spec-001-0.4.1…#TEST-050` and `spec-001-0.4.2…#TEST-051` directly, so the
  amendment reaches the row it needs to reach.
- `CON-020:71-74` performs the replacement in the required words: the
  control-object portion of `TEST-050` "is replaced, **not supplemented**, by
  `TEST-051` as corrected by `TEST-052`", `TEST-050`'s large-arm, outer-frame,
  manifest, cross-version and unchanged-hash assertions remain mandatory, and the
  4,096 positive witness "is retired and is a required **negative** witness".
- `TEST-052:96-100` runs 1, 23, 24, 255, 256, 2,047, 2,048 positive and 0, 2,049,
  4,095, 4,096 negative, and adds the meta-assertion "that no effective positive
  row still names 4,096".
- Nothing numeric moves. I recomputed the control class from `CON-019` —
  1 (map head) + 2 (`0:2`) + 2 (`1:kind`) + 35 (`2:` 32-octet bstr) + (2|3|4) +
  4,100 (`4:` 4,096-octet bstr) = **4,142 / 4,143 / 4,144** — reproducing
  `spec-001-0.4.2` exactly, and the large arm (`3: 1..62000`, `4: bstr .size
  64512`) is untouched, so every derived plaintext, ciphertext, relay and queue
  figure in the Selfsame set is unaffected. `v048:376` correctly updates the
  mandatory companion range to "TEST-030 through TEST-052".
- Artefact numbers are sequential above 0.4.2's `REQ-024`/`NFR-016`/`CON-019`/
  `ADR-017`/`TEST-051`.

**Does it accidentally widen shared protocol authority? No — and it says so.**
`REQ-025:47-49` forbids a new shared performative, kind number, sender rule,
state transition, frame, fixed allocation or relay-visible field; `:43-46`
confirms the transition is already bound by `offerCoreDigest` and the one
retained `intentDigest`, and that the migration-confirmation digest's "grammar and
authority belong to the Selfsame consumer specification". `ADR-018` states the
split correctly: room parsing and migration policy stay in the consumer. I
checked the shared reducer at `8e55160` and nothing in `REQ-025` requires it to
learn application semantics.

**Can the typed display object be built and consumed without a second untrusted
display channel? No — `M-3`.** That is the one finding against this amendment, and
it is a MEDIUM rather than a HIGH because the repair is a bounded, well-understood
type change entirely inside the crate `pair043` already has authority over.

**Verdict: `pair043` passes on its own terms with one MEDIUM.** It does not pass
into implementation, because the coordinated candidate it serves does not.

---

## Amendment-authority, anchor and observability audit

- **Anchors.** `v048:8`'s `amends:` uses fully qualified `[[Spec#Anchor]]` form
  throughout and now includes `REQ-807`; `v048:9`'s `conforms-to:` names
  `GATE-00`, `GATE-01`, `GATE-03` and `GATE-04`. `REQ-971` cites
  `spec-053-0.16.1-credential-v2-confirmed-migration#GATE-04` correctly — `GATE-04`
  exists only in the amendment, and citing it against the main document would have
  been wrong. `bus0161:8` and `pair043:8` are likewise fully qualified. **No
  dangling anchor found.**
- **Sequential numbers.** Selfsame: `REQ-970/971/972`, `NFR-910`, `CON-959/960/961`,
  `ADR-941/942`, `TEST-1083`–`TEST-1091` all follow 0.4.7's ceilings with no gap
  and no collision. cbcl-bus and cbcl-pairing verified above. **Clean.**
- **Supersession and effective text.** `v048:6-7` supersedes 0.4.7 and adopts
  0.4.4–0.4.7 "except the exact replacements and corrections named by this
  amendment", and `CON-961` is the named ledger — the discipline 0.4.7's `L-2`
  asked for, now applied consistently. Nine of ten items are exact; item 3 is
  incomplete (`L-2` above).
- **Ownership boundaries.** Correct in three of four places: the hub half is
  specified in `cbcl-bus`, the `TEST-050` retirement in `cbcl-pairing`, and
  `v048:329-330` explicitly disclaims Selfsame authority over it. The exception is
  `CON-960`'s prohibition on `DisplayField` (`M-3`) and `TEST-1027`'s assertion
  about a `cbcl-bus` route (`H-4`).
- **Test observability.** Most rows can prove their requirement. Four cannot as
  written: `TEST-1085` (undefined digest member, `H-2`), `TEST-1088` and
  `bus0161`'s `TEST-047` (assert zero writes for a path that writes, `H-1`),
  `TEST-1083` and `bus0161`'s `TEST-045` (require a reconnect that `C-1` forbids
  and a gate that `H-3` does not open), and `TEST-1089` (no relay-side assertion,
  `L-7`). A test promise that cannot prove its requirement is a defect, and these
  four are counted inside the findings above rather than separately.

---

## What would make this pass

The architecture does not need to change. Seven repairs, all bounded:

1. **`C-1`** — decide and state what `cbcl-pairaccount-grant` means for a
   credential/v2 device: remove the write, or amend `SPEC-076`'s `REQ-004`
   through its own channel to define ratification and make the confirmed
   migration the ratifying event. Add a post-commit assertion that the granted
   key passes `cbcl-core-enrol:resolve`. **This is the blocking one.**
2. **`H-1`** — put the credential/v2 gate in `dispatch`, before
   `verify-and-route`, citing the `grant`/`grantproof` precedent; keep the clause
   in the production build so the refusal is opaque and writes nothing; correct
   the two zero-write test rows.
3. **`H-2`** — define `fingerprintCanonicalBytes` (function, hash, domain,
   truncation, rendering) and either accept the BLAKE3 dependency explicitly or
   drop the hub-side re-render; cite `SPEC-004:3658-3659` for `deviceKeyDigest`.
4. **`H-3`** — name `cbcl-chat-path-b-gate:profile/0` as the second constant the
   fixture opens; say how `cbcl-chat-gate-closed-tests` runs in both builds; forbid
   any credential/v2 bypass of `path-b-profile` by name.
5. **`H-4`** — add a `cbcl-bus` clause owning `/selfsame/enrolment/sign` before any
   v2 `CON-214` grammar lands, and put it in `GATE-04`.
6. **`M-1`–`M-3`** — require the wallet to recompute `roomSetDigest` from the
   displayed rooms and state the honest recompute/carry split; extend
   `cbcl-core-dialect:hub-originated?`; specify the shared `AccountTransition`
   type and the non-constructible `DisplayField` in `pair043`'s `CON-020`.
7. **`L-1`–`L-7`** — seven single-line corrections, each with its repair stated
   above.

---

## Final gate decision

**Design review: REJECT.** One CRITICAL and four HIGH findings. Under the review
mandate, PASS is permitted only if no critical or high defect remains, and
`C-1` alone forecloses it: the accepted, explicitly irreversible path terminates
in a state where the person cannot reach any of their channels under either key,
and the protocol as specified provides no recovery.

**Implementation authority: not granted.** `v048:459-461` and `bus0161:16-18`
correctly withhold it pending PASS, and that hold stands. No source, schema,
fixture, vector, allocation, release or deployment change is authorized by this
review. The coordinated repository slices remain design-only.

**Production enablement authority: separate, and not addressed by this review
even in the counterfactual.** A PASS on a corrected 0.4.9 would authorize an
Elephant SPL/test-first plan and nothing else. Production credential/v2 authority
additionally requires, and this review closes none of them:

- `cbcl-bus` `GATE-01` and `GATE-03` closed, including every inherited upstream
  and human-security box they retain;
- `cbcl-bus` `GATE-04`'s seven boxes, of which the human security review of
  `DEC-012`'s compromised-Path-A-key takeover trace and the consent copy is the
  one this review most strongly recommends not be deferred, because the
  irreversibility clause makes that threat's consequence permanent;
- `hub-identity-authority-off-machine-ready` and did-crdt `durable-ready`;
- independent CPace evidence, cryptography and migration review, privacy and
  rollback review;
- and the owner's separate confirmation of the single-machine `immediate`
  deployment after inspecting the rollback target and relay drain.

I treat `GATE-01`, `GATE-03` and `GATE-04` as open throughout, as instructed, and
nothing in this review should be read as evidence toward any of their boxes.

**One note to the owner, outside the finding set.** The direction is right and
0.4.8 is the strongest amendment in this lineage: `ADR-941`'s observation — that
the account scope must exist before the DID, so the pairing ceremony's final
screen is the only place a `REQ-033` consent can be exact — is the insight the
previous four amendments were circling, and both repositories state it in their
own words rather than one asserting it about the other. The remaining defects are
all of one kind: statements about `cbcl-bus`'s running frame path, gate machinery
and device-grant tables that were written from the specification and not checked
against the module. A pass over `cbcl-chat-session-ws.lfe`'s `dispatch` →
`authorise-frame` → `route` chain and `cbcl-chat-pairaccount.lfe` with the
amendment in hand would have caught `C-1` and `H-1` before this review did.
