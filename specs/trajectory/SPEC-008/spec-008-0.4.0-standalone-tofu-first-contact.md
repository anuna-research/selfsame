---
id: SPEC-008-amendment-0.4.0
title: SPEC-008 0.4.0-draft — Standalone TOFU first-contact pairing
status: draft
version: 0.4.0-draft
tier: 1
review-gate: not-approved
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2)"
depends-on: "[[SPEC-008-production-pairing-claimant]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; cbcl-pairing SPEC-001 (WIRE AMENDMENT REQUIRED — see [[#ADR-905]])"
supersedes-rejection: "[[req-909-adversarial-review-2026-08-22]]"
last-updated: 2026-08-22
---

# SPEC-008 0.4.0-draft — Standalone TOFU first-contact pairing

> **This is a proposal for review, not ratified law.** It reverses
> [[SPEC-008-production-pairing-claimant#ADR-902]] and reopens the first-contact
> path that [[req-909-adversarial-review-2026-08-22]] rejected. It MUST pass a
> fresh cross-model adversarial review (Tier 1) and MUST NOT be implemented until
> the cbcl-pairing wire amendment it depends on ([[#ADR-905]]) is itself reviewed.
> Owner direction (2026-08-22): drop the allowlist; the person is notified on a
> new relay and accepts (adds to their own trust policy) or rejects (denies).

## Orientation

**Intent.** Let a wallet link with an application it has *never met* — no prior
enrolment, no held pairing-trust record, no compiled relay allowlist — while
keeping the two properties that matter: the person pairs with the **application
they think they are** (not an imposter), and connects only to a **relay they
have consented to**. Trust in the relay is the person's own, granted on first
use (TOFU) and remembered.

**What changes from 0.2.2.** Today the origin gate ([[SPEC-008-production-pairing-claimant#REQ-906]])
demands a *held authenticated profile* — written only by a completed
[[SPEC-004-application-scoped-identity]] enrolment — that pre-declares the relay,
and a *compiled digest registry* ([[SPEC-008-production-pairing-claimant#CON-903]]).
This amendment removes both. The trust anchor moves to **two live facts plus one
human decision**:

1. the invitation names a real `applicationId`, and the wallet **fetches and
   authenticates that application's profile from its own origin**
   ([[SPEC-004-application-scoped-identity#CON-220]]) — an anchor an attacker
   cannot forge without controlling that origin;
2. the authenticated profile **itself declares** the invitation's relay origin;
3. the person is shown the **authenticated application origin and the relay**, and
   — when the relay is new to them — **accepts or rejects** it. Accept records the
   relay in the person's own trust policy; every later invitation to that relay is
   admitted without a prompt.

**Metaphor.** 0.2.2 opened the door only toward relays a passport the person
already held had stamped. This amendment lets the person read the passport *live*
at the door (fetched from the issuer, not the courier who handed them the
invitation) and decide, once, whether to trust the road it points down.

**Structure.**

```
 invitation (untrusted)                    the person's wallet
┌───────────────────────┐  applicationId ┌────────────────────────────┐
│ applicationId  (NEW,   │───────────────▶│ [[#REQ-911]] fetch+auth the │
│   real CON-201 id)     │                │   profile from ITS OWN      │──▶ CON-220
│ relayOrigin            │   relayOrigin  │   origin (not the carrier)  │
│ credential-app const   │───────┐        └─────────────┬──────────────┘
└───────────────────────┘        │                      │ authenticated profile
                                 │        ┌─────────────▼──────────────┐
                                 └───────▶│ [[#REQ-910]] profile must    │
                                          │   declare this relayOrigin  │
                                          └─────────────┬──────────────┘
                                                        │
                                          ┌─────────────▼──────────────┐
                                          │ [[#REQ-912]] relay in the   │
                                          │  person's trust policy?     │
                                          │   yes → proceed             │
                                          │   no  → PROMPT accept/reject│──▶ [[#CON-905]] policy store
                                          └─────────────┬──────────────┘
                                                        │ accept (relay recorded)
                                          ┌─────────────▼──────────────┐
                                          │ [[#REQ-913]] ONLY NOW: open  │
                                          │  socket, CPace, consent,     │
                                          │  mint scope, derive identity │──▶ no side effect before here
                                          └────────────────────────────┘
      arrows point inward: nothing is trusted for being in the invitation;
      it is trusted for authenticating live and for the person accepting it.
```

**Decisions.** [[#ADR-904]] origin trust anchored in a *live-authenticated* profile
plus a *person-owned* TOFU policy, replacing the held record and the compiled
allowlist · [[#ADR-905]] the cbcl-pairing invitation must carry a real
`applicationId` (a wire amendment, reviewed for its own phishing surface).

**Load-bearing.** [[#REQ-910]] standalone admission · [[#REQ-911]] live application
authentication · [[#REQ-912]] person-owned relay trust policy · [[#REQ-913]]
post-consent ordering.

**Controls.**
- [[#REQ-911]] the profile SHALL be authenticated from the `applicationId`'s own
  origin (CON-220), NEVER from the profile the invitation carries or any cache the
  person never accepted.
- [[#REQ-912]] no socket SHALL open toward a relay origin the person's trust policy
  does not contain, until the person accepts it in this ceremony.
- [[#REQ-913]] NO identity side effect — no closure publication, no CON-204 alias
  provisioning, no account-scope mint, no key derivation — SHALL occur before the
  person approves the exact intent ([[SPEC-007-cbcl-pairing-cutover#REQ-812]],
  inherited, unwaivable).
- Retained: [[SPEC-008-production-pairing-claimant#REQ-907]] (the production-allocation
  hold is untouched), [[SPEC-008-production-pairing-claimant#REQ-908]] (loopback stays
  dev-only).

## How this answers the 2026-08-22 REJECT

Every blocking finding of [[req-909-adversarial-review-2026-08-22]] is named here
with the mechanism that resolves it. A reviewer should check each row against the
artefacts, not this table's prose.

| Prior finding | Why REQ-909 failed | How 0.4.0 resolves it |
|---|---|---|
| **F-1** (no wire source) | the invitation's `application` is the constant `anuna.io/credential/v1`, never an `applicationId`; live-fetch had nothing to fetch | [[#ADR-905]] + [[#CON-904]]: the invitation carries a **real** `applicationId` as a new, separately-recognised field — a **cbcl-pairing wire amendment** that is a hard dependency, not a wallet-only change. Absent that field, the wallet refuses; there is no fallback to the constant. |
| **F-2** (pre-consent side effects) | closure publication + alias provisioning happened before decline was possible | [[#REQ-913]]: strict ordering — authentication and relay-consent are **read-only**; scope mint, key derivation, and any publication happen **only after** exact-intent approval. A declined ceremony leaves the store byte-for-byte unchanged. TEST-920/921 assert this over a decline. |
| **F-3 / F-4** (relay digest ≠ application anchor; amplification) | the registry certified the relay operator, so any origin could serve a profile naming it; consent text was the only app barrier | [[#REQ-910]]/[[#REQ-911]]: the anchor is no longer a digest — it is **CON-220 origin-binding**. The profile is fetched from the `applicationId`'s *own* origin, so an attacker cannot present chat.anuna.io's identity without controlling chat.anuna.io. The person consents over the **authenticated origin**, shown as the compared value. Amplification is bounded by [[#REQ-913]] (no scope/closure minted pre-consent) and [[#CON-906]] (one live authentication per accepted ceremony). |
| **F-3 residual** (punycode lookalikes pass CON-201's ASCII rule) | — | [[#CON-906]] requires the consent surface to render the authenticated origin under the untrusted-value treatment and to display its Unicode/punycode form explicitly; carried as [[#OBS-904]] and an [[#open-questions]] item, because display hardening is not fully closed by this spec. |
| **F-5 / F-6** (step-order and hard-stop contradictions) | REQ-909's order contradicted ADR-914 and silently relaxed REQ-906 | [[#REQ-913]] states the order as one normative sequence and reaffirms the inherited hard-stop list explicitly; [[#ADR-904]] states the REQ-906 reversal openly rather than silently. |

## Requirements

### REQ-910 — Standalone first-contact admission

The claimant SHALL admit an invitation from an application the wallet holds no
prior record of, **iff** all hold, each before any network I/O toward the relay:
(a) the invitation carries a syntactically valid `applicationId` ([[#CON-904]]);
(b) the profile authenticated live from that `applicationId` ([[#REQ-911]]) declares
the invitation's `relayOrigin` among its `cbclPairingRelays`; and (c) the relay is
admitted by the person's trust policy ([[#REQ-912]]). Any failure SHALL refuse
before I/O, as one distinct secret-free message, with no repair path
(inherited: [[SPEC-007-cbcl-pairing-cutover#REQ-813]]). This requirement REPLACES
[[SPEC-008-production-pairing-claimant#REQ-906]] for first contact; a held record,
where one exists, remains a valid admission path (it already proves both facts).

Trace: [[#CON-904]] [[#REQ-911]] [[#REQ-912]] [[#TEST-918]] [[#TEST-919]]

### REQ-911 — Live application authentication is the anchor

The wallet SHALL obtain the application profile by fetching it from the
`applicationId`'s own origin and authenticating it under
[[SPEC-004-application-scoped-identity#CON-220]] — the same live, origin-bound
authentication the SPEC-004 enrolment path uses. It SHALL NOT trust the profile the
invitation carries, a profile from any other origin, or a cached profile the person
has not accepted. A fetch or authentication failure SHALL refuse
(`PairingProfileUnavailable`) before any relay I/O. The authenticated profile — not
the invitation — is the sole source of every downstream field
([[SPEC-008-production-pairing-claimant#CON-902]] applies unchanged, with its
`profile` and `account` sourced here).

Trace: [[SPEC-004-application-scoped-identity#CON-220]] [[#REQ-910]] [[#TEST-918]] [[#TEST-922]]

### REQ-912 — Person-owned relay trust policy (TOFU)

Relay-origin trust SHALL be a persistent, person-owned policy: a set of relay
origins the person has explicitly accepted ([[#CON-905]]). On an invitation whose
`relayOrigin` is **in** the policy, the ceremony proceeds without a prompt. On one
**not** in the policy, the wallet SHALL present the relay for a decision
([[#CON-906]]) and SHALL open no socket toward it until the person **accepts**;
accept records the origin in the policy, reject ends the attempt with no side effect
and no repair path. There SHALL be no compiled allowlist and no vendor-supplied
default entries: the policy begins empty and is built only by the person's accepts.
The person SHALL be able to review and remove entries ([[#CON-905]] operations).

Trace: [[#CON-905]] [[#CON-906]] [[#REQ-913]] [[#TEST-919]] [[#TEST-923]]

### REQ-913 — No identity side effect before approval

Authentication ([[#REQ-911]]) and relay consent ([[#REQ-912]]) SHALL be read-only:
they mint no scope, derive no key, publish no closure, and provision no
[[SPEC-004-application-scoped-identity#CON-204]] alias. The wallet SHALL perform
those effects ONLY after the person approves the exact intent the ceremony displays.
A ceremony the person declines — at the relay prompt or at intent consent — SHALL
leave the wallet's durable store unchanged. This restates and does not weaken the
inherited unwaivable hard stop [[SPEC-007-cbcl-pairing-cutover#REQ-812]].

Trace: [[SPEC-007-cbcl-pairing-cutover#REQ-812]] [[#TEST-920]] [[#TEST-921]]

## Contracts

### CON-904 — The amended invitation grammar (cbcl-pairing wire)

Endpoint/Interface: the cbcl-pairing invitation gains one field, a canonical
[[SPEC-004-application-scoped-identity#CON-201]] `applicationId`, carried *alongside*
(never replacing) the existing constant credential-application member.

Input grammar (the wallet's recogniser, in full before any use):
```
invitation      := existing-invitation-fields , application-id-field
application-id-field := "applicationId" ':' https-origin-path
https-origin-path    := "https://" host [ ":" port ] "/" 1*path-segment   ; CON-201 §applicationId
                        ; recognised by SPEC-004 ApplicationId::parse, byte-for-byte
```
The field SHALL be recognised by the **existing SPEC-004 `ApplicationId::parse`**,
never a second parser (one parser per language, LangSec Principle 5). An invitation
lacking the field, or whose field `ApplicationId::parse` rejects, SHALL refuse —
there is NO fallback to the constant credential-application member for identity.
Pre-conditions: the cbcl-pairing allocator emits the field ([[#ADR-905]]).
Post-conditions: `applicationId` is a canonical CON-201 origin the wallet can fetch.
Error model: absent/malformed → `InvitationMalformed`, before any I/O.
Implements: [[#REQ-910]]
Verified by: [[#TEST-918]] [[#TEST-924]]

### CON-905 — The relay trust policy store

Endpoint/Interface: a wallet-local, custody-adjacent store of accepted relay
origins, with operations `contains(origin) → bool`, `accept(origin)`,
`list() → [origin]`, `remove(origin)`. Persisted through the same `store` module the
pairing-trust index uses; survives restart; never leaves the device.
Input grammar: each entry is a canonical `https://host[:port]` origin (no path, no
query); `accept` SHALL canonicalise-and-recognise before writing, refusing any
non-canonical origin so the set cannot be poisoned with variants that compare
unequal to a real relay origin.
Pre-conditions: none (the set may be empty — the fail-closed initial state).
Post-conditions: `contains` is true for exactly the origins the person accepted and
has not removed.
Error model: a write failure surfaces (`StoreWriteFailed`) and the ceremony refuses —
an accept that was not durably recorded MUST NOT admit the socket.
Implements: [[#REQ-912]]
Verified by: [[#TEST-919]] [[#TEST-923]] [[#TEST-925]]

### CON-906 — First-contact consent presentation

Endpoint/Interface: the wallet SHALL present, before any socket I/O toward a new
relay, a single decision surface naming BOTH anchors:
- the **authenticated application origin** (from [[#REQ-911]]), under the
  untrusted-value treatment SPEC-004 uses for application self-description, rendered
  with its explicit Unicode/punycode form (F-3 residual);
- the **relay origin** being requested, flagged as new to this person.
The surface SHALL offer exactly accept and reject, no third control, no free-text
relay entry ([[SPEC-007-cbcl-pairing-cutover#REQ-813]]). Accept proceeds and records
the relay ([[#CON-905]]); reject ends the attempt. The exact-intent consent that
follows delivery ([[SPEC-008-production-pairing-claimant#CON-901]]) is unchanged and
is a SECOND, separate approval — relay consent is not intent consent.
Pre-conditions: [[#REQ-911]] passed; relay not in policy.
Post-conditions: exactly one of {policy gains the origin and the ceremony proceeds}
or {nothing changes and the attempt ends}.
Error model: none beyond the two outcomes.
Implements: [[#REQ-912]]
Verified by: [[#TEST-919]] [[#TEST-926]]

## Decisions

### ADR-904 — Origin trust: live-authenticated profile + person-owned TOFU, not held record or compiled allowlist

[[SPEC-008-production-pairing-claimant#ADR-902]] anchored relay trust in a *held*
authenticated profile plus a *compiled* digest registry
([[SPEC-008-production-pairing-claimant#CON-903]]). That is sound but forces an
enrolment-first bootstrap and makes the wallet vendor the authority on legitimate
relays. This amendment moves both anchors:
- **application** trust → a *live* CON-220 fetch from the invitation's `applicationId`
  ([[#REQ-911]]), which authenticates by origin-binding rather than by prior holding;
- **relay** trust → the *person's* accept ([[#REQ-912]]), replacing the compiled
  registry entirely (owner direction: no allowlist).
Rejected alternative: keep the compiled registry as a floor. Rejected because the
owner directed no allowlist, and because CON-220 origin-binding already supplies the
application anchor F-3/F-4 demanded — the relay is bound to the application by the
authenticated profile declaring it, and to the person by their accept. Consequence,
stated plainly: the person, not the wallet vendor and not a prior enrolment, is the
trust authority; the cost is that the first contact with any application shows the
person a relay decision they must make correctly (mitigated by [[#CON-906]]'s
authenticated-origin display). [[SPEC-008-production-pairing-claimant#CON-903]] and
its registry are RETIRED for first contact by this amendment.

### ADR-905 — The invitation must carry a real applicationId (cbcl-pairing wire amendment)

REQ-909 died on F-1: the invitation had no per-application identity. This amendment's
[[#CON-904]] REQUIRES the cbcl-pairing invitation to carry a canonical CON-201
`applicationId`. That is a change to the **cbcl-pairing wire and its allocator**, not
to this wallet — a cross-repo dependency that MUST be specified and adversarially
reviewed in cbcl-pairing (for its own phishing surface: e.g. an invitation naming one
application's id but another's relay, which [[#REQ-910]](b) already refuses by
requiring the authenticated profile to declare the relay) before this wallet-side work
begins. Until the cbcl-pairing amendment lands and its allocator emits the field, this
spec is unimplementable and stays `not-approved`. Rejected alternative: derive the
applicationId from the relay origin or the credential-application constant — both are
attacker-chosen or constant, i.e. F-1 again.

## Tests

Core (an implementer writes these in one sitting, no rig): [[#TEST-918]]–[[#TEST-921]].
Depth (needs a live relay / profile host / decline harness): [[#TEST-922]]–[[#TEST-926]],
deferrable with the named owner but specified.

### TEST-918 — positive: standalone admission (core)
An invitation with a valid `applicationId` whose live-authenticated profile declares
the invitation's relay, with the relay already in policy, assembles a context and
opens the socket. (REQ-910, REQ-911, CON-904)

### TEST-919 — positive: TOFU accept path (core)
A new relay prompts; on accept, the origin is recorded ([[#CON-905]]) and the ceremony
proceeds; a second invitation to the same relay does not prompt. (REQ-912, CON-906)

### TEST-920 — negative-output: decline leaves no side effect (core)
Declining at the relay prompt, and separately at intent consent, leaves the durable
store byte-identical to before the ceremony — no scope, no key, no closure, no alias.
(REQ-913, inherited REQ-812)

### TEST-921 — negative-output: refusal before I/O (core)
A missing/malformed `applicationId`, a profile that does not declare the relay, and a
rejected relay each refuse with no socket opened and no store write. (REQ-910, CON-904)

### TEST-922 — negative-input: forged profile (depth)
A profile served from an origin other than the `applicationId` (or failing CON-220)
refuses `PairingProfileUnavailable`; the invitation-carried profile is never trusted.
(REQ-911)

### TEST-923 — negative-input: non-canonical policy entry (depth)
`accept` refuses a non-canonical relay origin; `contains` never matches a variant that
compares unequal to a canonical origin. (CON-905)

### TEST-924 — negative-input: application/relay mismatch (depth)
An invitation naming application A's id but a relay only application B declares refuses
at REQ-910(b) — the cross-repo phishing surface ADR-905 names. (REQ-910, CON-904)

### TEST-925 — store durability (depth)
An accept not durably written refuses the socket (`StoreWriteFailed`), not admits it.
(CON-905)

### TEST-926 — consent presentation (depth)
The consent surface shows the authenticated origin (untrusted treatment, explicit
punycode form) and the new relay, offers only accept/reject, and carries no relay
free-text entry. (CON-906)

## Observability

### OBS-904 — Punycode/lookalike origins on the consent surface
The authenticated-origin display is the human anchor; a lookalike (punycode,
mixed-script) that a person misreads is the residual F-3 risk this spec does not fully
close. Instrument how origins are rendered and gather whether the explicit-form
display ([[#CON-906]]) is sufficient, feeding an [[#open-questions]] decision.

## Dependencies and sequencing

1. **cbcl-pairing wire amendment ([[#ADR-905]])** — invitation carries `applicationId`;
   allocator emits it; reviewed for its own phishing surface. **Blocks everything.**
2. **This spec's review** — Tier 1 cross-model adversarial review + human domain
   expert, over this doc and the wire amendment together.
3. **Wallet implementation** — CON-904 recogniser, REQ-911 live-auth reuse of the
   SPEC-004 CON-220 path, CON-905 policy store, CON-906 consent screen, REQ-913
   ordering, fresh-scope mint post-consent.
4. **Browser allocator (cbcl-bus)** — emit the `applicationId` in the invitation it
   allocates.

No wallet or allocator code is written before steps 1–2 close.

## Open questions

- **OQ-1 (F-3 residual).** Is [[#CON-906]]'s explicit-form origin display sufficient
  against punycode/mixed-script lookalikes, or does first contact need an additional
  application anchor (e.g. a short authenticated-origin fingerprint the person
  compares, as CON-221 does for the account key)? Owner: security review.
- **OQ-2.** Does dropping the compiled registry entirely ([[#ADR-904]]) remove a useful
  *operator* revocation lever (CON-903 could refuse a compromised relay operator across
  all wallets)? If operator revocation is needed, it must re-enter as something other
  than an allowlist. Owner: owner direction.
- **OQ-3.** Fresh-scope mint on first contact ([[#REQ-913]]): deterministic derivation
  vs fresh randomness, and its interaction with the person re-pairing the same
  application later. Owner: implementer + SPEC-004 cross-check.

## Changelog

- **0.4.0-draft — 2026-08-22 — standalone TOFU first-contact: authored, awaiting
  review.** Reverses [[SPEC-008-production-pairing-claimant#ADR-902]] and reopens the
  first-contact path [[req-909-adversarial-review-2026-08-22]] rejected, answering each
  of its findings (F-1 via a required cbcl-pairing wire amendment carrying a real
  `applicationId`; F-2 via strict post-consent ordering; F-3/F-4 via CON-220 live
  application authentication as the anchor and person-owned TOFU relay trust; F-5/F-6
  by stating the order and the ADR-902 reversal openly). Owner-directed (no allowlist).
  Not implemented; `not-approved`; blocked on the cbcl-pairing wire amendment.
