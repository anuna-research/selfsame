---
id: SPEC-008-amendment-0.4.23
title: SPEC-008 0.4.23-draft — Total result contracts and static nesting
status: draft
version: 0.4.23-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#CON-981]] only where [[spec-008-0.4.23-total-result-contracts-and-static-nesting#CON-982]] says so"
adopts: "All effective 0.4.4 through 0.4.22 clauses except the exact replacements and retirements in CON-982"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.9-draft; cbcl-bus SPEC-053 0.16.16-draft; [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]]"
generation-model-family: OpenAI GPT-5
generation-model-version: gpt-5.6-sol
generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6
generation-synthesis-trajectory: "[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#Tier-1-generation-provenance]] -> [[spec-008-0.4.22-claude-adversarial-review-2026-08-23#Findings]] -> [[spec-008-0.4.22-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]] -> this candidate"
last-updated: 2026-08-23
---

# SPEC-008 0.4.23-draft — Total result contracts and static nesting

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** This amendment addresses every 0.4.22
> review finding. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)` pair.
No identity effect precedes the applicable explicit consent.

## Tier-1 generation provenance

OpenAI GPT-5 model `gpt-5.6-sol` generated this coordinated synthesis. Its
Codex session is `01a029aa-9127-7c42-ad28-81512b91ded6`.

The initial draft is
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#Tier-1-generation-provenance]].
Its failing tests and attribution map are in
[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#Findings]].

The localised repair input is
[[spec-008-0.4.22-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]].
This draft applies all twelve repairs and records their exact dispositions.

The Mnesia re-trace removes two unreachable runtime terminals. It makes
nesting and abort-catching static inventory failures. It adds the omitted
successor owner, nested read, and consumer-success adaptations.

The trace re-scan broadens the version-site classifier. It restores the four
regression groups and the WebFinger read test. It also repairs every pointer,
parent disposition, lane label, schema qualifier, and residual named by N7.

A qualifying reviewer SHALL use another model family and a fresh session. The
report SHALL record its model, authentication path, session, and Circus
attempt. It SHALL verify distinctness from this generation record.

## Requirement

### REQ-1002 — One companion target and one scan authority govern

This requirement replaces
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#REQ-1001]] in
full.

An authority scan SHALL apply every exact retirement in CON-978, CON-979,
CON-980, CON-981, and CON-982 before comparing current values. Retired text
supplies no survivor, test, coordinated set, pointer, or replacement authority.

The scan covers numbered clauses, numbered list items, and unnumbered
normative paragraphs. A heading, changelog, or earlier gate label cannot make
conflicting normative text historical.

Only CON-982 supplies the current Selfsame correction set, cbcl-bus test set,
coordinated review set, and coordinated pointer set. CON-978 remains the sole
source of the current cbcl-pairing protocol survivor set.

Credential/v2 SHALL conform to cbcl-pairing SPEC-001 0.4.9-draft. That
candidate adopts the unchanged 0.4.8 protocol body and its earlier contracts.

The wallet and browser use separate typed machine-carrier and PAIR1
presence-field inputs. The pairing library enforces non-substitutability,
version isolation, CPace source, sender rules, state rules, and its envelope.

Parent [[SPEC-008-production-pairing-claimant#REQ-903]] remains effective. The
wallet provides live approve and decline. The two-stage credential/v2 decision
sequence remains under the amended SPEC-007 REQ-804 ordering.

Parent [[SPEC-008-production-pairing-claimant#REQ-904]] also remains effective.
Scan and paste decode the machine carrier into the same recognised type. The
separate PAIR1 presence field remains scan-only as a co-presence constraint.

The production scan is the completeness authority for v1 comparisons,
substitutions, and ingress conversions. A fixed list is evidence only and
cannot limit the scan.

The classifier includes every version constant, literal, comparison, emitted
member, and typed version-field assignment. A field-to-field copy on an ingest
path is a substitution. Extraction from untyped input into a typed
version-bearing value is an ingress conversion.

The scan includes `PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and
`payloadVersion`. It distinguishes production code from `#[cfg(test)]` code,
member-name constants, type declarations, format templates, and comments.

Named baseline witnesses include every site listed by REQ-1001. They also
include the comparison at
`crates/selfsame-app-identity/src/enrollment.rs:592`.

The ingest-copy witnesses are
`crates/selfsame-web-device/src/lib.rs:831`, `:847`, and `:861`. The direct
ingress-conversion witnesses are
`crates/selfsame-app-identity/src/ceremony.rs:413` and
`crates/selfsame-app-identity/src/provider_hint.rs:96`.

Credential/v2 reaches no classified v1 site. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.

No sequencing list independently enumerates a superseded companion test
range. Release blocking follows CON-978's protocol survivor set and CON-982's
review set.

## Non-functional requirement

### NFR-925 — Results are total and prohibited nesting is static

This requirement replaces
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#NFR-924]] in full.
It supplements
[[spec-008-0.4.19-singular-authority-and-webfinger-order#NFR-921]]. It replaces
only the three NFR-921 ranges named below.

The first range starts with "`withdraw/2` derives the requested alias URI".
It ends with "Its exact withdrawal write lock follows." The replacement is:

`withdraw/2` derives `requestedAlias` from `homeDid` before opening its
transaction. It passes that value to
`tombstone/3(account, homeDid, requestedAlias)`.

The live path takes the requested alias write lock first. It takes the exact
account write lock second.

An absent account row enters `already-withdrawn/2`. That path is non-mutating
and preserves idempotent success for the requested generation.

A present account row SHALL name the requested alias and home DID. The
write-locked alias row SHALL name that same account and home DID.

An absent or disagreeing alias row returns `#(error inconsistent-binding)`.
A disagreeing account row refuses. Neither case deletes any row.

Only reciprocal agreement continues. That branch takes the exact withdrawal
write lock and performs both live-row deletes under the held locks.

The second range starts with "`reinstate/2` omits the alias table". It ends
with "It performs no read-to-write upgrade." The replacement is:

`reinstate/2` and `lift-tombstone/2` take no alias or account lock. Their
transaction takes one whole withdrawal write lock before its first
`index_read`.

The exact delete runs under that table lock. Provision and withdrawal already
serialize through their withdrawal write locks. Reinstatement performs no
read-to-write upgrade.

The third range starts with "Every affected transaction runs at Mnesia level
1". It ends with "No partial effect survives." The replacement is:

Every affected transaction runs at Mnesia level 1 through one shared wrapper.
The wrapper calls `mnesia:transaction(Fun, [], 3)`.

The default unary call, `infinity`, and an unbounded argument overload are
non-conforming. The wrapper maps only `{aborted, nomore}` to retry exhaustion.

Mnesia converts a level-1 exhausted cyclic restart to `{aborted, nomore}`.
An exhausted `#cyclic{}` is not a separate public terminal.

Proper nested transactions commit silently at the outer wrapper. Prohibited
nesting therefore has no runtime terminal and no runtime classifier.

A repository scan enumerates every production `mnesia:transaction` call under
`apps/`. Every row names module, function, line, public owner, and transaction
body.

Every row also names wrapper, lane membership, terminal mapping, and
call-graph evidence. Omitted, unclassified, or unsupported out-of-scope rows
are non-conforming.

Every transaction body has a complete static call graph. A body SHALL NOT open
another transaction or contention retry loop. A body SHALL NOT catch an
`{aborted, _}` exit.

Lane membership follows transitive transaction-body table access:

1. Authority contains `cbcl-device-grant`, `cbcl-member`, and
   `cbcl-pairaccount-grant`.
2. Ceremony migration contains `cbcl-account-binding`, `cbcl-room`,
   `cbcl-roommember`, `cbcl-account-roommember`, `cbcl-invite`, and
   `cbcl-sealedgrant`.
3. Ceremony migration also contains every `cbcl-selfsame-*` table created by
   the effective credential/v2 design.
4. WebFinger contains `cbcl-webfinger-alias`, `cbcl-webfinger-account`, and
   `cbcl-webfinger-withdrawn`.
5. The transaction-based schema-migration site is
   `cbcl-store-migrate:legacy-rows/0` over `cbcl-msg`.
6. Out-of-scope means the proved transitive body touches none of those tables.

A row can occupy several runtime lanes. Authority takes terminal precedence,
then ceremony migration, then WebFinger.

The named `legacy-rows/0` site does not overlap those runtime lanes.
`cbcl-chat-roommember:migrate-since/0` is a second schema migration. It uses
`mnesia:transform_table/4` and lies outside the transaction-call inventory.

The nonce allocator's outer collision loop is a semantic uniqueness retry. It
is not a Mnesia contention retry. Each transaction invocation remains
non-nested.

Authority and ceremony-migration owners map retry exhaustion to
`#(error authority-busy)`.

Public collection and predicate owners use tagged success. They return
`#(ok Value)` or `#(error authority-busy)`. Busy never becomes an empty list,
`false`, `undefined`, `exists`, absence, or non-membership.

These authority owners expose that contract:

- `cbcl-chat-account-grants`: `confirmed-migrate-path-a-handle/3`,
  `bind-and-store-verified-device-grant/8`, and
  `store-verified-device-grant/6`;
- the same module: `device-grant/2`, `device-grants/1`, `peer-grants/1`,
  `accounts/0`, `revoke-observed/2`, and `revoke-device-grant/2`;
- `cbcl-chat-members`: `pubkey/1` and `enroll-or-check/2`;
- `cbcl-chat-pairaccount`: `claim/2`, `grants/1`, and
  `unratified-device?/1`; and
- `cbcl-chat-successor`: `accept/3`.

These ceremony-migration owners expose the same busy result:

- `cbcl-chat-account-grants`: `account-binding/1` and `bind-account/2`;
- `cbcl-chat-account-roommember`: `grant/5`, `redeem-and-grant/5`,
  `re-admit?/3`, `member-grant?/2`, `rooms-for-account/1`, and `since-seq/2`;
- `cbcl-chat-roommember`: `since-seq/2`, `grant/5`,
  `redeem-and-grant/5`, `re-admit?/3`, `member-grant?/2`, and `revoke/2`;
- `cbcl-chat-invite`: `mint/3` and `redeem/2`;
- `cbcl-chat-sealedgrant`: `put/3` and `get/1`; and
- `cbcl-chat-roomcfg`: `ensure-room/3`, `ensure-room/4`, `claim/3`,
  `claim/4`, `claim/5`, `list-public/0`, `get/1`, and `creator/1`.

The room configuration set also includes `get-dialects/1`, `add-dialect/4`,
`add-dialect-capturing/4`, `rollback-add/5`, and `remove-dialect/3`.

Transaction-only helpers return no public busy terminal. They are called only
inside one wrapper-owned transaction. The inventory proves every such edge.

`enroll-or-check/2` performs its read, device guard, decision, and possible
write in one wrapper transaction. Its transaction result reaches
`enrol-commit`.

`cbcl-chat-invite:mint/3` returns `#(ok Token)` only after commit.
`cbcl-chat-roomcfg:ensure-room/4` returns `ok` only after commit.

`bind-and-store-unbound/8` calls `cbcl-chat-members:pubkey-tx/1`. It SHALL NOT
call transaction-opening `pubkey/1`.

`cbcl-chat-successor:accept/3` uses a transaction-only device-grant read. Its
`account-accepted?/2` helper SHALL NOT call transaction-opening
`device-grant/2`.

The successor helpers at `cbcl-chat-successor.lfe:109` and `:134` SHALL NOT
catch an `{aborted, _}` exit. Their transaction aborts propagate to the shared
outer wrapper.

Every production consumer handles both tagged result forms. It unwraps
`#(ok Value)` before using `Value`. It handles `#(error authority-busy)`
explicitly and never through a wildcard default.

The consumer map includes the boolean contexts at
`cbcl-chat-room.lfe:263-270`, `cbcl-chat-pairgrant.lfe:78`, and
`cbcl-chat-session-ws.lfe:1050`. It also includes the collection consumer at
`cbcl-chat-session-ws.lfe:869`.

The map includes the `unratified-device?/1` consumers at
`cbcl-chat-session-ws.lfe:347` and `:385`. Tagged success cannot reach a
boolean operator, list function, pure admission map, or fail-closed wildcard.

At a WebSocket boundary, busy closes the connection with code 1013 and reason
`try-again-later`. It emits no allocation acknowledgement, refusal, invite,
grant, or enrollment frame.

The browser treats that close as non-terminal. It cancels the old socket
generation, reconnects, and starts a fresh correlated attempt.

At startup, busy prevents the listener from opening. It never becomes a
successful default-room claim.

For the busy class, name-squatting checks propagate busy to the 1013 boundary.
They cannot report that a name is free.

The `cbcl-provenance` wildcard in `addagent-target-free?/3` is an accepted
out-of-lane residual for non-retryable failures. This amendment does not claim
that the wildcard is safe outside the busy class.

Member enrollment performs no write after busy.

The WebFinger public owners remain `provision/3`, `withdraw/2`, `reinstate/2`,
and `lookup/1`. Their transaction bodies never open another transaction.

WebFinger mutations return `#(error authority-busy)` on retry exhaustion.
`lookup/1` is exempt from that result form and returns `unavailable`.

`cbcl-chat-webfinger-http` maps `unavailable` to HTTP 503. The response is
empty and carries `no-store`.

Contention never becomes `missing` or HTTP 404. Invalid syntax and committed
absence retain their existing 400 and 404 behavior.

`create-or-return/3`, `lookup-binding/1`, `tombstone/3`,
`already-withdrawn/2`, and `lift-tombstone/2` are WebFinger transaction bodies.
Their names specify locks, not public retry terminals.

Transaction-based schema exhaustion maps to
`#(error schema-migration-busy)`. The application refuses startup and opens no
listener. It never treats this result as `no-table`.

No partial effect survives any exhausted transaction. Existing success and
non-contention refusal meanings remain unchanged behind their declared result
shapes.

## Contract

### CON-982 — Review authority, traces, and pointers advance once

This contract supplements
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#CON-981]]. Every
earlier replacement remains effective unless this contract replaces its exact
current fragment.

REQ-1002 replaces REQ-1001 in full. NFR-925 replaces NFR-924 in full. ADR-960
replaces ADR-959 in full.

TEST-1150 replaces TEST-1148 in full. TEST-1151 replaces TEST-1149 in full.
Every transitive predecessor relationship remains intact.

This contract retires CON-981's current-value range beginning with "The exact
cbcl-bus set". It ends with did-crdt commit
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

The replacement text is:

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, TEST-109, and TEST-110.

Every effective Selfsame test through TEST-1108 also survives. TEST-1134,
TEST-1135, TEST-1139, TEST-1150, and TEST-1151 are the current corrections.

Those five correction tests collectively replace TEST-1129 through TEST-1133.
No superseded portion or open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.9, and
cbcl-bus 0.16.16. It contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

This contract retires CON-981's pointer paragraph beginning with "The Selfsame
parent". It ends with "participates in the coordinated gate."

The replacement text is:

The Selfsame parent [[SPEC-008-production-pairing-claimant]] points here and
names this candidate's open review gate.

The cbcl-bus parent [[SPEC-053-key-root-identity#DEC-012]] points to cbcl-bus
0.16.16. Cbcl-pairing 0.4.9 points its `consumer-design` to this candidate.

Cbcl-pairing's recorded generation provenance participates in the coordinated
gate.

Every F-A through F-E correction remains a hard stop.
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] also remains a hard stop.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It
authorizes no code, allocation, release, or deployment.

## Decision

### ADR-960 — Retryable unavailability is typed and total

Every affected transaction belongs to a table-derived lane. The shared wrapper
recognises one observable retry-exhaustion result.

Static inventory, not an unreachable runtime terminal, prohibits nesting and
abort-catching. Every changed success shape has a corresponding consumer
adaptation.

Busy is not absence, non-membership, name availability, or successful
creation. Socket consumers expose it as retryable 1013. Startup consumers keep
the listener closed.

The withdrawal live branch requires reciprocal account and alias agreement.
The idempotent absent-account branch remains non-mutating.

The production source scan is authoritative over version sites. A witness list
cannot weaken it. One current companion candidate governs credential/v2.

Authenticated display, post-consent effects, shared grammar, cap arithmetic,
and the sealed-grant graph remain unchanged.

## Test specification

### TEST-1150 — Total results, static nesting, and regressions close

Change only the installed profile digest while authority and issuer stay
fixed. Require grant verification and an atomic silent re-pin.

Change authority or issuer. Require the person prompt, cancellation, and
verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames.

Confirm and cancel unlink. Verify exact local removal, seed retention, and
unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consent decisions, and a new verified
grant. Every local-only re-grant shortcut fails.

Generate all twelve authority cycles and every `AR` contention case. Generate
the complete ceremony-migration and WebFinger graphs.

Require the restored acquisition point and invite-before-room order. Require
no additional upgrade or cycle.

Enumerate every production transaction call under `apps/`. Require each row's
owner, body, tables, lane set, wrapper, terminal, and call-graph proof.

Require the shared wrapper's exact retry count. Reject unbounded, nested,
omitted, or unsupported out-of-scope sites.

Force `{aborted, nomore}` at every affected public owner. Require the lane's
exact busy terminal, zero partial effect, and unchanged non-contention meanings.

Do not require a raw exhausted `#cyclic{}` or `nested_transaction` terminal.
Prove statically that no transaction body opens a transaction or catches an
`{aborted, _}` exit.

Require `cbcl-chat-successor:accept/3` in the authority inventory. Instrument
its transaction body and both helper edges.

Require a transaction-only device-grant read. Any `device-grant/2` edge or
abort-catching edge at `cbcl-chat-successor.lfe:109` or `:134` fails.

Exercise every public owner named by NFR-925. Collection and predicate owners
must produce `#(ok Value)` and `#(error authority-busy)`.

Exercise every production consumer under both forms. Require every success to
be unwrapped before use and every busy value to reach its declared boundary.

Drive the boolean and collection consumers named by NFR-925. The admission
decision remains total. No tagged tuple reaches a boolean operator, list
function, or pure admission field.

At each WebSocket consumer, require close code 1013 and
`try-again-later`. Require no acknowledgement, refusal, invite, grant, or
enrollment frame.

Retry after reconnect with a new socket generation and request identifier.
Require one correlated success and no duplicate durable effect.

Force member-enrollment exhaustion before its possible write. Require no
member row and no later rebind.

Force the in-lane half of name-squatting to exhaust. Require no name-free
result and a 1013 close.

Keep the `cbcl-provenance` wildcard recorded as an out-of-lane residual. Do not
claim this test repairs its non-retryable behavior.

Force exhaustion in `invite:mint/3` and `roomcfg:ensure-room/4`. Require no
token, no room row, and no reported success.

Instrument `bind-and-store-unbound/8`. Require only `pubkey-tx/1` inside its
outer transaction. Any `pubkey/1` edge fails.

Force the WebFinger terminal through `lookup/1`. Require `unavailable`, HTTP
503, an empty `no-store` response, and no HTTP 404.

Successful and missing WebFinger lookups retain their existing responses.
Mutations use `#(error authority-busy)` and never false absence.

Force transaction-based schema exhaustion. Require
`schema-migration-busy`, no `no-table`, no listener, and no skipped history.

Record `cbcl-chat-roommember:migrate-since/0` as a separate
`transform_table/4` schema migration outside the transaction inventory.

Provision reciprocal account and alias rows. Withdraw and require both live
rows deleted under alias-before-account ordering.

Delete the requested alias row while retaining the account row. Withdrawal
SHALL return `inconsistent-binding` and delete nothing.

Point the requested alias row at another account or DID. Require the same
closed result and zero deletion.

Remove the account row and retain a matching tombstone. Require the idempotent
already-withdrawn result and no mutation.

Require reinstatement's whole withdrawal write lock before its first
`index_read`. Require no alias or account lock.

Run the current cbcl-pairing TEST-050 exception. The control recognizer accepts
1, 23, 24, 255, 256, 2,047, and 2,048 octets.

It refuses 0, 2,049, 4,095, and 4,096 octets. Large values retain their exact
boundary rows. No positive control row names 4,096.

### TEST-1151 — Current authority and provenance are singular

Resolve every effective clause from 0.4.4 onward. Apply CON-978 through CON-982
before comparing values.

Require one cbcl-pairing protocol survivor set, cbcl-bus set, Selfsame
correction set, coordinated review set, and pointer set.

Require REQ-1002 as the sole successor of REQ-1001, REQ-1000, REQ-963, and
REQ-954. Require one current companion conformance target.

Require the retained dispositions of parent
[[SPEC-008-production-pairing-claimant#REQ-903]] and
[[SPEC-008-production-pairing-claimant#REQ-904]].

Scan all production Rust sources for every v1 comparison, substitution, and
ingress conversion under REQ-1002's classifier.

Require the named baseline witnesses, including `enrollment.rs:592` and the
five ingest witnesses. A newly classified production site must appear in the
generated scan output.

Require the parent pointer here and its open review gate. Require cbcl-bus
[[SPEC-053-key-root-identity#DEC-012]] at 0.16.16.

Require cbcl-pairing 0.4.9's `consumer-design` here. Require generation family,
version, session, and enumerated synthesis trajectory in all three candidates.

Require a different reviewer family and a fresh reviewer session. Require the
review to record its subscription or API authentication path.

Require the transaction inventory to cover every production call under
`apps/`. Require table-derived classifications and exact public-owner results.

Require all 42 cbcl-bus GATE-04 boxes. Require both non-conforming rendezvous
baselines and sealed cap arithmetic.

Require every installed-state clause in
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-995]]. Require
the four inline regression groups from TEST-1150.

Removing any current obligation fails.

## N7 disposition

| Finding | Disposition in this draft |
|---|---|
| N7-H-1 | NFR-925 exempts `lookup/1` from `authority-busy`; TEST-1150 restores `unavailable`, 503, empty `no-store`, and no 404. |
| N7-M-1 | NFR-925 requires both tagged forms at every consumer and names the boolean and collection contexts; TEST-1150 tests totality. |
| N7-M-2 | NFR-925 recognises only `{aborted, nomore}` and makes nesting static; TEST-1150 removes both unreachable forcings. |
| N7-M-3 | NFR-925 adds `successor:accept/3`, its transaction-only grant read, and the two forbidden catches; TEST-1150 instruments them. |
| N7-M-4 | REQ-1002 makes the source scan sole authority, adds `enrollment.rs:592`, and classifies the three web-device copies plus direct ingress conversions. |
| N7-M-5 | TEST-1150 restates all four dropped regression groups; TEST-1151 names REQ-995. |
| N7-L-1 | NFR-925 moves `account-binding/1` and `bind-account/2` into ceremony migration. |
| N7-L-2 | REQ-1002 restates parent REQ-903 and REQ-904. |
| N7-L-3 | CON-982 restores the parent's open-gate obligation and uses the Orientation artefact anchor. |
| N7-L-4 | NFR-925 scopes name-squatting to busy and records the provenance wildcard residual. |
| N7-L-5 | The provenance section links the initial draft, failing review, correction table, and this repair. |
| N7-L-6 | NFR-925 scopes the non-overlap claim and records `migrate-since/0`. |

## Changelog

- **0.4.23-draft — 2026-08-23 — review candidate, not approved.** Makes
  result-shape consumers total, makes nesting static, restores regressions,
  and repairs all N7 trace and pointer findings. No implementation or
  deployment authority is claimed.
