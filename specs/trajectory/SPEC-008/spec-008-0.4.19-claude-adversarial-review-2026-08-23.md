---
id: SPEC-008-review-0.4.19-claude-2026-08-23
title: SPEC-008 0.4.19 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.12 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.19-singular-authority-and-webfinger-order]]"
prior-review: "[[spec-008-0.4.18-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.19 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.12 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session started. I
recorded the gate result first, and printed no secret value.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; fi
ABSENT
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "orgName": "<redacted>",
  "orgId": "<redacted>",
  "email": "<redacted>",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai` and the reported `subscriptionType` is `max`. Account identifiers
are redacted above and appear nowhere in this report.

The reviewing model is Claude Opus 5 in a fresh context. This session called no
Anthropic API and no external web service. Every input came from the four local
repositories, the local cbcl-rs checkout, and the local OTP install. Separate
sessions authored the three candidates. This session read the candidates and the
source, and never the generator reasoning.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `d23dfcac5ca99d4372f7434206f4be4265c20baf` | candidate 0.4.19-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `4f26844cdd08600dc3d25df6adf454133e9cd261` | candidate 0.4.8-draft |
| cbcl-bus | `d23b8731a30c991b6e22bf5e3e5247b655af4bad` | candidate 0.16.12-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The sibling working trees carry user-owned uncommitted
paths. I read those repositories only through `git show COMMIT:PATH`, and I
changed nothing in them. The local OTP install is `28.5` with `mnesia-4.25.3`.

I read the complete effective amendment chain from 0.4.4 forward. I also read
the 0.4.18, 0.4.17, 0.4.16, and 0.4.15 reviews and both coordinated candidates.
I read [[PROTO-002-selfsame-rendezvous-v1]] and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]].

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 2 | N4-H-1, N4-H-2 |
| MEDIUM | 1 | N4-M-1 |
| LOW | 3 | N4-L-1, N4-L-2, N4-L-3 |

Verdict: **REJECT**.

Four of the six 0.4.18 findings close cleanly against real code. The four
`CON-976` replacement relationships are restated by name, so no 0.4.16 clause
revives. The WebFinger graph now has one table order, and that order is
implementable at the local OTP install. The withdrawal branches, the pointer
graph, and the retired maximum label all close.

The rejection rests on two ledger findings and one WebFinger terminal.
[[spec-008-0.4.19-singular-authority-and-webfinger-order#REQ-998]] widens the
resolver to every effective clause from 0.4.4 onward. The retirement work
stopped at 0.4.12. Seven earlier test-specification paragraphs, four earlier
numbered gate items, and one earlier contract still state competing ledgers. The new check in
[[spec-008-0.4.19-singular-authority-and-webfinger-order#TEST-1143]] therefore
fails against its own candidate, exactly as its predecessor did. Separately the
cbcl-bus twin left its own predecessor ledger effective, so the coordinated set
is not singular across the three repositories.

---

## Findings

### N4-H-1 (HIGH, release-blocking) — the ledger retirement stops at 0.4.12, so twelve earlier clauses still state competing survivor, hub, and coordinated ledgers

**Repository owner: selfsame (CON-978, and the untouched 0.4.5 through 0.4.11
clauses).**

`CON-978` at
`specs/trajectory/SPEC-008/spec-008-0.4.19-singular-authority-and-webfinger-order.md:193-215`
retires five earlier ledger sites. I checked every named boundary string against
its target and each one is unique inside its clause.

| Retirement | Target location | Boundary strings resolve |
|---|---|---|
| `CON-971` coordinated sentence | `spec-008-0.4.12-tagged-transition-closed-authority.md:277-278` | yes, both unique |
| `CON-973` coordinated sentence | `spec-008-0.4.14-deployed-admission-and-local-unlink.md:219-221` | yes, ends at the commit |
| 0.4.12 test preamble | `spec-008-0.4.12-tagged-transition-closed-authority.md:304-306` | yes, both unique |
| 0.4.13 test preamble | `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:314-317` | yes, both unique |
| 0.4.14 test preamble | `spec-008-0.4.14-deployed-admission-and-local-unlink.md:246-251` | yes, both unique |

Those five retirements are decidable and correct as far as they reach. The
defect is the reach. `REQ-998` at `:35-37` sets the audit scope:

> The specification authority resolver evaluates every effective numbered
> clause and unnumbered normative paragraph from 0.4.4 onward.

Seven earlier test-specification paragraphs sit in that scope, and no clause in
the chain retires or replaces any of them. Each states a cbcl-pairing survivor
range, and four also state a cbcl-bus range.

| Location | cbcl-pairing survivors | cbcl-bus set |
|---|---|---|
| `spec-008-0.4.5-standalone-tofu-first-contact.md:518-520` | TEST-030 through TEST-047 | — |
| `spec-008-0.4.6-standalone-tofu-first-contact.md:375-377` | TEST-030 through TEST-050 | — |
| `spec-008-0.4.7-standalone-tofu-first-contact.md:277-279` | TEST-030 through TEST-051 | — |
| `spec-008-0.4.8-standalone-tofu-first-contact.md:379-381` | TEST-030 through TEST-052 | TEST-043 through TEST-047 |
| `spec-008-0.4.9-standalone-tofu-first-contact.md:321-323` | TEST-030 through TEST-055 | TEST-043 through TEST-056 |
| `spec-008-0.4.10-standalone-tofu-first-contact.md:284-286` | TEST-030 through TEST-055 | TEST-043 through the new 0.16.3 tests |
| `spec-008-0.4.11-standalone-tofu-first-contact.md:356-360` | TEST-030 through TEST-055 | four ranges under the 0.16.4 ledger |
| `CON-978` at `:242-251` | TEST-030 through TEST-049, TEST-050 in part, TEST-051 through TEST-056, TEST-059 | TEST-043 through TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, TEST-098, TEST-101, TEST-102 |

**Four numbered clauses also state a coordinated review set.** These are not
unnumbered paragraphs, so even a narrow reading of the audit scope reaches them.

| Location | Stated coordinated review set |
|---|---|
| `spec-008-0.4.7-standalone-tofu-first-contact.md:357-360` | the complete effective set and four code baselines |
| `spec-008-0.4.8-standalone-tofu-first-contact.md:462-467` | cbcl-pairing 0.4.3, cbcl-bus 0.16.1 |
| `spec-008-0.4.9-standalone-tofu-first-contact.md:409-411` | cbcl-pairing 0.4.4, cbcl-bus 0.16.2 |
| `spec-008-0.4.10-standalone-tofu-first-contact.md:355-357` | cbcl-pairing 0.4.4, cbcl-bus 0.16.3 |
| `CON-978` at `:253-255` | cbcl-pairing 0.4.8, cbcl-bus 0.16.12, did-crdt |

**One further effective contract states a hub ledger.** `CON-970` at
`spec-008-0.4.11-standalone-tofu-first-contact.md:322-325` reads:

> The coordinated hub effective set is cbcl-bus 0.16.1, 0.16.2, 0.16.3, and
> 0.16.4 under 0.16.4's survivor ledger.

The same paragraph then states that cbcl-bus TEST-048 through TEST-056 survive
unchanged. Nothing retires `CON-970`, and the frontmatter of every later
amendment adopts it. `spec-008-0.4.12-tagged-transition-closed-authority.md:11`
reads `adopts: "All 0.4.4 through 0.4.11 clauses except the exact replacements
in CON-971"`, and `CON-971` at `:259-270` names eight replacements that reach
no preamble.

**The candidate fails its own three tests.** `REQ-998` at `:39-42` requires
exactly one current value for the cbcl-pairing survivor set, the cbcl-bus test
set, and the coordinated review set. `REQ-998` at `:50-53` also states that an
open-ended survivor range is non-conforming, and the 0.4.10 preamble states
one. `TEST-1143` at `:325-327` requires the same singularity across every
effective numbered clause and unnumbered normative paragraph. `ADR-956` at
`:276-278` claims that explicit current text retires every stale set.

**One disagreement is substantive rather than formal.** `CON-978` at `:242-244`
states that TEST-050 survives except its replaced control portion and its
retired positive witness. Six surviving preambles state TEST-050 as mandatory
without that exception. Two effective Selfsame clauses therefore disagree about
the content of one cbcl-pairing test.

The retired cbcl-pairing tests do not revive. I checked every surviving range
and none reaches TEST-057 or TEST-058, so the 0.4.15 `L-1` defect stays closed.

The coordinated twins do not carry this defect in the same form. Cbcl-pairing
holds exactly one survivor sentence in its whole chain, at
`specs/trajectory/SPEC-001/spec-001-0.4.8-split-room-shape-and-length.md:102-105`.

**Minimum sound correction.** Retire the 0.4.5 through 0.4.11
test-specification paragraphs by name, in the exact form `CON-978` already uses
for 0.4.12 through 0.4.14. Retire the four gate-sequencing coordinated
sentences and the `CON-970` hub-set paragraph. State the TEST-050 exception in
the same place, so no effective clause states TEST-050 in full.

---

### N4-H-2 (HIGH, release-blocking) — the cbcl-bus twin left REQ-072 effective, so the coordinated set still names Selfsame 0.4.18 and cbcl-bus 0.16.11

**Repository owner: cbcl-bus (REQ-074).**

`REQ-074` at
`specs/trajectory/SPEC-053/spec-053-0.16.12-singular-authority-and-webfinger-order.md:30-32`
opens with a preservation sentence rather than a replacement:

> The survivor lists and historical replacement relationships in REQ-072 remain
> effective. These additional replacements are exact:

Its five numbered items replace `NFR-018`, `ADR-029`, the 0.16.11 GATE-04,
`TEST-099`, and `TEST-100`. No item replaces `REQ-072`, and no sentence retires
any part of it. `REQ-072` at
`specs/trajectory/SPEC-053/spec-053-0.16.11-closed-ledgers-and-upgrade-free-operator-graph.md:55-57`
therefore stays effective, and it reads:

> The coordinated review set contains this amendment, Selfsame 0.4.18, and
> cbcl-pairing 0.4.8.

A whole-chain search of SPEC-053 returns exactly two coordinated-set
statements, at `0.16.11:55` and `0.16.12:53`. Both are effective and they
disagree on two of three members.

| Clause | Selfsame member | cbcl-bus member |
|---|---|---|
| `REQ-072` at `0.16.11:55-57` | 0.4.18 | 0.16.11 |
| `REQ-074` at `0.16.12:53-55` | 0.4.19 | 0.16.12 |

Two consequences follow, and both are release-blocking.

`ADR-030` at `0.16.12:173-175` states that the effective hub ledger names one
current test set and one coordinated review set. That claim is false against
its own chain. `TEST-102` at `0.16.12:283-284` requires static checks over the
exact three-repository coordinated set, and two effective statements make the
target ambiguous.

The same clause also leaves two hub test sets effective. `REQ-072` at
`0.16.11:51-53` states TEST-098 through TEST-100 as current, while `REQ-074` at
`0.16.12:49-51` states TEST-098, TEST-101, and TEST-102. The two numbered
replacements reconcile that pair, so I treat only the coordinated set as a flat
contradiction.

The Selfsame half points the other way. `CON-978` at `:253-255` names cbcl-bus
0.16.12, so an implementer following the Selfsame ledger and an implementer
following the surviving cbcl-bus ledger assemble different coordinated sets.
This is the identical defect class the 0.4.18 review filed against Selfsame, and
the cbcl-bus twin adopted the preservation sentence without adopting the
retirement.

**Minimum sound correction.** Retire the `REQ-072` coordinated-review sentence
by name in `REQ-074`, in the form `CON-978` uses for `CON-971` and `CON-973`.
Retire or restate the `REQ-072` current-hub-set sentence at the same time.

---

### N4-M-1 (MEDIUM, gate-blocking) — the bounded-retry terminal now reaches the deployed WebFinger read path, and no candidate states the resulting terminal or HTTP status

**Repository owner: selfsame (NFR-921, TEST-1142) and cbcl-bus (NFR-019,
TEST-101).**

`NFR-921` at
`specs/trajectory/SPEC-008/spec-008-0.4.19-singular-authority-and-webfinger-order.md:152-155`
closes the lock section:

> Every affected transaction runs at Mnesia level 1. It uses an explicit
> integer `Retries >= 2`. Nested transactions are prohibited. Consumers map
> `nomore`, exhausted `#cyclic{}`, and `nested_transaction` to
> `authority-busy`.

`TEST-1142` at `:311-321` names nine WebFinger operations and then requires
`authority-busy` on bounded retry exhaustion. Cbcl-bus `NFR-019` at `:159-163`
and `TEST-101` at `:269-279` carry the identical text.

That obligation now reaches a shipped public surface for the first time.
`cbcl-chat-webfinger-store:lookup/1` at
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:314-320` answers
`'unavailable` for every non-atomic transaction result. Its only production
consumer is `apps/cbcl_chat/src/shell/cbcl-chat-webfinger-http.lfe:27-38`,
whose dispatch is exact:

```text
:36   ('unavailable (empty 503 req state))
:37   (_            (empty 404 req state))
```

`authority-busy` appears nowhere in the cbcl-bus source. A repository search at
the declared baseline over `apps/` returns no hit, so it is specification
vocabulary introduced for the authority lane in 0.16.4.

Two readings follow and the text decides neither. Under the first reading the
WebFinger transactions are affected, so they answer `authority-busy`. The
handler then falls through to `:37`. Transient contention therefore returns
`404` on a live binding. Under the second reading they are exempt, so
`mnesia:transaction/1` at `:129`, `:201`, `:297`, and `:317` keeps its infinite
retry. The graph is then not level-1 bounded.

The first reading changes a resolver-visible answer. The module header at
`:14-20` states that a verifier performing the `CON-206` step-9 lookup is what
makes withdrawal bite. A `404` is the answer that says the binding does not
exist, and a `503` is the answer that says to retry. Both fail closed, so this
is a correctness and gate defect rather than a bypass.

The three other WebFinger entry points have no production caller today, which
`CON-965` item 5 at `spec-008-0.4.9-standalone-tofu-first-contact.md:277-278`
already records. `lookup/1` is the exception, and it is the one named in both
operation lists.

**Minimum sound correction.** State the WebFinger exhaustion terminal and its
HTTP status in `NFR-921` and `NFR-019`, or exempt the WebFinger lane from the
`authority-busy` mapping by name. Add the chosen result to both operation
lists.

---

### N4-L-1 (LOW) — the pre-derived alias rule forces a new arity, and both operation lists still name `tombstone/2`

**Repository owner: selfsame (NFR-921, TEST-1142) and cbcl-bus (NFR-019,
TEST-101).**

`NFR-921` at `:132-136` states the new withdrawal shape:

> `withdraw/2` derives the requested alias URI from the named home DID before
> the transaction. Its live-binding branch takes that exact alias write lock
> first.

`withdraw/2` at `cbcl-chat-webfinger-store.lfe:199-204` wraps
`(tombstone account home-did)` in one transaction. A URI derived before the
transaction has to reach `tombstone`, so the natural implementation is
`tombstone/3`. `TEST-1142` at `:311-313` and cbcl-bus `TEST-101` at `:269-271`
both name `cbcl-chat-webfinger-store:tombstone/2`.

A static graph generator keyed on the named arity finds no such function after
the mandated change. The alternative is a second derivation inside the
transaction, which satisfies the letter and duplicates the hash.

I verified that the derivation itself is sound. `cbcl-core-webfinger:account-uri/1`
at `apps/cbcl_chat/src/core/cbcl-core-webfinger.lfe:35-40` is total: it answers
`'undefined` outside the 1 through 4,096 octet range. `provision/3` at `:127-128`
already fixes the alias as that exact derivation, so a live row always agrees.

**Minimum sound correction.** Name the arity the mandated change produces in
both operation lists, or state that the derivation happens inside the
transaction before the first lock.

---

### N4-L-2 (LOW) — the reinstatement account lock names no table, reads no row, and carries no stated purpose

**Repository owner: selfsame (NFR-921) and cbcl-bus (NFR-019).**

`NFR-921` at `:142-145` states:

> `reinstate/2` omits the alias table. It takes the exact account write lock
> first. It then takes a whole `cbcl-webfinger-withdrawn` write lock before its
> first `index_read`.

`reinstate/2` at `cbcl-chat-webfinger-store.lfe:295-300` and `lift-tombstone/2`
at `:302-308` touch exactly one table today. The mandated account lock is
therefore new, it has no read behind it, and the surrounding text names its
table only by context.

The same requirement uses `stable account` for a different table in the
allocation paragraph at `:84-88`. The unqualified word therefore carries two
referents in one clause. `TEST-1142` at `:315-318` repeats it.

The lock is not needed for cycle freedom. Reinstatement and provision already
serialise on `cbcl-webfinger-withdrawn`, and the declared order places account
before withdrawal, so removing it creates no cycle either.

**Minimum sound correction.** Name `cbcl-webfinger-account` explicitly, and
state why reinstatement holds it. Otherwise drop the lock and state that
reinstatement takes only the whole withdrawal write lock.

---

### N4-L-3 (LOW) — the cbcl-bus amendment states two hub test sets in one document

**Repository owner: cbcl-bus (0.16.12 test-specification paragraph).**

`REQ-074` at
`specs/trajectory/SPEC-053/spec-053-0.16.12-singular-authority-and-webfinger-order.md:49-51`
states the exact current hub set including TEST-101 and TEST-102. The
unnumbered paragraph at `:243-245` states a different exact set:

> The exact survivor set is TEST-043 through TEST-065, TEST-068, TEST-070,
> TEST-093, TEST-094, and TEST-098.

The following sentence reconciles them, because the new tests replace only the
entries `REQ-074` names. A reader nevertheless meets two sentences that both
open with an exactness claim about one set. Selfsame `TEST-1143` at `:325-327`
requires exactly one cbcl-bus set, and a checker reading both repositories
finds two.

**Minimum sound correction.** Label the second paragraph as the pre-existing
survivor set rather than an exact set, or point it at `REQ-074`.

---

## Disposition of every 0.4.18 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N3-H-1** — four survivor ledgers and three coordinated review sets stay effective | **NOT CLOSED** | `CON-978` retires the `CON-971` and `CON-973` coordinated sentences and the 0.4.12, 0.4.13, and 0.4.14 test preambles. I checked every boundary string and each is unique inside its clause. `TEST-1143` widens the guard past contracts, which the prior review asked for. The audit still stops at 0.4.12: seven earlier test preambles, four earlier numbered gate items, and `CON-970` state competing ledgers, and one of them is open-ended. Refiled as `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-H-1]]`. |
| **N3-H-2** — `CON-976` is replaced with no clause preserving its four replacements | **CLOSED** | `CON-978` at `:169-180` restates all four by name. I traced each successor chain and every predecessor has one effective successor. NFR-918 leads to NFR-919, NFR-920, and NFR-921, and `CON-978` item 2 replaces all three in full. ADR-953 leads to ADR-956 by the same route. TEST-1129 through TEST-1133 keep their collective successor, and `CON-978` at `:249-250` names the current five. The `REQ-997` edge onto the `REQ-996` cap section survives outside `CON-978`, because `REQ-997` states it in its own body at `spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph.md:35-38`. I attempted to revive REQ-994, NFR-918, ADR-953, TEST-1129 through TEST-1138, TEST-1140, and TEST-1141, and every one has a current successor. |
| **N3-M-1** — the reinstatement order creates a cross-table cycle with two unnamed operations | **CLOSED** | `NFR-921` at `:118-145` names `provision/3`, `create-or-return/3`, `lookup/1`, `lookup-binding/1`, `withdraw/2`, the already-withdrawn branch, `reinstate/2`, and `lift-tombstone` with exact modes. One order now covers them all. I checked the deployed source: `create-or-return` at `cbcl-chat-webfinger-store.lfe:139-142` binds withdrawn, alias, then account, and `NFR-921` at `:125-126` requires that reorder by name. `tombstone/2` at `:206-229` reads account before `alias-agrees?/3` reads alias at `:233`, and the derived-alias rule reverses that pair. Under the corrected order every operation is a subsequence of alias, account, withdrawal, so the two cycles the prior review found are gone. `NFR-913` at `spec-008-0.4.11-standalone-tofu-first-contact.md:248` states the same finalizer order, so the normative matrix agrees. Residuals are filed as `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-M-1]]`, `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-L-1]]`, and `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-L-2]]`. |
| **N3-L-1** — the two withdrawal lock claims have no named test operation | **CLOSED** | `TEST-1142` at `:311-313` names `withdraw/2`, `tombstone/2`, and `already-withdrawn/2`, and `:315-318` requires alias-before-account ordering for live withdrawal. `NFR-921` at `:138-140` states the already-withdrawn modes: retained alias and account write locks, a whole-table read for the later `index_read`, and no later write lock. I verified both branches at the baseline. `tombstone/2` writes at `:222` and deletes at `:227-228`, and `already-withdrawn/2` at `:252-262` writes nothing. Cbcl-bus `TEST-101` carries the identical list. |
| **N3-L-2** — one leg of the pointer graph names the superseded candidate | **CLOSED** | `spec-001-0.4.8-split-room-shape-and-length.md:10` now reads `consumer-design: selfsame SPEC-008 0.4.19-draft` at `4f26844`. The only difference between `b02a3f5` and `4f26844` is that one line. `specs/SPEC-008-production-pairing-claimant.md:8` and `:17` name the 0.4.19 candidate, and `specs/SPEC-053-key-root-identity.md:15`, `:635`, and `:688-691` name the 0.16.12 candidate. `TEST-1143` at `:329-332` and cbcl-bus `TEST-102` at `:286-288` both require all three legs. |
| **N3-L-3** — one effective CON-975 sentence still requires a maximum sealed record | **CLOSED** | `CON-978` at `:189-191` retires that sentence by exact quotation, and the quoted text matches `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:297-298` after line unwrapping. The replacement obligation at `:257-258` reads that opaque cap-sized sealed offer and reverse records MUST transit both declared rendezvous implementations. I searched the whole chain for a surviving maximum label on the two caps. Every remaining hit sits inside a replaced clause: `0.4.16:124-129` is inside REQ-994, `0.4.16:352` is inside TEST-1131, `0.4.17:139-140` is inside the replaced REQ-996 cap section, `0.4.17:362` is inside TEST-1136, `0.4.12:181-184` is inside REQ-985, and `0.4.10:122-131` is replaced by CON-970 item 6. The one effective arithmetic clause labels correctly: `REQ-988` at `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:185-187` states that a recognizer ceiling above a reachable maximum is a cap and never a maximum. |

Four closed cleanly, one closed with three residual findings, one not closed.

## Disposition of every 0.4.17 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N2-H-1** — three Selfsame ledger clauses contradict each other | **NOT CLOSED** | The contradiction set changed membership and grew. `CON-971`, `CON-973`, and three preambles are now retired, and twelve earlier clauses remain. See `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-H-1]]`. |
| **N2-M-1** — reinstatement performs an unnamed upgrade | **CLOSED** | `NFR-921` at `:142-145` keeps the whole write lock before the first `index_read`. I re-verified implementability at the local OTP 28.5 install rather than from prose. `ixrlock/5` at `mnesia_locker.erl:1086-1102` calls `need_lock(Store, Tab, ?ALL, read)`, and `need_lock/4` at `:858-871` matches the stored pattern literally, so a held write entry does not answer a read pattern. `can_lock/4` at `:353-356` selects every write holder, and `can_queue(Tid, Tid, _, Res)` at `:410-411` returns the accumulator for the requester itself. The later exact delete issues no request, because `need_lock(Store, Tab, Key, write)` finds the stored table entry. |
| **N2-M-2** — the fresh-ceremony same-key re-grant test disappeared | **CLOSED** | `TEST-1142` at `:299-301` restates the case inline and requires current eligibility, capacity, both consent decisions, and a new verified grant. Cbcl-bus `TEST-101` at `:257-259` carries the identical paragraph, and `TEST-102` at `:291` requires every installed-state case inline. The governing rule verifies at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:200-203`, and `CON-978` keeps `REQ-995` effective. |
| **N2-M-3** — two recognizer caps were relabelled as maxima | **CLOSED** | `REQ-997` stays effective under `CON-978` at `:234`. Every figure re-derives against `REQ-988` at `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:178-180`. The reachable complete offer is 41,308 octets, so the residuals are 15,692 and 20,692 octets, the sealed cap bodies are 57,016 and 62,016 octets, and the transit residuals against 69,632 octets are 12,616 and 7,616 octets. The AEAD premise verifies at `crates/selfsame-core/src/seal.rs:143-165`, where `seal_offer` and `seal_bundle` call `encrypt` with a fixed role nonce and associated data that is authenticated rather than stored. The PROTO-002 cap verifies at `specs/PROTO-002-selfsame-rendezvous-v1.md:312-314` and `:716`. |
| **N2-L-1** — one `cbcl-sealedgrant` holder fell outside the declared scope | **CLOSED** | I re-enumerated every production holder at the declared implementation baseline rather than at the candidate commit. A repository search outside `test/` returns `cbcl-chat-invite.lfe`, `cbcl-chat-roommember.lfe`, and `cbcl-chat-sealedgrant.lfe`. `put-tx` at `cbcl-chat-sealedgrant.lfe:67-79` reaches `cbcl-chat-invite:peek-tx` at `cbcl-chat-invite.lfe:75-78`, which reads an exact key, then writes at `:73`. `get-tx` at `:94-101` reads at `:95` with the two-argument form and deletes at `:98`, which is today's upgrade. `NFR-921` at `:105-112` names both holders with exact modes. |

Four closed, one not closed.

## Disposition of every 0.4.16 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N-H-1** — the consent-surface completeness rule is inverted | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:117-119` reads that no filter, paging, collapse, or lazy rendering SHALL hide a room. That sentence sits outside the `REQ-997` replacement range, which starts at "The maxima in". The inverted 0.4.16 sentence at `:110` is inside REQ-994, and `CON-978` at `:171-172` keeps REQ-994 replaced. The shipped target verifies: `affectedChannelsNow()` at `apps/cbcl_chat/priv/web/app.js:602-603`, call sites at `:6409` and `:6443`, and the DOM query at `:603`. |
| **N-M-1** — the v1 trace omitted four version sites | **CLOSED** | The fifteen-line trace sits in the unreplaced part of `REQ-996` at `:158-176`. I read every line at the declared baseline and each resolves to the stated construct. I then ran an independent exhaustive scan over `crates/**/*.rs` for the seven version spellings. Every omitted production hit is a field declaration, a member-name constant, a comment, a constant definition, a format template, or a runtime pass-through. The three files whose omitted literals sit in tests verify: `#[cfg(test)]` opens at `crates/selfsame-app-identity-net/src/profile.rs:221`, `crates/selfsame-beam/src/enrollment.rs:199`, and `crates/selfsame-web-device/src/lib.rs:1521`. |
| **N-M-2** — the acquisition point and the `revoke-handle-tx` upgrade were dropped | **CLOSED** | `NFR-921` at `:100-103` restores both sentences and `:147-150` retains the opposite-order clause. Every named site verifies at the baseline. `cbcl-chat-roommember.lfe:155` reads with the two-argument form then writes at `:161`, and `cbcl-chat-account-roommember.lfe:67` does the same then writes at `:73`. `revoke/2` at `cbcl-chat-roommember.lfe:242-246` opens one transaction that deletes an exact key then calls `revoke-handle-tx`, which index-reads at `cbcl-chat-account-roommember.lfe:207` and deletes at `:208`. `migrate-legacy-handle-tx` at `:139-140` takes both whole write locks in the declared order before its `foldl`. |
| **N-M-3** — the twelve cycles used symbols whose legend was deleted | **CLOSED** | `NFR-921` at `:68-71` carries the complete legend and `:76-79` carries the unchanged twelve-cycle block. The binding to `NFR-913` stays bijective, because that matrix at `spec-008-0.4.11-standalone-tofu-first-contact.md:245-253` has exactly seven rows and `unratified read` is its only read-only row. Cbcl-bus `NFR-019` at `:75-86` carries the identical legend and cycle set. |
| **N-M-4** — the parent pointer had no target | **CLOSED** | `specs/SPEC-008-production-pairing-claimant.md:8` names the 0.4.19 candidate, `:9` carries the open candidate gate, and `:16-20` keeps the implemented 0.2.2 baseline distinct. The cbcl-bus parent resolves at `specs/SPEC-053-key-root-identity.md:15`, with `DEC-012` at `:635` and body references at `:688-691`. |
| **N-L-1** — `socketGeneration` had no definition | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:83-96` states creation, capture, cancellation, and reconnect semantics. That block is outside the `REQ-997` range, and `TEST-1134` remains effective without amendment. |
| **N-L-2** — two different definitions of one shared grammar production | **CLOSED** | I extracted the six-line grammar block from all three candidates and hashed it. The cbcl-pairing block at `spec-001-0.4.8-split-room-shape-and-length.md:33-40` hashes to SHA-256 prefix `f01037367e6f7b65`, which matches the recorded Selfsame and cbcl-bus value. The alphabet matches `is_symbol_char` at `crates/cbcl-parser/src/parser.rs:328-342` in cbcl-rs `febc669`, character for character. |
| **N-L-3** — the transit ledger omitted the in-repository baseline | **CLOSED** | `CON-978` at `:259-262` names both baselines. `crates/selfsame-rendezvous/src/lib.rs:48` reads `pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`. Did-crdt `src/service/rendezvous.rs:65` carries the same constant at `1f409a4`. |
| **N-L-4** — the sealed-record maximum was never derived | **CLOSED** | `REQ-997` derives every figure and residual in one block, and the derivation re-computes exactly. See the `N2-M-3` row above. |
| **N-L-5** — one live `cbcl-invite` lock fell outside both graph scopes | **CLOSED** | `NFR-921` at `:84` owns every `cbcl-invite` lock, `:106-108` names `put-tx` and its exact invite read, and `:114-116` places `cbcl-invite` first in the generated table order. Both halves verify at `cbcl-chat-sealedgrant.lfe:67-79` and `cbcl-chat-invite.lfe:75-78`. |

Ten closed. The `N-H-1` closure is no longer at risk, because
`[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-H-2]]` does not
touch replacement ancestry.

## Disposition of every 0.4.15 finding

| ID | Disposition | Evidence |
|---|---|---|
| **H-1** — unsigned advancing signal with no defined correlation | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:72-101` states the correlation member, its 32 random octets, its base64url encoding, and its one-use consumption. It states plainly that the frame is unsigned and origin-less. That block is outside the `REQ-997` range, and `TEST-1134` remains effective. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **CLOSED** | Both sites are present at `crates/selfsame-app-identity/src/enrollment.rs:373` and `:662`, and `:452` carries its label. My independent exhaustive scan found no missing production v1-pinned site. See the `N-M-1` row. |
| **M-2** — `write-grant` read-then-write upgrades | **CLOSED** | `NFR-921` at `:90-92` requires the three-argument read with `write` on the first read in both paths. Both deployed sites carry the upgrade today at `cbcl-chat-roommember.lfe:155` and `cbcl-chat-account-roommember.lfe:67`. |
| **M-3** — maxima never checked against the implemented rendezvous | **CLOSED** | `CON-978` at `:257-261` states the dependency on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. Both anchors resolve: `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:312-314` sets 69,632 bytes, and `CON-304` at `:716` and `:733` sets a `1..69632` body with `413` above it. The reference implementation's own non-conformance list at `:1016` records the 4,096-byte body. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` remains effective and states the terminal at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:185-187`. `TEST-1142` at `:295-297` requires the terminal and zero enrollment frames. The independent hub-side guard is real at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`, which aborts with `'account-upgrade-required`. |
| **L-1** — survivor set revives `TEST-057` | **CLOSED** | `CON-978` at `:242-245` states that TEST-057 and TEST-058 do not survive, and the 0.4.14 preamble that revived TEST-057 is now retired by name. I checked every surviving preamble in the chain and none reaches TEST-057. The TEST-050 disagreement is separate and is filed inside `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-H-1]]`. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED** | `REQ-995` restates the unlink action in full at `:194-198`. Cbcl-bus `TEST-101` states every installed-state case inline, and `TEST-102` requires that inlining. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:53-63` orders the four classes and states that length classification occurs only after `RoomShape` passes. Cbcl-pairing `REQ-030` and cbcl-bus `REQ-071` carry the same order. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` remains effective and splits the triggers at `:174-183`. The silent path cannot widen consent, because `REQ-981` keys the person-owned policy on the exact pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-95`. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-996` at `:126-135` keeps `migration-display-state-changed` non-terminal and `migration-plan-invalid` terminal. Both remain implementable against the shipped reducer, whose `Burned` branch returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:172-174`. |

Ten closed.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-978` names six current replacements and `REQ-981` is not among them. `REQ-981` still fixes the policy key as the exact application and relay pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-95`, and it still states that a v1 policy entry cannot suppress the first v2 prompt. Cbcl-bus GATE-04 retains the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched. `crates/selfsame-pairing/src/live.rs:479-481` commits one explicit local decision only after `LiveEffect::DisplayIntent`. The five pre-consent checks in `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:108-114` are outside the `REQ-997` range and remain ordered. No revival risk survives, because `CON-978` restates the four `CON-976` replacements by name. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-978` names nothing in `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202-231`. `REQ-995` still states that local unlink cannot re-grant authority and that a new grant needs both consent decisions. `TEST-1142` and cbcl-bus `TEST-101` state the re-grant case inline. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-978` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:903` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically. The `REQ-996` snapshot list adds no enrolment precondition, and `REQ-984` keeps the no-transition shape legal. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-025` at `spec-001-0.4.8-split-room-shape-and-length.md:96-99` preserves the no-relay-field property of `CON-024`. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at `src/cbcl_protocol.rs:34` and `MAX_FRAME_BODY_BYTES = 69_632` at `src/mailbox.rs:15` both sit above the 62,016-octet sealed cap body. Cbcl-bus GATE-04 retains the human-acceptance box for relay-visible size correlation. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:330-331` states that mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery. Every negative path this amendment keeps or adds creates nothing. The WebFinger terminal ambiguity in `[[spec-008-0.4.19-claude-adversarial-review-2026-08-23#N4-M-1]]` fails closed in both readings, and no finding in this review lets an endpoint failure authorise. |

## Repository assessments

### selfsame SPEC-008 0.4.19-draft (`d23dfca`)

Artefact numbers are sequential with no gap and no reuse. `REQ-998`, `NFR-921`,
`CON-978`, and `ADR-956` each advance their series by one, and `TEST-1142` and
`TEST-1143` follow `TEST-1141`.

`CON-978` is the strongest part of the amendment. It restates the four
`CON-976` replacement relationships by name, in the form the prior review asked
for. It also preserves the two earlier retirement ranges intact. Every
predecessor in the chain now has one current successor.

`NFR-921` gives every WebFinger path one implementable order, and that order
agrees with the normative matrix in `NFR-913`. I verified the lock semantics in
the local lock manager rather than from prose.

`REQ-998` is where the amendment fails. It declares a resolver over every
effective clause from 0.4.4 onward, and the retirement work reaches 0.4.12.
Twelve earlier clauses therefore remain, and the amendment's own static check
fails against them.

The candidate carries 29 distinct wikilink targets. A `zetl check --dead-links`
run over this vault reports exactly one dead target from it, which is
`[[SPEC-053-key-root-identity#DEC-012]]`. That is a cross-repository pointer
whose target lives in cbcl-bus. Under [[PROTO-001-usdd-agent-protocol]] a dead
link is visible debt rather than an error, and I verified the target by hand at
`d23b873`. The same run reports one dead target from this report, which is the
protocol page itself.

Open against it: `N4-H-1`, a share of `N4-M-1`, and shares of `N4-L-1` and
`N4-L-2`.

### cbcl-bus SPEC-053 0.16.12-draft (`d23b873`)

`NFR-019` and `TEST-101` are textually identical to their Selfsame twins apart
from identifiers and one added load-evidence sentence. I diffed both pairs and
found no substantive divergence.

The GATE-04 restatement holds 42 boxes by mechanical count, which matches
`TEST-102` and Selfsame `TEST-1143`. I diffed the 0.16.11 and 0.16.12 box lists
and only two boxes differ: the coordinated-set version and the graph scope
word. Every box remains open and the amendment states that it closes none.

`REQ-074` adopts the preservation sentence its predecessor introduced, and it
does not adopt the retirement discipline its Selfsame twin used. That is the
whole of `N4-H-2`.

Open against it: `N4-H-2`, `N4-L-3`, and shares of `N4-M-1`, `N4-L-1`, and
`N4-L-2`.

### cbcl-pairing SPEC-001 0.4.8-draft (`4f26844`)

One line changed since the 0.4.18 round, and it is the `consumer-design`
pointer the prior review asked for. Everything else is byte-identical.

`REQ-030` carries the `RoomShape` split, and its grammar block is byte-identical
to both twins. `CON-025` keeps the hub as the sole owner of the three admission
terminals and preserves every `CON-024` property. The `CON-025` survivor set
matches the Selfsame `CON-978` set exactly.

This chain carries exactly one survivor sentence, which is the property both
other chains lost. Nothing is open against it.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged. `src/service/rendezvous.rs:65`
carries the 4,096-byte cap, `:68` carries a 600-second TTL, and `:79` carries a
65,536-slot ceiling. The working tree carries user-owned uncommitted paths,
which I ignored in favour of the declared commit.

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Whole-table write lock followed by `index_read`.** Verified at the local OTP
  28.5 install rather than from prose. `need_lock/4` at
  `mnesia_locker.erl:858-871` matches the stored pattern literally, so a held
  write entry does not answer a read pattern. `ixrlock/5` at `:1086-1102`
  therefore issues its request, `can_lock/4` at `:353-356` selects the
  transaction's own write lock, and `can_queue(Tid, Tid, _, Res)` at `:410-411`
  returns it as compatible. Write-then-read is not an upgrade.
- **Exact write after a whole-table write.** `wlock/4` at `:826-843` calls
  `need_lock(Store, Tab, Key, write)`, which finds the stored table entry and
  answers `no`. The delete inside `lift-tombstone` issues no request.
- **The pre-derived alias rule against every existing store test.** I traced all
  four withdrawal outcomes. A stale generation still refuses at
  `cbcl-chat-webfinger-store-tests.lfe:318-319`, because the stored DID check
  runs first. The stray-account fixture at `:338-359` still answers
  `inconsistent-binding`. The derived alias equals the stored alias there, and
  `alias-agrees?/3` still compares the alias row's account field. An
  unknown account at `:257-260` still answers `unknown-account`. The corrupt
  case where the stored URI disagrees with the derived URI is new, and it
  refuses without locking the other alias.
- **Derivation totality.** `cbcl-core-webfinger:account-uri/1` at
  `cbcl-core-webfinger.lfe:35-40` answers `'undefined` outside 1 through 4,096
  octets, so a malformed home DID cannot raise outside a transaction.
- **WebFinger cycle freedom under the new order.** Provision takes alias,
  account, withdrawal. Lookup takes alias then account. Live withdrawal takes
  alias, account, withdrawal. The already-withdrawn branch takes alias, account,
  then a whole-table read. Reinstatement takes account then a whole-table write.
  Every operation is a subsequence of one order, so no cross-table cycle remains.
- **LFE binding order.** The three `create-or-return` locks depend on
  initialiser order, and the deployed order is withdrawn, alias, account.
  `NFR-921` requires that reorder by name.
- **WebFinger table scope.** A repository search at the declared baseline
  returns exactly two production modules touching the three tables:
  `cbcl-chat-webfinger-store.lfe` and `cbcl-chat-webfinger-http.lfe`. No
  authority-lane transaction touches them, so no cross-lane cycle exists today.
- **Transit arithmetic and carriers.** Sealing adds only the 16-octet tag. Both
  cbcl-pairing ceilings sit above the 62,016-octet sealed reverse cap body. Only
  the rendezvous lane is undersized, and all three candidates say so.
- **Grammar drift between the pinned revisions.** The alphabet in the shared
  grammar block matches `is_symbol_char` exactly at the pinned cbcl-rs revision.
- **The deployed browser channel expression.** `path-b-ceremony.mjs:118` reads
  a pattern strictly narrower than the shared room alphabet, so the replacement
  obligation in `REQ-996` names a real defect.
- **Credential/v2 lane presence.** I searched `apps/` and `priv/` at the
  declared baseline for `selfsamepairallocate`, `migrationRooms`,
  `roomSetDigest`, `socketGeneration`, `migration-plan-invalid`, and
  `credential-v2-canonical`. All six return nothing. That is honest planned work
  rather than an unsupported release claim.
- **Standalone first contact without enrolment.** The `REQ-996` snapshot list
  names an enrolled key, and `REQ-984` keeps the no-transition shape legal. A
  first-contact account snapshots an empty room set and no member, so the clause
  adds no implicit enrolment dependency.
- **Fifteen version sites.** Every line in the trace resolves at the declared
  baseline to the stated construct, and my independent scan found no omitted
  production v1 pin.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and
   credential/v2 transport depends on it. Its own non-conformance list at
   `specs/PROTO-002-selfsame-rendezvous-v1.md:1016` records the reference
   implementation's 4,096-byte body.
2. **The whole credential/v2 lane is planned rather than shipped,** as the
   six-symbol search above records.
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt
   durable readiness, and the separate owner approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration,** and the cbcl-bus
   parent records that deferral as in force.
5. **The unary transaction call sites in `apps/` remain unbounded today.**
   `NFR-921` mandates a change rather than describing current behaviour, and
   that change surfaces a busy result on writes that previously blocked.
6. **The typed cbcl-pairing display is a design.** Its two named types are
   absent from `src/`, which both the amendment and the prior reviews record.
7. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** Several artefact references in the
   amendment bodies are also plain text rather than wikilinks. The parent
   [[SPEC-008-production-pairing-claimant]] carries both blocks. This is an
   inherited condition of the amendment form rather than a defect this candidate
   introduces, and no prior review filed it.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation
plan. It authorizes no code, no public allocation, no gate opening, no release,
no deployment, and no production enablement. Every GATE-04, `GATE-00`,
`GATE-01`, and `GATE-03` box stays open. Every human review, every durability
obligation, and the separate owner approval for production deployment also stay
open and unaffected.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N4-H-1 | selfsame | Retire the 0.4.5 through 0.4.11 test-specification paragraphs, the four gate-sequencing coordinated sentences, and the `CON-970` hub-set paragraph, each by name. State the TEST-050 exception in the same place. |
| N4-H-2 | cbcl-bus | Retire the `REQ-072` coordinated-review sentence by name in `REQ-074`, and retire or restate its current-hub-set sentence. |
| N4-M-1 | selfsame, cbcl-bus | State the WebFinger exhaustion terminal and its HTTP status, or exempt the WebFinger lane by name, and add the chosen result to both operation lists. |
| N4-L-1 | selfsame, cbcl-bus | Name the arity the pre-derived alias rule produces, or state that the derivation happens inside the transaction before the first lock. |
| N4-L-2 | selfsame, cbcl-bus | Name `cbcl-webfinger-account` explicitly in the reinstatement clause and state why the lock is held, or drop it. |
| N4-L-3 | cbcl-bus | Label the 0.16.12 test-specification paragraph as the pre-existing survivor set, or point it at `REQ-074`. |
