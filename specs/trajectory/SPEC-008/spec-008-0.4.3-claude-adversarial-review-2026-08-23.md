---
id: spec-008-0.4.3-claude-adversarial-review-2026-08-23
title: SPEC-008 0.4.3-draft — Tier-1 fresh-context cross-model adversarial review
status: complete
tier: 1
verdict: REJECT
reviewer: independent fresh-context reviewer (defect-finding mandate, no access to the authoring conversation)
reviewed-artifact: "[[spec-008-0.4.3-standalone-tofu-first-contact]]"
reviewed-baselines: "selfsame ea2773fc1f76865c517521d599412e3351f925d7 (non-spec tree byte-identical to c6af6df75d033d2e44ae367457c1a754ba2c4c3a); cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
prior-review: "[[spec-008-0.4.2-claude-adversarial-review-2026-08-23]]"
date: 2026-08-23
---

# Verdict: REJECT

Three HIGH findings and six MEDIUM findings are unresolved. Each MEDIUM makes a
normative claim false, unimplementable, or a release-blocking test unpassable.
Under the stated bar — zero unresolved CRITICAL or HIGH, and no unresolved
MEDIUM that makes a normative claim false, unimplementable, insecure, or
untestable — this document does not authorise test-first planning.

Severity was not lowered anywhere to reach a passing count. Two of the three
HIGHs are single-sentence corrections; size of fix is not severity.

## Confirmed repository revisions

| Repository | Revision at review time | Difference from the candidate's "Reviewed baselines" table (`candidate:104-109`) |
|---|---|---|
| `selfsame` (this worktree, branch `circus/spec008-v043-review/1`) | `ea2773fc1f76865c517521d599412e3351f925d7` — *spec: close standalone pairing review findings*; clean tree | The candidate names `e699dfb` over code parent `c6af6df75d033d2e44ae367457c1a754ba2c4c3a`. `ea2773f`'s parent **is** `e699dfb`. `git diff --name-only c6af6df ea2773f | grep -v '^specs/'` is **empty**: the five changed paths are all `specs/trajectory/SPEC-008/*.md`. The reviewed code is therefore byte-identical to the named parent. **No material difference.** |
| `cbcl-pairing` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b`; untracked `.DS_Store` / `.zetl` only | Matches. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a`; clean | Matches. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; working tree carries an uncommitted edit to `specs/did-crdt-spec.md` and an untracked `docs/adr/ADR-007-xid-interoperability-posture.md` | Code matches. The two uncommitted files are documents and change no route, handler, or type traced below. **Recorded, as the candidate requires.** |

All four baselines are the ones the 0.4.1 and 0.4.2 reviews verified, so every
code claim here is directly comparable with both records.

The candidate's four "load-bearing observed behaviour" cells were re-verified
independently and are all accurate: `HomeKey::home_did` is pure
(`crates/selfsame-app-identity/src/hierarchy.rs:178-182`); `issuer::create` is
the first signer (`crates/selfsame-app-identity/src/issuer.rs:135-142`);
`selfsame-app-identity-net` exports no publication function
(`crates/selfsame-app-identity-net/src/state.rs` exports only
`path_b_union_revocations` and `nif_closures` at the top level);
`SessionPerformative` has exactly four variants and a one-way payload
(`cbcl-pairing/src/cbcl_protocol.rs:97-106`); `application-payload.body` is
`1..64512` (`cbcl-pairing/schemas/pairing-v1.cddl:158`); `enrol-allocator.mjs:60-62`
mints a per-ceremony device seed and `:74-80` POSTs caller octets to the signing
route; `provision/3` has no non-test caller; `pairaccount-standing-reply` carries
no ceremony correlation (`cbcl-chat-session-ws.lfe:1667-1678`); `build_router`
has no `CorsLayer` (`did-crdt/src/service/server.rs:141-160`); `create_did` does
an unconditional `state.docs.lock().insert(...)` (`handlers.rs:98`); and
`submit_delta` 404s on an unknown DID (`handlers.rs:292-295`).

## Executive rationale

0.4.3 is a large and mostly honest repair of 0.4.2. Measured against the five
HIGH and seven MEDIUM corrections the prior review demanded, I confirmed against
source that it closes **H-2, H-3, H-4, H-5, M-2, M-3, M-4, M-6(interpretation),
M-7, L-1, L-3, L-4 and L-5** and improves **H-1, M-1 and M-5** without closing
them. Specifically:

- **H-2 closed and closed well.** The BIP-39 collision is gone. `PAIR1-` plus 55
  Crockford Base32 characters is structurally disjoint from a twelve-word
  English phrase, the mutual-rejection rule runs in both directions
  (`REQ-933:209-214`), and the bit arithmetic is exact: `C(128) || T(128) ||
  checksum16(16) || 000` = 275 bits = 55 × 5 ✓.
- **H-3 closed.** `REQ-936:317-320` states the two keys as distinct roles and
  adds a proof of possession over ceremony/carrier/principal/offer digests. This
  now satisfies SPEC-004 REQ-215 (`specs/SPEC-004-…:974-983`) rather than
  contradicting it, since scope is 1:1 with `(applicationId, applicationAccountId)`.
- **H-4 closed.** `CON-931:831-838` defines the missing authenticated,
  account-keyed authority query, forbids the false-`NoBinding` webfinger probe
  by name, and makes all four states reachable.
- **M-2 closed, and by exactly the right mechanism.** `CON-924:638-643` binds
  the allocator's *release-embedded* profile digest into CPace. Because the
  allocator is the party holding `C`, this is literally the property
  `CON-220` step 6 asks for (`specs/SPEC-004-…:4138-4143`, "the record digest —
  asserted by a party holding `C`"). The circularity the prior review found is
  broken.
- **M-3 closed.** `CON-930:813-827` replaces the facade the prior review showed
  could not express the split. I confirmed the old defect is real
  (`crates/selfsame-web-device/src/lib.rs:1013-1015` requires
  `ClosureSource::StateResolver`, which `path_b.rs:428` stamps unconditionally)
  and that a private-field `#[wasm_bindgen]` class with a self-consuming
  `finish` is expressible. I also confirmed the CON-206 1-8 / 9-13 split is
  coherent: step 9 is the alias gate and explicitly tolerates either order
  (`specs/SPEC-004-…:3146-3150`).
- **M-4 closed on its substance.** Policy leaves `SealedRoot`, the origin ledger
  gets its own installation key, and the passcode/presence ordering is stated.
  Only the atomicity assertion remains defective (M-5 below).
- **M-7 closed and honest.** `REQ-939:408-415` states the bootstrap exception
  accurately. I verified `Document::new` returns `SignedDelta::unsigned`
  (`did-crdt/src/core/document.rs:345`) and that `selfsame_core::identity::
  sign_genesis` reuses the same op and `HlcTimestamp::default()`
  (`crates/selfsame-core/src/identity.rs:125-136`), so the content hashes agree
  and the wallet's two post-genesis deltas parent correctly.
- The **C-1 effect gate** and the **C-2 co-presence binding** from 0.4.1 remain
  closed and are, if anything, tighter.

It is nevertheless not adoptable as written.

**Three HIGH findings.**

1. **The M-1 anti-crowding closure is false as specified.** `REQ-933:207` says
   in terms "This is an additive relay command; v1 `claim` is unchanged." The v1
   `claim` carries no secret (`pairing-v1.cddl:29-32`), reaches any mailbox by
   locator from any connection (`cbcl-pairing/src/relay.rs:241-242, 438`), and a
   second distinct claim destroys the mailbox (`src/mailbox.rs:575`). The
   locator is in the public carrier in the clear (`CON-924:618`). Every observer
   of the QR therefore retains the free, deterministic ceremony kill that M-1
   named. `TEST-998:1103-1105` asserts the opposite of what `REQ-933:207`
   requires.
2. **The retained 120-second offer window and REQ-940's own deadline table are
   mutually incompatible on the success path.** `REQ-940:434` permits 180 s of
   idle waiting for `claim-v2` and `:435` a further 30 s for CPace, all of it
   *after* the hub has signed the offer under `CON-929:770-778`. `:443-446` then
   requires the 120-second CON-214/CON-219 window to be evaluated once,
   immediately before authorisation. `MAX_OFFER_WINDOW_SECONDS = 120`
   (`ceremony.rs:76`) is a grammar constraint on the issued window, not a
   configurable, and no clause widens it. A person who takes 130 seconds to
   transcribe 55 characters and complete a 64 MiB Argon2id unlock is refused,
   deterministically, every time.
3. **A parent specification's hard stops are neither listed nor dispositioned,
   and one of them is substantively amended.** `candidate:8` names
   "cbcl-pairing SPEC-001 REQ-002/NFR-002" in `amends:`; REQ-002 is a declared
   hard stop (`cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:1497`). The
   "Inherited hard-stop disposition" table (`candidate:134-154`) contains no
   cbcl-pairing row at all, and `candidate:1258-1260`'s coordinated-approval
   bullet omits both the cbcl-pairing and the did-crdt owners the frontmatter
   itself names. Substantively, `REQ-933:202-205` changes **REQ-005** ("become
   terminal on a third distinct claim", also a hard stop, `:1498`; text at `:316-320`) and
   `REQ-933:199-207` plus `NFR-905:541-547` change **REQ-003** ("no relay
   endpoint, message, phase, parser, store, limiter, reaper, or deployment
   change"). Neither is named anywhere.

Findings 1 and 3 interlock, which is the sharpest result of this review: the
minimum correction for HIGH-1 — make a v2 mailbox refuse the unauthenticated v1
`claim` — is precisely what completes the REQ-005 hard-stop amendment. **Under
either choice one of the candidate's normative claims is false today.**

Six MEDIUMs compound these. Two are executable arithmetic errors I reproduced
with an encoder model: the large-class relay body is **66,696**, not the 66,694
that `NFR-905:526` and `TEST-997:1093` assert as "executable encoder witnesses";
and the v2 application object is 64,558 rather than 64,560 for the
`logicalLength = 1` case `TEST-997:1090-1091` names. One is a minted grammar
that contradicts the implementation it claims to codify (`operatorId text
1..128` versus `[a-z0-9][a-z0-9-]{0,62}`). One leaves the version-1 branch of
the new dispatcher, the shipped production profile, and cbcl-bus SPEC-075
REQ-007 in a three-way contradiction. One prescribes a re-key that would
permanently block legitimate re-linking. One asserts crash-atomicity the named
store does not provide.

None is fatal to the architecture. The design's central choices — public carrier
plus a separately transcribed human secret in CPace, an independent anti-crowding
token, application-session provenance for the account label, one preliminary
consent and one final effect gate, a stable hub account principal with a distinct
device key, an opaque raw-closure WASM boundary, and a non-negotiated v2 dialect
inside unchanged transport bounds — all survive review. A 0.4.4 that makes these
corrections is a strong PASS candidate. This one is not.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct.

`candidate:NNN` references are to
`specs/trajectory/SPEC-008/spec-008-0.4.3-standalone-tofu-first-contact.md`.
All other `file:NNN` references are to the confirmed baselines above.

**There are no CRITICAL findings.**

---

### H-1 (HIGH) — Any observer of the public QR still crowds and destroys the mailbox, because `REQ-933` leaves the unauthenticated v1 `claim` reachable on a v2 mailbox, and its own test asserts the contrary

**Exact candidate clauses.** `REQ-933:199-207`:

> "The allocator gives the relay `claimCommitment = SHA-256("cbcl-pairing v2
> claim" || locator || T)` when allocating the mailbox. … A claimant presents
> `T` over TLS in the `claim-v2` command; the relay compares its digest in
> constant time before creating claimant membership, crowding, consuming, or
> otherwise changing the mailbox. … **This is an additive relay command; v1
> `claim` is unchanged.**"

Disposition table `candidate:120`: "**M-1 — public carrier can crowd the
mailbox** | … claim changes no mailbox state until `T` verifies."
`NFR-905:547`: "No v1 mailbox or command changes."
`CON-924:618`: the carrier carries `locator  direct 32-octet mailbox id`.

**Governing clauses.** `SPEC-007…#REQ-811` (retained at `candidate:148`):
failure, decline, **crowding**, cancellation, expiry and success all leave the
invitation consumed. `cbcl-pairing SPEC-001 REQ-005`
(`cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:316-320`): "The mailbox SHALL
allocate at most two memberships and become terminal on a third distinct claim."

**Code evidence.**

- `cbcl-pairing/schemas/pairing-v1.cddl:29-32`:

  ```cddl
  claim = {
    "type": "claim",
    "locator": locator
  }
  ```

  No secret, no commitment, no token. The locator alone suffices, and the v2
  carrier publishes it.
- `cbcl-pairing/src/relay.rs:241-242` dispatches `ClientMessage::Claim(locator)`
  from **any** connection; `:438` submits `MailboxCommand::Claim {
  claimant_hash }` where `claimant_hash` comes from the server's own
  `randomness.membership_token`, not from anything the client proves.
- `cbcl-pairing/src/mailbox.rs:552-577`:

  ```rust
  match &next.claimant {
      None => { /* installs the caller as claimant, status = Paired */ }
      Some(claimant) if claimant.hash == claimant_hash => Err(MembershipCollision),
      Some(_) => Ok(next.terminate(CloseReason::Crowded)),
  }
  ```
- `cbcl-pairing/src/relay.rs:467-470` broadcasts `Closed(Crowded)` to every
  member on that transition.
- `cbcl-pairing/src/endpoint.rs` burns the invitation on relay termination.

**Failure trace.**

1. The signed-in page displays the v2 QR. Under this design that display is safe
   by construction, so it appears on a projector, in a screen share, in a
   support screenshot, or within camera range — exactly as ADR-917 intends.
2. An observer decodes the carrier. No secret is required; `claim-commitment` is
   a public field and `locator` is in the clear.
3. The observer opens a TLS socket to `relay-origin` and sends
   `{"type":"claim","locator":[0,<mailbox-id>]}`. This is a **v1** command, and
   `REQ-933:207` says v1 `claim` is unchanged, so the relay accepts it.
4. If the observer claims first, `mailbox.rs:558-568` installs them as claimant
   and the mailbox is `Paired`. The legitimate wallet's `claim-v2` then finds a
   different claimant present — the wrong-`T` path is irrelevant, membership is
   already taken — and the ceremony cannot proceed.
5. If the wallet claimed first, the observer's bare `claim` hits `:575` and the
   mailbox is `terminate(Crowded)`. Both members receive `Closed(Crowded)`.
6. Either way REQ-811 leaves the invitation consumed, `REQ-933:221-222` burns
   `C`, the offer, the request and the ceremony, and the person must obtain a
   fresh carrier **and retype a fresh 55-character PAIR1 code**. The observer
   repeats at zero cost; the relay limiter is keyed on the observer's peer
   address (`cbcl-pairing/src/limiter.rs:62-70`, `relay.rs:214`) and a rotating
   source defeats it.
7. `TEST-998:1103-1105` requires "An observer holding only the public carrier
   cannot change, claim, crowd, or terminate the mailbox. Wrong `T` and unknown
   locator are observationally equal." Against a conforming implementation of
   `REQ-933:207` this row **fails**, and it also fails the distinguishability
   half: a bare `claim` on an unclaimed v2 mailbox returns `Claimed{…}` while an
   unknown locator returns an error, so the observer can enumerate live mailboxes.

Because the design deliberately makes the carrier public and deliberately
requires "a forwarded QR without the code" to be harmless-but-failing, this is
not an edge case; it is the design's own advertised threat scenario reaching the
one relay command that has no authentication.

**Minimum correction.** State in `REQ-933` that a mailbox allocated with a
`claimCommitment` is a v2 mailbox and that the v1 `claim` command against it is
refused with the same indistinguishable response as an unknown locator, changing
no state; and that after a successful `claim-v2` any further `claim` or
`claim-v2` on that mailbox is likewise a no-op refusal rather than a crowding
terminal. Then disposition `cbcl-pairing SPEC-001 REQ-005` explicitly (see H-3),
because that sentence *is* the REQ-005 amendment. Reword `NFR-905:547` from "No
v1 mailbox or command changes" to state precisely what does change: a new
optional per-mailbox commitment field, a new command, and a new refusal rule for
the v1 command on v2 mailboxes. Add a TEST-998 sub-row naming the v1 `claim`
byte sequence explicitly, so the corpus cannot pass by only exercising
`claim-v2`.

---

### H-2 (HIGH) — The 120-second CON-214/CON-219 window and REQ-940's own pre-offer deadlines cannot both hold, so a conforming implementation refuses ordinary honest ceremonies

**Exact candidate clauses.** `REQ-940:429-441`:

> "One absolute deadline of allocation time plus 600 seconds bounds the ceremony.
> Each state also has the following monotonic idle deadline measured from entry:
> … | await authenticated `claim-v2` | 180 s | | CPace/Finished bootstrap | 30 s |"

`REQ-940:443-447`: "The CON-214/CON-219 120-second offer/evidence window is
evaluated exactly once, immediately before REQ-934 authorisation and request-ID
consumption; it is not reinterpreted as a deadline for subsequent human steps."

`CON-929:770-778`: "The browser requests v2 allocation only over its existing
signed chat session. The request names one generated ceremony/request ID,
**carrier digest**, intent nonce, selected profile/descriptor digest, and
requested permission set. The hub … constructs every other field from its
authenticated profile and **server clock**." `CON-929:792-793`: "One committed
ceremony produces at most one offer and is burned on **allocation** failure or
terminal outcome." `REQ-936:324-325`: "Scope creation, device-key registration,
and one-use ceremony **allocation** SHALL commit atomically or return no offer."

`REQ-932:184-186`: "The wallet requests passcode and OS presence once for this
policy access, after descriptor recognition and **before the relay socket**."

**Governing clauses.** `SPEC-004…#CON-219`
(`specs/SPEC-004-application-scoped-identity.md`): `expiresAt` is later than
`issuedAt` by at most 120 seconds. `SPEC-004…#CON-214` step 4: the 120-second
window is checked against the local clock. Neither is amended: `CON-929:780-783`
enumerates exactly four amended fields — `accountPrincipalDigest`,
`applicationAccountDisplay`, `carrierDigest`, `devicePossessionProof` — and none
of them is the window.

**Code evidence.**

- `crates/selfsame-app-identity/src/ceremony.rs:76`
  `pub const MAX_OFFER_WINDOW_SECONDS: i64 = 120;` enforced as a *grammar*
  constraint at `:402`: `if expires_at <= issued_at || expires_at - issued_at >
  MAX_OFFER_WINDOW_SECONDS`. The hub cannot mint a longer window and remain
  conforming.
- `crates/selfsame-app-identity/src/enrollment.rs:67`
  `MAX_EVIDENCE_WINDOW_SECONDS = 120`, enforced at `:429`.
- `crates/selfsame-app-identity/src/authorise.rs:170`
  `if now >= payload.core.expires_at { return Err(AuthoriseError::OfferExpired) }` —
  one check, at authorisation, exactly as the candidate specifies.
- `src-tauri/src/custody.rs:62-64`: `ARGON_MEMORY_KIB = 64 * 1024`,
  `ARGON_PASSES = 3`, `ARGON_LANES = 1`. The module's own comment says this
  "runs at most a few times a day"; `CON-925:687-688` now requires **three**
  such operations per ceremony, the first of them before the relay socket.

**Failure trace.**

1. `t = 0`. The browser calls the hub. Under `CON-929:770-793` and
   `REQ-936:324-325` this one atomic call allocates the ceremony, creates the
   scope, registers the device key and **returns the signed offer**, whose
   `issuedAt` is the hub's server clock and whose `expiresAt` is at most
   `t + 120`. The carrier and PAIR1 code are rendered.
2. The person picks up the phone, unlocks it, opens Selfsame, scans the QR,
   reads and accepts the unverified-origin prompt (`REQ-932:161-165`), waits for
   the CON-220 fetch, transcribes 55 Crockford Base32 characters into a field
   with no paste and no autofill (`REQ-933:209-212`), enters the passcode, passes
   the OS presence check, and waits out one 64 MiB × 3-pass Argon2id derivation
   for the policy read (`REQ-932:184-186`), possibly accepting a new-pair prompt.
3. `t = 130 s`. `claim-v2` is sent. This is **inside** the 180-second idle
   deadline of `REQ-940:434`, so no timeout fires.
4. `t = 134 s`. Both Finished values verify. Inside the 30-second bootstrap
   deadline of `:435`.
5. The allocator sends the offer. The claimant runs `REQ-934:233-236`'s complete
   CON-214 predicate. `authorise.rs:170` evaluates `now (134) >= expires_at
   (120)` → `OfferExpired`. `REQ-940:443-446` says this is the one and only
   evaluation, so nothing rescues it.
6. The ceremony refuses **before request consumption or display** — which is
   exactly what `TEST-1010:1210-1211` demands — and burns the invitation. The
   person retries and hits the same wall, because the wall is the pre-offer human
   path, not a transient.

The 180-second idle budget for `await authenticated claim-v2` is therefore
unreachable-in-success beyond roughly 115 seconds. A normative deadline table
that permits a duration guaranteeing the next state's failure is incoherent, and
`TEST-1008:1184-1193`, the release-blocking local end-to-end row, is not
reliably passable.

The prior review's M-6 asked for one of two things: state the once-only
evaluation, **or** widen the window. 0.4.3 did the first and not the second, and
then added a deadline table that makes the second necessary.

**Minimum correction.** Choose one and state it normatively:

(a) Require the hub to construct and sign the CON-214/CON-219 offer **after**
    both Finished values, on a second authenticated session round trip keyed by
    ceremony ID — and then say so in `CON-929`, add the round trip to
    `REQ-937`'s projection or explicitly exclude it from the relay frame budget
    in `NFR-905`, and account for it in `REQ-940`'s absolute deadline; or

(b) Declare an explicit CON-219/CON-214 amendment widening the v2 offer window
    to a stated maximum (the pre-offer states sum to 210 s, so 300 s with the
    600 s absolute deadline is the coherent choice), add it to `amends:`, add
    `MAX_OFFER_WINDOW_SECONDS` to the coordinated-approval list, and state why a
    longer signed-evidence window is acceptable against CON-220's cache
    rationale; or

(c) Bound the pre-offer states so their sum is strictly below 120 seconds —
    which means cutting `await authenticated claim-v2` to roughly 75 s — and
    say in `REQ-940` that this is why.

Whichever is chosen, add a TEST-1010 row asserting that a ceremony consuming the
**full** `claim-v2` and bootstrap idle budgets still authorises successfully.

---

### H-3 (HIGH) — cbcl-pairing SPEC-001's hard stops are amended in substance, named in `amends:`, and dispositioned nowhere; the coordinated-approval list omits two owners the frontmatter itself names

**Exact candidate clauses.** `candidate:8` (`amends:`): "… **cbcl-pairing
SPEC-001 REQ-002/NFR-002**; **did-crdt HTTP service contract**".
`candidate:131-132`: "This amendment is a coordinated revision request; **it does
not pretend a child spec can silently waive its parents**." The table that
follows (`candidate:134-154`) contains rows for SPEC-008, SPEC-007 and SPEC-002
only. `candidate:1258-1260`: "The coordinated REQ-215, CON-201/211/214/219/220/221,
PROTO-003, SPEC-002, and SPEC-007 amendments require their affected human
owners' approval after this design review."

**Governing clauses.**
`cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:1486-1511` §Amendment Channels:

> "Hard stops: REQ-002, REQ-005, REQ-008, REQ-009, REQ-013, REQ-014, REQ-015,
> NFR-009, NFR-010, NFR-011, NFR-012, and every item under Production gates.
> **No channel can waive a hard stop without a new specification version and the
> required Tier-1 review.**"

- §REQ-005 (hard stop, `:316-320`): "The mailbox SHALL allocate at most two memberships and
  **become terminal on a third distinct claim**."
- §REQ-003 (not a hard stop, ordinary channel, `:289-293`): "Adding an application SHALL
  require only an endpoint-side profile. It requires **no relay endpoint,
  message, phase, parser, store, limiter, reaper, or deployment change**."
- §REQ-002 (hard stop, `:273-276`): "The relay SHALL NOT derive pairing keys, verify
  Finished values, retain plaintext, or interpret opaque bodies as application
  content."

**Evidence of substantive amendment.**

- **REQ-005 is changed.** `REQ-933:202-205`: a `claim-v2` whose digest does not
  match "changes no state" and is "indistinguishable from an unknown or already
  claimed mailbox"; `:205-206`: "Successful verification burns the commitment".
  After the burn, a third distinct `claim-v2` has no commitment to compare
  against and therefore also changes no state. A third distinct claim no longer
  makes the mailbox terminal. That is the REQ-005 rule, replaced.
- **REQ-003 is changed.** `claim-v2` is a new relay **message** and a new relay
  **parser** arm; `NFR-905:541-542` adds a per-mailbox 32-octet **store** field
  ("A v2 mailbox adds exactly one 32-octet claim commitment"); the relay's
  persisted mailbox record (`cbcl-pairing/src/storage.rs`) gains a field and
  therefore a format migration; `candidate:1227` names a "`cbcl-bus` relay"
  deployment slice. That is five of the eight things REQ-003 forbids.
- **REQ-002 is *satisfied*, not amended.** I checked this rather than accepting
  the frontmatter: `T` never enters CPace (`REQ-933:196`), the relay derives no
  pairing key, verifies no Finished value, retains no plaintext, and interprets
  no opaque body. The `amends:` entry for REQ-002 is therefore over-inclusive —
  but naming a hard stop in `amends:` and then omitting it from the
  hard-stop disposition table is precisely the M-5 defect class recurring.
- **NFR-002 is *satisfied*, not amended.** Per-membership counts are 5
  (allocator) and 6 (claimant) against `MAX_FRAMES_PER_MEMBERSHIP = 16`
  (`cbcl-pairing/src/mailbox.rs:13`) — verified below.
- **SPEC-007 NFR-802** ("Relay message, mailbox, lifetime, recognition-work, and
  unrelated-work limits SHALL equal the exact pinned `cbcl-pairing`
  specification and assets") is not in the hard-stop table either. Its *limits*
  are untouched, which `NFR-905` argues correctly; its *message set* and
  *mailbox record* are not.
- **did-crdt.** `candidate:8` names the "did-crdt HTTP service contract" as
  amended — correctly, since `REQ-939:417-419` and `CON-930:806-811` change
  `POST /dids` semantics and add CORS. `candidate:1258-1260` omits the did-crdt
  owner from the approval list, and `candidate:1248-1250` asks the owner only
  about *deployment*, not about the contract amendment.

**Failure trace.**

1. This review returns PASS. `candidate:1232-1238` step 2 begins encoding the
   design as an SPL theory, with "every upstream amendment approval" a generic
   gate.
2. The enumerated coordination list (`:1258-1260`) is the operative checklist and
   names SPEC-004, PROTO-003, SPEC-002 and SPEC-007 owners. Neither the
   cbcl-pairing owner nor the did-crdt owner appears.
3. `cbcl-pairing` SPEC-001 REQ-005 is amended by implementation, through
   "implementation behaviour" and "test fixtures" — two channels its own
   Amendment Channels section names as **not** amendable — with no new SPEC-001
   version and no SPEC-001 Tier-1 review.
4. The candidate's own opening sentence, "it does not pretend a child spec can
   silently waive its parents", is thereby false of the document.

**Minimum correction.** Add a `cbcl-pairing SPEC-001` block to the inherited
hard-stop table with rows for REQ-002 (**Retained** — with the four-clause
argument already in the REQ-805 row), REQ-003 (**Amended** — v2 adds one relay
command, one per-mailbox commitment field, one refusal rule and a storage
format revision; state that REQ-003 is not a hard stop and is amendable through
the ordinary cbcl-pairing channel), REQ-005 (**Amended for v2 mailboxes** — the
third-distinct-claim terminal is replaced by commitment-authenticated membership
plus indistinguishable no-op refusal; state that this is a hard stop requiring a
new SPEC-001 version and its own Tier-1 review), REQ-008/009/013/014/015
(**Retained**), and NFR-009 through NFR-012 (**Retained**, with the
peer-address-keyed limiter left untouched). Add `cbcl-pairing SPEC-001
REQ-003/REQ-005` to `amends:` and remove or re-label REQ-002/NFR-002 as
*satisfied*. Add SPEC-007 NFR-802 with its actual disposition. Add the
cbcl-pairing and did-crdt owners to `candidate:1258-1260`.

---

### M-1 (MEDIUM) — The "executable encoder witness" for the large relay body is off by two octets, and the fixed-size claim does not survive a short credential; `TEST-997` asserts two numbers a conforming encoder cannot produce

**Exact candidate clauses.** `NFR-905:502-508`:

> "`offer` and `payload` pad field 4 to exactly 64,512 octets; all control
> objects pad it to exactly 4,096 octets. … The encoded v2 application object
> maxima are therefore **64,560** and **4,144** octets respectively: one map
> byte; two two-byte uint pairs; one 35-byte digest pair; one four-byte length
> pair; and one key plus three-byte byte-string header plus the fixed body."

`NFR-905:524-528`: "At counter `0..15`, the existing deterministic `sealed-frame`
map adds **50 octets including the byte-string header**: complete relay bodies
are at most **66,694** and **6,278**. **These figures are executable encoder
witnesses, not estimates.**"

`TEST-997:1090-1095`: "Credential logical lengths 1, 61,999, and 62,000 pad into
a 64,512-byte field and a **64,560-byte application object** … Assert exact
large/control maxima 66,628/6,212 plaintext, 66,644/6,228 ciphertext, and
**66,694**/6,278 relay body."

**Code evidence.** `cbcl-pairing/src/wire.rs:877-888` gives the exact
`sealed-frame` member set:

```rust
ChannelFrame::Sealed { direction, counter, ciphertext } => map(vec![
    ("v",          Value::Integer(1.into())),
    ("kind",       Value::Text("sealed".into())),
    ("direction",  encoded_direction(*direction)),
    ("counter",    Value::Integer((*counter).into())),
    ("ciphertext", Value::Bytes(ciphertext.clone())),
]),
```

and `schemas/pairing-v1.cddl:125-131` bounds `ciphertext` at `17..69572`.
`sealed-plaintext` is `{ "control": bstr .size (1..2048), ? "body": bstr .size
(1..65536) }` (`:133-136`).

**The arithmetic, recomputed from the CDDL member sets and RFC 8949 §3
head encoding.** Byte-string heads are 1 byte for length ≤ 23, 2 for ≤ 255,
**3 for ≤ 65,535, and 5 for ≤ 2³²−1**.

| Layer | Large class | Control class |
|---|---:|---:|
| padded body (field 4) | 64,512 | 4,096 |
| v2 application object, `logicalLength` max | **64,560** ✓ | **4,144** ✓ |
| `sealed-plaintext` overhead with a 2,048-octet control | +2,068 ✓ | +2,068 ✓ |
| plaintext | **66,628** ✓ | **6,212** ✓ |
| + AES-GCM tag | **66,644** ✓ | **6,228** ✓ |
| `sealed-frame` non-ciphertext overhead | 47 | 47 |
| ciphertext byte-string head | **5** (66,644 > 65,535) | 3 (6,228 ≤ 65,535) |
| **complete relay body** | **66,696** ✗ candidate says 66,694 | **6,278** ✓ |
| enclosing `put` map | 66,721 ≤ 70,000 ✓ | 6,301 ✓ |

The 47 decomposes as: map(5) head 1; `"v"` 2 + value 1; `"kind"` 5 + `"sealed"`
7; `"direction"` 10 + value 1; `"counter"` 8 + value 1; `"ciphertext"` 11. The
candidate's "50 octets including the byte-string header" is correct for the
control class, where the ciphertext is 6,228 and takes a 3-byte head, and wrong
for the large class, where 66,644 crosses the 65,536 boundary and takes a 5-byte
head. **66,644 + 52 = 66,696.**

**Second, separate error in the same test row.** The `logicalLength` value is
CBOR-encoded shortest-form under deterministic encoding, so the "four-byte length
pair" is a maximum, not a constant. `logicalLength = 1` encodes as `03 01`,
giving a **64,558**-octet application object; `logicalLength = 200` gives 64,559;
only `logicalLength ≥ 256` gives 64,560. `TEST-997:1090-1091` asserts that
lengths **1**, 61,999 and 62,000 all "pad into … a 64,560-byte application
object". Length 1 does not.

**Failure trace.** An implementer writes the v2 encoder to the CDDL, runs the
release-blocking `TEST-997`, and gets `assert_eq!(66_696, 66_694)` and
`assert_eq!(64_558, 64_560)`. Both fail. The only ways to "pass" are to encode
`logicalLength` as a fixed-width uint — which is not deterministic CBOR and
would break `TEST-995`'s independent cross-implementation vectors — or to edit
the assertions, which is the failure mode `NFR-905:527`'s "executable encoder
witnesses, not estimates" exists to prevent.

**No bound is violated.** 66,696 ≤ the candidate's own 67,000 v2 cap ≤ 69,632
`MAX_FRAME_BODY_BYTES` (`mailbox.rs:15`), and the enclosing `put` at 66,721 ≤
70,000 `MAX_WIRE_MESSAGE` (`bin/cbcl-pairing-relay-ws.rs:32`). The queue budget
is unaffected because it is computed from the 67,000/7,000/8,192 caps, not from
these figures. The defect is in the stated witnesses and the test, not in the
safety margin.

**Minimum correction.** Change `NFR-905:524-526` to "the existing deterministic
`sealed-frame` map adds 47 octets plus the ciphertext byte-string head — 52
octets for the large class, whose ciphertext exceeds 65,535, and 50 for the
control class: complete relay bodies are at most **66,696** and **6,278**".
Change `TEST-997:1093` to 66,696. Change `TEST-997:1090-1091` to assert the
64,512-octet field for all three lengths and the **64,558/64,560** object bounds
for `logicalLength` 1 and 62,000 respectively, and add one sentence to
`NFR-905:502` recording that the three-octet variance in the encoded object is
the deliberate cost of deterministic integer encoding and is the one residual
length signal inside the fixed classes.

---

### M-2 (MEDIUM) — CON-923's minted `operatorId` grammar contradicts the recogniser and the generator it claims to codify, so two conforming v2 recognisers disagree

**Exact candidate clause.** `CON-923:573-580`:

```text
operatorId                  text 1..128
relayOrigin                 canonical HTTPS origin
priority                    uint 0..65535
weight                      uint 1..65535
privacyPolicyDigest         base64url-no-pad SHA-256
conformanceEvidenceDigest   base64url-no-pad SHA-256
```

`CON-923:582-586`: "A new dispatcher performs UTF-8/size/duplicate/depth
recognition, reads only the integer version discriminator, then applies **the
complete closed grammar for that version** before any semantic action."
`TEST-1009:1197-1199`: "Feed allocator and claimant the nine-name version-2
profile and every one-field addition/removal … **Both roles agree exactly.**"

**Governing clause.** `SPEC-004…#CON-201`
(`specs/SPEC-004-application-scoped-identity.md:2440-2450`): a grammar change
that lets "two conforming recognizers disagree about which profiles are valid
even when they computed the same digest" is the exact hazard the closed language
exists to prevent. `SPEC-007…#CON-806`: "The release recogniser rejects unknown
members, duplicate members, invalid digests, zero weight, and non-canonical
origins" — and fixes no character class for `operatorId`, so `CON-923` is the
sole normative source.

**Code evidence.**

- `crates/selfsame-app-identity/src/profile.rs:814-820`:

  ```rust
  fn provider_id(text: &str, path: &'static str) -> Result<(), ProfileError> {
      if is_provider_id(text) { Ok(()) }
      else { Err(bad(path, "does not match [a-z0-9][a-z0-9-]{0,62}")) }
  }
  ```

  applied at `:846` to `cbclPairingRelays[].operatorId`. That is **1..63
  characters** from `[a-z0-9-]` with a leading alphanumeric — not `text 1..128`.
- `cbcl-bus/scripts/gen-production-profile.mjs:293-295` enforces the same
  `[a-z0-9][a-z0-9-]{0,62}` in the shipped generator's self-check.
- The other five members do match: `MAX_RELAYS = 16` (`profile.rs:78`),
  `bounded_range` rejects outside `[0, 65535]` (`:900`), `weight == 0` is
  rejected (`:856-858`), and both digests are canonical unpadded base64url
  SHA-256.

**Failure trace.**

1. Implementer A reads `CON-923:574` and writes the v2 dispatcher's descriptor
   grammar as `text` with a 1..128 UTF-8 length bound.
2. Implementer B is the browser allocator, which reuses the vendored
   `ApplicationProfile::recognise` and inherits `[a-z0-9][a-z0-9-]{0,62}`.
3. A profile whose descriptor names `operatorId: "AU-Primary"` — valid UTF-8,
   10 characters, inside 1..128 — is **accepted** by A and **rejected** by B.
4. `TEST-1009:1199`'s "Both roles agree exactly" fails, and the two recognisers
   compute the same RFC 8785 profile digest while disagreeing about validity:
   verbatim the CON-201 hazard.
5. Worse for the allocator path, `CON-923:604-605` requires "A production v2
   allocator SHALL embed and recognise the exact version-2 profile it serves
   before allocation" — so a profile the serving side rejects and the wallet
   accepts (or the reverse) is a silent allocation failure rather than a
   recognisable refusal.

**Minimum correction.** Replace `operatorId  text 1..128` with the grammar the
recogniser and the generator both implement: `operatorId  ASCII
[a-z0-9][a-z0-9-]{0,62}` (1..63 octets), and state that this is the CON-806
descriptor grammar carried unchanged into version 2. If a wider identifier is
genuinely wanted, say so as an explicit widening, name `profile.rs:814-820` and
`gen-production-profile.mjs:293-295` as the artefacts that must change, and add
the widening to the SPEC-007 CON-806 coordinated amendment. Either way add a
`TEST-1009` sub-case for an `operatorId` that is valid under one candidate
grammar and invalid under the other.

---

### M-3 (MEDIUM) — Minting `profileVersion: 2` while declaring version 1 unamended strands the shipped profile, the shipped generator, SPEC-007 CON-806's v1 carrier, and cbcl-bus SPEC-075 REQ-007, and no slice owns the dispatcher

**Exact candidate clauses.** `candidate:115`: "**H-1 / L-1 …** | CON-923 defines a
new `profileVersion: 2` with the exact nine-name grammar intended for this path.
**Version 1 keeps its ratified grammar.**" `CON-923:553`: "CON-201 profile
version 1 is **not amended**." `CON-923:584-590`: "Version 1 therefore recognises
its nine required names **including `rendezvous`** and optional tenth
`pairingRecordRelays`; version 2 recognises only the nine names above. The
reviewed single-grammar `PROFILE_VERSION = 1` implementation and generator must
be split; relabelling their current nine-name output without dispatch is not
conformance." `TEST-1009:1200`: "**Version-1 recognisers continue to
accept/reject their unchanged ratified grammar.**"

**Governing clauses.** `SPEC-004…#CON-201:2469-2481`: the top-level member set is
"exactly the ten names below — `profileVersion`, `applicationId`,
`accountAuthority`, `verifierAudience`, `allowedPermissions`, `enrollment`,
**`rendezvous`**, `stateResolvers`, `revocation`, and the OPTIONAL
`pairingRecordRelays`", with "an unknown member at any depth is a rejection".
`SPEC-007…#CON-806` step 1: "recognise the authenticated profile **under
CON-201**", and its Profile grammar clause: "the coordinated SPEC-004 amendment
defines `cbclPairingRelays`". `cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md:229-236` §REQ-007: "The generated Selfsame application profile SHALL use the exact f2
profile member `cbclPairingRelays`. … The deployment **SHALL NOT emit legacy
`rendezvous`** …".

**Code evidence.**

- `crates/selfsame-app-identity/src/profile.rs:60` `pub const PROFILE_VERSION:
  i64 = 1;` and `:80-91`:

  ```rust
  const TOP_LEVEL_REQUIRED: &[&str] = &[
      "profileVersion", "applicationId", "accountAuthority",
      "verifierAudience", "allowedPermissions", "enrollment",
      "cbclPairingRelays", "stateResolvers", "revocation",
  ];
  const TOP_LEVEL_OPTIONAL: &[&str] = &[];
  ```

  `rendezvous` and `pairingRecordRelays` are absent from the recogniser
  entirely, and `:384-387` refuses any `profileVersion != 1`.
- `cbcl-bus/scripts/gen-production-profile.mjs:217` emits `profileVersion: 1`
  alongside `cbclPairingRelays` (`:213`), and `:309-319` rejects `rendezvous` as
  "prohibited by SPEC-075 REQ-007".

**Failure trace.**

1. CON-923 is ratified as drafted. The shipped chat.anuna.io profile object —
   `profileVersion: 1`, nine names including `cbclPairingRelays`, no
   `rendezvous` — is now conforming to **neither** version: not v1 (missing the
   required `rendezvous`, carrying the unknown `cbclPairingRelays`) and not v2
   (wrong version integer).
2. To satisfy `TEST-1009:1200`, someone must implement the v1 branch's
   `rendezvous` and `pairingRecordRelays` grammars — code that has never existed
   in `profile.rs` — and someone must teach the generator to emit them. cbcl-bus
   SPEC-075 REQ-007 forbids the generator from doing so. The candidate names
   neither SPEC-075 nor the resolution.
3. `SPEC-007 CON-806` step 1 requires the *existing, already-implemented*
   credential/v1 cbcl-pairing path to "recognise the authenticated profile under
   CON-201", and CON-806's descriptor now lives only in version 2
   (`candidate:152`). No clause says which profile version credential/v1
   consumes after the split. The moment chat.anuna.io re-mints its profile as
   version 2 to enable v2 pairing, the v1 path's recogniser refuses it — one
   well-known URI, one profile, two mutually exclusive grammars.
4. `CON-220`'s tier-3 `pairingRecordRelays` branch has no carrier in version 2.
   The candidate amends CON-220 step 6 (`REQ-932:169-177`) and is silent on
   tier 3.
5. The cross-repository slice table (`candidate:1219-1228`) names no owner for
   the version dispatcher. The `selfsame` pure-core row lists "v2 offer
   extensions and authoriser; closed display provenance; issuer preview type;
   bundle/preparation/receipt binding; policy grammar; compensation state" —
   the dispatcher lives in `crates/selfsame-app-identity/src/profile.rs`, which
   is pure core, and is absent. The browser WASM row is likewise silent even
   though `selfsame-web-device` calls the same `recognise`.

The v2 path itself is self-consistent and reachable — this is a real improvement
over 0.4.2's H-1, where neither side could recognise anything. What remains is
that the disposition sentence "Version 1 keeps its ratified grammar" is false of
every artefact at the baseline, and three consequences of the split are
undispositioned.

**Minimum correction.** Add to `CON-923`: (i) the shipped `profileVersion: 1`
object served by chat.anuna.io is non-conforming to ratified CON-201 and SHALL be
re-minted as version 2 before any v2 allocation; (ii) cbcl-bus SPEC-075 REQ-007
requires a coordinated amendment to emit `profileVersion: 2`, and it is added to
`amends:` and to `candidate:1258-1260`; (iii) `SPEC-007 CON-806` step 1 is
amended so the credential/v1 relay-selection path also recognises version 2, or
credential/v1 is declared to have no conforming profile carrier after the split
and its retirement date is named; (iv) `CON-220`'s tier-3 `pairingRecordRelays`
branch is structurally absent for version 2 and that is stated. Add a
"profile-version dispatcher and version-1 `rendezvous`/`pairingRecordRelays`
grammar" row to the `selfsame` pure-core slice and a "version-2 generator" row
to the `cbcl_chat browser` slice.

---

### M-4 (MEDIUM) — Re-keying the withdrawn-alias tombstone by stable account ID misdescribes the baseline and permanently blocks legitimate re-linking after any withdrawal

**Exact candidate clauses.** `REQ-936:299-303`: "Existing accounts are migrated
transactionally: allocate one ID; move the **handle-keyed** webfinger-account,
**withdrawn-alias**, pairaccount, replay, and pending-ceremony records under it
…". `CON-931:849-851`: "The hub's webfinger-account, **withdrawn-alias**,
pairaccount, account-authority, scope, and pending-ceremony records are **keyed
by stable account ID**."

**Governing clause.** `SPEC-004…#CON-204`: "the application account authority
SHALL create an account record keyed by **its full normalized value**" — the
`acct_uri`'s — "That record MAY map internally to an existing application
account, but the mapping is private provider data". Re-keying the private
internal mapping is permitted; re-keying the alias tombstone is a change to what
withdrawal protects.

**Code evidence.** `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe`:

- `:60` `(defrecord cbcl-webfinger-withdrawn alias account home-did)` and
  `:71-75` create the table with `attributes '(alias account home-did)`, `type
  set`, and `#(index (account))`. The **primary key is the alias URI**; the
  handle is only a secondary index. The candidate's "handle-keyed … withdrawn-alias"
  is factually wrong about the baseline.
- `:139-149` `create-or-return` reads the tombstone **by URI**:
  `(mnesia:read 'cbcl-webfinger-withdrawn uri 'write)` → `#(error withdrawn)`.
- `:245-257`, verbatim in the source:

  > "**ONE ACCOUNT CAN HOLD SEVERAL TOMBSTONES**, so the matching one is
  > SELECTED rather than taken from the head of the index. The tombstone is
  > keyed on the alias and the alias is a function of the DID, so withdrawing an
  > account and then provisioning it under a different DID leaves the first
  > marker in place and adds a second — reachable without any reinstatement."

  `tombstones-for/2` (`:264-268`) filters `index_read` by `home-did` precisely
  because several generations coexist.

**Failure trace.**

1. Migration runs. `cbcl-webfinger-withdrawn` is re-keyed by
   `applicationAccountId` as `CON-931:849-851` directs. Mnesia `set` semantics
   now permit exactly **one** tombstone per account.
2. Person P withdraws their binding to `did:crdt:AAA` (a device loss, a wallet
   reset, an ordinary unlink). One tombstone is written under `accountId(P)`.
3. P restores their wallet from the recovery phrase — which under `CON-925:682-683`
   also produces an empty relay policy and a fresh prompt — and runs a new v2
   ceremony. The hub retains `accountId(P)` and its CON-211 scope
   (`REQ-936:307-309`), so the wallet re-derives the **same** home DID and the
   same `acct_uri`. Even a different DID reaches the same wall.
4. `REQ-938` step 6 invokes `selfsamepairaccept`. The re-keyed
   `create-or-return` reads a tombstone for `accountId(P)` and returns
   `#(error withdrawn)`.
5. `REQ-938:373-374` requires "exact idempotent success", so the ceremony
   refuses — **after** the wallet has already, under `REQ-935:281-287`, called
   `issuer::create`, signed the grant, published the DID to every declared
   resolver and transmitted the reverse payload. `REQ-940:453-463` compensation
   fires: a CON-210 revocation is queued, the hub rolls back, and the person is
   told `not linked`.
6. There is no repair. `withdraw/2` and `reinstate/2` are the only levers, and
   under the re-key `reinstate` clears the single tombstone for the whole
   account rather than for one generation — which is a different, broader
   authority than the design intends and is not specified.
7. `TEST-1002:1136-1143` tests "withdrawn binding" refusal, which will pass. No
   row tests re-provisioning **after** a withdrawal, so the corpus cannot see
   this.

**Minimum correction.** State that `cbcl-webfinger-withdrawn` remains keyed by
the alias URI — which is already a function of the DID and therefore of the
scope and therefore of the account ID — and that only its secondary index moves
from handle to stable account ID. Correct `REQ-936:299-303` and `CON-931:849-851`
to describe the baseline accurately: `cbcl-webfinger-alias` is URI-keyed,
`cbcl-webfinger-account` is handle-keyed, `cbcl-webfinger-withdrawn` is
alias-keyed with a handle index, and `cbcl-chat-pairaccount` is keyed by the
composite `#(Handle DeviceKey)` with handle and devkey indexes
(`cbcl-chat-pairaccount.lfe:22-33`). Add a `TEST-1002` row: "withdraw, then
re-provision the same account under the same and under a different DID; the
multi-generation tombstone behaviour of `already-withdrawn/2` is preserved and
neither case refuses a legitimate re-link".

---

### M-5 (MEDIUM) — CON-925 asserts crash-atomic compare-and-swap that neither reviewed store backend provides, names no mechanism, and leaves a torn policy record with no repair path

**Exact candidate clauses.** `CON-925:667-671`: "Read requires successful AEAD
opening; write creates a fresh nonce and uses **atomic compare-and-swap on the
revision**. **A crash exposes the complete old or complete new document, never a
partial record**, and never changes `SealedRoot`. A lost CAS retries from a fresh
authenticated read." `CON-925:671-673`: "Corrupt, well-formed-but-forged,
root-fingerprint-mismatched, unreadable, presence-failing, CAS-exhausted, or
write-failing state **refuses**." `TEST-981:962-964`: "Crash before/after its
CAS: the policy is exactly old or new and the sealed root remains byte-identical."

**Code evidence.** The wallet has exactly two store backends and both expose a
plain destructive replace with no documented atomicity:

- Desktop (`src-tauri/src/store.rs:184-188`):

  ```rust
  pub fn set(name: &str, value: &str) -> Result<(), StoreError> {
      entry(name)?.set_password(value).map_err(|e| StoreError::Backend(e.to_string()))
  }
  ```

  over `keyring::Entry`, whose durability and replace semantics vary by platform
  backend (Keychain, Secret Service, Credential Manager) and are not specified.
- Android (`crates/tauri-plugin-selfsame-store/src/lib.rs:99-105`): "Store a
  record, **replacing** any previous value under the same key", through a mobile
  plugin call.
- `src-tauri/src/store.rs:26-31` `assert_durable` probes a *different* entry
  (`PROBE_ENTRY`, deliberately not `ROOT_ENTRY`, `:43-48`) at startup and cannot
  detect a torn write of the policy record.

Nothing in either path offers compare-and-swap. In-process CAS is trivially
implementable with a mutex and a revision check — that half is fine — but
"a crash exposes the complete old or complete new document" is a claim about the
**backend's** write atomicity, and the candidate neither requires a mechanism nor
names an artefact that would provide one.

**Failure trace.**

1. The person accepts a new `(applicationId, relayOrigin)` pair. The wallet
   seals the new policy document and calls `store::set`.
2. The process is killed, or the platform store's write is interrupted, mid-write.
3. The record now contains neither the old nor the new sealed blob. The next read
   fails AEAD opening.
4. `CON-925:671-673` requires refusal. `REQ-932:188-189`: "Policy unavailability,
   authentication failure, or CAS write failure opens no socket." **Every future
   v2 ceremony on this installation refuses, permanently.**
5. There is no repair. `CON-925:685-686` offers "Policy management may list and
   remove exact pairs", which requires opening the document. `CON-925:682-683`'s
   only exit is a full wallet restore from the recovery phrase — losing the
   device's application identities is a wildly disproportionate remedy for a
   relay-policy record the same clause calls "device-local".
6. `TEST-981:962-963` asserts the property the platform does not guarantee, so
   the row is either vacuous (a test harness that fakes an atomic store) or
   permanently red.

**Minimum correction.** Make the atomicity an obligation with a named mechanism
rather than an assertion: specify a two-slot scheme (`policy-a`, `policy-b`, plus
a tiny versioned pointer record whose single-field write is the commit point), or
write-new-key-then-delete-old, and require `assert_durable`-style verification of
the chosen mechanism at startup. Add one sentence giving the repair path for an
unopenable policy record — an explicit, presence-gated "reset relay policy"
operation that discards the record and re-prompts on next use, which is exactly
what `CON-925:682-683` already accepts as safe after a restore. Add a `TEST-981`
sub-row asserting that the reset path exists and that it cannot be reached
without passcode plus presence.

---

### M-6 (MEDIUM) — There is no specified transport for `claimCommitment` to reach the relay, and `NFR-905` forbids the only one the reviewed grammar admits

**Exact candidate clauses.** `REQ-933:199-201`: "The allocator gives the relay
`claimCommitment = SHA-256(…)` **when allocating the mailbox**."
`REQ-933:207`: "This is an additive relay command; v1 `claim` is unchanged" —
naming exactly one new command. `NFR-905:547`: "**No v1 mailbox or command
changes.**" `candidate:1227` (`cbcl-bus` relay slice): "additive
commitment-authenticated `claim-v2`" — again only one command.

**Code evidence.** `cbcl-pairing/schemas/pairing-v1.cddl:22-27`:

```cddl
allocate = {
  "type": "allocate",
  "locator-mode": 0..1,
  ? "ttl-seconds": 60..600
}
```

`client-message = bind / allocate / claim / open / put / ack / close / ping`
(`:16`), and `cbcl-pairing/src/wire.rs:608-611` recognises every client message
through `recognise_value(input, "client-message")`, which runs
`validate_cbor(rule, …)` against that closed CDDL (`:341-342`) after rejecting
non-deterministic encodings and duplicate keys (`:326-337`). An `allocate` map
carrying an extra `claim-commitment` member matches no alternative and is
refused as `RecognitionError::Schema`.

**Failure trace.** The implementer must place a 32-octet commitment in relay
state at allocation time. The three available routes are: (a) add an optional
member to `allocate` — a change to a v1 command, forbidden by `NFR-905:547` and
by the v1 rejection vectors `TEST-997:1094-1095` requires to be re-run
unchanged; (b) add an `allocate-v2` command — permitted, but named nowhere in
`REQ-933`, `NFR-905`, or either relay slice, so it is unbudgeted work and
unrepresented in the frame/work arithmetic; or (c) have the allocator send the
commitment in a separate post-allocation command — which needs a command too, and
opens a race in which the mailbox exists unprotected between allocation and
commitment, exactly the window H-1 describes. The requirement as written admits
no conforming implementation.

**Minimum correction.** Name `allocate-v2` explicitly in `REQ-933` as the second
additive relay command, with its closed grammar (`locator-mode`, `ttl-seconds`,
`claim-commitment  bstr .size 32`), state that a v2 mailbox is exactly one
allocated through it, and add it to the `cbcl-pairing` and `cbcl-bus relay`
slices. Restate `NFR-905:547` as "No v1 command grammar, v1 mailbox behaviour, or
shared bound changes; v2 adds two commands and one optional per-mailbox field."
Add the relay's persisted-record format revision (`cbcl-pairing/src/storage.rs`)
to the same slice.

---

### L-1 (LOW) — "per-connection and per-origin operation limiters" describes a limiter the relay does not have

`NFR-905:543-545`: "Each `claim-v2` attempt performs one bounded command parse,
one SHA-256, and one constant-time comparison under the existing
**per-connection and per-origin** operation limiters." The reviewed limiter is
neither. `cbcl-pairing/src/limiter.rs:1` describes it as "Pure operator-keyed,
bounded mailbox-operation limiter"; `:62-70` `PeerKey` is an operator-keyed HMAC
of one canonical **peer address**; `src/relay.rs:214` calls
`self.limiter.check(operation, canonical_peer_address, now)`. Budget is therefore
shared across every connection from one address and is not keyed by origin at
all. The substantive bound survives — `T` is 128 bits, so brute force is not the
threat, and `NFR-012` gives the IPv6 `/64` aggregation — but the sentence
mis-describes existing code in a work-bounds argument. Say "the existing
peer-address-keyed, per-operation sliding-window limiter (`limiter.rs`,
`COOLDOWN_SECONDS = 300`)".

### L-2 (LOW) — `amends:` remains over-inclusive in two entries

`candidate:8` lists `SPEC-004…#REQ-215` and `SPEC-002…#REQ-103`. Neither is
amended: `REQ-936:317-320` now *satisfies* REQ-215 (`specs/SPEC-004-…:974-983`)
by giving the device key its own per-installation lifecycle, and the hard-stop
table at `candidate:154` says SPEC-002 REQ-103 is "**Retained**". As noted in
H-3, `cbcl-pairing SPEC-001 REQ-002/NFR-002` is likewise satisfied rather than
amended while REQ-003/REQ-005 are amended and unlisted. Move satisfied contracts
to a "conformed to, not amended" list so `amends:` names only real amendments.

### L-3 (LOW) — The PAIR1 entry rules reject the most common honest input and do not say whether a local rejection burns the ceremony

`CON-924:634-636` rejects "whitespace, Unicode lookalikes, missing separators,
**mixed/lower case**, nonzero pad bits, and checksum failure". Mobile keyboards
default to lowercase, and the design requires 55 characters typed by hand with no
paste (`REQ-933:210-211`). Separately, `REQ-933:219-222` says "A wrong code …
fails before intent. Every terminal zeroizes `C` and burns the invitation, offer,
request, and ceremony" without distinguishing a **local** checksum or alphabet
rejection — which reaches no network and no relay — from a CPace failure. A
one-character substitution is caught locally with overwhelming probability by the
16-bit checksum, so this is the common case. As written an implementer may burn
the ceremony on the first typo, forcing a fresh QR and a fresh 55 characters.
State that case-normalisation to upper case before decoding is required (not
forbidden), and that a local grammar or checksum rejection permits re-entry
within the same state's idle deadline without burning anything.

### L-4 (LOW) — "passcode plus OS presence" is not achievable on the reviewed desktop backend

`CON-925:651-652` and `REQ-932:184-185` require "a successful passcode plus
OS-presence operation". `src-tauri/src/store.rs:200-203`:

```rust
pub fn require_presence(_reason: &str) -> Result<(), StoreError> {
    Ok(())
}
```

with the comment "On desktop the presence check *is* the passcode … There is no
biometric API Tauri exposes on desktop". The three-operation rule
(`CON-925:687-688`) therefore degrades to three passcode entries plus three
64 MiB × 3-pass Argon2id derivations (`custody.rs:62-64`) on desktop, and the
module's own note that this "runs at most a few times a day" no longer holds.
State the platform split, and record the three-derivation cost against the
H-2 timing budget.

### L-5 (LOW) — The residual that CON-924's two-digest binding does not cover is undeclared

`CON-924:638-643` makes the allocator pin its release-embedded profile digest
into CPace, which is the correct closure for 0.4.2 M-2. It does not close the
case where the same compromised origin serves both the substituted `.well-known`
profile **and** the browser bundle that embeds it: the two digests then agree and
Finished succeeds. This is bounded — the attacker must already control the origin
the person is signed into — and is strictly better than the 0.4.2 posture, but
`candidate:53-69`'s bounded-properties list and ADR-916/918 do not name it. Add
one sentence.

### L-6 (LOW) — Two normative obligations have no test row

`REQ-941:481-482` requires that "Release notes and the first-use prompt SHALL
state this residual", and `CON-925:687-688` requires "three distinct
passcode/presence operations; no derived wrapping/policy/root key survives
between them". Neither appears in TEST-978 through TEST-1010. `TEST-990` covers
preview zeroization only. Add a row for each, and note that
`TEST-1002:1140-1141`'s "change its display handle/index" step is exercisable at
the hub-store level even though `candidate:127` says the "nonexistent handle
rename is removed" — the two readings should be reconciled so the row is not
mistaken for a UI affordance requirement.

### L-7 (LOW) — The `claimCommitment` preimage does not pin its locator encoding

`REQ-933:199-200` writes `SHA-256("cbcl-pairing v2 claim" || locator || T)`.
`locator` in the reviewed grammar is a variant,
`[0, mailbox-id] / [1, 0..999999999]` (`pairing-v1.cddl:14`); `CON-924:618` pins
the carrier field to "direct 32-octet mailbox id" but `REQ-933` does not say
whether the preimage takes the raw 32 octets or the encoded array. Since the
domain string and `T` are fixed-length, pinning the raw 32-octet form makes the
preimage unambiguous. Say which; `TEST-995` cannot produce a cross-implementation
vector without it.

---

## Disposition of every 0.4.2 HIGH / MEDIUM / LOW finding

| 0.4.2 finding | 0.4.3's claim | Verified disposition |
|---|---|---|
| **H-1** — false eleven-name CON-201 grammar; disposition factually wrong | `candidate:115`: mint `profileVersion: 2` with the exact nine-name grammar; version 1 keeps its ratified grammar | **PARTIALLY CLOSED.** The v2 grammar at `CON-923:556-566` is byte-for-byte the shipped `TOP_LEVEL_REQUIRED` (`profile.rs:80-91`), in the same order, so the v2 path is reachable from both sides — the actual 0.4.1/0.4.2 defect is gone. `CON-923:584-585`'s description of ratified v1 matches `CON-201:2469-2481` exactly. Remaining: the shipped artefacts, SPEC-007 CON-806's v1 carrier, cbcl-bus SPEC-075 REQ-007 and the dispatcher slice — **M-3**; and the minted `operatorId` bound — **M-2**. |
| **H-2** — presence secret is the recovery-phrase encoding | `candidate:116`: prefixed Crockford Base32 plus mutual rejection | **CLOSED.** `REQ-933:191-214` and `CON-924:629-636`. The two inputs are structurally disjoint (prefix, separators, alphabet, case), mutual rejection is required in both directions, no parser/label/component/telemetry/buffer is shared, and `TEST-982:974-976` tests both directions. Bit arithmetic verified: 128 + 128 + 16 + 3 = 275 = 55 × 5. Residual usability wrinkles are **L-3**. |
| **H-3** — CON-919 required the device key to be the session key | `candidate:117`: stable account ID plus a distinct persistent per-installation device key with proof of possession | **CLOSED.** `REQ-936:317-320`, `CON-929:783-785`, `TEST-993:1056-1063`. Now satisfies SPEC-004 REQ-215 (`specs/SPEC-004-…:974-983`) rather than contradicting it, since REQ-936 makes scope 1:1 with `(applicationId, applicationAccountId)`. "a ceremony never relabels a session key as the device key" is the exact deletion the prior review asked for. |
| **H-4** — CON-221 applicability had no authority interface; false `NoBinding` | `candidate:118`: authenticated account-ID-keyed authority query | **CLOSED.** `CON-931:831-838` defines `selfsamepairauthority(ceremonyId)`, returns exactly `Unknown \| NoBinding \| Bound(did)`, maps `Bound(did) ≠ prepared` to `BoundDifferentDid`, and forbids by name the webfinger-by-prepared-DID probe that produced the false `NoBinding` (`cbcl-chat-webfinger-http.lfe:20-37`, `lookup(uri)`). `Unknown` covers unmigrated state and fails closed. All four states reachable; `TEST-1001:1128-1133` exercises all four including a pre-existing different-DID binding. |
| **H-5** — CON-921 mandated the handle-keyed alias primitive REQ-925 forbids | `candidate:119`: rekey by stable account ID and specify migration | **PARTIALLY CLOSED.** `CON-931:851-853` now says neither v2 command "calls the reviewed handle-keyed `provision/3` directly" and requires migration to complete before either command is reachable — the right shape, and consistent with CON-204's "MAY map internally … private provider data". But the migration clause mis-describes the baseline tables and would break withdrawal — **M-4**. |
| **M-1** — public carrier is a free ceremony kill | `candidate:120`: opaque commitment to independent `T`; "claim changes no mailbox state until `T` verifies" | **NOT CLOSED.** `REQ-933:207` leaves the unauthenticated v1 `claim` reachable on a v2 mailbox, and `TEST-998:1103-1105` asserts the contrary — **H-1**. The commitment mechanism itself is well designed; only its coverage is incomplete. |
| **M-2** — CON-220 step-6 pin is self-issued | `candidate:121`: bind both independently computed profile digests into CPace/Finished | **CLOSED.** `CON-924:638-643`. The allocator holds `C` and pins its release-embedded copy, which is verbatim the property `CON-220:4138-4143` asks for; `CON-923:604-605` forbids assembling the profile from runtime JSON. The wallet's offer-digest check is retained as a third, now non-circular, cross-check (`REQ-932:172-175`). Undeclared same-origin residual is **L-5**. |
| **M-3** — the named WASM facade cannot express the 1-8/9-13 split, and provenance is caller-asserted | `candidate:122`: new opaque split WASM object ingesting raw closure bytes | **CLOSED.** I re-verified both halves of the old defect: `verify_path_b_peer` runs the whole predicate in one call and requires `ClosureSource::StateResolver` (`selfsame-web-device/src/lib.rs:1013-1015`), which `path_b.rs:428` stamps unconditionally. `CON-930:813-827` replaces it with a private-field wasm-bindgen class, a self-consuming `finish`, raw-bytes ingestion and internal signature replay, and `:823-824` firewalls the old JSON facade from every v2 call edge. The split is coherent against CON-206: step 9 is the alias gate and explicitly tolerates either order (`specs/SPEC-004-…:3146-3150`), and `finish`'s `raw_hub_acceptance_ack` is CON-204's "fresh authenticated cache of that exact binding". `TEST-1000:1121-1122` is now falsifiable. |
| **M-4** — policy inside `SealedRoot`; unspecified atomicity and passcode ordering | `candidate:123`: separate CAS-written record, installation-keyed origin ledger, `SealedRoot` immutable, ordering explicit | **PARTIALLY CLOSED.** The three substantive corrections all land: separate keychain record, separate installation key for the pre-network ledger with no custody prompt (`CON-925:675-680`), `SealedRoot` never rewritten, and explicit ordering — "once for this policy access, after descriptor recognition and before the relay socket", key zeroized before the socket (`REQ-932:184-186`), three distinct operations with no key surviving between them (`CON-925:687-688`). `policyKey = HKDF-Expand(custodyWrappingKey, …)` is implementable inside `custody.rs` where `derive_wrapping_key` (`:359`) is private. Remaining: the atomicity assertion and the missing repair path — **M-5**. |
| **M-5** — hard-stop table incomplete and one row untruthful | `candidate:124`: table includes REQ-806/807 and accurately dispositions REQ-902 | **PARTIALLY CLOSED.** REQ-806 (`candidate:145`) and REQ-807 (`:146`) are present with accurate dispositions against their SPEC-007 texts. The REQ-902 row (`:136`) is now truthful: it separates the retained fixture prohibition from the direction-reversed context composition and names both sides' context sources. CON-806 step 2 is resolved for **both** roles (`:152`, `CON-923:598-603`), which the shipped `eligible()` applies to both `select` and `verify_invitation_origin` (`cbcl_relay.rs:84-92`) — correctly diagnosed. But a whole parent specification's hard stops are now missing — **H-3**. |
| **M-6** — the 120-second window is never reconciled with two human decisions | `candidate:125`: absolute 600 s plus per-state idle deadlines; window evaluated once at authorisation | **PARTIALLY CLOSED.** The *interpretation* half is closed and correct: `REQ-940:443-446` matches `authorise.rs:170`'s single check exactly, and `TEST-1010:1204-1211` tests it from both sides. Every state now has a number. The *feasibility* half is not: the pre-offer budget exceeds the window that was never widened — **H-2**. |
| **M-7** — unsigned resolver genesis; "requires a signed closure" false | `candidate:126`: matching authenticated genesis content plus signed post-genesis deltas | **CLOSED.** `REQ-939:408-415` is accurate against `did-crdt/src/core/document.rs:298-306, 345` (`SignedDelta::unsigned`, "empty signature") and `selfsame-core/src/identity.rs:125-136` (`sign_genesis` reuses the same op and `HlcTimestamp::default()`, so content hashes agree). "The wallet SHALL NOT claim that submitting its same-content signed genesis replaces the resolver's bootstrap genesis" is exactly the honest statement asked for. `TEST-999:1107-1114` now names the documented empty-signature bootstrap. `issuer::create` produces exactly the two post-genesis deltas `REQ-939` step 3 submits (`issuer.rs:150-165`, alias then assertion), and `default_relationships()` is `[Authentication]` only (`did-crdt/src/core/delta.rs:98-100`), so the separate assertion delta is genuinely required. |
| **L-1** — amending `profileVersion: 1` in place | version 2 minted | **CLOSED** as to the hazard; consequences are **M-3**. |
| **L-2** — `amends:` over- and under-inclusive | reconciled | **PARTIALLY CLOSED.** CON-209/CON-227 removed ✓; REQ-215, SPEC-002 and a SPEC-001 reference added ✓. Two entries are now over-inclusive and two cbcl-pairing requirements under-inclusive — **L-2**, **H-3**. |
| **L-3** — TEST-962 exercised a nonexistent handle rename | removed | **CLOSED** in substance; `TEST-1002:1140-1141` retains a store-level index change, which is exercisable. See **L-6**. |
| **L-4** — final-decline residue covered state but not disclosure | preliminary issuer disclosure explicit | **CLOSED.** `REQ-935:265-268` states the irreversible disclosure and requires the preliminary screen to say so; `TEST-991:1046-1047` asserts the application did receive the preview DID and that consent disclosed it. |
| **L-5** — the 67,000 margin should be computed | per-layer encoder arithmetic given | **PARTIALLY CLOSED.** The decomposition is given and is checkable, which is the right obligation — and checking it found a two-octet error — **M-1**. |

## Disposition of the 0.4.1 CRITICAL findings

| 0.4.1 finding | Verified disposition in 0.4.3 |
|---|---|
| **C-1** — identity effects before the final decision | **CLOSED and still closed.** `REQ-935:254-287` and the `CON-928:761-768` effect table place the request tombstone at authenticated display, one zeroized derivation at preliminary approval, and every signature, grant token, publication, issuance write, bundle, alias and payload strictly after the final-decision hash. Re-verified pure/first-signer facts: `HomeKey::home_did` → `selfsame_core::identity::derive_did` (`hierarchy.rs:178-182`), `issuer::create` → `identity::sign_genesis` is the first signature (`issuer.rs:135-137`), `use_hierarchy_root` runs a caller closure behind passcode plus presence and zeroizes (`custody.rs:312-330`). `CON-928:736-756` splits the APIs so they "cannot share a pending output type", and `TEST-992:1051-1054` requires a total order plus a preview-DID mutation refusal before the first signature. **Declared residual:** amended REQ-812 permits publication between final approval and payload; `REQ-940:453-457`'s approved-orphan rule bounds it. Accept as declared. |
| **C-2** — no co-presence binding; comparison value disclosed first; account overclaimed | **CLOSED and strengthened.** The public-carrier / transcribed-secret split survives with a better encoding (H-2 closed) and now carries an independent anti-crowding token. Provenance is a closed four-variant enum with no `claimed_by_secret_holder` and no promotable peer value (`REQ-934:247-252`, `CON-926:698-703`); the account row is required to read "Application account asserted by \<origin\>" and forbidden to imply Selfsame authenticated ownership. `TEST-983:978-985` and `TEST-987:1010-1015` are the right instruments. The deliberate forward-both residual remains declared (`REQ-933:227-228`, ADR-917). The three defects the prior review said arose from *how* C-2 was closed are now: H-2 fixed, M-2 fixed, M-1 **not** fixed (**H-1** here). |

## Inherited hard-stop audit

Every row below was checked against the parent text, not against the candidate's
summary.

| Stop | Candidate's disposition | Verified |
|---|---|---|
| SPEC-008 REQ-902 (hard stop) | Amended for the reverse direction; fixture prohibition retained | **Accurate.** Matches `SPEC-008…#REQ-902`'s two halves; `TEST-1007:1176-1182` keeps the symbol-absence enforcement, and `local_demo` really is the gated fixture module. |
| SPEC-008 REQ-905 (hard stop) | Amended; carrier non-navigable, code a distinct human channel | **Accurate**, and `TEST-982:970-976` tests the negative across QR, logs, analytics, storage, relay frames and the hub request. |
| SPEC-008 REQ-906 (hard stop) | Replaced by REQ-932 | **In channel.** SPEC-008's Amendment Channels permit a versioned revision by the repository owner; this document is that request and names SPEC-008 (0.2.2) in `amends:`. |
| SPEC-008 REQ-907 (hard stop) | Retained | **Accurate.** `PRODUCTION_ALLOCATION_ENABLED = false` (`crates/selfsame-pairing/src/release.rs:11`) with no environment, feature, CLI or runtime override, and `REQ-942:486-487` refuses to be its enablement path. |
| SPEC-008 REQ-908 | Retained | Accurate. |
| SPEC-008 CON-903 | Retired from claimant admission | Accurate and consistent with `CON-923:598-603`. |
| SPEC-007 REQ-803 (hard stop) | Retained | Accurate. |
| SPEC-007 REQ-804 (hard stop) | Amended for v2 two-stage consent | Accurate and in channel. |
| SPEC-007 REQ-805 (hard stop) | Narrowly amended for anti-crowding | **Accurate and well argued.** The five-clause enumeration at `candidate:144` maps exactly onto REQ-805's prohibited categories; `T` never enters CPace (`REQ-933:196`) and is zeroized after claim. |
| SPEC-007 REQ-806 / REQ-807 (hard stops) | Retained | Accurate; the M-5 omission is repaired. |
| SPEC-007 REQ-809 (hard stop) | Retained without exception | Accurate. |
| SPEC-007 REQ-811 (hard stop) | Retained | Accurate — and it is REQ-811 that makes H-1's crowding cost a burned invitation. |
| SPEC-007 REQ-812 | Narrowly amended | Correctly identified as **not** a SPEC-007 hard stop; amendable in channel. |
| SPEC-007 REQ-813 / CON-806 | Amended by REQ-932/CON-923 | Accurate; CON-806 step 2's removal is now resolved for both roles. |
| SPEC-007 REQ-814 / CON-801 / TEST-818 | Retained; 62,000 / 62,001 | Accurate. |
| SPEC-007 NFR-801 and production gate (hard stop) | Retained | Accurate; the open-gates section is truthful on every item I could check. |
| SPEC-007 **NFR-802** | **Absent from the table** | Its *limits* are untouched; its *message set* and *mailbox record* are not — **H-3**. |
| SPEC-002 REQ-103 | Retained | Accurate against `specs/SPEC-002-…#REQ-103` and CON-221's LifeHash-is-an-aid rule. |
| **cbcl-pairing SPEC-001 REQ-002, REQ-005, REQ-008, REQ-009, REQ-013, REQ-014, REQ-015, NFR-009–NFR-012** | **No rows at all** | REQ-002/008/009/013/014/015 and NFR-009–012 are all *satisfied* on inspection; **REQ-005 is amended and REQ-003 is amended**, neither listed nor dispositioned — **H-3**. |

## State and effect-order audit

Legend: **HS-1** = preliminary exact-intent approval; **HS-2** = final release approval.

| Effect | Candidate's placement | Verdict |
|---|---|---|
| v2 carrier recognition | pre-everything, zero I/O (`REQ-932:160`, TEST-978) | OK |
| Unverified-origin prompt | before any I/O (`REQ-932:161-165`) | OK |
| Origin-ledger write + carrier burn | pre-consent, pre-I/O, separate installation key, no passcode | OK — M-4's ordering complaint is answered |
| Live CON-220 fetch | after accept, one fetch, no redirect, no cache | OK; pin now non-circular |
| CON-923 descriptor recognition | before policy lookup | OK; grammar defects M-2/M-3 |
| **Passcode + OS presence (policy)** | after descriptor recognition, before the relay socket; key zeroized before the socket | OK — the ordering gap is closed; feeds H-2's budget |
| Sealed exact-pair policy read/write | pre-consent, separately consented | OK; atomicity M-5 |
| Mailbox claim | authenticated `claim-v2` before membership | **H-1** — v1 `claim` bypasses it |
| CPace / Finished | pre-consent; binds both profile digests, carrier digest, every carrier field, mailbox ID, claim commitment, roles, v2 domain | OK — the strongest part of the revision |
| Signed-offer authorisation (complete CON-214) | pre-display | OK; **H-2** on the window |
| **Request-ID consumption** | at authorisation, before display, on approve or reject | OK — matches CON-214 step 5 and `app_grant.rs:379-389` |
| Display construction | only from `AuthenticatedIntent` (CON-926) | OK |
| Passcode + presence (preview), hierarchy derive | after HS-1, inside a zeroizing closure | OK |
| `HomeKey::home_did` + fingerprint | after HS-1, pure | OK — verified pure |
| `preparation` send (DID disclosure) | after HS-1, before HS-2 | OK, and now **named** as an irreversible residual |
| Authority query | before comparison display, authenticated, account-ID-keyed | OK — H-4 closed |
| Passcode + presence (commit), `issuer::create`, grant token, JWS | after HS-2 only | OK |
| DID publication | after HS-2 only, non-destructive, genesis content compared | OK |
| Pending issuance write, bundle build, reverse payload | after HS-2 only | OK |
| CON-206 steps 1-8 inside opaque WASM | on payload receipt, raw bytes | OK — M-3 closed |
| Hub alias command | after steps 1-8, before step 9 | OK in order; keying M-4 |
| CON-206 steps 9-13 + CON-207 | after the correlated hub success, real device key | OK |
| Atomic IndexedDB install | after CON-206 | OK |
| `pairaccountgrant` submit **and await** correlated ack | after install | OK |
| Receipt | only after the matching acknowledgement | OK |
| Wallet `linked` | only after verifying the receipt | OK |
| Compensation | only where a released grant may exist | OK |

**Conclusion.** Final release remains the unique hard stop before every
signature, publication, issuance write, bundle, alias and payload. The permitted
pre-final mutations are exactly three — the CON-214 request tombstone, the
separately consented policy entry, and the installation-keyed origin-attempt
record — each named, each argued, and the first sanctioned verbatim by CON-214.
This holds up under adversarial reading and is unchanged from 0.4.2's strongest
property.

## Authority and display-provenance audit

| Value | Candidate's source | Real authority at the baseline | Verdict |
|---|---|---|---|
| Application ID / origin | live authenticated profile | CON-220 dereference | **Sound** |
| Profile identity | both endpoints' independently computed digests bound into CPace + offer digest equality | genuine and non-circular | **Sound** — M-2 closed |
| Relay origin | carrier + live descriptor + signed hint, three-way equality | genuine | **Sound**, contingent on M-2/M-3 grammar |
| Relay operatorship | application assertion, never rendered as verified | application assertion | **Sound and honest** (REQ-941, ADR-920) |
| Account label | `SignedApplicationSession`, rendered "Application account asserted by \<origin\>" | hub authenticates a session key bound to a handle (`cbcl-chat-session-ws.lfe:231-247`) | **Sound and truthful** |
| **Account principal** | hub-assigned 32-octet `applicationAccountId`, session-key indexed | new; existing `cbcl-account-binding` is a *different* axis (handle → Selfsame account DID, `cbcl-chat-account-grants.lfe:29, 69-74`) and is not disturbed | **Sound** |
| Device key | distinct per `(applicationId, applicationAccountId, installation)`, retained, with PoP over ceremony/carrier/principal/offer | baseline mints a fresh per-ceremony seed (`enrol-allocator.mjs:60-62`) | **Sound; real new work, correctly slotted** |
| Account scope | hub-created, atomic, one per `(applicationId, applicationAccountId)` | matches CON-211's immutability and atomic-first-use rules | **Sound** |
| Co-presence | 128-bit `C` in CPace | never in a machine channel | **Sound as bounded**; forwarding residual declared |
| Anti-crowding | 128-bit `T` + relay-held commitment | relay verifies a bearer, derives no key, reads no body | **Sound as designed, incompletely applied — H-1** |
| CON-221 applicability | application, from the authenticated account-keyed query; claimant supplies no bit | CON-221 assigns it to the application, fail-closed | **Sound — H-4 closed** |
| Issuer-state provenance | opaque WASM object, raw bytes, internal replay, provenance set only after replay | replaces the unconditional stamp at `path_b.rs:428` | **Sound — M-3 closed** |
| Alias authority | authenticated hub command under stable account ID; browser cannot call the store | `provision/3` has no non-test caller | **Sound; keying detail M-4** |

## Implementability audit

### `cbcl-pairing` (`8e55160`)

| Slice | Feasible? | Evidence |
|---|---|---|
| v2 carrier + `PresenceCode { c, claim_token }` | Yes | The v1 decoder is a closed CBOR recogniser; a v2 type with no `secret` member is additive, and `CON-924:625-627`'s typed constructor makes a v1 constructor structurally unable to take these inputs. |
| PAIR1 codec | Yes | 275-bit big-endian string, 55 chars, fixed alphabet, 16-bit domain-separated checksum, three zero pad bits — closed and testable. |
| v2 context / domains / CDDL / dialect | Yes | `PairingContext::derive` already carries `application` and `relay_origin`; adding both profile digests, the carrier digest and the claim commitment is a context extension. |
| Seven-performative reducer with reverse payload | Yes, sizeable | `SessionPerformative` has four variants (`cbcl_protocol.rs:97-106`) and `apply_payload` hard-codes direction, so the reverse payload is a new arm. Role-specific constructors (`CON-927:707-709`) are expressible. |
| Fixed body classes | Yes | `application-payload.body` is `1..64512` and `sealed-plaintext.body` `1..65536`; the v2 objects at 64,560 / 4,144 fit. |
| Frame budget | Yes | Allocator 5, claimant 6, against `MAX_FRAMES_PER_MEMBERSHIP = 16` and `seq: 0..15`. |
| Commitment-authenticated `claim-v2` | **Partly** | The command is expressible; the commitment has no specified transport — **M-6** — and the v1 `claim` is not closed off — **H-1**. |

### `selfsame` (code tree `c6af6df`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Split `preview_issuer` / `commit_issuance` | Yes | Verified pure/first-signer facts above; `use_hierarchy_root` already runs a caller closure and zeroizes. |
| Deterministic preview ↔ commit equality | Yes | `hierarchy::derive` + `derive_did` are pure functions of root, application ID and scope. |
| Resolver publication adapter | Yes, new | No publish function exists today; `POST /dids` and `POST /dids/:did/deltas` exist and the genesis content hash agrees. |
| Separate CAS policy record + installation-keyed ledger | **Partly** | Cryptography and placement are fine; the atomicity claim is not supported by either backend — **M-5**. |
| Closed display provenance / injected intent verifier | Yes | `authorise::authorise` and `replay::consume` already exist and are delegated to rather than duplicated (`CON-926:694-696`). |
| Profile-version dispatcher | Yes, but **unowned** | `profile.rs` is pure core; no slice names it — **M-3**. Also `enrollment.rs:394, 552` hard-pin `profileVersion == PROFILE_VERSION`, so the CON-214/CON-219 recognisers must become version-aware; `CON-929:780-783` enumerates four amended fields and does not mention the version discriminator. |

### `cbcl-bus` (`fdbe414`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Server-built signed offer, no caller octets | Yes, real work | `sign-under-open-gate` is profile-bound and refuses an undeclared `kid`; statement construction must move hub-side from `enrol-allocator.mjs:74-80`; `CON-929:793-795` correctly forbids `/selfsame/enrolment/sign`. |
| Stable account ID + indexes | Yes | The wire key is Ed25519, seed-backed and durable; a key→ID index is straightforward; `cbcl-account-binding` is a separate axis and is undisturbed. |
| Distinct retained device key + PoP | Yes | Requires replacing the per-ceremony `randomBytes(32)` with per-installation IndexedDB retention — named in the browser slice. |
| Authority query command | Yes, new | Reads the account-keyed record the store already has (`cbcl-webfinger-account`), which no route currently exposes. |
| Alias acceptance command | Yes | `provision/3` exists with CON-204's checks and no non-test caller. |
| Storage migration | **Partly** | Table keying is mis-described and the withdrawal re-key is harmful — **M-4**. |
| Correlated acknowledgement | Yes, new payload | `pairaccount-standing-reply` builds from handle/fps/newfp; CON-932's added fields are a wire change the hub slice names. |
| Opaque acceptance object + direct resolver fetch | Yes | Needs did-crdt CORS plus the new WASM class; both are named. |
| Version-2 profile generator | Yes, but **unowned and conflicting** | `gen-production-profile.mjs` emits `profileVersion: 1`; cbcl-bus SPEC-075 REQ-007 forbids `rendezvous` — **M-3**. |

### `did-crdt` (`1f409a4`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Read-only CORS on public resolution | Yes | `build_router:141-160` has no `CorsLayer`; `rendezvous::cors_headers` is the in-repo pattern; `GET`/`HEAD`/`OPTIONS` only, no credentials, no write routes — as `CON-930:809-811` specifies. |
| Serialized idempotent non-resetting create | Yes | `create_did` does an unconditional `insert` (`handlers.rs:98`); the fix is a presence check plus a genesis-derivation comparison under the existing `docs.lock()`. |
| Causal chain after bootstrap | Yes | `content_hash` excludes the signature, `sign_genesis` reuses the same op and zero timestamp, `submit_delta` verifies post-genesis signatures (`handlers.rs:313-317`) and 404s an unknown DID. |
| Pin impact | None | The changes are service-layer; `selfsame-core`'s `Document::new` usage and the SPEC-001 pin comment in `Cargo.toml:43` are untouched. |

## Bounds and work audit

Every figure recomputed from the CDDL member sets, the named constants, and
RFC 8949 head encoding before comparison with the candidate.

**Retained outer bounds — all verified unchanged.**

| Layer | Candidate | Baseline |
|---|---:|---|
| v2 credential object | 62,000 | `crates/selfsame-pairing/src/lib.rs:85`, `cbcl-pairing/src/profile.rs:334` |
| application body | 64,512 | `pairing-v1.cddl:158` |
| sealed-plaintext body | 65,536 | `pairing-v1.cddl:135` |
| sealed plaintext | 69,556 | `cbcl-pairing/src/channel.rs:21` |
| sealed ciphertext | 69,572 | `pairing-v1.cddl:130` |
| relay frame body | 69,632 | `cbcl-pairing/src/mailbox.rs:15` |
| WebSocket message | 70,000 | `bin/cbcl-pairing-relay-ws.rs:32` |

**Encoder chain.** Application object 64,560 / 4,144 ✓; `sealed-plaintext`
overhead with a maximal 2,048-octet control exactly 2,068 ✓; plaintext
66,628 / 6,212 ✓; ciphertext 66,644 / 6,228 ✓; `sealed-frame` **66,696** / 6,278
(candidate says 66,694 for the large class — **M-1**); enclosing `put`
66,721 / 6,301, both under 70,000 ✓. Every retained bound holds with margin:
66,696 ≤ 67,000 ≤ 69,632.

**Frame count.** 2 large + 5 control application frames + 4 bootstrap = 11;
per-membership 5 (allocator: offer, comparison, receipt, cpace-a, finished-a) and
6 (claimant: intent, preparation, final, payload, cpace-b, finished-b), both
below `MAX_FRAMES_PER_MEMBERSHIP = 16` and inside `seq: 0..15` ✓.

**Work budget.** 2 × 67,000 + 5 × 7,000 + 4 × 8,192 = **201,768** ✓.
536,870,912 / 201,768 = 2,660.86 ⇒ "at most 2,660" ✓. `DEFAULT_MAILBOX_CAP =
10,000` × 201,768 ≈ 2.0 GB, so the 512 MiB queue binds first ✓. Queue accounting
adds on `Put` and subtracts on acknowledgement/terminal (`mailbox.rs:511-514,
644`) and `Stored { replay: true }` makes "exact retries reuse a sequence and do
not add a body" accurate ✓.

**Relay metadata.** 32 × 10,000 = 320,000 octets ✓. Per-attempt work is one
bounded parse, one SHA-256 and one constant-time compare ✓; the limiter
description is wrong — **L-1**; the mismatch path stores nothing, queues nothing
and does not extend TTL ✓.

**Not a bounds problem but a work problem the section still misses:** nothing in
`NFR-905` bounds who may claim through the **v1** command — **H-1**.

## Tests and gates audit

All 33 rows TEST-978 … TEST-1010 are present with no gaps and no duplicates.

| Row | Verdict |
|---|---|
| 978 zero-I/O recognition | Sound; panicking doubles are the right instrument. |
| 979 origin prompt / burn / rate limit | Sound; tests the negatives. |
| 980 pair scope | Sound and load-bearing. |
| 981 forged policy | Strong, and it records the absent rollback counter — but the crash half is not satisfiable (**M-5**). |
| 982 code distinct from recovery | Strong; covers both directions and every machine channel. |
| 983 co-presence / wrong-account corpus | Strong. |
| 984 transplant matrix | Strong, including both endpoints' profile digests and v1↔v2 replay. |
| 985 / 986 / 987 offer, mutation, prohibited display | Strong; the compile-time and symbol rows are checkable. |
| 988 CON-214 consumption | Correct, including the failures-before-step-5 distinction. |
| 989 / 990 / 991 / 992 effect gate | The best rows in the document; 992's preview-DID mutation case is exactly right. |
| 993 scope and device keys | Strong. **Depends on an unspecified mechanism**: `REQ-936:309-311` defers the "authenticated account-recovery transaction" to a separate specification, and 993 makes it release-blocking. Either specify it or mark the row deferred. |
| 994 no signing oracle | Strong; "no call edge" is checkable. |
| 995 independent vectors | **Honestly red**, matching `cbcl-pairing/vectors/README.md`. Cannot be produced without the L-7 preimage pin. |
| 996 reducer terminals | Sound and complete. |
| 997 exact bounds | **Asserts two numbers a conforming encoder cannot produce — M-1.** |
| 998 relay work | Right rows; **its observer assertion contradicts `REQ-933:207` — H-1.** |
| 999 idempotent create | Sound, and now honest about the bootstrap genesis. |
| 1000 browser resolver path | Now falsifiable: "Prove JS cannot construct/clone/serialize the object or pass a valid-looking boolean fact set" is exactly the M-3 repair. |
| 1001 CON-221 | Runnable now that the authority command exists; all four states reachable. |
| 1002 alias authority | Right rows; **missing the withdraw-then-re-provision case — M-4.** |
| 1003 / 1004 full acceptance and rollback | Strong. |
| 1005 acknowledgement correlation | Strong; delay/drop/duplicate/substitute plus account/principal/device/ceremony mismatch. |
| 1006 post-payload compensation | Strong; "never releases the same grant ID twice" is the right invariant. |
| 1007 fixture and prohibited-production absence | Strong and checkable. |
| 1008 local E2E | Correctly gated behind the production hold; **not reliably passable under H-2.** |
| 1009 profile version 2 | Right shape; **"Both roles agree exactly" is unsatisfiable under M-2 and "Version-1 recognisers continue … their unchanged ratified grammar" is false of the baseline under M-3.** |
| 1010 deadlines | Correct for the once-only evaluation; **no row asserts that a ceremony consuming the full pre-offer idle budget still succeeds — H-2.** |

**Missing rows.** The v1 `claim` byte sequence against a v2 mailbox (H-1); a
ceremony consuming the full pre-offer budget (H-2); an `operatorId` valid under
one candidate grammar and invalid under the other (M-2); withdraw-then-re-provision
(M-4); an unopenable policy record and its repair (M-5); the REQ-941 release-note
and first-use-prompt obligation and CON-925's three-operation rule (L-6).

**Gates.** The sequencing (`candidate:1230-1250`) is disciplined and correctly
ordered: review, then failing pure-core tests and vectors, then pure types, then
effectful shells, custody and resolver before wallet issuance, hub before
browser, then the full gate battery, then the production hold. The production
statements are truthful and verifiable against `release.rs:11` and its
invariance tests. The "Explicit open production gates" section is accurate on
every item I could check, including the absent human cryptography review and the
absent independent CPace implementation. Its coordinated-amendment bullet is
incomplete — **H-3**.

## Residual-risk audit

**Accepted declared residuals** — bounded, stated, and I would carry each
forward into a corrected 0.4.4:

1. **Deliberate forwarding of both channels.** Declared at `candidate:65-69`,
   `REQ-933:227-228` and ADR-917, mitigated by UI text and by the
   application-asserted account label. Bounded because both channels must be
   forwarded by the person, the code is visibly not a recovery phrase, and the
   consent screen names the live authenticated origin. This is the honest cost
   of standalone first contact. **Accept.**
2. **Application-declared relay use is not operatorship.** REQ-941 and ADR-920
   refuse to dress `operatorId` as proof and defer global operator credentials.
   Bounded by exact-pair person policy: trusting `(A, R)` does not trust `(B, R)`.
   **Accept.**
3. **Publication after final approval, before payload.** Amended REQ-812 permits
   a permanently resolvable DID when the payload never lands. Bounded by human
   final approval, by scope stability (no new DID or scope on retry,
   `REQ-940:468-469`), and by the approved-orphan rule. **Accept.**
4. **Home-DID disclosure in `preparation` before the final decision.** Pairwise,
   necessary for the comparison to exist, now explicitly disclosed at consent
   time and asserted by TEST-991. **Accept.**
5. **No rollback-resistant counter on the relay policy.** Declared at
   `CON-925:683-685` and recorded by TEST-981. Bounded because a rolled-back
   policy can only re-prompt, never silently grant. **Accept.**
6. **The application verifies its own acceptance.** CON-206 in the browser is the
   application checking a grant it will use, and the wallet's `linked` rests on
   the application's receipt. Inherent to the architecture; the provenance
   language no longer overstates it. **Accept.**
7. **Roughly 2,660 concurrent worst-case ceremonies per relay.** A real capacity
   reduction bought with fixed-class traffic opacity, correctly computed.
   **Accept**, and record it in operator-facing notes.

**Undeclared residuals that should be declared** (not defects in themselves):

8. Same-origin profile substitution defeats the two-digest CPace binding — **L-5**.
9. Three 64 MiB × 3-pass Argon2id derivations per ceremony, with no OS presence
   check available on desktop — **L-4**, and a direct input to **H-2**.
10. The 3-octet length variance inside the "fixed" size classes — **M-1**.

**Not residuals — defects:** H-1, H-2, H-3, M-1 through M-6. None of these is a
bounded declared cost; each is a false, unimplementable or untestable normative
claim.

## Method

Every claim above was checked against the confirmed revisions rather than
against the candidate's summary, its disposition table, the prior review's
conclusions, or source comments. Where a comment and the behaviour disagreed I
took the behaviour.

Baselines confirmed by `git rev-parse HEAD` and `git status --porcelain` in each
repository, and by `git diff --name-only c6af6df ea2773f | grep -v '^specs/'`
(empty) in the Selfsame worktree to establish that the candidate commit changes
no code. No file outside this review was modified.

The candidate document and the complete 0.4.2 review were read in full. Governing
text read in full: SPEC-004 CON-201, CON-204, CON-206 (all thirteen steps and the
freshness tiers), CON-211, CON-214, CON-219, CON-220, CON-221, REQ-215, REQ-216;
SPEC-007 REQ-803–REQ-814, NFR-801, NFR-802, CON-801, CON-806, the Amendment
disposition table and the Amendment Channels; SPEC-008 REQ-902–REQ-908, CON-903
and its Amendment Channels; SPEC-002 REQ-103–REQ-105; cbcl-pairing SPEC-001
REQ-002, REQ-003, REQ-005, REQ-008, REQ-009, REQ-013, REQ-014, REQ-015,
NFR-002, NFR-003, NFR-009–NFR-012 and its Amendment Channels; and cbcl-bus
SPEC-075 REQ-007.

Code paths traced end to end: `ApplicationProfile::{recognise, from_value}`,
`TOP_LEVEL_REQUIRED`, `PROFILE_VERSION`, `cbcl_pairing_relays`, `provider_id`,
`bounded_range`; `cbcl_relay::{select, verify_invitation_origin, eligible,
is_loopback}`; `HomeKey::{home_did, public_key}`, `hierarchy::derive`,
`selfsame_core::identity::{derive_did, bootstrap, sign_genesis}`,
`issuer::create`; `Custody::use_hierarchy_root`, `derive_wrapping_key`, the
Argon2id parameters, `SealedRoot`, `store::{get, set, delete, require_presence,
assert_durable}` on both backends, and `tauri-plugin-selfsame-store`;
`authorise::authorise` (including the single expiry check),
`ceremony::MAX_OFFER_WINDOW_SECONDS`, `enrollment::MAX_EVIDENCE_WINDOW_SECONDS`
and the `profileVersion` pins, `replay::consume`, `prepare_issuance`,
`confirm::{AuthorityState, Applicability::decide}`, `assemble_claimant`;
`verify_path_b_peer`, `verify_path_b_peer_json`, `BrowserClosure`,
`issuer_state_of`, `ClosureSource`; the cbcl-pairing invitation and frame CDDL,
`decode_client_message`, `encode_channel_frame`, `recognise_value`,
`PairingContext::derive`, `channel::MAX_SEALED_PLAINTEXT`, `mailbox::{apply,
Claim, queue accounting, MAX_FRAMES_PER_MEMBERSHIP, MAX_FRAME_BODY_BYTES}`,
`relay::{claim, open, member_command}`, `limiter::{Operation, PeerKey}`, the
relay WS constants, and `SessionPerformative`; `did-crdt`'s `build_router`,
`create_did`, `submit_delta`, `get_closure`, `Document::new`,
`SignedDelta::unsigned`, `delta::default_relationships`; and in cbcl-bus
`enrol-allocator.mjs`, `cbcl-chat-session-ws.lfe` (`resolve-key`,
`do-pairaccountgrant`, `pairaccount-standing-reply`),
`cbcl-chat-webfinger-store.lfe` (`ensure-tables`, `provision`,
`create-or-return`, `tombstone`, `already-withdrawn`, `tombstones-for`),
`cbcl-chat-account-grants.lfe`, `cbcl-chat-pairaccount.lfe`, and
`scripts/gen-production-profile.mjs`.

The arithmetic in the bounds section was recomputed from the CDDL member sets
and RFC 8949 §3 head encoding with an executable encoder model before comparison
with the candidate's figures. That is how the two-octet `sealed-frame`
discrepancy and the `logicalLength` variance were found; both are reproducible
from `wire.rs:877-888` and `pairing-v1.cddl:125-136` alone.

No implementation code, no candidate amendment text, and no other file was
modified by this review.
