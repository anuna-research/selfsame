---
id: SPEC-008-review-0.4.18-claude-2026-08-23
title: SPEC-008 0.4.18 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.11 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph]]"
prior-review: "[[spec-008-0.4.17-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.18 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.11 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session began. I
recorded the gate result first, and printed no secret value.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; fi
ABSENT
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "orgName": "<redacted>",
  "orgId": "<redacted>",
  "email": "<redacted>",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai` and the reported `subscriptionType` is `max`. Account identifiers
are redacted above and appear nowhere in this report.

The reviewing model is Claude Opus 5 in a fresh context. This session called no
Anthropic API and no external web service. Every input came from the four local
repositories, the local cbcl-rs checkout, and the local OTP install. The
generating sessions authored the three candidates. This session read the
candidates and the source, and never the generator reasoning.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `2e72066f223f4c3ce159ac3458a9e93ce9593d81` | candidate 0.4.18-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `b02a3f5b6fc618ab22abc034ad6db542196c8a41` | candidate 0.4.8-draft |
| cbcl-bus | `ca098028432748464c4d5b0dd52f8122d69331e4` | candidate 0.16.11-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The sibling working trees carry user-owned uncommitted
paths. I read those repositories only through `git show COMMIT:PATH`, and I
changed nothing in them. The local OTP install is `28.5` with `mnesia-4.25.3`.

I read the complete effective amendment chain from 0.4.9 forward. I also read
the full 0.4.17, 0.4.16, and 0.4.15 reviews and both coordinated candidates. I
read [[PROTO-002-selfsame-rendezvous-v1]] and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]].

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 2 | N3-H-1, N3-H-2 |
| MEDIUM | 1 | N3-M-1 |
| LOW | 3 | N3-L-1, N3-L-2, N3-L-3 |

Verdict: **REJECT**.

Three of the five 0.4.17 findings close cleanly against real code. The cap and
maximum labels are now separated and every residual re-derives. The sealed-grant
scope gap closes and both holders verify. The re-grant case is restated inline
in both current tests.

The rejection rests on the two ledger findings. The `N2-H-1` correction retired
two competing ledger clauses and left three earlier ones untouched. Four survivor
ledgers and three coordinated review sets therefore remain effective at once. The new
singularity check in
[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#TEST-1141]]
therefore fails against its own candidate. Separately, the first full
replacement of a ledger contract in this chain carries no clause preserving that
contract's own replacement relationships. The operator lock correction also
introduces one cross-table cycle that no clause names.

---

## Findings

### N3-H-1 (HIGH, release-blocking) — the ledger retirement is incomplete, so four survivor ledgers and three coordinated review sets stay effective

**Repository owner: selfsame (CON-977, and the untouched CON-971, CON-973, and
three test-specification preambles).**

`CON-977` at
`specs/trajectory/SPEC-008/spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph.md:172-188`
retires exactly two ledger clauses. It names sentence ranges inside
[[spec-008-0.4.15-authenticated-allocation-and-consent#CON-974]] and
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]]. I checked
each range boundary and each is unique inside its clause, so the two
retirements are decidable and correct as far as they reach.

The scope sentence then reads:

> No other CON-974 or CON-975 sentence supplies a survivor ledger,
> current-correction ledger, or coordinated-review pointer.

That sentence is true, and it is also the whole defect. It bounds the audit to
`CON-974` and `CON-975`. Three earlier clauses supply exactly the same three
ledger roles, and nothing in the chain retires any of them.

**Two earlier contracts still name a coordinated review set.** I traced every
reference to `CON-971`, `CON-972`, and `CON-973` across 0.4.12 through 0.4.18.
`CON-972` at
`spec-008-0.4.13-reachable-bounds-and-durable-authority.md:262` is retired for
coordination pointers by `CON-973` item 4. The other two are not referenced
outside their own amendment.

| Clause | Location | Stated coordinated review set |
|---|---|---|
| `CON-971` | `spec-008-0.4.12-tagged-transition-closed-authority.md:277-278` | cbcl-pairing 0.4.4, cbcl-bus 0.16.5 |
| `CON-973` | `spec-008-0.4.14-deployed-admission-and-local-unlink.md:219-222` | cbcl-pairing 0.4.6, cbcl-bus 0.16.7 |
| `CON-977` | `spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph.md:220-222` | cbcl-pairing 0.4.8, cbcl-bus 0.16.11 |

Self-reference does not make those two sentences historical. `CON-974` and
`CON-975` carry the identical self-referential form, and this amendment judged
both stale enough to require an explicit retirement. Under the candidate's own
standard, `CON-971` and `CON-973` are equally live.

**Three test-specification preambles still name a survivor set.** Each sits
between the `## Test specification` heading and the first `TEST-###` heading of
its amendment, so no numbered replacement reaches it. I searched the whole
chain for any clause that retires one, and found none.

| Location | cbcl-pairing survivors | cbcl-bus set |
|---|---|---|
| `spec-008-0.4.12-tagged-transition-closed-authority.md:304-306` | TEST-030 through TEST-055 | TEST-043 through TEST-075 |
| `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:314-317` | TEST-030 through TEST-056 | TEST-076 through TEST-079 tail |
| `spec-008-0.4.14-deployed-admission-and-local-unlink.md:246-251` | TEST-051 through TEST-057 tail | TEST-080 through TEST-083 tail |
| `CON-977` at `:209-218` | TEST-051 through TEST-056, TEST-059 | TEST-093, TEST-094, TEST-098 through TEST-100 |

**The 0.4.15 `L-1` defect is therefore still open.** That finding was that the
Selfsame ledger names a cbcl-pairing test which cbcl-pairing retires. The
0.4.14 preamble reads *"TEST-051 through TEST-057"*. Cbcl-pairing `CON-025` at
`specs/trajectory/SPEC-001/spec-001-0.4.8-split-room-shape-and-length.md:102-105`
states that `TEST-057` and `TEST-058` do not survive. The defective sentence
moved from `CON-974` to the 0.4.14 preamble. This amendment retired only the
first of the two.

**The candidate contradicts itself in two places.** `ADR-955` at `:242-244`
claims *"One effective ledger names every survivor, current correction, and
coordinated candidate."* That claim is false. `TEST-1141` at `:306-308`
requires:

> The checks fail when two effective Selfsame contracts state different
> cbcl-pairing survivors, cbcl-bus tests, current Selfsame corrections, or
> coordinated review sets.

`CON-971`, `CON-973`, and `CON-977` are three effective Selfsame contracts
stating three different coordinated review sets. The new check fails on the
candidate that introduces it.

The guard also has a scope hole independent of the contracts. It is written over
*contracts*, and the three survivor preambles are not contracts. Retiring
`CON-971` and `CON-973` alone therefore leaves *"every survivor and replacement"*
in the same sentence with four incompatible targets.

Only the current-correction ledger is singular. `CON-974`'s and `CON-975`'s
correction-test sentences are inside the retired ranges, `CON-976` is replaced,
and no preamble states a correction-test set.

The coordinated twin does not carry this defect. Cbcl-bus `REQ-072` at
`specs/trajectory/SPEC-053/spec-053-0.16.11-closed-ledgers-and-upgrade-free-operator-graph.md:30-53`
inherits from `REQ-070`, and I verified at `ca09802` that `REQ-070` states no
coordinated review set. A whole-chain search for a coordinated-set statement in
SPEC-053 returns only the 0.16.11 prose line. Cbcl-pairing carries exactly one
survivor sentence in its whole chain.

**Minimum sound correction.** Retire the coordinated-review sentences in
`CON-971` and `CON-973` by name, and retire the survivor paragraphs in the
0.4.12, 0.4.13, and 0.4.14 test-specification sections by name. Widen the
`TEST-1141` guard from *contracts* to every effective clause and unnumbered
normative paragraph.

---

### N3-H-2 (HIGH, release-blocking) — CON-976 is replaced in full with no clause preserving its four replacement relationships, so the 0.4.16 clauses can revive

**Repository owner: selfsame (CON-977).**

`CON-977` opens at
`spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph.md:168-170`:

> This contract replaces
> [[spec-008-0.4.17-complete-consent-and-executable-ledgers#CON-976]] in full.

`CON-976` at
`spec-008-0.4.17-complete-consent-and-executable-ledgers.md:258-269` carried the
only statement of four replacements:

```text
1. REQ-996  replaces REQ-994
2. NFR-919  replaces NFR-918
3. ADR-954  replaces ADR-953
4. TEST-1134..TEST-1138 collectively replace TEST-1129..TEST-1133
```

`CON-977` restates every other `CON-976` obligation. It restates the hard
stops, the survivor ledger, the coordinated set, both rendezvous baselines, both
parent pointers, and the authorization scope. The four replacement
relationships are the single thing it does not restate.

The omission is deliberate-looking, because the very next paragraph at
`:186-187` performs that preservation for the two clauses it partially retires:

> The numbered replacement relationships and hard-stop sentences in CON-974 and
> CON-975 remain effective.

`CON-976` receives no such sentence. The frontmatter adoption rule reads
`adopts: "All effective 0.4.4 through 0.4.17 clauses except the exact
replacements in CON-977"`. Two readings follow, and the text decides neither.

Under the first reading a replacement is a historical event, so retiring the
clause that effected it changes nothing. Under the second reading replacement
authority lives in currently effective text, so removing `CON-976` removes the
authority. The explicit preservation for `CON-974` and `CON-975` is evidence
for the second reading. On the first reading that preservation sentence is
unnecessary.

Under the second reading, four 0.4.16 clauses revive beside their successors.
Two of them carry defects that earlier reviews closed.

| Revived clause | Location | Consequence |
|---|---|---|
| `REQ-994` | `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:109-110` | restores the inverted rule *"No filter, paging, collapse, or lazy rendering SHALL hide no room"* |
| `NFR-918` | `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:207-258` | restores a lock graph with no symbol legend, no acquisition point, and no `revoke-handle-tx` clause |
| `ADR-953` | `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:305` | competes with `ADR-955` |
| `TEST-1129`–`TEST-1133` | `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:324-385` | competes with the current correction tests and with the `CON-977` ledger |

The `REQ-994` sentence is the exact 0.4.16 `N-H-1` HIGH defect. It carries two
negations over one obligation, and its plain reading licenses hiding a room. The
hard stop it touches is F-B, because a hidden room is a migrated room nobody
approved. The `NFR-918` gaps are the 0.4.16 `N-M-2` and `N-M-3` defects.

The coordinated twin again does not carry this defect. Cbcl-bus `REQ-072` opens
with *"The survivor lists and historical replacement relationships in REQ-070
remain effective."* That is the exact sentence the Selfsame contract omits.

**Minimum sound correction.** Add a sentence to `CON-977` preserving the four
`CON-976` replacement relationships by name, in the form `REQ-072` already uses.
Add a static check to `TEST-1141` that fails when a replaced clause has no
effective replacing clause.

---

### N3-M-1 (MEDIUM, gate-blocking) — the mandated reinstatement order creates a cross-table cycle with two unnamed deployed webfinger operations

**Repository owner: selfsame (NFR-920) and cbcl-bus (NFR-018).**

`NFR-920` at
`spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph.md:148-152`
states the corrected reinstatement order:

> Reinstatement takes an exact stable-account write lock first. It then takes a
> whole `cbcl-webfinger-withdrawn` write lock before its first `index_read`.

I verified that this order removes the 0.4.17 upgrade and is implementable at
the local OTP 28.5 install. `need_lock/4` at `mnesia_locker.erl:858-871` matches
the stored lock pattern exactly. A held `{Tab, ?ALL}` write entry therefore does
not satisfy a later `read` pattern, and `ixrlock/5` at `:1086-1102` still issues
its request. `can_lock(Tid, read, {Tab, ?ALL}, _)` at `:353-356` selects the
transaction's own write lock. `can_queue(Tid, Tid, _, Res)` at `:410` then
returns it as compatible. The later exact delete needs no request at all, because
`need_lock(Store, Tab, Key, write)` finds the stored `{{locks, Tab, ?ALL}, write}`
entry and answers `no`. The clause is sound in isolation.

The defect is what the added stable-account lock does to the graph.
`reinstate/2` at
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:295-300` touches exactly
one table today. `lift-tombstone/2` at `:302-308` reads and deletes
`cbcl-webfinger-withdrawn` and nothing else. The correction makes reinstatement
span two tables in the order account, then withdrawn.

`provision/3` at `:124-134` spans the same tables in the opposite order.
`create-or-return/3` at `:139-149` binds three reads:

```text
:140  mnesia:read 'cbcl-webfinger-withdrawn uri     'write
:141  mnesia:read 'cbcl-webfinger-alias     uri     'write
:142  mnesia:read 'cbcl-webfinger-account   account 'write
```

I verified that LFE evaluates those initialisers left to right rather than
leaving the order open. `lfe_translate:to_let_body/6` at
`_build/default/lib/lfe/src/lfe_translate.erl:1124-1128` emits the first
binding's match and nests the remainder inside it. `lfe_eval:eval_let/2` at
`lfe_eval.erl:721-734` folds the bindings in the same direction. So the deployed
acquisition order is withdrawn, alias, account.

That is the exact reverse of the declared tail order. `NFR-920` at `:139-146`
states withdrawal as stable account, then alias, then tombstone, and the
already-withdrawn branch as stable account, then a whole-table tombstone read.
Both match `tombstone/2` at `:206-229` and `already-withdrawn/2` at `:252-262`.
Neither matches `create-or-return/3`.

Two additional cycles follow, and neither is a projection of the twelve
authority cycles.

```text
provision  holds W(withdrawn, uri)      seeks W(account, acct)
reinstate  holds W(account,  acct)      seeks W(withdrawn, ?ALL)

lookup     holds R(alias,    uri)       seeks R(account,  acct)
withdraw   holds W(account,  acct)      seeks W(alias,    uri)
```

The second pair needs no correction to exist. `lookup-binding/1` at `:322-335`
reads the alias at `:323` and the account at `:327`. `tombstone/2` reads the
account at `:207` before `alias-agrees?/3` reads the alias at `:233`. `lookup/1`
is the public WebFinger read path called from
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-http.lfe:27`, so it runs against an
operator withdrawal in production.

The three webfinger tables are inside the declared scope.
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] at `:245-253` names
*"alias, account, tombstone, finalization, status, and compensation exact
keys"* in its `credential/v2 finalizer` row.

`NFR-920` at `:154-158` then closes the graph:

> The migration graph includes every lock and exact mode. … Its only cycles are
> projections of the twelve authority cycles. It has no additional cycle or
> upgrade. A missing operation, unstated mode, upgrade, opposite order, or
> additional cycle is non-conforming.

`provision`, `create-or-return`, `lookup`, and `lookup-binding` are missing
operations under that sentence, and both cycles are additional cycles under it.
`TEST-1140` at `:285-287` requires *"Generate the complete migration graph.
Require the restored acquisition point, invite-before-room order, and no
additional upgrade or cycle."* Cbcl-bus `TEST-099` carries the same sentence. A
faithful generator over the baseline plus the declared corrections produces both
cycles, so neither test passes and the GATE-04 operator-graph box cannot close.

Neither cycle loses durable state. Mnesia's wait-die aborts one transaction, and
both callers already map an aborted transaction to a closed failure:
`provision/3` answers `#(error unavailable)` and `lookup/1` answers
`'unavailable'`. This is a graph-closure and gate defect, not a safety defect.

**Minimum sound correction.** Name `provision`, `create-or-return`, `lookup`,
and `lookup-binding` in `NFR-920` and `NFR-018` with their exact modes. Either
state one webfinger acquisition order that all four operations share, or record
the two cycles as declared projections with their wait-die outcome. Add both
operations to the `TEST-1140` and cbcl-bus `TEST-099` operation lists.

---

### N3-L-1 (LOW) — the two new withdrawal lock claims have no named test operation

**Repository owner: selfsame (TEST-1140) and cbcl-bus (TEST-099).**

`NFR-920` adds two normative claims about withdrawal at `:142-146`. The first
covers the live-binding branch. The second covers the already-withdrawn branch
and its whole-table tombstone read.

Both verify against `cbcl-chat-webfinger-store.lfe`. `tombstone/2` at `:207`
takes the exact account write lock first, `alias-agrees?/3` at `:233` takes the
exact alias write lock, and `:222` writes the tombstone. `already-withdrawn/2`
reaches `index_read` through `tombstones-for/2` at `:267` and again at `:257`,
and writes nothing. The second index read issues no new request, because
`need_lock` finds the stored whole-table read entry.

The named operation list at `TEST-1140:289-293` reads:

> Include both `write-grant` paths, both `redeem-and-grant` paths,
> `revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`,
> `cbcl-chat-sealedgrant:put-tx`, `cbcl-chat-sealedgrant:get-tx`, and
> `cbcl-chat-webfinger-store:reinstate/2`.

`withdraw/2` is absent, and so are `tombstone` and the already-withdrawn branch.
Cbcl-bus `TEST-099` at `:346-350` carries the identical list. The 0.4.17 review
raised the same gap for `reinstate/2`, and this amendment added that one
operation without adding its sibling.

**Minimum sound correction.** Add `cbcl-chat-webfinger-store:withdraw/2` to both
operation lists, and require the trace to cover the already-withdrawn branch.

---

### N3-L-2 (LOW) — one leg of the three-way coordinated pointer graph names the superseded Selfsame candidate

**Repository owner: cbcl-pairing (SPEC-001 0.4.8 frontmatter).**

The coordinated set is a three-way pointer graph, and two legs resolve.
`CON-977` at `:220-222` names cbcl-pairing 0.4.8 and cbcl-bus 0.16.11. Cbcl-bus
`REQ-072` at `:55-57` names Selfsame 0.4.18 and cbcl-pairing 0.4.8.

The third leg does not. `spec-001-0.4.8-split-room-shape-and-length.md:10` reads
`consumer-design: selfsame SPEC-008 0.4.17-draft`. Cbcl-pairing was not
re-issued for this round, so its own pointer still names the candidate that
0.4.18 partially supersedes.

Nothing substantive depends on it. `REQ-030` is unchanged, the grammar block is
byte-identical across all three candidates, and `CON-025`'s survivor set matches
`CON-977` exactly. A reader following the pointer nevertheless lands on the
retired maxima labels that `REQ-997` corrects. The static checks in `TEST-1141`
and cbcl-bus `TEST-100` cover both parent pointers and neither covers this one.

**Minimum sound correction.** Point `consumer-design` at the current Selfsame
candidate, or state in `CON-977` that the cbcl-pairing pointer is deliberately
pinned to the predecessor.

---

### N3-L-3 (LOW) — one effective CON-975 sentence still requires a "maximum sealed record" after REQ-997 retires that label

**Repository owner: selfsame (CON-977 retirement ranges).**

`REQ-997` at `:40-49` reclassifies both recognizer ceilings. The 57,000-octet
and 62,000-octet limits are caps, and *"maximum"* now names only a
grammar-reachable value. `TEST-1139` and the cbcl-bus GATE-04 box both require
opaque cap bodies of 57,016 and 62,016 octets.

The `CON-977` retirement range inside `CON-975` ends at *"supplies authority."*,
and the second retirement covers only the coordinated-set paragraph. The
paragraph at
`spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:296-298` survives
untouched, and it reads:

> Both rendezvous implementations MUST carry generated maximum sealed records.

Under the corrected labels that obligation now names a sealed reachable maximum
of 41,324 octets rather than a cap body. The cap obligation is strictly larger,
so no implementation can satisfy one and fail the other. The residue is a
retired label surviving inside an effective MUST, in the one amendment whose
stated purpose is label and ledger singularity.

**Minimum sound correction.** Extend the `CON-975` retirement range to that
sentence, or restate it in `CON-977` using the cap vocabulary.

---

## Disposition of every 0.4.17 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N2-H-1** — three Selfsame ledger clauses contradict each other | **NOT CLOSED** | `CON-977` retires the `CON-974` and `CON-975` ledger roles correctly, and each named sentence range is unique inside its clause. The audit stops there. `CON-971` at `spec-008-0.4.12-tagged-transition-closed-authority.md:277-278` and `CON-973` at `spec-008-0.4.14-deployed-admission-and-local-unlink.md:219-222` still state coordinated review sets, and nothing in 0.4.13 through 0.4.18 references either clause. Three test-specification preambles still state survivor sets, at `0.4.12:304-306`, `0.4.13:314-317`, and `0.4.14:246-251`. The 0.4.14 preamble revives cbcl-pairing `TEST-057`, so 0.4.15 `L-1` also stays open. Filed as `N3-H-1`. |
| **N2-M-1** — reinstatement performs an unnamed `index_read`-then-exact-write upgrade | **CLOSED, with a new cycle** | `NFR-920` at `:148-152` states the whole-table write lock before the first `index_read`, names `lift-tombstone`, and states that the operation performs no upgrade. I verified implementability at the local OTP 28.5 `mnesia-4.25.3` install rather than from prose: `need_lock/4` at `mnesia_locker.erl:858-871` matches `LockPattern` exactly, `can_queue/4` at `:410` admits the transaction's own lock, and the later exact delete requests nothing. `TEST-1140` names `reinstate/2` and `lift-tombstone`. Cbcl-bus `NFR-018` and `TEST-099` carry the same text. The residual is the account-then-withdrawn order against `create-or-return`, filed as `N3-M-1`. |
| **N2-M-2** — the fresh-ceremony same-key re-grant test disappeared | **CLOSED** | `TEST-1140` at `:281-283` restates the case inline: *"Delete the hub grant and run a full new ceremony with the retained key. Require current eligibility, capacity, both consent decisions, and a new verified grant. Every local-only re-grant shortcut fails."* Cbcl-bus `TEST-099` at `:338-340` carries the identical paragraph inline and no longer points at the replaced `TEST-091`. Cbcl-bus `TEST-100` adds *"Require TEST-099 to state every installed-state case inline."* The governing rule verifies at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:200-203`, and `CON-977` keeps `REQ-995` effective. GATE-04 box 28 now names an obligation two current tests state. |
| **N2-M-3** — two recognizer caps were relabelled as maxima | **CLOSED** | `REQ-997` states *"The 57,000-octet offer recognizer limit is a cap, not a maximum"* and keeps the reachable complete offer at 41,308 octets. Every figure re-derives against [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] at `:178-180`: 57,000 − 41,308 = 15,692; 62,000 − 41,308 = 20,692; 57,000 + 16 = 57,016; 69,632 − 57,016 = 12,616; 62,000 + 16 = 62,016; 69,632 − 62,016 = 7,616. The AEAD premise verifies at `crates/selfsame-core/src/seal.rs:143-165`, where `seal_offer` and `seal_bundle` call `ChaCha20Poly1305::encrypt` with a fixed `Role::nonce()` and unstored associated data. The PROTO-002 cap verifies at `specs/PROTO-002-selfsame-rendezvous-v1.md:311-313` and `:716`. `REQ-997` at `:68-70` also closes the implementability question: *"A cap-transit vector is an opaque cap-sized octet string. It need not be a grammar-valid offer or credential member."* `TEST-1139` states both vector kinds separately. Cbcl-bus `REQ-073` and `TEST-098` match. One label residue survives outside the replaced range, filed as `N3-L-3`. |
| **N2-L-1** — one `cbcl-sealedgrant` holder fell outside the declared scope | **CLOSED** | `NFR-920` at `:105-106` widens the scope to *"every room, finalizer-tail, `cbcl-invite`, and `cbcl-sealedgrant` lock"* and at `:126-133` names both holders with exact modes. I enumerated every production holder at `ca09802`: a repository search outside `test/` returns only `apps/cbcl_chat/src/shell/cbcl-chat-sealedgrant.lfe`, whose two transaction bodies are `put-tx` at `:67-79` and `get-tx` at `:94-101`. `put-tx` reaches `cbcl-chat-invite:peek-tx` at `cbcl-chat-invite.lfe:75-78`, which uses `mnesia:read/2` on an exact key, then writes at `:73`. `get-tx` reads at `:95` with `mnesia:read/2` and deletes at `:98`, which is today's upgrade; the mandated `mnesia:read/3` with `write` removes it, and `need_lock` then suppresses the delete's request. `TEST-1140` names `get-tx` and requires the write lock on its first read. |

Three closed cleanly, one closed with a new cycle, one not closed.

## Disposition of every 0.4.16 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N-H-1** — the consent-surface completeness rule is inverted | **CLOSED, at risk from `N3-H-2`** | `REQ-996` at `:117-124` is outside the `REQ-997` replacement range, which starts at *"The maxima in"*. It keeps the cbcl-bus form and the universal display rule. The shipped code the rule targets verifies at `ca09802`: `affectedChannelsNow()` at `apps/cbcl_chat/priv/web/app.js:602-603`, call sites at `:6409` and `:6443`, and the painter loop at `:666-670`. `TEST-1134` is effective without amendment and carries the maximum-plan render case. The risk is that `N3-H-2` can revive `REQ-994` and its inverted sentence. |
| **N-M-1** — the v1 trace omitted four `payloadVersion` sites | **CLOSED** | The fifteen-line trace sits in the unreplaced part of `REQ-996` at `:158-176`. I read every line at the declared baseline `0220cec2` and each resolves to the stated construct. I then ran an independent exhaustive scan over all `crates/**/*.rs` for `PROFILE_VERSION`, `profileVersion`, `profile_version`, `evidenceVersion`, `evidence_version`, `payloadVersion`, and `payload_version`. Ten production files carry hits. Every omitted production hit is a field declaration, a member-name constant, a comment, a constant definition, or a runtime pass-through with no v1 pin. The three files whose omitted literals sit in tests verify: `#[cfg(test)]` opens at `crates/selfsame-app-identity-net/src/profile.rs:221`, `crates/selfsame-beam/src/enrollment.rs:199`, and `crates/selfsame-web-device/src/lib.rs:1521`. `lib.rs:1369` belongs, because `core.profile_version` is the literal set at `:1346`. |
| **N-M-2** — the lock-acquisition point and the `revoke-handle-tx` upgrade were dropped | **CLOSED** | `NFR-920` at `:121-124` restores both sentences and the list at `:154-157` retains *"opposite order"*. Every named site verifies at `ca09802`. `cbcl-chat-roommember.lfe:155` reads with `mnesia:read/2` then writes at `:161`. `cbcl-chat-account-roommember.lfe:67` does the same then writes at `:73`. `cbcl-chat-roommember.lfe:242-246` opens one transaction that deletes an exact key then calls `revoke-handle-tx`, which index-reads at `cbcl-chat-account-roommember.lfe:208` and deletes at `:209`. `migrate-legacy-handle-tx` at `:139-140` takes both whole write locks in the declared order before its `foldl`. |
| **N-M-3** — the twelve cycles used symbols whose legend was deleted | **CLOSED** | `NFR-920` at `:89-92` carries the complete legend, and the twelve-cycle block at `:94-100` is unchanged. The binding to [[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] stays bijective, because that matrix at `:245-253` has exactly seven rows and `unratified read` is its only read-only row. Cbcl-bus `NFR-018` at `:119-130` carries the identical legend and cycle set. |
| **N-M-4** — the parent pointer had no target | **CLOSED** | `specs/SPEC-008-production-pairing-claimant.md:8` now names `[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph]]`, `:9` carries the open candidate gate, and `:16-21` states the increment in prose while keeping the implemented 0.2.2 baseline distinct. The cbcl-bus parent resolves at `specs/SPEC-053-key-root-identity.md:15`, with `DEC-012` at `:635` and body references at `:688-691` naming 0.16.11. `TEST-1141` and cbcl-bus `TEST-100` both require the pair. |
| **N-L-1** — `socketGeneration` had no definition | **CLOSED** | `REQ-996` at `:83-96` is outside the `REQ-997` range and keeps creation, capture, cancellation, and reconnect semantics. `TEST-1134` is effective without amendment and exercises the replaced-socket case. |
| **N-L-2** — two different definitions of one shared grammar production | **CLOSED** | I extracted the six-line grammar block from all three candidates and compared them. `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:39-46` hashes to SHA-256 prefix `f01037367e6f7b65`, and the cbcl-pairing block at `spec-001-0.4.8-split-room-shape-and-length.md:33-40` and the cbcl-bus block at `spec-053-0.16.10-complete-consent-and-executable-ledgers.md:81-88` are identical text. Cbcl-bus `CON-034` at `:222-224` names cbcl-pairing `REQ-030` as the source. The alphabet matches `is_symbol_char` at `crates/cbcl-parser/src/parser.rs:328-342` in cbcl-rs `febc669`, character for character. |
| **N-L-3** — the transit ledger omitted the in-repository baseline | **CLOSED** | `REQ-997` and `CON-977` both name the two baselines. Both verify: `crates/selfsame-rendezvous/src/lib.rs:48` reads `pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`, and did-crdt `src/service/rendezvous.rs:65` carries the same constant at `1f409a4`. `TEST-1139` requires *"Prove both reviewed 4,096-octet baselines fail."* |
| **N-L-4** — the sealed-record maximum was never derived | **CLOSED** | `REQ-997` derives every figure and residual in one block, and the derivation re-computes exactly. See the `N2-M-3` row above. |
| **N-L-5** — one live `cbcl-invite` lock fell outside both graph scopes | **CLOSED** | `NFR-920` at `:105` owns every `cbcl-invite` lock, `:126-129` names `put-tx` and its exact invite read, and `:135-137` places `cbcl-invite` first in the generated table order. Both halves verify at `cbcl-chat-sealedgrant.lfe:67-79` and `cbcl-chat-invite.lfe:75-78`. |

Ten closed. One closure is at risk from `N3-H-2` rather than from any defect in
its own correction.

## Disposition of every 0.4.15 finding

| ID | Disposition | Evidence |
|---|---|---|
| **H-1** — unsigned advancing signal with no defined correlation | **CLOSED** | `REQ-996` at `:72-101` is outside the `REQ-997` range. It states the correlation member, its 32 random octets, its base64url encoding, and its one-use consumption, and it states plainly that the frame *"is unsigned and origin-less"*. `TEST-1134` remains effective and swaps, duplicates, suppresses, reorders, replays, mutates, and removes the value. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **CLOSED** | Both sites are present at `enrollment.rs:373` and `:662`, and `:452` carries the `recognizer substitution` label. My independent exhaustive scan found no missing production v1-pinned site. See the `N-M-1` row. |
| **M-2** — `write-grant` read-then-write upgrades | **CLOSED** | `NFR-920` at `:111-113` requires `mnesia:read/3` with `write` on the first read in both paths, and both deployed sites carry the upgrade today at `cbcl-chat-roommember.lfe:155` and `cbcl-chat-account-roommember.lfe:67`. |
| **M-3** — maxima never checked against the implemented rendezvous | **CLOSED** | `REQ-997` and `CON-977` state the dependency on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and [[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Both anchors resolve: `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:311-313` sets 69,632 octets, and `CON-304` at `:709-733` sets a `1..69632` body with `413` above it. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` remains effective and states the terminal at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:185-187`. `TEST-1140` at `:277-279` requires the terminal and zero enrollment frames. The independent hub-side guard is real at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`, which aborts with `'account-upgrade-required`. |
| **L-1** — survivor set revives `TEST-057` | **NOT CLOSED** | `CON-977` states the corrected set and retires the `CON-974` sentence that carried the defect. The 0.4.14 test-specification preamble at `spec-008-0.4.14-deployed-admission-and-local-unlink.md:246-251` still reads *"TEST-051 through TEST-057"*, and nothing retires it. Filed inside `N3-H-1`. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED** | `REQ-995` restates the unlink action in full. The cbcl-bus regression is repaired: `TEST-099` states every installed-state case inline and carries no pointer to the replaced `TEST-091`, and `TEST-100` requires that inlining. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-996` at `:53-63` splits shape from length, orders the four classes, and states *"Length classification occurs only after `RoomShape` passes."* Cbcl-pairing `REQ-030` at `:47-50` and cbcl-bus `REQ-071` carry the same order. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` remains effective and splits the triggers at `:174-183`. The silent path cannot widen consent, because `REQ-981` keys the person-owned policy on the exact pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-91`. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-996` at `:126-135` keeps `migration-display-state-changed` non-terminal and `migration-plan-invalid` terminal. Both remain implementable against the shipped reducer, whose `Burned` branch returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:172-174`. |

Nine closed. One is not closed and is filed inside `N3-H-1`.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-977` names six replacements and `REQ-981` is not among them. `REQ-981` still fixes the policy key as the exact `(applicationId, relayOrigin)` pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-91`, and it still states that a v1 policy entry cannot suppress the first v2 prompt. Cbcl-bus GATE-04 retains the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application, cross-checked before approval | **HOLDS, at risk from `N3-H-2`** | The wallet half is untouched. `crates/selfsame-pairing/src/live.rs:479-480` commits one explicit local decision only after `LiveEffect::DisplayIntent`, and cbcl-pairing `src/profile.rs:90-101` declares the recognised `DisplayIntent` shape. The five pre-consent checks in `REQ-996` at `:108-114` are outside the `REQ-997` range and remain ordered. The risk is `N3-H-2`, which can revive the inverted `REQ-994` display sentence. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-977` names nothing in `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202-231`. `REQ-995` still states that local unlink cannot re-grant authority and that a new grant needs both consent decisions. The verification gap the 0.4.17 review recorded is repaired, because `TEST-1140` and cbcl-bus `TEST-099` both state the re-grant case inline. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-977` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:903` still declares `selfsamepairallocate(rawCarrier, requestId, intentNonce, requestedPermissions, deviceKeyJwk)`, so the stable scope and the one pending ceremony still commit atomically. The `REQ-996` snapshot list adds no enrolment precondition, and `REQ-984` keeps `{"kind":"no-transition"}` legal. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-025` preserves the no-relay-field property of `CON-024`. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at cbcl-pairing `src/cbcl_protocol.rs:34` and `MAX_FRAME_BODY_BYTES = 69_632` at `src/mailbox.rs:15` both sit above the 57,016-octet and 62,016-octet sealed cap bodies. Cbcl-bus GATE-04 retains the human-acceptance box for relay-visible size correlation. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-331` reads *"Mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery."* Every negative path this amendment keeps or adds creates nothing. The two new lock cycles in `N3-M-1` abort a transaction and commit no partial effect, and both callers answer a closed failure. No finding in this review lets an endpoint failure authorise. |

## Repository assessments

### selfsame SPEC-008 0.4.18-draft (`2e72066`)

Artefact numbers are sequential with no gap and no reuse. `REQ-997`, `NFR-920`,
`CON-977`, and `ADR-955` each advance their series by one, and `TEST-1139`
through `TEST-1141` follow `TEST-1138`.

`REQ-997` is the strongest clause in the amendment. It separates the reachable
maximum from both recognizer caps, states each residual, and states that a
cap-transit vector need not satisfy the grammar. That last sentence is what
makes `TEST-1139` implementable, and it is the sentence the 0.4.17 review asked
for.

`NFR-920` closes both 0.4.17 lock findings against real code and adds the
sealed-grant scope. Its reinstatement order is implementable at the local OTP
install, which I verified in the lock manager rather than from prose.

`CON-977` is where the amendment fails. Its two sentence-range retirements are
precise and decidable, and its audit reaches two clauses out of five. It also
replaces a ledger contract in full for the first time in this chain, without the
preservation sentence its cbcl-bus twin uses.

Nine of the ten wikilink targets resolve inside this vault.
`[[SPEC-053-key-root-identity#DEC-012]]` is a cross-repository pointer whose
target lives in cbcl-bus, so it reports dead here. Under
[[PROTO-001-usdd-agent-protocol]] a dead link is visible debt rather than an
error, and I verified the target by hand at `ca09802`.

Open against it: `N3-H-1`, `N3-H-2`, a share of `N3-M-1`, a share of `N3-L-1`,
and `N3-L-3`.

### cbcl-bus SPEC-053 0.16.11-draft (`ca09802`)

`REQ-072` states the survivor and replacement ledger exactly, and its opening
sentence preserves both the survivor lists and the historical replacement
relationships in `REQ-070`. Its ledger chain is monotone: `REQ-070` states no
coordinated review set, and a whole-chain search for such a statement returns
only the 0.16.11 prose line. This is the pattern the Selfsame twin needs.

The GATE-04 restatement holds 42 boxes by mechanical count, which matches both
`TEST-100` and Selfsame `TEST-1141`. Every box remains open and the amendment
states that it closes none. The parent pointer resolves at
`specs/SPEC-053-key-root-identity.md:15`, with `DEC-012` at `:635` and two body
references at `:688-691`.

`REQ-073` drops the *"No base64 expansion applies to the opaque rendezvous
body"* sentence its Selfsame twin keeps, and it writes *"The fixed nonce"* where
`REQ-997` writes *"The fixed direction-separated nonce"*. Neither changes an
obligation, and both are recorded rather than filed.

Open against it: shares of `N3-M-1` and `N3-L-1`.

### cbcl-pairing SPEC-001 0.4.8-draft (`b02a3f5`)

Unchanged from the 0.4.17 round and still clean on substance. `REQ-030` carries
the `RoomShape` split, and its grammar block is byte-identical to both twins.
`CON-025` keeps the hub as the sole owner of the three admission terminals and
preserves every `CON-024` property. `NFR-022` keeps every authenticated byte
ahead of the owned allocation and keeps the verifier verdict-only.

The test inventory is `TEST-001` through `TEST-059` with no gap, and the
`CON-025` survivor set matches the Selfsame `CON-977` set exactly. This chain
carries exactly one survivor sentence, which is the property the Selfsame chain
lost. `PathAToBDisplay` and `CredentialV2Display` remain absent from `src/`, so
the typed display stays design rather than code. The surfaces they attach to are
real at `src/profile.rs:90-130`.

Open against it: `N3-L-2`.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged. `src/service/rendezvous.rs:65`
carries `MAX_SLOT_BYTES = 4096`, `:68` carries a 600-second TTL, and `:79`
carries a 65,536-slot ceiling. The working tree carries user-owned uncommitted
paths, which I ignored in favour of the declared commit.

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Whole-table write lock followed by `index_read`.** Verified at the local OTP
  28.5 install rather than from prose. `need_lock/4` at
  `mnesia_locker.erl:858-871` matches `{{locks, Tab, ?ALL}, LockPattern}`
  exactly, so a held write entry does not satisfy a `read` pattern.
  `can_lock(Tid, read, {Tab, ?ALL}, _)` at `:353-356` selects every write holder
  including the requester itself, and `can_queue(Tid, Tid, _, Res)` at `:410`
  returns that as compatible. Write-then-read is not an upgrade.
- **Exact write after a whole-table write.** `wlock/4` at `:826-843` calls
  `need_lock(Store, Tab, Key, write)`, which finds the stored table entry and
  answers `no`. The delete inside `lift-tombstone` therefore issues no request.
- **`mnesia:read/2` default lock.** Both `redeem-tx` at `cbcl-chat-invite.lfe:58`
  and `get-tx` at `cbcl-chat-sealedgrant.lfe:95` use the two-argument form, which
  takes a read lock. Both are real upgrades today and both corrections remove
  them.
- **LFE binding order.** The three `create-or-return` locks depend on
  initialiser order, so I verified the order in the compiler rather than
  assuming it. `lfe_translate:to_let_body/6` nests each remaining binding inside
  the previous match, and `lfe_eval:eval_let/2` folds left to right.
- **Sealed-grant contention.** Two concurrent `get-tx` calls on one expired token
  can abort one as cyclic under the corrected write lock, which loses no durable
  state. No operation takes `cbcl-sealedgrant` before `cbcl-invite`, so the
  declared table order holds.
- **Path-A finalization lock order.** `confirmed-migrate-path-a-handle` at
  `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:118-141` reads the
  binding with `'write`. It then reads `cbcl-member` with `'write` through
  `cbcl-chat-members.lfe:37`. It then takes both whole room write locks at
  `cbcl-chat-account-roommember.lfe:139-140`. It carries no upgrade and matches
  the declared prefix.
- **Withdrawal lock order.** `tombstone/2` takes account, alias, then tombstone,
  which matches the declared sentence exactly. The already-withdrawn branch
  writes nothing and its second `index_read` issues no second request.
- **Transit arithmetic and carriers.** Sealing adds only the 16-octet tag.
  `MAX_FRAME_BODY_BYTES = 69_632` and `MAX_ADJACENT_BODY_OCTETS = 65_536` both
  sit above the 62,016-octet sealed reverse cap body. Only the rendezvous lane is
  undersized, and both candidates say so.
- **Grammar drift between the pinned revisions.** The repository pins cbcl-rs
  `1c6fa8f` in `cbcl-rs.sha` while the candidates pin `febc669`. I verified that
  `1c6fa8f` is an ancestor of `febc669` and that
  `crates/cbcl-parser/src/parser.rs` is byte-identical between them, so the
  alphabet claim holds under either pin.
- **`EnrolmentAllocator::new_native` reachability.** The function is real at
  `crates/selfsame-web-device/src/lib.rs:1318`, and its only JavaScript-exposed
  entry is the constructor at `:1419-1431`. The prohibition names a real symbol.
- **Cap identifier spellings.** `V2_MAX_OFFER_PAYLOAD_OCTETS` and
  `V2_MAX_CREDENTIAL_MEMBER_OCTETS` appear only in specification text, never in
  code, so `REQ-997` relabels no compiled constant. The retired *"maximum"*
  labels in 0.4.11 and 0.4.12 are already inside replaced ranges.
- **Standalone first contact without enrolment.** The `REQ-996` snapshot list
  names an enrolled key, and `REQ-984` keeps `{"kind":"no-transition"}` legal. A
  first-contact account snapshots an empty room set and no member, so the clause
  adds no implicit enrolment dependency.
- **Credential/v2 lane presence.** I searched `apps/` and `priv/` at `ca09802`
  for `selfsamepairallocate`, `migrationRooms`, `roomSetDigest`,
  `socketGeneration`, `migration-plan-invalid`, and `credential-v2-canonical`.
  All six return nothing. That is honest planned work rather than an unsupported
  release claim.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and
   credential/v2 transport depends on it. Its own non-conformance list at
   `specs/PROTO-002-selfsame-rendezvous-v1.md:1016` records the reference
   implementation's 4,096-octet body.
2. **The whole credential/v2 lane is planned rather than shipped,** as the
   six-symbol search above records.
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt
   durable readiness, and the separate owner approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration,** and the cbcl-bus
   parent records that deferral as in force.
5. **The unary `mnesia:transaction` call sites in `apps/` remain unbounded
   today.** `NFR-920` mandates a change rather than describing current
   behaviour, and that change surfaces `authority-busy` on writes that
   previously blocked.
6. **The typed cbcl-pairing display is a design.** Its two named types are
   absent from `src/`, which both the amendment and the prior reviews record.
7. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** Several artefact references in the
   amendment bodies are also plain text rather than wikilinks. The parent
   [[SPEC-008-production-pairing-claimant]] carries both blocks. This is an
   inherited condition of the amendment form rather than a defect this candidate
   introduces, and no prior review filed it.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation
plan. It authorizes no code, no public allocation, no gate opening, no release,
no deployment, and no production enablement. Every GATE-04, `GATE-00`,
`GATE-01`, and `GATE-03` box stays open. Every human review, every durability
obligation, and the separate owner approval for production deployment also stay
open and unaffected.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N3-H-1 | selfsame | Retire the coordinated-review sentences in `CON-971` and `CON-973`, retire the 0.4.12, 0.4.13, and 0.4.14 survivor preambles by name, and widen the `TEST-1141` guard past contracts. |
| N3-H-2 | selfsame | Preserve the four `CON-976` replacement relationships in `CON-977`, in the form cbcl-bus `REQ-072` already uses, and check for a replaced clause with no effective successor. |
| N3-M-1 | selfsame, cbcl-bus | Name `provision`, `create-or-return`, `lookup`, and `lookup-binding` with exact modes, fix one webfinger acquisition order or declare both cycles, and add them to both operation lists. |
| N3-L-1 | selfsame, cbcl-bus | Add `cbcl-chat-webfinger-store:withdraw/2` and its already-withdrawn branch to both named operation lists. |
| N3-L-2 | cbcl-pairing | Point `consumer-design` at the current Selfsame candidate, or record the pin deliberately in `CON-977`. |
| N3-L-3 | selfsame | Retire or restate the surviving `CON-975` *"maximum sealed records"* obligation under the corrected cap vocabulary. |
