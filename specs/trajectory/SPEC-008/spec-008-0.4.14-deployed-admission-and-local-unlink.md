---
id: SPEC-008-amendment-0.4.14
title: SPEC-008 0.4.14-draft — Deployed admission and person-owned unlink
status: draft
version: 0.4.14-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.13-reachable-bounds-and-durable-authority]] only where [[spec-008-0.4.14-deployed-admission-and-local-unlink#CON-973]] says so"
adopts: "All effective 0.4.4 through 0.4.13 clauses except the exact replacements in CON-973"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.6-draft; cbcl-bus SPEC-053 0.16.7-draft"
implementation-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
review-input: "[[spec-008-0.4.13-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.14-draft — Deployed admission and person-owned unlink

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.13 review found that the proposed
> remote clearing event does not exist and that the grammar and contention
> ledgers did not describe deployed behaviour completely. This amendment closes
> those findings without changing the standalone architecture. It authorizes no
> implementation, allocation, release, or deployment before a fresh coordinated
> Tier-1 PASS.

Standalone first contact remains the target. The wallet authenticates the
application live, prompts on the exact `(applicationId, relayOrigin)` pair, and
derives or grants nothing before the applicable consent. Cbcl-pairing still
owns the CPace ceremony and the private authenticated display boundary. There
is no preliminary enrolment and no compiled relay allowlist.

## Requirements

### REQ-990 — Credential/v2 admission matches deployed syntax and names exclusions

The tagged transition shapes and member sets in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] remain exact.
Credential/v2 uses these ASCII grammars:

```text
LegacyHandle = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )

CbclSymbolChar = ALPHA / DIGIT / "_" / "-" / "." / "/" / "!" /
                 "?" / "+" / "*" / "<" / ">" / "=" / "@"
CanonicalRoomText = "@" 1*128( CbclSymbolChar )
```

`CanonicalRoomText` uses the complete deployed recipient-symbol alphabet at
the pinned cbcl-rs revision `febc6691e6dd2d5f7116b1a4d84c984b64717564`.
The 129-octet limit is a credential/v2 admission cap on an otherwise unbounded
CBCL symbol, not a claimed deployed maximum. It is 129 in both UTF-8 content
and RFC-8785 string-content units. The alphabet excludes quote, backslash, and
controls, so neither unit expands.

The hub reads the complete durable room set, removes duplicate raw byte values,
and sorts it by unsigned octets before allocating any public carrier. Every
room SHALL satisfy `CanonicalRoomText`; the set SHALL contain at most 256
values; and the derived RFC-8785 array SHALL contain at most 33,793 octets. A
deployed-legal room longer than 129 octets, an excessive count, or an excessive
aggregate returns the closed `MigrationPlanTooLarge` browser terminal before
carrier allocation. A durable value outside the deployed recipient alphabet
returns `MigrationRoomUnrepresentable` at the same boundary. Neither result
creates a partial array, digest, carrier, pairing room, or migration.

`LegacyHandle` is an explicit credential/v2 admission precondition, not a hub
invariant or a claim about every historical client. A current account whose
durable handle does not satisfy that grammar returns the closed
`MigrationHandleUnrepresentable` browser terminal before carrier allocation.
The terminal MAY render a bounded escaped preview and digest, but SHALL NOT
copy an unbounded value into the DOM. It creates no pairing or migration state.

For an admitted account, `roomSetDigest` and `migrationRooms` cover the same
complete sorted raw room-byte set required by
[[spec-008-0.4.8-standalone-tofu-first-contact#REQ-972]]. There is no truncation,
paging, hidden remainder, or partial migration. Exact room-list consent applies
only after all admission checks pass. Human migration review SHALL accept the
129-octet, 256-room, aggregate, and legacy-handle exclusions before release.

The exact grammar maxima remain a 33-octet admitted handle and a 129-octet v2
room. Therefore every arithmetic result and residual in
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remains exact.

Closed version dispatch also remains mandatory. Its named cross-crate trace
includes all of these production substitution sites at the implementation
baseline:

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts
crates/selfsame-web-device/src/lib.rs:1343-1361 EnrolmentAllocator::new_native
                                                   offer, hint, and statement
crates/selfsame-app-identity/src/enrollment.rs:452 statement builder
```

Credential/v2 SHALL NOT call `EnrolmentAllocator::new_native`. Its profile,
provider hint, offer, statement, builder, recognizer, signer, and profile-facts
paths use only the version selected by the recognized v2 profile. No v2 path
compares with or substitutes a v1 constant or version-1 literal. Live v1 bytes
and behaviour remain frozen.

### REQ-991 — Durable browser standing verifies the grant and clears only by consent

The browser's local terminal is evidence that a credential/v2 grant is
installed, not a claim that the hub currently retains that grant. Its atomic
installation record contains the retained device seed, accepted installed
grant, exact ApplicationId, account scope, device public key, and grant
reference. Reload reconstructs installed account-device state only when:

1. the record is complete and recognized;
2. the seed re-derives the recorded device key;
3. the installed grant verifies under the issuer selected by the current
   CON-220-authenticated application profile; and
4. the verified grant names that exact ApplicationId, account scope, device
   key, and grant reference.

Corruption, a partial record, a profile mismatch, an invalid grant signature,
or any binding mismatch fails closed as `credential-v2-recovery-required`.
In-memory state, a cache bit, handle equality, or seed possession alone is not
authority.

While the verified installation record exists, handle changes display
`account-device-handle-change-refused` and send no enrollment frame. The hub
independently applies its durable grant and member authority on every frame.

No hub error or chat frame clears the installation record. In particular, the
deployed zero-signature `(error @room "not-issued")` frame is unauthenticated,
uncorrelated, and account-ambiguous. It MAY display the non-destructive
`wallet-authorization-not-issued` status, but it SHALL NOT delete, alter, or
downgrade local credential state.

The only ordinary clearing transition is a person-initiated browser action
labelled `unlink Selfsame wallet`. It requires an exact confirmation screen,
sends no enrollment frame, and atomically removes the accepted installed grant
and its installed account-device projection. It retains the device seed under
the existing recovery policy. Cancellation and every remote error leave the
record unchanged. Clearing local state does not claim to revoke a hub grant or
restore legacy authority. Any later frame, re-link, or enrollment remains
subject to ordinary hub authority. A remotely revoked grant can therefore
remain locally installed and fail closed until the person explicitly unlinks
or re-links. That deliberate over-refusal is accepted instead of treating an
unauthenticated frame as a destructive protocol event.

## Non-functional requirement

### NFR-916 — Contention evidence is derived from the normative lock matrix

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
For evidence, use these abbreviations: grant creation `G`, credential/v2
finalization `F`, exact revocation `XR`, and observed revocation `OR`. Member
enrollment is `ME`, pairaccount claim is `PC`, and per-frame authority read is
`AR`.

The closed cyclic set is derived, not guessed. For each authority table it
contains every unordered operation pair, including a same-operation pair.
Both operations in such a pair can hold `{Table, ?ALL}` before an exact write
on that table. The set also contains every opposite-order exact guard, exact
record, and incompatible `?ALL` pair. Exact-key identity, guard identity, and
distinct-account witnesses are part of the derivation.

Applied to the normative matrix, the expected cyclic pairs are:

```text
grant-table read then exact write: G×G, G×F, G×OR, F×F, F×OR, OR×OR
grant/member cross-table order:    G×ME, F×ME
finalizer exact-member order:      F×PC, F×AR
device-guard/grant-table order:    XR×OR
pairaccount read then exact write: PC×PC
```

`G×PC` does not derive and is removed. `F×PC` uses distinct device guards. `F`
holds an exact member write before `PC` requests member `?ALL`. `PC` holds grant
`?ALL` before `F` requests its exact grant write. `XR×OR` uses distinct account
guards and a shared device guard. A generated lock graph SHALL
reproduce the twelve pairs from the exact implemented acquisition trace. Any
implemented lock-mode or sequence change that adds or removes a pair requires
a specification amendment before gate evidence can pass.

Separately, evidence SHALL exercise `AR` against every writer. The closed writer
set is `G`, `F`, `XR`, `OR`, `ME`, and `PC`. The evidence distinguishes one-way
contention from a cycle.

Mnesia wait-die remains the deadlock-prevention mechanism. Every affected
operation uses `mnesia:transaction/2` with an explicit non-negative integer
`Retries`, or `mnesia:transaction/3` with `Args` and an explicit non-negative
integer `Retries`. Exhaustion returns `authority-busy`, has no partial effect,
and refuses closed at every consumer. The representative load gate records each
derived pair, each reader-writer contention case, retries, progress, latency,
throughput, and decision correctness. It claims no unmeasured performance.

## Contract

### CON-973 — The correction and survivor ledgers are exact

This amendment applies only these replacements:

1. REQ-990 replaces
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-987]] and
   supplements its unchanged arithmetic in
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]].
2. REQ-991 replaces
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-989]] in full.
3. NFR-916 replaces
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#NFR-915]] in full.
4. This contract replaces
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#CON-972]] only for
   coordination pointers and the replacements above. Its clarified CON-960
   transition-member binding remains effective.
5. ADR-951 replaces
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#ADR-950]] only for
   deployed admission and browser clearing. Constructive-maxima discipline
   remains effective.
6. TEST-1121 through TEST-1124 replace
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#TEST-1117]] through
   [[spec-008-0.4.13-reachable-bounds-and-durable-authority#TEST-1120]].

All original F-A through F-E corrections and SPEC-007 REQ-812 remain hard
stops. The coordinated review set is this amendment, cbcl-pairing 0.4.6,
cbcl-bus 0.16.7, and did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`. A fresh PASS authorizes only the
Elephant SPL and test-first implementation plan. It authorizes no code or
deployment.

## Decision

### ADR-951 — V2 admits a bounded deployed subset and local unlink is person-owned

The deployed CBCL room language is broader and unbounded, while credential/v2
must remain bounded before typed-display allocation. The protocol uses the
deployed alphabet with an explicit v2 element cap. It rejects the complete
migration before carrier allocation when a durable value is outside the
admitted subset. The legacy handle grammar is an admission
precondition, not a historical hub invariant. This preserves exact room-list
consent and never silently drops authority.

The current hub has no authenticated, correlated, account-scoped refusal.
Inventing one solely to clear browser state adds a new signed protocol and
issuer lifecycle. This increment instead gives destructive local authority to
the person. Remote refusal is non-destructive; explicit unlink is confirmed,
atomic, and locally auditable. The resulting post-revocation over-refusal is a
known fail-closed tradeoff and a release-review item.

## Test specification

Every effective Selfsame test through TEST-1108 remains mandatory. The exact
cbcl-pairing survivor set is TEST-030 through TEST-049, TEST-050 except its
replaced control-object portion and retired 4,096-octet positive witness, and
TEST-051 through TEST-057. The exact cbcl-bus set is TEST-043 through TEST-065,
TEST-068, TEST-070, and TEST-080 through TEST-083. No superseded portion or
open-ended range supplies authority.

### TEST-1121 — Deployed syntax and admission exclusions are exact

Accept deployed rooms containing each of `/ ! ? + * < > = @`, including a
leading symbol such as `@_draft`. Construct 256 distinct sorted 129-octet rooms
and reproduce the unchanged 33,793-octet array and every REQ-988 total.

Reject a deployed-legal 130-octet room as `MigrationPlanTooLarge`; reject a
non-symbol durable room as `MigrationRoomUnrepresentable`; and reject a handle
outside `LegacyHandle` as `MigrationHandleUnrepresentable`. Each result occurs
before carrier, digest, pairing-room, typed-display, or migration allocation,
reveals no unbounded DOM text, and leaves every room present. Mutate count,
ordering, uniqueness, unit, cap, and aggregate by one and require refusal.

### TEST-1122 — Every v1 substitution site is excluded from v2

Trace all named v1/v2 profile, hint, offer, statement, builder, recognizer,
signer, and profile-facts sites. Include `selfsame-web-device` lines 600 and
1343–1361 and `selfsame-app-identity` enrollment line 452. Prove the v2 call
graph cannot reach `EnrolmentAllocator::new_native`. Mutate each comparison or
output substitution and require static and vector failure. V1 bytes remain
exact.

### TEST-1123 — Installed standing survives reload and only consent clears it

Install credential/v2 atomically and reload. Verify the grant under the issuer
from the authenticated application profile and match its application, account,
device, and grant reference. Mutate each field, signature, seed, and transaction
member; require `credential-v2-recovery-required`.

While installed, require local handle refusal and zero enrollment frames.
Deliver genuine and forged `not-issued`, every generic error, timeout,
disconnect, another account, and another key; none clears local state. Confirm
and cancel `unlink Selfsame wallet`. Only confirmation atomically removes the
grant and projection, retains the seed, and restores local controls without
claiming hub authority. A revoked-but-installed grant stays fail-closed until
that action.

### TEST-1124 — Derived contention and coordination ledgers are closed

Generate a lock graph from the implemented trace and require exactly the twelve
NFR-916 cyclic pairs. Trigger each pair and every `AR`-writer contention case.
Require bounded integer retry or `authority-busy`, zero partial effects, writer
progress, and correct decisions. Static checks reject every unbounded Mnesia
transaction overload.

Require the exact three-repository survivor lists, the cbcl-bus 0.16.7 GATE-04,
and its parent DEC-012 pointer. Remove one admission error, grant-verification
arm, substitution site, derived pair, contention case, gate box, or replacement
and require effective-text failure.

## Changelog

- **0.4.14-draft — 2026-08-23 — review candidate, not approved.** Aligns v2
  admission with deployed syntax, makes unlink person-owned, verifies durable
  grants on reload, derives contention evidence, and closes coordination
  ledgers. No implementation or deployment authority is claimed.
