---
id: SPEC-008-amendment-0.4.9
title: SPEC-008 0.4.9-draft — Live device authority and pre-authorization hub gate
status: draft
version: 0.4.9-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-807]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-226]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.2-draft"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007; cbcl-bus SPEC-053 GATE-00, GATE-01, GATE-03, and corrected GATE-04"
supersedes: "[[spec-008-0.4.8-standalone-tofu-first-contact]]"
adopts: "All 0.4.4 through 0.4.8 clauses except the exact replacements and corrections named by this amendment"
review-inputs: "[[spec-008-0.4.8-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.2-draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
owner-direction: "2026-08-23 — standalone first-contact option 1 is the target architecture; implementation remains subordinate to fresh review PASS"
last-updated: 2026-08-23
---

# SPEC-008 0.4.9-draft — Live device authority and pre-authorization hub gate

> **Design target, not implementation authority.** The fresh Claude Opus/max
> Circus review rejected 0.4.8 with one critical, four high, three medium, and
> seven low findings while explicitly accepting the standalone architecture.
> This amendment corrects the seams against the running hub and shared display
> API. No code, schema, fixture, vector, allocation, release, or deployment
> change is authorized until another fresh-context review returns PASS over the
> complete coordinated set.

## Review disposition

| 0.4.8 finding | 0.4.9 disposition |
|---|---|
| **C-1 — irreversible transaction makes the new key unratified** | [[spec-008-0.4.9-standalone-tofu-first-contact#REQ-973]] removes every credential/v2 pairaccount write and late cap. The exact device grant plus binding is Path-B authority; a post-commit ordinary frame is a mandatory assertion. |
| **H-1 — WS gate runs after TOFU and default fan-out** | [[spec-008-0.4.9-standalone-tofu-first-contact#REQ-974]] adopts the hub's pre-authorization `dispatch/3` gate and closed kind set. Production requests cannot reach binding lookup, verification, enrolment, persistence, or fan-out. |
| **H-2 — undefined fingerprint bytes/BLAKE3 parity** | [[spec-008-0.4.9-standalone-tofu-first-contact#REQ-975]] removes the fingerprint from the protocol digest, length-prefixes the DID, defines the device-key digest, and retains `Fingerprint::hex()` only as a wallet-owned visual rendering. |
| **H-3 — positive fixture opens an unnamed second gate** | REQ-974 names both source-constant overlays, splits production-artefact closure evidence from fixture evidence, and forbids a credential/v2 Path-B bypass. |
| **H-4 — the live enrolment signer is an unauthenticated widening oracle** | [[spec-008-0.4.9-standalone-tofu-first-contact#REQ-976]] freezes the existing route to v1 and makes authenticated held ceremony state the only v2 signing input. |
| **M-1 — impossible/absent digest recomputation** | REQ-975 requires the wallet to recompute the displayed room-set digest, states the exact per-party recompute/carry split, and treats legacy/snapshot digests honestly as authenticated hub assertions. |
| **M-2 — hub replies absent from `hub-originated?`** | REQ-974 adopts the hub's exact client/hub kind classes and requires every hub-minted member to be unforgeable, unpersisted, and unfanned. |
| **M-3 — typed transition has no carrier** | cbcl-pairing 0.4.4 REQ-026/CON-021 makes display fields private and supplies the closed verifier-only typed carrier adopted by [[spec-008-0.4.9-standalone-tofu-first-contact#CON-964]]. |
| **L-1 — nonexistent device count index** | [[spec-008-0.4.9-standalone-tofu-first-contact#NFR-911]] adds and migrates `cbcl-device-grant.account`; no full-table count is described as an index. |
| **L-2 — stale webfinger primary key** | [[spec-008-0.4.9-standalone-tofu-first-contact#CON-965]] replaces the adopted row with primary key `account`. |
| **L-3 — nonexistent gate stated as baseline fact** | Effective text says “on landing”, never “at the reviewed baseline”. |
| **L-4 — mixed NUL notation/field-vector overclaim** | REQ-975 uses one literal `0x00` rule and names the logical member vector while excluding its wall-clock field. |
| **L-5 — offer bound lacks arithmetic** | NFR-911 uses a generated `56,000 + 6,000 = 62,000` proof and records exact residual. |
| **L-6 — oversize silently excludes the person** | NFR-911 states that linking is unavailable for that handle in this version and requires a later reviewed bounded increment. |
| **L-7 — no allocator-observable no-offer test** | [[spec-008-0.4.9-standalone-tofu-first-contact#TEST-1101]] observes the relay and requires zero offer objects for every terminal authority result. |

The original 0.4.0 F-A through F-E corrections remain unchanged: policy is
exact `(applicationId, relayOrigin)`, the application and transition are
authenticated before display, wallet derivation waits for preliminary consent,
the hub supplies a pending fresh scope, and per-application consent remains the
accepted mitigation rather than a claim of relay operatorship binding.

## Requirements

### REQ-973 — A confirmed credential/v2 device is immediately live without pairaccount

This requirement replaces every credential/v2 `pairaccountgrant`,
`pairaccountgranted`, pairaccount-authority, pairaccount-compensation, and
unratified-cap step adopted from 0.4.4 through 0.4.8. SPEC-076 remains unchanged
and is not part of credential/v2.

After the wallet's exact final decision, issuance, resolver publication, raw
grant verification, and browser strict-durability installation, the browser
sends authenticated `selfsamepairaccept` carrying the held ceremony and signed
migration confirmation. The hub's one transaction stores the exact verified
`cbcl-device-grant`, account binding, reciprocal alias/account projection,
migrated account memberships, stable indexes, finalization, and immutable
status. It writes no `cbcl-pairaccount-grant` row.

The proposed device key must have no unratified pairaccount row before carrier
allocation and at finalization. A pre-existing collision refuses before the
wallet connects; a concurrent collision refuses the transaction with the
complete legacy state intact and triggers only pre-commit compensation.

After commit, the browser closes the legacy-key socket and reconnects with the
distinct granted device key. In the named compiled-open fixture that key passes
the unchanged `cbcl-core-enrol:resolve` unratified check, ordinary Path-B gate,
device grant, membership, and permission checks. Only that new-key session may
recover `selfsamepairstatusresult`; only the byte-identical accepted status may
produce the claimant receipt and linked record.

The commit remains irreversible protocol acceptance. Lost acknowledgement is
recovered by status; no timeout restores legacy rows, deletes the binding, or
revokes the grant. A later unlink is a new confirmed protocol.

### REQ-974 — Production dispatch is the pre-authorization gate; the fixture opens two named gates

The wallet adopts cbcl-bus 0.16.2 REQ-042/REQ-044 and CON-015. On landing,
`cbcl-chat-credential-v2-gate:profile/0` is the sole credential/v2 enablement
point in each artefact and is a source constant. Production `dispatch/3`
classifies the exact six credential/v2 client kinds and six credential/v2
hub-result kinds plus `account-linked`, consults the closed gate, and returns
one opaque answer before binding lookup, key resolution, verification,
`authorise-frame`, TOFU enrolment, allocation, persistence, resolver/relay I/O,
or fan-out.

In any build that can emit them, every hub-result kind is a literal member of
`cbcl-core-dialect:hub-originated?/1`; a client-sent instance is `hub-only` and
cannot reach `do-publish`. No credential/v2 kind uses the default publisher.

The positive E2E build is explicitly
`credential-v2-e2e-fixture/NON_PRODUCTION_TWO_TIER1_GATES_OPEN`. It compiles
test-only source replacements for both
`cbcl-chat-credential-v2-gate:profile/0` and
`cbcl-chat-path-b-gate:profile/0`. It reads no runtime flag and may use only
local test relay/resolver origins. Credential/v2 code cannot bypass, cache,
shortcut, or special-case Path-B admission.

The same CI invocation first builds the production release and runs its exact
closed-constant, hostile-config, dispatcher, route, UI, relx, source-manifest,
and binary checks. It then builds the separately marked fixture and runs the
positive path. The fixture runtime does not assert its gates are closed; its
compensating controls are mandatory marker/local origins, production build
first, explicit source exclusion, and release-task refusal of any fixture/open
artefact. No Fly configuration can open production.

### REQ-975 — The person approves rooms bound by an implementable digest graph

The wallet and browser adopt cbcl-bus 0.16.2 CON-013 byte-for-byte. Every digest
domain terminates in exactly one `0x00`. The migration confirmation covers the
raw offer-core digest; a `u32be`-length-prefixed canonical account-DID UTF-8
string; authenticated legacy-key, room-set, and snapshot digests; snapshot
nonce; and `SHA-256(RFC8785(deviceKeyJwk))`.

No fingerprint rendering enters a protocol digest. The final surface still
renders `Fingerprint::hex()` from the locally derived DID under
[[SPEC-002-visual-key-fingerprint#REQ-103]]. Its correctness is a wallet/UI
test, not a hub BLAKE3 parity claim.

Before preliminary display, the wallet recomputes `offerCoreDigest`, descriptor,
carrier, intent, device-key, and `roomSetDigest` from the exact complete
`migrationRooms` it will display. A list/digest mismatch is terminal before the
person sees an approval affordance. The wallet cannot recompute the raw legacy
key or Mnesia snapshot; it accepts `legacyKeyDigest` and
`migrationSnapshotDigest` only as signed live-application assertions and copies
those authenticated raw values into the confirmation. The hub recomputes them
from locked state at finalization. The browser recomputes every input it holds.

The logical `cbcl-member` snapshot vector is exactly type tag, handle, and raw
enrolled public key; it deliberately excludes the stored wall-clock `enrolled`
attribute. Snapshot primary keys make duplicate rows impossible; any duplicate
input is terminal.

### REQ-976 — Enrolment-v1 signing and credential/v2 signing have disjoint grammars and call graphs

The app-identity implementation exposes a frozen v1 recognition/signing entry
that accepts exactly current CON-214 `evidenceVersion = 1`,
`profileVersion = 1`, and the current closed member set. Adding the distinct
profileVersion-2 credential/v2 grammar does not widen that entry. The cbcl-bus
NIF route `sign_enrollment_v1_profile_bound` calls it exclusively.

The deployed `/selfsame/enrolment/sign` remains usable for the existing CON-219
enrolment ceremony and remains an unauthenticated server-side v1 oracle under
its recorded posture. It refuses every v2/mixed statement before reading the
seed and never returns a v2 signature. Adopted TEST-1027's overbroad sentence
“an unauthenticated caller obtains no signature” is replaced by “an
unauthenticated caller obtains no credential/v2 signature”.

Credential/v2 evidence is signed only through the authenticated hub
`selfsamepairofferfinalize` path after both Finished values. The hub constructs
the exact statement from the live session and single-use held ceremony; request
bodies can supply only the held-core digest and proof under its exact device
key. Caller-provided statement/JWS octets, version choice, application ID,
scope, account principal, transition, or display fields are not accepted.

## Non-functional requirement

### NFR-911 — Bounds, device index, and continuity ownership are exact

The complete RFC-8785 OfferCoreV2 is at most 56,000 octets; the complete unique
room array is at most 256 entries and 48,000 encoded UTF-8 octets; every other
logical member has a combined generated maximum of 6,000 octets; therefore the
large logical body is at most 62,000 octets. The schema generator encodes all
fields at maxima and records the actual total and residual. A field-cap change
without a new passing arithmetic vector is a manifest failure.

`MigrationPlanTooLarge` before carrier allocation is a terminal exclusion: the
person cannot link that chat handle through credential/v2 in this version.
There is no partial, truncated, paged, console, or enrolment fallback hidden
behind that result. A multi-batch or subset-consent solution is a later
separately reviewed Tier-1 increment.

The hub adds and verifies a secondary index on `cbcl-device-grant.account`.
Every grant writer takes the same exact stable-account write lock before its
indexed device-cap read, preventing distinct-key phantom inserts at the cap.
Restore evidence covers the index definition and query result. The pairaccount
table remains in the identity/
per-frame continuity manifest solely because ordinary authorization reads it;
loss is fail-open for unratified devices. Successor loss remains fail-open for
spent keys.

The manifest explicitly excludes `cbcl-invite` as invitation/admission
continuity, and `cbcl-provenance` plus `cbcl-epoch-claim` as room/MLS
continuity. It makes no recovery claim about them. All alias, withdrawal,
binding, device, member, room, account-room, stable-account, scope, pending,
finalization, status, compensation, pairaccount, and successor state retained
by 0.4.8 NFR-910 remains in scope.

## Contracts

### CON-962 — Effect order ends in one live-device transaction

The effective order is:

```text
closed dispatch gate
-> live signed Path-A session
-> stable membership snapshot + pending account/scope/ceremony
-> public carrier
-> CPace + both Finished
-> server-built signed offer finalization
-> reciprocal authority result; allocator releases no offer on terminal
-> typed authenticated transition; wallet verifies displayed roomSetDigest
-> preliminary display and decision
-> zeroizing local DID preview + browser comparison
-> final DID/fingerprint/complete-room display and decision
-> wallet re-derives, signs, publishes, and issues
-> browser verifies and installs the distinct device key locally
-> authenticated selfsamepairaccept performs one device-grant migration commit
-> new-key Path-B reconnect and immutable status/ack recovery
-> receipt
-> wallet linked record
```

No wallet custody access, derivation, signing, publication, grant, or durable
identity state precedes preliminary approval. No hub binding, grant, alias,
account membership, or legacy-room deletion precedes signed final approval,
verified publication/grant, and browser install. Pending hub allocation and the
wallet request tombstone plus separately accepted TOFU/policy/rate state remain
the only permitted earlier mutations.

`selfsamepairaccept` replaces the earlier pending-alias-only meaning and all
subsequent pairaccount steps. Its hub result is `selfsamepairaccepted`.
`selfsamepairstatus`/`selfsamepairstatusresult` recover that exact immutable
outcome; neither can mint a second grant or migrate twice.

### CON-963 — Canonical confirmation and room-display check

The consumer implements the exact cbcl-bus CON-013 digest definitions, including
the confirmation-v2 domain, one-`0x00` rule, DID `u32be` length, RFC-8785 device
JWK hash, raw digest concatenation, and per-party recomputation table. The
credential_v2 corpus carries positive preimages/outputs and one mutation per
byte-bearing member in Rust, JavaScript, and LFE/Erlang.

The typed transition object retains the authenticated room array and supplied
room-set digest together. `IntentVerifier` recomputes the digest before it can
return success; cbcl-pairing therefore cannot emit the typed display for a
mismatch. The final display borrows that same immutable array and the one
retained intent digest. No UI callback can substitute a list or boolean.

### CON-964 — Shared typed display and frozen v1 boundary are owning changes

The consumer adopts cbcl-pairing 0.4.4 CON-021. `DisplayIntent`,
`DisplayField`, `CredentialV2Display`, and `PathAToBDisplay` have private fields
and read-only accessors. The injected credential/v2 verifier receives immutable
parsed input and returns only success/failure; the shared profile alone
constructs display. Credential/v2 never uses a generic field or the legacy
`claimed_by_secret_holder` bit.

The consumer also implements distinct app-identity functions for frozen
enrolment-v1 and credential/v2 recognition. Source/call-graph tests prove the
v1 signer cannot reach a version-general/v2 parser and the v2 verifier cannot
reach the HTTP v1 signing facade.

### CON-965 — Effective-text correction ledger

The effective set applies these replacements:

1. Every credential/v2 pairaccount effect in 0.4.4–0.4.8 is replaced by
   REQ-973/CON-962 and cbcl-bus REQ-041/CON-014. SPEC-076 is unchanged.
2. 0.4.8's handler-first/parser-absence GATE-00 shape is replaced by REQ-974's
   dispatch-before-authorization recogniser and two-artefact controls.
3. 0.4.8's fingerprint-bearing confirmation is replaced by REQ-975/CON-963;
   there is no hub fingerprint renderer.
4. 0.4.3's prose-only transition is replaced by cbcl-pairing 0.4.4.
5. Adopted CON-941's `cbcl-webfinger-account` primary key is `account`, not
   handle; its reciprocal store has no production caller before this increment.
6. The credential/v2 gate is an on-landing requirement, not a fact about the
   reviewed source baseline.
7. Every digest domain ends in one `0x00`; “logical member vector” excludes the
   `cbcl-member.enrolled` wall-clock field.
8. The offer core cap is 56,000 with a generated 6,000-octet non-core budget;
   oversize excludes that handle from this protocol version.
9. Every terminal reciprocal/authority result produces zero `offer` objects at
   the blind relay; this is allocator/relay-observed, not inferred by the wallet.
10. Adopted TEST-1027 requires that an unauthenticated caller obtain no
    credential/v2 signature; existing v1 CON-219 signing remains outside that
    claim.
11. `cbcl-device-grant.account` is a real migrated index; pairaccount remains a
    read-only negative per-frame input for credential/v2.
12. `cbcl-invite`, `cbcl-provenance`, and `cbcl-epoch-claim` have the explicit
    continuity exclusions in NFR-911 rather than falling through categories.

All prior 0.4.7/0.4.8 corrections not named above remain effective, including
the reachable Path-A entry, no console migration, fresh pending scope, exact
pair TOFU, same-invocation root correction, inert rendezvous-v1 dispatch, actual
membership primary keys, successor/pairaccount loss consequences, and the
cbcl-pairing TEST-050 replacement.

## Decisions

### ADR-943 — A confirmed credential is not an unratified pairing claim

The SPEC-076 ledger intentionally withholds authority pending a future owner
ratification UX. Credential/v2 already has two explicit wallet decisions and a
verified device grant. Combining them would convert accepted authority into an
inert key and lock the person out. The protocols share a device key only as a
negative collision input; they do not share a grant row or finalization verb.

### ADR-944 — Version-specific signing preserves both deployed and new flows

Removing enrolment signing would break the deployed sanctioned enrol-once path,
while widening its unauthenticated input grammar would defeat credential/v2
application authentication. A frozen v1 function preserves the former. A
server-state-only v2 signer provides the latter without letting one endpoint
choose another protocol's evidence version.

## Test specification

All effective TEST-1011 through TEST-1091 remain release-blocking except where
CON-965 replaces an expectation. cbcl-pairing TEST-030 through TEST-055 and
cbcl-bus TEST-043 through TEST-056 are mandatory.

### TEST-1092 — Real Path-A pairing exits under a live granted key

Use the real local browser, relay, wallet, and two-gate-open hub fixture. Require
the complete CON-962 order, no enrolment ceremony or held trust record, one
migrated account, unchanged pairaccount table, new-key ordinary Path-B frame,
status/ack/receipt, and one wallet linked record. The legacy key is refused.

### TEST-1093 — Production WS credential kinds are zero-effect refusals

For every client and hub kind, vary handle enrolment, signature, room standing,
body, configuration, and downstream state. Against the production artefact
require one opaque result and byte-identical member/message/blob/room/relay/
resolver state. No kind reaches `do-publish`. Against the fixture require hub
kinds client-sent to be `hub-only`, unpersisted, and unfanned.

### TEST-1094 — V1 and v2 signing cannot cross

After linking both grammars, run the complete v1 route corpus and v2 held-state
corpus. Direct HTTP v2/mixed statements receive opaque 503 before seed access;
valid v1 remains byte-compatible. V2 offer-finalization accepts no statement or
JWS octets and signs only state recomputed from the live session and ceremony.

### TEST-1095 — Typed transition construction is closed

Run cbcl-pairing TEST-053/054 through Selfsame. Compile-fail every generic or
struct-literal display construction; mutate every authenticated transition
member; require no display on verifier refusal. The UI receives read-only typed
fields and final display borrows the exact rooms/intent. Prove the mutable
profile owns and invokes the verdict-only verifier without an interior/global
escape hatch.

### TEST-1096 — Displayed rooms and confirmation digests agree independently

Reproduce the CON-963 corpus in three stacks. Mutate the room array without its
digest and refuse before display. Mutate every digest input and refuse at its
owning verifier. Prove no fingerprint enters the wire digest, the wallet renders
the normative hex from the local DID, and every party recomputes only held
inputs.

### TEST-1097 — Device-only migration is atomic under every race

Inject abort/crash at every transaction step and race membership, binding,
device cap/index (including two distinct keys at the last slot through different
grant writers), unratified collision, alias, withdrawal, and second ceremony.
Recovered state is wholly legacy or wholly accepted. No pairaccount row changes.
After accepted commit the new key passes the unchanged reducer and lost-ack
recovery never migrates or grants twice.

### TEST-1098 — Production closure and the two-gate fixture cannot cross

In one CI flow build/scan production first, then the marked fixture. Require
both exact source overlays only in the latter, ordinary Path-B gate use, local
origins, and visible warning. Attempt every release path with fixture material
and require build failure. Hostile runtime configuration never opens production.

### TEST-1099 — Every hub result is unforgeable

Enumerate the cbcl-bus REQ-042 hub set and require membership in
`hub-originated?/1`. A validly signed client-authored instance produces
`hub-only`, zero persistence, and zero fan-out. Mutation removal of any member
makes the test red.

### TEST-1100 — Bounds, indexes, and continuity claims are exact

Generate the maximum legal logical body and exact residual. Exercise every
boundary and terminal oversize copy. Verify the device account index migration
and equality with a reference scan. Delete/restore every in-scope authority
class and prove both fail-open consequences; verify invite/provenance/epoch rows
are named exclusions rather than silently covered.

### TEST-1101 — Effective text and relay withholding have no stale branch

Static/spec/code checks require every CON-965 replacement and reject any
credential/v2 pairaccount call, late handler gate, generic display constructor,
hub fingerprint renderer, version-general v1 signer, Path-B bypass, nonexistent
index, stale webfinger key, 60,000 core cap, or partial oversize fallback. For
every terminal authority cross-product, observe the blind mailbox and require
zero `offer` objects.

## Implementation and gate sequencing

The coordinated repository slices remain design-only until PASS. A PASS
authorizes an Elephant SPL/test-first plan, not production enablement.

1. Run a fresh-context Claude Opus/max Circus review over this amendment,
   cbcl-pairing 0.4.4, cbcl-bus 0.16.2, the complete adopted set, this review's
   1C/4H/3M/7L findings, and the exact four code baselines.
2. Only after PASS, encode every effective test as executable Elephant gates.
3. Land red schemas/types/vectors/pure reducers first; then wallet/hub/resolver
   effects and crash tests; then dormant production dispatch and test-only E2E
   wiring.
4. Keep production credential/v2 allocation closed. GATE-01, GATE-03, GATE-04,
   human cryptography/migration review, independent CPace evidence, both
   durability gates, privacy/rollback review, owner approval, and separate
   deployment confirmation remain required.

## Changelog

- **0.4.9-draft — 2026-08-23 — review candidate, not approved.** Keeps the
  standalone Path-A→B architecture, makes its new device immediately live,
  moves hub gating before authorization/TOFU, freezes the deployed signer to
  v1, closes typed display construction, and corrects every 0.4.8 finding. No
  implementation, production allocation, or deployment authority is claimed.
