---
id: SPEC-008-amendment-0.4.21
title: SPEC-008 0.4.21-draft — Singular ledgers and whole-graph retries
status: draft
version: 0.4.21-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy]] only where [[spec-008-0.4.21-singular-ledgers-and-whole-graph-retries#CON-980]] says so"
adopts: "All effective 0.4.4 through 0.4.20 clauses except the exact replacements and retirements in CON-980"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.8-draft; cbcl-bus SPEC-053 0.16.14-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.20-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.21-draft — Singular ledgers and whole-graph retries

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.20 review found two HIGH, one
> MEDIUM, and two LOW defects. This amendment closes those findings against the
> reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirement

### REQ-1000 — One resolver owns every current set and carrier rule

This requirement replaces
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#REQ-999]]
and
[[spec-008-0.4.6-standalone-tofu-first-contact#REQ-963]] in full.

An authority scan SHALL apply every exact retirement in CON-978, CON-979, and
CON-980 before it compares current values. A retired fragment supplies no
survivor, test, coordinated-set, pointer, or replacement authority.

The scan SHALL cover numbered clauses, numbered list items, and unnumbered
normative paragraphs. A heading, changelog, or earlier gate label does not make
conflicting normative text historical.

Within the adopted Selfsame amendment chain, only CON-980 supplies the current
Selfsame correction set, cbcl-bus test set, coordinated review set, and
coordinated pointer set. CON-978 remains the sole source of the current
cbcl-pairing survivor set, including its exact TEST-050 exception.

Credential/v2 SHALL conform to the current cbcl-pairing companion candidate
named by CON-980. The wallet and browser use separate typed machine-carrier and
PAIR1 presence-field inputs. The pairing library, not application glue,
enforces their non-substitutability, version isolation, CPace source,
sender/state rules, and fixed envelope.

No requirement or sequencing list is permitted to independently enumerate a
superseded companion test range. Release blocking follows the one survivor
authority and the one coordinated review set above.

## Non-functional requirement

### NFR-923 — Bounded retry covers every affected graph and preserves WebFinger semantics

This requirement replaces
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#NFR-922]]
in full. It supplements
[[spec-008-0.4.19-singular-authority-and-webfinger-order#NFR-921]] and replaces
only the three NFR-921 paragraphs identified below. Every other NFR-921
paragraph remains effective.

The first replacement starts with "`withdraw/2` derives the requested alias
URI". It ends with "Its exact withdrawal write lock follows." The replacement
text is:

`withdraw/2` derives the requested alias URI from the named home DID before
opening its bounded transaction. It passes that value into
`tombstone/3(account, homeDid, requestedAlias)`.

The live branch of `tombstone/3` takes the requested exact alias write lock
first. It takes the exact account write lock second.

An absent account row enters `already-withdrawn/2`. A present account row whose
stored account, alias, or home DID disagrees refuses without taking a second
alias lock. Only a present row that agrees on all three values continues on the
live branch and takes the exact `cbcl-webfinger-withdrawn` write lock. Creation
and both live-branch deletes reuse those held locks.

The second replacement starts with "`reinstate/2` omits the alias table". It
ends with "It performs no read-to-write upgrade." The replacement text is:

`reinstate/2` and `lift-tombstone/2` take no alias or account lock. Their
transaction takes one whole `cbcl-webfinger-withdrawn` write lock before its
first `index_read`.

The exact delete runs under that table lock. Provision and withdrawal already
serialize with it through their withdrawal write locks. Reinstatement performs
no read-to-write upgrade.

The third replacement starts with "Every affected transaction runs at Mnesia
level 1". It ends with "No partial effect survives." The replacement text is:

Every transaction boundary in the affected authority, migration, and
WebFinger graphs runs at Mnesia level 1 with an explicit integer
`Retries >= 2`. The default `mnesia:transaction/1`, `infinity`, and an
unbounded `(Fun, Args)` overload are non-conforming. Nested transactions and
nested retry loops are prohibited.

A repository-wide scan SHALL enumerate every production `mnesia:transaction`
call site under `apps/`. Each inventory row names its module, function, and
line, then classifies the site as authority, migration, WebFinger, or
out-of-scope with call-graph evidence. Every in-scope row also names its public
owner, transaction body, retry wrapper, success/refusal contract, and public
terminal mapping. An omitted site, unclassified site, or in-scope site outside
the shared bounded wrapper is non-conforming.

The shared retry wrapper maps `nomore`, exhausted `#cyclic{}`, and
`nested_transaction` to `authority-busy`. Every authority-lane and
migration-lane public consumer returns its previously specified closed
`authority-busy` result. No partial effect survives, and existing success and
non-contention refusal results remain unchanged.

Within the WebFinger lane, the public transaction owners are `provision/3`,
`withdraw/2`, `reinstate/2`, and `lookup/1`. Transaction bodies never open
another transaction or start a retry loop.

The three WebFinger mutation owners return `#(error authority-busy)` on retry
exhaustion. `lookup/1` maps the same terminal to the existing atom
`unavailable`. `cbcl-chat-webfinger-http` maps `unavailable` to HTTP 503 with
an empty body and `no-store`.

Contention SHALL NOT become `missing` or HTTP 404. Invalid resource syntax and
a committed absent binding retain their existing 400 and 404 behavior.

`create-or-return/3`, `lookup-binding/1`, `tombstone/3`,
`already-withdrawn/2`, and `lift-tombstone/2` are WebFinger transaction bodies.
Their names in the graph specify locks, not independent public retry
terminals.

## Contract

### CON-980 — Every stale ledger retires and current authority advances once

This contract supplements
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#CON-979]].
Every CON-979 replacement and retirement remains effective unless this
contract replaces its exact current-value fragment.

REQ-1000 replaces REQ-999 and REQ-963 in full. NFR-923 replaces NFR-922 in
full. ADR-958 replaces ADR-957 in full. TEST-1146 replaces TEST-1144 and
TEST-1073 in full. TEST-1147 replaces TEST-1145 in full.

Through TEST-1144, TEST-1146 remains the successor of TEST-1142, TEST-1140,
and TEST-1137. Through TEST-1145, TEST-1147 remains the successor of TEST-1143,
TEST-1141, and TEST-1138. Through ADR-957, ADR-958 remains the successor of
ADR-956, ADR-955, ADR-954, and ADR-953. No earlier replacement relationship is
lost.

This contract retires item 1 from three gate-and-sequencing lists:

1. In [[spec-008-0.4.4-standalone-tofu-first-contact]], the item starts with
   "A new fresh-context Claude-driver Circus review" and ends with
   "one coordinated verdict."
2. In [[spec-008-0.4.5-standalone-tofu-first-contact]], the item starts with
   "Run a fresh-context Claude-driver Circus Tier-1 adversarial review" and
   ends with "substitute for PASS."
3. In [[spec-008-0.4.6-standalone-tofu-first-contact]], the item starts with
   "Run a fresh-context Claude-driver Circus Tier-1 adversarial review" and
   ends with "substitute for PASS."

This contract retires item 2 from four sequencing lists:

1. In [[spec-008-0.4.5-standalone-tofu-first-contact]], the item starts with
   "Only after coordinated PASS, encode the design" and ends with
   "including CON-226 completeness and rollback."
2. In [[spec-008-0.4.6-standalone-tofu-first-contact]], the item starts with
   "Only after coordinated PASS, encode the effective design" and ends with
   "become named gates."
3. In [[spec-008-0.4.7-standalone-tofu-first-contact]], the item starts with
   "Only after PASS, encode TEST-1011" and ends with
   "as executable SPL Elephant gates."
4. In [[spec-008-0.4.8-standalone-tofu-first-contact]], the item starts with
   "Only after PASS, encode TEST-1011" and ends with
   "as executable Elephant gates."

Replacing REQ-963 retires its superseded conformance and TEST-030 through
TEST-050 sentences. Replacing TEST-1073 retires its positive 4,096-octet
control witness. The only current TEST-050 status is the exception in CON-978.

For avoidance of ambiguity, the CON-979 instruction that called its target one
paragraph is superseded. This contract retires the two-paragraph range in
CON-978 that starts with "The exact cbcl-bus set" and ends with
"`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`."

This contract retires the four-paragraph current-value range in CON-979. It
begins at the paragraph whose first text is "The exact cbcl-bus set" and ends
with "`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`." The replacement text is:

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, TEST-105, and TEST-106. Every effective Selfsame
test through TEST-1108 also survives.

TEST-1134, TEST-1135, TEST-1139, TEST-1146, and TEST-1147 are the current
Selfsame correction tests. No superseded portion or open-ended range supplies
authority.

These five current tests collectively replace TEST-1129 through TEST-1133.

The coordinated review set contains this amendment, cbcl-pairing 0.4.8, and
cbcl-bus 0.16.14. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

This contract retires the CON-979 pointer paragraph whose first text is "The
Selfsame parent" and whose final text is "at this candidate." The replacement
text is:

The Selfsame parent [[SPEC-008-production-pairing-claimant]] points to this
candidate and names its open review gate. The cbcl-bus parent
[[SPEC-053-key-root-identity#DEC-012]] points to the coordinated 0.16.14
candidate. Cbcl-pairing 0.4.8 points its `consumer-design` at this candidate.

Every F-A through F-E correction remains a hard stop. SPEC-007
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] also remains a hard stop.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It authorizes
no code, allocation, release, or deployment.

## Decision

### ADR-958 — Singular authority and whole-graph retry precede code

Every stale survivor, hub, coordinated-set, pointer, and sequencing sentence
retires by an exact boundary. One current coordinated set remains across all
three repositories.

Bounded retry is a whole-graph control across authority, migration, and
WebFinger transactions. WebFinger resolution exposes contention as HTTP 503,
never false absence. WebFinger mutations expose `authority-busy`; every lane
preserves its existing success and non-contention refusal contract.

Live withdrawal uses the pre-derived alias as its first lock key. An absent
account row enters the idempotent already-withdrawn branch. Only a present
disagreeing row refuses. Reinstatement touches only the withdrawal table.

The authenticated display, post-consent effect order, shared grammar, cap
arithmetic, and sealed-grant graph remain unchanged.

## Test specification

### TEST-1146 — Whole-graph retries, withdrawal branches, and bounds close

Change only the profile digest with authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consent decisions, and a new verified
grant. Every local-only re-grant shortcut fails.

Generate the twelve named authority cycles and every `AR` writer contention.
Generate the complete migration graph. Require the restored acquisition point,
invite-before-room order, and no additional upgrade or cycle.

Enumerate every production `mnesia:transaction` call site under `apps/`.
Require each row to carry a proved lane classification. Require each in-scope
row to name its public owner, body, wrapper, retry value, and terminal mapping.
Any unbounded in-scope, nested, omitted, or unclassified site fails.

Include both `write-grant` paths, both `redeem-and-grant` paths,
`revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`,
`cbcl-chat-sealedgrant:put-tx`, and `cbcl-chat-sealedgrant:get-tx`.

Include `cbcl-chat-webfinger-store:provision/3`, `create-or-return/3`,
`lookup/1`, `lookup-binding/1`, `withdraw/2`, `tombstone/3`,
`already-withdrawn/2`, `reinstate/2`, and `lift-tombstone/2`.

Require alias-before-account ordering for provision, lookup, and live
withdrawal. Require `tombstone/3` to use its pre-derived alias. Drive an absent
account row into the idempotent already-withdrawn branch. Drive a present
disagreeing row into refusal. Reject every second-alias lock.

Require reinstatement to take only the whole withdrawal write lock before its
first `index_read`. Require the sealed-grant write lock on the first `get-tx`
read.

Force `nomore`, exhausted `#cyclic{}`, and prohibited nesting at every affected
public owner. Require the lane's exact `authority-busy` terminal, no partial
effect, bounded progress, and unchanged non-contention results.

Force the WebFinger terminal through `lookup/1`. Require `unavailable`, HTTP
503, an empty `no-store` response, and no HTTP 404. Successful and missing
lookups retain their existing responses.

Run the current cbcl-pairing TEST-050 exception. The control recognizer accepts
lengths 1, 23, 24, 255, 256, 2,047, and 2,048 and refuses 0, 2,049, and 4,096.
Large values retain their current exact boundary rows. No positive control row
names 4,096.

### TEST-1147 — Every retired source, current pointer, and gate is singular

Static checks traverse every effective numbered clause, list item, and
unnumbered normative paragraph from 0.4.4 onward. They apply CON-978, CON-979,
and CON-980 retirements before comparing current values.

Require every retired test preamble and sequencing item to resolve uniquely.
Require REQ-963 and TEST-1073 to have one current successor. Require both
two-paragraph ledger boundaries and every pointer boundary to resolve uniquely.
Any surviving conflict or missing boundary fails.

Require exactly one cbcl-pairing survivor set, cbcl-bus test set, Selfsame
correction set, coordinated review set, and coordinated pointer set. Require
the TEST-050 exception and reject any full TEST-050 survivor claim or positive
4,096-octet control witness.

Require the Selfsame parent pointer to this candidate. Require cbcl-bus
DEC-012 to point to 0.16.14. Require cbcl-pairing 0.4.8 to point its
`consumer-design` to this candidate.

Require the whole-graph transaction inventory to cover every production
transaction site under `apps/` and prove every out-of-scope classification.
Require all 42 cbcl-bus GATE-04 boxes, all fifteen version sites, both
non-conforming rendezvous baselines, and sealed cap arithmetic. Removing any
obligation fails.

## Changelog

- **0.4.21-draft — 2026-08-23 — review candidate, not approved.** Retires the
  remaining stale ledgers, restores whole-graph bounded retry, and makes the
  idempotent withdrawal branch explicit. No implementation or deployment
  authority is claimed.
