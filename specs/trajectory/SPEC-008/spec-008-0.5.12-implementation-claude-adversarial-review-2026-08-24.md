---
id: spec-008-0.5.12-implementation-claude-adversarial-review-2026-08-24
title: SPEC-008 0.5.12 implementation — Tier-1 cross-model adversarial review
status: complete
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
generating-family: OpenAI GPT-5
verdict: REJECT
date: 2026-08-24
---

# SPEC-008 0.5.12 implementation — Tier-1 adversarial review

## Reviewer identity, subscription authentication path, Circus attempt, and independence

Reviewing family: Anthropic Claude, fresh context, driven through Circus with
the interactive Claude CLI. Circus attempt: `circus/spec008-v0512-review/3`
(attempt 3).

`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` were both tested for presence
without printing any value; both are **absent** from this environment. The
API-billed `circus-driver-claude` was not invoked.

`env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` recorded:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

All three are the required values, so the review-process precondition holds. No
email, organization identifier, token, key, or account identifier is recorded.

**Independence.** This session received no generating-session reasoning, plan,
or transcript. The independent pass ran first, entirely against `git show` /
`git diff` of the named commit objects and against executable behaviour of
clean detached worktrees and clones created from those objects. The six prior
reports and the 0.5.12 remediation note were opened only *after* that pass was
complete. No disposition was inherited: every 0.5.11, 0.5.10, 0.5.9, 0.5.8 and
0.5.7 item below was re-derived from code or re-executed here, and one item a
prior review closed is reopened on re-test (M-4).

**Candidate hygiene.** No candidate repository was modified. Every build, test
and mutation ran in scratch worktrees or scratch clones created from the exact
commit objects; each was restored and verified byte-clean afterwards.
`/Users/anuna-01/Code/{cbcl-bus,cbcl-pairing}` finished this session with zero
tracked modifications at the candidate commits. `selfsame`, `did-crdt` and
`cbcl-rs` retain the person's pre-existing uncommitted work (68, 5 and 27
tracked-dirty files) and their pre-existing HEADs; none of it was read as
candidate evidence, and none of it was modified. One observation, not a
finding: because the person's `did-crdt` checkout sits at service HEAD
`94db956d` rather than the pin `1f409a42` and carries tracked drift, the
TEST-1164 witness fails in their working layout. That is working state, not the
candidate.

## Exact commits reviewed

| Repository | Candidate | Pre-0.5.12 | Original coordinated baseline |
|---|---|---|---|
| Selfsame | `0dbfb3c4323aec568ac2803d58c0aa71c301edda` | `0c5801259802b767bd8f9230ff6cd32dff6fffa5` | `0220cec2dec44cd95d4f411ea4814d790b6716d2` |
| cbcl-bus | `100d5e4b5f8e6e36a61b2b27ffdb13ca61edb335` | `965d6cc44cfdf0f8d00a562bb587b7785396ee97` | `ce894a7f2debc111b610296b1a3780de6084e37e` |
| cbcl-pairing | `62ef4a968b46b4836374fcee1d78c410f730a7a7` | — | `c22f2d0526432c01c56d64be35a8d4f87516ec92` |
| did-crdt | pin `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` (service HEAD `94db956d7d5d5edf0f42afad073ab42f265a1752`) | — | — |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | — | — |

Governing 0.5.12 authority and remediation note: `73ad88dc361ae20820ba9d328b455dad83ab56f5`.

Governing parents read for the checked areas:
`specs/SPEC-008-production-pairing-claimant.md`,
`specs/SPEC-007-cbcl-pairing-cutover.md`,
cbcl-bus `specs/SPEC-053-key-root-identity.md`,
cbcl-pairing `specs/SPEC-001-reusable-blind-pairing.md`.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 4 |
| LOW | 4 (two carried-forward residuals, two new, all with explicit treatment) |

No exploit was found in the shipped credential/v2 consent, binding, effect-order
or installation predicates. The independent pass reproduced every claim the
generating agent made about the changed gates, and all five named mutants plus
six additional ones are killed. The rejection rests on four separate unmet
obligations, three of which are load-bearing gates:

1. the fifteen post-approval boundaries TEST-1162 names are exercised as five
   synthetic guard drops, and the production arm/disarm *placement* those
   boundaries denote survives mutation (M-1);
2. the 0.5.12 unlink guard itself introduces a new permanent stranding path,
   defeating the anti-stranding purpose of the contract it was added to satisfy
   (M-2);
3. the coordinated hub's continuous gate cannot execute at all — the browser
   WASM provenance check hard-fails on CI's host triple and blocks every later
   step in the same job, including the 975 EUnit tests (M-3);
4. REQ-1005's sole-`PROFILE_VERSION`-definition obligation is unimplemented and
   ungated, with genuinely split consumers (M-4).

## Findings

### M-1 — TEST-1162's fifteen named boundaries are five synthetic guard drops; the production arm/disarm placement survives mutation

**Severity: MEDIUM.** Owner: Selfsame.
Affected requirement: [[SPEC-008-production-pairing-claimant#TEST-1162]]
("Inject one terminal error after each successful final-approval persistence
and before each possible successful `PayloadPrepared` replacement… Delete or
disarm the guard and require the test to fail"), verifying
[[SPEC-008-production-pairing-claimant#CON-990]].

**Code evidence.** `src-tauri/src/cbcl_v2_completion.rs:2264-2283` builds a
fifteen-row `failure_matrix`. Every row runs the identical body: `persist_pending`,
construct a `PrePayloadPendingCleanup` anchored on `pending`, `overwrite_test_slot`
to `stored`, drop. The rows differ only in `stored`, which takes **five** distinct
values (`pending`, `acknowledged`, `planned`, `issuer_created`, `provisioned`) —
so ten of the fifteen labels are exact duplicates of another row. Nothing in the
test calls, drives, or injects a failure into `cbcl_v2_final_decide`
(`src-tauri/src/cbcl_v2_commands.rs:307-620`), which is where the fifteen named
boundaries — "final approval release", "acknowledgement read", "issuer
publication", "resolver verification", "grant construction", "payload
checkpoint preparation" — actually are. The 0.5.12 remediation note is candid
about this: "Each case exposes the durable phase appropriate to that boundary,
drops the armed compensation guard."

The guard *mechanism* is therefore well covered; the guard's *placement* is not.
Rust's `Drop` makes compensation automatic between arming and disarming, so the
only remaining variable in the production path is where those two statements sit
— and that is exactly what no test observes.

**Failure sequence (executed, mutant survives).** In a clean scratch clone I
moved the disarm one statement earlier, so the guard is released before the
durable `PayloadPrepared` write rather than after it:

```rust
// src-tauri/src/cbcl_v2_commands.rs:610-611, mutated
let payload_prepared = pre_payload_cleanup.disarm_after_payload(payload_prepared);
crate::cbcl_v2_completion::replace_pending(&durable, &payload_prepared)?;
```

This is a real regression: with it, a `replace_pending` failure at the payload
checkpoint returns with the guard already released, leaving the `Provisioned`
pending slot durable, un-compensated and — until the person finds and abandons
the interrupted-link row — blocking a fresh ceremony for that application. It
reintroduces 0.5.10 H-2 at one boundary. The mutant **survives every executable
gate**:

| Gate | Result under the mutant |
|---|---|
| `cargo test -p selfsame --locked --lib` | 66 passed, 0 failed, 1 ignored |
| CI's isolated TEST-1162 invocation | 1 passed, 0 failed |
| `npm run e2e:wallet-pairing` | 5 passed, 0 failed |

I reverted the mutation and re-ran the library suite (66 passed) to confirm the
tree was restored.

**Measurable correction.** Extract the post-final-approval sequence of
`cbcl_v2_final_decide` into a function whose fallible steps are injectable (a
step enum plus a fault sink, or a trait for the five effectful calls), and drive
it once per named boundary with the failure injected *at that boundary*.
Require the test to fail when the arm is moved after `persist_pending`, when any
`advance` is removed, and when `disarm_after_payload` is moved before
`replace_pending(&durable, &payload_prepared)`. The present fifteen-row matrix
may remain as the guard-unit test; it does not discharge TEST-1162's
injection clause on its own.

### M-2 — 0.5.12's new still-trusted-policy refusal makes a transient store failure permanently strand the application slot

**Severity: MEDIUM.** Owner: Selfsame. **New in 0.5.12** (regression against
0.5.11 behaviour).
Affected requirements: [[SPEC-008-production-pairing-claimant#REQ-1006]]
("Unlink SHALL apply to an installed record and to every recognised pending
record") and [[SPEC-008-production-pairing-claimant#CON-990]] ("Pre-payload
failure cannot strand an application slot").

**Code evidence.** `src-tauri/src/cbcl_v2_completion.rs:1540-1549`:

```rust
if crate::cbcl_v2_policy::state(application_id, relay_origin)?
    != crate::cbcl_v2_policy::ExactPairState::TrustedPair
{
    return Err(UiError::from("PairingCheckpointRefused"));
}
crate::cbcl_v2_policy::remove(application_id, relay_origin)?;
if store::delete(&entry).is_err() {
    let _ = crate::cbcl_v2_policy::insert(application_id, relay_origin);
    return Err(UiError::from("PairingCheckpointUnavailable"));
}
```

The guard refuses on **absence**, not on **change**. It is not paired with the
state observed when the slot was selected (`load_local_link`,
`cbcl_v2_completion.rs:1432-1450`, returns the slot alone), so the function
cannot distinguish "the policy row changed under my selection" from "the policy
row is simply not there". The immediately following lines are the code that can
create the second condition, and their repair is best-effort with the error
discarded.

0.5.11 did not have this property. Its version captured
`policy_was_present` and proceeded regardless, so a retry after the same failure
succeeded.

**Failure sequence.**

1. A pending or installed link exists for application `A` over relay `R`; the
   exact-pair row `(A, R)` is trusted.
2. The person confirms unlink with current-root presence.
3. `policy::remove(A, R)` succeeds — the policy record is written without the row.
4. `store::delete(slot_A)` fails. This is an ordinary transient keyring
   condition, not corruption: a locked macOS keychain (`errSecInteractionNotAllowed`),
   a dropped secret-service D-Bus connection, a device that slept mid-call.
   `store::delete` surfaces every non-`NoEntry` backend error
   (`src-tauri/src/store.rs:190-195`).
5. The compensating `policy::insert(A, R)` runs in the same failure window and
   also fails; its error is discarded by `let _ =`.
6. `unlink_local` returns `PairingCheckpointUnavailable`. The person retries
   after the store recovers.
7. Every retry now returns `PairingCheckpointRefused`, permanently.

I executed steps 6–7 and their consequences as a probe added to the candidate's
own TEST-1162 body in a scratch clone (then reverted). With the policy row
absent and the slot present, all of the following hold:

- three consecutive `unlink_local` calls on the selected slot refuse with
  `PairingCheckpointRefused`;
- re-reading the slot with `load_local_link` and unlinking the freshly-read
  value also refuses;
- `persist_pending` for a fresh ceremony on the same application refuses with
  `PairingApplicationAlreadyLinked`, so the slot cannot be reoccupied;
- re-inserting `(A, R)` into the policy is the only thing that unwedges it.

The interrupted-link row stays visible the whole time — `pending_links`
(`cbcl_v2_completion.rs:1395-1427`) does not consult the policy — so the person
is shown a row with an abandon control that can never succeed.

**Consequence.** The only in-application escape is to re-insert the exact-pair
row, and the sole production writer of that row is `bind_finished_profile`
(`src-tauri/src/cbcl_v2_claimant.rs:296-303`), which runs only after a complete
live CPace and both Finished values against the application's relay. The person
must therefore start a fresh pairing ceremony with the application they are
trying to remove, walk its new-pair TOFU prompt, and abandon it — and if the
application or relay is gone, which is the ordinary reason to unlink, no such
ceremony exists. The remaining escape is `forget_identity`
(`src-tauri/src/commands.rs:617-624`), which destroys the whole hierarchy root
and every sibling application. For an installed record this also means the
person cannot remove a credential locally.

**Measurable correction.** Refuse on observed change, not on absence. Have
`load_local_link` return the exact-pair state sampled with the slot, and have
`unlink_local` refuse only when the current state differs from the selected
state; when the selected state was already `NewPair`, delete the slot and skip
the policy removal. Require a test in which the policy row is absent *at
selection time* and unlink still releases the slot, alongside the existing case
in which the row changes *between* selection and deletion and unlink refuses.
An acceptable alternative is to delete the slot first and remove the policy
second, so a partial failure leaves a residual TOFU row (a re-prompt that is
skipped) rather than an unremovable credential — but then TEST-1162's "failed
slot deletion restores policy" clause must be restated.

### M-3 — the coordinated hub's continuous gate cannot execute: the browser WASM provenance check hard-fails on CI's host triple and blocks every later step in the job

**Severity: MEDIUM.** Owner: cbcl-bus.
Affected requirements: [[SPEC-008-production-pairing-claimant#CON-985]]
(the coordinated hub test set TEST-115 through TEST-119 and the coordinated
review set) and check 20's "one buildable source closure across … CI … and
browser WASM provenance".

**Code evidence.** `apps/cbcl_chat/priv/web/selfsame/selfsame_web_device.provenance.json`
records `"buildHost": "aarch64-apple-darwin"`.
`scripts/check-selfsame-wasm-rebuild.sh:76-84` enforces it in non-vendor mode:

```sh
actual_host="$(rustc -vV | awk '/^host:/{print $2}')"
…
build_host="$(node -p "require('${provenance}').buildHost")"
[[ "${actual_host}" == "${build_host}" ]] \
    || fail "host ${actual_host} cannot reproduce the ${build_host} artifact; …"
```

`.github/workflows/ci.yml:11-19` puts the only caller in the `build` job, which
is `runs-on: ubuntu-latest` inside `container: erlang:28`. Its `rustc -vV` host
can only be a `*-unknown-linux-gnu` triple, never `aarch64-apple-darwin`. The
step (`.github/workflows/ci.yml:165-169`) has no `if:` and no
`continue-on-error`. The repository's own documentation agrees that the artifact
is supposed to be produced on the CI host, not a Mac:
`apps/cbcl_chat/priv/web/selfsame/README.md:34-42` — "the artifact must be
vendored on the host recorded as `buildHost` — CI's `x86_64-unknown-linux-gnu`.
On a Mac, vendor through a `--platform linux/amd64` container". The provenance
record contradicts that instruction.

The cause is traceable: `89e57449` ("ci(temp): vendor the Selfsame WASM on the
runner and enforce buildHost", 2026-08-18) added the host guard together with a
temporary CI step that would vendor on the Linux runner and push the bytes to
`ci/wasm-vendor-harvest` for committing. That temporary step has since been
removed, but the harvest never landed: `buildHost` is `aarch64-apple-darwin` at
every commit of the provenance file from `417ded1` through the candidate
`100d5e4`.

**Failure sequence (reproduced).** I ran the real script from clean clones at
the exact four pinned revisions with only `buildHost` set to CI's triple — the
mirror image of what CI experiences:

```
check-selfsame-wasm-rebuild: host aarch64-apple-darwin cannot reproduce the
x86_64-unknown-linux-gnu artifact; re-run on x86_64-unknown-linux-gnu (CI's
container platform)
```

`set -euo pipefail` makes that a non-zero step. Because it is the fifth step of
the `build` job, everything after it is skipped: the pinned blind-relay shell
build, clippy and the cfg-injection negative test; `rebar3 compile` and all
three purity boundary gates; `rebar3 eunit` (the 975 tests); the SPEC-053
mutation harness self-test; the 42 discovered browser-core suites; and the
SPEC-075/SPEC-076 live invite and pair-account ceremonies.

**Consequence.** The coordinated increment has no green CI on the hub side, and
the 3.6 MB WebAssembly module the browser executes for Path-B verification and
the credential/v2 bootstrap has no working continuous provenance verification
on any platform CI runs. Every hub-side gate result reported for this candidate
— including the 975 EUnit tests I independently reproduced — is a local macOS
result, not continuous-gate evidence. Nothing about the artifact's *content* is
wrong: I rebuilt it byte-identically (see check 21). What is wrong is that only
a Mac can prove that.

**Measurable correction.** Re-vendor the package on the CI container platform,
as `README.md:34-42` already instructs, so `buildHost` and `wasmSha256` record
that host; then confirm the `build` job's WASM step passes and the job proceeds
to `rebar3 eunit`. If a macOS-vendored artifact is intended to remain
authoritative, the gate must instead record and compare a per-host digest set,
and the README's contradictory paragraph must be corrected. Either way, the
review evidence for this candidate should name the CI run in which the whole
`build` job is green.

### M-4 — REQ-1005's sole-definition obligation for `PROFILE_VERSION` is unimplemented, ungated, and the consumers are genuinely split

**Severity: MEDIUM.** Owner: Selfsame. **Reopened**: recorded as 0.5.1 L-4 and
marked CLOSED by the 0.5.7 review on a misreading.
Affected requirement: [[SPEC-008-production-pairing-claimant#REQ-1005]] — "The
implementation SHALL keep `profile::PROFILE_VERSION` as the sole constant
definition. The crate root MAY publicly re-export that item but SHALL NOT define
a second `pub const PROFILE_VERSION`. The production scan SHALL prove one
definition and any number of ordinary imports or re-exports."

**Code evidence.** Two independent definitions exist in the candidate:

```
crates/selfsame-app-identity/src/profile.rs:60:  pub const PROFILE_VERSION: i64 = 1;
crates/selfsame-app-identity/src/lib.rs:109:     pub const PROFILE_VERSION: i64 = 1;
```

`lib.rs:108-109` is a definition with its own doc comment, not a `pub use`. The
consumers are split between them:

| Consumer | Reads |
|---|---|
| `crates/selfsame-app-identity/src/profile.rs:385` (`ApplicationProfile::recognise` version check) | `profile::PROFILE_VERSION` |
| `crates/selfsame-app-identity/src/enrollment.rs:394, :452, :552` | `crate::PROFILE_VERSION` |
| `crates/selfsame-app-identity/src/provider_hint.rs:115` | `crate::PROFILE_VERSION` |
| `crates/selfsame-web-device/src/lib.rs:1508` | `selfsame_app_identity::PROFILE_VERSION` |

No executable scan enforces the clause: nothing under `scripts/`,
`.forgejo/workflows/` or `crates/*/tests/` counts `PROFILE_VERSION` definitions.

The 0.5.7 review's disposition of 0.5.1 L-4 reads, in full: "`…lib.rs:109` and
`…profile.rs:60` each declare `PROFILE_VERSION` at `0220cec2`. REQ-1005 records
one duplication and requires one definition." That states the violation and
marks the item CLOSED in the same sentence. REQ-1005's text is a normative
constraint on the implementation, and under [[SPEC-008-production-pairing-claimant#ADR-963]]
the parent states current law directly; recording the duplication in the spec
does not discharge it.

**Failure sequence.** Bumping the profile version — the exact maintenance event
REQ-1005's production scan exists to govern — requires editing two constants
that both document themselves as "the profile version this build speaks". Edit
`profile.rs:60` alone and `ApplicationProfile::recognise` accepts only v2
profiles while `enrollment.rs:452` still emits `profileVersion: 1`,
`enrollment.rs:394/:552` and `provider_hint.rs:115` still compare against 1, and
`selfsame-web-device` still emits 1. That is a live split between the profile
recogniser and the enrolment / provider-hint / web-device wire — precisely the
"two namespaces" outcome REQ-1005 forbids. Nothing fails to compile and no test
fails, because the two constants are never compared to each other.

**Measurable correction.** Replace `lib.rs:109` with
`pub use profile::PROFILE_VERSION;` and add an executable production-scan test
that fails when more than one `pub const PROFILE_VERSION` definition exists in
the workspace. I applied the one-line correction in a scratch clone: it compiles
with no warnings and `cargo test --workspace --locked` passes (805 tests, 73
green result lines, zero failures), which establishes the obligation is
unimplemented rather than infeasible.

### L-1 — a mid-chain post-approval failure still leaves an orphaned issuer DID publication with no compensating retraction

**Severity: LOW (residual, unchanged in kind from 0.5.8 L-1 / 0.5.9 L-1 /
0.5.10 L-1 / 0.5.11 L-1).** Owner: Selfsame.
Affected requirement: [[SPEC-007-cbcl-pairing-cutover#REQ-812]] compensation
clause, bounded by [[SPEC-004-application-scoped-identity#CON-204]].

Re-derived from code this session rather than inherited.
`src-tauri/src/cbcl_v2_commands.rs:524-530` publishes the issuer identity to the
profile's declared state resolver. I searched both credential/v2 modules for any
deactivation or retraction edge and found none: the only `is_deactivated()`
occurrences are *reads* of a resolved document
(`cbcl_v2_commands.rs:540`, `:1539`) plus one closure-observation field
(`:1084`). Any failure after publication — resolver refusal, grant
construction, payload construction, the payload checkpoint, or the guard's own
compensation — returns without retracting.

**Consequence.** A published-but-unused `did:crdt:` document remains resolvable.
It carries no account, scope binding, alias, WebFinger row or grant, and cannot
become authority: a fresh ceremony derives a different DID from a fresh
hub-minted `accountScopeId`, and both hub acceptance and wallet installation
(`install`, exact CAS plus live reciprocal WebFinger verification) require
evidence the orphan does not have.

**Operational treatment, explicit.** The 0.5.12 note carries this forward as the
bounded compensation limitation CON-204 already admits, and does not represent
it as closed. Two facts moderate it: `did-crdt` HEAD `94db956d` makes service
DID creation idempotent, so a retried publication of the same identity does not
multiply orphans; and the pinned wallet runtime compiles `did-crdt` with
`default-features = false`, so that fix reaches the deployed resolver rather
than the wallet. Operators should expect orphan documents proportional to the
mid-chain failure rate and must not read their presence as evidence of an issued
credential.

### L-2 — the CON-036 anonymous recovery limiter is keyed on the TCP peer address, so a reverse-proxied deployment shares one bucket

**Severity: LOW (residual, unchanged from 0.5.9 L-2 / 0.5.10 L-2 / 0.5.11 L-2).**
Owner: cbcl-bus / deployment.
Affected requirement: [[SPEC-008-production-pairing-claimant#CON-989]] recovery
availability; cbcl-bus SPEC-053 CON-036.

Re-derived this session.
`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-http.lfe:138-141`
derives the limiter key from `cowboy_req:peer`, i.e. the TCP peer;
`cbcl-chat-selfsame-v2-recovery-limit.lfe` applies a 60 s window with a
30-request budget per key. `config/router.sys.config:5-8` records that the
deployment sits behind the Fly proxy, which "supplies the sole trusted client-IP
header"; a grep of `apps/cbcl_chat/src/` for `x-forwarded-for` and
`fly-client-ip` returns nothing.

**Consequence.** Behind the proxy every wallet shares one 30-per-60 s bucket for
`/selfsame/pairing/v2/status`. Any unauthenticated client can exhaust it and
make post-relay-window recovery answer `503` for everyone. The failure is
availability-only and fails closed: the wallet maps `503` to `unavailable`
(`cbcl_v2_commands.rs:1194-1196`), retains the pending slot, discloses nothing
and can retry. No authority, identity or capability effect is reachable.

**Operational treatment, explicit.** Recorded as an operational deployment gate
requiring separate owner approval of production enablement. The bounded
correction, when that gate is approached, is to key the limiter on the trusted
proxy client-IP header when and only when the listener is configured as proxied,
keeping `cowboy_req:peer` otherwise so an unproxied deployment cannot be spoofed
by a caller-supplied header.

### L-3 — the new CI TEST-1162 step reports success when it selects no test

**Severity: LOW (new in 0.5.12).** Owner: Selfsame.
Affected requirement: [[SPEC-008-production-pairing-claimant#TEST-1162]] — "The
continuous gate SHALL execute this process-global keyring test in an isolated
test process."

`.forgejo/workflows/ci.yml:170-176` invokes the test by fully-qualified name
with `--ignored --exact`. That is correct today — I verified it selects exactly
the intended test (1 passed, 66 filtered out). But libtest exits 0 when a filter
matches nothing, and the test is `#[ignore]`d, so it is invisible to the default
library suite. I ran the same command with a drifted name:

```
running 0 tests
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 67 filtered out
EXIT=0
```

**Consequence.** A rename or module move of
`test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot`
silently deletes the entire TEST-1162 Rust gate while the step stays green — the
same class of silent coverage loss the cbcl-bus browser-suite ratchet comment
was written to prevent.

**Operational treatment, explicit.** The gate is correct at this commit and the
finding names a future-drift hazard, not a present gap. Until corrected,
reviewers must confirm the step's own output shows `1 passed` rather than trust
its exit status. Bounded correction: append a count assertion to the step, e.g.
pipe the output through `tee` and require `grep -q '^test result: ok. 1 passed'`,
or add `--format=terse` plus an explicit count check.

### L-4 — the credential/v1 conformance registry's implementation comment cites SPEC-008 CON-903 instead of SPEC-007 CON-806

**Severity: LOW.** Owner: Selfsame.
Affected requirement: [[SPEC-008-production-pairing-claimant#REQ-1006]] — "The
standing credential/v1 registry and selection control remain compiled and
unchanged. Their implementation comment SHALL cite
[[SPEC-007-cbcl-pairing-cutover#CON-806]], not
[[SPEC-008-production-pairing-claimant#CON-903]]."

`src-tauri/src/cbcl_registry.rs:1` reads:

```rust
//! SPEC-008 `CON-903` — the compiled approved-conformance registry.
```

CON-806 is cited only inside one entry's rationale at `:23`, not as the module's
attribution. The behavioural half of the requirement holds: the registry is
consumed only by the v1 path (`src-tauri/src/cbcl_pairing.rs:152`), and no
`cbcl_v2_*` module references `cbcl_registry`, `production_relay_policy`,
`APPROVED_CONFORMANCE` or `RelayPolicy`.

**Consequence.** The mislabel attributes a conformance-digest registry to
CON-903, whose own post-conditions say "No relay-only key, global allowlist,
conformance-digest registry, or application wildcard participates". A reader
tracing CON-903 from the code reaches a mechanism the contract explicitly
excludes. No capability, consent or wire behaviour is affected.

**Operational treatment, explicit.** Documentation-only; the requirement states
its own correction. Bounded correction: change the module header to cite
`SPEC-007 CON-806` and re-run the ordinary suite.

## Dispositions for all twenty-two required checks

| # | Check | Disposition |
|---|---|---|
| 1 | Live CON-220 application authentication and exact `(applicationId, relayOrigin)` consent precede relay socket creation; a relay trusted for one application never authorizes another | **PASS.** `cbcl_v2_recognise` (`cbcl_v2_commands.rs:150-171`) fetches and recognises the live profile and computes exact-pair state through `recognise_profile_candidate` (`cbcl_v2_claimant.rs:156-179`), which also requires exactly one matching relay descriptor, and opens no socket. `connect_wss` is reached only from `cbcl_v2_relay_decide` after `authorise_claimant_relay` consumes the single-use capability (`cbcl_v2_commands.rs:177-215`). The policy key is the full tuple at every call site (`cbcl_v2_policy.rs`), and `exact_pair_never_authorises_another_application_on_the_same_relay` passes. |
| 2 | The claimant binds the locally authenticated profile digest into the CPace public context and checks the peer-authenticated digest before policy seal, exact-intent display, or identity work | **PASS.** `CredentialV2Context::derive` (cbcl-pairing `src/credential_v2/context.rs:22-79`) places `profile_digest` into the channel identifier, both roles' associated data, and the thirteen-member public context, so a divergent digest breaks Finished. `bind_finished_profile` (`cbcl_v2_claimant.rs:277-311`) runs only on the `Established { transcript_hash }` effect, seals a newly approved row there, and re-checks an existing row rather than assuming it. The explicit equality check follows in `verify_offer` (`crates/selfsame-pairing/src/credential_v2.rs:358-367`): `recognised.profile_digest != *self.profile.digest()` refuses. |
| 3 | The display shows the locally authenticated application identity and cross-checks the wire claim before approval, closing historical F-B | **PASS.** Display authority is `CredentialV2IntentAuthority::new(recognised.claims, tofu_state)` built from the signed offer verified under the independently fetched profile, with descriptor, carrier, transcript, application, relay, ceremony and allocator key all cross-compared first (`credential_v2.rs:346-380`). `AuthenticatedOfferVerdict::verify` (`:406-420`) then requires the peer object's `application_id`, `https_origin`, `relay_origin` and `carrier_ceremony_id` to equal the authority's. `intent_view` (`cbcl_v2_commands.rs:1687-1719`) projects only verifier-produced values. The seven `compile_fail` doctests that make display types unconstructable by callers all pass. |
| 4 | No application identity key, scope, DID, signature, publication, alias, grant, or durable identity record occurs before its specified consent boundary, including [[SPEC-007-cbcl-pairing-cutover#REQ-812]] | **PASS.** Traced line by line at `cbcl_v2_commands.rs:307-620`. Before preliminary approval only the intent-decision object is built and sent. Preliminary approval authorises exactly one custody call, `preview_identity` (`:1669-1685`), which derives the home DID inside `Custody::use_hierarchy_root` and performs no signature, publication, resolver call, alias operation or durable write. Before final approval nothing signs, publishes, resolves, issues, persists or stages. Final approval writes one non-authorising sealed checkpoint (`persist_pending`, `:443`) before `build_issuer_artifacts` (`:516`), then follows the declared causal order issuer → publish → resolve-and-verify-closure → grant → payload with a durable `replace_pending` between stages. |
| 5 | Fresh account scope is hub-minted, exclusive, bound to the authenticated ceremony, used only after consent; no prior enrolment record needed | **PASS.** `account_scope_id` and `account_principal_digest` reach the wallet only inside the Ed25519-signed offer and are read from `offer.claims.account_provenance()` at both use sites (`cbcl_v2_commands.rs:378-382`, `:1677-1679`). The offer is bound to the carrier ceremony, descriptor digest and transcript hash. `crates/selfsame-pairing/src/credential_v2.rs` derives `account_principal_digest` hub-side per allocation. The credential/v2 path contains no `record_pairing_trust` edge, no `assemble_claimant` fallback, no held-enrolment precondition and no `EnrolmentAllocator::new_native` call: greps of `src-tauri/src/cbcl_v2_*.rs` return nothing for all four. |
| 6 | Relay operatorship ambiguity is not mistaken for authentication; the property is per-application exact-pair consent plus cryptographic application binding | **PASS (honest residual).** [[SPEC-008-production-pairing-claimant#ADR-902]] states in terms that "the application profile does not prove relay operatorship. Exact-pair consent contains that residual without claiming to solve it." Nothing in the candidate contradicts it: the relay origin must equal exactly one descriptor in the signed live profile, the person authorises the exact pair, and the profile digest is bound into CPace so a relay cannot substitute an application. |
| 7 | All credential/v2 wire bodies are closed, canonical, bounded, versioned, context-separated, and recognised before action on both sides | **PASS.** `crates/selfsame-pairing/src/credential_v2/bodies.rs:1204-1235`: `body_entries` refuses trailing bytes (`cursor.position() != bytes.len()`) and any non-canonical encoding (`to_canonical_vec(&value) != bytes`); `exact_fields` requires the exact member count and every named member, so unknown and missing both refuse; `field` refuses duplicates; reordering fails the canonical re-encode. Every body carries `carrierCeremonyId` and `predecessorDigest`, and the envelope carries the one retained `intentDigest`. Bounds are executable (`MAX_OFFER_CORE_BYTES`, `MAX_SIGNED_OFFER_BYTES`, `MAX_BROWSER_STAGING_RECEIPT_BYTES`, `MAX_FINAL_STATUS_*`, `valid_compact_jws`). The browser mirrors the same bounds in `credential-v2-allocator.mjs`'s `boundedB64u`/`fixedB64u`. |
| 8 | Allocator browser ceremony, relay protocol, wallet claimant, reverse issuance, final status, receipt, and installation form one end-to-end cbcl-pairing protocol | **PASS.** Traced end to end: `credential-v2-allocator.mjs` drives the browser allocator over `cbcl-pairing`'s relay wire; the wallet claimant is the five split functions of CON-986 and nothing else; reverse issuance runs inside `cbcl_v2_final_decide`; the hub's immutable `finalStatusJws` returns through the receipt (`from_authenticated_receipt`) or, after the relay window, through CON-989's direct HTTPS adapter; installation is the atomic CAS in `install`. No parallel ad-hoc enrolment path participates — see check 5's four negative greps. |
| 9 | The browser persists and reconciles every phase needed for restart without duplicate allocation, identity, grant, or acceptance effects | **PASS.** `credential-v2-allocator.mjs:615-633` enumerates eighteen recovery phases from `awaiting-allocation` to `receipt-sent`; `recogniseRecoveryAttempt` (`:644-707`) enforces an exact key set, per-field bounds and two cross-field consistency rules, and refuses an unlisted phase. Restoration cross-checks the restored protocol phase against the endpoint's own phase (`:802-816`) and holds `relayRecovered`/`hubRecovered` false until each side is reconciled (`:788-789`, `:1090`, `:1154-1155`, `:1233`, `:1277`), so no send happens before reconciliation. The hub side is idempotent per ceremony under the pending-row write lock. |
| 10 | The wallet's durable pending union, endpoint checkpoints, exclusive deadlines, final status recovery, and installation CAS cannot widen authority after crash, replay, timeout, cancellation, or wrong order | **PASS.** The slot is a single tagged union (`CredentialV2LinkSlot`, `deny_unknown_fields`, exclusive-tag test at `cbcl_v2_completion.rs:2434`). Every mutator is a compare-and-swap under `SLOT_LOCK`: `replace_pending` refuses a stale, installed or cross-application predecessor and pins nine immutable members; `install` refuses unless the current value equals the expected pending, and is idempotent on an identical installed value; `replace_installed` projects the replacement onto the expected and refuses anything but a profile-only change. The offer deadline is exclusive and re-sampled from a whole-UTC-second clock at all three required points (`:356`, `:503`, `:581`, all `>=`). Post-payload expiry preserves the slot; only verified accepted receipt, verified `not-finalized`, explicit unlink or root purge clears it. |
| 11 | Acknowledged `FinishedSent` restoration emits the allocator's required `Open` without a duplicate `Finished`; mutate that predicate | **PASS, mutation-confirmed.** cbcl-pairing `src/credential_v2/bootstrap.rs:129-140`: `cached_bootstrap_sequence` returns `Ok(None)` when `awaiting_ack` is false, and `allocator.rs:415-425` consults it only when a bootstrap frame is actually cached, so `reopen_effects` emits `Open` alone. (The claimant's own `restore` reconstructs only post-final endpoints, `claimant.rs:208-252`, so this is the allocator path.) Mutant 8 — reverting to `Err(Phase)` — fails `allocator_requests_900_and_checkpoints_before_carrier_and_each_cached_frame` at `credential_v2_allocator_session.rs:152`. |
| 12 | [[SPEC-008-production-pairing-claimant#CON-990]] arms immediately after durable final approval, covers every pre-payload return, removes only the same recognised attempt, and disarms only at durable `PayloadPrepared` | **PASS in code; verification incomplete — see M-1.** The guard is constructed on the statement immediately following `persist_pending(&durable)?` with no fallible statement between (`cbcl_v2_commands.rs:443-445`), advanced at `:485`, `:511`, `:522`, `:567`, and disarmed at `:611` only after `replace_pending(&durable, &payload_prepared)?` returns, before the payload frame is released at `:613-617`. `Drop` (`cbcl_v2_completion.rs:800-806`) discards the compensation result, so the original protocol error is never replaced. `remove_pre_payload_attempt` compares the immutable twelve-member attempt identity and deletes the *current recognised* value, which is what makes a committed-but-reported-failed `replace_pending` compensable. What no executable test observes is the placement of the arm and disarm statements themselves; M-1 demonstrates a surviving placement mutant. |
| 13 | A correct `PayloadPrepared` record survives compensation and later receipt failure for [[SPEC-008-production-pairing-claimant#CON-989]] recovery | **PASS, mutation-confirmed.** `remove_pre_payload_attempt` returns `Ok(())` without deleting when `current.is_payload_prepared()` (`cbcl_v2_completion.rs:1620-1622`). After the disarm, `cbcl_v2_finish` restores the session on every error path (`put_pending`) and never removes the slot, so payload-release and receipt failures retain it. Mutant 1 (retention removed) is killed. |
| 14 | Compensation refuses another attempt, another root generation, an installed record, and a sibling application | **PASS, mutation-confirmed.** `same_attempt` covers root generation, application, relay, profile digest, offer profile, carrier, offer, intent-approve, comparison, final-approve, preview issuer DID and offer deadline; a mismatch returns `PairingCheckpointRefused` without deleting. An `Installed` slot short-circuits before the comparison. The slot name is application-scoped (`slot_name`, with the sibling-separation test at `:2480`). Mutants 2, 3 and 6 are all killed. |
| 15 | Confirmed unlink authenticates current-root presence and removes only the exactly selected slot and still-trusted exact-pair policy; slot or policy replacement between selection and deletion refuses and retains the new state; failed slot deletion restores policy | **FAIL — M-2.** The presence, root-generation and exact-slot compare-and-delete halves all hold: `cbcl_v2_unlink` requires confirmation, a non-empty passcode, `require_root_generation` against the live root public key, and `Custody::use_hierarchy_root` before calling `unlink_local`; the slot CAS and the policy CAS both refuse and retain the new state (mutants 4 and 5 killed); failed slot deletion attempts a policy restore. The defect is that the policy guard refuses on *absence* rather than on *change*, and the only code that can create that absence is the two lines above it whose repair is best-effort and discarded. A transient store failure therefore converts into a permanently unremovable slot. |
| 16 | Every post-final-approval/pre-`PayloadPrepared` boundary named by [[SPEC-008-production-pairing-claimant#TEST-1162]] is exercised by a semantically adequate failure injection, not merely named in a table | **FAIL — M-1.** Fifteen labels collapse to five distinct durable phases; the injection is a synthetic guard drop, not a failure at the named production boundary; and the arm/disarm placement those boundaries denote survives mutation across the library suite, the isolated TEST-1162 invocation and the wallet browser suite. |
| 17 | The process-global TEST-1162 Rust test is executed in an isolated CI process despite `#[ignore]`; the wallet lifecycle browser suite is also an explicit CI step; verify the filters actually select the intended test | **PASS with a LOW residual (L-3).** `.forgejo/workflows/ci.yml:170-176` adds the isolated `cargo test -p selfsame --locked --lib -- --ignored --exact <fully-qualified name>` step, and `:319-322` adds `npm run e2e:wallet-pairing` with `CI=true`. I ran both verbatim: the Rust filter selects exactly one test (1 passed, 66 filtered out) and the browser suite runs five cases, all green. L-3 records that the Rust step reports success if the filter ever selects nothing. |
| 18 | Pending links are person-visible with only bounded public summary data; abandonment and fresh same-application retry work after restart | **PASS.** `PendingCredentialV2LinkSummary` (`cbcl_v2_completion.rs:315-323`) carries only `application_id`, `relay_origin` and a closed `phase` string; no checkpoint, grant, scope, recovery or profile bytes reach JavaScript. `pending_links` prunes stale index rows and lists every recognised pending phase, including pre-payload phases ineligible for terminal recovery, deliberately separate from `pending_application_ids`. `cbcl_v2_pending_links` is the only exposure. The browser case "TEST-1162 interrupted pre-payload links are visible and locally abandonable" passes, and the Rust case proves a fresh same-application ceremony persists after abandonment. One qualification: with the policy row absent, the row remains visible while its abandon control can never succeed — see M-2. |
| 19 | Installed reload reauthenticates live reciprocal authority before exposing capability; profile-only refresh is atomic; authority or issuer rotation prompts; handle change refuses; unavailable, oversized, and revoked stay distinct and fail closed | **PASS.** `cbcl_v2_reload_verify` (`cbcl_v2_commands.rs:739-950`) re-checks the root generation and the caller-named handle before any network call, then re-fetches the profile, re-fetches and matches the WebFinger JRD, resolves the Path-B quorum, agrees closures, re-verifies the reciprocal binding, and rehydrates the stored grant. Capability is true only for `Usable` and `ProfileRefresh`. `verify_reload` builds the profile-only replacement solely for `ProfileRefresh`, re-recognises the octets, requires the application, authority, digest and retained offer `kid` to hold, and commits through `replace_installed`'s CAS with the immutable projection. `classify_reload` is a total ordered cascade: root/application → `FreshPairingRequired`; handle → `HandleChanged`; authority or lost offer key → `AuthorityRotation`; issuer → `IssuerRotation`; revoked or grant mismatch → `Revoked`; digest → `ProfileRefresh`. `classify_webfinger_failure` is an exhaustive match over `NetError` with no wildcard: `NotFound → HubDeleted`, `TooLarge`/`Timeout`/`Transport`/`Refused → Unavailable`, `Recognition → InvalidBinding` (the received-but-unrecognisable case TEST-1163 permits). `record_retained` is true in every non-usable arm. Mutant 12 (`TooLarge → InvalidBinding`) is killed by `test_1163`. |
| 20 | Exact pins name one buildable source closure across Selfsame, cbcl-bus, [[SPEC-007-cbcl-pairing-cutover#ADR-802]], CI, Android, tests, and browser WASM provenance; the witness rejects each tracked-dirty sibling even under the labelled development override | **PARTIAL — pins PASS, CI execution FAILS (M-3).** All pins agree: Selfsame `cbcl-pairing.sha`/`cbcl-rs.sha`/`did-crdt.sha` = `62ef4a9`/`febc669`/`1f409a42`; cbcl-bus's four `.sha` files, `Dockerfile` ARGs 44-50 and the WASM provenance all match, including `selfsame.sha` = `0dbfb3c`; ADR-802's recorded baseline is `62ef4a9`; Selfsame CI and Android both read the three pin files and hash them into the cargo cache key. `crates/selfsame-pairing/build.rs` guards all three siblings and honours `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` only as a warning, while `crates/selfsame-pairing/tests/dependency.rs:32-54` has **no** override escape — it is the strict layer. I ran the full TEST-1164 matrix in a clean exact-pin closure with the override set: a tracked-byte change in `cbcl-pairing`, `cbcl-rs` and `did-crdt`, and a HEAD change in each of the three, **fail in all six cases**, each naming the right sibling; the restored clean closure passes. On the did-crdt question the brief asks me not to assume: `service` is a non-default feature (`did-crdt/Cargo.toml:12-16`), `pub mod service` is `#[cfg(feature = "service")]`, Selfsame's workspace consumes `did-crdt` with `default-features = false` (root `Cargo.toml:47`), no crate enables `service`, and the four post-pin commits touch only `src/service/*`, `src/bin`-adjacent server wiring and one spec. They participate in no credential/v2 runtime build. The failure is that the browser-WASM member of this closure has no executable continuous gate — see M-3. |
| 21 | The vendored WASM rebuild is byte-identical on rustc 1.96.0, wasm-bindgen 0.2.126, aarch64-apple-darwin, with SHA-256 `3834fc31…181388` | **PASS.** From four clean clones at the exact revisions, `scripts/check-selfsame-wasm-rebuild.sh` reported "Selfsame WASM rebuild matches the exact vendored package" (exit 0), comparing all four vendored files byte for byte. `shasum -a 256` of the vendored `selfsame_web_device_bg.wasm` is `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`, matching the claim and the provenance record exactly. Toolchain confirmed: `rustc 1.96.0 (ac68faa20 2026-05-25)`, `wasm-bindgen 0.2.126`, host `aarch64-apple-darwin`. |
| 22 | Production allocation remains closed and this review cannot authorize release, push, or deployment | **PASS.** `crates/selfsame-pairing/src/release.rs:11` declares `PRODUCTION_ALLOCATION_ENABLED: bool = false` and `:28` makes `require_production_allocation()` fail closed; both are asserted by `spec008_hold_invariance.rs` and by `rollback_hold.rs` through `std::hint::black_box`, so the constant cannot be folded away. cbcl-bus's relay keeps allocation behind two mutually exclusive, separately compiled features with the conformance surface absent from the release binary and production allocation requiring durable storage (`crates/cbcl-pairing-relay/src/main.rs:491-563`). This review authorizes nothing; see the closing statement. |

## Dispositions for the required mutants

Every mutation ran in a clean scratch clone with forced recompilation, was
verified applied, and was reverted afterwards; the tree was re-tested green
after each revert.

| # | Mutation | Gate | Result |
|---|---|---|---|
| 1 | Remove the `PayloadPrepared` retention in `remove_pre_payload_attempt` | isolated TEST-1162 | **KILLED** |
| 2 | Remove the immutable `same_attempt` comparison | isolated TEST-1162 | **KILLED** |
| 3 | Compensation deletes an `Installed` slot instead of refusing | isolated TEST-1162 | **KILLED** |
| 4 | Remove `unlink_local`'s exact slot compare-and-delete | isolated TEST-1162 | **KILLED** |
| 5 | Remove `unlink_local`'s still-trusted exact-pair policy check | isolated TEST-1162 | **KILLED** |
| 6 | Drop `root_generation` from the attempt-identity comparison | isolated TEST-1162 | **KILLED** |
| 7 | Empty the `PrePayloadPendingCleanup::drop` body (compensation guard drop) | isolated TEST-1162 | **KILLED** |
| 8 | Revert `cached_bootstrap_sequence` to `Err(Phase)` when `!awaiting_ack` | cbcl-pairing allocator suite | **KILLED** |
| 9 | Remove the hub's held-recovery-commitment comparison | cbcl-bus `rebar3 eunit` | **KILLED** (975 → 974; `test_119_recovery_status_is_locked_secret_free_and_linearizable_test` fails) |
| 10 | Replace `accept-tx`'s post-lock clock sample with the caller's `now` | cbcl-bus `rebar3 eunit` | **KILLED** (975 → 974) |
| 11 | Move `disarm_after_payload` before the durable `PayloadPrepared` write | library suite + isolated TEST-1162 + wallet browser suite | **SURVIVES — M-1** |
| 12 | Map an oversized WebFinger body to `InvalidBinding` (TEST-1163) | library suite | **KILLED** |
| — | CI test-selector drift: run the CI TEST-1162 command with a renamed test | `.forgejo` step semantics | **SURVIVES — L-3** (0 tests, exit 0) |

On the two "if load-bearing" mutants the brief names: the root-generation member
of the attempt comparison is load-bearing and killed (mutant 6); the compensation
guard drop is load-bearing and killed (mutant 7); the CI test selector is
load-bearing and its drift is **not** detected (L-3).

One further probe, not a mutation of production logic: applying REQ-1005's own
stated correction (`pub use profile::PROFILE_VERSION;` in place of the second
definition) compiles cleanly and passes the whole workspace suite — evidence for
M-4 that the obligation is unimplemented rather than infeasible.

## Dispositions for 0.5.11 M-1 and both LOW residuals

| Item | Disposition |
|---|---|
| **0.5.11 M-1** — four load-bearing CON-990/REQ-1006 predicates survive mutation, and the only test naming TEST-1162 runs in no automated gate | **CLOSED, independently re-executed.** All four named mutants — `PayloadPrepared` retention, `same_attempt` identity, the installed-slot refusal, and `unlink_local`'s compare-and-delete — are now killed by the isolated TEST-1162 case, as are the guard's `Drop` body and the root-generation member. Both gates are wired: the isolated Rust invocation at `.forgejo/workflows/ci.yml:170-176` and `npm run e2e:wallet-pairing` at `:319-322`, and I confirmed each selects and runs its intended tests. The 0.5.12 delta also added the exact-policy unlink guard TEST-1162's text requires, and mutant 5 kills its removal. What 0.5.12 did **not** close is the injection-site half of the same TEST-1162 clause (M-1), and the new guard introduces M-2. |
| **0.5.11 L-1** — orphaned issuer DID publication with no compensating retraction | **OPEN, unchanged in kind.** Re-derived from code here; re-recorded as this review's L-1 with consequence and operational treatment. |
| **0.5.11 L-2** — CON-036 anonymous recovery limiter keyed on the TCP peer address | **OPEN, unchanged.** Re-derived from code here; re-recorded as this review's L-2 with consequence and operational treatment. |

## Dispositions for every earlier named finding

### 0.5.10 findings

| Item | Disposition |
|---|---|
| **H-1** — restored allocator driven to `Terminal` at the acknowledged-`FinishedSent` checkpoint | **CLOSED, code- and mutation-confirmed.** See check 11 and mutant 8. |
| **H-2** — a terminal refusal after final approval but before payload send leaves an unremovable pending slot | **CLOSED in the automatic path; reopened in the confirmed-unlink path.** The compensation guard, `remove_pre_payload_attempt`, `pending_links`, `load_local_link`, `cbcl_v2_pending_links` and the interrupted-link UI all exist and behave as 0.5.10 demanded, and the verification obligation 0.5.11 raised is now largely met. But 0.5.12's new policy guard recreates an unremovable pending slot under a different, narrower trigger — M-2. |
| **L-1** — orphan DID publication with no compensating unpublish | **OPEN, unchanged.** This review's L-1. |
| **L-2** — CON-036 limiter keyed on the TCP peer address | **OPEN, unchanged.** This review's L-2. |
| **L-3** — `test_701` verified only the cbcl-pairing sibling | **CLOSED, executed.** `tests/dependency.rs` iterates all three siblings, checks HEAD and tracked status for each, and has no override escape; the full six-case TEST-1164 matrix fails in every case with the override set and passes on the clean closure. |
| **L-4** — an oversized WebFinger response reported as `revoked` | **CLOSED, mutation-confirmed.** `TooLarge → Unavailable` in an exhaustive `NetError` match with no wildcard; mutant 12 kills the reclassification. |

### 0.5.9 findings

| Item | Disposition |
|---|---|
| **H-1** — restored allocator cannot reopen at either bootstrap checkpoint holding a cached frame | **CLOSED.** `reopen_effects` emits `Open` plus at most the one cached frame at its recorded sequence; the acknowledged case is check 11. |
| **H-2** — REQ-1006 installed-state lifecycle unimplemented | **CLOSED, executed.** See check 19; `test_1159_reload_lifecycle_is_closed_over_every_required_state` and `test_1159_verified_profile_refresh_repins_only_live_profile_evidence` both pass in the default suite, and the browser case "TEST-1159 installed links reload, prompt on rotation, and unlink locally" passes. |
| **M-1** — hub signed `not-finalized` without authenticating the recovery token; commitment echoed from the caller | **CLOSED, mutation-confirmed.** `bind-recovery-commitment` (`cbcl-chat-selfsame-v2-store.lfe:535-547`) performs server-owned first-use binding under the pending-row write lock and aborts `rejected` on any later mismatch; the accepted arm additionally compares against the stored final row. Mutant 9 (comparison removed) is killed. |
| **M-2** — mutating accept transaction had no expiry gate | **CLOSED, mutation-confirmed.** `accept-tx` (`:612-634`) samples the clock only after the row write lock is held and aborts `expired` before projecting the witness. Mutant 10 is killed. |
| **M-3** — coordinated pins disagreed; Selfsame CI could not build the candidate | **CLOSED for Selfsame, build-confirmed.** A clean exact-pin closure builds and passes `cargo test --workspace --locked` (805 tests) with no override, and every pin file, Dockerfile ARG and provenance field agrees. The hub's CI is separately blocked for a different reason — M-3 in this report. |
| **L-1** — orphan DID publication and wedged slot | **SPLIT.** The wedged slot is closed in the automatic path (0.5.10 H-2 above); the orphan publication is this review's L-1. |
| **L-2** — limiter keyed on TCP peer address | **OPEN, unchanged.** This review's L-2. |

### 0.5.8 findings

| Item | Disposition |
|---|---|
| **H-1** — `selfsamepairstatus`/`selfsamepairabort` absent | **CLOSED.** The closed dialect members, hub-originated response class, session-WS handlers, pure decide functions and store transactions are all present and exercised; the 975-test EUnit suite and the 399-test browser-core suite both pass on the candidate. |
| **H-2** — CON-989 HTTPS recovery unimplemented | **CLOSED.** Traced on all three sides: the wallet adapter (`recover_claimant_completion`, `cbcl_v2_commands.rs:1110-1300`) restores only a post-final endpoint, POSTs the closed CBOR request, and installs only on a verified accepted status with an authority-rotation prompt before installation; the hub answers under one lock; cbcl-pairing supplies `CredentialV2RecoveredReceiptAuthority` and refuses to rebuild a grant, repeat a signature, republish a DID or resend a payload. The two defects 0.5.9 found inside it (its M-1, M-2) are closed and mutation-confirmed here. |
| **M-1** — cbcl-pairing's pin unenforced and inconsistent across three sources | **CLOSED.** Superseded by the three-sibling guard and witness; see check 20. |
| **L-1** — orphaned DID publication | **OPEN, unchanged in kind.** This review's L-1. |

### 0.5.7 findings

| Item | Disposition |
|---|---|
| **N-1** — socket-generation digest carried no reproducible encoding or bound | **CLOSED.** Two independent implementations produce the same canonical-CBOR preimage with the same bounds and both refuse out-of-bound input; the browser suite exercises the re-signed proof after a socket rebind. |
| **L-1** — one headroom assertion proved arithmetic rather than frame admission | **CLOSED.** Frame admission is executable end to end; `relay_work_bounds.rs` runs in the cbcl-pairing suite. |
| **L-2** — the global frame ceiling rested on an untested compatibility claim | **CLOSED.** The 69 632 ceiling is asserted at the relay recogniser, the endpoint checkpoint bound, the browser's `boundedB64u` limits and the relay queue configuration. |
| **L-3** — two member-naming conventions with no recorded split | **CLOSED (editorial).** ADR-963 makes the parent state current law directly; naming is consistent within it. |
| **L-4** — two citation forms for same-document and historical identifiers | **CLOSED (editorial).** |
| **L-5** — the v2 lifetime range admits carriers the coordinated hub always refuses | **OPEN as an honest, fail-closed residual, unchanged.** `decide-allocation` refuses `carrier-window` when the remaining carrier lifetime is under `pending-window-seconds` = 600 (cbcl-bus `apps/cbcl_chat/src/core/cbcl-core-selfsame-v2-pending.lfe:17`, `:35`) while the carrier grammar admits any `relay_expires_at`; the coordinated allocator always requests 900 s, so the coordinated path cannot produce such a carrier and a hand-made one is refused at allocation with no effect. Consequence is a refusal, never an acceptance. |
| **L-6** — one checkpoint key derivation stated less exactly than its sibling | **CLOSED, vector-confirmed.** Both derivations have exact executable vectors; `checkpoint_info_is_length_prefixed_and_application_separated` passes and pins the Selfsame-side wrapping key with application separation. |
| **L-7** — the finalize retry after a socket rebind needs a re-signed proof | **CLOSED, executed.** Exercised by the browser allocator suite; the hub re-verifies the proof on the `offered` retry path. |
| **0.5.1 L-4 (carried in the 0.5.7 table)** — one version constant declared twice under one name | **REOPENED — M-4.** The 0.5.7 disposition states both declarations exist and marks the item closed in the same sentence. REQ-1005 is a normative constraint on the implementation, both declarations are still present at the candidate, and the consumers are genuinely split. |

### F-A through F-E

| Hard stop | Disposition |
|---|---|
| **F-A** — exact application+relay, never relay-only | **CLOSED, code-confirmed.** `cbcl_v2_policy.rs` read in full: the row key is the full `(applicationId, relayOrigin)` tuple at every call site, rows are canonical and strictly ordered with a re-encode equality check, `MAX_ROWS` is bounded, and malformed state refuses rather than becoming a new TOFU prompt. `bind_finished_profile` writes a row only after both Finished values and re-checks an existing pair rather than assuming it. `exact_pair_never_authorises_another_application_on_the_same_relay` passes. |
| **F-B** — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed and executed.** See check 3. The seven `compile_fail` doctests (`profile::DisplayField`, `profile::DisplayIntent`, `CredentialV2Display` ×2, `PathAToBDisplay`, `EndpointCheckpointV2`, `CredentialV2RecoveredReceiptAuthority`) all pass in the cbcl-pairing doc-test run. |
| **F-C** — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed.** `recognise_claimant_invitation` → `authorise_claimant_relay` → `prepare_claimant` → `preview_claimant_identity` → `complete_claimant` is the only construction path; `recover_claimant_completion` is a terminal adapter that cannot construct, derive, sign, publish or resend. |
| **F-D** — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed.** See check 5. `CredentialV2Context::derive` binds the wallet's own profile digest into `ci`, both `ad` values and the public context, so Finished covers it. |
| **F-E** — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged).** ADR-902 states it; nothing in the candidate contradicts it; exact-pair consent is the stated containment. |

### [[SPEC-007-cbcl-pairing-cutover#REQ-812]]

| Clause | Disposition |
|---|---|
| No accepted credential or identity side effect before approved payload delivery (profiles without reverse issuance) | **Holds.** The v1 paths are untouched by the 0.5.12 delta, which changes only `src-tauri/src/cbcl_v2_completion.rs`, `src-tauri/src/cbcl_v2_commands.rs` and `.forgejo/workflows/ci.yml`. |
| No application identity effect before preliminary approval | **Holds.** The intent decision object is built and sent before any custody call. |
| Preliminary approval authorises one zeroizing derivation and pure DID/fingerprint computation only | **Holds.** `preview_identity` computes only `home_did` inside `Custody::use_hierarchy_root` and returns a string. |
| No signing, publication, resolution, issuance, persistence, capability staging or payload construction before final approval | **Holds.** Re-verified line by line at `cbcl_v2_commands.rs:307-560`. |
| Final approval authorises only the declared causal order | **Holds.** Re-derivation and byte-for-byte preview equality inside custody, then issuer creation, DID publication, closure resolution and verification, grant construction, payload construction — each with a durable `replace_pending` between stages. |
| One encrypted non-authorising completion checkpoint before the first causal effect | **Holds.** `persist_pending` at `:443` precedes `build_issuer_artifacts` at `:516`, and the sealed `EndpointCheckpointV2` is inside the persisted value. |
| Browser staging crash-safe and non-authorising until immutable hub acceptance | **Holds** for the in-band path and for the CON-989 post-window path. |
| Compensation touches only ceremony-owned effects and changes no pre-existing identity or operator withdrawal | **Holds in its prohibitive half.** No operator withdrawal or pre-existing identity is changed; the attempt-identity comparison confines removal to this ceremony. Two gaps remain in its positive half: the published issuer DID is still uncompensated (L-1), and confirmed unlink can become permanently unavailable (M-2). |

## Exact test commands and results

All commands ran from scratch worktrees or clean clones at the exact commits,
never from the candidate working trees.

| # | Command | Result |
|---|---|---|
| 1 | `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` | `authMethod=claude.ai`, `apiProvider=firstParty`, `subscriptionType=max` |
| 2 | Selfsame `cargo test -p selfsame --locked --lib` | **ok** — 66 passed, 0 failed, 1 ignored |
| 3 | Selfsame CI's isolated gate verbatim: `cargo test -p selfsame --locked --lib -- --ignored --exact cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot` | **ok** — 1 passed, 0 failed, 66 filtered out |
| 4 | The same command with a drifted test name | **ok, 0 tests run, exit 0** — evidence for L-3 |
| 5 | Selfsame `cargo test --workspace --locked` | **ok** — 805 passed, 0 failed, 6 ignored (TEST-1162 plus five live-network cases) |
| 6 | Selfsame `cargo clippy --workspace --all-targets --locked -- -D warnings` | **exit 0**, no warnings |
| 7 | Selfsame `CI=true npm run e2e:wallet-pairing` | **5 pass, 0 fail** (TEST-1161 ×2, TEST-814, TEST-1159, TEST-1162) |
| 8 | Selfsame `cargo test -p selfsame-pairing --locked --test dependency` (clean exact-pin closure, no override) | **ok** — 1 passed |
| 9 | TEST-1164 matrix with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1`: one tracked byte changed in `cbcl-pairing`, `cbcl-rs`, `did-crdt`; HEAD moved in `cbcl-pairing`, `did-crdt`, `cbcl-rs` | **FAILED in all six (intended)**, each naming the right sibling; restored clean closure **passed** |
| 10 | cbcl-pairing `cargo test --workspace --locked --all-features` | **ok** — 146 passed, 0 failed, 0 ignored across 29 targets, including 7 `compile_fail` doctests |
| 11 | cbcl-bus `rebar3 deps && rebar3 compile` | **exit 0**; all three purity boundary gates PASSED (cbcl_bus 25, cbcl_chat 67, cbcl_router 47 modules) |
| 12 | cbcl-bus `rebar3 eunit` | **All 975 tests passed** |
| 13 | cbcl-bus discovered browser-core suites (`find … -name '*.test.mjs'`, `node --test`) | **42 suites discovered** (ratchet requires ≥ 40), **399 tests, 399 pass, 0 fail** |
| 14 | cbcl-bus `scripts/check-selfsame-wasm-rebuild.sh` from clean clones at the four pinned revisions, rustc 1.96.0 / wasm-bindgen 0.2.126 / aarch64-apple-darwin | **"Selfsame WASM rebuild matches the exact vendored package"**, exit 0; vendored `.wasm` SHA-256 `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388` |
| 15 | The same script with `buildHost` set to CI's `x86_64-unknown-linux-gnu` | **FAILED**: "host … cannot reproduce the … artifact; re-run on … (CI's container platform)" — evidence for M-3 |
| 16 | Mutants 1–7 and 12 (Selfsame) | **KILLED** in every case |
| 17 | Mutant 8 (cbcl-pairing `cargo test --test credential_v2_allocator_session`) | **KILLED** — 2 passed, 1 failed |
| 18 | Mutants 9 and 10 (cbcl-bus `rebar3 eunit`) | **KILLED** in both cases — 974 passed, 1 failed |
| 19 | Mutant 11 (disarm before the durable `PayloadPrepared` write) against the library suite, the isolated TEST-1162 invocation and the wallet browser suite | **SURVIVES all three** — M-1 |
| 20 | REQ-1005 correction probe (`pub use profile::PROFILE_VERSION;`) with `cargo test --workspace --locked` | compiles with no warnings; **805 tests pass** — M-4 |
| 21 | M-2 permanence probe added to the candidate's own TEST-1162 body | **passed**, confirming three retries and a re-selection all refuse, `persist_pending` returns `PairingApplicationAlreadyLinked`, and only re-inserting the exact-pair row unwedges the slot |
| 22 | Post-revert re-runs of the Selfsame library suite after every mutation and probe | **ok** — 66 passed, 0 failed, 1 ignored; scratch trees verified byte-clean |

A collection failure or dependency-resolution failure was treated as no
evidence; none occurred in the runs above.

## What this verdict authorizes

Nothing is authorized by this report. **PASS does not authorize release, push,
or deployment**, and this is not a PASS. Production allocation remains held by
[[SPEC-007-cbcl-pairing-cutover#REQ-809]] and
[[SPEC-008-production-pairing-claimant#REQ-907]]; release, push and deployment
remain behind their own separate gates and the repository owner's approval.
Release and deployment controls are outside this review's authority.
