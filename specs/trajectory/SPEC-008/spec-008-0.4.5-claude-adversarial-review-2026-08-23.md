---
id: SPEC-008-0.4.5-claude-adversarial-review
title: Tier-1 fresh-context cross-model adversarial review — SPEC-008 0.4.5-draft (with adopted 0.4.4) and cbcl-pairing SPEC-001 0.3.0 + 0.4.0
status: complete
tier: 1
verdict: REJECT
verdict-selfsame-spec-008-0.4.5: REJECT
verdict-cbcl-pairing-spec-001-0.4.0: REJECT
verdict-cbcl-pairing-spec-001-0.3.0: PASS-as-adopted
reviewer: independent Circus adversarial reviewer, fresh context, no access to the authoring conversation
model: Claude Opus 5 (claude-opus-5)
review-date: 2026-08-23
candidates:
  - "[[spec-008-0.4.5-standalone-tofu-first-contact]] — specs/trajectory/SPEC-008/spec-008-0.4.5-standalone-tofu-first-contact.md at selfsame 08d60efb7901553b62c7ce70ba26d0388e5de766"
  - "[[spec-008-0.4.4-standalone-tofu-first-contact]] — adopted except where 0.4.5 replaces or corrects"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.4.0-credential-v2-endpoint-profile.md at cbcl-pairing 47424b7bf694b4dc090afe5c5af5b4c978a3fea9"
  - "/Users/anuna-01/Code/cbcl-pairing/specs/trajectory/SPEC-001/spec-001-0.3.0-commitment-authenticated-v2-mailboxes.md (adopted by 0.4.0)"
reviewed-revisions:
  selfsame-integration-baseline: 08d60efb7901553b62c7ce70ba26d0388e5de766
  selfsame-code-baseline: c6af6df75d033d2e44ae367457c1a754ba2c4c3a
  cbcl-pairing-spec-baseline: 47424b7bf694b4dc090afe5c5af5b4c978a3fea9
  cbcl-pairing-code-baseline: 8e55160a3234127bd60ce723fd93ba6b2b67a06b
  cbcl-bus: fdbe4142261923f48cced037a7f609e48a9fdd5a
  did-crdt: 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1
prior-review: "[[spec-008-0.4.4-claude-adversarial-review-2026-08-23]]"
findings: 0 critical, 2 high, 4 medium, 8 low
last-updated: 2026-08-23
---

# Verdict: REJECT

Two HIGH and four MEDIUM findings remain. The coordinated set does not pass.

- **Selfsame `SPEC-008` 0.4.5-draft — does not pass.** H-1, M-1, M-2, M-3, M-4.
- **cbcl-pairing `SPEC-001` 0.4.0-draft — does not pass.** H-2. Everything it
  states about the carrier, context, key schedule, envelope, intent digest,
  dialect, and store grammar was verified sound against `8e55160`, and it closes
  the prior review's H-1 for exactly the six parent contracts that review named.
  It fails on the seventh, which that review did not name and which credential/v2
  reverses in substance: `CON-007`, the parent's credential-profile contract.
- **cbcl-pairing `SPEC-001` 0.3.0-draft — sound as adopted.** Re-verified against
  `mailbox.rs`, `relay.rs`, `storage.rs`. `L-6` is closed completely by 0.4.0
  `CON-016`.

The 0.4.4 REJECT is substantially answered. H-2 and M-1 through M-4 and M-6 are
closed; L-1, L-2, L-3, L-5, L-6, L-8, L-9, L-10 are closed. H-1 is closed for the
contracts it named. M-5 is half closed: the store and did-crdt halves are closed
well, and the profile half is not closed but replaced by a different and larger
contradiction (H-1 below). L-4 and L-7 are half closed and each leaves a defect
behind (M-1 and L-1/L-2 below).

Neither HIGH is a security break. Both are authority/coherence defects of exactly
the class this gate exists to catch, and both have bounded corrections.

---

## Revision verification

| Repository | Reviewed revision | Verified |
|---|---|---|
| `selfsame` (this worktree, `circus/spec008-v045-review/2`) | `08d60efb7901553b62c7ce70ba26d0388e5de766` | `git status` clean at session start. `git diff --stat c6af6df75d033d2e44ae367457c1a754ba2c4c3a HEAD` lists **nine files, all under `specs/trajectory/SPEC-008/`, 10,244 insertions, 0 deletions**. The non-spec code baseline `c6af6df` therefore holds byte-for-byte. |
| `selfsame` code parent | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | Confirmed as the sole code ancestor of the candidate. |
| `cbcl-pairing` spec | `47424b7bf694b4dc090afe5c5af5b4c978a3fea9` | `git diff --stat 8e55160a3234127bd60ce723fd93ba6b2b67a06b HEAD` lists **two files, both under `specs/trajectory/SPEC-001/`, 1,023 insertions, 0 deletions**. Working tree carries untracked `.DS_Store`, `.zetl/`, `fuzz/.DS_Store`, `specs/.zetl/`, `src/.DS_Store` only. No tracked modification. |
| `cbcl-pairing` code parent | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | Holds exactly. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | `git status` clean. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | **Pre-existing dirt recorded, not edited:** modified `specs/did-crdt-spec.md`, untracked `.zetl/` and `docs/adr/ADR-007-xid-interoperability-posture.md`. No source, test, fixture, or asset file is dirty, so every code claim below reads committed `1f409a4`. |

No file outside
`specs/trajectory/SPEC-008/spec-008-0.4.5-claude-adversarial-review-2026-08-23.md`
was created or modified by this review.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct.

`v045:NNN` references
`specs/trajectory/SPEC-008/spec-008-0.4.5-standalone-tofu-first-contact.md`.
`v044:NNN` references
`specs/trajectory/SPEC-008/spec-008-0.4.4-standalone-tofu-first-contact.md`.
`companion040:` and `companion030:` reference the two cbcl-pairing trajectory
documents at `47424b7`. All other `file:NNN` references are to the confirmed
baselines.

**There are no CRITICAL findings.**

---

### H-1 (HIGH) — The amendment mandates a dispatcher whose version-1 branch provably rejects the one production profile it also mandates be frozen; credential/v1 is left with no recognisable profile at chat.anuna.io, and SPEC-007 CON-806 and cbcl-bus SPEC-075 REQ-007 are put in permanent contradiction with SPEC-004 CON-201 version 1

**Exact candidate clauses.**

`v045:132-138` (`REQ-957`):

> "This requirement **replaces 0.4.4 CON-933's instruction to re-mint the
> existing live ApplicationId** … The existing compatibility profile at
> `https://chat.anuna.io/selfsame/application` **SHALL remain byte-identical**
> during credential/v2 rollout and **is not claimed to satisfy the new v1/v2
> dispatcher**."

`v045:380-384` (`CON-947`): the generator produces "the existing compatibility
bytes … served unchanged throughout the rollout" and "one conforming version-2
output for `https://chat.anuna.io/selfsame/application/v2`, **used only by
credential/v2**."

`v045:388-392` (`CON-947`) release order: "**ship additive cbcl-pairing and
Selfsame recognition with allocation off**; ship did-crdt safe create/CORS; ship
hub side tables and v2 profile route with allocation off …"

Adopted and unreplaced, `v044:666-676` (`CON-933`):

> "A new dispatcher … reads only the integer version discriminator, then applies
> the complete closed grammar for that version before any semantic action.
> **Version 1 therefore recognises its nine required names including
> `rendezvous` and optional tenth `pairingRecordRelays`** … The reviewed
> single-grammar `PROFILE_VERSION = 1` implementation and generator **must be
> split**; relabelling their current nine-name output without dispatch is not
> conformance. **The currently shipped chat.anuna.io object — version 1 with
> `cbclPairingRelays` and without `rendezvous` — conforms to neither grammar.**"

Adopted and unreplaced, `v044:697-701` (`CON-933`):

> "**SPEC-007 CON-806 step 1 is amended so the credential/v1 relay-selection path
> also accepts a completely recognised version-2 profile** and selects only its
> `cbclPairingRelays`."

Also adopted: `v044:637` "CON-201 profile version 1 is not amended."

**Governing evidence.**

- `specs/SPEC-004-application-scoped-identity.md:2467-2472` (`CON-201`): the
  profile's "top-level value is an object whose member set is **exactly the ten
  names** below — `profileVersion`, `applicationId`, `accountAuthority`,
  `verifierAudience`, `allowedPermissions`, `enrollment`, **`rendezvous`**,
  `stateResolvers`, `revocation`, and the OPTIONAL `pairingRecordRelays`."
  `:2478-2480`: "**An unknown member at any depth is a rejection, not an
  extension point.**" There is no `cbclPairingRelays` in ratified version 1.
- `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-gate.lfe:24,28-55`:
  `routed?/0` returns `'true`, and the base64url octets decode to
  `profileVersion: 1`, `applicationId: https://chat.anuna.io/selfsame/application`,
  one `cbclPairingRelays` descriptor (`operatorId: anuna-1`,
  `relayOrigin: https://chat.anuna.io:9443`), **no `rendezvous`**, no
  `pairingRecordRelays`. The header comment records it as "the exact RFC 8785
  canonical document **minted 2026-08-22**" and "the ONE place the ratified
  octets live server-side".
- `cbcl-bus/apps/cbcl_chat/src/cbcl-chat-app.lfe:221-223`: the route
  `"/selfsame/application"` is compiled into the dispatch whenever `routed?/0`
  is true, so this object is being served now.
- `cbcl-bus/specs/SPEC-075-cbcl-pairing-relay-cutover.md:229-236` (`REQ-007`):
  "The generated Selfsame application profile **SHALL use the exact f2 profile
  member `cbclPairingRelays`** … The deployment **SHALL NOT emit legacy
  `rendezvous`** …"
- `specs/SPEC-007-cbcl-pairing-cutover.md:646-648` (`CON-806`): "**Profile
  grammar:** the coordinated SPEC-004 amendment defines `cbclPairingRelays` as
  one to sixteen closed descriptors", and `:660` step 1 "recognise the
  authenticated profile **under CON-201**". That coordinated CON-201 version-1
  amendment does not exist in either candidate; `v044:637` declines it explicitly.
- `crates/selfsame-app-identity/src/profile.rs:78-89`: today's single
  `TOP_LEVEL_REQUIRED` is the nine names **with** `cbclPairingRelays` and
  **without** `rendezvous`, `TOP_LEVEL_OPTIONAL` is empty, and `:384-390`
  requires `profileVersion == 1`. That is why the live object is accepted today
  and why splitting the recogniser is the change that breaks it.
- `crates/selfsame-app-identity/src/discovery.rs:231-236` and `:262-268`:
  `profile_request_path` returns the identifier's own path, so two ApplicationIds
  on one origin really can be served concurrently. The v2 half of `CON-947` is
  implementable; that is not what fails.

**Failure trace.**

1. This review returns PASS. `v045:658-674` step 3 commits failing schemas and
   pure-core tests; step 4 implements the pure Selfsame contracts.
   `v045:651` gives the pure core the "versioned profile dispatcher".
2. The dispatcher is written to `v044:666-676`: a version-1 branch that is
   ratified `CON-201` (requires `rendezvous`, rejects the unknown member
   `cbclPairingRelays`), and a version-2 branch that is `CON-933`'s nine names.
3. `CON-947` release step 1 ships that recognition. `REQ-957` freezes
   `https://chat.anuna.io/selfsame/application` byte-identical.
4. The frozen object declares `profileVersion: 1`. The dispatcher routes it to
   the version-1 branch, which rejects it twice over: `rendezvous` missing,
   `cbclPairingRelays` unknown. `REQ-957:138` concedes exactly this and stops.
5. The object cannot be repaired, because `SPEC-075 REQ-007` forbids the
   deployment from emitting `rendezvous` and requires `cbclPairingRelays`. The
   two governing contracts the candidate itself amends now disagree
   irreconcilably for the version-1 identity, and the candidate resolves neither.
6. credential/v1 is therefore left with **no recognisable profile at the one
   deployed origin**: its own profile fails `CON-201` version 1, and
   `CON-947:384` forbids it the version-2 profile. `CON-933`'s amendment of
   `SPEC-007 CON-806` step 1 — written precisely so credential/v1 could select
   relays from a version-2 profile once the live object was re-minted — becomes
   unreachable, and `SPEC-007 CON-806` becomes unsatisfiable at chat.anuna.io.
7. `CON-947:394-400`'s rollback reasoning ("The legacy profile never moved, so no
   profile reversal is needed") is true of the server and false of the client.
   The recogniser moved. Reverting it is a wallet release, not a feature gate,
   and no clause, slice row, or test row addresses that direction.
8. `TEST-1061` (`v045:612-618`) tests the server side only — "Old-client fixtures
   observe byte-identical legacy bytes" — and so cannot see any of this.

**Why this is not merely the disclosed residual.** `REQ-957:138` discloses that
the legacy bytes do not satisfy the dispatcher. It does not disposition the
consequence: no requirement retires credential/v1 at that ApplicationId, no
inherited-stop row records that `SPEC-007 CON-806` loses its only conforming
profile, `v044:697-701`'s CON-806 amendment is left in force pointing at nothing,
`SPEC-075 REQ-007` is still claimed as amended (`v045:8`, `v045:681-684`) while
0.4.5 removed the only clause that changed anything about it, and no test row
covers the client-side rollback. M-5 was raised because three outward-facing
one-way changes had no ordering; 0.4.5 fixed the store and did-crdt halves well
and replaced the profile half with a contradiction between three named contracts.

**Minimum correction.** Choose one and carry it through every dependent clause:

- **(a) Amend `CON-201` version 1** to admit `cbclPairingRelays` — the amendment
  `SPEC-007 CON-806:646-648` already asserts exists. That makes the frozen live
  object conforming under the version-1 branch, removes the contradiction with
  `SPEC-075 REQ-007`, and preserves the byte-identity `REQ-957` wants. It
  requires reversing `v044:637` ("CON-201 profile version 1 is not amended"),
  adding CON-201-v1 to `v045:8` and to the `v045:681-684` approval list, and a
  `TEST-1042`-class row asserting the exact live octets pass the version-1
  branch; or
- **(b) State that credential/v1 at `https://chat.anuna.io/selfsame/application`
  is retired by this release**, add a `SPEC-007 CON-806` row to the inherited
  table recording that it has no conforming profile at that identifier, delete or
  re-point `v044:697-701`, drop `SPEC-075 REQ-007` from `amends:` (nothing in
  0.4.5 changes it), and add a test row asserting the legacy route's only
  remaining consumers are pre-dispatcher builds.

Either way add a client-side rollback clause to `CON-947`/`CON-949`: the
recogniser split is a wallet release, and reverting it is not a feature gate.

---

### H-2 (HIGH) — cbcl-pairing `CON-007` — the parent's credential-profile contract, which pins the application identifier, puts the secret in the machine carrier, and *excludes a typed credential code* — is reversed in substance by credential/v2 and appears in no `amends:`, no existing-stop table, and no approval list in either coordinated amendment

**Governing clause.**
`cbcl-pairing/specs/SPEC-001-reusable-blind-pairing.md:913-926` (`CON-007`
Credential profile), verbatim:

```text
application       anuna.io/credential/v1
carrier           QR, deep link, NFC, or OS handover
PRS               16 CSPRNG octets
locator           direct 32-octet mailbox identifier
approval          explicit person action
display           application ID, HTTPS origin, requested scope
```

> "The machine carrier transports relay origin, locator, **and secret**. **A
> typed or spoken credential code is excluded.**
>
> The 16-octet secret enters CPace directly."

The exclusion is the deliberate contrast with `:889-902` (`CON-006` Agent
profile), whose carrier is "configured relay + numeric nameplate + **two words**"
and whose `PRS` is "two big-endian BIP-39 indices". `CON-006` is the typed-code
profile; `CON-007` is the machine-carrier profile, and says so.

`:1486-1496` §Amendment Channels: "**Through:** a reviewed, merged revision of
this specification with updated assets, tests, evidence, and **compatibility
disposition**. **Not amendable by:** … implementation behaviour, test fixtures,
or dependency updates." `CON-007` is not on the `:1497-1508` hard-stop list —
neither are `CON-001`, `CON-002`, `CON-004`, or `CON-005`, the four contracts the
0.4.4 review's H-1 was raised for on exactly this clause.

**What the candidates do to it.**

- `v044:706-724` (`CON-934`) with `companion040:178-194` (`CON-013`): the v2
  machine carrier carries `version`, `profile`, `application-context`,
  `profile-version`, `relay-origin`, `locator`, `ceremony-id`, `carrier-nonce`,
  `claim-commitment`, `relay-expires-at`, optional `expected-allocator-key`.
  **No secret.** `CON-013:199` makes an "embedded `C`/`T`" a recognition refusal.
- `v044:726-733` (`CON-934`): the `PAIR1-` presence code is "the canonical human
  representation … exactly eleven hyphen-separated groups of five Crockford
  Base32 characters", transcribed by the person. `v044:245-247`: "Selfsame SHALL
  accept the code only through a visually distinct pairing field … The field has
  no paste, autofill, password-manager, accessibility injection, deep-link,
  notification, QR, or peer-data path." That is a typed credential code, and its
  no-paste rule is defended as an "intentional co-presence constraint"
  (`v045:92-93`).
- `companion040:94-95` (`REQ-020`): "The CPace `PRS` SHALL be the raw sixteen
  octets `C`" — sourced from the typed code, not from the machine carrier.
- `v044:266-291` (`REQ-945`): the v2 display adds account principal, account
  label, permissions, device DID and platform binding to `CON-007`'s three-item
  `display` row.

**What the amendments say about it.** Nothing. `companion040:8` `amends:` names
`CON-001`, `CON-002`, `CON-004`, `CON-005`, `CON-009`, `CON-010`, `NFR-007`,
`NFR-008` — exactly the six contracts the 0.4.4 review's H-1 enumerated, and no
more. `companion040:52-67`'s existing-stop table has rows for REQ-002,
REQ-003/005, REQ-008, REQ-009, REQ-013/014/015, CON-001, CON-002, CON-004,
CON-005, NFR-007/CON-009, NFR-008/CON-010 and the production gates. **`CON-006`,
`CON-007` and `CON-008` have no row in either coordinated amendment**, and
`v044:134-168`'s inherited-hard-stop table has none either.
`companion040:484-489`'s approval list names four principals but describes the
change as the carrier/context/schedule/envelope/dialect set.

Nor is the gap filled by adding a v2 profile contract: `SPEC-001` catalogues its
application profiles as `CON-006` and `CON-007` with a six-row table each
(`application`, `carrier`, `PRS`, `locator`, `approval`, `display`).
`companion040` adds `CON-013`/`CON-014`/`CON-015`/`CON-016` — carrier CDDL,
envelope, dialects, assets — and no analogous credential/v2 profile row. The
parent's profile catalogue is left with two entries, one of which now
contradicts the design.

**Why REQ-003 and REQ-011 do not cover it.** `SPEC-001:289-293` REQ-003 permits
adding an application with "only an endpoint-side profile", and `:396-406`
REQ-011 gives application profiles application meaning. `companion040:395-401`
(`ADR-014`) expressly declines that basis: the amendment exists "preventing the
false claim that credential/v2 can be implemented as an application-only wrapper
around the v1 context and reducer." Having conceded that credential/v2 is a
protocol version rather than a profile addition, the amendment cannot then rely
on the profile-addition affordance to leave `CON-007` undisturbed.

**Failure trace.**

1. This review returns PASS. `v045:663-665` encodes the design as an SPL theory
   with "every upstream amendment approval" as a gate.
2. The cbcl-pairing owner is handed 0.3.0 and 0.4.0. 0.4.0's existing-stop table
   tells them CON-001/002/004/005/009/010 and NFR-007/008 change. They approve.
3. Implementation lands a credential profile whose secret is not in the machine
   carrier and whose CPace `PRS` arrives through a 55-character typed human code
   — the one carrier shape `CON-007` names and excludes.
4. `CON-007` is thereby amended by implementation and by new assets, which
   §Amendment Channels lists as **not** an amendment channel, with no revision
   covering it and no compatibility disposition.
5. `companion040:18-25`'s own opening — "the earlier companion described relay
   admission correctly but did not authorize or specify the endpoint protocol
   that consumed it" — is true of this document about `CON-007` in the same way
   it was true of 0.3.0 about `CON-004`.

**Minimum correction.** In `companion040`: add `CON-007` to `amends:`; add
`CON-006` (retained, no interference — the agent profile's typed channel and
entropy floor are untouched) and `CON-007` rows to the existing-stop table
stating exactly that credential/v2 moves the CPace secret out of the machine
carrier into a typed presence code, adds a second independent relay-claim bearer,
and widens `display`; and either add a `CON-0xx: Credential/v2 profile` contract
with the same six-row shape as `CON-006`/`CON-007`, or state in `CON-007`'s row
that the contract is hereby scoped to `anuna.io/credential/v1` and that the v2
profile table is `CON-013` plus `REQ-020`. Name `CON-007` in the approval
section. Add a `TEST-038`-adjacent row asserting a v1 endpoint refuses a carrier
with no `secret` member and a v2 endpoint refuses a carrier that carries one.

---

### M-1 (MEDIUM) — `CON-944`'s reset crash rule and release-blocking `TEST-1052` both assert a "remaining valid old slot" that the reset precondition makes unreachable; the row cannot be authored as written

**Exact candidate clauses.** `v045:288-293` (`CON-944`):

> "**When no slot is valid**, gated `reset relay policy` acquires the
> installation lock, deletes slot A and verifies absence, deletes slot B and
> verifies absence, then returns the ordinary both-absent empty state after a
> valid root opening. **A crash after one deletion leaves the other old valid
> slot authoritative**; retry completes reset."

`v045:550-556` (`TEST-1052`), release-blocking:

> "Crash after deleting either reset slot and **require the remaining valid old
> policy**; retry deletes both and reaches empty only after root opening."

**Governing clauses.** The reset trigger is `v044:792-793` (`CON-935`, adopted
and not replaced on this point): "**If at least one policy slot exists but
neither is valid**, policy management SHALL offer `reset relay policy` only
after the same passcode/platform-presence gate." `v045:288` restates it as "when
no slot is valid". The read rule, `v044:779-782`, is also adopted: "Both slots
absent means empty only after a valid root opening; **a present record that
yields no valid slot** … **refuses**." The ordinary update rule `v044:783-789`
cannot run either, because it "re-reads both slots, computes `revision + 1`" and
no revision is readable when no slot is valid.

**Failure trace.**

1. Reset is offered. Its precondition is that **no slot is valid**.
2. Reset deletes slot A and verifies absence. The process crashes.
3. The surviving state is: slot A absent, slot B **present and invalid** — the
   only thing it can be, given step 1.
4. `CON-944:292` says this "leaves the other old **valid** slot authoritative".
   There is no valid slot. The next read hits `v044:779-782` and **refuses**,
   which `v044:187-188` turns into "opens no socket".
5. The durable outcome is still correct — retry deletes B and reaches empty — but
   the stated crash outcome is false, and `TEST-1052`'s "require the remaining
   valid old policy" describes a fixture no conforming implementation can be
   driven into. An implementer cannot author the row; a harness that fabricates a
   valid slot before invoking reset is testing a path the contract forbids.

This is the residue of the L-4 fix. L-4's other half — the both-slots-valid-at-
equal-revision write target — is closed correctly (`v045:287-288`: slot A is the
deterministic current slot, the next update overwrites slot B, which preserves
the torn-write safety argument because A at revision *r* stays valid).

**Minimum correction.** Replace `v045:292` with the true outcome: a crash after
the first deletion leaves the other slot present and invalid, the ordinary read
still refuses, and a retry completes the reset. Rewrite `TEST-1052`'s second
sentence to require that refusal and the completing retry. If a reset from a
*valid* policy ("clear every accepted pair") is also intended, say so explicitly
in `CON-944`, because `v044:792` currently makes it unreachable and
`v044:810-812`'s "list and remove exact pairs" is an ordinary revisioned update,
not a reset.

---

### M-2 (MEDIUM) — The rollback conflict test is defined on a "revision" that the legacy Mnesia rows do not carry, and under the revision reading the compensation path restores tombstone rows — re-imposing a withdrawal an operator has just lifted, which `REQ-958` and `CON-941` both forbid the ceremony to touch

**Exact candidate clauses.** `v045:368-372` (`CON-946`):

> "The pending transaction captures **exact old legacy and side-index rows**.
> **Rollback restores them byte-for-byte or stays pending on a revision
> conflict.**"

`v044:1041-1049` (`CON-941`, adopted except for the reinstatement sentence):

> "After finalization it uses the captured versions to **restore the exact prior
> alias, account, tombstone, and pairaccount rows** … an intervening conflicting
> writer fails closed and keeps compensation pending. **It SHALL NOT create a
> withdrawal tombstone as compensation.**"

`v045:163-166` (`REQ-958`): "A v2 ceremony **SHALL NOT lift, delete, replace, or
downgrade** any `cbcl-webfinger-withdrawn` tombstone."

**Code evidence.**

- `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:54-56,62`:
  `(defrecord cbcl-webfinger-alias alias account home-did)`,
  `(defrecord cbcl-webfinger-account account alias home-did)`,
  `(defrecord cbcl-webfinger-withdrawn alias account home-did)`. **No revision,
  version, or sequence attribute on any of the three.**
- `cbcl-bus/apps/cbcl_chat/src/shell/cbcl-chat-pairaccount.lfe:31-36`:
  `(defrecord cbcl-pairaccount-grant key handle devkey granted state)`. `granted`
  is a millisecond timestamp, not a revision.
- `v045:355-358` puts "revision" only in the **new** side-index tables
  (`cbcl-selfsame-alias-index`, `-account-index`, `-withdrawal-index`,
  `-pairaccount-index`). A writer that touches a legacy row without touching a
  side index therefore bumps nothing.
- `cbcl-chat-webfinger-store.lfe:294-307`: `reinstate/2` is
  `(reinstate account home-did)`; it runs `lift-tombstone` in its own
  `mnesia:transaction` and `mnesia:delete`s the matching tombstone. It touches no
  side index and cannot, since none exists today. A grep across
  `cbcl-bus/apps/**` finds callers of `provision/3`, `withdraw/2` and
  `reinstate/2` **only in tests** — they are operator runbook levers, exactly as
  `REQ-958` says.

**Failure trace.**

1. Ceremony C runs for stable account *P*. `selfsamepairaccept` passes
   `REQ-958`'s tombstone check (C's own recomputed URI is not tombstoned, else it
   terminates before pending state) and captures the prior alias, account,
   **tombstone** and pairaccount rows for *P* per `CON-941:1043-1044`. Account *P*
   holds an unrelated tombstone generation *T* at URI *u* — a shape the store
   explicitly supports (`:245-247` "ONE ACCOUNT CAN HOLD SEVERAL TOMBSTONES").
2. While C is in flight, an operator determines *T* was aimed at the wrong
   account and runs `reinstate(P, did_u)`. `lift-tombstone` deletes the row at
   *u*. No side-index revision changes, because `reinstate/2` does not know the
   side tables exist.
3. C fails after finalization. Compensation runs `CON-941`'s rollback. Its
   conflict test is `CON-946:371`'s "revision conflict"; every revision it can
   read is unchanged. It therefore takes the "restores them byte-for-byte" branch
   and rewrites the captured tombstone row at *u*.
4. The operator's reinstatement is silently undone by the constrained party's
   ceremony, and a withdrawal tombstone now exists that the ceremony wrote —
   which `CON-941:1045-1046` forbids in the same paragraph.

The alternative reading — that "conflicting writer" means an exact byte
comparison of every captured row re-read under the write lock — is safe and is
what `CON-941:1044-1045` implies. But `CON-946:371` names the test as a revision
comparison, the legacy rows have no revision, and nothing says which governs. The
ambiguity sits directly on a revocation-table write, which is the boundary M-6
was raised about.

**Minimum correction.** Two sentences. In `CON-946`: legacy rows carry no
revision, so their conflict test is exact comparison of the captured bytes
against a write-locked re-read, and any difference leaves compensation pending;
the revision test applies only to the new side-index rows. In `CON-941` (or as a
`REQ-958` sentence): the ceremony captures and restores only the rows it may
write — alias, account, pairaccount and its own side-index rows — and
`cbcl-webfinger-withdrawn` is read-only to every v2 command in both directions,
so tombstone rows are outside the capture and outside the rollback. Extend
`TEST-1056` with a concurrent operator `reinstate/2` during a failing ceremony,
asserting the lift survives compensation.

---

### M-3 (MEDIUM) — The distinct v2 ApplicationId changes the person's derived home DID, account scope and reciprocal alias, and the reviewed account store admits one binding per account, so credential/v1 and credential/v2 are mutually exclusive per chat account — stated nowhere, and decisive for `REQ-946`'s CON-221 branch

**Exact candidate clauses.** `v045:477-483` (`ADR-929`):

> "A profile is an immutable application identity. Serving version 2 at a new
> canonical ApplicationId avoids content negotiation and lets the current live
> compatibility artifact remain unchanged during rollout and rollback. **The
> person still sees the same authenticated chat.anuna.io origin**; exact-pair
> TOFU records the new canonical v2 application ID."

`v045:139-144` (`REQ-957`) names the identifier and lists what must reflect it:
"The carrier, live fetch, embedded browser bytes, permissions, offer, policy
pair, and vectors all name that exact v2 identifier." Derived identity is not in
that list.

**Code evidence.**

- `crates/selfsame-app-identity/src/hierarchy.rs:267-283`:
  `application_node(root, application)` derives from the `ApplicationId`, and
  `:178-182` `HomeKey::home_did` derives the DID from that node. `:400`'s own
  test is named `a_one_octet_application_id_change_changes_the_home_key`. A
  distinct ApplicationId therefore yields a **distinct home DID** for the same
  root and the same person.
- `cbcl-chat-webfinger-store.lfe:29-35`: the alias is
  `base32(SHA-256(home_did))`, so a distinct home DID yields a **distinct
  reciprocal alias URI**.
- `cbcl-chat-webfinger-store.lfe:55,71-73`:
  `(defrecord cbcl-webfinger-account account alias home-did)`, created as
  `type set` keyed on `account`. **One account row holds exactly one alias and
  one home DID.** An account cannot hold both a v1-derived and a v2-derived
  binding.
- `v044:307-309` (`REQ-947`) already scopes the CON-211 scope per
  `(applicationId, applicationAccountId)`, so the scope diverges too.

**Failure trace.**

1. A chat account completes credential/v1 enrolment against
   `https://chat.anuna.io/selfsame/application`, provisioning alias
   *u1 = base32(SHA-256(did1))*.
2. The same person runs a credential/v2 ceremony. `CON-938`'s `preview_issuer`
   derives *did2 ≠ did1*, because the ApplicationId differs.
3. `CON-941`'s `selfsamepairauthority(ceremonyId)` "reads the account-keyed
   authority store" and returns `Bound(did1)`.
4. `v044:311-313` (`REQ-946`): the browser maps every other `Bound(did)` to
   `BoundDifferentDid`, and "`BoundDifferentDid` refuses."
5. Every account with a v1 binding is permanently refused by credential/v2, and
   the only repair is an operator `withdraw/2` — which `REQ-958` then makes
   require an operator `reinstate/2` before any further ceremony.

**Bounded today, unbounded on the stated rollout.** The exposure is currently
empty: `provision/3` has **no production caller** (a grep across
`cbcl-bus/apps/**/src` finds tests only), so no production alias row exists and
step 1 cannot have happened yet. That is precisely why this must be stated now —
`REQ-957:136-138` and `CON-947:380-383` promise the legacy identity is retained
"during credential/v2 rollout", which reads as coexistence, and the account store
cannot express coexistence.

**Minimum correction.** Add to `ADR-929` and `REQ-957`: the v2 ApplicationId
derives a distinct home DID, distinct CON-211 scope, and distinct reciprocal
alias; the reviewed `cbcl-webfinger-account` store is a `set` keyed on the opaque
account with one alias and one home DID, so an account holds at most one binding
and v1 and v2 linking are mutually exclusive per account. State that no
production binding exists today (`provision/3` has no production caller), which
is also L-1's outstanding clause, and that any future v1-to-v2 transition is an
operator `withdraw/2` plus `reinstate/2` followed by a fresh v2 ceremony. Extend
`TEST-1034` with a v1-bound account attempting a v2 ceremony and require
`BoundDifferentDid`.

---

### M-4 (MEDIUM) — `CON-950`'s per-DID lock covers only the HTTP write handlers; the did-crdt gossip merge is a second in-process writer to the same `DocStore` with its own unlocked best-effort save, so `REQ-961`'s "acknowledge only after durable commit" can be falsified after the acknowledgement

**Exact candidate clauses.** `v045:216-223` (`REQ-961`):

> "`POST /dids` and `POST /dids/:did/deltas` SHALL acknowledge success **only
> after the complete resulting document snapshot is durably committed**."

`v045:451-459` (`CON-950`):

> "The production did-crdt `AppState` SHALL expose a **keyed asynchronous
> mutation lock per DID** and a fallible durable-save adapter. Under that lock
> **a write handler** clones the prior document, applies creation or one verified
> delta to the clone, persists and read-verifies the complete candidate snapshot,
> and only then swaps the in-memory document and emits success. … **A crash after
> durable save but before the memory swap recovers the candidate at startup**."

**Code evidence.**

- `did-crdt/src/service/server.rs:90-92`: `AppState.docs` is
  "Canonical CRDT document store, **shared with the sync layer** when enabled."
- `did-crdt/src/sync/live.rs:336-370`: the gossip receive loop merges inbound
  deltas into that same store — `let mut guard = docs.lock(); merge_inbound(&mut
  guard, deliver, &policy)` — and then, on `MergeOutcome::Merged |
  Bootstrapped`, re-reads the document and calls `store.save(&doc).await`,
  logging any failure to stderr. It is a full write path with its own persist,
  and it takes no per-DID lock because none exists.
- `did-crdt/src/sync/store.rs:142-161`: `BlobStore::save` imports the bytes and
  then `set_tag(did_tag(did), hash)`. The tag is the commit point and it is
  **last-writer-wins**: whichever save calls `set_tag` last defines what
  `load_all` recovers.
- `did-crdt/fly.toml:12,22-35,41-44`: the production resolver runs the full node
  (`did-crdt-resolver`, `STORAGE_PATH=/data` on a mounted volume,
  `DISABLE_DHT_PUBLISH` unset, `auto_stop_machines = "off"`), so the node
  publishes to the DHT and the gossip path is live.

**Failure trace.**

1. The wallet's `POST /dids/:did/deltas` takes `CON-950`'s per-DID lock, clones
   *D0*, applies the assertion-method delta to the clone giving *D1*, saves *D1*,
   read-verifies it, swaps memory to *D1*, returns 202. Under `REQ-961` that 202
   means the issuer's state is durably committed.
2. Concurrently the gossip loop receives a remote delta *d2* for the same DID,
   takes only the global `docs.lock()`, merges into whatever is in memory, and
   awaits `store.save(&doc)`.
3. If the gossip save's `set_tag` lands after the HTTP save's, the durable
   snapshot is the gossip candidate, built from a clone the HTTP handler had
   already superseded. The acknowledged *D1* is no longer what `load_all`
   recovers at startup.
4. Symmetrically, if the HTTP swap lands after the gossip merge, *d2* is
   discarded from memory and overwritten on disk.
5. `REQ-961:221-223`'s "A save failure leaves the in-memory document
   byte-identical to its prior state" and `CON-950:457-459`'s crash-recovery
   claim are both stated over a document only one of two writers serialises.

The failure is a lost merge rather than corruption — this is a CRDT and the peer
retains *d2* — but it falsifies the acknowledgement guarantee that `REQ-961`
exists to establish, which is the guarantee the browser's CON-206 step 4/5
closure fetch depends on.

**Minimum correction.** One clause in `CON-950`: **every** writer to the document
store, including the sync/gossip merge path and any bootstrap admission, SHALL
acquire the same per-DID lock and use the same clone, durable-save, read-verify,
swap order, and no path may persist a snapshot it did not build under that lock.
Add the single-writer premise the design already relies on (`fly.toml:32-35`, one
machine bound to one volume) so the in-process lock is sufficient. Extend
`TEST-1064` with a concurrent gossip merge on the same DID during each HTTP write
boundary, asserting that the acknowledged state is present after restart and that
no accepted delta is dropped.

---

### L-1 (LOW) — `NFR-907`, whose stated job is to include every CBOR head, adds one that never occurs

`v045:234-237`: "A large object total comprises one map byte, two two-byte uint
pairs, one 35-byte digest pair, a two-, three-, or four-byte key-plus-value
`logicalLength` pair, and one key plus the **three- or five-byte** byte-string
head plus the fixed body."

Field 4 is `bstr .size 4096 / bstr .size 64512` (`companion040:272`). Both are
above 255 and below 65,536, so the head is `59 xxxx` — **always three octets**. A
five-octet head requires a byte string above 65,535, which occurs one layer up,
at the 66,644-octet ciphertext inside `sealed-frame`, and `v044:592-596` already
accounts for it there. `v044:586-587`'s original wording ("the three-byte
byte-string head") was correct on this point. Every total in `NFR-907` is right:
I recomputed 1+2+2+35+2+(1+3+64,512) = **64,558** at `logicalLength` 1..23; +1 =
**64,559** at 24..255; +2 = **64,560** at 256..62,000. Say "three-byte".

### L-2 (LOW) — The L-7 fix corrected the large class only; `TEST-1030`'s "every control type … encodes to 4,144" remains false for control bodies under 256 octets, and `TEST-1060` re-blesses it

`v044:1316-1317` (`TEST-1030`): "Every control type pads to 4,096 and **encodes
to 4,144**." `v045:606-610` (`TEST-1060`) asserts the large-class boundaries and
then "plus **all unchanged** plaintext, ciphertext, relay-body, queue, and v1
witnesses."

4,144 is the control-class **maximum**, reached only when `logicalLength` needs a
three-octet uint: 1+2+2+35+4+(1+3+4,096) = 4,144. A control body with a logical
length of 24..255 encodes to **4,143** and one of 1..23 to **4,142**.
`companion040:296-299` requires every non-offer body to carry a raw 32-octet
`predecessorDigest` plus its closed logical grammar; an `intent-approve` or
`final-approve` body under 256 octets is the expected case, not a corner. The
derived plaintext/ciphertext/relay-body maxima (6,212 / 6,228 / 6,278) are all
correct because they are computed from the maximum. Restate the control class as
4,142 / 4,143 / 4,144 by the same `logicalLength` boundaries, and give
`TEST-1060` the three control witnesses it gives the large class.

### L-3 (LOW) — `CON-934`'s closed carrier field list is never restated with `relay-expires-at`

`v045:181-183` (`REQ-959`) says the relay expiry "is a mandatory
CON-934/companion CON-013 carrier field". `companion040:189` has it.
`v044:711-721`'s "closed fields" list does not, and 0.4.5 declares no replacement
of that block. Because both documents are normative over "the exact canonical
carrier bytes" that feed `carrierDigest`, two implementations built from the two
field lists produce different digests and cannot reach Finished. Deterministic
CBOR key ordering makes position irrelevant, so the correction is one line:
restate `CON-934`'s field list with `relay-expires-at uint`, or say `CON-013` is
the sole normative carrier grammar and `CON-934` is illustrative.

### L-4 (LOW) — `CON-013` puts an allocator construction rule inside the claimant's recognition rules

`companion040:198-200`: "Extra keys, duplicate keys, trailing bytes,
non-deterministic CBOR, nameplates, embedded `C`/`T`, a non-direct locator, **or
a relay expiry not returned by the successful `allocated-v2` response** refuse."
The claimant never observes `allocated-v2`; only the allocator can apply that
test. Move it to the allocator's carrier-construction obligation, or state that
the claimant enforces it indirectly through `carrierDigest` equality at Finished
and the later signed-offer equality (`v045:190-193`), which is what actually
detects a substituted expiry.

### L-5 (LOW) — `CON-944`'s "completes the deletions" is ambiguous about the old root, and `TEST-1050`'s convergence set omits a state `CON-944` explicitly permits

`v045:298-304` lists the purge as marker, both policy slots, ledger record and
key, then "**delete the old `SealedRoot` last**", then "Startup seeing `purging`
**completes the deletions** and marker removal before opening or creating a
root." Read to include the root, an interrupted `forget` destroys the identity on
the next launch; read to exclude it, the old root survives with empty safety
state. `TEST-1050` (`v045:536-540`) disambiguates toward the second — "Startup
converges to **either the old root with empty safety state** or the new root with
empty safety state" — but `CON-944:303-304` also permits "a crash after root
deletion leaves no root and safely retries", which `TEST-1050`'s two-way
disjunction excludes. Both readings preserve the security property this contract
exists for (`v045:99-103`: no new root ever sees stale policy), because the root
is written only after both slots and the marker are verified absent, and startup
will not create a root while the marker is present — I checked that against
`src-tauri/src/custody.rs:190-197` (`restore` refuses when an identity exists),
`:344-346` (`forget` deletes only `ROOT_ENTRY`), and `:200-224` (a fresh 16-octet
Argon2id salt per seal, so the old `policyKey` cannot open under a restored
root). Say which deletions startup completes, and add the no-root state to
`TEST-1050`.

### L-6 (LOW) — `companion040 CON-014`'s CDDL admits a `logicalLength` its own prose forbids

`companion040:266-273` gives `3: 1..62000` and `4: bstr .size 4096 / bstr .size
64512`, so the grammar alone permits `logicalLength = 62,000` beside a
4,096-octet field 4. `:279-281`'s prose closes it — "`logicalLength` is at most
the selected field-4 length" — and `TEST-044` tests it. Since `CON-014` is
presented as the normative envelope CDDL and `CON-016` requires an asset hash
over it, express the constraint in the grammar (two arms, or a `.size` control)
rather than only in prose.

### L-7 (LOW) — Three superseded sentences survive in the adopted 0.4.4 text

The `adopts:` clause plus the disposition table carry these, but a reader working
from the effective candidate meets the stale wording first: `v044:510-511` "The
effective deadline is the earlier of the **state idle deadline** and the absolute
deadline" refers to a table `v045:178-179` replaced; `v044:1064` "**CON-939**
migration SHALL complete before either v2 command is reachable" is the L-3
misattribution that `CON-946` fixes only in its own text; and `v044:1053-1058`'s
legacy-key table still says `cbcl-chat-pairaccount` and "handle"-keyed account,
corrected only by `v045:353-358`. Two of the three are the exact strings L-3 and
L-1 asked to be renamed. Mark them replaced inline.

### L-8 (LOW) — `SPEC-075 REQ-007` is still claimed as amended after the only clause that changed it was removed

`v045:8` and `v045:681-684` keep "cbcl-bus SPEC-075 REQ-007" in `amends:` and in
the owner-approval list. The clause that changed it was `v044:678-680`'s
instruction to re-mint the live release profile as version 2, which `REQ-957:134`
replaces. Under 0.4.5 the frozen legacy object already satisfies
`SPEC-075 REQ-007:230-236` unchanged, and the new v2 generator output satisfies
it too (`cbclPairingRelays` present, `rendezvous` absent). Either drop it from
`amends:` or state what it is amended to. (The substantive half of this — that
`SPEC-075 REQ-007` and ratified `CON-201` version 1 now contradict each other
permanently — is H-1.)

---

## Disposition of every 0.4.4 finding

Verified against the 0.4.5 and companion text **and** against the code, not
against the candidate's own disposition table.

| 0.4.4 finding | 0.4.5 / companion claim | Verified disposition |
|---|---|---|
| **H-1** — v2 replaces cbcl-pairing's carrier grammar, CPace public context, key-schedule labels and application choreography; CON-001/002/004/005, NFR-007/CON-009, NFR-008/CON-010 amended in substance and dispositioned nowhere | `REQ-954` adopts SPEC-001 0.4.0, which explicitly amends all six and defines exact assets and vectors | **CLOSED for the six named contracts.** `companion040:8` names CON-001, CON-002, CON-004, CON-005, CON-009, CON-010, NFR-007, NFR-008; `:52-67` gives each a row; `:484-489` names four approvers. I verified each against the parent and the code: `CON-013`'s v2 domains `cbcl-pairing-ci/credential-v2`, `cbcl-pairing-ad/credential-v2`, `cbcl-pairing-public-context/credential-v2` are disjoint from `src/context.rs:7-9`'s three v1 domains; the label family `pairing-credential-v2 kc/key/iv/exporter/finished` is disjoint from `src/channel.rs:115-147`'s `pairing-v1 …`; `TH = SHA-512(cbor([bstr(pc), bstr(a), bstr(b)]))` matches `channel.rs:364-376` exactly; `PRK = HKDF-Extract-SHA512("", ISK)` matches `channel.rs:114`; the v2 AAD `{v:2,direction,counter,th}` is `channel.rs:341-362`'s map with the version bumped; `sid` = the raw 32-octet mailbox ID matches `context.rs:61`; `PRS` = raw 16 octets `C` is within `pairing-v1.cddl:7`'s `bstr .size (1..64)` secret handling. `CON-015` retains `begin -> all(cpace-a,cpace-b) -> all(finished-a,finished-b)` byte-for-byte against `dialects/blind-pairing-bootstrap-v1.cbcl:23-25`, and correctly adds role-projected refusal performatives because `dialects/blind-pairing-session-v1.cbcl:6-24` makes every performative `:from`/`:to`. `NFR-014`/`TEST-045` require an independent CPace group implementation. **The class recurs at `CON-007`, which that review did not name — H-2 above.** |
| **H-2** — `intentDigest` mandatory in every v2 object, defined nowhere, undefinable for the first object | `CON-943` defines one NUL-domain-separated digest of the proof-free core, requires it in the first object, and makes the authenticated core the only display source | **CLOSED.** `v045:250-252` and `companion040:288-292` state byte-identical formulas: `intentDigest = SHA-256(UTF8("selfsame credential/v2 intent" ‖ NUL) ‖ offerCoreDigest)` over raw 32 octets. `companion040:294-296` reconciles it with the parent name: "The parent CON-219 `offerDigest` is canonical base64url-no-pad of this same `offerCoreDigest`; **it is not a second digest**" — which matches `SPEC-004:3987-3990` exactly. The first-object problem is answered: field 2 of the offer is a digest of `OfferCoreV2`, a proper subset of the offer's own field-4 body, and nothing in that body contains field 2, so there is no fixed point. `TEST-1049`, `TEST-041` and `TEST-1060` cover the preimage, the mutations and the exact bytes. |
| **M-1** — same-device restore strands stale policy and refuses every v2 ceremony; `TEST-1014` asserts the contrary | `CON-944` defines a crash-recoverable installation-lifecycle purge before root deletion/restoration | **CLOSED.** `v045:95-103` (`REQ-955`) and `:295-304` (`CON-944`) make `forget`, `restore` and new-root creation execute the purge, so "a restored or new root therefore sees both policy slots absent". Verified reachable and necessary against `custody.rs:190-197` (restore refuses when an identity exists, so the path is `forget` then `restore`), `:344-346` (`forget` deletes only `ROOT_ENTRY = "root-v1"`, `store.rs:41`), and `:207-214` (fresh random salt per seal, so a different `custodyWrappingKey` and neither slot opens). `TEST-1050` splits the crash matrix. `L-5` records the one remaining ambiguity. |
| **M-2** — the installation-keyed origin ledger is mandatory, refuses on any failure, and has no repair | `CON-944` rebuilds the non-authoritative throttle empty under lock, records a bounded audit event, never ignores a valid ledger | **CLOSED.** `v045:105-112` and `:306-314` give exactly the repair the finding asked for, with all four guards: the rebuild is empty and authenticated, `OriginLedgerRebuilt` carries "reason token and time but no origin", there is "no passcode/custody call edge", and "a completely valid ledger is never reset, skipped, or replaced merely because its counter reached the limit". `TEST-1051` races two rebuilders and requires that a valid rate-limited ledger cannot be reset through the rebuild path. The declared worst case is honest: each rebuild restores five *prompted* origins and cannot accept an origin, relay or application pair, and every fetch remains behind `v044:176-179`'s explicit origin consent. |
| **M-3** — `CON-209`'s closed five-member hint gains carrier and transcript bindings; CON-209 in neither `amends:` nor the approval list; the change also moves CON-214's recogniser | `CON-945` adds CON-209 to amendment authority, pins the exact seven-member hint, replaces PROTO-003 `binding_hash` | **CLOSED.** `v045:8` and `:681-684` now carry CON-209. `v045:317-342` pins the seven members and their encodings — I checked the arithmetic: four digest fields, of which `descriptorDigest`, `offerDigest`, `carrierDigest` decode to 32 octets and `transcriptHash` to 64, consistent with `channel.rs:63` and `companion040:240`. The `binding_hash` replacement is sound: `SPEC-004:3298-3301` authenticates the hint under an AAD containing the PROTO-003 `binding_hash`, a value credential/v2 has none of, and `companion040:257-260`'s v2 AAD binds `th` — the same class of transcript authentication. `provider_hint.rs:55-61` and `:39-41` confirm the closed five-member grammar with `UnknownMember`, and `:114-116` confirms `profile_version != crate::PROFILE_VERSION` (`lib.rs:109` = 1) rejects a v2 hint today, so the v1-isolation claim is real work. `TEST-1053` mutates every member and asserts "No v2 path requests a PROTO-003 `binding_hash`". |
| **M-4** — `CON-226`'s completeness gate spans almost every amended contract and is named in no requirement, slice, test or gate step | `REQ-956` makes the amended corpus, completeness check, two-stack result, and changelog SHA-256 a release gate and implementation slice | **CLOSED.** `v045:8` and `:681-684` carry CON-226; `:114-130` (`REQ-956`) names the file, keeps it one corpus, enumerates the added groups, and restates the literal rule; `:344-347` (`CON-945`) adds the `credential_v2` group and keeps the corpus non-editable; `:564-568` (`TEST-1054`) regenerates, records the SHA-256, runs two stacks, runs the parent completeness checker, and deletes each required case in turn; `:651` puts it in the pure-core slice. Deferring the hash to "the eventual implementation changelog" is correct rather than evasive: `SPEC-004:4676-4678` binds the hash to "every Tier-1 amendment **that changes it**", and this draft changes no file. Minor: `REQ-956`'s enumeration omits CON-204 and CON-211, both on `v045:8`; the catch-all "each added or changed closed rejection token" covers them. `test-vectors/spec-004-v1.json` exists at the named path. |
| **M-5** — three outward-facing one-way changes and no deployment rollback; SPEC-007 REQ-810 missing; the live ratified profile re-minted with no ordering | `REQ-957`, `CON-947`, `CON-949` use a distinct v2 ApplicationId, additive side tables, feature gates, drain rules, and an explicit did-crdt forward-fix boundary; REQ-810 dispositioned | **PARTIALLY CLOSED — two of three halves; the third is replaced by H-1.** *Store half: closed well.* `CON-946`/`ADR-932` replace `mnesia:transform_table` plus `add_table_index` on legacy tables with new side-index tables, so no legacy arity or key moves; `REQ-957:148-151` correctly retracts the false claim that schema creation and data writes share one transaction. *did-crdt half: closed.* `CON-949:437-444` states the forward-fix-only boundary after the first v2 issuer and correctly notes CORS may be withdrawn without touching DID state — verified against `src/service/server.rs:141-158`, where only `rendezvous.rs:47-61` has CORS. *REQ-810: dispositioned* at `v045:154-159`. *Profile half: not closed.* Freezing a knowingly non-conforming version-1 object while mandating a dispatcher that rejects it is H-1. `CON-949:446-449` correctly records the Fly release number requirement, no canary, and cbcl-bus `immediate` — verified against `cbcl-bus/fly.toml:22-32` (exactly one machine, ever) and `did-crdt/fly.toml:32-44`. |
| **M-6** — `CON-941` lets a ceremony lift a withdrawal tombstone, converting an operator-only revocation lever into one the constrained party can trigger | `REQ-958` removes ceremony reinstatement; a tombstone requires the existing operator-only `reinstate/2` repair | **CLOSED, with a narrow residue — M-2 above.** `v045:161-174` and `ADR-931` take the review's first option. Verified against `cbcl-chat-webfinger-store.lfe:271-307`: `reinstate/2` really is `(reinstate account home-did)`, really does name the exact DID because "an account can hold several tombstones", really lifts the block without restoring the binding, and really has no HTTP projection (callers in tests only). `withdraw/2` is equally operator-only, so requiring operator reinstatement is symmetric and does not strand an honest person: no automated path can create the tombstone in the first place. `CON-946:373` adds "No v2 command calls `provision/3` or `reinstate/2`", and `TEST-1056` exercises old and rotated keys, then the out-of-band operator repair, then a fresh ceremony. The residue is that the adopted `CON-941` rollback still captures and restores tombstone rows. |
| **L-1** — table/module and handle/account confusion | `CON-946` names the actual table and the opaque provider account key | **CLOSED, one clause outstanding.** `v045:353-358` matches `cbcl-chat-pairaccount.lfe:31-36` (`cbcl-pairaccount-grant`, composite `#(Handle DeviceKey)`) and `cbcl-chat-webfinger-store.lfe:55,180-183,339-341` (`cbcl-webfinger-account` keyed on the opaque `account`, a 1..4096-octet binary, "the private provider-side identifier"). The finding's last clause — say that the webfinger migration is vacuous today because `provision/3` has no production caller — is not stated; it is now load-bearing for M-3. |
| **L-2** — repeated offer prepare undefined | `CON-948` makes byte-identical retry idempotent and a distinct retry terminal | **CLOSED.** `v045:195-198` and `:428-433`: compare-and-set on an empty held core, exact retry returns the held bytes including `issuedAt`/`expiresAt`, any changed transcript/session/ceremony/argument records terminal `OfferPrepareConflict`, "cannot mint new timestamps or evidence", and "cannot restart the 120-second window". `TEST-1057` asserts no second signature and no refreshed expiry. |
| **L-3** — migration attributed to the wrong contract | `CON-946` attributes migration to REQ-957 and CON-946 only | **CLOSED in the new text.** `v045:363-366`. The superseded `v044:1064` sentence survives inline — L-7. |
| **L-4** — reset revision and equal-slot edge undefined | `CON-944` resets by verified deletion, defines the one-slot crash state, chooses slot A on a valid equal-revision tie | **PARTIALLY CLOSED.** The equal-revision half is closed correctly and preserves the torn-write argument. The reset half replaces the "next revision" problem with a false crash claim and an unauthorable test row — **M-1 above**. |
| **L-5** — idle budgets exceed 600 seconds; no achievable envelope asserted | `CON-948` replaces composable idle budgets with cumulative checkpoints ending at 590 s | **CLOSED.** `v045:410-420`: 180 / 210 / 240 / 330 / 375 / 435 / 510 / 555 / 590. I recomputed the deltas — 180, 30, 30, 90, 45, 60, 75, 45, 35 — which sum to exactly 590, so the schedule is internally consistent and every checkpoint is strictly inside the relay's 600-second maximum. `mailbox.rs:7-11` confirms `DEFAULT_TTL_SECONDS = MAX_TTL_SECONDS = 600`, so a `ttlSeconds: 600` allocation really does yield the window the table assumes, and `companion030:255` bounds `ttl-seconds` to 60..600. `TEST-1058` fixes an achievable witness at 170/200/230/320/365/425/500/545/585 and crosses each checkpoint by one tick. |
| **L-6** — companion `mailbox-store-v2` CDDL references undefined rules | `CON-016` defines `store-status` and `store-sequence` completely | **CLOSED, and exact.** `companion040:359-379` against `src/storage.rs:196-241`: `store-status = [0] / [1] / [2, 0..3]` matches `encode`'s three status arms and `close_number` (`:362-369`: Closed 0, Crowded 1, Expired 2, Conflict 3) reason-for-reason; `store-sequence = [0..1, 0..15, bstr .size 32, 1..69632, null / bstr .size (1..69632)]` matches the five-element sequence encoding, with the owner number, `MAX_FRAMES_PER_MEMBERSHIP = 16` (`mailbox.rs:13`) giving `0..15`, and `NFR-004`'s 69,632 body bound. The tightening `[* bstr .size 32]` to `[1*2 bstr .size 32]` is correct and I verified it is not merely conservative: `mailbox.rs:389-398` rejects a snapshot with zero or more than two membership hashes and requires exactly 1 when waiting and exactly 2 when paired, and `:571-577` shows the third distinct claim terminates as `Crowded` without adding a hash. |
| **L-7** — `NFR-906`'s component list omits the three-byte `logicalLength` pair | `NFR-907` corrects the enumeration while retaining every reviewed total | **PARTIALLY CLOSED.** The three-byte pair and the 64,559 total are added and correct. Two defects remain: a five-byte byte-string head that never occurs (**L-1**), and the control class, whose "every control type … encodes to 4,144" is still asserted as an equality (**L-2**). |
| **L-8** — "allocation time" ambiguous between two events | `CON-948` anchors `t0` to the relay's committed `allocated-v2` and binds its `expiresAt` into the carrier | **CLOSED.** `v045:180-186` fixes relay-first ordering, makes hub failure close the mailbox and emit no carrier, and states "Hub allocation is not a second definition of `t0`". `TEST-1059` requires exact equality among `allocated-v2.expiresAt`, carrier expiry, hub held expiry and the signed offer. The claimant can compute `t0` because the expiry is in the carrier. Residue: `CON-934`'s field list was not restated (**L-3**) and one rule is misplaced (**L-4**). |
| **L-9** — parent REQ-903/904 and REQ-810 absent | `REQ-954` records REQ-903/904; `REQ-957` records REQ-810 | **CLOSED, and the dispositions are honest.** `v045:88-93` against `specs/SPEC-008-production-pairing-claimant.md:155-174`: REQ-903's live approve/decline is retained and the amendment says plainly that it "remains under the **amended** SPEC-007 REQ-804 ordering" rather than pretending REQ-903's "consent ordering unchanged" survives intact; REQ-904 is about carriers, and the PAIR1 field is not a carrier, so "Its scope does not include the separate PAIR1 field" is accurate rather than a dodge. `v045:154-159` gives REQ-810 its row. |
| **L-10** — two inconsistent domain separations | `CON-943` uses a generic canonical-CBOR account-principal preimage and a NUL-terminated presence-code domain | **CLOSED.** `v045:266-280`. The account principal moves to `SHA-256(DETERMINISTIC-CBOR(["selfsame-application-account-principal/v1", applicationId, applicationAccountId]))`, which drops the deployment name and removes the concatenation question entirely because CBOR length-prefixes both operands. The PAIR1 checksum gains its NUL terminator. All five v2 preimages are now uniform: `intentDigest`, the device proof (`v044:362-365`), the claim commitment (`v044:223-226`) and the presence code are NUL-terminated with fixed-length tails, and the account principal is CBOR. |

**0.4.3, 0.4.2 and 0.4.1 findings** remain closed. I re-checked the two the 0.4.4
review carried as substantive: the v2 nine-name grammar at `v044:641-651` is still
byte-for-byte `profile.rs:78-89` in the same order, and the BIP-39/PAIR1
separation (`v044:245-250`, plus `CON-934`'s bit arithmetic 128+128+16+3 = 275 =
55 x 5) is unchanged and complete.

---

## Inherited-hard-stop audit

| Stop | Owner | Hard stop? | 0.4.5 position | Verified |
|---|---|---|---|---|
| SPEC-008 REQ-902 | SPEC-008 0.2.2 | yes | amended (0.4.4), fixture prohibition retained | OK. `TEST-1040` unchanged and still release-blocking. |
| SPEC-008 REQ-905 | SPEC-008 0.2.2 | yes | amended; carrier non-navigable, no `C`/`T` | **OK now.** The carrier grammar it points at is `CON-001`, which `companion040:8,61` amends explicitly — the 0.4.4 caveat is discharged. |
| SPEC-008 REQ-906 / REQ-907 / REQ-908 / CON-903 | SPEC-008 0.2.2 | yes / yes / no / — | replaced / retained / retained / retired | OK. `REQ-953` and `v045:672-674` keep production allocation closed. |
| SPEC-008 REQ-903 / REQ-904 | SPEC-008 0.2.2 | no | recorded at `v045:88-93` | **OK.** L-9 closed; dispositions verified against `SPEC-008:155-174`. |
| SPEC-007 REQ-803 | SPEC-007 | yes | retained | OK. CON-206 still gates acceptance. |
| SPEC-007 REQ-804 | SPEC-007 | yes | amended for v2 | **OK now.** Its operative term "the exact recognised intent digest" is defined by `CON-943` and `companion040 CON-014`. |
| SPEC-007 REQ-805 | SPEC-007 | yes | narrowly amended | OK. `T` never enters CPace (`companion040:94`), is not persisted, is zeroized. |
| SPEC-007 REQ-806 / REQ-807 | SPEC-007 | yes | retained — no negotiation, no cross-version recognition | OK. `companion040:81-85` and `CON-016:388-391` mirror both on the endpoint side; `TEST-038` and `TEST-1017` test both directions. |
| SPEC-007 REQ-809 / NFR-801 / production gate | SPEC-007 | yes | retained without exception | OK. `v045:676-687`. |
| SPEC-007 REQ-811 | SPEC-007 | yes | retained | OK. |
| SPEC-007 REQ-810 | SPEC-007 | no | amended for this additive release | **OK as an amendment**, but its profile half is contradicted — H-1. |
| SPEC-007 REQ-812 / 813 / NFR-802 / CON-801 / CON-802 | SPEC-007 | no | amended / narrowly amended | OK. |
| SPEC-007 CON-806 | SPEC-007 | no | amended by 0.4.4 CON-933 | **NOT OK.** Step 1 recognises "under CON-201"; after this release no CON-201-conforming profile exists at chat.anuna.io — H-1. |
| SPEC-002 REQ-103 | SPEC-002 | — | conformed to | OK. Hex normative, LifeHash an aid. |
| cbcl-pairing REQ-002 | SPEC-001 | yes | retained | OK. The relay compares and erases an opaque bearer; it derives no key, verifies no Finished, interprets no body. |
| cbcl-pairing REQ-003 / REQ-005 | SPEC-001 | no / **yes** | amended by adopted 0.3.0 only | OK. REQ-005 is properly routed: a new SPEC-001 version, four named reviewers, Tier-1 review. |
| cbcl-pairing REQ-008 / 009 / 013 / 014 / 015 | SPEC-001 | yes | retained; REQ-009 "made two-stage" | OK. `companion040:58-60`. Both Finished values verify before any offer object can open; neither decision can name another digest. |
| cbcl-pairing NFR-009 / 010 / 011 / 012 | SPEC-001 | yes | retained | OK via adopted `NFR-013` and `TEST-037`. |
| cbcl-pairing NFR-001 – NFR-006 | SPEC-001 | no | retained | OK. NFR-001 is scoped to the agent carrier (`SPEC-001:476-478`) and is untouched. NFR-002's 16-frame per-membership cap holds with a wide margin — see the bounds audit. |
| cbcl-pairing CON-001 / 002 / 004 / 005 | SPEC-001 | no | amended by `companion040` | **OK.** H-1 of the prior review is discharged for these four. |
| cbcl-pairing CON-003 | SPEC-001 | no | amended by adopted 0.3.0 | OK. |
| cbcl-pairing NFR-007/CON-009, NFR-008/CON-010 | SPEC-001 | no | amended by `companion040 NFR-014/CON-016` and `CON-015` | **OK.** |
| **cbcl-pairing CON-006 / CON-007** | SPEC-001 | no | **absent from both amendments** | **H-2.** CON-006 is untouched in fact but has no row; CON-007 is reversed in substance. |
| cbcl-pairing CON-008 | SPEC-001 | no | absent | OK in substance: the limiter dimension `{operation, PeerKey}` is unchanged and adopted `NFR-013` bounds v2 work to the addressed command. Worth a row. |
| cbcl-pairing production gates | SPEC-001 | yes | retained in full | OK. `companion040:503-506`. |
| SPEC-004 CON-201 / 204 / 206 / 209 / 211 / 214 / 219 / 220 / 221 / 226 | SPEC-004 | Tier-1 | amended; owner approval required | OK as declared for nine of ten. **CON-201 version 1 is expressly *not* amended (`v044:637`), which is what makes H-1 unresolvable inside the current text.** |
| SPEC-004 REQ-215 | SPEC-004 | — | conformed to | OK. |
| PROTO-004 CON-502 / CON-503 | PROTO-004 | — | not named | **OK on inspection.** The v2 offer travels in `companion040 CON-014`'s envelope, not a PROTO-004 sealed record, and `CON-219:3946-3952` is amended (it is on `v045:8`). `CON-503:699-712`'s profile digest rule — `BASE64URL-NOPAD(SHA-256(RFC8785(committed_object)))` with the excluded members enumerated and never inputs to the digest — is *conformed to* by `CON-943`, not amended. Record the non-use of PROTO-004 for v2 in one sentence. |

---

## Amendment-authority audit

- **cbcl-pairing.** `SPEC-001:1488-1495` permits amendment only "through a
  reviewed, merged revision of this specification with updated assets, tests,
  evidence, and compatibility disposition", and expressly not by "implementation
  behaviour, test fixtures, or dependency updates". `companion030` and
  `companion040` are that vehicle for eleven contracts. `CON-007` is the twelfth
  and is missing — **H-2**.
- **Selfsame SPEC-004.** `v045:8` now names CON-201, 204, 206, 209, 211, 214,
  219, 220, 221, 226; `v045:681-684` puts the same list to its owner. Two prior
  omissions (CON-209, CON-226) are closed. `CON-201` *version 1* is deliberately
  excluded, which H-1 shows cannot hold.
- **Selfsame SPEC-007.** REQ-804, 805, 810, 812, 813, NFR-802, CON-801, CON-802,
  CON-806 are named. `CON-806` is named as amended but is left unsatisfiable —
  H-1.
- **cbcl-bus SPEC-075 REQ-007.** Named as amended; nothing in 0.4.5 changes it —
  **L-8**.
- **did-crdt HTTP service contract.** Named. `REQ-961`/`CON-950` change
  `POST /dids` semantics, add CORS, add fail-closed startup, and change
  `BlobStore::load_all`'s documented skip-and-continue behaviour
  (`src/sync/store.rs:186-196`). That last one is a library contract change and
  is not called out; it is implementable behind a strict mode.
- **Owner substitution.** `v045:658-662` and `companion040:486-489` both state
  that owner direction is necessary but cannot substitute for PASS. Correct, and
  respected by this review.

---

## State and effect-order audit

I re-traced the order against the APIs that must express it, taking 0.4.5's
replacements into account.

- **Pre-network.** Unchanged and sound: carrier recognition with no I/O, then the
  explicit origin prompt, then the burned contact capability, then the ledger.
  `REQ-955` adds the rebuild without introducing a custody edge — verified
  against `store.rs:170-197`, where `get`/`set`/`delete` are plain keychain
  operations with no presence requirement.
- **Policy key lifetime.** `v045:203-207` (`REQ-960`) closes the prior review's
  first non-finding edge exactly: one zeroizing closure, alive only while the
  new-pair prompt is visible, capped at 120 monotonic seconds, ended by socket
  open, cancel, timeout, backgrounding or any error. Expressible as a
  `Custody::use_policy_key` alongside `custody.rs:312-331`'s `use_hierarchy_root`.
  The cap composes with `CON-948`'s 180-second first checkpoint, though that
  leaves the tightest human budget in the schedule — see the bounds audit.
- **Offer authorisation.** Unchanged from 0.4.4 and still matches `CON-214`'s own
  step order and `authorise.rs:131-215`.
- **Preliminary consent to preview.** Unchanged. `hierarchy.rs:267-283` then
  `:178-182` `home_did` then `selfsame_core::identity::derive_did`. No signature,
  no randomness, no store write, no network.
- **Preview disclosure.** Still stated plainly (`v044:301-304`) and still
  asserted by `TEST-1024`.
- **CON-221 applicability.** Still decided by the application from
  `selfsamepairauthority`, `Unknown` fails closed, `BoundDifferentDid` refuses.
  **M-3** shows that last branch now fires for every v1-bound account.
- **Final consent to commit.** `CON-938`'s six-row effect table is unchanged and
  remains the unique gate. `TEST-1025` still requires a total call order after
  the final-decision hash.
- **Fourth custody operation.** `v045:209-214` closes the prior review's second
  non-finding edge: post-payload CON-210 compensation may require a fourth,
  separately explained passcode/presence operation; refusal leaves the ledger
  `pending`, reports `not linked`, and "never … claims automatic revocation".
  `TEST-1063` requires exactly three successful-path operations and a fourth
  labelled one. Verified necessary: a CON-210 revocation needs a signature under
  the hierarchy root, and `custody.rs:312-322` requires passcode plus presence
  for every `use_hierarchy_root`.
- **Acyclicity.** I rebuilt the whole graph and found no cycle:
  `carrier -> carrierDigest`; `profile -> profileDigest` (each side locally);
  `{carrierDigest, profileDigest, carrier fields} -> CI/AD/publicContext -> TH`;
  `OfferCoreV2` contains `carrierDigest`, `transcriptHash`, `profileDigest`,
  `descriptorDigest`, `applicationId`, `accountPrincipalDigest` and the rest, and
  yields `offerCoreDigest`, from which `offerDigest` and `intentDigest` derive;
  `devicePossessionProof = Ed25519(SHA-256(domain, offerCoreDigest,
  transcriptHash))` and is **not** a member of the core; hub CON-214 evidence
  signs the core fields **plus** the proof; the seven-member hint carries
  `offerDigest`, `carrierDigest`, `transcriptHash` and `descriptorDigest` and is
  excluded from the core by `CON-219:3987-3994`; each successor object's body
  carries `predecessorDigest = SHA-256(prior canonical object)`. Field 2 is an
  envelope member and the offer's field-4 body contains no field 2, so the first
  object commits to a proper subset of itself with no fixed point.
- **One authoritative applicationId.** `companion040:236-238` requires the
  recognised profile's `applicationId` to equal `application-context`, so the two
  independently computed profile digests must agree for Finished to agree;
  `REQ-945` then requires exact equality across invitation, profile, descriptor,
  offer, transcript and display. No caller or peer string is promoted before
  verification: `CON-943:258-262` builds `AuthenticatedIntent` only from typed
  fields in the verified core and evidence and forbids consuming "a
  peer-provided application ID or separate intent record",
  `companion040:116-118` forbids the shared reducer constructing display text
  from a generic peer claim, and `REQ-945`'s closed provenance enum has no
  boolean arm.

---

## Authority and provenance audit

- The hub signs only canonical state it constructed (`v044:964-972`); the
  prototype `/selfsame/enrolment/sign` route — self-described at
  `cbcl-chat-selfsame-application-http.lfe:56-78` as "PROTOTYPE POSTURE … signs a
  caller-supplied statement" — is explicitly unreachable from credential/v2, and
  `TEST-1027` proves it.
- The account label remains an application-session assertion, displayed as
  "Application account asserted by `<origin>`", never as an authenticated person.
- Relay operatorship and same-origin control remain declared residuals
  (`REQ-952`), and `v045:44-45` restates both rather than upgrading them.
- The withdrawal lever stays with the operator (`REQ-958`), and the one path by
  which the ceremony can still write that table is M-2.
- The v2 provider hint is a browser assertion authenticated by the channel and
  cross-checked against the hub-signed statement and the locally recognised
  carrier, profile and descriptor — the same trust level `CON-209` gave it under
  PROTO-004, not a higher one, and `CON-945:336-341` says so.

---

## Implementability audit per repository

**`cbcl-pairing` (`8e55160`).** Everything `companion040` requires is expressible
as additive code beside the v1 paths: a second context constructor beside
`context.rs:25`'s `derive`, a second label family beside `channel.rs:115-147`, a
version field in `sealed_aad` (`:341-362`), two new closed command arms beside
`wire.rs`'s union, an admission branch after `relay.rs:424`'s `resolve` and before
`transition` (and `relay.rs:428` already returns `Error(404)` for an unresolvable
locator, so the responses really are observationally equal), and an eighth store
element in `storage.rs:225-241`. The client-supplied `M` is admissible because
`relay.rs:360-362` already refuses a duplicate `mailbox_id` with `Error(503)`.
`mailbox.rs:290-302` clamps TTL to 60..600 with a 600 default, so `CON-948`'s
600-second premise holds. No v1 byte moves.

**`selfsame` (code tree `c6af6df`).** The pure-core work is the dispatcher, the
v2 offer/evidence/authoriser extension, the seven-member hint beside
`provider_hint.rs`'s five, the closed display provenance, and the corpus. The
wallet work is `Custody::use_policy_key`, the two-slot policy, the lifecycle
marker, the origin ledger, split preview/commit, and publication —
`crates/selfsame-app-identity-net/` exports `submit_revocation` and
`confirm_revocation` and no DID-creation operation, so `REQ-950` is genuinely new
code, correctly slice-listed. `store.rs`'s `get`/`set`/`delete` give exactly the
primitives `CON-944` needs (delete-then-`get`-absent is a real absence check), and
`store.rs:200-203`'s desktop `require_presence` returning `Ok(())` is why the
mobile/desktop split had to be stated. The one thing the dispatcher makes
impossible is recognising the live production profile — H-1.

**`cbcl-bus` (`fdbe414`).** `CON-946`'s side tables are ordinary
`mnesia:create_table` calls in the idempotent `ensure-tables` style already used
at `cbcl-chat-webfinger-store.lfe:67-95` and `cbcl-chat-pairaccount.lfe:29-40`;
they touch no legacy arity or key, so the "older binary ignores extra tables"
claim is sound and `TEST-1055` can assert it. Multi-table single-transaction
updates are native. The v2 profile route is one more tuple beside
`cbcl-chat-app.lfe:223`, and `discovery.rs:262-268` confirms the wallet will
dereference the identifier's own path, so two profiles coexist. `kid` needs only
to be on the applicationId **origin** (`enrollment.rs:468-477`), so the existing
enrolment key can serve both profiles. M-2 and M-3 are the two places where the
hub design and the reviewed store disagree.

**`did-crdt` (`1f409a4`).** `create_did` (`handlers.rs:77-99`) unconditionally
inserts over any existing document, `submit_delta` (`handlers.rs:326-345`) merges
under a lock it then drops before persisting, and `persist`
(`handlers.rs:440-452`) logs and counts a save failure while the handler has
already returned success — so `REQ-961`'s premise is exactly right and the fix is
real work. `AppState` already carries the optional `FsBlobStore`
(`server.rs:99-104`), and `fly.toml:22-35` gives the production node
`STORAGE_PATH=/data` on a mounted volume with `auto_stop_machines = "off"`, so
durable mode is the deployed configuration. The per-DID keyed lock and the
clone / save / read-verify / swap order are implementable; **M-4** is that they
are specified over only one of the two writers.

---

## Bounds and work audit

Recomputed independently from `schemas/pairing-v1.cddl:125-136`,
`src/channel.rs:20`, `src/mailbox.rs:13`, `src/bin/cbcl-pairing-relay-ws.rs:32-37`
and `companion040 CON-014`.

| Quantity | Recomputed | Candidate | Match |
|---|---:|---:|---|
| application object, `logicalLength` 1..23 | 1+2+2+35+2+(1+3+64,512) = **64,558** | 64,558 | yes |
| application object, 24..255 | +1 = **64,559** | 64,559 (`NFR-907`) | yes |
| application object, 256..62,000 | +2 = **64,560** | 64,560 | yes |
| control object maximum | 1+2+2+35+4+(1+3+4,096) = **4,144** | 4,144 | yes as a maximum; **L-2** as an equality |
| `sealed-plaintext` overhead, 2,048-octet control | 1+8+2,051+5+3 = **2,068** | 2,068 | yes |
| plaintext maxima | **66,628** / **6,212** | 66,628 / 6,212 | yes |
| ciphertext maxima (+16 GCM) | **66,644** / **6,228** | 66,644 / 6,228 | yes |
| `sealed-frame` non-body overhead | 1+3+12+11+9+11 = **47**; +5 head large, +3 control = **52** / **50** | 52 / 50 | yes |
| relay body maxima | **66,696** / **6,278** | 66,696 / 6,278 | yes (both under `NFR-004`'s 69,632) |
| worst-case queue per ceremony | 2x67,000 + 5x7,000 + 4x8,192 = **201,768** | 201,768 | yes |
| simultaneous worst-case ceremonies | floor(536,870,912 / 201,768) = **2,660** | 2,660 | yes (`DEFAULT_QUEUE_CAP = 512 MiB`) |
| v2 application frames | offer + 5 controls + payload = **2 large, 5 control** | 2 / 5 | yes, against `companion040 CON-015`'s projection |
| bootstrap frames | cpace-a, cpace-b, finished-a, finished-b = **4** (`begin` is a state, not a frame — `dialects/blind-pairing-bootstrap-v1.cbcl:23-25`) | 4 | yes |
| frames per membership | allocator **5**, claimant **6** | "No membership reaches the sixteen-frame ceiling" | yes, against `mailbox.rs:13` |
| v2 mailbox metadata | 32 octets x 10,000 = **320,000** | 320,000 | yes |
| cumulative schedule | deltas 180+30+30+90+45+60+75+45+35 = **590** | 590, all under 600 | yes |
| relay TTL | `MIN 60 / MAX 600 / DEFAULT 600` (`mailbox.rs:7-11`) | "original 600-second TTL" | yes |

**Work.** Each `claim-v2` is one bounded parse, one addressed lookup, one
SHA-256, one constant-time comparison, under the existing peer-address-keyed
per-operation limiter — no scan, no queue write, no TTL extension, no limiter
reset. NFR-009 through NFR-012 are preserved by adopted `NFR-013`.

**One feasibility observation, not a finding.** The first cumulative checkpoint
is 180 s from `t0`, and `t0` precedes the carrier's appearance on screen
(`REQ-959`: relay allocate, then hub allocate, then display). Inside it a person
must open the wallet, scan, read and accept an unverified-origin prompt, wait for
one profile fetch, complete a passcode and presence operation, read and accept a
new-pair prompt whose own cap is 120 s (`REQ-960`), and transcribe 55 Crockford
characters. `TEST-1058` fixes 170 s as the achievable witness. That is arithmetic
that closes, but it is the tightest human budget in the schedule and the one the
E2E gate should measure first; the 90 s preliminary and 75 s final decision
budgets (down from 120 s each in 0.4.4) deserve the same scrutiny.

---

## Tests and gates audit

- `TEST-1048` through `TEST-1064` cover every new 0.4.5 obligation, and
  `companion040 TEST-038` through `TEST-047` cover the endpoint.
  Prohibited-action and mutation rows are retained rather than replaced by
  positive tests.
- `TEST-1054` makes CON-226 completeness a hard gate with case deletion as the
  negative — the strongest form of that row.
- `TEST-1049`, `TEST-1053`, `TEST-1057`, `TEST-1059` and `TEST-1060` are each
  authorable against the definitions they test.
- **`TEST-1052` is not authorable as written** — M-1.
- **`TEST-1030`'s control-class equality is false** and `TEST-1060` re-blesses it
  — L-2.
- `TEST-1050`'s convergence set omits a state `CON-944` permits — L-5.
- Coverage gaps that follow from the findings: no row exercises the dispatcher
  against the exact live legacy octets (H-1); no row exercises a concurrent
  operator `reinstate/2` during compensation (M-2); no row exercises a v1-bound
  account entering a v2 ceremony (M-3); no row exercises a concurrent gossip
  merge during an HTTP write (M-4).
- Gate sequencing (`v045:656-674`) is correct in shape: PASS first, then the SPL
  theory, then failing schemas and vectors, then pure contracts, then effectful
  shells, then the gate suite, with production allocation held and separate owner
  confirmation before any outward deploy. `TEST-1028` and `companion040 TEST-045`
  both stay red until an independent CPace group implementation exists, which is
  correctly declared open.

---

## Deployment and rollback audit

| Change | Direction | 0.4.5 position | Verified |
|---|---|---|---|
| cbcl-pairing relay `allocate-v2`/`claim-v2`/store-v2 | additive | adopted 0.3.0 sequence: disable allocation, close or expire, wait 600 s, verify no store-v2 record, install v1-only | **Sound.** `storage.rs:255-256` refuses any domain other than `cbcl-pairing-mailbox-store/v1`, so a v1-only binary fails startup rather than silently dropping a v2 record. |
| hub side tables | additive | feature-gate rollback; tables retained; older binary ignores them | **Sound**, given `CON-946`'s no-transform rule and the existing idempotent `ensure-table` pattern. |
| hub v2 profile route | additive | withdraw only after allocation is off and live carriers expire | Sound. |
| **legacy v1 profile plus wallet recogniser split** | **client-side one-way** | "The legacy profile never moved, so no profile reversal is needed" | **NOT sound — H-1.** The server did not move; the recogniser did, and reverting it is a wallet release with no clause, slice or test. |
| did-crdt idempotent create and CORS | additive, then one-way | forward-fix only after the first v2 issuer; CORS withdrawable after allocation is off | **Sound**, and correctly reasoned: `server.rs:141-158` shows CORS is genuinely absent today and additive, and disabling it changes no DID state. |
| did-crdt durable-write and fail-closed startup | behaviour change | `REQ-961` / `CON-950` | Implementable; scope defect at M-4. Note it also changes `BlobStore::load_all`'s documented skip-and-continue contract (`src/sync/store.rs:186-196`). |
| Fly release recording, no canary, `immediate` | — | `CON-949:446-449` | **Verified true** against `cbcl-bus/fly.toml:22-32` (exactly one machine, ever) and `did-crdt/fly.toml:32-44` (volume-bound single machine). |

---

## Persistence audit

- **Wallet.** `CON-944`'s purge and slot rules are expressible over
  `store.rs:170-197` and are crash-safe in the direction that matters: a new root
  is written only after both slots and the marker are verified absent, and
  startup will not create a root while `purging` is present. L-5 records the
  ambiguity; M-1 records the false reset crash claim.
- **Relay.** Store-v2 is a total grammar (`CON-016`), matches the real encoder
  field-for-field, and fails startup rather than degrading to v1 (`TEST-046`).
- **Hub.** Additive tables plus one ordinary data transaction per authority
  change. The conflict test for revisionless legacy rows is the gap — M-2.
- **Browser.** Strict-durability IndexedDB install with byte-identical restore on
  failure, separately recorded as a compensatable stage rather than folded into a
  false cross-system atomic claim. Unchanged from 0.4.4 and sound.
- **Resolver.** `REQ-961`/`CON-950` close the best-effort-acknowledgement hazard
  and the corrupt-load hazard for the HTTP paths, without inventing an
  unimplementable atomicity claim: the ordering they specify — clone, save,
  read-verify, swap — is exactly what `BlobStore::save`'s import-then-`set_tag`
  structure (`src/sync/store.rs:142-161`) supports, and the two crash outcomes
  they claim follow from the tag being the commit point. They do not close it for
  the second writer — M-4.

---

## Residual risks

**Accurately declared, correctly bounded, and accepted as residuals:**

- Deliberate forwarding of both the carrier and the presence code
  (`v044:263-265`). Bounded by first-enrolment TOFU behind the CON-221 hex
  comparison.
- Relay operatorship is not proved (`REQ-952`, `ADR-926`), and same-origin
  control cannot recover a previously honest origin (`v044:548-560`). Both are
  restated at `v045:44-45` without upgrade.
- No rollback-resistant hardware counter for the policy record. The unsafe
  direction stays bounded: adding a pair needs the custody-derived key.
- The relay observes `T` at TLS termination on the successful claim.
- A lost `claimed-v2` strands the mailbox until expiry — strictly better than v1,
  where the same retry crowds.
- Repeated corruption of the origin-ledger key can repeatedly restore five unseen
  origins. Every one of them is still individually prompted and still cannot
  accept an origin, relay or application pair, and the attacker already holds
  keychain write access.
- Human cryptography review, independent CPace group math, the operator
  availability exception or a second operator, and privacy/security review all
  remain open and blocking (`v045:676-687`, `companion040:503-506`).

**Undeclared residuals that should be written down (each below the finding bar
on its own):**

- `CON-207` in the reverse direction is a self-challenge: the browser both holds
  and verifies the device key. It still binds a grant naming a *different* key,
  which is what `REQ-949` step 3 needs, but it is not third-party proof of
  possession. Carried unstated from 0.4.4.
- `CON-206` step 9 is satisfied in v2 by a hub-signed **pending** record, so the
  alias is not publicly resolvable at acceptance time — a real amendment of
  `CON-204`'s "provisions … and only then runs CON-206" ordering. It is sound
  because the verifier and the account authority are the same trust domain and
  finalization gates the receipt, and `CON-206` step 9's own text admits "a fresh
  authenticated cache of that exact binding". Say so. Carried unstated from 0.4.4.
- `CON-938`'s `commit_issuance` still lists "publishes through CON-940" inside
  the same bulleted sequence as the derivation, which reads as if an async
  network call sits inside a synchronous custody closure. It does not have to —
  `issuer.rs:134-190` signs all three deltas locally — but the wording invites
  the wrong implementation.
- `companion040 CON-013`'s public-context elements 6 and 7 are unlabelled; they
  are unambiguous only by parallel with the `credential-v2-ci` block's comments.
  Label them.
- `REQ-961`'s durable-storage requirement is checked at profile-generation time;
  nothing prevents a later redeploy of a declared resolver without
  `STORAGE_PATH`. One operational clause would close it.

---

## Method

Fresh context, no access to the authoring conversation, defect-finding mandate,
no subagents.

1. Confirmed all six revisions and both spec-only deltas; recorded did-crdt's
   pre-existing docs dirt without touching it.
2. Read the 0.4.5 amendment (699 lines), the adopted 0.4.4 design (1,581 lines),
   the complete 0.4.4 review (1,331 lines), `companion040` (514 lines) and
   `companion030`'s requirements and contracts, end to end.
3. Audited each of the eighteen 0.4.4 findings against the 0.4.5 text **and**
   against the code, and independently searched for defects the 0.4.4 review did
   not raise — which is where both HIGHs came from.
4. Re-traced every load-bearing code claim rather than accepting a disposition:
   cbcl-pairing `schemas/pairing-v1.cddl`, `context.rs`, `channel.rs`,
   `relay.rs`, `mailbox.rs`, `storage.rs`, `dialects/*.cbcl`,
   `bin/cbcl-pairing-relay-ws.rs`; selfsame `uri.rs`, `discovery.rs`,
   `profile.rs`, `provider_hint.rs`, `hierarchy.rs`, `enrollment.rs`,
   `src-tauri/src/store.rs`, `src-tauri/src/custody.rs`; cbcl-bus
   `cbcl-chat-webfinger-store.lfe`, `cbcl-chat-pairaccount.lfe`,
   `cbcl-chat-selfsame-application-gate.lfe`, `cbcl-chat-app.lfe`,
   `specs/SPEC-075-cbcl-pairing-relay-cutover.md`, `fly.toml`; did-crdt
   `service/handlers.rs`, `service/server.rs`, `sync/store.rs`, `sync/live.rs`,
   `bin/did-crdt-service.rs`, `fly.toml`.
5. Read the governing parents directly for every normative claim: SPEC-004
   CON-201, CON-206, CON-209, CON-214, CON-219, CON-226; SPEC-007 CON-806,
   REQ-810; SPEC-008 REQ-903/904; SPEC-002 REQ-103; PROTO-003 `binding_hash`;
   PROTO-004 CON-502/CON-503; SPEC-001 REQ-001 through REQ-015, NFR-001 through
   NFR-012, CON-001 through CON-010, and the Amendment Channels section.
6. Recomputed every numeric and cryptographic claim from the exact CDDL and
   constants rather than checking arithmetic against the candidate's own
   statement of it — every figure in the bounds table above was derived before
   being compared.
7. Applied the stated PASS bar without adjustment. Both HIGHs and all four
   MEDIUMs meet it: each makes a normative claim false, unsatisfiable, or
   ambiguous at an authority or effect boundary. No severity was lowered to let
   the design pass, and no finding was raised that I could not trace to an exact
   clause and an exact source line.
