---
id: SPEC-008-review-0.4.17-claude-2026-08-23
title: SPEC-008 0.4.17 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.10 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.17-complete-consent-and-executable-ledgers]]"
prior-review: "[[spec-008-0.4.16-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.17 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.10 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session began. I
recorded the gate result first, and printed no secret value.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; fi
ABSENT
$ env | grep -c ANTHROPIC_API_KEY
0
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "orgName": "hugo.oconnor@gmail.com's Organization",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. `ANTHROPIC_AUTH_TOKEN` is absent
as well. The reported `authMethod` is `claude.ai` and the reported
`subscriptionType` is `max`. This session called no Anthropic API and no
external web service.

The reviewing model is Claude Opus 5 in a fresh context. Every input came from
the four local repositories, the local cbcl-rs checkout, and the local OTP
install. The generating sessions authored the three candidates. This session
read the candidates and the source, and never the generator reasoning.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `50cdc0b6d9486be4f13418b79ce4dc4890061975` | candidate 0.4.17-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `b02a3f5b6fc618ab22abc034ad6db542196c8a41` | candidate 0.4.8-draft |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | declared implementation baseline |
| cbcl-bus | `4e435adb302377842e393f72b51f760137abaa9c` | candidate 0.16.10-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The sibling working trees carry user-owned uncommitted
paths. I read those repositories only through `git show COMMIT:PATH`, and I
changed nothing in them. The local OTP install is `28` with `mnesia-4.25.3`.

I read the complete effective amendment chain, the full 0.4.16 review, the
coordinated cbcl-bus and cbcl-pairing candidates,
[[PROTO-002-selfsame-rendezvous-v1]], and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]].

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | N2-H-1 |
| MEDIUM | 3 | N2-M-1, N2-M-2, N2-M-3 |
| LOW | 1 | N2-L-1 |

Verdict: **REJECT**.

This candidate set closes the 0.4.16 rejection well on substance. All ten
0.4.16 findings close, and I re-derived each closure against real code rather
than against predecessor prose. The double negative is gone. The version trace
is now complete and internally consistent. The lock legend binds every symbol
to an exact matrix row. Both parent pointers resolve.

The rejection rests on a defect the correction itself created. Three Selfsame
ledger clauses are now effective at once, and they contradict each other on
four separate ledgers. Two obligations disappeared inside a collective test
replacement. One live lock upgrade sits inside a graph declared closed.

---

## Findings

### N2-H-1 (HIGH, release-blocking) — three Selfsame ledger clauses are effective at once and contradict each other on four ledgers

**Repository owner: selfsame (CON-976, and the unreplaced CON-974 and CON-975).**

`CON-976` at
`specs/trajectory/SPEC-008/spec-008-0.4.17-complete-consent-and-executable-ledgers.md:256-270`
names exactly four replacements. Condensed from its numbered list:

```text
1. REQ-996  replaces REQ-994
2. NFR-919  replaces NFR-918
3. ADR-954  replaces ADR-953
4. TEST-1134..TEST-1138 collectively replace TEST-1129..TEST-1133
```

`CON-975` is not among them. The frontmatter reads
`adopts: "All effective 0.4.4 through 0.4.16 clauses except the exact replacements in CON-976"`.
`CON-975` is an effective 0.4.16 clause, so this amendment adopts it.

The chain knows how to retire a ledger clause. 0.4.14 `CON-973` item 4 at
`spec-008-0.4.14-deployed-admission-and-local-unlink.md:206-208` reads:

> This contract replaces
> [[spec-008-0.4.13-reachable-bounds-and-durable-authority#CON-972]] only for
> coordination pointers and the replacements above.

0.4.15 `CON-974` dropped that item. 0.4.16 `CON-975` dropped it. 0.4.17
`CON-976` drops it again. `CON-974` at
`spec-008-0.4.15-authenticated-allocation-and-consent.md:233-261` and `CON-975`
at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:261-301` are
therefore both still law.

The three effective clauses disagree on four ledgers:

| Ledger | CON-974 (effective) | CON-975 (effective) | CON-976 (candidate) |
|---|---|---|---|
| cbcl-pairing survivors | `TEST-051` through `TEST-058` | `TEST-051`–`TEST-056`, `TEST-058` | `TEST-051`–`TEST-056`, `TEST-059` |
| cbcl-bus set tail | `TEST-084`–`TEST-087` | `TEST-088`–`TEST-092` | `TEST-093`–`TEST-097` |
| Selfsame correction tests | `TEST-1125`–`TEST-1128` | `TEST-1129`–`TEST-1133` | `TEST-1134`–`TEST-1138` |
| coordinated review set | pairing 0.4.7, bus 0.16.8 | pairing 0.4.7, bus 0.16.9 | pairing 0.4.8, bus 0.16.10 |

Each row is a live contradiction rather than a stale narration. `CON-974` and
`CON-975` are current normative text under the adoption rule.

The consequence reaches two mandatory static checks. `TEST-1138` requires
checks over *"every survivor and replacement"* and *"the exact three-repository
test ledgers"*. cbcl-bus `TEST-097` requires *"exact three-repository test
ledgers"*. Three effective Selfsame ledgers give those checks three
incompatible targets, so neither check has one decidable pass condition.

The 0.4.15 `L-1` finding is therefore not closed. That finding was that the
Selfsame ledger named a cbcl-pairing test which cbcl-pairing replaces. The
0.4.16 review closed it against `CON-975`'s corrected sentence. The defective
sentence lives in `CON-974`, which nothing replaced, and it still reads
*"TEST-051 through TEST-058 also survive."* `TEST-057` sits inside that range,
and cbcl-pairing `CON-025` retires `TEST-057` and `TEST-058` together.

All three clauses close with the same guard. `CON-976` reads *"No superseded
portion or open-ended range supplies authority."* That guard cannot reach
`CON-974` or `CON-975`, because neither is a superseded portion. The guard
sentence is exactly what makes the defect invisible to a reader.

The coordinated twin does not carry this defect. cbcl-bus `REQ-070` at
`specs/trajectory/SPEC-053/spec-053-0.16.10-complete-consent-and-executable-ledgers.md:29-71`
opens with *"The survivor lists in REQ-067 remain effective"*, and `REQ-067`
states only replacements and survivors. `REQ-067` names no coordinated review
set and no current-correction-test set, so its statements stay true under
`REQ-070`. The Selfsame ledger clauses state both, so they cannot.

**Minimum sound correction.** Add an item to `CON-976` retiring `CON-974` and
`CON-975` for survivor ledgers, coordinated-set pointers, and
current-correction-test claims. Name each retired sentence. Add a static check
to `TEST-1138` that fails when two effective Selfsame clauses state different
survivor sets.

---

### N2-M-1 (MEDIUM, gate-blocking) — reinstatement is a named migration-graph operation whose deployed path performs an unnamed `index_read`-then-exact-write upgrade

**Repository owner: selfsame (NFR-919) and cbcl-bus (NFR-017).**

`NFR-919` names withdrawal and reinstatement as in-graph operations:

> Finalizer, compensation, status, and operator paths use one generated tail
> order. Compensation uses exact captured keys and no `?ALL` scan. Withdrawal
> and reinstatement take stable account before alias and tombstone keys.

It then closes the graph:

> The migration graph includes every lock and exact mode. … It has no
> additional cycle or upgrade. A missing operation, unstated mode, upgrade,
> opposite order, or additional cycle is non-conforming.

`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe` is the only deployed
implementation of those two operations. Its module header at `:46-51` exports
`withdraw/2` and `reinstate/2`, and `:53-60` declares the alias, account, and
tombstone records.

The withdrawal path is already upgrade-free. `tombstone/2` at `:206-229` takes
`mnesia:read 'cbcl-webfinger-account account 'write`, then
`alias-agrees?/3` at `:232-237` takes `mnesia:read 'cbcl-webfinger-alias uri 'write`,
then writes the tombstone at `:222`. Its table order is account, alias,
tombstone, which matches the clause exactly.

The reinstatement path carries the upgrade. `reinstate/2` at `:295-299` opens
one transaction over `lift-tombstone/2`. That function at `:302-308` calls
`tombstones-for/2`, which at `:264-267` calls
`mnesia:index_read 'cbcl-webfinger-withdrawn account 'account`. It then calls
`mnesia:delete 'cbcl-webfinger-withdrawn uri 'write` at `:306`.

I verified the lock granularity at the local OTP 28 `mnesia-4.25.3` install
rather than from prose. `ixrlock/5` at `mnesia_locker.erl:1086-1102` calls
`need_lock(Store, Tab, ?ALL, read)`. `need_lock/4` at `:858-871` returns `no`
only when the transaction store already holds a matching `{locks, Tab, ?ALL}`
entry. So `mnesia:index_read` takes a whole-table read lock here.

The failure is exact and it matches the shape of 0.4.16 `N-M-2`. Two concurrent
reinstatements each hold `{cbcl-webfinger-withdrawn, ?ALL}` read and each then
seek an exact write on that table. `can_lock/4` at `mnesia_locker.erl:358-361`
admits the transaction's own lock through `can_queue/4` at `:410`, and
refuses the other. Mnesia aborts one as cyclic. That is a read-to-write
upgrade cycle inside the declared graph.

The clause cannot be satisfied as written. Reinstatement finds the tombstone by
account and the table is keyed on alias, so the exact key is unknown before the
index read. The clause states no lock mode for the tombstone table, and it
scopes *"no `?ALL` scan"* to compensation alone. An implementer therefore has
to introduce a whole tombstone-table write lock that no clause declares, and
`NFR-919` itself calls an unstated mode non-conforming.

`TEST-1137` requires *"Generate the complete migration graph. Require the
restored acquisition point, invite-before-room order, and no additional
upgrade or cycle."* cbcl-bus `TEST-096` carries the same sentence. A faithful
generator over the baseline plus the four named corrections produces this
upgrade. Neither test passes. The GATE-04 room, tail, and invite graph box
cannot close.

The named-operation list has the same gap. `TEST-1137` reads *"Include both
`write-grant` paths, both `redeem-and-grant` paths, `revoke-handle-tx`,
`cbcl-chat-invite:redeem-tx`, and `cbcl-chat-sealedgrant:put-tx`."* Neither
`reinstate/2` nor `lift-tombstone/2` appears.

**Minimum sound correction.** State the tombstone table's lock mode for
reinstatement in `NFR-919` and `NFR-017`. Name `lift-tombstone` as an operation
whose `index_read`-then-exact-write upgrade is removed. Add `reinstate/2` to
the operation lists in `TEST-1137` and cbcl-bus `TEST-096`.

---

### N2-M-2 (MEDIUM) — the fresh-ceremony same-key re-grant test disappears inside a collective replacement, while its rule and its gate box remain

**Repository owner: selfsame (TEST-1137) and cbcl-bus (TEST-096).**

`CON-976` item 4 replaces `TEST-1129` through `TEST-1133` collectively. The
replaced `TEST-1132` at
`spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:360-372` is titled
*"Installed state refreshes, rotates, unlinks, and re-grants"*, and its third
paragraph reads:

> Delete the hub grant and run a full new ceremony with the retained key.
> Require current eligibility, capacity, both consents, and a new grant. Every
> local-only shortcut fails.

`TEST-1137` restates the refresh, rotation, handle-change, and unlink cases in
full. It states no re-grant case. A search of the complete 0.4.17 candidate for
`re-grant` and `regrant` returns nothing.

The rule survives without its test. `REQ-995` is effective without amendment,
and at `:200-203` it states:

> Grant deletion creates no permanent device-key tombstone. The same key MAY
> receive a new grant only through a fresh credential/v2 ceremony. That
> ceremony requires current authentication, eligibility, capacity, both consent
> decisions, and a newly verified grant. Local unlink cannot re-grant
> authority.

The gate also survives without its test. cbcl-bus GATE-04 box 28 requires
*"human review accepts relay-visible size correlation, local-unlink
over-refusal, and fresh-ceremony same-key re-grant"*. That box now names an
obligation that no effective Selfsame test states.

The cbcl-bus half fails twice over. `TEST-096` names one source for its
installed-state cases:

> Re-run every effective installed-state refresh, rotation, handle-change,
> unlink, and fresh-regrant case from
> [[spec-053-0.16.9-correlated-success-and-deliverable-bounds#TEST-091]].

`REQ-070` item 6 replaces `TEST-088` through `TEST-092` in full, and `TEST-091`
sits inside that range. So `TEST-096` sources its installed-state content from
a clause the same amendment retires.

That is a regression of 0.4.15 `L-2`, which found `REQ-993` sourcing the unlink
definition from a clause `CON-974` replaces in full. The Selfsame side learnt
that lesson and restates inline. The cbcl-bus side did not.

`TEST-091` also contains no re-grant case. Its four paragraphs at
`spec-053-0.16.9-correlated-success-and-deliverable-bounds.md:478-494` cover
refresh and rotation, handle change and unlink, the two lock graphs, and retry
exhaustion. So `TEST-096` names a `fresh-regrant` case that its named source
never carried.

The hard stop this touches is F-C. The lost sentence is the one requiring both
consents and refusing every local-only shortcut on a re-grant. `REQ-995` still
states that rule, so the stop holds in law. It no longer holds in verification.

**Minimum sound correction.** Restate the fresh-ceremony same-key re-grant case
inline in `TEST-1137`. Restate every installed-state case inline in cbcl-bus
`TEST-096` and delete the pointer to the replaced `TEST-091`.

---

### N2-M-3 (MEDIUM) — the sealed-record clause calls two recognizer caps "maxima", against the derivation it says is unchanged

**Repository owner: selfsame (REQ-996 and TEST-1136), cbcl-bus (REQ-071 and TEST-095).**

`REQ-996` states three sentences together:

> The maxima in
> [[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remain
> unchanged. The maximum offer plaintext is 57,000 octets. The maximum reverse
> plaintext is the 62,000-octet credential member.

`REQ-988` derives different figures and states an explicit labelling rule. At
`spec-008-0.4.13-reachable-bounds-and-durable-authority.md:174-181` it derives
`reachable complete offer = 41,308` with a `57,000-octet offer residual` of
`15,692` and a `62,000-octet CBCL member residual` of `20,692`. At `:185-186`
it states:

> A recognizer ceiling that is intentionally above a reachable maximum is
> labelled a cap, never a maximum.

57,000 and 62,000 are exactly such ceilings. `REQ-988` derives them as caps
with residuals. `REQ-996` relabels both as maxima in the sentence after the one
that adopts `REQ-988` unchanged.

The arithmetic itself is sound, and I verified its cryptographic premise
against source. `seal_offer` at `crates/selfsame-core/src/seal.rs:143-147` and
`seal_bundle` at `:158-165` call `ChaCha20Poly1305::encrypt` with
`Role::nonce()`. The module header at `:33-38` records that the key is
single-use and the nonces are fixed and direction-separated. The associated
data is `AD_OFFER` at `:52` and `AD_BUNDLE` plus transcript at `:55`, and
neither is stored. So only the 16-octet tag expands the record. Every residual
re-derives: 57,000 + 16 = 57,016 and 69,632 − 57,016 = 12,616; 62,000 + 16 =
62,016 and 69,632 − 62,016 = 7,616.

The consequence is a test whose first instruction has two readings.
`TEST-1136` requires *"Generate the 57,000-octet offer and 62,000-octet reverse
plaintext maxima."* cbcl-bus `TEST-095` carries the same sentence. Under the
grammar no legal offer payload reaches 57,000 octets, because `REQ-988` bounds
every component and derives 41,308. A generator author therefore has to decide
between a legal maximum offer and an opaque cap-sized blob, and the two differ
by 15,692 octets.

The transit conclusion is unaffected and stays conservative. Both sealed
records fit 69,632 octets under either reading. The defect is a clause whose
stated discipline is that every maximum is derived and labelled, contradicting
that discipline in its own correction.

**Minimum sound correction.** Rename both figures as caps in `REQ-996` and
`REQ-071`. State the sealed cap arithmetic against those caps and the reachable
41,308-octet offer separately. State in `TEST-1136` and `TEST-095` that the
transit record is an opaque cap-sized octet string.

---

### N2-L-1 (LOW) — `cbcl-sealedgrant` enters the generated table order while the one operation that reads then writes it stays outside the declared scope

**Repository owner: selfsame (NFR-919) and cbcl-bus (NFR-017).**

The 0.4.16 `N-L-5` question is answered for invites. `NFR-919` now reads *"The
graph includes every `cbcl-invite` holder. It includes
`cbcl-chat-sealedgrant:put-tx`, whose first invite access is an exact read
lock."* Both halves verify.
`apps/cbcl_chat/src/shell/cbcl-chat-sealedgrant.lfe:67-79` runs `put-tx`, which
calls `cbcl-chat-invite:peek-tx`. `peek-tx` at
`apps/cbcl_chat/src/shell/cbcl-chat-invite.lfe:75-78` uses `mnesia:read/2` on
an exact key and writes nothing.

The answer pulls a fourth table into the graph. `NFR-919` states the order:

> The generated table order places `cbcl-invite` before `cbcl-roommember`,
> `cbcl-account-roommember`, and `cbcl-sealedgrant`.

The scope sentence is narrower: *"The migration graph owns every room,
finalizer-tail, and `cbcl-invite` lock."*

One deployed operation locks `cbcl-sealedgrant` and holds no invite, room, or
tail lock. `get-tx` at `cbcl-chat-sealedgrant.lfe:94-101` calls
`mnesia:read 'cbcl-sealedgrant token`, and on the expiry branch calls
`mnesia:delete 'cbcl-sealedgrant token 'write`. That is an exact-key
read-to-write upgrade on a table the graph orders.

`cbcl-sealedgrant` is not a tail table. The `NFR-913` finalizer row names
alias, account, tombstone, finalization, status, and compensation keys, and
lists no sealed-grant table.

I traced the consequence and found no safety defect. Two concurrent `get-tx`
calls on one expired token can abort one as cyclic, which loses no durable
state. The defect is that a generator told to include every lock and exact mode
over a graph containing `cbcl-sealedgrant` cannot decide whether `get-tx`
belongs.

**Minimum sound correction.** State in `NFR-919` and `NFR-017` whether the
migration graph covers every `cbcl-sealedgrant` holder. Name `get-tx` and its
upgrade if it does.

---
## Disposition of every 0.4.16 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N-H-1** — the consent-surface completeness rule is inverted | **CLOSED** | `REQ-996` now carries the cbcl-bus form: *"No filter, paging, collapse, or lazy rendering SHALL hide a room."* It adds a universal rule: *"Every display path renders the complete ordered plan array. A summary MAY precede it but SHALL NOT replace, truncate, collapse, page, filter, or defer any room. The displayed room count and byte sequence equal the authenticated array at confirmation time."* cbcl-bus `REQ-071` is identical. `TEST-1134` and cbcl-bus `TEST-093` add the missing case: render a maximum plan through every filter, paging, collapse, and lazy-render path, require all 256 rooms, and require displayed count and ordered bytes to equal `migrationRooms`. The shipped code the rule targets is real: `affectedChannelsNow()` at `apps/cbcl_chat/priv/web/app.js:602-603` returns `[...document.querySelectorAll('.clog[data-room]')]`, its call sites are `app.js:6409` and `:6443`, and the painter loop at `app.js:666-670` iterates the whole array today. |
| **N-M-1** — the "complete" v1 trace omits four `payloadVersion` sites | **CLOSED** | The trace is fifteen lines and every line verifies at `0220cec2`. The four added sites are `ceremony.rs:185`, `:308`, `:460`, and `:510`, and that file holds no `#[cfg(test)]` in its 585 lines. I ran an independent exhaustive scan of all 107 production and test Rust files at the baseline for `PROFILE_VERSION`, `profileVersion`, `profile_version`, `evidenceVersion`, `evidence_version`, `payloadVersion`, and `payload_version`. Every v1-pinned production site is listed: six `PROFILE_VERSION` uses plus two definitions at `lib.rs:109` and `profile.rs:60`, and eight version-1 literals. `lib.rs:1369` is listed and is in scope, because `core.profile_version` is the literal `1` set at `:1346` in the same function. The six omitted sites carry runtime values and no v1 pin: `ceremony.rs:190`, `:413-416`, `enrollment.rs:592`, `:666`, `provider_hint.rs:67`, and `:96-99`. The excluded test-file sites are correct: `#[cfg(test)]` opens at `web-device/src/lib.rs:1521`, `beam/src/enrollment.rs:199`, and `app-identity-net/src/profile.rs:221`. The scan obligation now names four families including `payloadVersion`. |
| **N-M-2** — the lock-acquisition point and the `revoke-handle-tx` upgrade were dropped | **CLOSED** | `NFR-919` restores both: *"An operation touching both room tables takes both whole write locks in the declared order. It acquires them before its first room read or write. `revoke-handle-tx` therefore performs no `index_read`-then-exact-write upgrade."* The non-conformance list restores both terms: *"A missing operation, unstated mode, upgrade, opposite order, or additional cycle is non-conforming."* Every named site verifies. `cbcl-chat-roommember.lfe:153-161` and `cbcl-chat-account-roommember.lfe:65-79` both read with `mnesia:read/2` then write. `cbcl-chat-roommember.lfe:242-246` opens one transaction that deletes an exact `cbcl-roommember` key and then calls `revoke-handle-tx`, which at `cbcl-chat-account-roommember.lfe:201-209` index-reads then deletes. The finalizer claim verifies at `cbcl-chat-account-roommember.lfe:139-140`, which takes both whole write locks in the declared order. I also verified that the correction is implementable: a transaction already holding `{Tab, ?ALL}` write is granted the later `ix_read` lock immediately, because `can_queue/4` at `mnesia_locker.erl:410` treats a transaction's own lock as compatible. Write-then-read is not an upgrade. |
| **N-M-3** — the twelve cycles use symbols whose legend was deleted | **CLOSED** | `NFR-919` restores a complete legend and binds each symbol to an exact `NFR-913` row. Six symbols use the row labels verbatim: `grant creation`, `credential/v2 finalizer`, `exact grant revocation`, `observed revocation`, `new member enrollment`, and `pairaccount claim`. The seventh reads *"Per-frame authority read is `AR`; it uses the matrix's `unratified read` sequence."* The binding is bijective, because `NFR-913` at `spec-008-0.4.11-standalone-tofu-first-contact.md:245-253` has exactly seven rows and `unratified read` is the only read-only row. That binding also makes `F×AR` derivable, because a reader holding grant `?ALL` and seeking member `?ALL` contends with a finalizer holding an exact member write. cbcl-bus `NFR-017` carries the identical legend. |
| **N-M-4** — the parent pointer had no target and advertised an approved version | **CLOSED** | `specs/SPEC-008-production-pairing-claimant.md:8` now carries `candidate-amendment: "[[spec-008-0.4.17-complete-consent-and-executable-ledgers]] …"`, `:9` carries `candidate-review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review`, and `:16-21` states the open increment in prose. The approved implemented 0.2.2 baseline stays distinguished from the open candidate. The cbcl-bus parent resolves too: `specs/SPEC-053-key-root-identity.md:15` names the 0.16.10 candidate, `DEC-012` sits at `:635`, and the body cites the candidate at `:688-691`. `TEST-1138` names both repositories: *"They require the Selfsame parent pointer to this candidate. They also require the cbcl-bus parent DEC-012 pointer to 0.16.10."* |
| **N-L-1** — `socketGeneration` had no definition | **CLOSED** | `REQ-996` gives creation, capture, cancellation, and reconnect semantics: *"`socketGeneration` is a browser-local safe integer. The browser increments it before installing callbacks on each new WebSocket object. Every callback captures the generation of its socket. The value never crosses the wire and need not survive reload."* Cancellation is exact: *"Close, error, replacement, and reconnect atomically cancel every live entry for the old generation. The next socket uses the incremented generation. A late callback from the old socket therefore cannot match or cancel a new attempt."* `TEST-1134` exercises it: close and replace the socket with live entries, deliver every late callback after reconnect, and require the new generation to reject old callbacks. |
| **N-L-2** — two different definitions of one shared grammar production | **CLOSED** | cbcl-pairing 0.4.8 `REQ-030` carries the split. I extracted the six-line grammar block from all three candidates and hashed each: all three are byte-identical, at SHA-256 prefix `898a05409a645803`. cbcl-bus `CON-033` names the source explicitly: *"Cbcl-pairing [[spec-001-0.4.8-split-room-shape-and-length#REQ-030]] supplies the same `RoomShape` and `CanonicalRoomText` productions."* cbcl-pairing `ADR-023` records that the accepted language does not change. The alphabet matches `is_symbol_char` at `crates/cbcl-parser/src/parser.rs:328-342` in pinned cbcl-rs `febc669`, character for character. |
| **N-L-3** — the transit ledger omitted the in-repository baseline | **CLOSED** | `CON-976` names both: *"The reviewed `crates/selfsame-rendezvous` and did-crdt baselines each have a 4,096-octet cap. Both are deliberately non-conforming to [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]."* Both verify: `crates/selfsame-rendezvous/src/lib.rs:48` reads `pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`; did-crdt `src/service/rendezvous.rs:65` carries the same constant at `1f409a4`. `TEST-1136` requires *"Prove both reviewed 4,096-octet baselines fail"*, and cbcl-bus `TEST-095` repeats it. |
| **N-L-4** — the sealed-record maximum was never derived | **CLOSED, with a new labelling defect** | `REQ-996` now derives every figure and residual, and the AEAD premise verifies at `crates/selfsame-core/src/seal.rs:143-165`. The four numbers re-derive exactly. The residual defect is that both figures are recognizer caps rather than maxima, which is filed as `N2-M-3`. |
| **N-L-5** — one live `cbcl-invite` lock fell outside both graph scopes | **CLOSED, with a new scope residual** | `NFR-919` states *"The graph includes every `cbcl-invite` holder"* and names `cbcl-chat-sealedgrant:put-tx` with its exact modes. Both halves verify at `cbcl-chat-sealedgrant.lfe:67-79` and `cbcl-chat-invite.lfe:75-78`. The table order now places `cbcl-invite` first. The residual is `cbcl-sealedgrant` itself, filed as `N2-L-1`. |

Ten closed. Two closures carry a residual, and each residual is filed as its
own new finding rather than left inside the disposition.

## Disposition of every 0.4.15 finding

| ID | Disposition | Evidence |
|---|---|---|
| **H-1** — unsigned advancing signal with no defined correlation | **CLOSED** | `REQ-996` states the correlation member, its 32-octet generation, its encoding, and its one-use consumption. It states plainly that the frame *"is unsigned and origin-less"*, which matches `apps/cbcl_chat/src/core/cbcl-core-wire.lfe:185` and the `zero-sig` builder at `:31-32`. `TEST-1134` swaps, duplicates, suppresses, reorders, replays, mutates, and removes the value. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **CLOSED** | Both sites are present with correct surfaces, `:452` reads `recognizer substitution`, and the trace is now complete against an independent exhaustive scan. See `N-M-1` above. |
| **M-2** — `write-grant` read-then-write upgrades | **CLOSED** | `NFR-919` requires `mnesia:read/3` with `write` on the first read in both paths, and both deployed sites carry the upgrade today. |
| **M-3** — maxima never checked against the implemented rendezvous | **CLOSED** | `REQ-996` and `CON-976` state the dependency on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and [[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Both anchors resolve: `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:310-320` sets 69,632 octets, and `CON-304` at `:709-733` sets a `1..69632` body with `413` above it. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` remains effective and states the terminal. `TEST-1137` requires the terminal and zero enrollment frames. The hub-side guard stays independent at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`. |
| **L-1** — survivor set revives `TEST-057` | **NOT CLOSED** | `CON-976` states the corrected set. `CON-974` still states *"TEST-051 through TEST-058 also survive"*, and nothing replaces `CON-974`. Filed as `N2-H-1`. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED in selfsame, REGRESSED in cbcl-bus** | `REQ-995` restates the unlink action in full and drops the pointer. cbcl-bus `TEST-096` now sources its installed-state cases from `TEST-091`, which `REQ-070` item 6 replaces in full. Filed as `N2-M-2`. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-996` splits shape from length, orders the four classes, and states *"Length classification occurs only after `RoomShape` passes. An over-length room therefore has one result."* cbcl-pairing `REQ-030` and cbcl-bus `REQ-071` carry the same order. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` remains effective and splits the triggers. The silent path cannot widen consent, because `REQ-981` keys the person-owned policy on the exact `(applicationId, relayOrigin)` pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:87-88`. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-996` makes `migration-display-state-changed` non-terminal with a retained plan and a disabled confirmation, and makes `migration-plan-invalid` terminal for that ceremony. Both are implementable against the shipped reducer, whose `Burned` branch returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:172-174`. |

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-976` names four replacements and `REQ-981` is not among them. `REQ-981` still fixes the policy key as the exact pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:87-88`, and it still states that a v1 policy entry cannot suppress the first v2 prompt. cbcl-bus `REQ-071` restates the distinct ApplicationId in its first sentence. GATE-04 keeps the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application, cross-checked before approval | **HOLDS** | The wallet half is untouched: `crates/selfsame-pairing/src/live.rs:479` commits one explicit local decision only after `LiveEffect::DisplayIntent`, and cbcl-pairing `src/profile.rs:92-101` declares the recognised `DisplayIntent` shape. The five pre-consent checks in `REQ-996` are ordered and concrete, and the plan is built only from authenticated values. The room half is now sound in both text and test, which is the `N-H-1` closure. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS in law, weakened in verification** | `CON-976` names nothing in `CON-962`, so its effect order stands. A negative admission result *"creates no success, carrier allocation, pairing room, digest, or partial pending object."* A copied acknowledgement *"can start only the reversible transport wait."* `REQ-995` still states that local unlink cannot re-grant authority, and that a new grant requires both consent decisions. The test that verified that rule is gone, which is `N2-M-2`. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-976` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:903` still declares `selfsamepairallocate(rawCarrier, requestId, intentNonce, requestedPermissions, deviceKeyJwk)`, so the stable scope and the one pending ceremony still commit atomically. The `REQ-996` snapshot list adds no enrolment precondition, and `REQ-984` keeps `{"kind":"no-transition"}` as a legal shape. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. cbcl-pairing `CON-025` preserves the no-relay-field property of `CON-024`, which traces to `CON-022`. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at cbcl-pairing `src/cbcl_protocol.rs:34` and `MAX_FRAME_BODY_BYTES = 69_632` at `src/mailbox.rs:15` both sit above the 57,016-octet and 62,016-octet sealed records. GATE-04 retains the box requiring human acceptance of relay-visible size correlation. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-331` reads *"Mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery."* Every negative path this amendment adds or keeps creates nothing. The three admission results allocate nothing. `migration-plan-invalid`, `migration-display-state-changed`, `credential-v2-verification-unavailable`, and `hub-frame-refused` each send no frame and claim no authority. `CON-976` restates the stop. No finding in this review lets an endpoint failure authorise. |

---

## Repository assessments

### selfsame SPEC-008 0.4.17-draft (`50cdc0b`)

Artefact numbers are sequential with no gap and no reuse. `REQ-996`, `NFR-919`,
`CON-976`, and `ADR-954` each advance their series by one, and `TEST-1134`
through `TEST-1138` follow `TEST-1133`. The Selfsame test ledger re-derives
exactly. The replacement chain runs in these steps:

```text
TEST-1109..1112 -> 1113..1116 -> 1117..1120 -> 1121..1124
                -> 1125..1128 -> 1129..1133 -> 1134..1138
```

So *"every effective Selfsame test through TEST-1108"* plus the five correction
tests is the complete set.

Nine of the ten wikilink targets resolve inside this vault.
`[[SPEC-053-key-root-identity#DEC-012]]` is a cross-repository pointer whose
target lives in cbcl-bus, so it reports dead here. Under
[[PROTO-001-usdd-agent-protocol]] a dead link is visible debt rather than an
error, and I verified the target by hand at `4e435ad`.

The correlation paragraph is precise about what the acknowledgement does and
does not authenticate. The display-completeness rule is the strongest version
in the chain. The version trace is now complete and consistently criteria-ed.

Open against it: `N2-H-1`, `N2-M-1`, `N2-M-2`, `N2-M-3`, and `N2-L-1`.

### cbcl-bus SPEC-053 0.16.10-draft (`4e435ad`)

`REQ-070` states the survivor and replacement ledger exactly, and the six
replacements are individually named. Its ledger chain is monotone, because
`REQ-067` states no coordinated set and no current-correction-test claim. The
GATE-04 restatement holds 42 boxes by mechanical count, which matches both
`TEST-097` and Selfsame `TEST-1138`. Every box remains open and the amendment
states that it closes none.

The parent pointer resolves at `specs/SPEC-053-key-root-identity.md:15`, with
`DEC-012` at `:635` and two body references at `:688-691`.

Two divergences from the Selfsame twin are recorded rather than filed.
`NFR-017` reads *"The authority generator reproduces those twelve cycles"*
where `NFR-919` reads *"The generator SHALL reproduce"*, so the twin carries the
normative keyword and cbcl-bus does not. `NFR-017` also says *"The migration
generator owns"* where `NFR-919` says *"The migration graph owns"*. Neither
changes an obligation.

Open against it: shares of `N2-M-1`, `N2-M-2`, `N2-M-3`, and `N2-L-1`.

### cbcl-pairing SPEC-001 0.4.8-draft (`b02a3f5`)

Clean. `REQ-030` carries the `RoomShape` split, and its grammar block is
byte-identical to both twins. `ADR-023` records that the accepted language does
not change. `CON-025` keeps the hub as the sole owner of the three admission
terminals and preserves every `CON-024` property. `NFR-022` keeps every
authenticated byte ahead of the owned allocation and keeps the verifier
verdict-only.

The 33,793-octet arithmetic re-derives exactly. The test inventory is
`TEST-030` through `TEST-059` with no gap, and the survivor set matches the
Selfsame `CON-976` set exactly. `PathAToBDisplay` and `CredentialV2Display`
remain absent from `src/`, so the typed display stays design rather than code.
The surfaces they attach to are real at `src/profile.rs:92-130` and
`src/endpoint.rs`.

Open against it: a share of `N2-H-1`, because `CON-974` still names `TEST-057`
and `TEST-058` as survivors of this repository.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged. `src/service/rendezvous.rs:65`
carries `MAX_SLOT_BYTES = 4096`, `:68` carries a 600-second TTL, and `:79`
carries a 65,536-slot ceiling. The working tree carries user-owned uncommitted
paths, which I ignored in favour of the declared commit.

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Reconnect race across attempts.** A late callback from a replaced socket
  carries the old generation. The map key includes that generation, and every
  old entry is cancelled atomically, so the callback matches nothing.
- **Forged, replayed, and swapped acknowledgement.** A copied frame carrying the
  live value starts only the reversible transport wait. The later signed core
  carries `requestId`, so a swapped genuine success fails the browser's own
  request check before consent.
- **Correlator disclosure through the sealed offer.** `requestId` becomes visible
  to whoever opens the offer, and that happens after the map entry is consumed.
  Replay is closed by consumption rather than by secrecy.
- **Suppressed acknowledgement.** A withheld frame leaves the browser waiting,
  which denies service and allocates nothing.
- **Virtualised and clipped room lists.** `REQ-996` forbids deferral by name and
  requires the displayed count and ordered bytes to equal the authenticated
  array. `ADR-954` adds *"No presentation mechanism can hide a migrated room."*
  A scrolling list that renders every element satisfies both.
- **Bare `@` and non-ASCII rooms.** `RoomShape` requires one symbol character
  after `@`, so a one-octet room fails the shape and is
  `MigrationRoomUnrepresentable`. `CbclSymbolChar` is ASCII-only, so any
  multibyte value fails the same class.
- **Shipped `CHANNEL` narrower than the deployed parser.**
  `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:118` reads
  `/^@[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/`, which refuses most of the deployed
  alphabet. All three candidates require its replacement from one generated
  grammar.
- **Grammar drift between the pinned revisions.** The repository pins cbcl-rs
  `1c6fa8f` in `cbcl-rs.sha` while the candidates pin `febc669`. `1c6fa8f` is an
  ancestor of `febc669`, and `crates/cbcl-parser/src/parser.rs` is unchanged
  between them, so the alphabet claim holds under either pin.
- **`EnrolmentAllocator::new_native` reachability.** The function is real at
  `crates/selfsame-web-device/src/lib.rs:1318`, and its only JavaScript-exposed
  entry is the constructor at `:1419-1431`. The prohibition names a real symbol.
- **`index_read` lock granularity.** Verified against the local OTP 28 install
  rather than prose. `ixrlock/5` at `mnesia_locker.erl:1086-1102` requests a
  whole-table read lock through `need_lock(Store, Tab, ?ALL, read)`.
- **Whole-table write lock followed by `index_read`.**
  `need_lock(Store, Tab, ?ALL, read)` matches only a stored `read` entry, so a
  held table write lock does not suppress the request. The request is granted
  at once through `can_queue/4`, because the holder is the same transaction.
  Write-then-read is therefore not an upgrade, and the `N-M-2` correction is
  implementable.
- **Path-A finalization lock order.** `confirmed-migrate-path-a-handle` at
  `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:118-141` reads the
  binding with `'write`. It then reads `cbcl-member` with `'write` through
  `cbcl-chat-members.lfe:37`. It then takes both whole room write locks at
  `cbcl-chat-account-roommember.lfe:139-140`. It carries no upgrade and matches
  the declared prefix.
- **Withdrawal lock order.** `tombstone/2` takes account, alias, then tombstone,
  which matches *"stable account before alias and tombstone keys"* exactly.
- **Transit arithmetic and carriers.** Sealing adds only the 16-octet tag.
  `MAX_FRAME_BODY_BYTES = 69_632` and `MAX_ADJACENT_BODY_OCTETS = 65_536` both
  sit above the 62,016-octet sealed reverse record. Only the rendezvous lane is
  undersized.
- **Standalone first contact without enrolment.** The `REQ-996` snapshot list
  names an enrolled key, and `REQ-984` keeps `{"kind":"no-transition"}` legal.
  A first-contact account snapshots an empty room set and no member, so the
  clause adds no implicit enrolment dependency.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and
   credential/v2 transport depends on it. Its own non-conformance list at
   `specs/PROTO-002-selfsame-rendezvous-v1.md:1016` records the reference
   implementation's 4,096-octet body.
2. **The whole credential/v2 lane is planned rather than shipped.**
   `selfsamepairallocate`, `migrationRooms`, `roomSetDigest`, and
   `credential-v2-canonical.mjs` are all absent from `apps/` and `priv/` at
   `4e435ad`. I searched for each. That is honest planned work.
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt
   durable readiness, and the separate owner approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration,** and the parent
   specification records that deferral as in force.
5. **Browser account identity remains a human comparison,** owned by `CON-221`
   and 0.4.2 `REQ-926`.
6. **The unary `mnesia:transaction` call sites in `apps/` remain unbounded
   today.** `NFR-919` mandates a change rather than describing current
   behaviour, and that change surfaces `authority-busy` on writes that
   previously blocked.
7. **The typed cbcl-pairing display is a design.** Its two named types are
   absent from `src/`, which both the amendment and the prior review record.
8. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** The parent
   [[SPEC-008-production-pairing-claimant]] carries both. This is an inherited
   condition of the amendment form rather than a defect this candidate
   introduces, and no prior review filed it.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state
it. Such a PASS authorizes only the Elephant SPL and the test-first
implementation plan. It authorizes no code, no public allocation, no gate
opening, no release, no deployment, and no production enablement. Every
GATE-04, `GATE-00`, `GATE-01`, and `GATE-03` box stays open. Every human
review, every durability obligation, and the separate owner approval for
production deployment also stay open and unaffected.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N2-H-1 | selfsame | Retire `CON-974` and `CON-975` for survivor ledgers, coordinated-set pointers, and current-correction-test claims, and check for competing effective ledgers in `TEST-1138`. |
| N2-M-1 | selfsame, cbcl-bus | State the tombstone table's lock mode for reinstatement, name `lift-tombstone` as an upgrade to remove, and add `reinstate/2` to both operation lists. |
| N2-M-2 | selfsame, cbcl-bus | Restate the fresh-ceremony same-key re-grant case inline in `TEST-1137`, and restate every installed-state case inline in `TEST-096`. |
| N2-M-3 | selfsame, cbcl-bus | Rename 57,000 and 62,000 as caps, keep the reachable 41,308-octet offer distinct, and say that the transit record is an opaque octet string. |
| N2-L-1 | selfsame, cbcl-bus | State whether the migration graph covers every `cbcl-sealedgrant` holder, and name `get-tx` and its upgrade if it does. |
