---
id: spec-008-0.4.2-claude-adversarial-review-2026-08-23
title: SPEC-008 0.4.2-draft — Tier-1 fresh-context cross-model adversarial review
status: complete
tier: 1
reviewer: independent fresh-context reviewer (defect-finding mandate)
reviewed-artifact: "[[spec-008-0.4.2-standalone-tofu-first-contact]]"
reviewed-baselines: "selfsame ca77145 (code tree identical to c6af6df); cbcl-pairing 8e55160; cbcl-bus fdbe414; did-crdt 1f409a4"
prior-review: "[[spec-008-0.4.1-claude-adversarial-review-2026-08-23]]"
date: 2026-08-23
---

# Verdict: REJECT

## Confirmed repository revisions

| Repository | Revision at review time | Difference from the candidate's "Reviewed baselines" table |
|---|---|---|
| `selfsame` (this worktree) | `ca77145bf624f3dd58a40595dd970ed7200ea157` — *spec: harden standalone pairing with co-present CPace*; clean tree | The candidate names `f2501d9` over code parent `c6af6df`. `git diff --stat c6af6df..ca77145 -- ':!specs'` is **empty**: `f2501d9`, `704ada4`, `f196342` and `ca77145` are all specs-only. The reviewed code is therefore byte-identical to the named parent `c6af6df75d033d2e44ae367457c1a754ba2c4c3a`. No material difference. |
| `cbcl-pairing` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` — *docs: restore aligned pairing swimlane*; untracked `.DS_Store`/`.zetl` only | Matches. |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` — *ui(pairing): make cbcl-pairing THE way to link a wallet*; clean | Matches. |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` — *fix(rendezvous): bound storage, amortise expiry, cap body before buffering*; working tree carries an uncommitted edit to `specs/did-crdt-spec.md` and an untracked `docs/adr/ADR-007-xid-interoperability-posture.md` | Code matches. The two uncommitted files are documents and change no route, handler, or type traced below. |

All four baselines are the ones the 0.4.1 review verified, so every code claim in
this report is directly comparable with that record.

## Executive rationale

0.4.2 is a substantial, largely honest repair. Measured against the corrections
the 0.4.1 review demanded, it closes both CRITICAL findings and five of eight
MEDIUMs outright, and I reproduced the arithmetic and the code facts behind
those closures rather than accepting the disposition table:

- **C-1 is closed.** `HomeKey::home_did` really is a pure function of the derived
  public key (`crates/selfsame-app-identity/src/hierarchy.rs:178-182` →
  `selfsame_core::identity::derive_did`), and `issuer::create` really is the
  first thing that signs (`crates/selfsame-app-identity/src/issuer.rs:134-145`,
  `identity::sign_genesis`). The CON-918 preview/commit split is therefore
  implementable, deterministic, and re-derivable, and the effect table at
  candidate:620-626 places every signature, publication, issuance write, bundle,
  alias and payload strictly after the final decision.
- **H-2 is closed.** The 62,000 / 64,512 / 4,096 classes fit inside every
  unchanged bound in `schemas/pairing-v1.cddl`, `src/channel.rs:21`,
  `src/mailbox.rs:13-15` and `src/bin/cbcl-pairing-relay-ws.rs:32-37`. I
  recomputed the encoder chain and the 201,768-octet budget and both hold.
- **M-3 is closed.** Consumption before display is exactly what CON-214 step 5
  requires and exactly what `prepare_issuance` already does
  (`src-tauri/src/app_grant.rs:381-389`).
- **M-5/M-6/M-7/M-8, L-1/L-2/L-3 are closed** as design text.

It is nevertheless not implementable as written, and three of its load-bearing
closures rest on statements that the named code contradicts.

**Five HIGH findings.**

1. **H-1 is not closed; the claim that closes it is false.** CON-913 amends
   CON-201 to "eleven: its existing ten names plus required `cbclPairingRelays`"
   and the disposition table calls that member "already implemented"
   (candidate:116, 461-463). The shipped recogniser has a **nine**-name required
   set that *removes* `rendezvous` and `pairingRecordRelays`
   (`crates/selfsame-app-identity/src/profile.rs:80-91`), and the shipped
   chat.anuna.io profile generator classes `rendezvous` as a **legacy** member to
   reject (`cbcl-bus/scripts/gen-production-profile.mjs:309`). Ratifying CON-913
   as drafted makes the shipped profile non-conforming and the shipped
   recogniser non-conforming to CON-913 — so REQ-921's "complete CON-220
   recognition" is unreachable from either side.
2. **The presence secret is byte-for-byte the recovery-phrase encoding.**
   REQ-922 specifies "sixteen CSPRNG octets … canonical twelve-word BIP-39
   English encoding" (candidate:186-188). That is *exactly*
   `selfsame_core::derive::mnemonic_from_entropy(&[u8; 16])`
   (`crates/selfsame-core/src/derive.rs:66-68`) — the construction of the
   Selfsame root recovery phrase. The wallet would gain a routine, frequently
   prompted "type twelve BIP-39 words" input indistinguishable in shape from
   `Custody::restore`, in a flow whose UI copy is the exact instruction shape of
   a recovery-phrase attack.
3. **CON-919 requires the device key to be the chat session key.** "checks that
   the offered device JWK is that same key" (candidate:629-631) contradicts
   SPEC-004 REQ-215, the candidate's own REQ-925 ("reuse it for retries and
   later devices", candidate:264) and REQ-927 ("the actual retained browser
   device key", candidate:324), and the shipped browser, which draws a fresh
   per-ceremony device seed (`enrol-allocator.mjs:60-62`).
4. **REQ-924's CON-221 applicability has no authority interface and a
   false-`NoBinding` trap.** The only reachable authority projection is
   URI-keyed webfinger (`cbcl-chat-webfinger-http.lfe:27` → `store:lookup(uri)`),
   so the natural implementation asks "is *this DID's* alias bound?" and answers
   `NoBinding` for an account already bound to a **different** DID — precisely
   the false first-enrollment CON-221 exists to prevent. `BoundDifferentDid`
   (candidate:242) is unreachable and no hub command supplies it.
5. **CON-921 mandates handle-keyed alias provisioning that REQ-925 forbids.**
   `provision/3`'s account key is the handle
   (`cbcl-chat-webfinger-store.lfe:124-134, 339-341`), while REQ-925 says the
   handle "SHALL NOT key scope, **alias**, replay, or ceremony state"
   (candidate:260-261). No re-keying or migration slice is named.

Seven MEDIUM findings compound these, of which three matter most: the public
carrier is now a deterministic ceremony-kill primitive against a relay mailbox
that terminates on a second `Claim`; the CON-220 step-6 replacement substitutes a
pin asserted by the party being authenticated; and CON-920's "the existing
`verify_path_b_peer` acceptance facade" cannot express the steps-1-8/9-13 split
REQ-927 requires, while the browser JSON facade stamps
`ClosureSource::StateResolver` unconditionally from caller-asserted booleans.

None of these is fatal to the architecture. Every one is local and specifiable,
and the design's central choices — a public carrier plus a separately
transcribed human secret in CPace, application-session provenance for the
account label, one preliminary consent and one final effect gate, a
non-negotiated v2 dialect inside unchanged bounds — survive review. A 0.4.3 that
makes these corrections is a plausible PASS candidate. This one is not:
`PASS` requires no unresolved HIGH and no false implementability claim, and there
are five of the first and at least two of the second.

---

## Findings

Severity: CRITICAL = unsound as designed; HIGH = blocking design or
implementability defect; MEDIUM = must be fixed before implementation;
LOW = record and correct. Severity is not lowered for the size of the fix.

`file:NNN` references are to the confirmed baselines. `candidate:NNN` references
are to `specs/trajectory/SPEC-008/spec-008-0.4.2-standalone-tofu-first-contact.md`.

There are no CRITICAL findings.

---

### H-1 (HIGH) — The CON-201 amendment describes a grammar that neither the specification nor the implementation has, and the disposition that closes 0.4.1 H-1 is factually wrong

**Exact candidate clause.** `CON-913:461-465`: "CON-201 profile version 1 is
amended to have a closed permitted-name set of eleven: its existing ten names
plus required `cbclPairingRelays`. The original `pairingRecordRelays` remains
OPTIONAL, so a conforming object contains the ten required members or all eleven
permitted members." Disposition table `candidate:116`: "CON-913 explicitly amends
CON-201 with the **already implemented** eleventh `cbclPairingRelays` member and
reconciles CON-806."

**Governing clauses.** `SPEC-004-application-scoped-identity#CON-201`
(specs/SPEC-004-application-scoped-identity.md:2469-2481): "the top-level value
is an object whose member set is exactly the ten names below — `profileVersion`,
`applicationId`, `accountAuthority`, `verifierAudience`, `allowedPermissions`,
`enrollment`, **`rendezvous`**, `stateResolvers`, `revocation`, and the OPTIONAL
`pairingRecordRelays` … **An unknown member at any depth is a rejection, not an
extension point.**" `CON-220` step 4 (:4132): "run the complete CON-201
recognition before any semantic action." `SPEC-007…#CON-806` (:645-648): "the
coordinated SPEC-004 amendment defines `cbclPairingRelays`."
`SPEC-007…#Amendment disposition` (:718-733) disposes PROTO-002 as "Deprecated
for the Selfsame credential-pairing path" but **does not dispose CON-201 at
all**, so CON-201's required `rendezvous` member still stands unamended.

**Code evidence.**

- `crates/selfsame-app-identity/src/profile.rs:80-91`:

  ```rust
  const TOP_LEVEL_REQUIRED: &[&str] = &[
      "profileVersion", "applicationId", "accountAuthority",
      "verifierAudience", "allowedPermissions", "enrollment",
      "cbclPairingRelays", "stateResolvers", "revocation",
  ];
  const TOP_LEVEL_OPTIONAL: &[&str] = &[];
  ```

  Nine required names, zero optional. `rendezvous` and `pairingRecordRelays` are
  **absent from the recogniser entirely** — `grep -n 'rendezvous\|pairingRecordRelays'
  crates/selfsame-app-identity/src/profile.rs` returns nothing.
  `from_value` (`profile.rs:382`) calls
  `closed_members(value, "", TOP_LEVEL_REQUIRED, TOP_LEVEL_OPTIONAL)`, so a
  profile containing `rendezvous` is rejected as an unknown member.
- `cbcl-bus/scripts/gen-production-profile.mjs:179-233` assembles the real
  chat.anuna.io profile from exactly those nine members, and `:309` reads
  `const legacy = ['rendezvous', 'pairingUrl', 'pairingProtocol', 'pairingRoute'];`
  — the generator treats CON-201's required `rendezvous` member as a **legacy
  shape to reject**. The generator runs "the REAL vendored recogniser over the
  result" (`:4`), so the shipped profile and the shipped recogniser agree with
  each other and disagree with both CON-201 and CON-913.

**Failure trace.**

1. The SPEC-004 owner ratifies CON-913 verbatim: eleven permitted names, ten
   required (nine CON-201-required + `cbclPairingRelays`), `pairingRecordRelays`
   optional, `rendezvous` still required.
2. chat.anuna.io serves the profile its own generator produces. It has no
   `rendezvous` member.
3. The wallet runs CON-220 step 4 → complete CON-201 recognition. Under the
   ratified grammar a required member is missing → `UnverifiedApplication`.
   REQ-921 refuses before the relay socket. **No ceremony can start.**
4. Conversely, the profile owner adds `rendezvous` to satisfy the ratified
   grammar. `ApplicationProfile::recognise` now rejects it as an unknown member
   (`closed_members` with an empty optional set). The wallet, the vendored
   browser WASM (`selfsame-web-device` calls the same `recognise`), and
   `gen-production-profile.mjs`'s self-check all refuse. **Still no ceremony.**
5. Either way, REQ-921's "exactly one CON-913 descriptor … reached through
   complete CON-220 recognition" is unreachable — the exact defect 0.4.1 H-1
   named, restated with a different wrong member count.

**Minimum correction.** State the amendment against the grammar that exists and
is intended: profile version 1's permitted set becomes the **nine** names the
recogniser implements, with `rendezvous` and `pairingRecordRelays` **removed**
for the cbcl-pairing cutover and that removal dispositioned in SPEC-007's
Amendment disposition table (which today disposes PROTO-002/PROTO-003 but not
CON-201). Say which PROTO-002/PROTO-003 obligations lose their profile carrier
as a consequence, and reconcile CON-220's tier-3 `pairingRecordRelays` path,
which the removal deletes. Delete "already implemented eleventh member" — it is
the seventh of nine and it replaced two members. See also L-1 on amending
`profileVersion: 1` in place.

---

### H-2 (HIGH) — The presence secret uses the exact encoding of the Selfsame root recovery phrase, in the same wallet, with no separation required

**Exact candidate clause.** `REQ-922:186-188`: "The signed-in application surface
SHALL generate fresh `C` as sixteen CSPRNG octets and render it as the canonical
twelve-word BIP-39 English encoding." `REQ-922:200-204`: "The browser SHALL label
the words: 'Enter these words only in your Selfsame wallet while this chat page
is open. Do not send them to anyone.'" `CON-914:499-501`: "The twelve words
canonically decode exactly those sixteen octets plus BIP-39 checksum."
Cross-repository table: "mandatory twelve-word entry without paste".

**Governing clauses.** SPEC-001 REQ-002 / NFR-002, quoted verbatim in the
custody module the candidate builds on (`src-tauri/src/custody.rs:40-46`):
"**NFR-002 permits exactly one secret to be shown to a human: the twelve words of
REQ-002**, which are the recovery mechanism and are exempt by construction."
Neither SPEC-001 REQ-002 nor NFR-002 appears in the candidate's `amends:` list
(`candidate:8`).

**Code evidence.**

- `crates/selfsame-core/src/derive.rs:66-68`:

  ```rust
  pub fn mnemonic_from_entropy(entropy: &[u8; 16]) -> Mnemonic {
      Mnemonic::from_entropy_in(Language::English, entropy)
  ```

  Sixteen octets, BIP-39, English, twelve words — the same three parameters
  REQ-922 specifies for `C`.
- `crates/selfsame-core/src/derive.rs:76-82` `parse_mnemonic` accepts any valid
  twelve-word English phrase; `:172` asserts the generated phrase has twelve
  words.
- `src-tauri/src/commands.rs:274-278`: `Custody::restore(&phrase, &passcode)` —
  the wallet's restore command takes a twelve-word phrase string.
- `crates/selfsame-core/src/derive.rs:90-95`: that phrase is the input to
  `root_seed`, i.e. the whole identity.

The two values are mutually substitutable as *inputs*: a person's recovery phrase
is a syntactically valid `PresenceCode([u8;16])`, and a `PresenceCode` is a
syntactically valid recovery phrase.

**Failure trace (two directions).**

*Wrong box, root secret.* A page under the person's own real chat origin — or any
page — displays "your Selfsame pairing code is the twelve words on your recovery
card". The person types their **root recovery phrase** into the pairing entry
surface. BIP-39's checksum passes, the phrase decodes to sixteen octets, the
wallet uses it as `C`, CPace fails Finished, and the ceremony burns. The
protocol is unharmed; the root secret has just been typed into a field for which
the candidate specifies no zeroization, no autofill suppression, no distinct
handling, and no telemetry prohibition (TEST-951 checks only that `C` does not
leak into machine channels, not that the *recovery phrase* cannot be captured
by the same surface). `NFR-902`/`SPEC-007 NFR-801` cover carrier bytes, secrets
and frame plaintext in logs; nothing in the candidate extends them to a
mistyped root phrase.

*Right box, trained gesture.* The product now trains every user, on every first
contact with every application, that "a web page shows you twelve words and you
type them into Selfsame" is normal. That is the recovery-phrase attack, executed
by the legitimate product, at scale, with the wallet's own consent screen
providing the reassurance. The candidate's own warning text is what a phishing
page will copy.

**Minimum correction.** Give the presence code an encoding that cannot be
confused with a recovery phrase in either direction, and say so normatively: a
different wordlist, or a different word count with a domain-separated checksum,
or grouped Bech32/base32 with a visible non-word marker. Then require (a) the
pairing entry surface to refuse any input that is a valid twelve-word English
BIP-39 phrase, (b) the pairing and restore surfaces to be visually and
technically distinct with the same zeroization, non-autofill and no-log
handling, and (c) a test row for "recovery phrase typed into the pairing field
is refused before it is used as a password, retained, or logged". Add SPEC-001
REQ-002/NFR-002 to `amends:` or to the coordinated-approval list.

---

### H-3 (HIGH) — CON-919 requires the offered device key to be the chat session signing key, contradicting SPEC-004 REQ-215 and two of the candidate's own requirements

**Exact candidate clause.** `CON-919:628-632`: "The hub derives the account
principal from the session's verified signing key, **checks that the offered
device JWK is that same key**, loads or atomically creates the REQ-925 scope, and
constructs every other field from its authenticated profile and server clock."

**Governing clauses.** `SPEC-004…#REQ-215`
(specs/SPEC-004-application-scoped-identity.md:974-983): "A device SHALL generate
or provision a **distinct** Ed25519 key FOR each `(applicationId, accountScopeId,
device installation)` tuple. It SHALL NOT reuse a device key, device DID,
`cnf.jwk`, or proof nonce namespace between applications or between accounts in
one application." `SPEC-004…#CON-219` (:3963-3971): `deviceKeyJwk` "is the exact
key that CON-205 requires in `cnf.jwk`". REQ-215 is **not** in the candidate's
`amends:` list.

**Internal contradictions in the candidate.** `REQ-925:262-264`: the hub creates
one scope per `(applicationId, accountPrincipalDigest)` "and reuse[s] it for
retries and **later devices**". `REQ-927:324-325`: step 7 completes CON-206/207
"using the **actual retained browser device key**"; step 9 submits "the actual
device key in the existing signed `pairaccountgrant`".

**Code evidence.**

- `cbcl-bus/apps/cbcl_chat/priv/web/enrol-allocator.mjs:60-62`:
  `deviceSeed = randomBytes(32); // device signing seed — retained` — a fresh
  key per ceremony, explicitly retained by the application "because it is the
  private half of the key the wallet's grant binds" (`:46-53`).
- The chat session key is a different, durable key:
  `app.js:903-909` (`HANDLE_KEY_PAIR`, `HANDLE_KEY_PAIR_RECORD`),
  `app.js:1025-1031` (generated once, seed stored in IndexedDB), used to sign
  every hub frame (`cbcl-chat-session-ws.lfe:231-247` `account-binding` →
  `resolve-key` → `verify-and-route`).

**Failure trace.**

1. Read literally, the hub refuses any offer whose `deviceKeyJwk` is not the
   session signing key, so the browser must present its hub-authentication key
   as the CON-205 `cnf.jwk`.
2. That key already authenticates every chat frame in every room. Putting it in
   `cnf.jwk` reuses it across the whole application surface, which REQ-215
   forbids for exactly the pairwise-unlinkability reason it states.
3. REQ-925's "later devices" becomes dead text: a second device has a different
   session key, therefore a different `accountPrincipalDigest`, therefore
   (REQ-925:266-268) a **new account and a new scope**. The scope can never be
   reused for a later device, which is the property REQ-925 asserts.
4. Read the other way — the device key is the fresh retained key REQ-927 names —
   then CON-919's check is simply wrong and the hub binds nothing about the
   device key, which is the gap the check was presumably added to close.

**Minimum correction.** Delete "checks that the offered device JWK is that same
key". State the two keys as distinct roles: the session signing key is the
account principal input and the CON-919 authentication; the device JWK is a
CSPRNG key generated per `(applicationId, accountScopeId, device installation)`
per REQ-215, and the hub binds it by requiring a proof of possession over the
ceremony ID (or by accepting it as application-asserted and saying so, since the
browser *is* the device). Add REQ-215 to `amends:` only if any deviation
remains, and add a test row for two devices under one account sharing one scope
with two distinct device keys.

---

### H-4 (HIGH) — REQ-924's CON-221 applicability determination has no account-keyed authority interface, and the only reachable query returns a false `NoBinding` for an already-bound account

**Exact candidate clause.** `REQ-924:239-244`: "The application determines CON-221
applicability independently from live authority state. `Unknown` refuses.
`NoBinding` requires the application to display the prepared DID fingerprint and
record an explicit comparison action; `BoundSameDid` may report the existing
binding; **`BoundDifferentDid` refuses.**" `CON-921:672-680` defines exactly one
hub command, `selfsamepairaccept`, and no authority-state query.

**Governing clause.** `SPEC-004…#CON-221`
(specs/SPEC-004-application-scoped-identity.md:4190-4193): "Exactly when the
**account authority holds no binding for the authenticated application
account** — that account's first enrollment. The application SHALL determine
this from authority state, not from local cache, and SHALL treat an unreachable
authority as 'unknown' and fail closed rather than assume first use." And
:4226-4229: "**Subsequent enrollments.** No confirmation … a grant naming a
different issuer is rejected under CON-204."

**Code evidence.**

- The only externally reachable authority projection is
  `cbcl-chat-webfinger-http.lfe:20-37`: `GET /.well-known/webfinger?resource=<uri>`
  → `cbcl-chat-webfinger-store:lookup(uri)`. It is **URI-keyed**.
- `cbcl-chat-webfinger-store.lfe:314-336` exports `lookup(uri)` and
  `lookup-binding(uri)` — both URI-keyed. The account-keyed table
  `cbcl-webfinger-account` exists (`:151-170`) but no exported function and no
  route reads it by account for an external caller.
- The core acct URI is a function of the DID:
  `cbcl-core-webfinger.lfe:35-40`
  `account-uri(home-did) = "acct:ss-" ++ base32(SHA-256(home-did)) ++ "@" ++ authority`.
- The wallet's own type has only three states, with no
  `BoundDifferentDid`: `crates/selfsame-app-identity/src/confirm.rs:109-117`
  `AuthorityState::{NoBinding, Bound, Unknown}`, and this is the *wallet's*
  determination for the *wallet's* prompt, not the application's.

**Failure trace.**

1. Victim's chat account `P` already holds a webfinger binding to
   `did:crdt:AAA` from an earlier enrolment (`cbcl-webfinger-account[P] =
   {alias_AAA, AAA}`).
2. A second ceremony runs. `preparation` carries prepared DID `did:crdt:BBB`.
3. The application implements REQ-924 with the only query it has: it computes
   `acct_uri(BBB)` and issues `GET /.well-known/webfinger?resource=acct:ss-<h(BBB)>@chat.anuna.io`.
4. `lookup(acct_uri(BBB))` misses — no alias record exists for `BBB`'s URI. The
   route returns **404**.
5. The application reads that as `NoBinding`, enters the first-enrollment branch,
   displays the fingerprint of `BBB`, and the person confirms two matching hex
   strings. `BoundDifferentDid` never fires.
6. Because CON-221 is now believed satisfied, the application proceeds. The
   protection CON-221 exists to give — "a grant naming a different issuer is
   rejected" on a *subsequent* enrolment — has been converted into a first-use
   comparison the attacker's own `preparation` value trivially passes.
7. REQ-927 step 6 then calls `provision/3`, whose `create-or-return-live`
   (`cbcl-chat-webfinger-store.lfe:151-170`) does refuse the conflicting account
   record — so the ceremony fails late, after the wallet has already published a
   DID, signed a grant and released the payload under REQ-928/REQ-924, and
   compensation (REQ-929) is required. The fail-closed behaviour lives in the
   wrong place and at the wrong cost.

**Minimum correction.** Define, in CON-921, a second authenticated hub command —
`selfsamepairauthority(ceremonyId, accountPrincipalDigest)` — that reads the
account-keyed record and returns exactly `{Unknown | NoBinding | Bound(did)}`,
with an unreachable/erroring authority mapped to `Unknown` and refused. Require
REQ-924's four-way decision to be computed from that command's `Bound(did)`
against the prepared DID, and state explicitly that a webfinger lookup by the
*prepared* DID's URI is not an authority-state query and SHALL NOT be used.
Extend `AuthorityState` with `Bound(did)` in the pure core. Add a test row for
"account already bound to a different DID answers `BoundDifferentDid` before any
fingerprint display".

---

### H-5 (HIGH) — CON-921 mandates the handle-keyed alias primitive that REQ-925 forbids, and no re-keying slice exists

**Exact candidate clauses.** `REQ-925:260-261`: "The human-selected handle is
display metadata and SHALL NOT key scope, **alias**, replay, or ceremony state."
`CON-921:672-679`: the hub command "recomputes the account principal from the
live session, recomputes `acctUri` from `issuerDid`, requires the ceremony's
signed offer and accepted bundle bindings, and **calls
`cbcl-chat-webfinger-store:provision/3` as the application account authority**."

**Governing clause.** `SPEC-004…#CON-204`
(specs/SPEC-004-application-scoped-identity.md:2869-2879): "the **application
account authority** SHALL create an account record keyed by its full normalized
value and bind it to `home_did` … It SHALL reject an alias already bound to
another account or DID."

**Code evidence.**

- `cbcl-chat-webfinger-store.lfe:124` `(defun provision (account supplied-uri home-did)`;
  `:128` `(if (andalso (valid-account? account) (=:= uri expected))`;
  `:339-341` `valid-account?` accepts any 1..4096-octet binary — in every caller
  and test that binary is the chat **handle**.
- `:151-170` `create-or-return-live` writes `cbcl-webfinger-alias` keyed by URI
  and `cbcl-webfinger-account` keyed by `account`; `:206-230` `tombstone` and
  `:295-312` `reinstate` are keyed the same way.
- Every other hub account store is handle-keyed too:
  `cbcl-chat-session-ws.lfe:1667-1679` `do-pairaccountgrant`/`pairaccount-standing-reply`
  key `cbcl-chat-pairaccount:claim`/`grants` by `handle`;
  `:231` `cbcl-chat-account-grants:account-binding handle`.
- The handle is not stable under REQ-925's own principal: `app.js:1048-1057`
  renames the handle whenever the signing key changes, and `freshHandle()`
  (`app.js:909`) mints `@user-<random>`.

**Failure trace.**

1. `selfsamepairaccept` is implemented as CON-921 says, calling
   `provision(handle, acct_uri, issuer_did)`. The alias is now keyed by handle in
   `cbcl-webfinger-account`, which REQ-925 forbids.
2. The person's browser storage partially survives — the seed is intact, the
   `HANDLE_KEY_PAIR` record is not — or an operator renames the handle. Under
   REQ-925 the `accountPrincipalDigest` is unchanged, so the hub reuses the same
   scope, the wallet re-derives the same home DID, and the same `acct_uri`.
3. The new ceremony calls `provision(new_handle, acct_uri, issuer_did)`.
   `create-or-return-live` sees `by-alias = [record for old_handle]` and
   `by-account = []` — the `(list …) (list …)` arm does not match, the `_` arm
   fires, and the result is `#(error refused)` (`:169`).
4. REQ-927 step 6 requires "exact idempotent success", so the ceremony refuses
   after the wallet has published, signed and released. The person cannot
   re-link and cannot repair: `withdraw/2` is also account-keyed, so the old
   handle's binding is unreachable from the new session. The alias is **orphaned
   authority** — precisely the failure mode this review question asks about.
5. Implementing REQ-925 instead — re-key `cbcl-webfinger-account`,
   `cbcl-chat-pairaccount` and the withdrawn-tombstone table by
   `accountPrincipalDigest` — changes the meaning of every existing binding and
   every tombstone, and is named nowhere: the `cbcl-bus hub` row lists
   "account-principal/scope/ceremony tables" (new tables), not a migration of
   the existing ones, and CON-921 explicitly keeps the old primitive.

**Minimum correction.** Choose one and state it. Either (a) re-key the account
authority by `accountPrincipalDigest`: give `provision/3` a principal argument,
name the migration of `cbcl-webfinger-account`, `cbcl-webfinger-withdrawn` and
`cbcl-chat-pairaccount` in the `cbcl-bus hub` slice, and state what happens to
records written under the old key; or (b) keep the handle as the authority key
and amend REQ-925 to say the alias *is* handle-keyed, with an explicit statement
of the orphaning consequence on rename and a required repair path. Add a test
row for "same principal, new handle, existing alias" to TEST-971.

---

### M-1 (MEDIUM) — A public carrier plus a mailbox that terminates on a second claim makes ceremony denial free for any observer, and neither the requirement nor a test acknowledges it

**Exact candidate clauses.** `REQ-922:189-192`: "The public carrier SHALL contain
no `C`, word index, hash usable as a password verifier, CPace frame, or derived
key." `REQ-922:196-198`: "Wrong words, a public-carrier transplant, **a forwarded
QR without the words**, or any context mismatch fails Finished before intent.
**Every failure burns `C`, the invitation, offer, request, and ceremony.**"
`CON-914:487-495`: the carrier carries `locator  direct 32-octet mailbox id` in
the clear. `TEST-952:855-861` requires the forwarded-QR case to fail.

**Governing clause.** `SPEC-007…#REQ-811`
(specs/SPEC-007-cbcl-pairing-cutover.md:314-320): "Failure, decline, **crowding**,
cancellation, expiry, and success leave the invitation consumed." The candidate
retains REQ-811 unchanged (candidate:154).

**Code evidence.**

- `cbcl-pairing/schemas/pairing-v1.cddl:29-32`:
  `claim = { "type": "claim", "locator": locator }` — a claim carries **no
  secret**; the locator alone suffices.
- `cbcl-pairing/src/mailbox.rs:552-577`: a `Claim` on a mailbox that already has
  a claimant returns `Ok(next.terminate(CloseReason::Crowded))` — the mailbox is
  destroyed, not the intruder rejected.
- `cbcl-pairing/src/endpoint.rs:71-95`: `InvitationRecord::bind` burns on any
  mismatch (`self.spend()`), and `consume()` burns on relay termination.

**Failure trace.**

1. The signed-in page displays the QR. Under this design that is safe by
   construction, so it appears on a projector, in a screen share, in a support
   screenshot, or simply within camera range.
2. Any observer decodes the carrier — no secret is needed — connects to
   `relay-origin`, and sends `claim{locator}`.
3. If the observer claims first, the legitimate wallet's `claim` hits
   `mailbox.rs:575` and the mailbox terminates `Crowded`. If the wallet claimed
   first, the observer's claim terminates it. Either way REQ-811 leaves the
   invitation consumed and REQ-922 burns `C`, the offer, the request and the
   ceremony.
4. The person sees an indistinguishable failure and must obtain a fresh carrier
   and a fresh twelve words. The observer repeats at zero cost, with no rate
   limit that applies to them (the REQ-921 limit is claimant-side and
   origin-keyed; the relay limiter is per-connection).

The candidate's own threat framing makes this reachable: it *requires* a
forwarded QR without words to be harmless-but-failing, and the relay's failure
mode for that case is mailbox destruction. NFR-904 reasons about queue bytes and
frame counts but not about who may claim; no TEST row covers crowding.

**Minimum correction.** State the availability consequence in REQ-922 and
ADR-911's residual list. Then bound it: require the v2 carrier to carry an
`expected-claimant-key` commitment (the CDDL already reserves the member,
`pairing-v1.cddl:8`) or a claim token derived from `C`, so a claim without
knowledge of `C` is refused at the relay rather than crowding the mailbox; or
specify that a crowded v2 mailbox is re-allocatable by the allocator without
re-displaying new words. Add a TEST row: "a third party holding only the public
carrier cannot terminate a ceremony".

---

### M-2 (MEDIUM) — The CON-220 step-6 replacement is a pin asserted by the party being authenticated, and the CPace context omits the profile digest the browser already holds

**Exact candidate clause.** `REQ-921:167-172`: "This path has no PROTO-003 CON-409
record, so CON-220 step 6 is amended as follows: the wallet computes the live
profile digest, then requires exact equality with the digest in the later signed
CON-214 offer before display."

**Governing clause.** `SPEC-004…#CON-220`
(specs/SPEC-004-application-scoped-identity.md:4138-4143): "Step 6 is what makes
the fetch trustworthy rather than merely encrypted. TLS authenticates the
origin; the record digest — **asserted by a party holding `C`** — pins *which*
profile that origin served. A host that serves a substituted profile fails step
6." The tier-3 carve-out (:4145-4152) still ends at a CON-409 record from the
other party; it does not license a self-issued pin.

**Code evidence.** The CPace transcript already carries application context but
not the profile digest: `cbcl-pairing/src/context.rs:23-64` `PairingContext::derive`
binds `application` and `relay_origin`; the candidate's `CON-914:504-508` binds
"`carrierDigest`, every carrier field, the resolved mailbox ID, and the v2
domain" and `REQ-922:193-196` binds "the canonical public carrier digest,
`application-context`, relay origin, locator, ceremony ID, profile
`anuna.io/credential/v2`, and both fixed roles" — the credential *profile
identifier* string, not `SHA-256(RFC8785(application profile))`. Meanwhile the
browser is required by CON-201 (:2496-2498) to "embed an authenticated copy" of
the profile, and it does: `cbcl-bus/apps/cbcl_chat/priv/web/app.js:830`
`loadEmbeddedApplicationProfile()`.

**Failure trace.** The replacement pin is signed by `enrollment.requestSigningKeys`,
resolved from the profile the wallet just fetched (CON-214 step 2, :3693-3697).
An adversary who can substitute the profile served to the wallet substitutes the
signing key with it, so the offer verifies against the substituted profile and
the digests match. The check is circular: the party being authenticated supplies
both the artefact and its pin. The practical exposure is narrow — completing the
ceremony still needs `C`, so the attacker must also be the CPace peer — but the
candidate asserts sufficiency ("requires exact equality … before display") rather
than stating the residual, and it leaves an independent pin unused when one is
free.

**Minimum correction.** Bind `SHA-256(RFC8785(embedded application profile))` into
the v2 CPace public context in CON-914 alongside `carrierDigest`. The browser
holds the ratified copy and holds `C`, so the pin becomes an assertion by the
party holding `C` — the property CON-220 step 6 actually asks for — and a
wallet-side substituted profile fails Finished. State the remaining residual
(a compromised backend serving one profile to everyone) explicitly in ADR-914's
neighbourhood.

---

### M-3 (MEDIUM) — "The existing `verify_path_b_peer` acceptance facade" cannot express the required step split, and the boundary does not enforce what CON-920 claims it enforces

**Exact candidate clauses.** `REQ-928:362-366`: "The browser SHALL fetch the
signed closure directly from a declared resolver, replay it locally, and pass
`ClosureSource::StateResolver` to the **existing** `verify_path_b_peer` acceptance
facade." `CON-920:665-668`: "passes facts produced by local replay to the
**existing WASM facade**. A NIF result, hub assertion, unsigned projection, or
**caller-supplied `ClosureSource`** cannot enter this boundary."
`REQ-927:321-325` and `CON-921:682-683`: CON-206 "steps 1 through 8" before the
alias command, "steps 9 through 13" after it.

**Governing clause.** `SPEC-004…#CON-206` (:3122-3166) is one ordered
thirteen-step predicate; step 9 is "the sole gate on alias provisioning".

**Code evidence.**

- `crates/selfsame-web-device/src/lib.rs:1001-1044` `verify_path_b_peer` runs the
  whole predicate in one call (`rehydrate_verified_grant`) and takes the JRD as
  an input. There is no partial-verification entry point, so "run steps 1
  through 8, then provision, then run 9 through 13" is not expressible against
  the existing facade.
- `crates/selfsame-web-device/src/lib.rs:886-985` `verify_path_b_peer_json`
  builds `ResolverObservation` entirely from caller-supplied JSON booleans —
  `did_recomputed_ok`, `deltas_verified`, `locally_closed`, plus caller-supplied
  `assertion_methods` public keys and `also_known_as` (`BrowserClosure`,
  `:353-366`). **No delta is verified inside the module on this path.**
- `crates/selfsame-app-identity/src/path_b.rs:403-430` `issuer_state_of` sets
  `source: ClosureSource::StateResolver` **unconditionally**.

So the `ClosureSource` is indeed not a JSON field — but it is stamped
`StateResolver` regardless of provenance, and every fact that would justify the
label is caller-asserted. A JS layer that fabricates a closure and asserts
`deltas_verified: true` reaches the same accepting path as one that fetched and
replayed a real signed closure. `TEST-969:926-931` ("locally replay the signed
deltas, and **reach `ClosureSource::StateResolver`**") is satisfied vacuously,
because that source is unreachable-to-avoid.

**Failure trace.** Implementers read REQ-928/CON-920 as "no new WASM work beyond
CORS", wire the existing facade, and discover that (a) the alias command cannot
be gated on steps 1-8 without a new entry point, and (b) the mutation corpus in
TEST-973 cannot distinguish a fabricated closure from a resolved one, because the
module never sees deltas. The design's stated property — resolver-sourced,
locally replayed issuer state — is enforced by the browser's own JavaScript, not
by the boundary the contract names.

**Minimum correction.** Name the WASM slice honestly: a split acceptance API
(`verify_path_b_peer_prefix` for steps 1-8 and `verify_path_b_peer_finish` for
9-13, or a facade that takes the JRD lazily), and either move signed-delta replay
inside the module (taking raw closure bytes rather than booleans) or delete the
claim that a caller-supplied source "cannot enter this boundary" and state
plainly that resolver provenance is a JS-layer obligation. Rewrite TEST-969 to
assert something falsifiable: that a fabricated closure with a valid-looking
fact set is refused.

---

### M-4 (MEDIUM) — CON-915 puts high-frequency mutable state inside the record holding the sealed root seed, asserts atomicity the store does not provide, and never specifies the pre-relay passcode/presence ordering

**Exact candidate clauses.** `CON-915:517-521`: "One closed versioned policy
document is sealed with a fresh nonce under the existing passcode-derived custody
wrapping key and stored **inside the hardware-wrapped `SealedRoot` record**."
`CON-915:530-533`: "Read requires successful AEAD opening; write creates a fresh
nonce and **atomically replaces the root record**." `CON-915:527`:
`originAttempts = bounded rolling records used by REQ-921`. `REQ-921:158-163`
requires the origin-attempt record and the carrier burn *before* the fetch, and
`:182-183` "Policy unavailability, **authentication failure**, or write failure
opens no socket."

**Code evidence.**

- `src-tauri/src/custody.rs:98-137` `SealedRoot` holds `sealed_seed`,
  `sealed_hierarchy_root`, `salt`, both nonces, `root_public_key`,
  `backup_confirmed`, `persona`. It is written as one blob under one keychain
  entry (`store::ROOT_ENTRY`, `store.rs:42`).
- `src-tauri/src/custody.rs:312-330` `use_hierarchy_root` requires **both** the
  passcode (Argon2id, 64 MiB × 3 passes, `:60-64`) and
  `store::require_presence("Confirm to use your application identity")`.
- The write primitive is a plain replace: `SelfsameStore::set` — "Store a record,
  **replacing** any previous value under the same key"
  (`crates/tauri-plugin-selfsame-store/src/lib.rs:99-105`) — over Android
  `SharedPreferences` / `keyring`. Neither offers a documented atomic
  compare-and-swap or crash-safe replace.

**Failure trace.**

1. *Unspecified ordering.* REQ-921 requires a sealed read and a sealed write
   before the socket opens. Both need the Argon2id wrapping key and a presence
   check. The candidate's ceremony diagram (candidate:66-90) shows no passcode
   step before "exact (app,relay) policy gate", and CON-918 introduces the
   passcode only at `preview_issuer(authorised_offer, passcode)`. An implementer
   has no guidance on whether the person is prompted once (and the derived key
   cached across the whole ceremony — an unspecified secret lifetime for a value
   that also unwraps the root seed) or three times (scan, preview, commit).
2. *Blast radius.* Every carrier scan, including one from an unknown origin the
   person then rejects, rewrites the record that holds the sealed root seed and
   the hierarchy root. A partial write, a process kill, or a backend that
   truncates loses the identity; recovery is only from the BIP-39 phrase.
   `store::assert_durable` (`store.rs:26-31`) probes a *different* entry at
   startup and cannot detect this.
3. `CON-915:537-539` is candid about the missing rollback counter, which is
   correct — but rollback is not the risk here; destruction is.

**Minimum correction.** Put the policy and `originAttempts` in a **separate**
keychain entry sealed under a key derived from the same wrapping key with a
distinct HKDF label, bound to the root record's version and public key so it
cannot be transplanted; leave `SealedRoot` immutable during pairing. Specify the
passcode/presence ordering normatively: when the person is asked, how long a
derived wrapping key may live, that it is zeroized on every terminal, and that a
rejected-origin prompt must not require the passcode at all (make the
origin-attempt ledger a separate, non-root-record artefact if it must be written
before consent). Add a test row for "a crash during a policy write leaves the
root record readable".

---

### M-5 (MEDIUM) — The inherited hard-stop table is not complete and one of its rows is not truthful

**Exact candidate clauses.** Disposition of 0.4.1 H-6 (`candidate:121`): "The next
section dispositions **every** named SPEC-008 and SPEC-007 stop". Hard-stop table
row (`candidate:130`): "SPEC-008 REQ-902 | **Retained and strengthened.** No
demo/fixture context exists in an ordinary build; v2 uses live profile, signed
offer, real custody, resolver state, and the actual browser device key."

**Governing clauses.** `SPEC-007…#Amendment Channels`
(specs/SPEC-007-cbcl-pairing-cutover.md:1071-1079) names the hard stops:
REQ-803, REQ-804, REQ-805, **REQ-806**, **REQ-807**, REQ-809, REQ-811, NFR-801,
and every production gate. `SPEC-008…#Amendment Channels`
(specs/SPEC-008-production-pairing-claimant.md:92-94) inherits them and "cannot
waive" them. `SPEC-008…#REQ-902` (:139-147): "The claimant SHALL construct its
`SelfsameVerificationContext` exclusively from the person's real stored identity:
the authenticated application profile bytes …, the active account, **the
custody-held device key**, a fresh CON-207 device proof, and **issuer state
resolved through the existing verified webfinger path**."

**Evidence.**

- `grep -n 'REQ-806\|REQ-807' candidate` returns nothing. Both are declared
  SPEC-007 hard stops and both are touched: REQ-806 forbids advertising,
  negotiating, selecting or falling back to a non-`cbcl-pairing` engine, and v2
  introduces a second credential profile alongside v1; REQ-807 requires legacy
  carriers to be rejected "before subsequent network access, key derivation,
  **profile fetch**, or identity action", and REQ-921 introduces a pre-check
  profile fetch. Both are substantively satisfied by REQ-926 and TEST-953/TEST-947
  — which is why omitting them from a table the candidate calls complete is a
  defect of the *table*, not of the design.
- REQ-902's substance does not survive the direction reversal. In v2 the wallet
  is the **issuer**, not the acceptor: it constructs no
  `SelfsameVerificationContext`, holds no device key for this ceremony, makes no
  CON-207 proof, and resolves no webfinger issuer state. The candidate's own
  row substitutes "the **actual browser** device key" — a different party's key —
  for REQ-902's "the custody-held device key" and calls the result "Retained".
  Only the fixture-prohibition half is retained; the context-composition half is
  inapplicable and should say so.
- CON-806 is left ambiguous for the allocator. The hard-stop table
  (`candidate:158`) says CON-806's "Grammar, **selection**, uniqueness,
  release-build loopback refusal, and availability exception remain. Compiled
  evidence eligibility is removed from claimant TOFU admission."
  `CON-913:475-477` says only "For allocator selection, priority/weight and the
  existing approved availability exception remain" — omitting CON-806 step 2
  ("reject descriptors … missing approved conformance evidence"). In v2 the
  allocator is the browser, which has no compiled registry; the shipped
  `eligible()` applies `approved_conformance` to **both** paths
  (`crates/selfsame-app-identity/src/cbcl_relay.rs:83-93`, used by both `select`
  and `verify_invitation_origin`). Whether step 2 survives, and where, is
  undetermined.

**Minimum correction.** Add REQ-806 and REQ-807 rows with their actual
dispositions. Rewrite the REQ-902 row as "Amended: the fixture prohibition and
the compile-time symbol-absence enforcement are retained unchanged; the
verification-context composition clause is inapplicable because v2 reverses the
credential direction — the claimant issues rather than accepts — and the
corresponding obligations move to the browser under REQ-927." State explicitly
whether CON-806 step 2 applies to the browser allocator, and if it does, name the
artefact that carries the approved-conformance set into the browser.

---

### M-6 (MEDIUM) — The 120-second offer window is never reconciled with a ceremony that requires two human decisions, a cross-screen comparison and two Argon2id unlocks

**Exact candidate clauses.** `REQ-923:214-219` makes "validity window" an
exact-equality input across "the invitation, live profile, descriptor, signed
offer, CPace transcript, and display inputs". `REQ-929:392-393`: "Every v2 state
has one fixed deadline no longer than the invitation expiry." CON-219 is in
`amends:` but no clause widens the offer window.

**Governing clauses.** `SPEC-004…#CON-219` (:3969-3971): "`expiresAt` is later
than `issuedAt` by at most 120 seconds." `SPEC-004…#CON-214` step 4 (:3698):
"checks the 120-second window against its local clock."

**Code evidence.** `crates/selfsame-app-identity/src/ceremony.rs:76`
`MAX_OFFER_WINDOW_SECONDS: i64 = 120`, enforced at `:402-406`;
`crates/selfsame-app-identity/src/enrollment.rs:67` `MAX_EVIDENCE_WINDOW_SECONDS
= 120`, step 4 at `:606`. `authorise::authorise` checks `now >=
payload.core.expires_at` once, at `authorise.rs:170`, before display — so today
nothing re-gates the later steps. The relay TTL that bounds REQ-929's deadline is
`ttl-seconds: 60..600` (`cbcl-pairing/schemas/pairing-v1.cddl:24`).

**Failure trace.** Between the offer's `issuedAt` and the payload, v2 requires:
read and approve an exact-intent screen; a passcode entry plus an OS presence
prompt plus one Argon2id derivation at 64 MiB × 3 passes for `preview_issuer`; a
`preparation` round trip; an application authority query and fingerprint render;
a **human hex comparison across two screens**; a final approval; a second
passcode/presence/Argon2id for `commit_issuance`; `issuer::create`; and
publication to every declared resolver. Under 120 seconds this is not a
comfortable budget, and the candidate does not say whether the window is checked
once at authorisation (as the code does) or is a live constraint. If an
implementer re-checks it — a natural reading of "exact equality … for … validity
window" — the ceremony fails on real hardware for real people. If it is not
re-checked, the wallet signs and publishes against enrolment evidence that
expired minutes earlier, which is the exposure CON-220's cache rationale
(:4171-4178) composes the 120-second window against.

**Minimum correction.** State normatively that the CON-214/CON-219 window is
evaluated once, at REQ-923 authorisation, and that REQ-929's per-state deadlines
govern everything after it; or widen the window for v2 as a declared CON-219
amendment with a stated maximum and a stated rationale. Either way, give REQ-929
its actual numbers — the deadline for each of the seven states — rather than
"no longer than the invitation expiry".

---

### M-7 (MEDIUM) — The did-crdt bootstrap leaves an unsigned genesis in the resolver's DAG that the wallet's signed genesis can never replace, so CON-920's "requires a signed closure" is not literally satisfiable

**Exact candidate clauses.** `REQ-928:334-338`: "1. `POST /dids` with the derived
public key; … 3. submit the alias and assertion-method deltas in causal order".
`CON-920:665-666`: "The browser fetches the resolution result with
`includeClosure=true`, **requires a signed closure**, and passes facts produced
by local replay". `TEST-968:918-922`: "Create a DID, add alias/assertion/revocation
deltas, then repeat `POST /dids` … The complete DAG remains byte-identical."

**Code evidence.**

- `did-crdt/src/service/handlers.rs:77-96` `create_did` calls
  `Document::new(&body.public_key_multibase)`, whose creation delta is
  `SignedDelta::unsigned(did, op, timestamp, signer_key)`
  (`did-crdt/src/core/document.rs:345`) — "The returned `SignedDelta` has an
  **empty signature**" (`:304-306`).
- `did-crdt/src/core/delta.rs:363-386` `content_hash` = BLAKE3 over
  `{did, op, parents, timestamp}` — the **signature is not covered**. The
  wallet's `sign_genesis` (`crates/selfsame-core/src/identity.rs:125-136`) uses
  the same op, the same `HlcTimestamp::default()` and no parents, so it produces
  the identical content hash.
- `did-crdt/src/core/document.rs:528-534` `merge_verified_delta` returns `Ok(())`
  immediately when `self.dag.contains(&delta_hash)`. Submitting the wallet's
  signed genesis is therefore a **no-op**; the unsigned one stays.
- `did-crdt/src/service/handlers.rs:472-492` `get_closure` returns
  `doc.closure_bundle()`, i.e. the stored deltas — including the unsigned
  genesis.

**Assessment.** This is *not* fatal, and the good news is worth recording: because
the content hash excludes the signature, the wallet's alias and assertion deltas
parent correctly onto the server's genesis, so the causal chain converges — the
central implementability question for REQ-928 answers **yes**. And
`did-crdt/src/core/validate.rs:122-134` deliberately admits an empty proof value
for a genesis delta on a document with no verification methods, so local replay
succeeds. But CON-920's flat "requires a signed closure" is false for the genesis
delta of every DID bootstrapped this way, and TEST-968's "byte-identical DAG"
will be read as "the signed genesis I submitted is what the resolver holds",
which it is not.

**Minimum correction.** Say what is true: `POST /dids` establishes the genesis
op, whose content hash the wallet reproduces exactly and whose signature the
`did:crdt` admission rule exempts; the closure requirement is that **every
post-genesis delta carries a verifying signature under a materialised method**.
Add to REQ-928 that the wallet SHALL verify the resolver's returned genesis
content hash equals its own before submitting deltas, and add a TEST-968 case
asserting the resolver's genesis content hash equals `sign_genesis`'s.

---

### L-1 (LOW) — Amending `profileVersion: 1` in place is the hazard CON-201's own encoding section exists to prevent

`CON-913:461` amends "CON-201 profile version 1". CON-201 (:2477-2480) says "There
is no forward-compatibility affordance inside a profile; **a new field is a new
`profileVersion`**", and its encoding rationale (:2440-2450) names the exact
consequence: a change that lets "two conforming recognizers disagree about which
profiles are valid even when they computed the same digest". `profileVersion` is
also a signed `offer_core` member (CON-219 :3956) and a PAKE-transcript input via
`profileDigest`. Mint `profileVersion: 2` for the amended member set, or state
why an in-place change to a signed, digested, version-tagged document is safe
here and what a version-1 recogniser built before the amendment is expected to do.

### L-2 (LOW) — `amends:` is both over- and under-inclusive

`candidate:8` lists `CON-209` and `CON-227`; no body clause amends either
(`grep -n 'CON-209\|CON-227'` matches only the frontmatter). Conversely REQ-215
(H-3), SPEC-001 REQ-002/NFR-002 (H-2), and SPEC-002 — the normative authority for
the hex/LifeHash comparison CON-221 cites at :4197-4211 and which TEST-970 asserts
— are affected and unlisted. Reconcile the list with the body.

### L-3 (LOW) — TEST-962 exercises an affordance that does not exist

`TEST-962:900-901`: "Handle rename under that key reuses it." At the reviewed
baseline the handle changes only *because* the key changed
(`cbcl-bus/apps/cbcl_chat/priv/web/app.js:1048-1057`), and `freshHandle()`
(`:909`) mints a random name; there is no same-key rename path. Either name the
rename affordance as new work in the `cbcl_chat browser` slice or drop the row.

### L-4 (LOW) — The final-decline residue statement covers state but not disclosure

`REQ-924:245-249` guarantees hierarchy, resolver, grant, issuance, alias and
connection state are unchanged on final decline. True — and stricter than
CON-221's parent ordering, which has the application learn the issuer only from
an already-signed, already-released bundle (:4198-4201). But `preparation`
irreversibly discloses the derived home DID for `(applicationId, accountScopeId)`
to the counterparty before the person's final decision. It is a pairwise value
and the disclosure is necessary for the comparison to exist, so this is a
residual rather than a defect — but REQ-924 should name it rather than let
"unchanged" read as "nothing happened".

### L-5 (LOW) — The 67,000-octet cap's margin should be computed, not assumed

`NFR-904:446-448` caps the large-class relay body at 67,000. My reconstruction of
the maximal encoding — a 64,512-octet application body inside a v2 payload map,
inside `sealed-plaintext` with a maximal 2,048-octet CBCL control, plus the AEAD
tag and the `sealed-frame` map — lands at roughly 66,830 octets, leaving on the
order of 170 octets of headroom. The requirement to "prove that … the complete
control plus body remains within each outer bound" is the right obligation; state
the computed maximum and the per-layer overheads so the proof is checkable rather
than re-derived by each reader.

---

## Disposition of every 0.4.1 CRITICAL / HIGH / MEDIUM finding

| 0.4.1 finding | 0.4.2's claim | Verified disposition |
|---|---|---|
| **C-1** effects before the final decision | REQ-924/CON-918 permit only a zeroized unsigned preview; everything else follows final approval | **CLOSED.** Verified: `HomeKey::home_did` is pure (`hierarchy.rs:178-182`), `issuer::create` is the first signer (`issuer.rs:134-145`), the CON-918 effect table (candidate:620-626) and TEST-958/959/960/961 are correctly shaped, and the preview/commit re-derivation is deterministic (`hierarchy::derive` + `derive_did` are pure functions of root, application ID and scope). **Declared residual:** the amended REQ-812 permits publication and signature after final approval and before payload delivery; REQ-929 handles the orphan. Acceptable as declared. |
| **C-2** no co-presence binding; comparison value disclosed first; account overclaimed | REQ-922 public carrier + separately transcribed 128-bit `C` in CPace; provenance is `ApplicationSessionAssertion` | **CLOSED as to the correction the prior review demanded** — its own "minimum design correction" option 1 (a person-transcribed value entering the CPace channel identifier) plus option 3 (an explicit ADR residual and a consent-screen statement) are both delivered (ADR-911, REQ-922:202-204, TEST-952). Provenance is now truthful: no `AuthenticatedPerson`, no `claimed_by_secret_holder`, and the account row reads "Application account asserted by \<origin\>". **New defects arising from *how* it was done:** H-2 (the encoding collides with the recovery phrase), M-1 (a public carrier is a free ceremony-kill), M-2 (the profile pin became self-issued). |
| **H-1** undeclared relay profile member | CON-913 "explicitly amends CON-201 with the already implemented eleventh member" | **NOT CLOSED.** See H-1 above. The amendment describes an eleven-name grammar; the recogniser has nine and dropped two CON-201 members; the shipped profile generator classes one of them as legacy. |
| **H-2** wrong bounds / v1 drift | NFR-904 retains 62,000 and every shared bound; v2 uses the existing 64,512 and 4,096 classes | **CLOSED.** Verified against `schemas/pairing-v1.cddl:44,130,135,158`, `channel.rs:21`, `mailbox.rs:13-15`, `bin/cbcl-pairing-relay-ws.rs:32-37`, `crates/selfsame-pairing/src/lib.rs:85` and `cbcl-pairing/src/profile.rs:334`. Arithmetic reproduced below. No shared bound moves; the v1 62,000/62,001/69,632 vectors stand. |
| **H-3** browser cannot resolve issuer state | REQ-928/CON-920 add read-only CORS and require `ClosureSource::StateResolver` | **PARTIALLY CLOSED.** The CORS diagnosis is right (`did-crdt/src/service/server.rs:141-160` has no `CorsLayer`; `cors_headers()` at `src/service/rendezvous.rs:47` is used only in that module) and adding read-only CORS on `GET /:did` and `GET /dids/:did/closure` genuinely unblocks the browser. But the step split is not expressible against the named facade and the boundary does not enforce provenance — M-3. |
| **H-4** alias assigned to the browser | CON-921 assigns provisioning to an authenticated hub command | **PARTIALLY CLOSED.** Right party, right shape: `provision/3` exists with the CON-204 checks already implemented (`cbcl-chat-webfinger-store.lfe:124-170`) and has no non-test caller. But CON-921 mandates the handle-keyed primitive REQ-925 forbids — H-5. |
| **H-5** CON-221 unbound / inverted | REQ-926 makes the application determine applicability; bundle issuer must equal the prepared DID | **PARTIALLY CLOSED.** CON-221 is now in `amends:`; the issuer-equality requirement (REQ-927 step 3, TEST-970) is exactly the missing binding and is correct; the claimant supplies no applicability bit. But the application has no account-keyed authority interface and the reachable one produces a false `NoBinding` — H-4. |
| **H-6** inherited hard stops omitted | A complete SPEC-008 + SPEC-007 disposition table | **PARTIALLY CLOSED.** The table is a real improvement and most rows check out against the parent texts (REQ-907/REQ-908/REQ-809/REQ-811/REQ-814/CON-801/NFR-801 verified). REQ-812 is correctly identified as *not* a SPEC-007 hard stop (:1071-1079), so amending it is in channel. But REQ-806 and REQ-807 — both declared hard stops — are absent, and the REQ-902 row is not truthful — M-5. |
| **M-1** unbounded pre-consent fetch to an attacker origin | REQ-921 adds an unverified-origin prompt before I/O, a burned carrier, one fetch, and a five-origin/24 h limit | **CLOSED in design.** The prompt-before-I/O ordering is the correction the prior review asked for, and TEST-948 tests the negative. The state those controls require is what M-4 concerns. |
| **M-2** policy recognised, not authenticated | CON-915 seals the whole document under the custody wrapping key inside `SealedRoot` | **CLOSED in design.** AEAD-sealed with associated data binding the root-record version and a literal domain; well-formed forgery refuses; TEST-950 tests the forged-but-well-formed case the prior review named; and the absence of a rollback-resistant counter is declared rather than papered over (`CON-915:537-539`) — correct. Placement, atomicity and ordering are M-4. |
| **M-3** incomplete CON-214 predicate | REQ-923 consumes the valid request ID before display, on approval or rejection | **CLOSED.** Verified against CON-214's own step list (:3687-3701): the six steps all run "Before showing consent", so consumption-before-display satisfies step 5's "before any home-key signature, delta publication, or grant-bundle write, whether the request is approved or rejected". It also matches shipped behaviour (`src-tauri/src/app_grant.rs:379-389`). CON-918's table, CON-916's "only after … replay consumption", and TEST-957 (including "failures before step 5 are not [consumed]") are mutually consistent and consistent with CON-214's "the consumed-ID record is the sole permitted mutation after syntactically valid evidence reaches step 5". The declared widening (origin/policy/rate state) is separately consented and argued. |
| **M-4** receipt before hub acknowledgement | REQ-927 waits for the matching `pairaccountgranted`; CON-922 correlates it | **CLOSED in design.** The diagnosis is exact (`app.js:1746-1760` `handlePairAccountGranted` renders a system line and gates nothing; `cbcl-chat-session-ws.lfe:1667-1678` can answer `pairaccount-limit`/`pairaccount-store-failed`). REQ-927 step 9 and CON-922 fix it. The acknowledgement payload must be extended (`cbcl-core-wire:pairaccount-granted`), which the `cbcl-bus hub` slice names. |
| **M-5** work bounds under fixed envelopes | NFR-904 gives exact classes and a worst-case queue budget | **CLOSED.** 2 × 67,000 + 5 × 7,000 + 4 × 8,192 = 201,768 ✓. 536,870,912 / 201,768 = 2,660.86 → "at most 2,660" ✓. Frame counts verified against the two dialects. |
| **M-6** unsized objects | NFR-904 + CON-919 + CON-914 give every bound | **CLOSED.** `preparation` and every control body are exactly 4,096; `applicationAccountDisplay` is 1..128 NFC scalar values; `application-context` is canonical ApplicationId ASCII 1..2048; the envelope size is stated as a number. |
| **M-7** undefined chat-account lifetime | REQ-925 keys scope by the account signing-key fingerprint | **CLOSED in design.** The wire key is a durable seed-backed Ed25519 key (`app.js:885-905, 1025-1040`) so the principal is derivable, and "seed loss ⇒ new account" matches the code exactly (`app.js:1044-1057`). The refusal of silent rotation is right. The alias-keying consequence is H-5; the rename test is L-3. |
| **M-8** no timeout terminal, no compensation | REQ-929 + CON-917 + CON-922 | **CLOSED in design.** Every non-terminal state has an explicit timeout transition driven by a reducer clock input rather than a UI-invented absence; refusal and receipt are mutually exclusive terminals; the compensation ledger is keyed by ceremony/grant ID, is idempotent, and correctly refuses to convert a failed compensation into success. |
| **L-1/L-2/L-3** | durable review cited; CON-220 deviation stated; vectors honestly red | **CLOSED.** The 0.4.1 review exists and is cited by a fully-qualified wikilink; the CON-220 step-6 deviation is stated (its *sufficiency* is M-2); TEST-964 says in terms that it "remains red until the independent implementation includes CPace group math", matching `cbcl-pairing/vectors/README.md`. |

---

## State and effect-order audit

Legend: **HS-1** = preliminary exact-intent approval; **HS-2** = final release approval.

| Effect | Candidate's placement | Verdict |
|---|---|---|
| v2 carrier recognition | pre-everything, zero I/O (REQ-921:155-157, TEST-947) | OK |
| Unverified-origin prompt | before any I/O (REQ-921:157-160) | OK — the M-1 correction, correctly ordered |
| Origin-attempt ledger write + carrier burn | pre-consent, pre-I/O | OK in principle; placement/atomicity/passcode ordering unspecified — **M-4** |
| Live CON-220 fetch | after accept, one fetch, no redirect, no cache | OK; pin sufficiency — **M-2** |
| CON-913 descriptor + exact-pair policy gate | before socket open (REQ-921:174-183) | OK; grammar unimplementable — **H-1** |
| Policy write on accept | pre-consent, separately consented | OK as argued; record placement — **M-4** |
| Mailbox claim / CPace / Finished | pre-consent | OK; observer crowding — **M-1** |
| Signed-offer authorisation (complete CON-214) | pre-display | OK |
| **Request-ID consumption** | at authorisation, before display, on approve or reject | **OK — M-3 closed**, and it matches both CON-214 step 5 and `app_grant.rs:381` |
| Display construction | after `AuthenticatedIntent` only (CON-916) | OK |
| Custody presence + hierarchy derive | after HS-1, inside a zeroizing closure | OK |
| `HomeKey::home_did` + fingerprint | after HS-1, pure | OK — verified pure and deterministic |
| `preparation` send (DID disclosure) | after HS-1, before HS-2 | OK as state; irreversible disclosure unnamed — **L-4** |
| Application authority query | before comparison display | **H-4** — no interface; false `NoBinding` |
| `issuer::create`, grant token, JWS | after HS-2 only | OK |
| DID publication (`POST /dids` + deltas) | after HS-2 only | OK; bootstrap detail — **M-7** |
| Pending issuance write, bundle build | after HS-2 only | OK |
| Reverse payload send | after HS-2 only | OK |
| CON-206 steps 1-8 | on receipt of payload | OK in order; facade cannot split — **M-3** |
| Hub alias command | after steps 1-8, before step 9 | OK in order (CON-204 :2895-2906 permits it); keying — **H-5** |
| CON-206 steps 9-13 + CON-207 | after provisioning | OK |
| Atomic IndexedDB install | after CON-206 | OK |
| `pairaccountgrant` submit **and await** | after install | OK — M-4 closed |
| Receipt | only after the matching acknowledgement | OK |
| Wallet reports `linked` | only after verifying the receipt | OK |
| Compensation | only where a released grant may exist | OK |

**Conclusion.** Final release *is* the unique hard stop before every signature,
publication, issuance write, bundle, alias and payload. The three permitted
pre-final mutations — the CON-214 request tombstone, the separately consented
policy entry, and the origin-attempt record — are each named, each argued, and
two of the three are sanctioned by the parent contracts. This is the single
largest improvement over 0.4.1 and it holds up.

---

## Input-authority and display-provenance audit

| Displayed / decided value | Candidate's source | Real authority at the baseline | Verdict |
|---|---|---|---|
| Application ID / origin | live authenticated profile | CON-220 origin dereference | **Sound** |
| Action / authority summary | `LocalProtocolConstant` | removes the peer control at `cbcl-pairing/src/profile.rs:784-798` | **Sound** |
| Relay origin | carrier + live descriptor + signed hint, three-way equality | genuine | **Sound**, contingent on H-1 |
| Permission set, device DID/JWK/digest, IDs, digests, window | CON-214-signed offer | genuine | **Sound** |
| **Account label** | `SignedApplicationSession`, rendered "Application account asserted by \<origin\>" | the hub authenticates *a* sender's wire key bound to a handle (`cbcl-chat-session-ws.lfe:1662-1668`) | **Sound and truthful.** This is the direct fix for 0.4.1 C-2(b): no wire/backend assertion is promoted to an assertion that the scanning person owns the account, `AuthenticatedPerson` is prohibited, and the UI is required to say the account is application-asserted rather than civil or independently verified. |
| Presence of a co-present person | the separately transcribed 128-bit `C` | CPace over a value that never enters a machine channel | **Sound as bounded**, with two caveats: the deliberate forward-both residual is declared (REQ-922:202-204, ADR-911), and the encoding choice is **H-2** |
| **CON-221 fingerprint** | claimant `preparation`, bound to the accepted grant's `issuer` (REQ-927 step 3) | binding is new and correct | **Sound.** The counterpart still learns the value first — unavoidable in this ordering and no worse than CON-221's parent ordering, where the application learns it from a signed released bundle |
| **CON-221 applicability** | application, from live authority state; claimant supplies no bit | CON-221 assigns it to the application, fail-closed | **Right assignment, unreachable determination — H-4** |
| Relay operator | application-asserted, never rendered as verified (REQ-930) | application-asserted | **Sound and honest** |
| Issuer-state provenance | "`ClosureSource::StateResolver`", "facts produced by local replay" | stamped unconditionally from caller-asserted booleans (`path_b.rs:428`, `lib.rs:886-985`) | **Overstated — M-3** |

---

## Implementability audit

### `cbcl-pairing` (`8e55160`)

| Slice | Feasible? | Evidence |
|---|---|---|
| v2 carrier type + `PresenceCode([u8;16])` | Yes | The v1 decoder is a closed CBOR recogniser; a v2 type with no `secret` member is additive. |
| v2 context / domains / CDDL / dialect | Yes | `PairingContext::derive` (`src/context.rs:23-64`) already carries `application` and `relay_origin`; the public context is local-only (`src/channel.rs:95-104`). Adding the profile digest for M-2 is a one-line context extension. |
| Seven-performative reducer + role casts | Yes, sizeable | `SessionPerformative` has exactly four variants (`src/cbcl_protocol.rs:97-106`) and the session dialect exactly four `extend` forms with a three-edge `(then …)` chain; v2 needs seven with new casts. `apply_intent`/`apply_decision`/`apply_payload` hard-code direction (`src/endpoint.rs:982,1010,1043`), so the reverse payload is a new arm, not a flag. |
| Fixed 64,512 / 4,096 body classes | Yes | `application-payload.body` is already `1..64512` and `sealed-plaintext.body` `1..65536`. |
| Frame budget | Yes | Bootstrap is exactly four frames (`dialects/blind-pairing-bootstrap-v1.cbcl`); per-membership maxima are 5 (allocator) and 6 (claimant) against `MAX_FRAMES_PER_MEMBERSHIP = 16`. |
| Public-carrier claim protection | **No, as specified** | `claim` carries no secret (`schemas/pairing-v1.cddl:29-32`) and a second claim terminates the mailbox (`src/mailbox.rs:575`) — **M-1**. |

### `selfsame` core / adapter (code tree `c6af6df`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Split `preview_issuer` / `commit_issuance` | Yes | `HomeKey::home_did` is pure (`hierarchy.rs:178-182`); `issuer::create` is the first signer (`issuer.rs:134-145`); `use_hierarchy_root` already runs a caller closure and zeroizes (`custody.rs:312-330`). |
| Deterministic preview ↔ commit equality | Yes | `derive_did` = `Document::new(key_multibase(pk)).did`; same root + application + scope ⇒ same key ⇒ same DID. |
| Resolver publication adapter | Yes, new | No publish function exists today (`crates/selfsame-app-identity-net/src/state.rs` exports `resolve_closure`, `submit_revocation`, `confirm_revocation`); `POST /dids/:did/deltas` exists. Genesis content-hash agreement verified — see M-7. |
| Four-way `AuthorityState` | Yes, additive | `confirm.rs:109-117` has three variants; `Bound(did)` is a small change. |
| Sealed policy in custody | Yes, with the M-4 corrections | `SealedRoot` is a serde struct with `#[serde(default)]` precedent; the concern is placement and write atomicity, not cryptography. |
| CON-913 profile grammar | **No, as specified** | H-1. |

### `cbcl-bus` hub and browser (`fdbe414`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Server-built signed offer | Yes, real work | `sign-under-open-gate` is already profile-bound and refuses an undeclared `kid` (`cbcl-chat-enrolment-signer.lfe:60-107`); the statement construction currently lives in the browser WASM (`enrol-allocator.mjs:74-80`) and must move hub-side. The `/selfsame/enrolment/sign` route's own comment records the oracle posture (`cbcl-chat-selfsame-application-http.lfe:60-77`), and CON-919 correctly forbids reusing it. TEST-963 is the right row. |
| Account principal from the session | Yes | The chat wire key is Ed25519, seed-backed and durable (`app.js:885-905`), and every frame is verified against it (`cbcl-chat-session-ws.lfe:231-247`). |
| Device-key binding | **No, as specified** | H-3. |
| Alias command | Partly | `provision/3` exists with CON-204's checks (`cbcl-chat-webfinger-store.lfe:124-170`) and no caller — but keyed by handle: H-5. |
| Authority-state query for CON-221 | **Missing** | H-4. |
| Correlated acknowledgement | Yes, new payload | `pairaccount-standing-reply` builds the reply from `handle`/`fps`/`newfp` (`:1686-1696`); CON-922's added fields require a wire/NIF change, which the hub slice names. |
| v2 allocator ABI | Yes, breaking | `CbclAllocatorSession`'s constructor takes `bundle: Uint8Array` (`selfsame_web_device.d.ts:36`); the candidate names "v2 allocator ABI without pre-staged grant" — correctly, since this is a published wasm-bindgen ABI break. |
| Direct closure fetch + full CON-206/207 | Partly | Needs did-crdt CORS **and** the facade split and provenance work of M-3. |

### `did-crdt` (`1f409a4`)

| Slice | Feasible? | Evidence |
|---|---|---|
| Read-only CORS on public resolution | Yes | `build_router` (`src/service/server.rs:141-160`) has no `CorsLayer`; `cors_headers()` exists in `src/service/rendezvous.rs:47` as the pattern to follow. `GET`/`HEAD`/`OPTIONS` only, no credentials, no write routes — as CON-920 specifies. |
| Idempotent non-destructive create | Yes | `create_did` today does `state.docs.lock().insert(did, doc)` (`src/service/handlers.rs:98`) — an unconditional overwrite. The candidate's diagnosis is exact and the fix is a presence check plus a genesis-derivation comparison. |
| Causal chain after bootstrap | Yes | `content_hash` excludes the signature (`src/core/delta.rs:363-386`), so the wallet's alias delta parents onto the server's genesis. See M-7 for the unsigned-genesis wording. |

---

## Bounds and work audit

Every number below was recomputed from the CDDL and the constants, not taken
from the candidate.

**Retained outer bounds — all verified unchanged.**

| Layer | Candidate | Baseline |
|---|---:|---|
| v2 credential object | 62,000 | `crates/selfsame-pairing/src/lib.rs:85`, `cbcl-pairing/src/profile.rs:334,920` |
| application body | 64,512 | `schemas/pairing-v1.cddl:158` |
| sealed-plaintext body | 65,536 | `schemas/pairing-v1.cddl:135` |
| sealed plaintext | 69,556 | `cbcl-pairing/src/channel.rs:21` |
| sealed ciphertext | 69,572 | `schemas/pairing-v1.cddl:130`; `channel.rs:289` accepts `17..=MAX_SEALED_PLAINTEXT+16` |
| relay frame body | 69,632 | `cbcl-pairing/src/mailbox.rs:15`, `schemas/pairing-v1.cddl:44,78` |
| WebSocket message | 70,000 | `cbcl-pairing/src/bin/cbcl-pairing-relay-ws.rs:32` |

**Encoder chain at the maximum (large class).** 64,512-octet body + the v2
payload map's `type`/`intent-digest`/`payload-type` members ≈ 64,700; inside
`sealed-plaintext` with a maximal 2,048-octet control ≈ 66,770; + 16-octet AEAD
tag ≈ 66,786; + the `sealed-frame` map ≈ 66,830 ≤ 69,632; the enclosing `put`
≈ 66,860 ≤ 70,000. **Every bound holds, with roughly 170 octets of slack against
the candidate's own 67,000 cap** (L-5). Control class: 4,096-octet body ⇒
sealed-frame ≈ 6,220 ≤ 7,000 ✓. Bootstrap: `control` is `1..2048` and `value` is
64 octets, so ≈ 2,150 ≤ 8,192 ✓.

**Frame count.** REQ-926's projection yields 2 large + 5 control application
frames; the bootstrap dialect (`dialects/blind-pairing-bootstrap-v1.cbcl`) has
exactly `cpace-a`, `cpace-b`, `finished-a`, `finished-b` = 4. Per-membership:
allocator 5, claimant 6, both below `MAX_FRAMES_PER_MEMBERSHIP = 16` and within
`seq: 0..15` ✓ — the candidate's "no membership reaches the sixteen-frame
ceiling" is correct.

**Work budget.** 2 × 67,000 + 5 × 7,000 + 4 × 8,192 = 134,000 + 35,000 + 32,768 =
**201,768** ✓. 512 MiB = 536,870,912; 536,870,912 / 201,768 = 2,660.86 ⇒ **"at
most 2,660"** ✓. `DEFAULT_MAILBOX_CAP = 10,000` × 201,768 ≈ 2.02 GB, so the queue
binds first and the candidate's "the 10,000-mailbox cap remains independently
enforced" is right. Queue accounting adds on `Put` and subtracts on
acknowledgement/removal (`mailbox.rs:640-646, 679-693`), so "delete
acknowledged/terminal bodies" is real behaviour, and `Stored { replay: true }`
(`mailbox.rs:612`) makes "exact retries reuse a sequence and do not add a body"
accurate. No shared constant, CDDL bound, v1 vector or relay queue constant
changes, so `SPEC-007 NFR-802` ("Relay message, mailbox, lifetime,
recognition-work, and unrelated-work limits SHALL equal the exact pinned
`cbcl-pairing` specification and assets") is satisfied: the new caps are
encoder-side, not relay-side.

**Not a bounds problem but a work problem the section misses:** nothing in
NFR-904 bounds who may *claim* a mailbox, which is M-1.

---

## Tests and gates audit

| Row | Verdict |
|---|---|
| TEST-947 zero-I/O recognition | Sound; panicking doubles are the right instrument. |
| TEST-948 origin prompt / burn / rate limit | Sound; correctly tests the negatives. |
| TEST-949 pair scope | Sound and load-bearing; the removed-descriptor case is the right addition. |
| TEST-950 forged policy | Sound — tests the *well-formed forged* document 0.4.1 M-2 asked for, and records the absent rollback counter rather than asserting one. |
| TEST-951 words never in machine channels | Sound as far as it goes; **does not** cover the recovery-phrase confusion of H-2 in either direction. |
| TEST-952 co-presence / wrong-account corpus | Strong. Directly answers 0.4.1 C-2's missing row. **Missing:** a third party holding only the public carrier terminating the ceremony (M-1). |
| TEST-953 transplant matrix | Strong, including v1↔v2 replay. |
| TEST-954/955/956 offer, mutation, prohibited display | Strong. TEST-956's compile-time/symbol rows are the right shape. |
| TEST-957 CON-214 consumption | Correct, and correctly distinguishes failures before step 5. |
| TEST-958/959/960/961 effect gate | The best rows in the document. TEST-961's "mutate the preview DID between preview and commit and require refusal before the first signature" is exactly right. |
| TEST-962 scope follows the key | Sound except the rename case — L-3. |
| TEST-963 no signing oracle | Strong; "no call edge to `/selfsame/enrolment/sign`" is checkable. |
| TEST-964 independent vectors | **Honest and correctly red.** Matches `cbcl-pairing/vectors/README.md`. |
| TEST-965 reducer terminals | Sound and complete for wrong sender, reorder, duplicate, exact retry, refusal, close, expiry and silence. |
| TEST-966 exact bounds | Sound; re-running the v1 62,000/62,001/69,632 vectors unchanged is the right guard. |
| TEST-967 relay work | Sound. |
| TEST-968 idempotent create | Sound in intent; "the complete DAG remains byte-identical" will read as asserting a signed genesis the resolver does not hold — M-7. |
| TEST-969 browser resolver path | **Partly vacuous.** `ClosureSource::StateResolver` is stamped unconditionally (`path_b.rs:428`), so "reach `ClosureSource::StateResolver`" cannot fail. The CORS and refusal halves are sound — M-3. |
| TEST-970 CON-221 | Right rows, including the mutated-issuer refusal. **Not runnable** without the authority interface of H-4, and `BoundDifferentDid` cannot be produced. |
| TEST-971 alias authority | Right rows. Missing "same principal, new handle, existing alias" — H-5. |
| TEST-972 full CON-206/207 | Blocked on M-3's facade split; otherwise correct. |
| TEST-973/974/975 acceptance, acknowledgement, post-payload | Strong. TEST-974's delay/drop/duplicate/substitute matrix is the right answer to 0.4.1 M-4; TEST-975's "never releases the same grant ID twice" is the right invariant. |
| TEST-976 fixture and prohibited-production absence | Strong; `local_demo`, caller-octet signer, compiled registry and v1 fallback are all checkable symbol/edge assertions. |
| TEST-977 local E2E | Correctly gated behind the production hold. |

**Missing rows.** Recovery-phrase confusion in both directions (H-2);
carrier-observer mailbox crowding (M-1); an account already bound to a different
DID (H-4); same principal with a new handle (H-5); a fabricated closure with a
valid-looking fact set (M-3); a crash during a policy write (M-4); the offer
window elapsing between authorisation and final approval (M-6).

**Gates.** The sequencing (candidate:1005-1022) is disciplined and correctly
ordered — review, then failing pure-core tests and vectors, then pure types, then
effectful shells, then custody/resolver before wallet issuance, then hub before
browser. It now names the CORS and reproducible-WASM gates the prior review said
were missing. The production statements are truthful and verifiable:
`PRODUCTION_ALLOCATION_ENABLED = false` (`crates/selfsame-pairing/src/release.rs:11`)
with `tests/spec008_hold_invariance.rs:16` and `tests/rollback_hold.rs:13`
asserting it, and REQ-931 explicitly refuses to be its enablement path — which
satisfies SPEC-008 REQ-907 and does not disturb SPEC-007 REQ-809. The "Explicit
open production gates" section is accurate on every item I could check, including
the still-absent human cryptography review and the absent independent CPace
implementation. **Human-review and production gates are truthfully represented.**

---

## Residual-risk audit

Recorded whether or not the verdict were PASS, because a corrected 0.4.3 would
carry these forward:

1. **Deliberate forwarding of both channels.** Declared in the Outcome section
   and REQ-922, and mitigated only by UI text and the application-asserted
   account label. This is the honest cost of standalone first contact and I
   would accept it as stated — *provided* H-2 is fixed, because the mitigation is
   a human reading a warning next to a twelve-word entry field.
2. **Application-declared relay use is not operatorship.** REQ-930 and ADR-914
   are accurate, correctly scoped, and refuse to dress `operatorId` as proof.
   Accept as stated.
3. **Publication after final approval, before payload.** The amended REQ-812
   permits a permanently resolvable DID to exist when the payload never lands.
   Bounded by human final approval, by scope stability (no new DID on retry), and
   by REQ-929's orphan rule. Accept as declared.
4. **Home-DID disclosure in `preparation` before final approval.** Pairwise, and
   necessary for the comparison. Accept, but name it (L-4).
5. **No rollback-resistant counter on the relay policy.** Declared
   (`CON-915:537-539`). Accept as stated.
6. **The application verifies its own acceptance.** CON-206 in the browser is the
   application checking a grant it will use; the wallet's `linked` rests on the
   application's receipt. Inherent to the architecture, but the candidate's
   provenance language (M-3) should not imply more.
7. **Two-thousand-six-hundred concurrent ceremonies per relay.** A real capacity
   reduction from the fixed opacity classes, correctly computed and declared. The
   trade (traffic-analysis opacity for concurrency) is the right one to have made
   explicitly; record it in the operator-facing notes.

---

## Method

Every claim above was checked against the confirmed revisions rather than against
the candidate's summary, its disposition table, or source comments. Where a
comment and the behaviour disagreed I took the behaviour: `create_did`'s
"non-destructive" reading was rejected in favour of the `insert` at
`handlers.rs:98`, and `verify_path_b_peer_json`'s "resolver facts originate in the
browser's own resolver path" doc line was rejected in favour of the
caller-supplied `BrowserClosure` booleans it actually consumes.

Baselines confirmed at review time by `git log -1` and `git status` in each
repository, and by `git diff --stat c6af6df..HEAD -- ':!specs'` in the Selfsame
worktree to establish that the candidate commit changes no code.

Governing text read in full: SPEC-004 CON-201, CON-204, CON-206, CON-207,
CON-209 headings, CON-211, CON-214, CON-219, CON-220, CON-221, REQ-215, REQ-216,
and the SPEC-007 cutover disposition; SPEC-007 REQ-803 through REQ-814, NFR-802,
CON-801, CON-804, CON-806, the Amendment disposition table, the inherited
SPEC-004 Tier-1 gate ledger, the Production gate, and the Amendment Channels;
SPEC-008 REQ-902 through REQ-908, CON-903, ADR-902, and its Amendment Channels;
and the complete 0.4.1 review.

Code paths traced end to end: `ApplicationProfile::recognise`/`from_value`/
`cbcl_pairing_relays`/`TOP_LEVEL_REQUIRED` and `cbcl_relay::{select,
verify_invitation_origin, eligible, is_loopback}`; `HomeKey::{home_did,
public_key}`, `hierarchy::derive`, `selfsame_core::identity::{derive_did,
bootstrap, sign_genesis}`, `issuer::create`, `Custody::{use_hierarchy_root,
read, forget}`, `derive_wrapping_key`, `SealedRoot`, `store`/
`tauri-plugin-selfsame-store`; `authorise::authorise`, `replay::consume`,
`prepare_issuance`, `confirm_issuance`, `assemble_claimant`,
`Applicability::decide`, `confirm::AuthorityState`; `selfsame-app-identity-net`
`PathBClosureFacts`; `verify_path_b_peer`, `verify_path_b_peer_json`,
`BrowserClosure`, `issuer_state_of`, `ClosureSource`; `derive::{mnemonic_from_entropy,
parse_mnemonic, root_seed}` and `commands::restore`; the cbcl-pairing invitation
and frame CDDL, `PairingContext::derive`, `channel::MAX_SEALED_PLAINTEXT`,
`mailbox::{MailboxCommand, apply, queue accounting, MAX_FRAMES_PER_MEMBERSHIP,
MAX_FRAME_BODY_BYTES}`, the relay WS constants, both CBCL dialects,
`SessionPerformative`, `InvitationRecord::{new, bind, consume}`, and
`profile.rs`'s display path and 62,000-octet bound; `did-crdt`'s `build_router`,
`create_did`, `resolve_did`, `get_closure`, `Document::{new, merge_verified_delta,
merge_state}`, `SignedDelta::{new_genesis, new_with_parents, content_hash,
compute_signing_input}`, `validate::verify_signature`, and `rendezvous::cors_headers`;
and in cbcl-bus `enrol-allocator.mjs`, `app.js` identity/install/acknowledge paths,
`selfsame_web_device.d.ts`, `cbcl-chat-session-ws.lfe` frame verification and
`do-pairaccountgrant`, `cbcl-chat-webfinger-store.lfe`, `cbcl-core-webfinger.lfe`,
`cbcl-chat-webfinger-http.lfe`, `cbcl-chat-enrolment-signer.lfe`,
`cbcl-chat-selfsame-application-http.lfe`, and `scripts/gen-production-profile.mjs`.

Arithmetic in the bounds section was recomputed from the CDDL member sets and the
named constants; the 201,768-octet budget, the 2,660-ceremony figure, the
per-membership frame counts and the encoder chain at the maximum were each
derived independently before being compared with the candidate's numbers.

No implementation code, no candidate amendment text, and no other file was
modified by this review.
