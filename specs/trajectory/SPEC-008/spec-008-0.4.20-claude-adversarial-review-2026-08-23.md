---
id: SPEC-008-review-0.4.20-claude-2026-08-23
title: SPEC-008 0.4.20 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.13 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy]]"
prior-review: "[[spec-008-0.4.19-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.20 + cbcl-pairing 0.4.8 + cbcl-bus 0.16.13 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session started. I
recorded the gate result first, and printed no secret value.

```text
$ if [ -n "${ANTHROPIC_API_KEY+x}" ]; then echo PRESENT; else echo ABSENT; fi
ABSENT
$ env | grep -c ANTHROPIC_API_KEY
0
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "orgName": "<redacted>",
  "orgId": "<redacted>",
  "email": "<redacted>",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai` and the reported `subscriptionType` is `max`. Account identifiers
are redacted above and appear nowhere in this report.

The reviewing model is Claude Opus 5 in a fresh context. This session called no
Anthropic API and no external web service. Every input came from the four local
repositories, the local cbcl-rs checkout, and the local OTP install. Separate
sessions authored the three candidates. This session read the candidates and the
source, and never the generator reasoning.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `1728473ce9c2afe9da97b42bfe1da32f085ec796` | candidate 0.4.20-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `383d6cea013e12e429f0c65032401411b9c56a93` | candidate 0.4.8-draft |
| cbcl-bus | `2b5c7f22e605dab2887f732123f75ada35e5f703` | candidate 0.16.13-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The sibling working trees carry user-owned uncommitted
paths. I read those repositories only through `git show COMMIT:PATH`, and I
changed nothing in them. The local OTP install is `28.5` with `mnesia-4.25.3`.

I read the complete effective amendment chain from 0.4.4 forward. I also read
the 0.4.19, 0.4.18, 0.4.17, 0.4.16, and 0.4.15 reviews and both coordinated
candidates. I read [[PROTO-002-selfsame-rendezvous-v1]] and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]].

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 2 | N5-H-1, N5-H-2 |
| MEDIUM | 1 | N5-M-1 |
| LOW | 2 | N5-L-1, N5-L-2 |

Verdict: **REJECT**.

Three of the six 0.4.19 findings close cleanly against real code. The pre-derived
alias reaches `tombstone/3` in both operation lists. Reinstatement needs only the
whole withdrawal write lock, and I verified that at the local lock manager. The
WebFinger busy terminal now names its result, its atom, and its HTTP status.

The rejection rests on two clause-scope defects and one hub ledger. The
retirement in
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#CON-979]]
widens its own resolver to numbered list items and then reaches four of seven
such lists. Ten effective clauses from 0.4.4 through 0.4.8 still state competing
survivor, hub, and coordinated ledgers. Separately
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#NFR-922]]
replaces a whole-graph concurrency control with a four-function one, and its
replacement text states an owner set the reviewed code contradicts.

---

## Findings

### N5-H-1 (HIGH, release-blocking) — the retirement widens the resolver to list items and then reaches four of seven lists, so ten effective clauses still state competing ledgers

**Repository owner: selfsame (CON-979, and the untouched 0.4.4 through 0.4.8
clauses).**

`REQ-999` at
`specs/trajectory/SPEC-008/spec-008-0.4.20-complete-authority-retirement-and-observable-busy.md:42-44`
widens the audit scope past its predecessor:

> The scan SHALL cover numbered clauses, numbered list items, and unnumbered
> normative paragraphs. A heading or changelog does not make conflicting
> normative text historical.

The predecessor at
`spec-008-0.4.19-singular-authority-and-webfinger-order.md:35-36` named only
numbered clauses and unnumbered normative paragraphs. Numbered list items are new
scope, and `CON-979` at `:145-158` acts on that scope for exactly four documents.

I checked all seven retired test preambles and all four retired sequencing items
against their targets. Every start boundary is paragraph-initial and unique inside
its named document, and every end boundary terminates that same paragraph. The
`CON-970` retirement at `:160-162` resolves the same way. The retirement work is
correct as far as it reaches.

The defect is again the reach. Three earlier documents carry the same numbered
sequencing list, under the heading `## Gate and sequencing` rather than
`## Implementation and gate sequencing`. Their item 1 states a coordinated review
set and no clause retires it.

| Location | Stated coordinated review set |
|---|---|
| `spec-008-0.4.4-standalone-tofu-first-contact.md:1514-1519` | this revision, cbcl-pairing 0.3.0 at `7ddea48`, four code baselines |
| `spec-008-0.4.5-standalone-tofu-first-contact.md:658-662` | Selfsame 0.4.4 and 0.4.5, cbcl-pairing 0.3.0 and 0.4.0 |
| `spec-008-0.4.6-standalone-tofu-first-contact.md:475-480` | Selfsame 0.4.4/0.4.5/0.4.6, cbcl-pairing 0.3.0/0.4.0/0.4.1 |
| `CON-979` at `:189-191` | this amendment, cbcl-pairing 0.4.8, cbcl-bus 0.16.13, did-crdt |

**The retirement takes item 1 only, and item 2 of the same lists states a test
set.** `CON-979` at `:145` reads "This contract also retires item 1 from four
implementation-sequencing lists". Four surviving item-2 entries name a companion
range, and one also names a cbcl-bus range.

| Location | Stated companion set | Stated cbcl-bus set |
|---|---|---|
| `spec-008-0.4.5-standalone-tofu-first-contact.md:664-665` | TEST-030 through TEST-047 | — |
| `spec-008-0.4.6-standalone-tofu-first-contact.md:482-483` | TEST-030 through TEST-050 | — |
| `spec-008-0.4.7-standalone-tofu-first-contact.md:359-360` | TEST-030 through TEST-051 | — |
| `spec-008-0.4.8-standalone-tofu-first-contact.md:464-466` | TEST-030 through TEST-052 | TEST-043 through TEST-047 |
| `CON-978` at `:242-245` and `CON-979` at `:179-181` | TEST-030 through TEST-049, TEST-050 in part, TEST-051 through TEST-056, TEST-059 | TEST-043 through TEST-065, TEST-068, TEST-070, TEST-093, TEST-094, TEST-098, TEST-103, TEST-104 |

**One effective requirement states both a conformance set and a survivor set.**
`REQ-963` at `spec-008-0.4.6-standalone-tofu-first-contact.md:91-102` is never
replaced. I searched the whole chain for its identifier and found only its own
heading and one disposition row. It reads:

> Credential/v2 SHALL conform to the complete cbcl-pairing 0.3.0, 0.4.0, and
> 0.4.1 coordinated set.

Its closing sentence at `:102` reads:

> All companion TEST-030 through TEST-050 are release-blocking.

That is a normative SHALL over a superseded cbcl-pairing set. It is also a full
TEST-050 claim inside a live requirement.

**The singular TEST-050 claim is false against the chain.** `CON-979` at
`:164-165` reads:

> The retirements above remove every earlier sentence that states TEST-050 in
> full. The exception in CON-978 is the sole current TEST-050 status.

Three effective sites contradict that. `REQ-963` at `:102` and the 0.4.6 item 2 at
`:483` both state TEST-050 without the exception. `TEST-1073` at
`spec-008-0.4.6-standalone-tofu-first-contact.md:447-452` is worse, because it
mandates the retired witness itself:

> Run companion TEST-050 and assert all six NFR-908 totals. Include control
> lengths 1, 23, 24, 255, 256, and 4,096 …

`TEST-1073` survives under the current cbcl-bus-set sentence, which keeps every
effective Selfsame test through TEST-1108. Three effective obligations contradict
it. `CON-978` at `:243-244` retires TEST-050's positive witness. Selfsame
`TEST-1091` at `spec-008-0.4.8-standalone-tofu-first-contact.md:450-455` requires
rejecting any effective text restoring a 4,096 positive control witness.
Cbcl-pairing `TEST-052` at `spec-001-0.4.3-migration-display-and-test-replacement.md:112-115`
requires refusing 4,096 and asserts that no effective positive row still names it.
The effective `NFR-908` control range is `256..2,048` after
`spec-008-0.4.7-standalone-tofu-first-contact.md:251-252` replaced it.

**The candidate therefore fails its own new check.**
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#TEST-1145]]
at `:273-282` requires a traversal over every effective numbered clause, list
item, and unnumbered normative paragraph from 0.4.4 onward. It requires exactly
one cbcl-pairing survivor set, cbcl-bus test set, Selfsame correction set, and
coordinated review set. It also requires the TEST-050 exception and rejects any
full TEST-050 survivor claim. Ten effective sites defeat that traversal.

I verified the ledger roles that do close. The Selfsame correction set and the
coordinated pointer set each resolve to exactly one current value. I traced every
earlier pointer paragraph and every earlier correction-set sentence to a
replacement or a retirement.

**Minimum sound correction.** Retire the 0.4.4, 0.4.5, and 0.4.6 sequencing item 1
entries by name. Retire item 2 of the 0.4.5, 0.4.6, 0.4.7, and 0.4.8 lists. Retire
or restate the two `REQ-963` sentences and the `TEST-1073` control-length
sentence, so no effective text states TEST-050 in full.

---

### N5-H-2 (HIGH, release-blocking) — the replacement narrows bounded retry from the whole graph to four WebFinger functions, and names an owner set the code contradicts

**Repository owner: selfsame (NFR-922) and cbcl-bus (NFR-020).**

`NFR-922` at
`spec-008-0.4.20-complete-authority-retirement-and-observable-busy.md:87-88`
identifies its third replacement:

> The third replacement starts with "Every affected transaction runs at Mnesia
> level 1". It ends with "No partial effect survives."

Both boundaries resolve uniquely, and the replaced paragraph at
`spec-008-0.4.19-singular-authority-and-webfinger-order.md:152-155` is the whole
of the chain's bounded-retry control. The replacement text at `:90-92` reads:

> The public transaction owners are `provision/3`, `withdraw/2`, `reinstate/2`,
> and `lookup/1`. Each runs at Mnesia level 1 with an explicit integer
> `Retries >= 2`. Nested transactions are prohibited.

**That sentence is false about the reviewed code.** A repository search at the
declared cbcl-bus baseline returns 70 production `mnesia:transaction` call sites
under `apps/`, in 20 modules. Four of them are the WebFinger owners. The rest
include every writer the amendment's own cycle set names.

| Module | Production transaction sites | Role in the twelve cycles |
|---|---:|---|
| `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe` | 11 | `G`, `F`, `XR`, `OR` |
| `apps/cbcl_chat/src/shell/cbcl-chat-account-roommember.lfe` | 6 | `write-grant`, `revoke-handle-tx` |
| `apps/cbcl_chat/src/shell/cbcl-chat-roommember.lfe` | 5 | `write-grant`, `redeem-and-grant` |
| `apps/cbcl_chat/src/shell/cbcl-chat-invite.lfe` | 2 | `redeem-tx` |
| `apps/cbcl_chat/src/shell/cbcl-chat-members.lfe` | 2 | `ME` |
| `apps/cbcl_chat/src/shell/cbcl-chat-pairaccount.lfe` | 3 | `PC` |
| `apps/cbcl_chat/src/shell/cbcl-chat-sealedgrant.lfe` | 2 | `put-tx`, `get-tx` |
| `apps/cbcl_bus/src/store/cbcl-store-migrate.lfe` | 1 | migration graph |

`bind-and-store-verified-device-grant/8` opens its transaction at
`cbcl-chat-account-grants.lfe:84`. `revoke-observed/2` opens one at `:293` and
`revoke-device-grant/2` opens one at `:310`. Those are grant creation, observed
revocation, and exact revocation.

**Under the narrow reading the authority lane loses a control.** I traced the
predecessor chain and every earlier statement of the rule is replaced. `NFR-915`
at `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:249-254` made
`transaction/1` non-conforming. `NFR-917` at
`spec-008-0.4.15-authenticated-allocation-and-consent.md:223-228` added level 1,
`Retries >= 2`, and the `mnesia_tm:try_again/1` rationale. `CON-972` item 4,
`CON-974` item 3, and `CON-975` item 3 carry that chain forward. `CON-976`,
`CON-977`, and `CON-978` carry it as far as `NFR-921`. This replacement narrows
that clause.

Only one effective sentence pair survives for the authority lane. `NFR-913` at
`spec-008-0.4.11-standalone-tofu-first-contact.md:265-267` reads:

> Transactions use bounded retries. Retry exhaustion returns a closed
> `authority-busy` result and performs no partial effect.

That pair names no level and no integer floor. It names no nesting rule and no
terminal set.

Three consequences follow, and each is gate-blocking.

The cbcl-bus `GATE-04` box "level-1 integer retry exhaustion is closed and has
zero partial effect" now has no governing clause for the authority lane.
[[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#TEST-1144]] at
`:263-264` requires that exhaustion "in each public mutation", and the only
effective definition of a public transaction owner is the WebFinger four.
`cbcl-chat-sealedgrant:put/get` answer `#(error txn-failed)` on a non-atomic
result at `cbcl-chat-sealedgrant.lfe:63-65` and `:90-92`, and the same test still
names both operations at `:248-249`.

The cbcl-bus twin carries the identical sentence. `NFR-020` in
`specs/trajectory/SPEC-053/spec-053-0.16.13-complete-authority-retirement-and-observable-busy.md`
states the same four owners, so both repositories drop the same control.

**The retry mechanics themselves verify.** I read the local Mnesia rather than the
prose. `try_again/1` at `mnesia_tm.erl:929-931` retries only while `Retries > 1`,
so `Retries >= 2` is the true minimum. `maybe_restart/6` at `:918-928` returns
`{aborted, nomore}` on exhaustion and a bare `#cyclic{}` when the level is not 1.
`transaction/6` at `:811-823` returns `{aborted, nested_transaction}` on bad
nesting. `mnesia:transaction/2` accepts an integer at `mnesia.erl:729`. The three
mapped terminals are real and the mapping is implementable.

**Minimum sound correction.** State the four WebFinger owners as a WebFinger-lane
list, and restate the whole-graph obligation the replaced paragraph carried.
Otherwise name every authority-lane and migration-lane public owner, and give each
its terminal.

---

### N5-M-1 (MEDIUM, gate-blocking) — six earlier cbcl-bus paragraphs still claim an exact survivor set, so the hub ledger is not singular

**Repository owner: cbcl-bus (REQ-075, ADR-031).**

`REQ-075` at
`specs/trajectory/SPEC-053/spec-053-0.16.13-complete-authority-retirement-and-observable-busy.md:51-52`
retires one test-specification paragraph:

> The 0.16.12 test-specification paragraph starting with "The exact survivor
> set" also retires. Its range ends with "named by REQ-074."

That boundary resolves uniquely, and the amendment relabels its own paragraph as
"The pre-existing survivor set". Both halves of the 0.4.19 `N4-L-3` correction are
therefore present in 0.16.12 and 0.16.13.

Six earlier documents carry the identical paragraph shape and no clause retires
any of them. A whole-chain search for retirement language returns hits in 0.16.13
only.

| Location | Stated exact survivor set |
|---|---|
| `spec-053-0.16.6-reachable-bounds-and-durable-authority.md:371` | TEST-043 through TEST-065, TEST-068, TEST-070 |
| `spec-053-0.16.7-deployed-admission-and-local-unlink.md:333` | TEST-043 through TEST-065, TEST-068, TEST-070 |
| `spec-053-0.16.8-authenticated-allocation-and-consent.md:360` | TEST-043 through TEST-065, TEST-068, TEST-070 |
| `spec-053-0.16.9-correlated-success-and-deliverable-bounds.md:439` | TEST-043 through TEST-065, TEST-068, TEST-070 |
| `spec-053-0.16.10-complete-consent-and-executable-ledgers.md:401` | TEST-043 through TEST-065, TEST-068, TEST-070 |
| `spec-053-0.16.11-closed-ledgers-and-upgrade-free-operator-graph.md:308-310` | the same plus TEST-093 and TEST-094 |
| `REQ-075` at `:63-65` | the same plus TEST-098, TEST-103, and TEST-104 |

`ADR-031` at `:146` states "Every stale hub-set and coordinated-set sentence
retires by an exact boundary." That claim is false against its own chain.
`TEST-104` at `:270-271` requires one exact hub set, and a checker reading the
adopted chain finds seven.

The coordinated-set half of the 0.4.19 `N4-H-2` finding does close. A whole-chain
search returns coordinated-set sentences at `0.16.11:55`, `0.16.12:53`, and
`0.16.13:67`, and `REQ-075` retires the first two by exact boundary.

**Minimum sound correction.** Retire the six earlier test-specification paragraphs
by name, or relabel each as its amendment's pre-existing survivor set.

---

### N5-L-1 (LOW) — one replacement boundary names a paragraph and ends outside it

`CON-979` at
`spec-008-0.4.20-complete-authority-retirement-and-observable-busy.md:176-177`
reads:

> This contract replaces the CON-978 paragraph starting with "The exact cbcl-bus
> set". The replaced range ends at the did-crdt commit.

The named paragraph is `spec-008-0.4.19-singular-authority-and-webfinger-order.md:247-251`
and it ends "supplies authority." The did-crdt commit sits in the next paragraph
at `:253-255`. The label and the stated end therefore disagree.

The supplied replacement text at `:179-191` covers both paragraphs, so the intent
is recoverable. A checker taking the word "paragraph" literally still leaves
`CON-978` at `:253-255` effective, and that sentence names cbcl-bus 0.16.12 against
the candidate's 0.16.13. `TEST-1145` at `:277-278` fails any surviving boundary, so
the imprecision sits on a role the amendment declares singular.

**Minimum sound correction.** Name a two-paragraph range, or give the exact
terminal string rather than a description of it.

---

### N5-L-2 (LOW) — the reordered withdrawal collapses an absent account row into "a mismatch", which drops the idempotent repeat

`NFR-922` at `:68-74` states the new live branch:

> The live branch of `tombstone/3` takes the requested exact alias write lock
> first. It takes the exact account write lock second. Both rows MUST agree on
> the account, alias, and home DID. A mismatch refuses without taking a second
> alias lock.

The deployed `tombstone/2` at `cbcl-chat-webfinger-store.lfe:206-230` selects its
branch on the account read alone: an absent row routes to `already-withdrawn/2` at
`:230`. The replacement reverses the read order and never restates that selector,
so "a mismatch" now covers both a disagreeing row and an absent one.

An implementation refusing on every non-agreement loses the repeat-withdrawal
success. The parent forbids that outcome at
`specs/SPEC-053-key-root-identity.md:1998-2002`:

> It is idempotent for the binding it names — a second withdrawal of an
> already-withdrawn binding answers with the same success as the first.

The unreplaced `NFR-921` paragraph at `:138-140` still describes the
already-withdrawn branch and its locks, so the branch survives without a stated
entry condition. The parent clause governs, which is why this is LOW rather than
higher.

**Minimum sound correction.** State that an absent account row enters the
already-withdrawn branch, and that only a present disagreeing row refuses.

---

## Disposition of every 0.4.19 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N4-H-1** — the ledger retirement stops at 0.4.12 | **NOT CLOSED** | `CON-979` retires seven test preambles, four sequencing item-1 entries, and the `CON-970` hub paragraph. I checked every boundary after line unwrapping. Each start string is paragraph-initial and unique inside its named document, and each end string terminates that paragraph. `REQ-999` also widens the resolver to numbered list items, which the prior scope omitted. The reach still falls short of the declared scope: three earlier sequencing lists, four surviving item-2 entries, `REQ-963`, and `TEST-1073` state competing values. Refiled as `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-H-1]]`. |
| **N4-H-2** — the cbcl-bus twin left REQ-072 effective | **CLOSED, with a residual** | `REQ-075` at `:34-49` retires two `REQ-072` fragments and two `REQ-074` fragments by exact boundary. All four resolve uniquely after unwrapping. A whole-chain search now returns one effective coordinated-set sentence, at `0.16.13:67`. The current hub set is also singular inside `REQ-072`, `REQ-074`, and `REQ-075`. Ancestry survives: the unretired opening sentences of `REQ-072` and `REQ-074` still preserve the survivor lists, and `REQ-075` restates the `REQ-071`, `REQ-073`, `CON-034`, `TEST-093`, `TEST-094`, and `TEST-098` survivals. The residual is the six earlier test-specification paragraphs, filed as `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-M-1]]`. |
| **N4-M-1** — the bounded-retry terminal reaches the deployed read path with no stated status | **CLOSED** | `NFR-922` at `:98-106` states the mutation terminal `#(error authority-busy)`, the `lookup/1` mapping to `unavailable`, and the HTTP 503 with an empty body and `no-store`. It forbids `missing` and 404 for contention, and keeps 400 and 404 for invalid syntax and committed absence. Every leg verifies at the baseline. `cbcl-chat-webfinger-http.lfe:36` already answers 503 for `unavailable` and `:42` already sends `no-store`. `recognise-query` at `cbcl-core-webfinger.lfe:57-78` returns a canonical URI that `recognise-resource` always accepts, so a 400 request cannot reach the store. `provision/3`, `withdraw/2`, and `reinstate/2` have no production caller: the only non-test consumers are `apps/cbcl_chat/src/cbcl-chat-app.lfe:24` and `cbcl-chat-webfinger-http.lfe:27`. Changing their non-atomic result breaks nothing. |
| **N4-L-1** — the pre-derived alias forces a new arity, and both lists named `tombstone/2` | **CLOSED** | `NFR-922` at `:64-66` names `tombstone/3(account, homeDid, requestedAlias)` and requires the derivation before the transaction opens. `TEST-1144` at `:251-253` and cbcl-bus `TEST-103` both name `tombstone/3`, and both require the pre-derived alias and reject a second alias lock. The derivation stays total: `cbcl-core-webfinger:account-uri/1` at `cbcl-core-webfinger.lfe:35-40` answers `'undefined` outside 1 through 4,096 octets. I re-traced all four withdrawal outcomes against the store tests and each remains reachable under the reversed order. |
| **N4-L-2** — the reinstatement account lock named no table and read no row | **CLOSED** | `NFR-922` at `:79-85` drops the lock: `reinstate/2` and `lift-tombstone/2` take no alias or account lock, and one whole `cbcl-webfinger-withdrawn` write lock precedes the first `index_read`. The ambiguous unqualified word is gone. I verified implementability at the local OTP 28.5 install rather than from prose. `ixrlock/5` at `mnesia_locker.erl:1086-1102` calls `need_lock(Store, Tab, ?ALL, read)`, and `need_lock/4` at `:858-871` matches the stored pattern literally, so a held write entry does not answer a read pattern. The request therefore reaches `can_lock/4`, whose whole-table read clause at `:353-356` selects every write holder, and `can_queue(Tid, Tid, _, Res)` at `:410-411` returns the requester's own lock as compatible. The later exact delete issues no request, because `wlock/4` at `:826-843` finds the stored table entry. Cycle freedom holds: every WebFinger operation is an ordered subsequence of alias, account, withdrawal. |
| **N4-L-3** — the cbcl-bus amendment stated two hub test sets in one document | **CLOSED, with a residual** | `REQ-075` retires the 0.16.12 paragraph by exact boundary and relabels the 0.16.13 paragraph as "The pre-existing survivor set". Inside those two documents the ledger is singular. The same paragraph shape survives in six earlier documents, filed as `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-M-1]]`. |

Three closed cleanly, two closed with one shared residual, one not closed.

## Disposition of every 0.4.18 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N3-H-1** — four survivor ledgers and three coordinated review sets stay effective | **NOT CLOSED** | The contradiction set shrank again and did not empty. Seven test preambles, four sequencing item-1 entries, and `CON-970` retire correctly. Ten earlier sites remain. See `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-H-1]]`. |
| **N3-H-2** — `CON-976` is replaced with no clause preserving its four replacements | **CLOSED** | `CON-979` at `:116-119` keeps every `CON-978` replacement and retirement effective except the two fragments it names. It restates ancestry at `:171-174`: TEST-1144 succeeds TEST-1142, TEST-1140, and TEST-1137; TEST-1145 succeeds TEST-1143, TEST-1141, and TEST-1138; ADR-957 succeeds ADR-956, ADR-955, ADR-954, and ADR-953. I attempted to revive REQ-994, NFR-918, ADR-953, TEST-1129 through TEST-1138, TEST-1140, TEST-1141, TEST-1142, and TEST-1143, and every one has exactly one current successor. `CON-978` at `:234-237` sits outside both replaced ranges, so TEST-1134, TEST-1135, and TEST-1139 remain effective. |
| **N3-M-1** — the reinstatement order creates a cross-table cycle with two unnamed operations | **CLOSED** | `NFR-922` gives reinstatement one table and gives live withdrawal the pre-derived alias key. Every named operation is an ordered subsequence of alias, account, withdrawal, and the residual findings that carried this closure are themselves closed. See the `N4-L-1` and `N4-L-2` rows above. |
| **N3-L-1** — the two withdrawal lock claims have no named test operation | **CLOSED** | `TEST-1144` at `:251-258` names `withdraw/2`, `tombstone/3`, and `already-withdrawn/2`, and requires alias-before-account ordering for live withdrawal. The unreplaced `NFR-921` paragraph at `:138-140` keeps the already-withdrawn modes. Both branches verify at the baseline: `tombstone/2` writes at `cbcl-chat-webfinger-store.lfe:222` and deletes at `:227-228`, and `already-withdrawn/2` at `:252-262` writes nothing. |
| **N3-L-2** — one leg of the pointer graph names the superseded candidate | **CLOSED** | All three legs point at the current candidates. `specs/SPEC-008-production-pairing-claimant.md:8` and `:17-20` name the 0.4.20 candidate and its open gate. `specs/SPEC-053-key-root-identity.md:15` and `:688-691` name the 0.16.13 candidate. `spec-001-0.4.8-split-room-shape-and-length.md:10` reads `consumer-design: selfsame SPEC-008 0.4.20-draft` at `383d6ce`. |
| **N3-L-3** — one effective CON-975 sentence still requires a maximum sealed record | **CLOSED** | `CON-978` at `:189-191` retires that sentence and `CON-979` does not disturb the retirement. The current cap obligation at `CON-978:257-261` also sits outside both replaced ranges. `REQ-988` at `spec-008-0.4.13-reachable-bounds-and-durable-authority.md:185-187` still separates a cap from a maximum. |

Five closed, one not closed.

## Disposition of every 0.4.17 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N2-H-1** — three Selfsame ledger clauses contradict each other | **NOT CLOSED** | The membership changed again. Twelve clauses were open at 0.4.19 and ten are open now. See `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-H-1]]`. |
| **N2-M-1** — reinstatement performs an unnamed upgrade | **CLOSED** | `NFR-922` keeps the whole write lock before the first `index_read` and drops the account lock. I re-derived the lock semantics at the local OTP 28.5 install. See the `N4-L-2` row for the exact call chain. |
| **N2-M-2** — the fresh-ceremony same-key re-grant test disappeared | **CLOSED** | `TEST-1144` at `:239-241` restates the case inline and requires current eligibility, capacity, both consent decisions, and a new verified grant. Cbcl-bus `TEST-103` carries the identical paragraph, and `TEST-104` requires every installed-state case inline. `REQ-995` remains effective under `CON-978`, and `CON-979` does not touch it. |
| **N2-M-3** — two recognizer caps were relabelled as maxima | **CLOSED** | `REQ-997` stays effective and every figure re-derives. The reachable complete offer is 41,308 octets, so the residuals are 15,692 and 20,692 octets, the sealed cap bodies are 57,016 and 62,016 octets, and the transit residuals against 69,632 octets are 12,616 and 7,616 octets. The AEAD premise verifies at `crates/selfsame-core/src/seal.rs:143-165`, where `seal_offer` and `seal_bundle` pass a fixed role nonce and authenticated associated data. `REQ-305` verifies at `specs/PROTO-002-selfsame-rendezvous-v1.md:310-314`. |
| **N2-L-1** — one `cbcl-sealedgrant` holder fell outside the declared scope | **CLOSED** | `NFR-921` at `:105-112` names both holders with exact modes and `NFR-922` leaves that paragraph effective. Both halves verify at the baseline: `put-tx` at `cbcl-chat-sealedgrant.lfe:67-79` reaches `cbcl-chat-invite:peek-tx` at `cbcl-chat-invite.lfe:75-78`, which reads an exact key, and `get-tx` at `:94-101` reads then deletes, which is today's upgrade. |

Four closed, one not closed.

## Disposition of every 0.4.16 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N-H-1** — the consent-surface completeness rule is inverted | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:117-119` reads that no filter, paging, collapse, or lazy rendering SHALL hide a room. That sentence sits outside the `REQ-997` replacement range, and `CON-979` adds no replacement over `REQ-996`. The shipped target verifies: `affectedChannelsNow()` at `apps/cbcl_chat/priv/web/app.js:602-606`, with call sites at `:6409` and `:6443`. |
| **N-M-1** — the v1 trace omitted four version sites | **CLOSED** | The fifteen-line trace sits in the unreplaced part of `REQ-996` at `:158-176`. I read every line at the declared baseline and each resolves to the stated construct. I then ran an independent scan over `crates/**/*.rs` for the four version spellings. Every omitted production hit is a member-name constant, a field read, a constant definition, a format template, or a comment. The three files whose omitted literals sit in tests verify: `#[cfg(test)]` opens at `crates/selfsame-app-identity-net/src/profile.rs:221`, `crates/selfsame-beam/src/enrollment.rs:199`, and `crates/selfsame-web-device/src/lib.rs:1521`. |
| **N-M-2** — the acquisition point and the `revoke-handle-tx` upgrade were dropped | **CLOSED** | `NFR-921` at `:100-103` keeps both sentences and `NFR-922` replaces neither. `TEST-1144` at `:243-245` still requires the restored acquisition point and the invite-before-room order. |
| **N-M-3** — the twelve cycles used symbols whose legend was deleted | **CLOSED** | `NFR-921` at `:68-79` carries the legend and the cycle block, and neither paragraph is replaced. The binding to `NFR-913` stays bijective, because that matrix at `spec-008-0.4.11-standalone-tofu-first-contact.md:245-253` has exactly seven rows and one read-only row. |
| **N-M-4** — the parent pointer had no target | **CLOSED** | `specs/SPEC-008-production-pairing-claimant.md:8` names the 0.4.20 candidate and `:9` carries the open candidate gate. The implemented 0.2.2 baseline stays distinct at `:4-5`. The cbcl-bus parent resolves at `specs/SPEC-053-key-root-identity.md:15`, with `DEC-012` at `:635` and body references at `:688-691`. |
| **N-L-1** — `socketGeneration` had no definition | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:83-96` states creation, capture, cancellation, and reconnect semantics. That block is outside the `REQ-997` range, and `TEST-1134` remains effective without amendment. |
| **N-L-2** — two different definitions of one shared grammar production | **CLOSED** | The alphabet in `spec-001-0.4.8-split-room-shape-and-length.md:33-40` matches `is_symbol_char` at `crates/cbcl-parser/src/parser.rs:328-342` in cbcl-rs `febc669`, character for character. I compared all fourteen branches. |
| **N-L-3** — the transit ledger omitted the in-repository baseline | **CLOSED** | `CON-978` at `:257-261` names both baselines and `CON-979` leaves that paragraph effective. `crates/selfsame-rendezvous/src/lib.rs:48` reads `pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`. Did-crdt `src/service/rendezvous.rs:65` carries the same constant at `1f409a4`. |
| **N-L-4** — the sealed-record maximum was never derived | **CLOSED** | `REQ-997` derives every figure and residual in one block, and the derivation re-computes exactly. See the `N2-M-3` row above. |
| **N-L-5** — one live `cbcl-invite` lock fell outside both graph scopes | **CLOSED** | `NFR-921` at `:84` and `:105-116` own every `cbcl-invite` lock and place that table first. Both halves verify at `cbcl-chat-sealedgrant.lfe:67-79` and `cbcl-chat-invite.lfe:75-78`. |

Ten closed.

## Disposition of every 0.4.15 finding

| ID | Disposition | Evidence |
|---|---|---|
| **H-1** — unsigned advancing signal with no defined correlation | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:72-101` states the correlation member, its 32 random octets, its base64url encoding, and its one-use consumption. That block is outside the `REQ-997` range and outside every `CON-979` replacement. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **CLOSED** | Both sites resolve at `crates/selfsame-app-identity/src/enrollment.rs:373` and `:662`, and `:452` carries its label. My independent scan found no omitted production comparison or substitution site. See the `N-M-1` row. |
| **M-2** — `write-grant` read-then-write upgrades | **CLOSED** | `NFR-921` at `:90-92` requires the three-argument read with `write` on the first read in both paths, and `NFR-922` leaves that paragraph effective. Both deployed sites carry the upgrade today at `cbcl-chat-roommember.lfe:155` and `cbcl-chat-account-roommember.lfe:67`. |
| **M-3** — maxima never checked against the implemented rendezvous | **CLOSED** | `CON-978` at `:257-261` states the dependency on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. Both anchors resolve: `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:310-314` sets 69,632 bytes, and `CON-304` sets a `1..69632` body with `413` above it. The reference implementation's own non-conformance list at `:1016` records the 4,096-byte body. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` remains effective and states the terminal at `spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:185-187`. `TEST-1144` at `:235-237` requires the terminal and zero enrollment frames. The independent hub-side guard is real at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`. |
| **L-1** — survivor set revives `TEST-057` | **CLOSED** | `CON-978` at `:242-245` states that TEST-057 and TEST-058 do not survive, and every preamble that reached TEST-057 is now retired. I checked every surviving range in the chain and none reaches TEST-057 or TEST-058. The TEST-050 disagreement is separate and sits inside `[[spec-008-0.4.20-claude-adversarial-review-2026-08-23#N5-H-1]]`. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED** | `REQ-995` restates the unlink action in full at `:194-198`. Cbcl-bus `TEST-103` states every installed-state case inline, and `TEST-104` requires that inlining. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:53-63` orders the four classes and defers length classification until `RoomShape` passes. Cbcl-pairing `REQ-030` at `spec-001-0.4.8-split-room-shape-and-length.md:47-50` carries the same order. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` remains effective and splits the triggers at `:174-183`. The silent path cannot widen consent, because `REQ-981` keys the person-owned policy on the exact pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-95`. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-996` at `:126-135` keeps `migration-display-state-changed` non-terminal and `migration-plan-invalid` terminal. Both remain implementable against the shipped reducer, whose terminal branch returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:171-174`. |

Ten closed.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-979` names nothing in `REQ-981`, which still fixes the policy key as the exact application and relay pair at `spec-008-0.4.11-standalone-tofu-first-contact.md:84-95`. It still states that a v1 policy entry cannot suppress the first v2 prompt. Cbcl-bus `GATE-04` retains the box requiring the exact v2 policy pair to prompt. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched. `crates/selfsame-pairing/src/live.rs:479-481` commits one explicit local decision only after `LiveEffect::DisplayIntent`. The five pre-consent checks in `REQ-996` at `spec-008-0.4.17-complete-consent-and-executable-ledgers.md:108-114` are outside every replacement range and remain ordered. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-979` names nothing in `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202-231`. `REQ-995` still states that local unlink cannot re-grant authority. `TEST-1144` and cbcl-bus `TEST-103` state the re-grant case inline. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-979` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:903` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-025` at `spec-001-0.4.8-split-room-shape-and-length.md:96-99` preserves the no-relay-field property of `CON-024`. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at `src/cbcl_protocol.rs:34` and `MAX_FRAME_BODY_BYTES = 69_632` at `src/mailbox.rs:15` both sit above the 62,016-octet sealed cap body. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-331` states that mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery. Every negative path this amendment keeps or adds creates nothing. The new 503 result carries an empty body and reveals no binding, so no finding here lets an endpoint failure authorise. |

## Repository assessments

### selfsame SPEC-008 0.4.20-draft (`1728473`)

Artefact numbers are sequential with no gap and no reuse. `REQ-999`, `NFR-922`,
`CON-979`, and `ADR-957` each advance their series by one, and `TEST-1144` and
`TEST-1145` follow `TEST-1143`.

`CON-979` is the strongest part of the amendment. Thirteen of its fourteen
boundary-specified ranges resolve cleanly against one paragraph or list item. It preserves
replacement ancestry explicitly, so no predecessor loses its successor.

`NFR-922` closes the WebFinger terminal question completely. It names the atom,
the resolver mapping, the HTTP status, and the two statuses that do not change.
The lock changes it mandates are implementable at the local Mnesia.

`REQ-999` is where the amendment fails twice. It widens the resolver to numbered
list items and then retires four of the seven such lists. `NFR-922` separately
narrows a whole-graph concurrency control to four functions, and states an owner
set the reviewed code contradicts.

The candidate carries 22 distinct wikilinks over 14 distinct targets. A `zetl check --dead-links`
run over the `specs/` vault reports exactly one dead target from it, which is
`[[SPEC-053-key-root-identity#DEC-012]]`. That is a cross-repository pointer whose
target lives in cbcl-bus, and I verified it by hand at `2b5c7f2`. Under
[[PROTO-001-usdd-agent-protocol]] a dead link is visible debt rather than an
error. The same run reports one dead target from this report, which is the
protocol page itself.

Open against it: `N5-H-1`, a share of `N5-H-2`, `N5-L-1`, and a share of
`N5-L-2`.

### cbcl-bus SPEC-053 0.16.13-draft (`2b5c7f2`)

`NFR-020` and `TEST-103` are textually identical to their Selfsame twins apart
from identifiers and one added load-evidence paragraph. I diffed both pairs and
found no other divergence.

The `GATE-04` restatement holds 42 boxes by mechanical count, which matches
`TEST-104` and Selfsame `TEST-1145`. I diffed the 0.16.12 and 0.16.13 box lists and
exactly one box differs, which is the coordinated-set version. Every box remains
open and the amendment states that it closes none.

The GATE-04 replacement chain is complete from 0.16.1 through 0.16.13, so only one
gate list is effective. `REQ-075` retires four ledger fragments and one test
preamble, and all five boundaries resolve uniquely.

Open against it: `N5-M-1`, a share of `N5-H-2`, and a share of `N5-L-2`.

### cbcl-pairing SPEC-001 0.4.8-draft (`383d6ce`)

One line changed since the 0.4.19 round, and it is the `consumer-design` pointer
at `:10`. Everything else is byte-identical.

`REQ-030` carries the `RoomShape` split, and its grammar block matches both twins.
`CON-025` keeps the hub as the sole owner of the three admission terminals and
preserves every `CON-024` property. The `CON-025` survivor set matches the
Selfsame `CON-978` set exactly.

The room-array arithmetic re-computes: 256 rooms at 129 content octets plus two
quotes, plus 255 commas and two brackets, is 33,793 octets.

This chain carries exactly one survivor sentence, which is the property both other
chains lost. Nothing is open against it.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged. `src/service/rendezvous.rs:65`
carries the 4,096-byte cap, `:68` carries a 600-second TTL, and `:79` carries a
65,536-slot ceiling. The working tree carries user-owned uncommitted paths, which
I ignored in favour of the declared commit.

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Every CON-979 and NFR-922 boundary, mechanically.** I unwrapped every
  paragraph in the target documents and matched each start and end string. All
  seventeen Selfsame ranges and all eight cbcl-bus ranges are paragraph-initial
  and unique, except the one filed as `N5-L-1`.
- **Whole-table write lock followed by `index_read`.** Verified at the local OTP
  28.5 install rather than from prose. See the `N4-L-2` row for the call chain and
  the exact lines.
- **WebFinger cycle freedom under the new order.** Provision takes alias, account,
  withdrawal. Lookup takes alias then account. Live withdrawal takes alias,
  account, withdrawal. The already-withdrawn branch takes alias, account, then a
  whole-table read. Reinstatement takes one whole-table write. Every sequence is a
  subsequence of one order.
- **Reinstatement against provision and withdrawal.** Reinstatement requests no
  second table, so its whole-table write lock cannot participate in a cycle. The
  single-table conflict resolves through wait-die and the bounded retry.
- **The pre-derived alias against every existing store test.** I traced all four
  withdrawal outcomes. A stale generation still refuses, because the account row's
  stored DID is read under the second lock. The stray-account fixture still answers
  `inconsistent-binding`, because the alias row at the requested key names another
  account. An unknown account still reaches `already-withdrawn/2`.
- **The withdrawal refusal atoms.** Neither amendment chain names
  `wrong-generation` or `inconsistent-binding`, but the parent requires both
  refusals at `specs/SPEC-053-key-root-identity.md:1982-1996`. The reversed order
  preserves both, because the account row is still read.
- **The new 503 as an oracle.** Contention answers 503 whether or not a binding
  exists, so the parent's indistinguishability property at `:2004-2006` survives.
  Withdrawn and never-provisioned resources both stay 404.
- **The HTTP recogniser split.** `recognise-query` at `cbcl-core-webfinger.lfe:57-78`
  emits a canonical URI that `recognise-resource` at `:43-50` always accepts, so
  400 and 404 stay distinct. Both productions are fully anchored regular
  expressions and nothing is percent-decoded before recognition.
- **Consumers of the changed mutation results.** A search at the declared baseline
  returns two non-test consumers of the store: `apps/cbcl_chat/src/cbcl-chat-app.lfe:24`
  and `cbcl-chat-webfinger-http.lfe:27`. No production caller reads the mutation
  results, so the new atom breaks nothing.
- **The retired CON-970 survival sentence.** Its second sentence names six
  cbcl-bus 0.16.2 clauses. Cbcl-bus states the same survivals independently at
  `spec-053-0.16.4-distinct-v2-authority-ordering.md:40-50`, and the unretired
  opening sentences of `REQ-072` and `REQ-074` carry that list forward. Nothing is
  lost.
- **The coordinated pointer set.** Every earlier pointer paragraph sits inside a
  replaced contract or a replaced test. The role resolves to one current value.
- **The Selfsame correction set.** Every earlier correction-set sentence sits
  inside a retired range or a fully replaced contract. The role resolves to one
  current value.
- **Fifteen version sites.** Every line in the trace resolves at the declared
  baseline to the stated construct. My independent scan found no omitted
  production comparison or substitution.
- **Transit arithmetic and carriers.** Sealing adds only the 16-octet tag. Both
  cbcl-pairing ceilings sit above the 62,016-octet sealed reverse cap body. Only
  the rendezvous lane is undersized, and all three candidates say so.
- **Grammar drift between the pinned revisions.** The alphabet in the shared
  grammar block matches `is_symbol_char` exactly at the pinned cbcl-rs revision.
- **The deployed browser channel expression.** `path-b-ceremony.mjs:118` reads a
  pattern strictly narrower than the shared room alphabet, so the replacement
  obligation in `REQ-996` names a real defect.
- **Credential/v2 lane presence.** I searched `crates/` in selfsame and `apps/` in
  cbcl-bus at both declared baselines. The six symbols are `selfsamepairallocate`,
  `migrationRooms`, `roomSetDigest`, `socketGeneration`, `migration-plan-invalid`,
  and `credential-v2-canonical`. All twelve searches return nothing.
  `authority-busy` is also absent from `apps/`. That is honest planned work rather
  than an unsupported release claim.

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later gate
owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and credential/v2
   transport depends on it. Its own non-conformance list at
   `specs/PROTO-002-selfsame-rendezvous-v1.md:1016` records the reference
   implementation's 4,096-byte body.
2. **The whole credential/v2 lane is planned rather than shipped,** as the
   twelve-symbol search above records.
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt durable
   readiness, and the separate owner approval for production enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration,** and the cbcl-bus
   parent records that deferral as in force.
5. **The unary transaction call sites remain unbounded today.** `NFR-922` mandates
   a change for four of them rather than describing current behaviour. That change
   surfaces a busy result the resolver did not previously return.
6. **The typed cbcl-pairing display is a design.** Its two named types are absent
   from `src/`, which both the amendment and the prior reviews record.
7. **No amendment in the 0.4.x chain carries an Orientation block or an
   `## Amendment Channels` block.** Several artefact references in the amendment
   bodies are also plain text rather than wikilinks. The parent
   [[SPEC-008-production-pairing-claimant]] carries both blocks. This is an
   inherited condition of the amendment form rather than a defect this candidate
   introduces.

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation
plan. It authorizes no code, no public allocation, no gate opening, no release, no
deployment, and no production enablement. Every GATE-04, `GATE-00`, `GATE-01`, and
`GATE-03` box stays open. Every human review, every durability obligation, and the
separate owner approval for production deployment also stay open and unaffected.

This review changed no candidate specification, no source, no test, no fixture, no
generated file, and nothing in any sibling repository. It added exactly one file,
which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N5-H-1 | selfsame | Retire the 0.4.4, 0.4.5, and 0.4.6 sequencing item 1 entries, and item 2 of the 0.4.5, 0.4.6, 0.4.7, and 0.4.8 lists. Retire or restate the two `REQ-963` sentences and the `TEST-1073` control-length sentence. |
| N5-H-2 | selfsame, cbcl-bus | Scope the four named owners to the WebFinger lane, and restate the whole-graph level-1 and `Retries >= 2` obligation the replaced paragraph carried. |
| N5-M-1 | cbcl-bus | Retire the six earlier test-specification paragraphs by name, or relabel each as its own amendment's pre-existing survivor set. |
| N5-L-1 | selfsame | Name a two-paragraph range for the cbcl-bus-set replacement, or quote the exact terminal string. |
| N5-L-2 | selfsame, cbcl-bus | State that an absent account row enters the already-withdrawn branch, and that only a present disagreeing row refuses. |
