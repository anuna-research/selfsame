---
id: SPEC-008-amendment-0.4.18
title: SPEC-008 0.4.18-draft — Closed ledgers and upgrade-free operator graph
status: draft
version: 0.4.18-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.17-complete-consent-and-executable-ledgers]] only where [[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#CON-977]] says so"
adopts: "All effective 0.4.4 through 0.4.17 clauses except the exact replacements in CON-977"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.8-draft; cbcl-bus SPEC-053 0.16.11-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.17-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.18-draft — Closed ledgers and upgrade-free operator graph

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.17 review found one HIGH, three
> MEDIUM, and one LOW defect. This amendment closes those findings against the
> reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirement

### REQ-997 — Reachable maxima and recognizer caps remain distinct

This requirement replaces only the cap and transit section of
[[spec-008-0.4.17-complete-consent-and-executable-ledgers#REQ-996]]. The
replaced section starts with "The maxima in". It ends with "Its Tier-1 gate
remains open." Every other REQ-996 sentence remains effective.

The reachable maxima and residuals in
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remain
unchanged. The reachable complete offer is 41,308 octets. The 57,000-octet
offer recognizer limit is a cap, not a maximum. Its reachable residual is
15,692 octets.

The 62,000-octet credential-member recognizer limit is also a cap. Its
residual against the reachable complete offer is 20,692 octets. The existing
`V2_MAX_OFFER_PAYLOAD_OCTETS` and `V2_MAX_CREDENTIAL_MEMBER_OCTETS` identifier
spellings do not change those normative classifications.

ChaCha20-Poly1305 appends one 16-octet authentication tag. The fixed
direction-separated nonce and associated data add no stored octet. No base64
expansion applies to the opaque rendezvous body.

```text
reachable complete offer                   = 41,308
offer recognizer cap                        = 57,000
offer cap residual                          = 15,692
sealed offer cap body          = 57,000 + 16 = 57,016
PROTO-002 offer transit residual            = 12,616
credential-member recognizer cap            = 62,000
credential-member cap residual              = 20,692
sealed reverse cap body        = 62,000 + 16 = 62,016
PROTO-002 reverse transit residual           =  7,616
PROTO-002 body cap                           = 69,632
```

A cap-transit vector is an opaque cap-sized octet string. It need not be a
grammar-valid offer or credential member. A reachable-maximum vector remains
grammar-valid and separately proves the 41,308-octet result.

Both current rendezvous baselines accept only 4,096 octets. They cannot carry
either cap-sized sealed record. Production use therefore depends on
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and
[[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Its Tier-1 gate remains open.

## Non-functional requirement

### NFR-920 — The migration graph names every operator and sealed-grant lock

This requirement replaces
[[spec-008-0.4.17-complete-consent-and-executable-ledgers#NFR-919]] in full.

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
The finalizer's exact `cbcl-member` access is a write lock. Its whole
room-table locks are write locks.

Grant creation is `G`, and the credential/v2 finalizer is `F`. Exact grant
revocation is `XR`, and observed revocation is `OR`. New member enrollment is
`ME`, and pairaccount claim is `PC`. Per-frame authority read is `AR`. It uses
the matrix's `unratified read` sequence.

The authority graph covers only `cbcl-device-grant`, `cbcl-member`, and
`cbcl-pairaccount-grant`. Its exact cycle set remains:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

The generator SHALL reproduce those twelve cycles and every `AR` contention
case. It makes no claim about room or finalizer-tail tables.

The migration graph owns every room, finalizer-tail, `cbcl-invite`, and
`cbcl-sealedgrant` lock. Allocation takes stable account and pending ceremony
first. Binding and member locks follow. Allocation then takes whole
`cbcl-roommember` and `cbcl-account-roommember` read locks in that order.
Finalization uses the same prefix and whole write locks.

Both single-table `write-grant` paths take an exact write lock on their first
read. They use `mnesia:read/3` with `write`. They perform no read-to-write
upgrade.

Both `redeem-and-grant` variants join two exact write locks. They take the
`cbcl-invite` token lock first. The applicable `cbcl-roommember` or
`cbcl-account-roommember` key follows. `cbcl-chat-invite:redeem-tx` also takes
its write lock on the first read. It performs no upgrade before update or
delete.

An operation touching both room tables takes both whole write locks in the
declared order. It acquires those locks before its first room read or write.
`revoke-handle-tx` therefore performs no `index_read`-then-exact-write
upgrade.

The graph includes every `cbcl-invite` and `cbcl-sealedgrant` holder.
`cbcl-chat-sealedgrant:put-tx` first takes an exact `cbcl-invite` read lock.
It then takes an exact `cbcl-sealedgrant` write lock. The operation takes no
room lock and performs no upgrade.

`cbcl-chat-sealedgrant:get-tx` takes an exact `cbcl-sealedgrant` write lock on
its first read. It uses `mnesia:read/3` with `write`. The expiry delete reuses
that lock. The operation takes no invite or room lock and performs no upgrade.

The generated table order places `cbcl-invite` before `cbcl-roommember`,
`cbcl-account-roommember`, and `cbcl-sealedgrant`. It places
`cbcl-roommember` before `cbcl-account-roommember`.

Finalizer, compensation, status, and operator paths use one generated tail
order. Compensation uses exact captured keys and no `?ALL` scan. A live-binding
withdrawal takes an exact stable-account write lock first. Its exact alias and
tombstone write locks follow.

The already-withdrawn branch retains the exact stable-account write lock from
its first read. Its later tombstone `index_read` takes a whole-table read lock.
That non-mutating branch acquires no later write lock.

Reinstatement takes an exact stable-account write lock first. It then takes a
whole `cbcl-webfinger-withdrawn` write lock before its first `index_read`.
`lift-tombstone` performs the indexed lookup and exact delete under that whole
write lock. It performs no read-to-write upgrade. Read-only recovery acquires
no later write lock.

The migration graph includes every lock and exact mode. It includes cross-lane
authority locks. Its only cycles are projections of the twelve authority
cycles. It has no additional cycle or upgrade. A missing operation, unstated
mode, upgrade, opposite order, or additional cycle is non-conforming.

Every affected transaction runs at Mnesia level 1. It uses an explicit integer
`Retries >= 2`. Nested transactions are prohibited. Consumers map `nomore`,
exhausted `#cyclic{}`, and `nested_transaction` to `authority-busy`. No partial
effect survives.

## Contract

### CON-977 — One current ledger supplies all coordinated authority

This contract replaces
[[spec-008-0.4.17-complete-consent-and-executable-ledgers#CON-976]] in full.
It also retires only the stale ledger roles named below.

In
[[spec-008-0.4.15-authenticated-allocation-and-consent#CON-974]], this contract
replaces one stale sentence range. That range starts with "The exact
cbcl-pairing survivor set". It ends with "remains mandatory." This contract
also replaces the sentence starting with "The coordinated review set".

In
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]], this
contract replaces one stale sentence range. That range starts with "The exact
cbcl-pairing survivor set". It ends with "supplies authority." This contract
also replaces the paragraph starting with "The coordinated review set". That
paragraph ends with the link to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]].

The numbered replacement relationships and hard-stop sentences in CON-974 and
CON-975 remain effective. No other CON-974 or CON-975 sentence supplies a
survivor ledger, current-correction ledger, or coordinated-review pointer.

This amendment applies these additional replacements:

1. REQ-997 replaces only the cap and transit section of
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#REQ-996]].
2. NFR-920 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#NFR-919]] in full.
3. ADR-955 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#ADR-954]] in full.
4. TEST-1139 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1136]] in full.
5. TEST-1140 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1137]] in full.
6. TEST-1141 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1138]] in full.

REQ-995 and every unreplaced part of REQ-996 remain effective. TEST-1134 and
TEST-1135 remain effective without amendment. Every F-A through F-E correction
remains a hard stop. SPEC-007 REQ-812 also remains a hard stop.

The exact cbcl-pairing survivor set contains TEST-030 through TEST-049. It also
contains TEST-050 except its replaced control portion and retired positive
witness. That witness remains a negative obligation. TEST-051 through TEST-056
and TEST-059 survive. TEST-057 and TEST-058 do not survive.

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, and TEST-098 through TEST-100. Every effective Selfsame
test through TEST-1108 also survives. TEST-1134, TEST-1135, and TEST-1139
through TEST-1141 are the current correction tests. No superseded portion or
open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.8, and
cbcl-bus 0.16.11. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

The reviewed `crates/selfsame-rendezvous` and did-crdt baselines each have a
4,096-octet cap. Both are deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. Credential/v2 production
enablement depends on closing the PROTO-002 Tier-1 gate.

The Selfsame parent
[[SPEC-008-production-pairing-claimant]] points to this candidate and names its
open review gate. The cbcl-bus parent
[[SPEC-053-key-root-identity#DEC-012]] points to the coordinated 0.16.11
candidate. The cbcl-bus GATE-04 owns the cross-repository transit evidence.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It authorizes
no code, allocation, release, or deployment.

## Decision

### ADR-955 — Closed ledgers and explicit first-lock modes precede code

One effective ledger names every survivor, current correction, and coordinated
candidate. Historical replacement relationships remain visible without
competing current sets.

The person still sees every authenticated room in order. Socket correlation,
the complete version trace, and the shared grammar remain unchanged.

The migration graph now includes operator reinstatement and every sealed-grant
holder. Each mutating path takes its required write lock on the first access.
Recognizer caps remain distinct from grammar-reachable maxima.

## Test specification

### TEST-1139 — Reachable maxima and opaque cap bodies transit separately

Generate the grammar-valid 41,308-octet complete offer. Require the
15,692-octet offer-cap residual and the 20,692-octet credential-member-cap
residual.

Generate opaque offer and reverse plaintext vectors of 57,000 and 62,000
octets. Do not require those two vectors to satisfy the credential grammar.
Require sealed lengths of 57,016 and 62,016 octets. Require transit residuals
of 12,616 and 7,616 octets against the PROTO-002 cap.

Put, repeat-put, get, and repeat-get each cap-sized sealed record through both
rendezvous implementations. Reject 69,633 octets before storage. Prove both
reviewed 4,096-octet baselines fail. GATE-04 cannot close before both
implementations and the PROTO-002 gate pass.

### TEST-1140 — Installed state and every lock graph close independently

Change only the profile digest with authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consent decisions, and a new verified
grant. Every local-only re-grant shortcut fails.

Generate the twelve named authority cycles and every `AR` writer contention.
Generate the complete migration graph. Require the restored acquisition point,
invite-before-room order, and no additional upgrade or cycle.

Include both `write-grant` paths, both `redeem-and-grant` paths,
`revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`,
`cbcl-chat-sealedgrant:put-tx`, `cbcl-chat-sealedgrant:get-tx`, and
`cbcl-chat-webfinger-store:reinstate/2`. Include `lift-tombstone` in the
reinstatement trace.

Require the reinstatement table-write lock before its first `index_read`.
Require the sealed-grant write lock on the first `get-tx` read. Force bounded
retry exhaustion and prohibited nesting. Require `authority-busy`, no partial
effect, and writer progress.

### TEST-1141 — Effective ledgers, parent pointers, and gates are singular

Static checks require every survivor and replacement, every version site, and
all 42 cbcl-bus GATE-04 boxes. They require the Selfsame parent pointer to this
candidate. They also require the cbcl-bus parent DEC-012 pointer to 0.16.11.

The checks fail when two effective Selfsame contracts state different
cbcl-pairing survivors, cbcl-bus tests, current Selfsame corrections, or
coordinated review sets.

Require the exact three-repository test ledgers, two named non-conforming
rendezvous baselines, and explicit sealed cap arithmetic. Removing any
obligation fails.

## Changelog

- **0.4.18-draft — 2026-08-23 — review candidate, not approved.** Retires stale
  ledgers, closes operator and sealed-grant lock upgrades, restores re-grant
  verification, and separates maxima from caps. No implementation or deployment
  authority is claimed.
