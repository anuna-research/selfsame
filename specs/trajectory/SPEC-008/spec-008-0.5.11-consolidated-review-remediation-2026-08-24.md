---
id: SPEC-008-0.5.11-CONSOLIDATED-REMEDIATION
title: Consolidated remediation of the 0.5.10 implementation review
status: owner-authorized-review-candidate
tier: 1
date: 2026-08-24
authority: evidence-only; current law is stated directly in [[SPEC-008-production-pairing-claimant]]
review-gate: release-and-deployment-prohibited-pending-fresh-cross-model-pass
---

# SPEC-008 0.5.11 — Consolidated review remediation

The repository owner authorized this consolidated reissue and test-first
implementation before review PASS. That authorization changes the order of the
local feedback loop. It does not authorize release, production allocation, or
deployment.

## Review inputs

The fresh 0.5.10 implementation review is
[[spec-008-0.5.10-implementation-claude-adversarial-review-2026-08-24]]. It
reported zero CRITICAL findings, two HIGH findings, zero MEDIUM findings, and
four LOW findings.

- H-1 found that allocator restart from an acknowledged `FinishedSent`
  checkpoint incorrectly tried to resend a bootstrap frame and entered the
  application phase too early.
- H-2 found that a terminal wallet error after final-approval persistence but
  before durable payload preparation could strand an invisible pending slot.
- L-3 found that the explicit local dependency witness checked only the
  `cbcl-pairing` sibling.
- L-4 found that an oversized WebFinger response was classified as revoked
  rather than unavailable.

L-1, orphan issuer publication after a later terminal failure, remains the
bounded compensation limitation already admitted by
[[SPEC-004-application-scoped-identity#CON-204]]. L-2, reverse-proxy peer
identity for the anonymous recovery limiter, remains an operational deployment
gate. Neither is silently represented as closed.

## Consolidated correction

Current authority is stated directly in
[[SPEC-008-production-pairing-claimant#CON-990]],
[[SPEC-008-production-pairing-claimant#TEST-1162]],
[[SPEC-008-production-pairing-claimant#TEST-1163]], and
[[SPEC-008-production-pairing-claimant#TEST-1164]].

The allocator correction distinguishes a cached bootstrap frame that remains
outstanding from one whose acknowledgement is already durable. Restart always
reopens the relay room. It resends only an outstanding bootstrap frame. An
acknowledged `FinishedSent` checkpoint proceeds with no bootstrap resend and
waits for the first application object.

The wallet correction arms exact-attempt cleanup immediately after durable
final approval and disarms it only after durable `PayloadPrepared`. Every
intervening error runs best-effort cleanup without masking the original error.
The cleanup recognises the current stored phase so a committed-but-reported-
failed replacement cannot strand the slot. It retains any actual
`PayloadPrepared` value for signed final-status recovery.

Every surviving pending phase is independently visible to the person. Confirmed
unlink under current-root presence removes only that exact local application
slot, its exact `(applicationId, relayOrigin)` policy, and the profile cache
contained by the slot. It retains the hierarchy root, sibling applications, and
remote records and claims no remote revocation.

The reload correction classifies an oversized bounded WebFinger response as
unavailable. The dependency witness independently compares HEAD and tracked
status for `cbcl-pairing`, `cbcl-rs`, and `did-crdt`, including when the labelled
development build override is set.

## Gate

The candidate SHALL undergo another fresh-context, defect-finding Tier-1 review
against the real code and coordinated repository pins. The requested review
uses Circus with an interactive Claude Max subscription driver, with
`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` removed from the environment. The
Anthropic API driver is prohibited. A PASS closes the review gate only; a
separate owner decision remains required before any release or deployment.
