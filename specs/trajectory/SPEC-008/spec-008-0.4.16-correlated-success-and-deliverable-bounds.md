---
id: SPEC-008-amendment-0.4.16
title: SPEC-008 0.4.16-draft — Correlated success and deliverable bounds
status: draft
version: 0.4.16-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.15-authenticated-allocation-and-consent]] only where [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]] says so"
adopts: "All effective 0.4.4 through 0.4.15 clauses except the exact replacements in CON-975"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.7-draft; cbcl-bus SPEC-053 0.16.9-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.15-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.16-draft — Correlated success and deliverable bounds

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.15 review found one HIGH, four
> MEDIUM, and five LOW defects. This amendment closes each finding against the
> reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirements

### REQ-994 — Allocation acknowledgement and consent have bounded authority

The tagged transition shapes and member sets in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] remain exact.
Credential/v2 uses these ASCII grammars:

```text
LegacyHandle = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )

CbclSymbolChar = ALPHA / DIGIT / "_" / "-" / "." / "/" / "!" /
                 "?" / "+" / "*" / "<" / ">" / "=" / "@"
RoomShape = "@" 1*( CbclSymbolChar )
CanonicalRoomText = RoomShape with 2..129 ASCII octets
```

The alphabet matches the pinned cbcl-rs revision
`febc6691e6dd2d5f7116b1a4d84c984b64717564`. The 129-octet cap is a
credential/v2 admission limit. It is not a deployed CBCL-symbol maximum. The
serializer SHALL NOT escape `/`.

The hub applies the admission classes in this order:

1. A value that fails `RoomShape` is `MigrationRoomUnrepresentable`.
2. A shaped room over 129 octets is `MigrationPlanTooLarge`.
3. Count or aggregate overflow is `MigrationPlanTooLarge`.
4. A durable handle failing `LegacyHandle` is
   `MigrationHandleUnrepresentable`.

Length classification occurs only after `RoomShape` passes. An over-length
room therefore has one result. A room with an invalid shape remains
`MigrationRoomUnrepresentable`, regardless of length.

The hub is the sole evaluator of those three results. It evaluates them in the
level-1 allocation transaction. That transaction locks and snapshots the
enrolled key, complete durable room set, and pending ceremony. A negative
result creates no hub success, carrier allocation, pairing room, digest, or
partial pending object. The WebSocket handler MAY return only the existing
cause-neutral refusal.

The existing `requestId` is the allocation correlation member. The browser
generates 32 random octets for every attempt. Its canonical encoding is
base64url without padding. The signed `selfsamepairallocate` request contains
that exact value. The hub stores it with the pending ceremony.

`selfsamepairallocated` contains exactly the same `requestId`. It contains no
room, digest, account, grant, or authority value. Like every deployed
hub-to-browser frame, it is unsigned and origin-less. TLS authenticates the
hub connection. The echo prevents request confusion but does not authenticate
the migration plan.

The browser keeps an active map keyed by `(socketGeneration, requestId)`. It
accepts an acknowledgement only when exactly one live entry matches. It
atomically consumes the entry before starting the public carrier step. An
absent, unequal, stale, duplicate, or consumed value is discarded. It cannot
cancel or satisfy another attempt.

A copied acknowledgement with the active value can only start the reversible
transport wait. It cannot create hub state or reach consent. The later
hub-signed offer authenticates the plan and every consequential value. Missing
or invalid signed evidence terminates the attempt with no identity effect.

On success, `migrationRooms` is the complete unique raw room-byte array. The
hub sorts it by unsigned octets. `roomSetDigest` derives from that exact array.
The hub-signed offer binds both values through `offerCoreDigest` and
`intentDigest`. No browser DOM value participates.

Before final migration consent, the browser SHALL:

1. verify the v2 offer under the current authenticated profile;
2. recognize the exact v2 member set, transition tag, and application;
3. recompute `roomSetDigest` from `migrationRooms`;
4. compare every retained carrier, ceremony, transcript, and device binding;
5. build one private typed plan from those authenticated values.

The Path-B reducer renders exactly that plan's rooms in order.
`affectedChannelsNow()` and every `.clog[data-room]` query SHALL NOT source
`CeremonyObservation.GrantVerified.affectedChannels`. No filter, paging,
collapse, or lazy rendering SHALL hide no room.

`migration-display-state-changed` is non-terminal. It retains the verified
plan, disables confirmation, and sends no frame. A local rerender rebuilds the
complete list only from that plan. Confirmation returns only after byte-exact
comparison succeeds.

The shipped `path-b-ceremony.mjs` `CHANNEL` expression SHALL be replaced. Its
replacement is generated from the same `CanonicalRoomText` source. A malformed
authenticated plan becomes terminal `migration-plan-invalid` for that
ceremony. It creates no credential or hub effect. Recovery requires a new
ceremony.
It never becomes `invalid-grant-observation`.

The maxima in
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remain
unchanged. The current rendezvous baselines accept only 4,096 octets. They do
not carry the maximum credential/v2 records. Production use therefore depends
on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and
[[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Its Tier-1 gate remains open.

The complete production v1 comparison and substitution trace is:

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts substitution
crates/selfsame-web-device/src/lib.rs:1346      offer substitution
crates/selfsame-web-device/src/lib.rs:1360      provider-hint substitution
crates/selfsame-web-device/src/lib.rs:1369      statement substitution
crates/selfsame-app-identity/src/enrollment.rs:373 evidence-version comparison
crates/selfsame-app-identity/src/enrollment.rs:394 statement-payload comparison
crates/selfsame-app-identity/src/enrollment.rs:452 recognizer substitution
crates/selfsame-app-identity/src/enrollment.rs:552 statement-verification comparison
crates/selfsame-app-identity/src/enrollment.rs:662 evidence-version substitution
crates/selfsame-app-identity/src/profile.rs:385 profile comparison
crates/selfsame-app-identity/src/provider_hint.rs:115 provider-hint comparison
```

The trace is a baseline inventory, not a finite permission. The source scan
covers every production version constant and literal. It includes
`PROFILE_VERSION`, `profileVersion`, and `evidenceVersion`. Credential/v2 SHALL
reach no v1 comparison or substitution site. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.

### REQ-995 — Installed standing separates refresh, rotation, and clearing

The atomic installation record contains the retained device seed, accepted
grant, ApplicationId, account scope, device key, and grant reference. It also
contains the authenticated profile digest and exact `accountAuthority`. The
accepted grant pins its issuer.

Reload reconstructs installed account-device state only after these checks:

1. the record is complete and recognized;
2. the seed re-derives the recorded device key;
3. live authentication returns the exact ApplicationId;
4. the accepted authority selects the accepted issuer;
5. the grant verifies and matches every recorded binding.

Corrupt, partial, invalid, or wrongly bound state is
`credential-v2-recovery-required`. Resolver or issuer unavailability is the
non-terminal `credential-v2-verification-unavailable`. That state retains all
bytes, sends no frame, and claims no authority. An in-session action retries
verification.

A changed digest with unchanged `accountAuthority` and issuer is an ordinary
profile refresh. The existing grant SHALL re-verify under that same issuer.
Success atomically re-pins the new digest without a person prompt. Failure
preserves the old record and enters recovery.

A changed `accountAuthority` or selected issuer is
`credential-v2-application-authority-changed`. The browser displays the old
and new authority fingerprints. It also displays both issuer fingerprints. A
person MUST accept the change. Acceptance updates the record atomically with a
new successful grant verification. Cancellation preserves the old record.

While verified installation exists, a handle change is the local terminal
`account-device-handle-change-refused`. It sends no enrollment or authority
frame. It changes no local or hub state.

No hub error or chat frame clears the record. The deployed refusal is the
zero-signature `(error @room "bad-signature")`. The browser MAY display only
cause-neutral `hub-frame-refused`. No deployed or unknown slug diagnoses a
private cause or mutates credential state.

The only ordinary clear is the person action `unlink Selfsame wallet`. It
requires an exact confirmation screen. Cancellation leaves all state intact.
Acceptance atomically removes the installed grant and account-device
projection. It retains the device seed and sends no enrollment or authority
frame. Hub authority is unchanged.

Grant deletion creates no permanent device-key tombstone. The same key MAY
receive a new grant only through a fresh credential/v2 ceremony. That ceremony
requires current authentication, eligibility, capacity, both consent
decisions, and a newly verified grant. Local unlink cannot re-grant authority.

## Non-functional requirement

### NFR-918 — Room and authority lock graphs contain no hidden upgrade

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
The finalizer's exact `cbcl-member` access is a write lock. Its whole room-table
locks are write locks.

The authority graph covers only `cbcl-device-grant`, `cbcl-member`, and
`cbcl-pairaccount-grant`. Its exact cycle set remains:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

The generator SHALL reproduce those twelve cycles and every `AR` contention
case. It makes no claim about room or finalizer-tail tables.

The migration graph owns every room and tail lock. Allocation takes stable
account and pending ceremony first. Binding and member locks follow. It then
takes whole `cbcl-roommember` and `cbcl-account-roommember` read locks in that
order. Finalization uses the same prefix and whole write locks.

Both single-table `write-grant` paths take an exact write lock on their first
read. They use `mnesia:read/3` with `write`. They perform no read-to-write
upgrade.

Both `redeem-and-grant` variants join two exact write locks. They take the
`cbcl-invite` token lock first. The applicable `cbcl-roommember` or
`cbcl-account-roommember` key follows. `cbcl-chat-invite:redeem-tx` also takes
its write lock on the first read. It performs no upgrade before update or
delete.

The generated table order places `cbcl-invite` before both room tables. It
places `cbcl-roommember` before `cbcl-account-roommember`. Any operation
touching both room tables takes both whole write locks in that order.

Finalizer, compensation, status, and operator paths use one generated tail
order. Compensation uses exact captured keys and no `?ALL` scan. Withdrawal
and reinstatement take stable account before alias and tombstone keys.
Read-only recovery acquires no later write lock.

The migration graph includes every lock and exact mode. It includes cross-lane
authority locks. Its only cycles are projections of the twelve authority
cycles. It has no additional cycle or upgrade. Omission, opposite order, or an
unstated mode is non-conforming.

Every affected transaction runs at Mnesia level 1. It uses an explicit integer
`Retries >= 2`. Nested transactions are prohibited. Consumers map `nomore`,
exhausted `#cyclic{}`, and `nested_transaction` to `authority-busy`. No partial
effect survives.

## Contract

### CON-975 — The correction, dependency, and survivor ledgers are exact

This amendment applies only these replacements:

1. REQ-994 replaces
   [[spec-008-0.4.15-authenticated-allocation-and-consent#REQ-992]] in full.
2. REQ-995 replaces
   [[spec-008-0.4.15-authenticated-allocation-and-consent#REQ-993]] in full.
3. NFR-918 replaces
   [[spec-008-0.4.15-authenticated-allocation-and-consent#NFR-917]] in full.
4. ADR-953 replaces
   [[spec-008-0.4.15-authenticated-allocation-and-consent#ADR-952]] in full.
5. TEST-1129 through TEST-1133 replace
   [[spec-008-0.4.15-authenticated-allocation-and-consent#TEST-1125]] through
   [[spec-008-0.4.15-authenticated-allocation-and-consent#TEST-1128]].

Every F-A through F-E correction remains a hard stop. SPEC-007 REQ-812 also
remains a hard stop.

The exact cbcl-pairing survivor set contains TEST-030 through TEST-049. It also
contains TEST-050 except its replaced control portion and retired positive
witness. That witness remains a negative obligation. TEST-051 through TEST-056
and TEST-058 survive. TEST-057 does not survive because TEST-058 replaces it.

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
and TEST-088 through TEST-092. Every effective Selfsame test through TEST-1108
also survives. TEST-1129 through TEST-1133 are the current correction tests.
No superseded portion or open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.7, and
cbcl-bus 0.16.9. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`. That did-crdt baseline has a
4,096-octet rendezvous cap. It is deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]].

Credential/v2 production enablement depends on closing the PROTO-002 Tier-1
gate. Both rendezvous implementations MUST carry generated maximum sealed
records. The cbcl-bus GATE-04 owns the cross-repository transit evidence.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It
authorizes no code, allocation, release, or deployment.

## Decision

### ADR-953 — Correlation is anti-confusion and signed evidence authenticates

The existing `requestId` correlates allocation acknowledgement to one active
browser attempt. The acknowledgement is unsigned. It therefore provides no
application, account, or plan authority. The later hub-signed offer provides
that authority before consent.

Admission causes remain hub-local. A refusal or forged acknowledgement can
deny service but cannot create a grant. Cross-ceremony success confusion is
closed by the one-use request map.

An ordinary profile refresh is not an authority rotation. Exact authority or
issuer change remains person-confirmed. Local unlink remains person-owned.

Room grants and invite redemption take write locks on first read. Carrier
constructibility is insufficient without PROTO-002 transit evidence.

## Test specification

### TEST-1129 — Allocation acknowledgement cannot cross ceremonies

Run two same-account ceremonies concurrently on one socket generation. Swap,
duplicate, suppress, reorder, and replay their acknowledgements. Mutate and
remove `requestId`. Only the exact active value starts its own transport wait.
Consumption occurs before that transition.

Inject a copied acknowledgement with the active value but no hub allocation.
It reaches no consent, credential, or identity effect. Missing hub-signed
evidence terminates the attempt. Prove every hub admission failure creates no
allocation state or success.

### TEST-1130 — Grammar, display, and version paths are disjoint

Generate every symbol branch, bare `@`, maximum `/`, and every one-over case.
Require invalid shape to be `MigrationRoomUnrepresentable`. Require a shaped
over-length room to be `MigrationPlanTooLarge`.

Mutate the authenticated room array and every DOM condition. The display stays
exact or enters non-terminal `migration-display-state-changed`. Rerender only
from the plan. A malformed signed plan enters terminal
`migration-plan-invalid` and never `invalid-grant-observation`.

Scan every production version constant and literal. Include both
`evidenceVersion` sites. Prove v2 reaches none and v1 bytes remain exact.

### TEST-1131 — Maximum credential records transit the declared rendezvous

Generate maximum offer and reverse records with sealing overhead. Require each
record to fit the 69,632-octet PROTO-002 cap. Put, repeat-put, get, and
repeat-get each exact record through both rendezvous implementations.

Reject 69,633 octets before storage. Prove the declared did-crdt baseline fails
this test at 4,096 octets. GATE-04 cannot close before both implementations and
the PROTO-002 gate pass.

### TEST-1132 — Installed state refreshes, rotates, unlinks, and re-grants

Change only the profile digest. Keep authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consents, and a new grant. Every local-only
shortcut fails.

### TEST-1133 — Lock graphs and effective ledgers are closed

Generate the twelve authority cycles. Generate the complete migration graph.
Both `write-grant` paths and both `redeem-and-grant` paths use write locks on
first read. Require the invite-before-room order and no additional upgrade or
cycle.

Force bounded retry exhaustion and prohibited nesting. Require
`authority-busy`, no partial effect, and writer progress. Require the exact
three-repository ledgers, every version site, 42 GATE-04 boxes, and the parent
pointer. Removing any obligation fails.

## Changelog

- **0.4.16-draft — 2026-08-23 — review candidate, not approved.** Defines the
  unsigned acknowledgement boundary, deliverable carrier gate, exact version
  trace, lock upgrades, status classes, and installed-state transitions. No
  implementation or deployment authority is claimed.
