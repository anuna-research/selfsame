---
id: spec-008-0.5.17-final-claude-adversarial-review-2026-08-25
title: SPEC-008 0.5.17 — final Tier-1 cross-model adversarial review (REJECT)
status: complete
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
reviewer-model: claude-opus-5
generating-family: OpenAI GPT-5
verdict: REJECT
date: 2026-08-25
---

# SPEC-008 0.5.17 — final Tier-1 cross-model adversarial review

## Reviewer identity, authentication path, Circus attempt, and independence

**Reviewer family.** Anthropic Claude (`claude-opus-5`), running in the
interactive Claude CLI. The generating family is OpenAI GPT-5 (`gpt-5.6-sol`),
recorded identically in all four parents. The two families are distinct, so the
cross-model requirement is satisfied.

**Authentication path.** The review ran on the person's Claude Max
subscription. It used no Anthropic API credential. Both `ANTHROPIC_API_KEY`
and `ANTHROPIC_AUTH_TOKEN` were confirmed absent by testing whether each name
was set, without printing either value.

```
$ env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status
authMethod       : claude.ai
apiProvider      : firstParty
subscriptionType : max
```

Those are the three required values and the only three recorded here. No
secret, email, organization identifier, or account identifier is reproduced.

**Circus attempt.** Branch `circus/spec008-v0517-review/1`, worktree
`/Users/anuna-01/Code/selfsame-spec008-v0517-review-1`, attempt 1.

**Preceding incomplete attempt.** `spec008-v0516-review/1` ended with transport
exit 130 before a report or sentinel. It supplied no verdict, and this review
inherited none. That interruption is infrastructure and is not reported as a
candidate finding.

**Host.** Darwin 25.3.0 `arm64`; rustc/cargo 1.96.0; node v22.12.0; npm 11.13.0;
puppeteer 23.11.1 from the tracked lockfile.

**Commits reviewed.**

| Repository | Role | Revision |
|---|---|---|
| selfsame | executable candidate | `60abb8b25e858be8005151e47ea2a8044d130e80` |
| selfsame | governing authority | `9337fa9660ee47c01182dafc5f17b0ade9d1c963` |
| selfsame | preceding executable | `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7` |
| cbcl-bus | executable candidate | `14f26065cb147633416def15d0a7d8603bce2e09` |
| cbcl-bus | governing authority | `d35cdacfcd379d54771c655aea87f6f42a1db596` |
| cbcl-pairing | executable candidate | `62ef4a968b46b4836374fcee1d78c410f730a7a7` |
| cbcl-pairing | governing authority | `6f841595d0127279d0d6c1cb850c68900d379ade` |
| cbcl-rs | dependency | `febc6691e6dd2d5f7116b1a4d84c984b64717564` |
| did-crdt | runtime dependency | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` |

Every listed revision was confirmed to exist as a committed object before use.

**Independence statement.** Every measurement below was taken from clean scratch
copies I created myself, under `mktemp -d` scratch
`/tmp/spec008-0517-review.LmTjYZ`. The five-repository closure was built as
detached clones and each reported `git status --porcelain` empty:

```
selfsame       -> 60abb8b25e858be8005151e47ea2a8044d130e80 dirty=[0]
cbcl-pairing   -> 62ef4a968b46b4836374fcee1d78c410f730a7a7 dirty=[0]
cbcl-rs        -> febc6691e6dd2d5f7116b1a4d84c984b64717564 dirty=[0]
did-crdt       -> 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1 dirty=[0]
cbcl-bus       -> 14f26065cb147633416def15d0a7d8603bce2e09 dirty=[0]
```

The person's working trees were not used as evidence. `selfsame` carried more
than thirty tracked modifications across `crates/`, and `did-crdt`'s HEAD was
`94db956` rather than the pinned `1f409a42`. No output APK, patch script, or
dirty file of the person's was modified or deleted, and no directory was
deleted or overwritten during the review. Review scratch is left in place.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 1 |
| LOW | 4 |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. One MEDIUM stands:
**M-1**. SPEC-008 0.5.17's only substance is four-parent authority alignment,
and its own current law still carries two hub-parent versions. The singularity
that [[SPEC-008-production-pairing-claimant#TEST-1157]] requires in its first
sentence does not hold in the parent that contains it.

Everything else measured clean. The fail-closed invoke-discovery correction is
genuine and load-bearing: I verified all eight required claims, executed the
required mutation with an applied diff, and confirmed byte restoration. The
protocol regression pass reproduced the 0.5.15 review's PASS evidence for F-A
through F-E, authenticated intent display, consent ordering, transaction
cleanup, source authentication, and reproducible WASM. The single blocking
finding is narrow and documentation-only, and its correction is two lines.

## Findings

### M-1 (MEDIUM) — the alignment revision leaves two hub-parent versions in current law, so TEST-1157's own singularity requirement fails against the parent that states it

**Where.** `specs/SPEC-008-production-pairing-claimant.md:1443` and
`specs/SPEC-008-production-pairing-claimant.md:1854`, at governing authority
`9337fa9`.

**What.** [[SPEC-008-production-pairing-claimant#TEST-1157]] opens with:

> Require this parent to contain one protocol set, hub set, Selfsame set,
> review set, and pointer set.

Counting hub-parent citations in SPEC-008's current law — lines 1 through 2205,
that is, everything before `## Changelog`:

| Revision | `SPEC-053 0.17.13-draft` | `SPEC-053 0.17.9-draft` | `SPEC-053 0.17.12-draft` |
|---|---|---|---|
| 0.5.16 (`cd8d3b5`) | 0 | 5 | 1 |
| 0.5.17 (`9337fa9`) | 4 | 2 | 0 |

The hub set is not singular. The alignment commit `9337fa9` updated four
sibling citations — lines 1129, 1331, 1429, and 1542 — and missed two.

The second site is the more serious of the two, because it sits inside
TEST-1157 itself:

> Require this Selfsame parent and its open review gate. Require cbcl-bus
> SPEC-053 **0.17.9-draft** to name this coordinated review set.

cbcl-bus SPEC-053 0.17.9-draft is commit `1b145cb`. I read it. It names
`coordinated-wallet-design: selfsame SPEC-008 0.5.13-draft`,
`coordinated-safety-design: selfsame SPEC-007 0.3.6-draft`, and
`coordinated-pairing-design: cbcl-pairing SPEC-001 0.5.7-draft`. It does not
name this coordinated review set and cannot. A reviewer executing TEST-1157
verbatim therefore fails on two independent grounds: the parent does not carry
one hub set, and the hub parent the test names does not carry this review set.

The first site is inside CON-987, current law defining exact wire bodies:

> `migrationConfirmationDigest` is the exact raw digest defined by cbcl-bus
> SPEC-053 **0.17.9-draft** CON-012.

Line 1429 of the same section already cites `SPEC-053 0.17.13-draft CON-012`.
One normative section therefore resolves the same hub contract through two
different parent revisions.

**Why it is MEDIUM and not higher.** There is no wire consequence. I extracted
CON-012's `migrationConfirmationDigest` definition from both `1b145cb`
(0.17.9) and `d35cdac` (0.17.13) and diffed them: byte-identical, including the
domain-separation label, the length-prefixed `previewIssuerDid`, and the full
concatenation order through `deviceKeyDigest`. The defect also fails closed —
TEST-1157 as written produces a spurious refusal, never a spurious pass.

**Why it is MEDIUM and not LOW.** The two lines predate this revision, but
0.5.17 contains nothing else. Its changelog asserts that "the coordinated
review set now names Selfsame SPEC-007 0.3.8, cbcl-pairing SPEC-001 0.5.8, and
cbcl-bus SPEC-053 0.17.13," and its stated scope is exactly the sweep that
these two lines escaped. TEST-1157 is a Tier-1 verification requirement that
cannot be satisfied as written. An alignment-only revision whose alignment is
incomplete, in the specific test that enforces alignment, is a defect of that
revision.

**Failure scenario.** A reviewer executes TEST-1157 against SPEC-008 0.5.17.
The first requirement — one hub set — fails, because the parent carries both
`0.17.13-draft` and `0.17.9-draft`. The reviewer then reads
`cbcl-bus SPEC-053 0.17.9-draft` as directed and finds it names SPEC-008
0.5.13, SPEC-007 0.3.6, and SPEC-001 0.5.7. TEST-1157 fails, and the
coordinated Tier-1 gate cannot close.

**Correction.** Update both lines to `0.17.13-draft`. The three other parents
need no change; each was checked and is already singular.

### L-1 (LOW) — `skippedInvokeCallers` in the screen gate is tautologically empty and can never fire

`tests/screens.mjs` at `60abb8b`:

```js
  const scannedInvokeCallers = new Set();
  for (const file of invokeCallerFiles) {
    scannedInvokeCallers.add(file);
    ...
  }
  const skippedInvokeCallers = invokeCallerFiles.filter((file) => !scannedInvokeCallers.has(file));
  if (skippedInvokeCallers.length) {
    errors.push(`invoke-surface: discovered but did not scan ${skippedInvokeCallers.join(', ')}`);
  }
```

`scannedInvokeCallers` is populated with every member of `invokeCallerFiles`
unconditionally, inside the loop that iterates that same array. The filter is
therefore always empty and the error branch is unreachable. It reads as a
safety net where none exists.

This costs nothing today, because the protection that actually works is the
caller-count ratchet, which I proved kills the required mutation. But the dead
check makes the block look like it has two independent guards when it has one.
Either delete it or give it a real referent — for example, assert the scanned
set against a discovered set computed separately, rather than against a set
built from the discovered set.

### L-2 (LOW) — the ratchet compares caller count, not caller identity

`EXPECTED_INVOKE_CALLER_COUNT = 3` guards the cardinality of the discovered
set. A discovery change that simultaneously drops one caller and admits another
keeps the count at three and passes green.

[[SPEC-008-production-pairing-claimant#TEST-1169]] specifies exactly a
"reviewed caller-count ratchet," so the implementation conforms to its stated
law and this is not a conformance defect. Recorded as hardening: comparing the
sorted `invokeCallerFiles` join against a reviewed literal would make the
ratchet identity-stable at no extra cost, and the error message already
computes that string.

### L-3 (LOW) — discovery is extension-limited to `.js`

`discoverJavascriptModules` admits a file only when
`extname(entry.name) === '.js'`. An `.mjs` module under `src`, or an inline
`<script>` block in `src/index.html`, would not be discovered, the count would
remain three, and the gate would pass while an invoke caller went unscanned.

Currently latent and narrow. `src` holds exactly `app.js`, `app-identity.js`,
`pairing.js`, `index.html`, and `styles.css`; `index.html` has one `<script>`
tag and it is an external module reference with no inline body; and the gate's
own dev server maps only `.html`, `.js`, and `.css`, serving anything else as
`application/octet-stream`, so an `.mjs` frontend module would fail to load in
the browser before it could evade the scan.

### L-4 (LOW) — "candidate" denotes two different Selfsame revisions without a sentence saying so

SPEC-008 0.5.17 frontmatter records
`implementation-baseline: 60abb8b25e858be8005151e47ea2a8044d130e80`. Its
CON-985 paragraph records "The exact candidate closure is Selfsame
`48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`," and cbcl-bus's `selfsame.sha`
and `selfsame_web_device.provenance.json` both pin `48a0c74`.

I verified these are mutually consistent rather than contradictory: `48a0c74`
and `60abb8b` have byte-identical production trees (see the protocol regression
section), so WASM built from either is identical, and naming the build source
in the provenance record is correct. But
[[SPEC-008-production-pairing-claimant#TEST-1165]] says "From clean detached
checkouts at the five exact candidate revisions," and
`scripts/check-selfsame-wasm-rebuild.sh` requires
`git -C "${selfsame_path}" rev-parse HEAD` to equal `selfsame.sha`. A reviewer
who resolves "candidate" to the executable candidate `60abb8b` gets a refusal
from a correct gate. One sentence distinguishing the executable baseline from
the vendored-artefact source pin would remove the trap.

## Invoke-discovery mandate — all eight claims independently verified

All measurements ran from a clean `git archive` copy of `60abb8b` with the
tracked `node_modules` linked read-only.

| # | Claim | Result |
|---|---|---|
| 1 | Recursively discovers JavaScript modules below `src` | **VERIFIED** — empirically, see probe below |
| 2 | Derives invoke callers from discovered source, not a module list | **VERIFIED** |
| 3 | Scans every member of the derived caller set | **VERIFIED** — empirically, see probe below |
| 4 | Caller-count ratchet fails when discovery loses one caller | **VERIFIED** — mutation measured |
| 5 | Unmutated `CI=true npm run screens` exits zero | **VERIFIED** — EXIT=0 |
| 6 | Excluding `src/pairing.js` from discovery exits one | **VERIFIED** — EXIT=1 |
| 7 | That refusal is an `invoke-surface` caller-count error | **VERIFIED** — sole error, exact text below |
| 8 | Restoring the candidate returns the screen gate to green | **VERIFIED** — EXIT=0 after byte restoration |

### Claims 5 through 8 — the required mutation

Baseline, before any change:

```
$ shasum -a 256 tests/screens.mjs
01e76fcec3a54ecca426f9d9535358d31bc0059b5fb1cabcc5eb96969f6560de  tests/screens.mjs
$ git cat-file blob 60abb8b:tests/screens.mjs | shasum -a 256
01e76fcec3a54ecca426f9d9535358d31bc0059b5fb1cabcc5eb96969f6560de  -

$ CI=true npm run screens
    invoke-surface every invoke resolves; 10 registered and unreached (...)
    all screens rendered clean                              EXIT=0
```

The mutation was applied as a diff and the diff was displayed before the
measurement was counted, so this is not a no-op:

```diff
--- screens.mjs.pristine
+++ tests/screens.mjs
@@ -1047,7 +1047,7 @@
       const relativePath = join(relativeDirectory, entry.name);
       if (entry.isDirectory()) {
         discoverJavascriptModules(join(directory, entry.name), relativePath);
-      } else if (entry.isFile() && extname(entry.name) === '.js') {
+      } else if (entry.isFile() && extname(entry.name) === '.js' && entry.name !== 'pairing.js') {
         javascriptModules.push(relativePath);
       }
     }
```

```
$ shasum -a 256 tests/screens.mjs      # mutated, differs from baseline
90935643f1cebbacb057cf2e39049adad8f123a4eddaa63c45d9a9b587e5f28b  tests/screens.mjs

$ CI=true npm run screens                                   EXIT=1
ERRORS:
  invoke-surface: discovered 2 JavaScript invoke callers, expected 3 (src/app-identity.js, src/app.js) — caller discovery changed and must be reviewed
```

That is the only error raised, and it is an `invoke-surface` caller-count
error, satisfying claims 6 and 7 exactly as
[[SPEC-008-production-pairing-claimant#TEST-1169]] words them.

Byte restoration was verified against the committed object, not merely against
a backup:

```
$ git cat-file blob 60abb8b:tests/screens.mjs > tests/screens.mjs
$ shasum -a 256 tests/screens.mjs
01e76fcec3a54ecca426f9d9535358d31bc0059b5fb1cabcc5eb96969f6560de  tests/screens.mjs
$ cmp tests/screens.mjs screens.mjs.pristine                -> IDENTICAL

$ CI=true npm run screens
    all screens rendered clean                              EXIT=0
```

### Claims 1 and 3 — recursion and total scanning, proved rather than read

Structural reading is not evidence that recursion descends, because `src` is
flat at the candidate. In a second clean copy I planted a module two levels
down calling a command that `lib.rs` does not register:

```
$ mkdir -p src/nested/deeper
$ cat > src/nested/deeper/probe.js
  import { invoke } from '@tauri-apps/api/core';
  export const probe = () => invoke("definitely_not_registered_command");

$ CI=true npm run screens                                   EXIT=1
ERRORS:
  invoke-surface: discovered 4 JavaScript invoke callers, expected 3 (src/app-identity.js, src/app.js, src/nested/deeper/probe.js, src/pairing.js) — caller discovery changed and must be reviewed
  invoke-surface: src/nested/deeper/probe.js calls "definitely_not_registered_command", which lib.rs does not register — the call rejects and any screen after it claims a state nothing produced
```

Both errors matter. The first proves discovery descended two directory levels
and that the ratchet notices an addition as well as a loss. The second proves
the newly discovered module was actually **scanned**, not merely counted —
which is claim 3, and which a count-only check could not have produced.

### Claim 4 — the ratchet is load-bearing, not decorative

To establish that the ratchet is what closes the hole rather than incidental
scaffolding, I applied the `src/pairing.js` discovery exclusion together with a
neutralised ratchet (`EXPECTED_INVOKE_CALLER_COUNT = invokeCallerFiles.length`):

```
$ CI=true npm run screens                                   EXIT=0
    invoke-surface every invoke resolves; 23 registered and unreached (..., cbcl_v2_finish, cbcl_v2_recognise, cbcl_v2_relay_decide, ...)
    all screens rendered clean
```

The gate passes green, announces that "every invoke resolves," and silently
reports 23 unreached commands instead of 10 — the precise silent failure the
0.5.15 review recorded as its M-1. The count ratchet is the mechanism that
converts that into a refusal.

### Discovery-evading call forms

I checked whether any current call form escapes the two regexes. The filter is
`/\binvoke\s*\(/` and the extractor is `/invoke\(\s*["'](\w+)["']/g`, so a
space-separated `invoke (` would be counted but not extracted. No such form
exists, and no dynamic (non-literal) command name exists:

| File | literal `invoke("cmd")` | total `invoke(` |
|---|---|---|
| `src/app.js` | 18 | 18 |
| `src/app-identity.js` | 6 | 6 |
| `src/pairing.js` | 13 | 13 |

The counts are equal in every file, so every current call site is extracted.
`src/index.html` contains no inline script and no `invoke` text.

## Protocol regression mandate

### Scope of `48a0c749..60abb8b`

The range contains six commits. Its non-documentation change is confined to
`tests/screens.mjs`, but the range is **not** limited to that file: it also
modifies `specs/SPEC-008-production-pairing-claimant.md` and adds four
trajectory documents.

```
$ git diff --name-status 48a0c749 60abb8b
M	specs/SPEC-008-production-pairing-claimant.md
A	specs/trajectory/SPEC-008/spec-008-0.5.14-continuous-gate-reachability-remediation-2026-08-25.md
A	specs/trajectory/SPEC-008/spec-008-0.5.15-authenticated-relay-release-source-2026-08-25.md
A	specs/trajectory/SPEC-008/spec-008-0.5.15-implementation-claude-adversarial-review-2026-08-25.md
A	specs/trajectory/SPEC-008/spec-008-0.5.15-implementation-reviewer-prompt-2026-08-25.md
M	tests/screens.mjs
```

The substantive requirement — that no production tree object changed — holds
exactly. I compared tree object hashes rather than trusting the diff:

| Path | `48a0c74` vs `60abb8b` |
|---|---|
| `src`, `src-tauri`, `crates`, `scripts` | SAME |
| `contexts`, `test-vectors`, `outputs`, `docs`, `evidence` | SAME |
| `Cargo.toml`, `Cargo.lock`, `package.json`, `package-lock.json` | SAME |
| `cbcl-pairing.sha`, `cbcl-rs.sha`, `did-crdt.sha` | SAME |
| `plan.spl`, `spec-004.spl`, `README.md` | SAME |
| `tests` | DIFFERS — `tests/screens.mjs` only |

Because `crates` is byte-identical, the vendored WASM provenance pin of
`48a0c74` remains correct for the `60abb8b` candidate. This is the basis for
L-4 and for carrying the 0.5.15 code dispositions forward.

### Regression check of the 0.5.15 rejected review's PASS evidence

I read the rejected review at preserved commit `32217de` after completing the
independent measurements above, and re-tested rather than inherited. Its
verdict was REJECT on one MEDIUM — the hand-maintained invoke caller list —
which the 0.5.16 correction closes and which I re-verified from scratch.

| Item | 0.5.15 disposition | This review |
|---|---|---|
| F-A exact `(applicationId, relayOrigin)` | CLOSED | **CONFIRMED** — code re-read independently |
| F-B authenticated display | CLOSED | **CONFIRMED** — code re-read independently |
| F-C split plan/preview/executor | CLOSED | **CONFIRMED** — code re-read independently |
| F-D fresh scope end to end | CLOSED | **CONFIRMED** — signed-offer field under fetched profile |
| F-E no relay-operatorship proof | CLOSED (honest residual) | **CONFIRMED** — unchanged |
| REQ-812 post-consent derivation | CLOSED | **CONFIRMED** — code re-read independently |
| Authenticated intent display | PASS | **CONFIRMED** |
| Consent ordering | PASS | **CONFIRMED** |
| Transaction cleanup (TEST-1162) | PASS | **CONFIRMED** — executed, EXIT=0 |
| Source authentication (TEST-1168) | PASS | **CONFIRMED** — executed, drift refused |
| Reproducible WASM digest | PASS | **CONFIRMED** — digest matched, vendor suite green |
| Wallet pairing lifecycle capstone | PASS | **CONFIRMED** — executed, 5/5 |
| Two live cbcl-bus capstones | PASS | **NOT RUN** — host disk exhausted, see below |

**F-A.** `src-tauri/src/cbcl_v2_policy.rs`: the row key is the full
`(application_id, relay_origin)` tuple in `state`, `insert`, and `remove`.
`recognise` refuses on version mismatch, on `rows.len() > MAX_ROWS` (256), on
any unrecognised pair, on non-strict ordering, and on a non-canonical
re-encoding — it never degrades to an empty policy and a fresh TOFU prompt.
`recognise_pair` additionally requires `origin.origin == relay_origin`, so a
non-canonical origin cannot alias a trusted row. The only production write is
in `cbcl_v2_claimant.rs`, inside `bind_finished_profile`, which first requires
`is_awaiting_profile_authorisation()` — that is, after both Finished values —
and for an already-trusted pair re-checks `state(...) == TrustedPair` and
otherwise returns `PairingPolicyUnavailable`.

**F-B.** `cbcl_v2_commands.rs`'s `intent_view` constructs its view exclusively
from `CredentialV2Display` accessors — `application_id()`, `https_origin()`,
`relay_origin()`, `permissions()`, `device_binding()`, `account_provenance()`,
`tofu_state()`, `transition()`. No peer-supplied string reaches the view, and an
unrecognised TOFU state renders `"unknown"` rather than a trusted label.

**F-C.** `authorise_claimant_relay(plan, decision)` returns
`Option<RelaySocketCapability>`; `prepare_claimant(capability: RelaySocketCapability, ...)`
takes it **by value**, so the socket authority is single-use and cannot be
replayed. `Decline` returns `Ok(None)`. Every other `(pair_state, decision)`
combination returns `PairingRelayRefused`. `RelaySocketCapability`'s `Debug`
implementation prints `RelaySocketCapability([REDACTED])`.

**REQ-812.** `cbcl_v2_preliminary_decide` does not take the passcode at all on
the decline path, sends only the intent-decision object, and returns
`"declined"` — no derivation occurs. On approval it requires presence
(`PresenceRequired` when absent or empty) and only then calls
`preview_identity`, which performs exactly one
`Custody::use_hierarchy_root`. `cbcl_v2_final_decide`'s first durable boundary
performs no issuer or grant effect.

### Executed commands

```
$ cargo clippy --workspace --all-targets --locked -- -D warnings
    Finished `dev` profile ... in 39.39s                    EXIT=0

$ cargo test -p selfsame --locked --lib -- --ignored --exact \
    cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 66 filtered out
                                                            EXIT=0
$ grep -Fq "test result: ok. 1 passed; 0 failed; 0 ignored"   -> PASS
```

The CI step's own `grep -Fq` assertion holds, so a renamed or unselected test
could not have exited successfully.

```
$ CI=true npm run screens                                   EXIT=0
$ CI=true npm run e2e:wallet-pairing                        EXIT=0
ok 1 - TEST-1161 pending completion retains the authenticated offer profile for restart recovery
ok 2 - TEST-814 CBCL wallet states are keyboard complete and WCAG-clean
ok 3 - TEST-1161 restart recovery preserves pending through rotation consent
ok 4 - TEST-1159 installed links reload, prompt on rotation, and unlink locally
ok 5 - TEST-1162 interrupted pre-payload links are visible and locally abandonable
# tests 5  # pass 5  # fail 0
```

```
$ node --test apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs     EXIT=0
ok 1 - TEST-120: CI cannot silently select zero browser-package rebuilds
ok 2 - vendored Selfsame verifier has a valid, matching Path-B WASM ABI
ok 3 - TEST-024d: the vendored predicate is deterministic and touches no network
ok 4 - the vendored surface offers neither authorization nor a legacy pairing engine
ok 5 - the vendored constructors refuse inputs their own recognisers reject
ok 6 - the f2 claimant bootstrap binds the exact invitation relay and burns on cancel
# tests 6  # pass 6  # fail 0
```

Reproducible-WASM digest, measured directly against the vendored artefact:

```
$ shasum -a 256 apps/cbcl_chat/priv/web/selfsame/selfsame_web_device_bg.wasm
a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07
```

This equals the digest named in SPEC-008 CON-985, in
`selfsame_web_device.provenance.json`, and in the review mandate. The
provenance record's four source pins equal the closure exactly
(`cbclPairingSha` `62ef4a9`, `cbclRsSha` `febc669`, `didCrdtSha` `1f409a4`,
`selfsameSha` `48a0c74`), with `rustcVersion` 1.96.0, `wasmBindgenVersion`
0.2.126, and `buildHost` `x86_64-unknown-linux-gnu`.

cbcl-bus's pin files are self-consistent with the reviewed closure:
`cbcl-pairing.sha` `62ef4a9`, `cbcl-rs.sha` `febc669`, `did-crdt.sha`
`1f409a4`, `selfsame.sha` `48a0c74`. Selfsame's three pin files name the same
sibling revisions as the review mandate.

### Source authentication (TEST-1168), measured adversarially

The release builder's source authentication is a pure git gate that runs before
Cargo, so it is measurable without a full build.

Positive case, at the candidate pin — every condition the builder tests:

```
pin file        : 62ef4a968b46b4836374fcee1d78c410f730a7a7
sibling HEAD    : 62ef4a968b46b4836374fcee1d78c410f730a7a7
tracked drift   : [0] lines
untracked exec  : [0] entries
ancestry        : OK
executable diff : none
```

Executable drift, using TEST-1168's named pre-credential/v2 revision
`197d4cb3d1560ab5328df28fc984269799c510f9`, with both the pin and the sibling
checkout moved to it:

```
$ ./scripts/build-cbcl-pairing-relay.sh release                EXIT=1
build-cbcl-pairing-relay: latest pin has executable drift from Selfsame's reviewed baseline.
```

The refusal occurred before any compilation, which is what CON-985 requires.
`62ef4a9` is not an ancestor of `197d4cb`, so the `merge-base --is-ancestor`
guard is the one that fired.

Feature injection and cache substitution are refused for release builds:

```
$ RUSTFLAGS='--cfg feature="conformance-allocation"' ./scripts/build-cbcl-pairing-relay.sh release
build-cbcl-pairing-relay: RUSTFLAGS is forbidden for release builds.        EXIT=1

$ CARGO_TARGET_DIR=/tmp/x ./scripts/build-cbcl-pairing-relay.sh release
build-cbcl-pairing-relay: CARGO_TARGET_DIR is forbidden for release builds.
  Release artifacts are rebuilt in a fresh private Cargo target.            EXIT=1
```

## Current authority

### Four-parent review set and generation provenance

| Parent | Revision | Version | Names review set | Provenance |
|---|---|---|---|---|
| Selfsame SPEC-008 | `9337fa9` | 0.5.17-draft | partially — see M-1 | GPT-5 / gpt-5.6-sol / session `01a029aa` |
| Selfsame SPEC-007 | `9337fa9` | 0.3.8-draft | yes | GPT-5 / gpt-5.6-sol / session `01a029aa` |
| cbcl-pairing SPEC-001 | `6f84159` | 0.5.8-draft | yes | GPT-5 / gpt-5.6-sol / session `01a029aa` |
| cbcl-bus SPEC-053 | `d35cdac` | 0.17.13-draft | yes | GPT-5 / gpt-5.6-sol / session `01a029aa` |

All four record the same `generation-model-family`, `generation-model-version`,
and `generation-session`. Their `generation-synthesis-trajectory` values differ,
which is correct — each describes its own document's trajectory, and SPEC-053's
GATE-04 box requires shared provenance, not an identical trajectory string.

Three of the four name the coordinated set — SPEC-008 0.5.17, SPEC-007 0.3.8,
SPEC-001 0.5.8, SPEC-053 0.17.13 — without residue. SPEC-008 names it correctly
in its frontmatter, in its review-set paragraph, and in its changelog, but
retains two `0.17.9-draft` citations in current law. That is M-1.

Within SPEC-008's current law the pairing set (`SPEC-001 0.5.8-draft`, five
occurrences) and the safety set (`SPEC-007 0.3.8-draft`) are singular. Only the
hub set is not.

### Historical changelog and evidence versions remain historical

Verified for all four parents:

- SPEC-008: zero historical changelog entries removed or rewritten by `9337fa9`;
  the diff adds only the new 0.5.17 entry.
- SPEC-007: the same — only the new 0.3.8 entry is added.
- SPEC-053: zero historical `v0.17.x-draft` changelog lines removed by `d35cdac`.
- SPEC-001: only a new 0.5.8 entry, plus the revision-history summary range
  extended from `0.1.0 -> 0.5.7-draft` to `0.1.0 -> 0.5.8-draft`.
- `evidence/` is untouched in Selfsame (`48a0c74..9337fa9`) and in cbcl-pairing
  (`62ef4a9..6f84159`); cbcl-bus's alignment commit `a4f46c4..d35cdac` touches
  only `specs/SPEC-053-key-root-identity.md`.

### cbcl-pairing records credential/v2 locally complete, documentation-only

`6f84159` is a single commit above the executable candidate `62ef4a9` and
touches one file:

```
$ git diff --name-status 62ef4a9 6f84159
M	specs/SPEC-001-reusable-blind-pairing.md
```

Every other top-level tree object is byte-identical, including `src`, `tests`,
`dialects`, `schemas`, `vectors`, `mutations`, `fuzz`, `tools`, `Cargo.toml`,
and `Cargo.lock`. The change is documentation-only, as required.

It records what the mandate requires:

```
-implementation-status: credential-v1-local-complete; credential-v2-unimplemented
-implementation-baseline: c22f2d0526432c01c56d64be35a8d4f87516ec92
+implementation-status: credential-v1-local-complete; credential-v2-local-complete
+implementation-baseline: 62ef4a968b46b4836374fcee1d78c410f730a7a7
```

### Controlled-language lint

Strict descriptive lint, `usdd-lint.sh --type descriptive --strict`, whose
self-test passed on this host:

| Target | Errors | Delta from predecessor |
|---|---|---|
| SPEC-008 0.5.17 (`9337fa9`) | 3 | 0 — 0.5.16 also had 3 |
| SPEC-007 0.3.8 (`9337fa9`) | 3 | 0 — 0.3.7 also had 3 |
| 0.5.17 trajectory document | **0** | clean |
| cbcl-pairing SPEC-001 0.5.8 (`6f84159`) | 6 | 0 — 0.5.7 also had 6 |
| cbcl-bus SPEC-053 changed lines only | **0** | clean |

The 0.5.17 increment introduces no new lint debt anywhere. The cbcl-bus parent
was linted on changed lines only, as instructed, because unrelated full-parent
debt predates this work; those lines are clean.

The residual errors are pre-existing and are almost entirely the tool's
documented regular-language ceiling rather than genuine controlled-language
violations: they flag participial `required` and adjectival `optional` — "the
record required by CON-985", "the hub's optional replay tombstone", "proximity
required with a named mechanism" — not modal verbs. One genuine lowercase
normative modal exists, and it is pre-existing and outside this increment's
changed lines: in `SPEC-001-reusable-blind-pairing.md`, "They must not be
exposed directly to the public Internet." I record these as an observation
rather than a finding, because the increment under review neither introduced
them nor touched their lines.

Linting the cbcl-pairing changed-line fragment in isolation reports one
`sentence_over_limit`; that is a measurement artefact, because the fragment
concatenates seven YAML frontmatter lines into a single 37-word
pseudo-sentence. The full-parent lint, which parses frontmatter correctly,
shows 6 errors both before and after.

## Gates not run, and why

- **`CI=true npm run e2e:pairing` (TEST-704, TEST-709).** Attempted twice, in
  two independently constructed closures. Both attempts failed to build with
  `No space left on device (os error 28)`. The demo deliberately builds into a
  fresh `mkdtemp` Cargo target to prove a clean build, and the host volume was
  at 100 % capacity throughout this review — 228 GiB total, 195 GiB used,
  830 MiB available. This is a host limitation, not a candidate defect, and it
  is not counted as a finding. The first attempt failed for a different reason
  — an absent sibling closure — which I corrected by building the
  five-repository detached-clone closure before re-attempting.

- **The two live cbcl-bus capstones** (`run-cbcl-invite.sh`,
  `run-pairaccount.sh`). Not run, for the same reason. Each needs a full
  `rebar3 compile` plus release-profile Cargo builds, which cannot fit in
  830 MiB. The 0.5.15 review recorded the same host condition. Freeing the
  space would have required deleting review scratch, which this mandate
  forbids. Their 0.5.15 evidence is therefore carried as *unverified by this
  review* rather than as confirmed. Both drive the `anuna.io/credential/v1`
  context and, as the 0.5.15 review itself notes, exercise the relay, vendored
  verifier, and hub grant rather than the credential/v2 first-contact claimant,
  whose own end-to-end evidence is the Selfsame browser and wallet suites that
  did run green here.

- **Hub CI Linux WASM rebuild (TEST-1165).** Not run. It requires an
  `x86_64-unknown-linux-gnu` container and the CI runner platform; this host is
  `arm64` macOS, and SPEC-008 states explicitly that a local macOS rebuild is
  not evidence. I verified the artefact digest and the full provenance record
  instead, and both match exactly.

- **Production allocation, push, release, and deployment.** Prohibited by the
  mandate and by all four parents. Not attempted.

## Mutation restoration

Every mutation was applied to a clean scratch copy, never to the person's trees.

| Mutation | Location | Applied diff shown | Restored | Verified |
|---|---|---|---|---|
| Exclude `src/pairing.js` from discovery | scratch copy of `60abb8b` | yes | from committed blob | sha256 `01e76fce...60de`, `cmp` identical, gate green |
| Neutralise ratchet plus exclude `pairing.js` | separate probe copy | yes, both edits grep-confirmed | n/a — probe copy, never a review target | — |
| Nested `probe.js` discovery module | separate probe copy | n/a — added file | removed before the copy was reused | — |
| Pre-v2 baseline `197d4cb` | separate drift closure | n/a — new closure | n/a — separate closure | — |

No mutation was counted as measured without an applied, displayed diff. No
no-op mutation was counted. The one mutation that had to be reverted in place —
the required `src/pairing.js` exclusion — was restored from the committed blob
and verified byte-identical by both digest and `cmp`.

## Release and deployment authority

This review authorizes nothing. Production allocation, push, release, and
deployment remain prohibited, and remain so regardless of this verdict. The
verdict is REJECT, so the coordinated fresh-context Tier-1 PASS that SPEC-008
0.5.17, SPEC-007 0.3.8, SPEC-001 0.5.8, and SPEC-053 0.17.13 each require
remains open.

Correcting M-1 requires editing two lines in one file. It changes no wire
format, no executable byte, and no protocol behaviour. The next review can
therefore be scoped to the authority-singularity check; it does not need to
repeat the invoke-discovery mutation or the protocol regression pass, both of
which this review measured green from clean scratch at the candidate revisions.
