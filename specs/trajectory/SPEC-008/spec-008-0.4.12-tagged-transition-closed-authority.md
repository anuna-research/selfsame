---
id: SPEC-008-amendment-0.4.12
title: SPEC-008 0.4.12-draft — Tagged transition and closed authority outcome
status: draft
version: 0.4.12-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-004-application-scoped-identity#CON-201]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.5-draft; cbcl-bus [[SPEC-076-pairing-account-grant#REQ-004]]"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007; cbcl-bus SPEC-053 GATE-00, GATE-01, GATE-03, and corrected GATE-04"
supersedes: "[[spec-008-0.4.11-standalone-tofu-first-contact]] only where [[spec-008-0.4.12-tagged-transition-closed-authority#CON-971]] says so"
adopts: "All 0.4.4 through 0.4.11 clauses except the exact replacements in CON-971; cbcl-pairing SPEC-001 0.4.4 remains semantically unchanged"
review-inputs: "[[spec-008-0.4.11-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.5-draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
owner-direction: "2026-08-23 — standalone first-contact option 1 remains the target; implementation awaits fresh cross-model PASS"
last-updated: 2026-08-23
---

# SPEC-008 0.4.12-draft — Tagged transition and closed authority outcome

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Correction only.** The 0.4.11 review found the standalone architecture
> sound. It found zero critical, two high, three medium, and four low defects.
> This amendment closes those defects. It authorizes no product code, fixture,
> release, allocation, or deployment before a fresh cross-model PASS.

## Review disposition

| Finding | 0.4.12 disposition |
|---|---|
| H-1 — two offer-core shapes | [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] retains one tagged, nested `accountTransition`. |
| H-2 — claim-first recovery contradicts code | [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-986]] retains the intentional SPEC-076 refusal. |
| M-1 — bounds do not reproduce | [[spec-008-0.4.12-tagged-transition-closed-authority#REQ-985]] derives every level from one declared grammar. |
| M-2 — the hub gate loses earlier boxes | [[spec-008-0.4.12-tagged-transition-closed-authority#CON-971]] adopts the complete cbcl-bus 0.16.5 gate. |
| M-3 — v2 dispatch misses real sites | REQ-984 enumerates profile, hint, offer, statement, and signer boundaries. |
| L-1 — lock inversions are unnamed | [[spec-008-0.4.12-tagged-transition-closed-authority#NFR-914]] names wait-die and every known inversion. |
| L-2 — account devices lose handle change | REQ-986 states the accepted role boundary and local browser terminal. |
| L-3 — one payload constant has two meanings | REQ-985 gives the core and complete offer different constants. |
| L-4 — display bounds narrow international text | REQ-985 restores the complete 128-scalar grammar with 512-octet caps. |

The F-A through F-E disposition remains unchanged. Policy remains scoped to
the exact application and relay pair. Authenticated values precede display.
Custody stays closed before consent. The hub supplies the pending fresh scope.
Relay operatorship remains an accepted residual.

## Requirements

### REQ-984 — Every v2 object uses one tagged transition and one version branch

Credential/v2 retains the distinct ApplicationId
`https://chat.anuna.io/selfsame/application/v2`. Its profile and statement use
versions 2/2. The live v1 profile and its statement stay versions 1/1.

The v2 `OfferCoreV2` has the 22 members in
[[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]]. It always includes one
additional member named `accountTransition`. The complete top-level core has
exactly 23 members.

`accountTransition` has one of these exact closed RFC-8785 shapes:

```json
{"kind":"no-transition"}
```

```text
{
  "kind": "path-a-to-b",
  "legacyHandle": String,
  "legacyKeyDigest": Base64urlSha256,
  "migrationRooms": [CanonicalRoomText, ...],
  "migrationSnapshotDigest": Base64urlSha256,
  "roomSetDigest": Base64urlSha256,
  "snapshotNonce": Base64url32
}
```

The first shape implements `NoTransition`. The second implements `PathAToB`.
Unknown kinds, unknown members, missing members, and mixed variants refuse.
`accountDid` is not an offer-core member. It enters only the later
`migrationConfirmationDigest` defined by
[[spec-008-0.4.8-standalone-tofu-first-contact#REQ-972]].

Each `migrationRooms` value is exact canonical room text. Its UTF-8 bytes are
the `roomBytes` used by the room-set digest. Values are unique and sorted by
unsigned UTF-8 octets. The 48,000-octet limit covers the complete RFC-8785
array, including brackets, commas, quotes, and escaping.

App identity performs closed version dispatch before member recognition. The
v2 call graph has these distinct surfaces:

```text
profile-v2 recognizer
provider-hint-v2 recognizer
offer-v2 builder and recognizer
credential-v2 statement recognizer, builder, and signer
```

Exact function names MAY differ. Each surface owns its complete v2 member set
and its v2 version constant. No v2-reachable comparison uses
`crate::PROFILE_VERSION`, `profile::PROFILE_VERSION`, or a version-1 literal.
The v1 call graph cannot reach a v2 surface.

### REQ-985 — Credential/v2 bounds have one reproducible canonical fixed point

Every textual cap below limits both UTF-8 content and RFC-8785 escaped string
content. A value SHALL satisfy both limits before cloning. Existing URI, DID,
identifier, timestamp, and key grammars also apply.

```text
V2_MAX_APPLICATION_ID_OCTETS                 = 96
V2_MAX_RELAY_ORIGIN_OCTETS                  = 272
V2_MAX_REQUESTED_PERMISSIONS                = 4
V2_MAX_PERMISSION_OCTETS                    = 128
V2_MAX_RETURN_URI_OCTETS                    = 160
V2_MAX_PROVIDER_ID_OCTETS                   = 63
V2_MAX_PLATFORM_BINDING_ID_OCTETS           = 160
V2_MAX_TIMESTAMP_OCTETS                     = 32
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_OCTETS   = 512
V2_MAX_APPLICATION_ACCOUNT_DISPLAY_JCS      = 512
V2_MAX_CREDENTIAL_KID_OCTETS                = 128
V2_MAX_DID_OCTETS                           = 512
V2_MAX_LEGACY_HANDLE_OCTETS                 = 256
V2_MAX_MIGRATION_ROOMS                      = 256
V2_MAX_MIGRATION_ROOMS_JCS_OCTETS           = 48,000
V2_MAX_DEVICE_JWK_JCS_OCTETS                = 93
V2_MAX_PROVIDER_HINT_JCS_OCTETS             = 507
V2_MAX_OFFER_CORE_NON_ROOM_OCTETS           = 3,838
V2_MAX_OFFER_CORE_OCTETS                    = 52,000
V2_MAX_OFFER_PAYLOAD_OCTETS                 = 57,000
V2_MAX_CREDENTIAL_MEMBER_OCTETS             = 62,000
```

Application display text is NFC and contains 1 through 128 Unicode scalars.
It excludes C0 and C1 controls. The two 512-octet caps preserve every legal
four-octet scalar sequence. Quotes and backslashes remain inside the JCS cap.

The complete 21-member statement has maximum 2,560 RFC-8785 octets. The
protected header has maximum 187 octets. The fixed type is
`selfsame-credential-v2+jws`. Its `kid` uses the closed HTTPS-fragment grammar.

```text
base64url-no-pad(187)  = 250
base64url-no-pad(2560) = 3,414
base64url-no-pad(64)   = 86
two separators         = 2
--------------------------------
maximum compact JWS     = 3,752
policy cap              = 4,000
residual                 = 248
```

The maximum transition is `path-a-to-b`. Its tag is exact. The JWK is the
93-octet canonical object with `alg`, `crv`, `kty`, and `x`. The provider hint
is the exact seven-member object in
[[spec-008-0.4.5-standalone-tofu-first-contact#CON-945]]. It is 507 octets.

The 23 top-level core member contributions total 3,814 non-room octets. Two
braces and 22 commas add 24 octets. The exact non-room total is 3,838.

```text
 3,838  non-room core
48,000  complete migrationRooms array
------
51,838  complete maximum core
52,000  core cap
   162  residual
```

The complete offer adds three excluded members to the core:

```text
   112  devicePossessionProof member
 3,775  enrollmentEvidence member holding the compact JWS
   522  providerHint member holding the seven-member object
     3  separating commas
------
 4,412  complete addition
51,838  complete core
------
56,250  complete maximum offer payload
57,000  offer-payload recognizer cap
   750  offer-payload residual
62,000  CBCL credential member cap
 5,750  CBCL member residual
```

No auxiliary budget charges a core member twice. The generator constructs the
maximum statement, header, hint, transition, core, offer, and CBCL member. It
records every actual result and residual. Any legal value above a component
cap fails the generator and recognizer gates.

The browser source-disjoint implementation remains
`apps/cbcl_chat/priv/web/credential-v2-canonical.mjs`. Rust remains the hub
authority through the NIF. BEAM-through-NIF remains a marshalling lane.

### REQ-986 — Pairaccount and member keys keep intentionally different roles

This requirement replaces the claim-first recovery sentence in
[[spec-008-0.4.11-standalone-tofu-first-contact#REQ-983]]. It retains every
grant creator, deleter, index, guard, and conditional dominance rule there.

A member enrollment that wins the device guard writes its member row. A later
pairaccount claim sees that row and refuses. An exact member replay stays
idempotent. Another handle MAY use the same plain member key.

A valid pairaccount claim that wins first writes one unratified row. Later
member enrollment for that key refuses with `pairing-unratified`. It reaches no
member write. This version provides no pairaccount ratification or deletion
surface. The refusal therefore remains while that row exists.

This result retains cbcl-bus SPEC-076 REQ-004. Claimed-but-unratified account keys
cannot silently become legacy chat identities. Exact-key possession narrows who
can enter that state; it does not change the role boundary.

A credential/v2 device-grant key is account authority. It cannot enroll a new
legacy member identity under an absent handle. The browser intercepts handle
changes while account-bound. It displays the local terminal
`account-device-handle-change-refused` and sends no enrollment frame. The hub
still refuses any forged or stale attempt.

Plain member keys retain the deployed multi-handle behavior. Account devices
retain their stable account standing. A later handle-rebinding protocol is out
of scope and needs its own reviewed authority design.

## Non-functional requirement

### NFR-914 — Wait-die and every known lock inversion are explicit

The complete operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
Its finalizer and shared grant-core corrections remain effective.

Mnesia prevents deadlock through wait-die preemption in
`mnesia_locker:can_queue/4` and `allowed_to_be_queued/2`. The matrix controls
abort rate and evidence. It is not a deadlock proof.

These transaction pairs can produce expected cyclic aborts:

1. Different-account grant creators hold grant-table reads before record writes.
2. Grant creation takes a device guard before `grant ?ALL`.
3. Observed revocation takes `grant ?ALL` before sorted device guards.
4. The finalizer takes exact member state before `grant ?ALL`.
5. Member enrollment takes `grant ?ALL` before its exact member write.

Every affected operation uses fixed bounded `mnesia:transaction/2` or
`mnesia:transaction/3` restarts. The default unbounded restart form is
non-conforming. Exhaustion returns `authority-busy`, produces no partial effect,
and is a closed refusal at every consumer.

The representative load gate records each listed cyclic source. It also records
latency, throughput, retries, writer progress, and decision correctness. The
candidate claims no unmeasured performance property.

## Contract

### CON-971 — Effective text and coordinated gate closure are exact

The effective set applies these replacements:

1. REQ-984 replaces the offer-core paragraph in 0.4.11 REQ-982.
2. REQ-984 clarifies the JSON encoding in
   [[spec-008-0.4.8-standalone-tofu-first-contact#CON-960]].
3. REQ-985 replaces 0.4.11 REQ-982's bounds and arithmetic in full.
4. REQ-985 restores the display grammar from
   [[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]].
5. REQ-986 replaces the claim-first sentence in 0.4.11 REQ-983.
6. REQ-986 supplements that requirement with the account-device UI outcome.
7. NFR-914 supplements 0.4.11 NFR-913 and [[spec-008-0.4.11-standalone-tofu-first-contact#ADR-947]].
8. TEST-1113 through TEST-1116 replace TEST-1109 through TEST-1112.

All other 0.4.11 corrections survive. They include the distinct v2 trust
anchor, guarded grant deletion, local possession terminal, and source-disjoint
JavaScript. The cbcl-bus 0.16.5 gate explicitly restates every mandatory box.
No open-ended survivor phrase supplies release authority.

The coordinated review set is this amendment, cbcl-pairing 0.4.4, cbcl-bus
0.16.5, and did-crdt at the declared baseline. A PASS authorizes only the
Elephant SPL and test-first implementation plan. It authorizes no deployment.

## Decisions

### ADR-948 — A tagged nested sum is the single transition authority

The nested sum matches cbcl-pairing's private typed display. It represents both
open Path-B standing and Path-A migration. A flat mandatory migration shape
cannot represent `NoTransition`.

The explicit tag makes the RFC-8785 preimage decidable. Removing `accountDid`
also keeps confirmation-only data out of the earlier offer core. This choice is
accepted.

### ADR-949 — Unratified pairaccount standing does not become legacy membership

Possession proof prevents a third party from claiming another key. It does not
make an unratified key a verified chat identity. SPEC-076 keeps that boundary.

Adding a deleter creates a new authority surface. This amendment rejects
that expansion. It records the existing closed refusal and a truthful browser
outcome instead.

## Test specification

Every effective test through TEST-1108 remains mandatory. Cbcl-pairing
TEST-030 through TEST-055 remains mandatory. Cbcl-bus TEST-043 through TEST-075
is mandatory under its exact survivor ledger.

### TEST-1113 — Tagged transitions and all canonical bounds reproduce

Generate both exact transition variants. Reject unknown, mixed, missing, flat,
or untagged shapes. Require 23 top-level core members and no `accountDid`.

Construct every maximum. Require statement 2,560, header 187, compact JWS
3,752, JWK 93, hint 507, non-room core 3,838, core 51,838, full offer 56,250,
and every stated residual. Mutate every cap and member by one. Require refusal.

Generate room arrays from canonical room text and raw UTF-8 bytes. Require one
48,000-octet RFC-8785 array and the same raw-byte room-set digest. Escaping,
Unicode, sort, duplicate, and unit mutations refuse.

Rust and source-disjoint JavaScript reproduce every positive and negative
vector. BEAM-through-NIF reproduces marshalling results only. Static checks
reject WASM imports and caller-selected canonical preimages.

### TEST-1114 — Pairaccount and member race outcomes are closed

Race member enrollment and a valid pairaccount proof for one plain key. If
member wins, the claim refuses. If claim wins, later enrollment returns
`pairing-unratified` and writes no member row.

Exercise exact replay, another handle with a plain member key, and a grant-key
enrollment attempt. Require the documented results through the public caller.
Restart and restore preserve them.

Change the handle after credential/v2 account binding. Require the local
`account-device-handle-change-refused` terminal and zero enrollment frames.
Forge a frame and require the hub refusal.

### TEST-1115 — Every v2 version surface is source-disjoint from v1

Trace the profile, provider hint, offer, statement, builder, recognizer, and
signer call graphs. Require the exact v2 members and version constants. Reject
any v2-reachable global v1 constant or version-1 literal.

Keep the live v1 bytes, digest, route, builder, and signer exact. Mutate one
dispatch edge and require the static and vector suites to fail.

### TEST-1116 — Wait-die, complete gates, and authority refusal are observable

Instrument every lock operation. Trigger each cyclic source in NFR-914. Require
bounded retries or `authority-busy`, zero partial effects, and fail-closed
consumers.

Require the cbcl-bus 0.16.5 GATE-04 text byte-for-byte in gate evidence. Remove
one inherited or corrected box and require the effective-text check to fail.

## Changelog

- **0.4.12-draft — 2026-08-23 — review candidate, not approved.** Restores one
  transition shape, reproducible bounds, truthful authority outcomes, complete
  dispatch boundaries, wait-die evidence, and the complete hub gate.
