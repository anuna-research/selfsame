---
id: spec-008-0.5.13-implementation-reviewer-prompt-2026-08-25
title: SPEC-008 0.5.13 implementation — Tier-1 adversarial review prompt
status: active
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-25
---

# SPEC-008 0.5.13 implementation — Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
approve the candidate because its author reports green tests or killed mutants.
Independently review the complete standalone credential/v2 implementation.
Then test the 0.5.13 remediation of the rejected 0.5.12 review. Do not repair
the candidate. Write only the review report, commit it in the Circus worktree,
and complete through the appended Circus instruction.

## Authentication and isolation

This review runs through Circus with the interactive Claude CLI. It must use
the person's Claude Max subscription. The API-billed `circus-driver-claude`
must not run. The parent and child environments remove `ANTHROPIC_API_KEY` and
`ANTHROPIC_AUTH_TOKEN`.

Check that both variables are absent without printing values. Run `env -u
ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` and record only:

- `authMethod`
- `apiProvider`
- `subscriptionType`

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier. Any other result
blocks the review process. The generating family is OpenAI GPT-5. The reviewing
family is Anthropic Claude. This session receives no generating-session
reasoning or transcript.

## Exact candidate set

Use `git show COMMIT:path` and `git diff BASELINE..COMMIT`. Do not substitute
uncommitted files. The prompt commit on the Circus integration ref is not an
implementation candidate.

- Selfsame implementation candidate:
  `ff2f1394813a38ce9d0409726892b6e0c63d00ea`
  - pre-remediation implementation:
    `0dbfb3c4323aec568ac2803d58c0aa71c301edda`
  - governing 0.5.13 authority and remediation commit:
    `bd8bcff790750c9fe3672be72fffa694131a30ed`
  - original coordinated implementation baseline:
    `0220cec2dec44cd95d4f411ea4814d790b6716d2`
  - governing parents:
    `specs/SPEC-008-production-pairing-claimant.md` and
    `specs/SPEC-007-cbcl-pairing-cutover.md`
- cbcl-bus implementation candidate:
  `22fcc012ff4d4746f251b497c11495b1b26c0f29`
  - WASM remediation commit:
    `eaacb080304c57730a1e82be483e78aa8b8bb99e`
  - governing SPEC-053 0.17.9 authority commit:
    `1b145cbb4b3771a43abb18d3b3893fa1927e679a`
  - original coordinated implementation baseline:
    `ce894a7f2debc111b610296b1a3780de6084e37e`
  - governing parent: `specs/SPEC-053-key-root-identity.md`
- cbcl-pairing candidate:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
  - original coordinated implementation baseline:
    `c22f2d0526432c01c56d64be35a8d4f87516ec92`
  - governing parent: `specs/SPEC-001-reusable-blind-pairing.md`
- did-crdt service HEAD:
  `94db956d7d5d5edf0f42afad073ab42f265a1752`
  - dependency revision in the runtime closure:
    `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
  - determine from code whether later service-only changes participate
- cbcl-rs dependency revision:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`

The uncommitted working trees are not evidence. Create clean detached clones or
worktrees from the exact commits for all tests and mutations. Do not modify or
delete the person's dirty files, output APKs, or patch script.

The reviewed WASM closure has these additional exact values:

- host: `x86_64-unknown-linux-gnu`
- rustc: 1.96.0
- wasm-bindgen: 0.2.126
- WASM SHA-256:
  `23e14bf658321b49f759e43d002797e2f3cb3184fbe460c99b0b8f580ab5e781`

## Independent review mandate

Read each governing parent for the checked areas. Trace real producers, wire
recognisers, durable transitions, UI consumers, recovery routes, and deployment
boundaries. Verify claims against code before opening any prior review.

Determine independently whether:

1. Live CON-220 application authentication and exact
   `(applicationId, relayOrigin)` consent precede relay socket creation. A relay
   trusted for one application never authorizes another.
2. The claimant binds the locally authenticated profile digest into the CPace
   public context. It checks the peer-authenticated digest before policy seal,
   exact-intent display, or identity work.
3. The display shows the locally authenticated application identity. It
   cross-checks the wire claim before approval, closing historical F-B.
4. No identity key, scope, DID, signature, publication, alias, grant, or durable
   identity record occurs before its consent boundary. Include
   [[SPEC-007-cbcl-pairing-cutover#REQ-812]].
5. Fresh account scope is hub-minted, exclusive, and ceremony-bound. It is used
   only after consent, with no prior enrolment record.
6. Relay operatorship ambiguity is not represented as authentication. The
   actual property is per-application exact-pair consent plus cryptographic
   application binding.
7. Every credential/v2 body is closed, canonical, bounded, versioned,
   context-separated, and recognised before action on both sides.
8. Allocator, relay, claimant, reverse issuance, final status, receipt, and
   installation form one end-to-end cbcl-pairing protocol. No parallel
   enrolment transport substitutes for it.
9. Browser checkpoints and recovery reconcile every restart phase. They do not
   duplicate allocation, identity, grant, or acceptance effects.
10. Wallet pending state, endpoint checkpoints, deadlines, recovery, and
    installation CAS cannot widen authority after failure or wrong order.
11. Acknowledged `FinishedSent` allocator restoration emits `Open` without a
    duplicate `Finished`. Mutate the deciding predicate.
12. [[SPEC-008-production-pairing-claimant#CON-990]] arms before the initial
    pending persistence. Its typed transaction owns that persistence and every
    later transition. It disarms only after durable `PayloadPrepared`.
13. Every one of the fifteen exact `PrePayloadBoundary` values wraps the real
    production operation it names. No operation is duplicated, skipped, or
    represented only by a synthetic phase label.
14. Commit-then-error behavior at the initial persistence and each replacement
    still compensates the exact attempt. A committed `PayloadPrepared` survives.
15. Compensation refuses another attempt, root generation, installed record,
    and sibling application. It never deletes their state.
16. Confirmed unlink compares the current slot and exact-pair state with the
    values sampled at selection. Absence sampled at selection remains removable.
17. TEST-1162 injects failure through every real production hook and observes
    the actual causal order. The CI command proves exactly one isolated test ran.
18. Pending links are visible with bounded public summaries. Abandonment and a
    fresh same-application retry work after restart and partial unlink failure.
19. Installed reload reauthenticates reciprocal authority before exposing
    capability. Rotation, handle change, oversized input, unavailability, and
    revocation remain distinct and fail closed.
20. Exactly one `pub const PROFILE_VERSION` exists in the workspace. It is
    owned by `profile.rs`, re-exported at the crate root, and source-gated.
21. Exact pins form one buildable closure across wallet, hub, CI, Android,
    tests, and browser provenance. The strict witness rejects every tracked-
    dirty or wrong-HEAD sibling despite the development override.
22. Hub Linux CI rebuilds the four browser artifacts byte-identically from the
    exact closure. It cannot report success after zero rebuilds or an ignored
    failure.
23. Production allocation remains closed. The review cannot authorize release,
    push, or deployment.

Run focused and complete suites. Record exact commands and results. A test
collection failure, dependency failure, host mismatch, or empty selector is
not passing evidence.

## Required mutation re-test

Use clean scratch clones. Force recompilation after each source mutation. At a
minimum require an executable gate to kill each mutation below:

1. move transaction arming after the initial `persist_pending`;
2. move transaction disarming before the final durable replacement;
3. remove or rename one `PrePayloadBoundary` hook;
4. reorder two adjacent production hooks;
5. bypass initial commit-then-error compensation;
6. delete `PayloadPrepared` retention;
7. remove immutable same-attempt or root-generation comparison;
8. allow compensation to delete an installed or sibling slot;
9. remove unlink's slot comparison;
10. remove unlink's selected-policy-state comparison;
11. make absent-at-selection unlink refuse;
12. add a second `pub const PROFILE_VERSION` anywhere in the workspace;
13. drift the isolated TEST-1162 selector so it runs zero tests;
14. delete, rename, or mark ignored the hub CI WASM rebuild step;
15. mutate each WASM source pin or provenance source revision;
16. mutate the recorded host, compiler, wasm-bindgen version, digest, or one
    served browser byte; and
17. revert the acknowledged-`FinishedSent` allocator predicate.

For boundary injections, prove each of the fifteen production hooks executes
exactly once in causal source order. A table of labels or five durable phases
does not satisfy this requirement.

The generating agent reports that the arm-after-persist and
disarm-before-replace mutants fail the isolated TEST-1162 case. It also reports
that the restored isolated case passes and the exact selector records one
passed test. Independently reproduce those results.

The generating agent also reports a clean Linux `linux/amd64` build and a
second fresh rebuild matching all four vendored files. The browser vendor suite
reports six passed tests. Independently run the actual rebuild through a
compatible Linux container and reproduce the digest.

Run the complete Selfsame workspace tests and Clippy against a clean exact-pin
closure. Run the complete cbcl-pairing workspace with all features. Run hub
compile, EUnit, all discovered browser suites, the exact WASM rebuild, and the
relevant live or focused credential/v2 gates. Report any gate not run and why.

## Prior-review re-test

Only after the independent pass, read:

- `specs/trajectory/SPEC-008/spec-008-0.5.12-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.11-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.10-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.9-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`

Independently re-test all four 0.5.12 MEDIUM findings and four LOW findings.
Re-test every earlier named finding, F-A through F-E, and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Do not inherit any disposition.

The two expected residuals are the orphaned post-approval DID publication and
the proxy-shared anonymous recovery limiter. When an actual consequence is
wider than the parent records, reclassify that item.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.13-implementation-claude-adversarial-review-2026-08-25.md`.
The report must contain:

- reviewer identity, subscription auth path, Circus attempt, exact commits,
  and independence statement;
- verdict `PASS` or `REJECT` and severity counts;
- every finding with severity, code evidence, failure sequence, affected fully
  qualified requirement, and measurable correction;
- explicit dispositions for all twenty-three checks and seventeen mutations;
- explicit dispositions for every 0.5.12 and earlier finding, F-A through F-E,
  and [[SPEC-007-cbcl-pairing-cutover#REQ-812]];
- exact test commands and outcomes; and
- a statement that PASS does not authorize release, push, or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. A load-bearing missing
obligation is a finding even when the candidate fails closed. LOW residuals
are permitted only with explicit consequence and operational treatment. Release
and deployment controls are outside this review's authority.
