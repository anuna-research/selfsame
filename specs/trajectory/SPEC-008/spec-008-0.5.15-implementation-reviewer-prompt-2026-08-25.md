---
id: spec-008-0.5.15-implementation-reviewer-prompt-2026-08-25
title: SPEC-008 0.5.15 implementation — Tier-1 adversarial review prompt
status: active
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-25
---

# SPEC-008 0.5.15 implementation — Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Green
tests and killed mutants do not establish correctness by themselves.

Review the complete standalone credential/v2 implementation. Then review the
0.5.15 corrections to the rejected 0.5.13 implementation review.

Do not repair candidate code or specifications. Write only the review report,
commit it in the Circus worktree, and complete through Circus's appended
instruction.

## Authentication and isolation

This review runs through Circus and the interactive Claude CLI. It SHALL use
the person's Claude Max subscription. It SHALL NOT use the Anthropic API.

Both `ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent in the
review process. Check absence without printing either value.

Run `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status`.
Record only `authMethod`, `apiProvider`, and `subscriptionType`.

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier.

Any other authentication result blocks the review. The generating family is
OpenAI GPT-5. The reviewing family is Anthropic Claude.

## Exact candidate set

Use committed files, `git show COMMIT:path`, and exact diffs. Do not substitute
uncommitted working files. The reviewer-prompt commit is not implementation.

- Selfsame implementation candidate:
  `48a0c7499ab83bdf1d77f6cfb8a09562a7c046f7`
- Selfsame governing authority commit:
  `8f5249026e6e3352a78f365638be3225c0cc3fc6`
- Selfsame preceding implementation:
  `ff2f1394813a38ce9d0409726892b6e0c63d00ea`
- cbcl-bus implementation candidate:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-bus governing authority commit:
  `9ae1313ab8f9a95fe61f92c6f272010d795df871`
- cbcl-bus preceding browser-gate implementation:
  `22fcc012ff4d4746f251b497c11495b1b26c0f29`
- cbcl-pairing candidate:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs dependency:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt runtime dependency:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- did-crdt service HEAD is outside the runtime closure:
  `94db956d7d5d5edf0f42afad073ab42f265a1752`

The Circus worktree contains the committed Selfsame candidate and authority.
The clean cbcl-bus checkout contains its committed candidate and authority.

Use clean detached checkouts or committed object reads for every other
revision. The person's dirty working trees are not evidence.

Do not modify or delete the person's output APKs, patch script, or dirty
files.

The browser closure has these exact values:

- host: `x86_64-unknown-linux-gnu`
- rustc: 1.96.0
- wasm-bindgen: 0.2.126
- WASM SHA-256:
  `a8f82e98e2e74eb8b5bf882d53bedfc7b3a11486620910408b82eda2e969ec07`

## Governing authority

Read these current parents before prior reviews:

- `specs/SPEC-008-production-pairing-claimant.md` at 0.5.15;
- `specs/SPEC-007-cbcl-pairing-cutover.md` at 0.3.6;
- cbcl-pairing `specs/SPEC-001-reusable-blind-pairing.md` at 0.5.7; and
- cbcl-bus `specs/SPEC-053-key-root-identity.md` at 0.17.11.

Trace actual producers, recognisers, durable transitions, UI consumers,
recovery paths, release builders, and deployment boundaries.

## Independent protocol mandate

Determine independently whether every item below is true.

1. Live CON-220 authentication and exact-pair consent precede socket creation.
2. Consent is scoped to exact `(applicationId, relayOrigin)` values.
3. The claimant binds the locally authenticated profile digest into CPace.
4. Peer digest equality precedes policy sealing, intent display, and identity
   work.
5. The display names the locally authenticated application, not a wire claim.
6. Wire and local application identities cross-check before approval.
7. No key, scope, DID, signature, publication, alias, grant, or durable
   identity record occurs before its consent boundary.
8. Fresh account scope is hub-minted, exclusive, ceremony-bound, and used only
   after consent.
9. Relay operatorship is not represented as authentication. Exact-pair
   consent and application binding are the actual security properties.
10. Every credential/v2 body is closed, canonical, bounded, versioned, and
    context-separated before action.
11. Allocator, relay, claimant, issuance, status, receipt, and installation
    form one cbcl-pairing protocol.
12. No SPEC-004 enrolment transport substitutes for the standalone protocol.
13. Browser and wallet recovery cannot duplicate allocation, identity, grant,
    acceptance, or acknowledgement effects.
14. Transaction arming precedes initial pending persistence. Only durable
    `PayloadPrepared` commit can disarm it.
15. Each exact production boundary wraps the real operation it names once and
    in causal order.
16. Compensation is attempt-scoped and preserves committed payloads, installed
    records, roots, sibling applications, and newer attempts.
17. Confirmed unlink compares the selected slot and selected exact-pair policy
    state against current values.
18. Installed reload reauthenticates reciprocal authority before capability
    exposure.
19. Production allocation, push, release, and deployment remain closed.

Historical F-A through F-E and
[[SPEC-007-cbcl-pairing-cutover#REQ-812 — Post-consent identity derivation hard stop]]
are hard-stop regression checks.

## Rejected-review remediation mandate

Only after the independent protocol pass, read
`specs/trajectory/SPEC-008/spec-008-0.5.13-implementation-claude-adversarial-review-2026-08-25.md`.

Independently verify these corrections.

1. Deny-warnings workspace Clippy reaches credential/v2 browser restoration.
   The explicit lint disposition is narrow and justified.
2. Hub CI cannot skip the exact WASM rebuild with `if: false` or ignored
   failure behavior.
3. The vendor suite directly checks the exact host, compiler, and
   wasm-bindgen versions.
4. The screen harness enters current credential/v2 commands and a valid PAIR1
   invitation.
5. It renders pre-socket authentication, exact-pair consent, and authenticated
   exact-intent consent.
6. The invoke scanner covers every calling module and every handler prefix.
7. TEST-1162 reaches real transaction hooks in causal production order.
8. The release relay authenticates cbcl-pairing `62ef4a9` as its executable
   baseline before Cargo runs.
9. A pre-v2 executable baseline makes the release builder refuse.
10. The Linux rebuild reproduces the committed `a8f82e98...` WASM bytes.
11. The authority records that the lint disposition changed embedded source
    locations and invalidated the preceding digest.

Do not inherit the earlier report's dispositions. Re-test its three MEDIUM
findings and three LOW findings from code.

## Required mutation checks

Use clean scratch copies. Restore every mutation after measurement.

At minimum, require an executable gate to kill each mutation below.

1. Remove the credential/v2 restoration lint disposition.
2. Add `if: false` to the exact hub WASM rebuild step.
3. Add `continue-on-error: true` to that step.
4. Change the recorded build host.
5. Change the recorded rustc version.
6. Change the recorded wasm-bindgen version.
7. Restore credential/v1 screen stubs.
8. Remove the valid PAIR1 screen input.
9. Exclude `src/pairing.js` from invoke discovery.
10. Exclude the `app_grant` handler prefix.
11. Revert the release relay baseline to
    `197d4cb3d1560ab5328df28fc984269799c510f9`.
12. Inject `conformance-allocation` through `RUSTFLAGS` into a release build.
13. Change the recorded WASM digest.
14. Change one served WASM byte.
15. Move transaction arming after initial persistence.
16. Disarm before durable `PayloadPrepared` replacement.
17. Remove or reorder one production boundary hook.
18. Weaken unlink's slot or policy comparison.

Source-order scans alone do not prove transaction behavior. Synthetic phase
labels alone do not prove production-hook reachability.

## Focused executable evidence

Run focused commands that decide the listed claims. Record exact commands and
outcomes. A selector that runs no test is failure.

At minimum, run or independently reproduce:

- Selfsame locked deny-warnings workspace Clippy;
- isolated TEST-1162 with an exact one-test assertion;
- `CI=true npm run screens`;
- Selfsame browser and wallet pairing end-to-end suites;
- hub release-target isolation and relay feature matrices;
- the hub vendor suite and all discovered browser suites;
- a Linux x86_64 exact WASM comparison; and
- the live browser-wallet-relay and browser-wallet-relay-hub capstones.

The generating agent reports these results. Treat them as claims, not facts.

- Selfsame workspace tests and Clippy passed from exact clean pins.
- TEST-1162 ran exactly one test and passed.
- The screen suite and three Selfsame pairing end-to-end suites passed.
- Hub EUnit passed 975 tests.
- Forty-two discovered browser suites passed 400 tests.
- Relay production tests passed 10 plus 4 cases.
- Relay conformance tests passed 10 plus 6 cases.
- Both relay Clippy configurations passed.
- The poisoned-cache release isolation harness passed.
- The normal relay release build passed after baseline correction.
- The Linux vendor build measured `a8f82e98...`.
- One independent fresh Linux comparison matched every generated file.
- The browser-wallet ceremony ended `delivered` and `accepted`.
- The full hub capstone rendered `pairaccountgranted` after wallet acceptance.

Report any required gate not run and why. Distinguish code defects from local
infrastructure limits.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.15-implementation-claude-adversarial-review-2026-08-25.md`.

The report SHALL contain:

- reviewer family, subscription path, Circus attempt, commits, and an
  independence statement;
- verdict `PASS` or `REJECT` and severity counts;
- each finding's severity, code evidence, failure sequence, affected fully
  qualified requirement, and measurable correction;
- dispositions for all nineteen protocol checks;
- dispositions for all eleven rejected-review checks;
- dispositions for all eighteen mutations;
- dispositions for F-A through F-E and
  [[SPEC-007-cbcl-pairing-cutover#REQ-812 — Post-consent identity derivation hard stop]];
- exact executed commands and results; and
- a statement that PASS does not authorize push, release, or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. A load-bearing missing
obligation remains a finding when the implementation fails closed.

LOW residuals are permitted only with explicit consequences and operational
treatment. Release and deployment controls are outside this review's
authority.
