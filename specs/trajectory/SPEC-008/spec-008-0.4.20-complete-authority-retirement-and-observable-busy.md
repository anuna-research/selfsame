---
id: SPEC-008-amendment-0.4.20
title: SPEC-008 0.4.20-draft — Complete authority retirement and observable busy handling
status: draft
version: 0.4.20-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.19-singular-authority-and-webfinger-order]] only where [[spec-008-0.4.20-complete-authority-retirement-and-observable-busy#CON-979]] says so"
adopts: "All effective 0.4.4 through 0.4.19 clauses except the exact replacements and retirements in CON-979"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.8-draft; cbcl-bus SPEC-053 0.16.13-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.19-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.20-draft — Complete authority retirement and observable busy handling

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.19 review found two HIGH, one
> MEDIUM, and three LOW defects. This amendment closes those findings against
> the reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirement

### REQ-999 — Every authority source resolves through explicit current text

This requirement supplements
[[spec-008-0.4.19-singular-authority-and-webfinger-order#REQ-998]].

An authority scan SHALL apply every exact retirement in CON-979 before it
compares current values. A retired fragment supplies no survivor, test,
coordinated-set, pointer, or replacement authority.

The scan SHALL cover numbered clauses, numbered list items, and unnumbered
normative paragraphs. A heading or changelog does not make conflicting
normative text historical.

Within the adopted Selfsame amendment chain, only CON-979 supplies four current
values. They are the Selfsame correction set, cbcl-bus test set, coordinated
review set, and coordinated pointer set. CON-978 remains effective for the
cbcl-pairing survivor set and every unmodified obligation.

## Non-functional requirement

### NFR-922 — WebFinger contention is externally retryable and reinstatement uses one table

This requirement supplements
[[spec-008-0.4.19-singular-authority-and-webfinger-order#NFR-921]]. It replaces
only the three paragraphs identified below. Every other NFR-921 paragraph
remains effective.

The first replacement starts with "`withdraw/2` derives the requested alias
URI". It ends with "Its exact withdrawal write lock follows." The replacement
text is:

`withdraw/2` derives the requested alias URI from the named home DID before
opening its bounded transaction. It passes that value into
`tombstone/3(account, homeDid, requestedAlias)`.

The live branch of `tombstone/3` takes the requested exact alias write lock
first. It takes the exact account write lock second. Both rows MUST agree on
the account, alias, and home DID.

A mismatch refuses without taking a second alias lock. The exact
`cbcl-webfinger-withdrawn` write lock follows. Creation and both deletes reuse
those held locks.

The second replacement starts with "`reinstate/2` omits the alias table". It
ends with "It performs no read-to-write upgrade." The replacement text is:

`reinstate/2` and `lift-tombstone/2` take no alias or account lock. Their
transaction takes one whole `cbcl-webfinger-withdrawn` write lock before its
first `index_read`.

The exact delete runs under that table lock. Provision and withdrawal already
serialize with it through their withdrawal write locks. Reinstatement performs
no read-to-write upgrade.

The third replacement starts with "Every affected transaction runs at Mnesia
level 1". It ends with "No partial effect survives." The replacement text is:

The public transaction owners are `provision/3`, `withdraw/2`, `reinstate/2`,
and `lookup/1`. Each runs at Mnesia level 1 with an explicit integer
`Retries >= 2`. Nested transactions are prohibited.

The shared retry wrapper maps `nomore`, exhausted `#cyclic{}`, and
`nested_transaction` to `authority-busy`. Transaction bodies never start a
nested retry loop.

Mutation owners return `#(error authority-busy)` on that terminal. No partial
effect survives. Existing refusal and success results remain unchanged.

`lookup/1` is the public resolver adapter. It maps `authority-busy` to the
existing atom `unavailable`. `cbcl-chat-webfinger-http` maps `unavailable` to
HTTP 503 with an empty body and `no-store`.

Contention SHALL NOT become `missing` or HTTP 404. Invalid resource syntax and
a committed absent binding retain their existing 400 and 404 behavior.

`create-or-return/3`, `lookup-binding/1`, `tombstone/3`,
`already-withdrawn/2`, and `lift-tombstone/2` are transaction bodies. Their
names in the graph specify locks, not independent public retry terminals.

## Contract

### CON-979 — Earlier ledgers retire and the current coordinated set advances

This contract supplements
[[spec-008-0.4.19-singular-authority-and-webfinger-order#CON-978]]. Every
CON-978 replacement and retirement remains effective unless this contract
replaces its exact current-value fragment.

This contract retires seven unnumbered test-specification paragraphs:

1. The paragraph before
   [[spec-008-0.4.5-standalone-tofu-first-contact#TEST-1048]] starts with
   "All 0.4.4 TEST-1011" and ends with "also mandatory."
2. The paragraph before
   [[spec-008-0.4.6-standalone-tofu-first-contact#TEST-1065]] starts with
   "All effective TEST-1011" and ends with "are mandatory."
3. The paragraph before
   [[spec-008-0.4.7-standalone-tofu-first-contact#TEST-1075]] starts with
   "All effective TEST-1011" and ends with "mandatory."
4. The paragraph before
   [[spec-008-0.4.8-standalone-tofu-first-contact#TEST-1083]] starts with
   "All effective TEST-1011" and ends with "are mandatory."
5. The paragraph before
   [[spec-008-0.4.9-standalone-tofu-first-contact#TEST-1092]] starts with
   "All effective TEST-1011" and ends with "are mandatory."
6. The paragraph before
   [[spec-008-0.4.10-standalone-tofu-first-contact#TEST-1102]] starts with
   "All effective TEST-1011" and ends with "are mandatory."
7. The paragraph before
   [[spec-008-0.4.11-standalone-tofu-first-contact#TEST-1108]] starts with
   "All effective TEST-1011" and ends with "the 0.16.4 ledger."

This contract also retires item 1 from four implementation-sequencing lists:

1. In [[spec-008-0.4.7-standalone-tofu-first-contact]], the item starts with
   "Run fresh-context Claude-driver Circus review" and ends with
   "carried finding."
2. In [[spec-008-0.4.8-standalone-tofu-first-contact]], the item starts with
   "Run a fresh-context Claude Opus/max Circus review" and ends with
   "exact four code baselines."
3. In [[spec-008-0.4.9-standalone-tofu-first-contact]], the item starts with
   "Run a fresh-context Claude Opus/max Circus review" and ends with
   "exact four code baselines."
4. In [[spec-008-0.4.10-standalone-tofu-first-contact]], the item starts with
   "Run a fresh-context Claude Opus/max Circus review" and ends with
   "exact four code baselines."

This contract retires one paragraph in
[[spec-008-0.4.11-standalone-tofu-first-contact#CON-970]]. It starts with "The
coordinated hub effective set" and ends with "survive unchanged."

The retirements above remove every earlier sentence that states TEST-050 in
full. The exception in CON-978 is the sole current TEST-050 status.

NFR-922 replaces the three named NFR-921 paragraphs. ADR-957 replaces ADR-956
in full. TEST-1144 replaces TEST-1142 in full. TEST-1145 replaces TEST-1143 in
full.

Through TEST-1142, TEST-1144 remains the successor of TEST-1140 and TEST-1137.
Through TEST-1143, TEST-1145 remains the successor of TEST-1141 and TEST-1138.
Through ADR-956, ADR-957 remains the successor of ADR-955, ADR-954, and
ADR-953. No earlier replacement relationship is lost.

This contract replaces the CON-978 paragraph starting with "The exact cbcl-bus
set". The replaced range ends at the did-crdt commit. The replacement text is:

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, TEST-103, and TEST-104. Every effective Selfsame
test through TEST-1108 also survives.

TEST-1134, TEST-1135, TEST-1139, TEST-1144, and TEST-1145 are the current
Selfsame correction tests. No superseded portion or open-ended range supplies
authority.

These five current tests collectively replace TEST-1129 through TEST-1133.

The coordinated review set contains this amendment, cbcl-pairing 0.4.8, and
cbcl-bus 0.16.13. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

This contract replaces the CON-978 pointer paragraph starting with "The
Selfsame parent". The replaced range ends with "at this candidate." The
replacement text is:

The Selfsame parent [[SPEC-008-production-pairing-claimant]] points to this
candidate and names its open review gate. The cbcl-bus parent
[[SPEC-053-key-root-identity#DEC-012]] points to the coordinated 0.16.13
candidate. Cbcl-pairing 0.4.8 points its `consumer-design` at this candidate.

Every F-A through F-E correction remains a hard stop. SPEC-007
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] also remains a hard stop.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It authorizes
no code, allocation, release, or deployment.

## Decision

### ADR-957 — One authority ledger and one retryable WebFinger terminal precede code

Every stale survivor, hub, coordinated-set, and pointer sentence retires by an
exact boundary. One current coordinated set remains across all three
repositories.

WebFinger contention is an availability result. Public resolution returns
HTTP 503, never false absence. Mutation callers receive `authority-busy` and
can retry without a partial effect.

Live withdrawal uses the pre-derived alias as its first lock key.
Reinstatement touches only the withdrawal table. Both paths remain ordered
subsequences of alias, account, then withdrawal.

The authenticated display, post-consent effect order, shared grammar, cap
arithmetic, and sealed-grant graph remain unchanged.

## Test specification

### TEST-1144 — Installed state, lock graphs, and public busy results close

Change only the profile digest with authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consent decisions, and a new verified
grant. Every local-only re-grant shortcut fails.

Generate the twelve named authority cycles and every `AR` writer contention.
Generate the complete migration graph. Require the restored acquisition point,
invite-before-room order, and no additional upgrade or cycle.

Include both `write-grant` paths, both `redeem-and-grant` paths,
`revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`,
`cbcl-chat-sealedgrant:put-tx`, and `cbcl-chat-sealedgrant:get-tx`.

Include `cbcl-chat-webfinger-store:provision/3`, `create-or-return/3`,
`lookup/1`, `lookup-binding/1`, `withdraw/2`, `tombstone/3`,
`already-withdrawn/2`, `reinstate/2`, and `lift-tombstone/2`.

Require alias-before-account ordering for provision, lookup, and live
withdrawal. Require `tombstone/3` to use its pre-derived alias. Reject every
second-alias lock.

Require reinstatement to take only the whole withdrawal write lock before its
first `index_read`. Require the sealed-grant write lock on the first `get-tx`
read.

Force bounded retry exhaustion and prohibited nesting in each public mutation.
Require `#(error authority-busy)`, no partial effect, and writer progress.

Force the same terminal through `lookup/1`. Require `unavailable`, HTTP 503,
an empty `no-store` response, and no HTTP 404. Successful and missing lookups
retain their existing responses.

### TEST-1145 — Every retired source, current pointer, and gate is singular

Static checks traverse every effective numbered clause, list item, and
unnumbered normative paragraph from 0.4.4 onward. They apply CON-979's
retirements before comparing current values.

Require every one of the seven retired test preambles, four sequencing items,
and the CON-970 hub paragraph to resolve uniquely. Any surviving boundary or
missing boundary fails.

Require exactly one cbcl-pairing survivor set, cbcl-bus test set, Selfsame
correction set, and coordinated review set. Require the TEST-050 exception and
reject any full TEST-050 survivor claim.

Require the Selfsame parent pointer to this candidate. Require cbcl-bus
DEC-012 to point to 0.16.13. Require cbcl-pairing 0.4.8 to point its
`consumer-design` to this candidate.

Require every replacement, all 42 cbcl-bus GATE-04 boxes, all fifteen version
sites, both non-conforming rendezvous baselines, and sealed cap arithmetic.
Removing any obligation fails.

## Changelog

- **0.4.20-draft — 2026-08-23 — review candidate, not approved.** Completes
  authority retirement and makes WebFinger contention externally retryable.
  No implementation or deployment authority is claimed.
