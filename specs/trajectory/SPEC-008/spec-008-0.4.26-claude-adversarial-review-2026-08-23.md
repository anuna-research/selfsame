---
id: SPEC-008-review-0.4.26-claude-2026-08-23
title: SPEC-008 0.4.26 — Claude Tier-1 adversarial review 2026-08-23
status: final
tier: 1
verdict: REJECT
reviewed-candidate: "[[spec-008-0.4.26-resolved-authority-and-websocket-busy]]"
reviewed-coordinates: "selfsame 80ce4e8; cbcl-bus 4084661; cbcl-pairing 8c902f9; did-crdt 1f409a4"
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
reviewer-auth: claude.ai Max subscription, firstParty
reviewer-session: fresh cross-model session, Circus attempt spec008-v0426-final-review/1
last-updated: 2026-08-23
---

# SPEC-008 0.4.26 — Claude Tier-1 adversarial review 2026-08-23

Verdict: **REJECT**.

One CRITICAL, one HIGH, and three gate-blocking MEDIUM defects stand. Three LOW
defects stand.

The engineering content of this coordinated increment is sound. Every named
consumer line, every named version witness, and every named terminal resolves
against the code at the declared baselines. The defects are in the amendment
instrument, not in the mechanism it describes.

The bounded amendment loop SHOULD stop. Section
[[spec-008-0.4.26-claude-adversarial-review-2026-08-23#Stopping-rule-assessment]]
gives the reasoning.

## Authentication, model, and independence evidence

This reviewer is Anthropic Claude, model `claude-opus-5`, running through a
Claude.ai Max subscription. No Anthropic API key was read or used. The
environment variable `ANTHROPIC_API_KEY` was unset for the authentication check.

```console
$ env -u ANTHROPIC_API_KEY claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "email": "hugo.oconnor@gmail.com",
  "orgId": "270dc6a8-e8c3-4fae-aedf-503493359e91",
  "orgName": "hugo.oconnor@gmail.com's Organization",
  "subscriptionType": "max"
}
```

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |
| Anthropic API key | prohibited, unset, unused |

This is a fresh session. It carries no prior context from the 0.4.25 review or
from any generation session. The 0.4.25 Claude report was read only to
enumerate the seven N10 obligations. Every conclusion below was re-derived from
the candidates and from the code.

The candidates record `generation-model-family: OpenAI GPT-5`,
`generation-model-version: gpt-5.6-sol`, and
`generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6`. This reviewer is a
different model family and a different session, so the cross-model
distinctness obligation is satisfied. No generator reasoning was inferred. Only
artefacts and code were reviewed.

Circus attempt from `record.json`:

| Field | Value |
|---|---|
| `attempt_id` | `spec008-v0426-final-review/1` |
| `repository` | `selfsame` |
| `branch` | `circus/spec008-v0426-final-review/1` |
| `worktree_path` | `/Users/anuna-01/Code/selfsame-spec008-v0426-final-review-1` |
| `integration_ref` | `spec/spec-004-web-manual-binding` |
| `launched_at` | `2026-08-23T11:42:23Z` |

## Reviewed commits and worktree status

Every HEAD was resolved before use rather than assumed.

```console
$ git rev-parse HEAD && git rev-parse --abbrev-ref HEAD && git status --porcelain
80ce4e855eab6322b4dd5ba7a3a9390b7a71bd77
circus/spec008-v0426-final-review/1
(clean)
```

| Repository | Required | Observed HEAD | Worktree status |
|---|---|---|---|
| selfsame | `80ce4e8` | `80ce4e855eab6322b4dd5ba7a3a9390b7a71bd77` | clean |
| cbcl-bus | `4084661` | `4084661f6b794cae7e2598ce4c0cb178df5944de` | clean |
| cbcl-pairing | `8c902f9` | `8c902f9f27d5f5f8ed2852efc2f121c9f9ced02f` | untracked `.DS_Store`, `.zetl/`, `specs/.zetl/` only |
| did-crdt | `1f409a4…78a1` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | modified `specs/did-crdt-spec.md`, untracked `.zetl/`, `docs/adr/ADR-007-xid-interoperability-posture.md` |

The did-crdt working tree carries one modified specification and one untracked
ADR. Neither is part of the coordinated baseline, which is pinned to the commit
rather than to the working tree. The pinned commit resolves, so the coordinated
did-crdt reference holds. The dirty working tree is recorded, not accepted as
authority.

Each candidate was authored in the same commit as its coordinated siblings:

| Candidate | Commit | Subject |
|---|---|---|
| selfsame `0.4.26` | `80ce4e8` | `spec(pairing): consolidate final review authority` |
| cbcl-bus `0.16.19` | `4084661` | `spec(identity): consolidate final review authority` |
| cbcl-pairing `0.4.12` | `8c902f9` | `spec(pairing): advance resolved consumer pointer` |

## No implementation code changed from the named baselines

Review question 10. Verified mechanically in all three repositories.

```console
$ git diff --name-only 0220cec2dec44cd95d4f411ea4814d790b6716d2 HEAD | grep -v '^specs/'
(no output)
$ git diff --name-only 0220cec2dec44cd95d4f411ea4814d790b6716d2 HEAD | wc -l
      24
```

| Repository | Implementation baseline | Non-spec files changed | Total files changed |
|---|---|---|---|
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | 0 | 24, all under `specs/` |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | 0 | 13, all under `specs/` |
| cbcl-pairing | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | 0 | 6, all under `specs/` |

No `crates/`, `src-tauri/`, `apps/`, or `src/` path changed in any repository.
The implementation prohibition holds.

## Verdict and severity counts

| Severity | Count | Gate effect |
|---|---|---|
| CRITICAL | 1 | blocking |
| HIGH | 1 | blocking |
| MEDIUM | 3 | all gate-blocking |
| LOW | 3 | non-blocking |

The verdict rule requires REJECT on any CRITICAL, HIGH, or gate-blocking
MEDIUM finding. Five such findings stand.

No finding was downgraded to preserve momentum. Every finding below is a
genuine authority, implementability, or testability defect. None is style.

## Findings

### N11-C-1 (CRITICAL, gate-blocking) — complete replacement severs the retirement chain, reviving the superseded clause set in both repositories

The candidate answers N10-M-1 and N10-M-2 by abandoning exact-phrase
replacement for complete successor clauses. The technique is correct. Its
instrument is not: it retires the contracts that carried every earlier
retirement, and never re-declares them.

**The dropped sentence.** Every contract in the chain from CON-981 through
CON-984 carries an explicit preservation sentence. CON-985 does not.

```console
$ grep -c 'earlier replacement remains effective' spec-008-0.4.2{1,2,3,4,5,6}-*.md
0.4.21 → 0
0.4.22 → 1
0.4.23 → 1
0.4.24 → 1
0.4.25 → 1
0.4.26 → 0
```

- `spec-008-0.4.22…md:289` — "Every earlier replacement remains effective
  unless this contract replaces its exact current fragment."
- `spec-008-0.4.23…md:338` — same sentence.
- `spec-008-0.4.24…md:238` — same sentence.
- `spec-008-0.4.25…md:292` — "Every earlier replacement remains effective
  unless this contract states otherwise."
- `spec-008-0.4.26…` — absent.

The hub is exactly symmetric.

```console
$ grep -n 'remains effective unless' spec-053-0.16.1[5-9]*.md
spec-053-0.16.15-…:49:  earlier replacement remains effective unless this requirement replaces its
spec-053-0.16.16-…:56:  earlier replacement remains effective unless this requirement replaces its
spec-053-0.16.17-…:53:  earlier replacement remains effective unless this requirement replaces its
spec-053-0.16.18-…:54:  Every earlier replacement remains effective unless this requirement states
(0.16.19 — no match)
```

**The instrument that makes the loss operative.** Three clauses combine.

1. `spec-008-0.4.26…md:9` — `adopts: "All effective 0.4.4 through 0.4.22
   clauses plus the complete successor clauses in CON-985; 0.4.23 through
   0.4.25 provide historical evidence only"`.
2. CON-985 — "This contract replaces CON-982, CON-983, and CON-984 in full.
   Those contracts supply historical evidence only."
3. REQ-1005 — "Retired text supplies no survivor, test, coordinated set,
   pointer, or **replacement authority**."

Clause 3 is decisive. CON-982 is retired text. CON-982 declared four
replacements: REQ-1001 by REQ-1002, NFR-924 by NFR-925, ADR-959 by ADR-960, and
TEST-1148/TEST-1149 by TEST-1150/TEST-1151. CON-982 also retired CON-981's two
current-value ranges. Under REQ-1005's own
rule, none of that survives. Clause 1 then re-adopts the 0.4.22 clauses as
effective.

REQ-1005's neighbouring sentence reads "The authority scan applies every
retirement through CON-985 before comparing current values". That admits a
second reading, in which the chain's retirements do survive. The two sentences cannot
both govern. That ambiguity is itself the defect the candidate set out to
remove, and it is unresolvable inside the document.

**What revives, and what it contradicts.** Every row below was read at its
named line.

| Revived clause | Revived text | Current successor | Conflict |
|---|---|---|---|
| `NFR-921` (0.4.19:142-145) | "`reinstate/2` omits the alias table. It takes the exact account write lock first." | `NFR-928` — "`reinstate/2` and `lift-tombstone/2` take no alias or account lock." | Direct. Two incompatible lock graphs. |
| `NFR-921` (0.4.19:152-155) | "Consumers map `nomore`, exhausted `#cyclic{}`, and `nested_transaction` to `authority-busy`." | `NFR-928` — "The wrapper maps only `{aborted, nomore}` to retry exhaustion"; "An exhausted `#cyclic{}` is not a separate public terminal"; "Prohibited nesting therefore has no runtime terminal and no runtime classifier." | Direct. Undoes N7-M-2, which removed two unreachable runtime forcings. |
| `NFR-921` (0.4.19:132-136) | withdrawal "refuses a mismatched stored URI or home DID" as one undifferentiated refusal | `NFR-928` — `#(error wrong-generation)` and `#(error inconsistent-binding)` split | Direct. Undoes the N9-H-1 and N10-M-1 correction. |
| `REQ-1001` (0.4.22:102-104) | "It includes `PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and `payloadVersion`." | `REQ-1005` — closed five-identifier set adding `profile_version` | Direct. The four-identifier set cannot reach `lib.rs:831`, `:847`, `:861`. Undoes N10-M-3. |
| `REQ-1001` (0.4.22:78-97) | inventory labelling `lib.rs:600` "profile_facts substitution" | `REQ-1005` class table — `lib.rs:600` "emitted member" | Direct. Retired vocabulary returns. Undoes N9-L-2. |
| `CON-981` (0.4.22:312-313) | "TEST-043 … TEST-098, **TEST-107, and TEST-108**." | `CON-985` — "… TEST-098, **TEST-115, and TEST-116**." | Two hub test sets. |
| `CON-981` (0.4.22:315-316) | "TEST-1134, TEST-1135, TEST-1139, **TEST-1148, and TEST-1149** are the current corrections." | `CON-985` — "… **TEST-1156, and TEST-1157** are current." | Two Selfsame test sets. |
| `CON-981` (0.4.22:321-322) | "The coordinated review set contains this amendment, **cbcl-pairing 0.4.8, and cbcl-bus 0.16.15**." | `CON-985` — "cbcl-pairing 0.4.12, and cbcl-bus 0.16.19" | Two coordinated review sets. |
| `CON-981` (0.4.22:333-334) | "The cbcl-bus parent `[[SPEC-053-key-root-identity#DEC-012]]` points to cbcl-bus **0.16.15**." | `CON-985` — points to **0.16.19** | Two pointer sets. |
| `NFR-019` (0.16.12:149-152, :159-162) | hub twins of the `reinstate/2` and `nested_transaction` rows above | `NFR-026` | Direct, both rows. |
| `REQ-077` (0.16.15:64) | "current set contains TEST-043 through TEST-065, TEST-068, and TEST-070." | `REQ-081` — the larger set | Third competing hub test set. |

This defeats REQ-1005's central claim verbatim. That claim reads "Only CON-985
supplies the current Selfsame correction set, hub test set, coordinated review
set, and pointer set." CON-981 supplies all four. It does so as an adopted,
effective 0.4.22 clause.

It also defeats mandatory review question 5. No current test set is singular.

The failure mode is the one N10-M-2 named: an amendment whose effective
requirement has two readings. It has moved one level down the chain and gained
a wider blast radius. Whole clauses now revive where one range revived before.

Notably, cbcl-pairing `0.4.12` avoids this. `CON-029` states "Every protocol
survivor recorded through CON-028 remains effective" while its frontmatter
declares 0.4.9 through 0.4.11 historical pointer evidence only. That is the
correct technique. Neither `CON-985` nor hub `REQ-081` applies it.

**Minimum correction.**

1. In `CON-985`, restore the preservation sentence. Scope it to retirements as
   well as replacements: *"Every retirement and replacement recorded through
   CON-984 remains effective unless this contract states otherwise."*
2. In hub `REQ-081`, restore the same sentence scoped to `REQ-080`.
3. In `REQ-1005`, reword "Retired text supplies no survivor, test, coordinated
   set, pointer, or replacement authority". A reworded sentence leaves intact
   any retirement already recorded by a retired instrument. For example: *"A
   retired clause supplies no current survivor, test, coordinated set, or
   pointer value. A retirement it recorded remains effective."*
4. Alternatively, restate the pre-0.4.23 retirements inline in `CON-985` and hub
   `REQ-081`. No cross-version replacement instrument then stays load-bearing.
   Given the loop history, this option is preferable.

Owner: Selfsame owner and cbcl-bus owner, jointly.

### N11-H-1 (HIGH, gate-blocking) — hub REQ-081 retires a GATE-04 box that is effective under no reading, so the gate never names this coordinated set

`REQ-081` designates the effective gate: "The current GATE-04 remains the
42-box gate in `[[spec-053-0.16.15-explicit-busy-contracts-and-complete-traces#GATE-04]]`.
Only its effective first box is replaced."

The box count is correct.

```console
$ awk '/GATE-04/{f=1} f&&/^- \[/{c++} END{print "boxes:",c}' spec-053-0.16.15-…md
boxes: 42
```

The replacement target is not. The first-box chain, read at each named line:

| Amendment | Retires box beginning | Replaces with |
|---|---|---|
| `0.16.15` GATE-04 base | — | `- [ ] coordinated Selfsame 0.4.22, cbcl-pairing 0.4.8, and this amendment` |
| `0.16.16` REQ-078 (`:93`) | "`- [ ] coordinated Selfsame 0.4.22`" | "0.4.23, cbcl-pairing 0.4.9" |
| `0.16.17` REQ-079 (`:87`) | "`- [ ] coordinated Selfsame 0.4.23`" | "0.4.24, cbcl-pairing 0.4.10" |
| `0.16.18` REQ-080 (`:86`) | "`- [ ] coordinated Selfsame 0.4.24`" | "0.4.25, cbcl-pairing 0.4.11" |
| **`0.16.19` REQ-081** | **"`- [ ] coordinated Selfsame 0.4.24`"** | "0.4.26, cbcl-pairing 0.4.12" |

`REQ-081` targets `0.16.17`'s output, which `0.16.18` already superseded. It is
off by one generation.

Both readings fail:

- If `0.16.16` through `0.16.18` retain their replacements, the effective first
  box begins "`- [ ] coordinated Selfsame 0.4.25`". `REQ-081` does not name it,
  so nothing is retired and the gate still names Selfsame 0.4.25 and
  cbcl-pairing 0.4.11.
- `0.16.19`'s own `adopts:` line calls those amendments "historical evidence
  only". Under that reading the effective first box reverts to `0.16.15`'s
  "`- [ ] coordinated Selfsame 0.4.22`". `REQ-081` does not name that either,
  so the gate names Selfsame 0.4.22 and cbcl-pairing 0.4.8.

Under either reading the GATE-04 coordinated-set box never names Selfsame
0.4.26, cbcl-pairing 0.4.12, or cbcl-bus 0.16.19. `TEST-1157`'s "Require all 42
cbcl-bus GATE-04 boxes" and hub `TEST-116` therefore cannot resolve to a
consistent gate.

There is a second, methodological defect in the same clause. `REQ-081` uses the
exact-phrase retirement technique that N10-M-1 rejected. Its own candidate's
stated instrument is "complete successor clauses". Its own ADR-963 says "No
exact phrase boundary participates in current authority." The candidate
contradicts its declared method.

**Minimum correction.** Restate GATE-04's first box in full inside `REQ-081`
and drop the phrase-boundary retirement. Alternatively, retire the box
beginning "`- [ ] coordinated Selfsame 0.4.22`" and say so explicitly. That box
is the effective base box under `0.16.19`'s own `adopts:` line.

Owner: cbcl-bus owner.

### N11-M-1 (MEDIUM, gate-blocking) — REQ-1005 attributes `WIRE_VERSION` to a pinned clause that governs offer expiry, so TEST-1157's resolution requirement cannot pass

`REQ-1005` states: "`WIRE_VERSION` belongs to the invitation-wire namespace
under `[[SPEC-001-device-key-provisioning#REQ-016]]`. `CODE_VERSION` belongs to
the link-code namespace under `[[SPEC-001-device-key-provisioning#CON-001]]`."
and pins both at `anuna-ssi` commit
`c7d462029841ea1884bb6f089732058d8838728d`.

Review question 8. The commit and the document resolve.

```console
$ git -C /Users/anuna-01/Code/anuna-ssi log -1 --format='%H %ad %s' c7d4620…728d
c7d462029841ea1884bb6f089732058d8838728d Tue Jul 28 17:00:37 2026 +1000 feat(spec): v0.4.0 …
$ git show c7d4620…728d:specs/SPEC-001-device-key-provisioning.md | grep -n 'REQ-016'
366:- **REQ-016 — Offer expiry.** A client SHALL reject a bundle arriving more than
367:  300 s after it minted the corresponding offer.
```

`REQ-016` is a 300-second freshness rule. It governs no version namespace.

```console
$ git show c7d4620…728d:specs/SPEC-001-device-key-provisioning.md | grep -n 'WIRE_VERSION\|CODE_VERSION'
(no output)
```

Neither identifier appears anywhere in the pinned document.

`CON-001` — "Link code and signed offer" at line 1073 — is correct for
`CODE_VERSION`. Its grammar states
`data-part = 29BECH32CHAR ; 18 bytes: version(1) ‖ application-id(1) ‖ secret(16)`,
which is exactly `crates/selfsame-core/src/code.rs:78,137,167,288,307`.

`CON-001` is also the correct anchor for `WIRE_VERSION`. It carries the
`anuna-ssi-v1` dialect grammar `(extend link-offer (v app purpose key desc exp
sig))` and the wire form `(offer :v 1 …)`. That is precisely what
`WIRE_VERSION` is in this repository:

```console
$ grep -rn 'WIRE_VERSION' crates/selfsame-core/src/
crates/selfsame-core/src/lib.rs:58:pub const WIRE_VERSION: i64 = 1;
crates/selfsame-core/src/record.rs:329:            SExpr::Atom(Atom::Num(WIRE_VERSION)),
crates/selfsame-core/src/record.rs:396:        if num(&fields[0])… != WIRE_VERSION {
crates/selfsame-core/src/record.rs:462:            SExpr::Atom(Atom::Num(WIRE_VERSION)),
crates/selfsame-core/src/record.rs:490:        if num(&fields[0])… != WIRE_VERSION {
```

Lines 329 and 462 emit the offer and grant `:v` field; lines 396 and 490 compare
it. `REQ-018` at line 325 is a second defensible anchor, since its signed
encoding is `{version, application_id, purpose, device_public_key,
device_description, expiry}`. `REQ-016` is not.

`TEST-1157` requires: "Require `successionVersion`, `WIRE_VERSION`, and
`CODE_VERSION` to resolve to their named namespaces and governing contracts."
With `REQ-016` named, that test cannot pass. The candidate specifies an
unsatisfiable test, which makes this an implementability defect rather than an
attribution nit.

The third exclusion is correct. `successionVersion` at
`crates/selfsame-app-identity/src/succession.rs:107,110,195,262,324,338`
resolves to `[[SPEC-004-application-scoped-identity#REQ-231]]` ("Identity
succession is explicit, bounded, and confirmed", `:1275`) and
`[[SPEC-004-application-scoped-identity#CON-225]]`.

This finding also leaves N10-L-1 open. That finding said two of the three named
governing contracts resolve to no document in the coordinated set. The
candidate's disposition claims closure through "fully qualified links" and a
pinned commit. The pin is real; the governance claim it pins is false for one
of the two.

**Minimum correction.** In `REQ-1005`, change the `WIRE_VERSION` attribution to
`[[SPEC-001-device-key-provisioning#CON-001]]`, or to `#REQ-018` if the signed
scope is the intended anchor. Record which, so `TEST-1157` has one target.

Owner: Selfsame owner.

### N11-M-2 (MEDIUM, gate-blocking) — TEST-1156 drops both alias-row withdrawal cases, leaving a live NFR-928 branch with no test

`NFR-928` retains the branch: "The write-locked alias row SHALL name that same
account and home DID. An absent or disagreeing alias row returns
`#(error inconsistent-binding)`."

`TEST-1150` tested it with two distinct cases:

```text
spec-008-0.4.23-…md:502  Delete the requested alias row while retaining the account row. Withdrawal
                    :503  SHALL return `inconsistent-binding` and delete nothing.
                    :505  Point the requested alias row at another account or DID. Require the same
                    :506  closed result and zero deletion.
```

Both appear in `TEST-1148` (0.4.22:409, :412) and `TEST-1150` (0.4.23:502,
:505). `TEST-1156` replaces `TEST-1150` in full and omits both. Its withdrawal
block keeps only the reciprocal-success case, the matching-DID/other-alias
case, the wrong-generation case, the already-withdrawn case, and the
reinstatement lock case.

The branch is real and reachable in code. `alias-agrees?/3` at
`apps/cbcl_chat/src/shell/cbcl-chat-webfinger-store.lfe:232` reads
`cbcl-webfinger-alias` under a write lock and is the sole guard between a
refusal and the two live-row deletes at `:227-228`. An absent alias row and a
disagreeing alias row are distinct inputs to it.

The two retained cases exercise the *account* row. Neither exercises the
*alias* row that `alias-agrees?/3` reads. The branch is normative and
untested.

Hub `TEST-115` has the same gap against hub `NFR-026`'s identical sentence.
Its predecessors never carried the two cases. On the hub side this is therefore
recorded pre-existing debt rather than a candidate-created loss.

**Minimum correction.** Restate both cases in `TEST-1156`, and add them to hub
`TEST-115` for coordination.

Owner: Selfsame owner; cbcl-bus owner for the hub twin.

### N11-M-3 (MEDIUM, gate-blocking) — NFR-928 drops the sentence that confined tagged success to collection and predicate owners, leaving every other owner's success shape undetermined

`NFR-926` and `NFR-927` both carry: "Only collection and predicate owners adopt
tagged success. Every other public owner retains its declared success shape and
adds its lane's typed busy shape."

```console
$ grep -n 'Only collection and predicate\|retains its declared' spec-008-0.4.26-…md
(no output)
$ grep -n 'Only collection and predicate' spec-053-0.16.19-…md
(no output)
```

`NFR-928` keeps only the positive half: "Public collection and predicate owners
use tagged success. They return `#(ok Value)` or `#(error authority-busy)`."
There is no longer any statement about the other twenty-odd public owners. Hub
`NFR-026` is the same.

`NFR-928`'s closing sentence reads "Existing success and non-contention
refusal meanings remain unchanged behind their declared result shapes". That
preserves *meanings*, not shapes. It does not say which owners change shape.

The ambiguity lands on real match sites at the baseline:

| Consumer | Line | Current pattern | Undetermined |
|---|---|---|---|
| `cbcl-chat-rooms:public-cleartext-channel/1` | `rooms.lfe:55` | `(tuple 'ok (tuple 'public _ 'false))` | does `roomcfg:get/1` keep this shape? |
| `cbcl-chat-rooms:start-room/2` | `rooms.lfe:101` | `` `#(ok ,pid) `` | unaffected, but the wrapping owner is not stated |
| removal path | `session-ws.lfe:1077` | `('ok …)` from `cbcl-chat-room:evict` | unaffected |
| `create-channel/5` | `session-ws.lfe:1898` | returns bare `'ok` | does `claim/5` keep a bare result? |
| `cbcl-chat-invite:mint/3` | — | `NFR-928` says `#(ok Token)` "only after commit" | consistent, but stated for two owners only |

`NFR-928` names `mint/3` and `ensure-room/4` explicitly. It says nothing about
the remaining nineteen public owners:

```text
claim/3  claim/4  claim/5  get/1  creator/1  redeem/2  put/3
grant/5  redeem-and-grant/5  revoke/2  since-seq/2  bind-account/2
account-binding/1  store-verified-device-grant/6
bind-and-store-verified-device-grant/8  confirmed-migrate-path-a-handle/3
revoke-device-grant/2  claim/2  accept/3
```

An implementer cannot determine which of those change shape. `TEST-1156` forces
`{aborted, nomore}` at every affected public owner and requires the lane's exact
busy terminal. That clause does not supply the missing success side.

**Minimum correction.** Restate the sentence in `NFR-928` and hub `NFR-026`:
*"Only collection and predicate owners adopt tagged success. Every other public
owner retains its declared success shape and adds its lane's typed busy
shape."*

Owner: Selfsame owner and cbcl-bus owner.

### N11-L-1 (LOW) — REQ-1005 introduces two candidate-created dead links

`zetl` was run over each specification vault.

```console
$ env -u NO_COLOR zetl check --dead-links --json    # zetl 0.9.3, selfsame specs/
TOTAL dead links: 263
dead links sourced from spec-008-0.4.26-resolved-authority-and-websocket-busy:
  209 SPEC-001-device-key-provisioning
  210 SPEC-001-device-key-provisioning
  572 SPEC-053-key-root-identity
dead links sourced from spec-008-0.4.25-fail-closed-room-start-and-singular-authority:
  (none)
```

Disposition against the trajectory's own norm:

| Target | Sources across the 0.4.x candidates | Disposition |
|---|---|---|
| `SPEC-053-key-root-identity` | `0.4.10`, `0.4.17`, `0.4.18`, `0.4.19`, `0.4.20`, `0.4.21`, `0.4.22`, `0.4.23` (×2), `0.4.26` | recorded pre-existing vault debt — the standard cross-repository parent pointer |
| `SPEC-076-pairing-account-grant` | `0.4.10` | recorded pre-existing vault debt |
| `SPEC-001-device-key-provisioning` | **`0.4.26` only** | **candidate-created** |

`0.4.25` sourced zero dead links. `REQ-1004` rendered the same two references
as prose: "`WIRE_VERSION` belongs to the Selfsame core invitation-wire
namespace under Selfsame core SPEC-001 REQ-016". The N10-L-1
correction converted them to wikilinks, and `SPEC-001-device-key-provisioning`
is not a note in this vault:

```console
$ find . -name 'SPEC-001*' -not -path './.git/*'
(no output)
```

`REQ-1005` itself says these are "out-of-set namespace references" whose source
is `anuna-ssi`. A wikilink to an out-of-set vault is a dead link by
construction.

**Minimum correction.** Render both references as prose naming the `anuna-ssi`
repository, document, clause, and pinned commit, as `0.4.25` did, or add a stub
note. Do not add `anuna-ssi` to the coordinated set.

Owner: Selfsame owner.

### N11-L-2 (LOW) — TEST-1156 drops TEST-1150's explicit prohibition on the two unreachable forcings

`TEST-1150` (0.4.23:445-446) states: "Do not require a raw exhausted
`#cyclic{}` or `nested_transaction` terminal. Prove statically that no
transaction body opens a transaction or catches an `{aborted, _}` exit."

`TEST-1156` keeps only the second sentence. The prohibition is gone. N11-C-1 revives `NFR-921`'s "Consumers map `nomore`, exhausted `#cyclic{}`,
and `nested_transaction` to `authority-busy`". Combined with that revival,
nothing now stops a test author from restoring the two forcings that N7-M-2
removed as unreachable.

**Minimum correction.** Restate the sentence in `TEST-1156` and hub
`TEST-115`.

Owner: Selfsame owner; cbcl-bus owner for the hub twin.

### N11-L-3 (LOW, recorded pre-existing debt) — the lint command all three tests mandate does not exist in any repository

`TEST-1157`, hub `TEST-116`, and cbcl-pairing `TEST-063` each require: "Run
`tools/usdd-lint.sh --type descriptive --strict` against all three candidates."

```console
$ for r in selfsame-…-1 cbcl-bus cbcl-pairing; do ls $r/tools/usdd-lint.sh; done
ABSENT
ABSENT
ABSENT
$ find / -name 'usdd-lint.sh' 2>/dev/null | head -2
/Users/anuna-01/Code/anuna-dev-skill/tools/usdd-lint.sh
/Users/anuna-01/.claude/skills/anuna-dev/tools/usdd-lint.sh
```

The repository-relative path resolves nowhere. The tool exists only in the
`anuna-dev` skill. The clause predates this candidate — `0.4.25`'s
`TEST-1155` carries it verbatim — so it is recorded pre-existing debt, not a
candidate-created defect. It is reported because a mandated test that names an
unresolvable path is not mechanically runnable in CI.

**Minimum correction.** Name the tool by its skill-resolved location, or vendor
it into each repository's `tools/`.

Owner: Selfsame owner, cbcl-bus owner, cbcl-pairing owner.

## N10 disposition

Every N10 obligation was re-derived independently. The 0.4.25 report was read
only to enumerate the seven finding identifiers.

| N10 finding | Candidate's claim | This review's independent disposition |
|---|---|---|
| `N10-M-1` — corrected withdrawal range boundary occurs nowhere in its target | "NFR-928 replaces all three predecessors in full, so no phrase boundary remains current." | **Closed as stated, defect relocated.** `NFR-928` states the withdrawal text inline, so no phrase boundary is needed for it. But hub `REQ-081` still performs an exact-phrase retirement against GATE-04, and gets the phrase wrong — see `N11-H-1`. The technique was not eliminated, only moved. |
| `N10-M-2` — REQ-1004 never re-declares REQ-1002's range replacements, so the requirement has two readings | "REQ-1005 replaces all three predecessors in full and restates their complete current law." | **Closed at the REQ-1002…1004 level, recreated below it.** `REQ-1005` never re-declares `REQ-1002`'s replacement of `REQ-1001`. `REQ-1001` therefore revives, and the requirement again has two readings. See `N11-C-1`. |
| `N10-M-3` — the closed four-identifier set cannot match nine of twenty-seven witnesses | "REQ-1005 closes five identifiers and requires exact case-sensitive substring matching." | **Closed. Verified mechanically.** All 27 named witnesses match under the five-identifier case-sensitive scan; the three ingest substitutions at `lib.rs:831`, `:847`, `:861` are reached by `profile_version`. Undone in effect by `N11-C-1`'s revival of `REQ-1001`'s four-identifier set. |
| `N10-M-4` — room-start busy reaches a WebSocket consumer no clause names | "NFR-928 includes both WebSocket consumers and TEST-1156 forces their distinct terminals." | **Closed. Verified against code.** Both consumers named and both lines resolve exactly: `session-ws.lfe:1076` (removal → `revoke-failed`) and `:1788` (hello → 1013). `TEST-1156` forces both. |
| `N10-M-5` — the hub current-test range contains two tests declared replaced in full | "CON-985 states the post-consolidation hub test set without replaced tests." | **Closed at the CON-985 level.** `CON-985` and hub `REQ-081` state the identical set: TEST-043–065, 068, 070, 093, 094, 098, 115, 116. `TEST-115` and `TEST-116` are defined at `spec-053-0.16.19…md:479` and `:597`. Singularity is defeated again by `N11-C-1`. |
| `N10-L-1` — two of three named out-of-scope governing contracts resolve to no document, one collides with a real in-scope artefact | "REQ-1005 uses fully qualified links and pins the out-of-set source commit." | **Not closed.** The pin is real and resolves. The governance claim it pins is false for `WIRE_VERSION`: `REQ-016` at the pinned commit is "Offer expiry", a 300-second freshness rule — see `N11-M-1`. The fix also creates two dead links — see `N11-L-1`. |
| `N10-L-2` — the candidate states two corrected range boundaries, and exactly one differs | "Complete successor clauses remove every range-count claim." | **Closed for Selfsame.** `0.4.26` makes no range-count claim. Hub `REQ-081` retains one exact-phrase retirement, which is where `N11-H-1` lands. |

Score: three closed and verified, one closed-then-relocated, one
closed-at-level-recreated-below, one closed-for-one-repository, one not closed.

## F-A through F-E and SPEC-007 REQ-812 regression table

Review question 6. Re-tested against the wallet, cbcl-pairing, and allocator
paths at the declared baselines. A finding in these controls is CRITICAL by the review mandate.
No such finding stands.

| Control | Verdict | Code-backed evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact `(applicationId, relayOrigin)` pair | **HOLDS, not weakened** | `REQ-1005`, `NFR-928`, `CON-985`, and `ADR-963` name no clause inside `REQ-914`/`CON-908` (0.4.1) or `REQ-981` (0.4.11). A grep of `0.4.26` for `REQ-9xx`/`NFR-9xx`/`CON-97x` returns only `REQ-903`, `REQ-904`, `REQ-995`, `CON-978`, and `NFR-925`–`NFR-928`. The Selfsame implementation diff from `0220cec` is empty outside `specs/`. The cbcl-bus GATE-04 box requiring the exact v2 policy pair is among the 41 boxes `REQ-081` leaves byte-for-byte. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS as an open hard stop, not weakened** | cbcl-pairing diff from `c22f2d05` is empty outside `specs/`. `src/profile.rs:797` still sets `claimed_by_secret_holder: true`; `src/profile.rs:87` still declares the field; `recognise_intent` remains at `:206`, `:506`, `:584`, `:661`; `endpoint.rs:982` still holds `apply_intent`. `0.4.12` changes no protocol language and states so in `REQ-034`. The candidate re-asserts the control as a hard stop and adds nothing to the display path. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS, not weakened** | Selfsame diff from `0220cec` is empty outside `specs/`. `src-tauri/src/cbcl_context.rs:244` `assemble_claimant` still calls `Custody::use_hierarchy_root` and `hierarchy::derive` before the socket. `CON-962`'s effect order is untouched by `CON-985`. Every terminal `NFR-928` adds creates nothing. The 503 "performs no room start, room ingest, persistence, or channel fan-out". Supervised room start creates "no ETS registration, room process, room state, or fan-out". The poller "performs no eviction". The 1013 close "emits no allocation acknowledgement, invite, grant, or enrollment frame". |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS, not weakened** | `assemble_claimant` still requires `AccountScopeId::parse(&record.account_scope_id)`, so the bootstrap remains load-bearing. `CON-939` and `REQ-970` are named nowhere in `CON-985`. The counterpart hub obligation stands at `spec-053-0.16.1…md:201`: "The account DID cannot exist before the hub mints the account scope." The duty is carried, not prose-only. `NFR-928` lane rule 3 refers to `cbcl-selfsame-*` tables "created by the effective credential/v2 design"; a grep finds none at `ce894a7f`, which is correct for a forward-looking clause. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS, not weakened** | Neither candidate asserts a relay-operatorship binding property. cbcl-pairing `CON-029` states "Every protocol survivor recorded through CON-028 remains effective", which preserves the no-relay-field property. `REQ-034` fails "any protocol-body change relative to 0.4.8". |
| **SPEC-007 `REQ-812`** — endpoint failures cannot authorise | **HOLDS, not weakened** | `REQ-812` bars any accepted credential or identity side effect before approved payload delivery. Every unavailability terminal `NFR-928` introduces is a pure refusal. Verified against code. The webhook's `(after (cbcl-blobstore:release token))` at `cbcl-webhook-handler.lfe:85` already guarantees slot release on both 503 paths. `cbcl-chat-rooms:ingest/3` returns before `(get-or-start room)` at `rooms.lfe:49`, so no room starts. `session-ws.lfe:1077-1083` clears provenance and emits `agent-removed` only on `'ok`, so a `revoke-failed` terminal emits neither. |

`EnrolmentAllocator::new_native` is at
`crates/selfsame-web-device/src/lib.rs:1318`, reached from the wasm constructor
at `:1431` and from test code at `:2565`. `REQ-1005`'s prohibition —
"Credential/v2 … SHALL NOT call `EnrolmentAllocator::new_native`" — is
unchanged from `REQ-1004` and remains testable at that boundary.

## Authority-resolution table

Review question 1. Read at every named line.

| Question | Answer |
|---|---|
| Does complete replacement preserve every effective obligation from 0.4.23–0.4.25? | **No.** Two obligations are dropped outright: the collection/predicate scoping sentence (`N11-M-3`) and, in the test layer, both alias-row withdrawal cases plus the unreachable-forcing prohibition (`N11-M-2`, `N11-L-2`). |
| Does complete replacement preserve every effective obligation from 0.16.16–0.16.18? | **No.** Hub `NFR-026` drops the same scoping sentence. Hub `REQ-081`'s GATE-04 retirement resolves against no effective box (`N11-H-1`). |
| Omissions | `NFR-928`/`NFR-026`: "Only collection and predicate owners adopt tagged success…"; `TEST-1156`: two alias-row cases; `TEST-1156`: "Do not require a raw exhausted `#cyclic{}` or `nested_transaction` terminal." |
| Contradictions | Eleven revived-clause conflicts in `N11-C-1`. They span lock order, terminal classification, the version-identifier set, version vocabulary, two test sets, one review set, and one pointer set. |
| Duplicated survivors | `REQ-1001` beside `REQ-1005`; `NFR-924` beside `NFR-928`; `ADR-959` beside `ADR-963`; `TEST-1148`/`TEST-1149` beside `TEST-1156`/`TEST-1157`; `CON-981` beside `CON-985`; hub `REQ-077` beside `REQ-081`; hub `NFR-019` beside `NFR-026`. |
| Stale pointers | Revived `CON-981` points `DEC-012` at cbcl-bus 0.16.15 and names cbcl-pairing 0.4.8; GATE-04's first box names Selfsame 0.4.22 or 0.4.25 but never 0.4.26. |
| Ambiguous authority | `REQ-1005`'s "applies every retirement through CON-985" and "Retired text supplies no … replacement authority" cannot both govern. |

## Consumer-inventory table

Review question 3. Every named line was opened at the cbcl-bus implementation
baseline `ce894a7f`. Production sources are under
`apps/cbcl_chat/src/shell/` and `apps/cbcl_router/src/`; `_build` copies and
`test/` were excluded.

| Named site | Resolves | Producer | Current code | Specified terminal | Fails closed |
|---|---|---|---|---|---|
| `session-ws.lfe:512` | yes | `device-grants/1` | `'device-set (map 'devices (cbcl-chat-account-grants:device-grants account)` | unwrap before admission map | yes |
| `session-ws.lfe:869` | yes | `rooms-for-account/1` | `(cbcl-chat-account-roommember:rooms-for-account account))` under `lists:foreach` | unwrap before sink | yes |
| `session-ws.lfe:965` | yes | `list-public/0` | `(cbcl-core-wire:channels (cbcl-chat-roomcfg:list-public))` | unwrap before wire encoder | yes |
| `session-ws.lfe:1050` | yes | `member-grant?/2` | `(_ (if (cbcl-chat-roommember:member-grant? room name)` | no tagged value in a boolean | yes |
| `session-ws.lfe:1683` | yes | `grants/1` | `(pairaccount-standing-reply handle (cbcl-chat-pairaccount:grants handle)` | unwrap before `lists:map` | yes |
| `session-ws.lfe:347`, `:385` | yes | `unratified-device?/1` | `'pairing-unratified (cbcl-chat-pairaccount:unratified-device? pk)` | no tagged value in a predicate field | yes |
| **`session-ws.lfe:1076`** | yes | `get-or-start/1` | `(case (cbcl-chat-room:evict (cbcl-chat-rooms:get-or-start room) name)` | existing `revoke-failed`, provenance preserved, no removal ack | yes — `:1084` `(_ (reply-error … #"revoke-failed"))` already catches, and `:1078` clears provenance only on `'ok` |
| **`session-ws.lfe:1788`** | yes | `get-or-start/1` | `(let ((pid (cbcl-chat-rooms:get-or-start room))` then `cbcl-chat-room:join` | WebSocket 1013 `try-again-later`, no join, no membership grant | yes, once a guard precedes `join` |
| **`session-ws.lfe:1898`** | yes | `claim/5` | `('true (cbcl-chat-roomcfg:claim room vis cap handle dialects) 'ok)` — result discarded | 1013, never `'ok` | yes; the discard is exactly as the clause describes |
| `room.lfe:201` | yes | `roomcfg:get/1` in `init/1` | `(case (cbcl-chat-roomcfg:get room)` with a catch-all `cfg-result` clause | stop the child | yes, and this closes a live defect — see below |
| `room.lfe:221` | yes | `get-dialects/1` in `init-with-cfg` | `(dialects (case (cbcl-chat-roomcfg:get-dialects room) (`#(ok ,ds) ds) (_ '())))` | stop the child | yes |
| `room.lfe:550` | yes | `get-dialects/1` in `refresh-dialects` | same shape | retain prior dialects, emit no frame | yes |
| `room.lfe:263-270` | yes | `re-admit?/3` ×2 | `(andalso (=:= vis 'private) … (cbcl-chat-account-roommember:re-admit? …))` | no tagged value in `andalso` | yes |
| `room.lfe:677` | yes | `peer-grants/1` | `(cbcl-chat-account-grants:peer-grants account))` as the `lists:map` list | unwrap before `lists:map` | yes |
| `pairgrant.lfe:78` | yes | `member-grant?/2` | `((not (cbcl-chat-roommember:member-grant? room subject)) #(error not-a-member))` | no tagged value under `not` | yes |
| `path-b-poller.lfe:17` | yes | `accounts/0` | `(cbcl-chat-account-grants:accounts)))` as the `lists:foreach` list | OBS-907 `account-scan`, no resolver call, poller alive | yes |
| `path-b-poller.lfe:32` | yes | `revoke-observed/2` | `(case (cbcl-chat-account-grants:revoke-observed account ids) … (_ 'ok))` | OBS-907 `revocation-apply`, no eviction, next tick | yes |
| `rooms.lfe:49` | yes | `get-or-start/1` | `('ok (cbcl-chat-room:ingest (get-or-start room) payload sig))` | return without calling `room:ingest/3` | yes |
| `rooms.lfe:54`, `:60`, `:61` | yes | `roomcfg:get/1`, `claim/3`, `roomcfg:get/1` | the three webhook forcings, with `claim/3`'s result discarded at `:60` | `authority-busy` propagates | yes |
| `rooms.lfe:99-104` | yes | `start-room/2` | `(other (tuple 'reply other state))` | preserve the child-start failure | yes, already preserved today |
| `cbcl-webhook-handler.lfe:76` | yes | `rooms:ingest/3` | `(case (cbcl-chat-rooms:ingest room payload (zero-sig))` | 503 `authority-busy` / 503 `store-unavailable`, `no-store` | yes |

**Two live defects the inventory correctly identifies.** Both were confirmed by
reading the code, not by accepting the clause.

1. `cbcl-chat-room.lfe:200-215`. `init/1` special-cases only
   `#(error store-unavailable)` and binds everything else to `cfg-result`.
   `init-with-cfg` at `:212-215` then does
   `(case cfg-result (`#(ok ,vce) vce) (_ #(public undefined false)))`. Today an
   `authority-busy` result therefore selects the ADR-007 public and cleartext
   default — an open join and a cleartext backfill of a private
   room's history. `NFR-928`'s "Only `#(error unknown-room)` MAY select the
   ADR-007 `#(public undefined false)` default" closes exactly this.
2. `cbcl-webhook-handler.lfe:76-84`. The dispatch is `'ok` → 202,
   `#(error #(persist-failed _))` → 503, and `#(error _)` → **409
   `channel-not-public`**. Today a retry-exhausted contention failure is
   answered as a permanent policy rejection. `NFR-928`'s split into two
   distinct 503s, with "HTTP 409 remains only for a committed non-public-channel
   decision", closes exactly this. `rooms.lfe:66` and `:63` carry the two
   wildcard defaults that produce it.

**Reach beyond the named rows.** `NFR-928` makes the repository-wide scan the
completeness authority. It adds that "A scan limited to public-owner call sites
is non-conforming." Two transitive chains follow. They show the named rows are
evidence rather than a closure. They also show that the requirement's wording
is what makes them reachable:

- `session-ws.lfe:1898` is a *producer* inside `create-channel/5`. Its consumer
  is `ensure-channel/5` at `:1853`, whose consumer is the `do-hello` case at
  `:1776-1789`. That case matches `#(error no-such-channel)`,
  `#(error channel-exists)`, `#(error dialect-not-found)`, and `'ok`, with **no
  wildcard**. A new `#(error authority-busy)` matches no clause there. It
  raises a `case_clause` exit rather than a 1013 close. The clause reaches it only through the transitive scan, never
  through the named rows.
- `cbcl-chat-app.lfe:96` — `(cbcl-chat-roomcfg:claim #"@general" 'public
  'undefined)`, result discarded — is the startup default-room claim. `NFR-928`
  reaches it through "At startup, busy prevents the listener from opening. It
  never becomes a successful default-room claim", not through a named line.
  `cbcl-chat-app.lfe` carries eleven authority-owner call sites and is named
  nowhere.

Both are covered by the requirement as written. Neither is a defect. They are
recorded because they show the named evidence understates the inventory by a
non-trivial margin. `TEST-1157` requires the consumer inventory "to cover every
direct and transitive carrier". That clause is therefore load-bearing rather
than decorative.

**Carriage check on the 1013 terminal.** `NFR-928` places a WebSocket 1013
close at several boundaries. No 1013 close exists anywhere in the hub today:

```console
$ grep -rn '1013\|try-again-later\|try_again' apps/ --include='*.lfe' | grep -v _build
(no output)
```

The carriage nevertheless exists. `cbcl-agent-ws.lfe:18-19` declares
`websocket_handle/2` and `websocket_info/2` and returns Cowboy command lists of
the form `(tuple (list (tuple 'binary Bytes)) state)` at `:77`, `:103`, `:108`,
and `:324`. Cowboy accepts `{close, Code, Payload}` in the same command list, so
`(tuple (list (tuple 'close 1013 #"try-again-later")) state)` is expressible
without a new transport. The candidate correctly drops `0.4.25`'s false
"existing WebSocket 1013 terminal" wording at the `claim/5` row. This is
implementable and is not a finding. It is nonetheless a new mechanism with zero
current uses. The design names no owning module for it.

**WebFinger terminals.** `provision/3` at `cbcl-chat-webfinger-store.lfe:129`,
`withdraw/2` at `:201`, and `reinstate/2` at `:297` each call the bare unary
`(mnesia:transaction (lambda () …))`. Each maps every failure to
`#(error unavailable)`. `NFR-928` moves all three to the shared
`mnesia:transaction(Fun, [], 3)` wrapper. All three currently have **no
production consumer** — only `lookup/1` is consumed, at
`cbcl-chat-webfinger-http.lfe:27`. The mutation-terminal change is therefore
consumer-free and safe. `lookup/1` at `:314-320` already returns `'unavailable`,
which `cbcl-chat-webfinger-http.lfe:36` maps to an empty 503 whose base headers
carry `cache-control: no-store` at `:42`. `NFR-928`'s exemption and mapping are
verified true of the code as it stands.

## Pointer and test-set tables

Review question 5. Verified mechanically.

### Pointer set

| Pointer | Required target | Observed | Status |
|---|---|---|---|
| Selfsame parent `SPEC-008-production-pairing-claimant` | 0.4.26 | `candidate-amendment: "[[spec-008-0.4.26-resolved-authority-and-websocket-busy#CON-985]] …"` at `:8`, restated at `:17` | correct |
| cbcl-bus parent `SPEC-053-key-root-identity` `DEC-012` | 0.16.19 | `candidate-amendment: "[[spec-053-0.16.19-resolved-authority-and-websocket-busy#REQ-081]] …"` | correct |
| cbcl-pairing `consumer-design` | Selfsame 0.4.26 | `consumer-design: selfsame SPEC-008 0.4.26-draft` | correct |
| Revived `CON-981` `DEC-012` | — | points at cbcl-bus 0.16.15 | **duplicate, stale** |
| cbcl-bus GATE-04 first box | 0.4.26 / 0.4.12 / 0.16.19 | 0.4.22 or 0.4.25 depending on reading | **never names this set** |
| Generation provenance | all three | family, version, session, and enumerated trajectory present in all three frontmatters | correct |

### Test sets

| Set | `CON-985` / hub `REQ-081` | Competing survivor | Singular |
|---|---|---|---|
| Selfsame current | TEST-1134, TEST-1135, TEST-1139, TEST-1156, TEST-1157, plus every effective test through TEST-1108 | revived `CON-981`: TEST-1134, TEST-1135, TEST-1139, **TEST-1148, TEST-1149** | **no** |
| Hub current, as stated by Selfsame | TEST-043–065, 068, 070, 093, 094, 098, 115, 116 | revived `CON-981`: … **TEST-107, TEST-108** | **no** |
| Hub current, as stated by the hub | TEST-043–065, 068, 070, 093, 094, 098, 115, 116 | revived `REQ-077`: TEST-043–065, 068, 070 | **no** |
| cbcl-pairing protocol survivors | `CON-029` preserves "every protocol survivor recorded through CON-028" | none | **yes** |

Selfsame `CON-985` and hub `REQ-081` state byte-identical hub sets, so the two
candidates agree with each other. Both disagree with their own adopted
predecessors. `TEST-1134` (`0.4.17:321`), `TEST-1135` (`0.4.17:341`),
`TEST-1139` (`0.4.18:255`), `TEST-115` (`0.16.19:479`), `TEST-116`
(`0.16.19:597`), and `REQ-995` (`0.4.16:153`) all resolve.

Only cbcl-pairing is mechanically singular.

## REQ-1005 five-identifier scan

Review question 2. Run against the code at `0220cec`, exact case-sensitive
substring matching, over every Rust source in `crates/` and `src-tauri/`.

```console
$ grep -rn --include='*.rs' -e PROFILE_VERSION -e profileVersion -e profile_version \
      -e evidenceVersion -e payloadVersion crates/ | wc -l
     102
$ grep -rn --include='*.rs' … src-tauri/ | wc -l
       6
```

| Result | Finding |
|---|---|
| Named production witnesses | **27 of 27 resolve and match.** Every line was opened; each carries at least one of the five identifiers and matches its declared class. |
| `profile_version` coverage | Reaches the three ingest substitutions at `lib.rs:831`, `:847`, `:861` and the three typed assignments at `:1346`, `:1360`, `:1369`. `TEST-1157`'s "Require `profile_version` matches to include all three ingest substitutions" is satisfiable. |
| Test separation | `#[cfg(test)]` begins at `selfsame-web-device/src/lib.rs:1521`, `selfsame-beam/src/enrollment.rs:199`, `selfsame-app-identity-net/src/profile.rs:221`, `src-tauri/src/app_grant.rs:891`. All matches above those lines are production; all below are test. The four `selfsame-app-identity` files have no `#[cfg(test)]` block at all, so every match in them is production. |
| Unnamed production matches | None that the classifier cannot place. `selfsame-app-identity/src/context.rs:49,66` and `json.rs:61` are comments; `web-device/src/lib.rs:382,396,408` are struct field declarations; `:597` is a format template — all four categories are named exclusions in `REQ-1005`. `selfsame-beam` and `selfsame-app-identity-net` matches are test-only. `src-tauri` matches are test-only. |
| Exclusion of the three other namespaces | Correct by construction. `successionVersion`, `WIRE_VERSION`, and `CODE_VERSION` contain none of the five identifiers as a case-sensitive substring, and are absent from the 108 matches. |
| Governing-contract resolution | `successionVersion` → `SPEC-004 REQ-231`/`CON-225`: **correct**. `CODE_VERSION` → `SPEC-001 CON-001`: **correct**. `WIRE_VERSION` → `SPEC-001 REQ-016`: **wrong** — see `N11-M-1`. |

The scan mechanism is sound and complete. Only one of its three exclusion
attributions is wrong.

## Lint and zetl results, with pre-existing debt disposition

Review question 9.

### Strict controlled-language lint

Run as `sh usdd-lint.sh --type descriptive --strict FILE` using the
skill-resolved tool at `/Users/anuna-01/.claude/skills/anuna-dev/tools/usdd-lint.sh`,
because no repository carries `tools/usdd-lint.sh` — see `N11-L-3`.

| Candidate | Longest sentence | Scanned | Errors | Warnings | Exit |
|---|---|---|---|---|---|
| Selfsame `0.4.26` | 21 (limit 25) | 406 sentences, 3,782 words | 0 | 0 | 0 |
| cbcl-bus `0.16.19` | 20 (limit 25) | 314 sentences, 2,838 words | 0 | 0 | 0 |
| cbcl-pairing `0.4.12` | 20 (limit 25) | 49 sentences, 430 words | 0 | 0 | 0 |

All three pass strict lint cleanly. `TEST-1157`'s lint clause passes.

### zetl

`zetl 0.9.3`. Run per vault as
`env -u NO_COLOR zetl check --dead-links --json` and
`env -u NO_COLOR zetl check --fail-on error`.

| Vault | Dead links | Orphans | Syntax errors | SPL errors |
|---|---|---|---|---|
| selfsame `specs/` | 263 | 31 | 1 | 0 |
| cbcl-bus `specs/` | 567 | — | — | — |
| cbcl-pairing `specs/` | 0 | — | — | — |

Disposition:

| Item | Disposition |
|---|---|
| 260 of 263 selfsame dead links | **recorded pre-existing vault debt.** Sources are `EXP-001`, `EXP-002`, `SPEC-004`, and earlier trajectory documents. Targets include `PROTO-001-usdd-agent-protocol`, `SCREEN-001-authorise-a-device`, `Tauri`, `WebView`, `DID`, `ADB`, and `Appium`. None is sourced from a 0.4.26 clause. |
| 1 selfsame syntax error | **recorded pre-existing vault debt.** `EXP-002-e2e-linking-harness.md:263` — "Unclosed wikilink: `'[[Cargo'`". |
| `0.4.26:572` → `SPEC-053-key-root-identity` | **recorded pre-existing vault debt.** The same cross-repository parent pointer appears in `0.4.10`, `0.4.17`–`0.4.23`. |
| `0.4.26:209`, `:210` → `SPEC-001-device-key-provisioning` | **candidate-created.** No prior 0.4.x candidate links this target; `0.4.25` sourced zero dead links. Reported as `N11-L-1`. |
| 567 cbcl-bus dead links | **recorded pre-existing vault debt.** None is sourced from `0.16.19` or `0.16.18`. |
| cbcl-pairing | clean. |

`zetl check --fail-on error` exits non-zero on both the selfsame and cbcl-bus
vaults, driven entirely by pre-existing debt plus the two candidate-created
links above.

## Terminals: implementable and fail closed

Review question 4. Each terminal was checked against the code that carries it.

| Terminal | Implementable | Fails closed | Evidence |
|---|---|---|---|
| WebSocket 1013 `try-again-later` | yes | yes | Cowboy command list at `cbcl-agent-ws.lfe:77`, `:103`, `:108`, `:324` accepts `{close, Code, Payload}`. No membership grant, join, invite, or enrollment frame is emitted because the close replaces the command list entirely. |
| `revoke-failed` at the removal path | yes | yes | `session-ws.lfe:1084` already routes every non-`'ok` evict result there. Provenance is cleared and `agent-removed` built only inside the `'ok` branch at `:1077-1083`. |
| HTTP 503 `authority-busy` and 503 `store-unavailable` | yes | yes | `cbcl-webhook-handler.lfe:82-84` already distinguishes `persist-failed` from the wildcard; adding two more clauses is mechanical. `(after (cbcl-blobstore:release token))` at `:85` releases the slot on every path. No room start occurs because `rooms.lfe:47-50` returns `err` before `get-or-start` at `:49`. `cache-control: no-store` requires extending `respond/4` at `:95`. |
| Next-tick deferral at the Path-B poller | yes | yes | `path-b-poller.lfe:32-34` already discards non-`#(ok keys)` results without evicting; the change is to emit OBS-907 instead of a silent `'ok`. The `handle_info`/`handle_cast` timers at `:10-11` keep the poller alive. |
| Startup refusal | yes | yes | `cbcl-chat-app.lfe:96` discards the `@general` claim result today; refusing to open the listener is a supervisor-level decision the application already owns at `:88`. |
| Room-start refusal | yes | yes | `room.lfe:207-209` already returns `#(stop store-unavailable)` for one failure atom; adding `authority-busy` is one clause. `rooms.lfe:105-106` already preserves the child-start failure. No ETS insert occurs because `:103` is inside the `` `#(ok ,pid) `` branch. |
| `schema-migration-busy` | yes | yes | `cbcl-store-migrate:legacy-rows/0` at `apps/cbcl_bus/src/store/cbcl-store-migrate.lfe:78`, called from `:36`, is the single named transaction-based schema site; `cbcl-chat-roommember:migrate-since/0` at `cbcl-chat-app.lfe:88` uses `transform_table/4` and is correctly excluded. |

Absence of membership grant, join, room registration, persistence, policy
selection, and fan-out was confirmed on every negative path listed above. No
negative path reaches `ets:insert` at `rooms.lfe:103`, `cbcl-chat-room:join`,
`cbcl-chat-room:ingest`, or the fan-out at `room.lfe:555`.

The terminals are the strongest part of this candidate. Every one is
implementable, every one fails closed, and two of them close defects that are
live in the code today.

## Stopping-rule assessment

Each finding is classified.

| Finding | Class | Style? |
|---|---|---|
| `N11-C-1` | authority defect, with implementability consequences in the lock graph and terminal classifier | no |
| `N11-H-1` | authority defect at a gate | no |
| `N11-M-1` | implementability defect — a mandated test cannot pass | no |
| `N11-M-2` | testability defect — a normative branch has no test | no |
| `N11-M-3` | implementability defect — success shapes undetermined at real match sites | no |
| `N11-L-1` | authority hygiene | no |
| `N11-L-2` | testability hygiene | no |
| `N11-L-3` | testability hygiene, pre-existing | no |

None is style. All eight are genuine.

**The bounded amendment loop SHOULD stop, and the verdict is REJECT.**

The reasoning is not that the remaining work is large. It is small: one
sentence restored per repository, one GATE-04 target corrected, one link
attribution changed, two test cases restored, one scoping sentence restored.
Six edits.

The reasoning is that the loop has stopped converging on the class of defect
that keeps producing REJECTs.

1. **The engineering is done and verified.** Every mechanism this increment
   specifies was checked against the code in this review and holds. The
   consumer inventory's twenty-one named lines all resolve. The five-identifier
   scan reaches all twenty-seven witnesses. Both WebSocket room-start consumers
   are named with distinct, code-consistent terminals. Two live defects are correctly
   identified and correctly closed. They are `room.lfe:201`'s public-cleartext
   default on an unreadable config and `cbcl-webhook-handler.lfe:83`'s 409 for
   transient contention. The design is right.

2. **The failures are all in the amendment ledger, and they recur.** 0.4.25 was
   rejected for phrase-boundary replacements that did not resolve. 0.4.26
   abandons phrase boundaries and severs the retirement chain instead. That is
   the inverse error with one root cause: an instrument that does not resolve
   to one reading. `N10-M-2` and `N11-C-1` are the same defect at two depths.
   Eight consecutive candidates, 0.4.19 through 0.4.26, fail on authority
   resolution rather than on the mechanism.

3. **Each corrective pass introduces a new instance of the class it repairs.**
   `N10-L-1` asked for fully qualified links. That fix produced two dead links
   and one false governance claim. `N10-M-1` asked to eliminate
   phrase boundaries; the fix eliminated them from Selfsame and left one in the
   hub, pointing at the wrong phrase. This is the signature of a format that no
   longer fits the artefact.

4. **The correct next move is not 0.4.27.** It is a single consolidated re-issue
   of SPEC-008's authority section and SPEC-053's. That re-issue is one
   document with no cross-version replacement instrument at all. That is what
   `CON-985` reaches for. `CON-985` reaches it by keeping the preservation
   sentence and restating the pre-0.4.23 retirements inline.
   cbcl-pairing `0.4.12` shows the technique works when applied. `CON-029` reads
   "Every protocol survivor recorded through CON-028 remains effective".
   cbcl-pairing is the one repository whose sets are mechanically singular.

This matches the candidates' own gate language. `CON-985`: "A REJECT ends this
amendment loop. It returns the architecture to the owner for reconsideration
before another specification change." That is the correct disposition here.

## What this verdict authorizes and does not authorize

This REJECT authorizes no Elephant SPL, no test-first plan, no implementation,
no allocation, no release, and no deployment. Production invitation allocation
stays closed. The Tier-1 gate remains open. All 42 cbcl-bus GATE-04 boxes
remain open.

Every F-A through F-E hard stop remains a hard stop, as does
`[[SPEC-007-cbcl-pairing-cutover#REQ-812]]`. None was weakened by this
candidate and none was closed by it.

## Corrections and owners, collected

| # | Finding | Correction | Owner |
|---|---|---|---|
| 1 | `N11-C-1` | Restore a preservation sentence in `CON-985`: "Every retirement and replacement recorded through CON-984 remains effective unless this contract states otherwise." | Selfsame owner |
| 2 | `N11-C-1` | Restore the same sentence in hub `REQ-081`, scoped to `REQ-080`. | cbcl-bus owner |
| 3 | `N11-C-1` | Reword `REQ-1005`'s "Retired text supplies no … replacement authority" so a retirement recorded by a retired instrument survives; or restate the pre-0.4.23 retirements inline. | Selfsame owner |
| 4 | `N11-H-1` | Retire GATE-04's effective first box by its actual opening phrase, or restate the box in full and drop the phrase-boundary technique. | cbcl-bus owner |
| 5 | `N11-M-1` | Attribute `WIRE_VERSION` to `anuna-ssi` SPEC-001 `CON-001` (or `REQ-018`), not `REQ-016`. Keep the reference in prose, per correction 8. | Selfsame owner |
| 6 | `N11-M-2` | Restate the absent-alias-row and disagreeing-alias-row withdrawal cases in `TEST-1156`; add them to hub `TEST-115`. | Selfsame owner; cbcl-bus owner |
| 7 | `N11-M-3` | Restate "Only collection and predicate owners adopt tagged success. Every other public owner retains its declared success shape and adds its lane's typed busy shape." in `NFR-928` and hub `NFR-026`. | Selfsame owner; cbcl-bus owner |
| 8 | `N11-L-1` | Render the two `anuna-ssi` references as prose with repository, document, clause, and pinned commit, as `0.4.25` did. | Selfsame owner |
| 9 | `N11-L-2` | Restate "Do not require a raw exhausted `#cyclic{}` or `nested_transaction` terminal." in `TEST-1156` and hub `TEST-115`. | Selfsame owner; cbcl-bus owner |
| 10 | `N11-L-3` | Give the lint command a path that resolves, or vendor `usdd-lint.sh` into each repository. | all three owners |

Corrections 1 through 3 are the load-bearing ones. Without them the other seven
are edits to a document that does not resolve to one reading.

## Report self-audit

Every claim in this report names a file and a line, and every named line was
opened. No conclusion rests on a candidate's own assertion about the code.

Method limits, stated rather than hidden:

- Line numbers cite the declared implementation baselines, which equal each
  repository HEAD because no implementation file changed.
- The `#[cfg(test)]` boundary was used to separate production from test code.
  A conditional compilation form other than that attribute escapes it.
- The consumer inventory was traced by module-qualified call site. A call
  reached only through `apply/3` or a dynamic module name escapes it.
- The `anuna-ssi` repository was read at one pinned commit and is recorded as
  an out-of-set reference. It supplies no coordinated design authority here.
- This report passes `usdd-lint.sh --type descriptive --strict` with zero
  errors and zero warnings.

This review changed no candidate, parent, implementation, or external
repository file. Its only artefact is this report.

## Changelog

- **2026-08-23 — Claude Tier-1 adversarial review of the coordinated 0.4.26,
  0.16.19, and 0.4.12 candidates. Verdict REJECT.** One CRITICAL, one HIGH,
  three gate-blocking MEDIUM, three LOW. This review recommends ending the
  bounded amendment loop. No candidate, parent, code, or external repository
  file changed under this review.
