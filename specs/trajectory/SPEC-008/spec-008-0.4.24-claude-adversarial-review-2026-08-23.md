---
id: SPEC-008-review-0.4.24-claude-2026-08-23
title: SPEC-008 0.4.24 + cbcl-pairing 0.4.10 + cbcl-bus 0.16.17 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer-model-family: Anthropic Claude
reviewer-model-version: Claude Opus 5 (`claude-opus-5`)
reviewer-session: 03b89dda-1ef0-436d-bc28-1b180b822ad2
circus-attempt: spec008-v0424-review/1
review-date: 2026-08-23
review-input: "[[spec-008-0.4.24-explicit-background-and-http-busy-terminals]]"
prior-review: "[[spec-008-0.4.23-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.24 + cbcl-pairing 0.4.10 + cbcl-bus 0.16.17 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

Verdict: **REJECT**.

## Authentication, model, and independence evidence

The gate ran before any candidate prose entered this context. The gate result
was recorded first. No secret value was printed.

```text
$ if [ -n "${ANTHROPIC_API_KEY+x}" ]; then echo PRESENT; else echo ABSENT; fi
ABSENT
$ env | grep -c -i 'ANTHROPIC_API_KEY'
0
$ env | grep -iE '^(CLAUDE|ANTHROPIC)' | sed 's/=.*/=<redacted>/'
CLAUDECODE=<redacted>                      # nine CLAUDE_* rows in total,
CLAUDE_CODE_ENTRYPOINT=<redacted>          # every value redacted before display
CLAUDE_EFFORT=<redacted>                   # and no ANTHROPIC_* row returned
$ claude auth status --json     # only the three permitted fields are recorded
  authMethod       = claude.ai
  apiProvider      = firstParty
  subscriptionType = max
```

The variable is absent from the process environment. The reported `authMethod`
is `claude.ai`. The reported `apiProvider` is `firstParty`. The reported
`subscriptionType` is `max`. That is the acceptable subscription path.

The command output also carries an electronic-mail address, an organisation
name, and an organisation identifier. A field-selecting filter read those rows
only to reach the three permitted values. None of them appears in this report.

### Reviewer identity

| Field | Value |
|---|---|
| Model family | Anthropic Claude |
| Model | Claude Opus 5, exact identifier `claude-opus-5` |
| Harness | Claude Code `2.1.241`, reasoning effort `max` |
| Runtime session identifier | `03b89dda-1ef0-436d-bc28-1b180b822ad2` |
| Circus attempt | `spec008-v0424-review/1` |
| Circus worktree | `/Users/anuna-01/Code/selfsame-spec008-v0424-review-1` |
| Circus branch | `circus/spec008-v0424-review/1` |

The Circus attempt record is
`/Users/anuna-01/Code/selfsame/.git/circus/spec008-v0424-review/1/record.json`.
It carries `"attempt_id": "spec008-v0424-review/1"` and `"state": "prepared"`.
Its `prepared_at` value is `2026-08-23T10:09:17Z`.

### Distinctness from the generating record

| Property | Generation | This review | Distinct |
|---|---|---|---|
| Model family | OpenAI GPT-5 | Anthropic Claude | yes |
| Model version | `gpt-5.6-sol` | `claude-opus-5` | yes |
| Session | `01a029aa-9127-7c42-ad28-81512b91ded6` | `03b89dda-1ef0-436d-bc28-1b180b822ad2` | yes |
| Attempt | none recorded | `spec008-v0424-review/1` | yes |
| Context | authored all three candidates | fresh, candidates and code only | yes |

All three candidates record the same generating family, version, and session.
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#Tier-1-generation-provenance]]
carries the Selfsame record. The cbcl-bus and cbcl-pairing records agree with
it. This reviewer belongs to a different model family and a different session.
The [[PROTO-001-usdd-agent-protocol#Multi-Model Cognitive Diversity]] rule is
therefore satisfied on recorded evidence.

This session received the three candidates, the whole amendment chain, and the
three implementation baselines. It did not receive the generator's reasoning.

### Executed evidence beyond reading

Six classes of evidence were produced rather than reasoned about.

1. Every cross-module production consumer of an in-lane public owner was
   enumerated by a scan of production LFE under `apps/`.
2. Every production Rust source was scanned for the four version identifiers
   the candidate classifier names, and for every other version namespace.
3. Every named witness line was read at the declared baseline and compared
   with the construct its label claims.
4. Every named consumer line, boolean context, and webhook edge was read at
   the declared baseline.
5. The cbcl-bus GATE-04 boxes were counted mechanically over the effective
   gate.
6. `tools/usdd-lint.sh --type descriptive --strict` ran over all three
   candidates and over their two predecessors.

## Reviewed commits and baselines, resolved rather than assumed

| Repository | Role | Commit | Verified |
|---|---|---|---|
| selfsame | candidate | `7cf3cbd8d35998d4a279fffad80954a1daba5f80` | `spec(pairing): define every asynchronous busy terminal` |
| selfsame | implementation baseline | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | `crates/` is byte-identical at the candidate |
| cbcl-bus | candidate | `df8cf8995cb49521cc78a155a37abd3c5fdab1c6` | `spec(identity): define background busy terminals` |
| cbcl-bus | implementation baseline | `ce894a7f2debc111b610296b1a3780de6084e37e` | `apps/` is byte-identical at the candidate |
| cbcl-pairing | candidate | `56cf09920613647caa434297011e4a56eb88bf4b` | `spec(pairing): advance coordinated consumer pointer` |
| cbcl-pairing | implementation baseline | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | `spec(credential-v2): align deployed room admission` |
| did-crdt | coordinated set member | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | `fix(rendezvous): bound storage, amortise expiry, cap body` |

`git diff --stat` between each implementation baseline and its candidate is
empty for `crates/` and for `apps/`. The cbcl-pairing candidate adds four
specification files and no other content. Every code claim below therefore
holds at the declared baseline and at the candidate.

The reviewed candidate documents are
`specs/trajectory/SPEC-008/spec-008-0.4.24-explicit-background-and-http-busy-terminals.md`,
`specs/trajectory/SPEC-053/spec-053-0.16.17-background-and-webhook-busy-terminals.md`,
and
`specs/trajectory/SPEC-001/spec-001-0.4.10-coordinated-consumer-pointer.md`.

Effective authority was resolved from the whole amendment chain. The CON-978
through CON-983 retirements were applied before any current value was
compared. Headings, changelogs, review prose, and gate labels were not treated
as authority.

## Verdict and severity counts

| Severity | Count | Identifiers |
|---|---|---|
| CRITICAL | 0 | none |
| HIGH | 1 | [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-H-1]] |
| MEDIUM | 1 | [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-M-1]] |
| LOW | 6 | [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-1]], [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-2]], [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-3]], [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]], [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-5]], [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-6]] |

A PASS requires zero unresolved CRITICAL, HIGH, and MEDIUM defects. One
unresolved HIGH defect and one unresolved MEDIUM defect stand. The verdict is
therefore REJECT.

Every 0.4.23 finding closes on code-backed evidence, and no previously closed
0.4.22 finding regresses. The two blocking defects are new. One is a
consequence of the result-shape change that this amendment extends. The other
follows from the change of replacement verb from `replaces in full` to
`supplements`.

Verdict: **REJECT**.

## Findings

### N9-H-1 (HIGH, gate-blocking) — the new busy result retires the exact atom that the SPEC-055 room-start guard matches, and the fall-through default is a public cleartext channel

**Repository owner: cbcl-bus (NFR-024, TEST-111) and selfsame (NFR-926, TEST-1152).**

`cbcl-chat-roomcfg:get/1` at
`apps/cbcl_chat/src/shell/cbcl-chat-roomcfg.lfe:152-157` reads:

```lfe
(defun get (name)
  (case (mnesia:transaction (lambda () (mnesia:read 'cbcl-room name)))
    (`#(atomic (,r))
     `#(ok #(,(cbcl-room-visibility r) ,(cbcl-room-cap r) ,(cbcl-room-encrypted r))))
    (#(atomic ()) #(error unknown-room))
    (_            #(error store-unavailable))))
```

Every non-atomic transaction result becomes `#(error store-unavailable)`
today. That includes a retry-exhausted cyclic restart.

`cbcl-chat-room.lfe:200-215` consumes that owner at room-process start:

```lfe
(defun init (room)
  (case (cbcl-chat-roomcfg:get room)
    ;; SPEC-055: an UNREADABLE config must never start a room — the old
    ;; catch-all defaulted a private channel to public+cleartext when the
    ;; store was down (open join + cleartext backfill of its history). A true
    ;; miss keeps the ADR-007 public default; a store failure refuses to
    ;; start, so the join fails closed and the client retries.
    (#(error store-unavailable)
     (logger:error "cbcl-chat-room ~ts refusing to start: roomcfg unreadable" (list room))
     #(stop store-unavailable))
    (cfg-result (init-with-cfg room cfg-result))))

(defun init-with-cfg (room cfg-result)
  (let* ((cfg (case cfg-result
                (`#(ok ,vce) vce)
                (_ #(public undefined false))))   ; unconfigured rooms are public + cleartext
```

The guard matches one exact atom. Everything else reaches the second clause
and then the `#(public undefined false)` default. The comment records that
this default was a confidentiality defect, and that SPEC-055 repaired it.

**The amendment retypes the contention result and disposes of nothing.**
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#NFR-926]]
requires busy from `cbcl-chat-roomcfg:get/1` to propagate as
`#(error authority-busy)`. Surviving
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]] maps
ceremony-migration retry exhaustion to that same value. Neither clause says
what becomes of `store-unavailable`.

`NFR-925` protects only one class of existing meaning:

```text
Existing success and non-contention refusal meanings remain unchanged behind
their declared result shapes.
```

`store-unavailable` is a contention refusal, not a non-contention one. The
protection therefore does not reach it. The most direct conforming
implementation replaces the `_` arm of `get/1` with `#(error authority-busy)`.
That removes the only value `cbcl-chat-room.lfe:201` recognises.

**The consequence is a fail-open on a confidentiality control.** A private,
encrypted channel then starts as a public, cleartext channel. Join opens, and
the room replays its history in cleartext. That is the exact outcome the
SPEC-055 comment records as the repaired defect.

**The site sits on the webhook route the candidate claims to have closed.**
`cbcl-chat-rooms:ingest/3` at `:47-50` calls `public-cleartext-channel/1`
first and `get-or-start` second. `get-or-start` reaches
`cbcl-chat-rooms:start-room/2` at `:99-104`, the room supervisor, and room
`init/1`. `NFR-926` names three roomcfg consumer lines for that route, at
`cbcl-chat-rooms.lfe:54`, `:60`, and `:61`.
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#TEST-1152]]
enumerates the same three:

```text
Force busy independently at the webhook's initial get/1, claim/3, and
post-claim get/1.
```

`cbcl-chat-room.lfe:201` lies on that same HTTP request and appears in
neither the requirement nor the test. A reader of either concludes that three
sites exhaust the webhook's roomcfg surface.

**The forbidden-meaning enumeration does not reach the outcome.** `NFR-925`
forbids busy becoming an empty list, `false`, `undefined`, `exists`, absence,
or non-membership. A default channel policy is in none of those classes. This
is the same enumeration gap that
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-M-1]] found for a
silent `'ok` from a revocation applier.

**Why this is HIGH rather than CRITICAL.** `NFR-926` forbids handling busy
through a wildcard default, and the second `init/1` clause is a bare variable
pattern. A conforming implementation therefore has to touch the site.
`TEST-1152` also requires a generated inventory row naming this consumer's
busy branch and terminal boundary. Both mechanisms steer a careful
implementer to the safe repair. Neither states which terminal is correct, and
neither names the atom whose retirement creates the hazard.

**Why this is not LOW.** The affected control is channel confidentiality
rather than a delayed revocation cycle. The unsafe implementation is the
shortest one, not a perverse one. The candidate's own N8-M-1 disposition
asserts that the webhook terminal is now complete.

**Minimum sound correction.** State the disposition of
`#(error store-unavailable)` under the new contract. Either it is retained for
non-exhaustion failure and `authority-busy` is added beside it, or it is
replaced and every consumer matching it is named. Declare the busy terminal
for a supervised room-process start, and state that busy never becomes a
channel-visibility or encryption default. Add `cbcl-chat-room.lfe:201` and
`:221` to the consumer map. Add a fourth webhook busy forcing to `TEST-1152`
and `TEST-111` at room-process start, requiring a refused start and zero
fan-out.

---

### N9-M-1 (MEDIUM, gate-blocking) — three current-value clauses still name the previous round's coordinated versions, against a contract that requires each value to be singular

**Repository owner: selfsame (REQ-1003, CON-983, TEST-1153).**

Every earlier amendment in this chain replaced its pointer requirement and
its pointer test in full. `CON-982` states
`TEST-1151 replaces TEST-1149 in full` and `REQ-1002 replaces REQ-1001 in full`.

[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#CON-983]]
changes that verb:

```text
REQ-1003 supplements REQ-1002 and applies its two exact range replacements.
...
TEST-1152 supplements TEST-1150. TEST-1153 supplements TEST-1151.
```

`REQ-1003` replaces two ranges of `REQ-1002`. `TEST-1153` replaces nothing.
Three current-value statements therefore survive with the previous round's
values.

**The companion conformance target.** Effective `REQ-1002` states:

```text
Credential/v2 SHALL conform to cbcl-pairing SPEC-001 0.4.9-draft. That
candidate adopts the unchanged 0.4.8 protocol body and its earlier contracts.
```

That sentence sits between the parent-disposition paragraphs and the first
replaced range. Neither `REQ-1003` range reaches it. `CON-983` names
cbcl-pairing 0.4.10, and the candidate frontmatter names
`cbcl-pairing SPEC-001 0.3.0 through 0.4.10-draft`.

**The hub pointer.** Current `TEST-1151` states:

```text
Require the parent pointer here and its open review gate. Require cbcl-bus
[[SPEC-053-key-root-identity#DEC-012]] at 0.16.16.
```

`CON-983` states that DEC-012 points to cbcl-bus 0.16.17. The cbcl-bus parent
at `specs/SPEC-053-key-root-identity.md:688` points to 0.16.17. The two
current clauses disagree on one value.

**The companion pointer.** Current `TEST-1151` states:

```text
Require cbcl-pairing 0.4.9's `consumer-design` here.
```

`CON-983` states that cbcl-pairing 0.4.10 points its `consumer-design` to this
candidate. The cbcl-pairing frontmatter agrees with `CON-983` and disagrees
with `TEST-1151`.

**The candidate's own tests cannot be discharged.** `TEST-1151` requires
`one cbcl-pairing protocol survivor set, cbcl-bus set, Selfsame correction
set, coordinated review set, and pointer set`.
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#TEST-1153]]
requires `one current companion, one coordinated set, one pointer set, and one
open review gate`. Two current values exist for the companion target, for the
hub pointer, and for the companion pointer.

Under [[PROTO-001-usdd-agent-protocol#Compliance Evidence (self-report is inadmissible)]]
those rows resolve to `unverified` and block the phase, or they are falsely
passed. Under [[PROTO-001-usdd-agent-protocol#Error Response]] a conflicting
specification stops generation.

**A fourth instance is weaker and is recorded here rather than separately.**
`TEST-1151` still states:

```text
Scan all production Rust sources for every v1 comparison, substitution, and
ingress conversion under REQ-1002's classifier.
```

That is the exact row
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-3]] asked to align
with the classifier. `REQ-1003` aligns the requirement sentence and
`TEST-1153` adds a complete row. The narrow `TEST-1151` row survives as a
current obligation. It admits the wider scan rather than forbidding it, so it
is stale rather than contradictory.

**No unsafe implementation follows.** Cbcl-pairing 0.4.10 changes only a
pointer, and its `REQ-032` holds the protocol body at 0.4.8. Conforming to
0.4.9 or to 0.4.10 selects the same protocol language. The defect is that the
chain's singular-authority property, which this candidate tests for
explicitly, does not hold.

**Minimum sound correction.** Replace `TEST-1151` in full with `TEST-1153`. The
alternative is three exact fragment replacements. They are the `REQ-1002`
companion sentence to 0.4.10, the `TEST-1151` DEC-012 row to 0.16.17, and the
`TEST-1151` `consumer-design` row to 0.4.10. Retire the narrow `TEST-1151`
scan row in the same pass.

---

### N9-L-1 (LOW) — the collection-sink inventory states six, and two further sinks map busy to the empty list that the same requirement forbids

**Repository owner: selfsame (NFR-926) and cbcl-bus (NFR-024).**

`NFR-926` states a definite numeral:

```text
The six collection sinks include:
```

All six named lines resolve at the declared baseline, and each carries the
sink the candidate claims. That was checked line by line.

| Site | Owner | Sink at the baseline |
|---|---|---|
| `cbcl-chat-session-ws.lfe:512` | `device-grants/1` | pure admission map field |
| `cbcl-chat-session-ws.lfe:869` | `rooms-for-account/1` | `lists:foreach` list argument |
| `cbcl-chat-session-ws.lfe:965` | `list-public/0` | `cbcl-core-wire:channels` |
| `cbcl-chat-session-ws.lfe:1683` | `grants/1` | `lists:map` at `:1687` |
| `cbcl-chat-room.lfe:677` | `peer-grants/1` | `lists:map` list argument |
| `cbcl-chat-path-b-poller.lfe:17` | `accounts/0` | `lists:foreach` list argument |

`cbcl-chat-roomcfg:get-dialects/1` is a named ceremony-migration public owner
in the surviving `NFR-925` room-configuration set. It returns a collection.
Two production consumers exist, and both map every non-success result to the
empty list:

```lfe
;; cbcl-chat-room.lfe:221 and cbcl-chat-room.lfe:550
(dialects (case (cbcl-chat-roomcfg:get-dialects room)
            (`#(ok ,ds) ds)
            (_ '())))
```

`NFR-925` states that busy never becomes an empty list. These two sites
produce exactly that mapping, and neither appears in the inventory. The
collection-sink class therefore has eight known members rather than six.

A third unnamed site discards a shape-changed owner's result and reports
success. `cbcl-chat-session-ws.lfe:1898` reads:

```lfe
('true  (cbcl-chat-roomcfg:claim room vis cap handle dialects) 'ok)
```

`claim/5` at `cbcl-chat-roomcfg.lfe:130` answers `'exists` for every
non-atomic transaction result, which `NFR-925` also forbids. The candidate
names the sibling discard at `cbcl-chat-rooms.lfe:60` and omits this one.

No obligation is lost. The repository-wide production consumer scan is the
completeness authority, and `TEST-1152` drives `every newly discovered
collection sink`. The defect is a definite numeral in a normative inventory
sentence, which is the shape
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-1]] raised for a
definite singular.

**Minimum sound correction.** Drop the numeral, or add
`cbcl-chat-room.lfe:221`, `:550`, and `cbcl-chat-session-ws.lfe:1898` and
restate the count from the scan output.

---

### N9-L-2 (LOW) — ten of the eighteen incorporated witness labels contradict the classifier that shares their requirement

**Repository owner: selfsame (REQ-1003, TEST-1153).**

`REQ-1003` restates the eighteen retired-list witness lines verbatim. That
was checked against
[[spec-008-0.4.22-explicit-busy-contracts-and-complete-traces#REQ-1001]] line
by line, and the block matches byte for byte. All eighteen lines resolve at
the declared baseline.

The same requirement narrows one class name:

```text
A field-to-field copy on an ingest path is a substitution.
```

`REQ-1001` used the broader phrase `field substitution` for any assignment
into a version field. Ten incorporated labels carry the broad sense and match
no field-to-field copy on an ingest path.

| Witness | Label | Construct at the baseline | Class under REQ-1003 |
|---|---|---|---|
| `web-device/src/lib.rs:600` | profile_facts substitution | `selfsame_app_identity::PROFILE_VERSION,` inside `format!` | emitted member |
| `web-device/src/lib.rs:1346` | offer substitution | `profile_version: 1,` | version literal |
| `web-device/src/lib.rs:1360` | provider-hint substitution | `profile_version: 1,` | version literal |
| `web-device/src/lib.rs:1369` | statement substitution | `profile_version: core.profile_version,` | typed version-field assignment |
| `ceremony.rs:185` | payloadVersion substitution | `("payloadVersion", Json::int(1)),` | emitted member |
| `ceremony.rs:190` | profileVersion substitution | `("profileVersion", Json::int(self.profile_version)),` | emitted member |
| `ceremony.rs:460` | bundle payloadVersion substitution | `("payloadVersion", Json::int(1)),` | emitted member |
| `enrollment.rs:452` | recognizer substitution | `profile_version: crate::PROFILE_VERSION,` | typed version-field assignment |
| `enrollment.rs:662` | evidenceVersion substitution | `("evidenceVersion", Json::int(1)),` | emitted member |
| `enrollment.rs:666` | profileVersion substitution | `("profileVersion", Json::int(statement.profile_version)),` | emitted member |
| `provider_hint.rs:67` | profileVersion substitution | `("profileVersion", Json::int(self.profile_version)),` | emitted member |

The three genuine ingest copies are listed separately in the same
requirement, at `web-device/src/lib.rs:831`, `:847`, and `:861`. Each parses
untyped browser JSON into a typed struct field, so each matches the narrow
definition exactly.

`TEST-1153` requires a generated inventory carrying `path, line, namespace,
class, and production-or-test disposition for every match`. It also requires
`every incorporated eighteen-line witness to resolve`. A generated class column
disagrees with eleven incorporated labels, and the test states no rule for
reconciling them.

No scan coverage is lost, because the seven classes are exhaustive and the
scan is the completeness authority. The defect is an undecidable class column
for the incorporated rows.

**Minimum sound correction.** Relabel the eighteen lines under the seven
current classes. The alternative is to state that the incorporated labels are
the retired vocabulary, and that the generated class column governs.

---

### N9-L-3 (LOW) — the v1 identifier set is open, and a fifth production version namespace in the same crate has no recorded disposition

**Repository owner: selfsame (REQ-1003, TEST-1153).**

`REQ-1003` states the completeness claim over every classified site:

```text
The production scan is the completeness authority for every classified v1
site.
```

It then bounds the identifier set with an open verb:

```text
The scan includes `PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and
`payloadVersion`.
```

A scan of every production Rust source under `crates/` for those four
identifiers returns sixty-six matches. Every production match falls into a
named class, a named exclusion, or a named witness. That result is recorded
under attack surfaces below.

A fifth production version namespace exists in the same crate.
`crates/selfsame-app-identity/src/succession.rs` carries `successionVersion`
at six production sites:

| Site | Construct |
|---|---|
| `succession.rs:107` | member-name constant list |
| `succession.rs:110` | member-name constant |
| `succession.rs:195` | `!= Some(1)` comparison |
| `succession.rs:262` | `!= Some(1)` comparison |
| `succession.rs:324` | `("successionVersion", Json::int(1)),` |
| `succession.rs:338` | `("successionVersion", Json::int(1)),` |

Two of those are comparisons and two are emitted members with a version
literal. Both classes are named classes of the current classifier. The module
header binds the file to `CON-225` and `REQ-231` rather than to the credential
ceremony contracts.

`crates/selfsame-core/src/lib.rs:58` carries `WIRE_VERSION`, and
`crates/selfsame-core/src/code.rs:78` carries `CODE_VERSION`. Both belong to
the pairing and link-code namespaces.

The prior review recorded an exclusion for the pairing, link-code, and QR
namespaces in its own prose. No amendment in the chain states that exclusion.
`TEST-1153` states that `a newly discovered production site fails the test if
it is absent from the generated inventory`. That row is decidable only when
the identifier set is decidable, and `includes` leaves it open.

No unsafe credential implementation follows. The defect is an undecidable
inventory boundary inside a completeness claim.

**Minimum sound correction.** State the identifier set as closed for the
credential/v1 surface. Record `successionVersion`, `WIRE_VERSION`, and
`CODE_VERSION` as named out-of-scope namespaces. Name the governing contract
of each one.

---

### N9-L-4 (LOW) — two exact-fragment replacements duplicate the survivor text that follows them, and the cbcl-bus twin resolves to different prose

**Repository owner: selfsame (REQ-1003, NFR-926).**

Each replacement was resolved mechanically against its effective predecessor.
Every opening and closing phrase matches exactly, and every phrase is unique
inside its target. The declared counts agree with the actual fragments: two
ranges for `REQ-1003` and four for `NFR-926`.

Two replacement texts restate a paragraph that survives immediately after
their range.

`REQ-1003`'s second range ends at
`` `crates/selfsame-app-identity/src/provider_hint.rs:96`. ``. The paragraph
after that range in `REQ-1002` reads:

```text
Credential/v2 reaches no classified v1 site. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.
```

The replacement text closes with the identical paragraph. The resolved
requirement therefore carries it twice in succession.

`NFR-926`'s first range ends at `Neither case deletes any row.` The paragraph
after that range in `NFR-925` reads:

```text
Only reciprocal agreement continues. That branch takes the exact withdrawal
write lock and performs both live-row deletes under the held locks.
```

The replacement closes with this sentence:

```text
Only reciprocal agreement continues to the withdrawal lock and both live
deletes.
```

The resolved requirement states the same obligation twice, in two shapes.

The cbcl-bus twin does not. `NFR-024`'s first range ends at
`both live deletes.`, so the reciprocal sentence falls inside the replaced
range and is consumed. One coordinated obligation therefore resolves to
different prose in the two repositories, which is the shape
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-5]] raised.

Neither duplicate contradicts its twin, so no obligation is lost and no
implementation diverges. The defect is redundancy against the
[[PROTO-001-usdd-agent-protocol#Quality Attribute Checklist]] non-redundancy
attribute, plus a cross-repository prose divergence.

**Minimum sound correction.** Extend the `REQ-1003` second range to consume the
`Credential/v2` paragraph, and extend the `NFR-926` first range to consume the
reciprocal paragraph, matching the cbcl-bus boundary.

---

### N9-L-5 (LOW) — the retired current-value range carries away the replacement authority for five tests, and no clause restates it

**Repository owner: selfsame (CON-983).**

`CON-983` retires `CON-982`'s current-value range beginning with
`The exact cbcl-bus set` and ending with the did-crdt commit. Both boundary
phrases are unique inside `CON-982`, and the range resolves cleanly.

The retired text contains one sentence that the replacement does not restate:

```text
Those five correction tests collectively replace TEST-1129 through TEST-1133.
```

Effective `REQ-1002` states that retired text supplies no survivor, test,
coordinated set, pointer, or replacement authority. The replacement
relationship for `TEST-1129` through `TEST-1133` therefore has no current
source.

The current set stays bounded, because the replacement enumerates it. That
enumeration carries `TEST-1134`, `TEST-1135`, `TEST-1139`, and `TEST-1150`
through `TEST-1153`. It also carries every effective test through `TEST-1108`.
No retired number becomes current again. The audit trail from each retired number to its successor is
what the retirement removes.

This is the shape of
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N7-L-2]] and
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N7-L-3]], where an
exact retirement carried away a disposition that the replacement then
restored.

**Minimum sound correction.** Restate the collective-replacement sentence in
the `CON-983` replacement text, naming `TEST-1150` through `TEST-1153` as the
successors.

---

### N9-L-6 (LOW) — both the Selfsame and cbcl-bus candidates fail the strict controlled-language check

**Repository owner: selfsame (TEST-1152) and cbcl-bus (TEST-111).**

`P001-R082` in [[PROTO-001-usdd-agent-protocol#Registry]] is an `auto` row.
Its deciding command is `tools/usdd-lint.sh --strict`. The command ran over
all three candidates and over their two predecessors.

```text
$ usdd-lint.sh --type descriptive --strict --show <selfsame 0.4.24>
spec-008-0.4.24-explicit-background-and-http-busy-terminals.md:336: lowercase_modal
    Every existing TEST-1150 regression remains required, including its four ...
  ERROR   lowercase_modal        1
          TOTAL                  1 errors, 0 warnings

$ usdd-lint.sh --type descriptive --strict --show <cbcl-bus 0.16.17>
spec-053-0.16.17-background-and-webhook-busy-terminals.md:245: lowercase_modal
    Every existing TEST-109 regression remains required, including its four ...
  ERROR   lowercase_modal        1
          TOTAL                  1 errors, 0 warnings

$ usdd-lint.sh --type descriptive --strict <cbcl-pairing 0.4.10>
          TOTAL                  0 errors, 0 warnings
$ usdd-lint.sh --type descriptive --strict <selfsame 0.4.23>
          TOTAL                  2 errors, 0 warnings
$ usdd-lint.sh --type descriptive --strict <selfsame 0.4.22>
          TOTAL                  0 errors, 0 warnings
```

The lowercase word `required` carries an obligation in both sentences, so the
BCP 14 keyword form applies. The 0.4.23 predecessor carries two instances of
the same class, at its lines `455` and `539`, and no review recorded them.

The check is mechanical, and its predicate is an exact word match. Under
[[PROTO-001-usdd-agent-protocol#Compliance Evidence (self-report is inadmissible)]]
an `auto` gate stays unticked while its command reports an error.

**Minimum sound correction.** Capitalise the keyword in both candidates, and
add the strict lint command to the Tier-1 candidate checklist so the gate has
recorded evidence.

---

## Disposition of every 0.4.23 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N8-M-1** — two production consumer classes of shape-changed owners have no declared busy boundary | **CLOSED, with a residual recorded as [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-H-1]]** | `NFR-926` declares both missing terminals. For a periodic tick, busy from `accounts/0` emits `OBS-905` phase `account-scan`, reports no success, performs no resolver call, and leaves the supervised poller alive. Busy from `revoke-observed/2` emits phase `revocation-apply`, reports no applied union, performs no eviction, and defers the account to the next tick. The poller MAY continue other accounts and MUST NOT report the discarded union as applied. `cbcl-chat-path-b-poller.lfe:17` and `:32` resolve exactly: line 17 is `(lists:foreach (lambda (account) …) (cbcl-chat-account-grants:accounts))` and line 32 is `(case (cbcl-chat-account-grants:revoke-observed account ids) …)`. The resolver call sits inside the per-account lambda, so an empty account list performs none. `interval/0` at `:13` defaults to 60,000 milliseconds, so the next tick re-derives the union. `cbcl-chat-rooms.lfe:54`, `:60`, and `:61` resolve to the initial `get/1`, the discarded `claim/3`, and the post-claim `get/1`. `cbcl-webhook-handler.lfe:76` is the consuming HTTP edge, in the second OTP application. `NFR-926` maps busy to HTTP 503 with `authority-busy` and `no-store`, releases the reserved blob slot, and forbids room start, ingest, persistence, and fan-out. The `try … after` form at `:75-85` already releases the token on every branch. `ingest/3` at `:47-50` calls `public-cleartext-channel/1` before `get-or-start`, so a refused channel starts no room. The residual is the fourth roomcfg consumer on the same route. |
| **N8-L-1** — the consumer map names one collection consumer, and six exist | **CLOSED, with a residual recorded as [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-1]]** | `NFR-926` replaces the definite singular with the six-site enumeration and makes the repository-wide production consumer scan the completeness authority. All six lines resolve, and each carries the claimed sink. `TEST-1152` drives all six and `every newly discovered collection sink`. The residual is the numeral and two further known sinks. |
| **N8-L-2** — the baseline witness set is defined by reference into a requirement the same contract replaces in full | **CLOSED** | `REQ-1003` restates all eighteen lines inside the requirement and states that `This exact incorporation is independent of REQ-1001's retirement.` The block matches `REQ-1001` byte for byte, and all eighteen lines resolve at the declared baseline. No authority scan now has to decide whether retired text remains readable. |
| **N8-L-3** — the classifier names five site classes and the scan obligation names three | **CLOSED, with a residual recorded as [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-M-1]]** | `REQ-1003` replaces both narrow sentences with one exhaustive seven-class classifier: version-constant definition, version literal, comparison, emitted member, typed version-field assignment, ingest substitution, and untyped ingress conversion. `TEST-1153` scans under that same classifier and requires `both PROFILE_VERSION definitions to classify as production constant evidence`. `crates/selfsame-app-identity/src/lib.rs:109` and `crates/selfsame-app-identity/src/profile.rs:60` both read `pub const PROFILE_VERSION: i64 = 1;`, and neither sits under a `#[cfg(test)]` boundary. The residual is the surviving narrow `TEST-1151` scan row. |
| **N8-L-4** — the ingress-conversion witness set is a closed copula that omits a third production site | **CLOSED** | `REQ-1003` writes `Direct ingress-conversion evidence includes` and adds `crates/selfsame-app-identity/src/profile.rs:384`. That line reads `let version = integer(value, "profileVersion")?;`, where `value` is untyped `Json` and `version` is a typed `i64`. `TEST-1153` requires `profile.rs:384` to classify as an ingress conversion. The copula is now open, and the closing sentence states that named sites `cannot close or limit the repository-wide scan`. |
| **N8-L-5** — the authority owner list attaches the tagged-success contract to owners that are neither collections nor predicates | **CLOSED** | `NFR-926` replaces the two introductory sentences with `The authority public owners are:` and `The ceremony-migration public owners are:`, which is the cbcl-bus `NFR-023` wording. It adds `Only collection and predicate owners adopt tagged success. Every other public owner retains its declared success shape and adds the lane's typed busy shape.` The nearest-antecedent ambiguity is gone, and both bullet lists survive intact, including `cbcl-chat-successor:accept/3` and the two ceremony-migration rows moved by N7-L-1. |
| **N8-L-6** — the withdrawal refusal branch names no terminal for its new account-side disagreement case | **CLOSED** | `NFR-926` names both terminals: a present account row whose home DID differs returns `#(error wrong-generation)`, and one whose home DID agrees while its stored alias is not `requestedAlias` returns `#(error inconsistent-binding)`. The reciprocal alias case keeps `#(error inconsistent-binding)`. `Every refusal deletes zero alias, account, and withdrawal rows.` The deployed `tombstone/2` at `cbcl-chat-webfinger-store.lfe:206-230` answers `wrong-generation` at `:213` for a stale DID and `inconsistent-binding` at `:220` through `alias-agrees?`, and reaches its two deletes at `:227-228` only on the `'true` arm. `TEST-1152` drives both account-side cases and requires zero deletion from all three WebFinger tables, which are `cbcl-webfinger-alias`, `cbcl-webfinger-account`, and `cbcl-webfinger-withdrawn`. The cbcl-bus twin carries the identical repair in `NFR-024` and `TEST-111`. |

Seven findings were raised and seven close on code-backed evidence. Three
close with residual points that are recorded above as new defects.

## Regression disposition of every 0.4.22 finding

Each closed N7 finding was re-tested against the resolved text, after applying
the four `NFR-926` fragment replacements and the two `REQ-1003` range
replacements.

| ID | Regression verdict | Evidence |
|---|---|---|
| **N7-H-1** — the current test demands `authority-busy` from `lookup/1` | **NO REGRESSION** | The `lookup/1` exemption paragraph sits after the fourth `NFR-926` range boundary, so it survives untouched. `cbcl-chat-webfinger-http.lfe:36` still maps `'unavailable` to `503`, and `base-headers/0` at `:40-42` still carries `cache-control: no-store`. The `(_ (empty 404 …))` wildcard at `:37` never receives a busy value, because `lookup/1` is exempt from that result form. |
| **N7-M-1** — no consumer obligation for the success value | **NO REGRESSION** | The fourth `NFR-926` range replaces the totality sentence with a stronger one: every consumer handles the owner's declared success shape and `#(error authority-busy)`, unwraps `#(ok Value)` before use, and never handles busy through a wildcard default. The prohibition list grows from four sink classes to five by adding the wire encoder. |
| **N7-M-2** — two of three exhaustion classes are unreachable | **NO REGRESSION** | The third `NFR-921` range replacement is outside every 0.4.24 fragment. `{aborted, nomore}` remains the only wrapper terminal, and prohibited nesting remains a static inventory failure. |
| **N7-M-3** — one authority-lane transaction is missing from the owner list and the nesting repair | **NO REGRESSION** | The second `NFR-926` fragment replaces only the introductory sentence. The bullet list survives, and `cbcl-chat-successor: accept/3` is still its last row. The transaction-only device-grant read and the two forbidden catches at `cbcl-chat-successor.lfe:109` and `:134` sit outside every replaced range. |
| **N7-M-4** — the eighteen-site v1 inventory omits one production comparison | **NO REGRESSION** | `REQ-1003` keeps `crates/selfsame-app-identity/src/enrollment.rs:592`, the three ingest copies, and both earlier ingress conversions, and adds a third. All twenty-seven named sites resolve at the declared baseline. The completeness sentence `They cannot close or limit the repository-wide scan.` replaces the sentence the first range removed, so no scan authority is lost. |
| **N7-M-5** — the replacement test drops four inline cases | **NO REGRESSION** | `TEST-1150` survives, because `TEST-1152` supplements it. `TEST-1152` states that `Every existing TEST-1150 regression remains required, including its four inline REQ-995 groups and WebFinger read case.` |
| **N7-L-1** — two enumerated authority owners are ceremony-migration rows | **NO REGRESSION** | `account-binding/1` and `bind-account/2` remain the first ceremony-migration bullet. The third `NFR-926` fragment replaces only the introductory sentence. |
| **N7-L-2** — replacing REQ-954 drops the parent REQ-903 and REQ-904 dispositions | **NO REGRESSION** | Both parent paragraphs sit before the first `REQ-1003` range and survive. `specs/SPEC-008-production-pairing-claimant.md:163` and `:173` still carry the two parent requirements. |
| **N7-L-3** — the replacement pointer paragraph drops the open-gate half | **NO REGRESSION** | `CON-983`'s pointer replacement restates `The Selfsame parent [[SPEC-008-production-pairing-claimant]] points here and names this candidate's open review gate.` The parent frontmatter now names `CON-983`, and its Orientation blockquote states that the coordinated Tier-1 review gate is open. |
| **N7-L-4** — the name-squatting sentence is unscoped | **NO REGRESSION** | The scoped name-squatting sentence and the `cbcl-provenance` residual sit after the fourth range boundary and survive. |
| **N7-L-5** — the recorded trajectory is a summary line | **NO REGRESSION** | All three candidates enumerate a four-element trajectory. The Selfsame field names the initial draft, the failing-test artefact, the localised correction table, and this candidate. |
| **N7-L-6** — the schema-migration lane names one site and a second exists | **NO REGRESSION** | The `legacy-rows/0` non-overlap sentence and the `migrate-since/0` record sit outside every replaced range. `cbcl-chat-app.lfe:87-90` still consumes `migrate-since/0` and refuses startup on its error. |

Twelve findings were re-tested and none regresses. No fragment replacement in
this candidate reaches the text that closed any of them.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-983` names nothing inside `REQ-981`, which remains effective at `spec-008-0.4.11-standalone-tofu-first-contact.md:51`. The live profile stays byte-for-byte unchanged at `profileVersion = 1`. The cbcl-bus GATE-04 box requiring the exact v2 policy pair to prompt is one of the 41 boxes that survive byte-for-byte. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched at the declared baseline, which was verified by an empty `crates/` diff. `crates/selfsame-pairing/src/live.rs:479-488` still commits one explicit local decision only from `ClaimantPhase::Endpoint`, and its own doc comment binds that call to `LiveEffect::DisplayIntent`. Cbcl-pairing 0.4.10 adds one file and changes no protocol language, and its `REQ-032` holds the body at 0.4.8. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-983` names nothing inside `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202`. Every busy path this amendment adds creates nothing. The webhook 503 performs no room start, ingest, persistence, or fan-out, and releases its reservation. The poller performs no eviction on busy. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-983` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:901-912` still declares the five-argument allocation call, so the stable scope and the one pending ceremony still commit atomically or the browser displays no carrier. |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-027` states that every protocol survivor recorded through `CON-026` remains effective, and `CON-026` preserved the no-relay-field property of `CON-024`. The 0.4.10 diff adds one file and touches no protocol asset. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-330` states that mismatch, protocol violation, cancellation, relay closure, and expiry produce no accepted credential and no identity side effect before approved payload delivery. Every negative path this amendment adds creates nothing. The webhook 503 body carries a status slug and no binding. [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-H-1]] is a hub channel-policy gap rather than an endpoint failure, so `REQ-812` is not the clause it touches. |

`CON-983` restates every F-A through F-E correction and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] as hard stops.

## Repository assessments

### selfsame SPEC-008 0.4.24-draft (`7cf3cbd`)

The amendment applies exactly the replacements `CON-983` declares. `REQ-1003`
supplements `REQ-1002` and applies two exact range replacements. `NFR-926`
supplements `NFR-925` and applies four exact fragment replacements. `OBS-905`,
`ADR-961`, `TEST-1152`, and `TEST-1153` supplement their current sets.

Each range boundary was resolved against the predecessor text rather than
against a later restatement. Every opening and closing phrase matches exactly,
and every phrase is unique inside its target. The declared counts, two and
four, agree with the actual fragments.

Two replacement texts restate the survivor paragraph that follows their range,
which is [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]]. No
replacement removes a survivor obligation.

Every new identifier is unique and sequential. `REQ-1003`, `NFR-926`,
`OBS-905`, `CON-983`, `ADR-961`, `TEST-1152`, and `TEST-1153` each appear in
one file. The observation series runs `OBS-901` through `OBS-905` with no gap
and no reuse.

The Selfsame test set resolves to one enumeration. It carries every effective
test through `TEST-1108`. It also carries `TEST-1134`, `TEST-1135`,
`TEST-1139`, and `TEST-1150` through `TEST-1153`.

Four defects are recorded against this repository alone.
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-M-1]] concerns three
stale current-value clauses.
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-2]] and
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-3]] concern the v1
classifier. [[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-5]]
concerns a retired replacement authority.

### cbcl-bus SPEC-053 0.16.17-draft (`df8cf89`)

`REQ-079` supplements `REQ-078` and retires its current-value range. `NFR-024`
supplements `NFR-023` and replaces two exact ranges. Both range boundaries
resolve uniquely inside `NFR-023`.

`NFR-024`'s first range ends at `both live deletes.`, so it consumes the
reciprocal sentence that the Selfsame twin leaves standing. The second range
ends at `refusal wildcard.` and carries the same six collection sinks, boolean
contexts, poller terminals, and webhook terminal as `NFR-926`.

GATE-04 arithmetic is exact. The effective gate in
`spec-053-0.16.15-explicit-busy-contracts-and-complete-traces.md` carries 42
boxes between its heading and the following section, counted mechanically, and
zero closed boxes. Its first box begins
`- [ ] coordinated Selfsame 0.4.22` at 0.16.15. `REQ-078` replaced that box
with one naming 0.4.23, so the effective first box now begins
`- [ ] coordinated Selfsame 0.4.23`. `REQ-079` retires that exact box and
replaces it with one naming Selfsame 0.4.24, cbcl-pairing 0.4.10, and this
amendment. One box replaces one box, so 42 boxes remain, 41 survive
byte-for-byte, and no box closes.

The hub test set agrees across the two repositories. `REQ-079` and Selfsame
`CON-983` both state `TEST-043` through `TEST-065`, `TEST-068`, `TEST-070`,
`TEST-093`, `TEST-094`, `TEST-098`, and `TEST-109` through `TEST-112`.

The hub parent points here. `specs/SPEC-053-key-root-identity.md:15` and
`:688` name `REQ-079` at 0.16.17, and DEC-012's production deferral remains in
force.

[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-H-1]] and
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-1]] are shared with
selfsame, because both specify the same hub code.
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-6]] is also shared.

### cbcl-pairing SPEC-001 0.4.10-draft (`56cf099`)

The candidate adds one file and changes nothing else, which was verified by
diffing `56cf099` against its baseline. `REQ-032` states that the protocol body
remains `REQ-030`, `NFR-022`, and `ADR-023` from 0.4.8. `CON-027` retires only
the `consumer-design` pointer and states that every `CON-026` survivor remains
effective.

The frontmatter `consumer-design` value names Selfsame SPEC-008 0.4.24-draft.
Selfsame `CON-983` and cbcl-bus `REQ-079` both assert that pointer. The three
records agree.

The candidate passes the strict controlled-language check with zero errors and
zero warnings.

No defect is recorded against this repository. Its pointer is one of the three
values that the Selfsame `TEST-1151` rows contradict, and that defect belongs
to selfsame.

### did-crdt (`1f409a4`)

The commit exists and is `fix(rendezvous): bound storage, amortise expiry, cap
body before buffering`. It is a coordinated set member rather than a candidate.
Its 4,096-octet rendezvous cap remains deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. That non-conformance is a
recorded open gate rather than a defect.

## Attack surfaces examined and found sound

**The periodic Path-B poller, traced through both result forms.**
`cbcl-chat-account-grants:accounts/0` at `:280-285` maps every non-atomic
transaction result to `'()` today, which `NFR-925` forbids. Under the new
contract the tick receives `#(error authority-busy)`, emits `OBS-905` phase
`account-scan`, and iterates no account. The resolver call sits inside the
per-account lambda at `:17`, so none runs. `revoke-observed/2` at `:290-295`
answers `#(ok Keys)` on commit and `#(error revoke-failed)` otherwise. Under
the new contract busy emits phase `revocation-apply`, and the eviction
`lists:foreach` at `:33` never runs. The tick returns `'ok` from
`handle_info`, so the supervised `gen_server` stays alive. `interval/0`
defaults to 60,000 milliseconds and re-arms on every tick, so the next scan
re-derives the union from a fresh resolver read. `NFR-926` permits continuing
other accounts and forbids reporting the discarded union as applied.

**The observation's privacy bound.** `OBS-905` records only the event name and
the phase. It excludes the account DID, the credential identifier, the
resolver payload, the profile digest, and the grant key. A counter aggregating
by phase carries no per-account value. The signal states only that the current
tick reported no success for that phase.

**The webhook route, traced from both roomcfg owners to the HTTP terminal.**
`cbcl-chat-rooms:public-cleartext-channel/1` at `:53-66` consumes
`roomcfg:get/1` at `:54`, calls `claim/3` at `:60` and discards its result,
and re-reads `get/1` at `:61`. `ingest/3` at `:47-50` returns the error
unchanged, so a busy value reaches `cbcl-webhook-handler.lfe:76` intact.
`do-fan/6` reserves a blob slot at `:72` before the room mailbox. It releases
that slot in the `after` clause at `:85`. The reservation therefore releases on
every branch, including a busy 503. `ingest/3` calls `public-cleartext-channel/1` before
`get-or-start`, so a refused channel starts no room and fans nothing. The
handler currently maps `#(error #(persist-failed _))` to 503 at `:82-83` and
every other `#(error _)` to 409 at `:84`. `NFR-926` requires a busy clause
ahead of that wildcard, so 503 and 409 separate exactly. HTTP 409 then stays
the terminal only for a committed non-public-channel decision.

**The cross-application HTTP sink.** The consuming edge lives in
`apps/cbcl_router`, and the owner lives in `apps/cbcl_chat`. The `respond/4`
helper at `:95-100` sets only `content-type`. The busy 503 therefore carries a
new `cache-control: no-store` header rather than an observed one. That is
recorded as an open assumption below.

**The production consumer inventory, derived independently.** A cross-module
scan of production LFE under `apps/` for every shape-changed owner returns 61
call sites across ten consumer modules. Every named site in `NFR-926` appears
in that scan and resolves to the claimed construct. The scan also returns the
sites recorded in
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-H-1]] and
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-1]].

**Boolean-context smuggling.** The three named boolean contexts are complete.
`cbcl-chat-room.lfe:263-270` carries two `andalso` chains over
`account-roommember:re-admit?/3` and `roommember:re-admit?/3`.
`cbcl-chat-pairgrant.lfe:78` wraps `roommember:member-grant?/2` in `not` inside
a `cond`. `cbcl-chat-session-ws.lfe:1050` wraps the same owner in `if`. A
scan for `andalso`, `orelse`, `if`, `not`, `when`, and `cond` around every
in-lane predicate owner returns no fourth production site. The closed copula
`The boolean contexts are` is therefore accurate.

**Owner fall-through predicates.** `cbcl-chat-roommember:member-grant?/2` at
`:222-225` falls through to the account-side owner, and
`cbcl-chat-roommember:enrolled?/2` at `:233-236` answers `'false` for every
non-success from `cbcl-chat-members:pubkey/1`. `NFR-925` forbids busy becoming
`false` or non-membership, so both sites carry a named prohibition. Both
appear in the independent consumer scan.

**Withdrawal, traced through all three disagreement cases.** The deployed
`tombstone/2` at `cbcl-chat-webfinger-store.lfe:206-230` reads the account row
under a write lock. It derives `uri` from that row. It answers
`#(error wrong-generation)` at `:213` when the named DID differs. It answers
`#(error inconsistent-binding)` at `:220` when `alias-agrees?` at `:232-237`
finds an absent or disagreeing alias row. Both deletes at `:227-228` sit on the
`'true` arm alone. Under `NFR-926` the alias lock moves to `requestedAlias`,
and the alias-mismatch case gains its own terminal. The terminal order is
deterministic, because the home-DID test is stated first and unconditionally.
An account row whose DID agrees always carries the derived alias.
`provision/3` at `:124-134` writes the alias as the exact function of the home
DID. The second case therefore needs a corrupt store.

**WebFinger lock order under the new withdrawal design.** `create-or-return/3`
at `:139-149` takes write locks in the order withdrawn, alias, account.
`NFR-926` orders withdrawal as alias, account, withdrawn. The two orders
oppose on the withdrawn and alias pair, so provision and withdrawal can form a
Mnesia lock cycle. That is a contention case rather than a correctness case.
The shared wrapper recognises the exhausted cyclic restart as
`{aborted, nomore}` and maps it to the declared busy terminal, and no partial
effect survives. `TEST-1150` requires the complete WebFinger contention graph,
so the cycle is a generated test case rather than an unmodelled state.

**Version freeze across every classifier class.** Every production Rust source
under `crates/` was scanned for the four named identifiers, returning 66
matches. Every production match resolves to a named witness, a named class, or
a named exclusion. The exclusions are `#[cfg(test)]` code, type declarations,
member-name constants, format templates, and comments. The `#[cfg(test)]`
boundaries were verified individually: `selfsame-web-device/src/lib.rs:1521`,
`selfsame-beam/src/enrollment.rs:199`, and
`selfsame-app-identity-net/src/profile.rs:221` each open a trailing `mod tests`
that runs to end of file. No production Rust file outside `src/` carries a
match. All twenty-seven named sites resolve. That set carries the eighteen
incorporated witnesses, the additional comparison, and the three ingest copies.
It also carries the three ingress conversions and the two production constant
definitions.

**Application-start and schema-migration terminals.**
`cbcl-chat-app.lfe:96` calls `roomcfg:claim/3` for the default channel and
discards its result, which `NFR-925` covers with the startup terminal.
`roommember-schema-ready/0` at `:87-90` already refuses startup on a migration
error, and the listener opens only after migration succeeds.

**Cbcl-pairing protocol immutability.** The candidate diff adds four
specification files and changes no protocol asset. `REQ-032` and `TEST-061`
both fail any protocol-body change relative to 0.4.8.

## Residual assumptions and open gates, separated from defects

These are not defects. Each is a recorded assumption or a gate that stays open
by design.

1. **The coordinated Tier-1 gate is open.** All three candidates carry
   `review-gate: not-approved`. `CON-983` states that a fresh PASS authorises
   only the Elephant SPL and test-first plan.
2. **GATE-04 stays 42 boxes open.** This amendment closes none of them. The
   cbcl-bus text states that directly, and the mechanical count agrees.
3. **The PROTO-002 rendezvous gate stays open.** The reviewed did-crdt
   baseline caps at 4,096 octets and is deliberately non-conforming to
   [[PROTO-002-selfsame-rendezvous-v1#REQ-305]].
4. **The webhook `no-store` header is new behaviour.** `respond/4` at
   `cbcl-webhook-handler.lfe:95-100` sets only `content-type` today. The
   `cache-control: no-store` header on the busy 503 is a specified obligation
   rather than an observed one. The pre-existing `overloaded` and
   `persist-failed` 503 responses keep no cache directive, and no clause asks
   them to.
5. **The 1013 close code and the browser generation are new behaviour.**
   Neither `1013` nor `try-again-later` appears in the cbcl-bus source or web
   assets today.
6. **`OBS-905` and `OBS-013` are two identifiers for one signal.** The event
   name `path-b-revocation-authority-busy` and the two phase values agree
   across the repositories. Each repository allocates its own observation
   number, which is the existing convention in this chain.
7. **The `cbcl-provenance` wildcard is an accepted out-of-lane residual.**
   `NFR-925` records it and disclaims any repair of non-retryable behaviour.
8. **`cbcl-chat-successor:accept/3` has no production caller.** A scan of
   non-test callers returns none. The row is still production code, and the
   amendment covers it.
9. **Several in-lane owners have no production consumer.**
   `store-verified-device-grant/6`, `confirmed-migrate-path-a-handle/3`,
   `revoke-device-grant/2`, `bind-account/2`, `provision/3`, `withdraw/2`, and
   `reinstate/2` are reachable only from tests or operator paths today.
10. **The cbcl-pairing parent carries no amendment pointer.**
    `specs/SPEC-001-reusable-blind-pairing.md` references no trajectory
    amendment, and no candidate requires it to.
11. **The cbcl-bus and cbcl-pairing trajectory fields use plain text for their
    cross-repository steps.** A wikilink into another vault does not resolve,
    so the plain form is the practical one. The Selfsame record uses
    wikilinks throughout.
12. **The consumer inventory now has a named derivation mechanism.** The prior
    review recorded its absence. `NFR-926` and `NFR-024` both make a
    repository-wide production consumer scan the completeness authority, which
    closes that residual.

## What this verdict authorizes and does not authorize

This verdict is REJECT. It authorizes nothing.

It does not authorize implementation, an Elephant SPL theory, a test-first
plan, code, production allocation, a release, or a deployment. It does not
close any GATE-04 box. It does not alter DEC-012's production deferral. It
does not change the PROTO-002 Tier-1 gate.

Under `CON-983`, a later PASS on a corrected candidate authorizes only the
Elephant SPL and the test-first plan. Even that PASS authorizes no code until
the primary agent independently verifies and merges the review evidence.

This report is one reviewer's evidence. It is not a merge decision and not a
gate closure.

## Corrections and owners, collected

| Finding | Owner repositories | Minimum sound correction |
|---|---|---|
| N9-H-1 | cbcl-bus, selfsame | State the disposition of `#(error store-unavailable)` under the new busy contract. Declare the busy terminal for a supervised room-process start, and state that busy never becomes a channel-visibility or encryption default. Add `cbcl-chat-room.lfe:201` and `:221` to the consumer map. Add a fourth webhook busy forcing at room-process start to `TEST-1152` and `TEST-111`, requiring a refused start and zero fan-out. |
| N9-M-1 | selfsame | Replace `TEST-1151` in full, or add three exact fragment replacements: the `REQ-1002` companion sentence to cbcl-pairing 0.4.10, the `TEST-1151` DEC-012 row to cbcl-bus 0.16.17, and the `TEST-1151` `consumer-design` row to 0.4.10. Retire the narrow `TEST-1151` scan row in the same pass. |
| N9-L-1 | selfsame, cbcl-bus | Drop the numeral from the collection-sink sentence, or add `cbcl-chat-room.lfe:221`, `cbcl-chat-room.lfe:550`, and `cbcl-chat-session-ws.lfe:1898` and restate the count from the scan output. |
| N9-L-2 | selfsame | Relabel the eighteen incorporated lines under the seven current classes, or state that the incorporated labels are the retired vocabulary and that the generated class column governs. |
| N9-L-3 | selfsame | State the four-identifier set as closed for the credential/v1 surface, and record `successionVersion`, `WIRE_VERSION`, and `CODE_VERSION` as named out-of-scope namespaces with their governing contracts. |
| N9-L-4 | selfsame | Extend the `REQ-1003` second range to consume the `Credential/v2` paragraph, and extend the `NFR-926` first range to consume the reciprocal paragraph, matching the cbcl-bus boundary. |
| N9-L-5 | selfsame | Restate the collective-replacement sentence inside the `CON-983` replacement text, naming `TEST-1150` through `TEST-1153` as the successors of `TEST-1129` through `TEST-1133`. |
| N9-L-6 | selfsame, cbcl-bus | Capitalise the BCP 14 keyword in both candidates, and add `tools/usdd-lint.sh --strict` to the Tier-1 candidate checklist so the `P001-R082` gate has recorded evidence. |

## Report self-audit

| Property | Value | Source |
|---|---|---|
| Findings raised | 8 | this report |
| CRITICAL findings | 0 | severity table |
| HIGH findings | 1 | severity table |
| MEDIUM findings | 1 | severity table |
| LOW findings | 6 | severity table |
| 0.4.23 findings dispositioned | 7 | disposition table |
| 0.4.22 findings regression-tested | 12 | regression table |
| Architecture hard stops dispositioned | 6 | hard-stop table |
| Production consumer call sites enumerated | 61 | cross-module scan of production LFE under `apps/` |
| Named v1 witness sites verified | 27 | line-by-line read at the declared baseline |
| Production Rust version-identifier matches classified | 66 | scan of every `.rs` file under `crates/` |
| cbcl-bus GATE-04 boxes counted | 42 | `grep -c '^- \[ \]'` over the effective gate section |
| cbcl-bus GATE-04 boxes closed | 0 | `grep -cE '^- \[[xX]\]'` over the same section |

The severity table, the corrections table, and this audit agree on one HIGH,
one MEDIUM, and six LOW findings.

The counts this report states about itself are derived rather than recalled.
Both tool results are pasted below.

```text
$ tools/usdd-count.sh <this report>
artefacts   REQ=14 NFR=6 CON=9 TEST=15 ADR=2 OBS=3 SPEC=5 PROTO=2
gate rows   0  (pass=0 fail=0 unverified=0)
registry    0  (auto=0 auto*=0 review=0 advisory=0 duplicate_ids=0 missing_ids=0)
wikilinks   47  (anchored=44)

$ tools/usdd-lint.sh --type descriptive --strict <this report>
          longest_sentence       25 (limit 25, mode descriptive)
          scanned                417 sentences, 4687 words
          TOTAL                  0 errors, 0 warnings

$ grep -c '^### N9-' <this report>              # finding headings
8
$ grep -c '^| \*\*N8-' <0.4.23 disposition table>
7
$ grep -c '^| \*\*N7-' <0.4.22 regression table>
12
$ grep -c '^| N9-' <corrections table>
8
```

This report carries no gate rows and no registry rows, so both totals are
zero. It is a review record rather than a specification.

## Changelog

- **2026-08-23 — coordinated Tier-1 adversarial review of SPEC-008 0.4.24,
  cbcl-pairing 0.4.10, and cbcl-bus 0.16.17 — REJECT.** All seven 0.4.23
  findings close and no 0.4.22 finding regresses. One new HIGH defect and one
  new MEDIUM defect stand, with six new LOW defects.
