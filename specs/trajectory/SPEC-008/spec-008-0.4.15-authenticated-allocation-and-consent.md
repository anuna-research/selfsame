---
id: SPEC-008-amendment-0.4.15
title: SPEC-008 0.4.15-draft — Authenticated allocation and exact consent provenance
status: draft
version: 0.4.15-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.14-deployed-admission-and-local-unlink]] only where [[spec-008-0.4.15-authenticated-allocation-and-consent#CON-974]] says so"
adopts: "All effective 0.4.4 through 0.4.14 clauses except the exact replacements in CON-974"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.7-draft; cbcl-bus SPEC-053 0.16.8-draft"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.14-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.15-draft — Authenticated allocation and exact consent provenance

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.14 review found four HIGH, five
> MEDIUM, and six LOW defects. This amendment closes each finding against the
> shipped browser and hub code. It authorizes no implementation, allocation,
> release, or deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No custody, derivation, signing, publication, alias, or grant
effect precedes the applicable explicit consent.

## Requirements

### REQ-992 — Admission, allocation, and displayed rooms have exact owners

The tagged transition shapes and member sets in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] remain exact.
Credential/v2 uses these ASCII grammars:

```text
LegacyHandle = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )

CbclSymbolChar = ALPHA / DIGIT / "_" / "-" / "." / "/" / "!" /
                 "?" / "+" / "*" / "<" / ">" / "=" / "@"
CanonicalRoomText = "@" 1*128( CbclSymbolChar )
```

`CanonicalRoomText` uses the complete deployed recipient-symbol alphabet at
the pinned cbcl-rs revision
`febc6691e6dd2d5f7116b1a4d84c984b64717564`. The 129-octet limit is a
credential/v2 admission cap, not a deployed CBCL-symbol maximum. The serializer
SHALL NOT escape `/`. Therefore UTF-8 content and RFC-8785 string-content units
remain equal, including a maximum-length room containing `/`.

The hub is the sole evaluator of `MigrationPlanTooLarge`,
`MigrationRoomUnrepresentable`, and `MigrationHandleUnrepresentable`. It
evaluates all three inside the same level-1 allocation transaction that locks
and snapshots the enrolled key, complete durable room set, and pending
ceremony. A room is `MigrationRoomUnrepresentable` whenever its raw value fails
`CanonicalRoomText`, including bare `@`, a non-symbol value, or any other
grammar failure. Length, count, or aggregate overflow is
`MigrationPlanTooLarge`; a durable handle failing `LegacyHandle` is
`MigrationHandleUnrepresentable`.

Each name is a hub-local typed negative result, not a browser predicate and not
a new chat performative. A negative result emits no correlated
`selfsamepairallocated` success, no carrier, no pairing room, no digest, and no
pending partial object. The WebSocket handler MAY return only the existing
cause-neutral opaque refusal. The browser advances solely on the matching
`selfsamepairallocated` success for its signed request. Refusal, any other
frame, timeout, or suppression leaves that success absent and renders the
cause-neutral local `credential-v2-allocation-refused`. Hub test and audit
evidence retain the exact typed cause. Forging or suppressing a refusal can
therefore deny service but cannot allocate or advance a ceremony.

On success, `migrationRooms` is the complete unique raw room-byte array sorted
by unsigned octets. `roomSetDigest` is recomputed from that exact array. The
hub-signed credential/v2 offer binds both values through `offerCoreDigest` and
`intentDigest`. No browser DOM value participates in either value.

Before the browser displays final migration consent, it SHALL:

1. verify the v2 offer under the issuer selected by the current
   CON-220-authenticated profile;
2. recognize the exact v2 member set, transition tag, and application;
3. recompute `roomSetDigest` from the offer's `migrationRooms`;
4. compare every retained carrier, ceremony, transcript, core, and device
   binding; and
5. construct one private typed consent plan from those authenticated values.

The Path-B reducer renders exactly that plan's `migrationRooms` in order.
`affectedChannelsNow()` and every `.clog[data-room]` query SHALL be removed as
the source of `CeremonyObservation.GrantVerified.affectedChannels`. An
implementation MAY retain a local room list for another purpose. A difference
from the authenticated array is `migration-display-state-changed` and disables
confirmation. Neither list silently wins. Mutating, filtering,
lazy rendering, hiding, paging, or truncating the authenticated array is
non-conforming.

The shipped `apps/cbcl_chat/priv/web/path-b-ceremony.mjs` `CHANNEL` regular
expression is a current admission gate. It SHALL be replaced by a recognizer
generated from the same `CanonicalRoomText` source as credential/v2. A room
grammar failure is `migration-plan-invalid`, never
`burned('invalid-grant-observation')`. Browser, hub, wallet, and cbcl-pairing
recognizers SHALL share fixed vectors for every alphabet branch and boundary.

The exact maxima and residuals in
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remain
unchanged. The complete production v1 comparison and substitution trace at the
implementation baseline is:

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts substitution
crates/selfsame-web-device/src/lib.rs:1346      offer substitution
crates/selfsame-web-device/src/lib.rs:1360      provider-hint substitution
crates/selfsame-web-device/src/lib.rs:1369      statement substitution
crates/selfsame-app-identity/src/enrollment.rs:452 statement-builder substitution
crates/selfsame-app-identity/src/profile.rs:385 profile comparison
crates/selfsame-app-identity/src/provider_hint.rs:115 provider-hint comparison
crates/selfsame-app-identity/src/enrollment.rs:394 statement-payload comparison
crates/selfsame-app-identity/src/enrollment.rs:552 statement-verification comparison
```

The trace is an implementation-baseline inventory, not a finite permission.
Credential/v2 SHALL NOT reach any production v1 comparison or substitution
site, including one added later. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes and behavior remain pinned and
byte-identical.

### REQ-993 — Installed standing distinguishes invalidity, availability, and consent

The atomic credential/v2 installation record contains the retained device
seed, accepted grant, exact ApplicationId, account scope, device key, and grant
reference. It also contains the authenticated profile digest and exact
`accountAuthority`. Reload reconstructs installed account-device state only
after all of these checks:

1. the record is complete and recognized;
2. the seed re-derives the recorded device key;
3. live CON-220 authentication produces a profile whose ApplicationId, digest,
   and account authority match the accepted record;
4. the grant verifies under the issuer selected by that accepted authority;
   and
5. the verified grant names the exact ApplicationId, account scope, device key,
   and grant reference.

Corruption, partial state, an invalid signature, or a wrong binding is
`credential-v2-recovery-required`. An unreachable profile, authority, resolver,
or issuer is instead the non-terminal
`credential-v2-verification-unavailable`. That state retains the complete
record. It sends no enrollment or authority frame and claims no installed
authority. Connectivity restoration or an exact local action retries
verification without a page reload.

A newly authenticated profile with a different digest or account authority is
`credential-v2-application-authority-changed`, not invalidity and not automatic
rotation. A person must compare the old and new profile and authority
fingerprints and explicitly accept the change. Acceptance updates the recorded
profile values only in one atomic transaction. That transaction verifies the
existing grant under the new issuer and matches every binding. Failed
verification preserves the old record and enters recovery.

No hub error or chat frame clears the installation record. The deployed
authorization failure is the zero-signature `(error @room "bad-signature")`.
Six distinct hub authorization causes collapse to that one wire value. The
browser MAY display only the cause-neutral `hub-frame-refused`. It SHALL NOT
claim an absent, revoked, or expired grant. It SHALL NOT diagnose another local
hub reason. No deployed or unknown slug mutates credential state.

The only ordinary clear remains the person-confirmed
`unlink Selfsame wallet` action from
[[spec-008-0.4.14-deployed-admission-and-local-unlink#REQ-991]]. It retains the
seed and changes no hub state. Deleting a hub grant does not create a permanent
device-key tombstone. The same key MAY receive a new grant only through a fresh
successful credential/v2 ceremony. That ceremony requires live profile
authentication, current eligibility, capacity, both exact consent decisions,
and a new verified grant. Local unlink alone never re-grants or restores authority.

## Non-functional requirement

### NFR-917 — Authority and migration lock graphs have explicit scopes

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
The finalizer's exact `cbcl-member` access is a write lock. Its whole
`cbcl-roommember` and `cbcl-account-roommember` locks are write locks.

The twelve-pair closed graph covers only `cbcl-device-grant`, `cbcl-member`,
and `cbcl-pairaccount-grant`. Its writers are grant creation `G`, finalization
`F`, exact revocation `XR`, and observed revocation `OR`. It also covers member
enrollment `ME`, pairaccount claim `PC`, and authority read `AR`:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

The generator for that lane SHALL reproduce exactly those twelve cycles and
the `AR` contention cases. It SHALL NOT claim that the number describes room,
pending, alias, finalization, status, or compensation tables.

A separate migration graph owns those tables. Snapshot/allocation takes the
stable-account and pending-ceremony locks before binding/member locks, then
whole `cbcl-roommember` read and whole `cbcl-account-roommember` read locks in
that order. Finalization takes the same prefix and the two whole write locks in
that order. Any room revoke or other transaction touching both room tables
takes both whole write locks in that order. It acquires them before its first
room read or write. A single-table room grant takes only its exact record lock. This removes
the `index_read`-then-exact-write upgrade from `revoke-handle-tx`.

Every finalizer, compensation, status-recovery, and operator operation touching
two or more tail tables uses one generated total table/key order. Compensation
uses exact captured keys and no `?ALL` scan. Operator withdrawal and
reinstatement take the stable-account lock before alias and tombstone keys.
Read-only status recovery acquires no later write lock.

The migration graph includes every lock of every room or tail operation. This
includes authority-table locks when an operation crosses lanes. Its cycles
SHALL be only the projection of the twelve authority cycles above. It SHALL
generate zero additional cycles. A missing operation, unstated mode, upgrade,
opposite order, or additional cycle is non-conforming.

Every affected transaction runs at Mnesia transaction level 1 with an explicit
integer `Retries >= 2`; this is required because
`mnesia_tm:try_again/1` retries only while `Retries > 1`. Nested transactions
are prohibited. Consumers defensively map `{aborted, nomore}`, an exhausted
`#cyclic{}` abort, and `{aborted, nested_transaction}` to `authority-busy` with
no partial effect. Evidence records actual retries, progress, latency,
throughput, and decision correctness without claiming an unmeasured threshold.

## Contract

### CON-974 — The correction and survivor ledgers are exact

This amendment applies only these replacements:

1. REQ-992 replaces
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#REQ-990]] in full.
2. REQ-993 replaces
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#REQ-991]] in full.
3. NFR-917 replaces
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#NFR-916]] in full.
4. ADR-952 replaces
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#ADR-951]] in full.
5. TEST-1125 through TEST-1128 replace
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#TEST-1121]] through
   [[spec-008-0.4.14-deployed-admission-and-local-unlink#TEST-1124]].

Every original F-A through F-E correction and SPEC-007 REQ-812 remains a hard
stop. The exact cbcl-pairing survivor set contains TEST-030 through TEST-049.
It also contains TEST-050 except its replaced control-object portion and retired
4,096-octet positive witness. That witness remains a required negative.
TEST-051 through TEST-058 also survive. The exact cbcl-bus set is TEST-043 through TEST-065,
TEST-068, TEST-070, and TEST-084 through TEST-087. Every effective Selfsame test
through TEST-1108 plus TEST-1125 through TEST-1128 remains mandatory.

The coordinated review set is this amendment, cbcl-pairing 0.4.7, cbcl-bus
0.16.8, and did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`. A fresh PASS authorizes only the
Elephant SPL and test-first implementation plan. It authorizes no code,
allocation, release, or deployment.

## Decision

### ADR-952 — Success is authoritative; refusals and local clearing do not advance

The hub owns durable admission and only a correlated allocation success can
advance the browser. Exact negative causes remain hub-local because a delivered
refusal need not be trusted: its only permitted effect is denial. The signed v2
offer, not the DOM, supplies the room list the person approves.

The browser pins the profile and account authority observed at installation.
Transient network failure is not corruption, and authority rotation is not
silent. Remote control frames remain non-destructive; explicit local unlink is
person-owned. A deleted key can return only through the complete fresh protocol,
not by local state manipulation.

The lock evidence is split by what was actually derived. The three authority
tables retain the measured twelve-cycle residual. Room and finalizer-tail
operations share one order and add no cycle beyond that authority projection.

## Test specification

### TEST-1125 — Allocation and room consent use authenticated provenance

Exercise all three hub-local admission failures inside the snapshot transaction.
Require no `selfsamepairallocated`, carrier, room, digest, or partial pending
state, and one cause-neutral browser refusal. Suppress and forge refusals and
prove neither can advance. A matching success is the only advance.

Generate every `CbclSymbolChar`, bare `@`, leading punctuation, a maximum room
containing `/`, 256 sorted maximum rooms, and every one-over boundary. Prove `/`
is not escaped. Require hub, browser, wallet, and cbcl-pairing fixed points.

Verify the signed v2 offer and every digest before consent. Mutate
`migrationRooms`, `roomSetDigest`, offer signature, profile, carrier,
transcript, ceremony, and device binding. Mutate, hide, reorder, filter, lazily
render, and add DOM room panes. The displayed list remains exactly the
authenticated array or reaches `migration-display-state-changed`; confirmation
never uses DOM provenance. Grammar failure is `migration-plan-invalid`, not an
invalid-grant terminal.

### TEST-1126 — V1 and v2 call graphs are source-disjoint

Trace every production comparison and substitution site enumerated in REQ-992,
plus every site found by a complete source scan. Prove the v2 graph reaches
none, cannot reach `EnrolmentAllocator::new_native`, and uses its recognized v2
profile for every constructor, recognizer, verifier, signer, and profile-facts
output. Pin and compare byte-identical v1 outputs. Adding an unenumerated v1
site without extending the scan fails.

### TEST-1127 — Installed state handles availability, rotation, unlink, and re-grant

Install credential/v2 atomically and reload with a valid profile and issuer.
Mutate every installation field, seed, signature, and binding and require
recovery. Make the profile, resolver, authority, and issuer independently
unreachable; require the non-terminal unavailable state, retained bytes, no
frame, and successful in-session retry.

Rotate profile digest and account authority. Require an exact change prompt,
cancel and accept it, and prove no record update precedes a newly valid grant
verification. Deliver every deployed error slug, including genuine
`bad-signature`, plus forged/unknown slugs, timeout, and disconnect. None clears
or claims a specific cause. Only confirmed unlink clears local projection.

Delete a hub grant, unlink, then run a fresh full ceremony with the retained key.
Require all current checks and consent and a new valid grant. Attempt local-only
re-grant, stale-grant replay, capacity bypass, and eligibility bypass; all fail.

### TEST-1128 — Both lock graphs and effective ledgers are closed

Generate the authority graph and require exactly NFR-917's twelve pairs plus
all `AR` writer contention. Generate the complete room/tail graph from every
operation and exact lock mode. Require only the projected authority cycles and
both fixed orders. Require no additional cycle, upgrade, or omitted operation.

Run every affected operation at level 1 with `Retries >= 2`. Force wait-die,
`nomore`, `#cyclic{}`, and prohibited nesting; require `authority-busy`, no
partial effect, and writer progress. Require the exact three-repository test
ledgers, the cbcl-bus 0.16.8 parent pointer, and every gate box. Removing any
survivor, comparison site, lock operation, or human-review obligation fails.

## Changelog

- **0.4.15-draft — 2026-08-23 — review candidate, not approved.** Gives
  allocation, refusal, room display, reload, authority rotation, re-grant, and
  lock evidence one code-backed owner each. No implementation or deployment
  authority is claimed.
