---
id: spec-008-0.5.9-implementation-reviewer-prompt-2026-08-24
title: SPEC-008 0.5.9 remediation — Tier-1 adversarial implementation-review prompt
status: active
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-24
---

# SPEC-008 0.5.9 remediation — Tier-1 adversarial implementation-review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
approve work because it is extensive or because focused tests pass. Review the
complete coordinated standalone credential/v2 implementation, with particular
attention to the remediation of the previous implementation review. Do not
repair the candidate. Write only the review report, commit it in the Circus
worktree, and complete through the appended Circus instruction.

## Authentication and isolation

This review runs through Circus using the Claude TUI subscription driver with
`ANTHROPIC_API_KEY` removed from the child environment. Check that the variable
is absent without printing a value. Run `env -u ANTHROPIC_API_KEY claude auth
status` and record only:

- `authMethod`
- `apiProvider`
- `subscriptionType`

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier. A different result
is a review-process blocker. The generating family is OpenAI GPT-5; the
reviewing family is Anthropic Claude. This session receives no generating-
session reasoning.

## Exact candidate set

Use `git show COMMIT:path` and `git diff BASELINE..COMMIT`. Do not substitute
uncommitted files.

- Selfsame candidate `d01959f877d77d50a5c32f830664bd223ae9ebd8`
  - implementation baseline `0220cec2dec44cd95d4f411ea4814d790b6716d2`
  - previous implementation-review candidate
    `ada42dbdf38900977edcf79aab5d7ad05d7e70d4`
  - `specs/SPEC-008-production-pairing-claimant.md` version `0.5.8-draft`
  - `specs/SPEC-007-cbcl-pairing-cutover.md` version `0.3.7-draft`
- cbcl-bus candidate `79423c433e636ccf9c9cf83f6574c6e40c481715`
  - implementation baseline `ce894a7f2debc111b610296b1a3780de6084e37e`
  - previous implementation-review candidate
    `bc952995ed65b6b9f004205d0bb844ac80028c8c`
  - `specs/SPEC-053-key-root-identity.md` version `0.17.8-draft`
- cbcl-pairing candidate `aedbc4ca07068c3cf6356d95333f4c2ec0d374d9`
  - implementation baseline `c22f2d0526432c01c56d64be35a8d4f87516ec92`
  - previous implementation-review candidate
    `c1746a5705b3a6a58c4cd079cb5364003a733519`
  - `specs/SPEC-001-reusable-blind-pairing.md` version `0.5.7-draft`
- did-crdt candidate `94db956d7d5d5edf0f42afad073ab42f265a1752`
  - implementation baseline `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
  - `specs/SPEC-037-pairing-rendezvous-body-bound.md` remains deprecated

The owner explicitly authorized test-first implementation before review PASS.
Production release, public allocation, pushes, and deployment remain prohibited.

## Required independent pass

Read the four coordinated parent specifications and all four complete candidate
diffs before any earlier review. Trace the executable browser, hub, pairing-
library, and wallet paths. Treat prose and test names as untrusted until code or
a runnable test supports them.

Check at least these properties:

1. Relay consent is keyed exactly by `(applicationId, relayOrigin)`, and all
   displayed application facts come from the live authenticated profile rather
   than peer-controlled strings.
2. No application derivation, scope mint, publication, grant, alias, bundle, or
   durable identity effect occurs before its specified person approval. Fresh
   first-contact scope has authenticated, non-attacker-selected authority.
3. The design claims no relay operatorship proof; exact per-application consent
   is the explicit mitigation when a profile names another operator's relay.
4. CPace credential/v2 has one closed wire grammar and key schedule. Verify the
   cryptographic preimages, Finished order, and checkpoint key separation.
5. Browser installation-device possession is proved before offer signing, with
   exact socket-generation typing, bounds, and replay resistance.
6. Resolver closure replay cannot become resolver authority. Hub and wallet
   independently obtain and compare live authority.
7. Hub final acceptance is atomic and retries remain byte-identical. Browser
   activation follows signed final acceptance; wallet installation follows the
   authenticated receipt, live reciprocal binding, and durable compare-and-swap.
8. Browser process restart works at every pre-terminal checkpoint. A restored
   allocator must authenticate the replacement chat socket, query hub status,
   reopen the exact blind-relay membership, and resend only the exact cached
   frame. Check crash windows between checkpoint persistence and each external
   effect, including an `attempt: null` durable record.
9. `selfsamepairstatus` and `selfsamepairabort` have closed grammars, current-
   generation authentication, exact binding, bounded state, and no unauthorized
   advance. Explicit cancel and transport failure must not be confused.
10. Post-relay-window HTTPS recovery authenticates the recovery token,
    application, ceremony, offer, receipt commitment, current live profile, and
    authority rotation. It must not derive, publish, issue, resend payload, or
    install twice. HTTP is POST-only, CBOR-only, bounded, non-redirecting,
    uncacheable, and rate-limited before parsing or store lookup; limiter failure
    must fail closed.
11. Every restored display and decision authority survives checkpointing without
    trusting duplicated JavaScript state or allowing oversized checkpoint state.
12. Dependency pins name the exact cbcl-pairing candidate in Selfsame, cbcl-bus,
    SPEC-007 ADR-802, and the reproducible WASM provenance. Ordinary builds must
    reject a mismatched or dirty sibling; the vendored WASM must rebuild byte-
    identically from clean exact pins and expose the restored allocator ABI.
13. Crash, disconnect, timeout, replay, wrong order, cancel, expiry, and recovery
    cannot create a second identity effect or accepted authority. Evaluate the
    known orphan-DID residual separately and decide whether it is blocking.
14. Bounds and one-over-limit refusals are executable. The ordinary production
    entry remains closed; loopback staging and existing enrolment remain distinct.
15. Tests reach the named invariants. Mutate load-bearing predicates mentally or
    experimentally and identify surviving mutants, especially browser durable-
    state recognition, phase reconciliation, rate limiting, and pin guards.

Run focused suites and narrower adversarial tests as needed. Record exact
commands and outcomes. A collection failure or dependency-resolution failure is
not evidence that a requirement passed. You may use clean detached clones for
strict source-closure builds, but do not modify any candidate repository.

## Prior-review re-test

Only after the independent pass, read:

- `specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`

Independently re-test every 0.5.8 finding: H-1 browser/hub restart recovery,
H-2 authenticated HTTPS terminal recovery, M-1 dependency-pin drift, and L-1
orphan DID publication. Also re-test 0.5.7 N-1, F-A through F-E, and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Do not inherit any disposition.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.9-implementation-claude-adversarial-review-2026-08-24.md`.
The report SHALL contain:

- reviewer identity, auth path, Circus attempt, exact commits, and independence;
- verdict `PASS` or `REJECT` and severity counts;
- every finding with severity, code evidence, exploit or failure sequence,
  affected fully-qualified requirement, and measurable correction;
- explicit dispositions for all fifteen required checks;
- explicit dispositions for H-1, H-2, M-1, L-1, 0.5.7 N-1, F-A through F-E,
  and [[SPEC-007-cbcl-pairing-cutover#REQ-812]];
- exact test commands and results;
- a statement that PASS does not authorize release, push, or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. A load-bearing missing
obligation is a finding even if the candidate fails closed. LOW residuals may
remain only when their consequence and operational treatment are explicit.
Release and deployment controls are outside this review's authority.
