---
id: SPEC-008-review-0.4.16-claude-2026-08-23
title: SPEC-008 0.4.16 + cbcl-pairing 0.4.7 + cbcl-bus 0.16.9 — coordinated Tier-1 adversarial review
status: final
tier: 1
verdict: REJECT
reviewer: Claude Opus 5 (fresh context, adversarial)
review-date: 2026-08-23
review-input: "[[spec-008-0.4.16-correlated-success-and-deliverable-bounds]]"
prior-review: "[[spec-008-0.4.15-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.16 + cbcl-pairing 0.4.7 + cbcl-bus 0.16.9 — coordinated Tier-1 adversarial review: REJECT (2026-08-23)

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

## Model, authentication, and independence evidence

The launch driver removed the paid-API credential before this session began. I
recorded the gate result first, without printing any secret value.

```text
$ if [ -z "${ANTHROPIC_API_KEY+x}" ]; then echo ABSENT; fi
ABSENT
$ env | grep -c ANTHROPIC_API_KEY
0
$ claude auth status
{
  "loggedIn": true,
  "authMethod": "claude.ai",
  "apiProvider": "firstParty",
  "orgName": "hugo.oconnor@gmail.com's Organization",
  "subscriptionType": "max"
}
```

The variable is absent from the environment. The reported `authMethod` is
`claude.ai` and the reported `subscriptionType` is `max`. This session called
no Anthropic API and no external web service. The reviewing model is Claude
Opus 5 in a fresh context. Every input came from the four local repositories,
the local cbcl-rs checkout, and the local OTP install.

The generating sessions authored the three candidates. This session read the
candidates and the source, and never the generator reasoning.

## Reviewed commits and baselines, verified rather than assumed

| Repository | Commit | Role |
|---|---|---|
| selfsame (review worktree) | `f519de4178a76f8fbd6d472febf3c91ff969079d` | candidate 0.4.16-draft |
| selfsame | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | declared implementation baseline |
| cbcl-pairing | `83ac3d35c263a0a429524eade863ddbd3383862d` | candidate 0.4.7-draft |
| cbcl-bus | `c410c9237220e4852cfca7fff50493c4bf6e1f7d` | candidate 0.16.9-draft |
| cbcl-bus | `ce894a7f2debc111b610296b1a3780de6084e37e` | declared implementation baseline |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | declared coordinated baseline |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | pinned grammar revision |

Every commit resolves. The cbcl-pairing and did-crdt working trees carry
user-owned uncommitted paths. I read those two repositories only through
`git show COMMIT:PATH` and changed nothing in them. The local OTP install is
`28.5` with `mnesia-4.25.3`.

I read the complete effective amendment chain, the full 0.4.15 review, the
coordinated cbcl-bus and cbcl-pairing candidates,
[[PROTO-002-selfsame-rendezvous-v1]], and the parent
[[SPEC-007-cbcl-pairing-cutover#REQ-812]].

## Verdict and severity counts

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | N-H-1 |
| MEDIUM | 4 | N-M-1 … N-M-4 |
| LOW | 5 | N-L-1 … N-L-5 |

Verdict: **REJECT**.

`N-H-1` is release-blocking on its own. It inverts the only sentence that keeps
the migration consent surface complete, and the coordinated twin states the
opposite rule. `N-M-1` is a third repetition of a false completeness claim.
`N-M-2` and `N-M-3` each make a mandatory generator test unsatisfiable against
real code.

This candidate set closes the 0.4.15 rejection well. Every one of that review's
ten findings is closed on substance. The correlation member, the unsigned-frame
boundary, and the transit gate all re-derive against code. So do the two
`write-grant` upgrades, the handle terminal, and the unlink restatement. So do
the survivor ledger, the two status classes, and the refresh split.

The rejection rests on one inverted normative sentence, one incomplete
inventory, and three obligations that disappeared with the clauses this
amendment replaces in full.

---

## Findings

### N-H-1 (HIGH, release-blocking) — the consent-surface completeness rule is inverted in Selfsame, contradicts its cbcl-bus twin, and no test covers it

**Repository owner: selfsame (REQ-994), cbcl-bus (REQ-068) in coordination.**

`REQ-994` at
`specs/trajectory/SPEC-008/spec-008-0.4.16-correlated-success-and-deliverable-bounds.md:108-110`
reads:

> `affectedChannelsNow()` and every `.clog[data-room]` query SHALL NOT source
> `CeremonyObservation.GrantVerified.affectedChannels`. No filter, paging,
> collapse, or lazy rendering SHALL hide no room.

The coordinated cbcl-bus `REQ-068` at
`specs/trajectory/SPEC-053/spec-053-0.16.9-correlated-success-and-deliverable-bounds.md:152-154`
carries the identical paragraph with one different word:

> No filter, paging, collapse, or lazy rendering SHALL hide a room.

The Selfsame sentence carries two negations over one obligation. Read strictly,
`No X SHALL hide no room` licenses hiding, and it also states the opposite of
the rule its own twin states. This is the sole normative guarantee that the
person sees every room before an irreversible migration.

The obligation matters because of shipped code.
`apps/cbcl_chat/priv/web/path-b-ceremony.mjs:150-157` builds
`affectedChannels`, and `:192-200` stores it on the state. The painter at
`apps/cbcl_chat/priv/web/app.js:652-670` renders it into `link-channels`, and
its loop at `:666-670` iterates the whole array today. Nothing structural keeps
a later revision from filtering, paging, or collapsing that list. The inverted
sentence is the only forward guarantee against it. `affectedChannelsNow()` at
`app.js:602-603` is the DOM source both candidates forbid, and its call sites
at `app.js:6409` and `app.js:6443` are real.

No test closes the gap. `TEST-1130` requires only *"Mutate the authenticated
room array and every DOM condition."* cbcl-bus `TEST-089` carries the same
sentence. Neither one renders a plan through a filter, a page, a collapsed
group, or a lazy list, and neither one asserts the rendered count. The
0.4.15 review closed 0.4.14 `H-3` on the source of the list. This candidate
leaves the completeness of the list guaranteed by one inverted sentence.

The hard stop this touches is F-B. The exact-intent surface is what a person
approves, and a hidden room is a migrated room nobody consented to.

**Minimum sound correction.** Replace the Selfsame sentence with the cbcl-bus
form. Add a case to `TEST-1130` and `TEST-089` that renders a maximum plan
through every filter, paging, collapse, and lazy-render path. Require the
displayed room multiset to equal the authenticated array.

---

### N-M-1 (MEDIUM) — the block labelled "the complete production v1 comparison and substitution trace" omits all four production `payloadVersion` sites, and the named scan families omit that constant

**Repository owner: selfsame (REQ-994) and cbcl-bus (REQ-068), identical block.**

The eleven-line trace is a real improvement over the nine-line 0.4.15 block, and
every line in it verifies at the declared implementation baseline
`0220cec2dec44cd95d4f411ea4814d790b6716d2`:

```text
selfsame-web-device/src/lib.rs:600       selfsame_app_identity::PROFILE_VERSION,
selfsame-web-device/src/lib.rs:1346      profile_version: 1,          (OfferCore)
selfsame-web-device/src/lib.rs:1360      profile_version: 1,          (ProviderHint)
selfsame-web-device/src/lib.rs:1369      profile_version: core...     (EnrollmentStatement)
selfsame-app-identity/src/enrollment.rs:373  != Some(1)  evidenceVersion
selfsame-app-identity/src/enrollment.rs:394  != Some(crate::PROFILE_VERSION)
selfsame-app-identity/src/enrollment.rs:452  profile_version: crate::PROFILE_VERSION,
selfsame-app-identity/src/enrollment.rs:552  != crate::PROFILE_VERSION
selfsame-app-identity/src/enrollment.rs:662  ("evidenceVersion", Json::int(1)),
selfsame-app-identity/src/profile.rs:385     if version != PROFILE_VERSION {
selfsame-app-identity/src/provider_hint.rs:115  != crate::PROFILE_VERSION
```

The three enclosing-object labels also verify. `:1346` sits inside
`let core = OfferCore {`, `:1360` inside `let hint = ProviderHint {`, and
`:1369` inside `let statement = EnrollmentStatement {`. The 0.4.15 `M-1`
relabel of `:452` to `recognizer substitution` is correct.

A fourth version family is missing entirely.
`crates/selfsame-app-identity/src/ceremony.rs` holds no `#[cfg(test)]` anywhere
in its 585 lines, so every site in it is production. Four of them compare or
substitute the version-1 literal on `payloadVersion`:

| Site | Code | Surface |
|---|---|---|
| `ceremony.rs:185` | `("payloadVersion", Json::int(1)),` | **substitution**, in `OfferCore::to_json` (declared `:183`) |
| `ceremony.rs:308` | `if payload.get("payloadVersion").and_then(Json::as_i64) != Some(1)` | **comparison**, in `pub fn recognise_offer` (declared `:281`) |
| `ceremony.rs:460` | `("payloadVersion", Json::int(1)),` | **substitution**, in `pub fn build_bundle` (declared `:450`) |
| `ceremony.rs:510` | `if payload.get("payloadVersion").and_then(Json::as_i64) != Some(1)` | **comparison**, in `pub fn recognise_bundle` (declared `:487`) |

`payloadVersion` is not a marginal constant. It is the first member of the v2
core shape.
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] opens the
`OfferCoreV2` member table with `payloadVersion = 2`, so it is the top-level
discriminant between a v1 and a v2 offer. The scan obligation names three
families and stops: *"It includes `PROFILE_VERSION`, `profileVersion`, and
`evidenceVersion`."* `TEST-1130` and cbcl-bus `TEST-089` name only
*"both `evidenceVersion` sites"*.

An independent sweep confirms the rest. Eight `PROFILE_VERSION` occurrences
resolve to two constant definitions and six trace lines. Three `evidenceVersion`
occurrences outside a test resolve to a member-name entry at
`enrollment.rs:79` and the two trace lines. The one `("profileVersion", Json::int(1))`
outside the trace sits at `selfsame-app-identity-net/src/profile.rs:349`, below
that file's `#[cfg(test)]` at `:221`, so it is correctly excluded.

What is not wrong: the normative rule is universal. *"Credential/v2 SHALL reach
no v1 comparison or substitution site."* `TEST-1130` also requires *"Scan every
production version constant and literal."* A conforming implementation stays
safe. The defect is that a block whose own first word is "complete" is
incomplete for the third consecutive revision. The enumerated family list also
omits the discriminant `REQ-984` itself uses.

**Minimum sound correction.** Add the four `ceremony.rs` sites to the shared
block with their surfaces. Add `payloadVersion` to the named scan families in
`REQ-994`, `REQ-068`, `TEST-1130`, and `TEST-089`.

---

### N-M-2 (MEDIUM, gate-blocking) — both candidates drop the lock-acquisition point and the named `revoke-handle-tx` upgrade, while still asserting that no upgrade exists

**Repository owner: selfsame (NFR-918) and cbcl-bus (NFR-016).**

The two `write-grant` corrections that 0.4.15 `M-2` demanded are present and
exact. `NFR-918` requires *"Both single-table `write-grant` paths take an exact
write lock on their first read. They use `mnesia:read/3` with `write`."* Both
deployed paths carry the upgrade today, at
`apps/cbcl_chat/src/shell/cbcl-chat-roommember.lfe:153-161` and
`apps/cbcl_chat/src/shell/cbcl-chat-account-roommember.lfe:65-80`, so the
requirement names the right sites. The `redeem-and-grant` and
`cbcl-chat-invite:redeem-tx` clauses are also correct. Both variants call
`cbcl-chat-invite:redeem-tx` before `write-grant`, at
`cbcl-chat-roommember.lfe:137-142` and `cbcl-chat-account-roommember.lfe:53-63`.
`redeem-tx` at `cbcl-chat-invite.lfe:57-58` reads with `mnesia:read/2` before
`consume` writes or deletes with `write`.

The predecessor clause carried a third correction that is now gone. 0.4.15
`NFR-917` read:

> Any room revoke or other transaction touching both room tables takes both
> whole write locks in that order. It acquires them before its first room read
> or write. A single-table room grant takes only its exact record lock. This
> removes the `index_read`-then-exact-write upgrade from `revoke-handle-tx`.

cbcl-bus 0.16.8 `NFR-015` carried the same obligation in its own wording. It
placed the locks *"before its first room access"* and closed *"Thus
`revoke-handle-tx` performs no `index_read`-then-exact-write upgrade."*

`NFR-918` replaces `NFR-917` in full through
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]] item 3,
and `NFR-016` replaces `NFR-015` in full through cbcl-bus `REQ-067` item 3.
Both replacements keep only *"Any operation touching both room tables takes
both whole write locks in that order."* Both drop the acquisition point. Both
drop the named operation. Neither repository retains the sentence anywhere.

The upgrade is still live at the cbcl-bus baseline.
`cbcl-chat-roommember.lfe:242-245` opens one transaction that deletes an exact
`cbcl-roommember` key with `write` and then calls
`cbcl-chat-account-roommember:revoke-handle-tx`. That function at
`cbcl-chat-account-roommember.lfe:201-209` calls `mnesia:index_read` and then
`mnesia:delete` with `write` on the same table.

`mnesia:index_read` takes a whole-table read lock. I verified this at the local
OTP 28.5 `mnesia-4.25.3` install. `ixrlock/5` at `mnesia_locker.erl:1086-1101`
calls `need_lock(Store, Tab, ?ALL, read)`. `need_lock/4` returns `no` only when
a covering table lock is already held.

The consequence is exact. An implementation that takes the two whole write
locks at transaction entry is safe, because `need_lock` then suppresses the read
lock. An implementation that takes them lazily satisfies the surviving sentence
word for word and still performs a whole-table read-to-write upgrade on
`cbcl-account-roommember`. `NFR-918` asserts *"It has no additional cycle or
upgrade"* over the same graph, so the two readings disagree.

The non-conformance list is trimmed alongside. 0.4.15 read *"A missing
operation, unstated mode, upgrade, opposite order, or additional cycle is
non-conforming."* 0.4.16 reads *"Omission, opposite order, or an unstated mode
is non-conforming."* Both `upgrade` and `additional cycle` are gone from the
enumeration.

`TEST-1133` and cbcl-bus `TEST-091` require a generator to show *"no additional
upgrade or cycle."* A faithful generator over a lazily-locking conforming
implementation produces one. The test cannot pass and the GATE-04 room-graph box
cannot close.

**Minimum sound correction.** Restore *"It acquires them before its first room
read or write"* to `NFR-918` and `NFR-016`. Restore the sentence naming
`revoke-handle-tx`. Restore `upgrade` and `additional cycle` to the
non-conformance enumeration.

---

### N-M-3 (MEDIUM, gate-blocking) — the twelve authority cycles are stated in a symbol notation whose legend both candidates deleted

**Repository owner: selfsame (NFR-918) and cbcl-bus (NFR-016).**

`NFR-918` states the cycle set as a bare block of letter pairs:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

It then requires *"The generator SHALL reproduce those twelve cycles and every
`AR` contention case."* cbcl-bus `NFR-016` carries the identical block and the
identical obligation.

Neither clause defines `G`, `F`, `OR`, `ME`, `PC`, `AR`, or `XR`. The
predecessors did. 0.4.15 `NFR-917` carried the writer and reader legend:

> Its writers are grant creation `G`, finalization `F`, exact revocation `XR`,
> and observed revocation `OR`. It also covers member enrollment `ME`,
> pairaccount claim `PC`, and authority read `AR`.

cbcl-bus 0.16.8 `NFR-015` carried the same legend in its own wording. Both
replacements delete it.

The one pointer each clause inherits does not restore it.
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] carries the operation
matrix. Its seven rows are named in full words: `grant creation`,
`credential/v2 finalizer`, `exact grant revocation`, `observed revocation`,
`new member enrollment`, `pairaccount claim`, and `unratified read`. No symbol
appears in that clause. `NFR-913` separately names a `per-frame authority read`,
so `AR` has two plausible referents in the inherited text and no binding.

This is not a presentation defect. `TEST-1133` requires *"Generate the twelve
authority cycles"*, `NFR-918` requires the generator to reproduce *"every `AR`
contention case"*, and `NFR-918` declares omission non-conforming. A generator
author cannot bind `XR` or `AR` to a row from the effective text alone. Both
repositories lost the same legend in the same revision, which is the coordinated
ledger failure that 0.4.15 `M-4` already filed once.

**Minimum sound correction.** Restore the writer and reader legend to `NFR-918`
and `NFR-016`, binding each symbol to its exact `NFR-913` matrix row.

---

### N-M-4 (MEDIUM) — the mandated parent pointer has no target in this repository, and the parent still advertises an approved implemented version

**Repository owner: selfsame (TEST-1133 and the parent specification).**

`TEST-1133` requires static checks over *"the exact three-repository ledgers,
every version site, 42 GATE-04 boxes, and the parent pointer."*

On the cbcl-bus side that obligation is discharged. `TEST-092` names it exactly
as *"the parent DEC-012 pointer to 0.16.9"*, and
`specs/SPEC-053-key-root-identity.md:15` carries
`candidate-amendment: "[[spec-053-0.16.9-correlated-success-and-deliverable-bounds]] …"`,
with `DEC-012` at `:635` and two body references at `:688-691`.

On the Selfsame side there is no such pointer. The parent is
`specs/SPEC-008-production-pairing-claimant.md`. Its frontmatter carries
`version: 0.2.2`, `status: implemented`, `review-gate: approved`, and
`last-updated: 2026-08-20`. It holds no `candidate-amendment` key. A search of
`specs/*.md` for `spec-008-0.4` returns nothing, so no parent-layer document
links any amendment in the chain. The only trajectory reference in the parent is
a 2026-08-22 review path at `:404`.

Two things follow. First, the seventeen-document 0.4.x chain is unreachable from
its own parent, so the reviewer, implementer, and stakeholder reading paths all
terminate before the effective law. Second, the governing parent presents an
approved, implemented Tier-1 specification while a no-go Tier-1 increment is
open against it, with every candidate carrying
`review-gate: not-approved; implementation-prohibited`.

The obligation is also ambiguous. `TEST-1133` names *"the parent pointer"* in a
list whose other members are cross-repository. Under the cbcl-bus reading the
Selfsame chain stays unlinked. Under the Selfsame reading the check has no
target.

**Minimum sound correction.** Add a `candidate-amendment` pointer to
`specs/SPEC-008-production-pairing-claimant.md` naming
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds]], and state the
open Tier-1 increment in the parent. Name the repository of each parent pointer
in `TEST-1133`.

---

### N-L-1 (LOW) — `socketGeneration` is a load-bearing map key that no clause defines

**Repository owner: selfsame (REQ-994) and cbcl-bus (REQ-068 and ADR-027).**

`REQ-994` states *"The browser keeps an active map keyed by
`(socketGeneration, requestId)`."* cbcl-bus `ADR-027` calls it *"The one-use
`(socketGeneration, requestId)` map"*. The token appears three times across the
two candidates and nowhere else. It is absent from every earlier amendment, from
`apps/`, and from `crates/`.

Nothing states what advances a generation. Nothing states what happens to a live
entry when the socket reconnects. Nothing states whether an acknowledgement
arriving on a later generation is discarded. `TEST-1129` and cbcl-bus
`TEST-088` both presuppose the notion. Each requires two ceremonies
*"concurrently on one socket generation."*

The correlation strength does not rest on it. `requestId` is 32 random octets
per attempt, consumed once, so a degenerate constant generation leaves
cross-ceremony confusion closed. The reconnect case is fail-closed: an unmatched
entry leaves the browser waiting, which denies service and allocates nothing.
The defect is a term with no definition inside a clause where every other key is
exact.

**Minimum sound correction.** Define `socketGeneration` in `REQ-994` and
`REQ-068`, and state the reconnect outcome for a live entry.

---

### N-L-2 (LOW) — the coordinated set carries two different definitions of one shared grammar production while requiring a single generated source

**Repository owner: selfsame (REQ-994), cbcl-pairing (REQ-029), cbcl-bus (CON-031).**

`REQ-994` splits shape from length so that an over-length room has exactly one
outcome, which is the correct 0.4.15 `L-3` closure:

```text
RoomShape = "@" 1*( CbclSymbolChar )
CanonicalRoomText = RoomShape with 2..129 ASCII octets
```

cbcl-pairing `REQ-029` at commit `83ac3d3` keeps the pre-correction form:

```text
CanonicalRoomText = "@" 1*128( CbclSymbolChar )
```

The two productions accept the same language. They differ in what a generated
recognizer can report. Only the split form yields the two admission classes that
`REQ-994` orders, and cbcl-pairing is the repository that owns the independent
closed recognizer.

The conflict is with an explicit claim. cbcl-bus `CON-031` requires *"The
shipped regex, canonical module, wallet, and cbcl-pairing use one generated
grammar and vector source."* `REQ-994` requires the `CHANNEL` replacement to be
*"generated from the same `CanonicalRoomText` source."* One source cannot emit
two definitions of one production name.

cbcl-pairing 0.4.7 is unchanged from the revision 0.4.15 reviewed. Its
frontmatter still reads `consumer-design: selfsame SPEC-008 0.4.15-draft` and
`review-input: selfsame SPEC-008 0.4.14 Claude adversarial review`. The
divergence arrived when 0.4.16 changed the production and cbcl-pairing did not
follow.

The alphabet itself is correct in all three. I verified `CbclSymbolChar`
character for character against `is_symbol_char` at
`crates/cbcl-parser/src/parser.rs:328-342` in pinned cbcl-rs `febc669`. It is
ASCII alphanumeric plus `_ - . / ! ? + * < > = @`.

**Minimum sound correction.** Carry the `RoomShape` split into cbcl-pairing
`REQ-029`, or state in `CON-975` that the cbcl-pairing production is the
same language under a different presentation.

---

### N-L-3 (LOW) — the transit ledger discloses one non-conforming rendezvous and omits the one in this repository

**Repository owner: selfsame (CON-975 and TEST-1131), cbcl-bus (TEST-090).**

`REQ-994` states the plural fact correctly: *"The current rendezvous baselines
accept only 4,096 octets."* Both implementations verify.
`crates/selfsame-rendezvous/src/lib.rs:48` reads
`pub const MAX_SLOT_BYTES: usize = 4096;` and enforces it at `:129`. did-crdt
`src/service/rendezvous.rs:65` carries the same constant at the declared
baseline.

The ledger and the tests then name only one. `CON-975` states *"That did-crdt
baseline has a 4,096-octet rendezvous cap. It is deliberately non-conforming."*
`TEST-1131` requires *"Prove the declared did-crdt baseline fails this test at
4,096 octets."* cbcl-bus `TEST-090` repeats it.

The omitted implementation is the one
[[PROTO-002-selfsame-rendezvous-v1]] itself describes. Its
non-conformance list at `:1005-1021` opens with a match list for the reference
`crates/selfsame-rendezvous` implementation. It then records six gaps. The
second gap reads:

> its maximum body is 4,096 rather than 69,632 bytes

That crate lives in this repository. It is one of the two implementations the
GATE-04 transit box names.

The tests do exercise both. Each requires transit *"through both rendezvous
implementations."* The defect is disclosure in a contract that calls its
dependency ledger exact.

**Minimum sound correction.** Name `crates/selfsame-rendezvous` and its
4,096-octet cap in `CON-975` beside did-crdt, and require both baselines to fail
the transit test in `TEST-1131` and `TEST-090`.

---

### N-L-4 (LOW) — the sealed-record maximum is required to fit a cap that no clause derives it against

**Repository owner: selfsame (REQ-994 and TEST-1131).**

`TEST-1131` requires *"Generate maximum offer and reverse records with sealing
overhead. Require each record to fit the 69,632-octet PROTO-002 cap."*

[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] derives every
other maximum and prints its residual. It gives
`V2_MAX_OFFER_PAYLOAD_OCTETS = 57,000` and
`V2_MAX_CREDENTIAL_MEMBER_OCTETS = 62,000`, and states a reachable complete
offer of 41,308 octets. It declares no sealed-record size, no AEAD overhead
figure, and no reverse-record maximum. `REQ-994` adopts those maxima unchanged
and adds none.

The arithmetic does close. Sealing adds a nonce and a tag to an opaque byte
string. `seal_offer_bytes` at `crates/selfsame-web-device/src/lib.rs:671`
returns raw octets that `app.js:2112-2117` sends as
`application/octet-stream`. No base64 expansion applies.

The gap is that a generator author has to derive the figure the test requires.
That sits inside a requirement whose stated discipline is that every maximum is
derived and labelled.

**Minimum sound correction.** Add a sealed offer maximum, a sealed reverse
maximum, and each residual against 69,632 octets to the `REQ-988` inheritance in
`REQ-994`.

---

### N-L-5 (LOW) — one live `cbcl-invite` lock falls outside both declared graph scopes

**Repository owner: cbcl-bus (NFR-016) and selfsame (NFR-918).**

`NFR-918` places `cbcl-invite` inside the migration graph's generated table
order and states *"The migration graph includes every lock and exact mode."* Its
scope sentence is narrower: *"The migration graph owns every room and tail lock."*

One deployed operation takes a `cbcl-invite` lock and touches no room or tail
table. `apps/cbcl_chat/src/shell/cbcl-chat-sealedgrant.lfe:67-73` runs `put-tx`,
which calls `cbcl-chat-invite:peek-tx` and then writes `cbcl-sealedgrant`.
`peek-tx` at `cbcl-chat-invite.lfe:75-78` takes an exact-key read lock through
`mnesia:read/2`.

I traced the consequence and found no safety defect. `put-tx` performs no
`cbcl-invite` write, so it carries no upgrade, and it requests no room lock, so
it closes no cycle against `redeem-and-grant`. A concurrent redemption waits on
the read lock and then proceeds. The defect is that a generator asked to include
every `cbcl-invite` lock cannot decide from the scope sentence whether this
operation belongs in the graph.

**Minimum sound correction.** State in `NFR-918` and `NFR-016` whether the
migration graph covers every `cbcl-invite` holder, and name
`cbcl-chat-sealedgrant:put-tx` if it does.

---

## Disposition of every 0.4.15 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **H-1** — unsigned advancing signal, undefined correlation, no forgery test | **CLOSED** | `REQ-994` names the correlation member, and it is not invented. Adopted [[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]] already declares `selfsamepairallocate(rawCarrier, requestId, intentNonce, requestedPermissions, deviceKeyJwk)` and lists `requestId` in the `OfferCoreV2` member table. The candidate states plainly that the frame is *"unsigned and origin-less, like every deployed hub-to-browser frame"*, which matches `apps/cbcl_chat/src/core/cbcl-core-wire.lfe:185` and the `zero-sig` builder at `:31-36`. It states that the echo *"prevents request confusion but does not authenticate the migration plan"*, and that *"The later hub-signed offer authenticates the plan and every consequential value."* The cross-ceremony case closes twice over: the one-use `(socketGeneration, requestId)` map, and `CON-939` step 2, which already requires the browser to check every *"ceremony, and request value it knows"* against the signed core. `TEST-1129` and cbcl-bus `TEST-088` add swap, duplicate, suppress, reorder, replay, mutation, removal, and a copied acknowledgement with no hub allocation. Residual filed as `N-L-1`. |
| **M-1** — evidence-version sites omitted, recognizer mislabelled | **PARTLY CLOSED** | Both `enrollment.rs:373` and `:662` are added with correct surfaces, and `:452` is relabelled `recognizer substitution`. All eleven lines verify at the declared baseline. The scan obligation widens to *"every production version constant and literal."* Still open: four production `payloadVersion` sites in `ceremony.rs` and the named family list. Filed as `N-M-1`. |
| **M-2** — `write-grant` read-then-write upgrades | **PARTLY CLOSED** | `NFR-918` requires `mnesia:read/3` with `write` on first read in both paths, names both `redeem-and-grant` variants, places `cbcl-invite` before both room tables, and adds `cbcl-chat-invite:redeem-tx`. I verified every named site: `cbcl-chat-roommember.lfe:153-161`, `cbcl-chat-account-roommember.lfe:65-80`, `cbcl-chat-roommember.lfe:137-142`, `cbcl-chat-account-roommember.lfe:53-63`, and `cbcl-chat-invite.lfe:57-58` with `consume` at `:80-89`. The finalizer claim also verifies: `cbcl-chat-account-roommember.lfe:139-140` takes both whole locks with `write` in the stated order. Still open: the dropped acquisition point and the dropped `revoke-handle-tx` sentence. Filed as `N-M-2`. |
| **M-3** — maxima never checked against the only implemented rendezvous | **CLOSED** | `REQ-994` states that the current baselines *"accept only 4,096 octets"* and that *"Production use therefore depends on [[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and [[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Its Tier-1 gate remains open."* Both anchors resolve: `REQ-305` at `specs/PROTO-002-selfsame-rendezvous-v1.md:310-320` sets 69,632 octets, and `CON-304` at `:709-733` sets a `1..69632` body with `413` above it. `CON-975` records the did-crdt non-conformance and states that GATE-04 owns the cross-repository transit evidence. cbcl-bus GATE-04 gains the transit box, and I counted 42 boxes mechanically, which matches `TEST-1133` and `TEST-092`. `TEST-1131` requires put, repeat-put, get, and repeat-get of each exact record through both implementations, rejects 69,633 octets, and states that GATE-04 cannot close first. Residuals filed as `N-L-3` and `N-L-4`. |
| **M-4** — handle-change terminal lost from every ledger | **CLOSED** | `REQ-995` restates *"While verified installation exists, a handle change is the local terminal `account-device-handle-change-refused`. It sends no enrollment or authority frame. It changes no local or hub state."* `TEST-1132` requires the terminal and zero enrollment frames, and cbcl-bus `TEST-091` and `CON-032` carry the coordinated halves. The hub-side guard remains independent at `apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe:180-181`. |
| **L-1** — survivor set revives `TEST-057` | **CLOSED** | `CON-975` reads *"TEST-051 through TEST-056 and TEST-058 survive. TEST-057 does not survive because TEST-058 replaces it."* That agrees with cbcl-pairing `CON-024`, which states `TEST-058` replaces [[spec-001-0.4.6-deployed-room-admission#TEST-057]]. The guard sentence *"No superseded portion or open-ended range supplies authority"* is restored. |
| **L-2** — unlink sourced from a fully replaced clause | **CLOSED** | `REQ-995` restates the action in full: the exact confirmation screen, cancellation leaving state intact, atomic removal of grant and projection, seed retention, no enrollment or authority frame, and unchanged hub authority. The pointer to the replaced clause is gone. |
| **L-3** — over-length room satisfies two admission names | **CLOSED** | `REQ-994` splits `RoomShape` from length, orders the four admission classes, and adds *"Length classification occurs only after `RoomShape` passes. An over-length room therefore has one result."* cbcl-bus `REQ-068` carries the identical order. Residual filed as `N-L-2`. |
| **L-4** — ordinary profile edit presents as authority rotation | **CLOSED** | `REQ-995` splits the triggers. A changed digest under an unchanged `accountAuthority` and issuer re-verifies the existing grant and re-pins *"without a person prompt"*. A changed authority or issuer keeps the person-facing acceptance of both fingerprint pairs. The silent path cannot widen consent, because [[spec-008-0.4.11-standalone-tofu-first-contact#REQ-981]] keys the person-owned policy on the exact `(applicationId, relayOrigin)` pair, so a refreshed relay descriptor produces a new pair and a new prompt. |
| **L-5** — new browser state names carry no status class | **CLOSED** | `REQ-994` states *"`migration-display-state-changed` is non-terminal"* with a retained plan, disabled confirmation, and plan-sourced rerender, and makes `migration-plan-invalid` *"terminal … for that ceremony"* requiring a new ceremony. Both are implementable against the shipped reducer, whose `Burned` status returns state unchanged at `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:172`. |

Eight closed outright, two partly closed. No 0.4.15 finding regressed on
substance. Two new findings arise from text the replacements dropped, and
`N-H-1` arises from one word the replacement changed.

---

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay, and a new application prompts | **HOLDS** | [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]] names five replacements, and `REQ-981` is not among them. `REQ-981` still fixes the person-owned policy key as the exact pair `(https://chat.anuna.io/selfsame/application/v2, https://chat.anuna.io:9443)`, states that *"A v1 policy entry cannot suppress this first v2 prompt"*, and keeps both migration consent screens mandatory. cbcl-bus `REQ-068` restates the distinct ApplicationId in its first sentence. The `REQ-995` silent refresh does not reach this key. |
| **F-B** — the intent surface shows the authenticated application, cross-checked before approval | **FAILS on the room half** | The wallet half is untouched and sound: `crates/selfsame-pairing/src/live.rs:479-480` commits one explicit decision only after `LiveEffect::DisplayIntent`, and the application derives the fingerprint itself at `crates/selfsame-web-device/src/lib.rs:529-534`. The five pre-consent checks in `REQ-994` are ordered and concrete, and the plan is built only from authenticated values. The room list is sourced correctly from the authenticated array. Its completeness rests on one inverted sentence with no test, which is `N-H-1`. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes the applicable consent | **HOLDS** | `CON-975` names nothing in `CON-962`, so its effect order stands. Every clause this amendment adds is a grammar, admission, correlation, display, lock, or local-state rule. A negative admission result *"creates no hub success, carrier allocation, pairing room, digest, or partial pending object."* A copied acknowledgement *"can only start the reversible transport wait"* and *"cannot create hub state or reach consent."* Missing signed evidence *"terminates the attempt with no identity effect."* |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | Untouched by 0.4.16. `CON-975` names neither `CON-939` nor `REQ-970`, so `selfsamepairallocate` still loads or atomically creates the stable scope and stores one pending ceremony, with the scope and ceremony committing atomically or no carrier displaying. No enrolment record is implied. |
| **F-E** — relay operatorship is bound, or the residual is contained by per-application consent | **HOLDS** | Neither candidate claims a binding property for the relay operator. cbcl-pairing `CON-024` preserves the no-relay-field property. The size-correlation residual stays bounded: `MAX_ADJACENT_BODY_OCTETS = 65_536` at cbcl-pairing `src/cbcl_protocol.rs:34` and `MAX_FRAME_BODY_BYTES = 69_632` at `src/mailbox.rs:15`, both above the 57,000-octet offer and the 62,000-octet member. The GATE-04 box requiring human acceptance of relay-visible size correlation is retained. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause reads *"Mismatch, protocol violation, cancellation, relay closure, and expiry SHALL produce no accepted credential or identity side effect before approved payload delivery"* at `specs/SPEC-007-cbcl-pairing-cutover.md:328-334`. Every negative path this amendment adds or keeps creates nothing. The three admission results allocate nothing. `credential-v2-allocation-refused`, `credential-v2-verification-unavailable`, `hub-frame-refused`, `migration-plan-invalid`, and `migration-display-state-changed` each send no frame and claim no authority. `credential-v2-application-authority-changed` mutates the record only inside one transaction that first re-verifies the grant. The one mutating local path is subtractive and person-initiated. `CON-975` restates the stop. `N-H-1` bears on what a person sees, not on whether a failure creates an effect. |

---

## Repository assessments

### selfsame SPEC-008 0.4.16-draft (`f519de4`)

Artefact numbers are sequential with no gap or reuse. `REQ-994`, `REQ-995`,
`NFR-918`, `CON-975`, and `ADR-953` each advance their series by one, and
`TEST-1129` through `TEST-1133` follow `TEST-1128`. Every wikilink resolves. I
checked all fifteen distinct targets, including the eleven anchored external
ones, against their files and headings.

The eleven-line version trace verifies line for line. The grammar split is the
right correction. The correlation paragraph is precise about what the
acknowledgement does and does not authenticate. The installed-state clause is
the clearest version in the chain, and the refresh split is correct.

Open against it: `N-H-1`, `N-M-1`, `N-M-2`, `N-M-3`, `N-M-4`, `N-L-1`, `N-L-2`,
`N-L-3`, `N-L-4`, and `N-L-5`.

Two smaller divergences from the coordinated twin are recorded rather than
filed. `REQ-994` omits the 33-octet handle disclaimer that cbcl-bus `REQ-068`
carries, and it omits the stable account from the snapshot list its twin names.
Neither changes an obligation.

### cbcl-bus SPEC-053 0.16.9-draft (`c410c923`)

`REQ-067` states the survivor and replacement ledger exactly, and the eight
replacements are individually named. The GATE-04 restatement holds 42 boxes by
mechanical count, which matches both `TEST-092` and Selfsame `TEST-1133`. Every
box remains open and the amendment states that it closes none. The new transit
box requires maximum sealed records within 69,632 octets and rejection at
69,633.

The parent pointer resolves. `specs/SPEC-053-key-root-identity.md:15` names the
0.16.9 candidate, `DEC-012` sits at `:635`, and the body cites the candidate at
`:688-691`.

The survivor set is `TEST-043` through `TEST-065`, `TEST-068`, and `TEST-070`.
`TEST-088` through `TEST-092` replace `TEST-084` through `TEST-087`. That ledger
agrees with the Selfsame ledger exactly.

Open against it: shares of `N-H-1`, `N-M-1`, `N-M-2`, `N-M-3`, `N-L-1`,
`N-L-3`, and `N-L-5`.

### cbcl-pairing SPEC-001 0.4.7-draft (`83ac3d3`)

Unchanged from the revision 0.4.15 reviewed and found clean. `REQ-029` keeps the
hub as the owner of all three admission decisions and keeps cbcl-pairing as a
closed independent recognizer before private typed-display construction.
`NFR-021` keeps every authenticated byte ahead of the owned allocation and keeps
the verifier verdict-only.

The 33,793-octet arithmetic re-derives exactly, and the alphabet matches pinned
cbcl-rs character for character. `PathAToBDisplay` and `CredentialV2Display`
remain absent from `src/`, so the typed display is still design rather than
code. The surfaces they attach to are real at `src/profile.rs:92-128` and
`src/endpoint.rs:15-177`.

Open against it: a share of `N-L-2`.

### did-crdt (`1f409a4`)

Named as a coordinated baseline and unchanged. `src/service/rendezvous.rs:65`
carries `MAX_SLOT_BYTES = 4096`, `:68` carries a 600-second TTL, and `:79`
carries a 65,536-slot ceiling. The 4,096-octet cap is the disclosed dependency
`CON-975` records. The working tree carries user-owned uncommitted paths, which
I ignored in favour of the declared commit.

---

## Attack surfaces examined and found sound

These are recorded so a later reviewer does not repeat the work.

- **Forged, replayed, and swapped acknowledgement.** A copied frame carrying the
  live value starts only the reversible transport wait. It creates no hub state,
  because [[spec-008-0.4.4-standalone-tofu-first-contact#CON-939]] places scope
  and pending-ceremony creation inside the earlier signed command. The later
  signed core carries `requestId`, so a swapped genuine success fails the
  browser's own request check before consent.
- **Correlator disclosure through the sealed offer.** `requestId` becomes visible
  to whoever opens the offer, and that happens after the map entry is consumed.
  Replay is closed by consumption rather than by secrecy.
- **Suppressed acknowledgement.** A withheld frame leaves the browser waiting,
  which denies service and allocates nothing.
- **Silent profile re-pin.** A refreshed profile can change permissions and relay
  descriptors under an unchanged authority and issuer. It cannot widen consent,
  because `REQ-981` keys the policy on the exact application and relay pair. It
  cannot re-scope the installed grant, which pins its own permissions.
- **Bare `@` and non-ASCII rooms.** `RoomShape` requires one symbol character
  after `@`, so a one-octet room fails the shape and is
  `MigrationRoomUnrepresentable`. `CbclSymbolChar` is ASCII-only, so any
  multibyte value fails the same class.
- **Shipped `CHANNEL` narrower than the deployed parser.**
  `apps/cbcl_chat/priv/web/path-b-ceremony.mjs:118` reads
  `/^@[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/`, which refuses most of the deployed
  alphabet. Both candidates require its replacement from the generated grammar.
- **`invalid-grant-observation` misuse.** The shipped reducer burns at
  `path-b-ceremony.mjs:195` and returns state unchanged for `Burned` at `:172`.
  Both candidates forbid that reason for a grammar failure.
- **`EnrolmentAllocator::new_native` reachability.** The function is real at
  `crates/selfsame-web-device/src/lib.rs:1318`, and its only JavaScript-exposed
  entry is the constructor at `:1419-1431`. The prohibition names a real symbol.
- **`index_read` lock granularity.** Verified against the local OTP 28.5
  install rather than prose. `ixrlock/5` at `mnesia_locker.erl:1086-1101`
  requests a whole-table read lock through `need_lock(Store, Tab, ?ALL, read)`.
- **`cbcl-sealedgrant` against `redeem-and-grant`.** `put-tx` takes an exact
  `cbcl-invite` read lock and writes only `cbcl-sealedgrant`, so it closes no
  cycle. Recorded as a scope question at `N-L-5` rather than a safety defect.
- **Transit arithmetic.** Sealing operates on opaque octets, and the browser sends
  them as `application/octet-stream` at `apps/cbcl_chat/priv/web/app.js:2112-2117`.
  No base64 expansion applies, so the 62,000-octet member fits 69,632.
- **cbcl-pairing carriers.** `MAX_FRAME_BODY_BYTES = 69_632` and
  `MAX_ADJACENT_BODY_OCTETS = 65_536` both sit above every declared maximum. Only
  the rendezvous lane is undersized.

---

## Residual assumptions and still-open gates, separated from defects

None of these is a defect in this candidate set. Each is a dependency a later
gate owns.

1. **[[PROTO-002-selfsame-rendezvous-v1]] is entirely ungated,** and
   credential/v2 transport depends on it. Both candidates now state that
   dependency, which is the 0.4.15 `M-3` closure.
2. **The whole credential/v2 lane is planned rather than shipped.** Neither
   `selfsamepairallocate` nor any of the new state names exists in `apps/` or
   `crates/`. I searched for each. That is honest planned work.
3. **All 42 GATE-04 boxes stay open,** together with `GATE-00`, `GATE-01`, and
   `GATE-03`. So do every human review, hub off-machine readiness, did-crdt
   durable readiness, and the separate owner approval for production
   enablement.
4. **`DEC-012` still defers the Path-A to Path-B migration,** and the parent
   specification records that deferral as in force.
5. **Browser account identity remains a human comparison,** owned by `CON-221`
   and 0.4.2 `REQ-926`.
6. **The unary `mnesia:transaction` call sites in `apps/` remain unbounded
   today.** `NFR-918` mandates a change rather than describing current
   behaviour, and that change surfaces `authority-busy` on writes that
   previously blocked.
7. **The typed cbcl-pairing display is a design.** Its two named types are
   absent from `src/`, which both the amendment and the prior review record.

---

## What this verdict authorizes and does not authorize

This is a REJECT, so it authorizes nothing at all.

The scope of a future PASS on this set stays exactly as the candidates state it.
Such a PASS authorizes only the Elephant SPL and the test-first implementation
plan. It authorizes no code, no public allocation, no gate opening, no release,
no deployment, and no production enablement. Every GATE-04, `GATE-00`,
`GATE-01`, and `GATE-03` box stays open. Every human review, every durability
obligation, and the separate owner approval for production deployment also stay
open and unaffected.

This review changed no candidate specification, no source, no test, no fixture,
no generated file, and nothing in any sibling repository. It added exactly one
file, which is this report.

## Corrections and owners, collected

| ID | Owner | Minimum correction |
|---|---|---|
| N-H-1 | selfsame, cbcl-bus | Replace the double-negated sentence with the cbcl-bus form, and test a maximum plan through every filter, paging, collapse, and lazy-render path. |
| N-M-1 | selfsame, cbcl-bus | Add the four `ceremony.rs` `payloadVersion` sites and name that constant in every scan obligation. |
| N-M-2 | selfsame, cbcl-bus | Restore the acquisition point, the `revoke-handle-tx` sentence, and `upgrade` in the non-conformance list. |
| N-M-3 | selfsame, cbcl-bus | Restore the legend binding `G`, `F`, `XR`, `OR`, `ME`, `PC`, and `AR` to their `NFR-913` rows. |
| N-M-4 | selfsame | Point the parent specification at the current amendment, and name each parent pointer's repository in `TEST-1133`. |
| N-L-1 | selfsame, cbcl-bus | Define `socketGeneration` and state the reconnect outcome. |
| N-L-2 | cbcl-pairing, selfsame | Carry the `RoomShape` split into `REQ-029`, or record the presentation difference. |
| N-L-3 | selfsame, cbcl-bus | Name `crates/selfsame-rendezvous` beside did-crdt in the dependency ledger and both transit tests. |
| N-L-4 | selfsame | Derive the sealed offer and reverse maxima and their residuals against 69,632 octets. |
| N-L-5 | cbcl-bus, selfsame | State whether the migration graph covers every `cbcl-invite` holder. |
