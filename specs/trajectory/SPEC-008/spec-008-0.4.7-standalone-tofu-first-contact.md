---
id: SPEC-008-amendment-0.4.7
title: SPEC-008 0.4.7-draft — Legacy-member guard and authority durability boundary
status: draft
version: 0.4.7-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-007-cbcl-pairing-cutover#REQ-804]]; [[SPEC-007-cbcl-pairing-cutover#REQ-805]]; [[SPEC-007-cbcl-pairing-cutover#REQ-810]]; [[SPEC-007-cbcl-pairing-cutover#REQ-812]]; [[SPEC-007-cbcl-pairing-cutover#REQ-813]]; [[SPEC-007-cbcl-pairing-cutover#NFR-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; [[SPEC-007-cbcl-pairing-cutover#CON-802]]; [[SPEC-007-cbcl-pairing-cutover#CON-806]]; [[SPEC-004-application-scoped-identity#CON-201]]; [[SPEC-004-application-scoped-identity#CON-204]]; [[SPEC-004-application-scoped-identity#CON-206]]; [[SPEC-004-application-scoped-identity#CON-209]]; [[SPEC-004-application-scoped-identity#CON-211]]; [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-004-application-scoped-identity#CON-220]]; [[SPEC-004-application-scoped-identity#CON-221]]; [[SPEC-004-application-scoped-identity#CON-226]]; did-crdt HTTP and sync service contracts; cbcl-pairing SPEC-001 through coordinated 0.3.0, 0.4.0, 0.4.1, and 0.4.2 drafts"
conforms-to: "[[SPEC-004-application-scoped-identity#REQ-215]]; [[SPEC-002-visual-key-fingerprint#REQ-103]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-502]]; [[PROTO-004-selfsame-ceremony-envelope-v1#CON-503]]; cbcl-bus SPEC-075 REQ-007 unchanged; cbcl-bus SPEC-053 REQ-033 unchanged"
supersedes: "[[spec-008-0.4.6-standalone-tofu-first-contact]]"
adopts: "All 0.4.4, 0.4.5, and 0.4.6 clauses except the exact replacements and corrections named by this amendment"
review-inputs: "[[spec-008-0.4.6-claude-adversarial-review-2026-08-23#Verdict: REJECT]]; cbcl-pairing SPEC-001 0.3.0 at 7ddea48, 0.4.0 at 47424b7, 0.4.1 at 9e8ba1b, and 0.4.2 draft"
implementation-baselines: "selfsame c6af6df75d033d2e44ae367457c1a754ba2c4c3a; cbcl-pairing 8e55160a3234127bd60ce723fd93ba6b2b67a06b; cbcl-bus fdbe4142261923f48cced037a7f609e48a9fdd5a; did-crdt 1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1"
last-updated: 2026-08-23
---

# SPEC-008 0.4.7-draft — Legacy-member guard and authority durability boundary

> **Design target, not implementation authority.** The fresh Claude/Opus Circus
> review rejected 0.4.6 with zero critical, one high, three medium, and seven low
> findings. It passed cbcl-pairing 0.4.1 with one low. This amendment closes the
> Selfsame findings and adopts the small cbcl-pairing 0.4.2 correction. No source,
> schema, fixture, vector, release, allocation, or deployment change is authorized
> until another fresh-context Claude-driver Circus review returns PASS.

## Effective candidate

The effective candidate is the adopted 0.4.4, 0.4.5, and 0.4.6 Selfsame set,
except where this document replaces a clause, plus cbcl-pairing SPEC-001 0.3.0,
0.4.0, 0.4.1, and 0.4.2. The standalone outcome and exact-pair TOFU boundary do
not change.

## Disposition of the 0.4.6 review

| Finding | 0.4.7 disposition |
|---|---|
| **H-1 — Path-A member guard and room tables absent** | [[spec-008-0.4.7-standalone-tofu-first-contact#REQ-967]] and [[spec-008-0.4.7-standalone-tofu-first-contact#CON-956]] add `LegacyMemberRequiresConfirmedUpgrade` before preview derivation, enumerate both membership tables, and reserve their only migration writer to the separately confirmed SPEC-053 REQ-033 increment. |
| **M-1 — `descriptorDigest` preimage undefined** | [[spec-008-0.4.7-standalone-tofu-first-contact#REQ-968]] defines exact RFC-8785 descriptor bytes, SHA-256/base64url encoding, corpus cases, and independent recomputation. |
| **M-2 — v1-to-v2 runbook unnecessarily deletes v1 tombstone** | [[spec-008-0.4.7-standalone-tofu-first-contact#REQ-969]] removes `reinstate/2`; the distinct v1 tombstone remains as the durable withdrawal record. |
| **M-3 — hub authority has no off-machine durability** | [[spec-008-0.4.7-standalone-tofu-first-contact#NFR-909]] records the actual fail-open/fail-closed consequences and makes off-machine backup/restore a production-allocation hold. |
| **L-1/L-2/L-3 — v1 descriptor ranges, branch attribution, legacy selection** | [[spec-008-0.4.7-standalone-tofu-first-contact#CON-957]] pins the exact shared descriptor table, deterministic refusal precedence/tokens, and recognition-without-selection consequence. |
| **L-4 — control CDDL exceeds CON-015 cap** | cbcl-pairing SPEC-001 0.4.2 CON-019 makes `1..2048` normative and companion TEST-051 replaces the 4,096 witness. |
| **L-5 — non-crash restore path ambiguous** | [[spec-008-0.4.7-standalone-tofu-first-contact#CON-958]] allows the running purge operation to write its requested root after verified marker removal; only crash recovery requires a later retry. |
| **L-6 — deployment generator cannot mint rendezvous fixture** | [[spec-008-0.4.7-standalone-tofu-first-contact#CON-957]] assigns rendezvous-v1 only to the Selfsame corpus; the cbcl-bus generator keeps refusing it. |
| **L-7 — parent zero-production premise is stale** | [[spec-008-0.4.7-standalone-tofu-first-contact#CON-958]] retires that premise and pins the already-published live bytes as compatibility surface. |

## Requirements

### REQ-967 — A legacy Path-A member is a pre-preview terminal

This requirement replaces the account-authority state list in 0.4.4 REQ-946 and
CON-941 and supplements 0.4.6 CON-953. Immediately after authenticated offer
recognition and before `DisplayAuthenticatedIntent`, preliminary approval,
`preview_issuer`, custody access, hierarchy derivation, signature, publication,
or durable identity state, the browser calls `selfsamepairauthority(ceremonyId)`.

The correlated signed reply is exactly one of:

```text
Unknown
NoBinding
Bound(accountDid)
LegacyMemberRequiresConfirmedUpgrade(handle, legacyKeyDigest, roomSetDigest)
```

The hub returns the legacy state when the handle has a `cbcl-member` row and no
`cbcl-account-binding` row. It computes `legacyKeyDigest` from the exact stored
key and `roomSetDigest` from the deterministic-CBOR sorted complete legacy room
membership rows. The browser treats it as a distinct terminal, displays that
the chat account needs a separately confirmed account upgrade, closes the v2
ceremony, and performs zero wallet custody or identity effects.

`Unknown` also terminates pre-preview. `NoBinding` and `Bound(accountDid)` may be
held through preliminary preview; only then does the browser map the latter to
`BoundSameDid` or `BoundDifferentDid`. A claimant or caller cannot supply the
legacy classification.

Credential/v2 SHALL NOT bypass, silently perform, or partially reproduce the
Path-A-to-B migration. cbcl-bus SPEC-053 REQ-033 remains the sole authority: a
separate reviewed surface must name the account DID, display its fingerprint,
enumerate every affected channel, record explicit person confirmation, and call
`confirmed-migrate-path-a-handle/3`. That function has no production caller at
the reviewed baseline. Until a separately approved production surface or an
owner-run remote-console procedure proves the same confirmation evidence and
atomic room migration, legacy members remain deliberately v2-ineligible. This
is a production eligibility hold, not an invitation to write the binding row.

### REQ-968 — Selected relay descriptor hashing has one exact preimage

For cutover-v1 and credential-v2 `cbclPairingRelays`, canonical descriptor bytes
are the RFC-8785 serialization of the complete selected descriptor object with
exactly these six members and values:

```text
operatorId
relayOrigin
conformanceEvidenceDigest
privacyPolicyDigest
priority
weight
```

`descriptorDigest = BASE64URL-NO-PAD(SHA-256(canonical descriptor bytes))`.
Both browser and wallet select the descriptor from their independently
recognized profile, recompute those exact bytes and digest, and compare it to
the offer core, provider hint, and hub-signed evidence before display. Copying a
peer or hub digest without recomputation is non-conforming.

The CON-226 `credential_v2` group SHALL contain the complete preimage, canonical
bytes, digest, member deletion/addition/mutation cases, reordered input JSON that
canonicalizes identically, and independently reproduced output. The parent
rendezvous descriptor digest remains scoped to rendezvous-v1 and is not reused.

### REQ-969 — V1 withdrawal remains after a v2 transition

This requirement replaces the v1-to-v2 operator workflow in 0.4.6 REQ-965. For
an actual v1 reciprocal binding, the operator identifies the exact account and
v1 home DID and runs `withdraw/2`. That transaction deletes the v1 alias/account
binding and writes a tombstone keyed by the v1 alias URI.

Because credential/v2 derives a different home DID and alias URI, the v1
tombstone does not block the v2 alias check. The operator SHALL NOT run
`reinstate/2`; the v1 tombstone remains in force as the durable record of the
withdrawn v1 generation. After verifying the account projection is unbound, a
fresh v2 ceremony may proceed. Same-DID mistaken-withdrawal repair remains the
separate operator-only REQ-958 case and does not apply to this transition.

This workflow does not override REQ-967. A handle with legacy Path-A membership
must first pass the separately confirmed SPEC-053 REQ-033 upgrade; v1 reciprocal
binding state and legacy chat membership state are independent eligibility
classes.

### NFR-909 — Hub authority durability is an explicit production hold

At the reviewed deployment, every CON-953 table is a single-node Mnesia
`disc_copies` table under `/data/mnesia`. It survives an ordinary redeploy that
retains the Fly volume but has no off-machine replica. Litestream protects the
SQLite stores only. Loss of the machine/volume therefore:

- loses withdrawal tombstones and is fail-open for previously withdrawn alias
  generations;
- loses pending compensation and makes it unrecoverable; and
- loses account/binding/grant authority and is fail-closed for existing grants.

No clause may call that state permanent or disaster-recoverable. Production v2
allocation SHALL remain disabled until an owner-approved off-machine backup or
replica covers every `cbcl-webfinger-*`, `cbcl-selfsame-*`,
`cbcl-account-binding`, `cbcl-device-grant`, `cbcl-member`,
`cbcl-roommember`, `cbcl-account-roommember`, and `cbcl-pairaccount-grant` table;
defines an authenticated, encrypted, versioned snapshot and retention policy;
and passes a machine-loss restore drill that reproduces exact row bytes,
indexes, tombstones, pending compensation, schema version, and authority
queries before service readiness.

Local development and isolated staging may exercise v2 with allocation visibly
marked non-production. The production gate continuously reports
`hub-authority-off-machine-ready`; false blocks allocation and offer preparation.
This is independent of did-crdt `durable-ready`; both must be true.

## Contracts

### CON-956 — Legacy membership is guarded and outside v2 writes

The complete table disposition in 0.4.6 CON-953 gains:

| Table | Actual key | Credential/v2 treatment |
|---|---|---|
| `cbcl-roommember` | existing legacy room-membership composite key | read only for REQ-967 classification/digest; no v2 write or rollback |
| `cbcl-account-roommember` | `(Room, AccountDID, Handle)` | read only for conflict/classification; no v2 write or rollback |

`selfsamepairauthority` acquires the handle's `cbcl-account-binding` and
`cbcl-member` write locks before classification. For the legacy answer it also
takes the existing whole-table read locks needed to produce a complete stable
room set while membership writes serialize. It signs the classification and
digests into the ceremony reply. Allocation preflight classifies actual rows;
an unclassified, corrupt, key-mismatched, partially migrated, or mixed
representation is `Unknown`, never `NoBinding`.

No generic v2 command writes either room table. The only sanctioned writer that
moves legacy rows is `confirmed-migrate-path-a-handle/3`, which requires literal
`confirmed`, takes whole-table write locks on both membership tables, validates
the legacy enrolled key and all target absences, writes every account row,
deletes every legacy row, and commits the account binding in its one transaction.
Its post-commit `account-linked` notices remain required. A future production
caller is a separate SPEC-053 REQ-033 increment and is not created implicitly by
this pairing design.

CON-953's exact-key write locks are a minimum, not the complete Mnesia lock set.
Existing device-cap and pairaccount-count checks may take index or table-scope
read locks; migration uses the whole-table locks above. Implementations use one
documented lock order and treat Mnesia deadlock/abort as a closed terminal or
idempotent retry before effects, never as permission to skip a check.

### CON-957 — Exact descriptor grammar and deterministic v1 dispatch

The cutover-v1 and credential-v2 `cbclPairingRelays` descriptor grammar is the
same exact 0.4.4 CON-933 table: `operatorId` is 1..63 ASCII octets matching
`[a-z0-9][a-z0-9-]{0,62}`; `relayOrigin` is canonical HTTPS origin;
`conformanceEvidenceDigest` and `privacyPolicyDigest` are canonical
base64url-no-pad encodings of 32-octet SHA-256 values; `priority` is uint
0..65,535; `weight` is uint 1..65,535. Unknown/duplicate members, duplicate
operators/origins, invalid digests/origins, and out-of-range values refuse.
REQ-968 defines the digest of the selected complete object.

After closed JSON parse obtains `profileVersion = 1`, branch attribution uses
only top-level relay-family presence, with this precedence:

1. both `rendezvous` and `cbclPairingRelays` present →
   `ProfileV1MixedRelayFamilies`;
2. neither present → `ProfileV1MissingRelayFamily`;
3. only `rendezvous` present → rendezvous-v1 complete grammar;
4. only `cbclPairingRelays` present → cutover-v1 complete grammar.

`pairingRecordRelays` in cutover-v1 returns
`ProfileV1PairingRecordRelaysForbidden`. After branch attribution, missing
common required members are reported in CON-201's fixed member order, then
unknown members in Unicode code-point order, then nested value errors in
depth-first member/code-point order. The CON-226 corpus pins every token/path and
the both/neither/forbidden shapes so two stacks agree on which predicate fired.

A completely recognized rendezvous-v1 profile provides no
`cbclPairingRelays`; under [[SPEC-007-cbcl-pairing-cutover#REQ-806]] and
[[SPEC-007-cbcl-pairing-cutover#REQ-807]] it cannot allocate or select any
pairing engine. Recognition exists only for profile compatibility and never
advertises, negotiates, selects, or falls back to the legacy rendezvous or
pairing protocols.

Fixture ownership is separated. The Selfsame CON-226 recognizer corpus owns the
rendezvous-v1 positive fixture. The cbcl-bus deployment generator owns only the
frozen cutover-v1 and distinct credential/v2 outputs and SHALL continue to
reject every object containing `rendezvous` under SPEC-075 REQ-007.

### CON-958 — Root completion, published-v1 premise, and correction ledger

For a non-crashing forget/restore/new-root operation, once the running operation
has durably committed and read-verified `purging`, deleted and verified all old
safety state and the old root, and deleted/read-verified the marker, that same
invocation MAY write/read-verify the requested restored or new root. A crash at
or after marker commit loses the in-memory candidate; startup recovery converges
to no root and never reconstructs one, so the person explicitly retries in a
later invocation. A crash before marker commit leaves the old identity intact.

The parent ADR-224/CON-201 premise that no ratified production profile has ever
been published is retired at this baseline. The 1,235-octet profile at
`https://chat.anuna.io/selfsame/application`, with SHA-256 base64url-no-pad
`asBvYnvPMHVVU8zkFaFoz_FJg1DaZGSHQ5fxKxC8SZk`, was published and recognized on
2026-08-22. CON-951's cutover-v1 branch exists to preserve that compatibility
surface. Future in-place v1 amendments may not rely on a zero-deployment premise
and require their own compatibility proof.

The effective-text ledger additionally replaces 0.4.6 NFR-908's control range
`256..4,096` with `256..2,048`; maximum encoded size remains 4,144. The cbcl-bus
generator/corpus split is CON-957, and 0.4.4's “CON-201 profile version 1 is not
amended” sentence is replaced explicitly by CON-951 plus this published-profile
record.

## Decisions

### ADR-939 — Pairing does not silently upgrade Path-A membership

A legacy handle carries standing room authority. Writing only an account binding
either violates SPEC-053 REQ-033 or makes those rooms unreachable. Standalone
pairing therefore detects and refuses that state before touching the wallet. The
separate confirmed migration remains possible, but it is a distinct authority
increment with its own surface and gate rather than a hidden pairing side effect.

### ADR-940 — Hub volume durability is insufficient for production authority

Single-volume Mnesia is useful redeploy durability, not disaster recovery.
Withdrawal tombstones are fail-open on loss, so accepting machine-loss as an
availability residual would misstate a security boundary. Production allocation
waits for off-machine authority backup/replication and a restore drill; local
implementation and staging remain possible while that hold is visible.

## Test specification

All effective TEST-1011 through TEST-1074 remain release-blocking except where
the rows below replace an expectation. Companion TEST-030 through TEST-051 are
mandatory.

### TEST-1075 — Legacy member refuses before every wallet effect

Create `cbcl-member` plus legacy `cbcl-roommember` rows and no account binding.
Require the exact signed legacy status and deterministic key/room digests before
authenticated-intent display. Assert zero preliminary/final UI approval, zero
custody calls, zero hierarchy derivation, zero signature/publication, zero
resolver/hub identity writes, and no room/account mutation. Corrupt or partially
migrate each representation and require `Unknown`, not `NoBinding`.

### TEST-1076 — Confirmed migration remains the only room-authority writer

Trace every v2 positive, refusal, timeout, rollback, compensation, and retry and
prove neither membership table is written. Invoke
`confirmed-migrate-path-a-handle/3` separately without literal confirmation and
require no change. With a SPEC-053 fixture that names the account DID/fingerprint
and exact rooms and records confirmation, require one atomic migration, binding,
and post-commit notice per live room. No production caller or gate is inferred
from that test fixture.

### TEST-1077 — Descriptor digest preimage is independently reproducible

Two independent stacks RFC-8785 serialize the same complete six-member selected
descriptor and reproduce REQ-968's digest. Reorder input JSON and require the
same canonical bytes. Remove, add, or mutate every member and require a changed
digest or grammar refusal. Copy a hub value without local recomputation and
require the API/type gate to fail before display.

### TEST-1078 — V1 branch failures have deterministic identity

Exercise both/neither relay families, forbidden `pairingRecordRelays`, every
missing common member, multiple unknown members, and nested errors. Require the
exact CON-957 precedence/token/path in both stacks and the CON-226 corpus. A
recognized rendezvous-v1 fixture returns no eligible relay and cannot start any
legacy or cbcl-pairing engine.

### TEST-1079 — V1 withdrawal survives successful v2 transition

Create a v1 reciprocal binding, withdraw it, and assert the v1 tombstone remains.
Complete v2 using the distinct DID/alias and require that tombstone byte-for-byte
unchanged. Calling `reinstate/2` is absent from the transition trace. Same-DID
mistaken-withdrawal repair remains separately tested by TEST-1056.

### TEST-1080 — Hub production allocation fails without off-machine authority

Run the current single-volume/no-replica configuration and require
`hub-authority-off-machine-ready = false`, blocking allocation and offer
preparation. In the future conforming fixture, take an authenticated encrypted
snapshot, destroy the machine and volume, restore onto a fresh node, rebuild and
verify indexes, and require exact bindings, tombstones, pending compensation,
grants, memberships, and authority answers before readiness. Delete each table
class from the snapshot and require restore/gate failure.

### TEST-1081 — Normal root completion and crash recovery are distinct

Run non-crashing forget, restore, and new-root operations and require the same
invocation may write the requested root only after marker removal is verified.
Crash before marker commit and require the old identity intact; crash at every
later boundary and require startup recovery to no root/no safety state/no marker,
with a separate explicit retry needed to write a root.

### TEST-1082 — Control cap and effective correction ledger agree

Run cbcl-pairing TEST-051 and Selfsame envelope witnesses through 2,048. Refuse
2,049 and 4,096 while retaining the 4,144 maximum encoded object. Static checks
prove the deployment generator never owns a rendezvous fixture, the corpus does,
and no effective clause relies on the retired zero-production-profile premise.

## Implementation and gate sequencing

The 0.4.6 repository slices remain, with these additions: the hub authority
query/offer path adds the pre-preview legacy state; pure core adds exact
descriptor hashing and deterministic v1 refusal tokens; cbcl-pairing applies the
2,048 cap; release tooling adds the hub off-machine-readiness production hold.
No production caller for Path-A migration or off-machine backup is invented by
the local E2E implementation.

1. Run fresh-context Claude-driver Circus review over the complete effective set
   and exact four code baselines, re-auditing every 0.4.6 and carried finding.
2. Only after PASS, encode TEST-1011 through TEST-1082 and companion TEST-030
   through TEST-051 as executable SPL Elephant gates.
3. Commit failing schemas/vectors/corpus/pure-core tests before code, then
   effectful wallet/hub/resolver tests before wiring.
4. Validate local real-relay E2E with an eligible no-legacy-member account and
   validate the legacy-member refusal independently.
5. Keep production allocation disabled. Human cryptography, independent CPace,
   hub off-machine authority, Path-A migration surface, owner approvals, privacy,
   rollback, and separate deploy confirmation remain required.

## Changelog

- **0.4.7-draft — 2026-08-23 — review candidate, not approved.** Adds the
  pre-preview legacy-member authority guard and room-table disposition; defines
  descriptor hashing and deterministic v1 refusal; preserves the v1 tombstone
  across v2 transition; makes off-machine hub authority a production hold;
  aligns control CDDL, root recovery, fixture ownership, and published-v1
  compatibility premises. No implementation or deployment authority is claimed.
