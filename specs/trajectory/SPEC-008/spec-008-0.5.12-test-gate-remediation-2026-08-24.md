---
id: spec-008-0.5.12-test-gate-remediation-2026-08-24
title: SPEC-008 0.5.12 — executable interrupted-link gate remediation
status: owner-authorized-test-first-candidate
tier: 1
date: 2026-08-24
parent: "[[SPEC-008-production-pairing-claimant]]"
review-of: "[[spec-008-0.5.11-implementation-claude-adversarial-review-2026-08-24]]"
---

# SPEC-008 0.5.12 — executable interrupted-link gate remediation

## Decision and scope

The repository owner authorized test-first implementation before review PASS.
This reissue closes the sole blocking 0.5.11 finding without changing the
standalone protocol architecture. It does not authorize a push, production
allocation, release, or deployment.

The 0.5.11 fresh-context review found zero CRITICAL and zero HIGH defects and
found no exploit in the shipped predicates. Its one MEDIUM finding was missing
executable verification for [[SPEC-008-production-pairing-claimant#CON-990]]
and [[SPEC-008-production-pairing-claimant#TEST-1162]]. The correction is
therefore a bounded verification increment plus one exact-policy unlink guard
required by TEST-1162's existing text.

## Exact candidate closure

- Selfsame implementation:
  `0dbfb3c4323aec568ac2803d58c0aa71c301edda`
- cbcl-bus source-closure pin:
  `100d5e4b5f8e6e36a61b2b27ffdb13ca61edb335`
- cbcl-pairing:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-rs pin:
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`
- did-crdt pin:
  `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
- browser WASM SHA-256:
  `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`
- required toolchain: rustc/cargo 1.96.0, wasm-bindgen 0.2.126,
  aarch64-apple-darwin

## Executable closure of M-1

The isolated Rust case
`cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot`
now executes the following fifteen post-persist/pre-payload failure boundaries:

1. final approval release;
2. acknowledgement read;
3. acknowledgement recognition;
4. acknowledgement checkpoint replacement;
5. acknowledgement checkpoint commit;
6. plan construction;
7. planned-stage replacement;
8. issuer custody;
9. issuer-stage replacement;
10. issuer publication;
11. resolver verification;
12. grant construction;
13. provisioned-stage replacement;
14. payload construction; and
15. payload checkpoint preparation.

Each case exposes the durable phase appropriate to that boundary, drops the
armed compensation guard, requires the exact slot to be empty, and proves that
a fresh ceremony for the same application can persist. This also models a
backend that commits a replacement immediately before reporting an error.

The same test separately requires:

- a correct `PayloadPrepared` value to survive cleanup for
  [[SPEC-008-production-pairing-claimant#CON-989]] recovery;
- a guard for attempt A to retain attempt B;
- a guard for one root generation to retain a replacement under another root;
- a stale pending guard to retain an installed link;
- confirmed unlink to refuse when the selected slot was replaced; and
- confirmed unlink to refuse and retain the slot when the selected exact-pair
  policy changed.

`unlink_local` now requires the selected
`(applicationId, relayOrigin)` row still to be trusted immediately before its
existing policy-removal and exact-slot deletion sequence. A missing or changed
row returns `PairingCheckpointRefused`; a slot mismatch remains
`PairingApplicationNotLinked`. A failed slot deletion restores the trusted
policy row.

## Mutation evidence

The generating implementation run forced recompilation for each mutation. The
isolated TEST-1162 case failed when each load-bearing predicate was removed:

| Mutated predicate | Required failure |
|---|---|
| retain `PayloadPrepared` | killed |
| compare immutable attempt identity | killed |
| refuse an installed slot in compensation | killed |
| exact compare-and-delete in `unlink_local` | killed |
| require the selected exact-pair policy still to be trusted | killed |

The restored candidate passes the same isolated test. The explicit root-change
case exercises the existing root member of the immutable attempt comparison.

## Continuous gates

Selfsame CI now runs the process-global keyring test in its own process:

```text
cargo test -p selfsame --locked --lib -- --ignored --exact cbcl_v2_completion::tests::test_1162_pre_payload_failure_and_person_abandonment_release_the_exact_slot
```

The browser job now also runs:

```text
npm run e2e:wallet-pairing
```

The generating run passed the isolated Rust case, the ordinary Selfsame library
suite, Clippy with warnings denied, and the five-case wallet browser suite. The
local Rust commands used the explicitly labelled development override because
the person's sibling cbcl-rs checkout is tracked-dirty and the did-crdt checkout
is ahead of its pin; those runs are not release evidence. Separately, the exact
clean pinned sibling closure rebuilt the vendored WASM byte-for-byte at the
digest above.

## Residuals and gate

The 0.5.11 LOW findings remain explicit and nonblocking:

- a mid-chain post-approval failure can leave a non-authoritative orphan DID
  publication under [[SPEC-004-application-scoped-identity#CON-204]]; and
- the anonymous recovery limiter remains TCP-peer-scoped behind the Fly proxy,
  an availability-only production-gate concern.

A new fresh-context, cross-model Tier-1 review SHALL independently test this
increment and re-audit the complete coordinated candidate. PASS requires zero
CRITICAL, HIGH, and MEDIUM findings. Even PASS does not authorize a push,
production allocation, release, or deployment; those remain separately gated
by [[SPEC-008-production-pairing-claimant#CON-985]] and owner approval.
