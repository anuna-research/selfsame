---
id: spec-008-0.5.2-claude-adversarial-review
title: SPEC-008 0.5.2-draft — Tier-1 adversarial review of the coordinated standalone credential/v2 reissue
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 3
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `f6548edcef914545388e55010ba095109c2fed6a` SPEC-008 0.5.2-draft and SPEC-007 0.3.0-draft; cbcl-bus `bd6f3bcd42eb09e944988141d833a493d9cf1222` SPEC-053 0.17.2-draft; cbcl-pairing `724c5d2fc0e868143415ad5e4fb229594487d750` SPEC-001 0.5.1-draft; did-crdt `caecb6c3ebe5a8a10a2fb59bfb2e9701e03cfc43` SPEC-037 0.1.1 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 3. It imported no conclusion from any earlier Claude conversation. It read the 0.5.1 report only after completing an independent first pass over the four current parents and the five pinned code baselines."
date: 2026-08-24
last-updated: 2026-08-24
---

# SPEC-008 0.5.2-draft — Tier-1 adversarial review of the coordinated standalone credential/v2 reissue

Verdict: **REJECT**.

The reissue closes every defect the 0.5.1 review recorded. One carrier ceremony
identifier now governs the whole ceremony. Status recovery has a command. The
rendezvous dependency is gone. The offer core is closed and typed.

Two new CRITICAL defects survive verification. Both share one shape. The direct
reissue carried names forward and left their defining content in trajectory
documents that the same parents deny authority. The affected content is the
credential/v2 cryptographic channel and the credential/v2 application envelope.

## Orientation

Intent: decide whether the four current parents state one self-contained, safe,
bounded, implementable standalone first-contact architecture. The architecture
needs no prior SPEC-004 enrolment and no compiled relay allowlist.

Result: consent ordering, recovery, atomicity, persistence, bounds arithmetic,
and authority singularity now hold. The wire does not. No implementer can build
the credential/v2 channel or its objects from the current parents alone.

Controls:   this review changes no candidate parent, no code, and no sibling
            repository file
            this review commits only this report
            a REJECT verdict authorizes no implementation, allocation,
            release, or deployment
Open:       the corrections in
            [[spec-008-0.5.2-claude-adversarial-review-2026-08-24#Collected corrections and owners]]
            every GATE-04 box (owner: named gate owners)
Detail:     [[spec-008-0.5.2-claude-adversarial-review-2026-08-24#C-1 The credential/v2 key schedule exists in no current parent]]
            [[spec-008-0.5.2-claude-adversarial-review-2026-08-24#C-2 Nine of eleven logical body grammars exist in no current parent]]
            [[spec-008-0.5.2-claude-adversarial-review-2026-08-24#H-1 The pre-socket CON-220 gate loses its binding step and names no substitute]]

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL in this document are to be interpreted as
described in BCP 14 (RFC 2119, RFC 8174) when, and only when, they appear in
all capitals.

## Reviewer identity, authentication, and independence

The generating family is OpenAI GPT-5. This reviewer is Anthropic Claude,
runtime model `claude-opus-5`. The families differ, so the cross-model
condition holds.

`ANTHROPIC_API_KEY` was absent from the environment. The reviewer tested
parameter-set presence and counted exact environment matches. Both returned
absence. The reviewer printed no value and recorded no derivative.

The command `env -u ANTHROPIC_API_KEY claude auth status --json` returned these
three non-identifying fields:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

The three values match the gate exactly, so the review proceeded. This report
records no email, organization identifier, account identifier, access token, or
API key. It records no redacted derivative of one.

Driver SHA-256: `4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

Circus attempt: `spec008-consolidated-review` attempt 3, worktree
`/Users/anuna-01/Code/selfsame-spec008-consolidated-review-3`, branch
`circus/spec008-consolidated-review/3`.

Method: the reviewer read the four current parents and the five pinned code
baselines first. The reviewer opened the 0.5.1 report only after that pass
finished. Every prior disposition below is re-derived from candidate text and
pinned code, never adopted.

## Exact review set, baselines, and pre-implementation status

The reviewer confirmed each object with `git cat-file -t` before reading. Every
candidate artefact resolves.

| Repository | Candidate commit | Parent and version | Size |
|---|---|---|---|
| selfsame | `f6548edcef914545388e55010ba095109c2fed6a` | SPEC-008 0.5.2-draft | 1,575 lines |
| selfsame | `f6548edcef914545388e55010ba095109c2fed6a` | SPEC-007 0.3.0-draft | 1,278 lines |
| cbcl-bus | `bd6f3bcd42eb09e944988141d833a493d9cf1222` | SPEC-053 0.17.2-draft | 9,964 lines |
| cbcl-pairing | `724c5d2fc0e868143415ad5e4fb229594487d750` | SPEC-001 0.5.1-draft | 2,176 lines |
| did-crdt | `caecb6c3ebe5a8a10a2fb59bfb2e9701e03cfc43` | SPEC-037 0.1.1 deprecated | 79 lines |

The reviewer read every sibling artefact through `git show COMMIT:path`. No
working-tree substitute entered this review. The reviewer modified and cleaned
nothing outside this report.

The five implementation baselines resolve as commits. They are selfsame
`0220cec2dec44cd95d4f411ea4814d790b6716d2`, cbcl-bus
`ce894a7f2debc111b610296b1a3780de6084e37e`, cbcl-pairing
`c22f2d0526432c01c56d64be35a8d4f87516ec92`, did-crdt
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`, and the `anuna-ssi` namespace pin
`c7d462029841ea1884bb6f089732058d8838728d`.

The code is intentionally pre-implementation. No `selfsamepair` command,
`cbcl-selfsame-v2` table, `credential-v2` type, `CredentialV2` symbol, or
`PAIR1` recogniser exists at any baseline. This report separates a correctly
specified future change from a false claim about present code. It rejects no
finding for absent candidate code.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 2 | yes |
| HIGH | 2 | yes |
| MEDIUM | 3 | three break a live path, a declared recovery boundary, or a parser agreement |
| LOW | 4 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Seven blocking findings
survive, so the verdict is REJECT.

## Findings

### C-1 The credential/v2 key schedule exists in no current parent

Severity: CRITICAL. Owner: cbcl-pairing repository owner.

cbcl-pairing SPEC-001 0.5.1-draft opens its credential/v2 section with a
completeness claim. Line 1466 states that the section states the complete
current credential/v2 protocol directly. Line 1467 states that trajectory
documents supply synthesis evidence and no protocol authority.

CON-027 then names a structure it never defines. Line 1691 states that the
transcript hash is SHA-512 over deterministic CBOR containing public context
and both CPace frames.

No `credential-v2-public-context` rule exists in that parent. A repository-wide
identifier search at `724c5d2f` returns exactly two hits, both inside
`specs/trajectory/SPEC-001/spec-001-0.4.0-credential-v2-endpoint-profile.md`,
at lines 225 and 226.

The pinned normative asset does not carry it either.
`schemas/pairing-v1.cddl` at that commit hashes to
`c8f7e57a1a944dd999ebeeb20260368d3315ade26748fbab8361de2643240fc9`, which
equals the value SPEC-001 declares. That file contains zero `credential-v2`
rules and defines `pairing-public-context` for version 1 only, at line 98.

Version 1 cannot substitute. SPEC-001 REQ-031 at line 1711 states that no v1
context, label, key, Finished value, or AAD authenticates a v2 frame. The v1
rule also lacks the profile digest, carrier digest, ceremony, and commitment
members that CON-027 requires.

The declared `credential-v2-ad` rule is incomplete in the same way. Lines 1675
to 1680 give five members. The parent names only the first two. Its fourth and
fifth 32-octet members carry no name, type source, or value rule.

The surrounding prose contradicts that shape. Line 1687 states that the public
context fixes an expected allocator key and an absent claimant key. The rule
offers one nullable 32-octet slot and two mandatory ones, so an absent claimant
key has no representation.

Five further constructions are absent. The parent states no HKDF extraction
input and no `label || TH` info form. It states no per-label output length, no
Finished MAC algorithm, and no nonce derivation. Line 1709 names the AAD fields
and states no map, key names, or order.

The trajectory document holds every missing value. Lines 225 to 260 of
`spec-001-0.4.0-credential-v2-endpoint-profile.md` carry six constructions.

They are the public-context rule with its member tail, the inline `ci` and `ad`
member names, and this transcript hash:

```text
TH = SHA-512(deterministic-CBOR([
  bstr(publicContext), bstr(allocatorCpaceFrame), bstr(claimantCpaceFrame)
]))
```

They also carry `PRK = HKDF-Extract-SHA512("", ISK)`. They carry seven
`HKDF-Expand` forms with 32, 32, 32, 32, 12, 12, and 32-octet outputs.

They carry both `HMAC-SHA-512` Finished values. They carry the AAD map
`{ "v": 2, "direction": 0..1, "counter": uint, "th": bstr .size 64 }`.

That document carries no authority. The current parent denies it at line 1467,
and no clause re-adopts it.

The consequence reaches three parents. The transcript hash is a signed
`OfferCoreV2` member at cbcl-bus SPEC-053 line 6950, and line 6967 requires it
to decode to 64 octets. cbcl-bus REQ-082 at line 6666 requires the browser to
send it. Selfsame [[SPEC-008-production-pairing-claimant#REQ-1006]] requires
the verifier to authenticate the transcript.

Two independent implementations cannot agree on that value. They therefore
cannot agree on any traffic key, either Finished value, any AEAD frame, or the
signed offer. cbcl-pairing TEST-061 requires exactly that agreement, so the
test is undecidable.

This defect has the same shape as the 0.5.1 C-1 defect. The reissue carried a
name across and left the content behind in trajectory. This instance sits in
the cryptographic core rather than in a test list.

### C-2 Nine of eleven logical body grammars exist in no current parent

Severity: CRITICAL. Owner: selfsame repository owner, with the cbcl-pairing
owner for the shared content hash.

cbcl-pairing CON-028 declares eleven credential/v2 object kinds at line 1738.
They are `offer`, `intent-approve`, `intent-decline`, `preparation`,
`comparison-confirmed`, `binding-confirmed`, `refusal`, `final-approve`,
`final-decline`, `payload`, and `receipt`.

Lines 1773 and 1774 require every successor logical body to carry the exact
prior object content hash. The parent never defines an object content hash. The identifier
appears twice, at lines 1774 and 1956, and carries no construction either time.

The trajectory document defines it. CON-014 of
`spec-001-0.4.0-credential-v2-endpoint-profile.md` states that every successor
body except offer carries a raw `predecessorDigest`. That value is the SHA-256
of the canonical `credential-v2-object` bytes for the unique prior object.

The same trajectory contract assigns body-grammar ownership. It states that the
shared layer owns envelope, size, padding, field-2 equality, object content
hash, and predecessor equality. It states that the Selfsame profile owns each
closed logical body grammar.

The Selfsame parent supplies none of them. SPEC-008 0.5.2-draft contains
exactly two fenced blocks, at lines 39 and 674. The first is the orientation
diagram. The second is the PAIR1 checksum input.

The identifiers show the gap. The strings `logical body` and `predecessor`
appear zero times in SPEC-008. cbcl-bus SPEC-053 uses `logical` once, at line
7073, only as a bound assertion.

Only two of eleven bodies carry a grammar. cbcl-bus CON-012 gives
`signed-offer-v2` for the offer at line 7018. cbcl-pairing CON-028 gives
`credential-v2-receipt-body` for the receipt at line 1746.

Nine bodies carry none. They are `intent-approve`, `intent-decline`,
`preparation`, `comparison-confirmed`, `binding-confirmed`, `refusal`,
`final-approve`, `final-decline`, and `payload`.

The reverse payload has no usable pointer either. SPEC-007 REQ-814 bounds the
canonical CON-219 bundle payload at 62,000 octets. SPEC-004 CON-219 at line
3931 is a credential/v1 contract. Its object carries `"payloadVersion": 1` and
`ceremonyId`, and PROTO-004 CON-502 seals it.

Selfsame [[SPEC-008-production-pairing-claimant#REQ-1005]] forbids that reuse.
It states that credential/v2 reaches no classified credential-v1 site, and its
closed identifier set contains `payloadVersion`.

The prose descriptions do not close the gap. CON-986 states which values the
preparation and payload bodies carry. It states no encoding, member order,
member names, bound, or canonicalization for any of them.

The affected surfaces are the consent inputs and the identity payload. The
wallet parses `CredentialV2IntentInput` from a peer body, and cbcl-pairing
CON-029 requires byte equality between every overlapping peer value and the
separate authority input. No grammar defines the peer value set.

### H-1 The pre-socket CON-220 gate loses its binding step and names no substitute

Severity: HIGH. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#REQ-906]] at line 186 makes live CON-220
authentication the pre-socket gate. [[SPEC-008-production-pairing-claimant#CON-986]]
at line 1083 repeats it for `recognise_claimant_invitation`. The exact-pair TOFU
prompt and the durable CON-903 write both depend on that result.

SPEC-004 CON-220 states six ordered obligations. Step 6, at line 4136, requires
the profile's RFC 8785 SHA-256 digest to equal the `profileDigest` in the
[[PROTO-003-selfsame-pairing-v1#CON-409]] record for this ceremony.

SPEC-004 names that step load-bearing. Line 4138 states that step 6 makes the
fetch trustworthy rather than merely encrypted. Lines 4138 to 4140 state that
TLS authenticates the origin and the record digest pins which profile that
origin served.

Credential/v2 supplies no such record. SPEC-007 already deprecates CON-401
through CON-409 for Selfsame, at PROTO-003 lines 22 to 26.
[[SPEC-008-production-pairing-claimant#CON-986]] removes every rendezvous edge,
so no CON-409 record exists at any point.

The carrier cannot substitute. cbcl-pairing CON-027 lists eleven carrier
members and no profile digest. The wallet reaches the pre-socket gate holding
only its own live fetch.

CON-220's own deferral path also fails. Its tier-3 paragraph at line 4144
permits steps 1 to 5 first and then requires step 6 against a resolved record.
The credential/v2 path never resolves a record.

The pinned code makes the split explicit and treats the halves as jointly
necessary. `crates/selfsame-app-identity-net/src/profile.rs:9` to `:21` state
that TLS and the digest each authenticate one half and that neither suffices
alone. Lines 64 to 68 implement steps 1 to 5 and leave step 6 to the caller.

A substitute exists in the design and appears in no clause. cbcl-pairing
CON-027 places the profile digest inside the CPace channel identifier.

Line 1662 states that both peers authenticate their independently recognised
profile and this digest. That binding lands after CPace, so it lands after the
socket and after the CON-903 policy write.

Two implementers therefore diverge. One refuses at the unsatisfiable step and
the feature never runs. The other drops the step and ships a different security
posture. The parents leave that decision to the implementer.

SPEC-004 sits outside the coordinated review set.
[[SPEC-008-production-pairing-claimant#CON-985]] names four parents and
excludes SPEC-004, so no revision repairs CON-220 in this increment.

### H-2 A signed offer-core member has no defined input or canonicalization

Severity: HIGH. Owner: cbcl-bus repository owner, with the selfsame owner for
the descriptor grammar.

cbcl-bus CON-012 lists `descriptorDigest` as one of twenty mandatory
`OfferCoreV2` members, at line 6947. Line 6965 states only that it uses
canonical base64url for raw SHA-256 output.

No parent states what it digests. SPEC-004 defines `descriptorDigest` twice, at
lines 3285 and 3622, and both definitions cover a rendezvous provider
descriptor selected by `providerId`. Credential/v2 has no rendezvous provider.

SPEC-007 CON-806 defines the cbcl relay descriptor and stops short. It names
`operatorId`, `relayOrigin`, `priority`, `weight`, `privacyPolicyDigest`, and
`conformanceEvidenceDigest`. It states no digest rule for that object.

The member is load-bearing. cbcl-bus CON-012 line 7002 assigns it to the
exact-pair and carrier verifier. REQ-082 line 6677 requires the browser to
compare every descriptor value it knows before proceeding. The signed offer
covers it, so a mismatch refuses the ceremony.

The pinned Rust computes one candidate value. `crates/selfsame-app-identity/src/profile.rs:880`
sets `digest: sha256(&json::canonicalise(item))` for each descriptor. The hub
is LFE and the browser is JavaScript, so a code-only definition binds neither.

Descriptor selection is undefined as well. SPEC-007 CON-806 permits one to
sixteen descriptors per profile. No clause states which descriptor the digest
covers when a profile lists several at one relay origin.

### M-1 A coordinated safety parent contradicts the offer-core source rule

Severity: MEDIUM. Owner: selfsame repository owner.

SPEC-007 CON-806 carries one version-qualified credential/v2 paragraph. It
states that complete profile recognition makes a declared descriptor eligible
and that no compiled allowlist participates.

The next paragraph carries no version qualifier. It states that the application
identifier, HTTPS origin, and requested scope come from the recognised
credential intent.

[[SPEC-008-production-pairing-claimant#CON-902]] states the opposite for
credential/v2. Its authoritative-source table gives `application_id` as the
authenticated profile, never the wire claim.

cbcl-pairing CON-029 agrees with SPEC-008. It states that the application
identifier and transition come from the authenticated authority input and never
from an unchecked counterpart claim.

An implementer reading SPEC-007 as written takes the peer value. That reading
fails [[SPEC-008-production-pairing-claimant#TEST-1159]], which requires every
display value to originate in authenticated typed input.

Both documents are current Selfsame authority in the same increment. The
paragraph needs an explicit credential/v1 scope.

### M-2 Two parents disagree about the relay-origin bound

Severity: MEDIUM. Owner: cbcl-pairing repository owner, with the cbcl-bus owner.

cbcl-bus CON-012 line 6971 states that `relayOrigin` is a canonical HTTPS
origin of at most 272 ASCII octets. cbcl-pairing CON-027 builds the
credential/v2 carrier from the shared rule `relay-origin`.

That rule caps the value lower. `schemas/pairing-v1.cddl` line 13 declares
`relay-origin = tstr .size (1..255)`, and CON-027 states that the relay origin
satisfies the canonical origin grammar.

Byte equality joins the two. The offer core carries the relay origin, and the
wallet compares it against the carrier value it already recognised. A 256 to
272-octet origin passes one recogniser and fails the other.

The stricter bound is the wrong one. A maximal DNS name occupies 253 octets, so
a legitimate origin with scheme and port reaches 267 octets. The 272-octet cap
accommodates that case and the 255-octet cap refuses it.

The failure direction is closed, because the carrier refuses first. The two
parsers still disagree, and
[[SPEC-008-production-pairing-claimant#TEST-1157]] requires bounds to resolve.

### M-3 Post-final wallet recovery has no path once the ceremony window closes

Severity: MEDIUM. Owner: selfsame repository owner, with the cbcl-bus owner.

[[SPEC-008-production-pairing-claimant#CON-986]] states that crash or loss
before the wallet commit recovers from the authenticated receipt and retained
bounded ceremony state. The receipt crosses only the cbcl-pairing relay.

Three bounds close that window together. cbcl-pairing NFR-023 keeps the v2
mailbox lifetime between 60 and 600 seconds. cbcl-bus REQ-082 sets
`pendingExpiresAt` to the earlier carrier expiry or allocation plus 600
seconds. CON-986 removes the wallet pending value at expiry.

The hub state is already irreversible at that point. cbcl-bus REQ-082 states
that the final transaction deletes the legacy room rows. It also states that a
crash after final commit never rolls back identity or recreates them.

No later ceremony recovers the grant. cbcl-bus REQ-082 permits a fresh request
only after authenticated absent status, and finalized status is not absent.
[[SPEC-008-production-pairing-claimant#REQ-1006]] prohibits a local shortcut
using the retained derived key.

The outcome is an asymmetric terminal. The hub holds a finalized account and
migrated rooms. The wallet holds no installed record and no route to one.

[[SPEC-008-production-pairing-claimant#TEST-1158]] asks for the property this
window denies. It requires the reviewer to lose every response, restart each
component at every boundary, and require exact recovery.

The defect is bounded and creates no duplicate effect. It still leaves a
declared recovery boundary unreachable and a declared test undecidable.

### L-1 Two coordinated parents decompose one claimant differently

Severity: LOW. Owner: selfsame repository owner.

SPEC-007 CON-807 states that the standalone credential/v2 claimant uses four
ordered stages. It names `prepare_claimant`, `preview_claimant_identity`,
`complete_claimant`, and browser and wallet activation.

[[SPEC-008-production-pairing-claimant#CON-986]] states that five split
functions are the only credential/v2 construction path. It adds
`recognise_claimant_invitation` and `authorise_claimant_relay`.

The two lists do not contradict each other. CON-807's preconditions place the
exact-pair policy earlier, so its stage one starts after the SPEC-008 stage
two. The reader still meets two counts for one claimant.

### L-2 A profile grammar is attributed to a SPEC-004 amendment that does not exist

Severity: LOW. Owner: selfsame repository owner.

SPEC-007 CON-806 states that the coordinated SPEC-004 amendment defines
`cbclPairingRelays` as one to sixteen closed descriptors.

SPEC-004 at `f6548edc` contains zero occurrences of `cbclPairingRelays`. The
grammar exists in code only, at
`crates/selfsame-app-identity/src/profile.rs:249` to `:264` and `:859` to
`:880`.

Nothing load-bearing breaks, because CON-806 restates the member set inline.
The attribution still points at absent text.

### L-3 The registry module comment cites the contract that now excludes it

Severity: LOW. Owner: selfsame repository owner.

`src-tauri/src/cbcl_registry.rs:1` describes the file as
"SPEC-008 `CON-903` — the compiled approved-conformance registry".
[[SPEC-008-production-pairing-claimant#CON-903]] now excludes a
conformance-digest registry from the policy.

This is implementation work rather than normative truth. The comment disappears
with the module that
[[SPEC-008-production-pairing-claimant#CON-986]] removes. This finding carries
forward the 0.5.1 L-4 observation unchanged.

### L-4 One version constant is declared twice under one name

Severity: LOW. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#REQ-1005]] names
`crates/selfsame-app-identity/src/lib.rs:109` and
`crates/selfsame-app-identity/src/profile.rs:60` as production
version-constant evidence. Both lines read `pub const PROFILE_VERSION: i64 = 1;`.

`lib.rs:92` declares `pub mod profile;`, so the crate carries two distinct
constants with one name and one value. The classification is correct and the
duplication is unremarked.

## Disposition of the 0.5.1 findings

Each row is re-derived from the current candidates and the pinned code.

| 0.5.1 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 declared hub test set exists only in trajectory | **CLOSED** | cbcl-bus REQ-081 now declares TEST-115 through TEST-117 and states that base-parent tests stay current outside the coordinated set. All three headings exist. [[SPEC-008-production-pairing-claimant#CON-985]] mirrors the narrower set. Every declared Selfsame and cbcl-pairing test resolves as a heading. |
| H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | cbcl-pairing REQ-031 names the carrier ceremony ID the sole credential/v2 ceremony identifier. cbcl-bus REQ-082 extracts it from the carrier and allocates only `applicationAccountId`, `accountScopeId`, and `snapshotNonce`. `selfsamepairallocated` now returns it. |
| H-2 reconnect recovery has no command | **CLOSED** | cbcl-bus CON-012 adds a seventh command, `selfsamepairstatus`, with a device recovery proof and a socket-generation digest. REQ-082 rebinds the pending record to the new generation. [[SPEC-008-production-pairing-claimant#NFR-928]] recovers before any fresh allocation. |
| H-3 four offer-core members carry no grammar | **CLOSED** | `OfferCoreV2` now closes at twenty members and the reviewer counted twenty. `applicationAccountDisplay`, `platformBindingId`, and `returnUri` survive only inside a negative test row at SPEC-053 line 7664. `deviceDid` gains an exact 56-octet `did:key` grammar with a multicodec check. |
| H-4 rendezvous dependency has no consumer | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] forbids every Selfsame and did-crdt rendezvous edge. GATE-04 carries the matching box. did-crdt SPEC-037 0.1.1 deprecates the proposal and burns REQ-370, CON-370, ADR-370, and TEST-370. |
| H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The credential/v2 path no longer reads or writes a rendezvous slot, so the preserved-behaviour clause is gone with the dependency. |
| H-6 REQ-812 amendment uses an unavailable channel | **CLOSED** | SPEC-007 is now a 0.3.0-draft revision whose REQ-812 states the credential/v2 boundary directly. SPEC-008 carries no `amends` frontmatter member. Both parents name the same coordinated set. |
| H-7 hub names a cbcl-pairing test that does not exist | **CLOSED** | cbcl-bus TEST-115 line 7604 now names cbcl-pairing TEST-063. [[SPEC-008-production-pairing-claimant#TEST-1156]] names the same test with the same rows. |
| M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] scopes the prohibition to the credential/v2 path and states that the standing SPEC-004 caller remains unchanged. That caller is `src-tauri/src/app_grant.rs:539` inside `confirm_issuance`. |
| M-2 exact-pair consent boundary has no owning function | **CLOSED** | `authorise_claimant_relay` owns the prompt and the CON-903 write. `cbcl_pairing_relay_approve` and `cbcl_pairing_relay_decline` are its only UI entry points. `recognise_claimant_invitation` owns pre-socket recognition. |
| M-3 `WIRE_VERSION` names half of its namespace | **CLOSED** | [[SPEC-008-production-pairing-claimant#REQ-1005]] now states that `WIRE_VERSION` governs both link-offer and link-grant records. The pinned sites are `crates/selfsame-core/src/record.rs:329`, `:396`, `:462`, and `:490`. |
| M-4 did-crdt worst-case store rises seventeen-fold | **CLOSED** | SPEC-037 0.1.1 states that it makes no change to `src/service/rendezvous.rs`, its capacity, its buffering, its CORS behavior, or its deployed routes. |
| L-1 executor order stated in three places with two shapes | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-902]], [[SPEC-008-production-pairing-claimant#CON-986]], and [[SPEC-008-production-pairing-claimant#TEST-1158]] each state issuer creation, signing, publication, closure verification, WebFinger verification, and grant construction in one order. |
| L-2 schema-migration enumeration misses a third site | **CLOSED** | [[SPEC-008-production-pairing-claimant#NFR-928]] names `cbcl-chat-roomcfg:transform-room/1` as the third schema migration. The `mnesia:transform_table` call sits at `apps/cbcl_chat/src/shell/cbcl-chat-roomcfg.lfe:82`. |
| L-3 two failure modes misdescribe the pinned code | **CLOSED** | The parent now labels FM-1 and FM-2 historical and repaired, and states that the current baseline opens WSS and enables rustls. |
| L-4 stale `cbcl_registry.rs` comment | **OPEN, NON-BLOCKING** | The comment survives unchanged at the baseline; see [[spec-008-0.5.2-claude-adversarial-review-2026-08-24#L-3 The registry module comment cites the contract that now excludes it]]. |

## Hard-stop disposition, F-A through F-E

Each row is re-derived from the candidate text and the pinned code.

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-903]] keys the sealed set on `(applicationId, relayOrigin)` and excludes a relay-only key, a global allowlist, a digest registry, and a wildcard. [[SPEC-008-production-pairing-claimant#REQ-1006]] requires a new prompt when a profile names a previously accepted relay under another application. The replaced code takes the excluded path: `eligible` at `crates/selfsame-app-identity/src/cbcl_relay.rs:85` to `:93` requires `approved_conformance.contains(...)`. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input and none from the peer input. It forbids generic `DisplayField` values and the wire `authority_summary`. The pinned surface still carries both: `src-tauri/src/cbcl_pairing.rs:413` to `:428` and `src/pairing.js:98` to `:112`. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | The pinned `assemble_claimant` derives the key at `src-tauri/src/cbcl_context.rs:285`, creates the issuer at `:288`, verifies WebFinger at `:306`, and resolves the closure at `:317`, all before `connect_wss` at `src-tauri/src/cbcl_pairing.rs:177`. CON-902, CON-986, and SPEC-007 REQ-812 move every one of those after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end, no prior-enrolment cycle | **CLOSED** | cbcl-bus REQ-082 allocates the scope inside the authenticated hub transaction and replaces the `record.account_scope_id` read at `src-tauri/src/cbcl_context.rs:279`. The single carrier ceremony identifier and `selfsamepairstatus` complete the trace that H-1 and H-2 formerly cut. The account principal keeps the raw account identifier hub-private through a 32-octet CSPRNG preimage. |
| F-E what does and does not prove relay operatorship | **CLOSED** | [[SPEC-008-production-pairing-claimant#ADR-902]] states plainly that the application profile does not prove relay operatorship, and that exact-pair consent contains the residual without claiming to solve it. No parent contradicts it. |

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact TOFU resists a new application on an accepted relay | **YES.** A changed application identifier makes a new tuple and prompts again. ADR-902 retains the operatorship residual honestly. |
| 2 no unauthenticated value reaches either consent screen | **YES in the parents.** CON-029 constructs the display only from the authority input after byte equality. C-2 leaves the peer input set undefined, so the equality check has no grammar to run against. |
| 3 every effect occurs at its declared consent boundary | **YES.** REQ-902, CON-902, CON-986, SPEC-007 REQ-812, and TEST-1158 agree on one ordering with no gap. |
| 4 hub scope breaks the enrolment cycle without leaking the account identifier | **YES.** The principal digest commits a 32-octet CSPRNG value and the offer carries no raw account identifier. |
| 5 exactly one carrier ceremony identifier | **YES.** REQ-031, CON-027, CON-028, CON-030, CON-012, REQ-082, and TEST-064 use one 32-octet value throughout. |
| 6 recovery without duplicate effect or a two-generals shortcut | **PARTLY.** No duplicate effect exists. The post-final wallet window closes with the hub already finalized; see M-3. |
| 7 the bounds agree arithmetically | **YES except one cap.** See the wire assessment and M-2. |
| 8 cryptography complete for two implementations | **NO.** See C-1. |
| 9 activation only after immutable signed acceptance | **YES.** CON-986 and REQ-082 both order inactive staging, hub commit, JWS verification, activation, receipt, and installation. |
| 10 hub authentication, locks, capacity, expiry, and staging are total | **YES.** CON-012 states both lock orders, the phase projection, the capacity bounds, and the closed terminal set. |
| 11 rendezvous absent from every credential/v2 edge | **YES.** CON-986, GATE-04, and SPEC-037 0.1.1 agree, and no offer, decision, payload, or receipt edge names a rendezvous. |
| 12 credential/v1 bytes and behaviour frozen | **YES.** REQ-031, TEST-063, and REQ-1005 each require byte-identical v1 output and no v2 reach into a classified v1 site. |

## Repository and code-claim assessment

Every testable code claim resolved exactly at its stated baseline. This is the
strongest part of the reissue.

Selfsame version sites. All eighteen witness rows in
[[SPEC-008-production-pairing-claimant#REQ-1005]] resolve at
`0220cec2` with the stated class. `crates/selfsame-web-device/src/lib.rs:600`
is an emitted member, `:1346` and `:1360` are version literals, and `:1369` is
a typed field assignment. `:831`, `:847`, and `:861` sit inside `parse_*`
ingest functions and are substitutions.

The seven-class classifier is coherent against the code.
`crates/selfsame-app-identity/src/ceremony.rs:413`,
`provider_hint.rs:96`, and `profile.rs:384` each extract from untyped input, so
the ingress-conversion class fits. `enrollment.rs:592` compares, matching the
additional comparison row.

cbcl-bus consumer inventory. Every line citation in
[[SPEC-008-production-pairing-claimant#NFR-928]] resolves at `ce894a7f`.
`cbcl-chat-session-ws.lfe:512` consumes `device-grants` into an admission map.
`:869` uses `lists:foreach` over `rooms-for-account`. `:965` passes
`list-public` to the wire channel encoder. `:1683` maps `grants`. `:1050`,
`cbcl-chat-room.lfe:263` to `:270`, and `cbcl-chat-pairgrant.lfe:78` are
boolean contexts. `:1076` and `:1788` are the two `get-or-start` consumers.
`:1898` discards `claim/5`. `cbcl-chat-rooms.lfe:49`, `:54`, `:60`, `:61`, and
`:99` to `:104` and `cbcl-webhook-handler.lfe:76` complete the webhook rows.

The nesting claim is exact. `cbcl-chat-successor.lfe:134` calls
`cbcl-chat-account-grants:device-grant`, which opens its own transaction at
`cbcl-chat-account-grants.lfe:236`. Both successor helpers wrap that call in
`catch`, at `:109` and `:134`. NFR-928 prescribes the correct repair for both.

The retry claim is grounded. Every `mnesia:transaction` call under `apps/`
uses the default unary form at the baseline, so the whole inventory needs the
prescribed `mnesia:transaction(Fun, [], 3)` wrapper. The mapping from an
exhausted level-1 cyclic restart to `{aborted, nomore}` matches Mnesia
behaviour, and the absence of a runtime nesting terminal justifies the static
inventory.

cbcl-pairing bounds. `MAX_CONTROL_OCTETS` is 2,048 at `src/cbcl_protocol.rs:32`.
`MAX_FRAME_BODY_BYTES` is 69,632 at `src/mailbox.rs:15`. `MAX_WIRE_MESSAGE` is
70,000 in both relay binaries at `src/bin/cbcl-pairing-relay.rs:31` and
`src/bin/cbcl-pairing-relay-ws.rs:32`.

Normative asset hashes. All four assets hash to the declared values at
`724c5d2f`. The two dialect canonical hashes remain as declared.

Selfsame surfaces the design replaces. `cbcl_registry::production_relay_policy`
reaches production only through `src-tauri/src/cbcl_pairing.rs:30` and `:152`.
`assemble_claimant` reaches it only through `:170`. `record_pairing_trust` has
one production caller, at `src-tauri/src/app_grant.rs:539`.

## Consent assessment

The two-decision boundary is stated once and consistently in four places.
REQ-902, CON-902, CON-986, and SPEC-007 REQ-812 each place preliminary approval
before one zeroizing derivation and final approval before the first signature.

The preview disclosure is consented rather than silent. REQ-902 requires the
preliminary screen to state the disclosure, and CON-986 restricts the recipient
to the authenticated application inside the established channel.

The fresh-randomness rule closes the reuse path. CON-902 and REQ-082 both
require a new attempt to draw new account and scope values. Both require a
second pair of decisions when the preview DID differs.

Final approval produces one single-use effect capability bound to the intent,
offer core, transition, preview DID, application, account, scope, installation
key, permissions, relay, and expiry. That binding leaves no room for a
substituted target.

The display rule is sound where it is defined. The verifier returns a closed
verdict, the profile owns construction, and every displayed value comes from
the authority input. C-2 leaves the peer input side without a grammar.

## Wire and bounds assessment

The reviewer recomputed every declared bound from the CDDL. The arithmetic
agrees.

| Structure | Computed maximum | Declared bound | Result |
|---|---:|---:|---|
| control object | 4,144 octets | control arm | fits |
| control object sealed | 4,160 octets | 69,632 frame body | fits |
| large object | 64,560 octets | large arm | fits |
| large object sealed | 64,576 octets | 69,632 frame body | 5,056 spare |
| large object sealed | 64,576 octets | 70,000 wire ceiling | 5,424 spare |
| `signed-offer-v2` | 56,625 octets | 56,640 declared | 15 spare |
| `credential-v2-receipt-body` | 8,372 octets | 62,000 logical body | fits |

The 62,000-octet logical body sits inside the 64,512-octet padded field, and
the 8,192-octet JWS sits inside the receipt body. The 69,632-octet checkpoint
and pending bounds both exceed the largest cached frame.

The PAIR1 grammar closes exactly. Eleven groups of five Crockford characters
give 275 bits, and `C || T || checksum16 || 000` consumes 128, 128, 16, and 3
bits. The alphabet holds 32 symbols and omits `I`, `L`, `O`, and `U`.

The `deviceDid` grammar closes exactly. The fixed prefix `did:key:z6Mk`
occupies 12 octets and 44 base58 characters complete the 56-octet total, which
matches a real Ed25519 `did:key` value.

The `deviceKeyJwk` bound holds. The closed RFC 8785 object encodes to 79
octets, inside the declared 96-octet cap.

The envelope kind numbering closes without ambiguity. Eight control kinds
occupy 1 through 8 and three large kinds occupy 0, 9, and 10, matching the
eleven-name list in order.

The v2 admission wire is closed and exact. `allocate-v2`, `claim-v2`,
`allocated-v2`, and `claimed-v2` each name their member set, and the claim
commitment states its domain separator.

The two defects are C-1 for the channel inputs and M-2 for the relay-origin
cap.

## Cryptography assessment

The stated parts are precise. Seven constructions each name an exact domain
separator and an exact input order.

They are the claim commitment, the account principal, the offer-signature input,
and the intent digest. They are also the checkpoint key derivation, the
socket-generation digest, and the status-recovery signing input.

The `kid` rule resolves. `enrollment.requestSigningKeys[].kid` exists in the
profile grammar and in the pinned recogniser at
`crates/selfsame-app-identity/src/profile.rs:578` to `:606`. The same key signs
the offer and the final status.

Key separation is declared where the constructions exist. The checkpoint
wrapping key is a distinct HKDF-SHA512 child, the salt is the raw carrier
ceremony identifier, and no raw wrapping key enters the checkpoint.

The unstated parts block implementation. The public context and both unnamed
`ad` members are absent. The HKDF extraction, the info form, the output lengths,
the Finished MAC, the nonce derivation, and the AAD encoding are absent too.
See C-1.

## Recovery and atomicity assessment

The allocation path is complete. An exact retry on the same generation, request,
and carrier returns byte-identical bytes. A concurrent different request
refuses without allocating a second candidate account.

Reconnect is complete. `selfsamepairstatus` carries the original request and
carrier ceremony, a 16-octet recovery nonce, and a device recovery proof over
the socket-generation digest. Pending recovery rebinds the generation and
advances no phase.

Only authenticated absent status permits a fresh allocation. Busy, unavailable,
corrupt, aborted, expired, and ambiguous status all refuse, so no path
duplicates an account.

The crash boundaries are enumerated on both sides. A crash before allocation
commit leaves no state. A crash during final commit yields the complete pending
state or the complete installed state.

The endpoint checkpoint rule prevents replay from authorizing. Resume
retransmits only the cached frame, an inbound replay reproduces the same effect
or refuses, and an old checkpoint never authorizes a new transition.

The wallet checkpoint sits after final approval and before the first identity
effect. A preliminary decision never becomes a checkpointed effect capability,
so a restart before final approval abandons the ceremony.

The one gap is the closing window described in M-3.

## Persistence assessment

The wallet slot is a tagged pending-or-installed union rather than two records,
so no reader observes both. Verified receipt replaces the pending value
atomically.

The pending value excludes every key. It carries no hierarchy root, derived
key, issuer key, grant key, signature, publication result, or application
capability.

The installed record names its complete member set. Reload verifies grant,
profile, authority, issuer, and hub status before granting capability, and
unavailability stays distinct from revocation and absence.

The hub records are bounded and keyed. The pending record encodes to at most
69,632 octets. One active record exists per application, handle, and enrolled
key, and 65,536 active records cap the table.

Deletion triggers are total. Decline, expiry, and pre-commit failure delete the
account identifier, scope identifier, snapshot, and every ceremony-owned row. A
replay tombstone retains only a request digest and terminal class.

## Rendezvous assessment

The dependency is absent. CON-986 forbids every Selfsame and did-crdt
rendezvous read, write, route, and client edge on the credential/v2 path.

GATE-04 carries the matching release box, and TEST-1159 traces every offer,
decision, payload, and receipt transport for relay-only edges.

did-crdt SPEC-037 0.1.1 deprecates the body-bound proposal before
implementation. It changes no capacity, buffering, CORS behavior, or route, and
it burns the four identifiers.

The reviewer found no residual rendezvous edge in the credential/v2 design. The
only remaining rendezvous coupling is the indirect one in H-1, through
CON-220's unrepaired step 6.

## Compatibility assessment

Credential/v1 stays frozen. REQ-031 preserves its carrier secret, CPace
schedule, display, wire bytes, and decisions. TEST-063 re-runs every base
outcome and requires identical bytes, hashes, displays, stores, frames, and
verdicts.

The two versions cannot substitute for each other. Separate invitation types,
separate contexts, separate labels, separate Finished values, and separate AAD
prevent cross-version authentication.

The production scan is the completeness authority over version sites, and its
closed five-identifier set with a seven-class classifier is decidable. Named
witnesses cannot close or limit it.

`successionVersion`, `WIRE_VERSION`, and `CODE_VERSION` each resolve to a named
namespace outside the credential-v1 set. The `anuna-ssi` pin resolves as a
commit and is recorded as an explicit cross-vault deferral.

## Authority, pointer, and singularity assessment

Version agreement is exact. All four parents name SPEC-008 0.5.2-draft,
SPEC-007 0.3.0-draft, SPEC-001 0.5.1-draft, and SPEC-053 0.17.2-draft, and
GATE-04 box one repeats the same four.

Generation provenance agrees in all four. Each records OpenAI GPT-5,
`gpt-5.6-sol`, session `01a029aa-9127-7c42-ad28-81512b91ded6`, and a synthesis
trajectory line.

Consumer pointers close their loop. cbcl-pairing frontmatter names
`consumer-design: selfsame SPEC-008 0.5.2-draft`, and
[[SPEC-008-production-pairing-claimant#REQ-1005]] names cbcl-pairing SPEC-001
0.5.1-draft.

Test-set singularity now holds in all three vaults. TEST-901 through TEST-915
and TEST-1156 through TEST-1159 all exist as SPEC-008 headings. TEST-115
through TEST-117 all exist as SPEC-053 headings. TEST-001 through TEST-029 and
TEST-060 through TEST-065 all exist as SPEC-001 headings.

GATE-04 carries exactly 42 boxes between its heading and the following
contract. `OfferCoreV2` carries exactly 20 members. Both counts match their
declarations.

Anchor qualification holds. SPEC-008 contains zero bare `[[#anchor]]` links,
and every wikilink target names a document.

Strict descriptive lint reports clean on four extractions. The complete SPEC-008
parent, its consolidated current-law extraction, the complete SPEC-007 parent,
and the complete cbcl-pairing parent each return zero errors and zero warnings.
The SPEC-053 coordinated band from REQ-081 through TEST-117 also returns zero.

The remaining singularity defects are M-1 and L-1.

## Stopping-rule assessment

The stopping rule is stated and consistent. Every parent states that a fresh
PASS authorizes only the Elephant SPL and the test-first plan.

No parent claims production authority. GATE-04 states that this reissue closes
none of its 42 boxes. SPEC-001 keeps production allocation prohibited.
[[SPEC-008-production-pairing-claimant#REQ-907]] leaves the SPEC-007 REQ-809
hold untouched.

The mutation gate is decidable. Disabling the origin check, accepting an invalid
certificate, and reintroducing a non-demo fixture context each name one failing
test.

## Surfaces found sound

The reviewer probed these surfaces and found no defect.

- Exact-pair TOFU under a new application on an accepted relay, and under a
  changed relay origin for an accepted application.
- The zero-effect plan boundary, the single zeroizing preview derivation, and
  the byte-for-byte preview equality check before the first signature.
- The account principal construction, which keeps the raw application account
  identifier hub-private through preimage resistance over 32 CSPRNG octets.
- The single carrier ceremony identifier across carrier, CPace context,
  envelope, offer, seven hub commands, status recovery, checkpoint, receipt,
  and installed record.
- Both hub lock orders, the closed phase projection, the capacity bounds, and
  the single-commit final transaction.
- Browser inactive staging before hub finalization, and activation only after
  the authenticated immutable status.
- The declared bound arithmetic, recomputed independently from the CDDL.
- The complete Selfsame, cbcl-pairing, and cbcl-bus code-claim inventories.
- The absence of every rendezvous edge from the credential/v2 design.
- Credential/v1 freezing and version non-substitutability.

## Residual assumptions

These are unstated but not defects. They belong in the plan rather than in a
finding.

- The endpoint checkpoint holds a cached maximum outbound frame of about 64,576
  octets plus role, carrier, transcript, keys, and counters inside a 69,632-octet
  ciphertext. The reviewer estimates roughly 2,400 octets of spare capacity. No
  parent states that arithmetic.
- The hub pending record holds a 56,640-octet signed offer inside a
  69,632-octet bound. That fits only when the hub derives the room list and the
  offer core from the stored offer rather than storing a second copy.
- ADR-902's operatorship residual stands. An authenticated application can name
  a well-known relay origin it does not operate, and the person is the only
  check. The parent states this plainly.
- The browser's credential/v2 allocator endpoint needs a cbcl-pairing
  implementation reachable from JavaScript. GATE-04 forbids Selfsame WASM there
  and names no replacement.
- A hostile hub for one application can reuse an account scope and reproduce a
  previously approved preview DID. The blast radius stays inside that
  application's own namespace.

## Collected corrections and owners

| Finding | Correction | Owner |
|---|---|---|
| C-1 | State `credential-v2-public-context`, both unnamed `credential-v2-ad` members, the transcript-hash input order, the HKDF extraction, the info form, every output length, both Finished values, the nonce derivation, and the exact AAD map directly in the cbcl-pairing parent. Give the absent claimant key a nullable slot. | cbcl-pairing repository owner |
| C-2 | Define the object content hash in the cbcl-pairing parent. Define the nine missing logical body grammars in the Selfsame parent, including the credential/v2 reverse payload, and stop pointing that payload at the credential/v1 CON-219 object. | selfsame repository owner, with the cbcl-pairing owner |
| H-1 | State how the credential/v2 path satisfies or replaces CON-220 step 6 before the socket, or bring SPEC-004 into the coordinated set and revise CON-220. | selfsame repository owner |
| H-2 | Define the `descriptorDigest` input object, its canonicalization, and the descriptor-selection rule for a profile listing several descriptors. | cbcl-bus repository owner, with the selfsame owner |
| M-1 | Scope SPEC-007 CON-806's peer-sourced application identifier paragraph to credential/v1. | selfsame repository owner |
| M-2 | Reconcile the 255-octet carrier relay-origin cap with the 272-octet offer-core cap. | cbcl-pairing repository owner, with the cbcl-bus owner |
| M-3 | State the wallet's terminal disposition when the receipt cannot arrive before the ceremony window closes, or extend the recovery route past mailbox and pending expiry. | selfsame repository owner, with the cbcl-bus owner |
| L-1 | Reconcile SPEC-007 CON-807's four stages with CON-986's five owned functions. | selfsame repository owner |
| L-2 | Land the `cbclPairingRelays` grammar in SPEC-004, or attribute it to SPEC-007 CON-806 itself. | selfsame repository owner |
| L-3 | Remove the stale `cbcl_registry.rs` module comment with the module. | selfsame repository owner |
| L-4 | Record the duplicate `PROFILE_VERSION` declaration, or collapse it to one constant. | selfsame repository owner |

## What this verdict authorizes

This verdict authorizes nothing beyond correction and one further review.

It authorizes the four repository owners to correct the eleven findings above.
It authorizes a further coordinated fresh-context Tier-1 review of the
corrected set.

## What this verdict does not authorize

It does not authorize implementation of any credential/v2 clause. It does not
authorize an Elephant SPL or a test-first plan.

It does not authorize production invitation allocation. SPEC-007 REQ-809 and
the SPEC-007 Production gate remain untouched.

It does not authorize a release, a deployment, or an enablement flag. It closes
no GATE-04 box and no cbcl-pairing production gate.

It does not amend any parent. It changes no code, no vector, and no sibling
repository artefact.
