---
id: spec-008-0.5.12-implementation-reviewer-prompt-2026-08-24
title: SPEC-008 0.5.12 implementation — Tier-1 adversarial review prompt
status: active
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-24
---

# SPEC-008 0.5.12 implementation — Tier-1 adversarial review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects. Do not
approve work because the latest delta is bounded or because its author reports
green mutation tests. Independently review the complete coordinated standalone
credential/v2 implementation, then evaluate the 0.5.12 TEST-1162 remediation.
Do not repair the candidate. Write only the review report, commit it in the
Circus worktree, and complete through the appended Circus instruction.

## Authentication and isolation

This review runs through Circus with the interactive Claude CLI so it uses the
person's Claude Max subscription. The API-billed `circus-driver-claude` SHALL
NOT be invoked. The parent and child environments remove `ANTHROPIC_API_KEY`
and `ANTHROPIC_AUTH_TOKEN`. Check that both variables are absent without
printing values. Run `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude
auth status` and record only:

- `authMethod`
- `apiProvider`
- `subscriptionType`

The required values are `claude.ai`, `firstParty`, and `max`. Record no email,
organization identifier, token, key, or account identifier. A different result
is a review-process blocker. The generating family is OpenAI GPT-5; the
reviewing family is Anthropic Claude. This session receives no generating-
session reasoning or transcript.

## Exact candidate set

Use `git show COMMIT:path` and `git diff BASELINE..COMMIT`. Do not substitute
uncommitted files. The commits below are the candidate even though the Circus
integration ref also contains this prompt.

- Selfsame implementation candidate
  `0dbfb3c4323aec568ac2803d58c0aa71c301edda`
  - pre-0.5.12 implementation candidate
    `0c5801259802b767bd8f9230ff6cd32dff6fffa5`
  - governing 0.5.12 authority and remediation note commit
    `73ad88dc361ae20820ba9d328b455dad83ab56f5`
  - original coordinated implementation baseline
    `0220cec2dec44cd95d4f411ea4814d790b6716d2`
  - governing parents:
    `specs/SPEC-008-production-pairing-claimant.md` and
    `specs/SPEC-007-cbcl-pairing-cutover.md`
- cbcl-bus candidate
  `100d5e4b5f8e6e36a61b2b27ffdb13ca61edb335`
  - pre-0.5.12 candidate
    `965d6cc44cfdf0f8d00a562bb587b7785396ee97`
  - original coordinated implementation baseline
    `ce894a7f2debc111b610296b1a3780de6084e37e`
  - governing parent: `specs/SPEC-053-key-root-identity.md`
- cbcl-pairing candidate
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
  - original coordinated implementation baseline
    `c22f2d0526432c01c56d64be35a8d4f87516ec92`
  - governing parent: `specs/SPEC-001-reusable-blind-pairing.md`
- did-crdt service repository HEAD
  `94db956d7d5d5edf0f42afad073ab42f265a1752`
  - dependency revision pinned into the coordinated runtime build:
    `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`
  - determine from code whether service-only changes after the pin participate
    in credential/v2 runtime; do not assume they do
- cbcl-rs dependency revision
  `febc6691e6dd2d5f7116b1a4d84c984b64717564`

The candidate's uncommitted working trees are not evidence. Create clean
detached clones or worktrees from the exact commits for tests and mutations.
The person's existing dirty files, output APKs, and patch script are out of
scope and SHALL NOT be modified or deleted.

## Independent review mandate

Read each governing parent in full for the checked areas. Trace real producers,
wire recognisers, durable state transitions, UI consumers, recovery routes,
and deployment boundaries. Verify code claims against code, not trajectory
prose. Before opening any prior review, determine independently whether:

1. Live CON-220 application authentication and exact
   `(applicationId, relayOrigin)` consent precede relay socket creation. A
   relay trusted for one application never authorizes another application.
2. The claimant binds the locally authenticated profile digest into the CPace
   public context and checks the peer-authenticated digest before policy seal,
   exact-intent display, or identity work.
3. The display shows the locally authenticated application identity and
   cross-checks the wire claim before approval, closing historical F-B.
4. No application identity key, scope, DID, signature, publication, alias,
   grant, or durable identity record occurs before its specified consent
   boundary, including [[SPEC-007-cbcl-pairing-cutover#REQ-812]].
5. Fresh account scope is hub-minted, exclusive, bound to the authenticated
   ceremony, and used only after consent; no prior enrolment record is needed.
6. Relay operatorship ambiguity is not mistaken for authentication; the actual
   security property is per-application exact-pair consent plus cryptographic
   application binding.
7. All credential/v2 wire bodies are closed, canonical, bounded, versioned,
   context-separated, and recognised before action on both sides.
8. The allocator browser ceremony, relay protocol, wallet claimant, reverse
   issuance, final status, receipt, and installation form one end-to-end
   cbcl-pairing protocol rather than parallel ad-hoc enrolment.
9. The browser persists and reconciles every phase needed for restart without
   duplicate allocation, identity, grant, or acceptance effects.
10. The wallet's durable pending union, endpoint checkpoints, exclusive
    deadlines, final status recovery, and installation CAS cannot widen
    authority after crash, replay, timeout, cancellation, or wrong order.
11. Acknowledged `FinishedSent` claimant restoration emits the allocator's
    required `Open` without a duplicate `Finished`; mutate that predicate.
12. [[SPEC-008-production-pairing-claimant#CON-990]] arms immediately after
    durable final approval, covers every pre-payload return, removes only the
    same recognised attempt, and disarms only at durable `PayloadPrepared`.
13. A correct `PayloadPrepared` record survives compensation and later receipt
    failure for [[SPEC-008-production-pairing-claimant#CON-989]] recovery.
14. Compensation refuses another attempt, another root generation, an
    installed record, and a sibling application.
15. Confirmed unlink authenticates current-root presence and removes only the
    exactly selected slot and still-trusted exact-pair policy. Slot or policy
    replacement between selection and deletion refuses and retains the new
    state. Failed slot deletion restores policy.
16. Every post-final-approval/pre-`PayloadPrepared` boundary named by
    [[SPEC-008-production-pairing-claimant#TEST-1162]] is exercised by a
    semantically adequate failure injection, not merely named in a table.
17. The process-global TEST-1162 Rust test is executed in an isolated CI
    process despite `#[ignore]`; the wallet lifecycle browser suite is also an
    explicit CI step. Verify command filters actually select the intended test.
18. Pending links are person-visible with only bounded public summary data;
    abandonment and fresh same-application retry work after restart.
19. Installed reload reauthenticates live reciprocal authority before exposing
    capability. Profile-only refresh is atomic; authority or issuer rotation
    prompts; handle change refuses; unavailable, oversized, and revoked states
    remain distinct and fail closed.
20. Exact pins name one buildable source closure across Selfsame, cbcl-bus,
    [[SPEC-007-cbcl-pairing-cutover#ADR-802]], CI, Android, tests, and browser
    WASM provenance. The witness rejects each tracked-dirty sibling even under
    the labelled development override.
21. The vendored WASM rebuild is byte-identical on rustc 1.96.0,
    wasm-bindgen 0.2.126, aarch64-apple-darwin, with SHA-256
    `3834fc310ff699260fbbab35028ec883e61d7237b449f441d425351e27181388`.
22. Production allocation remains closed and this review cannot authorize
    release, push, or deployment.

Run focused suites and narrower adversarial tests as needed. Record exact
commands and outcomes. A collection failure or dependency-resolution failure
is not passing evidence.

## Required mutation re-test

In a clean scratch clone, force recompilation after each mutation. At minimum,
independently remove or invert each predicate below and require an executable
test to fail:

1. the `PayloadPrepared` retention in `remove_pre_payload_attempt`;
2. the immutable same-attempt comparison;
3. the installed-slot refusal in compensation;
4. `unlink_local`'s exact slot compare-and-delete; and
5. `unlink_local`'s still-trusted exact-pair policy check.

Also mutate or bypass the root-generation member of the attempt comparison,
the compensation guard drop, and the CI test selectors if they appear
load-bearing. A test that passes after a load-bearing predicate is removed is a
finding even if production code presently looks correct.

The generating agent reports:

- all five named mutants are killed by the isolated TEST-1162 case;
- the restored isolated TEST-1162 case passes;
- the ordinary Selfsame library suite passes with the ignored test separately
  gated;
- Selfsame Clippy passes with warnings denied;
- `npm run e2e:wallet-pairing` passes five cases;
- the complete cbcl-pairing workspace, exact clean cbcl-bus 975 EUnit tests,
  and cbcl-bus browser suites passed for the preceding protocol candidate; and
- the exact updated source closure rebuilds the vendored WASM byte-identically.

Independently confirm the changed gates and enough of the unchanged suites to
establish that the candidate, not an uncommitted substitute, satisfies the
claims.

## Prior-review re-test

Only after the independent pass, read:

- `specs/trajectory/SPEC-008/spec-008-0.5.11-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.10-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.9-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.8-implementation-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/spec-008-0.5.7-claude-adversarial-review-2026-08-24.md`
- `specs/trajectory/SPEC-008/req-909-adversarial-review-2026-08-22.md`

Independently re-test 0.5.11 M-1 and both LOW residuals. Re-test every named
0.5.10, 0.5.9, 0.5.8, and 0.5.7 finding, F-A through F-E, and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]]. Do not inherit any disposition.

## Output contract

Write
`specs/trajectory/SPEC-008/spec-008-0.5.12-implementation-claude-adversarial-review-2026-08-24.md`.
The report SHALL contain:

- reviewer identity, subscription auth path, Circus attempt, exact commits,
  and independence statement;
- verdict `PASS` or `REJECT` and severity counts;
- every finding with severity, code evidence, exploit or failure sequence,
  affected fully-qualified requirement, and measurable correction;
- explicit dispositions for all twenty-two required checks;
- explicit dispositions for all five required mutants, 0.5.11 M-1 and LOW
  residuals, every earlier named finding, F-A through F-E, and
  [[SPEC-007-cbcl-pairing-cutover#REQ-812]];
- exact test commands and results; and
- a statement that PASS does not authorize release, push, or deployment.

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. A load-bearing missing
obligation is a finding even if the candidate fails closed. LOW residuals may
remain only when their consequence and operational treatment are explicit.
Release and deployment controls are outside this review's authority.
