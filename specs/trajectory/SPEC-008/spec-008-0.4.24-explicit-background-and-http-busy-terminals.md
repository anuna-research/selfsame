---
id: SPEC-008-amendment-0.4.24
title: SPEC-008 0.4.24-draft — Explicit background and HTTP busy terminals
status: draft
version: 0.4.24-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.23-total-result-contracts-and-static-nesting#CON-982]] only where [[spec-008-0.4.24-explicit-background-and-http-busy-terminals#CON-983]] says so"
adopts: "All effective 0.4.4 through 0.4.23 clauses except the exact replacements in CON-983"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.10-draft; cbcl-bus SPEC-053 0.16.17-draft; [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]]"
generation-model-family: OpenAI GPT-5
generation-model-version: gpt-5.6-sol
generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6
generation-synthesis-trajectory: "[[spec-008-0.4.23-total-result-contracts-and-static-nesting#Tier-1-generation-provenance]] -> [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#Findings]] -> [[spec-008-0.4.23-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]] -> this candidate"
last-updated: 2026-08-23
---

# SPEC-008 0.4.24-draft — Explicit background and HTTP busy terminals

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** This amendment addresses every 0.4.23
> review finding. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. The live authenticated
application, exact `(applicationId, relayOrigin)` consent, authenticated intent
display, fresh-scope bootstrap, and post-consent effect order remain unchanged.

## Tier-1 generation provenance

OpenAI GPT-5 model `gpt-5.6-sol` generated this coordinated synthesis. Its
Codex session is `01a029aa-9127-7c42-ad28-81512b91ded6`.

The initial correction draft is
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#Tier-1-generation-provenance]].
Its failing-test evidence and attribution map are in
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#Findings]].

The localised correction input is
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#Corrections-and-owners-collected]].
This candidate applies its one MEDIUM and six LOW corrections.

The correction adds periodic-poller and webhook busy terminals. It makes the
production consumer scan authoritative and records every newly named sink.

It also restates the retired v1 witnesses, aligns every classifier class, adds
the missing ingress conversion, and makes withdrawal refusal deterministic.

A qualifying reviewer SHALL use another model family and a fresh session. The
report SHALL record its model, authentication path, session, and Circus
attempt. It SHALL verify distinctness from this generation record.

## Requirement

### REQ-1003 — One exhaustive classifier governs the v1 production scan

This requirement supplements
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#REQ-1002]]. It
replaces only the two REQ-1002 ranges named below.

The first range starts with "The production scan is the completeness
authority". It ends with "an ingress conversion." The replacement is:

The production scan is the completeness authority for every classified v1
site. Its classes are version-constant definition, version literal,
comparison, emitted member, typed version-field assignment, ingest
substitution, and untyped ingress conversion.

A field-to-field copy on an ingest path is a substitution. Extraction from
untyped input into a typed version-bearing value is an ingress conversion.

The scan includes `PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and
`payloadVersion`. It distinguishes production code from `#[cfg(test)]` code,
member-name constants, type declarations, format templates, and comments.

The second range starts with "Named baseline witnesses include every site
listed by REQ-1001". It ends with
"`crates/selfsame-app-identity/src/provider_hint.rs:96`." The replacement is:

The following eighteen retired-list witnesses are incorporated here as
evidence. This exact incorporation is independent of REQ-1001's retirement.

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts substitution
crates/selfsame-web-device/src/lib.rs:1346      offer substitution
crates/selfsame-web-device/src/lib.rs:1360      provider-hint substitution
crates/selfsame-web-device/src/lib.rs:1369      statement substitution
crates/selfsame-app-identity/src/ceremony.rs:185 payloadVersion substitution
crates/selfsame-app-identity/src/ceremony.rs:190 profileVersion substitution
crates/selfsame-app-identity/src/ceremony.rs:308 offer payloadVersion comparison
crates/selfsame-app-identity/src/ceremony.rs:460 bundle payloadVersion substitution
crates/selfsame-app-identity/src/ceremony.rs:510 bundle payloadVersion comparison
crates/selfsame-app-identity/src/enrollment.rs:373 evidenceVersion comparison
crates/selfsame-app-identity/src/enrollment.rs:394 statement profileVersion comparison
crates/selfsame-app-identity/src/enrollment.rs:452 recognizer substitution
crates/selfsame-app-identity/src/enrollment.rs:552 statement verification comparison
crates/selfsame-app-identity/src/enrollment.rs:662 evidenceVersion substitution
crates/selfsame-app-identity/src/enrollment.rs:666 profileVersion substitution
crates/selfsame-app-identity/src/profile.rs:385 profile comparison
crates/selfsame-app-identity/src/provider_hint.rs:67 profileVersion substitution
crates/selfsame-app-identity/src/provider_hint.rs:115 provider-hint comparison
```

Additional comparison evidence is
`crates/selfsame-app-identity/src/enrollment.rs:592`. Ingest-copy evidence is
`crates/selfsame-web-device/src/lib.rs:831`, `:847`, and `:861`.

Direct ingress-conversion evidence includes
`crates/selfsame-app-identity/src/ceremony.rs:413`,
`crates/selfsame-app-identity/src/provider_hint.rs:96`, and
`crates/selfsame-app-identity/src/profile.rs:384`.

Production version-constant evidence includes
`crates/selfsame-app-identity/src/lib.rs:109` and
`crates/selfsame-app-identity/src/profile.rs:60`.

These named sites are evidence only. They cannot close or limit the
repository-wide scan.

Credential/v2 reaches no classified v1 site. It SHALL NOT call
`EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.

## Non-functional requirement

### NFR-926 — Every production consumer has a typed busy terminal

This requirement supplements
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]]. It
replaces only the four NFR-925 fragments named below.

The first range starts with "A present account row SHALL name". It ends with
"Neither case deletes any row." The replacement is:

A present account row whose home DID differs returns
`#(error wrong-generation)`. If its home DID agrees but its stored alias is
not `requestedAlias`, it returns `#(error inconsistent-binding)`.

The write-locked alias row SHALL name that same account and home DID. An absent
or disagreeing alias row returns `#(error inconsistent-binding)`.

Every refusal deletes zero alias, account, and withdrawal rows. Only reciprocal
agreement continues to the withdrawal lock and both live deletes.

The second range is the sentence "These authority owners expose that
contract:". Its replacement is "The authority public owners are:".

The third fragment is the sentence "These ceremony-migration owners expose
the same busy result:". Its replacement is "The ceremony-migration public
owners are:".

Only collection and predicate owners adopt tagged success. Every other public
owner retains its declared success shape and adds the lane's typed busy shape.

The fourth range starts with "Every production consumer handles both tagged
result forms". It ends with "fail-closed wildcard." The replacement is:

A repository-wide production consumer scan is the completeness authority.
Every row names the public owner, consumer path and line, success sink, busy
branch, and terminal boundary.

Every production consumer handles the owner's declared success shape and
`#(error authority-busy)`. It unwraps `#(ok Value)` before use and never handles
busy through a wildcard default.

The six collection sinks include:

- `cbcl-chat-session-ws.lfe:512`, consuming `device-grants/1` into an
  admission map;
- `cbcl-chat-session-ws.lfe:869`, consuming `rooms-for-account/1` through
  `lists:foreach`;
- `cbcl-chat-session-ws.lfe:965`, consuming `list-public/0` through the wire
  channel encoder;
- `cbcl-chat-session-ws.lfe:1683`, consuming `grants/1` through `lists:map`;
- `cbcl-chat-room.lfe:677`, consuming `peer-grants/1` through `lists:map`; and
- `cbcl-chat-path-b-poller.lfe:17`, consuming `accounts/0` through
  `lists:foreach`.

The boolean contexts are `cbcl-chat-room.lfe:263-270`,
`cbcl-chat-pairgrant.lfe:78`, and `cbcl-chat-session-ws.lfe:1050`.

The map includes `unratified-device?/1` at
`cbcl-chat-session-ws.lfe:347` and `:385`. It also includes
`cbcl-chat-path-b-poller.lfe:17` and `:32`.

The webhook map includes `cbcl-chat-rooms.lfe:54`, `:60`, and `:61`. It
includes the consuming HTTP edge at
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76`.

Tagged success cannot reach a boolean operator, list function, pure admission
map, wire encoder, or refusal wildcard. The named sites are evidence and never
limit the production consumer scan.

For a periodic Path-B tick, busy from `accounts/0` emits OBS-905 with phase
`account-scan`. The tick reports no success, performs no resolver call, and
ends without crashing the supervised poller.

Busy from `revoke-observed/2` emits OBS-905 with phase `revocation-apply`. It
reports no applied union, performs no live-session eviction, and leaves that
account for a fresh resolver pass on the next scheduled tick.

The poller MAY continue other accounts after a `revocation-apply` busy result.
It MUST NOT report the discarded verified union as applied.

Busy from `cbcl-chat-roomcfg:get/1` or `claim/3` propagates through
`public-cleartext-channel/1` and `cbcl-chat-rooms:ingest/3` as
`#(error authority-busy)`.

The webhook handler maps that value to HTTP 503 with status
`authority-busy` and `cache-control: no-store`. It releases any reserved blob
slot and performs no room start, room ingest, persistence, or channel fan-out.

HTTP 409 remains the terminal only for a committed non-public-channel
decision. Busy never becomes that policy refusal.

## Observation

### OBS-905 — Background revocation busy is visible without identity data

The Path-B poller emits `path-b-revocation-authority-busy` for each busy
decision. Its phase is `account-scan` or `revocation-apply`.

The event records no account DID, credential identifier, resolver payload,
profile digest, or grant key. A counter MAY aggregate by phase.

The signal means the current tick reported no success for that phase. The
next scheduled tick retries from a fresh resolver read.

## Contract

### CON-983 — The N8 corrections and coordinated pointers advance once

This contract supplements
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#CON-982]]. Every
earlier replacement remains effective unless this contract replaces its exact
current fragment.

REQ-1003 supplements REQ-1002 and applies its two exact range replacements.
NFR-926 supplements NFR-925 and applies its four exact fragment replacements.

OBS-905 supplements the current observations. ADR-961 supplements ADR-960.
TEST-1152 supplements TEST-1150. TEST-1153 supplements TEST-1151.

This contract retires CON-982's current-value range beginning with "The exact
cbcl-bus set". It ends with did-crdt commit
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

The replacement text is:

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, and TEST-109 through TEST-112.

Every effective Selfsame test through TEST-1108 also survives. TEST-1134,
TEST-1135, TEST-1139, and TEST-1150 through TEST-1153 are current.

No superseded portion or open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.10, and
cbcl-bus 0.16.17. It contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

This contract retires CON-982's pointer paragraph beginning with "The
Selfsame parent". It ends with "participates in the coordinated gate."

The replacement text is:

The Selfsame parent [[SPEC-008-production-pairing-claimant]] points here and
names this candidate's open review gate.

The cbcl-bus SPEC-053 parent at DEC-012 points to cbcl-bus 0.16.17.
Cbcl-pairing 0.4.10 points its `consumer-design` to this candidate.

Cbcl-pairing's recorded generation provenance participates in the coordinated
gate.

Every F-A through F-E correction remains a hard stop.
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] also remains a hard stop.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It
authorizes no code, allocation, release, or deployment.

## Decision

### ADR-961 — Busy terminates at the consumer that owns the retry clock

The Path-B poller owns its next-tick retry clock. It records busy without
claiming a successful revocation pass.

The webhook handler owns its HTTP terminal. It returns retryable 503 without
converting storage contention into a channel-policy decision.

The repository-wide consumer scan, not a fixed witness list, proves that every
changed result reaches a declared boundary.

The repository-wide version scan covers every classifier class. Named sites
remain evidence and never finite permission.

Authenticated display, post-consent effects, shared grammar, cap arithmetic,
and the sealed-grant graph remain unchanged.

## Test specification

### TEST-1152 — Background, webhook, and withdrawal terminals are total

Generate the production consumer inventory from every public owner. Require
owner, consumer, success sink, busy branch, and terminal boundary for every
row.

Drive all six named collection sinks and every newly discovered collection
sink under success and busy. No tagged value reaches its sink before unwrap.

Force `accounts/0` busy during a scheduled Path-B tick. Require OBS-905 phase
`account-scan`, no resolver call, no success report, and a live poller.

Force `revoke-observed/2` busy after a verified union. Require OBS-905 phase
`revocation-apply`, no success report, no eviction, and a fresh resolver pass
on the next tick.

Force busy independently at the webhook's initial `get/1`, `claim/3`, and
post-claim `get/1`. Require HTTP 503 `authority-busy`, `no-store`, released
reservation, and zero room start, ingest, persistence, or fan-out.

Drive a committed private-channel decision. Require HTTP 409 and prove that
the busy branch never reaches that terminal.

Give withdrawal a present account row with the requested home DID and a
different stored alias. Require `#(error inconsistent-binding)` and zero
deletion from all three WebFinger tables.

Give withdrawal a present account row with another home DID. Require
`#(error wrong-generation)` and zero deletion.

Every existing TEST-1150 regression remains required, including its four
inline REQ-995 groups and WebFinger read case.

### TEST-1153 — Every v1 classifier class is scanned

Scan every production Rust source under REQ-1003's complete classifier.
Generate path, line, namespace, class, and production-or-test disposition for
every match.

Require every incorporated eighteen-line witness to resolve. Require the
additional comparison, three ingest copies, three ingress conversions, and
two production constant definitions to resolve.

Require `profile.rs:384` to classify as an ingress conversion. Require both
`PROFILE_VERSION` definitions to classify as production constant evidence.

A newly discovered production site fails the test if it is absent from the
generated inventory. A fixed witness list cannot make the scan pass.

Require credential/v2 to reach no classified v1 site. Require frozen v1 bytes
to remain byte-identical.

Resolve every effective clause through CON-983. Require one current companion,
one coordinated set, one pointer set, and one open review gate.

Require the one MEDIUM and six LOW findings from the 0.4.23 review to have
explicit code-backed dispositions. Re-test all twelve closed N7 findings and
every F-A through F-E hard stop.

## N8 disposition

| Finding | Disposition in this candidate |
|---|---|
| N8-M-1 | NFR-926 declares the poller and webhook terminals, adds every named edge, and defines OBS-905. TEST-1152 drives each terminal. |
| N8-L-1 | NFR-926 enumerates all six known collection sinks and makes the repository-wide production consumer scan authoritative. |
| N8-L-2 | REQ-1003 incorporates all eighteen retired-list witnesses directly and exempts none from the current scan. |
| N8-L-3 | REQ-1003 and TEST-1153 use the same complete classifier, including production constant definitions. |
| N8-L-4 | REQ-1003 adds `profile.rs:384` and uses `includes` so evidence never asserts a closed witness set. |
| N8-L-5 | NFR-926 adopts the unambiguous cbcl-bus owner-list wording and scopes tagged success to collections and predicates. |
| N8-L-6 | NFR-926 names both account-side refusal terminals. TEST-1152 requires zero deletion for each case. |

## Gate and changelog

Owner direction selects standalone first contact. A fresh-context cross-model
Tier-1 PASS remains mandatory before the Elephant SPL or test-first plan.

- **0.4.24-draft — 2026-08-23 — review candidate, not approved.** Adds the
  missing background and webhook busy terminals, exhaustive scan authority,
  and deterministic withdrawal refusal. No code or deployment authority is
  claimed.
