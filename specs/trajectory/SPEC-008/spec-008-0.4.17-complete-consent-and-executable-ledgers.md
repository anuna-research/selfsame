---
id: SPEC-008-amendment-0.4.17
title: SPEC-008 0.4.17-draft — Complete consent and executable ledgers
status: draft
version: 0.4.17-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.16-correlated-success-and-deliverable-bounds]] only where [[spec-008-0.4.17-complete-consent-and-executable-ledgers#CON-976]] says so"
adopts: "All effective 0.4.4 through 0.4.16 clauses except the exact replacements in CON-976"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.8-draft; cbcl-bus SPEC-053 0.16.10-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.16-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.17-draft — Complete consent and executable ledgers

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.16 review found one HIGH, four
> MEDIUM, and five LOW defects. This amendment closes each finding against the
> reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirement

### REQ-996 — Correlation, consent display, and transit bounds are complete

The tagged transition shapes and member sets in
[[spec-008-0.4.12-tagged-transition-closed-authority#REQ-984]] remain exact.
Credential/v2 uses these ASCII grammars:

```text
LegacyHandle = "@" 1*32( %x61-7A / DIGIT / "_" / "-" )

CbclSymbolChar = ALPHA / DIGIT / "_" / "-" / "." / "/" / "!" /
                 "?" / "+" / "*" / "<" / ">" / "=" / "@"
RoomShape = "@" 1*( CbclSymbolChar )
CanonicalRoomText = RoomShape with 2..129 ASCII octets
```

The alphabet matches pinned cbcl-rs revision
`febc6691e6dd2d5f7116b1a4d84c984b64717564`. The 129-octet cap is a
credential/v2 admission limit. It is not a deployed CBCL-symbol maximum. The
serializer SHALL NOT escape `/`.

The hub applies the admission classes in this order:

1. A value failing `RoomShape` is `MigrationRoomUnrepresentable`.
2. A shaped room over 129 octets is `MigrationPlanTooLarge`.
3. Count or aggregate overflow is `MigrationPlanTooLarge`.
4. A durable handle failing `LegacyHandle` is
   `MigrationHandleUnrepresentable`.

Length classification occurs only after `RoomShape` passes. An over-length
room therefore has one result. A room with invalid shape remains
`MigrationRoomUnrepresentable`, regardless of length.

The hub is the sole evaluator of those three results. It evaluates them in the
level-1 allocation transaction. That transaction locks and snapshots the
stable account, pending ceremony, enrolled key, and complete durable room set.
A negative result creates no success, carrier allocation, pairing room,
digest, or partial pending object. The WebSocket handler MAY return only the
existing cause-neutral refusal.

The existing `requestId` is the allocation correlation member. The browser
generates 32 random octets for every attempt. Its canonical encoding is
base64url without padding. The signed `selfsamepairallocate` request contains
that exact value. The hub stores it with the pending ceremony.

`selfsamepairallocated` contains exactly the same `requestId`. It contains no
room, digest, account, grant, or authority value. Like every deployed
hub-to-browser frame, it is unsigned and origin-less. TLS authenticates the
hub connection. The echo prevents request confusion but does not authenticate
the migration plan.

`socketGeneration` is a browser-local safe integer. The browser increments it
before installing callbacks on each new WebSocket object. Every callback
captures the generation of its socket. The value never crosses the wire and
need not survive reload.

The browser keys its active map by `(socketGeneration, requestId)`. An
acknowledgement advances only when exactly one live entry matches. The browser
atomically consumes that entry before starting the public carrier step. It
discards absent, unequal, stale, duplicate, or consumed values.

Close, error, replacement, and reconnect atomically cancel every live entry
for the old generation. The next socket uses the incremented generation. A
late callback from the old socket therefore cannot match or cancel a new
attempt.

A copied acknowledgement with the active value can start only the reversible
transport wait. It cannot create hub state or reach consent. The later
hub-signed offer authenticates the plan and every consequential value. Missing
or invalid signed evidence terminates the attempt with no identity effect.

On success, `migrationRooms` is the complete unique raw room-byte array. The
hub sorts it by unsigned octets. `roomSetDigest` derives from that exact array.
The hub-signed offer binds both values through `offerCoreDigest` and
`intentDigest`. No browser DOM value participates.

Before final migration consent, the browser SHALL:

1. verify the v2 offer under the current authenticated profile;
2. recognize the exact v2 member set, transition tag, and application;
3. recompute `roomSetDigest` from `migrationRooms`;
4. compare every retained carrier, ceremony, transcript, and device binding;
5. build one private typed plan from those authenticated values.

The Path-B reducer renders exactly that plan's rooms in order.
`affectedChannelsNow()` and every `.clog[data-room]` query SHALL NOT source
`CeremonyObservation.GrantVerified.affectedChannels`. No filter, paging,
collapse, or lazy rendering SHALL hide a room.

Every display path renders the complete ordered plan array. A summary MAY
precede it but SHALL NOT replace, truncate, collapse, page, filter, or defer any
room. The displayed room count and byte sequence equal the authenticated array
at confirmation time.

`migration-display-state-changed` is non-terminal. It retains the verified
plan, disables confirmation, and sends no frame. A local rerender rebuilds the
complete list only from that plan. Confirmation returns only after byte-exact
comparison succeeds.

The shipped `path-b-ceremony.mjs` `CHANNEL` expression SHALL be replaced. Its
replacement comes from the shared `CanonicalRoomText` source. A malformed
authenticated plan becomes terminal `migration-plan-invalid` for that
ceremony. It creates no credential or hub effect. Recovery requires a new
ceremony. It never becomes `invalid-grant-observation`.

The maxima in
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#REQ-988]] remain
unchanged. The maximum offer plaintext is 57,000 octets. The maximum reverse
plaintext is the 62,000-octet credential member.

ChaCha20-Poly1305 appends one 16-octet authentication tag. Its fixed
direction-separated nonce and associated data add no stored octet. No base64
expansion applies to the opaque rendezvous body.

```text
V2_MAX_SEALED_OFFER_OCTETS       = 57,000 + 16 = 57,016
PROTO-002 offer residual         = 69,632 - 57,016 = 12,616
V2_MAX_SEALED_REVERSE_OCTETS     = 62,000 + 16 = 62,016
PROTO-002 reverse residual       = 69,632 - 62,016 = 7,616
```

Both current rendezvous baselines accept only 4,096 octets. They cannot carry
either maximum sealed record. Production use therefore depends on
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]] and
[[PROTO-002-selfsame-rendezvous-v1#CON-304]]. Its Tier-1 gate remains open.

The complete production v1 comparison and substitution trace is:

```text
crates/selfsame-web-device/src/lib.rs:600       profile_facts substitution
crates/selfsame-web-device/src/lib.rs:1346      offer substitution
crates/selfsame-web-device/src/lib.rs:1360      provider-hint substitution
crates/selfsame-web-device/src/lib.rs:1369      statement substitution
crates/selfsame-app-identity/src/ceremony.rs:185 offer payloadVersion substitution
crates/selfsame-app-identity/src/ceremony.rs:308 offer payloadVersion comparison
crates/selfsame-app-identity/src/ceremony.rs:460 bundle payloadVersion substitution
crates/selfsame-app-identity/src/ceremony.rs:510 bundle payloadVersion comparison
crates/selfsame-app-identity/src/enrollment.rs:373 evidence-version comparison
crates/selfsame-app-identity/src/enrollment.rs:394 statement-payload comparison
crates/selfsame-app-identity/src/enrollment.rs:452 recognizer substitution
crates/selfsame-app-identity/src/enrollment.rs:552 statement-verification comparison
crates/selfsame-app-identity/src/enrollment.rs:662 evidence-version substitution
crates/selfsame-app-identity/src/profile.rs:385 profile comparison
crates/selfsame-app-identity/src/provider_hint.rs:115 provider-hint comparison
```

The trace is a baseline inventory, not finite permission. The source scan
covers every production version constant and literal. It includes
`PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and `payloadVersion`.
Credential/v2 SHALL reach no v1 comparison or substitution site. It SHALL NOT
call `EnrolmentAllocator::new_native`. Frozen v1 bytes remain byte-identical.

## Non-functional requirement

### NFR-919 — Authority and migration graphs are closed and executable

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
The finalizer's exact `cbcl-member` access is a write lock. Its whole room-table
locks are write locks.

Use the following bindings to the matrix rows. Grant creation is `G`, and the
credential/v2 finalizer is `F`. Exact grant revocation is `XR`, and observed
revocation is `OR`. New member enrollment is `ME`, and pairaccount claim is `PC`.
Per-frame authority read is `AR`; it uses the matrix's `unratified read`
sequence.

The authority graph covers only `cbcl-device-grant`, `cbcl-member`, and
`cbcl-pairaccount-grant`. Its exact cycle set remains:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

The generator SHALL reproduce those twelve cycles and every `AR` contention
case. It makes no claim about room or finalizer-tail tables.

The migration graph owns every room, finalizer-tail, and `cbcl-invite` lock.
Allocation takes stable account and pending ceremony first. Binding and member
locks follow. It then takes whole `cbcl-roommember` and
`cbcl-account-roommember` read locks in that order. Finalization uses the same
prefix and whole write locks.

Both single-table `write-grant` paths take an exact write lock on their first
read. They use `mnesia:read/3` with `write`. They perform no read-to-write
upgrade.

Both `redeem-and-grant` variants join two exact write locks. They take the
`cbcl-invite` token lock first. The applicable `cbcl-roommember` or
`cbcl-account-roommember` key follows. `cbcl-chat-invite:redeem-tx` also takes
its write lock on the first read. It performs no upgrade before update or
delete.

An operation touching both room tables takes both whole write locks in the
declared order. It acquires them before its first room read or write.
`revoke-handle-tx` therefore performs no `index_read`-then-exact-write upgrade.

The graph includes every `cbcl-invite` holder. It includes
`cbcl-chat-sealedgrant:put-tx`, whose first invite access is an exact read lock.
That operation then takes its exact `cbcl-sealedgrant` write. It takes no room
lock and performs no lock upgrade.

The generated table order places `cbcl-invite` before `cbcl-roommember`,
`cbcl-account-roommember`, and `cbcl-sealedgrant`. It places
`cbcl-roommember` before `cbcl-account-roommember`.

Finalizer, compensation, status, and operator paths use one generated tail
order. Compensation uses exact captured keys and no `?ALL` scan. Withdrawal
and reinstatement take stable account before alias and tombstone keys.
Read-only recovery acquires no later write lock.

The migration graph includes every lock and exact mode. It includes cross-lane
authority locks. Its only cycles are projections of the twelve authority
cycles. It has no additional cycle or upgrade. A missing operation, unstated
mode, upgrade, opposite order, or additional cycle is non-conforming.

Every affected transaction runs at Mnesia level 1. It uses an explicit integer
`Retries >= 2`. Nested transactions are prohibited. Consumers map `nomore`,
exhausted `#cyclic{}`, and `nested_transaction` to `authority-busy`. No partial
effect survives.

## Contract

### CON-976 — Replacements, dependencies, and parents are exact

This amendment applies only these replacements:

1. REQ-996 replaces
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-994]].
2. NFR-919 replaces
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#NFR-918]].
3. ADR-954 replaces
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#ADR-953]].
4. TEST-1134 through TEST-1138 collectively replace
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#TEST-1129]]
   through
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#TEST-1133]].

REQ-995 remains effective without amendment. Every F-A through F-E correction
remains a hard stop. SPEC-007 REQ-812 also remains a hard stop.

The exact cbcl-pairing survivor set contains TEST-030 through TEST-049. It also
contains TEST-050 except its replaced control portion and retired positive
witness. That witness remains a negative obligation. TEST-051 through TEST-056
and TEST-059 survive. TEST-057 and TEST-058 do not survive.

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
and TEST-093 through TEST-097. Every effective Selfsame test through TEST-1108
also survives. TEST-1134 through TEST-1138 are the current correction tests.
No superseded portion or open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.8, and
cbcl-bus 0.16.10. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

The reviewed `crates/selfsame-rendezvous` and did-crdt baselines each have a
4,096-octet cap. Both are deliberately non-conforming to
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. Credential/v2 production
enablement depends on closing the PROTO-002 Tier-1 gate.

The Selfsame parent
[[SPEC-008-production-pairing-claimant]] points to this candidate and names its
open review gate. The cbcl-bus parent
[[SPEC-053-key-root-identity#DEC-012]] points to the coordinated 0.16.10
candidate. The cbcl-bus GATE-04 owns the cross-repository transit evidence.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It authorizes
no code, allocation, release, or deployment.

## Decision

### ADR-954 — Complete display and executable traces precede implementation

The unsigned acknowledgement supplies only one-attempt correlation. Socket
generation prevents reconnect callbacks from crossing attempts. The later
hub-signed offer authenticates every plan value before complete display.

The person sees every authenticated room in order. No presentation mechanism
can hide a migrated room. Display completeness is part of exact consent, not a
browser preference.

The v1 inventory names every current version family. The lock graph defines
each symbol, acquisition point, invite holder, upgrade prohibition, and cycle
failure. Maximum sealed records carry explicit arithmetic and both
non-conforming baselines.

## Test specification

### TEST-1134 — Correlation and complete display cannot cross boundaries

Run two same-account ceremonies concurrently on one socket generation. Swap,
duplicate, suppress, reorder, and replay their acknowledgements. Mutate and
remove `requestId`. Only the exact active value starts its own transport wait.
Consumption occurs before that transition.

Close and replace the socket with live entries. Deliver every late callback
after reconnect. Require old entries to be cancelled and the new generation to
reject old callbacks.

Inject a copied acknowledgement with the active value but no hub allocation.
It reaches no consent, credential, or identity effect. Missing hub-signed
evidence terminates the attempt.

Render a maximum authenticated plan through every filter, paging, collapse,
and lazy-render path. Require every mechanism to remain disabled or display all
256 rooms. At confirmation, require the displayed count and ordered byte array
to equal `migrationRooms` exactly.

### TEST-1135 — Grammar, status, and version paths are disjoint

Generate every symbol branch, bare `@`, maximum `/`, and every one-over case.
Require invalid shape to be `MigrationRoomUnrepresentable`. Require a shaped
over-length room to be `MigrationPlanTooLarge`.

Use the same generated source in cbcl-pairing, wallet, canonical module, and
browser reducer. Require both grammar presentations and accepted languages to
be identical.

Mutate every DOM condition. The display stays exact or enters non-terminal
`migration-display-state-changed`. Rerender only from the plan. A malformed
signed plan enters terminal `migration-plan-invalid` and never
`invalid-grant-observation`.

Scan every production version constant and literal. Include all four
`payloadVersion` sites and both `evidenceVersion` sites. Prove v2 reaches none
and v1 bytes remain exact.

### TEST-1136 — Maximum sealed records transit both declared rendezvous

Generate the 57,000-octet offer and 62,000-octet reverse plaintext maxima.
Require ChaCha20-Poly1305 output lengths of 57,016 and 62,016 octets. Require
residuals of 12,616 and 7,616 octets against the PROTO-002 cap.

Put, repeat-put, get, and repeat-get each exact record through both rendezvous
implementations. Reject 69,633 octets before storage. Prove both reviewed
4,096-octet baselines fail. GATE-04 cannot close before both implementations
and the PROTO-002 gate pass.

### TEST-1137 — Installed state and every lock graph close independently

Change only the profile digest with authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Generate the twelve named authority cycles and every `AR` writer contention.
Generate the complete migration graph. Require the restored acquisition point,
invite-before-room order, and no additional upgrade or cycle.

Include both `write-grant` paths, both `redeem-and-grant` paths,
`revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`, and
`cbcl-chat-sealedgrant:put-tx`. Force bounded retry exhaustion and prohibited
nesting. Require `authority-busy`, no partial effect, and writer progress.

### TEST-1138 — Effective ledgers, parent pointers, and gates are closed

Static checks require every survivor and replacement, every version site, and
all 42 cbcl-bus GATE-04 boxes. They require the Selfsame parent pointer to this
candidate. They also require the cbcl-bus parent DEC-012 pointer to 0.16.10.

Require the exact three-repository test ledgers, two named non-conforming
rendezvous baselines, and explicit sealed-record arithmetic. Removing any
obligation fails.

## Changelog

- **0.4.17-draft — 2026-08-23 — review candidate, not approved.** Makes room
  consent complete, version and lock traces executable, both rendezvous gaps
  explicit, and parent routing exact. No implementation or deployment
  authority is claimed.
