---
id: spec-008-0.5.5-claude-adversarial-review
title: SPEC-008 0.5.5-draft — Tier-1 adversarial review of the coordinated bounded final-command reissue
status: complete
verdict: REJECT
tier: 1
review-type: cross-model-adversarial
reviewer-model-family: Anthropic Claude
reviewer-model: claude-opus-5
authentication-path: claude.ai subscription, first-party provider, max subscription
generated-under-review: OpenAI GPT-5 `gpt-5.6-sol`, Codex session `01a029aa-9127-7c42-ad28-81512b91ded6`
circus-attempt: spec008-consolidated-review attempt 6
driver-sha256: 4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e
candidate-commits: "selfsame `4620b7c776f519ef08be8621094fa638bf602b8b` SPEC-008 0.5.5-draft and SPEC-007 0.3.3-draft; cbcl-bus `8141fe4546e6c69293acc534f4aa92020af98a3e` SPEC-053 0.17.5-draft; cbcl-pairing `16a3634d29c30ea8991fa237e82a5a8189abfe35` SPEC-001 0.5.4-draft; did-crdt `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` SPEC-037 0.1.2 deprecated"
implementation-baselines: "selfsame `0220cec2dec44cd95d4f411ea4814d790b6716d2`; cbcl-bus `ce894a7f2debc111b610296b1a3780de6084e37e`; cbcl-pairing `c22f2d0526432c01c56d64be35a8d4f87516ec92`; did-crdt `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`; anuna-ssi namespace pin `c7d462029841ea1884bb6f089732058d8838728d`"
generation-session-statement: "This review ran in a fresh Anthropic Claude session under Circus attempt 6. It imported no conclusion from any earlier Claude conversation. It read the 0.5.4 report only after an independent first pass over the four coordinated parents, the deprecated did-crdt disposition, and the five pinned code baselines."
date: 2026-08-24
---

# SPEC-008 0.5.5-draft — Tier-1 adversarial review

Verdict: **REJECT**.

## Orientation

Intent: decide whether the coordinated 0.5.5 reissue is singular, safe,
bounded, and implementable without invention.

Metaphor: a bridge inspection before the first vehicle. Every span the previous
inspection questioned now carries its stated load. The plaque describing why the
bridge was built no longer matches the ground beneath it.

Structure:

```text
independent pass over four parents + five code baselines
        -> one new finding + six observations
        -> attempt-5 N-1 through N-9 re-test
        -> earlier N, C, H, M, L and F-A through F-E re-test
        -> verdict
```

Decisions:    zero CRITICAL findings; zero HIGH; one MEDIUM; six LOW
Load-bearing: the parent's named failure modes contradict the pinned baseline
Controls:     PASS requires zero CRITICAL, HIGH, and MEDIUM findings
Open:         one blocking correction and six recorded observations
Detail:       the finding sections and the assessment sections below

This report uses descriptive controlled language. Its wikilink anchors are
fully qualified. Cross-vault identifiers stay unlinked, matching the parents.

## Reviewer identity, authentication, and independence

The reviewer is Anthropic Claude, model `claude-opus-5`. The generating author
is OpenAI GPT-5 `gpt-5.6-sol`. The families differ, so the cross-model
requirement holds.

The launcher removed `ANTHROPIC_API_KEY` before starting this session. The
reviewer tested that variable in the process environment. The variable is unset,
and a counting scan of the environment matched zero entries. No value reached
this report.

`claude auth status --json` ran with that variable removed. It reported
`authMethod = claude.ai`, `apiProvider = firstParty`, and
`subscriptionType = max`. This report records no email, organization
identifier, access token, API key, or account identifier.

The driver SHA-256 is
`4593b4402d6bbcf04be2884ec32b8defc726dbd574aa58a23a74f1008150f95e`.

The session is fresh. The reviewer completed the independent pass before opening
the 0.5.4 report. Every prior finding below carries an independently re-derived
disposition.

## Exact review set, baselines, and pre-implementation status

Each candidate object exists and carries the declared version.

| Repository | Commit | Object | Declared version | Verified |
|---|---|---|---|---|
| selfsame | `4620b7c776f519ef08be8621094fa638bf602b8b` | `specs/SPEC-008-production-pairing-claimant.md` | 0.5.5-draft | yes |
| selfsame | `4620b7c776f519ef08be8621094fa638bf602b8b` | `specs/SPEC-007-cbcl-pairing-cutover.md` | 0.3.3-draft | yes |
| cbcl-bus | `8141fe4546e6c69293acc534f4aa92020af98a3e` | `specs/SPEC-053-key-root-identity.md` | 0.17.5-draft | yes |
| cbcl-pairing | `16a3634d29c30ea8991fa237e82a5a8189abfe35` | `specs/SPEC-001-reusable-blind-pairing.md` | 0.5.4-draft | yes |
| did-crdt | `6d1cdbebc097314323e7f8479cbfd28b3f8cc7d1` | `specs/SPEC-037-pairing-rendezvous-body-bound.md` | 0.1.2 deprecated | yes |

The SPEC-008 blob is `d9c59f6ffdcc8d123c979032defbd8e3cac8665d`. The SPEC-007
blob is `2401a05b0042455a9a446c0b10b11369189104a4`. The SPEC-053 blob is
`71f8169cac749b6f88e2e5de54027c4b56e74e88`. The SPEC-001 blob is
`2ae21fc15d0118ec57a6cdd5aceebeb14bca1e0d`. The SPEC-037 blob is
`5ff881b24a37e2ed3cd142f963769fa9617f379e`.

Every implementation baseline resolves as a commit. They are selfsame
`0220cec2`, cbcl-bus `ce894a7f`, cbcl-pairing `c22f2d05`, did-crdt `1f409a42`,
and the anuna-ssi namespace pin `c7d46202`.

The reviewer read every candidate through `git show COMMIT:path`. No
working-tree substitute entered this review.

The candidate is intentionally unimplemented. The reviewer treated absent
candidate code as expected. It judged the prose against the pinned baselines.

## Severity counts

| Severity | Count | Blocking |
|---|---:|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 | yes |
| LOW | 6 | no |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. One blocking finding
survives, so the verdict is REJECT.

## Findings

### N-1 Two named failure modes contradict the pinned baseline and the parent's own changelog

Severity: MEDIUM. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant]] opens its `Named failure modes` section
with a scope sentence. It states that four mechanical failures explain the
specification. It labels the first two repaired and calls the final two current
design inputs.

Both current design inputs misdescribe the declared implementation baseline
`0220cec2dec44cd95d4f411ea4814d790b6716d2`.

**FM-3 is false in two of its three clauses.** It states that every construction
site of `SelfsameVerificationContext` is `local_demo` or a test.

`src-tauri/src/cbcl_context.rs:342` constructs that value in production code.
The file's first `#[cfg(test)]` attribute sits at `:359`, so line `342` is not
test code. The construction closes `assemble_claimant`, declared at `:244`.

That function is reachable from the person's own screen.
`src-tauri/src/cbcl_pairing.rs:171` calls it inside the
`#[cfg(not(feature = "local-pairing-demo"))]` branch declared at `:140`. The
enclosing command is `cbcl_pairing_start`, which `src/pairing.js:52` invokes.
`crates/selfsame-web-device/src/lib.rs:205` is a second production site.

FM-3 also states that a production wallet cannot verify against a real profile.
`assemble_claimant` calls `selfsame_app_identity_net::profile::fetch_or_cached`
at `:265`. It then calls `cbcl_relay::verify_invitation_origin` at `:275`,
`webfinger::fetch_and_verify` at `:306`, and `state::resolve_closure` at `:317`.
Those calls verify one live profile, one live authority, and one live closure.

Only FM-3's middle clause holds. `app_grant.rs` commands are registered at
`src-tauri/src/lib.rs:96` through `:101`, and neither `src/index.html` nor
`src/pairing.js` invokes any of them.

**FM-4 states the exact inverse of the baseline.** It states that the binary
contains only the demo conformance digest `[19; 32]`.

`crates/selfsame-pairing/src/lib.rs:16` and `:17` gate `local_demo` behind the
`local-pairing-demo` feature. `local_demo.rs:15` holds the demo digest, so an
ordinary build omits it. [[SPEC-008-production-pairing-claimant#TEST-904]]
requires exactly that absence.

`src-tauri/src/cbcl_registry.rs:17` declares `APPROVED_CONFORMANCE` with one
production entry. Its bytes begin `0x70, 0x29, 0xf2, 0x22` at `:25`. The module
comment at `:9` and `:10` states that the demo digest never appears there.
`production_relay_policy()` returns that constant at `:38` with
`allow_loopback: false`.

An ordinary binary therefore carries the production `anuna-1` digest and omits
`[19; 32]`. FM-4 asserts the opposite of both facts.

The parent also contradicts itself. Its `0.2.1` changelog entry records the
`anuna-1` ratification on 2026-08-20 and names the same evidence document.
[[SPEC-008-production-pairing-claimant#REQ-1005]] treats that registry as
standing, and [[SPEC-008-production-pairing-claimant#REQ-1006]] requires it to
stay compiled and unchanged with a corrected comment.

The stale text is inherited. The `0.1.0` changelog entry dates the failure-mode
list to a 2026-08-18 gap analysis. The 0.5.1 review closed the same defect class
for FM-1 and FM-2 by relabelling them historical. No revision re-tested FM-3 or
FM-4 against the baseline they now pin.

The consequence is a planning defect rather than a mechanism defect. Every
requirement, contract, bound, and test in the parent stays internally
consistent. A plan built from the parent's descriptive baseline records no existing
production trust anchor, no existing real verification context, and no existing
live-profile path. All three exist at `0220cec2`.

The parent's own [[SPEC-008-production-pairing-claimant#TEST-1157]] requires
every current witness to resolve. This section does not.

Correction: restate FM-3 and FM-4 against `0220cec2`, or label both historical
in the manner already applied to FM-1 and FM-2.

### N-2 The generation provenance names the wrong correction input

Severity: LOW. Owner: selfsame repository owner.

The parent's `Tier-1 generation provenance` section names its immediate
correction input. It names
[[spec-008-0.5.2-claude-adversarial-review-2026-08-24#Collected corrections and owners]].

That anchor resolves. The pointer is nevertheless stale for this revision.

The 0.5.5 changelog states that this revision coordinates final-command capacity
and the remaining review observations. Those are the 0.5.4 report's N-1 through
N-9, recorded in its own collected-corrections table.

The same paragraph states that the reissue resolves the named report's
self-contained-authority defects. That description matches the 0.5.3 revision.

The section supplies evidence and no current value, so no obligation changes.

### N-3 The signed negative depends on an optional expiring tombstone

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus CON-036 permits one signed `not-finalized` response. It requires final
status to be absent and the locked pending state to be unable to finalize.

cbcl-bus REQ-082 states that a bounded replay tombstone MAY retain a request
digest and terminal class through the original expiry. That expiry is
`pendingExpiresAt`, at most 600 seconds after allocation.

A conforming hub therefore retains nothing after the offer window. cbcl-bus
TEST-119 requires a mutated ceremony to return an indistinguishable unknown
result, so an unheld ceremony cannot receive a signed negative.

The wallet needs recovery precisely after that window.
[[SPEC-008-production-pairing-claimant#CON-989]] states that `unknown` leaves
the pending slot unchanged. It also states that only a verified `not-finalized`,
explicit unlink, or root purge clears a post-payload pending slot.

A ceremony that expired at the hub after a durable payload send is therefore
clearable only by explicit unlink once the tombstone lapses. The outcome is
fail-closed, grants no capability, and follows a stated clause.

### N-4 One account-scope disposition is narrower than the value's travel

Severity: LOW. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#CON-986]] states that the hub-signed
account-principal digest and scope are authenticated non-secret inputs inside
the protected channel.

`accountScopeId` also travels outside that channel. cbcl-bus CON-012 carries it
in `FinalStatusCoreV2`, which the compact `finalStatusJws` payload contains.
cbcl-bus CON-036 returns that JWS over direct HTTPS.
[[SPEC-008-production-pairing-claimant#REQ-1006]] persists it in the installed
record.

[[SPEC-004-application-scoped-identity#REQ-217]] prohibits the raw or encoded
scope in a named list of artefacts. That list includes a status entry and any
other public protocol artefact.

No coordinated clause reconciles the two texts. The reviewer judges the posture
sound. Every reader of those objects is the allocating hub or the paired wallet,
and neither object is public.

[[SPEC-004-application-scoped-identity#ADR-223]]'s own radius discussion states
that no requirement claims the scope's confidentiality as a control.

### N-5 One capacity counter mixes transient and permanent records

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-bus REQ-082 caps active credential/v2 records at 65,536. It refuses before
allocation when that bound is exceeded.

cbcl-bus CON-036 states that immutable accepted statuses count inside the same
cap. Those statuses survive expiry, restart, room migration, and browser loss.

Lifetime successful pairings therefore reduce concurrent allocation capacity
monotonically. Only an explicit service data-erasure operation returns budget.

The behaviour is stated and fail-closed. cbcl-bus TEST-119 fills the index to
the cap and requires bounded work and typed unavailability.

### N-6 One narrow relay lifetime admits an allocation that cannot prepare

Severity: LOW. Owner: cbcl-bus repository owner.

cbcl-pairing NFR-023 keeps the v2 mailbox lifetime between 60 and 600 seconds.
cbcl-bus REQ-082 refuses a carrier with fewer than 300 remaining seconds, so a
carrier with exactly 300 remaining is admitted.

`pendingExpiresAt` then equals that carrier expiry. Offer preparation samples
`issuedAt` and expires the ceremony when the remaining interval falls below 300
seconds.

Preparation therefore succeeds only when `issuedAt` equals the allocation
second. Every later whole second atomically expires the ceremony.

The outcome is deterministic and fail-closed, and it writes no identity effect.

### N-7 Two consolidated contracts carry no trace lines

Severity: LOW. Owner: selfsame repository owner.

[[SPEC-008-production-pairing-claimant#CON-988]] and
[[SPEC-008-production-pairing-claimant#CON-989]] carry no `Implements:` line and
no `Verified by:` line.

Their neighbours carry both. [[SPEC-008-production-pairing-claimant#CON-901]],
[[SPEC-008-production-pairing-claimant#CON-902]],
[[SPEC-008-production-pairing-claimant#CON-903]],
[[SPEC-008-production-pairing-claimant#CON-986]], and
[[SPEC-008-production-pairing-claimant#CON-987]] state them.

Both contracts remain reachable. CON-988 has four inbound references and CON-989
has two. [[SPEC-008-production-pairing-claimant#TEST-1161]] names CON-989 in its
`Validates:` line.

The 0.5.5 revision closed the same asymmetry for CON-987. Two contracts still
carry the older shape.

## Disposition of the 0.5.4 findings

Each row is independently re-derived at the current candidates and the pinned
code.

| 0.5.4 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 two hub authority-command arguments carry no size bound | **CLOSED** | cbcl-bus CON-012 now bounds `rawGrant` at 1 through 49,152 compact-JWS ASCII octets and `resolverClosure` at 1 through 1,048,576 octets. It states that the closure bound equals the Selfsame resolver client's `MAX_CLOSURE_OCTETS`. That constant reads `1_048_576` at `crates/selfsame-app-identity-net/src/state.rs:342` and gates `bounded_body` at `:559`. The grant bound sits below `MAX_GRANT_OCTETS` at `crates/selfsame-app-identity/src/grant.rs:55` and `MAX_GRANT_CHARS` at `crates/selfsame-app-identity/src/ceremony.rs:70`, both `65_536`. CON-012 also sets Cowboy `max_frame_size` to 1,500,000 and refuses before the application parser. |
| N-2 the logical-body contract has no traceability edge | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-987]] now states `Implements:` [[SPEC-008-production-pairing-claimant#REQ-1006]] and `Verified by:` [[SPEC-008-production-pairing-claimant#TEST-1160]]. REQ-1006 states the matching inbound `Trace:` edge. |
| N-3 one signed staging-receipt member has no stated meaning | **CLOSED** | cbcl-bus CON-012 removes the member. `browser-staging-receipt-v2` now declares thirteen members, and the signature input enumerates the preceding twelve. The reviewer recomputed the grammar maximum as 3,015 octets, which matches the stated value exactly. The contract states that the browser-local staging generation does not cross the wire object. |
| N-4 one prohibition list depends on prepositional attachment | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] now opens the sentence with the credential/v2 scope. No attachment judgement remains, and REQ-1006 keeps the standing registry compiled. |
| N-5 two signature clauses name their input less exactly than the third | **CLOSED** | cbcl-bus CON-012 now states that the authority response signature and the staging receipt signature each cover that exact 32-octet input. The offer signature keeps its direct concatenation form. Every signer and key is named. |
| N-6 one current hub clause fails strict controlled-language lint | **CLOSED** | The reviewer extracted all fifteen SPEC-053 current-law sections by their exact heading boundaries. Every extraction returns zero errors and zero warnings under `usdd-lint.sh --type descriptive --strict`. |
| N-7 the bound authority outcome is unreachable in this increment | **CLOSED** | cbcl-bus CON-012 now records the `bound` arm as a fail-safe for a colliding, restored, or corrupt reciprocal-alias row. It states that a normal Path-A first link reaches only `no-binding`. |
| N-8 a backwards hub clock step yields a non-conforming offer core | **CLOSED** | cbcl-bus REQ-082 now returns closed `unavailable` for an interval above 600 seconds. It preserves the pending record, emits no offer core, names the backwards step, and permits an authenticated resample. |
| N-9 the post-payload checkpoint expiry rule names only one role | **CLOSED** | cbcl-pairing CON-030 now states that only a claimant checkpoint may use `null`, and that an allocator checkpoint always carries its numeric expiry. It adds that the allocator never inherits the retention rule. TEST-065 requires refusal for a substituted allocator `null`. |

## Disposition of the 0.5.3 findings

| 0.5.3 finding | Disposition | Re-derived evidence |
|---|---|---|
| N-1 hub authority response has no grammar | **CLOSED** | cbcl-bus CON-012 declares `authority-status-response-v2` as a six-member deterministic-CBOR array. The reviewer recomputed the maximum bound-DID encoding as 1 + 45 + 34 + 34 + 6 + 515 + 66, which equals the stated 701 octets against a 768-octet transport bound. |
| N-2 ordinary-build registry prohibition contradicts two current tests | **CLOSED** | REQ-1006 scopes the prohibition to credential/v2 and keeps the credential/v1 registry compiled and unchanged. `src-tauri/src/cbcl_registry.rs:17` survives. |
| N-3 offer window has no stated terminal effect | **CLOSED** | cbcl-bus REQ-082 samples `issuedAt` under the pending lock, expires below 300 remaining seconds, and refuses above 600. CON-012 makes the interval exclusive with no clock-skew allowance. |
| N-4 post-payload refusal strands claimant recovery | **CLOSED** | cbcl-pairing CON-028 projects `payload -> receipt` with no refusal arm. CON-030 makes an attempted post-payload refusal invalid input, and CON-032 keeps the checkpoint in that state. |
| N-5 eleven object kinds carry no integer assignment | **CLOSED** | cbcl-pairing CON-028 assigns `0 offer` through `10 receipt`. The control arm fixes `1: 1..8` and the large arm fixes `1: 0 / 9 / 10`, so the arms and the table agree. |
| N-6 claimant checkpoint wrapping key has no info encoding | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-986]] states `checkpointInfo` with explicit `U32BE` length prefixes. It names the raw 32-octet salt and the extract order. `crates/selfsame-app-identity/src/hierarchy.rs:101` declares `HierarchyRoot(Zeroizing<[u8; 64]>)`. |
| N-7 one checkpoint member carries no name | **CLOSED** | cbcl-pairing CON-030 comments and restates the fifth member as `carrierCeremonyId`, and TEST-065 requires the equality. |
| N-8 declared Selfsame test set omits the other Selfsame parent | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-985]] names TEST-901 through TEST-915 and TEST-1156 through TEST-1161 here, and TEST-801 through TEST-821 in the other parent. The reviewer counted fifteen, six, and twenty-one headings. |
| N-9 one construction count cannot be resolved | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-988]] numbers seven ordered steps, and the reviewer counted seven. |
| N-10 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | REQ-1006 requires the comment to cite [[SPEC-007-cbcl-pairing-cutover#CON-806]]. `src-tauri/src/cbcl_registry.rs:1` still reads the old citation at `0220cec2`, which is declared implementation work. |
| N-11 one code citation names the wrong line range | **CLOSED** | The parent cites `src/pairing.js:159`, and that line reads `async function scan() {` at `0220cec2`. |

## Disposition of the 0.5.2 findings

| 0.5.2 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 credential/v2 key schedule exists in no current parent | **CLOSED** | cbcl-pairing CON-031 states a thirteen-member public context, `TH`, an extract under an empty salt, seven expands, both Finished values, the counter rule, the nonce rule, and the AAD map. The reviewer counted thirteen members. Version-1 labels stay disjoint at `src/channel.rs:118` through `:122`, which read `pairing-v1`. |
| C-2 nine of eleven logical body grammars exist in no current parent | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-987]] declares nine bodies, and the reviewer counted nine. The offer resolves to cbcl-bus `signed-offer-v2` and the receipt to cbcl-pairing `credential-v2-receipt-body`. |
| H-1 pre-socket CON-220 gate loses its binding step | **CLOSED** | [[SPEC-004-application-scoped-identity#CON-220]] numbers six obligations, and its steps 1 through 5 match CON-988's restatement exactly. Step 6 needs a CON-409 record that credential/v2 never creates, and CON-988 substitutes explicit CPace digest binding. |
| H-2 signed offer-core member has no defined input | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-806]] fixes six descriptor members and unique origins. cbcl-bus CON-035 selects the unique descriptor by exact relay-origin equality and refuses caller digests. |
| M-1 safety parent contradicts the offer-core source rule | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-806]] states that credential/v2 takes the application identifier and origin only from the CON-988-bound live profile. |
| M-2 two parents disagree about the relay-origin bound | **CLOSED** | cbcl-pairing CON-027 declares a 272-octet version-2 origin and keeps version 1 unchanged. `schemas/pairing-v1.cddl:13` still reads `relay-origin = tstr .size (1..255)` at `c22f2d05`. |
| M-3 post-final wallet recovery has no path after the window | **CLOSED** | CON-989, cbcl-bus CON-036, and cbcl-pairing CON-032 state the token, commitment, route, closed request, four-arm response, and reducer. The reviewer recomputed the request maximum as 2,187 octets against 2,304, and the largest response as 8,294 octets against 9,216. |
| L-1 two parents decompose one claimant differently | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-807]] and CON-986 name the same five functions and the same recovery adapter. |
| L-2 profile grammar attributed to an absent SPEC-004 amendment | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#CON-806]] states that it defines `cbclPairingRelays` itself. |
| L-3 registry module comment cites the excluding contract | **CLOSED as a normative instruction** | See the 0.5.3 N-10 row. |
| L-4 one version constant declared twice under one name | **CLOSED** | REQ-1005 records one duplication. `crates/selfsame-app-identity/src/lib.rs:109` and `crates/selfsame-app-identity/src/profile.rs:60` each read `pub const PROFILE_VERSION: i64 = 1;` at `0220cec2`. |

## Disposition of the 0.5.1 findings

| 0.5.1 finding | Disposition | Re-derived evidence |
|---|---|---|
| C-1 declared hub test set exists only in trajectory | **CLOSED** | cbcl-bus declares TEST-115 through TEST-119, and the reviewer counted five headings. |
| H-1 ceremony identifier unreachable and ambiguous | **CLOSED** | cbcl-pairing REQ-031 names the carrier ceremony identifier the sole credential/v2 identifier. The reviewer traced it through carrier, context, every body, every command, every response, both checkpoints, recovery, receipt, and installed state. |
| H-2 reconnect recovery has no command | **CLOSED** | cbcl-bus CON-012 carries `selfsamepairstatus`, the socket-generation digest, and the device recovery proof. `cbcl-chat-session-ws.lfe:62` and `:63` define `gen-conn-nonce` as sixteen strong random octets. |
| H-3 four offer-core members carry no grammar | **CLOSED** | The reviewer counted exactly twenty `OfferCoreV2` members. `deviceDid` carries the 56-octet `did:key` grammar with a multicodec check. |
| H-4 rendezvous dependency has no consumer | **CLOSED** | CON-986 forbids every Selfsame and did-crdt rendezvous edge. did-crdt SPEC-037 0.1.2 keeps the deprecation and burns its former identifiers. |
| H-5 Selfsame rendezvous has no capacity bound | **CLOSED** | The candidate diff from `1f409a42` to `6d1cdbeb` adds one specification file and changes nothing else. `src/service/rendezvous.rs` still declares `MAX_SLOT_BYTES` as 4096. |
| H-6 REQ-812 amendment uses an unavailable channel | **CLOSED** | [[SPEC-007-cbcl-pairing-cutover#REQ-812]] states the boundary directly, and all four parents record the same coordinated set and session. |
| H-7 hub names a cbcl-pairing test that does not exist | **CLOSED** | cbcl-pairing TEST-063 exists, and the reviewer counted eight credential/v2 test headings. |
| M-1 `record_pairing_trust` prohibition reaches a live path | **CLOSED** | CON-986 scopes the prohibition to credential/v2. The standing caller is `src-tauri/src/app_grant.rs:539`. |
| M-2 exact-pair consent boundary has no owning function | **CLOSED** | CON-986 gives `authorise_claimant_relay` the prompt and gives `prepare_claimant` the policy write after CPace binding. |
| M-3 `WIRE_VERSION` names half of its namespace | **CLOSED** | REQ-1005 states that the constant governs both record kinds. `crates/selfsame-core/src/record.rs:329` and `:396` carry the offer sites, and `:462` and `:490` carry the grant sites. |
| M-4 did-crdt worst-case store rises seventeen-fold | **CLOSED** | The rendezvous service stays byte-identical at the candidate commit. |
| L-1 executor order stated in three places with two shapes | **CLOSED** | CON-902, CON-986, and TEST-1158 state one order. |
| L-2 schema-migration enumeration misses a third site | **CLOSED** | [[SPEC-008-production-pairing-claimant#NFR-928]] names `cbcl-chat-roomcfg:transform-room/1`, and `cbcl-chat-roomcfg.lfe:81` defines it over `mnesia:transform_table`. |
| L-3 two failure modes misdescribe the pinned code | **REOPENED for FM-3 and FM-4** | The 0.5.1 correction relabelled FM-1 and FM-2. FM-1 holds at `src-tauri/src/cbcl_pairing.rs:177`, and FM-2 holds at `src-tauri/Cargo.toml:46`, which enables `rustls-tls-webpki-roots`. FM-3 and FM-4 were never re-tested; see [[spec-008-0.5.5-claude-adversarial-review-2026-08-24#N-1 Two named failure modes contradict the pinned baseline and the parent's own changelog]]. |
| L-4 stale `cbcl_registry.rs` comment | **CLOSED as a normative instruction** | See the 0.5.3 N-10 row. |

## Hard-stop disposition, F-A through F-E

| Hard stop | Disposition | Re-derived evidence |
|---|---|---|
| F-A exact application plus relay, never relay-only | **CLOSED** | [[SPEC-008-production-pairing-claimant#CON-903]] keys the sealed set on the exact pair. It excludes a relay-only key, a global allowlist, a conformance-digest registry, and an application wildcard. REQ-1006 requires a fresh prompt for a new application on an accepted relay. The replaced control is `crates/selfsame-app-identity/src/cbcl_relay.rs:68`. |
| F-B authenticated display, never the peer's claimed string | **CLOSED** | cbcl-pairing CON-029 copies every displayed value from the authority input. It requires byte equality on every overlapping value and forbids generic fields and the wire `authority_summary`. REQ-1006 adds peer labels, caller booleans, and DOM room values. The pinned surface still carries both prohibited shapes at `src-tauri/src/cbcl_pairing.rs:57` and `:63` through `:66`. |
| F-C split plan, preview, and executor replace the current call order | **CLOSED** | At `0220cec2` the scope read, derivation, issuer creation, WebFinger call, and closure resolution all precede `connect_wss`. CON-902, CON-986, and [[SPEC-007-cbcl-pairing-cutover#REQ-812]] move every one after final approval, and TEST-1158 instruments them. |
| F-D pending fresh scope traced end to end | **CLOSED** | cbcl-bus REQ-082 allocates the account identifier, scope, and snapshot nonce inside one level-1 transaction. The stable principal commits the raw identifier through a domain-separated SHA-256 preimage, and `OfferCoreV2` carries no raw account identifier. |
| F-E what does and does not prove relay operatorship | **CLOSED** | [[SPEC-008-production-pairing-claimant#ADR-902]] states that the application profile does not prove relay operatorship. [[SPEC-007-cbcl-pairing-cutover#CON-806]] keeps operator and privacy policy display-only, and TEST-915 keeps the two-operator depth test open. |

## Inherited SPEC-007 REQ-812 boundary

[[SPEC-007-cbcl-pairing-cutover#REQ-812]] remains a hard stop and states the
credential/v2 boundary directly.

| REQ-812 clause | Coordinated agreement |
|---|---|
| no application identity effect before preliminary approval | REQ-902, CON-902, CON-986, and TEST-1158 agree. |
| preliminary approval authorizes one zeroizing derivation and a pure preview | CON-986 authorizes one custody call and forbids signature, publication, grant, alias, bundle, and durable write. |
| no signing, publication, resolution, issuance, persistence, staging, or payload before final approval | CON-986 and cbcl-bus REQ-082 agree. The hub performs no account, alias, grant, membership, room, or successor write at preliminary approval. |
| final approval authorizes only the declared causal construction | CON-986 lists one order, and TEST-1158 requires it. |
| one encrypted non-authorizing completion checkpoint before the first causal effect | CON-986 requires the sealed pending completion first and states its exact wrapping-key derivation. |
| browser staging is crash-safe and non-authorizing until immutable acceptance | cbcl-bus CON-012 defines a thirteen-member staging receipt with twelve signed members. The reviewer recomputed its maximum as 3,015 octets against the stated 4,608. |
| compensation touches only ceremony-owned effects | CON-986 routes compensation through [[SPEC-004-application-scoped-identity#CON-204]] and forbids a change to operator withdrawal state. |

The boundary holds.
[[spec-008-0.5.5-claude-adversarial-review-2026-08-24#N-1 Two named failure modes contradict the pinned baseline and the parent's own changelog]]
does not breach it. That finding concerns descriptive prose and creates no early
identity effect.

## Adversarial question assessment

| Question | Result |
|---|---|
| 1 exact application-relay TOFU resists a new application; operatorship residual honest | **YES.** CON-903 keys on the exact pair, REQ-1006 requires a fresh prompt for a new application on an accepted relay, and ADR-902 states the residual plainly. |
| 2 CPace binds the origin-authenticated profile before durable policy, display, custody, or effect | **YES.** CON-988 returns an origin-recognised candidate, denies it verifier, display, policy, and key authority, and requires both Finished values first. The profile digest enters `ci`, `ad`, and the public context. |
| 3 no peer, carrier, DOM, route, verifier, or caller value reaches either consent screen early | **YES.** cbcl-pairing CON-029 builds the display only from the authority input after byte equality. The verifier returns a closed verdict, and the final screen adds only the locally derived DID and fingerprint. |
| 4 every effect follows its consent gate | **YES.** REQ-902, CON-902, CON-986, REQ-812, CON-807, cbcl-bus REQ-082, and TEST-1158 state one ordering with no gap. The preview disclosure follows preliminary approval and the screen states it. |
| 5 fresh scope bootstrap removes the enrolment cycle without leaking the account identifier | **YES.** The hub allocates fresh CSPRNG values inside its authenticated transaction. The principal commits the raw identifier through a domain-separated preimage, and no offer member carries it. |
| 6 one carrier ceremony identifier throughout | **YES.** The reviewer traced the value through carrier, CPace context, all eleven bodies, all seven commands, all eight responses, both checkpoints, recovery, receipt, and installed state. |
| 7 two implementations reproduce the schedule, kinds, bodies, predecessors, and checkpoint KDF | **YES.** CON-031 states the thirteen-member context and the complete schedule. CON-028 assigns all eleven kinds. CON-987 states nine bodies, and CON-986 states the checkpoint KDF with explicit length prefixes. Every hashed map uses integer or short text keys, so both deterministic orderings coincide. |
| 8 every declared maximum recomputes | **YES.** The reviewer recomputed 50,221, 1,583, 1,570, 701, 3,015, 1,470,680, and 29,320 by hand and matched each exactly. The 49,152, 1,048,576, and 1,500,000 values match the code and the transport. |
| 9 Cowboy refuses complete and fragmented oversize before the parser | **YES.** Cowboy 2.12.0 `cowboy_websocket.erl:450` refuses a single frame above `max_frame_size`. Line `:494` refuses the cumulative fragment buffer before accumulation. Line `:665` maps `badsize` to close code 1009. Decoded argument bounds refuse before lookup, mutation, or NIF entry. |
| 10 the closure limit matches the resolver client; the grant limit matches payload and recognizer | **YES.** `MAX_CLOSURE_OCTETS` reads `1_048_576`. The 49,152 grant bound equals CON-987's payload member and sits below the `65_536` generic recognizer. |
| 11 authority and staging signature inputs are exact 32-octet results; every signer fixed | **YES.** Both clauses state the exact 32-octet input over fully enumerated deterministic-CBOR values. The hub uses the offer `kid` and key, and the browser uses the installation device key. |
| 12 removing the staging generation leaves stale substitution prevented | **YES.** The receipt binds application, ceremony, account, scope, device, offer, payload, grant, issuer, profile, and recovery commitment under a deterministic signature. A stale receipt for the same ceremony is byte-identical to the current one. |
| 13 signed `bound` status is a safe fail-safe and cannot authorize a mismatched preview | **YES.** cbcl-bus CON-012 records the fail-safe scope. CON-987 requires a bound DID byte-identical to `previewIssuerDid` and permits only refusal otherwise. |
| 14 one exclusive deadline with exact outcomes at every boundary | **YES.** `expiresAt` equals `pendingExpiresAt` with no skew allowance. Below 300 seconds expires, above 600 returns closed `unavailable`, equality refuses, and post-payload expiry preserves the slot for recovery only. |
| 15 null checkpoint expiry cannot be misused; allocator expiry composes with recovery | **YES.** Only a claimant checkpoint may use `null`, and only after durable payload send. After allocator expiry the browser's hub-pending and staging state drive signed status recovery. |
| 16 no retry, rebind, crash, replay, rotation, erasure, or expiry duplicates an effect | **YES.** Exact retransmission is the only idempotence. Every hub retry returns byte-identical responses, the wallet slot is a tagged union, and recovery cannot install without the retained pending slot. |
| 17 hub locks, capacity, migration, terminals, grammar, limiter, retention, and staging total | **YES with two observations.** All three lock orders, both capacity bounds, the terminal classes, the closed response set, the limiter, and the retention rule are stated. See N-3 and N-5. |
| 18 credential/v1 bytes and valid production behavior stay frozen under the new ceiling | **YES.** The ceiling binds only the authenticated chat WebSocket, whose largest declared field decodes to 262,144 octets at `cbcl-chat-session-ws.lfe:1727`. The cbcl-pairing relay keeps its 70,000-octet ceiling, and version-1 labels, origins, and identifiers stay disjoint. |
| 19 did-crdt absent from every credential/v2 edge; the sole HTTPS exception is status recovery | **YES.** CON-986 forbids every rendezvous edge, TEST-1159 permits only the CON-989 route, and the did-crdt candidate diff adds one specification file and no code. |
| 20 every current clause traceable and strict-lint clean without trajectory authority | **YES with one observation.** All four parents and every extracted section return zero errors and zero warnings. Every self-anchor resolves, and every wikilink target exists. See N-7. |

## Repository and code-claim assessment

Every testable code claim except the failure-mode section resolved exactly at
its stated baseline.

**Selfsame version sites.** All eighteen witness rows in
[[SPEC-008-production-pairing-claimant#REQ-1005]] resolve at `0220cec2` with the
stated class. The additional comparison, ingest-substitution, and untyped
ingress rows also resolve. The recorded duplication is real at
`crates/selfsame-app-identity/src/lib.rs:109` and
`crates/selfsame-app-identity/src/profile.rs:60`.

**cbcl-bus consumer sites.** Every line
[[SPEC-008-production-pairing-claimant#NFR-928]] names resolves with the stated
consumer. The reviewer checked every named line individually at `ce894a7f`.

**The named repairs match real defects.** `cbcl-chat-successor.lfe:109` wraps a
call in `catch`, and `:134` both catches and opens a second transaction through
`device-grant/2`. `cbcl-chat-rooms.lfe:66` catches every other error in one
wildcard. NFR-928 forbids exactly those edges.

**The Mnesia terminal claim holds.** OTP 28.5 `mnesia_tm.erl:926` returns
`{aborted, nomore}` when `try_again/1` reports `no`. Line `:921` restarts only
at `tidstore` level 1, and `:923` returns the raw reason at a deeper level.
Line `:929` always retries under `infinity`, so the default unary call never
reaches the terminal. Line `:879` returns `{atomic, Value}` for a nested commit,
so nesting has no runtime classifier and needs static proof.

**The Cowboy bound holds for fragments.** `cowboy_websocket.erl:494` guards
`byte_size(Payload) + byte_size(SoFar) > MaxFrameSize` before it appends to the
fragment buffer. The guard matches both `nofin` and `fin` fragments.

**The WebFinger owners match.** `cbcl-chat-webfinger-store.lfe` exports
`provision/3`, `withdraw/2`, `reinstate/2`, and `lookup/1`. The current
`tombstone/2` becomes `tombstone/3`, which is a stated change and not a
misdescription.

**The upstream bounds exist.** cbcl-pairing `src/mailbox.rs:15` sets 69,632,
`src/cbcl_protocol.rs:32` sets 2,048, and both relay binaries set 70,000.
`src/profile.rs:334` bounds the credential at 62,000.

**The failure-mode section does not resolve.** See
[[spec-008-0.5.5-claude-adversarial-review-2026-08-24#N-1 Two named failure modes contradict the pinned baseline and the parent's own changelog]].

## Consent assessment

Two decisions gate every identity effect. The exact-pair decision precedes the
socket, and the two-stage ceremony decision precedes derivation and delivery.

Rejection at the pair prompt creates no policy entry, socket, wallet scope, key,
alias, DID, grant, or publication. The durable row waits for the CPace-bound
profile digest.

Preliminary approval authorizes one zeroizing derivation, a pure DID and
fingerprint, and one disclosure the screen states. Final approval authorizes the
declared construction and nothing earlier.

Every displayed value comes from the authenticated authority input. No peer
string, DOM value, caller boolean, or verifier return reaches either screen.

## Wire and bounds assessment

Eleven object kinds map to integers 0 through 10. Only the offer, payload, and
receipt use the large arm.

Every declared maximum recomputes exactly. Control bodies stay within 2,048
octets and large bodies within 62,000. The complete objects stay within 69,632
and 70,000.

The final hub command sums to 1,470,680 encoded argument octets and leaves
29,320 for syntax under a 1,500,000-octet frame. Cowboy refuses a larger
complete or fragmented message with close code 1009.

## Cryptography assessment

The public context contains thirteen enumerated members. The transcript, extract,
seven expands, both Finished values, the nonce rule, and the AAD map are exact.

Both parents use the same extract argument order. Every label is disjoint from
version 1, whose pinned labels read `pairing-v1`.

The checkpoint wrapping key states explicit length prefixes, an explicit salt,
and an explicit output length. Every signature names its exact input, signer,
and key.

## Recovery and atomicity assessment

The claimant checkpoint precedes the first identity effect. The browser writes
one inactive staging record before hub finalization.

The hub commits one final transaction or none. The browser activates only after
verifying the immutable status, and the wallet installs only after verifying the
same bytes.

Signed status recovery reproduces the ordinary receipt transition without
rebuilding a grant, repeating a signature, republishing a DID, or resending a
payload.

## Persistence assessment

The wallet slot is one tagged pending-or-installed union. The hub holds one
pending record per ceremony and one immutable status per finalized account.

Every crash boundary recovers exactly one of absent, the complete pending
record, or the complete finalized state.

Capacity is bounded at 69,632 octets per pending record and 65,536 records in
total. See [[spec-008-0.5.5-claude-adversarial-review-2026-08-24#N-5 One capacity counter mixes transient and permanent records]].

## Rendezvous assessment

No credential/v2 object reaches a Selfsame or did-crdt rendezvous slot. The only
non-relay route is the signed post-window status recovery to the authenticated
application origin.

did-crdt SPEC-037 0.1.2 records the deprecation and burns its former
identifiers. The candidate diff adds one specification file and changes no code.

## Compatibility assessment

Credential/v1 keeps its carrier, schedule, display, wire bytes, and decisions.
The version-1 recogniser cannot read a version-2 carrier.

The new WebSocket ceiling binds only the authenticated chat socket. Its largest
declared field decodes to 262,144 octets, well below the ceiling.

The standing registry and selection control stay compiled and unchanged, with
one corrected implementation comment.

## Authority assessment

Each parent states its own current law and names the same coordinated set. All
five documents record the same generation family, version, and session.

Every version pointer between parents matches the reviewed object. No duplicate
artefact identifier exists, and no burned identifier is reused.

The trajectory pointer in the provenance section is stale. See
[[spec-008-0.5.5-claude-adversarial-review-2026-08-24#N-2 The generation provenance names the wrong correction input]].

## Stopping-rule assessment

The reviewer stopped after the finding rate reached one blocking defect and six
observations across a complete pass.

The pass covered four parents, one deprecated disposition, five pinned
baselines, one Erlang runtime source, and one Cowboy release. It recomputed
every declared maximum by hand rather than by trusting the text.

Further search does not change the verdict, because one MEDIUM finding already
blocks PASS.

## Surfaces found sound

- The two-stage consent boundary and its instrumented prohibition list.
- The seven-step CON-220 substitute and its explicit CPace digest binding.
- The complete credential/v2 key schedule and its version-1 disjointness.
- All eleven logical bodies, their predecessors, and their recomputed maxima.
- The Cowboy frame ceiling, including its cumulative fragment guard.
- The Mnesia lane model, its level-1 requirement, and its static nesting proof.
- The signed final status, its recovery token, and its commitment binding.
- The exact-pair policy contract and its honest operatorship residual.
- Strict controlled-language lint across all four parents and every section.

## Residual assumptions

- The application profile does not prove relay operatorship, and exact-pair
  consent contains that residual without solving it.
- Explicit service data erasure of an accepted status leaves the wallet
  fail-closed with no capability and the hub with an orphaned account.
- The browser's comparison assertion stays a browser-side claim. The wallet's
  own final screen and its local DID remain the wallet's protection.
- The library checkpoint key is described in prose rather than a grammar block.
  A checkpoint never crosses to a peer, so only self-consistency is required.
- The credential/v2 profile fetch is stated as live at every attempt. That rule
  is stricter than the cached-profile permission in
  [[SPEC-004-application-scoped-identity#CON-220]].
- Two depth tests stay open with named owners, covering a real relay ceremony
  and two independently operated relays.

## What this verdict authorizes and does not authorize

This verdict authorizes nothing. It records one blocking correction and six
non-blocking observations for the next coordinated reissue.

It does not authorize the Elephant plan, a test-first plan, implementation,
production allocation, release, or deployment.

It closes no GATE-04 box and changes no production gate. It changes no
requirement, contract, test, or bound in any parent.

## Collected corrections and owners

| Finding | Severity | Owner | Correction |
|---|---|---|---|
| N-1 | MEDIUM | selfsame | Restate FM-3 and FM-4 against `0220cec2`, or label both historical as FM-1 and FM-2 already are. |
| N-2 | LOW | selfsame | Point the provenance section at the 0.5.4 report and describe this revision's own corrections. |
| N-3 | LOW | cbcl-bus | State how a hub distinguishes terminal absence from an unheld ceremony once the optional tombstone lapses. |
| N-4 | LOW | selfsame | Record once that the account scope also travels in signed final status and installed state. |
| N-5 | LOW | cbcl-bus | Record that finalized statuses permanently consume allocation budget, or separate the two counters. |
| N-6 | LOW | cbcl-bus | Refuse a carrier whose remaining lifetime cannot support a whole offer window. |
| N-7 | LOW | selfsame | Give CON-988 and CON-989 the `Implements:` and `Verified by:` lines their neighbours carry. |
