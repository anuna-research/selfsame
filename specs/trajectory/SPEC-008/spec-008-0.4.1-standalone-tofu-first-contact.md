---
id: SPEC-008-amendment-0.4.1
title: SPEC-008 0.4.1-draft — Standalone TOFU first-contact pairing over a signed Selfsame offer
status: draft
version: 0.4.1-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-fresh-cross-model-review
amends: "[[SPEC-008-production-pairing-claimant]] (0.2.2); [[SPEC-004-application-scoped-identity#CON-214]]; [[SPEC-004-application-scoped-identity#CON-219]]; [[SPEC-007-cbcl-pairing-cutover#CON-801]]; cbcl-pairing SPEC-001"
supersedes: "[[spec-008-0.4.0-standalone-tofu-first-contact]]"
review-inputs: "[[req-909-adversarial-review-2026-08-22]]; [[req-909-adversarial-review-2026-08-22#Review of SPEC-008 0.4.0 draft (standalone TOFU first contact)]]"
last-updated: 2026-08-22
---

# SPEC-008 0.4.1-draft — Standalone TOFU first-contact pairing over a signed Selfsame offer

> **Design target, not implementation authority.** The owner selected standalone
> first-contact pairing and person-owned relay TOFU on 2026-08-22. This document
> replaces the rejected 0.4.0 design. No source change is authorised until a
> fresh-context, different-model Tier-1 adversarial review returns PASS over this
> document and the cited source baselines. A PASS authorises test-first planning;
> it is not production approval.

## Orientation

**Outcome.** A signed-in browser at chat.anuna.io allocates one cbcl-pairing
invitation. A Selfsame wallet that has never enrolled with chat authenticates the
application live, asks the person whether that exact application may use that exact
relay, runs CPace, authenticates the application's signed account/device offer, and
shows exact grant consent. Only after that consent may it derive or publish an
application-scoped identity. It returns the resulting grant through cbcl-pairing.
The browser accepts and durably installs that grant before either side reports the
link complete.

This is one protocol ceremony. It does **not** run the separate SPEC-004
rendezvous enrolment first. It nevertheless reuses the security object that made
that enrolment sound: the closed CON-219 offer and its backend-signed CON-214
evidence. cbcl-pairing replaces the rendezvous and envelope transport; it does not
replace application authentication, account/device binding, grant issuance, grant
acceptance, or person consent.

**Three decisions, not one.** The wallet distinguishes:

1. **relay admission** — may authenticated application A use relay R? Accepting
   first use writes only the person-owned `(applicationId, relayOrigin)` policy;
2. **grant consent** — may A's authenticated account issue the displayed
   permissions to the displayed application device? This is the hard stop before
   identity effects;
3. **account comparison** — when CON-221 requires it, does the issuer fingerprint
   on both screens match? Only its final approval releases the grant.

No decision substitutes for either later decision.

```text
 signed-in browser / allocator                         Selfsame / claimant
 ─────────────────────────────                         ───────────────────
 allocate v2 invitation
   profile = credential/v2
   application-context = canonical applicationId ───▶ CON-220 live profile fetch
   relay-origin ─────────────────────────────────────▶ exact profile relay match
                                                       policy[(app, relay)]?
                                                       new: accept or reject
                                                       accepted: only then open WSS

 CPace + Finished  ◀════════ blind relay ═══════════▶ CPace + Finished

 authenticated hub constructs signed CON-219 offer
 intent(offer, pairing binding) ─────────────────────▶ CON-214/219 authorisation
                                                       authenticated exact display
                                                       person grant-consent decision
                                                       ─────── hard stop ───────
                            after consent only: derive, issue, publish
 preparation(issuer comparison) ◀─────────────────────
 show independent fingerprint                         show local fingerprint
                                                       person final comparison
 final-approve + grant bundle ◀────────────────────────
 CON-204 provision + CON-206/207 acceptance
 atomic connection install + signed hub grant
 receipt(bundle digest) ─────────────────────────────▶ report linked
```

## Verified baseline and newly exposed defects

The design was traced against these exact revisions before authoring:

| Repository | Revision | Relevant observed behaviour |
|---|---|---|
| `selfsame` | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | `cbcl_pairing_start` requires a held record; `assemble_claimant` derives the hierarchy before the socket; `SelfsameEndpoint` forwards `DisplayIntent` and cross-checks application claims only at `DeliverGrant` |
| `cbcl-pairing` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | invitation `application` is the profile constant; allocator authors both intent claims; `apply_intent` displays them; grammar permits only allocator intent/payload and claimant decision |
| `cbcl-bus` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | production invite loads `local-pairing-fixture.json`; the fixture supplies recipient, bundle, profile, account, and device key before a claimant exists; `delivered` submits that fixture key |
| `did-crdt` | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | retained as the state resolver; it is not the pairing transport in this design |

The five 0.4.0 review findings remain mandatory. The trace also exposed four
implementation premises that 0.4.0 did not examine:

- **F-F — pre-staged recipient/grant.** `AllocatorRelaySession::new` takes a
  complete `CredentialTransfer` and checks it before allocation. The deployed
  browser obtains this from a deterministic fixture. A first-contact allocator
  cannot know a new wallet-derived recipient or hold a wallet-signed grant.
- **F-G — wrong payload direction.** cbcl-pairing v1 fixes allocator → claimant
  payload flow. SPEC-004 enrolment requires the wallet claimant to issue a grant
  to the application allocator after consent.
- **F-H — no application acceptance at the production affordance.** The current
  invite terminal reports `delivered` and submits fixture material. The existing
  enrolment helper recognises and stores a bundle but does not expose a complete
  browser-side CON-206 acceptance facade. Delivery is not acceptance.
- **F-I — unauthenticated signing oracle.** `/selfsame/enrolment/sign` signs a
  caller-supplied statement; its own source labels this prototype posture. CORS
  limits drive-by browsers but does not authenticate a server-side caller or bind
  the signed statement to the active chat account.

This amendment treats F-F through F-I as blocking, not as later polish.

## Disposition of the 0.4.0 review

| Finding | 0.4.1 disposition |
|---|---|
| **F-A — relay-scoped trust** | [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-914]] and [[spec-008-0.4.1-standalone-tofu-first-contact#CON-908]] key policy by exact `(applicationId, relayOrigin)`. A new application on a familiar relay prompts. |
| **F-B — untrusted display** | [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-915]] and [[spec-008-0.4.1-standalone-tofu-first-contact#CON-909]] make a claimant-supplied authenticated intent verifier a pre-display gate. The peer's `applicationId`, origin, account label, device, permission, or authority summary is never rendered as authenticated merely because it was carried on the wire. |
| **F-C — pre-consent derivation** | [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-916]] splits read-only preflight/intent recognition from post-consent issuance. `assemble_claimant` is not reused. The existing `prepare_issuance` boundary is called only after the grant-consent event. |
| **F-D — no fresh scope** | [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-917]] keeps CON-211 ownership where SPEC-004 puts it: the authenticated application account creates and durably reuses the 32-octet scope. The hub inserts it into a signed CON-219 offer; the wallet neither guesses nor mints it. |
| **F-E — no operatorship binding** | [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-920]] accepts the narrower security property explicitly. Version 1 does not claim relay operatorship. Per-application consent mitigates relay reuse; `operatorId` is application-asserted metadata and is never displayed as independently verified. |

## Requirements

### REQ-914 — Live application authentication and pair-scoped relay TOFU

Before opening a socket to the invitation relay, the claimant SHALL:

1. recognise a v2 invitation under [[spec-008-0.4.1-standalone-tofu-first-contact#CON-907]];
2. parse its credential-profile application context with the sole SPEC-004
   `ApplicationId::parse` recogniser;
3. fetch the profile live by dereferencing that exact identifier under
   [[SPEC-004-application-scoped-identity#CON-220]] with no redirect or stale
   first-contact cache;
4. require exactly one recognised `cbclPairingRelays` descriptor whose
   `relayOrigin` equals the invitation origin byte-for-byte; and
5. admit only an existing exact `(applicationId, relayOrigin)` policy entry or
   an explicit person acceptance of that exact new pair.

An application new to the wallet SHALL prompt even when its relay is already
accepted for another application. A new relay SHALL prompt even when the
application is known. Removing the relay from the live profile SHALL refuse even
when a local policy entry remains. Reject, fetch failure, malformed context,
zero/multiple descriptor match, or policy-write failure SHALL open no relay socket.
The compiled conformance-digest allowlist and held pairing-trust record SHALL NOT
participate in this decision.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-907]]
[[spec-008-0.4.1-standalone-tofu-first-contact#CON-908]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-927]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-928]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-929]]

### REQ-915 — Authenticated exact intent before display or consent

After CPace and both Finished values, the allocator SHALL send one encrypted,
pairing-bound CON-219 offer. Before emitting a display effect, the claimant SHALL
apply the complete [[SPEC-004-application-scoped-identity#CON-214]] and
[[SPEC-004-application-scoped-identity#CON-219]] authorisation predicate through
[[spec-008-0.4.1-standalone-tofu-first-contact#CON-909]]. It SHALL require exact
equality among:

- invitation application context, live profile `applicationId`, signed offer
  `applicationId`, and the authenticated display value;
- invitation relay origin, live profile relay descriptor, signed provider hint,
  and the pairing-session binding;
- signed active-application-account label and the label displayed as the account;
- offered device DID/JWK and key digest;
- signed requested permission set and the displayed permission set; and
- offer/request/ceremony identifiers, time window, profile digest, descriptor
  digest, and intent/session binding.

The display SHALL source application ID and origin from the live authenticated
profile, and account/device/permission facts only from the fully authorised signed
offer. The profile SHALL provide the fixed action and authority summary. No
peer-authored presentation string is eligible for an authenticated label. Any
mismatch terminates with zero `DisplayIntent` effects and zero consent affordances.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-909]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-930]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-931]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-932]]

### REQ-916 — Grant consent precedes every wallet identity effect

The following claimant work SHALL be read-only before the person accepts the exact
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-915]] intent: invitation
recognition, live profile fetch, relay-policy lookup or explicit relay-policy
acceptance, socket/CPace/Finished processing, signed-offer authorisation, replay
lookup, and display construction.

Only after the local grant-consent event MAY the wallet consume the request ID,
unlock custody, call `hierarchy::derive`, call `issuer::create`, sign a grant,
publish DID state, prepare a CON-221 issuer comparison, or write issuance state.
The application account scope already exists in the signed offer; the wallet SHALL
NOT mint or persist a substitute. Alias provisioning occurs on the application side
only after grant consent and receipt of the wallet's preparation or grant.

Declining relay admission writes no policy. Accepting relay admission may write
exactly the policy entry the person accepted, even if they later decline the grant;
that independent policy decision is not an identity effect. Declining the exact
grant intent SHALL leave hierarchy, DID, grant, alias, issuance, and request-consumed
state byte-for-byte unchanged. This requirement preserves
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] and replaces the false ordering claim in
0.4.0.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-910]]
[[spec-008-0.4.1-standalone-tofu-first-contact#CON-911]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-933]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-934]]

### REQ-917 — The authenticated application account owns the stable scope and offer

The chat hub SHALL create one CSPRNG 32-octet account scope for each authenticated
`(applicationId, chat account)` pair, commit it atomically before first use, and
reuse it on retries and later pairings as [[SPEC-004-application-scoped-identity#CON-211]]
requires. It SHALL construct and sign the complete CON-219 offer from authenticated
server-side ceremony state reached through the existing signed chat session. It
SHALL NOT sign arbitrary caller-supplied CON-214 statement octets.

The signed offer extension SHALL include a bounded application-account display
value obtained from the verified chat sender, and a pairing-session binding over
the exact v2 invitation public context plus intent nonce. The display value is for
exact human intent; the random account scope remains the sole derivation input and
SHALL NOT appear in a consent label, URL, relay metadata, log, or policy entry.

The unauthenticated `/selfsame/enrolment/sign` oracle SHALL NOT be used by this
path. A request from an unauthenticated chat connection, for another handle, with
a replayed ceremony/request ID, or with a device key different from the current
offer request SHALL produce no signature and no ceremony state.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-911]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-935]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-936]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-937]]

### REQ-918 — Credential-profile v2 carries preparation and grant claimant-to-allocator

The production Selfsame credential profile SHALL use the versioned session grammar
in [[spec-008-0.4.1-standalone-tofu-first-contact#CON-910]]. The allocator sends the
signed offer intent. After grant consent, the claimant may send one preparation
bound to its digest. After the required CON-221 comparison, the claimant sends the
final decision and, only on approval, one grant bundle. The allocator sends a
terminal receipt only after application acceptance.

Version 1's allocator-authored claimant claim, allocator-to-claimant credential
payload, and pre-staged `CredentialTransfer` SHALL NOT be used on the production
path. There is no version negotiation, downgrade, or fallback: a v1 invitation at
the production surface receives the existing unsupported/refused result.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-907]]
[[spec-008-0.4.1-standalone-tofu-first-contact#CON-910]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-938]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-939]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-940]]

### REQ-919 — Application acceptance, durable installation, and end-to-end receipt

On grant receipt, the browser SHALL, in order:

1. recognise the closed CON-219 bundle and require its request and ceremony IDs
   to equal its own offer;
2. provision the recomputed reciprocal account binding under CON-204;
3. resolve or consume sufficiently fresh issuer state;
4. invoke the complete CON-206 predicate with the live profile, authenticated
   active chat account context, exact offered device key, exact permission set,
   current time, revocation evidence, and a fresh CON-207 proof made with the
   retained device private key;
5. atomically persist `{device private seed, accepted grant octets, issuer
   closure/reference, applicationId, account scope reference, ceremony ID}`;
6. submit the existing signed `pairaccountgrant` for the **actual offered device
   key**, never fixture material; and
7. emit the v2 receipt bound to the accepted bundle digest.

Any failure through step 6 emits a refusal, no success receipt, and no partially
installed connection. The wallet SHALL report `linked` only after it recognises the
matching receipt. Relay delivery, payload decryption, bundle recognition, a local
write, or the hub grant alone is not end-to-end success.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-912]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-941]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-942]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-943]]

### REQ-920 — Relay authority is not inferred from the application profile

Version 1 SHALL make no claim that the application operates, owns, endorses the
security practices of, or can revoke an operator merely because its live profile
lists a relay. The authenticated fact is only: application A declared relay R for
this protocol. The person-owned fact is only: the person accepted `(A, R)`.

`operatorId` MAY be retained as an application-asserted descriptor key, but SHALL
NOT be rendered with authenticated-operator provenance or used as a global trust
key. A malicious application may name a popular third-party relay; it still gets a
new application-scoped prompt and must later produce its own valid signed offer.
Global operator revocation and proof of operatorship remain outside this version.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-908]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-944]]

### NFR-903 — Bounded, opaque credential carriage

The v2 credential carrier SHALL admit every valid CON-219 offer and bundle up to
its normative 69,607-octet payload bound. It SHALL use one fixed-size,
strictly-recognised credential envelope inside pairing AEAD so the blind relay does
not learn the logical offer or grant length. All enclosing frame, relay, CBCL body,
and WebSocket bounds SHALL be raised together from one named constant sufficient
for the fixed envelope, CBCL control, canonical CBOR overhead, and AEAD tag.

Trace: [[spec-008-0.4.1-standalone-tofu-first-contact#CON-910]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-945]]

## Contracts

### CON-907 — Version-2 invitation and transcript context

cbcl-pairing SHALL add a separate v2 carrier grammar; it SHALL NOT reinterpret a
v1 map:

```cddl
pairing-invitation-v2 = {
  "version": 2,
  "suite": "CPACE25519-SHA512-D21",
  "profile": "anuna.io/credential/v2",
  "application-context": bstr .size (1..2048),
  "relay-origin": tstr .size (1..255),
  "locator": locator,
  "secret": bstr .size 16,
  ? "expected-allocator-key": bstr .size 32,
  ? "expected-claimant-key": bstr .size 32
}
```

For `anuna.io/credential/v2`, `application-context` is exact UTF-8 ASCII accepted
by `ApplicationId::parse`; the 2,048-octet carrier bound is additional resource
policy, not a second URI grammar. The terms `profile` and `application-context`
are deliberately distinct: the first selects cbcl-pairing application semantics;
the second selects the live Selfsame application profile.

The exact context bytes SHALL enter the v2 CPace channel identifier, secure-channel
public context, invitation digest, attempt binding, and Finished/session transcript.
They are carried only out of band and as cryptographic input; the relay receives no
cleartext application context. Any mutation produces a different CPace/session
transcript and no accepted application message.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-914]]
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-918]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-927]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-939]]

### CON-908 — One atomic person-owned application/relay policy

The secure wallet store SHALL contain one closed, versioned document under one
entry, not an index plus independently writable records:

```json
{"entries":[{"acceptedAt":1787356800,"applicationId":"https://chat.anuna.io/selfsame/application","relayOrigin":"https://chat.anuna.io:9443"}],"version":1}
```

The exact top-level and entry member sets above apply. Entries are sorted by
`(applicationId, relayOrigin)`, duplicates are rejected, origins pass the existing
`UriPolicy::ORIGIN` recogniser, identifiers pass `ApplicationId::parse`, and the
document is bounded to 64 entries and 32,768 octets. `contains`, `accept`, `list`,
and `remove` recognise the complete document before acting. Accept and remove each
replace the one secure-store value atomically; an unavailable/corrupt/write-failing
store refuses admission. The initial state is the recognised empty document.

The policy UI shows application ID, exact A-label origin, relay origin, acceptance
time, and remove. It has no free-text add or edit. Relay admission shows the exact
live-authenticated application ID/origin and exact new relay, with Accept and Reject
only. Removing an entry affects the next ceremony and does not cancel unrelated
live sessions.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-914]]
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-920]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-928]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-929]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-944]]

### CON-909 — Claimant-authenticated intent verifier and display provenance

cbcl-pairing's application-profile boundary SHALL gain an injected, endpoint-local
intent verifier analogous to `GrantVerifier`. For credential/v2 claimant use, the
Selfsame adapter supplies a verifier holding only the v2 invitation context, exact
live `FetchedProfile` octets, selected relay descriptor, local time/clock policy,
and platform/manual observation. It consumes the complete decrypted intent and
returns either:

- a `VerifiedProfileIntent` containing an opaque binding plus display fields with
  closed provenance (`AuthenticatedApplication`, `AuthenticatedAccount`,
  `AuthenticatedOffer`, or `LocalProtocol`); or
- refusal, which causes terminal profile failure before `DisplayIntent`.

The credential/v2 verifier SHALL call the existing `ceremony::recognise_offer` and
`authorise::authorise` predicates. It SHALL NOT duplicate their JSON, URI, JWS, or
offer grammars. `CredentialProfile::recognise_intent` SHALL display only the
returned verified fields. The existing `claimed_by_secret_holder: bool` is
insufficient and SHALL be replaced by the closed provenance type. The action and
authority summary are fixed profile constants and removed from peer control in v2.

The allocator uses a local v2 verifier over the hub-returned offer before sending
it; this catches local construction drift but does not substitute for claimant
verification.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-915]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-930]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-931]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-932]]

### CON-910 — Credential-profile v2 projected session

The v2 CBCL session dialect and endpoint reducer SHALL permit exactly:

```text
begin -> intent
intent -> decline -> terminal
intent -> preparation
preparation -> final-approve -> payload -> receipt -> terminal
preparation -> final-decline -> terminal
payload -> refusal -> terminal
```

Roles and meanings are fixed:

| Performative | Sender | Adjacent body |
|---|---|---|
| `intent` | allocator | credential/v2 fixed envelope containing one CON-219 offer and fresh intent nonce |
| `decline` | claimant | exact intent digest; zero profile body |
| `preparation` | claimant | exact intent digest; fixed-size profile body containing CON-221 applicability and, when required, issuer DID |
| `final-approve` / `final-decline` | claimant | exact intent and preparation digests |
| `payload` | claimant | exact intent/final-decision digests; credential/v2 fixed envelope containing one CON-219 bundle |
| `receipt` / `refusal` | allocator | exact payload digest and closed outcome; no grant or identity plaintext |

`preparation` is emitted only after the shell reports the local grant-consent
event and completes the permitted post-consent preparation. When CON-221 is
required, `final-approve` requires a second explicit person action. When it is not
required, the same grant-consent action may causally release preparation and final
approval. No payload follows decline or final-decline. Receipt follows only
application acceptance; refusal is terminal and cannot be converted to success.

The credential envelope is exactly four-byte big-endian logical length in
`1..69,607`, that many logical payload octets, and all-zero padding to one fixed
size. Non-zero padding, impossible length, trailing data, wrong payload role, or a
valid CON-219 object in the wrong performative is rejection by one recogniser.

Version 1 remains byte-stable for its existing vectors. Version 2 uses distinct
CDDL, context domains, dialect hash, application profile ID, and vectors. No field
or branch negotiates between them.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-916]]
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-918]]
[[spec-008-0.4.1-standalone-tofu-first-contact#NFR-903]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-933]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-938]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-940]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-945]]

### CON-911 — CON-219 over cbcl-pairing and authenticated chat offer allocation

SPEC-004 SHALL admit cbcl-pairing credential/v2 as a transport for the same logical
CON-219 offer and bundle. Those values are carried directly inside pairing's
transcript-bound AEAD and fixed envelope; they SHALL NOT be nested in the retired
PROTO-004 envelope on this path. Their closed JSON grammars, offer digest,
enrollment signature, request/ceremony equality, and grant acceptance rules remain
authoritative.

The CON-209/214 provider binding SHALL gain one explicit cbcl-pairing branch. Its
`providerId` and `descriptorDigest` name the exact recognised
`cbclPairingRelays` descriptor; `pairingSessionBinding` commits to the v2
invitation public context and intent nonce. The v2 offer/evidence closed grammar
also carries `applicationAccountDisplay`, a bounded application-authenticated
label that is signed and displayed but never used for hierarchy derivation. This
is a versioned grammar amendment; v1 evidence is not accepted as v2.

For chat, the offer allocator is an authenticated signed-frame hub operation. The
hub obtains the active handle from its verified session, gets-or-creates the stable
scope in its durable store, constructs the offer core from its ratified profile and
the browser's offered device public key, allocates fresh one-use IDs and expiry,
binds the exact relay/session, signs with the currently declared enrollment key,
and returns the complete offer. Caller-supplied application ID, account label,
scope, permission set, profile digest, provider digest, timestamps, or evidence
JWS are not accepted as authority.

The wallet reuses `app_grant`'s pure review and post-consent issuance functions.
It adds a pairing-specific pending context keyed by ceremony and intent digest; an
overlapping ceremony cannot replace it. After grant consent it consumes the
request ID, derives/issues, publishes the issuer closure to declared state
resolvers, and produces preparation. After final approval it builds the bundle
and passes it to the claimant endpoint for payload transmission. It does not call
`assemble_claimant` or `SelfsameVerificationContext`, because on this flow the
wallet is the issuer, not the recipient verifier.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-916]]
[[spec-008-0.4.1-standalone-tofu-first-contact#REQ-917]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-934]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-935]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-936]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-937]]

### CON-912 — Browser acceptance and atomic connection installation

The vendored Selfsame WASM SHALL expose one subject-side acceptance object whose
constructor retains the browser-generated device seed, exact hub-built offer,
ratified profile octets, and authenticated active account handle. Its `accept`
method takes the exact bundle, issuer resolution/provisioning evidence, local
time, and CSPRNG verifier challenge; it drives CON-206 and CON-207 and returns an
opaque accepted-connection value. No JSON field peeking or bundle recognition
alone constructs that value.

`cbcl-invite.mjs` SHALL allocate from a real offer session, not
`local-pairing-fixture.json`. `app.js` SHALL persist only the opaque accepted
connection plus its retained private seed in one IndexedDB transaction, then
submit `pairaccountgrant` with the accepted offered device key. The allocator
endpoint receives the application acceptance result before it can construct a
receipt. The fixture remains test-only and is absent from the production
affordance and release assets.

Implements: [[spec-008-0.4.1-standalone-tofu-first-contact#REQ-919]]
Verified by: [[spec-008-0.4.1-standalone-tofu-first-contact#TEST-941]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-942]]
[[spec-008-0.4.1-standalone-tofu-first-contact#TEST-943]]

## Decisions

### ADR-906 — TOFU authorises one application's use of one relay

**Decision.** Replace ADR-902's held-record plus compiled registry with one empty,
person-owned exact-pair policy. A policy entry says only that this person accepted
application A using relay R. Live profile membership remains mandatory on every
attempt, so local consent cannot keep a descriptor alive after the application
removes it.

**Rejected:** relay-only policy. It recreates F-A. **Rejected:** automatic trust
for another application using an accepted relay. It is relay-only policy under a
different spelling. **Rejected:** vendor seed entries or digest floor. The owner
explicitly selected no allowlist.

### ADR-907 — Standalone pairing transports the signed offer; it does not invent a weaker bootstrap

**Decision.** Reuse CON-214/219 application authentication and account/device offer
semantics inside cbcl-pairing. The browser is still the invitation/mailbox
allocator; the wallet is still the grant issuer. Therefore credential/v2 reverses
the grant direction relative to the demo credential/v1 profile and adds the
preparation/final receipt states the real ceremony needs.

**Rejected:** mint scope and derive a wallet verifier context after consent, then
accept a credential the allocator already held. The allocator cannot hold a grant
signed by a first-contact wallet, and `assemble_claimant` models the wrong role.
**Rejected:** keep the deterministic production fixture. It proves one known test
pair, not account linking.

### ADR-908 — F-E is accepted as a bounded residual, not mislabeled as operatorship

**Decision.** This version takes F-E's permitted mitigation: per-application relay
consent plus later signed-offer authentication. It does not introduce a relay
operator PKI. A third-party relay can be unavailable, log allowed routing metadata,
or attempt protocol manipulation; CPace, Finished, transcript binding, AEAD, and
application signatures prevent those behaviours from becoming a different
application or accepted grant.

**Consequence.** There is no vendor-global emergency block for a compromised relay
and no independently verified operator label. The person or application removes
the exact pair. A future operatorship credential is a new Tier-1 amendment, not an
interpretation of `operatorId`.

### ADR-909 — Production uses a non-negotiated credential/v2 cutover

**Decision.** Add v2 alongside byte-stable v1 for vectors, then make the production
Selfsame affordance accept and emit v2 only. Distinct schema, domain strings, profile
ID, and dialect make cross-version confusion a recognition failure. No capability
list or peer-selected version exists.

## Test specification

Core tests are written before implementation. Depth and end-to-end rows may require
the live profile/signing/relay harness but remain release-blocking.

- **TEST-927 (positive + negative, REQ-914/CON-907):** v2 application context
  parses with `ApplicationId::parse`; absent, oversized, invalid UTF-8, non-canonical
  URI, or any v1 reinterpretation refuses before application-origin or relay I/O.
- **TEST-928 (scope invariant, REQ-914/CON-908):** accepting `(A,R)` admits only
  `(A,R)`; `(B,R)` and `(A,S)` each prompt. Remove and live-profile descriptor
  removal each restore refusal.
- **TEST-929 (negative-output, CON-908):** reject, corrupt store, 65th entry,
  duplicate/noncanonical pair, and failed atomic write open zero relay sockets;
  restart preserves exactly the accepted set.
- **TEST-930 (positive, REQ-915/CON-909):** a live profile plus matching signed
  offer emits one display whose app/origin/account/device/permission provenance is
  authenticated and whose action/summary is profile-local.
- **TEST-931 (mutation, REQ-915):** mutate independently invitation app context,
  live profile ID/digest, relay/provider descriptor, account label, scope, device
  DID/JWK/digest, permission, request/ceremony ID, time, or pairing binding; every
  mutant emits zero display and zero consent.
- **TEST-932 (prohibited action, CON-909):** wire `authority-summary`, wire
  application ID, and every `claimed_by_secret_holder` display shortcut are absent
  from credential/v2; mutation restoring any one does not produce an authenticated
  display.
- **TEST-933 (negative-output, REQ-916):** snapshot custody calls, hierarchy calls,
  issuer calls, resolver writes, grant signatures, alias writes, issuance writes,
  and request ledger. Preflight, CPace, malformed intent, and exact-intent decline
  leave every counter/state at zero/unchanged; accepted relay policy is the sole
  separately authorised write.
- **TEST-934 (ordering mutation, REQ-916/CON-911):** moving any custody/derive/
  issue/publish call before the local grant-consent event is killed. After consent,
  publication failure sends neither preparation nor payload.
- **TEST-935 (positive, REQ-917):** two retries for one authenticated chat account
  receive the same durable scope and fresh request/ceremony IDs; a different account
  gets an unrelated scope; the wallet uses the exact offered scope.
- **TEST-936 (negative-input, REQ-917):** unauthenticated, wrong-handle, wrong-key,
  replay, caller-chosen scope/profile/permission/digest/time, and arbitrary raw
  statement requests receive no enrollment signature and write no ceremony.
- **TEST-937 (binding mutation, REQ-917/CON-911):** transplant a valid signed offer
  to another invitation, mailbox, relay, intent nonce, or chat account; claimant
  authorisation refuses before display.
- **TEST-938 (positive + decline, REQ-918/CON-910):** exact v2 order reaches receipt;
  decline before preparation and final-decline after preparation each reach terminal
  with zero payload.
- **TEST-939 (independent vectors, REQ-918/CON-907):** two implementations reproduce
  v2 invitation, context, Finished, every application frame, transcript digest, and
  receipt bytes; every v1 vector remains unchanged.
- **TEST-940 (protocol mutation, REQ-918):** wrong sender, reordered/skipped/
  duplicated performative, digest mismatch, payload after decline, receipt before
  acceptance, and v1/v2 crossover all terminate without grant acceptance.
- **TEST-941 (positive, REQ-919/CON-912):** browser provisions the alias, resolves
  issuer state, proves the offered private key, passes all thirteen CON-206 steps,
  commits one connection, submits the actual key, emits receipt, and wallet reports
  linked.
- **TEST-942 (negative-output, REQ-919):** mutate each CON-206/207 input and force
  alias, resolver, IndexedDB, hub-grant, and receipt failures independently; no case
  reports linked or leaves a partial accepted connection.
- **TEST-943 (prohibited action, CON-912):** production assets and call graph contain
  no `local-pairing-fixture.json`, `CredentialTransfer` pre-stage, fixture recipient,
  fixture grant, or delivered-as-accepted shortcut.
- **TEST-944 (threat, REQ-920):** malicious application B validly declares popular
  relay R already accepted for A. B receives a new `(B,R)` prompt; even after accept,
  only B's own live profile and B-signed exact offer can reach display. UI never marks
  R's operator as independently verified.
- **TEST-945 (boundary + opacity, NFR-903):** logical lengths 1 and 69,607 round-trip;
  0 and 69,608, false length, non-zero padding, frame under/overflow, and nested
  alternate envelopes refuse. Relay observations are identical in size for a small
  and maximum valid logical credential payload.
- **TEST-946 (full external E2E):** a clean wallet and signed-in chat.anuna.io browser
  complete new-relay acceptance, signed offer, authenticated display, approval,
  comparison, wallet issuance/publication, browser CON-206/207 acceptance, durable
  reload, hub `pairaccountgranted` acknowledgement, and wallet receipt through the
  real `:9443` blind relay. A second pairing for the same `(application, relay)` omits
  only the relay prompt. This row requires explicit owner approval before any Fly
  deployment or production run.

## Cross-repository implementation boundary

No implementation begins until the mandated review passes. After PASS, the
executable theory decomposes at least these independently evidenced units:

| Repository | Required slice |
|---|---|
| `cbcl-pairing` | v2 CDDL/context/dialect/vectors; intent-verifier display boundary; preparation/final decision/reverse payload/receipt reducer; unified frame bound |
| `selfsame` core/adapter | v2 invitation; credential offer authoriser; post-consent issuer path; pairing pending-state binding; resolver publication; browser subject-side CON-206/207 acceptance WASM |
| `selfsame` wallet UI | pair-scoped policy prompt/manage/remove; authenticated exact intent; CON-221 comparison; truthful receipt terminal |
| `cbcl-bus` hub | authenticated offer-allocation command; durable account scope and one-use ceremony state; signed v2 CON-214 evidence; real device-key account grant |
| `cbcl_chat` browser | non-fixture allocator; retained device seed; offer/session binding; fingerprint display; alias provisioning; acceptance + atomic install; receipt |
| `cbcl-bus` relay | v2 frame bound and opacity/boundary tests; no application parsing |

`did-crdt` remains the declared state resolver. No new pairing route or mailbox is
added there.

## Gate and sequencing

1. Fresh-context cross-model adversarial review of this exact revision and the
   four cited code baselines, with a defect-finding mandate. Owner approval alone
   is insufficient.
2. If and only if PASS: author and validate the executable SPEC-008 Elephant
   theory, including every TEST-927–TEST-946 acceptance row and the upstream spec
   amendment tasks.
3. Commit failing core tests and independent vectors before production code.
4. Implement cbcl-pairing and Selfsame adapters before wiring the wallet/browser
   shells; implement the authenticated hub offer allocator before removing the
   fixture.
5. Run core, depth, red, prohibited-action, mutation, WASM reproducibility,
   Android, browser, and local real-relay E2E gates.
6. Ask the owner separately before deploying cbcl-bus. Fly's single-machine
   `immediate` strategy has no canary; the prior release is the rollback target.

## Open items retained as explicit release gates

- Human cryptography review of the v2 CPace context inputs, context-domain
  separation, session dialect, AEAD counters, and independent vectors remains
  required by [[SPEC-007-cbcl-pairing-cutover#Production gate]].
- The SPEC-004 owner must approve the versioned CON-209/214/219 transport and
  authenticated account-display amendments. The cbcl-pairing owner must approve
  credential/v2 and the raised bound. These are approvals, not implementation
  assumptions.
- Global relay-operator revocation and operatorship proof remain out of scope by
  [[spec-008-0.4.1-standalone-tofu-first-contact#ADR-908]].

## Changelog

- **0.4.1-draft — 2026-08-22 — review candidate, not approved.** Replaces
  relay-scoped TOFU with exact application/relay policy; replaces wire-claimed
  display with a claimant-authenticated intent verifier; replaces pre-socket
  `assemble_claimant` with post-consent grant issuance; sources the stable scope
  and signed offer from the authenticated application account; explicitly accepts
  the non-operatorship residual; and adds credential/v2 preparation, reverse grant,
  application acceptance, receipt, frame bounds, signing-oracle closure, and
  fixture removal. No implementation exists under this amendment.
