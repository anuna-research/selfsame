---
id: spec-008-0.5.17-authority-singularity-reviewer-prompt-2026-08-25
title: SPEC-008 0.5.17 — authority singularity Tier-1 review prompt
status: active
tier: 1
mode: descriptive
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
date: 2026-08-25
---

# SPEC-008 0.5.17 — authority singularity Tier-1 review prompt

You are the fresh-context, cross-model Tier-1 reviewer. Find defects in the
two-line authority-singularity correction.

Do not repair code or specifications. Write only the review report, commit it
in the Circus worktree, and complete through Circus's appended instruction.

## Authentication

Use the interactive Claude Max subscription, never the Anthropic API. Both
`ANTHROPIC_API_KEY` and `ANTHROPIC_AUTH_TOKEN` SHALL be absent.

Check absence without printing either value. Run
`env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status`.

Record only `authMethod`, `apiProvider`, and `subscriptionType`. Require
`claude.ai`, `firstParty`, and `max`.

## Exact correction

- Corrected Selfsame authority:
  `7878146ab3c1a12c8b8e88a9c2b0bfef2b313776`
- Rejected Selfsame authority:
  `9337fa9660ee47c01182dafc5f17b0ade9d1c963`
- Preserved rejected report:
  `a7684f6`
- Selfsame executable candidate:
  `60abb8b25e858be8005151e47ea2a8044d130e80`
- cbcl-bus executable candidate:
  `14f26065cb147633416def15d0a7d8603bce2e09`
- cbcl-bus authority:
  `d35cdacfcd379d54771c655aea87f6f42a1db596`
- cbcl-pairing executable candidate:
  `62ef4a968b46b4836374fcee1d78c410f730a7a7`
- cbcl-pairing authority:
  `6f841595d0127279d0d6c1cb850c68900d379ade`

Use committed objects. Do not use dirty working files as evidence. Do not
modify or delete any user file or review scratch.

## Independent correction checks

Before reading the rejected report, verify these claims:

1. `9337fa9..7878146` changes exactly two lines in one specification file.
2. Both changes replace hub 0.17.9 with hub 0.17.13.
3. SPEC-008 current law ends immediately before `## Changelog`.
4. Current law contains only `SPEC-053 0.17.13-draft` hub-parent citations.
5. The current-law count is six, with zero 0.17.9 and zero 0.17.12 citations.
6. TEST-1157 names hub 0.17.13 as the coordinated review-set parent.
7. Hub SPEC-053 0.17.13 names SPEC-008 0.5.17, SPEC-007 0.3.8, and SPEC-001 0.5.8.
8. CON-987 resolves `migrationConfirmationDigest` through hub 0.17.13 CON-012.
9. No executable tree, protocol parent, hub parent, or pairing parent changed.

Run strict descriptive lint on corrected SPEC-008. Require zero errors and
zero warnings with the current anuna-dev linter.

Then read
`spec-008-0.5.17-final-claude-adversarial-review-2026-08-25.md`. Verify that
its M-1 is exactly the defect corrected here.

The preceding reviewer states that this review need not repeat the invoke
mutation or protocol regression pass. Carry them only after verifying that no
executable or other authority byte changed.

Existing LOW observations do not block PASS. Report any new or worsened defect
at its measured severity.

Production allocation, push, release, and deployment remain prohibited.

## Verdict and report

Return PASS only with zero CRITICAL, HIGH, and MEDIUM findings.

Write the report to
`specs/trajectory/SPEC-008/spec-008-0.5.17-authority-singularity-claude-adversarial-review-2026-08-25.md`.

Record exact revisions, commands, counts, lint results, authentication path,
Circus attempt, findings, and verdict. Record no secret or account identifier.
