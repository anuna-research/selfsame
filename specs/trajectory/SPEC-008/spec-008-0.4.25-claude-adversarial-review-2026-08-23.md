---
id: SPEC-008-review-0.4.25
title: SPEC-008 0.4.25 — coordinated Tier-1 adversarial review
status: active
version: 1.0.0
tier: 1
verdict: REJECT
reviewed-candidate: "[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]]"
reviewer-model-family: Anthropic Claude
reviewer-model-version: claude-opus-5
reviewer-session: session_01FXGMWFvzTwdeGc9PcRTZvK
reviewer-circus-attempt: spec008-v0425-review/1
generation-model-family: OpenAI GPT-5
generation-model-version: gpt-5.6-sol
generation-session: 01a029aa-9127-7c42-ad28-81512b91ded6
last-updated: 2026-08-23
---

# SPEC-008 0.4.25 — coordinated Tier-1 adversarial review

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

Verdict: **REJECT**.

Five MEDIUM defects stand. Two LOW defects stand. No CRITICAL or HIGH defect
stands.

The 0.4.24 HIGH finding closes on code-backed evidence. Four of the six 0.4.24
LOW findings close. The two remaining LOW findings produce five of the seven
new defects, because their repairs introduce fresh resolution failures.

## Authentication, model, and independence evidence

### Authentication gate

The gate ran before any candidate prose entered this context. Its result was
recorded at the moment of retrieval, per
[[PROTO-001-usdd-agent-protocol#Externalised Verification Record]].

```text
$ env | grep -c '^ANTHROPIC_API_KEY='
0
```

The environment carries no `ANTHROPIC_API_KEY`. The subscription path is
therefore the only credential in use.

```text
$ claude auth status --json      # recorded fields only
authMethod       = claude.ai
apiProvider      = firstParty
subscriptionType = max
```

The three values match the gate. No electronic mail address, organisation
name, organisation identifier, token, or secret appears in this report. The
command emits further fields, and this report omits every one of them.

### Reviewer identity

| Property | Value |
|---|---|
| Model family | Anthropic Claude |
| Model | Opus 5 |
| Resolved model identifier | `claude-opus-5` |
| Claude session identifier | `session_01FXGMWFvzTwdeGc9PcRTZvK` |
| Circus attempt identifier | `spec008-v0425-review/1` |
| Circus branch | `circus/spec008-v0425-review/1` |
| Circus worktree | `/Users/anuna-01/Code/selfsame-spec008-v0425-review-1` |
| Circus attempt state at launch | `prepared`, recorded `2026-08-23T10:47:19Z` |
| Circus integration reference | `spec/spec-004-web-manual-binding` |

The attempt record lives at
`.git/circus/spec008-v0425-review/1/record.json` in the primary checkout.

### Distinctness from the generating record

| Axis | Generator | Reviewer | Distinct |
|---|---|---|---|
| Model family | OpenAI GPT-5 | Anthropic Claude | yes |
| Model version | `gpt-5.6-sol` | `claude-opus-5` | yes |
| Session | `01a029aa-9127-7c42-ad28-81512b91ded6` | `session_01FXGMWFvzTwdeGc9PcRTZvK` | yes |
| Vendor | OpenAI | Anthropic | yes |
| Context | Codex generation session | fresh Circus worktree | yes |

All three candidates record the same generation session. This review records a
different vendor, a different family, and a different session identifier.
[[PROTO-001-usdd-agent-protocol#Multi-Model Cognitive Diversity]] requires
cross-model review at Tier 1, and that requirement is satisfied.

This context received the three candidates, their effective predecessors, the
named code baselines, and a defect-finding mandate. It received no generator
reasoning, per cross-model protocol step 3.

### Executed evidence beyond reading

Every claim below rests on a command, a file read at a named line, or a
mechanical count. The following commands ran during this review.

```text
$ tools/usdd-lint.sh --type descriptive --strict <selfsame 0.4.25>
          longest_sentence       20 (limit 25, mode descriptive)
          scanned                209 sentences, 1992 words
          TOTAL                  0 errors, 0 warnings

$ tools/usdd-lint.sh --type descriptive --strict <cbcl-bus 0.16.18>
          longest_sentence       20 (limit 25, mode descriptive)
          scanned                168 sentences, 1539 words
          TOTAL                  0 errors, 0 warnings

$ tools/usdd-lint.sh --type descriptive --strict <cbcl-pairing 0.4.11>
          longest_sentence       20 (limit 25, mode descriptive)
          scanned                47 sentences, 410 words
          TOTAL                  0 errors, 0 warnings
```

```text
$ grep -Fc "both live deletes under the held locks." <selfsame 0.4.23>
0
$ grep -Fc "both live-row deletes under the held locks." <selfsame 0.4.23>
1
$ grep -Fc "A present account row SHALL name" <selfsame 0.4.23>
1
$ grep -Fc "These authority owners expose that contract:" <selfsame 0.4.23>
1
$ grep -Fc "These ceremony-migration owners expose the same busy result:" <selfsame 0.4.23>
1
$ grep -Fc "Every production consumer handles both tagged result forms" <selfsame 0.4.23>
1
$ grep -Fc "fail-closed wildcard." <selfsame 0.4.23>
1
```

```text
$ grep -Fc "An absent account row enters non-mutating" <cbcl-bus 0.16.16>
1
$ grep -Fc "both live deletes." <cbcl-bus 0.16.16>
1
$ grep -Fc "Every production consumer handles both result forms" <cbcl-bus 0.16.16>
1
$ grep -Fc "refusal wildcard." <cbcl-bus 0.16.16>
1
```

```text
$ awk 'NR>257 && NR<318' <cbcl-bus 0.16.15> | grep -c '^- \[ \]'
42
$ awk 'NR>257 && NR<318' <cbcl-bus 0.16.15> | grep -cE '^- \[[xX]\]'
0
```

```text
$ grep -rn --include='*.rs' -E 'PROFILE_VERSION|profileVersion|evidenceVersion|payloadVersion' crates/ | wc -l
59
$ grep -rn --include='*.rs' -E 'PROFILE_VERSION|profileVersion|evidenceVersion|payloadVersion' crates/*/src | wc -l
41
$ grep -rni --include='*.rs' -E 'PROFILE_VERSION|profileVersion|evidenceVersion|payloadVersion' crates/*/src | wc -l
68
```

```text
$ git diff --stat <selfsame baseline> <selfsame candidate> -- crates/
(empty)
$ git diff --stat <cbcl-bus baseline> <cbcl-bus candidate> -- . ':(exclude)specs/'
(empty)
$ git diff --stat <cbcl-pairing baseline> <cbcl-pairing candidate> -- . ':(exclude)specs/'
(empty)
```

Neither candidate touches code. Every code claim therefore resolves at the
declared implementation baseline.

## Reviewed commits and baselines, resolved rather than assumed

| Repository | Role | Commit | Resolved subject |
|---|---|---|---|
| selfsame | candidate | `657a1af5fff892aab6bac51f990071ce97a00553` | `spec(pairing): preserve fail-closed room start` |
| selfsame | implementation baseline | `0220cec2dec44cd95d4f411ea4814d790b6716d2` | `Merge circus/spec008-v0414-review/1 into spec/spec-004-web-manual-binding` |
| cbcl-bus | candidate | `8007febb3a984dcb26b77f736310cdc7c016e761` | `spec(identity): preserve fail-closed room start` |
| cbcl-bus | implementation baseline | `ce894a7f2debc111b610296b1a3780de6084e37e` | `spec(path-b): align admission and local unlink` |
| cbcl-pairing | candidate | `2ee44449a043dc701a6e91f91b96c30fbfb11976` | `spec(pairing): advance coordinated consumer pointer` |
| cbcl-pairing | implementation baseline | `c22f2d0526432c01c56d64be35a8d4f87516ec92` | declared baseline of the pointer chain |
| did-crdt | coordinated member | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | `fix(rendezvous): bound storage, amortise expiry, cap body before buffering` |

Reviewed candidate documents:

- `specs/trajectory/SPEC-008/spec-008-0.4.25-fail-closed-room-start-and-singular-authority.md`;
- `specs/trajectory/SPEC-053/spec-053-0.16.18-fail-closed-room-start.md`; and
- `specs/trajectory/SPEC-001/spec-001-0.4.11-coordinated-consumer-pointer.md`.

Each cbcl-bus baseline is an ancestor of its candidate, which was checked with
`git merge-base --is-ancestor`. Effective authority was resolved by reading
each predecessor and applying every declared retirement and replacement.
Headings, changelogs, review prose, and retired text supplied no authority.

## Verdict and severity counts

| Severity | Count | Gate effect |
|---|---|---|
| CRITICAL | 0 | none |
| HIGH | 0 | none |
| MEDIUM | 5 | blocking |
| LOW | 2 | not blocking on their own |

A PASS requires zero unresolved CRITICAL, HIGH, and MEDIUM findings. Five
MEDIUM findings stand, so the verdict is REJECT.

| ID | Severity | One-line statement |
|---|---|---|
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]] | MEDIUM | The corrected withdrawal range boundary occurs nowhere in its target, so the replacement does not resolve. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]] | MEDIUM | REQ-1004 replaces REQ-1003 in full and never re-declares REQ-1003's two REQ-1002 ranges. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]] | MEDIUM | The closed four-identifier set cannot match nine of the twenty-seven witnesses that share its requirement. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]] | MEDIUM | The new room-start busy value reaches a WebSocket consumer that no clause names and no generated row reaches. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-5]] | MEDIUM | The hub current-test range contains two tests the same requirement declares replaced in full. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]] | LOW | Two of three named out-of-scope governing contracts resolve to no document in the coordinated set. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-2]] | LOW | The candidate states two corrected range boundaries, and exactly one boundary differs from its predecessor. |

## Findings

### N10-M-1 (MEDIUM, gate-blocking) — the corrected withdrawal range boundary occurs nowhere in its target, so the one replacement that repaired a 0.4.24 finding does not resolve

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#NFR-927]]).**

[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]] asked for one
boundary extension. Its minimum sound correction reads:

```text
Extend the `REQ-1003` second range to consume the `Credential/v2` paragraph,
and extend the `NFR-926` first range to consume the reciprocal paragraph,
matching the cbcl-bus boundary.
```

[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#NFR-927]]
applies the NFR half:

```text
The first range starts with "A present account row SHALL name". It ends with
"both live deletes under the held locks."
```

The target is
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]]. Its
text at lines `154` and `155` reads:

```text
Only reciprocal agreement continues. That branch takes the exact withdrawal
write lock and performs both live-row deletes under the held locks.
```

The declared closing phrase is `both live deletes under the held locks.` The
actual phrase is `both live-row deletes under the held locks.` The two differ
by the token `-row`.

Both strings were counted mechanically inside the target file. The declared
phrase returns zero matches. The actual phrase returns one match. Every other
declared boundary in this candidate returns exactly one match.

**The predecessor boundary resolved.**
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#NFR-926]]
declared the same range ending at `Neither case deletes any row.`, which
occurs once in `NFR-925`. The repair therefore replaces a resolvable boundary
with an unresolvable one. That is the regression class this review is
instructed to reject.

**The phrase belongs to the twin rather than to the target.** The cbcl-bus
predecessor
[[spec-053-0.16.16-total-result-contracts-and-static-nesting#NFR-023]] ends
its corresponding sentence with `both live deletes.`, and that string resolves
once there. The candidate appears to have taken the cbcl-bus phrase and
appended the Selfsame tail. The composite matches neither document.

**Two readings follow, and the candidate selects neither.** Under a repair
reading, the range ends at line `155` and the duplication closes. Under a
strict reading, no range resolves, so no replacement occurs. Every sentence
from `A present account row SHALL name` through `under the held locks.` stays
current beside the new replacement text.

Under the strict reading the vaguer predecessor sentence `A disagreeing
account row refuses.` survives beside the precise
`#(error wrong-generation)` terminal that closed
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-6]]. Neither
sentence contradicts the other, so no unsafe implementation follows.

**Why this is MEDIUM rather than LOW.** The amendment's only mechanism for
changing effective text is the exact range replacement.
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]
requires an authority scan to `Resolve every effective clause from 0.4.4
through CON-984`. A range with a phantom boundary defeats that scan, and the
row resolves to `unverified` under
[[PROTO-001-usdd-agent-protocol#Compliance Evidence (self-report is inadmissible)]].

**Why this is not HIGH.** Both readings preserve every withdrawal obligation.
Every refusal still deletes zero rows under either text. The defect is
resolution rather than behaviour.

**Minimum sound correction.** Correct the declared closing phrase to the
exact predecessor text, which reads `both live-row deletes under the held locks`
with a trailing period.

The alternative restores the earlier boundary at `Neither case deletes any row`
and adds a second exact range for the reciprocal paragraph.

---

### N10-M-2 (MEDIUM, gate-blocking) — REQ-1004 replaces REQ-1003 in full and never re-declares its two REQ-1002 range replacements, so the effective requirement has two readings

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
and
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]]).**

[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#REQ-1003]]
supplements
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#REQ-1002]] and
declares two exact range replacements against it. The first replaces the
narrow classifier with the seven-class classifier. The second replaces the
witness paragraph.

[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
opens with a full replacement and one sentence substitution:

```text
This requirement replaces REQ-1003 in full.
It also replaces this current sentence in REQ-1002: ...
```

It declares no REQ-1002 range. The paired non-functional requirement takes the
opposite approach in the same document:

```text
It applies the four NFR-925 replacements declared by NFR-926, with two
corrected range boundaries.
```

The authors state the re-application for the NFR chain and omit it for the REQ
chain. `REQ-1002` remains current, because nothing replaces it in full, and
the candidate itself substitutes one of its sentences.

**Reading A keeps REQ-1003's ranges effective.**
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]]
states `Every earlier replacement remains effective unless this contract
states otherwise.` Effective `REQ-1002` then carries the seven-class
classifier, the eighteen-line witness block with its retired vocabulary
labels, and the `Credential/v2` paragraph twice in succession. The duplication
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]] reported
survives untouched.

**Reading B retires those ranges with REQ-1003.** Effective `REQ-1002` then
reverts to its own lines `34` to `36`:

```text
The production scan is the completeness authority for v1 comparisons,
substitutions, and ingress conversions.
```

That is the three-class completeness sentence
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-3]] rejected. Its
line `51` to `55` closed copula returns as well:

```text
The direct ingress-conversion witnesses are
`crates/selfsame-app-identity/src/ceremony.rs:413` and
`crates/selfsame-app-identity/src/provider_hint.rs:96`.
```

That is the closed copula
[[spec-008-0.4.23-claude-adversarial-review-2026-08-23#N8-L-4]] rejected, and
it omits the third production site `profile.rs:384`.

**One sentence survives under both readings.** `REQ-1002` line `43` and
`REQ-1003`'s replacement text are byte-identical:

```text
The scan includes `PROFILE_VERSION`, `profileVersion`, `evidenceVersion`, and
`payloadVersion`.
```

`REQ-1004` states the same four identifiers as a closed set. The open copula
therefore stays current beside the closed one, whichever reading applies.
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-3]] asked for the
set to become closed, and the open sentence blocks that outcome.

**No conflict-resolution rule reaches this.** Effective `REQ-1002` states its
own scan rule:

```text
The scan covers numbered clauses, numbered list items, and unnumbered
normative paragraphs. A heading, changelog, or earlier gate label cannot make
conflicting normative text historical.
```

Only an exact retirement removes conflicting text, and nothing retires that
sentence.

**No unsafe implementation follows.** The seven-class scan is a superset of
the three-class scan, and the credential freeze obligation is stated plainly
in `REQ-1004`. The defect is that one reading leaves two 0.4.24 findings open
and the other regresses two 0.4.23 findings.

**Minimum sound correction.** Add one sentence to
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
stating that it applies REQ-1003's two REQ-1002 range replacements, with the
second range extended through the `Credential/v2` paragraph. Add one exact
sentence replacement retiring `The scan includes …` in favour of the closed
set. The alternative is to replace `REQ-1002` in full.

---

### N10-M-3 (MEDIUM, gate-blocking) — the closed four-identifier set cannot match nine of the twenty-seven witnesses named in the same requirement, and the three ingest substitutions are among them

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
and
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]).**

[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
closes the identifier set:

```text
Its closed identifier set is `PROFILE_VERSION`, `profileVersion`,
`evidenceVersion`, and `payloadVersion`.
```

Every one of the twenty-seven named witness lines was read at the declared
baseline. All twenty-seven resolve, and each construct matches its assigned
class. Nine of them contain none of the four identifiers.

| Witness | Line text at the baseline | Class in REQ-1004 |
|---|---|---|
| `web-device/src/lib.rs:831` | `profile_version: value.profile_version,` | ingest substitution |
| `web-device/src/lib.rs:847` | `profile_version: value.profile_version,` | ingest substitution |
| `web-device/src/lib.rs:861` | `profile_version: value.profile_version,` | ingest substitution |
| `web-device/src/lib.rs:1346` | `profile_version: 1,` | version literal |
| `web-device/src/lib.rs:1360` | `profile_version: 1,` | version literal |
| `web-device/src/lib.rs:1369` | `profile_version: core.profile_version,` | typed version-field assignment |
| `app-identity/src/ceremony.rs:413` | `profile_version: payload` | untyped ingress conversion |
| `app-identity/src/provider_hint.rs:96` | `profile_version: value` | untyped ingress conversion |
| `app-identity/src/enrollment.rs:592` | `\|\| statement.profile_version != observed.offer.profile_version` | comparison |

Each of those lines carries the Rust snake-case field `profile_version`, and
the closed set names no such identifier.

**The matching discipline is undeclared, and the two readings differ by
twenty-seven production rows.** A case-sensitive scan of every production Rust
source returns 41 matching lines. A case-insensitive scan returns 68, because
`PROFILE_VERSION` then also matches `profile_version`. `REQ-1004` states
neither discipline, and it lists `PROFILE_VERSION` and `profileVersion`
separately, which reads as case-sensitive intent.

**The test row cannot be discharged under the plain reading.**
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]
requires:

```text
Scan every production Rust source under REQ-1004's closed four-identifier set
and seven-class classifier.
...
Require every current witness and exclusion to resolve.
```

Nine current witnesses cannot appear in a case-sensitive scan output, so the
row fails. A reading under which every witness resolves has to widen the set
that the same requirement calls closed.

**The lost class is the security-bearing one.** `REQ-1004` states:

```text
Named sites are evidence only. They cannot close or limit the production
scan.
```

The scan is therefore the completeness authority for `Credential/v2 reaches no
classified credential-v1 site` and for `Frozen v1 bytes remain
byte-identical`. Under the plain reading that authority cannot see any of the
three ingest substitutions. Each of those parses untyped browser input into a
typed version field.

**Why this is MEDIUM rather than LOW.**
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-3]] was LOW because
an open verb left the boundary undecidable. The repair closes the boundary and
makes the requirement contradict its own evidence. An implementer generating
the inventory has to guess, and one guess makes the candidate's own test fail.

**Why this is not HIGH.** The freeze obligation is stated plainly and does not
depend on the scan for its force. No conforming implementation calls
`EnrolmentAllocator::new_native` from credential/v2.

**Minimum sound correction.** Add `profile_version` to the closed set, making
it five identifiers, and state the matching discipline as exact case-sensitive
substring matching. The alternative is to declare case-insensitive matching and
to record the resulting 68-line production baseline.

---

### N10-M-4 (MEDIUM, gate-blocking) — the newly declared room-start busy value reaches a WebSocket consumer that no clause names, and the generated inventory structurally cannot reach it

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#NFR-927]] and
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1154]])
and cbcl-bus
([[spec-053-0.16.18-fail-closed-room-start#NFR-025]] and
[[spec-053-0.16.18-fail-closed-room-start#TEST-113]]).**

Both candidates declare a new propagation:

```text
`cbcl-chat-rooms:get-or-start/1` preserves an `authority-busy` child-start
failure. `cbcl-chat-rooms:ingest/3` returns that value without calling
`cbcl-chat-room:ingest/3`.
```

`cbcl-chat-rooms:get-or-start/1` has exactly three production consumers at the
declared baseline. A repository-wide scan of production LFE under `apps/`
returns them and no fourth.

| Consumer | Construct at the baseline | Named by the candidates |
|---|---|---|
| `cbcl-chat-rooms.lfe:49` | `(cbcl-chat-room:ingest (get-or-start room) payload sig)` | yes |
| `cbcl-chat-session-ws.lfe:1076` | `(cbcl-chat-room:evict (cbcl-chat-rooms:get-or-start room) name)` | no |
| `cbcl-chat-session-ws.lfe:1788` | `(let ((pid (cbcl-chat-rooms:get-or-start room)) …)` | no |

**One unnamed consumer degrades safely, and one does not.**
`cbcl-chat-room:evict/2` at `:98-102` wraps its call:

```lfe
(defun evict (pid handle)
  (case (catch (gen_server:call pid (tuple 'evict handle) (evict-timeout-ms)))
    ('ok 'ok)
    (`#(error ,reason) (tuple 'error reason))
    (_ #(error revoke-failed))))
```

The `catch` converts the exit into `#(error revoke-failed)`, and
`cbcl-chat-session-ws.lfe:1084` answers with that slug. That site is sound.

`cbcl-chat-room:join/9` at `:83-84` does not:

```lfe
(defun join (pid spid handle pk cap n caps account pathb)
  (gen_server:call pid (tuple 'join spid handle pk cap n caps account pathb)))
```

`do-hello` at `cbcl-chat-session-ws.lfe:1757` calls it at `:1797` with the
`get-or-start` result as its process reference. No `try` and no `catch`
encloses `:1788` through `:1819`, which was checked across the whole function
body.

**A refused room start therefore exits the session process.** Room `init/1`
answers `#(stop Reason)`. `supervisor:start_child` answers `#(error Reason)`.
`cbcl-chat-rooms.lfe:105-106` returns that tuple unchanged as the reply, and
the candidates require exactly that preservation. A `gen_server:call` whose
destination is `#(error authority-busy)` reads that tuple as a name-and-node
pair and exits.

**The declared terminal for this lane is not reached.**
`NFR-927` states `Busy reaches the existing WebSocket 1013 terminal and never
becomes 'ok.` for the sibling consumer at `cbcl-chat-session-ws.lfe:1898`. The
join path instead produces an uncontrolled exit.

The socket then closes on an internal-error code rather than the declared
retryable one. The browser generation logic that
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]] describes
never engages.

**The generated inventory cannot supply the missing row.**
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1154]]
states this scan domain:

```text
Generate the production consumer inventory from every public owner.
```

The public owner set is the closed enumeration in
[[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]]. Two
closed copulas open it:

```text
The authority public owners are:
The ceremony-migration public owners are:
```

That enumeration contains no `cbcl-chat-rooms` function and no
`cbcl-chat-room` function.

The candidates recognise this gap for the webhook route and close it by hand.
They name `cbcl-chat-rooms.lfe:49`, `:99-104`, `cbcl-chat-room.lfe:201`,
`:221`, and `cbcl-webhook-handler.lfe:76`.

The same hand enumeration omits the WebSocket route. A generated inventory
therefore produces no row for `cbcl-chat-session-ws.lfe:1788`. The test passes
green while the join path exits under the contention the amendment names.

**The requirement text and the test text disagree on scope.** `NFR-927` states
`A repository-wide production consumer scan is the completeness authority` and
`Every consumer handles the owner's success shape and #(error
authority-busy)`. `TEST-1154` narrows the scan to public owners. The
obligation binds a site that the evidence mechanism cannot see, which
[[PROTO-001-usdd-agent-protocol#Compliance Evidence (self-report is inadmissible)]]
treats as an unticked gate.

**Why this is MEDIUM rather than HIGH.** The outcome is an uncontrolled exit
rather than an admission. No durable effect precedes the exit at either site:
`:1788` runs before the join, and `:1076` runs before the durable revocation.
The session fails closed, ungracefully.

**Why this is not LOW.** The hazard predates the amendment for
`store-unavailable`, and the amendment now routes a second and far more
frequent value along it. Contention is ordinary load; store death is not. The
amendment also declares this exact propagation, so the omission sits inside
the clause that introduces the value.

**Minimum sound correction.** Add `cbcl-chat-session-ws.lfe:1788` to the
inventory with the WebSocket 1013 terminal, and add `:1076` with its
`revoke-failed` terminal. State that the production consumer scan covers every
consumer of a value carrying `authority-busy`, and not only consumers of a
declared public owner. Add a forcing to
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1154]] and
[[spec-053-0.16.18-fail-closed-room-start#TEST-113]] driving a WebSocket hello
against a room whose start refuses, requiring the 1013 close and zero
membership grant.

---

### N10-M-5 (MEDIUM, gate-blocking) — the hub current-test range contains two tests the same requirement declares replaced in full

**Repository owner: cbcl-bus
([[spec-053-0.16.18-fail-closed-room-start#REQ-080]] and
[[spec-053-0.16.18-fail-closed-room-start#TEST-114]]) and selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]]).**

[[spec-053-0.16.18-fail-closed-room-start#REQ-080]] states the replacement:

```text
TEST-113 replaces TEST-111 in full. TEST-114 replaces TEST-112 in full.
```

Three lines later the same requirement states the current set:

```text
It also contains TEST-093, TEST-094, TEST-098, and TEST-109 through TEST-114.
```

The range `TEST-109 through TEST-114` contains `TEST-111` and `TEST-112`. One
sentence retires them, and the next sentence declares them current.

**The predecessor was coherent, so this is new.**
[[spec-053-0.16.17-background-and-webhook-busy-terminals#REQ-079]] states
`OBS-013, ADR-035, TEST-111, and TEST-112 supplement their current sets.` A
supplementing test legitimately joins a contiguous range. The verb changed to
`replaces` at 0.16.18 while the range stayed contiguous.

**The Selfsame document repeats the same range.**
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#CON-984]]
states `The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068,
TEST-070, TEST-093, TEST-094, TEST-098, and TEST-109 through TEST-114.` Both
repositories therefore carry the contradiction.

**The Selfsame set avoids it for its own tests.** The same contract states
`TEST-1134, TEST-1135, TEST-1139, TEST-1150, TEST-1154, and TEST-1155 are
current.` That enumeration excludes the replaced `TEST-1151`, `TEST-1152`, and
`TEST-1153`. The hub range uses the opposite form.

**The candidate's own test cannot be discharged.**
[[spec-053-0.16.18-fail-closed-room-start#TEST-114]] states this obligation:

```text
Resolve every effective clause through REQ-080. Require one current test set,
one coordinated review set, and one pointer set.
```

[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]
states a matching one:

```text
Require one protocol survivor set, hub set, Selfsame correction set, review
set, and pointer set.
```

A set that both contains and excludes two members is not one set.

**No obligation is lost.** `TEST-113` carries every `TEST-111` obligation and
adds the room-start forcings, and `TEST-114` carries every `TEST-112`
obligation. The retired tests forbid nothing the successors permit. The defect
is the singular-authority property, which is the same property
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-M-1]] found broken
and graded MEDIUM.

**Minimum sound correction.** State the hub current set as `TEST-093`,
`TEST-094`, `TEST-098`, `TEST-109`, `TEST-110`, `TEST-113`, and `TEST-114`, in
both repositories.

---

### N10-L-1 (LOW) — two of the three named out-of-scope governing contracts resolve to no document in the coordinated set, and one collides with a real in-scope artefact

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
and
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]).**

[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-3]] asked the
candidate to `Name the governing contract of each one.`
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
names three:

```text
- `successionVersion` belongs to identity succession under
  [[SPEC-004-application-scoped-identity#REQ-231]] and
  [[SPEC-004-application-scoped-identity#CON-225]];
- `WIRE_VERSION` belongs to the Selfsame core invitation-wire namespace under
  Selfsame core SPEC-001 REQ-016; and
- `CODE_VERSION` belongs to the Selfsame core link-code namespace under
  Selfsame core SPEC-001 CON-001.
```

The first resolves. `specs/SPEC-004-application-scoped-identity.md:1275`
carries `REQ-231: Identity succession is explicit, bounded, and confirmed`, and
`:4417` carries `CON-225: Application identifier succession`. The module header
of `crates/selfsame-app-identity/src/succession.rs:1` reads `Application
identifier succession — CON-225, REQ-231, REQ-202`, so the code agrees.

The second and third do not resolve. The reviewed selfsame vault contains no
`SPEC-001` document. A search of `specs/` returns none, and the repository
`README.md:312` locates the document in a sibling checkout at
`../anuna-ssi/specs/SPEC-001-device-key-provisioning.md`. That checkout is
outside the coordinated set and carries no pinned commit.

**The bare identifier collides with an in-scope artefact.**
`specs/SPEC-001-reusable-blind-pairing.md:681` in cbcl-pairing carries
`CON-001: Invitation and carrier boundary`. That contract is inside the
coordinated set.

The same requirement calls its document `cbcl-pairing SPEC-001` three lines
earlier. The candidate frontmatter declares the same document as its
conformance target. A reader resolving `SPEC-001 CON-001` therefore lands on
the wrong artefact. `REQ-016` occurs zero times in that document.

**One test row cannot be discharged.**
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#TEST-1155]]
states this row:

```text
Require `successionVersion`, `WIRE_VERSION`, and `CODE_VERSION` to resolve to
their named out-of-scope namespaces and governing contracts.
```

Two named contracts resolve to no reviewable document.

**The namespaces themselves are correct.**
`crates/selfsame-core/src/lib.rs:58` reads `pub const WIRE_VERSION: i64 = 1;`
and `crates/selfsame-core/src/code.rs:78` reads `const CODE_VERSION: u8 = 1;`.
Both sit outside the credential ceremony crates, so the exclusion is sound on
its merits.

**Two of three exclusions also break the wikilink obligation.** The SPEC-004
pair uses wikilinks and the two SPEC-001 references use plain text.
[[PROTO-001-usdd-agent-protocol#Wikilinks (REQUIRED in Downstream Outputs)]]
states that every output cross-reference is a wikilink, and registry row
`P001-R013` records the gap in tooling rather than in the obligation.

**Minimum sound correction.** Name the document by its descriptive stem and
wikilink it, as `[[SPEC-001-device-key-provisioning#REQ-016]]` and
`[[SPEC-001-device-key-provisioning#CON-001]]`. Record it as an out-of-set
reference, or pin its commit in the coordinated review set.

---

### N10-L-2 (LOW) — the candidate states two corrected range boundaries, and exactly one boundary differs from its predecessor

**Repository owner: selfsame
([[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#NFR-927]]).**

The candidate states:

```text
It applies the four NFR-925 replacements declared by NFR-926, with two
corrected range boundaries.
```

Every boundary declaration in
[[spec-008-0.4.24-explicit-background-and-http-busy-terminals#NFR-926]] was
compared with its counterpart.

| Fragment | NFR-926 boundary | NFR-927 boundary | Changed |
|---|---|---|---|
| 1 | starts `A present account row SHALL name`, ends `Neither case deletes any row.` | starts `A present account row SHALL name`, ends `both live deletes under the held locks.` | end only |
| 2 | the sentence `These authority owners expose that contract:` | identical | no |
| 3 | the sentence `These ceremony-migration owners expose the same busy result:` | identical | no |
| 4 | starts `Every production consumer handles both tagged result forms`, ends `fail-closed wildcard.` | identical | no |

Exactly one boundary differs. The second correction that
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]] requested
belongs to the REQ chain, and
[[spec-008-0.4.25-fail-closed-room-start-and-singular-authority#REQ-1004]]
declares no range at all, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]].

[[PROTO-001-usdd-agent-protocol#Compliance Evidence (self-report is inadmissible)]]
treats a self-stated count as a claim that needs derivation. This count
disagrees with the document that states it.

**Minimum sound correction.** State one corrected boundary, or supply the
second one in `REQ-1004` as
[[spec-008-0.4.24-claude-adversarial-review-2026-08-23#N9-L-4]] asked.

---

## Disposition of every 0.4.24 finding

| ID | Disposition | Code-backed evidence |
|---|---|---|
| **N9-H-1** — the new busy result retires the atom the SPEC-055 room-start guard matches | **CLOSED, with a residual recorded as [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]]** | `NFR-927` states `Retry-exhausted transaction contention returns #(error authority-busy). Other transaction failure from cbcl-chat-roomcfg:get/1 remains #(error store-unavailable).` Both atoms therefore survive, and `cbcl-chat-room.lfe:207-209` keeps the exact value its guard matches. `NFR-927` adds `At supervised room start, authority-busy and store-unavailable both stop the child. They create no ETS registration, room process, room state, or fan-out.` The registration claim resolves: `cbcl-chat-rooms.lfe:99-104` inserts into `cbcl-rooms` only on the `#(ok ,pid)` arm and monitors only there. `NFR-927` adds `Only #(error unknown-room) MAY select the ADR-007 #(public undefined false) default`, which reaches the wildcard at `cbcl-chat-room.lfe:213-215` that the prior finding identified. It adds `Neither result SHALL become #(error unknown-room), a private-channel decision, or a channel configuration`, which closes the enumeration gap the prior finding reported. `cbcl-chat-room.lfe:201` and `:221` now appear in the webhook inventory, and both lines resolve to the claimed constructs. `TEST-1154` adds a fourth forcing at supervised room-process start and a fifth at the `get-dialects/1` read, each requiring `zero ETS registration, zero default configuration, and zero fan-out`. It also adds `Force store-unavailable on the webhook route`, so the previously unreachable 409 path for an unreadable config is closed. `cbcl-bus` `NFR-025` and `TEST-113` carry byte-equivalent obligations. |
| **N9-M-1** — three current-value clauses name the previous round's coordinated versions | **CLOSED, with a residual recorded as [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-5]]** | The candidate takes the primary correction. `CON-984` states `TEST-1155 replaces TEST-1151 and TEST-1153 in full`, and `TEST-1155` repeats that replacement. The narrow `TEST-1151` scan row retires with it. `REQ-1004` replaces the exact sentence `Credential/v2 SHALL conform to cbcl-pairing SPEC-001 0.4.9-draft.`, which resolves once at `spec-008-0.4.23-total-result-contracts-and-static-nesting.md:80`, with the `0.4.11-draft` form. All three pointer values now agree: `specs/SPEC-008-production-pairing-claimant.md:8` and `:15` name `CON-984`; `specs/SPEC-053-key-root-identity.md:15` and `:688` name `REQ-080` at 0.16.18; the cbcl-pairing frontmatter names `consumer-design: selfsame SPEC-008 0.4.25-draft`. `REQ-080` and `REQ-033` assert the same three values. The residual is a different range, in the hub test set. |
| **N9-L-1** — the collection-sink inventory states six, and two further sinks exist | **CLOSED** | `NFR-927` replaces `The six collection sinks include:` with `Known collection sinks include:`, so the definite numeral is gone. It adds `cbcl-chat-room.lfe:221` and `:550, consuming get-dialects/1 into room state or a room-configuration frame`, and adds `the discarded claim/5 result at cbcl-chat-session-ws.lfe:1898`. All eight lines resolve at the baseline: `:221` and `:550` each read `(dialects (case (cbcl-chat-roomcfg:get-dialects room) (\`#(ok ,ds) ds) (_ '())))`, and `:1898` reads `('true (cbcl-chat-roomcfg:claim room vis cap handle dialects) 'ok)`. The other six lines were re-checked and each carries the sink the candidate claims. `cbcl-bus` `NFR-025` carries the identical list. |
| **N9-L-2** — ten of the eighteen incorporated witness labels contradict the classifier | **CLOSED** | `REQ-1004` converts the block into a two-column table whose second column is `Current class`, and states `The class column below governs and replaces every retired vocabulary label.` That is the second option of the minimum sound correction. All eighteen rows were read at the baseline and each construct matches its assigned class: `lib.rs:600` is a `format!` argument list entry classified as an emitted member; `lib.rs:1346` and `:1360` read `profile_version: 1,` and classify as version literals; `ceremony.rs:308` and `:510` read `!= Some(1)` and classify as comparisons. No undecidable class column remains. |
| **N9-L-3** — the identifier set is open, and a fifth namespace has no disposition | **NOT CLOSED, recorded as [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]], [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]], and [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]]** | `REQ-1004` states the set closed and names three out-of-scope namespaces, which is the requested shape. Three defects remain. The closed set matches none of nine of its own witnesses. The open sentence `The scan includes …` stays current in effective `REQ-1002`. Two of the three governing contracts resolve to no document in the coordinated set. |
| **N9-L-4** — two exact replacements duplicate the survivor text that follows them | **NOT CLOSED, recorded as [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]] and [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]]** | The NFR half declares a corrected boundary whose phrase returns zero matches in the target. The REQ half declares no range, so the `Credential/v2` duplication survives under the reading that keeps REQ-1003's ranges effective. The candidate's disposition states `no duplicate survivor text remains`, and the evidence does not support that. |
| **N9-L-5** — the retired range carries away the replacement authority for five tests | **CLOSED** | `CON-984` restates the relationship: `TEST-1134, TEST-1135, TEST-1139, TEST-1154, and TEST-1155 collectively replace TEST-1129 through TEST-1133. No superseded portion or open range supplies authority.` The audit trail from each retired number to a current successor exists again. `TEST-1150` is current and absent from that successor list, and no obligation is lost, because `TEST-1154` states `Every existing TEST-1150 regression remains REQUIRED. This includes its four inline REQ-995 groups and WebFinger read case.` |
| **N9-L-6** — both candidates fail the strict controlled-language check | **CLOSED** | `tools/usdd-lint.sh --type descriptive --strict` ran over all three candidates and returned `0 errors, 0 warnings` for each. The lowercase `required` in both predecessors becomes `REQUIRED` at `spec-008-0.4.25-fail-closed-room-start-and-singular-authority.md:410` and `spec-053-0.16.18-fail-closed-room-start.md:312`. `TEST-1155`, `TEST-114`, and `TEST-062` each require the strict command over all three candidates, so the `P001-R082` gate now has a named mechanism. |

Eight findings were raised at 0.4.24. Six close on code-backed evidence, and
two remain open. Two of the six close with residuals recorded above as new
defects.

## Regression disposition of every 0.4.23 finding

Each closed N8 finding was re-tested against the resolved text, under both
readings of
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]].

| ID | Regression verdict | Evidence |
|---|---|---|
| **N8-M-1** — two consumer classes of shape-changed owners have no busy boundary | **NO REGRESSION** | `NFR-927` keeps both Path-B terminals verbatim in substance. Busy from `accounts/0` emits `OBS-905` phase `account-scan`, `calls no resolver`, and `leaves the supervised poller alive`. Busy from `revoke-observed/2` emits phase `revocation-apply`, reports no applied union, and performs no eviction. `cbcl-chat-path-b-poller.lfe:17` and `:32` resolve exactly, and the eviction `lists:foreach` at `:33` sits inside the `#(ok ,keys)` arm alone. |
| **N8-L-1** — the consumer map names one collection consumer, and six exist | **NO REGRESSION** | The enumeration grows from six to eight and drops its numeral. Every named line resolves. |
| **N8-L-2** — the witness set is defined by reference into a replaced requirement | **NO REGRESSION** | `REQ-1004` carries all eighteen lines inside its own text as a table. No authority scan has to read retired text. |
| **N8-L-3** — the classifier names five site classes and the scan obligation names three | **CONDITIONAL REGRESSION** | `REQ-1004` states the seven-class classifier in its own text, so the obligation exists. Under reading B of [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]], effective `REQ-1002` lines `34` to `36` revert to `for v1 comparisons, substitutions, and ingress conversions`, and the narrow sentence returns as current text beside the wide one. |
| **N8-L-4** — the ingress-conversion witness set is a closed copula omitting a third site | **CONDITIONAL REGRESSION** | `REQ-1004` writes `Untyped ingress-conversion evidence includes` and names `profile.rs:384`, which reads `let version = integer(value, "profileVersion")?;`. Under reading B, effective `REQ-1002` lines `51` to `55` revert to `The direct ingress-conversion witnesses are` with two sites. |
| **N8-L-5** — the authority owner list attaches tagged success to non-collection owners | **NO REGRESSION** | The second and third fragments resolve once each and are unchanged from `NFR-926`. `Only collection and predicate owners adopt tagged success. Every other public owner retains its declared success shape and adds its lane's typed busy shape.` survives verbatim. |
| **N8-L-6** — the withdrawal refusal branch names no terminal for the account-side case | **NO REGRESSION** | Both terminals appear in the new replacement text, and the cbcl-bus twin resolves cleanly. Under the strict reading of [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]] the older text also survives, and it forbids deletion in every refusal, so no permissive reading appears. `cbcl-chat-webfinger-store.lfe:213`, `:220`, `:227`, and `:228` were re-read and behave as the prior review recorded. |

Seven findings were re-tested. Five carry no regression, and two regress only
under one reading of an ambiguity already recorded as a MEDIUM defect.

## Regression disposition of every 0.4.22 finding

Each closed N7 finding was re-tested against the four resolved `NFR-925`
fragments and the one resolved `REQ-1002` sentence.

| ID | Regression verdict | Evidence |
|---|---|---|
| **N7-H-1** — the current test demands `authority-busy` from `lookup/1` | **NO REGRESSION** | The `lookup/1` exemption sits after the fourth fragment's `fail-closed wildcard.` boundary and survives. `cbcl-chat-webfinger-http.lfe:36` still maps `'unavailable` to `503`, and `base-headers/0` at `:40-42` still carries `cache-control: no-store`. |
| **N7-M-1** — no consumer obligation for the success value | **NO REGRESSION** | The fourth fragment's replacement keeps the stronger totality sentence and adds the channel-policy default to the prohibited sink list. |
| **N7-M-2** — two of three exhaustion classes are unreachable | **NO REGRESSION** | The `NFR-921` third range lies outside every fragment. `{aborted, nomore}` remains the only wrapper terminal. |
| **N7-M-3** — one authority-lane transaction is missing from the owner list | **NO REGRESSION** | The second fragment replaces one introductory sentence. `cbcl-chat-successor: accept/3` remains the last authority bullet, and the two forbidden catches at `cbcl-chat-successor.lfe:109` and `:134` sit outside every fragment. |
| **N7-M-4** — the eighteen-site inventory omits one production comparison | **NO REGRESSION IN TEXT** | `REQ-1004` keeps `enrollment.rs:592`, the three ingest copies, and all three ingress conversions. All twenty-seven named sites resolve. The scan that finds them is weakened by [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]], and the named list itself loses nothing. |
| **N7-M-5** — the replacement test drops four inline cases | **NO REGRESSION** | `TEST-1154` states `Every existing TEST-1150 regression remains REQUIRED`, and `TEST-1150` stays current in `CON-984`. |
| **N7-L-1** — two enumerated authority owners are ceremony-migration rows | **NO REGRESSION** | `account-binding/1` and `bind-account/2` remain the first ceremony-migration bullet. The third fragment replaces one sentence. |
| **N7-L-2** — replacing REQ-954 drops the parent dispositions | **NO REGRESSION** | Both parent paragraphs sit before the replaced sentence in `REQ-1002` and survive under both readings. `specs/SPEC-008-production-pairing-claimant.md:163` and `:173` still carry `REQ-903` and `REQ-904`, and `TEST-1155` requires their retained dispositions. |
| **N7-L-3** — the replacement pointer paragraph drops the open-gate half | **NO REGRESSION** | `CON-984` restates `The Selfsame parent points here and names this candidate's open review gate.` The parent Orientation blockquote at `specs/SPEC-008-production-pairing-claimant.md:16-20` states the gate is open. |
| **N7-L-4** — the name-squatting sentence is unscoped | **NO REGRESSION** | The scoped sentence and the `cbcl-provenance` residual sit after the fourth boundary and survive. |
| **N7-L-5** — the recorded trajectory is a summary line | **NO REGRESSION** | All three candidates enumerate a four-element trajectory in `generation-synthesis-trajectory`. |
| **N7-L-6** — the schema-migration lane names one site and a second exists | **NO REGRESSION** | The `legacy-rows/0` and `migrate-since/0` sentences sit outside every fragment. `cbcl-chat-app.lfe:87-90` still refuses startup on a migration error. |

Twelve findings were re-tested and none regresses.

## Disposition of the architecture hard stops

| Stop | Verdict | Evidence |
|---|---|---|
| **F-A** — policy is scoped to the exact application and relay | **HOLDS** | `CON-984` names nothing inside `REQ-981`, which stands at `spec-008-0.4.11-standalone-tofu-first-contact.md:51`. Its text keeps the live profile `byte-for-byte unchanged` at `profileVersion = 1`. The cbcl-bus GATE-04 box requiring the exact v2 policy pair sits among the 41 boxes that survive byte-for-byte. |
| **F-B** — the intent surface shows the authenticated application | **HOLDS** | The wallet half is untouched, which was checked with an empty `crates/` diff between baseline and candidate. `crates/selfsame-pairing/src/live.rs:479-488` still commits one explicit local decision, only from `ClaimantPhase::Endpoint`, and its doc comment binds that call to `LiveEffect::DisplayIntent`. Cbcl-pairing 0.4.11 adds one file and changes no protocol language. |
| **F-C** — no derivation, mint, publication, alias, or grant precedes consent | **HOLDS** | `CON-984` names nothing inside `CON-962`, whose effect order stands at `spec-008-0.4.9-standalone-tofu-first-contact.md:202`. Every new unavailability path creates nothing: either 503 `performs no room start, room ingest, persistence, or channel fan-out`, and the poller `performs no eviction`. |
| **F-D** — standalone first contact has a concrete fresh-scope bootstrap | **HOLDS** | `CON-984` names neither `CON-939` nor `REQ-970`. `CON-939` at `spec-008-0.4.4-standalone-tofu-first-contact.md:901-912` still declares the five-argument allocation call, and still states that `The scope plus pending ceremony commit atomically or the browser displays no carrier.` |
| **F-E** — relay operatorship is bound, or the residual is contained | **HOLDS** | Neither candidate claims a binding property for the relay operator. Cbcl-pairing `CON-028` states `Every protocol survivor recorded through CON-027 remains effective`, and `CON-027` preserved the no-relay-field property of `CON-024`. The 0.4.11 diff adds one file and touches no protocol asset. |
| **SPEC-007 REQ-812** — endpoint failures cannot authorise | **HOLDS** | The clause at `specs/SPEC-007-cbcl-pairing-cutover.md:328-330` states that mismatch, protocol violation, cancellation, relay closure, and expiry produce no accepted credential and no identity side effect before approved payload delivery. Every negative path added here creates nothing. [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]] is a hub session-liveness gap rather than an endpoint failure, and it produces no credential and no grant. |

`CON-984` restates every F-A through F-E correction and
[[SPEC-007-cbcl-pairing-cutover#REQ-812]] as hard stops, so the declaration
matches the finding.

## Repository assessments

### selfsame SPEC-008 0.4.25-draft (`657a1af`)

The commit adds one amendment file and advances two parent pointers. It
changes no code, which was checked with an empty `crates/` diff against the
declared baseline.

Every new identifier is unique and sequential. `REQ-1004`, `NFR-927`,
`OBS-906`, `CON-984`, `ADR-962`, `TEST-1154`, and `TEST-1155` each appear in
one file across the whole trajectory. The observation series reads `OBS-904`,
`OBS-905`, `OBS-906` with no gap and no reuse.

Three of the four declared `NFR-925` fragment boundaries resolve uniquely. The
fourth returns zero matches, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]]. The single
`REQ-1002` sentence replacement resolves uniquely at line `80`.

The requirement chain leaves `REQ-1002` in two readings, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]]. The closed
identifier set contradicts nine of its own witnesses, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]]. Two named
governing contracts resolve to no reviewable document, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]]. The
corrected-boundary count is wrong, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-2]].

The document passes the strict controlled-language check with zero errors and
zero warnings.

### cbcl-bus SPEC-053 0.16.18-draft (`8007feb`)

The commit adds one amendment file and advances the parent pointer at two
lines. It changes no code.

Both declared `NFR-023` range boundaries resolve uniquely, which was checked
with four exact string counts. The first range ends at `both live deletes.`
and consumes the reciprocal sentence, so the twin carries no boundary defect.

GATE-04 arithmetic is exact. The effective gate in
[[spec-053-0.16.15-explicit-busy-contracts-and-complete-traces#GATE-04]]
carries 42 open boxes and zero closed boxes. Both counts are mechanical, taken
between its heading at line `257` and the following heading at line `318`.
`REQ-078`
replaced the first box with one naming Selfsame 0.4.23, and `REQ-079` replaced
that with one naming Selfsame 0.4.24. `REQ-080` retires a box beginning
`- [ ] coordinated Selfsame 0.4.24` and ending `record generation provenance
and receive fresh cross-model PASS;`, and both phrases match the effective
first box. One box replaces one box, so 42 remain, 41 survive byte-for-byte,
and none closes.

`NFR-025` carries the same eight collection sinks, three boolean contexts,
poller terminals, room-start terminals, and webhook terminals as `NFR-927`.
The two documents state the same obligations in the same order.

The current-test range contradicts the same requirement's replacement
sentences, which is
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-5]].
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]] is shared with
selfsame, because both specify the same hub code.

The document passes the strict controlled-language check with zero errors and
zero warnings.

### cbcl-pairing SPEC-001 0.4.11-draft (`2ee4444`)

The candidate commit adds exactly one file of 124 lines and changes nothing
else, which was checked with `git show --stat`. The diff against the declared
baseline touches no file outside `specs/`.

`REQ-033` states that the complete protocol body remains
[[spec-001-0.4.8-split-room-shape-and-length#REQ-030]],
[[spec-001-0.4.8-split-room-shape-and-length#NFR-022]], and
[[spec-001-0.4.8-split-room-shape-and-length#ADR-023]]. All three artefacts
exist at lines `45`, `91`, and `127` of that amendment. `CON-028` retires only
the `consumer-design` pointer and states that every `CON-027` survivor remains
effective.

The frontmatter `consumer-design` names Selfsame SPEC-008 0.4.25-draft.
Selfsame `CON-984` and cbcl-bus `REQ-080` both assert that pointer, so the
three records agree. `REQ-033` also names cbcl-bus 0.16.18 as the coordinated
hub design.

`REQ-033` and `TEST-062` each fail any protocol-body change relative to 0.4.8,
which satisfies the review instruction that this candidate change only its
consumer pointer.

The document passes the strict controlled-language check with zero errors and
zero warnings. No defect is recorded against this repository.

### did-crdt (`1f409a4`)

The commit exists and resolves to `fix(rendezvous): bound storage, amortise
expiry, cap body before buffering (codex 7)`. It is a coordinated set member
rather than a candidate.

`src/service/rendezvous.rs:65` reads `pub const MAX_SLOT_BYTES: usize = 4096;`.
[[PROTO-002-selfsame-rendezvous-v1#REQ-305]] at
`specs/PROTO-002-selfsame-rendezvous-v1.md:310` requires acceptance of
non-empty records up to 69,632 bytes. The cap is deliberately non-conforming,
and that non-conformance is a recorded open gate rather than a defect.

## Attack surfaces examined and found sound

**Room start, traced from both unavailability values through every stage.**
`cbcl-chat-roomcfg:get/1` at `:152-157` maps `#(atomic ())` to
`#(error unknown-room)` and every other non-atomic result to
`#(error store-unavailable)`. `cbcl-chat-room:init/1` at `:200-210` matches the
one atom and stops. `init-with-cfg/2` at `:212-215` applies the
`#(public undefined false)` default through a bare variable pattern. Under the
new contract only `unknown-room` reaches that pattern. `NFR-927` stops the
child on both unavailability values, and permits only committed absence to
select the default. `cbcl-chat-rooms.lfe:99-104` inserts into `cbcl-rooms` and
monitors only on `#(ok ,pid)`, so a refused start leaves no registration. A
stopped `init/1` leaves no process, so no room state and no fan-out exist.

**The webhook route, traced through all five forcings.**
`cbcl-chat-rooms:public-cleartext-channel/1` at `:53-66` consumes `get/1` at
`:54`, calls `claim/3` at `:60` and discards its result, and re-reads `get/1`
at `:61`. Its `_` clause at `:66` maps every other result to
`#(error channel-not-public)`, which `cbcl-webhook-handler.lfe:84` answers as
409 today. `NFR-927` forbids handling busy through a wildcard default and
states `HTTP 409 remains only for a committed non-public-channel decision`, so
both unavailability values separate from that terminal. `ingest/3` at `:47-50`
calls `public-cleartext-channel/1` before `get-or-start`, so a refused channel
starts no room. `do-fan/6` reserves a blob slot at `:72` and releases it in the
`after` clause at `:85`. The reservation therefore releases on every branch,
including both 503 answers. The fourth and fifth forcings reach `cbcl-chat-room.lfe:201`
and `:221`, and `TEST-1154` drives each.

**The distinct meanings of the two results.** `authority-busy` names
retry-exhausted contention, produced by the shared wrapper's
`mnesia:transaction(Fun, [], 3)` mapping of `{aborted, nomore}`.
`store-unavailable` names every other room-configuration transaction failure.
`NFR-927` keeps them distinct `through every consumer` and gives each its own
HTTP status behind a common 503 class and a common `no-store` policy. Neither
becomes `unknown-room`, so neither reaches the channel-policy default.

**The declared-dialect read at start and at refresh.**
`cbcl-chat-roomcfg:get-dialects/1` at `:171-174` answers `#(ok Dialects)` or
`#(error unknown-room)`. Its two production consumers at
`cbcl-chat-room.lfe:221` and `:550` map every other result to `'()`.
`NFR-927` stops the child on busy at start, and at refresh requires the prior
dialect set to be retained with no frame emitted. `handle_cast` at `:546-558`
computes the set, writes `state2`, and fans a `roomcfg` frame. The conforming
behaviour is a branch that returns `(tuple 'noreply state)` before the fanout.
Both obligations are stated and testable.

**Every other production consumer of `cbcl-chat-roomcfg:get/1`.** A
repository-wide scan returns six production call sites. Three are named. The
three unnamed ones were traced. `room-encrypted?/1` at `:1286-1290` already
handles `store-unavailable` and maps every other result to `#(ok false)`.
`encrypted-room?/1` at `:2020-2023` maps every non-encrypted result to
`'false`. Line `:1994` uses it inside an `andalso` gate that holds a publish
in an encrypted channel. `ensure-channel/5` at `:1853-1861` treats every
non-success as an absent channel, which reaches `create-channel/5` and the
SPEC-069 create-intent guard at `:1884-1888`. All three sit behind wildcard
defaults, and `NFR-927` forbids exactly that.

`NFR-927` also states that neither value becomes a private-channel decision or
a channel configuration. It keeps `roomcfg:get/1` a declared public owner, so
the generated inventory enumerates every consumer of it.

The requirement therefore reaches all three sites without naming them.
`ADR-962` states that named witnesses support the scan and never limit it.

**The periodic Path-B poller, traced through both result forms.**
`cbcl-chat-path-b-poller.lfe:17` iterates `cbcl-chat-account-grants:accounts/0`
through `lists:foreach`, and the resolver call sits inside the per-account
lambda, so an empty scan performs none. `:32` consumes `revoke-observed/2`, and
the eviction `lists:foreach` at `:33` sits inside the `#(ok ,keys)` arm alone.
`interval/0` at `:13` defaults to 60,000 milliseconds. `handle_info` at `:11`
re-arms the timeout on every tick, so a deferred account receives a fresh
resolver pass.

`NFR-927` requires `OBS-905` phase `account-scan` and phase
`revocation-apply`. It forbids reporting the discarded union as applied. It
permits continuing other accounts, and keeps the supervised poller alive.

**Observation privacy.** `OBS-906` records the terminal boundary rather than
the room name. It excludes the room name, the channel capability, the account
DID, the credential, the profile digest, and the grant key.

`OBS-905` remains the poller signal and records only the event and the phase.
Neither observation claims a successful operation. A counter aggregating by
terminal boundary carries no per-room and no per-account value.

**Withdrawal, traced through all three disagreement cases.** `tombstone/2` at
`cbcl-chat-webfinger-store.lfe:206-230` reads the account row under a write
lock and derives `uri` from it. It answers `#(error wrong-generation)` at
`:213` when the named home DID differs.

It answers `#(error inconsistent-binding)` at `:220` when `alias-agrees?` at
`:232-237` finds an absent or disagreeing alias row. Both deletes at `:227`
and `:228` sit on the `'true` arm alone, so every refusal deletes zero rows. The terminal order is deterministic, because the
home-DID test is stated first and unconditionally. `TEST-1154` and `TEST-113`
each drive both account-side cases and require zero deletion from all three
WebFinger tables.

**Boolean-context smuggling.** The three named boolean contexts resolve.
`cbcl-chat-room.lfe:263-270` carries two `andalso` chains over
`account-roommember:re-admit?/3` and `roommember:re-admit?/3`.
`cbcl-chat-pairgrant.lfe:78` wraps `roommember:member-grant?/2` in `not` inside
a `cond`. `cbcl-chat-session-ws.lfe:1050` wraps the same owner in `if`. The
closed copula concerns tagged success entering a boolean operator, and no
fourth production site places a declared predicate owner in such a position.

**Owner fall-through predicates.** `cbcl-chat-roommember:member-grant?/2` at
`:222-225` falls through to the account-side owner. `enrolled?/2` at
`:233-236` answers `'false` for every non-success from
`cbcl-chat-members:pubkey/1`.

`since-seq/2` at `:113-117` falls through to the account-side owner on every
non-atomic result. All three are wildcard defaults over declared public
owners, so the requirement and the generated inventory reach them.

**Discarded results.** Three production sites discard a declared owner's
result. `cbcl-chat-rooms.lfe:60` and `cbcl-chat-session-ws.lfe:1898` are named.
`cbcl-chat-session-ws.lfe:1155` discards `rollback-add/5`, and
`cbcl-chat-app.lfe:96` discards the default-channel `claim/3`. The first is
reached by the scan because `rollback-add/5` is a declared owner. The second is
covered by the surviving `NFR-925` startup terminal, which states that busy
prevents the listener from opening.

**The cross-application HTTP sink.** The consuming edge lives at
`apps/cbcl_router/src/cbcl-webhook-handler.lfe:76`, and the owner lives in
`apps/cbcl_chat`. `cbcl-chat-rooms:ingest/3` has exactly one production
consumer, which is that line. `respond/4` at `:95-100` sets only
`content-type`, so both `no-store` headers are new behaviour rather than
observed behaviour.

**Non-resurrecting lookups.** `cbcl-chat-rooms:lookup/1` at `:28-34` never
starts a room and answers `#(error not-live)` on any failure. Its three
production consumers at `cbcl-chat-session-ws.lfe:866`,
`cbcl-chat-account-grants.lfe:150`, and the `cbcl-chat-room.lfe:188` comment
are unaffected by the new result shapes.

**Version freeze across every classifier class.** All twenty-seven named
witness sites resolve at the declared baseline, and each construct matches its
assigned class. A case-sensitive scan of every production Rust source returns
41 lines, and a case-insensitive scan returns 68. `successionVersion` occupies
six sites in `crates/selfsame-app-identity/src/succession.rs`, and that module
header binds the file to `CON-225`, `REQ-231`, and `REQ-202`.
`crates/selfsame-core/src/lib.rs:58` and `crates/selfsame-core/src/code.rs:78`
carry the two core namespaces. All three namespaces sit outside the credential
ceremony crates, so the exclusions are correct on their merits.

**Authenticated exact-intent display and pre-consent effect ordering.** The
wallet half is byte-identical to the declared baseline.
`crates/selfsame-pairing/src/live.rs:480-488` commits one decision from one
phase, and `CON-962`'s effect order places `preliminary display and decision`
last. `CON-984` names neither artefact, so neither changes.

**Fresh-scope bootstrap and per-pair trust on first use.** `CON-939` still
requires the stable scope and one pending ceremony to commit atomically before
any carrier is displayed. `REQ-981` still holds the live profile byte-for-byte
unchanged at `profileVersion = 1`, and the reviewed Rust confirms two
`pub const PROFILE_VERSION: i64 = 1;` definitions with no third value.

**Relay-operator mitigation.** Neither candidate claims a binding property for
the relay operator, and cbcl-pairing preserves the no-relay-field property
through its survivor chain. The residual remains contained rather than
repaired, which matches the F-E disposition.

**Cbcl-pairing protocol immutability.** The candidate commit adds one file of
124 lines and touches no protocol asset. `REQ-033` and `TEST-062` both fail any
protocol-body change relative to 0.4.8.

## Residual assumptions and open gates, separated from defects

These are not defects. Each is a recorded assumption or a gate that stays open
by design.

1. **The coordinated Tier-1 gate is open.** All three candidates carry
   `review-gate: not-approved`. `CON-984` states that a fresh PASS authorises
   only the Elephant SPL and test-first plan.
2. **GATE-04 stays 42 boxes open.** This amendment closes none. The cbcl-bus
   text states that directly, and the mechanical count agrees.
3. **The PROTO-002 rendezvous gate stays open.** The reviewed did-crdt
   baseline caps at 4,096 octets and is deliberately non-conforming to
   [[PROTO-002-selfsame-rendezvous-v1#REQ-305]].
4. **Both `no-store` headers on the webhook are new behaviour.** `respond/4`
   at `cbcl-webhook-handler.lfe:95-100` sets only `content-type` today. The
   pre-existing `overloaded` and `persist-failed` 503 answers keep no cache
   directive, and no clause asks them to.
5. **The 1013 close code and the browser generation are new behaviour.**
   Neither `1013` nor `try-again-later` appears in the cbcl-bus source or web
   assets today. The terminal is declared by the surviving
   [[spec-008-0.4.23-total-result-contracts-and-static-nesting#NFR-925]], so
   the phrase `the existing WebSocket 1013 terminal` refers to the effective
   specification rather than to deployed code.
6. **`OBS-905` and `OBS-013` are two identifiers for one signal, and `OBS-906`
   and `OBS-014` are two for another.** Event names and phase values agree
   across the repositories. Each repository allocates its own observation
   number, which is the existing convention in this chain.
7. **The `cbcl-provenance` wildcard is an accepted out-of-lane residual.**
   `NFR-925` records it and disclaims any repair of non-retryable behaviour.
8. **`cbcl-chat-successor:accept/3` has no production caller.** A scan of
   non-test callers returns none. The row is still production code.
9. **Several in-lane owners have no production consumer.**
   `store-verified-device-grant/6`, `confirmed-migrate-path-a-handle/3`,
   `revoke-device-grant/2`, `bind-account/2`, `provision/3`, `withdraw/2`, and
   `reinstate/2` are reachable only from tests or operator paths today.
10. **The cbcl-pairing parent carries no amendment pointer.**
    `specs/SPEC-001-reusable-blind-pairing.md` references no trajectory
    amendment, and no candidate requires it to.
11. **The cbcl-bus and cbcl-pairing trajectory fields use plain text for their
    cross-repository steps.** A wikilink into another vault does not resolve,
    so the plain form is the practical one.
12. **`get-dialects/1` still answers `unknown-room` for a non-contention
    transaction failure.** The amendment disposes of `store-unavailable` only
    for `cbcl-chat-roomcfg:get/1`. A dialect read that fails without exhaustion
    therefore yields an empty declared set rather than a refusal. That
    behaviour predates the amendment, affects capability negotiation rather
    than channel confidentiality, and no clause claims to repair it.
13. **The `SPEC-001-device-key-provisioning` document lives outside the
    reviewed set.** Its sibling checkout carries no pinned commit here, so
    [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]] records a
    reference defect rather than a content judgement about that document.

## Stopping-rule assessment

The verdict is REJECT. Each blocking finding was tested against the question
of whether it is a genuine safety or implementability blocker.

| Finding | Genuine blocker | Kind | Reasoning |
|---|---|---|---|
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]] | yes | implementability | A declared exact boundary returns zero matches in its target, so the effective text has two readings and the candidate's own authority scan cannot run. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]] | yes | implementability | The effective requirement has two readings. One leaves two 0.4.24 findings open, and the other regresses two 0.4.23 findings. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]] | yes | implementability | The completeness authority for the credential freeze cannot see nine of its own witnesses under the plain reading, including all three ingest substitutions. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]] | yes | safety-adjacent and implementability | A declared propagation reaches an unnamed consumer that exits the session process, and the generated inventory structurally cannot produce its row. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-5]] | yes | implementability | One requirement declares two tests replaced in full and current in the same passage, so the singularity test cannot be discharged. |

None of the five rests on style, redundancy, or discretionary precision. Each names
a specific sentence, a mechanical check that decides it, and a correction of
one or two sentences.

Two findings are LOW and do not block on their own.
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]] concerns
reference resolution for two exclusions.
[[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-2]] concerns a
self-stated count.

**Convergence trajectory.** The 0.4.24 round produced one HIGH, one MEDIUM, and
six LOW findings. This round produces zero HIGH, five MEDIUM, and two LOW. The
maximum severity falls, which is the opposite of the severity-inversion
anti-pattern in
[[PROTO-001-usdd-agent-protocol#Success Criteria]]. The count of MEDIUM
findings rises because four of them are residuals of two LOW repairs rather
than new subject matter.

**Adversary exhaustion is not reached.** The findings are neither fabricated
nor stylistic, and none duplicates an earlier pass. Four surfaces produced no
new defect at all: the Path-B poller, withdrawal, observation privacy, and the
architecture hard stops. Those four are converging.

**The corrections are small.** Four of the five blocking findings need one or
two sentences each. The fifth needs one inventory row and one scope sentence in
each repository. No structural redesign follows from this verdict.

## What this verdict authorizes and does not authorize

This verdict is REJECT. It authorizes nothing.

It does not authorize implementation, an Elephant SPL theory, a test-first
plan, code, production allocation, a release, or a deployment. It does not
close any GATE-04 box. It does not alter the DEC-012 production deferral. It
does not change the PROTO-002 Tier-1 gate. It does not change the approved
0.2.2 baseline of [[SPEC-008-production-pairing-claimant]].

Under `CON-984`, a later PASS on a corrected candidate authorizes only the
Elephant SPL and the test-first plan. Even that PASS authorizes no code until
the primary agent independently verifies and merges this evidence.

This report is one reviewer's evidence. It is not a merge decision and not a
gate closure.

## Corrections and owners, collected

| Finding | Owner repositories | Minimum sound correction |
|---|---|---|
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-1]] | selfsame | Change the first `NFR-925` range's closing phrase to `both live-row deletes under the held locks.` The alternative is to restore the `Neither case deletes any row.` boundary and add a second exact range for the reciprocal paragraph. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-2]] | selfsame | State in `REQ-1004` that it applies REQ-1003's two `REQ-1002` range replacements, with the second range extended through the `Credential/v2` paragraph. Add an exact sentence replacement retiring `The scan includes …` in favour of the closed set. The alternative is to replace `REQ-1002` in full. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-3]] | selfsame | Add `profile_version` to the closed identifier set, making it five, and state the matching discipline as exact case-sensitive substring matching. The alternative is to declare case-insensitive matching and record the resulting 68-line production baseline. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-4]] | selfsame, cbcl-bus | Add `cbcl-chat-session-ws.lfe:1788` with the WebSocket 1013 terminal and `:1076` with its `revoke-failed` terminal. State that the production consumer scan covers every consumer of a value carrying `authority-busy`, not only consumers of a declared public owner. Add a WebSocket hello forcing against a refused room start to `TEST-1154` and `TEST-113`, requiring the 1013 close and zero membership grant. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-M-5]] | cbcl-bus, selfsame | State the hub current test set as `TEST-093`, `TEST-094`, `TEST-098`, `TEST-109`, `TEST-110`, `TEST-113`, and `TEST-114`, in both repositories. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-1]] | selfsame | Reference the document by its descriptive stem as `[[SPEC-001-device-key-provisioning#REQ-016]]` and `[[SPEC-001-device-key-provisioning#CON-001]]`. Record it as an out-of-set reference, or pin its commit in the coordinated review set. |
| [[spec-008-0.4.25-claude-adversarial-review-2026-08-23#N10-L-2]] | selfsame | State one corrected range boundary, or supply the second one in `REQ-1004`. |

## Report self-audit

| Property | Value | Source |
|---|---|---|
| Findings raised | 7 | this report |
| CRITICAL findings | 0 | severity table |
| HIGH findings | 0 | severity table |
| MEDIUM findings | 5 | severity table |
| LOW findings | 2 | severity table |
| 0.4.24 findings dispositioned | 8 | disposition table |
| 0.4.23 findings regression-tested | 7 | regression table |
| 0.4.22 findings regression-tested | 12 | regression table |
| Architecture hard stops dispositioned | 6 | hard-stop table |
| Named v1 witness sites verified | 27 | line-by-line read at the declared baseline |
| Named v1 witnesses invisible to the closed set | 9 | exact-match test per line |
| Production Rust matches, case-sensitive | 41 | scan of every `.rs` under `crates/*/src` |
| Production Rust matches, case-insensitive | 68 | same scan with `-i` |
| Declared boundary phrases checked | 11 | exact string counts in both targets |
| Declared boundary phrases returning zero matches | 1 | same counts |
| cbcl-bus GATE-04 boxes counted | 42 | `grep -c '^- \[ \]'` over the effective gate section |
| cbcl-bus GATE-04 boxes closed | 0 | `grep -cE '^- \[[xX]\]'` over the same section |
| `get-or-start/1` production consumers | 3 | repository-wide scan of production LFE under `apps/` |
| `roomcfg:get/1` production consumers | 6 | same scan |

The severity table, the corrections table, and this audit agree on zero
CRITICAL, zero HIGH, five MEDIUM, and two LOW findings.

The counts this report states about itself are derived rather than recalled.
Both tool results are pasted below.

```text
$ tools/usdd-count.sh <this report>
artefacts   REQ=17 NFR=7 CON=8 TEST=15 ADR=4 OBS=5 SPEC=7 PROTO=2
gate rows   0  (pass=0 fail=0 unverified=0)
registry    0  (auto=0 auto*=0 review=0 advisory=0 duplicate_ids=0 missing_ids=0)
wikilinks   115  (anchored=114)
```

```text
$ tools/usdd-lint.sh --type descriptive --strict <this report>
          longest_sentence       25 (limit 25, mode descriptive)
          scanned                462 sentences, 5293 words
          TOTAL                  0 errors, 0 warnings
```

```text
$ grep -c '^### N10-' <this report>              # finding headings
7
$ grep -c '^| \*\*N9-' <0.4.24 disposition table>
8
$ grep -c '^| \*\*N8-' <0.4.23 regression table>
7
$ grep -c '^| \*\*N7-' <0.4.22 regression table>
12
$ sed -n '/^## Corrections and owners, collected$/,/^## Report self-audit$/p' <this report> | grep -c '^| \[\['
7
$ sed -n '/^## Stopping-rule assessment$/,/^\*\*Convergence/p' <this report> | grep -c '^| \[\['
5
```

This report carries no gate rows and no registry rows, so both totals are
zero. It is a review record rather than a specification.

## Changelog

- **2026-08-23 — coordinated Tier-1 adversarial review of SPEC-008 0.4.25,
  cbcl-pairing 0.4.11, and cbcl-bus 0.16.18 — REJECT.** The 0.4.24 HIGH
  finding closes on code-backed evidence, and no 0.4.22 finding regresses.
  Five new MEDIUM defects and two new LOW defects stand. Four of the five
  MEDIUM defects are residuals of two LOW repairs from the previous round.
