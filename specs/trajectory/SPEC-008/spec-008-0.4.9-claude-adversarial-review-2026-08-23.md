---
id: spec-008-0.4.9-claude-adversarial-review-2026-08-23
title: Tier-1 adversarial review — SPEC-008 0.4.9 coordinated standalone pairing
status: final
tier: 1
kind: review
verdict: REJECT
reviews: "[[spec-008-0.4.9-standalone-tofu-first-contact]]; cbcl-pairing SPEC-001 0.4.4-draft; cbcl-bus SPEC-053 0.16.2-draft and amended parent SPEC-053-key-root-identity"
severity-counts: "1 critical, 1 high, 3 medium, 4 low"
driver: Claude Opus 5 (claude-opus-5), fresh-context cross-model Circus reviewer, defect-finding mandate
date: 2026-08-23
---

# Tier-1 adversarial review — SPEC-008 0.4.9 coordinated standalone pairing

## Model and driver context

- **Driver model:** Claude Opus 5 (`claude-opus-5`), Circus fresh-context
  Tier-1 reviewer, defect-finding mandate only. No drafting, no implementation,
  no gate-closing authority.
- **Working tree:** `/Users/anuna-01/Code/selfsame-spec008-v049-review-1`
  (worktree of `selfsame`, branch `circus/spec008-v049-review/1`).
- **Sibling repositories read at their own checkouts:** `/Users/anuna-01/Code/cbcl-pairing`,
  `/Users/anuna-01/Code/cbcl-bus`, `/Users/anuna-01/Code/did-crdt`.
- **Method:** every claim below was checked against source at the named
  baselines. Prose, comments and disposition tables were treated as
  unverified assertions. Where a clause names a module, function, record,
  index, constant or route, that object was located (or shown absent) in code.

### Reviewed commits

| Repository | Candidate commit | Declared implementation baseline | Verified relationship |
|---|---|---|---|
| selfsame | `1fe9347032b16c03695a7ef70585e175af92e82d` | `c6af6df75d033d2e44ae367457c1a754ba2c4c3a` | ancestor; `git diff c6af6df..HEAD` touches **0** non-`specs/` files |
| cbcl-pairing | `c9c7d5cfe27eed6252072b3b2c99b436978860eb` | `8e55160a3234127bd60ce723fd93ba6b2b67a06b` | ancestor; diff is 6 files, all under `specs/trajectory/SPEC-001/` |
| cbcl-bus | `740530c947830b009d19c47a682eb5f352af3091` | `fdbe4142261923f48cced037a7f609e48a9fdd5a` | ancestor; diff is 3 files, all under `specs/` |
| did-crdt | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` | same | candidate *is* the baseline |

Each candidate commit is therefore spec-only relative to its declared code
baseline, so the checked-out worktrees hold exactly the code the amendments
claim to be written against. All code citations below are at those baselines.

### Documents read

Selfsame SPEC-008 0.4.0 through 0.4.9 and every intervening adversarial review;
the pairing completion runbook; `req-909-adversarial-review-2026-08-22.md`;
`HANDOFF-pairing-integration.md`. cbcl-pairing SPEC-001 0.3.0, 0.4.0, 0.4.1,
0.4.2, 0.4.3, 0.4.4. cbcl-bus `SPEC-053-key-root-identity` (amended parent),
SPEC-053 0.16.1 and 0.16.2, `SPEC-076-pairing-account-grant`. Referenced
parent contracts CON-201/204/206/211/214/219/220/221/226, REQ-215, REQ-103,
PROTO-004 CON-502/503, SPEC-075 REQ-007, GATE-00/01/03/04.

---

# Verdict: REJECT

**1 critical, 1 high, 3 medium, 4 low.**

The standalone first-contact architecture remains sound and is not the reason
for this verdict. The consent ordering in `CON-962` is correct, the `H-1`
dispatch relocation is exactly right against the real `cbcl-chat-session-ws`,
the `H-2` digest replacement lands on a definition that already exists and is
already implemented in Rust, and the `M-3` display carrier is genuinely
implementable against the real `cbcl-pairing` types with no lifetime or
object-safety obstacle. Nine of the fifteen 0.4.8 findings are closed on the
evidence, not on the disposition table.

The verdict turns on two defects that the corrections themselves create:

1. **`C-1` is not closed; it is relocated and made worse.** Removing the
   `cbcl-pairaccount-grant` write is necessary and correct. But `REQ-973` then
   makes *the absence of an unratified pairaccount row for the granted device
   key* a per-frame liveness precondition of an irreversible, unrecoverable
   migration — and checks it only at two pre-commit instants. At the reviewed
   cbcl-bus baseline any party who can TOFU-enrol a handle can create that row
   for an arbitrary 32-octet key at any time, permanently, with no deletion
   path in the codebase. Post-commit, that produces the exact total lockout
   `C-1` was raised to prevent, now reachable by a third party.

2. **`profileVersion = 2` is not an available discriminator.** The coordinated
   set identifies the credential/v2 grammar by `profileVersion = 2`, but
   `profileVersion` is a profile-*document* member: the live ratified CON-220
   document serves `profileVersion: 1`, `ApplicationProfile::recognise` rejects
   any other value, and `enrollment::verify` cross-checks the statement against
   both the fetched profile and the offer. As specified, credential/v2 evidence
   cannot verify; and the obvious repair — re-ratifying the document to
   version 2 — breaks the deployed CON-219 flow that `ADR-944`/`DEC-016` exist
   to preserve.

Both have small, sound corrections. Neither is an architectural objection.

---

# Findings

## C-1 (CRITICAL) — after the irreversible commit, any TOFU-enrolled handle can permanently brick the granted device key by claiming it as its own paired device; `REQ-973`'s two pre-commit checks are a TOCTOU against a live, ungated, undeletable writer, and the result is the identical both-keys-inert lockout that 0.4.8 `C-1` named

**Owner: cbcl-bus** (`REQ-041`, `CON-014`, `SPEC-076` `REQ-002`/`ADR-001`),
with a consequential correction in Selfsame `REQ-973`/`NFR-911`.

### What 0.4.9 asserts

`REQ-973`:

> The proposed device key must have no unratified pairaccount row before
> carrier allocation and at finalization. A pre-existing collision refuses
> before the wallet connects; a concurrent collision refuses the transaction
> with the complete legacy state intact.

`NFR-911` / `CON-965` clause 11:

> pairaccount remains a read-only negative per-frame input for credential/v2.

`CON-965` clause 11 is accurate about *credential/v2's* behaviour and says
nothing about anyone else's. That is the gap.

### The per-frame consequence, in code

`apps/cbcl_chat/src/core/cbcl-core-enrol.lfe:125-131` — the check runs before
the binding case, on every path, fail-closed:

```lfe
(if (not (=:= 'false (maps:get 'pairing-unratified frame-key 'unknown)))
  #(reject pairing-unratified)
  (case (maps:get 'binding handle 'undefined)
    ('unbound            (tofu handle frame-key))
    (`#(key ,bound)      ...)
    (`#(account ,account) (account-bound ...))
```

Both fact-resolvers supply it —
`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:347` (`selector-facts`) and
`:385` (`account-standing-facts`):

```lfe
'pairing-unratified (cbcl-chat-pairaccount:unratified-device? pk)
```

`apps/cbcl_chat/src/shell/cbcl-chat-pairaccount.lfe:97-104` — the read is
**global over the device-key index and ignores which handle claimed it**:

```lfe
(defun unratified-device? (pk)
  (case (mnesia:transaction
          (lambda ()
            (lists:any
              (lambda (rec) (=:= 'unratified (cbcl-pairaccount-grant-state rec)))
              (mnesia:index_read 'cbcl-pairaccount-grant pk 'devkey))))
```

### The writer, and why it is reachable by anyone

`apps/cbcl_chat/src/shell/cbcl-chat-session-ws.lfe:942` — `route-member` has the
verb in its production table:

```lfe
('pairaccountgrant (do-pairaccountgrant room handle msg state))
```

`do-pairaccountgrant` (`:1667-1678`):

```lfe
(defun do-pairaccountgrant (room handle msg state)
  (case (cbcl-core-pairaccount:decode-device-key (cbcl-core-msg:kw #"device" msg))
    (`#(error ,_) (reply-error state (or-room room) #"bad-pairaccount-device"))
    (`#(ok ,devkey)
     (case (cbcl-chat-pairaccount:claim handle devkey)
```

`devkey` is taken **verbatim from the frame's `:device` keyword**.
`cbcl-core-pairaccount:decode-device-key/1` checks only "strict base64 of
exactly 32 octets". Nothing binds it to the sender, the sender's account, or
possession of the corresponding private key. `claim-tx` then writes
`state = 'unratified`.

Reachability for an *unenrolled* attacker, traced clause by clause:

1. `handle-frame` → `dispatch` → `binding = #(error not-enrolled)`
   (`cbcl-chat-account-grants:account-binding/1`).
2. `enrol-facts` → `handle-facts handle 'unbound` + `selector-facts`,
   permission `'undefined` (`cbcl-chat-session-ws.lfe:316-318`).
3. `cbcl-core-enrol:resolve` → `spent = false`, `pairing-unratified = false`
   (the *attacker's own* key), binding `'unbound` → `tofu/2` →
   `accept (tuple 'key key)`. `tofu` consults no permission.
4. `enrol-commit` enrols the handle; `route` → `hub-originated?('pairaccountgrant)`
   is **false** (only `pairaccountgranted` is listed,
   `cbcl-core-dialect.lfe:79-84`) → `route-member` → `do-pairaccountgrant`.

`do-pairaccountgrant` performs **no room-membership check** (contrast
`do-invite`, which calls `current-member-pid`), consults **no gate** (`grep`
for `gate` in `cbcl-chat-pairaccount.lfe` and `cbcl-core-pairaccount.lfe`
returns nothing), and the module exports **no delete and no ratify**:

```lfe
(defmodule cbcl-chat-pairaccount
  (export (ensure-table 0) (claim 2) (grants 1) (unratified-device? 1)))
```

The cap is per *claiming handle*, not per claimed key:
`unratified-count-tx` folds `index_read ... 'handle`, and
`cbcl-core-pairaccount:max-unratified` is `2`. TOFU makes handles free, so the
supply of claims is unbounded.

### Failure scenario

1. A person completes the 0.4.9 ceremony. `finalize-credential-v2-migration/1`
   commits: legacy rooms deleted, `cbcl-account-binding` written, verified
   `cbcl-device-grant` stored for the new device key `K`. Per `REQ-041` "the
   legacy key cannot pass after binding". Per `REQ-973` the commit is
   "irreversible protocol acceptance"; "no timeout restores legacy rows,
   deletes the binding, or revokes the grant."
2. `K` is a public Ed25519 key. It is in the device-grant VC, in the DID
   document the wallet published to did-crdt, and in the grant carriage the hub
   relays. It is not secret in any threat model.
3. Any party opens `/chat/v1`, TOFU-enrols a throwaway handle, and sends
   `(pairaccountgrant @anyroom :device "<std-base64 of K>" :from @throwaway)`.
   One row: `key = {throwaway, K}`, `devkey = K`, `state = 'unratified`.
4. Every subsequent frame signed by `K`: `account-standing-facts` sets
   `pairing-unratified = true`; `cbcl-core-enrol:resolve` returns
   `#(reject pairing-unratified)` before reaching `account-bound`; the shell
   collapses it to `bad-signature`.
5. The legacy key is refused (binding exists). The new key is refused
   (unratified). `REQ-973`'s recovery — "Only that new-key session may recover
   `selfsamepairstatusresult`" — needs a session under `K`, which cannot be
   established. There is no code path that deletes the row. The account is
   permanently inert.

Cost to the attacker: one WebSocket connection and one frame per victim.

### Why this is a defect of *this* increment, not only of SPEC-076

`SPEC-076` `ADR-001` states the accepted residual risk as:

> a malicious account holder can register a device key without running a
> ceremony. That is acceptable at this tier because the grant is inert until
> ratified ([[#REQ-004]]) and **binds only the attacker's own account**

That mitigation is false about the implementation. The *row* binds to the
attacker's account (composite key, per-handle cap). The *REQ-004 consequence*
binds to the **device key**, globally, through `unratified-device?/1`'s
`index_read` on `devkey`. `ADR-001`'s acceptance therefore does not cover the
effect the 0.4.9 design depends on.

0.4.9 is what converts this from a bounded weakness into an unrecoverable one.
It (a) makes a device key *live* rather than dormant, (b) makes the migration
that produced it *irreversible by contract*, (c) kills the legacy key, and
(d) verifies the required negative fact only at two pre-commit instants while
leaving the writer that can falsify it live, ungated and permanent. `REQ-041`'s
"An existing unratified collision is `DeviceKeyPairingUnratified` before
carrier allocation, or `AuthorityChanged` if it appears concurrently" is
complete for the ceremony window and silent for all time after it.

`ADR-943`'s reasoning — "Combining them would convert accepted authority into
an inert key and lock the person out" — is exactly right, and is exactly what
still happens; the trigger simply moved from the protocol to a third party.

### Minimum sound correction (cbcl-bus owns all three parts)

1. **Bind the claim to possession.** `pairaccountgrant` must carry proof that
   the claimant holds the private half of `:device` — a signature by that key
   over a hub-issued nonce, in the shape `cbcl-chat-grant-nonce` already
   provides for CON-007. `cbcl-chat-successor:accept/3` is the in-repo
   precedent: it refuses a pointer unless `held-the-handle?` proves the
   predecessor key is the handle's enrolled key. The pairaccount store has no
   equivalent and its own header comment explains why it believed one was
   unnecessary ("the envelope verification one step earlier … already covered
   the canonical claim bytes") — which authenticates the *sender*, never the
   *claimed key*.
2. **Make a verified grant dominate an unratified claim.** `claim-tx` must
   refuse a `devkey` that already has a `cbcl-device-grant` row or a binding,
   and `unratified-device?/1` must answer `false` for a key with a verified
   device grant. Either alone leaves a race; both together make the granted
   key's liveness monotone.
3. **State the property honestly in the design.** `REQ-973`/`NFR-911` must stop
   describing "no unratified pairaccount row" as a stable post-commit fact
   established by two pre-commit checks, and instead name the invariant
   ("a device key with a verified grant is never rendered inert by a
   third-party pairing claim") together with the cbcl-bus change that
   establishes it. `GATE-04`'s second box — "`cbcl-chat-pairaccount` has no
   credential/v2 writer/call edge and the post-commit granted key passes the
   ordinary per-frame reducer" — must be extended to "…and cannot be made to
   fail it by any other party".

Until then this is also a **live production exposure at the reviewed cbcl-bus
baseline, independent of credential/v2**: any TOFU handle can today
permanently disable any existing key-bound member's enrolled key, and with it
that member's handle. That is out of this amendment's scope but should not
wait for it.

---

## H-1 (HIGH) — the credential/v2 grammar is discriminated by `profileVersion = 2`, but `profileVersion` is a member of the ratified CON-220 profile document; the live document serves `1`, `ApplicationProfile::recognise` rejects anything else, and `enrollment::verify` cross-checks the statement against both the fetched profile and the offer — so credential/v2 evidence cannot verify as specified, and the obvious repair breaks the deployed CON-219 flow `ADR-944`/`DEC-016` exist to protect

**Owner: Selfsame** (SPEC-004 `CON-214`/`CON-220`; SPEC-008 `REQ-976`),
coordinated with **cbcl-bus** (`REQ-043`, the served document and its pinned
digest).

### What the coordinated set says

Selfsame `REQ-976`:

> a frozen v1 recognition/signing entry that accepts exactly current CON-214
> `evidenceVersion = 1`, `profileVersion = 1`, and the current closed member
> set. **Adding the distinct profileVersion-2 credential/v2 grammar does not
> widen that entry.**

cbcl-bus `REQ-043`: "accepts exactly the current canonical CON-214 member set
with `evidenceVersion = 1` and `profileVersion = 1`, and rejects every other
version". `TEST-050`: "reject profileVersion 2". `TEST-1094`: "Direct HTTP
v2/mixed statements receive opaque 503".

So `profileVersion = 2` is the chosen discriminator between the two grammars.

### What `profileVersion` actually is

It is a member of the **application profile document**, not a free statement
field.

`crates/selfsame-app-identity/src/profile.rs:60,81,384-387`:

```rust
pub const PROFILE_VERSION: i64 = 1;
...
    "profileVersion",
...
        let version = integer(value, "profileVersion")?;
        if version != PROFILE_VERSION {
            ... "profileVersion", ...
```

`ApplicationProfile::recognise` **refuses any profile document whose
`profileVersion` is not 1.**

The live, ratified, production-served document is version 1.
`apps/cbcl_chat/src/shell/cbcl-chat-selfsame-application-gate.lfe` embeds a
base64url constant that decodes to, among other members,
`"cbclPairingRelays":[…],"profileVersion":1,…`. Its exact bytes are pinned by a
Tier-1 closed-gate test,
`apps/cbcl_chat/test/cbcl-chat-gate-closed-tests.lfe:27-33`:

```lfe
(deftest serving-gate-answers-the-ratified-octets
  (let ((`#(ok ,octets) (cbcl-chat-selfsame-application-gate:document)))
    (is-equal 1235 (byte_size octets))
    (is-equal (binary:decode_hex #"6ac06f627bcf30755553cce415a168cff1498350da6464874397f12b10bc4999")
              (crypto:hash 'sha256 octets))))
```

and the module header says "any change here is a new ratification, never an
edit."

### Why a `profileVersion: 2` statement cannot verify

`crates/selfsame-app-identity/src/enrollment.rs:551-556` and `588-595`:

```rust
    if statement.application_id != observed.profile.application_id.as_str()
        || statement.profile_version != crate::PROFILE_VERSION
        || statement.profile_digest != codec::b64url(observed.profile.digest())
    { return Err(EnrollmentError::ProfileMismatch); }
...
        || statement.profile_version != observed.offer.profile_version
```

and `recognise_payload` (`:394`) refuses the statement outright:

```rust
    if payload.get("profileVersion").and_then(Json::as_i64) != Some(crate::PROFILE_VERSION) {
        return Err(EnrollmentError::EnrollmentMalformed);
    }
```

A credential/v2 statement carrying `profileVersion: 2` fails at three
independent points against a version-1 profile. Nothing in SPEC-008 0.4.9,
SPEC-001 0.4.4 or SPEC-053 0.16.2 owns a change to `PROFILE_VERSION`, to
`ApplicationProfile::recognise`, or to the served document.

### Why the obvious repair is worse

Re-ratifying `chat.anuna.io`'s profile to `profileVersion: 2` makes every
**v1** statement fail the same three checks (`statement.profile_version` = 1
vs a version-2 observed profile and offer). The serving gate returns exactly
one document (`document/0` has no version parameter and `applicationId` is its
own locator under CON-220), so one application cannot serve both. That breaks
the deployed CON-219 enrolment ceremony — the flow `ADR-944` ("Removing
enrolment signing would break the already deployed sanctioned enrol-once path")
and `DEC-016` are written to preserve. The frozen v1 *signer* would survive; v1
*verification* would not.

### Minimum sound correction

**Preferred — Selfsame owns it, and it is a one-word change.** Discriminate
credential/v2 by **`evidenceVersion`**, not `profileVersion`. `evidenceVersion`
is statement-local: `recognise_payload` compares it to a literal `1`
(`enrollment.rs:373`), it is **not** in `OFFER_CORE_MEMBERS`
(`ceremony.rs:79-93`), and it is never compared to the profile or the offer.
Setting `evidenceVersion = 2` for credential/v2 leaves the ratified
version-1 profile document, its pinned digest, `PROFILE_VERSION`,
`ApplicationProfile::recognise` and the whole deployed CON-219 path untouched,
while giving the frozen v1 entry a total and testable discriminator. Every
occurrence of "profileVersion 2" in `REQ-976`, `CON-964`, `TEST-1094`,
cbcl-bus `REQ-043` and `TEST-050` must move with it.

**Alternative — if `profileVersion` really must move**, then the coordinated
set must additionally own: a CON-220 re-ratification to a version-2 document; a
stated coexistence mechanism for v1 and v2 at one `applicationId`; migration of
the 1235-byte/`6ac06f62…` pin in `cbcl-chat-gate-closed-tests`; a widened
`ApplicationProfile::recognise`; and an explicit statement of the deployed
CON-219 break and its migration. That is a separate Tier-1 increment against
SPEC-004, not a clause in this one.

---

## M-1 (MEDIUM) — `NFR-911`/`NFR-009`'s 6,000-octet non-core budget is contradicted by the deployed `ENROLLMENT_JWS.max_octets = 8_192` that credential/v2's compact evidence inherits; the mandated generated arithmetic cannot pass at declared maxima, and `NFR-009` makes that a manifest failure

**Owner: Selfsame `NFR-911` and cbcl-bus `NFR-009` jointly.**

`NFR-911`:

> The complete RFC-8785 OfferCoreV2 is at most 56,000 octets; … every other
> logical member has a combined generated maximum of 6,000 octets; therefore
> the large logical body is at most 62,000 octets. The schema generator encodes
> all fields at maxima and records the actual total and residual. A field-cap
> change without a new passing arithmetic vector is a manifest failure.

cbcl-bus `NFR-009` names the constituents of that 6,000: "Its non-core
canonical members, **compact evidence**, provider hint, proof, keys, and
enclosing map have a combined maximum budget of 6,000 octets."

`enrollmentEvidence` is a non-core member —
`crates/selfsame-app-identity/src/ceremony.rs:96`:

```rust
pub const OFFER_EXCLUDED_MEMBERS: &[&str] = &["enrollmentEvidence", "providerHint"];
```

and its size ceiling is `crates/selfsame-app-identity/src/enrollment.rs:54-64`:

```rust
pub const ENROLLMENT_JWS: JwsPolicy = JwsPolicy {
    typ: "selfsame-enrollment+jws",
    ...
    max_octets: 8_192,
```

**8,192 > 6,000 for the evidence member alone**, before `providerHint`, proof,
keys and the enclosing map. At declared maxima the generator asserts
`56,000 + (≥8,192 + …) ≤ 62,000`, which is false. The 62,000 ceiling is not
adjustable upward: it is a real cbcl-pairing constant,
`src/profile.rs:334` and `:920`:

```rust
        bounded_bytes(&self.credential, 1, 62_000)...
```

L-5 of the 0.4.8 review asked for arithmetic instead of assertion. 0.4.9
supplies arithmetic, but the arithmetic is unsatisfiable against the deployed
constant. (For context, the whole offer payload bound,
`ceremony.rs:64 MAX_PAYLOAD_OCTETS = 69_607`, is itself above 62,000; the caps
across the two crates are not currently reconciled.)

**Minimum sound correction.** Either declare an explicit credential/v2 evidence
cap strictly below the non-core budget and own that constant in the
coordinated set (a v2 statement is ~600–900 octets in practice, so a 3,000- or
4,000-octet v2 policy is ample and honest), or restate the budget split so the
sum of the *actual declared* maxima is ≤ 62,000 and show that sum. Either way
`NFR-911` and `NFR-009` must cite the constant they are budgeting against by
name, so the generator assertion is checkable before implementation rather
than at manifest time.

---

## M-2 (MEDIUM) — adopted `CON-960` still says the credential/v2 `IntentVerifier` **returns** a closed typed `AccountTransition`, which directly contradicts cbcl-pairing 0.4.4 `CON-021`/`ADR-019`'s verdict-only verifier; `CON-965`'s replacement ledger — whose entire job is to name such replacements — does not name it

**Owner: Selfsame `CON-965`.**

0.4.9's `adopts:` is "All 0.4.4 through 0.4.8 clauses except the exact
replacements and corrections named by this amendment." Adopted 0.4.8 `CON-960`
says:

> The credential/v2 `IntentVerifier` **returns a closed typed
> `AccountTransition`** only after the live profile, carrier, descriptor,
> transcript, offer, hub evidence, session account, and every transition field
> agree.

cbcl-pairing 0.4.4 `REQ-026`/`CON-021` says the opposite, and `ADR-019`
explains why the opposite is the security property:

> The verifier can return only `Ok(())` or a closed profile error; it cannot
> return, replace, append, or mutate display data.
>
> …allowing it to return display fields recreates the untrusted display channel
> the design is closing.

`CON-964` states the verdict-only rule for the consumer, but `CON-965`'s
twelve-item ledger names only "0.4.3's prose-only transition is replaced by
cbcl-pairing 0.4.4" (item 4). `CON-960` is a 0.4.8 clause, not a 0.4.3 one, and
its returns-a-transition sentence is left standing in the effective set.

This is not cosmetic. It is two effective clauses giving opposite answers about
the single authority boundary `M-3` was raised to close. An implementer reading
the adopted text literally would build the exact channel `ADR-019` forbids.
`TEST-1095` would eventually catch it, but the design should not require a test
to arbitrate between two of its own contracts. It is also precisely the class
of defect 0.4.8's `L-2` ("root correction not ledgered") established the ledger
to prevent.

**Minimum sound correction.** Add a `CON-965` item: "0.4.8 `CON-960`'s
`IntentVerifier`-returns-`AccountTransition` sentence is replaced by
cbcl-pairing 0.4.4 `CON-021`/`ADR-019` and `CON-964`: the verifier receives the
exact parsed input and returns only `Ok(())` or a closed profile error; the
shared profile alone constructs `CredentialV2Display`." One sentence, one
owner.

---

## M-3 (MEDIUM) — "the same exact stable-account write lock" is required of *every* device-grant writer "including the existing Path-B grant entry points", but no stable-account row exists for accounts created outside credential/v2, and one of the two real device-grant writers has no cap read at all, so the phantom-insert mitigation is under-specified for the writers it names

**Owner: cbcl-bus** (`NFR-009`, `CON-014`), consequentially Selfsame `NFR-911`.

`NFR-009`:

> Every device-grant writer first takes the same write lock on the exact
> stable-account row, then uses the account index for its cap read; the
> per-account lock prevents a phantom insert race between distinct composite
> device keys.

`CON-014`: "All device-grant writers use that same stable-account write lock
before the indexed cap check, **including the existing Path-B grant entry
points**".

Two problems at the baseline.

**(a) There are two device-grant writers, and one has no cap read.**
`apps/cbcl_chat/src/shell/cbcl-chat-account-grants.lfe`:

- `bind-and-store-verified-device-grant/8` → `store-device-tx/7`, which does
  enforce the cap (`:189-203` via `account-device-count/1` at `:204-207`, currently a whole-table
  `mnesia:foldl` — the object 0.4.8 `L-1` correctly called "not an index" and
  which `NFR-911` correctly replaces with a real `account` index).
- `store-verified-device-grant/6` (`:216`), exported, which writes a
  `cbcl-device-grant` row with **no `account-device-count` call, no
  `max-devices` argument, and no cap check of any kind**.

`store-verified-device-grant/6` has no production caller today (`grep` finds
only its definition and export), so this is latent rather than live. But
`NFR-009`'s rule is phrased as "takes the lock *before its indexed device-cap
read*", which is vacuous for a writer that has no cap read. Adding the lock to
it without adding a cap check leaves a straightforward cap bypass the moment it
acquires a caller.

**(b) "the exact stable-account row" has no referent for pre-existing
accounts.** No `stable-account` table exists at the baseline; enumerating every
`mnesia:create_table` in `apps/` yields `cbcl-account-binding`,
`cbcl-account-roommember`, `cbcl-device-grant`, `cbcl-epoch-claim`,
`cbcl-grant-nonce`, `cbcl-groupinfo`, `cbcl-invite`, `cbcl-keypkg`,
`cbcl-member`, `cbcl-pairaccount-grant`, `cbcl-pairgrant`, `cbcl-pairing`,
`cbcl-provenance`, `cbcl-room`, `cbcl-roommember`, `cbcl-sealedgrant`,
`cbcl-successor-pointer`, plus the three webfinger tables.
`cbcl-account-binding` is keyed by **handle**. The stable-account-keyed
authority table is a *new* table this increment creates (adopted 0.4.4
`CON-941`: "New account-authority, scope, pending-ceremony, device-registration,
and compensation tables are keyed by stable account ID"), which is legitimate —
but the existing Path-B writer serves accounts that will never have a row in
it. Mnesia will happily lock an absent key, so mutual exclusion is achievable;
what is missing is the statement that both writers derive **the same lock key
by the same rule** from an account DID that the existing Path-B writer holds,
and that the lock is taken on a fixed table regardless of row presence.

**Minimum sound correction.** In `NFR-009`/`CON-014`: (i) name the exact table
and the exact key-derivation rule for the stable-account lock, and state that
the lock is taken on the key whether or not the row exists; (ii) require the
cap check itself — not merely the lock — of every device-grant writer, and
dispose of `store-verified-device-grant/6` explicitly (give it the cap and the
lock, or remove the export, as 0.4.8 `CON-959` already did for
`bind-account/2`); (iii) mirror the disposition in Selfsame `NFR-911`.
`TEST-048`/`TEST-1097`'s "two distinct device keys at the last cap slot through
different grant writers" is the right test and currently has only one writer
that can fail it.

---

## L-1 (LOW) — `account-linked` is a pre-existing SPEC-053 `REQ-033` hub notice, not a credential/v2 kind; folding it into `REQ-042`'s "closed hub-minted set" misattributes it and makes `TEST-1099`/`TEST-054`'s mutation criterion assert `REQ-033`'s protection rather than credential/v2's

**Owner: cbcl-bus `REQ-042`.**

`REQ-042`'s closed hub-minted set lists six credential/v2 results plus
`account-linked`. But `account-linked` is already a live production kind: it is
in `cbcl-core-msg:custom-kind` (`:133`, "hub->client REQ-033 notice"), already
in `cbcl-core-dialect:hub-originated?/1` (`:82`), and already emitted by the
existing REQ-033 path — `cbcl-chat-account-grants.lfe:138`,
`fan-account-linked-notices/3`, "each affected *live* room receives one
post-commit `account-linked` notice".

No security consequence: in production, dispatch-gating it yields
`bad-signature` instead of `hub-only`, and both are zero-effect refusals. But
`TEST-1099`/`TEST-054` require "Remove each literal under mutation testing and
require the suite to fail" — removing `account-linked` from `hub-originated?`
breaks REQ-033's forgery protection, which is a different contract's property
being scored as credential/v2 evidence.

**Minimum sound correction.** Split the set: name the six credential/v2
results as the closed v2 hub set, and record `account-linked` as a *shared*
kind that credential/v2 reuses under SPEC-053 REQ-033's existing ownership,
already resident in `hub-originated?/1`. Scope the mutation criterion to the
six.

---

## L-2 (LOW) — the encrypted credential body's length is relay-observable and proxies the person's room count; nothing in the effective set requires padding, despite the blind-relay claims the design leans on

**Owner: Selfsame `NFR-911` (or cbcl-pairing, if padding lands in the carrier).**

`CON-021` correctly establishes that `AccountTransition` "adds no relay-visible
field", and I confirmed cbcl-pairing has no `offer` concept at the relay at all
(`grep -rn "offer" src/*.rs` in cbcl-pairing returns nothing) — the transition
travels inside the AEAD-protected `credential` field. But the *length* of that
field is on the wire: `src/mailbox.rs:421` reads `sequence.body_len` and
`src/cbcl_protocol.rs:34` bounds `MAX_ADJACENT_BODY_OCTETS = 65_536`.

`migrationRooms` may be up to 256 entries and 48,000 octets, so the ciphertext
length ranges over roughly three orders of magnitude and is a direct proxy for
how many rooms the person belongs to — disclosed to the relay operator, who
under `F-E` is explicitly *not* assumed trustworthy and whose untrustworthiness
per-application consent is the accepted mitigation for.

**Minimum sound correction.** Either state the leak explicitly as an accepted
residual in `NFR-911` (with the room-count granularity named), or require the
credential/v2 body to be padded to a small set of fixed buckets before sealing
and add a vector for it. Stating it is sufficient for this tier; leaving it
unstated is not, because `GATE-04`'s privacy/rollback review box has nothing to
review against.

---

## L-3 (LOW) — `CON-013`/`CON-014` require the hub to compute deterministic CBOR and RFC-8785 canonical bytes, and cbcl-bus has neither primitive in LFE/Erlang at the baseline; the increment does not name them as new hub components

**Owner: cbcl-bus `CON-013`.**

`CON-013` retains 0.16.1's `roomSetCbor` and snapshot deterministic-CBOR
definitions and requires "The hub recomputes both from locked state at
finalization"; `CON-014` requires the final transaction to recheck "the digest
graph", which now includes `deviceKeyDigest = SHA-256(RFC8785(deviceKeyJwk))`.

Searching `apps/` at the cbcl-bus baseline finds **no CBOR encoder and no
RFC-8785/JCS canonicaliser** on the Erlang/LFE side (`grep -rni "cbor"` in
`apps/` excluding tests returns nothing; the only `canonicali*` hits are an
unrelated `term_to_binary` audit-log helper and browser-side JS).

This is not a blocker — both are small against these restricted grammars. The
`deviceKeyJwk` case is nearly free: `ceremony.rs:278` fixes it at exactly four
members, `DEVICE_JWK_MEMBERS = ["kty","crv","alg","x"]`, three of them constant
strings, and the RFC-8785 order is already the alphabetical `alg, crv, kty, x`.
The snapshot CBOR needs only byte strings, text strings, non-negative integers,
null and arrays. But it *is* a deliberate third implementation of a shared
encoding in a codebase whose own modules refuse that on principle
(`cbcl-chat-enrolment-signer.lfe`: "a second reading of `CON-214` in LFE is the
parser differential this design exists to avoid").

**Minimum sound correction.** Name the two new hub primitives explicitly in
`CON-013` as owned deliverables with their restricted grammars stated, or route
them through the NIF facade instead; and keep `TEST-1096`/`TEST-052`'s
three-stack vector corpus as the differential control, which is the right
mitigation and is already required.

---

## L-4 (LOW) — `build()`, the infallible RFC-8785 serialiser, is the actual widening vector inside `recognise_unsigned_payload`'s round-trip check; `REQ-976`/`CON-964` freeze recognisers, signers and call graphs but never name it

**Owner: Selfsame `REQ-976`/`CON-964`.**

`REQ-976` freezes "a v1 recognition/signing entry"; `CON-964` requires
"Source/call-graph tests prove the v1 signer cannot reach a version-general/v2
parser". The real call graph is
`cbcl-chat-enrolment-signer:sign/1` → `sign-with/2` →
`cbcl_selfsame_erl:sign_enrollment_profile_bound/4` →
`selfsame-beam::sign_enrollment_profile_bound` →
`sign_enrollment_statement` → `enrollment::recognise_unsigned(statement, kid)`
(`crates/selfsame-beam/src/enrollment.rs:44,130-153`).

But `recognise_unsigned` (and `recognise_unsigned_payload`) close their grammar
partly by a **round-trip through the shared serialiser**
(`enrollment.rs:318-320`, `:352-354`):

```rust
    if crate::json::canonicalise(&build(&statement)) != statement_octets {
        return Err(EnrollmentError::EnrollmentMalformed);
    }
```

`build` (`:660-686`) is shared, infallible, and emits members from a fixed
list. If credential/v2 teaches `build` to emit v2 members conditionally, the
v1 round-trip check silently changes shape. A call-graph test that names only
recognisers and signers will not see it, because `build` is on the *inside* of
the frozen entry rather than upstream of it.

**Minimum sound correction.** Extend `REQ-976`/`CON-964` to name the serialiser
alongside the recogniser: the frozen v1 entry must round-trip through a v1-only
builder, and the source/call-graph test must assert that the v1 path reaches no
version-general `build`. One clause; the test seam already exists.

---

# Disposition of the original `F-A` – `F-E`

| Finding | Disposition | Evidence |
|---|---|---|
| **F-A** — trust is application+relay scoped; a new application prompts | **HOLDS** | Exact `(applicationId, relayOrigin)` pair TOFU retained unchanged (0.4.5/0.4.6 effective text; 0.4.9 restates it). `relayOrigin` provenance verified authentic, not wire-claimed: it is a member of the CON-220 profile's `cbclPairingRelays` descriptor, present in the live ratified document (`cbcl-chat-selfsame-application-gate.lfe`, decodes to `"relayOrigin":"https://chat.anuna.io:9443"`), and its canonical descriptor digest is fixed by 0.4.7 `REQ-968`/`CON-957`. |
| **F-B** — approval displays the CON-220-authenticated application/transition; cross-check precedes display | **HOLDS, with M-2** | `CON-963`: "`IntentVerifier` recomputes the digest before it can return success; cbcl-pairing therefore cannot emit the typed display for a mismatch." cbcl-pairing `REQ-026`: "Only after `Ok(())` may the profile construct `CredentialV2Display` from those exact parsed bytes." Verified constructible: all three `impl ApplicationProfile` are in-crate (`src/profile.rs:490,571,648`) and every `DisplayIntent`/`DisplayField` construction is in-crate (`:784-795`), so private fields break no external producer. **M-2** records that adopted `CON-960` still contradicts the verdict-only rule. |
| **F-C** — no custody access, derivation, scope mint, signing, publication, alias provisioning or grant precedes the appropriate explicit consent | **HOLDS** | `CON-962`'s order is intact and correct: nothing wallet-side precedes preliminary approval; no hub binding/grant/alias/membership/room-deletion precedes signed final approval, verified publication/grant and browser install. Pending hub allocation, the wallet request tombstone and separately accepted TOFU/policy/rate state remain the only earlier mutations. `C-1` is a post-commit defect, not an ordering defect. |
| **F-D** — standalone first contact has a concrete fresh account-scope bootstrap | **HOLDS** | Adopted `REQ-970`: the gated allocation command "allocates one pending CSPRNG `applicationAccountId` and CON-211 `accountScopeId`" before the public carrier; those become durable only at the final transaction; decline/expiry/pre-commit failure deletes them; retry may derive a different preview DID and must display it again. `ADR-941` states the ordering dependency correctly. |
| **F-E** — per-app consent is a mitigation, not a relay-operatorship binding claim | **HOLDS** | 0.4.9 restates it as such and claims nothing further. See **L-2** for one unstated residual (ciphertext length as a room-count proxy) that this mitigation does not cover. |

---

# Disposition of every 0.4.8 finding (1C / 4H / 3M / 7L)

Re-tested against code, not against the 0.4.9 disposition table.

| 0.4.8 finding | 0.4.9 claim | Re-test verdict | Evidence |
|---|---|---|---|
| **C-1** (CRITICAL) — the transaction writes a `cbcl-pairaccount-grant` for the key the person must reconnect with; both keys inert | `REQ-973` removes every credential/v2 pairaccount write and late cap | **NOT CLOSED — relocated and worsened.** See **C-1** above. | The write removal is correct and verified necessary (`cbcl-chat-pairaccount.lfe:52-68` stamps `'unratified`; `cbcl-core-enrol.lfe:125-131` refuses before the binding case). But the resulting negative fact is falsifiable by any third party at any time via the ungated, undeletable `do-pairaccountgrant` writer, and `REQ-973` checks it only pre-commit. |
| **H-1** (HIGH) — WS gate runs after TOFU and default fan-out; `REQ-971` unsatisfiable | `REQ-974` adopts a pre-authorization `dispatch/3` gate and closed kind set | **CLOSED.** | Verified structurally exact. `handle-frame` (`:205-211`) performs only `rearm-ping`, `cbcl-signed-frame:decode` and `cbcl-parser:parse-message` before `dispatch/3` — no durable state, no I/O, no fan-out. `dispatch/3` (`:213-235`) reads the binding only in its `_` branch, so a first clause in `dispatch` genuinely precedes binding lookup, `resolve-key`, `verify-frame`, `authorise-frame`, TOFU and `route`. The fall-through hazard is real and correctly described: `custom-kind` defaults to `'other` (`cbcl-core-msg.lfe:140`) and `route-member`'s default is `do-publish`. The required production parser change (naming the 13 head symbols) is named by `REQ-042` and is the necessary complement. |
| **H-2** (HIGH) — `fingerprintCanonicalBytes` undefined; hub has no BLAKE3 | `REQ-975` removes the fingerprint, length-prefixes the DID, defines the device-key digest | **CLOSED.** | `grep -rni blake3` in cbcl-bus `apps/` returns nothing, confirming the original finding. The replacement lands on an *already-implemented* definition: `enrollment::device_key_digest` (`enrollment.rs:264-274`) is exactly `SHA-256(canonicalise(deviceKeyJwk))`, and `deviceKeyJwk` is a closed four-member object (`ceremony.rs:272-278`, `DEVICE_JWK_MEMBERS = ["kty","crv","alg","x"]`) already in RFC-8785 order. The confirmation preimage is unambiguous: domain‖`0x00`, then 32-octet `offerCoreDigest`, then `u32be(len)`‖DID, then four 32-octet values — exactly one variable-length member, and it is length-prefixed. `Fingerprint::hex()` correctly demoted to a wallet/UI test under REQ-103. **L-3** records the residual hub primitives. |
| **H-3** (HIGH) — the positive fixture opens an unnamed second Tier-1 gate and collides with `cbcl-chat-gate-closed-tests` | `REQ-974`/`REQ-044` name both overlays, split production-artefact evidence from fixture evidence, forbid a Path-B bypass | **CLOSED.** | The second gate is real and correctly named: `cbcl-chat-path-b-gate:profile/0` returns `#(error tier-1-unapproved)` as a source constant, and `account-facts` (`cbcl-chat-session-ws.lfe:349-357`) consults it on **every** account-bound frame via `path-b-profile/0` (`:803`), so a credential/v2-created account genuinely cannot reconnect without it. The crossover is resolved by scoping: `cbcl-chat-gate-closed-tests` (including `path-b-gate-is-closed`, `:18-22`) runs against the production artefact first; closed-constant assertions are declared inapplicable to the fixture runtime, with four named compensating controls. See residual R-2 on build mechanics. |
| **H-4** (HIGH) — the live `/selfsame/enrolment/sign` is an unauthenticated widening oracle | `REQ-976`/`REQ-043` freeze the route to v1 and make held ceremony state the only v2 signing input | **CLOSED in shape; see H-1(new) on the discriminator.** | The hazard is verified exactly as described: `sign/1` gates on the **serving** document (open since 2026-08-22) and then signs caller-supplied octets — `sign` → `sign-with` → `sign_enrollment_profile_bound` → `sign_enrollment_statement` → `enrollment::recognise_unsigned` (`selfsame-beam/src/enrollment.rs:44,153`). Widening `recognise_unsigned` would therefore make the deployed route a v2 oracle. The frozen-v1 entry + server-state-only v2 signer is the right shape and is implementable. **H-1(new)** shows the chosen version discriminator is unavailable; **L-4** notes the shared `build()`. |
| **M-1** (MEDIUM) — wallet cannot recompute two of three digests, and is nowhere required to recompute the one it can | `REQ-975`/`CON-013` require the room-set recompute before display and state the per-party split honestly | **CLOSED.** | `CON-013`: the wallet recomputes `offerCoreDigest`, descriptor/carrier/intent, `deviceKeyDigest` and `roomSetDigest` "from the exact `migrationRooms` array it will display", refusing before preliminary display on mismatch; it "cannot recompute `legacyKeyDigest` or `migrationSnapshotDigest`; those are explicitly hub-only assertions … copied as raw authenticated values"; the hub recomputes both from locked state. "No party claims to recompute an input it does not hold" — which is the honest statement the finding asked for. |
| **M-2** (MEDIUM) — no credential/v2 reply performative is in `hub-originated?` | `REQ-974`/`REQ-042` require every hub-minted member to be a literal member | **CLOSED.** | Verified: `hub-originated?/1` (`cbcl-core-dialect.lfe:79-84`) is a closed `lists:member` literal list and is the only thing between a client-authored hub verb and `do-publish` (`route/8`, `:928-931`). `account-linked` is already present. See **L-1** on set hygiene. |
| **M-3** (MEDIUM) — the typed `AccountTransition` has no carrier; `DisplayIntent`/`DisplayField` are public-field structs | cbcl-pairing 0.4.4 `REQ-026`/`CON-021` supplies the opaque typed carrier | **CLOSED — verified implementable, not merely asserted.** | Baseline confirmed: `src/profile.rs:81-101` — both structs public with public fields; `:206` — `recognise_intent(&self, …)`. The 0.4.4 changes are all sound against the real code: `profile: Box<dyn ApplicationProfile>` is an owned field (`src/endpoint.rs:295`); both call sites are already inside `&mut self` methods (`send_intent`, `:534`; `apply_intent`, `:982`) and the `Result` returned captures no borrow of `self`, so `self.fail(…)` in the `Err` arm still borrow-checks; `&mut self` preserves object safety for `dyn ApplicationProfile`; `authorize_payload(&mut self, …)` is the in-crate precedent. All three profile impls and every display construction are in-crate, so private fields break no external producer. `DisplayIntent` owning a private `DisplayBody` with a borrowing `authenticated(&self) -> AuthenticatedDisplay<'_>` is not self-referential and survives being moved into `EndpointEffect::DisplayIntent`. The only consumer breakage is the deliberate kind (`selfsame-pairing/examples/local-wallet.rs:91-94` and `tests/live_sessions.rs:120-121` read `intent.application` / `intent.fields` directly), which `NFR-018` explicitly permits. **M-2(new)** records the contradicting adopted clause. |
| **L-1** (LOW) — the lock order names a nonexistent "device count index" | `NFR-911` adds and migrates `cbcl-device-grant.account` | **PARTIALLY CLOSED → M-3(new).** | Original finding confirmed: `ensure-device-table` (`cbcl-chat-account-grants.lfe:53-61`) declares no index, and `account-device-count/1` (`:204-207`) is a whole-table `mnesia:foldl`. Adding the index is correct and there is an in-repo idempotent precedent (`cbcl-chat-webfinger-store.lfe:97-110`, `ensure-indexes`/`add-index`). But the accompanying stable-account lock rule is under-specified and one writer has no cap read — see **M-3**. |
| **L-2** (LOW) — `CON-941` states `cbcl-webfinger-account`'s primary key is the handle | `CON-965` clause 5 replaces it with `account` | **CLOSED.** | Verified: `cbcl-chat-webfinger-store.lfe:54` — `(defrecord cbcl-webfinger-account account alias home-did)` and `:70` creates it with `attributes '(account alias home-did)`, so the primary key is `account`. `CON-965`'s further note ("its reciprocal store has no production caller before this increment") is consistent with the module's own comments. |
| **L-3** (LOW) — a nonexistent gate stated as a present-tense fact about the baseline | Effective text says "on landing" | **CLOSED.** | `cbcl-chat-credential-v2-gate` does not exist at the cbcl-bus baseline (`find` returns only `cbcl-chat-path-b-gate.lfe`), and `REQ-974` correctly phrases it as an obligation: "On landing, `cbcl-chat-credential-v2-gate:profile/0` is the sole credential/v2 enablement point". `CON-965` clause 6 ledgers it. |
| **L-4** (LOW) — two escape notations for one NUL; a "field vector" `CON-012` does not supply | `REQ-975`/`CON-013` use one literal `0x00` and name the logical member vector | **CLOSED.** | One rule, stated once: "Every literal digest domain in this contract ends in exactly one `0x00` octet; no other separator is implied." The member vector is now exact and verified against the records: `cbcl-member` is `(handle pubkey enrolled)` (`cbcl-chat-members.lfe:13`), so `['cbcl-member', handle, enrolledPublicKey]` correctly excludes the wall-clock `enrolled`; `cbcl-roommember` is `(key room handle pubkey granted via since)` (`cbcl-chat-roommember.lfe:28`), matching `CON-012`'s eight-element vector exactly. The 0.4.8 `CON-959` key encodings are also exact: `pkey` builds `u32be(len(room))‖room‖handle` (`cbcl-chat-roommember.lfe:259-261`) and `u32be(len(room))‖room‖u32be(len(account))‖account‖handle` (`cbcl-chat-account-roommember.lfe:234-239`). |
| **L-5** (LOW) — the offer-core cap is asserted to fit the logical bound without arithmetic | `NFR-911` supplies a generated `56,000 + 6,000 = 62,000` proof and residual | **NOT CLOSED → M-1(new).** | The arithmetic is now present and the 62,000 target is verified real (`cbcl-pairing/src/profile.rs:334,920`). But the 6,000 non-core budget is contradicted by `ENROLLMENT_JWS.max_octets = 8_192`, which the credential/v2 compact evidence inherits and which `NFR-009` explicitly counts inside that budget. See **M-1**. |
| **L-6** (LOW) — `MigrationPlanTooLarge` silently excludes the person | `NFR-911` makes it an explicit unavailable-for-this-handle result with no hidden fallback | **CLOSED.** | "the person cannot link that chat handle through credential/v2 in this version. There is no partial, truncated, paged, console, or enrolment fallback hidden behind that result." `TEST-055` requires the UI to say so. The consequence is now stated rather than implied. |
| **L-7** (LOW) — the no-`offer` rule is stated but not asserted from the observing side | `TEST-1101`/`CON-965` clause 9 make it allocator/relay-observed | **CLOSED.** | Implementable: cbcl-pairing's relay distinguishes control frames from adjacent bodies (`src/cbcl_protocol.rs:32-34`, `src/mailbox.rs:421`), so "zero `offer` objects" is observable as zero application-payload frames after Finished, in-process against `RelayService`; the did-crdt rendezvous mailbox (`src/service/rendezvous.rs`) is likewise directly inspectable for an unwritten slot. |

---

# Verification of the coordinated amendments

## cbcl-pairing SPEC-001 0.4.4 — passes on its own terms

`REQ-026`, `NFR-018`, `CON-021`, `ADR-019`, `TEST-053`–`TEST-055` are internally
consistent and implementable against `8e55160`. Specifically verified:

- The named receiver change is safe. `recognise_intent(&self)` →
  `(&mut self)` type-checks at both call sites and keeps `dyn` object safety;
  no interior mutability or global verifier is needed, exactly as `TEST-053`
  requires ("without a global, `Mutex`, `RefCell`, or verifier returned by the
  consumer").
- Private fields break no external producer: all three `ApplicationProfile`
  impls and all `DisplayIntent`/`DisplayField` construction are in-crate.
- `DisplayIntent` owning a crate-private `DisplayBody` with a borrowing
  `authenticated()` accessor is not self-referential and survives the move into
  `EndpointEffect::DisplayIntent`.
- `ProfileIntent::new(display, binding)` remains the crate-internal seam; the
  endpoint retains only digest, control hash and opaque binding
  (`endpoint.rs:996-1003`), so `AccountTransition` adds no second retained
  authority channel — and, verified independently, no relay-visible field, since
  cbcl-pairing has no `offer` concept at the relay at all.
- `TEST-055`'s control-cap witnesses are grounded: `MAX_CONTROL_OCTETS = 2_048`
  (`src/cbcl_protocol.rs:32`), so 2,048 positive and 2,049/4,095/4,096 negative
  is the real constant, not a restated one.

**One caveat, owned by Selfsame, not by cbcl-pairing:** `M-2` — the consumer's
adopted `CON-960` still contradicts `ADR-019`.

## cbcl-bus SPEC-053 0.16.2 and its amended parent — passes except where noted

`REQ-041`–`REQ-044`, `NFR-009`, `CON-013`–`CON-015`, `ADR-018`, `DEC-016`,
`GATE-04` and `TEST-048`–`TEST-056` are coherent and, except for the items
below, implementable against `fdbe414`. Specifically verified:

- `REQ-041`'s negative eligibility read is implementable: `cbcl-pairaccount-grant`
  already carries `(tuple 'index (list 'handle 'devkey))`
  (`cbcl-chat-pairaccount.lfe:35`), so "exact unratified devkey index read"
  in `CON-014`'s lock order is a real index read.
- `CON-014`'s membership staging matches the existing primitive exactly:
  `migrate-legacy-handle-tx/3` (`cbcl-chat-account-roommember.lfe:134-172`)
  already takes whole-table write locks on both membership tables, stages every
  target row preserving `granted`/`via`/`since`, and deletes legacy rows only
  after staging — the ordering `CON-014` prescribes.
- `REQ-041`'s post-commit reconnect claim is structurally sound: with no
  pairaccount row, `resolve`'s `pairing-unratified` clause passes and
  `account-bound` requires `selector-present`, `revoked = false`,
  `issued = true` (satisfied by the written `cbcl-device-grant`, read by
  `path-b-issued?`, `cbcl-chat-session-ws.lfe:393-397`), `standing = ok` and
  the permission set — all reachable in a Path-B-open build.
- The `GATE-04` box list is complete and this amendment closes none of them,
  as it states.

**Not verified / defective:** `C-1` (the post-commit falsifiability of the
pairaccount precondition, owned here), `M-3` (stable-account lock and the
uncapped second writer), `L-1` (`account-linked` attribution), `L-3` (absent
hub CBOR/JCS primitives), and cbcl-bus's half of `H-1(new)` (the served
profile document) and `M-1` (`NFR-009`'s 6,000 budget).

---

# Residual assumptions and open gates — *not* design blockers

These are recorded so they are not mistaken for findings, and so `GATE-04`'s
reviewers have them in one place.

- **R-1 — post-commit authority depends on continuous resolver availability.**
  After the irreversible commit the legacy key is dead and the granted key's
  `standing` is established through `path-b-establish-standing` →
  `path-b-sidecar` → `cbcl-chat-path-b-resolver:resolve`
  (`cbcl-chat-session-ws.lfe:437-452,806-809`), which fails closed. A did-crdt
  outage is therefore a transient total lockout with no legacy fallback, by
  design. The DID document is published and browser-verified *before* commit,
  so this is availability, not correctness; `GATE-04` already requires did-crdt
  `durable-ready` continuously true. Worth naming in the human-review box.
- **R-2 — the two-gate fixture is an unbuilt build-system seam.** `REQ-044`
  requires compiling *replacements* for two modules that live in `src/`.
  rebar3's `test` profile adds `{src_dirs, ["src","test"]}` (`rebar.config:128`)
  but has no per-profile source *exclusion*, so two same-named modules would
  collide. `REQ-044`'s "excludes the overlay directory by an explicit source
  manifest" acknowledges this. Real release-guard tooling exists to build on
  (`scripts/check-native-release-guards.sh`,
  `check-release-target-isolation.sh`, `check-retired-pairing-absence.sh`,
  `gen-production-profile.mjs`), so this is effort, not impossibility.
- **R-3 — production kind recognition is a mild existence oracle.** Adding the
  13 head symbols to `cbcl-core-msg:custom-kind` in production makes
  `selfsamepairallocate` answer `bad-signature` while an unknown verb is still
  published, revealing that the kind set exists. The set is public in the
  specs; no downstream check is disclosed, so `GATE-00` condition 3 is not
  breached. Noted, not filed.
- **R-4 — the `CredentialV2GateWitness` unforgeability mechanism is
  unspecified.** `REQ-042` requires a "non-serializable" witness. This is
  achievable in BEAM (a closure or `make_ref/0`, neither producible by
  `cbcl-parser` from wire bytes), but the amendment names no mechanism. An
  implementation choice, not a defect — provided the chosen term is one the
  wire parser demonstrably cannot construct.
- **R-5 — `cbcl-grant-nonce` is absent from the continuity manifest.** It is
  CON-007's expiring one-time challenge ledger
  (`cbcl-chat-grant-nonce.lfe:1-7`, record carries `expires`), i.e. per-ceremony
  rather than per-frame identity state, so its omission from `NFR-911` is
  defensible. Recorded so a later reviewer does not re-derive it.
- **R-6 — unchanged surfaces not re-opened.** CPace transcript/role/application
  binding, the PROTO-004 envelope, the rendezvous carrier
  (`MAX_SLOT_BYTES = 4096`, read-once, `MAX_SLOTS = 65_536`,
  `SLOT_TTL_SECONDS = 600`) and SPEC-075 relay opacity are untouched by 0.4.9
  and were verified only for the absence of new contact, not re-audited.
- **R-7 — the compromised-Path-A-key takeover remains an accepted, gated
  risk.** Whoever holds the legacy enrolled key can drive the whole ceremony
  and irreversibly bind the handle to an account of their choosing.
  `GATE-04` already routes this to human security review; 0.4.9 does not
  change it and this review does not re-litigate it.
- **R-8 — device-key loss after migration is recoverable, and is not a
  finding.** The wallet retains account root authority and can issue a fresh
  Path-B device grant to a new browser, so an evicted or cleared IndexedDB is
  not a lockout. This is stated because "browser-install evidence" is a
  browser self-assertion the hub cannot verify, which would otherwise look
  like an unrecoverable single point of failure.

---

# What would make this pass

1. **cbcl-bus** — close `C-1`: bind `pairaccountgrant` to possession of the
   claimed device key; make a verified `cbcl-device-grant` dominate an
   unratified pairing claim in both `claim-tx` and `unratified-device?/1`; and
   extend `GATE-04`'s second box to "cannot be made to fail it by any other
   party". **Selfsame** — restate `REQ-973`/`NFR-911` so the invariant, not two
   pre-commit checks, is what the design relies on.
2. **Selfsame** — close `H-1`: move the credential/v2 discriminator from
   `profileVersion` to `evidenceVersion` throughout `REQ-976`, `CON-964` and
   `TEST-1094`, and coordinate the same change into cbcl-bus `REQ-043` and
   `TEST-050`. (Or, if `profileVersion` must move, open the separate SPEC-004
   increment that owns CON-220 re-ratification, dual-version coexistence, the
   pinned-digest migration, and the deployed-CON-219 break.)
3. **Selfsame `NFR-911` + cbcl-bus `NFR-009`** — close `M-1`: declare an
   explicit credential/v2 evidence cap below the non-core budget, or restate
   the split so the sum of declared maxima is ≤ 62,000, citing
   `ENROLLMENT_JWS.max_octets` by name.
4. **Selfsame `CON-965`** — close `M-2`: ledger the replacement of `CON-960`'s
   `IntentVerifier`-returns-`AccountTransition` sentence.
5. **cbcl-bus `NFR-009`/`CON-014`** — close `M-3`: name the stable-account lock
   table and key rule including absent rows, require the cap check of every
   device-grant writer, and dispose of `store-verified-device-grant/6`
   explicitly.
6. **The four lows** — `L-1` through `L-4`, each a bounded clause change with a
   single named owner.

None of these is architectural. `C-1` and `H-1` are the only two that block.

---

# Authority statement

This review confers **no implementation, allocation, enablement, release or
deployment authority whatsoever**, and a PASS from a future review would not
either. Specifically:

- The verdict is **REJECT**; the coordinated set is not approved.
- Even on a later PASS, `SPEC-008` 0.4.9's own sequencing holds: a PASS
  authorizes an Elephant SPL/test-first plan and nothing else.
- `GATE-04` remains no-go with every box open. `GATE-01` and `GATE-03` remain
  required and are not addressed here. Production credential/v2 allocation
  stays closed, both Tier-1 source constants stay closed, human
  cryptography/migration/privacy review, independent CPace evidence, both
  durability gates, owner approval and separate deployment confirmation all
  remain outstanding.
- This review changed no implementation code, candidate spec, test, manifest or
  configuration. It adds exactly this one file.
