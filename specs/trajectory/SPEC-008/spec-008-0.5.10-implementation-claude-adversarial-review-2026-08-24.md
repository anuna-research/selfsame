---
id: spec-008-0.5.10-implementation-claude-adversarial-review-2026-08-24
title: SPEC-008 0.5.10 implementation — Tier-1 cross-model adversarial review
status: final
tier: 1
review-type: cross-model-adversarial
reviewer-family: Anthropic Claude
generating-family: OpenAI GPT-5
verdict: REJECT
date: 2026-08-24
---

# SPEC-008 0.5.10 implementation — Tier-1 adversarial review

## Reviewer identity, authentication path, Circus attempt, and independence

**Reviewer model family.** Anthropic Claude (Opus 5, `claude-opus-5`), fresh
session, interactive Claude CLI. The generating family is OpenAI GPT-5
(`gpt-5.6-sol`). This session received no generating-session reasoning,
transcript, plan, or intermediate artefact.

**Subscription authentication path.** `ANTHROPIC_API_KEY` and
`ANTHROPIC_AUTH_TOKEN` were both confirmed absent in the parent environment
without printing any value. `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN
claude auth status` reported:

| Field | Value |
|---|---|
| `authMethod` | `claude.ai` |
| `apiProvider` | `firstParty` |
| `subscriptionType` | `max` |

These are the three required values. No email, organization identifier, account
identifier, token, or key is recorded here. The API-billed
`circus-driver-claude` was not invoked.

**Circus attempt.** Worktree `/Users/anuna-01/Code/selfsame-spec008-final-review-2`,
branch `circus/spec008-final-review/2`, attempt 2, base `8b7977a`.

**Independence statement.** The four governing parent specifications, the
complete candidate diffs, and the executable browser, hub, pairing-library and
wallet paths were read *before* any earlier review was opened. Every prior
finding was independently re-derived from code or from a runnable test; no
disposition was inherited. Prose, test names, commit messages, and this
prompt's own claims were treated as untrusted until code or an executed test
supported them. Every build and test ran in clean detached clones under the
session scratch directory; no candidate repository was modified and no
uncommitted file was read as candidate evidence.

## Exact commits reviewed

| Repository | Candidate | Prior implementation-review candidate | Original baseline |
|---|---|---|---|
| selfsame | `f054bad1840ffb5ce3840bba87c79a8f21bd3740` | `d01959f877d77d50a5c32f830664bd223ae9ebd8` | `0220cec2dec44cd95d4f411ea4814d790b6716d2` |
| cbcl-bus | `bc1cf5d23029e36543df37d32f3234a2f6bb20c5` | `79423c433e636ccf9c9cf83f6574c6e40c481715` | `ce894a7f2debc111b610296b1a3780de6084e37e` |
| cbcl-pairing | `3d013306cab68c9e258e149387e420058b57e80f` | `aedbc4ca07068c3cf6356d95333f4c2ec0d374d9` | `c22f2d0526432c01c56d64be35a8d4f87516ec92` |
| did-crdt | `94db956d7d5d5edf0f42afad073ab42f265a1752` (repo HEAD) | — | `1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1` (the pinned revision) |
| cbcl-rs | `febc6691e6dd2d5f7116b1a4d84c984b64717564` | — | — |

Governing parents read in full or in the load-bearing sections:
`specs/SPEC-008-production-pairing-claimant.md` (0.5.8-draft),
`specs/SPEC-007-cbcl-pairing-cutover.md`, cbcl-bus
`specs/SPEC-053-key-root-identity.md`, cbcl-pairing
`specs/SPEC-001-reusable-blind-pairing.md`.

Code-only change volume across the candidate ranges: selfsame 14,204
insertions / 216 deletions; cbcl-bus 10,010 / 28; cbcl-pairing 11,059 / 47.

## Verdict

**REJECT.**

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 2 |
| MEDIUM | 0 |
| LOW | 4 |

PASS requires zero CRITICAL, HIGH, and MEDIUM findings. Two HIGH findings are
open. Both are executable or single-line-traceable, both fail closed, and both
are load-bearing obligations that the candidate's own governing contract states
explicitly.

The candidate is substantially stronger than 0.5.9. The 0.5.9 H-2 (installed
lifecycle), M-1 (recovery-commitment binding), M-2 (expiry linearization) and
M-3 (coordinated pins) are all closed and were confirmed closed by independent
mutation testing and clean-closure builds. The two HIGH findings below are
residuals *inside* the mechanisms written to close 0.5.9 H-1 and H-2: the
bootstrap-reopen repair covers three of the four bootstrap checkpoints but not
the fourth, and the new installed-link lifecycle covers the installed slot but
leaves the pending slot with no terminal-refusal removal path at all.

---

## Findings

### H-1 — A restored browser allocator is driven to `Terminal` at the fourth bootstrap checkpoint: the `FinishedSent` state whose frame the relay has already acknowledged

**Severity: HIGH.** Owner: cbcl-pairing repository owner (defect); cbcl-bus
repository owner (unreached client path).

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#CON-986]] ("A crash after that commit
recovers the exact bounded pending record"; the allocator's crash-safe
checkpoint discipline);
[[SPEC-008-production-pairing-claimant#TEST-1156]] ("After credential/v2
allocation socket loss, reconnect with a new socket generation … Require one
correlated result and no duplicate durable effect");
[[SPEC-008-production-pairing-claimant#TEST-1158]] ("Restart from each later
boundary and require only the cached frame …"); cbcl-bus SPEC-053 CON-012
reconnect clause. This review's required check 8 names the state directly:
"Browser restart works at **every** pre-terminal checkpoint … Drive
`ShareSent` and `FinishedSent`, not only allocated and established states."

**Code evidence.** The allocator keeps two independent cached-frame slots.
`CredentialV2RelayState.cached_outbound`
(`cbcl-pairing/src/credential_v2/bootstrap.rs:35`) holds *application* frames
only; `CredentialV2AllocatorBootstrap.cached_outbound`
(`bootstrap.rs:177`) holds the *bootstrap* CPace share and Finished frames.
The remediation added `cached_bootstrap_sequence`
(`bootstrap.rs:131-138`) for the bootstrap arm of `reopen_effects`
(`allocator.rs:414-427`):

```rust
pub(super) fn cached_bootstrap_sequence(&self) -> Result<u8, CredentialV2Error> {
    if !self.awaiting_ack || self.cached_outbound.is_some() {
        return Err(CredentialV2Error::Phase);
    }
    self.next_local_sequence
        .checked_sub(1)
        .ok_or(CredentialV2Error::Counter)
}
```

The guard requires `awaiting_ack == true`. In the `Established` arm the
relay-level cache and `awaiting_ack` move together — `cache_application_frame`
(`bootstrap.rs:106-120`) sets both and `acknowledge_application_frame`
(`bootstrap.rs:140-149`) clears both — so `cached_application_sequence` can
never fail when `cached_outbound_frame()` is `Some`. In the `Bootstrap` arm
they are **decoupled**: `acknowledge_bootstrap_frame` (`bootstrap.rs:87-100`)
clears `awaiting_ack`, but the bootstrap's own `cached_outbound` is cleared
only in `confirm` (`bootstrap.rs:384`), after the peer Finished arrives.

That decoupled state is a real, checkpointed, pre-terminal state.
`local_acknowledged`'s `FinishedSent` arm (`allocator.rs:588-597`) handles the
relay acknowledging the allocator Finished frame *before* the claimant Finished
frame arrives, and seals a checkpoint with no effects. `projection_is_consistent`
(`bootstrap.rs:571-624`) *requires* `cached_outbound` to be
`Some(Finished{Allocator})` in that phase, so the state is not merely reachable
— it is mandatory. On restart, `reopen_effects` reaches
`cached_bootstrap_sequence` with `awaiting_ack == false` and returns
`Err(Phase)`; `receive` (`allocator.rs:319-333`) then sets
`AllocatorState::Terminal` and clears `after_persist`.

**Failure sequence (executed, not argued).** A clean detached clone of
cbcl-pairing at `3d01330` was given
`tests/adv_reopen_after_finished_ack.rs`, which drives a real allocator through
`Bind → Welcome → AllocateV2 → AllocatedV2 (gen 1) → Frame(claimant CPace,
peer_seq 0) (gen 2) → Acknowledged{seq:0} (gen 3) → Acknowledged{seq:1}
(gen 4)`, then restores from the generation-4 sealed checkpoint with the exact
carrier, generation, profile digest and wrapping key and delivers `Welcome`:

```
restored: Ok       start(): Bind
Welcome: panicked at tests/adv_reopen_after_finished_ack.rs:165:
  a restored allocator reopens its blind-relay membership: Phase
```

`CredentialV2AllocatorSession::restore` succeeds and `start()` correctly emits
`Bind`; the `Welcome` that should emit `Open` alone (nothing needs resending —
the relay already acknowledged the frame) returns `Err(Phase)` and terminates
the session.

This reaches the browser unmitigated. `selfsame-web-device`'s
`CredentialV2BrowserAllocatorSession::receive`
(`crates/selfsame-web-device/src/lib.rs:649-676`) maps the error to
`JsError::new("the credential/v2 relay message was refused")`, and the
controller admits `'finished-sent'` as a restored protocol phase
(`cbcl-bus/apps/cbcl_chat/priv/web/credential-v2-allocator.mjs:804-805`). A tab
reload, sleep, or network flip inside the single relay round trip between the
relay acknowledging the allocator Finished and the claimant Finished arriving
permanently strands that ceremony.

It fails closed — no identity effect, no capability, no duplicate — which is
why this is HIGH and not CRITICAL.

**Why the candidate's own tests miss it.** The new
`assert_restored_reopens_cached_bootstrap_frame` helper
(`tests/credential_v2_allocator_session.rs:81-124`) is invoked exactly twice:
at generation 2 (`ShareSent`) and generation 3 (`FinishedSent`, ack pending) —
the two states where `awaiting_ack` is true. The generation-4 checkpoint is
created and persisted at `:277-286` and never restored. The browser-side test
`credential-v2-allocator.test.mjs:604-659` does cover
`restoredProtocolPhase: 'finished-sent'`, but its `protocol` object is a hand-
written stub whose `receive` unconditionally returns two `send` effects; it
never reaches the WASM or the Rust reducer, so it cannot observe this class of
defect at all. No test in any repository restores a real allocator at the
acknowledged-`FinishedSent` state.

**Measurable correction.** Make the bootstrap arm total over the states
`projection_is_consistent` admits: when the bootstrap holds a cached frame and
`relay.awaiting_ack` is false, the frame is already acknowledged, so
`reopen_effects` must emit `Open` alone rather than erroring. Concretely,
replace the unconditional `cached_bootstrap_sequence` call with one that yields
`None` (not `Err`) when `!awaiting_ack`, keeping the `Err` only for the
genuinely corrupt combination (`awaiting_ack && relay.cached_outbound.is_some()`).
Add integration tests that restore and reopen at *all four* bootstrap
checkpoints — `Allocated`, `ShareSent`, `FinishedSent` (ack pending),
`FinishedSent` (acked) — asserting `Open` plus a `Put` byte-identical to the
frame originally released at that phase where one is outstanding, and `Open`
alone where none is. Replace or supplement the browser stub so at least one
browser test drives the real WASM allocator across a bootstrap restart.

---

### H-2 — A terminal refusal after final approval but before payload send leaves a pending slot that nothing can remove: the application can never be linked again short of a full root purge

**Severity: HIGH.** Owner: selfsame repository owner.

**Affected requirement.**
[[SPEC-008-production-pairing-claimant#CON-986]], verbatim: "Decline,
**terminal refusal before payload**, authenticated signed `not-finalized`,
explicit unlink, or root purge removes the pending value." Also
[[SPEC-008-production-pairing-claimant#REQ-1006]] ("Re-grant after hub-side
deletion requires a complete fresh pairing ceremony" — here no fresh ceremony
is possible at all) and this review's required check 15.

**Code evidence.** The application's secure-store slot is a tagged
pending-or-installed union. Five facts, each single-line, close the trap:

1. `persist_pending` (`src-tauri/src/cbcl_v2_completion.rs:1194-1214`) refuses
   with `PairingApplicationAlreadyLinked` (`:1210`) whenever the slot holds any
   value other than a byte-identical retry of the same pending record. A fresh
   ceremony always carries a fresh hub-minted `carrierCeremonyId`, so it is
   never a byte-identical retry.
2. `cbcl_v2_unlink` (`src-tauri/src/cbcl_v2_commands.rs:941-970`) begins with
   `load_installed` (`:958`), and `load_installed`
   (`cbcl_v2_completion.rs:1301-1317`) returns `PairingApplicationNotLinked`
   for a `CredentialV2LinkSlot::Pending(_)`. Unlink cannot reach a pending slot.
3. `cbcl_v2_pending_recoveries` → `pending_application_ids`
   (`cbcl_v2_completion.rs:1221-1259`) surfaces an application to the recovery
   UI only when its stage is `PendingCredentialV2Stage::PayloadPrepared`
   (`:1237-1239`) *and* the relay window has expired. A slot stuck at
   `FinalApproval`, `Planned`, `IssuerCreated`, or `Provisioned` is never
   listed; `src/pairing.js:441-446` drives the UI from exactly that list.
4. `remove_pending` (`cbcl_v2_completion.rs:1393`) has exactly one caller in
   the whole tree — `cbcl_v2_commands.rs:1256`, inside the
   `Recovery::NotFinalized` branch of `recover_claimant_completion`. Even if
   the person invoked recovery manually, that branch is unreachable for a
   pre-payload slot: `recover_claimant_completion` calls
   `durable.payload_facts()` (`cbcl_v2_commands.rs:1118`), and `payload_facts`
   (`cbcl_v2_completion.rs:547-559`) returns `PairingCheckpointRefused` for
   every stage except `PayloadPrepared`.
5. `cbcl_v2_cancel` (`cbcl_v2_commands.rs:1341-1360`) clears only the in-memory
   `AppSession` and sends a relay `Close`. It never touches the durable slot.

The only remaining escape is `purge_all_links`
(`cbcl_v2_completion.rs:1417-1430`), reachable only from the root-lifecycle
purge (`src-tauri/src/commands.rs:617`), which deletes **every** application's
credential/v2 link.

**Failure sequence.** `cbcl_v2_final_decide` writes the pending slot at
`cbcl_v2_commands.rs:443` (`persist_pending`) before the first identity
signature, exactly as CON-986 requires. It then performs, in order: issuer
construction (`:511`), `publish_issuer_identity` (`:520`), `resolve_closure`
(`:527`), grant construction (`:546`). Each of the two network steps returns
`Err(UiError::from("PairingResolverUnavailable"))` on failure
(`:522`, `:524`, `:533`) and each of the four returns `Err` on refusal. None of
those returns removes or downgrades the pending slot.

So: the person approves twice, the resolver is briefly unavailable, the command
returns "PairingResolverUnavailable" — and from that moment:

- the application does not appear in the installed-links list
  (`installed_links` at `cbcl_v2_completion.rs:1264` skips pending),
- it does not appear in the recovery list (fact 3),
- unlink refuses it (fact 2),
- a new ceremony runs the whole two-decision flow, performs a live custody
  preview derivation in `cbcl_v2_preliminary_decide`, opens the relay, consumes
  a hub allocation — and only then dies at `persist_pending` with
  `PairingApplicationAlreadyLinked` (fact 1),
- and the person's only remedy is to destroy every credential/v2 link on the
  device.

No identity effect leaks and no capability is granted, which is why this is
HIGH and not CRITICAL. But the obligation CON-986 states is not merely
under-tested here; it is entirely absent from the implementation.

**Relationship to the prior review.** 0.5.9 L-1 observed that a mid-chain
post-approval failure "now also wedges the application's only wallet slot" and
attributed the wedge to 0.5.9 H-2 (no installed lifecycle at all). H-2 was
closed for the *installed* half. The wedge itself was not closed: the pending
half still has no terminal-refusal removal path, and the new unlink command is
installed-only by construction.

**Measurable correction.** Implement CON-986's "terminal refusal before
payload" clause. Concretely: on every `Err` return from `cbcl_v2_final_decide`
that occurs while `durable.stage` is a pre-`PayloadPrepared` stage, call
`remove_pending(&durable)` (best-effort, logged, never masking the original
error) before returning. Independently, add a person-visible escape that does
not depend on that path succeeding — either extend `cbcl_v2_unlink` to accept a
pending slot under the same explicit-confirmation and presence gate (removing
only the pending value, the exact `(applicationId, relayOrigin)` policy row,
and application-owned cached material), or list pre-payload pending slots in
`cbcl_v2_pending_recoveries` with an outcome that offers explicit abandonment.
Add tests that (a) drive a post-final pre-payload failure and assert the slot is
empty afterwards, and (b) assert that a fresh ceremony for the same application
succeeds after that failure.

---

### L-1 — A mid-chain post-approval failure still leaves an orphaned DID publication with no compensating unpublish

**Severity: LOW (residual, unchanged in kind from 0.5.8 L-1 / 0.5.9 L-1).**
Owner: selfsame repository owner.

**Code evidence.** `cbcl_v2_commands.rs:520-533`:
`publish_issuer_identity` runs, and only then `resolve_closure` and its
verification. A failure or refusal after publication returns without any
unpublish or tombstone.

**Consequence, stated explicitly.** A `did:crdt` document exists at the
resolver that no grant, account, alias, or WebFinger record references. It
confers no capability, creates no hub account, and grants no application
access. Because a fresh ceremony receives a fresh hub-minted `accountScopeId`
and therefore derives a different home DID, orphans do not collide or
accumulate under one identifier; one orphan is created per failed
post-publication ceremony. CON-986's compensation clause is satisfied in its
prohibitive half — nothing changes an operator withdrawal or a pre-existing
identity — and unsatisfied in its positive half for this one effect.

**Operational treatment.** Accept as a known residual for the local
test-first phase. Before production allocation, either move publication after
closure verification (publishing to a staging resolver first), or add a
ceremony-owned tombstone/deactivation compensation reachable from the same
error paths as H-2's correction, and record orphan count in OBS-902's refusal
classes so operators can observe the rate.

---

### L-2 — The CON-036 anonymous limiter is keyed on the TCP peer address, so a reverse-proxied deployment shares one bucket

**Severity: LOW (residual, unchanged from 0.5.9 L-2).** Owner: cbcl-bus
repository owner.

**Code evidence.** `cbcl-chat-selfsame-application-http.lfe:138-142`:
`peer-ip` reads `cowboy_req:peer/1` and falls back to the literal `#"unknown"`.
`cbcl-chat-selfsame-v2-recovery-limit.lfe` keys its fixed window on that value.

**Consequence.** Behind a reverse proxy every wallet in the world shares one
30-requests-per-60-seconds bucket, so one client can rate-limit every other
client off the recovery route. The route fails closed (503, no body, no
authority disclosed), so no authority leaks; the loss is availability of the
post-window recovery path only, and the wallet treats 503 as `unavailable`,
preserving its pending slot.

**Operational treatment.** Either deploy the route without an intermediary, or
resolve the client address from a trusted-proxy-validated forwarded header
before keying, or add a second limiter dimension keyed on
`carrierCeremonyId` (which is not secret and is already in the bounded request)
so one ceremony cannot exhaust the shared bucket. Until then the deployment
runbook must state that this route is not safe to expose behind an
unconfigured proxy.

---

### L-3 — `test_701` verifies only the cbcl-pairing sibling, so with the labelled development override set, `cbcl-rs` and `did-crdt` drift survives the whole test suite

**Severity: LOW.** Owner: selfsame repository owner.

**Code evidence.** `crates/selfsame-pairing/build.rs:56-99` checks all three
siblings, but on the override path it returns `expected` (the *pinned*
revision) rather than the actual HEAD, so `SELFSAME_CBCL_PAIRING_REVISION`
always reports the pin. `crates/selfsame-pairing/tests/dependency.rs` then
compares `baseline.revision` against the same pin file (always true) and
independently `git rev-parse`s **only** `../cbcl-pairing` (`:24-43`). Nothing
in the test suite reads the `cbcl-rs` or `did-crdt` sibling HEAD.

**Executed evidence.** With `did-crdt` moved to `94db956d` and
`SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` set, `cargo test --locked -p
selfsame-pairing --test dependency` exits 0. Without the override the build
panics (`did-crdt source integrity check failed: expected 1f409a42…, found
94db956d…`, exit 101).

**Consequence.** Ordinary builds and CI do reject a drifted sibling, and the
override path emits `cargo:warning=UNPINNED DEVELOPMENT BUILD: did-crdt …;
release evidence is invalid` naming the exact sibling, so the required check-16
property holds. What does not hold is mutation resistance at the *test* layer:
a run with the override set produces a green suite whose closure is not the
pinned one, and only the build warning distinguishes it.

**Operational treatment.** Release evidence must be collected without the
override (the warning is the operational signal that it was not). To close it,
extend `test_701` to `git rev-parse` and tracked-status all three siblings
against their `.sha` files, and have `build.rs` return the *actual* revision on
the override path so the compiled baseline cannot claim a pin it was not built
against.

---

### L-4 — An oversized or otherwise unclassified WebFinger response is reported to the person as `revoked` rather than as unavailability

**Severity: LOW.** Owner: selfsame repository owner.

**Code evidence.** `cbcl_v2_commands.rs:789-812`: `webfinger::fetch` errors are
classified as `NotFound → HubDeleted` and `Timeout | Transport | Refused →
Unavailable`; the remaining `Err(_)` arm — which catches
`NetError::TooLarge` (`crates/selfsame-app-identity-net/src/lib.rs:87-89`) —
falls through to `reload_verified_view(..., grant_matches: false, revoked: true, ...)`,
which `classify_reload` maps to `CredentialV2ReloadOutcome::Revoked` and
`reload_view` renders as `"revoked"`.

**Consequence.** REQ-1006 requires unavailability to be distinct from
revocation. The installed record is correctly retained (`record_retained: true`)
and no capability is granted, so nothing is destroyed and the failure is closed;
what is wrong is the reported cause. An application that serves an oversized JRD
tells every wallet that its grant was revoked. Under REQ-908 the surface is not
truthful in that case.

**Operational treatment.** Map `NetError::TooLarge` (and any future
non-`NotFound` protocol refusal) to `Unavailable`, or introduce a distinct
`binding-unverifiable` outcome, and cover it in
`test_1159_reload_lifecycle_is_closed_over_every_required_state`.

---

## Dispositions for all nineteen required checks

| # | Check | Disposition |
|---|---|---|
| 1 | Relay consent keyed exactly by `(applicationId, relayOrigin)`; displayed facts from the live CON-220-authenticated profile | **PASS.** `src-tauri/src/cbcl_v2_policy.rs` read in full: sorted, canonical, bounded (`MAX_ROWS = 256`), re-encode-compare on load, corrupt/reordered/non-HTTPS state refuses rather than becoming a fresh TOFU prompt; both module tests re-run green. The display authority is `CredentialV2WalletOfferVerifier` built from `self.profile` (the fetched profile) in `cbcl_v2_claimant.rs:288-295`; `intent_view` (`cbcl_v2_commands.rs:1666-1698`) reads only `CredentialV2Display` accessors. Wire `authority_summary`, generic `DisplayField`, and peer strings have no path in. |
| 2 | No derivation, scope mint, publication, grant, alias, bundle, or durable identity effect before its approval; fresh first-contact scope has authenticated, non-attacker-selected authority | **PASS.** `recognise_claimant_invitation` opens no socket and performs only the CON-220 1–5 fetch. `authorise_claimant_relay` writes no policy. `prepare_claimant` builds a memory-only core. The single custody derivation is `preview_identity` (`cbcl_v2_commands.rs:1648-1664`), reached only after preliminary approval + presence. Every signature, publication, resolver call and grant sits after final approval at `cbcl_v2_commands.rs:511-560`. Scope and account-principal digest are hub-minted (`crypto:strong_rand_bytes(32)` in the store's `write-new`) and reach the wallet only inside the signed offer. |
| 3 | No relay-operator proof claimed; exact per-application consent is the explicit mitigation | **PASS.** ADR-902 states the residual in terms. Nothing in the candidate asserts operatorship: the profile's `cbcl_pairing_relays` descriptor is required to name the exact carrier relay origin (`cbcl_v2_claimant.rs:160-166`, exactly one match required), and that is the only relay assertion consumed. |
| 4 | One closed CPace wire grammar and key schedule; preimages, Finished order, checkpoint-key separation | **PASS.** `credential_v2/channel.rs:49-107`: one `Hkdf::<Sha512>` over the ISK with a 64-octet transcript hash over `[public_context, allocator_frame, claimant_frame]`; distinct labels for `kc A`/`kc B`, `key A-B`/`key B-A`, `iv A-B`/`iv B-A`, `exporter`. `confirm` (`:127-141`) rejects a Finished frame whose side equals the local side and compares in constant time. `sealed_aad` binds `v`, direction, counter and `th`. Checkpoint keys are a separate HKDF-SHA512 child (`checkpoint.rs` `derive_key` with `cbcl-pairing checkpoint key/v2`), and the Selfsame-side claimant checkpoint wrapping key (`cbcl_v2_completion.rs:1167-1188`) matches CON-986 byte for byte: `U32BE(len)‖label‖U32BE(len)‖applicationId`, `HKDF-Extract-SHA512(carrierCeremonyId, hierarchyRoot)`, 32-octet expand. `receipt_recovery_token` = `HMAC-SHA-256(EXPORTER, domain‖TH)` and the commitment = `SHA-256(domain‖token‖ceremony‖applicationId)`, both matching CON-989. |
| 5 | Browser installation-device possession proved before offer signing; exact socket-generation typing, bounds, replay resistance | **PASS.** `finalize-current-credential-v2` (`cbcl-chat-enrolment-signer.lfe:124-138`) verifies the proof, and only then reads the signing seed and enters the signing NIF, which verifies again. `socket_generation_digest` (`cbcl-pairing-erl/src/lib.rs:68-86`) enforces `hubId` 1–64, `recoveryNonce` == 16, `frameKey` == 32 and CBOR byte-string types; the browser reproduces the identical construction (`credential-v2-allocator.mjs:68-88`) with the same bounds. Replay is bound by `offer-core-digest` + `socket-generation` + `ceremony` inside the signed proof input. |
| 6 | Resolver-closure replay cannot become resolver authority; hub and wallet independently obtain and compare live authority | **PASS.** The hub fetches its own evidence via `path-b-sidecar` → `cbcl-chat-path-b-resolver:resolve` (`cbcl-chat-session-ws.lfe:826-830`) and passes both the browser-carried closure *and* that independent evidence into `verify_credential_v2_acceptance`. The wallet independently resolves through `resolve_path_b_quorum` + `agree_closures` in both `cbcl_v2_reload_verify` (`:868-885`) and `verify_live_installation`. Neither side accepts the other's closure as authority. |
| 7 | Hub final acceptance atomic, retries byte-identical; browser activation follows signed final acceptance; wallet installation follows authenticated receipt, live reciprocal binding, durable CAS | **PASS.** `accept-tx` → `accept-new` (`cbcl-chat-selfsame-v2-store.lfe:690-725`) writes the account row, the immutable final row and the pending phase change in one Mnesia transaction, with `new-account-row` and `new-final-row` taking table write locks and aborting on any principal/scope/DID/request-id/ceremony conflict. `accept-retry` (`:779-786`) returns the stored `acceptance-ack` only after `final-matches?` compares fourteen fields including the staging-receipt and resolver-closure digests, so retries are byte-identical. `finalized_at` is pinned by `accept-context` and replayed from the final row, which is what makes the JWS reproducible. Wallet installation runs `verify_live_installation` then `install(&durable, &installed, &jrd)`, and `replace_installed` (`cbcl_v2_completion.rs:1322-1352`) is an exact compare-and-swap. |
| 8 | Browser restart works at every pre-terminal checkpoint; restored allocator authenticates, queries status, reopens membership, resends only the exact cached frame; drive `ShareSent` and `FinishedSent` | **FAIL — H-1.** Three of the four bootstrap checkpoints reopen correctly (verified by executing the candidate's own new assertions and by an added generation-4 probe). The acknowledged-`FinishedSent` checkpoint returns `Err(Phase)` and terminates the session. |
| 9 | `selfsamepairstatus` / `selfsamepairabort`: closed grammars, current-generation authentication, exact binding, bounded state, no unauthorized advance; cancel and transport failure not confused | **PASS.** `cbcl-core-dialect.lfe:100-166` closes both grammars over an exact sorted keyword set with no positional terms, no content, `@hub` room and a present `from` handle. `decode-credential-v2-status` requires `recovery-nonce == conn_nonce` (`cbcl-chat-session-ws.lfe:2043-2044`) so a status query cannot be replayed onto another socket. `status-held` verifies the installation-device proof under the pending row's own key. `decide-status` (`cbcl-core-selfsame-v2-pending.lfe:96-127`) only replays, rebinds the socket generation, reports absence, or closes on expiry — it never advances a phase. `decide-abort` (`:134-155`) requires the same generation, a live pre-final phase, and accepts `expired` only at or after the exclusive deadline, so an explicit `aborted` cancel cannot be relabelled as a timeout. Bounded state via `shared-capacity` (65,536) and `pending-window-seconds` (600). |
| 10 | Post-relay-window HTTPS recovery authenticates token, application, ceremony, offer, receipt commitment, live profile, authority rotation; does not derive, publish, issue, resend payload, or install twice; HTTP POST-only, CBOR-only, bounded, non-redirecting, uncacheable, rate-limited before parsing or lookup; limiter fails closed | **PASS.** Server: `dispatch-post` → 405 on any other method; `recovery-request?` requires empty query string, absent `content-encoding`, and `application/cbor` with no parameters; body capped at 2,304 octets → 413; `status-headers` is `no-store`; the limiter runs *before* the content-type check, the body read, the recogniser and the locked lookup, and every non-`ok` limiter result (including an absent gen_server, via `catch`) is the same empty 503. Client: `pairing_status.rs` posts CBOR only, refuses redirection, content-encoding, cookies, a cacheable response, an oversized body, and any status outside `{200,202,404,503}`. `recover_claimant_completion` performs no derivation, publication, issuance or payload resend — it only builds the CON-028 receipt from the returned JWS, authenticates it through the sealed body authority, and installs once via the tagged-union CAS. |
| 11 | Recovery-commitment binding is server-owned; first correct use bound atomically under the pending-row write lock; wrong or absent commitments indistinguishable from unknown; signed `not-finalized` never echoes an unauthenticated caller value; pending and finalized retries | **PASS, mutation-tested.** `bind-recovery-commitment` (`store.lfe:535-547`) binds on first use inside `accept-context-tx`, which holds the row under `mnesia:read … 'write`, and aborts on any later mismatch. `recovery-with-final` and `recovery-without-final` (`:948-990`) both return the bare atom `'unknown` on a wrong application ID or a wrong/absent commitment — `secure-equal?` requires two 32-octet binaries, so an unbound `'undefined` fails closed. The signed `not-finalized` map is built from `(cbcl-selfsame-v2-pending-receipt-recovery-commitment pending)`, the stored value, never from `facts`. Two independent mutations (accept a mismatched second commitment; drop the `recovery-without-final` commitment guard) were both killed by the candidate's own suite. |
| 12 | Expiry sampled and enforced at the mutating accept transaction's linearization point after the row lock; a verifier delay crossing expiry cannot project a witness or create durable acceptance; accepted retries keep idempotent behaviour | **PASS, mutation-tested.** `accept-tx` (`store.lfe:612-639`) re-samples the clock *after* `mnesia:read … 'write` and aborts `'expired` before `accept-projected` runs, so the witness is never projected and no account, migration, WebFinger or final row is written. `accepted`-phase retries skip the expiry gate and return the stored ack. Mutating the re-sample back to the pre-verification `facts` timestamp was killed by `test-119-accept-resamples-expiry-after-live-verification`. |
| 13 | Every restored display and decision authority survives checkpointing without trusting duplicated JavaScript state or admitting oversized state | **PASS.** `recogniseRecoveryAttempt` (`credential-v2-allocator.mjs:646-707`) enforces an exact key set, a closed phase set, fixed 32/64-octet fields, and explicit maxima (carrier 69,632; offer core 56,000; signed offer 56,640; authority status 768), plus paired-presence invariants. Nothing restored from JS becomes authority: `restore_offer_context` (`selfsame-web-device/src/lib.rs:568-628`) re-recognises the signed offer under the live profile, recomputes both digests, matches `request_id`, `intent_nonce`, `expires_at` and the carrier ceremony, re-verifies the authority-status response under the offer `kid`, and calls `require_bound_offer` — every JS-supplied value is an input to a signature check, never a trusted fact. |
| 14 | Installed-link reload verifies stored grant, current profile, resolver authority, issuer, reciprocal WebFinger binding, and hub status before exposing capability; profile-only refresh atomic; rotation prompts; handle change refuses; unavailable and revoked distinct and non-destructive | **PASS with L-4.** `verify_reload` begins with `validate()`, which re-verifies the retained `finalStatusJws` under the historical profile and offer `kid` (`cbcl_v2_completion.rs:1031-1053`). `cbcl_v2_reload_verify` then fetches the live profile, checks the retained offer key is still declared with the same JWK, fetches the JRD, resolves the Path-B quorum, agrees closures, checks revocation and reciprocal binding, and rehydrates the stored grant. `classify_reload` orders root/application → handle → authority → issuer → revoked → profile-refresh → usable, and `reload_view` grants `capability: true` only for `Usable` and `ProfileRefresh`. `record_retained` is `true` on every branch. Profile re-pin is an exact CAS that preserves the immutable offer profile and hub acknowledgement. The one defect is the classification of `NetError::TooLarge` as `revoked` (L-4). |
| 15 | Confirmed unlink removes only the selected slot, its exact pair policy, and application-owned cached material; neither purges the root nor claims remote revocation; re-grant after hub deletion requires a fresh ceremony | **PASS for the installed slot; see H-2 for the pending slot.** `cbcl_v2_unlink` requires explicit `confirmation` and a non-empty passcode, authenticates presence through `Custody::use_hierarchy_root` without using the root, and returns `remote_revocation_claimed: false`. `unlink_installed` (`cbcl_v2_completion.rs:1356-1385`) compares the exact record, removes the exact `(applicationId, relayOrigin)` policy row, rolls that removal back if the slot delete fails, deletes the slot, and prunes only that index entry. No re-grant shortcut exists — the five split functions are the only construction path. H-2 is filed separately because it concerns the *pending* half of the same slot. |
| 16 | Pins name one exact buildable closure across Selfsame, cbcl-bus, ADR-802, CI, Android, tests and WASM provenance; ordinary builds reject mismatched or tracked-dirty siblings unless the labelled override is set; vendored WASM rebuilds byte-identically and exposes the restored allocator ABI | **PASS with L-3.** All declared sites agree: selfsame `cbcl-pairing.sha` = `3d01330`, `cbcl-rs.sha` = `febc669`, new `did-crdt.sha` = `1f409a42`; cbcl-bus `cbcl-pairing.sha` = `3d01330`, `selfsame.sha` = `f054bad`, `cbcl-rs.sha` = `febc669`, `did-crdt.sha` = `1f409a42`; SPEC-007 ADR-802 names `3d01330`; CI and Android both now read `did-crdt.sha` and hash it into the cache key; the WASM provenance names all four plus rustc 1.96.0 / wasm-bindgen 0.2.126 / aarch64-apple-darwin. `cargo test --workspace --locked` passes in a clean exact-pin closure with no override (804 tests). A mismatched sibling panics the build. `check-selfsame-wasm-rebuild.sh` reproduced the vendored package byte-identically from clean pins, and the vendored `.wasm` hashes to the recorded `0b63969d…`. The `did-crdt` post-pin work (`94db956d`) is confined to `src/service/*`, which is behind the `service` feature; every Selfsame crate consumes `did-crdt` through the workspace entry with `default-features = false` and no crate enables `service`, so those changes participate in no credential/v2 runtime. L-3 records the test-layer asymmetry. |
| 17 | Crash, disconnect, timeout, replay, wrong order, cancel, expiry, recovery cannot create a second identity effect or accepted authority; orphan-DID residual evaluated separately | **PASS on the duplication property; LOW on the orphan.** Hub-side uniqueness is enforced by four index reads plus the final-row conflict check inside the accept transaction; retries replay stored bytes. Wallet-side, the slot is a tagged union under a process mutex with exact compare-and-swap on every transition, and the recovery adapter cannot rebuild a grant, re-sign, republish, or resend a payload. Every failure observed in this review fails closed. The orphan-DID residual is L-1 and is **not blocking**: it creates no capability, no account and no authority, it cannot be reused as authority (a fresh ceremony derives a different DID from a fresh hub-minted scope), and its consequence and operational treatment are stated above. |
| 18 | Bounds and one-over-limit refusals executable; ordinary production entry closed; loopback staging and existing enrolment distinct | **PASS.** `PRODUCTION_ALLOCATION_ENABLED = false` (`crates/selfsame-pairing/src/release.rs:11`) and `require_production_allocation()` fails closed, asserted by `spec008_hold_invariance.rs` and `rollback_hold.rs` (the latter through `black_box`, so the constant cannot be folded away). The loopback `ws://` mapping and the fixture context are both behind `#[cfg(feature = "local-pairing-demo")]` (`src-tauri/src/cbcl_pairing.rs:17-35`) and absent from the ordinary build. The credential/v2 modules contain no `record_pairing_trust`, `assemble_claimant`, or conformance-registry edge. Bounds are executable and one-over-limit refusals are asserted throughout (`decode-v2-b64` min/max plus canonical re-encode; `credential_v2/channel.rs` `MAX_PLAINTEXT` and the `17..=MAX+16` ciphertext window; the recovery limiter's `decide/6` boundary tests). |
| 19 | Tests reach the named invariants; identify surviving mutants | **PARTIAL.** Three hub mutants (recovery-commitment rebinding, recovery commitment guard, expiry re-sample) were experimentally killed. The pin guard was experimentally killed for an ordinary build. **Two surviving mutants were found and are the two HIGH findings.** (a) Bootstrap reopen: no test in any repository restores a real allocator at the acknowledged-`FinishedSent` checkpoint; the browser test that names that phase drives a hand-written `protocol` stub rather than the WASM, so it cannot observe reducer errors at all. (b) Installed-link/unlink and pending removal: `remove_pending` has exactly one caller and no test drives a post-final pre-payload failure, so deleting CON-986's terminal-refusal clause from the implementation — which is what happened — changes no test result. Two further test-strength observations: `test_701` does not detect `cbcl-rs`/`did-crdt` drift under the labelled override (L-3), and `test_1159_reload_lifecycle_…` does not cover the `NetError::TooLarge` classification (L-4). Phase reconciliation, rate limiting and browser durable-state recognition were probed and are adequately covered. |

---

## Dispositions for every named prior-review item

### 0.5.9 findings

| Item | Disposition |
|---|---|
| **H-1** — restored browser allocator cannot reopen the relay at either bootstrap checkpoint holding a cached frame | **PARTIALLY CLOSED; RE-OPENED as this review's H-1.** The remediation is real: `cached_bootstrap_sequence` (`bootstrap.rs:131-138`) fixes the two states the 0.5.9 finding named, and both are now covered by executable assertions I re-ran. The correction the 0.5.9 report asked for said "restore and reopen at bootstrap phases `Allocated`, `Claimed`, `ShareSent`, and `FinishedSent`". `FinishedSent` has two distinct checkpointed sub-states; only the ack-pending one was implemented and tested. An added probe proves the other returns `Err(Phase)` and drives the session to `Terminal`. |
| **H-2** — REQ-1006 installed-state lifecycle entirely unimplemented | **CLOSED for the installed half, code-confirmed and test-confirmed.** `cbcl_v2_installed_links`, `cbcl_v2_reload_verify`, `cbcl_v2_unlink`, `classify_reload`, `verify_reload`, `replace_installed`, `unlink_installed`, the `installed-link` UI in `index.html`/`pairing.js`, and `test_1159_reload_lifecycle_is_closed_over_every_required_state` all exist and pass. Handle change refuses, rotation prompts, unavailable/revoked/hub-deleted are distinct, and the record is never cleared. The *pending* half of the same slot has no removal path, filed as this review's H-2. L-4 records one classification defect inside the new mechanism. |
| **M-1** — hub signs `not-finalized` without authenticating the recovery token; echoes the caller's own commitment | **CLOSED, mutation-tested.** See check 11. Binding is server-owned under the pending-row write lock; wrong and absent commitments are indistinguishable from `unknown`; the signed negative carries the stored commitment. Two independent mutations were killed. |
| **M-2** — mutating accept transaction has no expiry gate | **CLOSED, mutation-tested.** See check 12. The clock is re-sampled after the row lock and before witness projection; the stale-sample mutation was killed. |
| **M-3** — coordinated pins disagree; Selfsame CI cannot build the candidate | **CLOSED, build-confirmed.** `did-crdt.sha` now exists and is the sole source of truth; `cbcl-rs.sha` advanced to `febc669`; `build.rs` guards all three siblings; CI and Android read the file; `test-vectors/*.json`, `vectors.rs`, `pinned_derivation.rs` and `con_226_corpus.rs` all read the pin rather than a second copy. `cargo test --workspace --locked` passes in a clean exact-pin closure with no override. L-3 records the residual test-layer asymmetry. |
| **L-1** — orphaned DID publication (and wedged slot) | **SPLIT.** The orphan publication is **OPEN, unchanged in kind**, re-recorded as this review's L-1 with explicit consequence and operational treatment. The wedged-slot consequence, which 0.5.9 attributed to its H-2, is **NOT closed** and is escalated to this review's H-2 with its own code evidence. |
| **L-2** — CON-036 limiter keyed on the TCP peer address | **OPEN, unchanged.** Re-recorded as this review's L-2 with explicit consequence and operational treatment. |

### 0.5.8 findings

| Item | Disposition |
|---|---|
| **H-1** — `selfsamepairstatus`/`selfsamepairabort` absent | **CLOSED.** Independently re-derived: closed dialect validators, response types, session-ws handlers, pure `decide-status`/`decide-abort`, store transactions with device-proof verification under lock, and browser send/receive paths. Re-tested against check 9's full property list; all hold. |
| **H-2** — CON-989 HTTPS recovery unimplemented | **CLOSED.** All three sides exist and were traced end to end (check 10). The two defects 0.5.9 found inside the new mechanism (M-1, M-2) are now closed too. |
| **M-1** — dependency-pin drift | **CLOSED.** Superseded by 0.5.9 M-3, now closed (above). |
| **L-1** — orphaned DID publication | **OPEN, unchanged in kind.** This review's L-1. |

### 0.5.7 findings

| Item | Disposition |
|---|---|
| **N-1** — socket-generation digest typing/bound ambiguity | **CLOSED, independently re-derived in two implementations.** The browser (`credential-v2-allocator.mjs:68-88`) emits `0x84` array head, one CBOR text label, then three CBOR byte strings, and refuses `hubId` outside 1–64, `recoveryNonce ≠ 16`, `framePublicKey ≠ 32`. The Rust NIF (`cbcl-pairing-erl/src/lib.rs:68-86`) enforces the identical types and bounds. The digest also binds the recovery path: `decode-credential-v2-status` requires the request nonce to equal the live connection nonce. |
| **F-A** — exact application+relay, never relay-only | **CLOSED, code-confirmed.** `cbcl_v2_policy.rs` read in full; the two module tests re-run green; `bind_finished_profile` writes the row only after both Finished values, and an existing pair is re-checked rather than assumed. |
| **F-B** — authenticated display, never the peer's claimed string | **CLOSED, code-confirmed and executed.** The seven `compile fail` doctests in `cbcl_pairing` (including `profile::DisplayField` and `profile::DisplayIntent`) all pass under `cargo test --locked`. `CredentialV2WalletOfferVerifier` returns a closed verdict and cannot emit or mutate display text; `CredentialV2Display` validates every field (`display.rs`, bounded application ID / origins / permissions / rooms). |
| **F-C** — split plan/preview/executor replaces the old call order | **CLOSED, code-confirmed.** The five functions are the only credential/v2 construction path; `recover_claimant_completion` is a terminal adapter that cannot derive, sign, publish or resend. CON-988's seven ordered steps were traced individually against `recognise_profile_candidate` → `authorise_claimant_relay` → `prepare_claimant` → `bind_finished_profile`. |
| **F-D** — fresh scope traced end to end, no attacker-selected authority | **CLOSED, code-confirmed.** Scope, account-principal digest and snapshot nonce are hub-minted per allocation and reach the wallet only inside the signed offer; the wallet derives its home key from that authenticated scope and never accepts a peer-supplied one. `CredentialV2Context::derive` places the wallet's own `profileDigest` in `ci`, both `ad` values and the public context, so Finished binds it. |
| **F-E** — no relay-operatorship proof is claimed | **CLOSED (honest residual, unchanged).** ADR-902 states it; nothing in the candidate contradicts it. |

### [[SPEC-007-cbcl-pairing-cutover#REQ-812]]

| Clause | Disposition |
|---|---|
| No accepted credential/identity effect before approved payload delivery (v1 profiles) | **Holds.** v1 paths untouched; `test-vectors/spec-00{1,4}-v1.json` changed only in the recorded `did_crdt_revision` string, and `pinned_derivation.rs` proves the derivation itself is unchanged between the two revisions. |
| No application identity effect before preliminary approval | **Holds.** Traced through `cbcl_v2_preliminary_decide`; the decision object is built and sent before any custody call, and presence is required before the derivation. |
| Preliminary approval authorises one zeroizing derivation and pure preview only | **Holds.** `preview_identity` (`cbcl_v2_commands.rs:1648-1664`) computes only `home_did` inside `Custody::use_hierarchy_root` and returns a `String`. |
| No signing/publish/resolve/issue/persist/stage/payload before final approval | **Holds.** Re-verified line by line at `cbcl_v2_commands.rs:400-560`. |
| Final approval authorises only the declared causal order | **Holds.** issuer → publish → resolve+verify closure → grant → payload, each with a durable `replace_pending` between stages. |
| One encrypted non-authorising completion checkpoint before the first causal effect | **Holds.** `persist_pending` at `:443` precedes `build_issuer_artifacts` at `:511`; the sealed `EndpointCheckpointV2` is inside it. |
| Browser staging crash-safe and non-authorising until immutable hub acceptance | **Holds** for the in-band path and for the CON-989 post-window path. |
| Compensation touches only ceremony-owned effects | **Holds in its prohibitive half; incomplete in its positive half.** No operator withdrawal or pre-existing identity is changed. Two ceremony-owned effects are not compensated: the published DID (L-1) and the pending slot itself (H-2). |

### REQ-909 (2026-08-22 withdrawn first-contact amendment) — recurrence check

Independently re-tested. F-1 does not recur: the carrier carries a real
`application_context` application ID, parsed by `ApplicationId::parse` before
any use, and the profile's own `applicationId` must equal it. F-2 does not
recur: every closure, publication, alias and grant effect sits behind final
approval. F-3/F-4's residual (a relay descriptor certifies its operator only to
the extent the profile does) persists and is the same honest residual as F-E.
F-5/F-6 were internal contradictions of the withdrawn text and have no analogue
in the current architecture.

---

## Exact test commands and results

All work ran in clean detached clones under the session scratch directory. No
candidate repository was modified; no uncommitted file was read as candidate
evidence.

| # | Command | Result |
|---|---|---|
| 1 | `echo "${ANTHROPIC_API_KEY:+YES}${ANTHROPIC_API_KEY:-NO}"` / same for `ANTHROPIC_AUTH_TOKEN` | both `NO` — absent, no value printed |
| 2 | `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN claude auth status` | `authMethod=claude.ai`, `apiProvider=firstParty`, `subscriptionType=max` |
| 3 | cbcl-pairing @ `3d01330` clean clone: `cargo test --locked` | **exit 0** — 27 suites all `ok`, 0 failed, including 7 `compile fail` doctests |
| 4 | Same clone + added `tests/adv_reopen_after_finished_ack.rs`: `cargo test --locked --test adv_reopen_after_finished_ack` | **FAILED (intended)** — restore at generation 4 succeeds, `start()` emits `Bind`, `Welcome` returns `Phase`. Evidence for H-1 |
| 5 | selfsame @ `f054bad` clean closure (cbcl-pairing@`3d01330`, cbcl-rs@`febc669`, did-crdt@`1f409a42`, all clean): `env -u SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING cargo test --workspace --locked` | **exit 0** — 73 suites, 804 passed, 0 failed |
| 6 | Same closure: `cargo test --locked -p selfsame-pairing --test dependency` with the override unset | **exit 0** — `test_701_compiled_dependency_matches_the_pinned_candidate ... ok`. Confirms the reported clean exact-pin dependency result |
| 7 | `git -C ../did-crdt checkout 94db956`, then repeat #6 | **exit 101** — `did-crdt source integrity check failed: expected 1f409a4229…, found 94db956d7d…` |
| 8 | Same, with `SELFSAME_ALLOW_UNPINNED_CBCL_PAIRING=1` | **exit 0** with `cargo:warning=UNPINNED DEVELOPMENT BUILD: did-crdt …; release evidence is invalid`. Evidence for L-3 |
| 9 | cbcl-bus @ `bc1cf5d` clean clone (sibling cbcl-pairing@`3d01330`): `rebar3 eunit` | **exit 0** — `All 975 tests passed.` Matches the reported count exactly |
| 10 | Same clone: `rebar3 eunit --module=cbcl-chat-selfsame-v2-store-tests` | **exit 0** — `All 9 tests passed.` Matches the reported focused-module count |
| 11 | Mutation A — `bind-recovery-commitment` accepts a mismatched second commitment (forced recompile) | **killed** — `Failed: 1. Passed: 8` |
| 12 | Mutation B — `recovery-without-final` drops the commitment guard (forced recompile) | **killed** — `Failed: 2. Passed: 7` |
| 13 | Mutation C — `accept-tx` uses the pre-verification `now` instead of the post-lock re-sample (forced recompile) | **killed** — `Failed: 1. Passed: 8` |
| 14 | `node --test apps/cbcl_chat/priv/web/credential-v2-allocator.test.mjs` | **19 pass, 0 fail** (the report's "4" appears to name a subset) |
| 15 | `node --test apps/cbcl_chat/priv/web/selfsame/selfsame-vendor.test.mjs` | **5 pass, 0 fail** — matches the reported vendored-ABI count; asserts `CredentialV2BrowserAllocatorSession`, `.restore`, `restore_offer_context`, `restored_phase`, `restored_presence_code` |
| 16 | selfsame clean closure: `npm ci && node --test tests/spec-007-wallet-pairing.mjs` | **4 pass, 0 fail** — matches the reported wallet-browser count |
| 17 | cbcl-bus: `bash scripts/check-selfsame-wasm-rebuild.sh` with all four siblings clean at the cbcl-bus pins | **exit 0** — `Selfsame WASM rebuild matches the exact vendored package` |
| 18 | `git show bc1cf5d:…/selfsame_web_device_bg.wasm \| shasum -a 256` | `0b63969d70107224fa8b120e6ad7cdc725d175f89a0e49f97960810810c7f6d4` — equals `provenance.wasmSha256` |
| 19 | `rustc --version` / `cargo --version` / `wasm-bindgen --version` / `rustc -vV` host | `1.96.0` / `1.96.0` / `0.2.126` / `aarch64-apple-darwin` — matches provenance |
| 20 | Call-graph absence checks for H-2: `grep -n 'remove_pending\|purge_all_links\|load_pending' src-tauri/src/*.rs src/*.js` | `remove_pending` has exactly one call site (`cbcl_v2_commands.rs:1256`, the `NotFinalized` branch); `purge_all_links` only from the root purge (`commands.rs:617`) |

**Evidence hygiene.** Two runs are recorded honestly as environmental aborts
and are counted as evidence for nothing. (a) The first `rebar3 eunit` attempt
died with `No space left on device` while compiling the release NIF; the host
volume was at 100%. The scratch tree was pruned and run #9 completed cleanly
from that state. (b) The first `node --test tests/spec-007-wallet-pairing.mjs`
attempt failed with `ERR_MODULE_NOT_FOUND: axe-core` because the clean clone
had no `node_modules`; that is a dependency-resolution failure, not a result,
and run #16 is the recorded evidence after `npm ci`. Separately, one mutation
run initially appeared to survive because `rebar3` had not recompiled the
edited module on mtime alone; every mutation result recorded above is from a
run in which the target `.beam` was deleted first and the module verifiably
recompiled.

---

## Surfaces examined and found sound

Beyond the checks tabulated above: the credential/v2 CBOR body grammars against
CON-987's CDDL (closed maps, exact sizes, `predecessorDigest` chaining, the
`decision`/`result`/`reason` literal sets); `CredentialV2PresenceCode`
recognition against REQ-1006 (Crockford Base32, eleven groups of five, zero pad
bits, `checksum16` preimage, BIP-39 rejection) and its strict separation from
the machine carrier; the claimant's relay-state machine and its
`resume_cached_frame` guard (which, unlike the allocator's, is consistent
because the relay-level cache and `awaiting_ack` move together);
`cbcl_transport::relay_target`'s canonical-origin-only mapping; the enrolment
signer's gate-first ordering and profile-bound `kid` selection; the browser
staging receipt path (`buildCredentialV2StagingReceipt` signs only the
Rust-owned input); the WebFinger store's credential/v2 provisioning inside the
accept transaction; and the link index's canonical, bounded, secret-free
encoding.

---

## What this verdict authorizes

Nothing beyond the record of this review.

**A PASS would not have authorized release, push, or deployment, and this
REJECT authorizes none of them either.** Production release, public
allocation, pushes, and deployment remain prohibited, exactly as the owner's
test-first authorization states. `PRODUCTION_ALLOCATION_ENABLED` remains
`false` and `require_production_allocation()` remains fail-closed; nothing in
this review alters, reinterprets, or provides an enablement path for
[[SPEC-007-cbcl-pairing-cutover#REQ-809]] or the SPEC-007 Production gate.
Release and deployment controls are outside this review's authority.

No candidate repository was modified by this review. The one test file written
as evidence for H-1 lives only in a scratch clone and is reproduced in full in
the finding above.
