---
id: SPEC-008-amendment-0.4.19
title: SPEC-008 0.4.19-draft — Singular authority and one WebFinger order
status: draft
version: 0.4.19-draft
tier: 1
review-gate: not-approved; implementation-prohibited-pending-coordinated-fresh-cross-model-review
supersedes: "[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph]] only where [[spec-008-0.4.19-singular-authority-and-webfinger-order#CON-978]] says so"
adopts: "All effective 0.4.4 through 0.4.18 clauses except the exact replacements in CON-978"
conforms-to: "cbcl-pairing SPEC-001 0.3.0 through 0.4.8-draft; cbcl-bus SPEC-053 0.16.12-draft; [[PROTO-002-selfsame-rendezvous-v1]]"
implementation-baseline: 0220cec2dec44cd95d4f411ea4814d790b6716d2
review-input: "[[spec-008-0.4.18-claude-adversarial-review-2026-08-23]]"
last-updated: 2026-08-23
---

# SPEC-008 0.4.19-draft — Singular authority and one WebFinger order

The key words MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT,
RECOMMENDED, MAY, and OPTIONAL use BCP 14.

> **Coordinated correction only.** The 0.4.18 review found two HIGH, one
> MEDIUM, and three LOW defects. This amendment closes those findings against
> the reviewed code. It authorizes no implementation, allocation, release, or
> deployment before a coordinated fresh-context Tier-1 PASS.

Standalone first contact remains the target. There is no preliminary
enrolment and no compiled relay allowlist. The wallet authenticates the
application live. It prompts for the exact `(applicationId, relayOrigin)`
policy pair. No identity effect precedes the applicable explicit consent.

## Requirement

### REQ-998 — Effective authority is transitive and singular

The specification authority resolver evaluates every effective numbered
clause and unnumbered normative paragraph from 0.4.4 onward. It SHALL resolve
exactly one current value for each of these roles:

1. cbcl-pairing survivor set;
2. cbcl-bus test set;
3. current Selfsame correction tests;
4. coordinated review set;
5. parent and consumer pointers.

A replacement edge remains effective only when current text preserves or
restates that edge. Replacing a contract SHALL NOT silently revive any clause
that contract replaced. Every replaced clause has one effective successor or
an explicit current retirement.

An unnumbered normative paragraph has the same replacement discipline as a
numbered clause. A conflicting current value, missing successor, stale pointer,
or open-ended survivor range is non-conforming. Historical prose supplies no
current authority by implication.

## Non-functional requirement

### NFR-921 — Every WebFinger path follows alias, account, then withdrawal

This requirement replaces
[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#NFR-920]] in
full.

The operation matrix in
[[spec-008-0.4.11-standalone-tofu-first-contact#NFR-913]] remains normative.
The finalizer's exact `cbcl-member` access is a write lock. Its whole
room-table locks are write locks.

Grant creation is `G`, and the credential/v2 finalizer is `F`. Exact grant
revocation is `XR`, and observed revocation is `OR`. New member enrollment is
`ME`, and pairaccount claim is `PC`. Per-frame authority read is `AR`. It uses
the matrix's `unratified read` sequence.

The authority graph covers only `cbcl-device-grant`, `cbcl-member`, and
`cbcl-pairaccount-grant`. Its exact cycle set remains:

```text
G×G, G×F, G×OR, F×F, F×OR, OR×OR,
G×ME, F×ME, F×PC, F×AR, XR×OR, PC×PC
```

The generator SHALL reproduce those twelve cycles and every `AR` contention
case. It makes no claim about room or finalizer-tail tables.

The migration graph owns every room, finalizer-tail, `cbcl-invite`, and
`cbcl-sealedgrant` lock. Allocation takes stable account and pending ceremony
first. Binding and member locks follow. Allocation then takes whole
`cbcl-roommember` and `cbcl-account-roommember` read locks in that order.
Finalization uses the same prefix and whole write locks.

Both single-table `write-grant` paths take an exact write lock on their first
read. They use `mnesia:read/3` with `write`. They perform no read-to-write
upgrade.

Both `redeem-and-grant` variants join two exact write locks. They take the
`cbcl-invite` token lock first. The applicable `cbcl-roommember` or
`cbcl-account-roommember` key follows. `cbcl-chat-invite:redeem-tx` also takes
its write lock on the first read. It performs no upgrade before update or
delete.

An operation touching both room tables takes both whole write locks in the
declared order. It acquires those locks before its first room read or write.
`revoke-handle-tx` therefore performs no `index_read`-then-exact-write
upgrade.

The graph includes every `cbcl-invite` and `cbcl-sealedgrant` holder.
`cbcl-chat-sealedgrant:put-tx` first takes an exact `cbcl-invite` read lock.
It then takes an exact `cbcl-sealedgrant` write lock. The operation takes no
room lock and performs no upgrade.

`cbcl-chat-sealedgrant:get-tx` takes an exact `cbcl-sealedgrant` write lock on
its first read. It uses `mnesia:read/3` with `write`. The expiry delete reuses
that lock. The operation takes no invite or room lock and performs no upgrade.

The generated table order places `cbcl-invite` before `cbcl-roommember`,
`cbcl-account-roommember`, and `cbcl-sealedgrant`. It places
`cbcl-roommember` before `cbcl-account-roommember`.

Finalizer, compensation, status, and operator paths use one generated tail
order. Compensation uses exact captured keys and no `?ALL` scan. The WebFinger
table order is exact alias, exact account, then withdrawal. An operation that
omits a table retains the relative order of the tables it uses.

`provision/3` and `create-or-return/3` take the exact alias write lock first.
They take the exact account write lock second. They take the exact
`cbcl-webfinger-withdrawn` write lock third. The current withdrawn-first reads
SHALL be reordered.

`lookup/1` and `lookup-binding/1` take the exact alias read lock first. They
take the exact account read lock second. They take no withdrawal lock and no
later write lock.

`withdraw/2` derives the requested alias URI from the named home DID before the
transaction. Its live-binding branch takes that exact alias write lock first.
It takes the exact account write lock second. It refuses a mismatched stored
URI or home DID without locking another alias. Its exact withdrawal write lock
follows.

The already-withdrawn branch retains the alias and account write locks from its
first reads. Its later withdrawal `index_read` takes a whole-table read lock.
That non-mutating branch acquires no later write lock.

`reinstate/2` omits the alias table. It takes the exact account write lock
first. It then takes a whole `cbcl-webfinger-withdrawn` write lock before its
first `index_read`. `lift-tombstone` performs the indexed lookup and exact
delete under that lock. It performs no read-to-write upgrade.

The migration graph includes every lock and exact mode. It includes cross-lane
authority locks. Its only cycles are projections of the twelve authority
cycles. It has no additional cycle or upgrade. A missing operation, unstated
mode, upgrade, opposite order, or additional cycle is non-conforming.

Every affected transaction runs at Mnesia level 1. It uses an explicit integer
`Retries >= 2`. Nested transactions are prohibited. Consumers map `nomore`,
exhausted `#cyclic{}`, and `nested_transaction` to `authority-busy`. No partial
effect survives.

## Contract

### CON-978 — Current authority preserves ancestry and retires every stale set

This contract replaces
[[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#CON-977]] in
full. [[spec-008-0.4.17-complete-consent-and-executable-ledgers#CON-976]]
remains replaced in full.

The numbered replacement relationships and hard-stop sentences in
[[spec-008-0.4.15-authenticated-allocation-and-consent#CON-974]] and
[[spec-008-0.4.16-correlated-success-and-deliverable-bounds#CON-975]] remain
effective. The four CON-976 replacement relationships also remain effective:

1. REQ-996 replaced
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#REQ-994]].
2. NFR-919 replaced
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#NFR-918]].
3. ADR-954 replaced
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#ADR-953]].
4. TEST-1134 through TEST-1138 collectively replaced
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#TEST-1129]]
   through
   [[spec-008-0.4.16-correlated-success-and-deliverable-bounds#TEST-1133]].

This contract preserves the retirement ranges that CON-977 established inside
CON-974 and CON-975. The CON-974 range starts with "The exact cbcl-pairing
survivor set" and ends with "remains mandatory." Its sentence starting with
"The coordinated review set" also remains retired.

The CON-975 range starts with "The exact cbcl-pairing survivor set" and ends
with "supplies authority." Its paragraph starting with "The coordinated review
set" also remains retired. This contract additionally retires the CON-975
sentence "Both rendezvous implementations MUST carry generated maximum sealed
records." The current cap obligation appears below.

This contract retires the coordinated-review sentence in
[[spec-008-0.4.12-tagged-transition-closed-authority#CON-971]]. That sentence
starts with "The coordinated review set". It ends with "declared baseline."

This contract also retires the coordinated-review sentence in
[[spec-008-0.4.14-deployed-admission-and-local-unlink#CON-973]]. That sentence
starts with "The coordinated review set". It ends at the did-crdt commit.

This contract retires three unnumbered normative survivor paragraphs. The
first follows the `## Test specification` heading in
[[spec-008-0.4.12-tagged-transition-closed-authority#TEST-1113]]. It starts
with "Every effective test through TEST-1108". It ends with "exact survivor
ledger."

The second precedes
[[spec-008-0.4.13-reachable-bounds-and-durable-authority#TEST-1117]]. It starts
with "Every effective Selfsame test through TEST-1108". It ends with "supplies
authority."

The third precedes
[[spec-008-0.4.14-deployed-admission-and-local-unlink#TEST-1121]]. It starts
with "Every effective Selfsame test through TEST-1108". It ends with "supplies
authority."

This amendment applies these current replacements:

1. REQ-998 supplements
   [[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#REQ-997]].
2. NFR-921 replaces NFR-920, NFR-919, and NFR-918 in full.
3. ADR-956 replaces ADR-955, ADR-954, and ADR-953 in full.
4. TEST-1139 replaces
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1136]] in full.
5. TEST-1142 replaces
   [[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#TEST-1140]]
   and
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1137]] in full.
6. TEST-1143 replaces
   [[spec-008-0.4.18-closed-ledgers-and-upgrade-free-operator-graph#TEST-1141]]
   and
   [[spec-008-0.4.17-complete-consent-and-executable-ledgers#TEST-1138]] in full.

REQ-995, REQ-997, and every unreplaced part of REQ-996 remain effective.
TEST-1134, TEST-1135, and TEST-1139 remain effective. The current five tests
collectively replace TEST-1129 through TEST-1133. No replaced clause lacks an
effective successor.

Every F-A through F-E correction remains a hard stop. SPEC-007 REQ-812 also
remains a hard stop.

The exact cbcl-pairing survivor set contains TEST-030 through TEST-049. It also
contains TEST-050 except its replaced control portion and retired positive
witness. That witness remains a negative obligation. TEST-051 through TEST-056
and TEST-059 survive. TEST-057 and TEST-058 do not survive.

The exact cbcl-bus set contains TEST-043 through TEST-065, TEST-068, TEST-070,
TEST-093, TEST-094, TEST-098, TEST-101, and TEST-102. Every effective Selfsame
test through TEST-1108 also survives. TEST-1134, TEST-1135, TEST-1139,
TEST-1142, and TEST-1143 are the current correction tests. No superseded
portion or open-ended range supplies authority.

The coordinated review set contains this amendment, cbcl-pairing 0.4.8, and
cbcl-bus 0.16.12. It also contains did-crdt at
`1f409a4229d07a62dd4cc6b2dce3b5a2e18e78a1`.

Opaque cap-sized sealed offer and reverse records MUST transit both declared
rendezvous implementations. The reviewed `crates/selfsame-rendezvous` and
did-crdt baselines each have a 4,096-octet cap. Both are deliberately
non-conforming to [[PROTO-002-selfsame-rendezvous-v1#REQ-305]]. Production
enablement depends on closing the PROTO-002 Tier-1 gate.

The Selfsame parent
[[SPEC-008-production-pairing-claimant]] points to this candidate and names its
open review gate. The cbcl-bus parent
[[SPEC-053-key-root-identity#DEC-012]] points to the coordinated 0.16.12
candidate. Cbcl-pairing 0.4.8 points its `consumer-design` at this candidate.

A fresh PASS authorizes only the Elephant SPL and test-first plan. It authorizes
no code, allocation, release, or deployment.

## Decision

### ADR-956 — One authority resolver and one WebFinger order precede code

Every numbered clause and unnumbered normative paragraph participates in
effective-authority resolution. Explicit current text preserves replacement
ancestry and retires every stale set.

The WebFinger graph follows alias, account, then withdrawal. Provision,
lookup, withdrawal, and reinstatement use that shared order or an ordered
subsequence. No cross-table cycle remains.

The authenticated display, post-consent effect order, shared grammar, cap
arithmetic, and sealed-grant graph remain unchanged.

## Test specification

### TEST-1142 — Installed state and every complete lock graph close

Change only the profile digest with authority and issuer fixed. Require grant
verification and atomic silent re-pin. Change authority or issuer. Require the
person prompt, cancellation, and verified atomic acceptance.

Attempt a handle change while installed. Require
`account-device-handle-change-refused` and zero enrollment frames. Confirm and
cancel unlink. Verify exact removal, seed retention, and unchanged hub state.

Delete the hub grant and run a full new ceremony with the retained key. Require
current eligibility, capacity, both consent decisions, and a new verified
grant. Every local-only re-grant shortcut fails.

Generate the twelve named authority cycles and every `AR` writer contention.
Generate the complete migration graph. Require the restored acquisition point,
invite-before-room order, and no additional upgrade or cycle.

Include both `write-grant` paths, both `redeem-and-grant` paths,
`revoke-handle-tx`, `cbcl-chat-invite:redeem-tx`,
`cbcl-chat-sealedgrant:put-tx`, and `cbcl-chat-sealedgrant:get-tx`.

Include `cbcl-chat-webfinger-store:provision/3`, `create-or-return/3`,
`lookup/1`, `lookup-binding/1`, `withdraw/2`, `tombstone/2`,
`already-withdrawn/2`, `reinstate/2`, and `lift-tombstone/2`.

Require alias-before-account ordering for provision, lookup, and live
withdrawal. Require `cbcl-webfinger-withdrawn` locking after account where
used. Require the reinstatement table-write lock before its first `index_read`.
Require the sealed-grant write lock on the first `get-tx` read.

Force bounded retry exhaustion and prohibited nesting. Require
`authority-busy`, no partial effect, and writer progress.

### TEST-1143 — Effective authority, pointers, and gates are singular

Static checks traverse every effective numbered clause and unnumbered
normative paragraph. They require exactly one cbcl-pairing survivor set,
cbcl-bus set, current Selfsame correction set, and coordinated review set.

The checks fail when any replaced clause lacks an effective successor. They
also fail on a stale parent or consumer pointer. They require the Selfsame
parent pointer to this candidate, the cbcl-bus DEC-012 pointer to 0.16.12, and
the cbcl-pairing `consumer-design` pointer to this candidate.

Require every survivor and replacement, all 42 cbcl-bus GATE-04 boxes, all
fifteen version sites, both non-conforming rendezvous baselines, and sealed cap
arithmetic. Removing any obligation fails.

## Changelog

- **0.4.19-draft — 2026-08-23 — review candidate, not approved.** Retires every
  stale ledger, preserves replacement ancestry, aligns the coordinated pointer
  graph, and gives all WebFinger paths one order. No implementation or
  deployment authority is claimed.
